# Universal List Architecture — moved, retired as a duplicate

> **ABSORBED — canonical copy is `docs/internal/development/tools/universal-list-architecture-guide.md`.**
> Resolved by Docs (#1585, 2026-09-02): this file and the `tools/` copy were both created in the
> same commit (`de126a66b`, 2025-08-06 17:48:38) — a same-commit reorg artifact, not a
> version-bump pair (887 diff lines out of ~1000, genuinely different detail levels, no clean
> supersede relationship). Neither copy is objectively "more complete" — picked `tools/` as
> canonical because it's the longer file and the one `docs/internal/development/tools/README.md`
> already points to. This copy's distinct content (finer-grained per-phase timestamps, "Domain
> Expert Pattern Recognition" framing) is preserved in git history, not merged into the canonical
> copy — a genuine judgment call under real ambiguity, documented rather than silently decided.
>
> This file is kept as a pointer stub, not deleted, so any existing link to this path resolves.

**Use [`docs/internal/development/tools/universal-list-architecture-guide.md`](../universal-list-architecture-guide.md) instead.**
