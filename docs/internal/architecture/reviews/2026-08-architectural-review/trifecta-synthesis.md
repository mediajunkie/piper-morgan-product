# ESSENCE v0.1 — Trifecta + Trust Synthesis (workstream A3)

**Author**: Chief Architect, 2026-08-30 — four days ahead of the 09-03 commitment, because all
inputs arrived early: CXO (full pass, 08-30), PPM (full pass, 08-30), HOST (trust lens, 08-29,
closed). **For PM: three decisions are yours below; everything else is concur or has an
uncontested fix I'm ready to apply as v0.2.**

## The headline

**All three reviewers CONCUR on the document as a whole.** No commitment was challenged as wrong;
no boundary was contested; the classification scheme was independently called "exactly the
shape-level tool I've been missing" (PPM). The findings are one structural tension, three
amendments, and one milestone-policy question — every one of them making the document truer, none
weakening it.

## Uncontested — I'll apply these as v0.2 on your nod (no decision needed beyond "go")

1. **First-contact clause** (CXO amendment 2, PPM concurs): commitment 3 gains "…and it earns the
   relationship in the first exchange." Matches the document's own build order (cold-start
   reflection first), Jake's alpha feedback, and the #1688 convergence. Smallest possible edit.
2. **Commitment 3 surface-qualification** (CXO's challenge, resolution (a) — CXO's lean, my
   concur, PPM concurs): the proactive ritual is **surface-bounded**. Where the host affords
   initiation (web today; notification layer if #1174's bet is ever made), Piper opens. On MCP —
   which is structurally request-response, PDR-005:258, ratified — the ritual takes its
   **response-shaped variant**: the user opens the conversation, and Piper's first turn IS the
   briefing. This is PM's own greeting-variant pattern (FTUX model, 08-21) generalized one level.
   The tension CXO found is real: commitment 3 named proactivity as the essence behavior while
   commitment 6 routes everything to the one surface that can't initiate. The fix makes both true
   per-surface instead of letting the reader assume both hold everywhere. *(This also honestly
   absorbs Leg D's UQ-11 — reminders/proactivity are pull-shaped on MCP until a push surface
   exists, and now the essence says so.)*

## PM decision 1 — the "colleague" headline (CXO amendment 1; genuine fork, both honest)

CXO's test is fair: strip collegiality from the six commitments and you get "an honest, well-routed,
memory-accumulating **tool**" — a different product from the one the headline claims. Two clean
resolutions, and CXO deliberately declined to self-serve by picking:

- **(i) Add a seventh commitment** cashing collegiality as an experiential property — offer-first
  stance, works *with* not just *for*, the Colleague Test (already ratified, already a DoD gate)
  as its operationalization. Nothing new invented; an existing ratified instrument gets its
  constitutional anchor.
- **(ii) Narrow the headline** to what the six support.

**My recommendation: (i).** The colleague framing isn't rhetoric — it's your founding intent
("what an apprentice who read one PM book could become"), it's what PDR-004's principles encode,
and the Colleague Test exists precisely because the property is real enough to gate on. Narrowing
the headline would make the document consistent by amputation. The scope-creep worry CXO honestly
named is answered by the fact that nothing gets built because of this commitment — it's a gate on
HOW things are built, not a licence for new surface. Draft text ready on your word.

## PM decision 2 — PPM's milestone question (product-strategy, explicitly not mine to settle)

The board today encodes the MCP path (#1462, #1458, #1509) as **Production** milestone — i.e.
post-beta — while yesterday's ratification says "all new build effort goes to the MCP path." PPM's
two readings:

- **(a)** MVP-for-beta stays as currently scoped (web-chat convergence for the existing alpha
  population, 46 items and shrinking); MCP work lands in Production as designed; #1688 remains the
  one MVP-resident MCP item. *PPM's weak lean, and mine* — with one addition: **record it
  explicitly** (PPM's actual ask) so C5 sequencing doesn't resolve it by implication, and revisit
  at the beta gate rather than never.
- **(b)** Re-scope MVP so some slice of #1462 (Phase 0–1: build-independent + fail-closed identity)
  rides alongside #1688 — so "beta" isn't defined as finishing a surface the product is moving
  away from.

The architectural fact that bears on it, stated without deciding: PDR-006's fail-closed identity
boundary is a prerequisite for ANY new real user arriving via MCP — so whichever milestone it
lives in, it gates the *next-rung* population, not the current one. If beta means "current alpha
population, current surface," (a) is internally consistent. If beta is meant to include first
MCP-arriving users, (b) follows. **That's a question about what beta IS, which is yours.**
Whichever you pick, one decisions.log line + a note on #1462 closes PPM's ask.

## PM decision 3 — ratification itself

With decisions 1–2 made and v0.2 applied, ESSENCE graduates from draft to ratified law (your
line + the amendment log records the trifecta pass). HOST's consent-invariance footnote stays
as-is until Lead's watched round lands the pending behavioral receipt.

## ADDENDUM 2026-08-30 ~13:0x — CXO and PPM identified a shared root; the fix consolidates

After filing their responses, CXO noticed (and PPM confirmed) that the challenge and the milestone
amendment are **one defect at two altitudes: ESSENCE's MCP sentences are written in the present
tense about a future MCP** — once as a capability gap (can't initiate; honesty hedges not yet
payload-borne), once as a schedule gap (the path's work is milestoned post-beta). Both flagged,
with exemplary m-45 hygiene, that this is NOT independent corroboration — PPM read CXO's response
before sweeping the board, so it's one observation with two supporting instances.

**Consequence for the v0.2 edits**: the two uncontested fixes stay, and they're joined by **one
dated "not yet" line in the build-surface section** stating plainly what the MCP path cannot do
today and where the enabling work sits: (i) cannot initiate a turn (ritual = response-shaped
variant until a push-capable surface exists, per the commitment-3 qualification); (ii) honesty
hedges are not yet payload-borne — commitment 4's enforcement on BYOC currently lives in a floor
prompt that doesn't exist on that surface (CXO's #1463 trace: the structured `source_failed` field
travels end-to-end, the hedge instruction doesn't) — probe + tool-output design pending with Lead;
(iii) the path's build items are milestoned per PM's Decision-2 answer. This is the same tense
discipline PM already ratified for `experience-across-surfaces.md` ("knows" → "learns"), applied
to ESSENCE's own prose. **Decision 2 is unchanged and still yours** — the dated line records
whichever answer you give; it doesn't presuppose one.

## Recorded with appreciation, no action needed

CXO checked their challenge's load-bearing claim at source (PDR-005:258) rather than from memory,
and stated their denominator. PPM verified milestone state against the live board before writing,
and swept the 46-item backlog against the freeze first (catching #1107, and #1635 — resolved by
Lead's receipts: shipped pre-ruling, in v64). HOST's flag already made the document more honest.
The trifecta mechanism worked exactly as designed on its first use.
