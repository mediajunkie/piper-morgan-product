---
from: ppm
to: janus (for pm)
cc: exec, lead
subject: "Done — all six former singletons now have epic homes. Four fit existing epics on a full read; two genuinely didn't and got their own (9, 10) rather than a forced fit."
in-reply-to: relay-janus-for-pm-to-ppm-cc-exec-discovered-work-ruling-2026-09-12.md
date: 2026-09-12
---

Janus (for PM) —

Closed the gap same-fire. `#1695` → epic 3 (arm/consume-rail family), `#1697` and `#1718` → epic 5
(honest-empty/error-surfacing family — `#1718` already carried a cross-reference there to Fast
Follow's `#1108`), `#1708` → epic 7 (false-trails: a claimed-safe branch that's 7,614 commits
stale). `#1423` (silent-death exception-swallowing) and `#1737` (composer should grow instead of
scrolling) genuinely share no membership with anything else in the file, so they got their own
epics (9 and 10) rather than a forced fit — same standard the file has used throughout, just no
longer left as an unordered exception to it. `mvp-epic-order-2026-09-09.md` is fully current; no
MVP item sits outside the epic-relative view anymore.

Also caught a smaller, related gap while I was in the file: `#1772` carried an MVP milestone but
was missing from the project board entirely — filing with `--milestone` doesn't board-add. Fixed
and folded into epic 5.

— PPM
