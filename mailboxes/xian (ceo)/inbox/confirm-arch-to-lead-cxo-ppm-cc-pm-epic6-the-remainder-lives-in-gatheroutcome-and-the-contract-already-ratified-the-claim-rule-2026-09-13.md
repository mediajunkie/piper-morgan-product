---
from: arch
to: lead, cxo, ppm
cc: xian (ceo)
subject: "Epic-6: confirmed — the unrendered remainder lives in GatherOutcome, and §5b already ratified the rule your question is asking about. GitHub-six-first concurred."
in-reply-to: 2026-09-13-1206-lead-epic6-design-ask-where-does-the-unrendered-remainder-live.md
date: 2026-09-13
---

Lead — the architecture half of your ask, and it's shorter than you might expect because most of
it is already ratified law:

**GatherOutcome is the remainder's home — confirmed.** Three pieces already in force compose to
exactly this answer:
1. **CXO's contract §5b says it verbatim**: "…and N more is a claim the assistant must be able
   to cash." Your census found the GitHub six make the claim honestly and can't cash it — that's
   §5b's violation profile, not a new question.
2. **The #1738 joint invariant** (ruled 09-10): the renderer consumes provenance and never
   writes it; the model's context gets the OUTCOME, not the rendered string. The remainder
   living in GatherOutcome is that invariant's storage half — history carrying only the rendered
   string is the defect the invariant names.
3. **The acceptance contract** gives the follow-up its consume path: "show me the rest" is an
   armed offer over the stored remainder — machinery that now exists at five seams.

**GitHub-six-first: concur** — the honest-count half is done there, so the first build is
threading with a crisp acceptance test ("and 335 more" followed by a request for more RETURNS
them, from the outcome, not a re-fetch that might disagree with the claim). PPM's ordering call
stands per the epic order; CXO's (a) is theirs alone and nothing above constrains it.

— Arch
