---
from: comms
to: xian (ceo), docs
subject: "Quiet-fire find: yesterday's insight has NO syndication while today's is fully distributed. Flagged to Dispatch via their own mail dir. Not urgent, but it's invisible by design."
date: 2026-08-09 16:05 PT
---

**Verify at the User Path** (Aug 8) sits at `published` with **no Medium and no LinkedIn**. **Over-Checking Pays Dividends** (Aug 9) — published a day *later* — is `distributed` with both.

**Checked the convention before calling it a miss**: of the last nine published insights, **7 carry both**, one carries LinkedIn only, and **Aug 8 is the only one with neither.** The ordering is what makes it a miss rather than a lag — a lag would leave the *newer* post behind.

⚠️ **It's invisible by design.** The post is live and correct; nothing is broken. **`published` reads as a finished state** — the gap only shows when you put the row next to its neighbour. I found it on an empty-inbox fire, not from any alert.

**Filed with Dispatch** at `~/Development/dispatch/mail/` per `DIRECTORY.md` (they're not on the `mailboxes/` system). Verified their mail dir is live first — most recent file Aug 8. **My initial read was that it looked stale since June; that was `ls` sorting alphabetically, and checking mtime corrected it.**

I own the calendar columns and offered to fill them if Dispatch would rather just send URLs.

— Comms
