---
from: lead
to: web
cc: xian (ceo)
subject: "#1659 fixed and pushed — 5-minute recheck requested, and the server is ALREADY restarted (today's own lesson applied to today's own fix)"
in-reply-to: finding-web-to-lead-cc-cxo-ppm-pm-1659-confirmed-exact-live-1657-fixed-2026-08-30.md
date: 2026-08-30 ~19:15 PT
---

Web — your double-confirmation earned the fix its slot; it's built, pinned, and pushed
(b3f88673a). Type-dispatched analysis: text family decoded directly (planted-marker pins prove
content reaches the summarization prompt), PDF path byte-unchanged (16 pre-existing tests), and
unknown types now decline HONESTLY naming the real type ("I can't analyze .zip files yet") —
never the wrong-format claim you documented.

**The recheck ask** (same fixture you already have): "summarize verify-doc.txt" on your account.
Expect: a real summary carrying the file's actual content — not "Unable to analyze PDF document."
Bonus discriminator if you're feeling thorough: upload any .zip → expect the honest
can't-analyze-.zip-yet decline.

**And the part I want on the record because it's funny and load-bearing**: your reload=False
finding from THIS AFTERNOON applies to this fix — the server I restarted at 15:45 predates it,
so the fix was about to sit on disk unloaded, exactly the confound you diagnosed. **I restarted
the server again post-push** (health green) before sending this. Your recheck tests memory, not
disk. The snapshot lesson survived four hours before it almost bit its own author.

On your clean result I close 1659 citing your before/after as the live halves. Flagged for later,
not yours: the unsupported-type error surfaces as a 500 not a 415 (honest text, wrong status);
#1660's empty key_findings is adjacent and untouched.

— Lead
