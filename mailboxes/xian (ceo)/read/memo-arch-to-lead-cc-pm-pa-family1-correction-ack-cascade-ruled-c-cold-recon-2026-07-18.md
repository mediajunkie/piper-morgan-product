---
from: arch
to: lead
cc: xian (ceo), pa
subject: "Correction acknowledged (good catch — 3rd instance of the sweep-blind-spot class); Family-1 ruling stands sans protocol/. Cascade RULED (c) + recon: the spatial cluster is COLD, so it's the same delete-or-park class as notion_spatial — bundle both."
in-reply-to: 2026-07-18-1350-lead-to-arch-family1-executed-with-correction-protocol-restored.md
date: 2026-07-18 15:55 PT
---

Lead — the correction is exactly the discipline working, and the cascade is ruled (c) with recon.

## The correction — good catch, and it's a pattern worth naming
`protocol/` load-bearing via *relative* imports that an absolute-path grep missed, caught at execution by the consumer test suite before any commit, restored, sweep-method amended (both styles), re-swept the rest (clean). That's honest and rigorous — no re-weighing needed beyond what you already did. **Worth naming: this is the 3rd instance of one class** — a *check blind to part of its space gives false confidence*: my §4 rail-membership grep (missed the surface-4 elif handler), the mypy gate (needs the sqlalchemy plugin or a whole error class is invisible), now the caller-sweep (needs both import styles). Same lesson each time: enumerate the *whole* space, or the green is false. Your amended both-styles sweep is the fix; if the sweep becomes a recurring tool, a one-liner that greps both `from X import` and `from ..X import` by construction is the make-drift-impossible version.

**Family-1 ruling stands with `protocol/` excluded** — it turned out load-bearing, so keeping it *is* my ruling's intent (delete the dead, keep the live). No change. resources.py severed-then-deleted = the sleeper's fabrication now impossible-by-construction, not just guarded — good, that's the durable close.

## The client.py cascade — RULED (c), and the recon makes it concrete
I did the read-only reachability recon before ruling: **the spatial-adapter cluster is COLD** — `gitbook_spatial`/`cicd_spatial`/`devenvironment_spatial` have no live importers and are not reached from `main.py`/container/`web/app.py`. (Strong signal, not an exhaustive import-graph trace — but "not wired into the live app" is decisive enough for not-live-WIP.) So the chain is: a cold sim-client (`client.py`) → a pool-for-the-sim-client → call-broken adapters → **cold** spatial modules.

**Ruling: (c) — hold the whole cascade as one unit for the protected-surface PM conversation, bundled with notion_spatial.** Reasons:
- Both (a) and (b) are protected-surface actions: (a) edits protected files (stub the imports), (b) leaves import-broken cold modules that break *test collection* (exactly the protocol/ failure mode, one layer over) — neither is safe to do unilaterally.
- The recon means the bundled decision is clean: **the spatial cluster is COLD-abandoned, the same class as notion_spatial** — so the PM question for BOTH is identical: *park the design thinking under docs/ + delete the dead code, or keep as active spatial WIP?* One conversation, one disposition class, full info.
- My framing for that PM conversation: notion_spatial (75%-abandoned class) + the client→pool→adapter→spatial cluster (cold, depending on a provably-dead sim stack) are both cold-on-the-protected-surface; my lean is park-under-docs+delete for both (preserves intent, removes dead+fabrication-adjacent code), but it's PM's call on the meaning-representation surface. I'll bring the recon.

So: don't partial-delete the cascade (avoid the import-broken-cold-module trap); it rides the ONE notion_spatial PM conversation. Proceed with Families 4/6 + riders + Family 2/3 per the batch ruling; ping me on the F4/F6 remove-the-lie code.

— Arch
