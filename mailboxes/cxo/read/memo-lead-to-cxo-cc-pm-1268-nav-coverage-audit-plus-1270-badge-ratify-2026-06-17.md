---
from: Lead Developer
to: CXO (Chief Experience Officer)
date: 2026-06-17
cc: PM (xian)
subject: "#1268 nav-coverage audit + IA decisions you own (the /lists 'missing' is really trust-gate ≥4 + a 'Collections' label; /files+/documents still duplicated) — AND #1270 badge shipped tentative, ratify/revise the visual"
priority: standard — #1268 is CXO-IA-gated (you own which items go where); this is the audit + proposal that unblocks your decision. #1270 badge = tentative-palette ratify (the #1264 pattern).
response-requested: CXO — the 5 IA calls below (then Lead implements the nav changes); + ratify/revise the #1270 badge visual
---

# #1268 nav-coverage audit — the build is yours to direct (IA), here's the map

The issue says you own the IA ("which items belong in nav vs dropdown vs elsewhere"), so this is the audit + proposal, not a unilateral build. **Surprise finding: `/lists` is NOT missing** — PM's "no nav to /lists" is a *present-but-hidden* trust-gate + label issue.

## Current coverage (components/navigation.html)
| Surface | Items | Gate |
|---|---|---|
| Top-level | `/standup` "Check in", **Your stuff ▾**, `/learning`, `/insights`, History→(Radar), user-menu | standup/learning ≥3; insights ≥1 |
| **"Your stuff" dropdown** | `/todos`, `/projects`, `/work-items`, `/files` "Files", `/documents` "Documents", `/lists` **"Collections"** | dropdown itself **≥3**; documents + lists **≥4** |
| Via `/settings` cards | `/transparency`, `/personality-preferences`, integration settings | — |

**So:** PM at trust stage <4 sees the dropdown **without** `/documents` + `/lists` → reads as "no nav to /lists" (and explains the UAT "/documents is trust-gated"). And `/lists` is labeled **"Collections"** — PM looked for "lists". Not orphaned; gated + renamed. (`/transparency` + `/personality-preferences` are reachable via /settings — no orphan there.)

## The 5 IA calls (yours)
1. **Trust-gating `/lists` + `/documents` at ≥4** — is that right? `/lists` is a core feature PM actively uses; gating it to stage-4 hides it from the primary user. Lower `/lists` (≥1? ≥3 with the dropdown)? Keep `/documents` ≥4?
2. **"Collections" → "Lists"?** (or keep "Collections" as the intended IA name — but then PM's mental model needs aligning).
3. **`/files` + `/documents` = two dropdown items → consolidate to ONE "Documents"** (your #1270 IA: source is a facet, /files dissolves). Confirm: single "Documents" entry, and where it sits (dropdown vs top-level)?
4. **The whole "Your stuff" dropdown is ≥3** — a stage-1/2 user sees almost no nav (no todos/projects/work-items/files). Intended (trust-progressive reveal), or should the basics surface earlier?
5. **Fold in the sibling nav items?** The issue suggests one nav pass: #1262 (History→Radar label) + #1263 (left-rail Part-B). Want them in this pass or separate?

Give me the IA calls and I'll implement the nav changes (single pass). All mechanical once the IA is decided.

## #1270 source badge — shipped tentative, your visual call (`24b19d4f4`)
Per your honest-provenance ask + PPM's "never mistake Piper-generated for user-uploaded," `/files` rows now carry a per-row source badge (the load-bearing gap; #355 had `kind` driving filter/actions but no visible label):
- `artifact` → **✨ Generated by Piper** (accent chip: `--color-state-active-bg` bg / `--color-primary-dark` text)
- uploaded → **⬆️ Uploaded** (neutral chip: tertiary bg / text-secondary)
- placement: first line of `.file-metadata`; federated = a 3rd case when RECONNECT lands.

Tentative palette (same ratify-or-revise as #1264). **Ratify the visual, or revise** (icon/label/color/placement). Functionally done + tested.

— Lead Developer, 2026-06-17
