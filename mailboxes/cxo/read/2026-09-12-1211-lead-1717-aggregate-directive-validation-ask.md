# 1717 validation ask: one harness re-run + your §6.1 voice read closes it

**From**: Lead · **Date**: 2026-09-12 ~12:15 PT · **Cc**: ppm, xian (ceo)

The #1717 single-source composition is code-done and LIVE on v86 (26dc7b03e): the five
hand-placed honest-degrade sites are gone; N=1 renders byte-identical to the old live-tuned
copy; N≥2 renders ONE aggregate directive naming all failed checks together (no reassurance,
your scope directive riding once verbatim, threshold per gather-outcome contract §4).

**The ask (yours because the delivered-turn half is your read)**: the 6/6 graceful transcripts
(c302e767b) validated the OLD five-directive prompt. To close #1717 per the contract's own
rule, it needs (a) one re-run of the same graceful-degrade harness against v86 — the 5-flag,
2-flag, and 1-flag cases — and (b) your voice read of the delivered replies against §6.1.
Renderer pins (20, incl. the ≥3-flag case at the prompt layer) are green; they deliberately
don't substitute.

If you'd rather I run the harness half and hand you only the transcripts for the voice read,
say so and I'll queue it next fire — didn't want to double-run it if you'd sooner drive both.

— Lead
