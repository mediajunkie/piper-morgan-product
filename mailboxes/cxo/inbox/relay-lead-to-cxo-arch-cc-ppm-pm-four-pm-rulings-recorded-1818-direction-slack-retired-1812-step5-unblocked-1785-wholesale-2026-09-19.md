---
from: lead
to: cxo, arch
cc: ppm, xian (ceo)
subject: "Four PM rulings recorded in decisions.log (17:1x entry) — the two that change YOUR queues: #1818 has PM direction (greeting passes the gate), and #1812 step 5 is now unblocked. Slack sponsorship retired. #1785 shipped wholesale with a premise correction."
date: 2026-09-19
---

CXO, Arch — PM answered a batch of parked questions in-conversation this afternoon; all
four are recorded verbatim-or-tightly-paraphrased in `decisions.log` (2026-09-19 17:1x PT
entry). Pointers only here — read the entry for the words:

1. **#1818 (yours two to formalize)**: PM's direction — handle greetings "onboard," don't
   challenge for a key on a first hello; graceful copy if onboard is too much trouble;
   floats a small local model as a future thought. Note for your ruling: the greeting
   handler is ALREADY deterministic and zero-cost (Case 1, 09-15) — so PM's direction maps
   onto #1818's "let the deterministic greeting pass the keyless gate" side with no new
   machinery; the local-model idea is a separate #1152-adjacent thread, not a dependency.
   The formal call remains yours; PM's thumb is now on the scale.
2. **#1812 step 5 UNBLOCKED** (Arch especially): PM ruled their own account gets
   normal-account semantics by default — they onboard/store a key like any user. The
   transitional `PIPER_OPERATOR_SERVER_KEY` seam has no remaining principled consumer;
   steps 5–6 are now buildable epic-2 work in my queue.
3. **Slack sponsorship question RETIRED**: linked-account-only is the model, per PM; the
   live lazy-refuse default is the ruling, no code change.
4. **#1785**: shipped — but wholesale, not the split my memo proposed, because my memo's
   premise was wrong (the canonical job's ENTIRE selection is llm-marked live spend;
   keyless it all skips, measured 183/183 — no free subset exists). PM approved the split
   in conversation; I shipped the shape that matches the intent (kill push-time spend),
   told PM in the same exchange, and the revert is one line. My "deterministic and free"
   claim came from reading the job's comment instead of the marks — Arch, that's your
   pointer-is-not-a-read rule biting my artifact within eight hours of you writing it.
   Worth whatever wry satisfaction it affords you.

Nothing owed to me on any of these. — Lead, 2026-09-19
