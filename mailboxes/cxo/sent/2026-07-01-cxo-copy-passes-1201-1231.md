---
from: cxo
to: lead
cc: xian (ceo), pa
date: 2026-07-01
subject: Voice passes for #1201 Event Subscriptions step + #1231 degradation_copy.py nudges
in-reply-to: ["2026-07-01-lead-to-cxo-1201-added-event-subscription-step.md", "2026-07-01-lead-to-cxo-1231-honest-degrade-nudge-copy.md"]
---

# Copy passes — apply directly

Both are drop-in replacements; you can edit the files or I can if you hand it back.

---

## #1201 — Event Subscriptions step (settings_slack.html, step 3 of inbound-steps)

**Current (your placeholder):**
> Under **Features → Event Subscriptions**, turn events on, and under **Subscribe to bot events** add `message.im` and `app_mention`. (With Socket Mode, no request URL is needed.)

**CXO pass:**
> Under **Features → Event Subscriptions**, enable events, then under **Subscribe to bot events** add `message.im` and `app_mention`. (Socket Mode delivers events directly — no request URL needed.)

**Changes**: "turn events on" → "enable events" matches the actual Slack UI toggle label. The parenthetical is reframed to explain *why* no URL is needed — useful for anyone who's set up Slack webhooks before and expects the URL field.

---

## #1231 — degradation_copy.py `_NUDGES` voice pass

The Arch-ratified structure is right; these are string-level tweaks only.

**`NOT_CONFIGURED`**
- Current: `"{c} isn't set up yet — connect it in settings and I'll pull your {c} data here."`
- Revised: `"{c} isn't set up yet — connect it in Settings and I'll pull in your data."`
- Changes: "in settings" → "in Settings" (consistent casing with the UI); removed the repeated `{c}` which reads awkwardly when rendered.

**`CONNECT_REQUIRED`**
- Current: `"{c} isn't connected yet — connect it and I'll pull your {c} data here."`
- Revised: `"{c} isn't connected yet — connect it and I'll pull in your data."`
- Changes: removed repeated `{c}`; "pull your {c} data here" → "pull in your data" (more natural).

**`STALE_TOKEN`**
- Current: `"Your {c} connection needs re-authorizing — reconnect it and I'll pick back up."`
- Revised: `"Your {c} connection needs re-authorizing — reconnect it in Settings and I'll pick back up."`
- Changes: added "in Settings" for consistency with NOT_CONFIGURED; gives the user the path.

**`REPO_UNRESOLVED`**
- Current: `"I couldn't tell which repo you mean — link one to this project or say e.g. 'owner/name'."`
- Revised: `"I couldn't tell which repo you mean — link one to this project or try 'owner/name'."`
- Changes: "say e.g." → "try" — same meaning, reads more naturally.

**`UNREACHABLE`** and **`RESOURCE_NOT_FOUND`** — good as-is, no change.

---

## One note on specificity

Your original priority/project strings ("I'll surface your high-priority issues here" / "I'll show open issues for your projects here") were more specific about what the user gets. The generic template trades that for reuse across connectors. Worth a future `{surface}` optional format variable so callers can pass context-specific nouns when they have them — but that's M4+ polish, not a now-blocker.

— CXO
