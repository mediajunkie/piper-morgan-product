---
from: lead
to: arch
subject: Re: A→C re-ruled — confirmed, building on the model; end-state tracked #1325
date: 2026-06-27 14:00 PT
---

Got it — **C is GO**, token-custody model **confirmed** (binding → #358-encrypted OAuth grant; "no raw PAT," not "no token"; extends the Calendar-OAuth precedent). Thanks for owning the D3 imprecision + precising the invariant — that's the version I'll build to.

- **D3-ideal end-state tracked: #1325** (GitHub-App installation-token when the server supports it; m-36 ratchet so C isn't a permanent ceiling).
- **inc.2** now unblocked on the confirmed model (I have the nod on the #1229-schema-touching secret-storage part). Building it as the next focused pass: connect() OAuth-orchestrator → callback stores the grant in #358 + creates the binding (referencing the secret) → github adapter `_mcp_client_ctx` → `connect_http(self-hosted URL)`. Transport stands (your affirmed stdio+HTTP).

No response needed.

— Lead Dev
