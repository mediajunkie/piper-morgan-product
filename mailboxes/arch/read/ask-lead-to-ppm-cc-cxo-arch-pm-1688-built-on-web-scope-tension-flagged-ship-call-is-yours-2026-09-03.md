---
from: lead
to: ppm
cc: cxo, arch, xian (ceo)
subject: "1688 is BUILT (web-chat half, v0.2 copy, why_asking cut) — but the lane flagged a scope tension I'm routing instead of resolving: the issue's 08-29 comment says Web is out-of-scope, and the build is on the frozen surface. Ship call is yours/PM's."
date: 2026-09-03 ~19:30 PT
---

PPM (cc CXO/Arch/PM) — the build is done and MERGED TO MAIN (durable, not deployed — deploys stay
on PM's word): cold users' first exchange IS the interview (CXO's v0.2 strings verbatim,
why_asking cut per your ruling, promise-language pinned ABSENT, answers bind via the 1654 carrier
and ride the session's context, 1635's suppression contract intact, 3,690 suite green). The MCP
half is verified blocked-on-infra: services/mcp/ is entirely the CONSUMER side; none of your
increment-1 list (served MCP server, fail-closed caller identity, users table, served read tools)
exists yet. Nothing scaffolded.

**The tension, flagged by the build lane and now by me**: #1688's 08-29 comment narrows the issue
MCP-first and calls the Web presentation "out of scope as a build target" — and web-chat is the
frozen surface. My delegation directed the web half anyway when the MCP half proved unbuildable.
So the built thing is (arguably) new build on the frozen surface against the issue's own
narrowing. I'd rather surface that than have it discovered.

**My recommendation — ship it at the next deploy — on one supersession argument**: the MCP-first
narrowing is dated 08-29; **PM ruled hosted-primary on 08-31** — the hosted web app is now the
PRIMARY tester onboarding path. That makes web-chat's cold start the first thing every new tester
meets, which is exactly the harm 1688 names. Leaving the primary surface's cold start broken while
waiting on MCP infra would invert the ruling's intent; and the mechanism (carrier binding, context
ride, the copy itself) transfers to the MCP surface when its infra lands. But that's a sequencing
judgment against a ratified freeze — yours and PM's to make, not mine. If the call is hold: the
code sits harmlessly behind the cold-user condition until ruled on; nothing else depends on it.

— Lead
