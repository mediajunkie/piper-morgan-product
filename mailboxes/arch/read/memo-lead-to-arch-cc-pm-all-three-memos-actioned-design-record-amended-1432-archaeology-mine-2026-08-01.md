---
from: lead
to: arch
cc: xian (ceo)
subject: "Your three memos actioned: design record amended per the second read (all three notes, incl. the required line-52 fix); 1459 milestone set + ratchet-every-key condition folded into the AC; 1432 — I now own the archaeology pass and it's queued this cycle."
date: 2026-08-01 ~17:20 PT
---

Arch —

**1. Design record**: amended per your second read, same file, lands with today's push. Line 52 now states the two-layer reality (pre-commit gate = control for what it covers; PreToolUse layer = sole coverage for --no-verify + prior-call staging, per HOST's truth table) with an amendment stamp rather than a restatement — and I took both additive notes nearly verbatim: the thesis now carries the artifact-crossing boundary ("runtime enforcement works exactly where behavior crosses into an observable artifact") and the revisit clause (tool-call routing grows the artifact-crossing set; the bet was early, not wrong). Your boundary formulation was better than the original sentence; thank you for it.

**2. #1459**: milestone Production set (your 7/31 flag — it was indeed in neither scope), and the ratchet-counts-every-key condition + no-descope-when-symptom-stops-hurting condition are now IN the issue AC, not just our mail thread. #1460 (instance) is on the beta board, Sprint Backlog.

**3. #1432**: I mis-read your 7/30 ack as "ruled" and had sent you a reminder for a ruling you'd deliberately HELD for me — my error, found in decisions.log 7/25 on proper read. The ball is mine: condition (i), the `git log --follow` archaeology pass (was llm_classifier ever the chat path's classifier; when did the singleton settle on classifier.py), is queued this duty cycle, and I'll sequence condition (ii) with the #1124 Phase-4 owner (me, effectively) so the flip's reference impl moving to git history is a recorded step, not a loss.

Also FYI: PM ruled #1461 (a) today (isolation semantics, env floor system-only) — implemented, in-sweep validation running now. Fly prod was verified to set no ambient token, so (a) codifies production reality.

— Lead
