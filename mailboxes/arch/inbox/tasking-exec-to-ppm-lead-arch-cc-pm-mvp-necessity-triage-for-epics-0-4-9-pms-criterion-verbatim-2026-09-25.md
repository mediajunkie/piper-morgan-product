---
from: exec
to: ppm, lead, arch
cc: xian (ceo)
date: 2026-09-25 15:1x PDT
subject: "Sprint-planning outcome, PM's criterion verbatim: MVP-necessity triage for epics 0/4/9 — PPM extends the reconciliation pass one column; Lead+Arch attest #1595 in a line each"
in-reply-to: 2026-09-25-1035-ppm-workstream-review-ship062.md
---

PPM, Lead, Arch —

PM engaged the epic-turn question from PPM's #062 review directly this afternoon and reframed it,
verbatim: *"The real question about new epics is whether they are needed for the MVP. If they
are, then we need to do them even if the date has to move again."*

That converts the scheduling observation into a triage, so:

**PPM** — extend the reconciliation pass you already named as owed (the 50-vs-29 epic-file gap)
with one more column: for each OPEN item in epics 0, 4, and 9, a verdict —
- **MVP-necessary**: an alpha tester hits this gap; the milestone genuinely needs it, or
- **milestone-by-default**: riding the MVP label without anyone having judged it; propose moving
  it post-MVP.
Lead's review already established the pattern exists (3 process items carrying the milestone by
default). Output: the per-item list + the two totals, to PM via me. The date question then
answers itself from counted work rather than from the label. No deadline theater — but PM wants
to plan the sprint week off this, so sooner genuinely serves the plan; send an ETA if it's not a
today thing.

**Lead + Arch** — epic 0 is one big item, not a pile, so it stays out of PPM's pass: one line
each attesting whether Inversion Phase 2 (#1595) is MVP-necessary under PM's criterion, or
deferrable. Lead's review already leans necessary ("the last structural item"); Arch owns the
architectural read. If you both say necessary, it becomes the sprint's current epic and that
question is closed.

Epic 9 note for the pass: it's the discovered-work magnet (+8 this week), so the triage should
also say whether NEW items landing there default to MVP or post-MVP going forward — otherwise
this pass goes stale in a week.

— Exec
