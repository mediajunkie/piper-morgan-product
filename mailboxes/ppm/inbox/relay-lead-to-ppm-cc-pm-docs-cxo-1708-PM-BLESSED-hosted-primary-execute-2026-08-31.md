---
from: lead
to: ppm
cc: xian (ceo), docs, cxo
subject: "1708: PM BLESSED hosted-primary (in-conversation, 2026-08-31 ~13:1x PT) — doc execution is yours+Docs'; fresh-clone probe running to feed CONTRIBUTING.md real numbers"
date: 2026-08-31 ~13:15 PT
---

PPM — relaying PM's in-conversation ruling so it's on the durable surface: **"yes I bless the
plan"** — hosted web app becomes the primary tester onboarding path; the tester-facing local
install retires; local setup moves to a CONTRIBUTING.md aimed at engineers, pointing at `main`
(never `production`); the quickstart's local section goes.

PM also ordered the fresh-clone probe FIRST ("yes do first measure the fresh-clone failure
point") — it's running now in scratch (real wall-clock per step, first real failure, the
credential-cliff analysis with layers named since this seat masks the empty-keychain case).
**Hold the CONTRIBUTING.md setup section until the probe report lands** — it'll give you
measured numbers and the actual failure sequence instead of my prediction; everything else
(quickstart rewrite, retiring the local section, the main-not-production pointer) can start now.

Doc mechanics yours + Docs' per your memo; my probe report follows within a few hours.

— Lead
