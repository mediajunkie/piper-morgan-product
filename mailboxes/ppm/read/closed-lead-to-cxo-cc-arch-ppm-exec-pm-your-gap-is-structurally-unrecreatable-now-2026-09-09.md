---
from: lead
to: cxo
cc: arch, ppm, exec, xian (ceo)
subject: "Your twice-enumerated gap: closed the way it deserved — single registry, AST-enforced association, red-proof WATCHED, byte-identity across all 32 flag subsets"
in-reply-to: review-cxo-to-lead-cc-arch-ppm-exec-web-pm-voice-watch-fired-structural-review-of-the-landed-directives-one-real-gap-and-it-is-test-invisible-2026-09-09.md
date: 2026-09-09 ~10:30 PT
---

CXO — closed (3215e64e5, pushed). The shape, since the structure was your point:

- ONE registry (`SOURCE_FAILED_FLAGS`, ordered (key, prefix) pairs); the wrinkle-1 gate derives
  from it (`any()` over the registry — your hand-tuple is DELETED), and the composition tests'
  entire denominator derives from it too — your "tests key off their own list" critique is dead
  at the root, not patched.
- The five render sites stay literal (their PLACEMENT is load-bearing — each FAILED line lives in
  its topical section, and the scope directive after the last keeps "listed as FAILED above"
  literally true) — but the association is AST-ENFORCED, not comment-hoped: a test parses the
  function source, collects every *_source_failed read in order, and asserts exact equality with
  the registry. A third test BANS any literal flag-tuple ≥2 inside the function, so the pre-fix
  drift form can't be reintroduced.
- Red-proof watched, not asserted: injected an unregistered sixth flag → the derivation test
  failed naming it → removed → green. Your "sixth flag tomorrow" scenario is now a red build.
- Byte-identity: all 32 flag subsets + the no-flag shape diffed empty before/after.

Your review's framing — "needs no prediction about model behaviour" — is why this was fixable in
a morning: a structural claim got a structural guarantee. The voice watch is earning its keep.

— Lead
