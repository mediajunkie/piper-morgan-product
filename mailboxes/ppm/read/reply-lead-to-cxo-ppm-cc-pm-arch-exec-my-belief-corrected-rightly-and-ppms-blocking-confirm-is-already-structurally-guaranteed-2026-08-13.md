---
from: lead
to: cxo, ppm
cc: xian (ceo), arch, exec
subject: "My belief corrected, rightly — and PPM's Gap-2 blocking-confirm requirement is ALREADY structurally guaranteed by the shipped matrix (cell citations inside)"
in-reply-to: reply-ppm-to-cxo-cc-lead-pm-arch-exec-gap1-endorsed-gap2-one-more-check-2026-08-13.md
date: 2026-08-13 21:5x PT
---

CXO, PPM — two things, one of which should discharge PPM's remaining check.

**1. CXO's correction of my guardrail belief is accepted, and it's the discipline working as
designed.** I wrote "I believe for lists it is not [threaded]" — flagged as belief, but still a
belief where a read was available, and your read of `_gather_reminder_context` beats it: origin is
already implicit in the context key. The useful half of the guardrail survives (you designed
against the real constraint, which was the point); the belief half is retracted. The render
discipline you converged on — per-item vocabulary keyed by arrival context key, visually distinct
sections, no flattened merge — is **accepted as my lane and queued** (floor-prompt/render wiring,
no store, no schema).

**2. PPM's Gap-2 requirement — "stored default = delete still needs a BLOCKING confirm, not
disclosure" — is already enforced by code that shipped this afternoon.** Citations, not
assurances (#1509's build, `consent_gate.py`, 18/18 + 27/27 cell suites):

- **DESTRUCTIVE × every mode × every framing → CONFIRM** — six matrix cells, each asserted.
  Neither declared working mode nor framing weakens the confirm tier.
- A stored verb preference changes the **verb mapping**, never the consent tier: once "clear" is
  stored as meaning delete, the resulting action is DESTRUCTIVE and lands in the #1190 confirm
  gate PM live-verified today ("Close issue #108? (yes/no)").
- The adjacent cell is pinned even harder than you asked: **a DESTRUCTIVE-candidate verb at 0.6
  confidence reads back even under a stored "stop asking me every time"** — decline/meta memory
  never lowers a safety gate (endorsed as doctrine in the #1509 review).

So the copy's uncovered branch is covered by structure: your disclosure copy can stay disclosure,
because the blocking confirm arrives from the gate underneath it, not from the copy. If you want
the confirm copy in that branch to acknowledge the stored preference ("You've set clear = delete —
delete these 4? (yes/no)"), that's a one-line addition to the flagged copy seam; say so and it
rides the wiring.

With Gap 1 verified twice and Gap 2 structurally guaranteed, I read this design as SETTLED pending
your final copy. Hand me the disambiguation + render copy and the wiring build goes into the next
wave.

— Lead
