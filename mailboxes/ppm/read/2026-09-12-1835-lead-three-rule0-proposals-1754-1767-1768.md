# Three Rule-0 dead-code proposals in one memo — 1754, 1767, 1768 (all same family)

**From**: Lead · **Date**: 2026-09-12 ~18:50 PT · **Cc**: ppm, xian (ceo)

All three are dead-code findings from today's lanes, all in the ConversationHandler /
conscious-pipeline neighborhood the #1759 deletion just cleaned, each with evidence already
on its issue. Batching the proposals so your review is one pass; each gets its own GO/HOLD.

1. **#1754 (chitchat half)** — farewell/thanks/chitchat branches live-unreachable behind the
   greeting-only gate; the three format_*_conscious functions have no other production
   callers; RESPONSES has zero production reads (evidence posted by the 1759 lane, verified
   fresh that day). **Propose DELETE** under the same principle you stated for 1759 — with
   one open question first: is chitchat-beyond-greeting a product intention anyone holds?
   If yes, this is complete-not-delete and belongs in an epic; my read is no (the floor
   handles conversational turns now, better).
2. **#1767** — the SECOND clarification mechanism (file-disambiguation set_clarification trio
   + fields on ConversationSession); only referents are a collect-ignored archive test.
   **Propose DELETE** — same second-parallel-mechanism argument as 1759, weaker stakes.
3. **#1768** — classify_conscious: zero production callers but a LARGE dedicated test surface
   (two test files, lens wiring tests, an enforcement exemption). **Propose DELETE code +
   surgical test excision** but flagging honestly: the test surface size is why this needs
   your eyes and not just my GO — if the lens-wiring tests pin behavior something live
   inherits, the sweep must prove that before cutting (the lane that executes will run the
   full deletion discipline either way; your ruling is about intent, theirs is about safety).

If you GO on any subset, they run as separate delete-module-safely lanes citing this memo +
your reply. No urgency signal implied either way — they're queue items, not blockers.

— Lead
