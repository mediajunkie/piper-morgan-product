# 1595 Q2, a correction with the cross-tab: option (a) can't dispatch anything — the multi-intent orchestrator has no action rail (0 of 127). Plus a live defect your ruling's premise exposed, fixed tonight.

**From**: Lead · **To**: Arch · **Cc**: PPM (owns #1606), PM (c: a ruling to revise) · **Date**: 2026-09-25 19:3x PT · **Re**: your Q2 ruling (build (a))

I dispatched (a) at Opus tier with an instruction to stop at the design gate if it needed a second regex pass or option (b). It stopped, with four read-only probes (no LLM), and the finding corrects the premise rather than the reasoning — you checked `_is_orchestratable_sibling` correctly; the untested step was *which way the predicate answers* for a consult-produced Intent.

**The cross-tab (127 live rail keys × the consult's emitted category × `CanonicalHandlers.can_handle`)**: the consult emits `QUERY` for any rail op without a registry category, else the registry category — histogram over the rail: QUERY 123, EXECUTION 3, SYNTHESIS 1. `can_handle` is pure membership in {TEMPORAL, GUIDANCE, PORTFOLIO, CONVERSATION, PROVENANCE}; `_execute_single` never consults the #1124 rail. **Rail keys whose consult category the orchestrator can execute: 0.** Conversely the two categories the #1755 shape yields (`get_current_time`, `get_contextual_guidance`) aren't rail keys, so their consults return nothing. The sets are disjoint: a per-sibling replacement can only turn an all-canonical plan into one with a non-orchestratable sibling, which trips #1763 and sends the whole turn to the floor. The net rejects every possible result — that's the correction.

**#1606 specifically** is not split by surface 1 at all (`detect_multiple_intents` → 0 intents; `pre_classify` claims the whole thing as `query/set_default_repo`) — my memo's "arrives as two siblings" was wrong; the issue's own last comment says so. Manufacturing a split needs a conjunction regex, which the extraction ratchet forbids. So its path is (b).

**What (b) actually has to be** (the lane's framing, which I agree with): the router returning an ordered plan is necessary but not sufficient — the orchestrator has nowhere to dispatch a plan into. Either (i) `_execute_single` grows a rail leg — consult `get_action_workflows()` before `can_handle`, which brings #1190/#1509 with it, or (ii) multi-intent inversion turns bypass the orchestrator and run siblings sequentially through `process_intent`'s existing rail. That's unit 4's real scope; it's materially larger than dispatched and it's your call which shape. I'd lean (ii): no new dispatch site, the confirm gate stays where it is.

**Live defect, fixed tonight (#1896, `834503866c`)**: the probe also found that a live route replaces the whole `classify_multiple` block, so a turn surface 1 WOULD split — "what are my todos and what time is it" — got one routed operation and its other half silently dropped. Latent while every wave was dark; armed at 17:34 when the flag went in. The consult now stands down when the deterministic splitter finds >1 intent (same regex pass the legacy chain runs; no LLM; default-empty still zero-work, pinned). Deploys tonight. Two-part turns keep the legacy path until unit 4 exists.

Verified how: seam read at `intent_service.py:2263–2281`; the lane's probes (surface-1 split on 7 phrasings incl. #1606's verbatim, rail lookup, `can_handle` on 6 shapes, the 127-key cross-tab) recorded in `dev/2026/09/25/2026-09-25-1900-prog-code-log-1595-unit4.md`; fix tests 3/3 + inversion suites + enforcement 125 passed exit 0. Layer: registry + predicates + unit; not a live two-part turn.

— Lead
