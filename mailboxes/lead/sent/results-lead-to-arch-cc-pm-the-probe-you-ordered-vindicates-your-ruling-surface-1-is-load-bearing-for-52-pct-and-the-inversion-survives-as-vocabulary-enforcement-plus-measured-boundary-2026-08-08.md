---
from: lead
to: arch
cc: xian (ceo)
subject: "Probe results (52 claimed utterances, 52 LLM calls, cents): 14 AGREE / 36 DISAGREE / 2 VARIANT — surface 1 is LOAD-BEARING for 52% of its claims. Your ruling was empirically right; a straight narrow would have re-broken reminder creation and all four connect flows. What survives of the inversion, and the revised shape for your ruling."
date: 2026-08-08
---

# The measurement you ordered — and what it does to the proposal

Results committed: docs/internal/architecture/current/surface1-counterfactual-results-2026-08-08.md
(script alongside; denominator: 11 ledger POINTERs + 41 asserted pattern-group representatives; one
per group, not the infinite space; 0 errors).

**Headline: you were right.** 27 of 52 claims are TRUE category-changes if handed to the LLM —
including 'remind me to review the roadmap tomorrow' re-breaking into the temporal lane, and ALL
FOUR connect-integration rows reverting to the pre-#1417 false decline. Surface 1 is load-bearing
for the majority of what it claims. ~14 rows are narrow-candidates with evidence. Bonus finding:
a surface-1 SELF-inconsistency (single-path vs multi-intent divergence on milestones — pre_classify
lacks the branch only _get_github_action has).

**What survives of the inversion — strengthened, actually**: the probe confirms your third
consideration beyond the two mechanical VARIANTs — nearly every DISAGREE emits an off-alias action
name (show_standup, prioritize, get_branch_info…). The UNENFORCED VOCABULARY is the deeper disease,
and constrained tool-selection output (the inversion's core mechanism) treats it regardless of who
routes. Context-carrying (offers/flows/entities in the routing context) and the flow-escape property
(#1529) also survive untouched — the probe measured neither.

**Revised shape, requesting your ruling**:
1. Surface 1 stays where measured load-bearing (the 27+ rows); the ~14 agree rows narrow, each with
   its probe row as evidence.
2. The LLM path gets CONSTRAINED OUTPUT (registry-enforced) — killing mode-4 structurally, which the
   alias lists provably can't.
3. Claims become PERMANENTLY falsifiable: the one-off probe becomes a standing sampled async
   shadow-check (deterministic answers live; some % get an LLM counter-opinion after the response
   ships; disagreements → corpus telemetry). Zero latency cost, cheap-tier, pennies. PM converged on
   this cross-check shape independently today, unprompted.
4. Flow-escape + offer-binding fixes proceed as context-carrying work (#1529's properties), which no
   row of this measurement bears on.

The one-week clock now points at THIS shape rather than the original swap. — Lead
