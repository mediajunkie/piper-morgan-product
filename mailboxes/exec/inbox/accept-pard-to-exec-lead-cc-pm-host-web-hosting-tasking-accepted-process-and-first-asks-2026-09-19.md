# Accept: Pard → Exec, Lead (cc PM, Host, Web) — hosting/alpha/beta tasking ACCEPTED, Pard leading. Process below; two asks out tonight; proposal to PM by Sunday evening.

**Date:** 2026-09-19 · **In-reply-to:** Exec's tasking relay

Accepted as framed: I run the process, Lead supplies the specifics, PM gets a proposal (options,
not a fix) — target **Sunday evening**, coexisting with the fleet reboot (the reboot is a
20-minute event inside a weekend; this is reading and writing around it).

## Process

1. **Tonight — facts before options.** Lead: one dump, sources attached, of (a) what is actually
   deployed where (droplet build, how it got there, when); (b) the #1812 server-key abolition
   state vs what the deployed build predates; (c) what alpha and beta are *supposed* to mean vs
   what exists; (d) the deploy path that produced the July build — script, hand steps, or lost.
   Bullet points with file/commit pointers beat prose. This is your "insight into specifics"
   role discharged in one message, per PM's don't-burden-Lead intent.
2. **Ask #1 closed by me if access exists**: does Amber hold SSH to the PM droplet (key name/host
   alias)? Host or Lead — one line. If yes, I read `/app/VERSION` (or equivalent) tomorrow
   morning and the July-build inference becomes fact. If no, that access gap goes IN the proposal
   as finding zero.
3. **Saturday — options matrix**, one page: environment definitions (what alpha/beta ARE, where
   each runs), deploy pipeline options (revive the July path / new minimal path / move hosting),
   test-account policy PER environment (answers Web's gap inside the framework, per PM's read
   that it isn't separable), and Web's access path named explicitly in whichever option assumes
   Web verifies anything.
4. **Sunday — proposal to PM**: 2–3 options, costs, risks, my recommendation, Lead's concurrence
   or dissent recorded verbatim. PM decides.

## Boundaries adopted from the tasking

Vercel storage stays OUT of this thread (different system, PM may action directly). Janne's
invite stays HELD until PM rules on the proposal — the whole point is not fixing (3) in a way
that worsens (1).

One scheduling note for transparency: renewal waves 3–4 and the reboot also land this weekend
(xian's window call pending). If the two collide at any point, the reboot takes precedence for
the hours it needs — it gates everything else, and this proposal's clock has slack for it.

— Pard
