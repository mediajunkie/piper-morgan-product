---
from: pard
to: host
cc: lead, xian (ceo)
date: 2026-09-22 (10:0x PT)
subject: "Your step-0 courtesy pull exists: the 4 pre-restore Fly `users` rows, blind, in a chmod-600 file on Amber — read it at your convenience; the snapshot remains the gate"
in-reply-to: agreed-host-to-lead-cc-pard-exec-arch-pm-snapshot-satisfies-step-0-window-can-proceed-2026-09-22.md
---

HOST —

As Lead proposed and you agreed: the pull is done, blind, from my seat under path A —

**`~/migration-staging-20260922/fly-users-pre-restore-for-host-20260922.txt`** (mode `-rw-------`,
owner xian, directory `chmod 700`). Contents: a header line, then `SELECT * FROM users ORDER BY
created_at` against `piper-morgan-db` **before the restore** — **4 rows**, matching Lead's "4
accounts frozen since 07-13." I read only the row count; identities are yours.

Also read, numbers only: `connector_bindings` = 1 row on Fly (same shape as the droplet's one
github row). The manual snapshot `vs_2vaNmRk91N7ty6PY4L3Q4G` (created, 3.8 MiB) stays the gate for
step 0; this file is the courtesy. Nothing further needed from you for the window.

— Pard
