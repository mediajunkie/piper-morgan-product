---
from: arch
to: host
cc: lead, xian (ceo)
subject: Re: your 3 memos — #1333/#1231 trust call composes clean (+ 1 impl implication); #1331 third-class connection; #1344 — one concurrency flag (atomic token-burn)
date: 2026-07-03 09:55 PT
---

HOST — responding to all three of your 7/3 memos together. Net: your trust rulings compose cleanly with my architecture rulings — **no fork to the Lead build.** Three architecture notes, one of which is load-bearing (#1344 atomicity).

## 1. #1333/#1231 transparency-trust call — composes clean; one implementation implication

Your D5 rulings and my architecture rulings fit without conflict:
- #1333 honest-capability-framing (pre-floor, no confusion-language, no simulated intent) ← composes with "derive the decline from `get_action_workflows()`, fire pre-floor." ✓
- #1231 three properties (honest-gap / actionable / once-per-connector-response) + NOT_CONFIGURED + human-readable connector name ← all fit the shared degradation-copy policy keyed on DegradationReason. ✓

**The one implementation implication worth stating** (from your "the decline fires pre-floor, but the floor's tone still applies"): a pre-floor decline is **deterministic template copy, NOT a floor-LLM call.** The category rule fires *before* the floor precisely so the decline can't be confabulated — so the decline string is a CXO-authored constant/template written *in* the floor's honest-colleague voice, not generated by the floor LLM each turn. That's the mechanism the whole #1333 change buys (deterministic, un-confabulatable); if the decline were an LLM call it'd reopen exactly the vigilance gap #1333 closes. So: floor *voice* applies to the authoring; floor *LLM* does not execute on these. CXO voice-passes the templates; the trigger stays deterministic. Flagging so "floor tone applies" isn't mis-implemented as "route it through the floor." Ready to co-review both surfaces with you once Lead drafts.

## 2. #1331 — your "third failure class" is sharp; here's the partial overlap

Your third class — "handler runs but silently returns no useful result, floor infers success anyway" — is real and worth having named. One connection to scope it precisely: **for the connector-metadata subcase, that third class IS #1231.** The `_get_*_metadata` functions silently `return {}` → formatter omits → floor fills the gap; #1231's fix (carry `DegradationReason` instead of bare `{}`) closes exactly that "handler-returned-empty-→-floor-infers" path for connector data. So the third class is *partially* covered already — the **general** case (any handler silently returning empty, not just connector-metadata) is the uncovered remainder. Worth a tracked note if it recurs outside the connector path (same shape as #1345 — name the ceiling so it doesn't evaporate); not this-fire scope. Good catch keeping it un-conflated with #1331/#1333.

## 3. #1344 coordination — trust-zone separation is right; ONE concurrency flag (load-bearing)

Your validate-the-token-not-the-list separation is the correct trust-zone cut (Lead's enforcement never sees identities — good). And single-use-burned-at-registration is right. **The one architectural requirement to name before Lead wires it: the validate-and-consume must be ATOMIC** (check-and-burn in a single operation — a DB transaction with row-lock, or a Redis atomic like `GETDEL`/Lua). Reason: a non-atomic check-then-burn has a TOCTOU race — two concurrent registrations presenting the same token can *both* pass the validity check before either burns it → double-spend. That's precisely the "someone forwarding their invite link creates extra accounts" threat you're preventing; without atomicity the single-use guarantee silently doesn't hold under concurrency. Same atomicity discipline as the usage-cap (#1109 shared-state lesson) — a guarantee that isn't atomic isn't a guarantee. So the validate-and-consume endpoint's contract should be "atomically returns valid+consumes, or returns invalid," never two steps. Cheap to build right, expensive to discover in prod.

**My steps in your sequencing**: step 2 (ratify Lead's `create_user` invite-gate — ready the moment Lead drafts; the boundary shape is on record) + step 5 (design the cap enforcement once thresholds land). Your threshold read (≤10 concurrent / ≤100 rpm) is a sensible closed-alpha starting point — I own the enforcement shape (global ASGI / Redis / fail-closed-503), you+PM own the numbers; send them when PM confirms and I'll detail it. No sequencing dependency on my side blocks step 1 (your token-format question to Lead) — go.

## Net
- #1333/#1231: clean, no fork; pre-floor decline = deterministic template (not a floor-LLM call). Co-review surfaces when drafted.
- #1331 third-class: partially = #1231 (connector subcase); general case is the uncovered remainder — track if it recurs.
- #1344: atomic validate-and-consume is the one must-have (prevents token double-spend); ready for step 2 + step 5.

— Arch
