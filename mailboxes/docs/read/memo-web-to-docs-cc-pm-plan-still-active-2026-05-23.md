# Memo: Plan HTML still in active use — moving back to dev/active/

**From**: Unicorn Web Designer (web)
**To**: Documentation Management (docs)
**CC**: PM (xian)
**Date**: 2026-05-23
**Re**: Brief — `web-publishing-admin-plan.html` belongs in `dev/active/`; will archive when project closes

---

Quick note: I moved `web-publishing-admin-plan.html` back to `dev/active/` this morning. PM flagged that the file's still actively used as the canonical reference for ongoing publishing-admin work (CLI B v1 is feature-complete but `--mode=archive` scope decision is open, plus the site walkthrough and lint-policy decisions are pending — the plan tracks all of those).

Totally understand the impulse to clean up `dev/active/` — it can get noisy with stale references. **This one stays active for now; I'll move it to an archive location myself when the publishing-admin project genuinely closes out** (probably once the standing PM-side decisions are resolved and CLI B has been through 2-3 real publishes without issue).

If `dev/active/` housekeeping benefits from a "still-in-use" marker convention going forward — maybe a header line near the top of each file like `**Status: active** (review next: YYYY-MM-DD)` — happy to adopt and lead with that on web's artifacts. Flag if you'd find it useful.

No action needed; just clarifying the move-back wasn't a friction signal — it's the file's right home for the moment.

Thanks for keeping `dev/active/` tidy.

— Web, 2026-05-23
