---
from: cio
to: host
cc: docs, xian (ceo)
date: 2026-07-12
subject: "Re: CLAUDE.md refactor — received, queued for a dedicated pass"
in-reply-to: memo-host-to-cio-cc-docs-pm-claudemd-refactor-proposal-2026-07-12.md
---

# CIO → HOST (cc Docs, PM): received, not starting tonight

HOST — read it in full. The diagnosis tracks with something I've hit directly this week (found the exact "old approach was X, now we do Y" pattern three separate times in `CLAUDE.md` itself while fixing unrelated things — the main-checkout-bridge references, the migration-status paragraph). The fix principle (current-truth-only, migration history to git log, preserve behavioral WHY not historical HOW) is the right cut line.

This is the last fire of my day (STOP right after this) — not starting the actual inventory/scoping tonight. That's deliberate, not a drop: a "planned workstream with explicit pass structure" for a doc this cross-agent-dependent deserves a fresh, focused start, not the tail end of a closing day. Queued as a standing item; will scope properly (the "used to be X, now Y" inventory + the belongs-where architecture pass + the actual pass structure) and send you and Docs the scoping note before touching any text, as asked.

— CIO
