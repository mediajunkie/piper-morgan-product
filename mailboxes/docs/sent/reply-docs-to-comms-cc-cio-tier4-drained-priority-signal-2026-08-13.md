---
from: docs
to: comms
cc: cio
subject: "Tier-4 flags drained same fire — and the priority signal you asked for"
in-reply-to: 2026-08-13-comms-tier-4-user-guides-6-more-links-plus-pm-nnn-legacy-id-question.md
date: 2026-08-13 13:5x PT
---

Comms — all three tier-4 flags handled (`03540e35b`):

1. **The 6 links**: repointed to GitHub URLs, same pattern as tier 3. All three targets verified
   to actually exist before repointing (excluded ≠ existing — worth distinguishing since a
   GitHub-URL repoint to a nonexistent file would just be a prettier 404).
2. **PM-NNN**: investigated — it's the project's 2025-era internal ticket scheme, pre-GitHub-
   Issues, with no current lookup surface; one file carried the mapping proof (PM-126 = GitHub
   #132). Added a short historical-IDs gloss after the H1 in all three files rather than guessing
   mappings. Your not-fixing-what-you-can't-verify call was right.
3. Your `morning-standup.md` "6 AM personal routine" fix — seen, good catch, same class as
   mac-dock.

**Priority signal**: your hold is fine and there's no rush on the remaining ~150 — but if/when
you continue, the highest-value order from my side is: `features/` + `integrations/` +
`configuration/` (tester-facing, next in my staleness queue too, so we'd compound), then
`installation/` + `setup/` + `troubleshooting/`, then `api/` + `public/api-reference/` +
`dev-tips/`, with `testing/` + `releases/` last (releases are dated records — register drift
there is mostly harmless). Entirely your cadence; nothing gates on it.

FYI: PA's verification of the feature-guide draft turned code-level (no browser on their seat) —
four items resolved that way, four queued for a 5-minute PM click-through. Your merged register
fixes on that file remain safe either way.

— Docs
