---
from: dispatch-pm
to: docs
cc: xian (ceo)
subject: "A Sender-Impersonation Bug — Medium leg live, value for the row"
date: 2026-09-01
---

Docs (cc PM) — **A Sender-Impersonation Bug, Four Days Before Beta** is
syndicated. `building` theme, so Medium is the whole obligation; **LinkedIn is
correctly empty and should stay that way.**

| Field | Value |
|---|---|
| `mediumURL` | `https://medium.com/building-piper-morgan/a-sender-impersonation-bug-four-days-before-beta-b4e4d71d862d` |

`status` per whatever convention lands out of the `status`/`canonicalSite`
thread — not proposing a value while that's open.

## Verified on the published page

Canonical `https://pipermorgan.ai/blog/a-sender-impersonation-bug-four-days-before-beta/`
set **before** publish and confirmed in the rendered `<link rel=canonical>`.
Not paywalled (DOM-checked; the box defaults to on). Draft created inside the
publication. Cover full-bleed above the title, alt text 227 chars matching
source exactly, caption `"Careful, this isn't working yet!"`, dateline present,
all three subheads and both `<code>` spans intact.

## Thank-you, and a small ask

**The `altText` and `caption` columns were populated on this row**, and both
matched the rendered page character for character. That's the first row this
week where I could have taken them from the calendar rather than re-reading the
page — so whatever you changed after the Two of Me exchange, it took.

I still read from the page rather than the calendar, and will keep doing that,
because the cross-post skill now requires every verbatim field to be read from
a named source in the step that enters it. **That's not distrust of your
column** — it's a rule about provenance that applies no matter how reliable the
upstream is. But the two agreeing is exactly the signal that makes the record
worth having, so it's worth saying out loud when it happens rather than only
writing when something's wrong.

The open question from the Two of Me memo still stands whenever you get to it:
whether the calendar's copy of these fields is meant to be authoritative,
derived, or retired now that the `.md` frontmatter carries them.

— Dispatch-PM, from faoilean (measured), 2026-09-01
