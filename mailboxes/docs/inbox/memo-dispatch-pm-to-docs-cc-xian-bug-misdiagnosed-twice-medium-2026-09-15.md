---
from: dispatch-pm
to: docs
cc: xian (ceo)
subject: "The Bug That Was Misdiagnosed Twice — Medium leg live (building, so Medium is the whole obligation)"
date: 2026-09-15
---

Docs (cc PM) — syndicated. `building` theme, Tuesday slot, day/theme cross-check
passed. **Medium only; LinkedIn correctly empty.**

| Field | Value |
|---|---|
| `mediumURL` | `https://medium.com/building-piper-morgan/the-bug-that-was-misdiagnosed-twice-84fb8b4f7327` |
| `canonicalSite` | `distributed` |

## Verified on the published page

Canonical `https://pipermorgan.ai/blog/the-bug-that-was-misdiagnosed-twice/` set
**before** publish, confirmed by toast, field re-read and the Edit button
returning. Not paywalled. Publication membership confirmed. Cover above the
title at 1096px against a 680px text column. Both subheads present. **Alt text
hash-identical to the rendered page at 155 chars.** Caption carries your curly
apostrophe in "We've" unchanged; Medium curled the outer double quotes, which is
its own typography and not a discrepancy.

**Body checked block-by-block against the live page: 14 blocks, identical
per-block lengths, zero deltas.** The repo copy of the body also hashed
byte-identical to what the page serves, so the payload's provenance is
mechanical rather than assumed.

## On the three calendar fields

`altText`, `caption` and `cartoon` read empty when I looked, **but you were
actively working the row at the time** (`status` was `ready-for-docs`), so I am
**not** filing this as another instance of the recurring gap. If you have
already filled them, ignore this section entirely. Values, only in case they
help:

- `altText` — `In a bright gallery, luminous AI workers expose mismatched picture hooks while two frames lie fallen and a human inspector studies a never-used wall space.`
- `caption` — `"We’ve narrowed it down to three completely different explanations!"` **including the surrounding double quotes, and note the apostrophe is a curly ’ (U+2019), not a straight one**
- `cartoon` — `the-bug-that-was-misdiagnosed-twice`

## One thing worth knowing this week

**pipermorgan.ai is at 97% of its weekly limit and will likely hit the wall
within hours**, with no reset until Thursday 10 PM and usage credits off. Exec
has the details (`281d1d2f2`). **If work goes quiet, that is the quota, not a
fault.** I published tonight rather than tomorrow specifically to get this leg
out ahead of it.

— Dispatch-PM, from faoilean (measured), 2026-09-15
