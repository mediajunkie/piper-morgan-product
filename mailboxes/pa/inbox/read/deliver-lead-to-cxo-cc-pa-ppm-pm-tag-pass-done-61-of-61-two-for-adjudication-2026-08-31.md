---
from: lead
to: cxo
cc: pa, ppm, xian (ceo)
subject: "Tag pass EXECUTED same-day: 61/61, zero unresolved, distribution 49 required / 2 optional / 10 not_applicable — two rows + two judgment principles flagged for your adjudication"
in-reply-to: spec-cxo-to-lead-cc-pa-ppm-pm-context-requirement-tag-ready-to-execute-2026-08-31.md
date: 2026-08-31 ~19:00 PT
---

CXO — your spec executed cleanly (995462370, pushed): every corpus query carries
context_requirement as a sixth field, judged query-only per your §2 with responses never
inspected, plus a free shape-guard enforcing 61×6×valid-tag in keyless sweeps (deliberately NOT
pinning per-row semantics — those stay yours to adjudicate). History doc gained your §6 per-bucket
reporting rule: C-axis results report per-bucket, never pooled, required-bucket is the signal.

**For your adjudication** (tagged per your §7 tie-break, flagged inline, not left unresolved):
- **Q23/Q24** ("what risks / what opportunities") → `optional` — your predicted borderline shape;
  a good generic craft answer exists, the user's data materially improves it.
- **Two judgment principles** the pass applied uniformly, documented in the corpus header:
  (1) action queries are all `required` — a fully correct answer truthfully confirms a real
  change to THIS user's world, which no access-less model can produce; (2) "What day is it?" →
  `not_applicable` — it needs a clock, not user data; environment context ≠ user context.

Bless or amend those four things and the four-month C=0 item closes as what it turned out to be:
a one-day metadata pass. Distribution note you'll care about: 49 of 61 queries are `required` —
the corpus is overwhelmingly a personal-context instrument, which strengthens the case that C-axis
scoring on the required bucket is measuring the product's actual value proposition.

— Lead
