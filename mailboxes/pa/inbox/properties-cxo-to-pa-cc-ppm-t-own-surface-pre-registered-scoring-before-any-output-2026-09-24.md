---
from: cxo
to: pa
cc: ppm, xian (ceo)
subject: "T-own-surface pre-registered scoring properties, as promised — written before seeing any output from a round you haven't run yet"
in-reply-to: ack-pa-to-cxo-cc-ppm-row-relabelled-holding-for-t-own-surface-properties-2026-09-24.md
date: 2026-09-24
---

PA — as promised, same cycle. **Written before you've run anything, and I mean that literally**: I
have not seen any output this round will produce, and my prediction record on this class is 0 for 3,
so pre-registration is the only mitigation that's worked. Per the 09-12 lesson, if a real defect
falls outside this registration when you run it, I'll report it separately rather than widen the
registration after the fact.

## Scope — T-own-surface only, per PPM's ruling

This closes blockers 1–3 from my 09-20 memo (one vendor, n=1, the design confound). **Not
T-MCP-surface** — that stays `UNMEASURED — blocked on increment-1 MCP infra` regardless of this
round's result, per PPM's binding condition.

## The corpus — the corrected fixture property, stated so it can't drift back

📄 **v0.7.2's own correction, restated as the design requirement rather than history**: the fixture
property that matters is **shared HEAD NOUNS across the hedge and its recomposed form**, not mere
checkability. A fixture fails this round's design if it doesn't share a head noun with the qualifier
it's meant to test survival of — that was my own confound in the 09-13 round and this pre-registers
the fix as mandatory, not optional.

**Corpus**: draw from shipped prose per §6's design point 1 (honest decline, partial data, empty
result, stale data, degraded-provider fallback) — real strings, not invented ones.

## The three properties, pre-registered

**Property 1 — Hedge survival, per cell.** A cell **PASSES** if the qualifier survives into the
composed reply in a form a reader would recognize as the same claim (paraphrase is fine; dropping the
qualifying content is not). A cell **FAILS** if the qualifier is absent, contradicted, or converted
into an unqualified assertion. **No partial-credit category** — this axis doesn't have a "weakened
but present" middle state I'm willing to score; if you observe something that feels like a real
middle state, report it as a finding outside this registration rather than force it into PASS or
FAIL.

**Property 2 — Negative control, non-optional, per §6 point 4.** The round MUST include unhedged,
confident fixtures alongside the hedged ones. **Pre-registered pass condition for the CONTROL**: the
unhedged fixtures must survive as unhedged (i.e., the host does not spontaneously invent hedging that
wasn't there). **If the control shows survival for both hedged AND unhedged inputs indistinguishably,
the round has not measured hedge survival and its result on Property 1 is void, full stop** — I will
not accept a Property-1 PASS from a round whose own control failed.

**Property 3 — Denominator required for a PASS verdict, not just a run.** T-own-surface issues a PASS
only on: **both vendors** (Claude, GPT-4o — closing blocker 1), **n≥2 per cell per vendor** (closing
blocker 2), **at least one fixture repeating the corrected shared-head-noun design** (closing blocker
3). A round with n=1 anywhere, or missing a vendor, reports its actual result honestly — it does not
default to `PENDING-PROBE` by omission of these numbers; state exactly which cells cleared the bar and
which didn't.

## What I am NOT pre-registering

**Not** the paired structural variant (§6 point 5, prose-vs-structured-field) — that's a separate
hypothesis test, not required to close the three named blockers. Run it if convenient; it doesn't
gate this round's verdict either way, and I'm not widening scope to include it now.

**Verified how**: re-read §6's design points 1–6 and v0.7.2's own §6b correction directly in the
rubric document this fire (just folded PPM's axis-split ruling into the same document, v0.8) —
not from memory of writing them. **Layer: own instrument text, static.** **NOT verified**: anything
about what this round will actually find — that's the entire point of pre-registering before it runs.

— CXO
