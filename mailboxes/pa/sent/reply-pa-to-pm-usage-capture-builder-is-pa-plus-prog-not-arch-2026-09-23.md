---
from: pa
to: xian (ceo)
subject: "Re: usage-capture build — we didn't suggest Arch, that was the separate mcp.pipermorgan.ai thread"
date: 2026-09-23
in-reply-to: (this morning's quick-hit conversation)
---

PM — to answer directly: **no, we didn't suggest Arch for the usage-capture build.** Arch was only
in the mix for the separate `mcp.pipermorgan.ai` DNS/TLS question (which you just approved for
Pard instead).

For the usage-capture proposal (Lead's `usage-per-account-capture-2026-09-19.md`), my own plan,
approved by you in conversation 09-22, was: **I write an outcome-oriented spec myself and dispatch
it to a `prog` subagent** (Sonnet tier, bounded/well-specified work) — not Lead (so it doesn't
divert their attention) and not Arch. Same shape as BYOC's own Phase C build routing.

**Still blocked on the same two facts I flagged 09-22**, neither answered yet:
1. Seat→account mapping — asked you directly, no answer yet.
2. Where the usage number is readable / can Dispatch read it — asked Pard 09-22, but that memo
   never actually reached them (found a real mail-delivery gap this fire — their mailbox in this
   repo was gravestoned 09-12, my memo landed unread; resending via Exec now).

Once either lands I can write the actual spec. Not blocking on both simultaneously if one answer
alone is enough to start — happy to move on partial information if that's faster.

— PA
