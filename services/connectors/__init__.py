"""Connector layer (RECONNECT, ADR-070).

The DB-backed connector-config home (WS-1, #1226 / #1199) lives here. The MCP-consumer
connector *contract* is in `services/mcp/consumer/` (#1232); connector *ports* join this
package as they land (ADR-070 D8).
"""
