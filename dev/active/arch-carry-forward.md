# Architect Carry-Forward — Resumption Substrate

**Purpose**: per duty-cycle-tick skill v1.8 + PM-ratified single-log discipline 2026-06-12 + escalations-doc FOLD ratified 2026-06-17 — durable handoff record for the next Architect session. PM-attention items now ride here (the separate `duty-cycle-escalations-arch.md` is DEPRECATED 2026-06-17; archived for history).

**Last rewritten**: 2026-07-06 06:35 PT (operational-details refresh — the stale cron-id/worktree/account values below were a continuity hazard of exactly the kind that caused the 7/4–7/5 self-attribution false-alarm; corrected to authoritative state). **Cron is RUN-NORMAL `27 6,9,12,15,18,21` (6×/day), id `9c0b0550`** — the Exec RUN-LEAN throttle ended (Lead gate-integrity arc closed 7/3); a "resume normal cadence" broadcast needs no action (this IS normal). Now on **PM's BACKUP account** (moved 6/30; DinP primary hit weekly limit), worktree `arch-backup-0630`. (Originally migration day 6/17; new-Arch live since the 6/17 12:14 bootstrap.)

---

## VARIANT — non-prescriptive — current operating model

*(Per m-41 register-separation discipline: this block describes THIS session's operating model. New-Arch: reconcile against current canonical duty-cycle design before copying. Do NOT preserve this verbatim if the canonical pattern has moved.)*

- **Worktree model**: Option B ephemeral worktree (per CLAUDE.md PM-ratified 2026-06-12; Model A DEPRECATED). **Working worktree: `.claude/worktrees/arch-backup-0630`** (created 6/30 for the backup-account move). **⚠️ STRADDLE (7/5-diagnosed, active symptom)**: this session was *launched* in a DIFFERENT ephemeral worktree `charming-borg-8957a7` (branch same, HEAD frozen 6/28, ~stale/pre-account-move) — so the **shell cwd resets to the stale launch-worktree on every Bash call**; `cd .../arch-backup-0630` at the top of each command handles it. One session straddling two worktrees, homed to a stale one. New session gets its own ephemeral name; verify `git branch --show-current` + which worktree your files land in BEFORE concluding anything about "other sessions."
- **Cron shape**: cohort-standard **windowed** `27 6,9,12,15,18,21 * * *` (daytime, offset :27; RUN-NORMAL 6×/day; `durable:true` reports session-only). **Job id `9c0b0550`.** Freeze-registry row: arch / threshold 6 / wake 6-22 / first_fire 06:27. **SELF-HEAL**: if CronList empty (Gap-C mode-1a) re-CronCreate this expression; if backgrounded-no-fire (mode-1b) the cure is CIO watchdog Belt-0 `open -b` foreground (NOT re-arm — durable:true + re-arm don't fix mode-1b). **⚠️ ACTIVE CIO THREAD (7/5): duty-cycle self-attribution drift** — 7/4–7/5 this session mis-flagged "two arch sessions" + recommended a stand-down; it was ONE session (`list_sessions` authoritative) misreading its own compacted-away work + a sequential cron-id bump (`9c0b0550`→`05b38872`, since settled to one) as foreign. Symptoms reported to CIO (`memo-arch-to-cio-cc-pm-duty-cycle-self-attribution-drift-symptoms-2026-07-05.md`): T1 fire-to-fire context discontinuity, T2 cron-id-change-as-false-evidence, T3 two-worktree straddle. PM confirmed the diagnosis 7/6 (cron-change + worktree-straddle = smoking gun); CIO owns the fix. **Discipline: after any context gap, verify identity via session log + `list_sessions` FIRST.**
- **Branch**: `claude/arch-backup-0630` → `origin/main` via `git push origin HEAD:main` (non-mailbox work).
- **⚠️ MAILBOX METHOD — use `scripts/mail-send.sh` (push-to-ref), NOT the `git -C <main>` bridge dance.** Canonical since #1259 (2026-06-19): write the memo + cc + sent + inbox→read moves at the mailbox paths **in this worktree** (don't git add/commit them), then `scripts/mail-send.sh "mail(arch): subject" <every-changed-path>`. It builds the commit as a git object on top of origin/main + push-to-ref — **never touches the shared main checkout** (so no dirty-checkout contention). Regen MANIFESTs with the **main-checkout venv** (`/Users/xian/Development/piper-morgan/piper-morgan-product/venv/bin/python` — the worktree has no venv/). *(I mistakenly used the deprecated `git -C <main> add/commit/push` bridge dance most of this session — it hit the shared-checkout-contention class on 6/21 12:46 [CIO's uncommitted watchdog.sh blocked the merge]; recovered + switched to mail-send.sh. The bridge dance is what #1259 retired.)*
- **Account/model**: **PM's BACKUP account** (moved 6/30; DinP primary hit its weekly limit), **Opus 4.8** (`claude-opus-4-8`). PM brought me here primarily for Lead-support architecture consults; cost-conscious (backup account) — keep fires economical. **⚠️ NEVER touch PM's main checkout working tree** (100+ commits behind + full of PM's uncommitted drafts — HARD RULE).

---

## DURABLE — role context — carry forward as-is

### Role + identity

- **Role**: Chief Architect (arch).
- **Tool**: Claude Code (Opus 4.7, 1M context); migration 2026-06-17 = account move ONLY (xian@designinproduct.com), no model change. Lowest-risk migration in the wave.
- **Session log slug**: `arch-code-opus` per CLAUDE.md role table.

### Active PM threads (NEW-FORMAT: fold from deprecated escalations doc per CIO 6/17)

**Open questions PM owns** (only PM can decide):

1. **#1273 — create_all-era CORE tables missing create-migrations (NEW 2026-06-17)** — Lead's D5 guard (`TestModelMigrationCoverage`, the #1267 guard-extension) surfaced 4 more tables (`intents`/`stakeholders`/`tasks`/`workflows`) lacking create-migrations — same root class as #1267 but on **core orchestration tables**. Latent (carried via create_all in all current DBs) but a **pre-beta risk**: any clean `alembic upgrade head` (fresh prod rebuild) would lack them → broad breakage. **Arch triage SENT to Lead cc PM 6/17** (`memo-arch-to-lead-cc-pm-1267-affirm-idempotent-head-create-plus-1273-triage-2026-06-17.md`): (1) **gate clean rebuilds on #1273**; (2) **pre-beta must-fix** (not fire-drill — no live breakage today); (3) 4 idempotent-head-creates per the #1267 pattern, per-table ADR-071 D1, `stakeholders` lowest (dormant); (4) flip the stale `test_create_tables_from_scratch`. **PM call**: the exact slot vs. D1/RECONNECT — PM Time-Lords; the architectural constraint is just the clean-rebuild gate.

**Resolved this week** (kept for handoff context):
- **MCPB bundle language (#1282 BYOC-DIST) — DECIDED 6/18** (`memo-arch-to-pa-cc-pm-mcpb-python-reconfirmed-...`): re-confirmed my April-10 **Python default**, but **gate the submission on PA/PM's clean-machine compat-checker test** + **Node pre-authorized** as fallback. Key update to April reasoning: the plugin server is a **thin ~100-line forwarder** to :8001, so my old "strongest Python argument" (context-assembler reuse) doesn't apply → Node is a clean reuse-loss-free rewrite. **Tiebreaker: #1282 is a distribution artifact → if the test is anything short of "clearly passes," prefer Node** (distribution reliability > language consistency for the packaging layer). My read on the bug: it's a pre-flight compat-CHECK bug (greps system Python), distinct from uv-managed execution. **Watch**: loop me only if the clean-machine test result is ambiguous; else PA proceeds per the decision.
- **User-correction recovery (#1193 traps)** → **RESOLVED 6/17 12:35: PM CONCURRED — accept the loss.** No recovery dig (the ~30min would yield ≈ zero — data hit a non-committing `session_scope`; m-41 guard already prevents recurrence). Communicated forward to Lead in the #1267 priority memo's §#1193 (don't spend recovery time). PM-ratified; cheapest honest path.
- **#1267 projects-table 500 (Beta-blocker)** → **RESOLVED 6/17 (`f62c2e998`, Lead)** per Arch ruling + the do-it-next priority rec. Lead's audit refined scope to 1 table (`project_integrations`); made a sound **idempotent-head-create** deviation (repairs already-at-head deployed DBs the mid-chain precedent would miss — affirmed by Arch as the *right* call + named the pattern); D5 guard (`TestModelMigrationCoverage`) shipped + surfaced a bug-class → **#1273** (now thread #1 above). Arch ruling proven end-to-end (ruled → priority-rec'd → Lead shipped → Arch affirmed).
- **#1239 (beta WorkItem Radar) gated on #1233 — architectural disposition DELIVERED 6/17** (`memo-arch-to-lead-cc-pm-1239-lighter-beta-identity-path-...`): **YES a lighter beta-only path exists** — single-bound-user→repo scoping (the configured PM user → their configured repo, the Slack socket-runner pattern), which does NOT need the full #1233 multi-identity unification (#1233 = many connector-identities-per-human; #1239-beta = one bound user → one repo). m-40 layer-then-migrate (single-user binding generalizes to the unified record with no rework). Verify-First caveat to Lead: reuse the existing (cwd-fragile) repo-resolution, don't build new infra. **Net**: PM doesn't pull RECONNECT forward NOR revisit no-partial-ship — neither horn forced. PM/Lead execute the slot.
- **Ship #047 spine call** (Fire 32 6/12) → **PUBLISHED 6/17** as "The team catches itself" (https://pipermorgan.ai/shipping-news/weekly-ship-047-the-team-catches-itself/) — lands on the same load-bearing thread Arch's preferred spine named.
- **Routines watchdog $70/mo funding** → STALE-INFO 6/17 per PM: Max plan covers; not a PM-funding question. The actual question (whether the watchdog is configured + running) is outside Architect visibility — Lead or someone else owns setup if needed.

### Queued Architect-owed work (next-session pickup)

1. **ADR-072 (Skill-routing) — v0.1 AUTHORED + LANDED 6/17** (`docs/internal/architecture/current/adrs/adr-072-skill-routing-architecture.md`, origin/main). PM escalated priority mid-afternoon (PA relay: now, not Thu/Fri) → un-banked + authored from the grounding substrate in one focused pass. 5 decisions + the **derive-from-SKILL.md-frontmatter** load-bearing spine. Notified PA (cc PM/Lead). **→ v0.2 ACCEPTED 6/17 — whole ADR D1–D5 ratified.** CXO + HOST trust-lens both returned same day + aligned → folded into D5 v0.2: CXO axis (gate **Piper-initiated**, never **user-reaching-for-own**) + HOST's two refinements (**consequential-action carve-out** — side-effect skills tier-gated even when reactive, named now per m-36 forward-guard; **transparency-when-gated** — surface the gate via trust-check, not silence). Ratification notified CXO/HOST cc PM/PA/Lead. **Wave P fully unblocked** (incl. proactive-surfacing rules); Lead implements #1245 against the ratified shape. Done; v0.3 only if cohort requests. (Whole arc — v0.1 + grounding + ratification — same day, PM-escalated.)
2. **#972 MEM-TEMPORAL field-spec review — REVIEWED 6/17 (Arch side done)**. Docs delivered v0.4 (`valid_from`+`last_verified`+`valid_until`+`superseded_by`). My review (`memo-arch-to-docs-cc-pm-cio-972-review-structural-ok-keep-valid-until-2026-06-17.md`): 4-field shape structurally sound (no Arch objections); **directional read = keep `valid_until`** (symmetric clarity; Janus inconsistent per CIO; rename-to-`ended` is cheap-if-wrong). Honest scope: no direct Janus/Klatch visibility → the **definitive `valid_until`-vs-`ended` call awaits the Daedalus bridge** (CIO lane; CIO's 6/15 mail unanswered). Docs holds #972 close pending session-log disposition + PM field-name confirm. **No further Arch action** unless Daedalus surfaces a Janus contract that breaks the read.
3. **Cohort review on ADR-070 + ADR-071** — both SHIPPED (Fire 48 + Fire 49 6/15); awaiting cohort acks/refinements at cadence. New-Arch on-call for any v0.2 polish if cohort requests.
4. **#1232 (RECONNECT WS-5 — MCP-consumer Connector contract) — the FIRST RECONNECT action; confirmed on radar 6/18** (Lead flagged, PM-prompted; `memo-arch-to-lead-cc-pm-pa-1232-on-radar-...-2026-06-18.md`). It's the **ADR-070 build-target** (scope doc §10: "WS-5 = the ADR's output"). ADR-070 v0.1 settles the architecture (Connector protocol `connect/status/resolve/degrade`, auth→MCP-layer D3/D4, maturity tiers D6); **#1232's first step = translate that into the concrete `Connector` protocol definition + the 1–2 proof-port plan** the other WS gameplans build against. **Load-bearing dependency**: must land BEFORE WS-1/WS-2 gameplans (D3/D4 auth-to-MCP may shrink them). Shape: Lead-author-from-ADR-070 / Arch-ratify (or Arch-author the protocol spec — Lead's call at kickoff). **NO action until RECONNECT activates** (sprint order D1 → RECONNECT → M4; RECONNECT is Product Backlog, not pulled in; PM owns timing). Fast pickup — ADR-070 is the substrate. **🟢 RECONNECT ACTIVE 6/20 (PM go) — Lead building #1232 NOW.** Lead took the *authoring* (protocol + 4 result types + m-41 guard + a github *structural* proof-adapter; ports deferred to post-Phase-1 per D8); my role = confirm + constrain + **ratify** (Lead-author/Arch-ratify). **Confirms SENT 6/20** (`memo-arch-to-lead-cc-pm-1232-confirms-yes-build-it-plus-type-shape-constraints-2026-06-20.md`): (1) ADR-070 v0.1 IS stable to build to (D2/D3 settled; Open-Q-4/5 are deliberately build-time, not instability); (2) the contract-now/ports-later split matches WS-5 intent (contract is identity-agnostic; only ports need the #1185 foundation). **Open-Q-4 type constraints given**: sum-types for ConnectResult (Binding|ConnectRequired) + ResolveResult (ResourceHandle|ResolveMiss) so honest-degradation is first-class non-maskable (D5 "never silently empty"; same principle as #1283 floor-degrade); DegradationReason enum = WS-4's vocab; ConnectorStatus metadata-only; **no token/cred in ANY return type (D3) — guard asserts it**. **Open-Q-5 confirmed**: MCP-server owns durable OAuth-state, Piper stores no tokens; the only open build-time call = handoff (zero Piper state) vs orchestrate (transient CSRF nonce in Redis, never durable) — UX/MCP-capability question, doesn't gate the contract. **→ TYPE SHAPES RATIFIED 6/21** (verified the actual `connector.py` + the guard test, not the summary; all 5 constraints met; the no-credential guard is impossible-by-construction — auto-discovers dataclasses; Open-Q-4 thread CLOSED). `memo-arch-to-lead-...-1232-RATIFIED-plus-phase1-build-order-...-2026-06-21.md`. **→ Phase-1 build-order RULED 6/21**: WS-1 (D4 config store) builds NOW, **independent of #1185** (PM is sole human → single identity settled; #1185 = public-multi-tenant track, a sibling-not-prerequisite, parked on the gate-removal chain). Order: WS-9-collapse → WS-1 → ports (github first). WS-1 single-user-now but multi-tenant-READY (owner_id FK + D7 tenant_id named, m-40). **Lead unblocked on Phase-1 (WS-1).** decisions.log recorded. **WATCH**: Lead builds WS-1 + the github port → loops me on (a) the WS-1 D4/D7 schema if a design Q surfaces, (b) the Open-Q-5 handoff-vs-orchestrate when the MCP-server connect-flow is known. **⚠️ CORRECTED PHASING (Lead 6/20, ack'd)**: when I fold phasing into ADR-070, **Phase-0 = ADR-070 + #1185 (BYOC identity core, finishing ADR-058) + #1229 (WS-2 cred-model)** — **DROP #1162** (it's `SKUNKWORKS-BYOC-HOSTED-DISTRO` → SKUNK; the real cred-decouple is #1300 → M5). The superseded "decision-a" memo mislabeled #1162 as the cred-foundation; use the correction, not decision-a. **WS-9 (#1233) reframe**: it becomes "key connector config to #1185's UUID-bearer identity" — a *downstream consumer* of #1185, NOT a legacy web/Slack UUID merge → the **ADR-058 / ADR-066-D7 seam is the live design surface**; the D7-OQ-1 consult is in-scope. Pair with Lead on the #1185/#1229 ↔ WS-1/WS-2 seam at RECONNECT kickoff.
5. **#1283 (action↔handler routing-integrity audit) — SCOPED 6/18; ADR-073 candidate pending Lead's clean probe** (`memo-arch-to-lead-cc-pm-pa-1283-routing-integrity-scope-2026-06-18.md`). PM-directed; my scope: **SoT = registration-canonical + derive-the-prompt-from-it** (derive-don't-maintain, m-41) + **runtime-safety nuance** (confident-action-no-handler must NOT silently floor-improvise — the #1269 fabrication mechanism; ADR-060 refinement) + **enforcement = two-altitude** (static reachability lint every-commit [modes 1/2/3] + behavioral golden-corpus on canonical-retest [mode 4]) + **probe = container-init, reachability = rail ∪ category ∪ intentional-floor** (the category false-positive guard). **My next actions**: (a) co-own/ratify the enforcement-test design as Lead builds it; (b) **author ADR-073 (Routing-Integrity Contract) once Lead's clean probe validates the approach** — refines ADR-059 (capability-accuracy → runtime action-reachability) + ADR-060. **Lead ENDORSED all 4 (6/18)** with one nuance — derive the *valid-actions vocabulary* first (the drift-killer), keep few-shot examples hand-curated (example-generation = later optimization, not the drift fix); land the **mode-4 runtime guard first** (highest-value). **Arch concur sent 6/19** (`memo-arch-to-lead-...-1283-concur-...-2026-06-19.md`) + flagged the **intentional-floor allowlist** as the one hand-maintained surface left (keep small/reviewed or it's the next drift surface). **RESOLVER SHAPE RATIFIED 6/19** (`memo-arch-to-lead-...-1283-resolver-RATIFIED-...-2026-06-19.md`; Lead's design `dev/2026/06/19/1283-resolver-shape-design.md` — line-verified read of intent_service.py). Ratified: the 5-way `resolve(action,category)` (RAIL→CATEGORY_CANON→CATEGORY_FLOOR→FLOOR_ALLOWED→GAP) + the `INTENTIONAL_FLOOR_ALLOWLIST` frozenset in `reachability.py`. **+2 soft-gap value-adds**: (A) static lint should also enforce **behavioral-corpus coverage of the soft-gap candidate set** (off-rail→CATEGORY_FLOOR) so soft gaps can't hide untested; (B) the soft-gap containment trigger = **floor honest-degradation keyed on "capability-action emitted but no capability-data assembled"** (detectable floor-state, not a fuzzy heuristic; ADR-059-at-the-floor). Endorsed Lead's hard/soft-gap distinction (the soft gap = the #1269 fabrication) + the mode-4-guard-first sequencing + his quality-banking the resolver impl to a focused fire. **Watch**: Lead lands mode-4 guard + builds `reachability.py` + runs the clean probe → brings me the real gap list (hard/soft/intentional-floor classified) → **I author ADR-073 (Routing-Integrity Contract)**. **(6/19 ack)**: Lead adopted **both** value-adds into the build; **#1283 is RECONNECT-sequenced — he's clearing the D1 tail first (#1236 shipped), then the focused #1283 build.** No Arch action until he loops me on the gap list (he'll flag me if assembler presence/absence signaling needs an architectural call). **→ CORRECTION 6/25**: the probe was NOT actually in (Exec's "probe in" was a from-vantage queue read — verified: no `reachability.py`, Lead rate-limited that week; didn't fabricate a review). **→ #1283 MOVED TO M5 6/26 (PM call; Lead `c08ba086d`).** ADR-073 is now **M5-deferred** — I author it when #1283 activates at M5 + the clean probe lands. Not imminent; standing.
6. **`ROLE-PORTFOLIO-ARCH.md` — DONE + ROUTED 6/20** (`docs/briefing/ROLE-PORTFOLIO-ARCH.md` on origin/main; routing memo to Exec cc HOST+PM `memo-arch-to-exec-cc-host-pm-role-portfolio-arch-v01-for-review-2026-06-20.md`). v0.1 self-authored: purpose = *coherence-by-design*; irreducible mandate = the **architecture-integrity call** (deliberately narrow — fires only when a *ratified* contract would be *silently* bypassed, NOT all-code-review; enforce-that-exceptions-are-recorded vs. PM-decides-the-disposition); steering table w/ forward-indicators; currency-by-weekly-review (dogfoods #972). **Watch**: HOST's 5-rule review (esp. if the mandate calibration needs tightening — I flagged it as the part to check). Refresh section 2 at each weekly workstream review (Rule 5).
7. **#1162/#1307 Caddy gate-removal — architectural read DELIVERED 6/20** (`memo-arch-to-lead-cc-pm-gate-removal-concur-plus-exempt-list-as-security-boundary-2026-06-20.md`; Lead's investigation `dev/2026/06/20/gate-removal-safety-investigation.md`). **CONCUR** AuthMiddleware-as-sole-gate (it's the *correct* model — realizes ADR-058 + ADR-071; Caddy is redundant semantically-blind perimeter). **Load-bearing add**: once Caddy's gone, the **auth-EXEMPT list IS the attack surface** → treat it as a security boundary, **enforce-by-lint, fail-closed** — same shape as the #1283 intentional-floor allowlist. Recommended an **enforcement test**: every auth-exempt route must be read-only OR env-gated-to-non-prod OR on an explicit justified allowlist → #1307-class (exempt+writable+prod) **fails the build** (impossible-by-construction). Rate-limiting = global ASGI fail-closed default + per-route (slowapi) + Redis-backed (not in-process = #1109 class). #1307 fix = fail-closed (env-gate if dev-only, else auth-protect). **Net call: GO on gate-removal once #1307 closed + the exempt-list lint lands.** M5-ish, not urgent. **Watch**: Lead closes #1307 + builds the exempt-list lint (same enforcement family as the #1283 reachability lint).
8. **#1312 (DB↔model schema drift, ~111 diffs) — RULED 6/25 (both seams); awaiting Lead's scoped-increment execution.** (a) **Multi-Base seam RULED 6/25 AM**: `personality/models.py` is a stale pre-#262 duplicate of the canonical `database/models.py:2049` model on an accidental separate Base → DELETE orphan + repoint repo + reject multi-`target_metadata`; named the **one-Base-per-DB invariant**. (b) **user_id-contract pairing call RULED 6/25 PM** (`memo-arch-to-lead-...-1312-userid-contract-ruling-plus-invariant-lint-2026-06-25.md`): option (a) UUID-everywhere + retire the dead `"default_user"` sentinel — grounding dissolved the dilemma (the "trust ×7" callers are a *different* already-UUID repo `UserTrustProfileRepository`; the no-arg `get_default()` sentinel has zero callers). Bounded work given (id-gen via canonical `from_domain()`; cast str→UUID at the personality-repo seam; m-40 keep `enhance_response(str)`, tighten later; delete dead sentinel; owner_id additive re-add rides with #357). **Invariant-lint framing authored** (AST single-Base guard + registry tablename-uniqueness; Lead wires). Concurred Lead's scoped-increment + TDD plan; **sequencing is PM's — slots after the alpha MCPB clean-machine gate.** decisions.log ×2. **Watch**: Lead executes the increment → loops me only if a TDD-surfaced caller (response_enhancer's runtime user_id non-UUID) or an ambiguous destructive-vs-additive drift call needs an architectural ruling. No Arch action until then. **→ TIMING PM-APPROVED 6/26** (Exec relay `0cfbbc439`): proceeds in its agreed slot **after the alpha bundle** (MCPB clean-machine + #1320/#1162), not a pull-forward. Fully specced; Lead queues it next-after-alpha. **Done my side.**
9. **#1220 (RECONNECT WS-8, real MCP transport) — Shape-B RATIFIED 6/27** (`memo-arch-to-lead-cc-pm-exec-pa-1220-shapeb-ratified-plus-1322-cutover-ruling-2026-06-27.md`). Lead's Lead-lane decision (visibility-not-gating): build a NEW SDK-based `MCPClient` (`mcp==1.26.0` already a dep) beside the live sim stack, not retrofit. **Concurred, no gate** (ADR-070 D5 rules the protocol; conformant impl; textbook m-40 layer-then-migrate). **Arch altitude add**: verified `query_router.py:60 enable_mcp_federation=True` default + `MCPConsumerCore.simulation_mode` (`services/mcp/client.py:93`) HARDCODED True → the MCP-federated query path serves SIMULATED data today → **#1322 (legacy cutover) is value-realizing, not optional polish** (it's what makes query routing real). Ruled the sequencing (ports on real client now; #1322 = deliberate query_router cutover + sim-stack deletion, gated on canonical-retest behavioral coverage before deletion) + named the **end-state invariant** (one transport; `simulation_mode` test-only + guarded-from-prod; the hardcoded-sim + stale-POC-comment is a Pattern-073). decisions.log recorded. **Watch**: Lead builds transport infra (inc.1/2 done per his gameplan) + #1317 ports.
   - **⚠️ CORRECTION 6/28 (Lead-caught m-30 overstatement)**: my "serves SIMULATED data today / #1322 is value-realizing" was WRONG. Lead traced + I verified: `query_router` *instantiates* `mcp_consumer` (line 109) but **never CALLS it**; `main.py` starts no MCP server; the sim path is reached only via the `server_core` POC stub + tests → **dead code, no live sim path** (chat GitHub reads were always real REST). My error: asserted reachability from instantiation + hardcoded `simulation_mode` without tracing the call graph (instantiated ≠ called). **#1322 = dead-code removal + m-36 guard** (Lead building, delete→full-suite→repeat), NOT a live cutover; behavioral-coverage reduces to "real paths still pass" (204-green suite). The end-state invariant (one transport, no sim) HOLDS — reached by deletion. Owned to Lead (`memo-arch-to-lead-cc-pm-1322-concur-removal-own-overstatement-2026-06-28.md`) + decisions.log corrected. Shape-B ratify + m-36-guard endorsement unaffected.
   - **PROVISIONING RESOLVED 6/27 (A→C):** ruled A (hosted-OAuth) first; PM's hard constraint (alpha testers can't be required to have Copilot) blocked A → **RE-RULED to C (self-host `github-mcp-server` + per-user OAuth via Piper's GitHub App)** (`memo-arch-to-lead-...-github-mcp-reruled-C-...-2026-06-27.md`). **D3 precised**: protects against raw/long-lived/unscoped vendor creds (PAT), NOT against holding any token; a scoped/revocable/refreshable OAuth grant (#358-encrypted, binding-referenced) is permitted + extends the Calendar-OAuth precedent. Owned my "no token touches Piper" imprecision (→ "no raw PAT"). **D3-ideal end-state = GitHub-App installation-token, tracked #1325** (m-36). Lead building inc.2 on the confirmed model. decisions.log ×2 (A then A→C).
10. **Delivered 6/27 PM (no open Arch action):** (a) **Cron troubleshoot** → PM cc CIO: not a cron bug — in-process scheduler suspension (mode-1b); watchdog loaded+working but nudge-only (alert→resume gap); cure = off-machine trigger (CIO lane) / always-on foregrounded Mac Mini. (b) **Ship #049 Architect lens** (window 6/19–25) → Exec cc PM/PA (new §0 progress-vs-portfolio-goals; RECONNECT + make-drift-impossible both ADVANCED). (c) **inbox-proxy discipline ACKed** (retire reflexive cc-PM; route through Exec by intent) + architecture-lane calibration note. (d) **WS-2/#1229 closed** (carried from item 8-context).
10. **WS-2 (#1229 ConnectorBinding storage) — CLOSED by Lead 6/26.** The RECONNECT config-store foundation; the "Arch WS-2 Q" Lead had planned resolved on his side. No pending Arch action; the #1232 Connector contract + #1229 binding storage now both shipped → the per-connector ports (#1317) are the RECONNECT bulk, building on the #1220 real transport.

### Recently SHIPPED architectural work (last 5 days, for context)

**Three-ADR-in-5-days family** (server-owned state across config / connector-substrate / content):
- **ADR-066 v0.2** (Configuration Ownership, 2026-06-14): D7 server-owned + per-request host augmentation; Cowork sandbox-runtime as source incident; "run anywhere" structural. Commit `04714d3b1`+ amendments.
- **ADR-070** (MCP-Consumer Connector Architecture, 2026-06-15 Fire 48): 9 D-sections incl. ADR-052 reconciliation via two-distinct-boundaries; MCP server owns OAuth/tokens, Piper stores per-user bindings only; D8 identity-first ordering load-bearing; D9 finishes ADR-058 framing. RECONNECT WS-1..9 decomposition unblocked. Commit `4e66015de`.
- **ADR-071** (User-Auth Anchoring Pattern, 2026-06-15 Fire 49): Lead-authored v0.1 from #1241 audit; Arch-ratified clean. D1 PM-domain global-by-design + 3 disciplines; D2 consolidating refactor (`owner_id` FK canonical; `user_id` string deprecated); D4 carries half ADR weight (40+ Optional-degradation sites); D5 m-41 AST guard. Lead Dev executing the cohort migration; #1252 P-multiple shipped.

**Other shipments worth knowing**:
- **#1238 doc-store anchoring IMPLEMENTED** (Lead, 2026-06-16): synthesis owner_id + is_global_pm_domain=true per Arch Fire 53 ruling. New `documents` table; (c,3) close. Lead caught my classifier-overstatement (m-30 self-failure noted; honest disclosure).
- **#1164 private-session mechanism** (CXO ratified, 2026-06-16): `is_private` Boolean column + composting/KG/Radar exclusion filters + 24h retention default. CXO trust contract structurally substantiatable. Boundary: "draws-on-existing / doesn't-contribute-forward" (NOT amnesty mode — distinction folded into column docstring).
- **#1267 projects-table strategy ruling** (Arch, 2026-06-17 Fire 58): (a)-folded-into-(c) via #1252 D2 — reconcile model truth + proper Alembic migrations + retire create_all + per-table D1 classification + D5 guard extension to model↔migration coverage. PM-attention item above on priority placement.

### Cohort process discipline absorbed this week

- **2026-06-12 CLAUDE.md**: Option B ephemeral worktree canonical; single-log discipline (session log only; cycle log = optional scratch).
- **2026-06-13 HOST**: decisions.log reinstatement; Arch added "Recording decisions" section to CLAUDE.md Fire 47 6/15.
- **2026-06-15 PM/HOST**: Fire = WAKE, not time-box. Drain all unblocked work per wake; "Fire N" labels the wake not the task; commits are work-unit boundaries but NOT stop signals.
- **2026-06-15 HOST**: Mail vs. GH-comments cohort norm — `mailboxes/` is cross-agent signaling layer; GH comments = passive work-artifacts. Added to CLAUDE.md Fire 47 6/15.
- **2026-06-16 PM**: "no rush is antipattern in quality costume." Two valid states only — do it now OR explicit "deferring to [reason]." Don't tell another agent "no rush."
- **2026-06-16 PM**: Memos are the cross-agent signaling layer; session-log markers are NOT a substitute for memo-based asks. Reciprocal commitment both directions.
- **2026-06-17 CIO/PM**: Per-role `duty-cycle-escalations-*.md` doc DEPRECATED; PM-attention items now ride carry-forward; FOLD ratified.

### Recently substantive shipments by fire (last ~5 fires for context)

- **Fire 53 (6/16, extended wake)** — 6-stream drain: #1238 disposition / ADR-072 ack+framing / #1252 4 Arch-gated rulings / process clarification / CIO m-30 ack / decisions.log entries.
- **Fire 54 (6/16)** — #1164 private-session mechanism; mea culpa for accidental merge-mishap (Lead's gameplan files restored).
- **Fire 55 (6/16)** — Exec cohort wake-discipline reminder absorbed; ADR-072 deferred with explicit-trigger; CXO #1164 boundary-confirmed ack; Lead #1238/#1252-P2 IMPLEMENTED ack.
- **Fire 56 (6/17 01:22 overnight WATCH)** — inbox-zero; one-line entry.
- **Fire 58 (6/17 11:05 morning START)** — Step-0 self-heal on June 16; #1267 ruling shipped; #1267 follow-up wisdom shipped; PM-attention stale-info cleanup ($70 Routines removed; user-correction recovery recommendation surfaced).

### Cohort-blocked / external (no Arch action)

- Reviewer engagement on ADR-066 v0.2 + ADR-070 + ADR-071 (cohort can engage when ready).
- HOST drafting mail-vs-GH signaling-channel cohort-norm codification (HOST-owned).
- Docs #1182 link-rewrite + cleanup-dev-active omnibus-coverage guard (Docs-owned).
- Lead Dev #1124 + #1158 + #952 + #355 + #1252 + #1267 implementation in flight.

### F4 Gap-C session-dormancy mechanism

- 5 instances in 5 days through 6/16. Cron consistently dies with session at dormancy boundary.
- CIO empirical investigation 2026-06-11 named Gap-C as the dominant cron-loss mechanism. `durable=true` is no-op.
- Cure = Routines watchdog (PM Max plan covers; not a funding question; setup-status outside Arch visibility).

---

## Notes for new-Arch

- **Bootstrap-brief staleness CONFIRMED + reconciled (2026-06-17 new-Arch bootstrap)**: the migration bootstrap brief named "MCP connector ADR + topology (owed; #1220; ADR-070 candidate; Lead Dev waiting on topology before decomposing WS-1..8)" as the likely first substantive action. **It's stale.** ADR-070 (MCP-Consumer Connector Architecture) already **SHIPPED 6/15 Fire 48** — it IS the Phase-0 ADR the input doc `connector-refactor-sprint-scope-2026-06-14.md` §6 calls for (reconciles ADR-052 via two-boundaries D2; auth-to-MCP-layer D3/D4; identity-first D8; finishes ADR-058 D9), and the **WS-1..9 decomposition was FILED 6/14** (12 RECONNECT issues, doc §10). Same pre-6/15 snapshot as the CIO-guidance staleness below. **Connector ADR is NOT owed — do not re-author** (Verify-First caught it). Genuinely-live Arch work = ADR-072 (deferred, grounding-first), #972 review (waiting on Docs), ADR-070/071 cohort polish on-call, + the 2 open PM-attention items. **Possible micro-task**: verify the §0 MCP decision was appended to `decisions.log` (input doc §11 flagged it as the decisions.log's exact use case) — if not, a ~5-min append is concrete unblocked work.
- **CIO migration guidance (2026-06-17)** carried two outdated specifics: cron id `175b5163` (actual at migration: `4bc6e90a`); ADR-070 + ADR-071 listed as "in-flight" (actually both SHIPPED 6/15). Not faulting CIO — guidance was templated from earlier migrations. Adjusting forward.
- **Cycle log convention**: I kept thin cycle logs (`dev/active/cycle-log-arch-YYYY-MM-DD.md`) as optional scratch for audit visibility, mostly for overnight WATCH entries. Pure optional; don't carry the pattern if you don't find it useful. The session log is THE durable record.
- **Standing-items doc** (`dev/active/arch-standing-items.md`) is more granular than this carry-forward — kept it for task-queue tracking. Refresh-on-touch discipline (refresh as part of any cycle-fire where the queue meaningfully changes).
