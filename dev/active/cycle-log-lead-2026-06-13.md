# Lead Dev cycle log — 2026-06-13 (Saturday; m-31 append-only)

## Fire 1 (07:39 PDT — 07:17 START)
New-day START. 06-12 closed clean (DAY-CLOSED ✓). Cron 3cbea126 CronDelete'd (Rule 1, substantive fire). Mail: Arch skunkworks ph2 lens CC → read/ (no action; converges w/ my infra input). Weekend prime-time → advancing #1165 init-recursion leak (recommended top, unblocked infra) autonomously. Detail accrues below + session log.

### Fire 1 cont. — #1165 init-recursion leak DEFINITIVELY root-caused (~07:45–08:15)
Verify-first via raw traceback (`canonical-retest-2026-06-12-2216.log:258-349`). Corrected my earlier "init re-enters init" read: it's a **single linear stack** (get_api_key env-var-fallback warning → structlog → stdlib callHandlers → StreamHandler.emit → Formatter.format → RecursionError); the post-frames are exception-chaining, not recursion. **Harness-only** (240 in-process app boots/process under function-scoped fixtures accumulate logging/runtime state → ~boot-49 tipover; boots 1-48 pass; production boots once → never hits it). **No clean app-side idempotency fix** (filter dedup'd, structlog module-level, no per-boot addHandler in lifespan). **Fix = gate-harness boot-once** (recommend Option 2: canonical-suite-specific session-scoped app fixture; gate-semantics change → PM/Arch nod before retrofit). Definitive analysis on #1165. Did NOT unilaterally change the load-bearing gate harness — surfaced for ratification (matches PM's "next-move is your call" framing).

## Fire 2 (10:47 PDT — the 10:17 fire, ~30m late: REPL busy with PM through UAT Q&A)
**WORK PARTS.** Cron 4aaed4c5 healthy (single, my expr); CronDelete'd at fire start (Rule 1 — substantive); re-arm at end. Lead inbox empty (matches SessionStart hook — lead not in unread list). Server :8001 healthy (200). Discarded mailbox-MANIFEST regen noise in worktree (19 files, other agents' mail on origin/main; "only MANIFESTs changed, no mail moved" — derived artifacts I can't commit from a feature branch anyway).

**Item 3 (corpus/scoring expansion) — SCOPED.** PM's morning sequence item 3, motivated by "100%-pass + wiring-bugs can coexist." Read the full canonical harness (61 queries, 4 tiers). Diagnosed **5 coverage holes**; the load-bearing find = **#1131** (stateless judge false-fails honest data responses → judge is floor-only *for a reason* → "extend judge to all queries" is WRONG; canonical/action need **ground-truth assertions**, not more judge). Q16 is the proof case. Wrote scoping doc `docs/internal/testing/canonical-suite-expansion-scoping.md` (`9977b4e6e`); filed **#1213** (priority TBD — PM triage). Priority: P3 multi-turn (guards #1122/#1207, non-gated, start first) > P1 ground-truth assertions (PM-gated, biggest catcher) > P2 honest-degradation > P4 raise judge difficulty > P5 breadth. Flagged which are PM-gated (semantics) vs. just-do (additive).

**HELD (PM's call — pending answers from my 10:24 reply):** (a) where the UAT 1-6 steps live durably (#1165 vs runbook) — offered #1165; (b) whether I drive UAT items 2-4 (the chat-query ones #1155/#496/#497) like I drove #953/#1143 server-side. Not freezing on these per Jun-6 rule (advance other unblocked work; hold only the specific thread). Item-3 scoping was the unblocked advance.

### Fire 2 cont. — PM UAT results + the #1133 flattening dive (PM-directed, ~11:04–11:5x)
PM ran the manual UAT: (1) #1133 PASS functionally BUT flagged **history-sidebar concept flattening** (out of scope for #1133 → wants forensic + CXO/PPM memo); (2)#1155 PASS; (3)#496 PASS ("good answer from limited context — perfect for the test"); (4)#497 untested + **calendar connect errored**; (5) workstyle-learning — PM asks seeded-vs-github/conversational?; (6) trusts server-side.

**Dispatched 3 parallel forensic subagents** (PM sanctioned delegation). Findings:
- **#1133 flattening = 3rd recurrence, predicted.** MUX = "Modeled User Experience" (glossary; ADR-045 grammar "Entities experience Moments in Places"). Vision EXISTS + explicit: **PDR-002 Layer 2** ("entities primary, conversations navigation aids"; Layer1=continue-which-chat, Layer2=what-Piper-knows; lists "just show conversations" as anti-pattern). Feb-1 archaeology + Feb-6 cathedral-memo already named + **predicted** this. Current build: right sidebar → `/api/v1/conversations`; richer `/users/me/history` (#1021) unwired; #1133 test asserts only structure. Gap = no binding link vision→impl + no mockup (NOT under-articulation). "Being good" framing (CXO 6/3): memory-surfacing is "ours to define"; history-nav is the dominant-paradigm box (left nav).
- **Current build**: modules don't reset (intentional, PM-aware) + not collapsible (net-new); **seed-leak** confirmed (dev composting seed → durable journal → home "recently", no seed filter, dup from 2-lessons-per-object); GitHub "no open issues" = unresolved default_repo for m1-test (→ #1199/#1195, not new).
- **Workstyle (PM Q5)**: both cited observations are **100% SEEDED verbatim** (`dev/2026/05/28/seed-uat-insights-m1test.py`; conf 0.64→MED, 0.41→LOW), NOT github/conversational. CAVEAT: NO is_seed flag exists → "these are seed placeholders" is the LLM **confabulating** a distinction it can't make.

**Calendar error** (server log): `/settings/integrations/calendar/connect` → 503 "Google Calendar OAuth not configured" (no credentials.json / GOOGLE_CLIENT_ID/SECRET). Config gap, not code bug.

**Deliverables**: CXO/PPM memo DRAFT `dev/active/memo-lead-to-cxo-ppm-history-sidebar-flattening-2026-06-13.md` (held for PM blessing before bridge-delivery). Filed **#1214** (seed-leak), **#1215** (calendar 503), **#1216** (workstyle confabulation). HELD: send memo as-is vs PM edits.

### Fire 2 cont. — PM responses + memo sent + UAT 2-4 (~11:3x–11:4x)
PM: (a) **send memo** → delivered to cxo+ppm inboxes + CC xian(ceo) + lead/sent via bridge (`82710ef12`, on origin/main); (b) **calendar config via UI, not back-channel** → re-scoped #1215 (UI affordance; dropped keychain-store framing); (c) **UAT steps**: "log today's, that's adequate" (no runbook — they change over time) → posted complete UAT record on #1165; (d) **drive UAT 2-4**.
**UAT items 2-4 driven server-side** (minted JWT, token in-process to skip the header gotcha; server already has the real key so no client env-strip): #1155 'work on next' ✅ (floor/PRIORITY, 1345ch), #496 'top priority' ✅ (795ch), #497 'focus on' ✅ (1465ch — PM hadn't tested) — all substantive colleague answers, zero error fingerprints. #1165 UAT record complete (items 1-6 all ✅; flattening escalation + 5 discovered issues listed).

### Fire 2 cont. — #1214 seed-leak FIXED (PM-greenlit, ~12:5x–13:0x)
Verify-first: traced the marker through the pipeline — `_extract_topic_tags` (composting_pipeline.py:548) carries the SeedCompostable's type/category (`dev_seed`/`seed_demo_object`) into `context_tags`. Empirically confirmed on real m1-test (33 insights = **28 dev_seed** [obj_id `seed-…`, 4 `/seed` batches] + **5 uat-anniversary** workstyle). Fix: `_surface_composted_insights` over-fetches (`_RECENCY_FETCH_CAP`=50) then drops `_DEV_SEED_TAGS`-tagged insights → seeds neither render nor crowd real reflections off top-N. Home-surface-only (shared `list_for_user` untouched). 21 home tests (incl. new exclusion+no-crowd test) + 29 downstream PASS; empirical end-to-end PASS (zero seed content). `5ab281912`→`066fba429` on main. **#1214 CLOSED** w/ evidence. Surfaced: after the filter the module falls through to the 5 uat-anniversary workstyle seeds → **flagged to PM** (entangled w/ #1216 + flattening memo — the surface shows ALL provenances). (Push note: worktree mailbox-MANIFEST regen noise blocked the first merge; `git checkout -- mailboxes/` then re-merged — 2nd recurrence this session.)

### Fire 2 cont. — mail + #1213 P3 SHIPPED (~13:1x–13:2x)
PM order: **#1213→#1215→#1216**, batch questions, work autonomously. **Mail** (2 unread the resume hook missed): **CXO already responded** to the flattening memo — *consolidate*: Layer1=left nav, Layer2=Radar=home modules, **retire the redundant right chat-list sidebar** (the redundancy is what drives the recurrence); CXO owns the entities-surfacing mockup + an honest-provenance design principle; PPM owns the object-model/`is_seed` field. **PA M3-queue memo**: puts **#1210 SAFETY first** ("regardless of sprint state") — **forks with PM's #1213-next**; + 2 new in-lane issues **#1217** (ethics floor assumes Lead Dev is human → blocks PM→engineer assignment) + **#1218** (`#NNN` triggers close_issue at 1.0 over prioritization intent). Both memos triaged to read/. **#1216** comment added (home-recently fall-through + CXO honest-provenance principle + cross-lane ownership → satisfies PM's (a) condition).
**Investigate-first WIN**: AAXT golden scenarios ALREADY guard multi-turn antecedent (incl. the #1122 regression test) — but **GATED** (`AAXT_ENABLED`+$0.50/run). So the #1213 scoping's "zero multi-turn coverage" claim was WRONG → corrected the doc. Real gap = a **cheap every-PR** guard. **P3 = `TestCanonicalMultiTurn`** (deterministic, no judge): doc-antecedent (string-not-contains the "which document" punt) + conversation-pronoun. **Verified live (2 passed, 45.8s)** + **non-vacuity proven** (T2 routes `update_document_query`, resolves antecedent to T1's doc name; nonexistent doc → no mutation). Scoping doc Hole-4 + P3 corrected/marked ✅.
**Batched for PM** (not blocking): #1210-first fork vs your #1213-next; sequencing of new #1217/#1218.

### Fire 2 cont. — #1215 investigated → already-built (~13:3x)
Verify-first traced the calendar-OAuth-via-UI loop: **already shipped (#577)**. `templates/settings_calendar.html` has the client_id/secret form + Google-Cloud-Console link + Save→`/calendar/app-credentials`→`IntegrationConfigService.store_google_credentials`→keychain; `GoogleCalendarOAuthHandler` reads env>IntegrationConfigService(keychain). PM's 503 = clicked **Connect** before entering creds. **PM's "config via UI" directive ALREADY satisfied.** Remainder: (1) PM-gated — obtain+enter a Google Cloud OAuth client (can't do for them); (2) optional Lead UX polish — gate Connect on creds-status (the #1212 graceful-degradation theme). #1215 re-scoped via comment. → **BLOCKED on PM** → surfacing the batch (queue worked to natural gates: #1213-P3 done; #1215 PM-gated; #1216 partly cross-lane).

### Fire 2 cont. — #1216 Lead-side guard SHIPPED (~13:3x–13:4x)
Root cause sharper than scoped: the insight-pull floor prompt surfaced topic_tags INCLUDING seed markers (`uat-anniversary-*`, `dev_seed`, `seed_demo_object`) → LLM reasonably inferred seed-ness → announced the ungroundable seed-vs-real claim. **Fix**: filter internal tags at the single chokepoint `_gather_insight_pull_context` (context_assembler.py); legit topical tags preserved. **10 unit tests** (incl. new filter test) + **live end-to-end** PASS — Piper now says "internal bookkeeping… too abstract" (content, grounded) + STILL surfaces the real github-triage observation; zero "seed/placeholder". `261bcabb7` on main. #1216 commented; stays open as honesty/provenance anchor (PPM provenance field + CXO empty-state design remain — composes, doesn't conflict). **PM's named queue #1213→#1215→#1216 now ALL addressed** (P3 shipped / #1215 found-already-built / #1216 guard shipped). Remaining = PM-gated batch (#1210 fork, #1213 P1/P4, #1217/#1218, #1215 polish).

### Fire 2 cont. — #1210 classifier safety fix SHIPPED + CLOSED (~13:4x–13:5x)
PA/triage's #1-priority safety item; did it as pre-authorized unblocked work (named queue done → no ordering conflict). Verify-first traced consumers (autonomous_executor uses classify_action; #1195 inner gate `execute_with_safety` REQUIRES SAFE) + the allowlist (incl. verb-less reads attention/shipped/next_todo_query — which a naive `_query`-strip would've broken). **Root**: substring scan → the `_query` suffix's "query" green-lit mutating close/reopen/comment_issue_query. **Fix**: confirmation verbs (+close/reopen) matched as exact TOKENS checked BEFORE safe; safe scan also token-based (kills widget→get / list_labels→label false-positives); destructive stays broad substring. Verb-less reads stay SAFE via "query" token → #1195 reads unaffected. New **28-test suite** (none existed) + updated the #1195 test that pinned the bug-as-precondition. **39 passed.** `d6d017d46` on main. **#1210 CLOSED** w/ evidence. Filed **#1219** (latent: list_releases_query/shipped_this_week classifier false-negatives — safe-side, pre-existing). Defense-in-depth restored (both gates block mutations).

### Fire 2 cont. — #1218 + #1217 NON-REPRODUCTION (~13:5x)
PA's two new issues from the consult-piper eval. Reproduced 8 varied probes against live :8001 (pre_classifier unchanged): **ALL route correctly** — #NNN prioritization ("prioritize #1210 or #1216?"→compare_priorities 0.95), #NNN status/details (→get_ticket_status / get_issue_details), close-phrasings ("close out this sprint"→PRIORITY, "close to done"→query) — none mis-route to close_issue; AND PA's exact #1217 phrasing ("which M3 issues should Lead Dev work on next?"→PRIORITY/get_priorities floor=True, **NOT ethics-blocked**). **Neither #1217 nor #1218 reproduces via direct /api/v1/intent.** Hypothesis: the consult-piper ENRICHMENT path (injects GitHub context + re-asks) is the real trigger surface, not bare intent. Documented non-repro + requested PA's exact input/path on both issues. Did NOT blind-fix (narrowing pre-classifier regexes without a repro risks breaking legit routing — investigate-before-extending). → BOTH BLOCKED on PA's exact repro.

### AUTONOMOUS RUN COMPLETE (13:5x)
All cleanly-unblocked work done this run: **shipped #1214 (seed-leak, closed), #1213-P3 (multi-turn guard), #1216 (confabulation guard), #1210 (classifier safety, closed)** + filed #1219. PM's named queue #1213→#1215→#1216 fully addressed. Everything remaining is gated/blocked: #1215 (PM obtains Google OAuth creds), #1218/#1217 (PA's exact repro), #1213 P1/P4 (PM semantics go-ahead), #1215 Connect-polish (PM). Cron 7ac1b623 armed (next 16:17). Standing by for PM.

### Fire 2 cont. — PM clarifications + History→Radar relay (~14:0x)
PM: (1) the PA-fork was PA's perspective-triage (focused on their own recent work), **NOT a priority override** — my handling (honor named order + surface the fork + do #1210 as plain unblocked work) matched intent. Weight cross-role input as perspective; PM's direct word governs (reinforces [[feedback_honor_durable_instructions_under_cross_pressure]] + Time-Lord no-false-urgency). (2) **No users at risk → sensible order, no urgency.** (3) **Design lean: "use Radar where the UI has History now"** — kills the seems-like-chat-history flatten at root (the slot stops being a chat list). Engineering: feasible (sidebar renders `/api/v1/conversations` now → render the Radar entity/insight surface instead, reuse the home modules); **nuance = fold chat-SEARCH into Radar entity-search** (conversations = one entity type per PDR-002), don't keep a separate list. **Relayed** PM's placement lean + engineering shape + the chat-search nuance to CXO/PPM (cc PM): `memo-lead-to-cxo-ppm-cc-pm-radar-in-history-slot-placement-2026-06-13.md` (main `3d1f82fd1`). Sensible order = design(CXO mockup)-then-build; I implement the slot swap when the mockup lands. Tracking #1090.

## Fire 3 (16:20 PDT — 16:17 fire; WORK; PM heads-down on #1215 OAuth console setup)
Battery-shutoff recovery earlier this hour: cron died → re-armed; server back to 200; DB healthy; all prior work safe on origin/main. PM session green-lit: **raise-scoring (P1+P4)**, **ratified Radar consolidation**, **agreed #1206 reframe** (PM tells Docs), and noted I can **self-invoke /consult-piper** for the #1217/#1218 repro. Gave PM the **#1215 Google OAuth step-by-step** — durable doc `docs/internal/operations/calendar-oauth-setup.md` (designinproduct.com account, both redirect URIs, calendar.readonly+userinfo.email scopes) + chat walkthrough.
This fire (CronDelete'd 088a8d13 per Rule 1; mail empty):
- **Radar consolidation RATIFIED → relayed** to CXO/PPM cc PM (`bf4a8934d`): lean→confirmed, proceed with the mockup with certainty.
- **#1213 P4 SHIPPED** (`698ad5295`): raised the canonical judge bar — STRICT (default: verdict==PASS, drops MARGINAL-as-pass) with `CANONICAL_JUDGE_STRICT=false` toggle restoring the old PASS|MARGINAL bar (PM's "compare new/old over time" = an env flip). **Verify-first WIN**: a live Q1 run caught an over-reach — my initial Context>=2 floor false-failed context-less queries (identity "what's your name" → R=3 C=1 T=3 = PASS, but C=1 is *correct*, no user-data to ground). Dropped the floor (data-grounding = P1's deterministic job, not a judge floor); re-verified Q1 fails-under-floor → passes-under-PASS-only. Gated tier (CANONICAL_JUDGE_ENABLED) → zero every-PR-suite impact.
Remaining green-lit: **P1** (ground-truth assertions — bigger, next fire), **#1217/#1218 self-repro via /consult-piper** (next; verify server LLM path first). Cron re-armed.

### Fire 3 cont. — calendar connect flow FIXED (root, not bailout) (~16:4x–16:5x)
PM hit the real dead-end: Integration Health dashboard's Calendar "Connect" with no way to enter creds. PM: *"we don't want to just bail me out. we want to fix the issue."* Traced TWO bugs: **(A)** dashboard (`integrations.html`) OAuth "Connect" fired `/connect` directly → 503 when app-creds unconfigured, no path to the creds form; **(B)** config page (`settings_calendar.html`) `connectCalendar()` did `window.location.href` to the `/calendar/connect` JSON endpoint → showed raw JSON, never started OAuth. **Fix**: (A) OAuth not-connected Connect → links to `/settings/integrations/<name>` (config page), mirroring the API-key services; (B) `connectCalendar` fetches `/connect` → redirects to `auth_url` (+ 503 guidance). **Verified**: authed in-process render (both 200, fixes present, old nav gone) → then confirmed **LIVE on :8001** (Jinja auto-reload picked up both, no restart). `0ee653ad2` on main + main-checkout ff'd. (Debug: a grep-regex artifact — `${...}` read as regex — gave false 0s mid-verify; `grep -F` confirmed all-present. UI-fix-memory honored: real render, not curl-200.) PM completes the OAuth from the now-working flow.

### Fire 3 cont. — CALENDAR CONNECTED + verified e2e; #1215 CLOSED (~17:4x)
PM connected Google Calendar via the fixed flow. **Verified live** (m1-test, :8001): status `configured=True valid=True`; "what's my week look like?" → **real events** (Product Coffee, Bridge Town Hall…) via the MCP calendar adapter (`week_calendar`); "what should I focus on?" → now **calendar-ENRICHED** ("only 12 minutes before your next thing") = the time-aware richer answer that motivated the whole effort. Full arc: #577 config-via-UI existed → connect-flow bugs fixed (`0ee653ad2`) → PM created the OAuth client (designinproduct.com) + connected → verified. **#1215 CLOSED** w/ evidence. Setup guide durable at `docs/internal/operations/calendar-oauth-setup.md`. Bonus: the dashboard-dead-end fix applies to all OAuth-app services (slack too); the saga surfaced the MCP-connection-layer idea → **#1220** filed.

### Fire 3 cont. — #1213 P1 ground-truth SHIPPED (todos slice) (~17:5x)
PM: "move to #1213." Built **P1 (ground-truth assertions — the biggest hole)**. Verify-first: live-confirmed add-todo executes (`create_todo`) + show-todos echoes the title verbatim → the API-seed approach. `TestCanonicalGroundTruth.test_show_todos_reflects_seeded_todo`: seed a uuid-marked todo → assert "Show my todos" reflects it (deterministic marker echo, **no judge** → sidesteps #1131's stateless-judge limit). Live verified (1 passed, 17s); **non-vacuous** (marker is only in the *add* query, so it appears in *show* only if the handler read the seeded todo). `50f218193` on main. Todos = the cleanly-seedable target; external-data types (issues/milestones/calendar) need a seedable-fixture/mock approach → follow-on (harder, external state). Cleaned up the m1-test `P1EXPLORE` marker via `complete_todo` (bonus: live-verified complete + honest empty-state). **#1213 status: P3 ✅ + P4 ✅ + P1-todos ✅; remaining = P2 (honest-degradation), P5 (breadth), P1-external-extension.**

### Fire 3 cont. — #1213 expansion CORE COMPLETE (P1/P2/P3/P4/P5) (~18:1x)
PM: "keep going, assume I want the whole thing." Completed the core:
- **P5/P1-lifecycle**: TestCanonicalGroundTruth 2nd test (add→complete→drops-from-active; catches "complete didn't complete" wiring). 2 GT tests pass. `56b9e1241`.
- **P2**: ERROR_FINGERPRINTS 4→11; **verified 60/60 non-Q16 = zero false-positives** (Q16 still flagged = real #1212). `f2e0170c6`.
- Full file collects clean (Routing 61, Structure 157, Quality 25 gated, MultiTurn 2, GroundTruth 2).
**#1213 CORE DONE**: P1(reflect+lifecycle) + P2(broaden) + P3(multi-turn) + P4(raised-judge) + P5(depth-breadth), all verified, every-PR (no judge cost except the gated quality tier). Summary comment on #1213. **Remaining follow-ons** (documented in scoping + #1213, not forced into fragile versions): P1-external-data (needs a mock-adapter harness — new pattern), P2-action-detector (ties #1212), optional projects/lists GT. **Surfacing the mock-harness as a close-call to PM** (build-now vs track) per "discuss close calls."

### Fire 3 cont. — mock-harness PLAN written + tracked #1221 (~18:2x)
PM: track the follow-ons + write the mock-harness plan, then decide build/elsewhere/pause. Wrote `docs/internal/testing/mock-adapter-groundtruth-harness-plan.md` (`242ec6564`), grounded in **real adapter seams** (verified): `GoogleCalendarMCPAdapter.get_events_in_range`/`get_todays_events`/…; `GitHubMCPSpatialAdapter.list_github_issues_direct`/`list_milestones`/…. Design = patch the adapter's public **async** fetch method (class-level `AsyncMock`) around the in-process request (works because ASGITransport runs the handler in-process), assert the response reflects known data. Scenarios: reflect / empty-state-honesty / degradation (folds in P2-detector). Key correction: risk is **patch-point maintenance, NOT runtime flakiness** — mocking *reduces* flakiness vs live external calls. Effort ~M (half-day); first slice = calendar reflect. Filed **#1221** (tracked, plan-ready). **Awaiting PM: build / focus elsewhere / pause.**

### Fire 3 cont. — mock-harness BUILT (calendar slice) (~18:3x)
PM: "build it, starting with the calendar slice." Verify-first traced `_handle_week_calendar_query` (intent_service.py:5013) → `CalendarIntegrationRouter.authenticate()` + `.get_events_in_range()` (deterministic formatter, event dict = start_time/summary/duration_minutes/is_all_day). Built `TestCanonicalGroundTruthMocked` (patches both methods, class-level AsyncMock, around the in-process request): **reflect** (known event renders), **empty** (honest "didn't find any events", no fabrication), **degradation** (adapter raises → conversational message via `_make_error_result`/UserFriendlyErrorService #876, no raw leak). **3 passed (6.3s)**; non-vacuous (marker only in mocked events). `6874834ef` on main. Harness PATTERN proven. #1221/plan updated. **Remaining: GitHub slice** (same shape on `GitHubMCPSpatialAdapter.list_*` — more involved: multiple github query handlers to trace).

### Fire 3 cont. — mock-harness GitHub slice + #1221 CLOSED → #1213 expansion COMPLETE (~18:4x)
PM: keep going (whole thing). Traced `_handle_list_milestones_query` (intent_service.py:4389) → `GitHubIntegrationRouter.list_milestones_via_mcp()` (clean single method, milestone dict = title/due_on/open_issues, renders title). Added GitHub slice to `TestCanonicalGroundTruthMocked`: milestones **reflect** + **empty**. **All 5 mock tests pass** (3 calendar + 2 github, 6.2s) across two integrations. `eefc47ad5` on main. **#1221 CLOSED** (harness delivered + proven on calendar + github; further methods trivial follow-on). Plan doc → DELIVERED.
**#1213 EXPANSION COMPLETE**: P1 (todos reflect+lifecycle + external-data calendar+github via mock-harness) · P2 (fingerprints 4→11, verified) · P3 (multi-turn) · P4 (raised-judge toggle) · P5 (depth-breadth). All verified, every-PR (no judge cost except the gated quality tier). Only trivial incremental tails remain (more github methods; P2 deeper per-action detector tied #1212; more seedable types).

### Fire 3 cont. — #1212 Q16 graceful-degradation FIXED + CLOSED (~19:5x)
PM: "move to #1212." Verify-first reproduced Q16 in-process: real error = `GitHubConfiguration.__post_init__` (github_config.py:31) raises 'Repository must be in owner/repo format' on **EMPTY** default_repository (empty='no default', valid, NOT malformed); `create_issue` constructs that config internally → threw → UserFriendlyErrorService 'no pattern' → generic 'something unexpected happened'. **Honest self-correction**: my first edit (handler repo-check) didn't fire — the raise was in the CONFIG (inside create_issue's internal construction), not on the passed repo_name; re-traced via the raise-site grep (github_config.py:33) → found the real root. **Fix (2 parts)**: (1) github_config.py — empty default_repository valid (only non-empty-malformed raises); (2) _handle_create_issue — pre-check resolved repo → honest actionable degradation (mirrors the missing-token pre-flight). **Verified**: Q16 → honest "couldn't tell which repository" (no generic error, routing unchanged, NO issue created); **all 3 Q16 canonical tests pass → the 242/1/0 baseline's lone red is GREEN, suite clean**. No regression (github-issue+repo-resolver 47 pass; the 1 failing multi-user-config test fails identically pre-change → pre-existing pm_start bug → filed **#1222**). `4971e1994`. **#1212 CLOSED.**

## Fire 4 (19:57 PDT — 19:17 fire; PM mid-conversation, asked "keep going or call it here?")
CronDelete'd 7a12f7cb (Rule 1). Date/branch ok; server 200; origin/main current. **Mail**: 1 — PA #973 MEM-CACHE-AUDIT (post-M3 queue addition; response-requested: ack + confirm sequencing). **Acked** (`3a28dc908`): #973 queues after M3 (won't bump #1210/M3); will coordinate the STABLE/DYNAMIC annotation with Docs when it's time; M3 remaining = #1165 gate + #1216 + flattening direction. Incoming → read/.
**WORK disposition**: cron-named next (#1217/#1218 self-repro via /consult-piper) requires the consult-piper ENRICHMENT flow — the bugs DON'T repro on direct /api/v1/intent (verified earlier; trigger is the enrichment path, not the bare query). PM is mid-conversation (pending "keep going/pause"), so rather than autonomously launch a heavy/investigative consult-piper run mid-decision, kept this fire light (mail handled) + re-surfacing the #1217/#1218-repro-vs-pause choice to PM. Cron re-armed. **#1213/#1212 all done; M3 substantially clear** (closed today: #1210/#1212/#1214/#1215/#1221).

### Fire 4 cont. — #1217/#1218 repro via consult-piper relay → NON-REPRO (PM: keep going) (~20:0x)
PM said keep going → ran the consult-piper relay path (`ask_piper` MCP, PM-enabled — the actual relay consult-piper uses, which may route differently than direct /api/v1/intent). **#1217** "which M3 issues should Lead Dev work on next?" (exact phrasing) → PRIORITY/get_priorities (floor_hit, 0.85), normal helpful answer, **NOT ethics-blocked**. **#1218** "Between #1216 and #1222, which should I prioritize?" (#NNN) → PRIORITY/compare_priorities (0.95), **NOT close_issue**. **Both NON-REPRODUCING via ask_piper too** (already non-repro on direct intent — 10 total probes across 2 paths). Conclusion: **context-dependent on PA's specific consult-piper SESSION** (accumulated multi-turn state), not any single query (or resolved since filing). Documented thoroughly on both issues; → **BLOCKED on PA's exact session capture** (exhausted my repro paths; fixing blind would risk breaking currently-correct routing). Clean Lead-lane unblocked queue now thin (remaining: #1199 default-repo-unify [completes #1212 create-issue loop, involved-arch], #1216 provenance [PPM lane], History→Radar [CXO mockup]).
