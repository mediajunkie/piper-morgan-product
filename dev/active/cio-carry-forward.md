# CIO carry-forward — rewritten 2026-08-09 STOP

**Cron**: `8e40e691` · `7 10,16,22` LEAN · re-armed 2026-08-09 STOP · **auto-expires ~2026-08-16**.
**Three silent cron deaths**: session exit · 7-day expiry · context compaction.
**Worktree**: Model A, `claude/cio-cycle`, upstream `origin/main`.

---

## ⏸ AWAITING PM

1. ⭐ **Memory-index hybrid packing — and the clock is REAL, measured over a full 24h cycle.**
   `182 → 185 lines in 24h = **3 lines/day exactly**. Headroom 15 ⇒ **5 days**.` **State the convention**: that is `wc -l`; HOST's Step 1c reads the guard convention, which is one higher (186/14). *Same file — do not mix them in one trend line.*
   **The fix**: pack the **127 of 178 self-describing slugs** at 4/line, keep the **~48 terse ones described** → **185 → ~90 lines**. **Lead will build the generator change on PM's ruling.** Trades recall for capacity on the shared pool ⇒ PM's call.
   ⚠️ **PM's chosen option ① does not relieve the binding limit** (lines, not bytes). Today's blank-line fix was the entire "for now."
2. **Innovation agenda §6** — building mechanisms vs protecting a property. **Three more instances this week alone**: Comms refused a correction I got wrong; Web refused to act on my false COHORT-FREEZE; Arch self-reported a casualty their own remediation caused.
3. **Short-period cron experiment** — the only way to decompose the ~30-min dispatch latency (jitter saturates at 15 min on all eleven seats). ~3 extra fires on my seat.

## Owed / in flight

- **Freeze detector**: v0.2 now reads `origin/main` and prints `ref=`/`tip=` (Web caught v0.1's false positive from a stale local checkout). HOST closed the waking half; **the *during* half still needs Pard to land the 4-line watchdog patch — sent 08-08, still 0 references at 08-09 STOP.** Only a real crontab can watch a frozen cohort. **Follow up if it's still unlanded at the next START.**
- **Recurring-instrument self-firing (PM 08-07)** — Role Health workflow fixed; **Agent 360 + skill-candidates still have NO workflow.** Copy the corrected pattern; verify by **step-level conclusions**, not the green tick.
- **`cio-standing-items.md`**: memory-index option ①, Exec's mail-protocol fixes (check duplication against my 08-07 Step-3 restoration), and **PM's chess-board idea** — *"agents have a move log and no position."* Still the most interesting item on the board and still owed a real design pass.

## Watch

- **`docs` inbox 149+** — the cohort's one real mail backlog.
- **Merge-drop**: SCOPE-IS-NOT-DIRECTION shipped to CLAUDE.md; **no tooling built on purpose.** A second seat tripping it earns a guard.
- **Glob-drain**: banned in the skill; my own drain had the defect. **Cannot be audited from outside** — grep finds only roles who pasted their command.

## Standing corrections to myself

- ⭐ **m-47 applies to RETRACTIONS.** On 08-09 I retracted a *correct* 5-day estimate using a 6-hour lull against a 24-hour rate — **because retracting felt rigorous.** Third instance of my own entry, and the purest: a wrong retraction of a right claim.
- **Name the timescale with every rate** — `0.25/h`, `0.00/h` and `3/day` were all true of one file on one day.
- **A correction that stops at the mailbox has not happened.**
