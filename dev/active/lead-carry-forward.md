# Lead Dev carry-forward (ephemeral session state — read at fire-time, not frozen in the prompt)

**Updated**: 2026-06-17 ~09:10 PT. **F2 #1171**: **22/22 pages on shell** (home #1266 migrated); **footer REMOVED** (PM: "apps aren't documents"); **#1265 skip-link** shell-owned + #main-content target (done, on main, fixed a latent missing-target bug); **#1264 nav-tok** color/type/radius banked, spacing+4-colors+shadow+11px/3px+extract = CXO-palette-gated (memo in cxo/inbox). **PM UAT round 2 (6/17 AM)**: footer-anchor ✓, projects ✓ (dev-repaired), **disconnect STILL no-op → almost certainly browser-cache** (fix verified correct+live server-side; PM to hard-refresh `/settings/calendar` + grab console if persists). **#1266 home = MIGRATED** (`f3b559320`, F2 **22/22**, 842 tests; server restarted PID 63579 for the `hide_floating_widget` route) — **NEEDS PM UAT**: home's two-column .app-layout now in the shell's centered 1200 main → full-width-vs-centered design choice + live chat/history/Radar behavior; revertible. **#1267 projects-table 500** (Beta-blocker) = model↔migration `owner_id` DRIFT + create_all-vs-migrations → **ROUTED to Architect** (memo `mailboxes/arch/inbox/…1267-projects-table-drift-strategy`; dev unblocked via create_all repair; don't write a unilateral migration — it'd bake in a drift resolution). **Filed 6/17**: #1267, #1268 (nav coverage /lists), #1269 (standup legacy output), #1270 (documents/files object-model → CXO+PPM memo). **PM away for UAT**; pending-PM: disconnect-hard-refresh · home-UAT (when back) · #1267 Architect strategy. Clean meantime candidates if continuing: #1234 (reference_resolver bugs), briefing-refresh-if-stale. ⚠️ `| tail` masked a pytest fail in a commit-chain today (broken push, fixed) — never gate a commit on `pytest | tail`; use PIPESTATUS. Prior 6/16: #1252-P2 + #1238 shipped+closed.
**Session**: Opus 4.8, ephemeral worktree `interesting-beaver-7ee19c`, branch `claude/interesting-beaver-7ee19c`
**Cron**: SUSPENDED (CronList = no jobs; deleted when PM said "proceed with unblocked work"). Canonical expr to re-arm at idle/STOP: `17 22,7,10,13,16,19 * * *`. Cron = idle-wakeup, NOT a work-clock (PM 6/15) — stay suspended while actively building #1238; re-arm only at genuine idle / day-close STOP.

## ▶▶ RESUME ANCHOR (6/16 ~20:30 — READ THIS FIRST post-compaction) ◀◀

**WHERE WE ARE — #1252 P2 + #1238 BOTH SHIPPED this session (post-compaction)**:
- **#1252 P2 doc-store anchoring DONE + on main** (commit chain → `a5b227f0b`): `documents` table (owner_id FK→users.id + `is_global_pm_domain` D1 marker on the DB row per Arch) + ingest-anchoring (`DocumentService._ingest_and_anchor`; **fixed the broken CLI `add`** str→UploadFile via `ingest_path`) + backfill (1 doc `pdf_88388894`→PM `a25db09c`) + the **(c,3) leak CLOSE** (3 reads owner-scoped via `get_readable_base_ids`; callers threaded). 22 tests. Migration `a1238documents` (additive, reversible, applied on dev PG).
- **#1238 (REAL = RADAR-DOC-SOURCE / DocumentEntitySource) DONE + on main** (`8ba37c5de`): `DocumentService.list_for_user` + `DocumentRepository.list_for_owner` (owner-scoped — personal radar, NOT global) + `DocumentEntitySource` + `_build_feed` registration + **per-source isolation** in `RadarFeed.assemble`. 68 tests. **OPEN for PM UAT** (live Document cards ride #1236 `?radar=1`).
- **⚠️ CONFLATION found + surfaced to PM**: the cohort/Arch shorthand "#1238 doc-store" ≠ #1238's actual body (which is the Radar Document source). My anchoring work = the **#1252-P2 prerequisite** that #1238's own STOP-condition predicted; it unblocked #1238, and I built both. Lesson: read the WHOLE issue body at Phase 0, not the relayed framing.
- **#1238 UAT PASSED + CLOSED** (2026-06-16, `completed`): PM verified the Document card renders in Radar at `/?radar=1`. **Login gotcha (→ #1261)**: web login wants the **username `xian`**, NOT the email `xian@pobox.com` (+ no password recovery); resolve_pm_owner_id's username='xian'→a25db09c was correct/accessible. Server restarted fresh PID 76538 (Monday-stale → tonight's code).
- **Follow-ups filed this session**: **#1260** (D7 PM-identity config, Arch-greenlit) · **#1261** (M5: password recovery + login-identifier clarity for beta) · **#1262** (D1: nav label "History" opens "Radar" panel) · **#1263** (D1: left conversation-list rail not re-skinned to Part-B). #1257 (P7 cutover). 
- **Owed/next**: Remaining #1237 entity-sources: WorkItem #1239 (gated #1233), People #1240 (gated PPM model). **F2 cohort migration (~21 pages) WAITS on CXO cohort-confirm** (memo `d281f2c1c`, 4 decisions). **#1164** fully specced (CXO+Arch settled) — build after #1257 P7 cutover (Arch-sequenced; `is_private` marker = same pattern as is_global_pm_domain). **PM pending Q**: file-vs-CXO-sync for #1262/#1263 → resolved by filing (no-shyness default; correctable).
- **F2 #1171 page-shell — P1+P2 SHIPPED this session** (`1e141b970`, CXO spec `dev/active/design-floor-F2-page-shell-spec-2026-06-16.md` is binding): `layouts/app_shell.html` + `web/static/css/app-shell.css` (chrome-not-page-overridable shell, token-only, 5 real-render tests) + `insights.html` migrated (proof-of-pattern + **#1251 item-3** "Correct this"). **793 template tests green.** **Phase-0 found 27 standalone pages (not ~6)** → 4 cohorting/scope decisions surfaced to **CXO (cc PM, memo `d281f2c1c`)**: (1) cohort confirm (~21 migrate / ~5 login·setup·404·500·network-error stay standalone), (2) item-2 242-line insights-CSS scope (rec: separate), (3) nav-component ~500-line inline-CSS scope (rec: separate), (4) aside default-off v1 OK. **CXO confirmed all 4 (6/17) → cohort migration DONE: 21 of 22 app pages on the shell** (piloted subagent fan-out: 1 pilot + 4 parallel agents, central-verified, 839 template tests green); **home deferred → #1266** (special inline-chat/Radar page); standalone-5 stay by-design. **app_shell chrome-completed FIRST** (`5a8caf385`: chat.js/marked/permissions + window.currentUser — else 21 widgets/user-menus break). Fixed a pre-existing `tabs.html` recursion bug (self-include in HTML-comment doc → crashed project_detail). **F2 = structurally-done / token-cleanup-pending**: remaining-to-fully-close = **#1264** (nav-component ~500-hex tokenization, CXO says required-for token-only) + **#1265** (skip-link a11y under shell) + **#1266** (home) + item-2 per-page CSS. **PM UAT (authed browser)**: work_items "+Add" dialog, settings disconnect (Dialog.confirm), lists perms, documents trust-gate, standup generate. Gameplan/audit: `dev/2026/06/16/1171-F2-*.md`. **#1251**: item-1 (nav) retired-into-shell; item-3 (Correct this) done; **item-2 deferred** (folds into per-page CSS tokenization). #1048 keep-generic: CXO+PPM concurred (ready to close — CXO/PM's call).
- _(The detailed #1238 EXECUTION PLAN that followed here is now COMPLETED — trimmed.)_

**SHIPPED THIS SESSION (all on origin/main)**: P7 ADDITIVE step (owner_id UUID col + backfill on insights/conversations/conversational_memory_entries/standup_conversations — migrations a1252insownerid/a1252convowner/a1252memstandupowner) · D5-degradation 16→0 + P6 lint · #1248 jest-CI CLOSED · #1251 item-1 (/insights global nav) · #1227 Slack mrkdwn (impl-complete, awaiting PM real-Slack UAT) · #1257 filed (P7 cutover, deferred per PM) · memory repo cut-over+graceful.

**#1238 / #1252-P2 PLAN — ✅ COMPLETED this session** (see RESUME ANCHOR above). The Arch-ruling plan (new `documents` table + owner_id=PM + is_global_pm_domain marker on the DB row + ingest-anchor [+broken-CLI fix] + backfill + the 3 reads owner-scoped with cross-owner test) is all done + on main; #1238 DocumentEntitySource built on top. The classifier caller was a false-positive (real callers: document_handlers/morning_standup/CLI). Gameplan+audit at `dev/2026/06/16/1238-*.md`.

**THEN (also now-unblocked, after #1238)**:
- **P8 D1 marker** (Arch: marker COLUMN `is_global_pm_domain` Boolean nullable=False default=False) on D1-exemption tables (PM-domain cluster + doc-store) + D5-guard composes on it ("read applies principal OR row.is_global_pm_domain OR routes through explicit unauthenticated handler").
- **#1257 P7 cutover** (rulings in): conversations-orphans = **DELETE the 83 pre-FK** (`DELETE FROM conversations WHERE NOT EXISTS (SELECT 1 FROM users WHERE id::text=conversations.user_id)`; back up first; loop Arch if count≠~83); mandatory-principal = keep Optional + the guard refinement (no code change; D5-degradation-0 was load-bearing); the ~18-test-file UUID-identifier churn remains the meaty part.
- **F2 #1171** page-shell: CXO will spec on my "go" (sent) — server-side template-include + per-page content block; only insights.html extends base today; migrate ~6 standalone pages onto the shell.

**Owed/awaiting**: CXO reply SENT (pending-CXO list: F2 spec, #1251 items 2+3, #1164 semantic, #1249≈#1255 dedup, #1048 keep-generic). UAT-pending (PM): #1250 learning-toggle, #1236 Radar `/?radar=1`, #1227 Slack round-trip. Future follow-up candidate (Arch): `SYSTEM_USER_ID` constant for internal calls.

---
## LATEST STATE (older snapshot 6/16 07:55 — superseded by the RESUME ANCHOR above) — what a re-rouse needs
**Server**: up (health 200), serving fresh `dialog.js` (verified — `Dialog.open` present). Static + templates serve fresh from the worktree cwd; **F1 added NO Python routes** → no restart needed for F1 UAT. (Last route restart was #1184's PATCH, PID per prior note.)

## LATEST STATE (updated 6/16 07:55 — refactor EXECUTING; 9 increments done [artifacts D3, #1250, conversations a,3, insights a,3, learning.py 7-route D4, #1248 jest CI CLOSED, P6 D5 AST-guard, D5-degradation 16→0]; #1238 gated on Arch) — what a re-rouse needs
- **CRON DISCIPLINE (PM-corrected 6/15 ~19:52)**: cron is **idle-wakeup, NOT a work-clock** — SUSPEND it (CronDelete) while actively working; re-arm ONLY when going idle with nothing in flight. Don't race it / don't wrap-because-cron. (I'd drifted to keep-armed-while-working via the skill's Rule-2; memory `feedback_cron_off_when_engaged_on_when_idle` reinforced + CIO flagged to reconcile the skill.)
- **F3 #1172 COMPLETE + CLOSED** — token-lint baseline **ZERO**, CI gate live; all 70→0 migrated. The final 19: 3 purples replaced w/ palette (`--color-primary`/`-dark`, PM-approved); 16 em/rem fonts (4 chat markdown → allow-comment; 12 UI-chrome → type-scale tokens). Closed-properly (description+evidence+`completed`). px-vs-rem = CXO's separate Standard-1 a11y item (token refs auto-adopt a rem switch).
- **F1 #1170 (Dialog primitive) COMPLETE + CLOSED** — PM UAT PASSED 6/15 (styled dialogs confirmed across insights/conversation-delete/learning-toggle-confirm); closed-properly (status banner + delivered checklist + evidence + `completed`). 26 jest + native-dialog gate 0 + 784 template green.
- **MAIN FULLY GREEN** — both prior red tests fixed: trustStage (repointed to base.html, committed) + nav `test_documents_and_files_both_present` (PM confirmed keep both Files+Documents; #1146 wired /files distinct). **784 template tests pass.**
- **Attribution lesson re-pinned** (`feedback_verify_lane_before_attributing_not_web`): the 2 red tests were NOT Web (product repo, Lead/CXO lane — nav one was my own #1146). I'd misattributed to Web (repeat of 6/14); PM caught it. Git author = shared `mediajunkie` identity → use commit-msg + session-log to ID the agent, never assume.
- **ADR-071 RATIFIED** → unblocks the consolidating refactor (D2 user_id→owner_id, doc-store #1238 first, m-40 shim) + Radar WorkItem render-guard #1239.
- **Filed**: #1248 (jest harness CI — PM said wire it after fixing its 6 form-validation failures; I'll take it) · #1249 (inline-edit D2 primitive) · #1246 (#1184 pdf/docx Fast-Follow).
- **PM F1 UAT round (6/15 ~18:30)**: F1 PASSED. 2 bugs found, both pre-existing/separate, filed: **#1250** (learning-toggle 500 — hardcoded `TEST_USER_ID` not in `users` → FK violation; toggle NEVER worked; textbook ADR-071 instance) + **#1251** (/insights UI polish: no-page-nav→F2, design-drift→CXO, "Correct" wording→CXO).
- **CONSOLIDATING REFACTOR — KICKED OFF gameplan-first (6/15 ~19:15); both audit-cascade gates PASSED:**
  - **Umbrella issue #1252** created + ISSUE-gate audited vs feature.md (2 gaps fixed: Example-UX + per-phase deliverables). `dev/2026/06/15/1252-issue-audit.md`.
  - **Gameplan #1252** written + GAMEPLAN-gate audited vs gameplan-template (2 gaps fixed: Phase 0.8 post-completion-integration + multi-agent/integration-test discipline). `dev/2026/06/15/1252-gameplan.md` + `1252-gameplan-audit.md`. All on origin/main.
  - **Scope (ADR-071 D2–D6)**: P1 convention+shim · P2 doc-store #1238 · P3 stakeholders · P4 (a,3) leak fixes · P5 D4 threading **+#1250** · P6 D5 AST guards · P7 user_id→owner_id consolidation · P8 D1 exemption marker · **parallel #1248 jest CI**.
  - **EXECUTING. P4 increment-1 DONE (19:21 fire)**: `artifacts.get_by_id` → filter in the SELECT not post-hoc (D3); behavior-preserved; 26 artifacts + 78 repo tests green; on main. **Caller-analysis finding (recorded on #1252): only artifacts was a standalone P4 fix (its 3 callers pass owner_id); the other 3 (a,3) methods (`insights.get_for_object`, `knowledge.get_node_by_id`, `conversations.get_by_id`) have callers WITHOUT the principal → they FOLD INTO P5 (D4 threading), not standalone P4.** Also: deny-on-None over-reach caught by a failing existence-check test → corrected to behavior-preserving; None→deny tightening defers to the D5 guard (P6).
  - **#1250 DONE (21:06 fire, first D4 instance)**: learning settings GET/PUT → `Depends(get_current_user)` + `current_user.user_id` (real users.id), not hardcoded TEST_USER_ID. Toggle now persists (FK valid). **Repaired the silently-FK-failing learning integration suite** (fixture seeds the test principal): 9 green (were red). On main. **Ready for PM re-UAT** (#1250 open till confirmed / P5 close). **P1-foundation finding established**: canonical principal = `JWTClaims.user_id` (UUID = users.id FK target); `sub` = its string form; the anchoring "inconsistency" is string-vs-UUID of the SAME identity. The fix pattern (proven by #1250): route takes `current_user: JWTClaims = Depends(get_current_user)`, uses `current_user.user_id`.
  - **conversations.get_by_id (a,3) DONE (21:30)**: scopes in the SELECT; 5 routes thread `current_user.sub`; m-40 warn-shim; 2 cross-owner scoping tests (in-memory SQLite) + route suite green (14). On main. The audit's verified (a,3) #1 closed. **Pattern for (a,3) fixes proven** (artifacts + conversations): add optional principal param → scope-when-provided + warn-when-None (m-40) → thread the callers that have the principal → DB-backed cross-owner test.
  - **insights.get_for_object (a,3) DONE (6/16 06:17, increment-4)**: the fetch-by-object cross-owner leak. m-40: repo (`repositories.py:2333`) scopes by `user_id` when provided + WARN-shim when None; journal wrapper (`composting_pipeline.py:297`) threads through; fake (`_fake_insight_journal.py:131`) mirrors; the one real caller (`dev_composting.py:192`, DEV-only readback) threaded with its seeding `user_id`. TDD: 2 cross-owner scoping tests (`test_insight_repository_1035.py`) red→green; **82 passed** (repo + composting pipeline/scheduler/seed) — no regressions. On main.
  - **#1238 (doc-store, P2) — Phase-0 done, GATED on Arch (6/16)**: ingest is **CLI-only** (`cli/commands/documents.py:95`→`ingestion.py:ingest_pdf`→`collection.add`), NO web upload route, NO per-user principal at write; 3 reads (`find_decisions`/`get_relevant_context`/`suggest_documents`) unscoped; callers = document_handlers/classifier/morning_standup. **Ingest-anchoring fork needs Arch** (CLI docs have no user principal: configured-PM-owner [lean #1] vs `is_global_pm_domain` vs CLI `--owner`). Backfill policy: PM said existing rows → **assign to PM**. Posted Phase-0 to #1238 (comment 4719098084). **→ looped Arch (mail); resume #1238 fix once Arch rules.**
  - **knowledge.get_node_by_id (a,3) — INVESTIGATED 6/16, NO FIX NEEDED**: the repo (`repositories.py:913`) ALREADY has the m-40 scope-when-provided shim (`if owner_id: filters.append(...owner_id==...)`); routes (`knowledge_graph.py:135/215/216`) pass `current_user.sub`; service callers (`knowledge_graph_service.py:287/338/376/379`) pass `owner_id if owner_id and not is_admin else None` — the None-path is a **deliberate SEC-RBAC admin-bypass, NOT an un-threaded caller**. An insights-style blanket WARN would MIS-FIRE on every legit admin op → correctly did NOT add it. Only unscoped call is `semantic_indexing_service.py:415` (`index_subgraph`) which is **dead code (zero callers)** — latent, deferred to the D5 guard (P6) to flag if/when wired. Net: get_node_by_id is already anchored; making admin-bypass-None *explicit* (vs accidental-None) is a P6/D5 design concern, not a now-fix.
  - **learning.py pattern routes (D4, #1250 sibling) — SHIPPED 6/16 (increment-5)**: 7 ACTIVE routes hardcoded `TEST_USER_ID` (list_patterns/get_pattern reads + delete/enable/disable/execute/feedback MUTATING — any authed user could delete/execute the shared principal's patterns = cross-user read+WRITE leak). Threaded `current_user: JWTClaims = Depends(get_current_user)` + `current_user.user_id` into all 7; **removed the TEST_USER_ID constant entirely**. TDD: new `TestPatternRouteUserIsolation1252` (read-scoped + cross-user-mutate-denied at SQL+DB level); both integration tests' direct calls updated with `_TEST_CLAIMS` stand-ins. 21/22 green. **Pre-existing failure found (NOT mine, documented on #1224 as 4th cluster)**: `test_complete_learning_cycle` line 132 = `capture_action` dedup in `learning_handler.py` (unrelated; fails in isolation, before my edits run).
  - **#1248 jest CI — DONE + CLOSED 6/16 (increment-6)**: 6 form-validation failures were stale #643 assertions (validator messages reworded "needs"-based; tests not updated) → fixed, 94/94 jest green. Added `.github/workflows/frontend-tests.yml` (path-filtered web/static/js + tests/frontend, setup-node@20, `npm ci && npm test`) — first JS/jest CI job; **CI run 27621834772 = success** on the wiring push. package-lock was already tracked (#1170). Closed-properly (banner + checked boxes + evidence + CI-run link).
  - **P6 D5 AST guard — SHIPPED 6/16 (increment-7)**: grounded the ADR's "40+ sites" → actually **16** (the `context.get("user_id") if … else None` degradation). Built `scripts/principal_threading_lint.py` (AST-based; ratchet mirrors token-lint/native-dialog-lint), 10 TDD tests green, baseline `.principal-threading-baseline.txt` (16), CI gate in `lint.yml`. Verified baseline→0 / new-site→fail. Prevents recurrence (answers #1241). The 16 ratchet to zero as D4 threading lands.
  - **D4 D5-degradation surface — FULLY ELIMINATED 6/16 (increments 8+9, 16→0)**: all 16 `…get("user_id") if … else None` sites removed. intent_service.py ×8 → one sanctioned accessor `_principal_from_intent(intent)` (TDD equivalence); the other 8 (classifier/conversation_handler/canonical_handlers/intent_types/simple_response_handler, 4 packages) → the behaviour-preserving inline idiom `(X or {}).get("user_id")` (existing codebase idiom; avoided cross-package import-cycle risk). **D5 guard baseline now empty = zero-tolerance.** 1892+1713 unit tests green, no regression. The silent-degradation recurrence risk (#1241) is gone. **Deeper "required-principal threaded end-to-end" enforcement remains the D4 follow-on** (substantial; wants the `process_intent`-caller-boundary analysis + Arch pattern-align) — but it's now isolated/clean, not scattered.
  - **P3 stakeholders — DORMANT, DEFERRED (6/16)**: `Stakeholder` model has zero reads/writes → no (c,3) leak; preemptive schema work, low-value now. Do when stakeholders are used, or fold into P7.
  - **P7 (user_id→owner_id, D2) — SCOPED + GAMEPLANNED (6/16)**: gameplan `dev/2026/06/16/1252-P7-owner-id-consolidation-gameplan.md`. Targets: insights.user_id (String), conversations.user_id (String), artifacts.owner_id (String) → owner_id UUID FK; (verify feedback/memory/standup). **Backfill is a clean CAST — 0 non-UUID values in alpha DB.** Per-table Alembic m-40 (add-FK→backfill→shim→not-null→drop). **Execution wants Arch sequencing sign-off** (orphan-owner disposition ties to #1238 fork) — UNLESS FK-validity check shows no orphans (then fully unblocked). Recommended order: artifacts (1 row) → insights (33) → conversations (490).
  - **deeper-D4 required-principal — RESOLVED 6/16 (verified, no code work)**: `process_intent` user_id is **optional by design** (unauthenticated path supported); **all 4 boundary callers thread it** (web route + 3 Slack). Practical D4 (kill silent degradation + thread-from-boundary) is COMPLETE. "Make mandatory" would break unauthenticated → Arch/product interpretation question, not refactor. No further code work.
  - **P7 — PM chose (2) non-breaking-additive-first (6/16); EXECUTING m-40 layer-step.** Scope-surprise (STOP-#1): converting String→UUID breaks ~18 test files (non-UUID identifiers `"alpha"`/`"user-A"`; CrossDialectUUID ValueErrors on load) → the breaking reader-migration/drop is a deliberate follow-on (gameplan ⚠️SCOPE-DISCOVERY). Order (PM's trust): insights→conversations; artifacts deferred (already owner_id-String).
    - ✅ **insights additive SHIPPED**: `owner_id` CrossDialectUUID nullable+indexed added alongside `user_id` (FK-less); Alembic `a1252insownerid` (add+backfill `user_id::uuid`); dev DB verified filled=33/null=0/reversible; **1238 tests green** (non-breaking).
    - ✅ **conversations additive SHIPPED**: `owner_id` … Alembic `a1252convowner`; dev DB filled=490/null=0/reversible; 118 db/conversation tests green.
    - ✅ **memory + standup additive SHIPPED (batched)**: `a1252memstandupowner` adds owner_id to `conversational_memory_entries` (0 rows) + `standup_conversations` (36, 0 non-UUID); dev DB verified filled=36/0 + 0/0, reversible, alembic at head. **feedback** = already `postgresql.UUID`+FK (naming-only, NOT additive); **artifacts** = already owner_id-String (breaking type-change). Both → breaking/naming pass.
    - ✅ **P7 ADDITIVE STEP COMPLETE** for all String-user_id P7 tables (insights, conversations, memory, standup). Migration chain head = `a1252memstandupowner`.
  - **BREAKING PASS — IN PROGRESS (PM "go!" 6/16 ~10:33; un-deferred).** Table-by-table, green at each step: dual-write `owner_id=UUID(user_id)` + migrate repo readers user_id→owner_id + fix tests (non-UUID→real UUIDs) → verify → commit. `user_id` columns dropped LAST (final pass). Order: memory→standup→insights→conversations→feedback(rename)→artifacts(type-change).
    - ✅ **memory increment-1 DONE**: repo migrated (save_entry dual-writes owner_id; get/delete scope owner_id); added `test_conversational_memory_repository_1252.py`. **Fixed a HANG**: `ConversationalMemoryEntryDB.entities_mentioned` was raw `postgresql.JSONB` → SQLite table-create CompileError → aiosqlite thread hang. Fixed → `.with_variant(JSON(),"sqlite")` (#1180 pattern; Postgres unchanged → no migration). **214 memory tests pass in 3s.** Lesson: `| tail` hides hangs (buffers to EOF); use faulthandler + unbuffered.
    - ✅ **FORK RESOLVED (PM 6/16): Option B — bank the additive step, DEFER the cutover.** Cutover durably tracked in **#1257** ("[REFACTOR] P7 cutover…") — PM condition "get to it eventually" satisfied by the tracked home (per [[feedback_minimal_deliverable_needs_fleshing_out_plan]]). **P7's deliverable = the additive step (owner_id added+backfilled+canonical on all 4 tables, on main, non-breaking).** memory = 1-of-6 already cut over, now made **graceful** (non-UUID principal → empty/None, not fail-loud; 184 memory tests green) so it's safe sitting ahead. #1257 covers the remaining cutover (insights/conversations/standup/feedback-rename/artifacts-type-change + memory-finish) with the prereqs (prod non-UUID decision, test-suite UUID-identifier convention, conversations-orphan-FK).
    - ⏸ **(historical) standup (increment-2) ATTEMPTED → REVERTED; STRATEGIC FORK surfaced to PM.** The reader/writer cutover (`UUID(user_id)`) broke a CONSUMER test (`test_conversation_state::test_create_persists_and_returns`, `create_conversation(user_id="user1")` → `UUID()` raises). **Finding: non-UUID identifiers are pervasive in the consumer/integration test layer (not just repo tests), and fail-loud `UUID()` is a real prod non-UUID-principal behavior change.** Cutover ripples far beyond per-table repo tests. Reverted standup to green (additive owner_id stays). **THE FORK**: (A) full cutover now = multi-session UUID-identifier grind across ~dozens of consumer/integration test files + a prod non-UUID-principal decision; **(B) RECOMMENDED**: keep the additive step (owner_id populated+canonical on all 4 tables, DONE, non-breaking) as P7's deliverable + DEFER the reader-cutover/user_id-drop to a dedicated effort (needs a test-suite-wide UUID-identifier convention + the prod non-UUID decision first). **memory increment-1 (committed `0d4bc9f8a`) is the one table already cut over** (works/tested but has the same latent fail-loud; revisit under the chosen strategy — keep+graceful, or revert-to-dual-write for uniformity). Also note: `test_conversation_state` has pre-existing #1224 failures (orthogonal).
  - **Arch-gated**: #1238/P2 doc-store · P8 D1 exemption marker · conversations-orphan-disposition · mandatory-vs-optional-principal interpretation. The clean code-level refactor arc is exhausted (verified).
  - **CRON**: re-armed when pausing here for PM/Arch steer (idle-wakeup; suspend again when active work resumes).
  - **PM-gated, pending**: **#1250 re-UAT** (settings learning-toggle fix shipped 6/15; PM confirms it works → close / P5). **#1238 disposition** (Arch ruling). **#1253 cohort CI-drift** (PM/CIO/Arch coordination — see below).
  - **DISCOVERED 6/16 — #1253 (filed, technical-debt)**: the Code Quality `ruff format --check .` gate is **red cohort-wide — 207 files would reformat** under the pinned CI ruff 0.6.9 (e.g. `web/startup.py`, pre-existing/other-agent). The gate's been pushed-past (protects nothing while red — same shape as the ungated jest harness #1248 just fixed). Likely cause: agents' local ruff newer than the 0.6.9 pin. **NOT a solo fix** (207-file reformat = cohort-coordination + version reconciliation + pre-commit hook). My new P6 file was kept ruff-clean; pre-existing-drifted files I edited (repositories.py, dev_composting.py) left for the cohort pass. **My lesson**: run `ruff format` pre-commit (CLAUDE.md only names fix-newlines.sh) + check CI after first push.
- Also queued (lower): #1249 inline-edit D2 primitive; F2 #1171 sketch + CXO align (CXO leans server-side include/block); #1246 (#1184 pdf/docx Fast-Follow).

## Constraints (durable)
- **Alpha-account data is NOT precious (PM 6/16)**: "no real or precious data in any alpha account I've used; even if some is real it was only ever test/pre-release." → The ADR-071 anchoring refactor (#1252/#1241) is **pre-release correctness + multi-tenancy-prep, NOT live-leak-plugging**. **STOP-condition "user data at risk" is cleared for alpha/test data** — data-migration steps (P7 owner_id consolidation, #1238 backfill) proceed WITHOUT precious-data caution. Drop "leak/emergency" framing; these are correctness gaps in test data. (Recorded on #1241 for Arch's remediation-urgency framing.)
- **Project-board changes: Lead may apply them WHEN PM AUTHORIZES (per-instance); default is READ + PROPOSE.** PM 6/14 refinement: "you can do these things when I authorize." Propose by default; on explicit PM go-ahead for a specific board op, do it. Path to *standing* delegation: **document conventions → learn → skillify** (conventions now documented at `docs/internal/planning/sprint-board-structure.md`); until skillified, board ops are per-authorization, not autonomous.
- Board reads: pull the FULL set (count==limit ⇒ truncated; project has **1061** items as of 6/14 — verified-good, not truncated), exact `.milestone.title` match, `grep -xF` (not `comm`) for issue-number set-ops.
- **WRITE TO WORKTREE PATHS** (`…/.claude/worktrees/interesting-beaver-7ee19c/…`), never bare main paths — Fire 13 lost 3 edits to the main checkout via bare paths (the shared main tree also actively reverts files). One-glance check: does the path contain `/.claude/worktrees/`?
- **Process-rigor calibration (PM 6/14): "quick wins ok but flywheel for everything else."** Small discrete fixes (D1 quick wins #1225/#1228, #1227, isolated bugs) proceed DIRECTLY — implement + test (real render, not curl-200) + → Review; NO audit-cascade/gameplan ceremony. **Everything substantive (RECONNECT WS builds, M4, etc.) gets the full excellence flywheel**: audit-cascade at Issue→Gameplan→Prompts→Execute + close-issue-properly. (MEMORY.md over size limit → not pinned there; lives here + session log.)

## Milestones / sprints / releases — CANONICAL: `docs/internal/planning/sprint-board-structure.md`
**REFER TO THAT DOC** (PM-authored 6/14). PM flagged 6/15 that I keep getting this wrong — read it until internalized. The model:
- **Milestone = a RELEASE.** Open: **MVP = 0.9 beta (CURRENT, due Jul 4 2026)** → **Production = 1.0 (Aug 1)** → **Fast Follow = 1.0.1 (Sep 4)** → Post-MVP (Dec) → Enterprise (theoretical 2027). Closed: Foundation, GREAT, Alpha (0.8).
- **Sprints = tracks WITHIN a milestone** (a superepic/track, not a sub-release, not a calendar week). The MVP/0.9-beta milestone's sprints: M0/M1/M2/M3 (done) + remaining **M4 (Trust+Learning) · RECONNECT (connector) · D1 (Beta design quality) · M5 (Distribution+Polish)**.
- **⚠️ MY RECURRING ERROR (corrected 6/15)**: beta 0.9 = the MVP milestone, which **releases AFTER all its sprints finish (~Jul 4)**. So **M4 is a sprint INSIDE beta-0.9, NOT "after beta."** D1 (Radar surface) + M4 (entity-model) are SIBLING MVP sprints — both ship in 0.9 beta; **no critical-path conflict**. Trust/entity-model work can be M3 or M4 (our choice) as long as it's done before the MVP release. Don't treat sprints as releases or M4 as post-beta.
- Some MVP-tagged work (#358 encryption, connector full-migration) may move to Production.

## CURRENT STATE: M3 DONE; sprint order = D1 → RECONNECT → M4 → M5 (PM-agreed 6/14)
M3 closed (PM declared). Connector decision RATIFIED: **MCP, not native** (scope doc §0). Sprint plan & order all PM-agreed 6/14. We do **D1 now** (unblocked) while Arch designs the RECONNECT ADR (gates the connector build + M4's identity-dependent items).

### RECONNECT (Connector Refactor) — 12 issues FILED + prefixed (PM-authorized 6/14)
- **All 9 workstreams covered**, prefixed `RECONNECT-WS{n}:` (MVP / Product Backlog): WS-1 #1226+#1199 · WS-2 **#1229** · WS-3 **#1230** · WS-4 **#1231** · WS-5 **#1232** (ADR output) · WS-6 #1201 · WS-7 #1109+#1110 · WS-8 #1220 · WS-9 **#1233** · discrete #1227 (quick win).
- New #1229–1233 created + board-placed (Sprint=RECONNECT, Status=Product Backlog); existing 7 renamed. §10 of scope doc updated to FILED.
- **Arch is working the ADR** (PM 6/14) → refines WS-2/WS-5/WS-1 scope (how much auth/config moves to MCP). Issue bodies are tracking targets, not frozen specs.
- **#1227** = the one ADR-independent quick win.
- **Claim-grounding pass (6/14, 5 agents)**: issues well-grounded, nothing fabricated; corrections applied (scope §2a/§2c + comments #1226/#1229/#1230). **Key gap → Arch**: cite **ADR-058** (multi-tenancy — RECONNECT partly *finishes* it, esp WS-2/7/9) + reconcile **ADR-052** (tool-based vs external-MCP-server) in the WS-5 ADR — flagged on #1232 + scope §11. §0 MCP decision now in `decisions.log`. New bug **#1235** (`/turns` display returns oldest-50, no offset).
- **`/audit-cascade` skill (Pattern-049, ISSUE gate, 6/14)**: the REAL template-conformance gate (distinct from the grounding pass above). All 12 RECONNECT issues → full `feature.md` conformance (**16/16**, verified; PM bar = full-now via 5-agent fan-out). Matrix: `dev/2026/06/14/RECONNECT-issue-phase-audit.md`. Issue gate done → next cascade gates (Gameplan→Prompts→Execute) run **per-WS post-ADR**. LESSON: when PM names a skill, invoke it (don't improvise a same-named pass).

### D1 (Beta design quality) — 10 issues, PROPOSED build order (awaiting PM bless to make durable / board-reflect)
- **Track A — quick wins (D1): DONE on my side.** ✅ **#1223 CLOSED** (close-issue-properly, PM-approved; RESOLVED banner + evidence in description). ✅ **#1228 → Review** (Slack `d1cd99ca6` + web-half: pre-existing "Thinking..." indicator ANIMATED `9ae3f03bd` — already there, completed-in-place, not rebuilt). ✅ **#1218 CLOSED** (cannot-reproduce). **#1174 → M4** (left D1). **#1225** → likely folds into CXO's home-module redesign (PM to confirm; not a standalone build). #1227 = RECONNECT flywheel (not a quick win). [Web = website lane; product front-end = Lead+CXO — lesson logged in Notes.]
  - Discovered + filed **#1234** (2 PRE-EXISTING reference_resolver failures: `_find_candidates` window bug [#1223-adjacent] + definite-ref 66.67% accuracy) — un-sprinted, PM triage.
- **Track B1 — #1090 Radar/entities slot-swap (CXO HANDED OFF 6/14 — BUILDING via full flywheel).** Mockup = binding spec (`dev/active/radar-entities-surfacing-mockup-2026-06-14.html`); PM-ratified + loved; #1090 now in **D1**. **#1090 is a scoping TRACKER** ("UI work lands as separate tickets") → flywheel issue-gate = **carve a focused implementation issue** (child of #1090) — **awaiting PM nod** (board op). Build: slot-swap into `templates/components/history_sidebar.html` (739L); Part-B Card (`cards.css`/`insight_card.html`); attention-first; entity-search-subsumes-chat; two states (real-only / empty+1 example); honest provenance (#1214/#1216). **PPM dep**: entity-catalog backend DOESN'T EXIST (only MUX design catalogs). ⚠️ **#706 ("MUX-OBJECTS-VIEWS Discovery Epic") is CLOSED — it was the DESIGN discovery, NOT the backend impl** → the impl of WorkItem/Person/Document as queryable `EntitySource`s has **NO clean open tracking issue** (minimal-now-→-stub-forever risk; surfaced to PM Fire 28). **TODO: confirm w/ PPM whether the entity-model impl is tracked their lane; if not, PROPOSE carving a follow-up (child of #1090, like #1236) — PM-gated board op.** v1 = build surface + wire conversations (#1021 `user_history.py`); slot the other 3 at the `EntitySource` seam as PPM lands the model. **Closure gate (CXO)**: AC must cite PDR-002 Layer-2 + the mockup. Flywheel: carve issue → gameplan+audit (DDD) → prompts+audit (if fan-out) → TDD → close-properly. **Progress**: ✅ **#1236 carved** (issue gate; D1/In-Progress/child-of-#1090); ✅ **gameplan written + self-audited** (`dev/2026/06/14/1236-radar-gameplan.md`; DDD = RadarEntity/EntitySource/RadarFeed). ✅ **Phase 0.5 contract read** (sidebar = client-JS-fetch → Radar render = JS + server endpoint; #1021 lacks lifecycle/open-questions → v1 derives lifecycle from recency + honest meta). ✅ **Phase 1 domain DONE** (TDD 8/8 green): `services/radar/` (models/sources/feed — RadarEntity/EntitySource/ConversationEntitySource/RadarFeed). ✅ **Phase 2 BACKEND done** (`web/api/routes/radar.py` GET `/api/v1/radar` → ConversationEntitySource→RadarFeed, registered in app.py, **TDD 11 green**). Render = **FRAME-AGNOSTIC component** (F2 #1171 re-homes it — CXO coherence) + **fold #1164** (privacy toggle preserved in the footer). ✅ **Phase 2 FRONTEND DONE** (Fire 28, commit `98f460cdc` → on main): `history_sidebar.html` radar-card CSS + `renderRadarCard`(XSS-safe textContent)/`renderRadar`(empty-explainer + attention-first + honest ●/○ provenance) + `updateRadar` namespace export; `home.html` `loadRadar()` behind **`?radar=1` flag (default off → no regression)** + graceful fallback. **Full #1236 suite 84 green** (8 domain + 3 route + 17 frontend + 56 sidebar). **Server restarted → route LIVE → UAT-ready at `http://localhost:8001/?radar=1`.** Live authenticated end-to-end smoke = PM's UAT gate (route auth-gated; all-non-auth-testable verified).
  - **⚠️ SCOPE EXPANDED (PM 6/14 ~21:50): "no partial ship — alpha→beta — ship it all"** = full **4-type Radar (WorkItems/Documents/People/Conversations, PDR-002)** is a **BETA-SHIP REQUIREMENT**, NOT conversations-only-now-+-slot-later. Backend recon: Conversation ✅; **Document** ⚠️ small add (`DocumentService` no per-user list — **Lead-solo**); **WorkItem** ⚠️ needs identity map **#1233**; **People** ❌ no backend = **PPM entity-model lane** (typed relationship-network map, also backs **#1217** ethics floor — per CXO #1217 memo). **PPM memo SENT** (cc CXO, on main `6b8d372a7`): contract + sequencing + umbrella-tracker proposal. **PLAN (awaiting PM steer)**: start Document solo → carve umbrella "Radar: 4 EntitySources" (child #1090, **PM-gated**) → sequence WorkItem(#1233)+People(PPM). The overlook = integration-tracking gap between surface(Lead) + model(PPM/CXO) workstreams; + my slip citing closed epic 706 unverified. **Closure**: #1236 close-properly waits until all 4 surface (or PM re-scopes #1236 = surface-only + the 4 sources = the umbrella).
  - **PLANNED via full flywheel (PM 6/14 "no rush, plan properly, write issues, run audit-cascade")**: 4 issues created — **#1237** umbrella (RADAR-ENTITY-SOURCES, child #1090) + **#1238** Document (Lead, UNBLOCKED) + **#1239** WorkItem (gated #1233) + **#1240** People (gated PPM entity-model). **Audit-cascade ISSUE gate run** (fresh-eyes vs feature.md, all gaps fixed); matrix + the **dependency-completeness miss** (initial #1236 audit checked conformance not referent-soundness; #706 was closed) → `dev/2026/06/14/radar-entity-sources-issue-phase-audit.md`. **Proposed audit-cascade amendment → CIO/skill-owner**: add Referent-Verification/Dependency-Completeness check. **TONIGHT-STARTABLE = #1238 Document only** (gameplan+audit → Phase-0 contract read → TDD); #1239/#1240 gated. **Board**: #1237-1240 → Sprint=**D1** (PM-authorized 6/14, done). Next gates (Gameplan→Prompts→Execute) run per-child when picked up.
  - **#1238 Document → Phase-0 STOP (6/14, NO code): `DocumentService` is a GLOBAL ChromaDB collection, NO user-scoping** (no owner field; ingester doesn't stamp one; no upload caller passes user_id). Can't list "the user's documents"; global-per-user would leak across users (ADR-058). #1238 reclassified BLOCKED on a **document-store user-scoping prerequisite** (data-layer: owner on ingest + upload path + backfill + scope queries — Arch/PPM-flavored, NOT a small wrap). Documented on #1238 + #1237. **→ ALL 3 remaining sources now gated** (Doc=doc-store scoping; WorkItem=#1233; People=PPM model) — nothing in the entity-source workstream cleanly buildable without a prerequisite. **TODO (PM steer)**: carve the doc-store-user-scoping prerequisite issue (offered); resume tomorrow. Vindicates the dependency-completeness lesson (even the "unblocked small" one had a false backend assumption).
  - **#1241 ARCH-AUDIT filed (PM-directed flag)**: "content not anchored to user auth — extent + remediation (multi-tenancy completeness)." Documents = confirmed instance #1; the audit = inventory all content stores + root-cause the RECURRING failure ("not our first attempt" — PM) + propose a canonical anchoring pattern (+ADR). Arch-lens (memo sent cc CIO/PM, on main `21bc3fe32`). #1238 blocks on it. **AUDIT DONE + Arch GREENLIT ADR-071 (6/15)**: D1 ruling (PM-domain global-by-design + explicit-exemption marker + per-user-render-guard [→ #1239 WorkItem needs NO schema change] + tenant_id multi-tenant path); D2=consolidating refactor (`owner_id` FK canonical / `user_id` string deprecated / none forbidden; m-40 shim migration); D4=half the ADR weight (40+ resolution-degradation sites, principal-as-required-param + AST guard). **ADR-071 v0.1 DRAFTED + looped for Arch ratify** (`adr-071-user-auth-anchoring-pattern.md`, on main `06f227b72`; indexed + decisions.log'd; Arch memo `1836e91ad`). **GATED on Arch RATIFICATION** → then the consolidating refactor (doc-store + stakeholders first = the (c,3) privacy gaps; (a,3) leak paths + D4 resolution cleanup follow). **Next unblocked (non-auth-track): #1184 artifact-rename / #1202 files follow-on** (started #1184 scoping). Audit detail: `dev/2026/06/15/1241-content-anchoring-audit.md`. **F3 #1172 remainder** = CXO-gated, now explicitly routed to CXO (memo `1d6c815aa`; var-fallback ruling + ambiguous/no-match colors + off-scale spacing/radius + em/rem fonts).

### D1 design-floor F3 #1172 — IN PROGRESS (the unblocked tonight-work, PM 6/14)
**Built the token-lint gate** (TDD 16 green; `scripts/token_lint.py` + `tests/unit/test_token_lint.py`; commit `9baa53aeb` on main). Catches hex/rgb/hsl colors + spacing px + border-radius + font literals; allow-list (relative units, hairlines, unitless line-height, inline `/* token-lint-allow */`). **Inventory: 70 violations** (color 30 [23 bare + 7 var-fallback], type 25, spacing 12, radius 3). **Radius convergence done** (`--radius-*` dup = 0 uses). **⚠️ CXO Q (route when resuming F3)**: 7 color flags are `var(--token,#hexfallback)` (repo-wide pattern incl. my Radar CSS) — flag or allow? CXO owns the F3 spec. **Remaining**: CXO ruling → migrate 63 clear-drift → wire CI (`lint.yml`, post-migration) → stylelint durable upgrade. Other design-floor: C1 #1173 spec-complete (builds in F2 shell); F1 #1170/F2 #1171 want ~20-min CXO primitives-sync.
  - **UPDATE June 15 (~06:55): F3 mechanism FINISHED** (on main `edfab2d48`). var-fallback→ALLOW (default, CXO confirm); **baseline ratchet → CI gate LIVE** in `lint.yml` (red-on-new; spec's primary Done); 19 tests; **9 exact-match type violations migrated** (same-value, zero visual change; 63→54). The **54 baselined = ~2/3 DESIGN-DECISIONS not mechanical** (off-scale spacing/radius, em/rem fonts, rgba colors → CXO) + ~1/3 clean color exact-matches (next solo batch). #1172 → recommend Review. **CXO gate**: var-fallback ruling + the design-decision migration calls (PM nudged CXO 6/15). **Next solo**: the clean color exact-match migration batch (ratchet the baseline down).
  - **UPDATE June 15 ~11:00**: **#1184 CLOSED PROPERLY** (rename backend+UI + md/txt format; UAT-passed; deferred→**#1246** pdf/docx+save-time-picker Fast Follow; inline-edit UX→CXO; board Status=Done). **F3 #1172 done** (mechanism live; migration tail CXO-gated). **QUEUED (owed, post-D1-breath this week)**: `ROLE-PORTFOLIO-LEAD-DEV.md` (Exec pilot wave; model on `docs/briefing/ROLE-PORTFOLIO-HOST.md`; Section-4 irreducible-mandate = data-safety/security-integrity call).
  - **D1 NEXT (June 15 assessment)**: design-floor **F1 #1170 + F2 #1171 want a ~20-min CXO primitives-sync** (the unblock; C1 #1173 builds-in-F2-shell after F2). **#1164 privacy-toggle** = UI-only stub → backend wiring (the provenance gate: private session → no `observed` entities, per CXO) is a discrete likely-unblocked D1 build. **#1202 files-follow-on**: its "export formats" bullet OVERLAPS #1184(done)/#1246 → reconcile; share/versions/tagging distinct. **#1048** stage-visual = CXO design Q. Entity-sources #1237-1240 gated (ADR-071 ratify + M4 model).
- **Track B2 — design-floor #1169–1173 (separate; STILL awaiting CXO guidance).** Lead builds + CXO guidance-first. NOTE: the 6/14 CXO memo was the **#1090 Radar handoff, NOT** the design-floor specs (Dialog/shell/tokens/chat-page) — those are still pending. Order when unblocked: tokens(#1172a)→Dialog(#1170)→shell(#1171)→chat-page(#1173)→CI-gate(#1172b last).
- **Track C — resolved:** #1218 CLOSED (cannot-repro); #1174 → M4. (Both off the D1 active list.)

## Carried / queued (not immediate)
- **#1216** provenance field (is_seed/source on InsightDB) — handoff memo SENT to PPM (`a9010ef1e`); PM moved to **M4**.
- **#1144** TEST-DISCIPLINE-REFACTOR (real fixtures not MagicMock) — **deferred to PM** (sets a fixture pattern; next flywheel pickup once PM steers).
- **#1224** pre-existing test failures (3 clusters: standup conversation-state, perf-indexes, error-message integration/perf) — filed for triage (env vs real); PM moved to **M5**.
- **#1209** AutonomousExecutor fleshing-out — **M4** (an MVP milestone, NOT Fast Follow — corrected 6/13).
- **#1211** shadowing+broad-except AST sweep (Arch-recommended) — Lead, unscheduled.
- **#1090** #1133→Radar re-scope captured here — **M5** polish.
- **#973** MEM-CACHE-AUDIT — queued (M3 now closed; can pick up when D1 leaves room; acked to PA).
- **ADR-066 D7 consult (Lead Dev owed, FUTURE)** — Arch's ADR-066 v0.2 (Config Ownership: config/creds **server-owned**; host per-request ephemeral only) governs RECONNECT WS-1/WS-2 (grounded in scope §8). **D7 OQ-1** (handshake-materialization timing) consult lands when Skunkworks BYOC **Phase 2a** scopes — not now. CC memo → lead/read/ (Fire 18).

## Server
Restart env-stripped from the worktree if begun fresh next session:
`env -u ANTHROPIC_API_KEY -u ANTHROPIC_BASE_URL -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_CUSTOM_HEADERS POSTGRES_PORT=5433 nohup venv/bin/python main.py > /tmp/piper-server.log 2>&1 &` (the ephemeral worktree nests in main → finds main's .env/venv).

## Mail state
- Inbox: clear (lead).
- Sent today: #1216 PPM handoff; History→Radar RATIFIED → CXO/PPM.

## Notes
- **gh comment/issue bodies with backticks**: use `--body-file` (Write the file) or a single-quoted heredoc `<<'EOF'`, NEVER inline `--body "... \`code\` ..."` — backticks in a double-quoted bash string are command substitution (bit me 2× on 6/16: a path got executed → eaten from the comment). Same trap as `\`` in a Python double-quoted string.
- DB up (port 5433). Canonical suite green (Q16 fixed → 243/0/0).
- **Bridge discipline**: `git stash push -- <paths>` (NEVER `-u`) on the shared main checkout — `-u` swept Web's untracked log today (recovered).
- **GitHub prefs band-aid** (`data/github_preferences.json`, untracked runtime) keeps the GitHub floor working post-#1042; it is NOT the fix → deleted by RECONNECT WS-1. Two identities mapped: web `a25db09c…` + Slack `009afc8c…`.
- **LESSON (6/14 misattribution)**: I saw product front-end commits, ASSUMED "the Web agent's lane," routed product work to Web + alarmed PM — on an unverified lane assumption. Truth: **Web = website** (`piper-morgan-website`); those commits were **Lead (me) + CXO**. Rule: **detect a cross-lane anomaly → VERIFY the lane (agent role/log) + FLAG to PM; don't rationalize it into an action, and don't unilaterally route cross-lane work** (PM in the division-of-labor loop). Role map: **Lead/CXO = product; Web = website; they're separate repos.**
- **"SOMEONE" ISSUE — RE-DIAGNOSED (Fire 28, sharpened after PM's "why share?" pushback; supersedes my first imprecise "26-instance contention" framing)**: dev/ files reverting mid-session (Fire 26 entry + carry-forward B1 lost). **Worktree isolation actually WORKS** — 18 worktrees, each agent on its own branch/checkout; another worktree can't touch mine. **`origin/main` shared = by design + correct** (the integration trunk; continuity model depends on it). **`dev/active` = shared DIRECTORY but PER-ROLE files** (verified: arch/cio/exec/host/lead/pa each own theirs) → not shared-mutable-state across roles. My own sync is scoped (`git checkout -- mailboxes/*/MANIFEST.md` only — never touches dev/) and `git merge` ABORTS on uncommitted changes (doesn't silently revert). **So the real mechanism = TWO things stacking: (1) my PATH-TRAP discipline slip** (writing to a BARE main-checkout path → edit lands in the shared main *checkout*, never my worktree → looks "reverted" + gets clobbered there); **(2) the MAILBOX BRIDGE** — mailbox writes MUST commit on `main` (hook blocks feature-branch mailbox commits), so every agent funnels mailbox commits through the SINGLE shared main *checkout* (checkout/pull/stash/commit/push) = the genuine concurrent-write hotspot where a mislanded dev/ file dies. **Local mitigation (HOLDING):** worktree paths ALWAYS + commit dev/ writes IMMEDIATELY + verify-by-content + commit-before-sync + explicit paths only. **Sharper architectural Q → Arch/CIO** (PROPOSE, PM-gated): not "stop sharing main" but "**is funneling every agent's mailbox commit through one shared main checkout the right transport at dozens-of-agents scale**, or should mailbox delivery move off shared-checkout-commit (per-agent push / queue)?" **Honest caveat**: reconstructing the exact revert after-the-fact; path-trap is the evidenced recurring cause but can't rule out a same-worktree process. Forensics available if PM wants certainty.
