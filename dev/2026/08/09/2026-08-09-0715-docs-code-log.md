# Session Log: Documentation Management (Docs) — 2026-08-09

**Role**: Documentation Management (Docs)
**Date**: 2026-08-09 (Sunday)
**Model**: Sonnet 5
**Host**: Amber · **Environment**: code
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` · website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`
**Cron**: `34e95366` — `57 6,9,12,15,18,21` (re-armed at yesterday's STOP)
**Prior day**: `dev/2026/08/08/2026-08-08-0713-docs-code-log.md` — **`DAY-CLOSED: 2026-08-08` verified** ✓

---

## Fire 1 (07:15 PDT) — START

**Step 0**: prior day carries its `DAY-CLOSED` marker — proper STOP, nothing to retroactively close.
**Step 1**: cron `34e95366`, exactly one job, no duplicates.
**Step 2**: synced before reading anything. 0 behind, clean.

**Day-of-week duty check**: today is Sunday — no Docs-owned trigger fires today (weekly audit is
Monday, monthly is 1st Monday). Worth noting for context, not action: today is the actual beta target
date per PM's Aug 9 ruling — not a Docs-owned deadline, but worth being aware of in case anything
publish-adjacent comes up today.

Registry, mail loop, gated-item checks next.

---

## PM engaged 07:37 — "Over-Checking Pays Dividends" proofread + publish, plus a retro fix

PM: today's post ready for proofread and publish.

**Found the title had changed same-morning**: calendar showed "Over-Checking Pays Dividends" (was
"Has") — and Comms' publish-ready memo flagged that yesterday's already-published post ("Verify at the
User Path") teased the OLD title on its live footer. PM's instruction, quoted in the memo: "Publish
with new, then fix."

**Ran the full checklist independently** (v1.9, bumped again today — PM's word-order discriminator for
the negation-reveal check landed same morning: the tic is *leading* with the denial, not the mere
presence of a negative). Clean, matched Comms' own audit exactly. One false-positive self-caught before
reporting: check #14's issue-ref grep matched "feedbac" — a substring of "feedback," not a commit hash.

**Published**: slug `over-checking-pays-dividends` (used the corrected title, not the stale draft
filename `over-checking-has-dividends.md`). Dry-run reviewed first, diff reviewed before commit.
Calendar updated, draft + image archived.

**Retro fix**: targeted single-string replacement in `blog-content.json` for hashId `25e803dd5cfe`
(yesterday's post) — `Over-Checking Has Dividends` → `Pays` in the footer tease. Verified exactly one
occurrence before AND after the edit (not just "did the replace run"), committed separately from
today's publish so the fix has its own traceable commit rather than being buried in an unrelated diff.

**Both live-verified independently**: today's new post (distinctive body phrase) and yesterday's
corrected tease (the new title text present on the old page) — two separate backgrounded polls, not
one check standing in for both. Status codes re-confirmed after content on both: 200/200. Replied to
Comms confirming both, closed the loop.

Gated items unchanged (PDR-007, website#31 — website#31 line-count question now day three unanswered,
still not chasing).

---

## PM engaged — Dispatch's recurring stale-calendar friction, investigated and resolved

PM: asked why Dispatch keeps hitting stale-calendar friction on crossposts, whether Web ever replied
to a fix request, and whether Web is waiting on a go-ahead.

**Traced the full history rather than answering from a guess**: found two genuinely distinct root
causes conflated in memory. (1) The admin calendar UI's build-vs-runtime staleness — Web fixed this
cleanly on 07-29 (commit `18be9d1`, verified via routing-layer proof + deliberate bad-token test). (2)
Dispatch's own local-checkout staleness when reading draft content — a different mechanism entirely
(`scripts/sync-pm-local.sh`'s deliberate not-every-commit sync policy), diagnosed and handed to
Dispatch directly on 08-01.

**Found the real answer wasn't either of those**: Web's 07-29 memo left two explicit follow-on
questions addressed to me — extend the runtime-read fix to `/admin/publish-queue`? and which direction
to fix `copy-editorial-calendar.js`'s broken worktree-sibling-path? — and I never replied. Web named
this precisely in their own Aug 7 workstream review: "genuinely stuck on someone else's queue, not
mine." Not Web waiting on PM; Web waiting on me, for 11 days, unnoticed until PM asked a direct
question that forced tracing the actual chain rather than accepting either "must be stuck on Web" or
"must be resolved already" as the answer.

**Resolved both, unblocked, same fire**: replied to Web with clear decisions — yes, extend the fix to
publish-queue (same value proposition as the calendar page); prefer the GitHub API over a sibling-path
walk for the worktree fix (Model A worktrees are stable per-agent paths, not a fixed relative layout a
path-walk can safely assume, and the API route is already the production-proven mechanism). Named
explicitly why this likely explains at least part of Dispatch's ongoing friction — the worktree gap is
the exact shape that produces a silent empty-calendar fallback.

**Owned the miss plainly rather than deflecting**: told PM directly this had been sitting with me, not
Web, before resolving it — the honest answer to "did Web reply / are they waiting on a go-ahead" was
neither of the two framings implied by the question.

---

## Fire 2 (10:15 PDT) — WORK

Cron `34e95366` verified, one job. Worktree-collision check passed. Synced clean, 0 behind.

Mail loop found Web's reply: both fixes from earlier this morning shipped within the hour
(`piper-morgan-website` commit `1b95fa5`). **Verified before closing rather than trusting the memo**:
checked the commit exists and read its actual diff — matched Web's description exactly (API-first
ordering in `copy-editorial-calendar.js`, `/admin/publish-queue` converted to the same runtime-read
pattern as `/admin/calendar`, both with the reasoning captured in the commit message itself). Replied
confirming the verification, closed the loop, updated carry-forward.

Gated items unchanged (PDR-007, website#31).

---

## PM engaged — Over-Checking Pays Dividends syndication, two quick updates

PM sent the Medium URL first (`89d116dbe8c4`), noting LinkedIn was posting concurrently. Set
`mediumURL`, deliberately held `status` at `published` rather than bumping to `distributed` — only one
platform was confirmed live at that point, and the status semantic means live-on-blog-AND-cross-posted,
not "at least one platform." Committed + pushed.

PM then sent the LinkedIn URL (`7tixc`). Set `linkedinURL`, `liPubDate`, and bumped `status` to
`distributed` now that both platforms are confirmed. Whole-file verify clean both times, view rebuilt,
committed + pushed separately for each update rather than batching.

---

## Fire 3 (13:15 PDT) — WORK

Cron `34e95366` verified, one job. Worktree-collision check passed. Synced clean, 0 behind.

Mail loop found Dispatch-DinP's formal calendar-update memo for "Over-Checking Pays Dividends" —
already fulfilled directly with PM earlier this fire, calendar row confirmed matching exactly. Two
real follow-ons in the same memo: (1) a retro-fix note — PM had corrected the Has→Pays title on
Medium and LinkedIn too, not just the canonical site fix I'd made — added to the 08-08 calendar row so
the record covers all three platforms. (2) A genuine question about `canonicalSite` semantics
(observed set to `distributed` before any syndication happened, seemingly contradicting the field
name). **Checked both skills I own rather than answering from memory**: `publish-to-blog` and
`update-calendar` are both already internally consistent and explicit that `canonicalSite=distributed`
is a deliberate pipeline-dedup signal set at blog-publish time, independent of `status` — not a drift
on my end. Quoted both sources verbatim in the reply rather than paraphrasing, and pointed Dispatch at
their own tooling as the likely place the inconsistency actually lives.

Gated items unchanged (PDR-007, website#31).

---

## Fire 4 (16:15 PDT) — WORK

Cron `34e95366` verified, one job. Worktree-collision check passed. Synced clean, 0 behind.

Mail loop found Comms' find: "Verify at the User Path" (Aug 8) has no Medium/LinkedIn syndication,
unlike 7 of the last 9 published insights — the newer post (Over-Checking, Aug 9) is fully distributed
while the older one isn't, which is what makes it a real gap rather than an in-flight lag. Verified
against the calendar directly rather than trusting the report — matched exactly. Comms had already
routed it to Dispatch and offered to fill columns herself, so nothing actionable on my end yet; replied
confirming the check, added a watch item to carry-forward for whenever URLs land.

Gated items unchanged (PDR-007, website#31).
