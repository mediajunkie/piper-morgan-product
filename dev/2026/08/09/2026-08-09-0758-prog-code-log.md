# Session Log — 2026-08-09 07:58 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, isolated worktree `worktree-agent-a5bad82b7397cb1d7`)
**Model**: Fable 5 (claude-fable-5)
**Task pair**: #1542 (word-form duration reminders) + #1545 (malformed row kills Insight Journal list)
**Constraints**: no gh, no push, no mailboxes; commit in worktree; env-stripped test runs.

## Plan
- #1542: extend `services/intent_service/temporal_utils.py` (word-number durations via the
  find_explicit_clock_time finder architecture — a `find_explicit_duration` sibling, no forked parser)
  + `todo_handlers._TIME_EXPR` (shared number-token source imported from temporal_utils so the two
  can't drift). Failing-first: PM's two verbatim phrasings + word-duration matrix; extend
  `TestExplicitClockTimeInvariant1490`.
- #1545: per-row `to_domain()` inside `InsightRepository.list_for_user` loop; new
  `list_for_user_with_skips` returns `(insights, skipped_count)`; route payload gains
  `skipped_count`; honest line in `templates/insights.html`. Failing-first: seeded malformed
  learning JSON row.

## Work

### Failing-first evidence (pre-fix, 08:01)
- **#1542**: 25 failures across the three layers — parser matrix (10, e.g. PM verbatim
  "remind me to stretch in two hours" → `got 2026-08-10 09:00 (label 'tomorrow morning')`),
  slot extraction (7, duration-first returned None / trailing duration swallowed), handler
  end-to-end (2, both PM phrasings), and the 6 new `TestExplicitClockTimeInvariant1490`
  word-duration params. Digit-form regression cases passed pre-fix (correct baseline).
- **#1545**: 4 failures — `list_for_user` raised the bad row's
  `ValueError: Invalid isoformat string: 'definitely-not-a-timestamp'` wholesale (the exact
  live kill-all), `list_for_user_with_skips` AttributeError, route `skipped_count` KeyError.
  (Note: repo tests use real `InsightDB` rows + mocked session — aiosqlite isn't installed
  in the runner venv, so the sibling #1031/#1035 SQLite suites skip there.)

### Fix
- `services/intent_service/temporal_utils.py`: `_NUMBER_WORDS` (a/an, one–twenty),
  `DURATION_NUMBER_SRC` (non-capturing, longest-first), `_DURATION_RE`,
  `find_explicit_duration()` — the find_explicit_clock_time-mirror finder; the inline
  digit-only "in N units" branch in `parse_reminder_time` now delegates to it.
- `services/intent_service/todo_handlers.py`: `_TIME_EXPR` duration alternation uses the
  imported `DURATION_NUMBER_SRC` (single source, no drift).
- `services/database/repositories.py`: `InsightRepository.list_for_user_with_skips`
  (per-row `to_domain()` with error log id+error; returns `(insights, skipped)`);
  `list_for_user` delegates — List contract unchanged, all callers inherit resilience.
- `web/api/routes/insights.py`: `list_insights` uses the with-skips variant; payload +
  log gain `skipped_count`.
- `templates/insights.html`: `#insights-skipped-notice` + JS to render
  "N insight(s) could not be displayed" when `skipped_count > 0`.

### Post-fix verification (08:04–08:10)
- New/extended tests: 65 passed.
- Full mandated scope `tests/unit/services/intent_service/ tests/unit/web/
  tests/test_architecture_enforcement.py` (env-stripped): first run 1 failed
  (`test_github_query_handlers.py::TestCommentIssueResults::test_formats_comment_confirmation_correctly`
  — unrelated surface), passes in isolation AND on identical full-suite rerun
  (**2808 passed, 0 failed**). Assessed as order/timing flake, not this diff.
- Sibling one-bad-row shapes NOTED (not fixed, per task): in insights.py the by-id
  endpoints fail per-row only (scoped, acceptable); repo-layer siblings
  `get_for_object` / `get_unsurfaced` / `get_for_context` still run `to_domain()`
  unguarded per row — same latent kill-all for Pull/Push retrieval paths.

## Discovered issues filed
None (no `bd`/gh allowed in this lane; sibling repo-layer shapes reported to Lead in the
task report for triage).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-stripped test runs; worktree discipline; failing-first /
  evidence norms), test_reminder_time_binding_1490.py docstring (invariant framing),
  feedback_verify_at_the_right_layer (confirmed pre-fix failure mode is the real ValueError).
- **Loaded but not referenced**: memory index at large.
- **Wanted but not found**: nothing.
