# Session Log — 2026-08-09 0745 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, isolated worktree `agent-a7f1b4dcd313e23bd`)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1547 — canonical IntegrationStatusService + highest-damage surface migration
(status-truth audit `docs/internal/operations/status-truth-audit-2026-08-09.md`)
**Constraints**: no `gh`, no push, no mailboxes; commit in worktree only.

## Work

- 0745 Read the audit doc (mandatory first read) + all coordinates: route
  internals (`_github_oauth_bound` :306, `_get_integration_config_status` :428),
  canonical_handlers F1/F3/F4 sites, context_assembler/conversational_floor F2
  sites, feed_factory :87, the four plugins, the two pinned regression tests,
  and every unit test touching those surfaces.
- 0746 Baseline: required suites (tests/unit/web, tests/unit/services/
  {intent_service,integrations,radar}, tests/test_architecture_enforcement.py)
  = **3400 passed, 4 skipped**. Pinned tests (1513/1329) green.
- 0747 **Step 1** — failing-first (`ModuleNotFoundError`) then built
  `services/integrations/integration_status_service.py`: `github_oauth_bound`
  (hoisted verbatim), `get_config_status` (hoisted + `via` reporting),
  health-monitor singleton moved here, `IntegrationStatusService.get_status/
  get_all/is_configured`. 13 service tests green. Route made thin consumer
  (delegating `_github_oauth_bound` / `_get_integration_config_status` /
  `_get_health_monitor`); pinned tests pass with ZERO diff to their files.
  One design deviation from the verbatim hoist: `last_check` stays None for a
  registered-but-never-tested component (register_component stamps a timestamp
  at registration; reporting it as a "check" would be an m-44 violation).
- 0752 **Steps 2-4** — failing-first (TypeError on new signature; radar test
  failed against stashed pre-change provider), then migrated:
  F1 `_format_integration_setup_guidance` (async, user_id, display names, demo
  gone structurally); F2 identity-context integrations + `github_connected`
  from service, floor renders BOTH directions + an actual GitHub line;
  F3 `_get_project_metadata(projects, user_id)` service gate (threaded user_id
  through all 7 prod call sites incl. intent_service.py); F4
  `_get_priority_metadata` + Radar `WorkItemProvider` binding-first.
- 0757 **Step 5 (F5)** — verified zero non-test consumers (grep fetch/URL over
  web/templates, web/static, services, scripts: none; only demo's own test
  references its own /status). Deleted the four plugin /status sub-routes.
- 0758 **Step 6** — real plugins' get_status(): `configured: None` +
  `configured_note`; demo untouched. Enforcement ratchet
  `TestRegistryConfiguredReadRatchet` added (MAX_REGISTRY_STATUS_CONSUMERS=1 —
  the F6 Slack-help site; denominator guards: scan-space floor 300 files,
  plugin-file existence; tightness test; fabrication regression test).
- 0801 Updated 9 tests pinning the retired behavior + made 5 more hermetic
  (they were passing via the conftest-injected GITHUB_TOKEN env). Updated
  tests/plugins contract tests to the new plugin contract (None-configured +
  no /status; demo exempt).
- 0806 Final: required suites **3441 passed, 4 skipped, 0 failed** (net +41 new
  tests, zero regressions); tests/plugins 192 passed; compile-ok; newlines fixed.

## Discovered issues filed

None filed (no `gh` per task constraints). Two follow-ups flagged in report for
Lead: (a) post-gate `GitHubDomainService.get_connection_status()` in F3/F4 is
still PAT-side — OAuth-only users pass the gate then degrade CONNECT_REQUIRED;
(b) F6-F10 remain (F6 is the last registry-status consumer, ratchet at 1).

## Memory & briefing surfaces referenced this session

- **Referenced**: status-truth-audit-2026-08-09.md (all coordinates + sequencing);
  CLAUDE.md env-strip prescription (test runs); m-43/m-44 (layer naming +
  denominator guards in the ratchet); intent-routing-stack doc pointer (not
  needed — no routing-surface change); TestPreFloorDispatchSiteRatchet (shape
  mirror).
- **Loaded but not referenced**: memory index bulk; briefing table.
- **Wanted but not found**: a map of which tests pin which integration surface
  (reconstructed by grep).
