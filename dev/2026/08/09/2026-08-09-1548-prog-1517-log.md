# Session Log — 2026-08-09 15:48 — Coding Agent (prog) — #1517

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: .claude/worktrees/agent-a0e511ed94628f329 (isolated; commit local only, no push, no mailboxes, no gh)
**Task**: #1517 capability gaslighting — (1) diagnose (report-only) the routing miss that dropped
"remind me at 9:41 today to X" to the floor; (2) build floor capability manifest (registry-derived)
+ anti-retraction instruction + tests.

## Log
- 15:48 — Session start. Read intent-routing-stack.md (mandatory). 4-surface chain mapped.
- 15:52 — Part 1 (routing miss, DIAGNOSE-ONLY, no patch — routing moratorium honored). Verified
  deterministically by direct execution (probe script, env-stripped venv): BOTH transcript turns
  return `pre_classify: None` and no multi-intent. Root findings:
  1. Surface 1 miss (both turns): `REMINDER_PATTERNS` (pre_classifier.py:645-651) requires
     "to|about" IMMEDIATELY after "remind me" — `\bremind\s+me\s+(?:to|about)\b`. "remind me
     AT <time> ... to X" inserts the time phrase between "me" and "to" → no match. Turn 1
     succeeded only because the LLM classifier (surface 2) happened to emit
     EXECUTION/create_reminder.
  2. Surface 2 variance (turn 2): the classifier prompt (services/intent_service/prompts.py)
     contains ZERO reminder guidance ("remind" absent; create_reminder not in prompt vocabulary)
     while listing "today"/clock-time as TEMPORAL indicators (prompts.py:12,31-44). The
     numeric-time + "today" shape ("at 9:41 today") is denser in the prompt's TEMPORAL cues →
     non-EXECUTION emission.
  3. Structural: create_reminder is dispatched ONLY inside `_handle_execution_intent`
     (intent_service.py:6849) — category-gated, NOT on the #1124 rail (workflow_entries.py has
     no create_reminder key). A correct action name with wrong category still floors.
  4. Corroboration the emission was non-EXECUTION: ANY EXECUTION-category turn either dispatches
     or deterministically honest-declines via `_get_contextual_fallback` — whose "remind me"
     branch (intent_service.py:7062-7069, #1426 corrected copy) AFFIRMS the capability. The
     observed denial + Slack /remind recommendation is floor prose → the turn never entered
     `_handle_execution_intent`; it took the Action-Gate/canonical-generic path to the floor
     (TEMPORAL keyword-miss → canonical → generic-detect → floor at intent_service.py:1561-1575,
     or a directly floor-routed category at :1532).
- 15:57 — Part 2 (BUILD). Failing-first: new test file collected red
  (ImportError: capability_manifest_block) before implementation. Then:
  - `wired_chat_actions()` in workflow_dispatcher.py — DERIVED manifest source: registry
    action_triggered entries (dedup by entry identity, canonical=first key) ∪
    ActionMapper.ACTION_MAPPING targets minus unknown_intent (the legacy elif chain enumeration).
    Never hand-maintained.
  - `capability_manifest_block()` in conversational_floor.py — static prose is
    capability-name-free (tested); derived list is the only carrier. Wired into
    `_get_system_prompt` on EVERY non-denial floor turn (try/except + warning log, never fatal).
  - Anti-retraction CRITICAL block added to FLOOR_SYSTEM_PROMPT_ADDENDUM (#1517), written as the
    dual of #1331's no-reassertion rule (both pinned coexisting).
  - Tests: tests/unit/services/intent_service/test_floor_capability_honesty_1517.py — 13 tests,
    layer named honestly in docstrings (derivation + prompt-content, NOT live LLM behavior).
    Includes manifest ∩ UNWIRED_WRITE_DECLINES == ∅ (the freshness invariant at the manifest
    layer, complementing #1433's ratchet which doesn't reach floor prose).
- 15:59 — Full suites env-stripped: tests/unit/services/intent_service/ +
  tests/unit/services/conversation/ + tests/test_architecture_enforcement.py -m "not llm":
  **2361 passed, 2 deselected, 0 failures** (99.7s). Zero regressions.

## Discovered issues filed
- None (no `gh` in this worktree per constraints). Two candidates for the caller to consider
  filing/noting: (a) pre-classifier REMINDER_PATTERNS "remind me at <time> to X" gap — corpus
  material under the routing moratorium, NOT patched; (b) create_reminder absent from the #1124
  rail (category-gated legacy dispatch) — migration candidate under the existing #1124 roadmap.

## Memory & briefing surfaces referenced this session
- Referenced: docs/internal/architecture/current/intent-routing-stack.md (4-surface chain — the
  whole Part-1 diagnosis frame); CLAUDE.md intent-dispatch rule (no new elifs; none added);
  m-43/m-44 (layer-naming in test docstrings + vacuity guard in derivation test).
- Loaded but not referenced: glossary, mailbox/worktree discipline sections (constrained role).
- Wanted but not found: none.
