# Session Log — 2026-08-09 15:48 — Coding Agent (prog) — #1507 + #1508

**Role**: Coding Agent (prog)
**Model**: Claude Fable 5 (claude-fable-5)
**Worktree**: .claude/worktrees/agent-ae878f4d745247388 (branch worktree-agent-ae878f4d745247388)
**Task**: #1507 (remove os.environ GITHUB_TOKEN write in github/save) + #1508 (require_admin on 4 admin cache routes)

## 15:48 — Session start / verify-first

Found BOTH fixes already committed and reachable from HEAD (Lead's 2026-08-08 06:47 fire):
- 328f10de4 fix(1507): github/save no longer writes os.environ GITHUB_TOKEN process-wide
- 0ebca2544 fix(1508): require_admin on the 4 admin cache-mutation routes
- 88a0b4cfb docs(lead): 0647 fire — 1507+1508 built

Per CLAUDE.md STOP condition 3 / verify-first: did NOT duplicate. Pivoted to verification:
confirm each AC is satisfied by the existing commits, produce independent red/green
evidence, run the required test set env-stripped, do the GITHUB_TOKEN reader sweep
(not recorded anywhere by the prior fire), report.

## 15:50 — Verification results

**#1507**: env write is gone (settings_integrations.py:1979-1989 now keychain-only +
cache clear, with #1507 rationale comment). Pin test is AST-level (bans the whole
env-mutation class in the module, m-44 denominator guard included).

**#1508**: all four mutating routes (`intent-cache-clear`, `piper-config-cache-clear`,
`user-context-cache-clear`, `user-context-cache-invalidate/{session_id}`) carry
`Depends(require_admin)` (admin.py:125/161/192/209). Read-only metrics/health routes
deliberately ungated and pinned so the fix can't widen. No new auth mechanism; routes
NOT moved to /api/v1 (correctly left for #1499's class).

**Behavioral-403 coverage note**: the 1508 tests are router-derived wiring pins, not
per-route HTTP 403 tests. Composition is sound because require_admin itself has full
behavioral coverage in test_global_credential_write_admin_1485.py
(test_require_admin_refuses_non_admin_403, test_require_admin_returns_claims_for_admin,
fail-closed 503 on DB error, plus HTTP-level 403/no-write tests on the 1485 routes).
Dependency proven to 403 + dependency proven wired on all four = equivalent coverage.

## 15:52 — Failing-first evidence (reproduced at this seat)

Working tree verified clean (`git diff HEAD -- <both files>` empty) before the
direction-safe restore. Checked out pre-fix versions (328f10de4^ and 0ebca2544^),
ran the new tests:
- 1507 pin RED: `AssertionError: process-wide env mutation … line 1985: os.environ[...] assignment`
- 1508 pin RED: `mutating admin routes WITHOUT require_admin: {all four paths}`
Restored HEAD versions (`git checkout HEAD -- …`, tree back to clean) → all 4 tests GREEN.

## 15:55 — GITHUB_TOKEN reader sweep (every env-read site + disposition)

Actual env READS (message-strings/comments excluded):
1. `services/integrations/github/config_service.py:177` — env fallback gated
   `not is_real_user` (system-only) per #1461(a). Real users never receive it.
   No dependence on the removed refill; #1507 restores this gate's integrity. OK.
2. `services/integrations/integration_status_service.py:145` — status badge; env
   presence OR user-scoped keychain (#1513 already fixed this reader). OK.
3. `web/api/routes/settings_integrations.py:2137` (`_list_repos_native_pat`) —
   keychain user-scoped FIRST, env second. A user who saved a PAT hits keychain;
   nothing breaks without the refill (no-token → 401 "GitHub not connected", which
   IS the #1461(a)-correct answer). RESIDUE: the env fallback still hands a real
   user the ambient token on dev seats with GITHUB_TOKEN set — #1461-class, on a
   transitional path scheduled for retirement (ADR-070 D6). Reported, not hacked.
4. `services/infrastructure/github_repo_validator.py:42` (`get_github_token`) —
   env-only; callers (canonical_handlers:4622, conversation_handler:493,
   repositories.py:89, setup.py:1210) pass no token. Without env token it SOFT-SKIPS
   (validated=False, no failure) — nothing breaks. Ambient credential used only for
   read-only repo-metadata GET; low risk; noted.
5. `services/infrastructure/config/config_validator.py:77` — /health/config ops
   surface; presence check only, no user-path token use. OK.
6. `services/config/github_config.py:73` (`validate_environment`) — NO callers found
   in services/, web/, main.py. Dead code candidate; noted.
7. `services/connectors/disconnect.py:65` — env POP (write, not read): any user's
   disconnect pops GITHUB_TOKEN/GITHUB_API_TOKEN/GH_TOKEN process-wide. Pre-#1507
   this cleaned up the save-route's own residue; post-#1507 it deletes a genuinely
   provisioned SYSTEM env credential on any user's disconnect — the mirror image of
   #1507 (cross-tenant destructive mutation). Discovered work; gh prohibited this
   session → escalating in report for parent to file.

## 15:57 — Test runs (env-stripped: ANTHROPIC_API_KEY/BASE_URL/AUTH_TOKEN/CUSTOM_HEADERS
unset; venv /Users/xian/Development/piper-morgan-worktrees/lead/venv; -m "not llm" -p no:cacheprovider)

- tests/unit/web/ → **635 passed**, 11 warnings, 16.28s
- tests/unit/services/{auth,integrations,infrastructure,connectors,security} (targeted
  to touched surfaces) → **864 passed, 3 skipped**, 10.05s
- tests/test_architecture_enforcement.py → **39 passed**, 5.07s
- The 4 new pin tests (1507+1508) → green at HEAD, red at pre-fix (above)

**Total: 1,538 passed, 3 skipped, 0 failed. Zero regressions.**

## Discovered issues to file (gh prohibited this session — routed via report)
- disconnect.py env-pop is a cross-tenant process-wide mutation (mirror of #1507)
- `_list_repos_native_pat` env fallback for real users = #1461(a) residue on a
  transitional path (may be moot at ADR-070 D6 retirement)
- `GitHubConfiguration.validate_environment` appears caller-less (dead code candidate)

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (STOP condition 3 → verify-don't-duplicate; SCOPE IS NOT
  DIRECTION → diff-before-checkout during red-proof; env-strip prescription for test
  runs; m-44 denominator framing in sweep report), #1461(a) gating comment in
  config_service.py (reader-sweep dispositions), 1485/1507/1508 commit messages.
- **Loaded but not referenced**: memory index entries; briefing docs (task was
  self-contained).
- **Wanted but not found**: a recorded reader-sweep from the 08-08 fire (had to redo
  it — this log now records it).

## Wrap
Session log committed locally. No push, no mailboxes, no gh (per constraints).
Temp runner script deleted after use (command recorded in §15:57 header).
