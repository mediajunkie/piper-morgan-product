# Session Log — 2026-08-09 09:40 — prog (Coding Agent, Claude Code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: `.claude/worktrees/agent-ab82a92399df9e617` (branch `worktree-agent-ab82a92399df9e617`)
**Task**: #1510 FTUX-TRUST compose vs. execute — UNGATED half only (declaration surface + collaborate-first default). Layer diagnosis first.

**Constraints**: commit locally only (no push), no mailboxes/, no gh, do NOT touch workflow_entries.py, no pre-classifier regex changes, nothing inferential (counters/thresholds/decay blocked pending PM).

## Plan
1. Read intent-routing-stack.md (mandatory) + PPM unblock memo
2. Trace "help me write a ticket about X" through the 4-surface chain — layer verdict w/ file:line
3. Find existing user-preference storage; extend for declared working-mode
4. Failing-first Jake-replay test; collaborate-first default behavior
5. Run suites env-stripped; zero regressions

## Entries
- 09:40 — Session start, log created.
- 10:05 — **Layer diagnosis done** (AC-1). Trace of "help me write a ticket about X":
  - Surface 0 (B3): no update-verb/referent shape → falls through (`classifier.py::_resolve_issue_referent`).
  - Surface 1 (pre-classifier): NO create-issue/ticket patterns exist (`pre_classifier.py:463-489` = close/reopen/comment/review/list only) → falls through.
  - Surface 2 (LLM classifier): `services/intent_service/prompts.py` has ZERO compose/draft action vocabulary. Line 252 teaches "create a ticket for the login bug" → `execution/create_ticket`; line 249 teaches "how do I create a ticket?" → GUIDANCE. A compose-phrased task with a concrete topic has no compose-side action name to land on → emits `create_ticket`/`create_issue`.
  - Surface 3 (rail): `create_ticket` aliases `create_issue_entry` (`workflow_entries.py:551-556`) → `_handle_create_issue` (`intent_service.py:7230`) → `github_router.create_issue` unconditionally; only asks are missing-repo (7339) / missing-title (7362). No mode/confirmation check.
  - **Verdict: (a) Understanding — structurally cannot distinguish (vocabulary has no compose action) — compounded by (b) Acting executing any write action with zero mode awareness.** Classifier half = corpus/inversion material under moratorium; building action-layer half.
  - Storage finding: per-user persistent prefs = `users.preferences` JSONB (`services/database/models.py:161`), written via pattern at `intent_service.py:2638-2646`; `UserPreferenceManager` is in-memory only (not persistent, per-instance) — NOT suitable.
- Plan: new `services/intent_service/collaboration_gate.py` (WorkingMode enum, DB-backed get/set, framing classifier, declaration detector, gate); wire declaration surface at top of `_process_intent_internal` and gate at top of `_handle_create_issue`. Failing-first test `test_collaboration_gate_1510.py`.
- 09:50 — **Failing-first evidence**: gate module written, wiring NOT yet done. Run:
  `TestJakeReplay::test_jake_shape_collaborates_instead_of_creating` FAILED —
  `AssertionError: Expected mock to not have been awaited. Awaited 1 times.` (i.e. the Jake
  shape "help me write a ticket about the login timeout on mobile" DID fire
  `github_router.create_issue`). 1 failed, 32 passed.
- 09:52 — Wired both halves (declaration surface in `_process_intent_internal`; collaborate-first
  gate at top of `_handle_create_issue`). One test-side fix: the execute-mode replay message used
  "for" where the slot-fill about-form needs "about" (the handler's honest-ask intercepted before
  the write, masking the gate result) → changed phrasing. **39/39 green.**
- 09:55 — Full required suites, env-stripped, `-m "not llm" -p no:cacheprovider`:
  `tests/unit/services/intent_service/ + tests/unit/services/intent/ + tests/test_architecture_enforcement.py`
  → **2325 passed, 2 deselected, 0 failed** (45.24s). Zero regressions.
- 09:57 — Updated `docs/internal/architecture/current/intent-routing-stack.md` (its own consult
  rule: change makes doc stale → same commit) with the two new deterministic checks.

## Deliberately NOT built (with reasons)
- Anything inferential (counters, thresholds, decay, graduation) — explicitly blocked pending PM
  (PPM unblock memo 2026-08-09).
- No classifier/prompt/pre-classifier changes — routing moratorium. Diagnosis says the classifier
  cannot see compose-vs-execute (no compose-side action vocabulary); that half is corpus/inversion
  material, reported not built.
- No `workflow_entries.py` changes — parallel agent landing `effect` enum; local
  `GATED_WRITE_ACTIONS` mapping with `TODO(#1510)` in collaboration_gate.py is the integration point.
- Gate wired for create-issue family only — close/reopen/comment await the effect enum + #1190
  (destructive-mutation confirmation) coordination; gating them now would collide with #1190's
  confirmation-turn design.
- Pending-draft continuation state (user says "yes, file it" after a collaboration draft binds to
  the drafted title) — needs conversation-state machinery adjacent to #1190; the gate's copy
  instead teaches the explicit create form that works today.
- Anonymous-user declarations — no user row to persist to; declaration check skips when user_id
  is None (falls through to normal routing).

## Discovered issues filed
- None filed (no `gh`/`bd` allowed in this worktree). For Lead to consider tracking: (1) classifier
  compose-vocabulary gap (corpus/inversion material, per diagnosis); (2) pending-draft
  confirmation continuity; (3) extending the gate once WorkflowEntry.effect lands.

## Memory & briefing surfaces referenced this session
- **Referenced**: `docs/internal/architecture/current/intent-routing-stack.md` (mandatory 4-surface
  map; located every trace hop), PPM unblock memo (authorization boundary),
  CLAUDE.md (ratchet rule, verify-first, failing-first), `test_create_issue_title_1543.py` +
  `test_chat_write_gates_1220.py` (handler test harness pattern), `intent_service.py:2638` JSONB
  write pattern (persistence seam).
- **Loaded but not referenced**: memory index (MEMORY.md) entries broadly; glossary.
- **Wanted but not found**: a single doc naming which preference store is canonical for per-user
  durable prefs (had to derive that UserPreferenceManager is in-memory-only from code).
