# Web session — 2026-08-09 (Sunday, beta target date)

**Role**: Unicorn Web Designer (piper-morgan-website)
**Account**: pipermorgan.ai / Amber
**Model**: Claude Opus 5 (1M context)
**Prior session**: `dev/2026/08/08/2026-08-08-0628-web-code-log.md` — **DAY-CLOSED: 2026-08-08** (explicit STOP at 21:39; day arc: a serious merge-deletion incident, checked own exposure clean, same-day correction of a standing-practice check that was itself superseded within hours).
**Trigger**: cron fire `104cb687`, `22 6,9,12,15,18,21 * * *`, first fire of the day (06:28)

---

## START (06:28, heartbeat landed 06:28:19)

### Step 0 self-heal

8/8 closed properly with an explicit STOP and `<!-- DAY-CLOSED: 2026-08-08 -->` marker (verified via grep). No self-heal needed.

### Step 1b — cohort-wide freeze check (new, all roles, v1.24)

`scripts/cohort-freeze-detect.sh` → rc=0, ordinary wake, no cohort-wide freeze. Nothing to note.

### Skill update noticed at wake

`duty-cycle-tick` is now v1.25. Most significantly, Step 3's mail/task loop has been formally
restored to PM's original May-ratified specificity (direct-vs-cc distinction, do-not-stop-after-one,
loop until truly at (0,0), the `mail: N direct, N read; M cc, skimmed` assertion) — this exactly
matches the practice I self-adopted Friday night (8/7) after the same restoration was announced by
mail. No behavior change needed on my end; confirming alignment rather than adjusting anything.

### Environment

| Check | Result |
|---|---|
| Cron | one job, no duplicates, `104cb687` |
| Product worktree | `claude/web-cycle`, 0 ahead/behind, clean |
| Website worktree | `claude/web-cycle`, 0 ahead/behind, clean |

### Carry-forward — open threads inherited from 8/8

- **Two Docs-owned decisions** (publish-queue staleness, `copy-editorial-calendar.js` fix direction) — flagged in the Ship #055 report, still no rush.
- **Predecessor's ~3.5-week-old question** (CLI B, `--mode=archive`) — also on record via the Ship report.
- **PM design/obs-pass backlog** — same structural blocker, also on record.
- **BYOC/GTM**: no action pending until PM picks a narrative angle.
- **Merge-safety practice (corrected 8/8)**: if a future sync ever conflicts, use unfiltered `git diff --stat <merge>^2 <merge>`, never `git restore --staged` mid-merge.
- **Standing practice**: `mail: N direct, N read; M cc, skimmed` in every fire log entry, loop mail→task→mail until genuinely at (0,0) — now formally codified in the skill (v1.25), not just self-adopted.

---

## Fire log

| Fire | Time | Action | Notes |
|---|---|---|---|
| cron | 06:28 | START | Wake-time heartbeat emitted first. Cohort-freeze check clean (rc=0). Noticed and confirmed alignment with the newly-restored Step 3 mail/task-loop discipline (v1.25) — no behavior change needed, already compliant since 8/7. No self-heal needed. Environment verified, both worktrees synced clean. **mail: 0 direct, 1 read; 0 cc.** (PA's Jake feature-register row correction — product-repo, not Web's lane, read for context.) Triaged, sent (`95bb0d8aa`). Task loop: no movement. Re-checked mail after task loop — empty, genuinely at (0,0). |
| cron | 06:32 | — | ⚠️ **Repeated the exact mistake I self-caught and documented on 8/6**: called `duty-cycle-heartbeat.sh web START --if-quiet` again at fire-close out of habit, matching how I close WORK/WATCH fires. START always writes unconditionally regardless of `--if-quiet` — same lesson, same failure, three days later. Second START row for today (`06:32:38`, four minutes after the real wake at `06:28:31`), confirmed via `cat`. Documenting this one plainly rather than treating the earlier note as having fixed it — it evidently didn't change the actual habit, only added a line to a log. **Concrete change this time, not just a note**: for START fires specifically, skip the fire-close heartbeat call entirely — the wake-time emission already covers the fire, and there is no WORK/WATCH-style completion signal that applies to a START. Writing that as an explicit rule in my own carry-forward, not just an observation, since the observation alone already failed once. |
| cron | 09:28 | WORK | Synced both worktrees clean. **mail: 2 direct, 2 read in full; 3 cc, skimmed.** Direct: Docs resolved both 11-day-old decisions from my 7/29 memo (extend staleness fix to `/admin/publish-queue`: yes; `copy-editorial-calendar.js` fix: prefer the API over the path-walk); Janus relayed PM's direct design feedback that pipermorgan.ai/blog's hero pushes real content down too far. CC (not Web's lane, skimmed/triaged): Arch's inbox-glob "read/ is a claim about cognition" procedure (self-checked — my own mail-triage practice has never globbed, clean); Exec's third-form denominator finding (MVP gate count fell 26→17 while 48 unmilestoned issues appeared); PA's citation-staleness correction on Arch's understanding-layer ruling. Shipped all three real fixes this fire, website commit `1b95fa5`, pushed to origin/main. See narrative below for detail. Replied to Docs and Janus (cc Exec/PM) with what shipped. Task loop: no separate standing items surfaced beyond the two now-resolved. Re-checked mail after — empty, genuinely at (0,0). Inbox fully triaged to `read/` via individually-displayed-then-moved memos (5 total), verified landed on `origin/main` via `git ls-tree`. Carry-forward updated: two Docs-owned decisions moved from "stalled" to "resolved+shipped"; new blog-hero thread added with its one open loop (no visual confirmation possible, no browser on this host). |

### Fire 09:28 — three shipped fixes (detail)

**1. `copy-editorial-calendar.js`** (`scripts/copy-editorial-calendar.js`): reordered source-strategy
priority so the GitHub Contents API is tried first, with the local sibling-checkout path now a fallback
only reached on no-token/fetch-failure. Previously the reverse — a worktree-provisioning change could
silently resolve the sibling path to something unexpected and never reach the battle-tested API branch
at all. Docs' explicit ruling: prefer the already-proven mechanism over a path-walk assumption about
worktree layout. Verified with a deliberate bad-token test (`GITHUB_DRAFT_TOKEN=deliberately-invalid-token`)
— logged HTTP 401, fell through cleanly to the sibling check, then the header-only placeholder, matching
the original behavior's safety net exactly. `node --check` clean.

**2. `/admin/publish-queue`** (`src/app/admin/publish-queue/page.tsx`): converted to the same
`loadCalendarLive()` + `export const dynamic = 'force-dynamic'` pattern shipped on `/admin/calendar`
2026-08-04, with the identical live/stale banner UX. Confirmed this was a straightforward extension, not
requiring a separate runtime path for the prebuild-generated `publish-queue-data.json` — `loadCalendarLive()`
already returns the typed `CalendarEntry[]` that `readyToPublish`/`recentlyPublished`/`syndicationGaps`/
`imageMetadataGaps` already consume unchanged. Checked both repos for external consumers of the static
JSON mirror before leaving it build-time — none found; added a note on the page that the JSON now lags
the (live) page above it. Verified: `npm run build` shows the route as `ƒ` (dynamic) not `○` (static),
matching `/admin/calendar`'s marker; `npx tsc --noEmit` clean on touched files; served the production
build locally and confirmed real section data rendered (24 ready-to-publish, 3 image-metadata gaps
against the local build-time snapshot, correctly reported as "stale" with reason `GITHUB_DRAFT_TOKEN not
set` since this host has no token — same expected behavior as `/admin/calendar` under the same condition).

**3. Blog hero sizing** (`src/components/molecules/Hero.tsx`, `src/app/(public)/blog/page.tsx`,
`src/app/(public)/blog/page/[pageNumber]/page.tsx`): PM's direct design feedback, relayed by Janus —
the blog page's top area (hero/header) pushes real content down too far, same shape as a hero-sizing
issue PM's designer friend Yoni flagged and Janus just fixed on DinP's homepage. Traced without a
screenshot (no browser on this host): both blog pages reuse the shared `Hero` component at full
marketing weight (`pt-16 md:pt-24 pb-8 md:pb-12` padding, large headline+highlight, full subheadline,
two CTAs) — the same treatment the homepage correctly uses to orient a first-time visitor, wrong weight
for a content index where the post list is the actual destination. Added an opt-in `compact` prop to
`Hero` (reduced padding to `pt-8 md:pt-10 pb-6 md:pb-8`, reduced heading/subheadline bottom margins from
`mb-8` to `mb-4`), applied only to the two blog pages — copy and CTAs untouched, purely the vertical-
space fix PM described. Verified no regression: built and served the site locally, confirmed via the
rendered RSC payload that `/blog`'s Hero section carries the compact classes while `/` (homepage) still
carries the original full-size classes — the prop is correctly opt-in and scoped. **Open loop, named
explicitly in the reply to Janus/PM**: no way to visually confirm the actual result without a browser on
this host; flagged that as the one thing still worth PM's own eyes before calling this fully done.

**Verification summary across all three**: full `npm run build` clean (408 static pages + the two now-
dynamic admin routes); `npx tsc --noEmit` shows zero errors in any touched file (the pre-existing
`searchParams`/`pathname` null-check errors elsewhere in the repo are unrelated and untouched); one
commit (`1b95fa5`), pushed clean to `origin/main` on the first attempt.
