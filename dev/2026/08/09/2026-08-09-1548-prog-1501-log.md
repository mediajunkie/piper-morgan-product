# Session Log — 2026-08-09 15:48 — Coding Agent (prog) — Issue #1501
**Role**: Coding Agent (prog)
**Tool**: Claude Code
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a3054b59fe5a6c170 (isolated worktree; commit local only, no push)
**Task**: #1501 SECURITY — owner-scope ProjectQueryService reads (find_project_by_name, list_active_projects, count_active_projects), mirroring #1421's get_default_project pattern. Failing-first tests; keep AST guards green.

## Log
- 15:48 — Session start. Log created. Beginning verify-first read: project_queries.py, #1421 pattern, tests, audit doc.
- 15:50 — Verify-first complete. Findings:
  - `ProjectRepository` already supports optional `owner_id`/`is_admin` on all three underlying methods (list_active_projects, count_active_projects, find_by_name); the SERVICE simply never passed a principal, so the optional filter never engaged. #1421's service-level `get_default_project(owner_id)` has NO is_admin param (fail-closed only) — mirrored that exactly; admin surfaces use the repo's is_admin bypass directly.
  - Caller trace (absolute + relative imports + method-name grep, repo-wide): ProjectQueryService has ZERO production callers. Only importers: `services/queries/__init__.py` (re-export) and two `tests/archive/` scripts (excluded from collection via pytest.ini/pyproject `--ignore=tests/archive`; one imports the deleted `query_router` module and cannot even run). All same-named call sites elsewhere (PortfolioService, ProjectRepository callers in canonical_handlers, intent_service, user_context_service, first_meeting_detector, web/api/routes/projects.py) already thread user_id/owner_id — except `ProjectContext.resolve_project` (services/project_context/project_context.py:50/:56), bare repo reads in a class with no principal in scope and zero production callers (#1421 census B item 31).
- 15:53 — RED evidence (leak demo vs unfixed code, live DB): `find_project_by_name(a_name)` with NO principal returned tenant A's project; `list_active_projects()` returned 62 projects across all tenants; `count_active_projects()` = 62 global. New test file also red (TypeError: unexpected keyword 'owner_id').
- 15:55 — Fix shipped in `services/queries/project_queries.py`: all three methods take required `owner_id: Optional[str]`, fail-closed (`[]`/None/0 + structlog warning, repo never touched) on falsy principal; scoped repo call otherwise. New tests green (4/4): cross-user name-lookup isolation (non-None principals per audit standing rule), list/count scoped per owner, no-principal fail-closed with rows present, tripwire-repo short-circuit proof.
- 15:57 — Guard extension (tests/test_architecture_enforcement.py, TestPrincipalThreadingGuards1532): PRINCIPAL_KEYED_READERS += the three method names (#1501 reader-set extension); `_passes_principal` now recognizes `owner_id` kwarg alongside `user_id`; ALLOWED_UNTHREADED += ProjectContext.resolve_project entry (2 call sites, 1 tuple) with rationale, MAX 1→3, direction still DOWN. portfolio_service.py:209 converted to kwarg call so the guard sees its principal. All 39 enforcement tests pass — and since Guard 1 asserts violations == allowlist EXACTLY (non-empty), the pass is behavioral proof the extended scanner detects the project-read sites (m-43).
- 15:58 — Full required suite: first run 5933 passed + 1 failed = `tests/unit/services/place/test_place_service.py::TestGitHubPlace::test_github_place_has_name`. Verified PRE-EXISTING at HEAD via stash/run/pop in this isolated worktree (fails identically with my changes stashed; Place naming, unrelated). Deselected rerun: **7419 passed, 228 skipped, 0 failed** (tests/unit/services/ + tests/test_architecture_enforcement.py, -m "not llm").

## Discovered issues (for Lead to file — this session has no `gh`)
1. Pre-existing failure: `TestGitHubPlace::test_github_place_has_name` asserts `"test-repo" in place.name` but Place name is `'GitHub'` — fails at HEAD (d469a5310), blocks any maxfail=1 run of tests/unit/services/.
2. `ProjectQueryService.get_project_by_id` / `get_project_details` remain owner-UNscoped (repo get_by_id supports owner_id/is_admin but service doesn't thread it) — same class as #1501 but by-id (UUID-unguessability-bounded, like the audit's F3 note). Out of #1501's named scope; should be its own issue.
3. `ProjectContext.resolve_project` (project_context.py:50/:56) does bare global `repo.list_active_projects()` reads + LLM-inferred name matching against ALL tenants' projects — dead code (zero prod callers) but the same leak shape; fix-or-delete candidate. Now pinned visible in the Guard-1 allowlist.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (verify-first, evidence, m-43/m-44, commit discipline, auto-close-keyword gotcha — commit message phrased to avoid `fix #1501` closing the live issue); docs/internal/operations/principal-dropping-audit-2026-08-08.md (guard design, non-None-principal standing rule); tests/unit/services/database/test_default_project_owner_scoped_1421.py (test shape mirrored); repositories.py #1421/#1470 docstrings (repo semantics).
- **Loaded but not referenced**: memory index (MEMORY.md) — task was fully specified by the briefing prompt.
- **Wanted but not found**: none.
