# Session Log — Lead Developer — 2026-08-11

**Role**: Lead Developer (`lead-code`) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/lead` (Model A, stable) on `claude/lead-cycle`
**Started**: 13:18 PDT — session resumed post-reboot (Amber macOS 26.6), log opened at first
post-resume work. Prior logs (08-09, 08-10) were closed out at stand-down; this is the new day's log.

---

## Post-reboot restore (13:18)

Executed Pard's `docs/notices/post-reboot-nudge-2026-08-11.md` (mediajunkie repo).

**Item 1 — duty cycle re-armed.** `CronList` confirmed **no scheduled jobs** — the pre-reboot
job `da7e9309` died exactly as predicted. Re-armed from the verbatim schedule recorded in
`docs/handoff-lead-2026-08-11.md` §8c → new job **`2a4809de`**, `17 6,9,12,15,18,21 * * *`.
Re-verified with a second `CronList`: exactly one job, matching expression. The handoff record
is what made this mechanical instead of a reconstruction.

⚠️ New 7-day expiry ~2026-08-18. The row's pre-reboot `AUTO-EXPIRES ~2026-08-12` flag is
superseded; pre-reboot expiry dates across the session-scoped cohort are all stale now.

**Registry cleared, condition-first.** `dev/active/duty-cycle-registry.tsv:81` was parked with an
explicit falsifiable condition ("clear ONLY when CronList shows exactly one armed job … never on
intent"). Condition observed before the note came out, not after. Two push races with other
resuming agents — rebased and retried both times; pushed at `27cd5d870`.

**Item 2** — N/A. Piper Morgan's eleven are already accounted for as session-scoped `CronCreate`.

**Item 3 — first tool call: no prompt.** First post-resume call was `Bash`/`cat` on the notice
itself; returned contents with no trust or permission dialog. Every call since — `CronList`,
`CronCreate`, git reads, and a `git push` to `main` — also unprompted. Caveat reported honestly:
a `/compact` ran immediately before that call, so this is the first *post-compaction* call; if the
resume emitted a prompt before that boundary I can't see it. Several PM seats will share that shape.

Replied to Pard: `docs/mail/memo-lead-to-pard-post-reboot-mechanism-and-first-tool-call-2026-08-11.md`
(mediajunkie, pushed `ae5f42f`).

## State carried in from the stand-down

Unchanged from `docs/handoff-lead-2026-08-11.md` — nothing has moved while the host was down:

- 🔴 **Ninth cut still staged and unshipped** at `31a09b331` (#1589 calendar-greeting honesty,
  #1590 default-repo read-time recovery). Awaiting PM's deploy word. Deploy procedure and the
  "verify with `fly status`, not a bare curl" warning are in handoff §3.
- Inbox holds three items, two carried from stand-down (PPM empty-standup gap — substance already
  on #1591; PA casing-variant, cohort lane) plus a new one from Docs (role-portfolio/environment
  status stale). To drain on the next fire.
- Next build work: Understanding-Layer Inversion Phase 1 (quality-banked to a fresh session — that
  session is now available), then #1572, then #1423/#1522/#1592.

---

## Fire 1 — 15:31 PDT (WORK)

Cron healthy (`2a4809de`, one job). Synced, drained inbox to empty.

**Docs' two stale-doc flags — both refreshed, same fire.** Docs correctly declined to rewrite
either (doesn't own the narratives) and said "not urgent, when you get to it" — which per standing
convention means sequentially next, not someday.

- `docs/briefing/ROLE-PORTFOLIO-LEAD-DEV.md` §2 — 52 days stale (June/D1, "Beta 0.9 due Jul 4").
  Rewritten to August/Beta-Blockers against **GitHub-verified counts** (MVP 51 open / 1,034 closed ·
  Production 163 · Fast Follow 43), D1 items retired rather than carried, milestone sequence stated,
  each goal keeping the framework's "how I tell I'm moving" line. Front-matter dates updated.
- `docs/internal/operations/environment-status.md` — ~9 months stale (Nov 2025, predates production
  existing). Every cell **re-read live**: dev head `l1466slack`, app 0.8.11.0, DB 18 MB; Fly
  production release v48, machine started, 1/1 checks. Two production cells left explicitly
  **"not verified"** — `fly ssh console -C "alembic current"` was blocked from this seat, and a
  guessed migration hash is precisely what gets carried forward for nine months. An empty cell beats
  a plausible one.

🔴 **What the verification actually caught — the real find of the fire.** Checking the DB-size line
meant looking at containers: `piper-postgres`, `piper-redis`, `piper-chromadb` were **all
`Exited (255)`, six hours after the reboot**, nothing having restarted them. `piper-ghmcp` came back
on its own; the data services did not. Nothing alerts on it — the first symptom would be a
connection error *inside* whatever an agent was already doing, which reads as a bug in that work
rather than a dead environment.

Restarted, verified healthy, documented as a standing post-reboot step, and filed as **#1594**
(milestone Ongoing — dev-env hygiene, not a public-beta gate; first set to Production, corrected
after actually applying the MVP→Production→Fast Follow test rather than defaulting).

The framing worth keeping: **`resumed=24 failed=0 cold=0` is a claim about sessions, not about the
services those sessions depend on.** Different layers; only one was checked this morning (m-43).

**Mail**: replied to Docs (cc PM) with all three findings; moved three read items to `read/`; inbox
empty. Sent via `mail-send.sh` → `1c7ce3ee1`.

**Carried, unchanged**: ninth cut still staged at `31a09b331` awaiting PM's deploy word.

### Fire 1, continued — the closure gap and the untracked epic

**#1573 closed with evidence.** Went to work it as URGENT-small and found it **already built, tested,
and deployed** (`65e1a4222`, 08-10 08:28, ~10.5h before v48 was cut) — the closure step was missed
while the nine-cut run was in flight. Verified rather than inferred: read the diff (4 changes, all
matching the prescribed fix), ran the tests (78 passed in the file; 18 directly on
deadline-proximity/source-failed). Evidence written into the **description**, not just a comment.

The part worth keeping: the fix doesn't just stop the TypeError, it makes the failure **speakable**.
A generic `source_failed` rendered on the floor as a *reminder*-check failure, so a todo-lookup
failure would have been reported as the wrong thing. The todo-specific flag is what lets the floor
say "couldn't check todos" instead of silently reporting none — the #1425 honesty property, and the
part that would have been easy to omit while still closing the crash.

**That one closure was not isolated — swept for more.** 16 open MVP issues are the **subject** of a
`fix(NNNN)`/`feat(NNNN)` commit already on main. Some are legitimately multi-slice (#1423, #1436).
**Did not bulk-close** — dispatched three subagents to verify each against its actual acceptance
criteria with tests run, reporting verdicts back to me. A wrong CLOSEABLE is far more costly than a
wrong NOT-CLOSEABLE: it makes remaining work invisible.

⚠️ **Implication for PM's read of the sprint**: the MVP open count (51) is **overstated by an
unknown amount**. "How much is left" is the number PM is steering on.

**🔴 Filed #1595 — the Inversion epic, which did not exist.** The largest remaining MVP build,
Arch-RATIFIED 08-09 across all four decisions, carrying a **cohort-wide standing moratorium** — and
it had no GitHub issue for three days. Every routing failure filed in that window was tagged "corpus
material for the inversion" and pointed at nothing trackable. Untracked work is invisible work,
*including when it is the most important work.*

Filed from Arch's ruling **verbatim in decisions.log**, not from my own summary — which turned out to
matter (below). Acceptance criteria carry: registry-derived schema (never hand-written); grammar is
canonical actions ~31–38, **not** the 106 rail keys (aliases stay input-side); Haiku-class + enforced
structured output; each of the ~14 AGREE rows cites its own probe row; **per-category** corpus
non-regression, never aggregate; `"what reminders do I have?"` in Exhibit A (Arch's one demand);
every `pin:` row in the gate; Phase-3 ratchet asserts corpus non-regression alongside shrink;
pattern→corpus-case conversion is a **step in** the deletion procedure, not an intention.

**⚠️ Corrected my own carry-forward in the process.** My 08-10 note said the floor-honesty
fabrication cases should ride the inversion's judge corpus "rather than building a second
instrument." True, but it flattened Arch's ruling and reversed the emphasis: Arch ruled #1517
**DECOUPLED** — a trust/safety defect, not a routing defect, reproducing whenever the floor is
reached however routing got there. *"Coupling it to a month-long rebuild leaves a live honesty defect
waiting on an architecture bet."* Spec now, ship against the **current** floor; the inversion adopts
rather than contains it. **Reading my own summary instead of the ruling would have parked a live
honesty defect behind a month-long build.** Recorded in the carry-forward as a standing correction.

Sprint field on #1595 left **unset deliberately** — Sprint changes are PM-gated and I won't infer one
even for the MVP spine. Milestone MVP + board add done.

### Fire 1, continued — the two things I owed Exec

**`docs/internal/operations/failure-class-vocabulary.md` (v1).** 16 product failure families +
4 process families, each citing the instance that earned it, consolidated from five audit docs,
PM's live testing, and the fortnight's memos. Every one had been named repeatedly by several
roles and written down in one place by nobody — which is *why* the new-class contract was
uncomputable, not because the classes were unknown.

Leads with the meta-pattern three audits reached independently in their own words: *"one mechanism
behind 6 of 10 findings"* · *"one missing value, five improvised clocks"* · *"one defect wearing
eight numbers."* When findings cluster, the default hypothesis is **one mechanism wearing N issue
numbers**, not N bugs — which bears directly on the rate, since counting a cluster as N inflates it.

**`scripts/discovery-rate.py` now computes new-class rate — and its first run caught me faking a
measurement.** My initial version printed `(all previously named)` for weeks with **zero tagged
issues**. That is a false clear, and it is precisely **class 5 of the vocabulary I had just
finished writing** — an all-clear emitted identically whether the check found nothing, measured the
wrong object, or never ran. I wrote the class and built it into the instrument inside the same hour.

Caught by **running** it, not reading it. Same lesson as the hooks investigation and the runbook's
six described-not-run steps. Reported to Exec rather than quietly fixed — this failure mode is
plainly not one that knowing about prevents.

Fixed: every week prints its own coverage; an untagged week reads `NOT MEASURED` with its
denominator. Honest current state — **1 of 190 issues tagged (0%)**; not back-filling, because a
class assigned retroactively by the person who wants the curve to bend is not evidence. Flagged to
Exec that Sep 1 is three weeks out and forward-only tagging makes for a thin window — better said
now than delivered as a sparse curve on the day.

Memo sent to Exec (cc PM, PPM) → `5b6076469`. Heartbeat: WORK.

**Sequencing note (explicit, not a vague defer):** Inversion Phase 0 is next in the queue and is
now trackable as #1595. Holding it until the three closure-verification agents return, because
16 individual close/don't-close judgments need real attention and Phase 0's corpus baseline needs
uninterrupted attention — doing them interleaved would do both badly. This is ordering within the
fire, not deferral past it.

### Fire 1, continued — closure sweep results, and two escalations

Three agents returned. **The caution was justified**: of 16 candidates, only 9 were genuinely
closeable. A bulk close would have hidden live defects and a beta blocker.

**Closed with evidence in the description (9)**: #1573, #1507, #1508, #1518, #1529, #1532,
#1541, #1558, #1560. Each verified independently — pins **falsified** where they existed
(#1508's `require_admin` stripped in-memory to confirm the test fails by name; #1558's AST pin
re-tested with the `pop` reintroduced), load-bearing claims re-checked rather than accepted
(#1558's safety rests on the env fallback being system-only — confirmed at
`config_service.py:156` with both branches gated), and exclusions verified (#1532's "demo-only"
caller sits behind a router mounted **nowhere**).

**Held open, with findings recorded (7)**:
- **#1411** — PM's own gate (*"stays open until the pair works for a user"*) unmet; last verdict
  was "initial fail. partial pass," and no cut has happened since the follow-up fix merged.
- **#1431** — **reproduced the defect myself at HEAD**: `"show me my archived projects"` →
  STATUS/`get_project_status` at 1.0. The **`me` token is the discriminator** (drop it → PORTFOLIO,
  works). #1570's rail key can't help; surface 1 claims it before any action is emitted.
- **#1485** — all four ACs met, blocked by the `is_admin` gap below.
- **#1480** — the client-side half is verified by **grepping the JS file**; the JS never executes,
  and that is the exact line that carried the bug. Wrong layer for the failure mode.
- **#1423 / #1436** — genuine multi-slice (~17 of 66 sites; 15% of the mypy census retired).

**Filed (8)**: #1594 (docker post-reboot), #1595 (Inversion epic), #1596 (floor amnesia, split
from #1529), #1597 (live-verification backlog), #1598 (ungated metrics routes), #1599 (is_admin
blocker), #1600 (CI red), #1601 (`str(None)` → `"None"`).

**MVP open: 51 → 48.** The number barely moved; the composition changed substantially.

#### 🔴 Two escalations to PM (STOP condition + beta blocker)

1. **Enforcement ratchets red on `main` for two days** — verified locally: `silent_death_core`
   225 vs 209, `todo_markers` 73 vs 71, both `@pytest.mark.smoke` (**gating**). Architecture
   Enforcement has failed on every push since 08-09 15:07; mypy gate 17 over across 5 codes.
   The ceilings weren't raised — they were **passed, repeatedly, while the workflow read red and
   the signal was absorbed.** That is the ratchets' own failure class happening to the ratchets,
   and a guard red on every push trains people to skip it. **Two of the 16 new silent-death sites
   came from #1570's own commit.** → #1600, PM's call on priority.
2. **`is_admin` is set on nobody** — verified directly: **1377 users, 0 admins**; no
   `%xian%`/`%pipermorgan%` account; migration seeds `xian@example.com` (a placeholder); no code
   path sets it. #1485/#1508 are *correct* fixes that converted a dormant documented gap into a
   live block — **PM cannot save the Slack app token** (#1201) with beta Slack inbound on. → #1599.

#### Corrected our own record before PM retests

#1570's comment promised *"'show me my archived projects' returns archived rows"* after the next
cut. **False** — the commit never claimed it; the verification bullet did. Corrected on the issue
with the working phrasing. A stated retest step that fails spends PM's time and reads as a
regression rather than a scope boundary — the fabrication class appearing in our issue record
rather than in the product.

Escalation memo → PM cc Arch/Exec/PPM (`e9744dfe4`). Carry-forward updated.

### Fire 1, continued — NINTH CUT DEPLOYED (PM's word, 16:0x)

`fly deploy` from the shared checkout at `ad8c079e8`. **Fly v49**, machine `2869e7ec495248` on
version 49, `started`, 1/1 checks passing.

**Checked what was actually shipping before deploying**, since HEAD had moved well past the staged
`31a09b331` during the day: `git diff --stat 31a09b331..HEAD -- services/ web/ templates/ alembic/ main.py`
→ **zero application-code changes**; only three helper scripts (`discovery-rate.py`, `scan-inbox.py`,
`sprint-truth.py`), none of which the running app loads. So v49 ships exactly the ninth cut's
application behavior — worth verifying rather than assuming, because "deploy the ninth cut" and
"deploy HEAD" had drifted apart by ~9 hours of commits.

**Verified at the right layer, in order**: ancestry (`merge-base --is-ancestor` → OK) → `fly releases`
(v49 complete) → `fly status` (machine on 49, checks passing) → *then* an app-level check
(`/health` 200 returning real JSON with `intent_service: healthy`; `/` 302 to login as expected).
The bare curl came **last and as corroboration**, never as the completion signal — that ordering is
the 08-10 near-miss lesson.

⚠️ One thing worth recording: the deploy emitted `WARNING The app is not listening on the expected
address` mid-roll, listing only `/.fly/hallpass` as bound. It resolved by the time checks ran, and
the proxy now routes real requests successfully — so it was a bind-timing artifact, not a defect.
Noting it because the warning text is alarming and will recur; the discriminator is whether the
proxy actually serves afterwards.

Retest notes posted to #1589 and #1590 with the specific user path (start a fresh conversation).
