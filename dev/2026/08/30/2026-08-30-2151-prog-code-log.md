# Session Log — 2026-08-30 21:51 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1660 — handle_analyze_document returns key_findings from recommendations (always empty)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)

## Work

### #1660 fix — real key_findings surface; empty findings render honestly

**Claimed vs found**: exactly as the issue and the 1659 lane's report described.
`services/intent_service/document_handlers.py` upload branch read
`analysis_result.recommendations` (hardwired `[]` on every happy path in
`DocumentAnalyzer.analyze()` / `analyze_text()` / `_analyze_plain_text_bytes`)
under the name `key_findings`; the real findings in `.key_findings` were
dropped. The #1657 artifact branch already read the right field. On the
analyzer's two honest-failure paths (unsupported type, PdfReadError) BOTH
fields are non-empty — so the old code showed the recommendation mislabeled
as a finding and lost the actual finding.

**Fix** (`services/intent_service/document_handlers.py`):
- Upload branch: `key_findings` ← `.key_findings`; `recommendations` surfaced
  as its own key (non-empty only on honest-failure paths — not silently
  dropped, not mislabeled).
- Artifact branch: `recommendations` key added for response-shape parity.
- `handle_summarize_document` `format=detailed` (#1648 honesty): findings
  present → real "Key Findings:" section; genuinely empty → "No key findings
  were extracted from this document." — never an empty header dressed as
  content. Failure-path recommendations render under "Recommendations:".
  Reads via `.get()` so pre-1660 analysis dicts (e.g. the 1624 rail suite's
  mock) don't KeyError.

**Tests**: new `tests/unit/services/intent_service/test_analyze_findings_1660.py`
(7 tests: planted-marker content flow at the handler boundary per the
1659/1661 idiom, both-fields-honest on the failure path, artifact-branch
shape parity, detailed-render honesty x4). One fixture line added to
`test_summarize_files_view_1657.py` (SimpleNamespace gains `recommendations=[]`
— the real dataclass always has it).

**Evidence**:
- 1660 + 1657 + 1624 rail + analyzer suites: 76 passed
- Both ratchet files (`test_architecture_enforcement.py`,
  `test_completion_ratchets.py`): 49 passed
- `tests/unit/services/intent_service` + `unit/services/analysis` +
  `unit/web/api/routes` + `tests/intent`: 4115 passed, 1 failed —
  `test_bypass_prevention.py::test_middleware_is_registered`
  (exempt_paths 13 != 12), **pre-existing**: A/B/A stash run fails
  identically without my diff; already tracked as #1637.

**Discovered issues filed**: None (the one failure found is pre-existing and
already tracked in #1637).
