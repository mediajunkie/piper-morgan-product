# Session Log — 2026-08-30 16:29 — Coding Agent (prog), delegated by Lead

**Role**: Coding Agent (prog) — #1699 calendar-adapter eager-construction surgery (Arch-ruled)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)
**Distinct delegation** from today's Batch-2 (0638) and Batch-3 (1240) prog sessions — new session, new log per the one-log-per-delegation precedent.

## Mandate

#1699: stop the calendar adapter's eager construction of the legacy MCP sim stack.
`GoogleCalendarMCPAdapter.__init__` unconditionally built `MCPConsumerCore()` (service
discovery + connection pool) while the adapter's own `_server_params_for` raises
NotImplementedError (#1220's open decision) before any of it could run. Arch's ruling
(mailboxes/lead/read/ruling-arch-to-lead-cc-pm-batch2-both-questions-answered-2026-08-30.md §1):
lazy-construct or remove outright; whole scope, nothing more. Do NOT touch #1220's decision
space, `_server_params_for`, USE_SPATIAL_CALENDAR's default, or services/standup/.

**HARD requirement** (Lead's second 1699 comment, proximity correction): the standup calendar
assembly path (services/standup/assembler.py:214 → calendar_integration_router → spatial leg)
is PM-live and PM-tested — pin it byte-identical before/after, before-pin written FIRST.

## Work record

- **16:29** Session start. Synced worktree (`git pull --ff-only`, was 17 behind, now at
  origin/main tip 0deca950d). Read issue 1699 + both comments, Arch ruling §1, router,
  adapter, consumer_core, assembler, mcp package inits.
- **Fresh-sweep findings** (verify-first):
  - `adapter.mcp_consumer` is a WRITE-ONLY attribute: assigned at google_calendar_adapter.py:192,
    read by NOTHING anywhere in the repo (only other `MCPConsumerCore()` constructor site is
    scripts/demo_mcp_consumer_github.py building its own instance). github_adapter.py has no
    MCPConsumerCore reference at all — the calendar adapter was the sim stack's last live
    constructor, exactly as mcp_client.py's census note said.
- **Surgery shape chosen: REMOVE OUTRIGHT** (Arch allowed either). Rationale: zero readers
  means lazy construction would preserve an attribute nothing ever accesses — more code to
  keep an unreachable path warm. Removal is the smaller diff: the import line + the
  assignment line, replaced with a dated tombstone comment.
- **Before-pin (written FIRST)**: scratchpad script exercising the REAL chain
  StandupAssembler._append_calendar_events → StandupCalendarProvider → real
  CalendarIntegrationRouter → real GoogleCalendarMCPAdapter._fetch_todays_events →
  _process_event, with ONLY the two external boundaries faked (Google API client object at
  `adapter._service`; `_get_user_timezone` → fixed tz — layer named per m-43). Deterministic
  fixtures (2030 event times, fixed now_epoch, empty sources). BEFORE payload sha256
  `a8774615275f0452cfe54f3f8b05e5d21da9af22a322ba48222daa7a9d629c0c`.
- **Before-spy**: MCPConsumerCore.__init__ spied at definition site — 1 construction per
  router build, 3 across the full standup pin path (every standup assembled the sim stack).
- **16:34 Surgery**: removed `from .consumer_core import MCPConsumerCore` +
  `self.mcp_consumer = MCPConsumerCore()` from google_calendar_adapter.py (tombstone comment
  in place); same-commit doc-sync of mcp_client.py's historical note (it said the stack
  "survives only via GoogleCalendarMCPAdapter's constructor plumbing" — now records the 1699
  removal; stack is import-reachable only, constructed by nothing at runtime).
- **After-proof**:
  - Byte-identical: AFTER payload sha256 identical
    (`a8774615…c0c` in-process; `0da3769cbb46d6b177831df4e49447aeb5b8a953b172425f838ec87676eb9a87`
    file-level with structlog preamble stripped — the raw stdout files differ only in two
    wall-clock structlog config-loader lines, not calendar-path output).
  - Constructor spy: 0 after router import+construction, 0 after full standup pin path.
  - NotImplementedError path unchanged: `_server_params_for` still raises with the #1220 text.
  - Spatial leg still live: router.spatial_calendar is a real GoogleCalendarMCPAdapter.
- **Regression test committed**: tests/unit/services/mcp/consumer/test_no_eager_sim_stack_1699.py
  (2 tests: adapter init and router init construct zero MCPConsumerCore — definition-site spy,
  catches any import shape). 2 passed.
- **Suites** (env-strip + POSTGRES_PORT=5433 + `-m "not llm"`):
  - Both ratchet files (test_architecture_enforcement.py + test_completion_ratchets.py): 49 passed.
  - tests/unit/services/intent_service: 3588 passed, 3 deselected.
  - tests/unit/services/standup + tests/unit/services/mcp: 587 passed.
  - Calendar + standup surface suites (integration/test_calendar_integration.py,
    unit/services/test_calendar_router_userid_threading.py, security/test_integration_flow_isolation.py,
    unit/web/api/test_calendar_status_scoped_key_1434.py, setup/settings calendar routes,
    test_standup_today_1269.py, test_morning_standup_structured_items_1034.py): 95 passed.

## Discovered work

- None filed. (Checked github_adapter for the same eager-construction pattern — clean.
  The sim stack modules themselves remain, as mandated: their disposal is a later,
  separately-gated step now that nothing constructs them.)

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-strip prescription, sync-before-work discipline, m-43
  name-the-layer, bare-issue-number commit rule); issue 1699 + comments (scope + HARD pin);
  Arch ruling memo §1 (surgery scope + escape hatch); mcp_client.py census note (last-constructor
  claim, verified fresh).
- **Loaded but not referenced**: memory index (no memory files opened).
- **Wanted but not found**: none.
