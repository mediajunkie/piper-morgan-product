# Session Log — 2026-08-30 06:38 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Task**: Execute Batch 2 of Arch's 2026-08-29 disposal routing — the census's dead families.
Families per Lead's delegation: straight-delete family, narrative_bridge triplets (3 packages),
whole-package dead (queries, debugging, editorial, analytics, user, project_context), legacy MCP
simulation stack, remaining spatial-adapter MCP consumer twins (incl. the flagged orphan
`services/integrations/mcp/gitbook_adapter.py`), web/ root scratch files.

## Sources read (all binding)
- Routing memo: `mailboxes/lead/read/routing-arch-to-lead-cc-pm-disposal-spatial-plus-census-dead-families-2026-08-29.md`
- Census (incl. correction block + caveats): `docs/internal/architecture/reviews/2026-08-architectural-review/findings/leg-b-live-state-census.md`
  — census is caller EVIDENCE, not a skip-verification pass; fresh sweep per module is the bar
- Batch 1 template: commits facab7028..519334cb4, epic 1698, decisions.log 2026-08-29 ~19:0x entry,
  session log `dev/2026/08/29/2026-08-29-1838-prog-code-log.md`
- Skill: `.claude/skills/delete-module-safely/SKILL.md` v1.0
- PM condition (verbatim, rides every disposal commit): "every disposed module must remain findable
  by commit-hash reference in the disposal record (not kept in the live tree) so a future
  investigation into why an approach was tried or abandoned can still locate it — this is a
  standing requirement on the disposal, not just on the original nine."

## SCOPE GUARD (STOP if sweep implicates)
KEEP: place_service, place_detector, spatial_intent_classifier, github_spatial,
home_state_service, spatial_adapter base class, slack spatial_adapter/spatial_types (socket path).
Also HELD from Batch 1 (untouched): services/mcp/consumer/slack_adapter.py.
STOP on: any live importer found; any module whose fresh sweep contradicts the census premise.

## Work
- Fire start (06:38) — synced 0-behind origin/main; read memo, census (incl. correction block),
  Batch 1 commits + log + skill; epic 1698 reachable via gh.
- Baselines (06:4x): collection 13,678 / 0 errors (matches Batch 1 end state); CI-replica mypy
  venv rebuilt (py3.11.15 + mypy 2.3.0 / sqlalchemy 2.0.23 / pydantic 2.12.5 / fastapi 0.115.14)
  in scratchpad; local --measure baseline captured + raw 1,895-line error list for per-file
  attribution. Local platform skew IDENTICAL to Batch 1's finding (arg_type +1, assignment −1,
  misc −1, truthy_function −1 vs ceilings on the untouched tree) — deltas are the measure, CI
  authoritative on absolutes.
- **Family W (06:5x) — DELETED web/ root scratch (16 files, ~2.3K lines, non-Python)**:
  app.py.backup-personality, debug-markdown-flow.js, the 11 test-*.js harnesses,
  markdown-flow-analysis.md, REFACTOR_TDD_TODO.md, REFACTOR_TODO.md.
  Sweep: repo-wide filename grep (all surfaces incl. web/templates, web/static, package.json,
  tests/frontend, .github/workflows, scripts, Makefile): 0 live referents for all 16; the only
  live-doc referent was technical-spec.md:53 naming two scratch harnesses as the UI test
  coverage — corrected same commit (real coverage lives in tests/frontend/unit/).
  bot-message-renderer.js confirmed LIVE (chat.js, frontend unit test) and kept — census's "12
  loose .js" excluded it, sweep agrees. Design record: NOT needed — these are scratch harnesses
  and TODO docs for the 2025 response-path unification refactor that SHIPPED (live
  bot-message-renderer.js + tests/frontend/unit/); the .md analysis diagnoses a since-fixed
  regex. Ratchets: detectors scan production .py only — no counter movement possible; verified
  collection 13,678 unchanged. No mypy impact (non-Python). Commit b45631ddb (prior art
  e3f61e6b2, last tree containing the family).
- **Family S (07:0x) — DELETED the remaining spatial MCP consumer twin**:
  services/integrations/mcp/gitbook_adapter.py (317 LOC, GitBookMCPAdapter), the orphan Batch 1
  flagged (its sole importer intelligence/spatial/gitbook_spatial died in 2d2ae3b0f).
  Sweep: absolute/relative import styles + class name + bare module name over
  services/web/tests/scripts/main.py/alembic/config/.github — 0 importers, 0 test referents,
  0 CI/config referents; integrations/mcp/__init__ exports nothing (one comment line).
  TokenCounter (its import) confirmed LIVE elsewhere (mcp/consumer/notion_adapter, 9 call
  sites) — no new orphan. BaseSpatialAdapter base class untouched (keep-list).
  Design record: covered by Batch 1's existing record (GitBook column in the per-connector
  8-dimension table); gitbook-integration-plan.md's 2026-08-29 dated note already names "the
  GitBook MCP adapters" as disposed — no doc edits needed.
  Mypy deltas measured two independent ways (post-minus-baseline AND direct per-file
  attribution on raw baseline) — identical: override −5, union-attr −1; no other code moved.
  Ceilings lowered same commit (override 22→17, union_attr 166→165).
  Collection 13,678 unchanged, 0 errors. Commit 7823f5d44 (prior art b45631ddb).
- **Family M (07:1x) — STOP, not cut: legacy MCP simulation stack.** Fresh sweep contradicts the
  census's "no live caller": services/integrations/calendar/calendar_integration_router.py:73-76
  (LIVE, USE_SPATIAL_CALENDAR=true default) constructs GoogleCalendarMCPAdapter, whose __init__
  (google_calendar_adapter.py:192) unconditionally runs MCPConsumerCore() — which constructs
  MCPServiceDiscovery() + MCPConnectionPool() (consumer_core.py:33-34) and imports
  MCPProtocolClient. The stack is live-CONSTRUCTED on the default calendar path, not merely
  loaded. Corroborating prior art: services/mcp/__init__.py:10 already records client.py "HELD
  pending the connection_pool->adapters->spatial cascade ruling (protected-adjacent)". Deleting
  requires surgery on live google_calendar_adapter (its dead-MCP plumbing, #1220 territory) —
  a fix-or-delete ruling beyond this disposal mandate. Batch-1 slack_adapter precedent.
  Also noted: mcp/consumer/mcp_client.py is LIVE (real SDK transport, #1220 — NOT part of the
  sim stack) but its docstring claims the sim stack "stays live in services/queries/
  query_router.py", a file that no longer exists — correcting in the queries-package commit.
- **Family P (07:2x-…) — whole-package dead (queries, debugging, editorial, analytics, user,
  project_context).** Shared sweep evidence: 0 CI/config/workflow referents for all six; 0
  dynamic-import/string-dispatch hits; 0 TODO/FIXME markers in any target; per-file mypy
  attribution from raw baseline: project_context call-arg 1; slack_inspector attr-defined 2;
  slack_monitoring var-annotated 1; all other targets 0.
  - P1: analytics (__init__-only, 1 line) + user (__init__-only, comments) + editorial
    (calendar.py 90 + draft.py 142 — #998 compose-UI scaffolding; #998 is CLOSED and no compose
    route exists in web/): 0 importers each (abs/rel/function-local/class+function names:
    parse_draft, write_draft, resolve_draft_path, list_drafts_needing_finishing swept
    repo-wide — only self-hits). Collection 13,678 unchanged post-cut. Commit 5be9c17e1
    (prior art 7823f5d44).
  - P2: queries package (conversation_queries 238 + project_queries 89 + __init__) + the 3
    orphaned QueryRouter config files (feature_flags/query_degradation_rollout.json,
    development+production query_router_degradation.env.template — 0 readers swept) — QueryRouter
    itself long-deleted.
    Production importers: 0 (confirmed census). Test importers: 3 files —
    (a) test_project_queries_owner_scoped_1501.py: DEDICATED (4 tests), rides. ⚠️ FLAG for
    Lead/Arch: 1501 (2026-08-09) owner-scope-hardened ProjectQueryService as part of the
    principal-keyed-reader sweep — recent security investment in a dead module. The pattern
    enforcement survives: test_architecture_enforcement's PRINCIPAL_KEYED_READERS matches by
    attr name and covers the LIVE readers (PortfolioService, ProjectRepository); its violation
    counting is unaffected by deleting these conformer call sites (verified: unscoped 54 /
    repo 36 unchanged post-cut).
    (b) test_temporal_context_integration.py: MIXED (#1452 wave-5 rewrite framed the service
    as "LIVE system" without a caller check — the handlers are live, the service is not) —
    SURGERY: excised TestTemporalContextService (5 tests) + service fixture + unused datetime
    import; kept TestCanonicalHandlersTemporal (live subject); 2/2 pass post-surgery;
    docstring records the excision.
    (c) test_temporal_context_standalone.py: PM-034-era standalone scratch runner (0 collected
    items, redundant to the integration file) — rides.
    tests/archive/ importers left untouched (excluded from collection via --ignore, already
    reference the long-deleted query_router — quarantined surface).
    Rider: mcp/consumer/mcp_client.py stale docstring corrected (claimed sim stack "stays live
    in services/queries/query_router.py" — census-flagged fabrication; file is LIVE, doc-only
    edit).
    Collection 13,678 → 13,669 (exactly −9: 5 excised + 4 riding). Detectors 198/54/36
    unchanged; mypy attribution 0 for all deleted files. Commit 9c915a18b (prior art 5be9c17e1).
  - P3: project_context package (project_context.py 86 + exceptions.py 10 + __init__ 12 —
    PM-009-era project resolution, never wired; the LIVE ProjectContext in domain/models.py:552
    is a DIFFERENT class, name-collision trap noted in the spec correction).
    Importers: scripts/workflow_reality_check.py (import was UNUSED — its own line 62 says
    "Skip ProjectContext for now"; import excised, script compiles) +
    tests/domain/test_project_context.py (DEDICATED, 6 tests — rides).
    Riders: technical-spec.md dated notes on §2.7 (project_context) AND §2.6 + §3.2 (the
    queries/QueryRouter sections — P2 subjects whose spec staleness surfaced during this doc
    sweep).
    Collection 13,669 → 13,663 (exactly the 6 riding tests). Mypy: call-arg −1 (direct
    attribution: project_context.py:46); ceiling 40→39 same commit. Commit aa516f4b1 (prior
    art 9c915a18b).
  - P4: debugging package (commands.py 200 + slack_inspector.py 484 + __init__) + RIDER
    services/api/slack_monitoring.py (its SOLE importer — census-dead itself: router never
    mounted; verified against the string-registration layer too: RouterInitializer.mount_router
    call sites in app.py/startup.py have no slack_monitoring string; the only /api/v1/slack
    referents are the LIVE slack plugin router prefix and a path-match line in
    enhanced_error_middleware that also serves the live plugin — left alone).
    slack_inspector's upstream deps (observability/slack_monitor, infrastructure/task_manager)
    LIVE elsewhere — no new orphans. 0 test importers, 0 dedicated tests.
    Design record: NOT needed — PM-078 diagnostic tooling whose design spec is preserved
    verbatim in the pm078 plan doc (now carrying a dated disposal note).
    Mypy: attr-defined −2 (slack_inspector.py:234,239), var-annotated −1
    (slack_monitoring.py:330) — post-minus-baseline matches direct attribution exactly, no
    other code moved; ceilings 204→202, 80→79 same commit.
    Collection 13,663 unchanged. Commit 6729d3952 (prior art aa516f4b1).
  - Family-P gate run: TestPrincipalThreadingGuards1532 guard-1 shrink-lock FIRED on P3's
    deletion (ProjectContext.resolve_project's 2 allowlisted unthreaded reads left the set —
    the allowlist comment predicted exactly this: "Direction is DOWN … as those die").
    ALLOWED_UNTHREADED tuple removed + MAX_UNTHREADED_PRINCIPAL_READS 3→1. Landed as a
    follow-up commit attributed to aa516f4b1 rather than an amend — the later commits'
    messages already cite earlier hashes and the disposal record's hash-stability warning
    forbids rewriting. Suites 51/51 green after.
