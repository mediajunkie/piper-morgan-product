# Session Log — 2026-08-30 18:48 — prog-code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1659 — non-PDF uploads unsummarizable (DocumentAnalyzer ran pypdf on every file type)
**Branch**: claude/lead-cycle (Lead's worktree), commit BARE-referenced, no push (per delegation)

## Work

- Verified what the upload path stores for type: `uploaded_files.file_type` = the request's
  MIME Content-Type verbatim (`web/api/routes/files.py:184`, gated by ALLOWED_MIME_TYPES:
  text/plain, application/pdf, docx MIME, text/markdown, application/json). Storage filename
  is `{timestamp}_{file_id}_{original_filename}`, so the original extension survives in
  `storage_path` — that's the fallback dispatch key for legacy/NULL-file_type rows and for
  existing bare `analyze(path)` callers.
- `services/analysis/document_analyzer.py`: added `classify_document_type()` (MIME →
  extension → %PDF magic-sniff precedence), `extract_document_text()` (shared type-dispatched
  raw extraction, `max_pages` for the PDF cap), `UnsupportedDocumentTypeError`, and a
  `_summarize_text()` helper shared by `analyze_text()` (#1657, behavior unchanged) and the
  new `_analyze_plain_text_bytes()`. `analyze()` now takes optional `file_type`/`filename`
  and dispatches: text → decode + LLM summary; unknown → honest message naming the REAL type
  ("I can't analyze .zip files yet"); pdf → the pre-#1659 pypdf path unchanged.
- `services/intent_service/document_handlers.py`: `handle_analyze_document` passes
  `file_type` + `filename` into `analyze()`; the three inline unconditional-pypdf sites
  (question, compare, reference-in-conversation) replaced with `extract_document_text()`.
- Tests: 17 new tests in `tests/unit/services/analysis/test_document_analyzer.py`
  (Test1659TypeDispatch, Test1659ExtractDocumentText, Test1659Classifier) — planted-marker
  content-flow proof (the #1661 probe idiom, asserted at the LLM-prompt boundary), extension
  fallback, empty file, honest no-LLM fallback, .zip/.docx honest messages, magic-sniff.

## Evidence

- `tests/unit/services/analysis/test_document_analyzer.py`: 33 passed (16 pre-existing PDF
  tests untouched and green — PDF path pinned unchanged).
- `test_summarize_document_rail_1624.py` + `test_architecture_enforcement.py` +
  `test_completion_ratchets.py`: 70 passed (both ratchet files green; no new regex, no new
  dispatch site).
- `tests/unit/services/analysis/` + `tests/unit/services/intent_service/`: 3640 passed.
- document/file/web-api suites (test_document_handlers, test_summarize_files_view_1657,
  test_documents_per_user_key_1185, tests/unit/web/api, file repository/resolver/scoring):
  605 passed.

## Discovered work / notes

- #1660 overlap noted, not touched: `handle_analyze_document` still returns
  `"key_findings": analysis_result.recommendations` (always []) for uploads — the empty-
  key-findings bug is adjacent to but independent of this dispatch fix.
- KG pypdf sites (`ingestion._extract_pdf_chunks`, `processors.PDFProcessor`) and
  `content_extractors.PDFExtractor` are PDF-named/type-scoped by design — out of #1659 scope.
- .docx is upload-allowed but has no extractor; it now gets the honest "I can't analyze
  .docx files yet" instead of the false corrupted-PDF claim. A real docx extractor would be
  new scope (no issue filed by me; Lead may want one).
- Pre-existing untracked `uploads/ac61e65c-…/` dir in the worktree — not mine, left alone.
