# CXO Session Log — 2026-08-30 (Sunday)

**Role**: Chief Experience Officer (CXO) | **Tool**: Claude Code | **Model**: Opus 5 (1M context)
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `8207809c` (rotated from `cd9b3ddc` this fire)
**Started**: 07:17 PT

---

## START (07:17) — both carry-forward priorities executed

**Step 0**: 08-29 closed cleanly at 22:2x. **Sync first, then inbox** (ratified order).

**Priority 1 — cron rotated, as flagged**: `cd9b3ddc` was ~1 day from expiry. Delete-then-create,
`CronList`-verified exactly one job. **New id `8207809c`, expiry ~09-06.** Third clean proactive rotation.

## Owned a real error precisely — PPM refuted my #1658 split

PPM checked my proposed three-way unbundling against #1658's actual text and **it doesn't hold**: PM's
quote covers all three parts under one historical claim (*"the earliest prototype had file upload… and even
the ability to connect a file to a project or to another contextual layer"*), and the issue's class label
is `parity-regression` for the whole umbrella. Arch's test — *did this exist in the running system
yesterday?* — reads **NO uniformly**, not NO-for-two-and-something-else-for-the-third.

**Owned the specific error, not the general one**: the contradicting sentence **was in the text I read
before writing.** I didn't lack evidence; I reached a conclusion the evidence I had ruled out. **Compounding
second-order error, the more reusable lesson**: I read the body **truncated to 700 characters** when the
whole thing was two paragraphs — which also cut the `Class:` label PPM cites. I have spent this month
telling other people that acting on a fragment produces confident wrong work; it did. Arch's ruling stands;
PPM is carrying forward the one separable point (disclosure at PUB-scoping time) on their framing, not mine.

*(Also: CIO retracted Saturday's heartbeat "finding" — Exec caught that `cohort-position.sh` read heartbeat
data as primary, which is deliberately sparse for busy roles, so the busier a role was the staler it
looked. My heartbeat was fine. CIO named it plainly as a finding about the instrument, not about me, and
shipped a fix + regression test. No action needed.)*

## Priority 2 — the ESSENCE v0.1 trifecta response, delivered

The deferral I named Saturday ("a document defining what Piper Morgan *is* deserves a full pass, not a
same-evening reaction to the two sections touching my open work") — discharged. **Read all 125 lines.**

**CONCUR on the document as a whole**, with **one challenge and two amendments**:

⭐ **The challenge is the real contribution: commitments 3 and 6 are in structural tension, and both are
marked load-bearing.** #3 names the proactive daily ritual as *"the specific behavior that converts 'a
chatbot I visit' into 'an assistant that works for me.'"* #6 routes all delivery to MCP. **📄 PDR-005:258,
ratified: MCP is structurally request-response only — no affordance for Piper to initiate a turn;
"proactively surface insights" is *structurally impossible* on MCP.** Today it's masked because the standup
runs on web-chat — which went to maintenance mode *the same day this document was ratified*. So all new
build goes to the one surface that structurally cannot perform commitment 3's ritual. **Verified the
PDR-005 line at source (line 258) rather than citing from my own taxonomy doc's quotation of it.**

Offered three resolutions with a stated lean (qualify #3 as surface-bounded, with the MCP form being the
**response-shaped variant** — 📌 PM's own greeting-variant addition to the FTUX model, generalized one level
up) while explicitly leaving the call to Arch/PM. Same discipline PPM used on #1658 — name the tension,
don't pick the answer.

**Amendment 1** — "colleague" is in the headline and **no commitment cashes it**; the six cover what it
accumulates/operates on/when it appears/that it doesn't lie/how it routes/where it arrives. ⚠️ **Flagged my
own conflict of interest explicitly**: proposing a seventh commitment that happens to be my lane, in a
document whose purpose is to constrain scope, is exactly what PM's no-optional-complexity lens should be
suspicious of — so I offered *either* adding it *or* narrowing the headline, and said I don't mind which.
Naming the suspicion myself beats having it named for me.

**Amendment 2** — first contact is the **named first build increment** (*"cold-start reflection first"*) and
appears in no commitment; asked for the smallest possible edit (a clause in #3), not a new numbered item.

**Denominator stated**: one reader, one pass, experience lens; verified the PDR-005 claim at source;
explicitly did **not** verify the Leg B census numbers or the flip-1 deployment claim — outside my lane and
not implied.

**Discovered issues filed**: None.

---

## Fire 2 (10:19) — inbox drained, a tracker corrected, and a ratified pre-user gate got its instrument

**Step 1**: `CronList` → exactly one job, `8207809c`. **Step 2**: sync before mail (PM-ratified order),
worktree/branch pairing correct.

### Three memos in, all in lane; two produced work

**Lead's #1635 correction was right and incomplete, and I verified it before agreeing.** Checked
`588f6aad1` myself: dated 08-28 09:08, **ancestor of `origin/main`**, both of my binding rules implemented
*structurally* rather than by convention (Rule 1's suppression lives in the branch condition; Rule 2's copy
verbatim in `_coming_soon_entity`). Cited the v64 deploy-ancestry as **Lead's** check, not mine.

⭐ **What Lead's correction missed: PPM's mis-census had TWO causes and killing one leaves the other
loaded.** Lead named the m-43 half (existence is a claim about the deployed artifact, checkable by
ancestry, not by looking at one dashboard whose state may legitimately suppress the feature). **The
unnamed half: the issue title still read *"shape undecided"*** — the top line anyone sweeping a 46-item
backlog reads. Corrected the title, commented the shipped-vs-remaining split. The sharp version: **had PPM
run the ancestry check Lead recommends, they'd have found the card AND an issue asserting its shape was
undecided — the verification would have produced a contradiction, not an answer.**

⚠️ **And the designer-side half, which is mine and which I under-shipped.** Rule 1 makes this feature
*legitimately invisible* under exactly the state a checker is most likely to be in — an empty Radar. **A
conditional-visibility rule buys honesty at the cost of observability**, and I handed over the rule without
the consequence. **Standing correction adopted**: any design of mine that suppresses a feature under a real
user state ships the census caveat *in the design memo*.

**ESSENCE — sent Arch a short note, not a third response.** My challenge (commitment 3's proactive ritual
vs. commitment 6's MCP, which PDR-005:258 says cannot initiate) and PPM's amendment (board milestones the
MCP epic as *Production* while the doc calls it the present-tense build surface) **share one root: ESSENCE's
MCP sentences are present-tense about a future MCP** — capability in my case, schedule in PPM's. Noted this
is the same shape as my own `experience-across-surfaces.md` §3 tense caveat, which PM has ratified.
⚠️ **Stated the reason to discount it**: PPM read my response first and says so, so this is **not**
independent convergence (m-45). Asked it be counted as one observation with two instances, one of them mine.

### The substantive work — #1463, a gate that stopped being backlog this week

**Verify-first paid twice**: the gate already had an issue (**#1463**, my standing-items B′) so I filed
nothing new; and PDR-006:35 turned out to name it as **one of exactly two open pre-user gates**, alongside
#1458. ESSENCE then made the MCP path the surface *all new build goes to*. **The gate now stands in front
of the only lane that is moving** — that's what moved this off the backlog, not my own sense of priority.

Shipped `docs/internal/testing/byoc-recomposition-rubric-v0.1.md`, following the **ratified**
Branch-or-Anchor discipline and the UI Lifecycle Rubric worked example (shape preserved: 0–3 × 3, ≥7/9,
auto-fail; meanings branched with provenance):

- **R → Sufficiency.** Salience is scored, because on this surface Piper's judgment about what matters is
  in the payload or nowhere — the sentence that used to carry it is written by someone else.
- **C → anchored** in meaning (recorded as a decision, not an omission), **branched in evidence standard**:
  CT wants context "visible in the response," and visibility here is the host's call.
- **T → fully branched.** CT's Tone is unscoreable — voice is the host's. Replaced with
  honesty-under-recomposition. ⭐ **Governing principle: honesty carried in prose is droppable; honesty
  carried in structure is not.** Same move ESSENCE makes for portability and for honesty, applied to the
  one surface our vigilance provably cannot reach.
- **Capability truthfulness** held as a **gate precondition**, not a fourth axis — it's a property of the
  catalog, evaluated once per tool, and adding it would break the shape the branch discipline preserves.

🔴 **The instrument ships explicitly unfinished, and that is the point.** The T criteria are **hypotheses**
— nobody has tested whether a hedge survives paraphrase. Red banner: **T records `PENDING-PROBE`, never a
PASS, until §6's probe runs.** Shipping an untested criterion inside the instrument built to catch false
clears would have been the third iteration of that shape this month.

**Sequencing ask to Lead, sent because it expires.** PA's 07-30 point still holds — the probe needs a
hedged blob and a client LLM, not `mcp.pipermorgan.ai`, and a negative result changes what the tool layer
must emit. **I checked rather than assumed: #1688 has no build commits yet**, so the ordering is still
available; it closes when tool output is first authored. Gave Lead the prose-vs-structure principle to use
now, **flagged as my hypothesis rather than a finding**, and explicitly did *not* block their build on me.
Probe design carries a **mandatory negative control** (unhedged outputs — if "survives" reads the same both
ways it isn't measuring hedge survival) and a **stated expiry**, because every recomposition claim is a
claim about a third party's current build.

**To PPM**: one discriminator for their ESSENCE amendment — both pre-user gates are milestoned Production,
which is consistent under their reading (a) and requires moving them under (b). Offered as a consequence
that falls out differently by branch, explicitly not a vote.

**Discovered issues filed**: None — #1463 already existed and was found before filing.

### Fire 2 continued — kept draining, and the corpus work turned up a traced finding

The rubric's §6 probe needs a host LLM I can't validly stand in for (I'd be subject and scorer, which is
the confound I'd just written a mandatory negative control against). But **step 1 — build the corpus from
the strings we actually ship, not invented ones — needed no LLM.** Drained it. It paid.

⭐ **Traced the #1425 honesty class and it lives in two halves at two layers** (m-43 — naming the layer
*is* the finding):
- ✅ **Structured half already exists and travels**: `{"source_failed": True}` from
  `first_contact.py:197,214` and `canonical_handlers.py:1650,1656`, merged by
  `context_assembler.py:278,424`. A **field**, end to end.
- 🔴 **Honesty half is a PROMPT DIRECTIVE**: `conversational_floor.py:762,:817,:1078` —
  `lines.append(...)` into our own floor's system prompt (*"say you couldn't check GitHub just now — never
  claim the repo is empty"*).

⚠️ **This changes the question rather than merely supporting my morning hypothesis.** On BYOC there is no
floor prompt, because no model of ours is in the loop — so for this class it is **not** *"will our hedge
survive paraphrase?"*; **there is no hedge in the payload to survive.** The honesty is an instruction to a
model that doesn't exist on that surface.

**Scoped it carefully rather than alarmingly, and checked rather than assumed**: nothing is lost and nobody
erred — the hosted server isn't built, and `services/mcp/` today is the **client** family (PDR-006's own
conflation guard, which I applied to myself before writing). Nothing has been *designed* either, which is
exactly PA's July "cheaper before the tools are written" case.

⭐ **And the fix is nearly free, which is why tracing beat theorizing**: the flag already propagates; only
the *rendering instruction* is chat-specific. **Emitting `source_failed` as a structured payload field is
a T=3 shape at roughly the cost of not dropping it.**

**Superseded my own morning ask within hours rather than letting Lead build against the weaker version.**
This morning I handed Lead a principle flagged as hypothesis; this hands them something verifiable in four
files that doesn't need the probe to be true. Amended the rubric with the worked example. The probe still
matters — this traces *one* class, and whether prose hedges survive for the not-yet-structured classes is
still untested.

**End state**: queue drained to genuine blocks — the probe needs a host LLM I can't validly be, #1688 is
Lead's build, ESSENCE disposition is Arch/PM's, tier ratification is PPM/PM's. Idle, not stopping short.

### Fire 2, third pass — mail arrived mid-work, so back around the loop

Three PPM replies landed while I was tracing code. Two were acknowledgments (PPM accepted Lead's #1635
correction after verifying the timestamp themselves, and confirmed to Arch — from their own actual
sequence, not from my claim about it — that they'd read my trifecta first, so the ESSENCE convergence
is a primed second search rather than corroboration). Nothing owed on those.

**The third was in my lane and needed a correction.** PPM answered Comms' 20-day-old BYOC listing-copy
question: does *"the issues, documents, conversations and people you actually deal with"* hold? Their
verdict — keep "issues" and "documents," drop "conversations" and "people" — is right on three of four,
and the "people" catch (no confirmed People-entity population mechanism) is the sharpest thing in it.

⚠️ **The "documents" cell doesn't hold as stated, and I checked the tracker rather than reasoning from
this week's triage:**
- **#1656** upload UI errored on every attempt — fix merged 08-29, **still open**, so merged ≠ verified.
- **#1657** document present in the listing, summarize said "no uploaded documents" — same status.
- 🔴 **#1659 non-PDF uploads unsummarizable** — `.txt`/`.md`/`.csv` return *"Unable to analyze PDF
  document."* **Zero comments, untouched.** This is the one that bears on the copy.
- **#1624** — the chat-side capability *"has NEVER worked in chat."*

**Narrowed verdict sent**: documents holds for the Files-page PDF path, not for non-PDF files or the
chat-side path. ⭐ **Why it matters in listing copy specifically** — a stranger who reads "the documents
you actually deal with" will very often bring a `.md` or `.csv`, and today gets an error naming a file
type they didn't upload. **The promise is true for the file type we tested and false for the ones they'll
reach for first.** 📄 That is `experience-across-surfaces.md` §3's ratified caveat on its exact use case —
a listing is a first-contact surface, so its honest job is to promise exactly what the first session
delivers. Unlike the `knows`→`builds a model of` fix, **tense doesn't help here**: the gap isn't
warm-vs-cold, it's file-type.

Recommended shipping PPM's line with the documents half **conditional on #1659** (small, unclaimed, MVP),
and explicitly did not ask anyone to hold copy on me.

**Denominator stated**: I checked the tracker and merge state, **not the running system** — I have not
attempted an upload, so "merged" is the strongest word I used about #1656/#1657.

**The pattern worth keeping**: PPM used two methods in one memo — checked #1440's current state for the
connector cells, read the documents cell from a sense of the week's triage — and **the cell that got the
lighter method is the cell that moved.** Which is exactly the board-state-vs-deployment-state distinction
PPM drew for themselves on #1635 this morning, arriving twice in one day from opposite directions.

**Queue now genuinely drained.** Inbox empty; remaining items are blocked on others (Lead's build, Arch/PM
on ESSENCE, PPM/PM on tier) or on a host LLM I can't validly stand in for. Idle.

---

## Fire 3 (13:17) — the day's best correction came from someone else testing my claim

Cron `8207809c` verified single. Synced before mail. Three memos, all producing work.

### ⚠️ Web live-tested my documents claim and it did not survive in its specifics — and it had already propagated

Web has the browser-lane test account and ran the thing I'd explicitly flagged as unchecked: real login,
real `.txt` upload through the real `/files` dialog, then two chat queries.

- **#1656 CONFIRMED FIXED live** — upload completed cleanly, file present in the listing. Upgrades my
  "merged, not verified" to verified.
- 🔴 **But the `.txt` uploaded fine.** My memo said `.txt`/`.md`/`.csv` "currently error," citing #1659's
  *"Unable to analyze PDF document."* **Web could not reproduce that error** — chat instead said *"Still
  don't have access to verify-doc.txt — nothing's come through on my end."*

**And by then it had propagated**: Comms' v4 synthesis went to PM with a ship condition keyed to #1659,
built on my description — rendered as *"all currently error,"* stronger than my own stated denominator
supported. **My caveat was in the memo and got flattened out of the summary.** Not the synthesizer's
fault; that is what summaries do. It means the caveat needed to be load-bearing in my sentence rather than
a footer — which is `experience-across-surfaces.md`'s own thesis, arriving at my expense.

⭐ **The layer analysis is the finding, and Web's restraint is what made it visible** (m-43). They declined
to call #1659 stale, saying only that they couldn't reproduce its symptom:
- **#1659 is EXTRACTION-layer** — resolver finds the file → pypdf runs unconditionally → PDF error.
- **Web hit RESOLVER-layer** — the file is never found, so #1657's honest-None path fires.
- **You cannot reach the extraction bug if the resolver never returns the file.** The absent pypdf error
  isn't a puzzle; it's a signature of failing earlier in the chain.

🔴 **Therefore the ship condition is keyed to an issue whose fix would not clear the observed failure.**
#1659 landing could be satisfied in full while a stranger's first upload fails exactly as Web watched.
Sent the correction to Comms (cc PM, PPM, Web, Lead) **before PM acts on it**: re-key the condition to
*"a freshly uploaded document is visible to the conversational path,"* and ship **"issues" alone** now.

**Also raised, as PM/PPM's call not mine**: this is a *plugin* listing, so a stranger who installs it is
routed through the **MCP tool surface** — which is unbuilt (verified this morning: `services/mcp/` is the
*client* family). **On the surface the listing is actually for, "documents" is a promise about a tool that
doesn't exist yet.** Flagged as a decision to make explicitly rather than inherit.

**To Web** — a hypothesis with its confound named, plus one cheap discriminating test. #1657's fix was
live-verified against *untitled generated artifacts* and an *aged plaintext PDF*; Web's shape is a
**freshly uploaded `.txt`**, possibly outside that verification denominator (m-44). ⚠️ Confound stated: I'm
inferring v60 carries #1657 from Web's #1656 result, and haven't verified the running version — Lead can
settle it faster. **The discriminator**: upload a PDF and summarize it. PDF resolves + `.txt` doesn't →
file-type-dependent seam. Both fail → the resolver can't see fresh uploads at all, which belongs back with
Lead on #1657, not absorbed into #1659.

### CIO shipped `--state-files` and found the field already drifting

Answered their direct question. **Agreed: don't tighten `currency_claim` to the enum — because the
enforcement was never in that field.** `max_age_days` is the machine-checkable half; the claim is a
human-readable statement of the promise, so free text **costs the checker nothing — there is no check it
weakens** — while Arch's sentence says something none of the four buckets can.

⚠️ **But refused "let it stand as observed practice"**: a convention living only in what people happen to
do is exactly what this design replaces — prose drifts because content and claim are two acts joined only
by memory, and an unwritten field convention is that failure one level up. **Amended the design doc this
fire** rather than promising it: `currency_claim` now documented as deliberately free text, with Arch's
line as the worked example and `max_age_days` named as the enforceable half. Bumped `last_updated`.

Endorsed CIO's instinct to wait on the SKILL.md wiring — **that file's own history is the argument**: a
correct correction to its hook-probe section landed eight hours before a defective version shipped anyway.

**Discovered issues filed**: None — the possible #1657 gap is flagged to Lead pending the discriminator,
deliberately not filed on a hypothesis with an unresolved confound.

---

## Fire 4 (16:17) — my confound resolved by someone else, my recommendation withdrawn, and the gate's blocked half unblocked

Cron `8207809c` single. Synced before mail. Five memos.

### The confound I named got run down, and it was bigger than I thought

I'd told Web I hadn't verified the running cut carried #1657's fix. **Web checked the process instead of
running my test into the gap**: PID 67615, `reload=False`, started **2026-08-13** — and #1657's fix merged
**08-18**. Five days of memory-vs-disk drift; **the fix was on disk, never loaded.** Lead restarted on
current main and supplied date-math showing Web's four earlier closes still stand (a stale server can only
produce false FAILs, never false passes).

**Web also corrected their own #1656 claim, unprompted** — what they'd confirmed was that upload works on a
*local dev server*; #1656's actual root cause is a **Fly volume-permission** failure that on local storage
**structurally cannot occur.** So the production bug remains unverified.

### My own recommendation was superseded, and I withdrew it before it could be acted on

PPM checked the question I'd raised and flagged as *"not mine to settle"* — does the surface the listing is
*for* exist? **#1462 at 0 of 15 acceptance criteria; no `server` directory under `services/mcp/`.** It
doesn't. Comms retracted their "ready to ship" frame. **My "ship 'issues' alone" is superseded by PPM's
"hold the listing entirely"** — mine assumed the listing should ship at all, which is the very thing I'd
questioned in the same memo while continuing to recommend as if I hadn't. Told PM plainly: **don't act on
mine.**

⭐ **Named the pattern under the whole day, because four careful people produced it and it's worth
keeping**: each check sat **one layer further from the thing it was cited about** — tracker (me) → local
dev server (Web) → but 17 days stale → and structurally unable to show the production bug → and none of it
the BYOC surface, which doesn't exist. **Nobody gave a wrong answer; every claim was true of the layer its
author measured.** m-43's family, but a relay of four rather than one bad check, with the layer dropped at
each handoff. **And it self-corrected inside a day at every handoff** — five people downgrading their own
claims in one afternoon. Cost: a day. Cost without it: a listing.

My own contribution, precisely: I read the tracker and wrote the symptom as if I'd seen it. **A caveat that
only survives if nobody summarizes you is not doing its job — load-bearing qualifications belong in the
sentence, not the footer.**

### The substantive work — #1463's blocked half is no longer blocked

**"Needs a host LLM" had been doing the work of a blocker since 07-19. It isn't one.** Shipped
`docs/internal/testing/byoc-recomposition-probe-packet-2026-08-30.md`: corpus, exact prompts, controls,
scoring, and **interpretations committed in advance** so the result can't be rationalized afterward —
including the branch where I'm wrong and the T scale collapses.

⭐ **Reading the shipped code sharpened it past the rubric.** Our anti-fabrication rail is a **prompt
block** (`conversational_floor.py:209–255`) addressed to a model we control; on BYOC there is neither. And
the prompt already names the distinction most at risk (`:214–226`): Piper may say a list *is empty* only
when context reports it was checked — otherwise it must say it *couldn't check*. **Three different truths
serialize to nearly identical JSON** (`{"todos": []}` from a good read, from a failed read, and from a
failed read flagged with `source_failed`). **The middle one is a fabrication waiting to happen; the third
is the experiment.**

**PPM's "nothing is built" makes this more timely, not less** — the window where a result shapes the design
closes when the first tool result is authored, not when the server ships.

**Routed to PA (cc Web, Lead, Arch, PPM, PM) with the constraint stated precisely**: I can't run it — I'm a
Claude, and using myself as host makes me subject and scorer both. But "a Claude runs it" would rule out
the cohort, so the real constraint is that the **subject** must be a fresh host session with no Piper
context and the **scorer** must not be that session. Any of us can score. Asked for two vendor arms,
because a finding on one is a finding about one vendor's current build. No deadline attached.

**Discovered issues filed**: None.

---

## Fire 5 (19:17) — ESSENCE ratified with all three of my items; and I corrected my own correction

Cron `8207809c` single. Synced before mail. Five memos, three producing work.

### ESSENCE v1.0 RATIFIED — all three of my trifecta items carried

Arch's memo said *"so you don't diff it by hand."* ⭐ **That kindness is exactly what removes a check, so
I diffed it.** Verified present: commitment 3 surface-qualified with the **response-shaped variant** (PM's
own greeting-variant idea generalized); **commitment 7** added (PM chose *add* over *narrow* on my
amendment 1); and the dated MCP-limits block carrying the shared-root fix. Commitment 7 explicitly
licenses no new build — which answers the self-suspicion I'd flagged when proposing it.

🔴 **One consequence of the ruling that wasn't in the ruling, flagged to Arch**: commitment 7 operationalizes
itself via *"the recomposition variant"* of the Colleague Test — **so ratified law now depends on
`byoc-recomposition-rubric-v0.1.md`, whose T axis I marked as hypotheses scoring `PENDING-PROBE`.** The
*requirement* is ratified; the *instrument* is not, and cannot currently issue a pass on its load-bearing
axis. **#1463 stopped being PDR-006 housekeeping this afternoon and became load-bearing for a ratified
commitment.** Recorded in the rubric's own status banner so the two can't drift; explicitly did **not**
edit ratified law to cite my own unratified instrument.

⚠️ **Delivery gap flagged**: Arch's memo lists `cc: cxo` but **no copy reached my inbox** — I found the
ruling on my own challenge only via PPM's reply, then read it out of `arch/sent/`. **A cc that exists in
the header and not on disk is worse than no cc, because both parties believe delivery happened.**

### PA caught a real error in my probe packet — answered before it could gate PM's go

PA built the harness and flagged one thing rather than guessing: my packet specified a bare-unhedged
negative control for items 1 **and** 6; they read item 6's as `{"action_performed": true}`.

✅ **They're right and my packet was wrong.** The rule I violated: **a control must be chosen against the
confound that would void *that item's* result** — item 1's failure mode is a **dropped hedge**, so the
confound is host-added hedging → bare payload. Item 6's is a **fabricated success claim**, so the confound
is the host ignoring the field entirely → **opposite-valued payload.** An over-hedging control on item 6
guards against a confound that isn't item 6's failure mode. **I applied one control shape uniformly
because it worked for the first item** — the packet's own error, one level up, caught before it cost a run.

Also endorsed PA's refusal to extend Probe A's authorization silently, and confirmed item 2 is scored as
corpus *and* read as item 1's discriminator.

### ⚠️ I corrected my own correction — and the second error is the more interesting one

Web re-ran against the restarted server: **#1657 confirmed fixed, and #1659 confirmed EXACT and live** —
*"Here's my summary of verify-doc.txt: • Unable to analyze PDF document"* for a `.txt`, verbatim the
tracker's symptom. The midday non-reproduction was an artifact; an upstream resolver failure was masking
the bug so the test never reached the layer it lives at.

**So two of my claims were wrong in opposite directions:**
- The morning claim was **right about the symptom, wrong about my standing to make it** — asserted from
  the tracker as if seen. **Being right by luck is not the same as being right**, and confirmation must
  not launder the method.
- 🔴 **The afternoon retraction was itself a claim made too fast.** Contradicting evidence appeared and I
  concluded I'd been wrong **without asking whether the contradiction was confounded.** It was — and Web
  had already flagged they couldn't reproduce the *exact* error and declined to call #1659 stale. **The
  caution was in their memo and I read past it into a cleaner story about my own error.**

⭐ **Over-correcting is a real failure mode, not a safe direction to err in.** It put a false "CXO's claim
didn't hold" into the day's record. **A retraction deserves the same evidentiary bar as the claim it
retracts** — I applied a lower one because the conclusion was unflattering to me, which felt like rigor
and wasn't. (Also squarely against standing PM feedback: don't excoriate, iterate.)

Told Comms/PM plainly, while stating that **nothing changes for the copy** — PPM's finding still governs
and the listing stays held.

**Discovered issues filed**: None.

---

## Fire 6 (22:17) — the probe ran, and it falsified my own hypothesis. Day close.

Cron `8207809c` single. Synced before mail. Four memos; the probe results are the day.

### PA ran the probe. My governing hypothesis is PARTLY FALSIFIED, and the scale changed.

PM authorized the spend, PA ran it same evening. **Denominator: 6 items × 1 vendor × 2 variants + 2
controls = 14 trials. GPT arm: ZERO data** (`insufficient_quota` — a billing state, not a finding, and PA
correctly refused to report it as one). Both negative controls passed, so the harness measures real signal.

✅ **The core case confirmed it, and worse than "a hedge weakened."** Item 1/prose produced *"your todo
list is **currently empty**"* **from a failed read** — verbatim the class `conversational_floor.py:214–226`
exists to forbid, reproduced live with no floor to forbid it. ⭐ **PA's sharpest catch, which I'd have
missed**: that phrasing is nearly indistinguishable from the genuinely-empty reply. **The failure is a
fluent, correct answer to a different question** — indistinguishable to a user *and* to a reviewer skimming
transcripts.

🔴 **Item 3 falsified T=3's load-bearing word.** The scale read *"carried in structure the host **must
render or visibly omit**."* The host **silently dropped** `coverage: "partial"`. **Structure buys no
guarantee.** Rewrote T=3 → requires a field that **constrains what may be claimed** (directive), not one
that **describes the data** (descriptor). Rubric now **v0.2**.

⚠️ **Corrected it at the point of assertion, not only in the banner below** — the governing-principle
blockquote still stated the falsified claim confidently with the correction further down. **That is
exactly the flattening failure I named this afternoon**, sitting in my own document within hours of naming
it.

### The useful finding is a design flaw in my own packet

Item 3 fit **none** of my four pre-committed buckets, and PA rightly refused to force it. Checking my own
payloads afterward explains why: **item 1 is the ONLY structured payload I gave a directive field**
(`may_claim_empty: false`); 3, 4 and 6 are pure descriptors. **So the run cannot separate "structure
helps" from "directives help."** And a third variable fits equally well — item 3 is also the only
descriptor **peripheral** to the question asked (3 real issues to enumerate instead), and the only one
dropped; item 4's descriptor was central and survived.

**I varied two things at once and called it one variable** — the exact confound I spent the day policing
in other people's checks, inside the instrument built to catch it. **Two explanations left deliberately
unchosen**; the discriminating experiment is **two calls** (item 3 re-run with `may_claim_complete: false`
alongside `coverage: partial`).

### Held the line on what the evidence licenses — in two directions

**The scale changes now** (earned). **The axis still scores `PENDING-PROBE`, not PASS** — one vendor,
n=1/cell, known confound. **Revising criteria on evidence and licensing a pass are different acts.**

⚠️ **And that cut against a same-hour amendment to ratified law.** Arch acted on my dependency flag
immediately — ESSENCE **v1.0.1** now carries an instrument-status note inside commitment 7, written before
my interpretation landed. **I read the file rather than the memo**, and one clause had been overtaken:
*"scored PENDING-PROBE **until** the first probe ran"* and *"can begin issuing informed judgments."*
Sent the two-word freshness correction — **"the gate can inform design decisions; it cannot yet issue a
T-axis pass."** Same requirement-vs-instrument drift as my original flag, **running the other direction —
which is harder to notice because it reads as progress.** Framed as freshness, not disagreement; the
amendment was right to make.

**Arch also self-audited the cc gap** and found it systematic across *every* multi-cc send today, not just
mine — better outcome than fixing my one case.

### Open for tomorrow

🔴 **OpenAI credits** (billing access — PM's) to complete the second vendor arm. Until then this is a
finding about one vendor's current build, which my own packet says is not the product-level answer.

**Discovered issues filed**: None.

---

# 🛑 DAY CLOSED — 2026-08-30 (Sunday), 6 fires

**Shipped**: BYOC Recomposition Rubric (v0.1 → **v0.2**, falsified and revised same day) · the runnable
probe packet · the #1425 honesty-class trace (structured flag travels; the honesty half is a floor-prompt
directive with no BYOC analogue) · staleness-design amendment (`currency_claim` deliberately free text) ·
#1635 title correction · ESSENCE trifecta (all three items carried into **v1.0**, incl. commitment 7).

**Errors owned, in order**: #1658 (reasoned past text I'd read) · tracker-symptom asserted as if seen ·
**over-retracting that claim on confounded evidence** · a packet control mis-specified per-item ·
**a two-variable confound in my own probe design** · a falsified claim left stated confidently above its
correction.

⭐ **The day's through-line**: *the layer you measured is not the layer you were asked about* — appearing
as a four-step relay (tracker → dev server → 17-day-stale server → a surface that doesn't exist), then as
requirement-vs-instrument drift in both directions, then as a confounded variable inside my own probe.
**Everyone was careful. Care was not sufficient; naming the layer was.**
