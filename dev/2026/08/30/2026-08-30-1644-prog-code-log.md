# Session Log — 2026-08-30 16:44 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Task**: Three small ops issues routed by Docs 2026-08-30 — 1594, 1618, 1636.
Verify-first per issue; smallest correct fix or report why not; commits per
issue, bare numbers, no close-keywords, no push (Lead pushes/closes).

## Issue 1594 — post-reboot Docker data services don't restart

- Claimed vs found: verified exactly as filed. `restart: unless-stopped`
  present on piper-app/orchestration/ghmcp, absent on postgres/redis/chromadb;
  live containers showed `restart=no` (which is why ghmcp survived the 08-11
  reboot and the data services didn't).
- Fix: added `restart: unless-stopped` to the three data services in
  docker-compose.yml AND applied to the live containers via
  `docker update --restart unless-stopped` (compose edits don't touch running
  containers until recreate; docker update is zero-downtime).
- Evidence: `docker compose config` exit 0, rendered config shows
  unless-stopped for all three; `docker inspect` on live containers shows
  restart=unless-stopped, status=running. Layer caveat (m-43): this verifies
  config + live daemon policy, NOT an actual reboot — the acceptance line
  "verified by docker ps after a reboot" remains open until the next real
  host restart. Preflight-gate option (Pard coordination) not taken — out of
  smallest-fix scope, remains open per the issue's own option 2.
- Commit: 9c0461783.

## Issue 1618 — issue-checkbox-lint false-positives

- Claimed vs found: partially drifted from the framing. The hook ALREADY
  requires keyword proximity (`resolve[sd]? #N` etc.) — the incident's bare
  `#11` never matched; only the keyword-adjacent item-5 ref did. Real gap:
  no issue-state check.
- Fix: `.claude/hooks/issue-checkbox-lint.sh` now fetches body,state in one
  gh call and skips non-OPEN issues (a close-keyword next to a closed
  issue's number can't auto-close anything, so the warning's premise fails);
  plus the issue's "at minimum" loose-matching caveat line in warning text.
- Evidence: bash -n clean; behavioral harness (scratchpad, hook pipeline
  verbatim from the message-scan layer down, real gh): the exact 1618
  incident message now → no warning; `fixes 1594`-shaped ref to an open
  issue with unchecked boxes still warns; bare `#N` confirms proximity
  already required. BONUS live repro mid-session: first draft of the 1618
  commit message quoted the incident string and tripped the OLD hook —
  twice (one per config layer), on closed issue 5. Layer caveat: the
  executing hook path is the MAIN CHECKOUT's copy
  (`piper-morgan-product/.claude/hooks/...`), so this fix goes live only
  after it lands on main and PM's checkout syncs. Amended the commit message
  to drop the keyword-adjacent ref (CLAUDE.md auto-close gotcha).
- Commit: 05b9cfa5b (amended).

## Issue 1636 — Blog Eras cluster field empty/stale

- Claimed vs found: verified still current, no drift. Website repo
  `data/blog-metadata.csv`: 370 rows, 191 empty cluster, 9 matching current
  era slugs (the-sprint 8, the-foundation 1; the-build/methodology/
  reflection = 0), ~170 pre-migration slugs. Docs' 122-mapping sync this
  morning didn't change the distribution. Staleness finding also holds:
  latest CSV date 2026-08-11 vs publishes through late August.
- No fix: the issue explicitly needs a design decision (backfill by era date
  ranges vs reconsider `cluster` as join key) — reported to Lead, not
  guessed, per brief. Decision-support gathered (read-only): a pure
  date-range backfill (workDate > chatDate > pubDate) is mechanically clean —
  100% of rows parse, all five eras populate (89/68/80/34/34), 65 posts fall
  post-2026-03-31 (the known new-era gap), and ZERO of the 9 valid cluster
  values conflict with their date-derived era.
- No commit (nothing changed; website main checkout touched read-only).

## Wrap

- Commits on claude/lead-cycle, NOT pushed (per brief; Lead merges/pushes).
- Ratchet files: no Python touched; not run (n/a per brief's condition).
- Discovered issues filed: none (the hook-path layering note above is
  reported to Lead in-channel, not a new defect — it's how hooks resolve).

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (auto-close keyword gotcha → commit-message
  amend; m-43 name-the-layer → verification caveats; PM main-checkout
  hard rule → website repo read-only); Docs routing memo
  (mailboxes/lead/read/nudge-docs-to-lead-3-open-issues-...-2026-08-30.md);
  feedback_calendar_workdate_is_source_work_period (date-column choice for
  the 1636 simulation).
- **Loaded but not referenced**: memory index bulk; briefing table.
- **Wanted but not found**: none.
