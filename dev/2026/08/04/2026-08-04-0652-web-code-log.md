# Web session — 2026-08-04 (Tuesday)

**Role**: Unicorn Web Designer (piper-morgan-website)
**Account**: pipermorgan.ai / Amber
**Model**: Claude Opus 5 (1M context)
**Prior session**: `dev/2026/08/03/2026-08-03-0652-web-code-log.md` — **DAY-CLOSED: 2026-08-03** (explicit STOP at 21:52, day arc: `BRIEFING-ESSENTIAL-WEB.md` written closing a 6-week gap, Web added to CLAUDE.md/ROSTER.md, two stale carry-forward items retired, zero website-repo code changes).
**Trigger**: cron fire `fafad118`, `22 6,9,12,15,18,21 * * *`, first fire of the day (06:52)

---

## START (06:52)

### Step 0 self-heal

8/3 closed properly with an explicit STOP and `<!-- DAY-CLOSED: 2026-08-03 -->` marker (verified via grep). No self-heal needed.

### Environment

| Check | Result |
|---|---|
| Cron | one job, no duplicates, `fafad118` |
| Product worktree | `claude/web-cycle`, 0 ahead/behind, clean |
| Website worktree | `claude/web-cycle`, 0 ahead/behind, clean |

⚠️ **Cron expiry imminent**: `fafad118` armed 2026-07-29, session-only 7-day auto-expiry — **~2026-08-05, now 1 day out.** This fire firing normally is itself evidence it hasn't expired yet; the real signal to watch for is a *missed* scheduled slot, not the countdown.

Website repo has had no new commits since 2026-08-02 (`a081ef5`) — two days quiet, nothing to act on absent a specific ask.

### Carry-forward — open threads inherited from 8/3

- **Two Docs-flagged gaps** (publish-queue staleness class, `copy-editorial-calendar.js` sibling-path break) — awaiting Docs' preference. No rush.
- **Predecessor's two unanswered questions** (CLI B tested-since-May?, `--mode=archive` still wanted?) — no rush, open since 7/19.
- **PM design/obs-pass backlog** (`web-standing-items.md`) — flagged stale, not resolved; no browser on this host to re-verify.
- **Docs' tier-placement call** on Web in `ROSTER.md` (Tier 2 vs. 3) — HOST supplied evidence 8/3; genuinely Docs' decision, not mine to chase.
- **Beta target**: 2026-08-08 (Saturday), ratified, `decisions.log:303` — carried as a cohort fact, relevant if any Web-lane work ties to launch-day readiness.

---

## Fire log

| Fire | Time | Action | Notes |
|---|---|---|---|
| cron | 06:52 | START | No self-heal needed (8/3 closed properly). Environment verified, both worktrees synced clean. Cron expiry now 1 day out — firing normally is the proof of life. Carry-forward + standing-items reviewed — nothing new. |
| cron | 09:52 | WATCH | Quiet fire — mail inbox empty, no task-loop movement. Both worktrees synced clean, 0 ahead/behind. Fired on schedule — cron still alive. |

## Fire (12:52) — DUTY CYCLE TICK, WORK

- **(12:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Synced both worktrees, clean, current.
- **(12:55–14:15)** — Mail loop: 1 memo — **Comms found a real bug, not just a documentation gap**: `pipermorgan.ai/blog/<any-nonexistent-slug>/` and `/blog/page/<out-of-range>/` both return HTTP 200 with the not-found page's body — a soft 404, invisible to status-code checks. Comms found it inside their own Monday all-clear (an absence-check that would have returned "clean" against a dead URL just as readily as a real one) and led with owning that before anything else. Squarely a website-repo bug in my lane, so traced root cause rather than just documenting the workaround:
  - **Reproduced live first** (`curl -o /dev/null -w`) before reading any code — confirmed Comms' exact numbers.
  - **Read the route code**: both `[slug]/page.tsx` and `[pageNumber]/page.tsx` correctly call `notFound()` — not a logic bug in the handlers.
  - **Found the actual mechanism** via response headers: `x-nextjs-prerender: 1` + `x-vercel-cache: HIT` on the 200 response — the not-found shell was being served from Vercel's ISR/static cache with a stale 200 status, a known Next.js App Router + Vercel edge-cache interaction when `dynamicParams` defaults to `true` and an unrouted param falls through to a dynamic render.
  - **Fix is safe here specifically**: both routes import `medium-posts.json` at build time, so no slug/page-number outside `generateStaticParams()` can ever become valid without a rebuild — there's no legitimate "new post not yet in the list" case to protect. Set `export const dynamicParams = false` on both routes, forcing unknown params to 404 immediately at the routing layer instead of attempting (and mis-caching) a dynamic render.
  - **Verified thoroughly, not just by reasoning**: `tsc --noEmit` showed pre-existing unrelated errors (confirmed not introduced by this change — different files); `next build` succeeded with both routes still `●` (SSG) for known params; `next lint` clean on both files; then **actually ran the built app locally** (`next build && next start`, no secrets needed since the data is static JSON) and confirmed real behavior end-to-end: known slug/page → 200, unknown slug/page → 404 with the not-found page's actual content rendered (verified via response body, not just status code — the same "assert presence" discipline Comms' own memo was about).
  - Committed (`03b77d9d`, website repo), pushed to `main`. **Not yet verified live** — Vercel needs to auto-deploy; flagged in carry-forward to re-check `pipermorgan.ai` next fire.
  - Replied to Comms (cc Docs, PM, HOST, PA, cohort) with the root cause, the fix, and direct answers to both things Comms' memo asked: confirmed this was a real bug (not something the run-of-show note alone would have caught) and confirmed Comms' Monday all-clear conclusion was independently still correct (checked a real live page, not a dead one). Triaged Comms' memo to `read/`, sent (`3f835d555`) with copies to comms/docs/xian inboxes + sent mirror, verified landed via `git ls-tree origin/main`.
- **(14:16)** — Task loop: carry-forward updated with the new active thread (soft-404 fix, pending live verification); standing-items unchanged. Re-synced both worktrees before final commit — no new upstream conflicts.

**Cron**: unchanged, armed (`fafad118`) — expiry window ~2026-08-05, now within a day.

## Fire (15:52) — DUTY CYCLE TICK, WORK

- **(15:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Synced both worktrees, both clean, 0 ahead/behind.
- **(15:53)** — Checked whether the soft-404 fix had landed live before touching mail: `curl` against `pipermorgan.ai` confirmed it — known slug/page still 200, `zzz-not-real` and `page/999` now genuinely 404. Deploy landed.
- **(15:55–16:35)** — Mail loop: 3 memos, two substantive threads:
  - **Comms independently re-ran this morning's exact probes against production** and confirmed the fix live with real before/after numbers (30,122B/200 → 24,069B/404, etc.), correctly identified an earlier "byte drift" observation as actually being my deploy landing rather than noise, and flagged a genuinely open question: `/blog/the-list-that-lies/` (publishing tonight) is currently a **cached 404** (`age: 5642`) — will it correctly flip to 200 when the real post publishes and a new deployment lands? Comms was explicit: "I believe your reasoning and can't verify it." Rather than just reassure, worked through the actual mechanism: `dynamicParams: false` 404s are build-time/routing-layer, not Vercel's `fetch()`-based Data Cache (the mechanism with documented cross-deployment persistence) — so a genuine new deployment should serve the new static page directly, bypassing the reject path entirely, rather than needing per-entry cache invalidation. Stated the limit of my own certainty plainly (don't know Vercel's internals well enough to fully rule out a deployment-independent path-keyed cache) rather than overclaiming. Replied with the precise mechanism, cc Docs/PM/HOST/PA, and left the actual verification to tonight's real publish, which is the only test that settles it.
  - **CXO shipped a fix to `check-refresh-promises.py`** (following HOST's own non-author-verification of it, which found HOST's portfolio genuinely lapsed across 4 workstream reviews) and named **Web as one of 7 roles whose refresh-currency claim is prose with no checkable trigger behind it** — "each declares it stays current; nothing can contradict any of them." Checked this against my own portfolio rather than assuming it didn't apply: **it was correct, and worse than "unverifiable"** — `ROLE-PORTFOLIO-WEB.md` claimed "the session-open act is the refresh mechanism," and that's demonstrably false; real work (the briefing gap, the registry fix, today's soft-404 fix) shipped across 5 days and dozens of session STARTs without section 2 being touched once. Same exact shape as HOST's own finding — an assertion that two activities (reading vs. writing) are the same activity. Refreshed section 2's actual content with today's real priorities (not busywork — the briefing/registry gap and the soft-404 fix), then corrected §5 and the frontmatter `refresh_discipline` field to state the honest version: I notice drift by re-reading and decide by hand whether to update — vigilance, not a mechanism. **Deliberately did not register a `refresh_trigger_glob`** just to look covered: my session logs fire 6x/day, and a naive "any trigger after last_updated" check (correct for weekly workstream reviews) would misreport constant staleness against a daily artifact — a different but equally real semantic mismatch, not an excuse to dodge the finding. Replied to CXO (cc HOST/CIO/Docs/PM/cohort) with the full self-audit.
  - Triaged all three memos to `read/`, sent all four outbound pieces (two new memos + their cc copies + sent mirrors + the three triage moves) in one `mail-send.sh` call (`5cd4ab32d`), verified every path landed via `git ls-tree origin/main`.
- **(16:36)** — Task loop: carry-forward updated with both new closures/findings (soft-404 live-verified + the one open watch item; the portfolio self-audit). Portfolio file itself (`docs/briefing/ROLE-PORTFOLIO-WEB.md`) edited but not yet committed — not a mailbox path, goes through the normal commit below.

**Cron**: unchanged, armed (`fafad118`) — expiry window ~2026-08-05, now within a day.

## Fire (18:52) — DUTY CYCLE TICK, WORK

- **(18:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Synced both worktrees — website worktree picked up `51e3f0f` (Docs' publish of "The List That Lies," the exact post that was the open cached-404 watch item from this afternoon).
- **(18:53–19:15)** — Mail loop: 4 memos, three threads:
  - **CXO shipped `refresh_verifiability: by-hand`** — a new frontmatter declaration bucket built directly in response to my earlier finding that registering a mismatched `refresh_trigger_glob` against my 6x/day session logs would produce a confident wrong signal, not a correct one. CXO named this precisely: the old script's only way off the "unverifiable" list was a bad-faith glob, which "punishes the only person who examined theirs." Added the field to my own portfolio, then ran the checker myself to confirm it actually registers correctly (`kept by hand, DECLARED: 1` — moved out of the undeclared-6 bucket) rather than trusting the memo's description of the fix.
  - **HOST confirmed their own portfolio refresh was real** (not a date bump) and reiterated the coverage framing; nothing further needed from Web beyond having already been named as one of "three of three checked" roles whose stated mechanism turned out to be an assertion.
  - **CIO shipped Step 5b (heartbeat emission) as a numbered step**, since the mechanism had existed since 7/28 as unrun prose inside Step 4 — explaining five straight mornings of false freeze-watchdog alarms. **Comms then found a real logic gap in the fix**: the rationale requires the heartbeat at wake, but Step 5b places it at the end of the fire — so a START fire that takes long enough to miss the sweep window stays invisible regardless, because the suppression check only fires after the (late) commit already happened. Checked this against my own morning rather than just reading it: **I was one of the exact 3 roles (arch, lead, web) flagged in the 06:46:06 alarm today** — my commit landed at 06:53:13, 7 minutes after the sweep, same shape as Comms' arch example. Reported this as a third independent data point to CIO/HOST (cc Comms/cohort) before tomorrow's test, since Comms explicitly asked to be heard before rather than after. Did not propose editing CIO's skill myself, same restraint Comms showed.
  - Triaged all four memos to `read/`, sent the new memo + copies + triage moves in one `mail-send.sh` call (`8919265ea`), verified drained.
- **(19:45–19:55)** — Checked the actual soft-404 watch item rather than waiting to be asked: `curl` against `pipermorgan.ai/blog/the-list-that-lies/` — **200, real post content (41,952 bytes, correct title), first check after publish, no manual intervention needed.** This closes the one open question from this afternoon's fix cleanly — the deployment-scoped-cache reasoning held against a real publish, not just synthetic test slugs. Sent a closing memo to Comms/Docs/PM/HOST/PA (`ee752b8a1`), updated carry-forward to mark the soft-404 thread fully resolved with no open items.
- **(19:56)** — Task loop: both new closures folded into carry-forward. Both worktrees re-verified clean before final commit.

**Cron**: unchanged, armed (`fafad118`) — expiry window ~2026-08-05, now within hours.
