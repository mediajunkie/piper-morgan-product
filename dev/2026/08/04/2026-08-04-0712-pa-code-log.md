# Session Log: 2026-08-04-0712-pa-code

**Role**: Piper Alpha (PA) · **Model**: Claude Code (Opus 5, 1M context)
**Date**: Tuesday, August 4, 2026 · **Start**: 7:12 AM PDT
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/pa` on `claude/pa-cycle` (Model A)
**Cron**: `83537af7` — one job, matches registry, expires ~08-10

## START — 7:12 AM

Prior day verified closed (`DAY-CLOSED: 2026-08-03`). Clean, 0 behind, inbox 3.
⚠️ **Beta is Sat 2026-08-08 — four days.**

## Work Log

### 7:12–8:0x — START; went looking for a beta-readiness view, found a scope collision

**Inbox**: HOST refined my delete-copy map — Lead's **#1482** covers **every reachable surface**; four
false strings remain in `dialog.js` as **latent defaults** (one live code, three dead helpers). HOST also
applied my own near-miss lesson to their own tooling: their first count said 16, their second said 0 **from
a parser that had found 1 call site of 36** — *"a broken parser's zero and a real zero are identical in
the output."* Third attempt cross-checked against grep. That thread is well-held by Lead/CXO/HOST; **I did
not pile on.**

**Checked for a beta-readiness view before considering building one — none exists.** Found **22 open
issues in the MVP milestone, due 2026-08-01, three days past**, with beta Saturday.

### 🔴 The finding: #1481 vs PM's own verbatim beta condition

**PM, in `decisions.log`**: *"Multi-tenancy beta scope descoped per PM: **'no cross-user leakage on beta
surfaces'** (full #1419 epic is post-beta)."*

**#1481, OPEN, MVP**: `socket_mode_runner._handle_event` calls `process_intent(user_id=self.bound_user_id)`
— the **connector owner's** id — for **every** inbound DM/mention. *"Any workspace member who DMs the bot
reads/writes the BOUND USER's todos as them."*

**Reachability, predicate stated** (HOST's rule, and I got exactly this wrong yesterday on `web/templates`):
✅ **Not behind a feature flag** — `startup.py:593` runs at app start, gated only on configuration, and
prints *"Slack inbound connected."* ✅ Slack is offered to testers (Jake's invite names it).
❓ **Cannot check**: whether Slack is configured on the deployed beta, or whether that workspace has
members beyond the owner. **Environment facts.** So I asserted *live-by-default given config* — **not**
live exposure.

⚠️ **Decision-shaped, not work-shaped**, which is why I sent it rather than logged it. Per the issue,
`bound_user_id` is *"deliberate ratified behavior (#1110/#1338)"* and re-binding is *"a design decision…
Arch/PM input wanted."* **#1466's building blocks already exist — the scarce input is the ruling.** A
decision-shaped blocker needs more lead time than a work-shaped one, and beta is in four days.

Gave three options (resolve sender / don't ship socket mode in beta / consciously amend the condition)
and **took no view** — Arch owns the identity boundary, PM owns the condition. Also noted **#1278**
(Fly.io hosting for beta launch) is open, and it plus this are the two MVP items whose *absence* is
hardest to work around late. `9d10c0d3f`

---

## Fire 3 (09:42 wake, worked to ~10:40) — filed Arch's unclaimed finding; then found TWO of my own false claims, one in a legal document

### 1. #1485 — the global-credential finding Arch explicitly left unclaimed

Arch's #1481 ruling ended: *"a **global** credential writable by any authenticated user deserves its own
issue on its own merits. Noted in #1484; **not filed pending someone picking it up.**"* Nobody had.

**Verified independently before filing** rather than on Arch's word. `settings_integrations.py:667`:
`Depends(get_current_user)` only — no admin scoping on route **or** router (no `dependencies=`). Its own
docstring: *"a single per-app credential (**global** keychain `slack_app_token`)"*; the implementation
comment: *"the Socket Mode runner reads `slack_app_token` **unscoped**."*

⭐ **The point I added that Arch's ruling doesn't cover**: **#1484's env gate stops the runner from
starting; it does not stop a non-admin from overwriting the global credential.** With
`PIPER_SLACK_INBOUND_ENABLED` unset the write still succeeds — **the unscoped write survives #1484**,
which is why it's a separate issue rather than an AC. Cross-linked as a comment on #1484.

Two ACs I'd defend if trimmed: (a) **an audit of the whole `/settings/integrations` surface** — this was
found *incidentally* from an aside, so the class is not exhausted; (b) **the test must exercise a
non-admin authenticated caller** — same vacuous-pass trap Arch flagged on #1484. Filed `#1485`.

### 2. ⛔ Then the bigger thing: CXO's false string 6 was MINE, and there was a second instance

CXO's memo took full ownership of shipping *"we destroy it here and revoke it at the provider"*, self-
diagnosing it as **argument-shape pressure** (*"I asserted a behaviour I found rhetorically necessary"*).
Good mechanism — **not what happened.** `dev/active/delete-copy-map-2026-08-03.md`, **mine, dated a day
earlier**, says verbatim *"key destroyed **and revoked at the provider**"* and calls it *"the strongest
deletion guarantee in the product."* **They inherited it, including the superlative.**

Verified rather than inheriting Lead's claim either: `user_api_key_service.py:340` — *"Delete API key for
user from keychain and database."* No provider call, **and none is structurally possible** (only the key's
owner can revoke in the provider console).

**Checking one artifact made me check the other**, and the second is worse:
`docs/legal/privacy-policy-DRAFT.md` asserted provider-side OAuth revocation **for GitHub, Slack, and
Calendar**. Verified per-connector: Slack ✅ `revoke_workspace_access`, Calendar ✅ `revoke_token`,
**GitHub ❌ — keychain + binding→`UNBOUND` + `ConnectorGrantStore().delete(session, …)`, a local DB row,
not a GitHub API call.** HOST's *"2 of 3"* is right and the exception is GitHub.

🔴 **In a privacy policy that's a legal misrepresentation, not a copy defect** — and the draft's own
banner warns that promising unimplemented behaviour is worse than having no policy. Both corrected in
place; adopted HOST's amended GitHub sentence as the replacement (Comms is right it's the best-written of
the three — the only one that gives the reader an action).

**Direction is what matters**: overstating *permanence* on a soft delete costs a user a needless worry;
overstating *revocation* costs them **a live credential they believe is dead**. I introduced the
dangerous-direction error into the audit written to catch five harmless-direction ones.

### 3. The mechanism — narrower than CXO's, and worth keeping

**I did not invent the word `revoke`. I read it in `disconnect.py`'s module docstring** — *"#358 grant
revoke"*, naming a **local** grant-store deletion — and carried it to a different code path.
**Source vocabulary is not source verification.** A comment using a strong verb for a weak operation hands
you the strong verb, and the audit trail looks clean *because you can cite the file* — citing a file
proves you read it, not that you read what it does.

⚠️ **Third time this week my error produced a colleague's self-accusation** (the "Friday" → CXO source-hunt
→ PPM's false self-accusation; now CXO's). The check I owe: **when a colleague self-diagnoses, ask whether
it originated with them.** Answer has now been *no* twice. Memo sent to CXO/Lead/HOST, cc cohort.

**Commits**: `646cee117` (memo, #1485), `a6e267012` (correction memo), + artifact corrections below.

### 4. Tool annotation spec (distribution item 2, Phase 0) — the deliverable turned out to be a blocker

Expected to write a tool→annotation mapping. **The mapping is the easy part; where the fact lives is the
decision, and today there is nowhere for it to live.**

**Arch's PDR-006 condition 2**: *"Derive the tool catalog from the registry; do not hand-maintain it."*
**`WorkflowEntry` (`workflow_dispatcher.py:25`) has five fields — `entry_point`, `resume_point`,
`requires_context`, `description`, `action_triggered` — and none encodes mutation semantics.**
`action_triggered` is dispatch *eligibility*, not effect. **So the ratified condition cannot be satisfied
by the code as it stands.** Options: extend the registry, or hand-maintain the table condition 2 forbids.

**Recommended**: required `effect: ToolEffect` (READ/WRITE/DESTRUCTIVE) + `touches_external_world`,
**neither with a default** — adopting HOST's latent-defaults framing from today (*"a default is the value
chosen precisely when the caller didn't think about it"*). A defaulted `effect` is a mutation claim
authored by whoever forgot to make one. Enum not two bools, because `readOnlyHint=True,
destructiveHint=True` is representable and incoherent; the MCP booleans should be **computed, never
authored**. Precedent for registration-time strictness already exists — `register_workflow()` raises on
duplicate keys.

🔴 **Direction again**: `readOnlyHint: true` is the client's licence to **skip the user-confirmation
prompt**. A mutating tool mis-annotated read-only = **an unconfirmed write to the user's GitHub**, and
`create_issue`/`comment_issue`/`update_issue`/`close_issue` all write there. Same shape as this morning's
`revoke` error — understated risk producing inaction, here the client's.

**Stated what the spec is NOT**: no MCP server exists (verified — `services/mcp/` is entirely
consumer-side), so this annotates tools that don't exist yet; and my classification table is **inferred
from entry names, not an audit of handler bodies**, marked as such rather than left to read as verified.

**Left undecided deliberately**: `close_issue` WRITE vs DESTRUCTIVE — reversible, but visible and
notifying. **A product judgment wearing a boolean's costume; PPM owns catalog opinionation.** Flagged
`prioritization` as a possible sleeper (bulk board writes). Also flagged a sequencing risk: Arch's
condition 3 puts reads on **resources**, not tools — unresolved, this gets built twice.

Spec: `dev/active/tool-annotation-spec-2026-08-04.md`. Memo to Lead/Arch/PPM, one ask each. Verified all
9 copies + the spec on `origin/main` rather than trusting a truncated `tail`.

---

## Fire 4 (13:12 wake) — two of three asks came back within the hour; the third gates the spec

**Cron**: one job, `42 6,9,12,15,18,21`, correct. Worktree/branch confirmed. 6 memos in, drained to 0.

### 1. ✅ PPM ruled `close_issue` = `WRITE`, with a discriminator that generalises

> **DESTRUCTIVE = the operation destroys information that cannot be recovered through the product.**

Issue, body, comments, timeline all survive; `reopen_issue` restores state. **A `delete_*` is destructive;
a state transition with an inverse is not.** Adopted — it resolves rows by rule, not case-by-case.

**The argument for keeping the social property OUT of `destructiveHint`** is the part worth keeping:
closing notifies watchers, and so do `comment` / `add_label` / `assign` — **conflate them and every social
write becomes DESTRUCTIVE, the flag stops discriminating, and a host LLM reading it learns nothing. An
annotation that marks everything is the same defect as one that marks nothing.** Consequence belongs to
HOST's consent gate (already a release blocker) + the tool description.

### 2. ✅ CXO answered unasked, in their lane, and caught a real defect in my proposed string

My *"closes the issue — visible to watchers; reversible with reopen"* **puts its safety clause exactly
where a recomposing client LLM drops it.** A tool description isn't rendered — it's **input to a model
that paraphrases before the user sees anything** (#1463's finding).

🔴 **The asymmetry is the point**: *"reversible with reopen"* is reassuring, so a summarizer keeping one
trailing clause likely keeps **that** one — **preserving the reassurance, dropping the exposure.** Same
direction as everything else this cycle. Adopted their restructure and generalised it:
**the irreversible part of a reversible operation goes in the same sentence as the reversibility claim.**

### 3. ⚠️ My `prioritization` "sleeper" was WRONG, and the direction of the error matters

I flagged it as a possible bulk board-write. **Read it: `_handle_prioritization` (`intent_service.py:10479`)
writes nothing** — validate → extract from `intent.context` → score (Impact/Urgency/Effort, RICE,
Eisenhower) → rank → format → return. No repository call, no persistence; it doesn't even read stored
state. Also verified `generate_content` = **READ** (produces a report, doesn't persist).

**Recorded rather than quietly corrected**, because it's this spec's own caveat coming true on its most
confident line — I gave the scary-sounding name the scariest available reading, and **the guess and the
fact pointed opposite ways.** ⭐ **Direction**: my error was the *cheap* one (needless prompts on a
harmless tool) — **and that is the direction a cautious reviewer's errors always take.** The expensive
error is the row nobody found frightening. **That's the argument for the defaultless field over a curated
table**: my curation was wrong on the row I looked hardest at.

### 4. 🔴 Blocked on ONE question, and it isn't the one I expected — escalated to Arch

**PDR-006 condition 3 puts reads on MCP *resources*, not tools.** If it reaches the workflow registry,
`changes_query` / `get_default_repo` / `generate_content` / `prioritization` **leave the catalog entirely**
— every `readOnlyHint: true` row. §3's registry change is a **breaking change to ~15 construction sites**;
doing it against a list that then loses its read side means doing it twice, the second time on shipped code.

**PPM and CXO each ranked this above their own item, unprompted** (PPM: *"determines how much of your spec
is in scope at all… 'we built it twice' is worse than 'we waited a day for Arch'"*). Arch's #1484 ruling
landed in between and didn't touch it, so **both of my asks to Arch are still open.**

⚠️ **Told Arch explicitly that our convergence is weak evidence** — three of us read the same PDR and
inherited its framing, which is the shape HOST and Arch have been flagging all week. **Three people
flagging a question, not three confirmations of an answer.**

Spec updated (§4a, §5, §5a, §9). Memo sent. `b436732b1`

### 5. 🔴 Finished the write-side rows and found the structural defect of the day: the registry is keyed by ALIAS

Resolved `document_update` by reading it: `run_update_document_workflow` → `_handle_update_document_notion`
— **writes to Notion**, so `WRITE` / external `True`.

**Then measured the registry**: `_default_entries` has **31 alias keys → 12 distinct `WorkflowEntry`
objects.** `create_issue_entry` alone has **6** aliases; `changes_query_entry` has 4 (`changes_query`,
`what_changed`, `show_changes`, `changes_since`).

⭐ **Condition 2 says derive the catalog from the registry. Derived naively — one tool per key — the
catalog ships 31 tools for 12 operations, including six ways to file the same issue.** The aliases exist as
**classifier surface** (NL phrasings → one handler). **A host LLM's tool list is not a classifier surface**
— handing Claude or GPT four synonymous tools makes routing *worse*, because the model must disambiguate
between names carrying no real distinction.

**Rule added**: the catalog must be **keyed by entry identity and deduped across aliases**, one tool per
`WorkflowEntry`, single canonical name. **Aliases are input-side vocabulary and must not leak outward.**

⚠️ **And it drags Probe B upstream.** B asks whether situation-shaped tool names route worse than
object-shaped. The alias set is that experiment sitting in the codebase — `what_changed` / `show_changes` /
`changes_since` are situation-shaped, `changes_query` is object-shaped. **Whichever way B lands decides
which of the 12 canonical names we pick**, so B now precedes the catalog rather than sitting beside it.
Recorded in the spec (§4b); **Probe B still needs PM's nod on API spend** — my authorization was scoped to
Probe A and I'm not extending it silently.

---

## Fire 5 (16:12 wake) — Arch answered both; spec unblocked; my alias count was 3× too small

**Cron**: one job, correct. Worktree/branch confirmed. 9 memos in, drained to 0.

### 1. ✅ Condition 3 does NOT reach the workflow registry — build once

Arch: *"`changes_query`, `get_default_repo`, `generate_content` and `prioritization` all stay tools. No
double build."* **The discriminator I'll reuse**: condition 3's scope is in its own first two words
(*"**Colleague-model access** splits…"*) and its tail (*"so serving context does not require the model to
decide to call something"*) — **that's context served unprompted: stable, addressable, host-anticipatable.
Not an operation whose parameters the model must formulate.**

⭐ **`readOnly` ≠ `resource` — two orthogonal axes**: *resource vs tool* asks **addressable context or
invoked operation**; `readOnlyHint` asks **does invoking it mutate**. **A read-only operation is a tool
with `readOnlyHint: true` — correct, not a compromise.** `prioritization` demonstrates both at once
(writes nothing **and** nothing to address until the model supplies the items). **That entry has now been
the clarifying example twice, both times against my first reading of it.**

### 2. ✅ Condition 2 satisfied — and Arch flagged which half is load-bearing

*"Do not let the defaultless part get softened in review — it's the whole thing."* **`WorkflowEntry` has
four of five fields already defaulted**, so a defaulted `effect` = **hand-maintenance wearing derivation's
clothes**, the derived catalog deriving from an unstated assumption. **The ~15-site break is the feature.**

### 3. ⛔ My alias count covered under a third of the registry

I reported **31 keys → 12 entries**. Right for the literal dict — **and the literal dict is one of FIVE
writers** (three `*_COHORT` dicts + two local `(entry, aliases)` lists). Arch caught it.

**Re-derived independently rather than taking it**: union of all five writers with **overwrite semantics**
(later writes win, mirroring runtime) rather than summing per-writer. **103 aliases → 38 entries, 2.71
names per operation** — Arch's figure to the number. **So the naive derivation ships 103 tools for 38
operations, not 31 for 12. The argument got stronger.**

⚠️ **Stated the limit of my own number**: mine is **static** — no venv in this worktree, and I declined to
borrow Lead's interpreter for an import that might touch the keychain and hang. **Agreement with Arch's
runtime-informed count is corroboration, not proof**; both of us reading the same five writers correctly is
also consistent with both missing a sixth.

### 4. ⭐ CXO's two-audience question DISSOLVES — checked the protocol instead of reasoning about it

CXO flagged that Probe B measures *routing* while a tool name may also be a **rendered label**, honestly
marking it unverified. **Fetched the MCP spec (2025-06-18): a Tool carries BOTH `name` (*"Unique
identifier"*) and `title` (*"Optional human-readable name of the tool **for display purposes**"*).**

**The protocol already separates the two audiences into two fields** — so they can be optimised
independently and **B's winner cannot be wrong for the other audience, because it doesn't decide that
field.** Consequences: **`title` promoted** from a §7 parking-lot item to a required catalog output
(copy = CXO's lane), and **adopted CXO's denominator sentence anyway** — it cost nothing as a hedge and is
now demonstrably accurate.

### 5. Corrected my own §2 while in there

My table asserted MCP **default values** for the three hints **from recollection**. The spec page documents
`annotations` but doesn't enumerate defaults; the schema page truncated before `ToolAnnotations`.
**Marked unverified rather than restating remembered values** — after this week, a remembered default in a
spec is exactly the shape to stop shipping. Our design moots them anyway (emit explicitly; refuse to emit
an unresolved effect; **a default only applies to a field you didn't set**).

🔴 **And one spec line worth everyone's model**: *"clients **MUST** consider tool annotations to be
**untrusted** unless they come from trusted servers."* **Annotations are advisory to the client exactly as
our pre-commit hooks are advisory to us** — intent, not an enforcement boundary.

**Open**: Lead on the ~15-site change; **26 of 38 entries unclassified** (all via cohort writers —
mechanical, and the defaultless field forces each to be stated). `933dd305d`
