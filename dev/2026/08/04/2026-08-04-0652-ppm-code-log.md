# Session Log — Principal Product Manager (PPM)

**Date**: 2026-08-04 (Tuesday) · **beta is Sat 2026-08-08 — four days**
**Started**: 07:22 PDT (START fire, 06:52 slot)
**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Host**: Amber · **Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A)
**Branch**: `claude/ppm-cycle` · **Cron**: `8e6b845c`
**Prior log**: `dev/2026/08/03/2026-08-03-0652-ppm-code-log.md` — ✅ DAY-CLOSED verified at Step 0

---

## Entries

### 07:22 — START

Step 0 verified, one cron, sync clean, 0 behind. **Ten memos**, including an URGENT that reads as a
beta-scope decision rather than an engineering item — triaging by that first.

**Overnight movement on my filings**: **#1482 SHIPPED** (Lead, with one fact correction to CXO's
string 6) — filed 19:45 last night, fixed by morning. HOST then found **#1482 covers every
*reachable* surface, with four false strings remaining as latent defaults** — a scope gap on my own
issue.

**All four of my filings remain milestone-less** (#1462, #1476, #1477, #1482) — PM's field.

### 07:55 — ⛔ #1481's collision with PM's own beta condition: the fix is scope, not engineering

**PA found a real collision** and correctly routed it as a decision rather than work. Verified their
reachability claim independently rather than on their word: `web/startup.py` →
`SlackSocketModePhase` *"Starts only when fully configured (app-level token + a user-scoped bot
token) — **honest absence otherwise**."* **Not feature-flagged; gated only on configuration.**

- **#1481**: *"Socket-mode DM/mention path binds EVERY Slack sender to the connector owner's
  principal"* — cross-user leakage on a surface, by its own title.
- **PM's verbatim condition** (`decisions.log`): *"no cross-user leakage on beta surfaces"* (#1419
  epic descoped post-beta).

**My recommendation — option 3, and stated strongly rather than as a preference:**

| | Option | Cost |
|---|---|---|
| 1 | Fix #1481 by Saturday | ⚠️ **Not a patch.** Sibling **#1466**: the *"Slack user → Piper user principal mapping **does not exist**"* — so this is **building a mapping layer in four days.** |
| 2 | Ship with the leak | ❌ Exactly what the condition rules out. |
| 3 | ⭐ **Don't configure Slack inbound for beta** | ✅ **Zero engineering, reversible, already the code's documented behaviour.** |

**The argument that makes it more than a preference**: PM descoped **#1419 (multi-tenancy) to
post-beta** *with* the no-leakage condition. **#1481 is that epic's debt surfacing on one surface.**
Scoping the surface out isn't a new decision — **it's the one PM already made, followed through.**
Fixing #1481 in four days would *reverse* the descope by the back door: doing the deferred
multi-tenancy work under deadline, on the riskiest schedule available.

**Left Arch the question that could collapse my recommendation**: is "unconfigured" a *real*
boundary or merely an absent one? If any path starts that runner without both tokens, option 3 dies
and option 1 is back. **That's their call, not mine, and I said so.**

### 08:10 — #1482 shipped overnight; split the reachable fix from the latent tail

**Lead shipped #1482 (`ce31b09d6`) within hours of my filing it**, with one fact correction to CXO's
string 6. **HOST then verified coverage with the predicate stated** — 0 of 36 call sites render the
false default, cross-checked parser (34) against grep (36).

**HOST's own process is the story**: first count **16** (wrong window — flagged every form dialog);
second count **0** *from a parser that had matched 1 of 36 call sites* — **clean because it didn't
run**. *A broken parser's zero and a real zero are identical in the output* — CXO's flag-B lesson,
one day later, in HOST's own tooling. Third attempt cross-checked before believing it.

**My call as the issue's filer**: #1482's scope was *"the word must match the behaviour on the six
surfaces a user can reach"* — **done and verified**. Commented that resolution on the issue rather
than expanding it, and **filed the residue as #1483**: four false strings no caller reaches
(`dialog.js:87` live-but-unreached; `:512/521/530` dead). **Split deliberately** so a shipped fix
isn't held open by a non-urgent tail, and the tail isn't lost.

HOST's two arguments carried onto #1483 because they're right: **a default is the value chosen
exactly when the caller didn't think about it**, and **dead code carrying a false claim is a trap
for whoever revives it — *because* `confirmDelete` looks like the house helper.** Marked
not-beta-blocking on my read; nothing renders today.

### Fire 2 (10:22, WORK) — Arch ruled: my decision was right, my mechanism was absent

**Arch answered the falsifier I handed them, and the answer splits.**

✅ **My scope call stands, fully concurred**: Slack inbound is not a beta surface; it's #1419's debt
on one surface; fixing #1481 in four days reverses PM's descope by the back door. **#1481 + #1466 →
Production with #1419.**

❌ **But I tested the wrong property.** My falsifier was *"does any path start the runner without
both tokens?"* — **Arch checked it and my literal premise was correct**: `build_runner()` is the sole
construction site, nothing builds without tokens. **The risk was never there.** It's that
**the tokens are user-suppliable at runtime through shipped UI**: `POST
/api/v1/settings/integrations/slack/app-token` is gated on `Depends(get_current_user)` **only**,
writes a **global** credential, and calls `restart_socket_runner` *"at runtime (no app restart)."*
**Any authenticated beta user can switch the surface on themselves** — and `_resolve_bound_user()`
takes the **earliest-created** user, so the leaked principal is plausibly the founder account.

**The variant worth naming, because it's new this week**: my falsifier was **correctly named,
correctly tested, and named the wrong property.** Not a partial view (roadmap §M4, the `cut`), not a
broken check (CXO's flag B, HOST's parser) — **a sound test of the wrong variable.** I asked *"can it
start without tokens?"* when the question was ***"can the tokens appear without us?"***

**Arch's naming of the shape is the sharpest yet**: *"we chose not to configure it"* and *"it cannot
start"* produce **byte-identical observable state** — same absent runner, same skip log, same
honest-absence banner — **and diverge completely under one authenticated POST.** m-44 at the config
layer. **Not configuring something is a state; a beta condition needs a control.**

**The practical outcome is better than this morning, and I said so rather than dwelling on the
correction**: the beta-blocking item is now **#1484 — a 4-line fail-closed gate at the chokepoint,
default off, fully reversible** — **not #1481's principal-mapping layer.** Arch's framing: *"still
essentially PPM's zero-cost option; it just spends four lines to make it true rather than hoped."*

**Underlined the AC Arch flagged**: the test must assert **token-present + flag-unset**. Asserting
token-absent **passes vacuously today and proves nothing** — the same false-green shape three of us
hit this week, pre-empted before it's written.

**Also connected #1485 (PA) as the same story**: any authenticated user can write the *global* Slack
app token. The surface can be switched on by any user, and once on it binds every sender to the
founder's principal. One story, two issues.

### 11:20 — PA's annotation spec: a product ruling and a sequencing flag

PA's spec found its own centre is a blocker: **`WorkflowEntry` has five fields and none encodes
mutation semantics**, so **Arch's condition 2 (derive, don't hand-maintain) cannot be satisfied
today** — there's nothing to derive `readOnlyHint` from. Options: extend the registry, or
hand-maintain the thing condition 2 forbids.

**PA left one call to me and cited why**: *is `close_issue` WRITE or DESTRUCTIVE?* — *"a product
judgment wearing a boolean's costume, and you established the catalog is where opinionation lives."*

**Ruled WRITE**, with a discriminator meant to decide the next twenty cases rather than this one:
> **DESTRUCTIVE = the operation destroys information that cannot be recovered through the product.**

The issue, body, comments and timeline all survive; `reopen_issue` restores state fully.

**But PA's instinct was right and I didn't want the ruling to bury it.** Closing someone's issue
notifies watchers and cannot be un-notified. **That's a second property, and it isn't
`destructiveHint`** — it's HOST's consent gate. Followed the conflated version through to show why:
if "notifies other people" makes a tool destructive, then `comment`, `add_label` and `assign` all
become destructive, **the flag stops discriminating, and a host LLM reading it learns nothing.**
*An annotation that marks everything has the same defect as one that marks nothing.* So: WRITE in
the annotation, inside the consent gate's scope, with the visibility stated in the **tool
description** — which is where opinionation belongs.

**Weighted PA's sequencing risk higher than my own ruling**: Arch's condition 3 puts reads on MCP
**resources**, not tools — so if read-side entries migrate, **an annotation table built against
today's tool list gets rebuilt.** That's Arch's to resolve and it gates implementation more than the
registry-field question does.

**Endorsed the REQUIRED, defaultless field**, and named why the defaultless half is load-bearing —
it's HOST's #1483 argument from this morning: *a default is the value chosen exactly when the author
wasn't deciding.*

**Recorded the whole finding on #1462** (comment) because **it lands on acceptance criteria I
wrote** — a builder should learn from the AC that the registry can't satisfy condition 2 today,
rather than discovering it mid-implementation.

### Fire 3 (13:22, WORK) — the same shape three times in one thread, and a moved premise

**#1484 now carries the MVP milestone** — someone set it.

**CXO caught that the fix I endorsed creates a permanent lie.** Verified their load-bearing citation
at source: `settings_integrations.py:714` `state = "listening" if connected else "connecting"`, and
the docstring at `:108` — *"'connecting' (token set, not yet connected / connect failed)"*. So a
**flag-refused runner (`None`) → `connected=False` → `"connecting"` → the yellow badge saying "try
saving the token again."** A beta user would follow on-page instructions into a permanent retry
loop, with the true cause — a build flag they cannot see — never named.

**⭐ The contribution I added: this is the THIRD instance of one shape, inside one thread, in one
day.**
1. **Arch, morning**: *"we chose not to configure it"* vs *"it cannot start"* — **byte-identical**
   observable state. That's why the scope call needed a mechanism.
2. **CXO, now**: *"refused by policy"* vs *"connecting, keep trying"* — **identical yellow badge**.
   **The mechanism we added to fix (1) reproduces the same defect one layer up.**
3. **The AC Arch flagged**: a test asserting token-absent **passes vacuously** — clean because it
   didn't measure.

**Config, UI, test — same failure: a state that renders identically to a different state.** My read
of why: the system's honest-absence convention is *"return nothing and let the caller infer"*, so
**`None` does double duty everywhere and every consumer resolves the ambiguity differently.**

**The durable form I'd want kept**: ***a state the user cannot act on must never render as one that
invites action.***

**And the fix is smaller than CXO's 3 strings** — the enum *already* declares three states
(`listening` / `connecting` / `not_enabled`), and **the flag-refused case has none of its own**, so
it falls into the one meaning *keep waiting*. Cheapest correct version, without touching
`build_runner`'s contract: **the status route reads the same env var and returns a distinct
`disabled` state.** One `os.getenv`, one enum value — **then CXO's strings have something true to
say.** Discriminator offered: `connecting` = *the user can fix this by retrying*; `disabled` = *the
user cannot fix this at all*; **those must never share a badge.** Copy stays CXO's; my point is the
string can't be right until the state is.

**Then PA moved a premise I'd recorded this morning**, so I amended #1462 rather than leaving it:
**`_default_entries` is keyed by ALIAS — 31 keys → 12 distinct entries** (`create_issue_entry` alone
has 6). **Derived naively, the catalog ships 31 tools for 12 operations.**

**Why it's a product problem and not a de-dup chore** — and this is the part that unifies it with
CXO's earlier work: **aliases are classifier surface, and a host LLM's tool list is not one.**
Aliases make *input* forgiving; a catalog is *output to a model that must choose*, where synonyms
make routing **worse**. **Forgiving input and unambiguous output are opposite requirements** —
the same "different audiences" principle CXO established for name vs description, **one axis up.**

So condition 2 needs **two** things from the registry, neither of which is hand-maintaining a
catalog: the required mutation-semantics field, **and a canonical tool identity per entry** so the
catalog derives from 12 operations rather than 31 phrasings.
