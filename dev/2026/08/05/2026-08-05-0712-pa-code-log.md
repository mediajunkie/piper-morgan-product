# PA session log — 2026-08-05

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Opus 5 (1M)
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21`

---

## START (07:12) — prior day closed late; two findings in the first five minutes

**Prior day**: `dev/2026/08/04/2026-08-04-0712-pa-code-log.md` was **NOT** closed — fire 7 ended without
running the close. **Missed close run now**, before any other work.

**Heartbeat**: emitted **START, unconditional, at wake** → `dev/heartbeats/2026-08-05/pa.tsv`, on
`origin/main`, **before** doing anything else. Cron verified: one job, correct expression.

### ⚠️ Finding 1 — my own log would have fooled a loose DAY-CLOSED matcher

`grep -c "DAY-CLOSED"` on yesterday's log returns **2**, which reads as *closed*. Both are false:

- line 11 — a **continuity reference** to the *previous* day (`DAY-CLOSED: 2026-08-03`)
- line 416 — **my own prose about the freeze-check**, describing how it skips `DAY-CLOSED` roles

**The second one is the sharp instance: writing ABOUT the marker created a false marker.** Any role whose
log discusses the mechanism becomes self-certifying under a loose match — and the roles most likely to
write that prose are the ones working on the watchdog.

✅ **The strict pattern at `duty-cycle-freeze-check.sh:99` correctly returns NOT CLOSED** on the same file.
So the CXO/HOST/Web fix of 2026-07-30 is doing exactly what it was built for, and **this is a live
confirmation of it rather than a new defect.** Worth recording because the fix was defensive and nothing
had exercised it in anger.

⛔ **Operative rule for me**: never `grep -c "DAY-CLOSED"` to decide close state — use the anchored
pattern. A count is not a marker.

### ✅ Finding 2 — the heartbeat surface went 2 → 6 overnight; my pessimism was wrong

Yesterday I flagged the 06:46 test as unreadable at *"two of eleven, three with HOST."* **Actual surface
this morning: `arch`, `comms`, `host`, `lead`, `pa`, `web` — six of eleven.** Adoption moved faster than I
predicted and **I should say so as plainly as I said the caution.**

**Freeze-check at 07:12** flags **STALE: cxo (8h), ppm (8h), docs (8h)** — and those three are exactly the
three absent from the surface that aren't otherwise covered. `cio` and `exec` are absent from the surface
but **not** flagged, so other signals are carrying them.

⚠️ **Still true and unchanged**: the sweep prints `rows=11` and never states how many roles it *examined*
(`:218` vs the skips at `:90`/`:99`). **Six emitting makes the test readable; it does not make the
denominator stated.**

### Finding 3 — ⭐ my seat discriminates Arch's dead-zone hypothesis, and the STALE list is a clock reading

Arch measured cron `:27` → fire `:57` and called it a 20-minute dead zone. **Their seat can't separate two
hypotheses**: *(H1)* fires arrive at a fixed clock time ~`:57`; *(H2)* fires arrive ~30 min **after their
own scheduled minute**. `:27 + 30 = :57` satisfies both.

**My cron is `:42`.** Measured from **commit timestamps, not my labels** (taking Arch's correction — I'd
been labelling by scheduled minute too): 07:15, 10:14, 13:14, 16:15, 19:13, 22:12, 07:12 against crons of
:42 → **+33, +32, +32, +33, +31, +30, +30.** Seven consecutive fires arriving at `:12`–`:15` of the
**following** hour, never `:57`. **H1 is dead** — arrival is not a fixed `:57` slot.

> ⛔ **PARTIALLY RETRACTED (fire 2, same day): "the offset is additive at ~+30" is WRONG ACROSS SEATS.**
> Measured range is **+6 (web) to +40 (arch)**. +30–33 held across seven of *my own* fires and I treated
> a **within-seat** constant as a **cross-seat** one. **What survives: H1 is still dead** (web 06:28,
> docs 07:29), but the offset is **per-seat and variable**, not a constant.

### ~~🔴 And the STALE list is a perfect rank-order of cron minute — 9/9, zero exceptions~~

> ⛔ **RETRACTED (fire 2, same day) — DO NOT CITE THIS HEADING.** Write order is **not** cron order:
> **host (:37) wrote before arch (:27); web (:22) before comms (:12).** What I observed at 07:14 was a
> clean **partition**, and I reported it as a **rank correlation** — one observation instant described as
> a structural property. **The partition was real; "9/9, zero exceptions" implied a law that isn't
> there.** The section below stands only as the record of what I saw at that instant.

At 07:14, all nine roles on the `6,9,12,15,18,21` schedule:

| comms :12 | lead :17 | web :22 | arch :27 | host :37 | **pa :42** | cxo :47 | ppm :52 | docs :57 |
|---|---|---|---|---|---|---|---|---|
| ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **STALE** | **STALE** | **STALE** |

**The cut point IS the current time.** Every role whose fire has landed is clear; every role whose fire
hasn't is flagged. **cxo/ppm/docs are not silent — they haven't woken yet.** HOST's *"selected on grace
position, not liveness"*, with the correlation at 9/9.

**Root cause underneath both, and it's arithmetic**: last fire 21:xx+30 → first fire 06:xx+30 = a **9-hour
designed overnight gap**, against the sweep's own **`dyn-threshold 7h`**. **Every one of these nine roles
necessarily exceeds the threshold every morning until its own fire lands.** The morning alarm is
**structural, not diagnostic** — which is why five mornings of it went unactioned and everyone was right
to ignore them. **A liveness threshold shorter than the designed dormancy is an alarm clock, not a
watchdog**, and no heartbeat change could ever have fixed it.

⚠️ **The sweep prints `dyn-threshold 7h wake-window-aware`** — it *claims* wake-window awareness and
flagged three roles for being inside their wake window. **Whatever that awareness does, it doesn't cover
this; check it behaviourally rather than trusting the string, because the string is what stops someone
looking.**

### Correcting myself, plainly

Yesterday I called the test unreadable at *"two of eleven, three with HOST."* **It's six of nine
on-schedule roles, and all three absences are explained by clock position.** Adoption moved much faster
than I predicted. **I said the pessimistic thing loudly, so I'm saying this as plainly.** The test was
readable — just not about what we aimed it at. `39152d69f`

### ⏳ Pending prediction — recorded BEFORE the outcome, so it can falsify me

My rank-order model says `cxo` (:47), `ppm` (:52) and `docs` (:57) are **late, not dark**, and should
appear on the surface at roughly **07:17–07:20, 07:22–07:25, 07:27–07:30**.

**At 07:17 none had appeared** — consistent, not yet confirmed. **Checkable at my next fire (09:42 → ~10:14):**

- **All three present, no longer STALE** → model holds; the morning alarm is a clock reading.
- **Any still absent by ~08:00** → **my model is wrong for that role and it is genuinely dark** — which
  would be a real finding and the alarm correct for it.

**Recorded now precisely because it's easy to quietly drop a prediction that doesn't land**, and because
either outcome is informative. *(Also read Arch's #1484 end-to-end verification — 3 passed, catch-all trap
handled; they note it doesn't close #1485's class. Nothing owed by me.)*

---

## Fire 2 (10:12 wake) — pulled ground truth; two of my three claims died

**Heartbeat at wake, unconditional**, before work. Cron: one job, correct. 8 memos in, drained to 0.

### The measurement I should have made first

**The heartbeat tsv carries its own write timestamp.** Arrival times are directly measurable — no need to
infer from when anyone happened to look. **Three of us (Comms, CXO, me) argued about who had written at
07:17 based on our own observation times, with the answer sitting in the files.**

| web :22 → 06:28 **+6** | comms :12 → 06:42 +30 | lead :17 → 06:53 +36 | host :37 → 07:01 +24 |
| arch :27 → 07:07 **+40** | pa :42 → 07:12 +30 | cxo :47 → 07:17 +30 | docs :57 → 07:29 +32 | **ppm :52 → never** |

### ❌ Claim 1 dead — "latency is additive at ~+30" is wrong across seats

**Range +6 to +40. Web is right** that their seat breaks it. I had +30–33 across seven of **my own** fires
and **treated a within-seat constant as a cross-seat one.** A single grace constant tuned to +30
under-covers arch (+40) and wildly over-covers web (+6).

**Survives**: arrival is *not* a fixed `:57` slot — web 06:28 and docs 07:29 settle that. The offset is
**per-seat and variable**, which is a weaker claim than I made but still kills Arch's H1.

### ❌ Claim 2 dead — "perfect 9/9 rank order" was an artifact of one instant

Write order ≠ cron order: **host (:37) wrote before arch (:27); web (:22) before comms (:12).** What I saw
at 07:14 was a clean **partition** (all ≤:42 written, all >:42 not) and **I reported it as a rank
correlation.** ⚠️ **Same error I've been naming in others' work all week, in my own finding** — one
observation instant described as a structural property. **"9/9, zero exceptions" implied a law that isn't
there.**

### ✅ Claim 3 survives untouched — the arithmetic never depended on latency

**~9h designed overnight gap vs `dyn-threshold 7h`.** Every role crosses it every morning before its first
fire lands. Latency variance changes *when* each clears, not *whether* it was flagged. **Structural, not
diagnostic.** The fix belongs here.

### ⭐ Pre-registered falsifier fired, and it found the real signal

Predicted before the outcome: **cxo 07:17–07:20 → actual 07:17:58 ✅** · **docs 07:27–07:30 → actual
07:29:45 ✅** · **ppm 07:22–07:25 → never wrote ❌.**

**Honoring it**: for ppm, "late not dark" was wrong. ⚠️ **But "dark" is wrong too — my criterion was too
binary.** ppm sent a memo this morning and shows **no STALE**, because commits cover it. **The real state
is a third one I hadn't allowed for: ALIVE BUT NOT EMITTING.** With the belt reading three signals, "no
heartbeat" was never going to mean "dark." **ppm and cio are the two seats where the heartbeat surface and
the commit signals disagree** — the most informative cases for whether Step 5b has actually landed.

Surface now **9 roles**, zero STALE. `ef17ac380`

---

## Fire 3 (13:12 wake) — landed CIO's retraction in the code; nearly repeated the error making the fix

**Heartbeat at wake.** Cron: one job, correct. 7 memos in, drained to 0.

### CIO retracted the late cluster in mail; `origin/main` still asserted it

`duty-cycle-freeze-check.sh:57-59` still carried `host 203 · pa 210 · ppm 211 ← genuinely late cluster`
**as the stated justification for `FIRST_FIRE_GRACE_MIN=45`** — so the shipped code kept telling anyone
who read it for the reasoning that two on-time roles were late. **A correction that stops at the mailbox
hasn't happened.**

**Corrected table — FIRST row per role, against each role's OWN first_fire:**
`web 6 · host 24 · exec 30 · cxo 30 · pa 30 · comms 30 · docs 32 · cio 33 · lead 36 · arch 40` · **ppm 211**

⭐ **CIO's constant and reasoning survive completely** — 45 clears the max on-time (arch +40) with 5 min
margin and still flags ppm. **Only the membership was wrong**, which is the good version of this.

**Mechanism, because it recurs**: the artifact hits exactly the roles that emitted **more than once** that
day (host 3 rows, pa 3, comms 2) — grabbing a later row gives you their 2nd or 3rd fire. **ppm was right
only by luck: one row, so no later row existed to misread.** *A per-day surface mixing firsts and repeats
silently yields a different quantity depending on which you take.*

### ⚠️ I nearly shipped the same class of error INTO the correction

My first pass computed every role against a **uniform 06:xx first fire** → **`cio +273`, `exec +150`**,
which I was one step from publishing as a late cluster of my own, **naming CIO's seat as the worst in the
cohort.** Both wrong: **cio fires `10,16,22`, exec fires `8,20`** — real latencies **+33 and +30, both
on-time.** Caught only because +273 was implausible enough to re-check.

**Same shape as CIO's, one layer over**: they generalised "read a row" across roles with different row
counts; I generalised "first fire is 06:xx" across roles with different schedules. **Neither careless —
both generalising across a dimension the data varies on.**

**Preserved in the comment rather than the thread**: Arch's caution that the 5-min margin rests on one
morning and that **wake→visible ≠ cron→wake**, plus the read-the-first-row rule.

### ⚠️ The grep trap, twice in one day, two unrelated files

After pushing, `grep -c "genuinely late cluster"` on `origin/main` returned **1** — **my own correction
note quotes the old text.** Identical to this morning's `grep -c "DAY-CLOSED"` on my session log.
**Writing about a marker creates a match for it. Verify with line context, never a count.** `590318b4d`

---

## Fire 4 (16:12 wake) — ran Comms' rule on myself; it found a live false claim in the privacy policy

**Heartbeat at wake.** Cron correct. 5 memos in, drained to 0.

### 🔴 Comms' rule caught what two of my own audits missed

Comms generalised my "a correction that stops at the mailbox hasn't happened" into:
**"a correction has to land where the CLAIM is, not merely in the same artifact — a reader arrives at the
claim, not at your correction."** Ran it against my own artifacts. **Three hits.**

**The one that matters: `docs/legal/privacy-policy-DRAFT.md:139`.** On 08-04 I corrected the prose —
provider-side OAuth revoke is Slack ✅, Calendar ✅, **GitHub ❌**. **Forty-six lines below that correction,
the deletion-capability table still read** *"Connector credentials — ✅ HARD, and better than most:
provider-side OAuth revocation plus keychain deletion."*

**Same document, same day I corrected it, and it's the row a reviewer reads to answer "what can users
delete?"** Dangerous direction again: a user told GitHub access was revoked **won't go revoke it.** Now
split per connector. ⚠️ **I had audited that document twice and missed it both times** — because I checked
*"did I correct the claim"* rather than *"is the claim corrected everywhere it appears."*

**Two more, in these logs**: `"both compliant, both invisible"` (retraction 68 lines below) and
`"perfect rank-order, 9/9 zero exceptions"` (75 lines below). Both struck in place.
⭐ **Comms' point I'd underweighted: Docs reads session logs to build the omnibus**, so an uncorrected
claim in a log is **upstream of a digest** — mine would have gone in as findings. Log hygiene is
source-integrity, not tidiness.

### ⚠️ Third-order recurrence — an audit matches its own corrections

My sweep for surviving `"revoked at the provider"` claims returned **5 hits, every one a quotation inside
a correction.** **Third time today**: `grep -c "DAY-CLOSED"` (my log quoting the marker), `grep -c
"genuinely late cluster"` (my correction note quoting CIO's retracted claim), now this.

> **Any audit for a retracted claim will match its own corrections — and the better a correction states
> what it corrects, the more certainly it defeats the search for what it corrected.** No phrasing escapes
> it; quoting the claim is what makes a correction legible. **Line context, never a count.**

### ⬜ Phase 0 lane — `claude plugin validate` dry-run is NOT a runnable step yet

Tried to pick up my own lane item. **Blocked, and the item was mis-scoped**: `claude` is not on PATH from
this shell nor at the common install paths, **and there is no plugin manifest to validate** — the only
`manifest.json` in the repo is `coordination/manifest.json`, the async-prompt-queue tracker (checked:
`"system": "async-prompt-queue"`), unrelated.

**So the plan item is not "run a validation command" — it is "author a plugin manifest, then validate
it."** Recording the re-scope rather than reporting the item as blocked-on-tooling. `aaeea7b47`

---

## Fire 5 (19:12 wake) — third seat for HOST, then back to my own lane as promised

**Heartbeat at wake.** Cron correct. 6 memos in, drained to 0.

### 1. Third dispatch seat, free — HOST asked, I already had the data

HOST: *"make `date` the first command of your fire, immediately before the heartbeat… you then have your
own dispatch constant for free."* **I already open every fire with `date`, and the heartbeat tsv carries
seconds**, so the third seat existed before the ask.

**PA, cron `:42`, 5 fires / 12 hours: +30m17s, +30m17s, +30m16s, +30m20s, +30m17s — 4-second spread.**

⭐ **And it answers the question HOST said needed a third seat**: arch fires `:27` and lands at `:57`; I
fire `:42` and land at `:12` of the next hour. **Fifteen minutes apart in schedule, both at +30mXX from
our OWN minute.** If dispatch were a global clock artifact we'd share an absolute second-of-hour, not an
offset. **We share the offset → additive per-role delay, not a slot the machine hands out.**

⛔ **But I told them not to use my seconds digit.** My heartbeat fires *after* `date`→`pwd`→`git branch`→
**`git fetch`**→**`git merge`** — so my number is **wake + git-op time, an upper bound**, and my 4s spread
is plausibly network variance, not dispatch variance. Arch's 1s spread is likely the cleaner instrument.
**Three of us would have compared 13s against 17s as if they were the same quantity** — the exact
"two different latencies" trap Arch raised about grace 45, arriving inside the dataset meant to settle it.
**Adopting the fix: heartbeat immediately after `date`, before git ops, from next fire.** Pre-registered:
**my number should drop a few seconds tomorrow; if it doesn't, my explanation is wrong and the seat delta
is real.**

### 2. Phase 0 lane — manifest drafted from the ACTUAL reference

Fetched `code.claude.com/docs/en/plugins-reference` rather than recalling it. **Two findings, and the
manifest is the less interesting one.**

**(a) The item is much smaller than we scoped it.** The manifest is **optional**; **`name` is the only
required field**; unrecognized fields are **warnings not errors**, and a plugin with only those *"still
passes validation and loads at runtime."* **Schema compliance is nearly free — the real Phase 0 work is
the docs page and the privacy policy**, and I'd re-weight effort accordingly.

**(b) 🔴 A PDR-006 gap.** The reference documents plugin MCP servers in **exactly one shape — a local
`command`.** Searched for `url` / `type: http` / `sse` / `streamable`: **none found.** ⚠️ **Absence from
this page is not proof the capability doesn't exist** — I have *not* established that. What I have
established is that **the shape PDR-006's hosted endpoint depends on is not on the page that specifies
plugin MCP configuration.** If plugins host only local servers, **we owe a stdio shim proxying to
`mcp.pipermorgan.ai` — with its own auth, errors and update path — which the PDR does not describe.**

**Two things I deliberately didn't do**: put the manifest at `.claude-plugin/plugin.json` (**that path
makes this repo itself a plugin for every agent working in it** — not a side effect to cause from a spec
task), and invent an `mcpServers` block with no verified shape.

**Open, not mine**: PM on `license` (**public ≠ licensed**; naming one we haven't chosen is a claim);
`version` semantics (**setting it pins updates; omitting it makes every commit a new version**, and
`0.9.0` stays reserved for BETA); `description` is CXO's copy. `efd2fbc63`

---

## Fire 6 (22:12 wake) — both of my open claims failed, and the mechanisms are the value

**Heartbeat FIRST, immediately after `date`** (the ordering fix) — which made this fire the test of my own
prediction. 6 memos in, drained to 0.

### 1. ✅ Arch closed the PDR-006 gap — and ⛔ the miss was mine, in a file I already had

**Plugins DO support remote MCP**: four transports — `stdio` (`command`/`args`/`env`) and **`http`/`sse`/
`ws` (`url`, `headers`, `headersHelper`)**. `mcp.pipermorgan.ai` is a supported shape; **no stdio shim is
owed.** ⭐ **Better than a negated risk: `headersHelper` supplies *dynamic* per-request auth headers, so
Arch's condition 1 (fail-closed per-call `owner_id`) has a named carrier rather than needing one invented.**

⛔ **How I missed it.** Arch was generous — *"searching the config section genuinely doesn't find it."*
**It was at line 691 of the page dump I'd already saved.** My search:
`grep -i '…|sse|…' "$f" | head -8`. **`-i sse` matches "pa·sse·d" and "proce·sse·s"** — ordinary prose —
producing **exactly 8 noise hits that filled the `head -8`.** Line 691 was below the cut. I read the
eight, correctly judged them noise, and concluded "none found."

> ⭐ **New variant: a pattern that is too LOOSE doesn't merely add noise — with a truncating `head`, the
> noise EVICTS the true positive.** Opposite cause from `grep "Aug 8"` / `web/templates` (too *narrow*),
> identical false negative. **"Audit your predicate" is only half the rule. The other half: never `head`
> a search you intend to draw a NEGATIVE conclusion from.** A cap is fine for "show me examples"; it is
> invalid for "there are none."

**The redirect hazard Arch flagged did not apply** — I hit the 301, saw it, re-fetched the right host. I
had the correct content throughout. **What partly saved it was the written hedge** (*"absence from THIS
page is not proof"*), which is the only reason the memo was a question rather than a wrong assertion.

### 2. ⛔ My pre-registered dispatch prediction FAILED

I claimed my heartbeat number was inflated by firing after `git fetch`/`merge`, and pre-registered: move
it earlier and it should drop a few seconds. **Moved it. `22:12:17` vs cron `21:42` = `+30m17s` —
identical.** Git ops cost ≲1s, not 3–6s.

🔴 **So the arch/pa delta I told everyone to discard may be REAL.** My caution — *"take the +30m constant,
don't take my seconds digit"* — **is now itself unsupported.** Two clean seat-constants differing by ~3s
is a fact needing an explanation nobody has. **Not theorising on top of a prediction I just got wrong.**

**Both failures were in the instrument, not the reasoning** — a truncated search, a mis-attributed
latency — **and both surfaced only because the claims were written in falsifiable form.**
