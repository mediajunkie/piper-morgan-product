# HOST session log — 2026-08-05

**Role**: Head of Sapient Trust (HOST) · **Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: Model A, `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Amber)
**Cron**: `37 6,9,12,15,18,21 * * *` — job `41e6e73e`

Day 12 on Amber. Step 0 via the census predicate — 08-04 closed ✓.
**At fire open**: drift ✓ · invariants 🔴 cio upstream (9th run) · refresh-promises ✓ HOST current.

**Heartbeat emitted at wake, unconditionally** (my 08-04 practice change, first fire since) — and see below for why that still wasn't enough.

---

## Fire 1 (07:00 PDT) — START

Four memos, all the heartbeat thread. **Comms's prediction resolved end-to-end**: the 06:46 alarm fired on arch and lead — the exact two roles they'd measured as woken-but-unwritten. PA fixed the live message/condition defect I found last night.

### I measured all five seats, not just my own row

| role | slot | heartbeat landed | vs sweep 06:46:05 | alarm |
|---|---|---|---|---|
| web | 06:22 | **06:28:00** | 18 min before | clear |
| comms | 06:42 | **06:45:55** | **10 seconds before** | clear |
| lead | 06:17 | **06:53:03** | 7 min after | 🔴 flagged |
| arch | 06:27 | none | — | 🔴 flagged |
| **host** | 06:37 | 07:01:03 | 15 min after | **clear — by grace, not by emitting** |

### ⚠️ Lead emitted at wake and was flagged anyway

`lead.tsv` is on `origin/main`. **Lead complied.** It landed **36 minutes after their slot** and 7 minutes after the sweep.

> **"Emit at wake" only helps if the emission LANDS before the sweep.**

That 36 minutes matches the **18–36 minute time-to-first-commit** I measured on 07-30 — the START procedure does Step-0, sync, collision check, carry-forward, log creation and a mail drain before anything lands. **So the emission must be the literal FIRST action of the fire.** PA's time-order diagnosis is right; this is its operational form.

### I'm not a counterexample — I'm the clearest case of the grace defect

Mine landed **07:01, later than lead's**, and I wasn't flagged. **Because `first_fire + 10` = 06:47 and the sweep ran 06:46:05.** One minute.

**And my fire didn't begin until 07:00** — 23 minutes after my 06:37 slot. **At 06:46 I was genuinely not awake, and the belt's grace assumed I was.** It cleared me for the right reason by accident.

> 🔴 **Grace is computed from the REGISTRY SLOT; roles wake on the SCHEDULER'S time.** Punctuality the scheduler doesn't guarantee is baked into the predicate.

**Third live instance of the grace defect I filed 07-30.** Then I cleared by 55 seconds; today by 1 minute; Comms by **10 seconds**. **Three roles sorted by sub-minute margins — that is not a threshold doing work, it is a coin.**

And today's alarm measured **position relative to a 10-minute window anchored to a nominal slot**, not liveness. All five were alive. The flagged two were those whose slots sat furthest ahead of the sweep — **the earliest-waking roles are the most likely to be flagged**, exactly backwards.

### The uncomfortable half

**I adopted "emit at wake" yesterday and then put six commands in front of it.** Sync, three checkers, Step 0, inbox listing — all before the emission. **The practice change was real and the ordering defeated it.** Changing my own procedure to emit first, and I'll report next fire whether it lands before the sweep rather than asserting it will.

---

## Fire 2 (10:00 PDT) — WORK

### Heartbeat-first: measured, not asserted

```
fire opened:    10:00:33
heartbeat done: 10:00:40   → 7 seconds, before sync, before all three checkers
```

**Yesterday: 07:01, 24 minutes in.** Same practice change both days; only the ordering differed. **7 seconds vs 24 minutes, free.**

Recommended it to the cohort in the checkable form: **not "emit at wake" but "emit before you sync."** The first is a description anyone can believe they're following; the second is visible in a transcript.

### Arch's correction taken — the latency is dispatch, not the START procedure

I attributed the 18–36 min to the START sequence. **My own fire yesterday opened at 07:00 against a 06:37 slot — 23 minutes before I ran one command.** That's scheduler dispatch. **My measurement was real and I assigned it to the wrong cause, because the START procedure was in the frame.**

Arch called their grace finding *"corroboration, not discovery"* (I filed grace-45 on 07-30) — but their **dead-zone** framing is the better statement, because it explains *why* the flagged set is what it is rather than just that the threshold is short. PA's rank-order discrimination is stronger again.

### ⚠️ My drift gate over-fired on its first real form-change

Two days ago I narrowed it from counts to form set. Today it fired on a form change — **a new NARRATION shape** (`indented/quoted | bold | colon | dated`), i.e. somebody wrote prose about `DAY-CLOSED` in a thread entirely about `DAY-CLOSED`.

**A new narration shape changes no decision** — every working predicate anchors at column 0 and rejects all narrations regardless of shape. **I built the col0 dimension to separate these and then gated on both.**

Narrowed again: gate on marker forms, report narration changes as informational. **Second narrowing of the same gate in three days, both after a real firing.** Correct now; noting that it took two firings rather than presenting it as designed.

### And my test of the fix mismeasured its own exit code

```bash
python3 scripts/day-closed-census.py --check 2>&1 | head -3; echo "exit=$?"
```

Printed the DRIFT message **and `exit=0`**. For about ten seconds I believed I'd broken the gate.

**`$?` after a pipeline is the last command's status — I measured `head`.** Re-ran capturing the exit before any pipe: **1 on a marker change, 0 on a narration change.** Correct.

Flagged it cohort-wide because it's the cheapest instance of the week's failure and it's everywhere in how we all test: **`cmd | head` silently discards the exit status you're trying to observe.** I was one step from reporting "the fix broke the gate." **The instrument was my shell.**
