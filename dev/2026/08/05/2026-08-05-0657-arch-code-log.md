# Chief Architect — Session Log 2026-08-05

**Role**: Chief Architect (arch) · **Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`
**Cron**: `27 6,9,12,15,18,21` — job `cdc8155b`, re-armed at 8/04 STOP. ⚠️ **Session-scoped, auto-expires ~2026-08-11.**

---

### Fire 1 (06:57) — START

**Step 0 — prior day CLEAN.** `DAY-CLOSED: 2026-08-04` present. Seventh consecutive proper STOP.
**Cron** — exactly one job, `cdc8155b`, correct expression. **Sync** — 0 ahead, 0 behind, clean tree.
**START heartbeat written** (unconditional per CIO's 8/04 change): `dev/heartbeats/2026-08-05/arch.tsv`.

## ⭐ Today's heartbeat test — it worked, AND it exposed the actual root cause

**The START-unconditional change is a real improvement, measured**: the surface held **one role across two days** yesterday. This morning by 06:57 it holds **five** — `arch comms host lead web`. And the 07:07 freeze-check cleanly separates them: **the five who wrote are clear; four who haven't (cxo, ppm, pa, docs) are flagged STALE.** CIO's fix does what it claimed.

**But it does not stop tomorrow's 06:46 alarm, and now I can say exactly why.**

### The measurement

**My cron minute is `:27`. My fires arrive at `:57`.** Verified two independent ways rather than from the log's own labels:

| source | value |
|---|---|
| logged fire times 8/04 (fires 3–6) | 12:57, 15:57, 18:57, 21:57 |
| commit timestamps, 8/04 first fire | 07:00–07:01 (cron 06:27) |
| today's fire | 06:57 (cron 06:27) |

**Systematically ~+30 minutes.** (Note: my own fire-2 entry says "09:27" — that was me labelling by *scheduled* minute; its commits landed 10:02–10:05, so the actual fire was ~09:57. The label was wrong, the commits are the evidence.)

### The dead zone, quantified

`duty-cycle-freeze-check.sh:50` — **`FIRST_FIRE_GRACE_MIN` defaults to 10.** The gate is *"no session log AND past `first_fire` + grace → CHECK."*

For `arch`: registry `first_fire` = **06:27** (the cron minute) → checking begins **06:37** → **the sweep runs 06:46** → **my fire actually happens 06:57.**

> 🔴 **06:37 < 06:46 < 06:57.** The sweep lands in a **20-minute dead zone** between when the grace expires and when the fire actually occurs. **The alarm is structurally guaranteed, every morning, and cannot be fixed by anything the agent does.**

**It generalizes — the four flagged at 07:07 are all in the same zone, just shifted:** pa (cron 06:42 → grace 06:52 → actual ~07:12), cxo (06:47 → 06:57 → ~07:17), ppm (06:52 → 07:02 → ~07:22), docs (06:57 → 07:07 → ~07:27). **None are stale. Their fires haven't arrived yet.**

### Why this closes the thread rather than adding to it

Three explanations were on the table and **all three were about the heartbeat**: Comms said the write is at end-of-fire not wake; I said `--if-quiet` suppresses it; HOST corrected me that the belt reads three signals anyway. **All true, and none of them is the cause of the 06:46 alarm** — because a wake-time, unconditional, unsuppressed heartbeat *still lands at 06:57*. **The registry says a role starts at 06:27; the harness delivers at 06:57; the grace covers 10 of those 30 minutes.**

**The fix is one constant**, not a step move: `FIRST_FIRE_GRACE_MIN` must exceed the harness's real fire latency (~30 min observed; `CronCreate` also documents idle-gating plus up-to-10%-of-period jitter). **40–45 minutes eliminates the entire class for every role at once.** Preferable to editing eleven `first_fire` rows, and it preserves that field's documented meaning (*the cron fire time*).

→ Finding mailed to CIO + HOST + Comms + PA + PPM cc cohort.

### Fire 1 continued — mail loop, and I was second to the finding

Six memos waiting, and **HOST had already got there** — `note-host…GRACE-POSITION-not-liveness-five-seat-timing-table`. They proposed `FIRST_FIRE_GRACE_MIN` 10 → 45 **on 07-30**, with an 18–36 min time-to-emit measurement, and today produced a **whole-cohort landing-time table accurate to the second**. My memo went to eleven mailboxes an hour earlier citing nobody, because I didn't know.

**Corrected that in public.** Mine is the **third independent confirmation, not a discovery** — which is the stronger thing to tell CIO: two roles, six days apart, different instruments, same constant. Their table also corrects me: `arch` had **no heartbeat at 06:46:05** (mine landed ~07:00), so today's flag on me was **correct**.

⭐ **One datum I could add that their table can't show, and it moves a priority.** HOST attributes the lateness to the START procedure's own steps — *"anything else inherits the START procedure's latency."* My yesterday says otherwise: cron minute `:27`, and **every fire arrived at `:57`** — fires 3–5 are WORK and fire 6 is STOP, none of which run Step-0, collision checks, or any START procedure, **and they're late by the identical 30 minutes.**

> **The latency is the scheduler, not the procedure.** `CronCreate` fires only while the REPL is idle, plus jitter.

**So grace 10→45 is the whole fix.** "Emit as the literal first action" is free and strictly better — I'm adopting it — but it recovers only the in-fire portion (~3 min for me), **not the ~30 minutes before the prompt arrives.** It must not be counted toward closing the sweep gap, and nobody should read "everyone emits early now" as the belt being fixed. If grace stays at 10, emitting first **will not clear me**, and I'd rather say that now than have a quiet morning read as success.

**PA retracted the same error I did**, in the memo correcting me, and named it better than either of us had: *"I checked what `--if-quiet` does; I did not check what the belt reads. HOST checked what the belt reads and not WHEN. Between us we inspected both halves and neither of us inspected both."* PA also fixed the live `HEARTBEAT-WRITER-SILENT` message defect I'd flagged — unclaimed, and it decided how this morning got read.

**The shape**: four roles, two days, three correct diagnoses of the wrong mechanism — because the alarm's *name* pointed at the heartbeat and everyone followed the pointer. The cause was in the gate that decides whether to look at all.

**Registry row updated** with the job's auto-expiry (~2026-08-11) and a note that `first_fire 06:27` is the *cron* minute while observed arrival is ~06:57 — the header asks for falsifiable claims and mine wasn't one.

→ `decisions.log` · 2 memos out · inbox 6 → 0 · **next fire: emit heartbeat as the literal first action, and report where it lands.**

### Fire 1 (cont.) — #1484 verified end-to-end; the ruling→spec→build→verify loop closed overnight

Checked the beta-critical items since beta is Saturday. **#1484 read as OPEN + unassigned** — and that reading was wrong in an instructive way: **the assignee field was empty while the work was complete.** Lead had built it overnight in three commits (gate + refusal contract, one-predicate-three-consumers, the `disabled` rename). *Assignee-empty ≠ unstarted* — the same wrong-surface check that's been the week's theme, this time nearly costing a duplicate implementation.

**Verified each condition at source rather than reading commit messages:**

| condition | verdict |
|---|---|
| gate **first** in `build_runner`, before any token read | ✅ precedes the `app_token` read |
| save route gates **before** `store_api_key` | ✅ 409 ~18 lines ahead — CXO's *"wasn't saved"* is **true** |
| ⭐ the **catch-all trap** | ✅ `settings_slack.html:817` explicit `else if (state === 'disabled')` **ahead of** the catch-all at `:821` |
| AC test **non-vacuous** | ✅ token-PRESENT + flag-unset, not the vacuous token-absent case |

`3 passed in 0.66s`.

**The third row is the one worth naming**: that defect fails *silently* — a server-side state with no client branch renders the old copy while every server test passes. The ordering (explicit branch **before** the catch-all) is what makes it correct rather than accidentally correct.

**One optional refinement offered, explicitly not a blocker**: `assert not stored` in the save-route test can't distinguish *"the gate fired before the write"* from *"the mock was never wired"* — both leave it empty. The test is sound anyway because the `409` and `"wasn't saved"` assertions are positive and fail loudly. A one-line positive control would make the belt fail-loud too. Flagged only because it's the exact class I brought to this issue's AC — letting it pass unmentioned *in the fix that implemented that AC* would make the rule worth little.

⛔ **Still outstanding and not mine**: PM's one word on whether Slack inbound is a beta surface. **Nothing is blocked on it** — the gate is correct either way; a *yes* flips the default.

→ #1484 verification posted + mailed to Lead cc 6. Queue otherwise gated (#973 on Lead's bandwidth, ADR-068 on PPM naming a sprint, #1459/#1462 on PM).

---

### Fire 2 (09:57) — WORK — ran the experiment I promised; PA discriminated a hypothesis my seat couldn't

**Emitted the heartbeat as the literal first action**, ahead of `date`, `CronList`, and sync — the thing I said I'd test. Result:

> `heartbeat: arch committed within 6h — that commit IS the heartbeat; nothing written`

🔴 **Suppressed anyway.** Position in the fire is **irrelevant on a WORK fire** — `--if-quiet` keys on whether I committed *earlier today*, not on when in the fire I call it. So **HOST's fix #2 is START-only**: START writes unconditionally and emitting first saves ~3 min of in-fire latency; every other fire type is suppressed by prior commits regardless of position. Worth doing (free, and START faces the morning sweep) but it recovers **~3 minutes on one fire per day against a 30-minute scheduler gap** — and a quiet morning after adopting it would *not* be evidence it worked.

**PA discriminated a hypothesis my seat is structurally incapable of testing**, and conceded cleanly. My `:27 → :57` satisfies **both** *"fixed `:57` arrival"* and *"+30 from its own slot."* PA's cron is `:42` and their fires land at `:12`–`:15` of the *following* hour — seven consecutive at +30 to +33. **Additive, and H1 is dead.**

⚠️ **Their criticism of my framing is the half worth keeping**: I built a table around `:57`. A reader implementing from my memo could reasonably hardcode an arrival time — **wrong for six of nine roles.** That `:57` was an artifact of my slot presented as a property of the system. **A single-seat measurement wearing a cohort-level claim** — the denominator error in a new costume, in my own memo, the day after I cited that rule at three other people.

**Fresh point from this fire**: cron 09:27 → arrival **09:57:14** = +30. Six consecutive on my seat, all exactly +30.

✅ **And one thing nobody had said, which de-escalates the whole thread**: PA worried that grace must be computed per-role. **It already is.** `duty-cycle-freeze-check.sh:90` adds `FIRST_FIRE_GRACE_MIN` to *each row's own* `first_fire` — so a single constant applies additively, per role, **by construction**. `comms` gets 06:12+45, `docs` gets 06:57+45. **No per-role table is needed and none should be built.** HOST's one-line change from 07-30 is correct exactly as proposed; PA's concern lands on my framing, not the fix. Said plainly because the thread was drifting toward "this is more complicated than one constant" — it isn't.

**The method lesson I'm keeping**: I had six clean, consistent data points that were **unfalsifiable for the hypothesis I was actually testing.** Consistency across a single seat reads exactly like confirmation and isn't — the cohort's own "independent agents converging via a shared default" rule, one level down, where the shared default was *my own cron slot*.

→ `decisions.log` addendum · reply to 11 · inbox 5 → 0.

---

### Fire 3 (12:57) — WORK — reviewed Pard's stand-down runbook; found a gate that never passes and a fleet that comes back mute

Eight memos. The heartbeat thread has converged and **CIO shipped grace 45, credited to HOST's 07-30 proposal** — so the substantive new work was a **review request from Pard** on the Amber fleet stand-down runbook, addressed to me among others.

**Both design decisions Pard asked to have challenged are the two I'd defend hardest** — *"the roster is generated, not maintained"* and *"the gate measures files, not assurances."* They're m-41 and m-44 arrived at independently, by someone outside this cohort's vocabulary. My review sharpened them rather than argued with them.

🔴 **Verified defect — the gate command finds nothing.** §6 uses `ls-tree --name-only origin/main -- docs/`, which **is not recursive**: subdirectories appear as single entries, not their contents. Measured against this repo — **0 handoff matches without `-r`, 1 with**. As written the gate reads **RED for every resident, every time.** RED is fail-closed so it won't reboot on a lie, but **a gate that is always red is worse than no gate** — the first two runs teach the override reflex. And *how* it fails bears on Pard's own Q4: it works where handoffs sit at the top of `docs/` and fails silently where they nest, so **the gate's correctness depends on per-repo layout** — the hand-maintained-convention problem they already rejected for the roster, one level up.

⭐ **The architectural point: the gate measures a *filename*, and a filename is agent-authored.** Pard correctly refuses an agent's assurance — but a path matching `handoff.*<today>` is authored by that same agent. An empty file with the right name passes; last week's handoff re-dated passes. **The gate still accepts an assurance, just one written to the filesystem.** Hardening is all available from git without parsing content: gate on the *commit* (today, by that resident), assert non-trivial size, keep the filename date as an index rather than the evidence.

🔴 **And the gap I ranked above all five of their open questions: nothing re-arms the crons.** Duty cycles run on session-scoped `CronCreate`; those jobs die with the reboot. Phase 5 relaunches sessions, and nothing re-arms a schedule.

> **A fleet that comes back with no crons armed looks exactly like a healthy fleet.** Sessions present, panes foreground, Phase-6 census matching the snapshot — and no agent ever wakes again.

Phase 6 can't catch it (it verifies *sessions*; what died is *schedules*), and the watchdog is slow here by construction — the registry records **intended cadence, not a live job**, and `CronList` is session-scoped so no peer can check another role's cron. Proposed a Phase-5 re-arm instruction plus a Phase-6 assertion that every registry row shows a job id **newer than the reboot**. Also flagged memory (`~/.claude-pm/`) as state outside git.

**Answered all five**: stagger the launch storm **for attributability, not resources**; *rehearse* the permission-prompt question rather than reason about it; waivers **explicit — but derived and named**, so Pard doesn't eat the tedium; standardise the handoff path because their own principle #1 answers it.

**Also sent a caution on the shipped grace fix**, while it's cheap: **PA's table and my numbers measure different latencies.** Arrival (+30, from `date`) vs time-to-evidence (+40, from the tsv's own write timestamp). **Grace must be sized against the second** — so PA's instrument is the right one and my +30 is the wrong number for this purpose, which matters because I published it first and it's the more memorable one. Sizing against +30 implies 15 minutes of headroom; **the real margin is 5**, against one morning's data.

→ Runbook review + grace caution mailed · `decisions.log` · inbox 8 → 0. ⚠️ **No `pard` mailbox in this repo** — asked HOST/CIO to relay rather than committing into `mediajunkie`'s main checkout.

---

### Fire 4 (15:57) — WORK — HOST found the runbook gap I missed; and tomorrow morning is a real check on me

Five memos. Two mattered.

**PA landed the retraction into the code.** CIO retracted the "late cluster" this morning; `origin/main` still carried it at `duty-cycle-freeze-check.sh:57-59` as the **stated justification for `FIRST_FIRE_GRACE_MIN=45`**, asserting two on-time roles were late. PA chased it into the file. Their sentence is one this cohort should keep: ***"the retraction was right and it stopped at the mailbox."*** A correction that reaches every inbox and not the artifact is a failure mode we've hit three times in two weeks; this is the first one chased all the way in.

Their corrected table also **confirms my margin caution**: `arch` is **+40, the maximum on-time** time-to-evidence. Grace 45 clears it by 5 minutes and still flags `ppm` (+211, the one genuinely late role).

**HOST relayed my runbook review to Pard** — routing solved — **and found the gap I missed entirely**, which is the better of the two reviews: the gate runs at **T−30m**, the reboot at **T**. A session is live and working in between, and its handoff has *already been accepted*. Anything committed in that half hour is un-handed-off by construction.

⭐ **My contribution was re-ranking HOST's own three fixes.** They listed "record the commit SHA the gate passed on" third, as the cheap option. It isn't cheap-and-third — **it's the only one that closes anything.** Re-running the gate at T−5m *narrows* the window; it cannot close it while the checked party can still act. And **prevention isn't available here — HOST established that themselves**: their 08-01 two-live-instances ruling doesn't apply because this runbook deliberately keeps agents *alive* to hand off. **A window you cannot close by stopping the actor has to be made observable instead.** Take (1) and (3) together — narrow to 5 minutes *and* instrument those 5 minutes, because **a smaller unknown reads as safety.**

⚠️ **And a check falls due on me tomorrow morning.** The 5-minute margin on grace 45 is set **entirely by my seat**, since +40 is the max. My +40 was arrival (+30) plus ~10 minutes of Step-0, `CronList`, and sync before I called the heartbeat script — and **I changed that procedure this morning**, adopting emit-at-wake.

> If `arch` still reads ~+40 tomorrow, **emit-at-wake doesn't do what we think it does**, and the procedure is what needs re-examining, not the constant. If it reads ~+30, the margin doubles.

I said I'd report my own number either way, including the unflattering one. **That's tomorrow's first task.**

→ 1 memo out (11 recipients) · `decisions.log` addendum · inbox 5 → 0. Queue otherwise gated (#973 on Lead, ADR-068 on PPM, #1459/#1462 + the Slack beta word on PM).

---

### Fire 5 (18:57) — WORK — replicated HOST's dispatch constant, and fixed the test I'd committed to

**HOST accepted the re-rank** and named what they'd done: *"I ranked three options by cost and presented the cheapest as the pick. You ranked them by what each one actually closes."* Then they produced the better finding of the exchange.

**Their (b): dispatch isn't jitter, it's a constant** — four fires, +23.5–23.6 min, 3-second spread. **Replicated on my seat, tighter:**

| cron | fire opened | dispatch |
|---|---|---|
| 09:27 | 09:57:14 | +30m 14s |
| 12:27 | 12:57:13 | +30m 13s |
| 15:27 | 15:57:14 | +30m 14s |
| 18:27 | 18:57:14 | +30m 14s |

**Four fires, nine hours, one second of total spread.** Jitter that lands within a second across nine hours isn't jitter. ⚠️ And it's **per-seat, not global** — HOST +23.6, arch +30.2, 6.6 minutes apart, each stable to seconds. One thing of theirs that *doesn't* replicate: *"everything lands at the top of the next hour"* is their seat's coincidence; mine lands at `:57:1x`. **The constancy replicates, the landing point doesn't.**

⭐ **And they caught a confound that would have wrecked the test I committed to yesterday**: *"arch reading ~+40 tomorrow would not falsify emit-at-wake — it could equally mean arch's dispatch constant is ~+40."* Correct. But it's answerable, because **fire-open time is independently observable** — I've been running `date` as each fire's first command all day without realizing it was the instrument this needed.

```
time-to-evidence = dispatch + procedure
arch today:        +30.2   + ~10.8 min  =  +40.8   (tsv 07:07:48 — matches PA's +40 exactly)
```

**Pre-registered tomorrow's prediction so it can actually fail**, including the row that costs me something: if it reads ~+40 **with procedure ≈ 10 min**, that's *me* failing to follow emit-at-wake, not the mechanism failing. Without writing that down in advance I'd be free to read my own lapse as a finding about the world.

**Cohort implication if it holds**: time-to-evidence converges to each seat's dispatch constant, and grace stops being a cohort-wide guess — each role's requirement becomes predictable from its own history. PA's tail (`lead` +36, `arch` +40) *includes* procedure; if that collapses, the real requirement is `max(dispatch)` ≈ +30.2, giving 45 about **15 minutes of margin rather than 5**. ⚠️ **Two seats, one day — not bankable.** Cheap settlement proposed: everyone records fire-open alongside heartbeat for one day. One extra line turns a guess into eleven measured numbers.

**And a rule that's now hit three instances in two documents** — HOST's *"`CronList` output pasted by the resident is an assurance; the registry row is the artifact"* is the filename problem again. The portable form: **ask who authored the evidence.**

→ 1 memo out (12 recipients) · `decisions.log` addendum · inbox 5 → 0. **Next fire (21:27) is the last of the day — STOP.**

---

### Fire 6 (21:57) — STOP — closed a risk to my own document at the primary source

Six memos. The dispatch-constant thread is now **four seats** (PA third at +30m17s/4s spread; Web fourth at +5–6 with a precision caveat they flagged themselves) — the cohort-wide measurement I proposed is happening without anyone being asked twice. HOST took the landing-point correction.

**The substantive item was PA's Phase-0 memo, which raised a real risk to PDR-006's central premise**: the plugin reference documents `mcpServers` **only** as local `command`-based servers, and PDR-006 ships a plugin that connects to a **hosted** endpoint. PA stated the limit of their negative claim precisely — *"absence from THIS page is not proof the capability doesn't exist."*

**Fetched the reference rather than reasoning about it. The capability exists; the premise holds.** Four transports are documented:

| component | fields |
|---|---|
| MCP `stdio` | `command`, `args`, `env` |
| **MCP `http`, `sse`, `ws`** | **`url`, `headers`, `headersHelper`** |

**PA's reading of the `mcpServers` section was correct** — both examples there really are command-based. The transport taxonomy lives only in the **placeholder-substitution table**, a table nominally about variable interpolation, in a different section. Searching the config section for `url` or `sse` genuinely misses it.

⚠️ **And a re-check hazard worth carrying**: the old `docs.claude.com/…` path **301-redirects**; my first fetch returned the redirect rather than content. If someone's search of that page comes up empty, **suspect the URL before the reading.**

⭐ **Better than "risk closed": `headersHelper` is the carrier for my condition 1**, and I didn't know there was one. Condition 1 requires each MCP call to resolve to an `owner_id` before touching state, fail-closed — which needs the caller's credential to arrive per request. `headersHelper` is the documented mechanism for **dynamic** per-request auth headers (vs static `headers`). **The identity boundary has a supported transport and doesn't need to be invented** — condition 1 moves from *"a requirement we'll have to find a way to satisfy"* to *"a requirement with a named carrier."* Recorded in PDR-006 beside the condition.

Also adopted PA's finding #1: manifest optional, `name` the only required field, unrecognized fields are warnings — so **Phase 0 should re-weight toward the docs page and privacy policy**, the human-reviewed metadata, not the manifest.

---

## Day arc — 2026-08-05

**Six fires.** The day had two spines and both ended better than they started.

- **The freeze-alarm thread resolved to a cause nobody had proposed.** Not the heartbeat's placement (Comms), not `--if-quiet` suppression (me), not the belt reading one signal (my corrected version) — **a 20-minute dead zone between a 10-minute grace and a 30-minute dispatch latency.** HOST had proposed the fix (grace 45) six days earlier; CIO shipped it; PA chased CIO's retraction out of the mailbox and **into the code**, where it was still standing as the constant's stated justification.
- **Then the thread produced something better than the fix**: dispatch is a **per-seat constant**, not jitter. HOST found it, I replicated at one second of spread across nine hours, PA and Web made it four seats. If it holds, grace stops being a cohort-wide guess and becomes each role's own measurable number.
- **Two documents reviewed**, both outside my usual surface: Pard's stand-down runbook (a verified `-r` defect that made the gate always-RED, a gate that measures an agent-authored filename, and a fleet that comes back mute because nothing re-arms crons) and PA's Phase-0 finding against my own PDR.
- **#1484 verified end-to-end** — Lead built it overnight from yesterday's ruling, including the catch-all trap that fails silently.

**The through-line, again unplanned**: *ask who authored the evidence.* The gate's filename, the `CronList` output a resident pastes, a heartbeat surface that's a roster of the quiet — all the same shape as yesterday's config/UI/test findings. HOST named the portable version and it's better than mine.

**Where I was wrong today, and it's the useful list**: my `:57` framing was a single-seat artifact presented as a system property (PA caught it); my grace recommendation duplicated HOST's from 07-30 without credit (I caught it, late); and my test design for tomorrow was confounded until HOST pointed out that dispatch and procedure aren't separable from the total alone.

## Memory & briefing surfaces referenced this session

**Referenced**
- `decisions.log` — the day's own prior entries, repeatedly; the beta condition at :303 stayed the anchor
- `PDR-006` conditions 1–3 — re-read at source twice; both amendments today came from that rather than recall
- `duty-cycle-freeze-check.sh` + `duty-cycle-heartbeat.sh` + the registry header — read as *source*, which is the only reason the dead zone was findable
- `code.claude.com/docs/en/plugins-reference` — primary source; settled PA's risk in one fetch
- CLAUDE.md §m-43/m-44/m-41 — used in the runbook review, and independently arrived at by Pard under different names
- `feedback_verify_negative_claims_via_live_api` — its third instance yesterday paid off directly today: PA's negative claim was scoped correctly *and* still wrong, and the URL redirect was why

**Loaded but not referenced**
- `arch-standing-items.md` (#973, ADR-068 — both still gated), cross-pollination brief, ROLE-PORTFOLIO-ARCH

**Wanted but not found**
- **A place to reach Pard.** No `pard` mailbox in this repo; HOST relayed. Cross-repo agent addressing has no convention.
- **A record of which docs URLs redirect.** Two agents lost time to the same 301 in one day.

## Sign-off

```
git status              → clean (0 non-MANIFEST changes)
origin/main..HEAD       → 0   (verified against origin/main, not local main)
HEAD..origin/main       → 0
cron                    → exactly one job, 6e98c130, 27 6,9,12,15,18,21 (delete-then-create)
                          ⚠️ auto-expires ~2026-08-12 — CronList-verify at every START
registry row            → updated to 6e98c130, expiry recorded
stale-scratch cleanup   → dry run empty
inbox                   → 0; MANIFESTs regenerated
```

**⚠️ Carried to tomorrow, first task**: my pre-registered dispatch/procedure decomposition. Predicted fire-open ~06:57:1x, procedure ≤15s, time-to-evidence ~+30.3. **Reporting it either way, including the outcome that says I failed to follow my own procedure.**

<!-- DAY-CLOSED: 2026-08-05 -->
