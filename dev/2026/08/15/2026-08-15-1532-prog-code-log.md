# Session Log — 2026-08-15 15:32 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1621 — live-verification fixture: programmatic login + real-turn driver against a locally-started server
**Branch**: worktree-agent-a39cdb3f3e037ce61 (isolated worktree; Lead merges after review)

## Plan

Issue #1621 spec: (a) server lifecycle (env-stripped launch, health-wait, teardown);
(b) throwaway users with real logins → cookie jars; (c) turn driver (POST /api/v1/intent,
parsed response); (d) FK-aware cleanup verified by count.

Investigation findings (verify-first):
- Login: POST /api/v1/auth/login (form data username/password) → auth_token + refresh_token cookies (web/api/routes/auth.py:77).
- Turns: POST /api/v1/intent, JSON {message, session_id}; auth via cookie or bearer (web/api/routes/intent.py:299). Returns 200 even on degradation — degraded shapes carry error_type (service_unavailable / anonymous_key_required / session_expired); driver must refuse to fake-pass on those.
- Port: PIPER_PORT env var (main.py:37); 8001 busy with another session's server (left alone) — fixture picks its own free port.
- Existing infra to reuse, not duplicate: tests/e2e/conftest.py (in-process ASGI user-create + login pattern) and tests/conftest.py delete_test_user_fully (FK-ordered cascade). The #1621 delta: real subprocess server (env-stripped) + count-VERIFIED cleanup (the 08-13 false-clear lesson) + generic information_schema sweep to catch tables the curated cascade misses.
- Worked example: reminder create + clarify — pre-classified deterministically (pre_classifier.py:1548 action="create_reminder"), no LLM needed; "at 25:99" hits the #1490 unbindable-clock clarify branch deterministically.

## Work

- Fire 1 (15:32) — investigation complete (routes, models, e2e infra, cleanup cascade, reminder path); building tests/live/ fixture family.
- 15:37 — Built `tests/live/` fixture family (conftest.py: live_server env-stripped subprocess + health-wait + teardown-guaranteed; live_user throwaway w/ real bcrypt hash; turn_driver real-login cookie jar + degradation-refusing turn driver; count-verified FK cleanup incl. generic information_schema sweep) + worked example `test_reminder_live.py` (reminder create + #1490 clarify, both DB-asserted). Added `live` marker to pytest.ini. Opt-in gate verified (2 skipped without PIPER_LIVE=1).
- 15:37 — **First live run: 2 passed, 2 cleanup-verification ERRORS — the count check caught a REAL false clear on its first firing.** `delete_test_user_fully` left the users row. Probed: `session_activity.owner_id` is VARCHAR in the dev DB (model declares UUID — #1603-class drift) → `CAST(:u AS uuid)` raises → aborts the asyncpg transaction → all ~27 subsequent statements (incl. users delete) silently no-op behind `except: pass`. Measured blast radius: **38 leaked `e2e_test_%` users** in dev Postgres.
- 15:38 — Fixed `tests/conftest.py::delete_test_user_fully`: commit-per-statement + rollback-on-error (abort can no longer poison the cascade); session_activity predicate → `owner_id::text = :u` (type-robust both directions). Re-run: tests/live 2 passed, cleanup verified 0 rows. Transcript captured via new PIPER_LIVE_TRANSCRIPT=1 flag (real request/response of both turns). Server log confirms fresh boot → login → 2 intent turns → auth/me → clean shutdown; nothing left running (ephemeral port, never touched the dev server on 8001).
- 15:40 — Ratchets: tests/test_completion_ratchets.py + tests/test_architecture_enforcement.py → **46 passed**. e2e regression check on the shared-cleanup change: tests/e2e/test_auth_flow_e2e.py → **6 passed**.
- 15:42 — Discovered-work filed: **#1629** (session_activity drift + cascade abort-poisoning + 38-user sweep). `bd` CLI not present in this worktree env; used `gh issue create`.

## Honest scope

- Verifies the real-turn HTTP layer (m-43 named in every file header). Does NOT verify rendered UI (#1480 browser half stays manual). LLM-routed turns depend on server-side Keychain credentials; the worked example deliberately uses the deterministic pre-classified reminder path so the harness itself never depends on LLM availability, and the driver refuses degradation-shaped 200s as passes.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md #1258 env-stripping recipe (server launch env scrub); m-43/m-44 (layer-naming headers + count-verified cleanup design); 2026-08-13 lead log false-clear postscript (cleanup-count requirement + varchar/uuid ::text recipe); tests/e2e/conftest.py + tests/conftest.py (verify-first reuse, not duplication); pre_classifier.py #1521/#903 (deterministic worked-example choice); feedback_pause_before_irrevocable_actions (left the port-8001 server alone; ephemeral port instead).
- **Loaded but not referenced**: mailbox/duty-cycle/blog-publishing memory entries (not this lane).
- **Wanted but not found**: none — the #1532 hand-roll recipe was findable in the 08-13 lead log.
