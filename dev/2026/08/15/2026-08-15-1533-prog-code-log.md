# Session Log — 2026-08-15 15:33 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Implement #1625 — "Reminder surfacing is relentless" (PM design ruling)
**Branch/worktree**: isolated agent worktree (`.claude/worktrees/agent-aad6798c0ace8e87c`), commit only, no push

## Plan (after investigation)

Surfacing path (read whole chain before editing):
- `services/intent_service/todo_handlers.py:100` `get_due_reminders` — due texts (or None sentinel, #1425)
- `services/intent_service/context_assembler.py:354-369` — #1566 rider: reminder ctx rides EVERY gather_context call (cached TTL-30s, #984)
- `services/intent_service/conversational_floor.py:667-706` — renders the DUE REMINDERS block into the floor prompt
- Radar: `services/radar/` (models/sources/feed/feed_factory) + `web/api/routes/radar.py` + JS in `templates/components/history_sidebar.html`

Changes:
1. Session mentioned-set (verified_inference `_SESSION_DECLINES` lifetime pattern) in context_assembler, keyed on reminder identity (surfaced text), gating the due-reminder keys post-cache. New reminder due mid-session → still mentioned once.
2. Guided-flow suppression: no due-reminder block while a guided process (standup interview etc.) is active for the session — probe via new read-only `ProcessRegistry.any_active`; do NOT mark mentioned when suppressed.
3. Radar pin: `EntityType.REMINDER`, `RadarEntity.pinned`, `ReminderEntitySource` (due reminders, pinned, observed), pinned-first ordering in `RadarFeed.assemble`, pinned section at top in sidebar JS. Radar-route-only wiring (not standup's shared sources).
4. Tests: new `test_reminder_mention_gate_1625.py` + radar domain/source additions; run touched suites + ratchets.

## Work

### 15:33–16:00 — #1625 implemented, all suites green

**Conversation gate** (`services/intent_service/context_assembler.py`):
- Module-level `_SESSION_REMINDER_MENTIONS` (cap 2048, oldest-first eviction — verified_inference `_SESSION_DECLINES` lifetime pattern; session-scoped, never persisted).
- `_gate_due_reminder_mentions()` applied in `gather_context`'s #1566 rider, AFTER the #984 per-user cache (mention-set is per-session; gating inside the cached compute would cross sessions). Returns a new dict (cache reference never mutated).
- Gate 1: active guided flow (new read-only `ProcessRegistry.any_active`, `services/process/registry.py`) → due-reminder keys dropped, NOT marked mentioned (suppression ≠ mention). Fails OPEN on probe error (quieting layer must not break the #1566 surfacing promise).
- Gate 2: identity-keyed filter — only unmentioned reminders included + marked; a NEW reminder coming due mid-session is still mentioned once. No session_id → ungated (#1566 behavior preserved; also keeps 1566 suite green).
- `source_failed` (#1425 honesty) passes through both gates untouched.

**Radar pin** (`services/radar/*`, `web/api/routes/radar.py`, `templates/components/history_sidebar.html`):
- `EntityType.REMINDER`, `RadarEntity.pinned` (default False), `ReminderEntitySource` (due texts → pinned observed "due" cards), `DueReminderProvider` in feed_factory (same `get_due_reminders` read as the conversational rider — surfaces can't disagree; [] on failure).
- `RadarFeed.assemble` sorts `(not pinned, -attention)` — pinned locks above attention order.
- Wired in the Radar route's `_build_feed` ONLY — deliberately NOT in shared `build_entity_sources` (standup behavior unchanged; reminders-in-standup would be a separate design call). Route serializes `pinned`.
- JS: pinned section at top with "📌 Due reminders" label + `.radar-pinned`/`.radar-card--pinned` styles. CXO placement pass post-merge per Lead's brief.

**Tests**: new `tests/unit/services/intent_service/test_reminder_mention_gate_1625.py` (13 tests: once-then-quiet incl. PM's 4-reply shape, new-reminder-mid-session, session independence, no-session passthrough, source_failed honesty, no-mutation-of-cached-slice, mid-interview suppression + suppression-is-not-a-mention + fail-open, any_active probe). Additions to radar domain/route/template suites.
**Runs**: targeted new+touched 177 passed; reminder/process/standup 679 passed 7 skipped; gather_context suites 200 passed; design-system+templates+standup-route 997 passed; ratchets 46 passed; FULL intent_service unit suite 2779 passed.

Discovered issues: none filed (nothing unrelated found broken).
