# Session Log — 2026-08-15 18:48 — prog (Coding Agent, Claude Code)

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1628 — chat-side issue listings render GitHub titles verbatim; lift `_display_title` (#1622, radar seam) into a shared util and apply at chat-side render sites.
**Branch/worktree**: isolated agent worktree (Lead merges after review; no push)

## Plan
1. Shared util: `services/utils/text_sanitation.py` → `display_title(value, fallback)` (services/utils is the established cross-service helper home: datetime_utils, markdown_formatter, env_hygiene).
2. `services/radar/sources.py` imports it (aliased `_display_title`), local def deleted — behavior identical.
3. Guard chat-side render sites (full enumeration in wrap-up / report).
4. New tests: util matrix + list_issues_query listing + first_contact block + canonical_handlers priority/status formatters.
5. Radar + standup suites untouched and green; ratchets green.

## Work
- 18:48–19:05 — #1628 implemented.
  - New shared util `services/utils/text_sanitation.py::display_title` (lifted verbatim from `services/radar/sources.py:_display_title`, #1622 semantics unchanged).
  - `services/radar/sources.py` now imports it aliased as `_display_title`; local def deleted; radar+standup suites green untouched (435 passed).
  - Chat-side render sites guarded (full enumeration in the report to Lead): intent_service.py (shipped-this-week, stale PRs, single-issue detail, close/reopen did-you-mean singles + multi-match lists, already-closed/already-open + success messages, open-issues listing, open-PRs listing, milestones listing), canonical_handlers.py (project-status preview x3, high-priority issues x3), first_contact.py demo block, issue_intelligence.py priority enhancement.
  - NOT guarded (data/prompt-context, not render sites): context_assembler blocked/high-priority gathers, canonical_handlers metadata gathers, place_service (counts only), pm_number_manager, intent_service learn-patterns data return, fuzzy-match internals.
  - Tests: new `tests/unit/services/intent_service/test_degenerate_title_chat_1628.py` (21 tests: util matrix, list_issues_query listing, first-contact block, canonical priority/status formatters). One pre-existing assertion updated in `test_canonical_handlers.py::test_format_handles_missing_issue_title` ("Untitled" → id-carrying placeholder — it pinned the old default this change deliberately replaces).
  - Evidence: affected suites 3353+1 passed after fix (intent_service/intent/features/radar/standup); touched files re-run 238 passed; ratchets 46 passed.

## Discovered issues filed
- None. (Noted for Lead: `services/features/issue_intelligence.py` engine appears unwired — no instantiation outside its module; guarded anyway since it renders chat text if ever wired.)

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (worktree/commit discipline, evidence discipline); #1622 implementation-evidence comment (placeholder convention, guard semantics); tests/unit/services/intent_service/test_list_issues_connector_1322.py (harness pattern for handler tests).
- **Loaded but not referenced**: memory index (MEMORY.md).
- **Wanted but not found**: a map of chat-render seams (had to grep-trace all title render sites by hand).
