# Session Log — 2026-08-29 15:40 — Coding Agent (prog)

**Role**: Coding Agent, delegated by Lead Developer (shared lead worktree, sequential lane)
**Model**: Fable 5 (claude-fable-5)
**Tasks**: #1693 (todo task-text extraction) then #1572 (per-user timezone, reminder layer)

## Fix 1 — #1693 todo task-text extraction

**Verify-first**: `_extract_todo_text` (services/intent_service/todo_handlers.py) had three
patterns: `add [a] [new] todo:? TEXT`, `create ...`, `^todo:? TEXT` — colon-or-space only,
literal `todo` token, no `new/one more` lead-ins. Read the #1654/#1679 reminder helpers
(`_extract_reminder_text`, `_strip_trailing_time_expressions`, `_is_pure_time_expression`)
first — separate family, untouched.

**Fix** (extraction-only, no routing change): `_TODO_TOKEN = to[-\s]?do` (todo / to-do /
to do, command position ONLY — no message pre-normalization, which would corrupt task text
like "remember to do laundry"), `_TODO_SEP` accepting `:`, `-`, `–`, `—`, or whitespace,
plus `new|another|one more` lead-ins.

**Tests**: tests/unit/services/intent_service/test_todo_text_extraction_1693.py — 17 tests:
PM's 3 exact failing phrasings, the 2 that passed (regression pins), teach-copy form,
verbatim-text pin, reminder boundary both ways. 17/17 pass; full todo/reminder suites
(test_todo_handlers.py, 1654, 1490, 1542) 116/116 pass.

## Fix 2 — #1572 per-user timezone (reminder layer)

(in progress — see below)
