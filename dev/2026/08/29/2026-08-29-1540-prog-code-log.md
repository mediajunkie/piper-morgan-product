# Session Log — 2026-08-29 15:40 — Coding Agent (prog)

**Role**: Coding Agent, delegated by Lead Developer (shared lead worktree, sequential lane)
**Model**: Fable 5 (claude-fable-5)
**Tasks**: #1693 (todo task-text extraction) then #1572 (per-user timezone, reminder layer)

## Fix 1 — #1693 todo task-text extraction

**Verify-first**: `_extract_todo_text` (services/intent_service/todo_handlers.py) had three
patterns: `add [a] [new] todo:? TEXT`, `create ...`, `^todo:? TEXT` — colon-or-space only,
literal `todo` token, no `new/one more` lead-ins. Read the #1654/#1679 reminder helpers
(`_extract_reminder_text`, `_strip_trailing_time_expressions`, `_is_pure_time_expression`)
first — separate family, untouched.

**Fix** (extraction-only, no routing change): `_TODO_TOKEN = to[-\s]?do` (todo / to-do /
to do, command position ONLY — no message pre-normalization, which would corrupt task text
like "remember to do laundry"), `_TODO_SEP` accepting `:`, `-`, `–`, `—`, or whitespace,
plus `new|another|one more` lead-ins.

**Tests**: tests/unit/services/intent_service/test_todo_text_extraction_1693.py — 17 tests:
PM's 3 exact failing phrasings, the 2 that passed (regression pins), teach-copy form,
verbatim-text pin, reminder boundary both ways. 17/17 pass; full todo/reminder suites
(test_todo_handlers.py, 1654, 1490, 1542) 116/116 pass.

## Fix 2 — #1572 per-user timezone (reminder layer)

**Verify-first findings** (per the 2026-08-10 time audit, confirmed live in code):
- Supply was 0%: no tz column in 79 migrations, no browser capture (auth.js posts only
  username/password), no writer. `parse_reminder_time` had NO tz param;
  `parse_relative_date`'s `user_timezone` param was dead (body never read it).
- BUT: `users.preferences` JSONB (#1422) already exists as the per-user pref store, with a
  proven read/write pattern in collaboration_gate.py (#1510) — so tz storage needs a
  preference key, NOT a migration (within the "one column/preference key" scope line).
- UserPreferenceManager (services/domain/) is the in-memory facade the audit flagged —
  deliberately NOT touched; supply goes to the real DB store.

**Implementation**:
- NEW `services/utils/user_timezone.py`: TIMEZONE_PREF_KEY ("timezone" in users.preferences),
  `is_valid_iana_timezone`, `resolve_zone`, async `get_user_timezone`/`save_user_timezone` —
  every path fail-safes to None/False (unknown tz → pre-#1572 behavior).
- Capture: `web/static/js/auth.js` posts `browser_timezone` (Intl.DateTimeFormat()
  .resolvedOptions().timeZone, try/caught) with login; `web/api/routes/auth.py` login takes
  optional `browser_timezone` Form field (named to avoid shadowing datetime.timezone),
  validates IANA, writes the pref riding the last_login_at commit. Invalid/missing never
  blocks login.
- Parse: `temporal_utils._anchor_now(user_timezone)` — the ONE anchor; threaded into
  `parse_reminder_time(message, user_timezone=None)` (new param) and revived
  `parse_relative_date`'s dead param. Past-checks (#1562 today-refusal) now run on the
  user's clock when known.
- Render: `todo_handlers._format_reminder_when(when, user_tz)` — one renderer for the save
  confirmation (`_reminder_saved_message`, new user_tz param) and the reminders list
  (replaces the hardcoded "UTC" line). User tz → "4:00 PM PDT"; unknown → labeled-UTC face
  byte-identical to before.
- Threaded at all 3 parse sites + all 3 save-message sites (primary handler, #1648
  time-answer seam, #1654 task-answer seam — principal = user_id or offer_user).

**Tests**: tests/unit/services/intent_service/test_user_timezone_1572.py — 20 tests, all
clock math on a FIXED injected instant (PM's live failure moment 2026-08-29 20:49 UTC =
1:49 PM PDT; fallback tests pin TZ=UTC): PM's '4pm today' case saves 23:00 UTC with tz and
keeps the past-today refusal without (fail-safe pin), Tokyo tomorrow-9am, bare-clock roll on
user clock, parse_relative_date day bounds, render pins both ways, validator, DB round-trip
+ corrupt-value fail-safes. Plus login-capture integration test in
tests/auth/test_auth_endpoints.py (valid stored / invalid ignored / absent unchanged).

**Test evidence**: 20/20 new file; auth endpoints 16 passed; both ratchets
(test_architecture_enforcement.py + test_completion_ratchets.py) 46 passed; FULL
tests/unit/services/intent_service/ suite 3507 passed, 3 deselected (-m "not llm",
env-stripped, POSTGRES_PORT=5433). node --check on auth.js OK.

**Honest scope**: reminder parse + reminder/save/list rendering complete per the asked scope.
NOT done (audit F2/F3 remainder, separate issues): the other render surfaces (todos page raw
ISO, standup naive generated_at, agenda ISO), calendar adapter's two "todays", Slack
users.info capture, threading tz into calendar-query callers of parse_relative_date.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-strip prescription, evidence discipline, verify-first);
  docs/internal/operations/time-handling-audit-2026-08-10.md (canonical model + attach
  points); issues #1693/#1572/#747; collaboration_gate.py JSONB write pattern.
- **Loaded but not referenced**: memory index entries surfaced at session start.
- **Wanted but not found**: none.
