# Session Log — 2026-08-29 15:58 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent, delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, lane shared per delegation)
**Task**: Two sequential reworks that failed PM's live round v64 (2026-08-29):
- **1543 rework**: raw-capture moved from title to body; repo-routing phrase leaked into title. Strip 'in <repo>' from title, body must never echo the raw command.
- **1649 rework**: 'titled 'X'' quoted-value extraction dropped on first turn; slot-answer turn captured entire correction sentence as title. Extract quoted values; metacommentary never enters slot.

Constraints: handler/slot-fill only, no routing/classifier changes, no new elif dispatch. Commits reference issues BARE (1543/1649, no #, no close-keywords). Do NOT push.

## Work

- 15:58 — Read PM's 2026-08-29 fail comments on both issues, prior fix commits (ededd68cf for 1543, a91572ff0 for 1649). Investigating current extraction code.
- 16:05 — Root causes reproduced deterministically:
  - **1649 first turn**: the standalone titled-pattern used a shared quote class knowing straight + curly-double but NOT curly-single quotes. `titled ‘Login timeout’` extracted nothing while the description pattern (paired `_qspan`) captured its slot — byte-exact PM's "description captured, title dropped" split. (Straight-quote input matches the old pattern, so smart-quote input is the only mechanism that reproduces the live transcript.)
  - **1649 answer turn**: `_bind_body_prose` had no notion of a dictated/quoted value in an answer — `derive_subject_from_prose` took the whole correction sentence.
  - **1543 title**: about-form captured "the login timeout in test-piper-morgan"; `strip_trailing_repo_clause` only knows `in owner/repo` and `in X repository` shapes — a bare repo name is unknowable as routing at extraction time. Routing itself resolved via the DEFAULT repo coinciding with the named one (`extract_natural_repo_name` also requires the "repo(sitory)" word).
  - **1543 body**: `description = context or slots.get("body") or intent.original_message` — the raw-command echo (the #1554 finding, confirmed live v64).
- 16:15 — Fixes, all handler/slot-fill side, no routing/classifier changes:
  - `_slotfill_issue_request`: folded titled-form into the marker pattern (`subject|titled?|called|named` + `_qspan`, all four quote styles, IGNORECASE, + `should/must be` fillers).
  - `_handle_create_issue`: description fallback is now `""` — body is described content or nothing, never the command echo; after repo finalization, new pure `strip_repo_phrase_for(title, repository)` strips a trailing phrase NAMING the routed repo (named-resolved or default-coinciding); phrases naming anything else stay.
  - `repo_clarification.py`: `strip_repo_phrase_for` pure helper (trailing-only, case-insensitive, never empties a title).
  - `drafted_issue.py`: new pure `extract_title_answer` (anchored marker-led dictation + bare-quoted-whole-turn, both quote styles, metacommentary stripped); `_bind_body_prose` consults it before derive; dictated titles never seed the body; empty-body render says "(no body yet)".
- 16:20 — Tests: 13 new pins in test_create_issue_title_1543.py (PM v64 verbatim e2e: title "the login timeout", body "", owner/repo asserted; mid-sentence/no-phrase/description-after-comma/non-repo-phrase variants; pure helper suite) and 17 new in test_drafted_issue_explicit_slots_1649.py (all four quote styles first-turn; extractor unit suite incl. no-steal negatives; PM correction-turn e2e with file-it kwargs; bare-answer derive path; set-aside intact). Runs (env-stripped, -m "not llm"): the two files 62 passed; tests/unit/services/intent_service/ + both ratchet files 3580 passed; tests/unit/services/intent/ 53 passed; create_issue-adjacent suites (1220 writes, 695 command, registry) 38 passed.
