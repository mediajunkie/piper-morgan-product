# Lead Developer session log — 2026-08-29 (Saturday)
Role: Lead Developer (lead-code) · Model: Fable 5 · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron 3aca3cab (expires ~9/2, rotate ~8/31)

## Fire 1 (06:37) — START; #1638 disposal dispatched on Arch's GO

Synced (36 merged). One memo: Arch ruled #1638 DISPOSE (zero callers, ~376 lines). Lane dispatched
under delete-module-safely with re-run-the-sweep-yourself required; stop-if-live-caller. PM's test
round expected this morning: security → "flip it" → todo/reminder → rest (tracker holds the plan).
1677/1488 close rides the watched flip.

## ~07:0x — #1638 DISPOSED + CLOSED; the sitting's last blocked item resolved

Lane re-ran the sweep independently (numbers per category), cut 376 lines + 3 dedicated test
files + sole-purpose doc, surgered the mixed #1624 file (21/21), verified integration coverage
independently held before its file rode the cut, corrected 7 survivor docs same commit, ADR-004
left as Arch's record. Collection −20 clean; ratchets 46 both sides; smoke −12 = exactly the
deleted marks. Re-verified, pushed, closed with evidence. Every item from PM's sitting now has
its final state.

## Fire (09:37) — 1677 close-mystery SOLVED with the receipt; carry-forward fully rewritten

PPM's discrepancy: 1677 closed 19:39 Fri, before my "not closing" memo. Event lookup: closed by
commit 312981354 — PPM'S OWN mail push, subject "ask(ppm): close #1677/#1488 properly…" — the
documented keyword gotcha ATE ITS OWN ASK. 1488 escaped (slash form). Reopened with evidence;
criteria unchanged. Exec/PM's staleness catch: carry-forward looked maintained but my edits were
surgical row-replacements — "v54 LIVE" survived from 8/19 untouched. FULL REWRITE pushed;
rewrite-at-STOP rule adopted INTO the file. Both answered with the diagnosis, not just the fix.

## Fire (12:37) — Arch's staged flip accepted; census layer-claim corrected WITH LIVE RECEIPT

Arch's PM-ratified staged proposal (shadow → READs → create_todo; chat = last major investment,
new build → MCP/BYOC) accepted; execution mine. Their census said the live flag was "unset in
every deployment config" — contradicting my record, so I probed the RUNNING machine before
replying either way: PIPER_INVERSION_LIVE_CATEGORIES=read_status IS set (fly secrets — invisible
to file census). Flip-1 was genuinely live all along; 0 events = real traffic absence. SHADOW
confirmed off = Stage 0 is real work. Plan: compress stages into PM's watched round (one restart)
— shadow+READs at "flip it", create_todo at the todo cluster. Carry-forward updated per the
freshness rule. PPM's 1677 loop closed on their side (was their commit; no action).

## ~13:0x — THE FLIP IS EXECUTED (PM's word, 12:49)

fly secrets: PIPER_INVERSION_LIVE_CATEGORIES=read_status,read_referent,read_synthesis,create_todo
+ PIPER_INVERSION_SHADOW=1. Verified IN THE RUNNING ENV post-restart; /health green. The
Inversion now carries 4 READ groups + its first live WRITE, shadow computing legacy
counterfactuals. Rollback = unset. Tracker republished (round split three ways per Exec's brief:
PM conversational (a) · Lead deterministic (b) · Web browser (c), reports to me). PM testing
todos NOW — watched, exactly as designed. #1677/#1488 close on their transcript if clean.

## ~13:3x — deterministic set verified: 5 CLOSED on evidence, 1 correctly held open

Lane verified all six at HEAD with pins + independent real-Postgres probes at the layer each
defect actually lived: 1472 (enum binds + ratchet), 1493 (0.1s-from-UTC vs 25,200s naive skew),
1501 (two-tenant probe; fail-closed [], 0, None), 1545 (malformed row skipped=1), 1548 (PUT via
real repo, row persisted). All five closed citing the evidence. #1431: fix holds everywhere but
PM's exact phrasing still pre-classifies to STATUS — stays open on #1579/#1580, same as the 8/11
determination. In Review shrinks by 5 with zero PM minutes spent.

## ~14:0x — PM has the 16-step list; round in progress

Tracker rebuilt as one literal ordered round (flow-ordered, type/expect/fail per step, fabrication
watch as standing rule). PM testing now. WAITING on: PM's transcript (→ 1677/1488 close if clean),
Web's browser-lane report. Nothing else drainable; holding.

## ~14:1x — PM ROUND, STEP 1 RESULT: the FLIP ITSELF PASSED; extraction gap filed

5 todo-create draws, ZERO create_ticket misroutes — the #1488 class absent under the flip; both
canonical forms created with ✓ tells. Failures: 'new todo - X' (dash), 'to-do' hyphen forms → the
handler's own honest didn't-catch ask (never a wrong action, never a fabrication). Worst part:
teach-copy suggests 'add todo: [description]' and the hyphenated twin of exactly that form fails
— teach-then-deny-lite. Filed with the layer hypothesis marked unverified. Awaiting steps 2–16.

## ~14:2x — round results 2+3: one real new bug filed; test-2 verdict PENDING a mode check

Test 2 ("todo: water the pets" created WITHOUT asking): NOT calling it yet — the #1685 hold
applies to AMBIGUOUS framing under the consent matrix, and if PM's account mode is execute,
PROCEED is correct and MY tracker step was written unconditioned (my error, not the product's).
Verify PM's declared mode before verdict. Test 3: chain worked until the LAST link — offer armed,
PM said "Yes please.", consent NOT consumed, deflected to teach-a-workaround. Filed (offer-consume
gap, #1651/#1652 family). #1625+#1654 visibly behaving in the same transcript. PM asked when the
timezone (#1572) gets fixed — committing it to the next build slot alongside the #1693 extraction
fix. PM erranding 20 min; verify mode + prep both fixes during the gap.

## Fire (~15:00) — mode verdict delivered; fix lane running; Web's first browser closes land

Test-2 verdict WITH RECEIPT: PM's testing account (xian@dinp.xyz) has DECLARED execute mode
(probed via read_declared_working_mode on the deployed DB; other two accounts default collaborate).
One-step ambiguous create is the correct EXECUTE-mode matrix cell — my tracker step was
unconditioned on mode, my error, told PM plainly. Fix lane dispatched (1693 extraction + 1572
timezone, sequential, one lane = no worktree race). Web's browser lane: #1480 CLOSED on their
evidence (served-JS byte-diff + real function body vs 6 attack vectors — house discipline done
right); 1512/1568/1578/1581 banked pending the test credential I now owe them (real signup path,
queued). Arch ack'd the census correction + endorsed the plan (filed).

## ~15:5x — round steps 4–7: two PASS (1679, 1542), two FAIL with transcripts (1543, 1649)

4: PASS — asks WHAT, saves with 📅 (UTC label = 1572, in flight). 5: PASS — word-form duration
parsed. 6: FAIL — 1543's raw-capture MOVED: title improved but repo phrase leaked into it AND the
body is now the verbatim command text; real issue #111 created. 7: FAIL — 1649 exact defect
(explicit title dropped, re-asked) + WORSE: the correction sentence captured verbatim as title.
Both annotated with PM's transcripts, back to In Progress. Rework queued BEHIND the running
1693+1572 lane (one lane at a time in this worktree). Brusque outward warning = copy note for CXO.

## ~16:1x — 1693+1572 MERGED+PUSHED; 1543/1649 rework lane dispatched

Fix lane landed both: 1693 pattern-level to-do/dash acceptance (deliberately NOT pre-normalization
— the lane caught that normalizing would corrupt "remember to do laundry" task text; pinned
verbatim), 1572 complete at asked scope with NO migration (users.preferences JSONB key riding the
existing #1510 pattern; capture at login via auth.js Intl; parse anchored to user clock incl. the
#1562 past-refusal; render "4:00 PM PDT" shared by save+list faces; fail-safe to UTC pinned with
fixed instants). PM's exact 1:49 PM moment is a test fixture. Remainder honestly scoped to the
already-filed audit issues (todos-page ISO, standup faces, calendar). Re-verified 3507+46, pushed.
Rework lane (1543 raw-capture-moved + 1649 quoted-slot extraction) dispatched with PM's
transcripts as the pins; commit-subject rule passed along (bare numbers — yesterday's auto-close).

## ~16:4x — 1543/1649 rework MERGED; root causes better than my framing; compose residue filed

Lane REPRODUCED PM's exact failure split before fixing: 1649's title-drop was CURLY-SINGLE QUOTES
(chat auto-substitution) unknown to the standalone titled-pattern — the only mechanism consistent
with description-captured/title-dropped. 1543's body echo was the fallback chain's
original_message tail (the relocated defect the prior fix itself filed as 1554). Fixes: body never
echoes; repo phrase stripped post-finalization; four quote styles; dictated slot answers extracted
with metacommentary excluded. 62+3580+53+38 green, ratchets green, pushed. Compose-framing
residue = new issue (arm path doesn't resolve bare repo names). Staged pile for next deploy word:
1693 + 1572 + 1543/1649 rework.

## ~16:5x — TEST 8: the step-8 target HELD; #1527 graduated to active fix; PM's layer question

PASS on what step 8 tested: armed confirm never stolen by the aside (1650), and NO fabricated
deletion — "I don't have an execution result confirming it happened this turn" is 1648's contract
speaking. FAIL around it: 1527's pre-classifier greed misrouted delete-reminder THREE times, incl.
the phrase Piper itself taught — teach-then-deny authored by the system. PM asked the right
question: why didn't the Inversion help? Because surface 1 (pre-classifier) runs BEFORE the
consult; the flip replaced surface 2 only; delete_reminder is DESTRUCTIVE and unflippable today.
Gate-clean fix = NARROW the greedy claims (removes deprecated-layer surface, doesn't patch it).
Dispatching. PM exhausted + in testing mode — keep replies short.
