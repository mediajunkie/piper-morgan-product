# Session Log — 2026-08-29 16:23 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: 1527 — narrow the pre-classifier's greedy delete-claims so reminder/todo-noun
deletes stop misrouting to the project lane (HIGH PRIORITY — PM hit it three times in
one live exchange 2026-08-29, including a phrase the system itself taught)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)

## Work

- Read `gh issue view 1527 --comments` (2026-08-29 comment: PM's verbatim transcript,
  three misroutes, flip ON but surface 1 runs before the Inversion consult), read
  `docs/internal/architecture/current/intent-routing-stack.md` in full (4-surface chain),
  traced the claiming patterns and the correct destination (#1666 delete_todo rail).
- **The claiming patterns** (`services/intent_service/pre_classifier.py`,
  `PORTFOLIO_PATTERNS` delete-operations group): `\bdelete\s+(?:my\s+)?(?:the\s+)?(?:project\s+)?(.+)`
  plus the `\bremove\s+…` and `\bget rid of\s+…` siblings — greedy `(.+)` claimed every
  delete+noun turn as PORTFOLIO/manage_portfolio at confidence 1.0, on BOTH pattern entry
  surfaces (`pre_classify` and `detect_multiple_intents` share the list). Pre-fix probe
  confirmed: all of PM's phrasings + remove/get-rid/todo-vocab variants → manage_portfolio.
- **The narrowing** (removes claims, adds none):
  1. `REMINDER_TODO_NOUN_GUARD = r"(?!.*\b(?:reminders?|to-?dos?|tasks?)\b)"` — negative
     lookahead on the three delete-family patterns only (archive/hide/restore untouched).
     Guarded miss = fall-through to the LLM lane, whose delete_todo emission dispatches
     the #1666 DESTRUCTIVE rail family.
  2. Companion narrowing, exposed by the fix: `REMINDER_QUERY_BLOCKERS` gains
     `\bget\s+rid\s+of\b` — with the portfolio claim gone, 'get rid of my reminders'
     reached the reminder LIST lane (the destructive-verb blocker missed the phrasal
     form). A destructive ask must never be answered with a list.
- Post-fix probe: PM's three phrasings + 'delete my reminders' + all vocab variants →
  None on both entry surfaces; 'delete the alpha project' / 'delete my project alpha' /
  'remove the old project' / 'get rid of the alpha project' / archive family → still
  PORTFOLIO/manage_portfolio; reminder list reads still claim list_reminders_query.
- **Tests**: `tests/unit/services/intent_service/test_reminder_delete_misroute_1527.py`
  (33 tests, all passing): surface-1 declines (both entry points, parametrized over PM's
  exact phrasings + vocab), no over-narrowing (project lane keeps claims), list-lane
  boundary, and e2e through the REAL `process_intent` (1666 idiom: classification stubbed
  to the surface-2 delete_todo emission, todo boundary explosive-delete) — routed
  destination is the todo family (never "couldn't find a project"), and the #1666
  title-bound confirm still arms on the numbered shape ('Delete todo 1: "hydrate"?').
- **Doc**: intent-routing-stack.md surface-1 row updated in the same commit (#1527
  narrowing note — the change alters surface-1 behavior).
- **Verification**: new file 33/33 green; `tests/test_architecture_enforcement.py` +
  `tests/test_completion_ratchets.py` 46/46 green; full
  `tests/unit/services/intent_service/` 3567 passed, 3 deselected (-m "not llm",
  env-stripped, POSTGRES_PORT=5433); `tests/unit/services/test_pre_classifier.py` 32/32.

## Discovered work (not yet filed — GitHub API rate-limited at time of writing)

- **Named-target todo/reminder delete resolution**: post-fix, PM's exact no-number
  phrasings ('delete my hydrate reminder') correctly reach the delete_todo rail, but
  `handle_delete_todo` / `build_todo_delete_confirmation` resolve NUMERIC targets only —
  the no-number leg answers "Which todo should I remove? Try: 'delete todo [number]'"
  (honest todo-family clarification, no misroute, nothing deleted) rather than arming a
  title-bound confirm for the named row. Out of scope here per the task's explicit
  "do NOT change what happens after correct routing." Could not verify whether already
  tracked (rate limit); needs either an existing-issue check or a new issue once the
  API clears. `complete_todo` has fuzzy title matching (`_extract_completion_text` /
  `_find_best_matching_todo`) that delete lacks — a plausible shape for the fix.

## Wrap

- Committed to claude/lead-cycle referencing 1527 (bare number, no close keywords).
- NOT pushed, per delegation instructions.

## Memory & briefing surfaces referenced this session

- **Referenced**: intent-routing-stack.md (mandatory pre-read — 4-surface chain, where
  the flip sits, #1666 rail semantics); CLAUDE.md (narrowing direction, bare issue
  number in commits, env-strip, m-43 layer-naming in test docstrings);
  issue 1527 comments (PM transcript, sanctioned fix direction).
- **Loaded but not referenced**: memory index entries (project/feedback slugs beyond
  the rate-limit pattern).
- **Wanted but not found**: a corpus/issue registry searchable offline — the GitHub
  rate limit blocked the discovered-work duplicate check.
