# Session Log — 2026-08-29 16:41 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree)
**Task**: Operationalize the PM-ratified (2026-08-29, live round) extraction-pattern policy — ratchet + gate text + decisions.log, one commit.

## Work

- **TestExtractionPatternRatchet** added to `tests/test_architecture_enforcement.py`, mirroring the
  `TestPreFloorDispatchSiteRatchet` idiom (ceiling test + tight-equality test + explicit vacuity test).
  Mechanism: AST-locates each named span and counts regex literals (pattern-list elements + inline
  `re`/`_re` call patterns + named pattern constants). Ceilings frozen at MEASURED counts
  (scratch script with identical logic, run before writing the test):
  - todo-create: **5** (`_extract_todo_text` 3, `_TODO_TOKEN` 1, `_TODO_SEP` 1)
  - reminder-extraction: **11** (`_extract_reminder_text` 8, `_strip_trailing_time_expressions` 1,
    `_is_pure_time_expression` 1, `_has_time_signal` 1)
  - issue-slot-extraction: **15** (drafted_issue: `_ANSWER_QSPAN`/`_ANSWER_MARKER_QUOTED_RE`/
    `_ANSWER_BARE_QUOTED_RE`/`_ANSWER_MARKER_UNQUOTED_RE`/`_ANSWER_METACOMMENT_RE` 1 each,
    `extract_title_answer` 0; intent_service `_slotfill_issue_request` 10 incl. `_qspan` uses)
  - pre-classifier: **567** across 35 `*PATTERNS` class lists (vacuity floor ≥30 lists)
- **Failure-mode proof** (scratch edits, reverted, NOT committed):
  - Added a throwaway 4th pattern to `_extract_todo_text` → `test_no_new_extraction_patterns`
    FAILED ("todo-create: 6 > ceiling 5") and `test_extraction_ratchet_stays_tight` FAILED
    ("actual 6, ceiling 5"). Reverted via exact inverse edit; `git diff` clean.
  - Renamed `_TODO_SEP` → `test_named_spans_still_exist` FAILED naming the missing span. Reverted; clean.
- **Gate text**:
  - CLAUDE.md — one sentence appended to "Verify First, Create Second" (NOTE: the Lead's grep
    anchor 'supersession' does not exist in CLAUDE.md — verified against fetched origin/main tip;
    anchored to the fundamentals paragraph instead, deviation reported to Lead).
  - `docs/internal/architecture/current/intent-routing-stack.md` — same sentence at the tail of the
    surface-1 (pre-classifier) table cell, naming the handler-side slot-fill spans.
- **decisions.log** — ruling appended (16:41 PT): ratification context, policy, enforcement
  mechanism + frozen counts, measurement plan (pre-claim shadow probe, separate build item).

## Verification

- `tests/test_architecture_enforcement.py -m "not llm"` → **44 passed** (new ratchet included)
- `tests/unit/services/intent_service -m "not llm"` → **3567 passed, 3 deselected** (155.97s)
- Env: env-strip (`ANTHROPIC_*`), `POSTGRES_PORT=5433`, repo venv.

## Discovered issues filed

None (gh API rate-limited per Lead's brief; nothing requiring a new tracking issue was found).

## Wrap

One commit (numbers bare, no close-keywords), NOT pushed per Lead's instruction — Lead owns the push.
