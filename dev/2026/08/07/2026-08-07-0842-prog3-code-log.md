# Session Log — 2026-08-07 08:42 — Coding Agent (prog3) — Claude Code

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-aeff96c0465d3a24f (agent worktree; NOT pushing to origin/main — Lead merges after composition audit)
**Task**: Fix two sibling slot-extraction brittleness bugs from PM's 8/7 walkthrough
- #1490 — reminder parser loses WHAT slot when time precedes task; 'at at' duplication in confirmation copy
- #1492 — project-name extraction fails on trailing punctuation / quotes / 'called X' / adjective-position

Constraints: D4 (pattern-table/extraction-code only, NO classifier prompt changes); failing-first tests; no regressions; two commits, one per issue.

## Work

- 08:42 — Session start. Read both issues via gh. Beginning investigation of extraction code.
- 09:05 — #1490 diagnosed + fixed, failing-first (17 failed pre-fix, 54/54 post-fix in test_reminders.py):
  - Root cause A (WHAT slot lost): `_extract_reminder_text` patterns in `services/intent_service/todo_handlers.py` required `to|about` immediately after the command phrase; "remind me tomorrow at 3pm to review the PR" never matched ("tomorrow" ≠ "to "). Fix: shared `_TIME_EXPR` class constant + three time-first patterns (`remind me <time>+ to/about <task>`, plus set/create-reminder variants) ahead of the generic forms; neither shadows the other.
  - Root cause B ('at at'): `services/intent_service/temporal_utils.py` line 130 did `f"tomorrow at {group(0).split('tomorrow')[1]}"` — the fragment already contained "at 3pm". Same class of bug at line 165 (`f"at {group(0)}"` where group(0) is "at 5pm" / "today at 5pm"). Both fixed by stripping the leading at/today before re-adding the prefix.
  - Cleanup loop: trailing punctuation + compound trailing time exprs now stripped until stable, so "remind me to review PRs tomorrow at 3pm." → "review prs". DELIBERATE minor text refinement: this phrasing previously saved text "review prs tomorrow" (single-pass strip left the residue); now fully clean per the function's documented intent. No test pinned the old residue. Flagged for Lead's composition audit.
  - Regression: tests/unit/services/intent_service/ full suite 1941 passed 1 skipped; tests/intent_service/test_todo_handlers.py + pattern-073 suites 28 passed.
  - D4 respected: no classifier/prompt changes — classification already routed the failing phrasing to the handler (PM's repro response came from handle_create_reminder's help copy).
  - Committed: 42effd9a7 (worktree branch, NOT pushed — Lead merges after composition audit).
- 09:35 — #1492 diagnosed + fixed, failing-first (14 failed pre-fix, 28/28 post-fix):
  - Root cause: name cleaning was a nested helper in `canonical_handlers._handle_portfolio_query` that only stripped a fixed list of bare trailing words — no punctuation, no quote unwrapping, no 'called/named X', no adjective-position 'my Test project'.
  - Fix: hoisted `clean_project_name` to module level in `services/onboarding/portfolio_service.py` (next to the pattern tables it serves; exported via services/onboarding/__init__.py), THEN upgraded it: iterative strip of trailing punctuation + politeness tails, leading called/named, quote unwrap (straight + curly), and a final trailing ' project(s)' strip guarded against emptying the name. Hoist happened first with the buggy logic verbatim so the failing-first run demonstrates real behavior failures (e.g. 'test project, please.'), not ImportError.
  - Downstream lookup find_project_by_name is case-insensitive, so extraction to lowercase 'test' resolves project 'Test'.
  - DISCOVERY: tests/unit/services/onboarding/test_portfolio_service.py is blanket-skipped ('ADR-059: onboarding on ice') including its pattern-extraction pins, even though the archive/restore chat path is live (PM walkthrough + #1464). New live test file tests/unit/services/intent_service/test_project_name_extraction_1492.py re-pins the working forms alongside the #1492 cases. Flagged as discovered work for the report.
  - Regression: intent_service + onboarding + 1464 suites: 1977 passed, 219 skipped (pre-existing ADR-059 skips), 0 failed; 1464 DB-backed suite 8/8 including chat-level handler.
  - D4 respected: pattern-table/extraction-code only; ARCHIVE/DELETE/RESTORE pattern tables themselves untouched.
  - Committed: 1b31c39ef (worktree branch, NOT pushed).
- 09:50 — Wrap. Discovered work filed: #1494 (ADR-059 blanket skip on test_portfolio_service.py shadows live-path extraction/service pins). Working tree clean; two commits (42effd9a7, 1b31c39ef) held on worktree branch per task HARD RULE — Lead merges after composition audit. Sign-off checklist adapted: `git status` clean; branch deliberately NOT pushed/merged (option b equivalent — this report to Lead is the notice).

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (session protocol, failing-first/evidence discipline, D4 constraint framing, auto-close-keyword gotcha — commit messages avoid 'fixes #N'); issue #1490/#1492 bodies (verbatim test cases); tests/unit/services/database/test_project_archive_restore_1464.py (live-path conventions + proof archive path is chat-reachable).
- **Loaded but not referenced**: MEMORY.md index (no individual memory files opened); briefing docs (task was self-contained).
- **Wanted but not found**: nothing — `bd create` (named in CLAUDE.md Discovered Work Discipline) doesn't exist on this host; used `gh issue create` instead. Worth a CLAUDE.md touch-up if `bd` is gone for good.
