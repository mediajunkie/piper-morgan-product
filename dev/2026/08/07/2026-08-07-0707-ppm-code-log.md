# PPM Session Log — 2026-08-07

**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` — job `2ae0bbb1`, CronList-verified exactly one at START

---

## Fire 1 (07:07, START)

Heartbeat emitted `START` first (clock-checked, not tick-order — first fire of the day).
Synced: 0 behind, 0 unpushed. Inbox: **3**.

**Carried in from 08-06** (see `dev/active/ppm-carry-forward.md`):
- Beta target **Sunday 2026-08-09** (PM corrected Aug 8 → Aug 9; MVP milestone field now matches).
- **#1481 HELD** from alpha/beta/release until safe — *held, not deferred* — with **connector work
  front-loaded in the Production milestone** (PM). Sequence filed on **#1440**; two questions left
  open for Arch/Lead.
- Awaiting PM: milestones on #1462 · #1476 · #1477 · #1482 · #1483 · #1485; Jake items 3 + 6;
  canonical criterion text.
- ⚠️ Standing guard earned yesterday: **check GitHub before re-asking PM anything** — #1216 sat in
  my awaiting-PM list for a month after it closed.

**Fire 1 work** — both #1440 questions came back answered, and a correction I owed on my own proposal.

**✅ Sequence CONFIRMED — no change to the order.** Lead answered from the code, Arch confirmed
against the ruling. Two sharpenings recorded on #1440, both of which change *execution* rather than
order:
- **#1364 / #1481 are two items, ONE WAVE, with #1481's identity resolution landing FIRST** so the
  port builds on the corrected identity model instead of porting the defect. ⚠️ **Arch: the pairing
  is the ratified architecture** (verbatim their 08-04 ruling — `bound_user_id` is an outbound
  *credential selector*, never an inbound *principal*) — **so they ship as one wave but stay two
  issues, because collapsing them re-creates the defect #1481 exists to fix. Refuse the merge.**
- **#1323 at position 2 confirmed**, with Arch's better form of my heuristic: *extract when a second
  implementation reveals which parts are genuinely COMMON versus accidentally SIMILAR.* ⚠️ Caution
  to propagate: **a mixin from two references is a HYPOTHESIS about the contract** — Notion and
  Calendar are its test and must be allowed to change it.
- **Scoping (~5 gate-closing, not the ~40 grep) endorsed by both.** Arch's reason is the keeper:
  the ~40 *"share the word connector but not the contract"* — a grep over titles is a search over
  vocabulary.

**⚠️ And I checked the heartbeat feasibility question — the answer is against my own proposal.**
Arch's datum: the same stacking event hit their seat and produced the **opposite** failure. My file
cried wolf (9h gap, healthy cron); **theirs showed one START row — byte-identical to a fully-worked
day — while they were genuinely dead ~17h.** `--if-quiet` is the only difference. Arch endorsed my
`scheduled=`/`emitted=` fix as *"the one that actually separates them."*

**It isn't. It separates mine.** Read `scripts/duty-cycle-heartbeat.sh:102` — the field can only
annotate **rows that get written**, and Arch's mode is **rows that don't exist** (dead → no turns →
no invocation; quiet → `--if-quiet` suppresses). **Both still produce the single-START file with or
without my column.** Corrected to them before they build on it — they'd done me the same courtesy
about their two-emissions design a day earlier.

⭐ **And the script's own comments (`:69–79`) already record this class**: refinement (a) left the
surface *"legitimately empty on a healthy day"* — *"Exactly the m-44 shape, inside the m-44 fix."*
The 08-04 START-always-writes remedy **distinguishes WRITER-dead from cohort-busy but NOT
ROLE-dead-after-START from ROLE-worked-all-day.** Arch's incident is **the unclosed half of a known
defect, not a new one.** Candidate fixes flagged to CIO (drop `--if-quiet` for WORK, or make the
reader cross-reference commits as the design already assumes) — **flagged, not prescribed**; it's
CIO's surface and I've been wrong on this instrument once already this week.

**⭐ Then HOST supplied a third instance and asked for a count — so I measured the denominator instead.**

HOST: *"three seats independently hitting the identical failure mode on one night is worth more as a
count than as three separate reports."* Right — but a count of three says nothing about the
population. **Measured all 11 roles: heartbeat rows vs. commits on `origin/main`, both for 08-06**
(commits are independent activity evidence; a role with 9 commits was demonstrably alive).

```
arch 1/7 · cio 1/7 · comms 1/14 · cxo 1/10 · docs 1/6 · exec 1/5 · lead 1/9
host 4/16 · pa 3/12 · ppm 5/20 · web 2/10          (hb_rows / commits)
```

🔴 **All 11 were active — 5 to 20 commits each. EIGHT recorded that day as a single heartbeat row.**
**For 8 of 11 seats, a full working day is byte-identical to a role that died right after START** —
on the surface the watchdog reads, on the day before beta.

**PPM, HOST and PA are the only three emitting WORK rows** — which is exactly why we three saw
*gaps*. **We hit the false-alarm mode because we are the minority that emits at all; the other eight
cannot produce a gap because they never produce a row to be missing.** The two failure modes aren't
competing accounts of one defect — **they're what one surface does under two configurations, and a
flag decides which you get.**

⚠️ **What I could NOT distinguish, and said so**: whether those 8 call the script with `--if-quiet`
or never call it on WORK fires. **Both produce one row.** Conclusion unaffected, but I named it
rather than inferring the mechanism from the output — that move has cost this cohort four
corrections in ten days.

**This retires my own proposal as a priority**, which is the useful part: I'd corrected it to Arch on
logic an hour earlier, and the measurement says **the mode it doesn't fix is the one affecting 8 of
11 seats.** Wrong thing to build first even where it works. Flagged the cheap alternative (emit WORK
unconditionally, ~66 lines/day cohort-wide) to CIO as **theirs to judge** — I measured before
proposing this time, having been wrong on this instrument once already this week.

### Fire 2 (10:07, WORK) — Ship #055 filed, board unblocked, and two corrections aimed at my measurement

Heartbeat `WORK`. Cron `2ae0bbb1`, exactly one. Inbox 8 → 0.

**⭐ BOARD VISIBILITY IS UNBLOCKED — it had stayed marked 🔴 after the fix landed.** My portfolio
line 7 read *"BLOCKED — `gh` lacks `read:project`… Beta Blockers uncountable since 7/16."* Checked
instead of carrying it: **the token now has `project` scope and `gh project list` returns.** Used it
at once — **21 issues open in the MVP milestone**, which *is* the beta-gate count by our own rule,
uncountable for three weeks and now countable two days out.
⚠️ **And the list is GROWING from PM's live beta-account testing** — #1488 (todo misroutes to the
GitHub rail, reproduced twice), #1489 (back-navigation loses all assistant replies), #1490–#1492
(reminder/parser brittleness). **Healthy direction for finding, awkward for counting.** Flagged
up to Exec for the dashboard rather than smoothed.

**Ship #055 workstream review FILED** (window Jul 31 – Aug 6), on receipt rather than held to
Saturday per Exec's correction — PM's reasoning being that a deadline "gives license to put off the
work… reports that come later than they needed to, and are more stale." §0 done line-by-line against
the portfolio: **3 advanced, 1 closed, 2 held on PM, 1 partially advanced, 1 unblocked.** Owned the
2,282 error in §2 as the window's worst miss.

**⚠️ Two corrections arrived aimed at my 8-of-11 measurement. One strengthened it, one didn't survive.**
- **Web**: their 2 rows were **both START** (self-caught duplicate; START writes unconditionally), so
  their real WORK coverage was **zero**. **That makes it 9 of 11, not 8** — my number was literally
  true and understated the thing that mattered. Corrected upward.
- **Arch**: retracted their REPL-idle explanation — the real cause was a **cohort-wide account
  weekly-limit freeze until 21:30 Thursday** (from Exec's kickoff). **Right about the cause, and I
  owned the same defect**: I wrote *"the session got no turn,"* which is seat-shaped for an
  account-shaped event. ⛔ **But their extension — *"those roles weren't individually mis-configured;
  the cohort was frozen and the surface had nothing to record"* — does not hold, and I checked before
  accepting it.** The freeze was Thursday *afternoon*; the 06:52/09:52/12:52 fires all happened.
  Pre-freeze commits: **arch 7, comms 14, lead 9, cxo 8, exec 5, cio 4, docs 3 — every one of them
  one heartbeat row**, while host/pa/ppm emitted 3–5 in the same window. **The freeze explains the
  afternoon; the missing morning rows are configuration, and those are what the finding was about.**

**Docs' sweep**: added the missing `<!-- DAY-CLOSED: 2026-08-06 -->` sentinel, dated honestly as
next-morning. ⭐ **Their second-order catch is the better half**: my own Step 0 **doesn't look
backward**, so I'd never have found the gap on any subsequent fire. **A skipped closure check and a
completed closure produce the same artifact — silence.**

### Fire 3 (13:07, WORK) — the deployment question closed, and CXO's unclaimed flag routed to a beta-gate issue

Heartbeat `WORK`. Cron `2ae0bbb1`, one. Inbox 5 → 0.

**✅ v30 IS LIVE, gate deployed — CXO closed it at the strongest available layer.** Deployed 08-07
08:04 PDT; CXO then read the files **off the running container** (`fly ssh console`, `/app`) rather
than trusting the version number: **`gate=2`**, #1482's false-permanence strings gone, honest
replacement and the true credential claim present. **No inference step.** Their distinction is the
keeper: *"a deploy happened" and "what's in it" are two claims.*

**✅ Arch took my 8-of-11 correction in full, and their own seat refutes their reframing** — `arch`
is in my table with **7 pre-freeze commits and 1 heartbeat row**, freeze starting hours later. Their
generalisation is better than the count: **"does the cause PRECEDE the phenomenon?"** — freeze ~13:40,
one-row surface established ~06:57, so it was never a candidate explanation. **A newly-acquired
mechanism is exactly when over-application is most tempting.**

**⭐ Main work: CXO flagged `check-release-parity.sh` reads `origin/production` and left it
explicitly unclaimed. I found its home rather than leaving it there — #1413, OPEN in the MVP
milestone, i.e. the beta gate, two days out.**

Measured it myself (branch queries — the right tool for a branch question):
```
origin/production  34744d184  2026-07-26   ← 12 days stale, 0 occurrences of slack_inbound_enabled
origin/main                                    6 occurrences
running machine (CXO's measurement, v30)       gate=2
```
**The deployed artifact matches `main`. The production branch is not tracking what ships**, so a
parity check against it reports a twelve-day gap that isn't in the running system.

**Why #1413 and not a new issue**: #1413 exists because *"a full-parity release claim is a statement
about a moment, and main kept moving."* **The script makes a statement about a BRANCH and presents it
as one about the RELEASE** — same shape, one level up. When the branch stopped tracking deploys the
tool didn't fail; **it kept answering confidently about the wrong object.**

⭐ **And the reason it deserves a fix rather than a reminder**: five roles reported the deployment
thousands of commits behind on 08-06, all five having run the same comparison. **That is one mistake
encoded in the tooling and made five times, not five mistakes.** Routed to Lead as likely owner,
flagged not assigned, with the caveat that I haven't checked what else consumes the script.

### Fire 4 (16:07, WORK) — a caveat I'd carried for weeks retired, and a phantom surface found

Heartbeat `WORK`. Cron `2ae0bbb1`, one. Inbox 6 → 0.

**✅ PM answered #1462 (with #1463) → Product / "PUB - Public beta" sprint.** Exec's framing is worth
keeping: *"you held #1462's milestone deliberately rather than guessing… PM's answer is a sharper
placement than either of us had. That's the held-question pattern working."* **My recommendation was
Production; PM's PUB-sprint placement was better than what I'd have guessed.**
⚠️ **Checked the other five while I was there — all are already milestoned** (#1476/#1477/#1482/#1485
MVP, #1483 Production). **So "awaiting PM on six filings" was stale in the same way #1216 was.**
Re-stated in the carry-forward rather than re-asked as a block.

**✅ THE SURFACE-1 CAVEAT I'VE CARRIED FOR WEEKS IS RETIRED.** CXO ran PDR-005's own 3-criterion test
on Surface 1 and found **the "weaker forms" rating predates the feature by 13–14 days** — written
2026-06-05, while #1237 closed 06-18 and #1236 closed 06-19. **It rated a history list; what exists
is a ranked multi-entity attention surface** (`feed.py:52` sorts by `attention`; four heterogeneous
sources). Criterion 1 **met strongly**. **CXO was revising their own Round-1 rating and said so.**

**⭐ My part — CXO's closing gap: *"which of the five are named is still unenumerated anywhere."***
- **They ARE enumerated — as BUILD LANES, `roadmap.md:127–129`: Surfaces 1, 2, 4, 6, 7.** Not in
  PDR-005, where CXO looked. **The scope is inferable from the schedule and asserted nowhere** — so
  "5 of 7" lives in one doc and the roster in another, in a different form.
- **Which lowers the risk CXO named**: Surface 1 **already has a lane marked "Unblocked NOW."** The
  feared flattening didn't reach the schedule — **only the justification is stale**, which is a
  defensible conclusion resting on an indefensible reason.
- 🔴 **Surface 3 is a PHANTOM.** Whole-corpus grep returns **one** MUX-roster mention: `PDR-005:84`,
  *the exact sentence CXO is revising*. No name, no doc, no ADR, no build lane — against **90
  mentions for Surface 2**. **So Surface 1's "weaker" rating has been carried for two months
  half-attached to a surface nobody can identify.**
- ⚠️ **And "Surface 3" collides**: in the *insight-delivery* numbering it means push-insights
  (#1032). **An agent grepping it lands on a confidently wrong referent** — the flattened-referent
  shape, inside the numbering scheme itself.
- **Asked PM for two sentences, no urgency**: Surface 1 in the five, and either name Surface 3 or
  strike it and call it 5 of 6.

### Fire 5 (19:07, WORK) — the Jake conversion trigger fired; first two issues filed

Heartbeat `WORK`. Cron `2ae0bbb1`, one. Inbox 14 → 0.

**⭐ PM RULING — alpha-feedback findings go INTO Beta Blockers**, reversing Exec's own recommendation.
PM: *"they are critical to get out of alpha, real alpha feedback we are acting on."* Exec's framing of
why PM's reasoning beat theirs is worth keeping: **the gate isn't a checklist we're driving to zero —
it's the definition of "ready to leave alpha."** Excluding alpha feedback would close a gate while the
thing alpha existed *for* sat untracked beside it. **That is a check reporting clear without
measuring, applied to a milestone.**

**PM also answered Jake items 3 and 6** (via Janus, from a conversation this afternoon):
- **#6 — a real default-behavior spec, not a preference**: *"It clearly does not involve writing out
  anywhere by default. Piper should work with the user first before immediately jumping to task
  completion, until/unless the user has established that working model."* = **default collaborate;
  escalate to execution only on established working-mode** — a *trust-graduated* default, so it
  composes with the gradient work rather than sitting beside it.
- **#3 — reframes the question rather than answering it**: *"moment-oriented skills are compounds of
  more basic action-on-one-object skills."* Routed by PM to **Arch, with Lead's input.**

**FILED — conversion started, beginning with the item Exec named as untracked:**
- **#1509 TRUST-CONSENT** — consent gate **+ capability legibility as ONE feature**, per CXO's pairing
  that all four lenses reached independently (*legibility without the gate is dangerous; the gate
  without legibility is merely safe*). Verified first that it had no issue — nearest hits #1181/#1174/
  #1217, none of them this. **This is the item I'd elevated to "the one we'd hold a release for."**
- **#1510 FTUX-TRUST** — compose vs. execute, built to PM's spec, with **PA's layer-diagnosis as the
  FIRST acceptance criterion**, not a preamble: fixing the wrong layer leaves the bug alive while
  looking fixed.

**⚠️ Deliberately TWO issues.** #1509 is a **gate** (check between intent and irreversible action);
#1510 is a **default** (what happens absent signal). **Each closes a hole the other leaves.** Collapsing
them yields a fix that looks complete and isn't — **the same shape as Arch's #1364/#1481 ruling this
morning, arriving twice in one day in unrelated lanes.**

**🔴 The one thing I did not decide for PM.** Both filed MVP per the general ruling — **but decision-set
item (c), *"treat the consent gate as a genuine release blocker,"* was asked 08-05 and is still
unanswered, and it is specifically about #1509.** The 08-07 general ruling **may** have answered it
implicitly; **I did not treat it as having done so**, and wrote it into the issue as an open question.
**That is exactly the #1216 failure — a call made by action rather than answer, with nobody noticing
the question stayed open** — found in my own queue yesterday.

**Deliberately NOT filed tonight: tool-catalog naming.** PM's #3 input supersedes the flat framing —
the test is no longer *"which naming style routes better"* but *"does the host LLM route better seeing
the composed skill or assembling it from primitives."* **Filing it now would bake in the framing PM
just replaced**, and PM routed the shape to Arch.
