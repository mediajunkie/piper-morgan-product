# Session Log — 2026-08-07 18:51 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, isolated worktree agent-ab846312fced200fc)
**Model**: Fable 5 (claude-fable-5)
**Task**: Fix #1485 — gate global `slack_app_token` write (and sibling global-credential writes) behind admin authority.

## Plan
- Investigation done: privileged-user concept EXISTS — `users.is_admin` DB column (#357 SEC-RBAC; migration cd320b81e4c6 grants xian@example.com). No admin flag in JWTClaims; `request.state.is_admin` is read (files.py/ui.py) but never set. `require_scopes(["admin"])` exists but no token is ever issued an admin scope → unusable without new issuance work.
- Mechanism: new `require_admin` FastAPI dependency in `services/auth/auth_middleware.py` — live DB check of `users.is_admin`, fail-closed.
- Audit of global writes in settings_integrations.py: `/slack/app-token` (708), `/slack/app-credentials` (646), `/calendar/app-credentials` (952). Per-user writes (1197 calendar refresh token user-scoped key, 1977 github_token username-scoped) untouched. setup.py has global writes too — discovered, out of this route file's scope, to be flagged.
- Failing-first test: tests/unit/web/api/routes/test_global_credential_write_admin_1485.py

## Work
- 18:53 Failing-first captured: with `require_admin` existing but routes unwired, non-admin POSTs to all three global-write routes returned **200 and performed the write** (`calendar_app_credentials_saved user_id=<non-admin uuid>` in test output) — 3 failed, 7 passed.
- 18:54 Wired `Depends(require_admin)` on `/slack/app-token`, `/slack/app-credentials`, `/calendar/app-credentials`. New suite 10/10 green.
- Regression: test_slack_inbound_1201 + test_inbound_deployment_gate_1484 + socket_runner_lifecycle + test_architecture_enforcement = 43 passed; settings/keychain suites = 78 passed; tests/unit/services/auth = 49 passed; full tests/unit/web/api/routes = 395 passed. #1484's 409-before-write contract pinned again in the new suite (admin + flag-unset → 409, no write).
- Mechanism: `users.is_admin` (#357) checked live via new `require_admin` dependency (`services/auth/auth_middleware.py`), fail-closed (DB error → 503, refusal logged with actor identity). Chose it over `require_scopes(["admin"])` because no token is ever issued an admin scope (auth.py issues `scopes=["user"]`) — a scope gate would lock out PM too.

## Discovered issues (for Lead to file/route)
1. **Setup routes write GLOBAL credentials UNAUTHENTICATED with no post-setup lockout** — `/api/v1/setup` is wholesale auth-exempt (auth_middleware.py:65) and `web/api/routes/setup.py` writes global slack client creds (:779) and global `openai`/`anthropic` LLM keys (:978/:985, comments literally say "No username = global") without checking `setup_complete` first. Worse class than #1485.
2. **Beta deployment step required**: migration cd320b81e4c6 seeds `is_admin=true` only for `xian@example.com`. If PM's beta row has a different email, ZERO admins exist and PM is locked out of these writes until `UPDATE users SET is_admin=true WHERE email='<pm email>'`.
3. `request.state.is_admin` is read (files.py, ui.py) but never assigned anywhere — those admin bypasses are permanently False (pre-existing).
4. No API path exists to clear/rotate `slack_app_token` (overwrite only) — noted, minor.

## Memory & briefing surfaces referenced this session
- Referenced: CLAUDE.md (test-command env stripping; failing-first discipline; discovered-work discipline), issue #1484/#1485 bodies (contract + AC).
- Loaded but not referenced: memory index.
- Wanted but not found: any doc stating which account on beta is admin.
