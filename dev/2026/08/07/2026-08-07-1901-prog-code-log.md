# Session Log — 2026-08-07 19:01 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, dispatched by Lead)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: `.claude/worktrees/agent-acbd16578b85c9e38` (isolated agent worktree)
**Task**: Fix #1471 — pre-classifier precedence: "connect my calendar" collides with the temporal calendar pattern → (temporal, get_current_time) instead of the integration-connect lane.

## Work

- Read `docs/internal/architecture/current/intent-routing-stack.md` (mandatory) before touching surface 1. The fix is entirely surface-1 (pre-classifier) precedence; the doc does not enumerate surface-1 internal pattern ordering, so it is not made stale by this change (checked).
- **Failing-first evidence** (pre-fix, static, both entry surfaces):
  - `pre_classify("connect my calendar")` → `(temporal, get_current_time)`; same for "link my calendar", "set up my calendar".
  - `detect_multiple_intents("connect my calendar")` → `[(temporal, get_current_time)]` — the dominant chat path (classify_multiple) misroutes independently of pre_classify because its pattern_groups table has no integration-connect group at all.
  - New 1471 tests run against stashed pre-fix source: FAILED (`'connect my calendar' routed to IntentCategory.TEMPORAL`).
- **Fix** (`services/intent_service/pre_classifier.py`), pattern-table only per the issue's D4 constraint:
  1. Extracted `_integration_connect_match()` (blockers + connect-verb×integration-noun patterns, shared by both entry surfaces).
  2. `pre_classify`: moved the #1417 integration-connect check ABOVE the TEMPORAL check — connect verbs now out-rank the temporal calendar-noun patterns.
  3. `detect_multiple_intents`: at the TEMPORAL group, an integration-connect match substitutes the guidance-lane intent for the temporal one (multi-intent messages keep their other parts); GUIDANCE_PATTERNS dedupe guard prevents a double guidance intent ("help me set up my calendar").
  4. New INTEGRATION_CONNECT_BLOCKER `\b(meeting|event|appointment|invite|reminder)s?\b` — calendar event-writes ("add a meeting to my calendar") keep their pre-1471 routing instead of flipping to setup guidance.
- **Ledger** (`services/intent_service/chat_pointers.py`): `integration:calendar` POINTER switched to the natural phrasing "connect my calendar"; `page:/settings/integrations/calendar` keeps "link my google calendar" — both utterances ledgered as verified POINTERs per the issue's regression requirement (#1433 ratchet asserts both resolve deterministically).
- **Tests**: 9 new tests / 15 rows in `tests/unit/services/intent_service/test_integration_connect_preclassifier_1417.py` (#1471 section): positive precedence rows, multi-intent path row, substitution-keeps-greeting row, dedupe row, temporal regressions, event-write blocker rows.

## Verification

- `tests/unit/services/intent_service/test_integration_connect_preclassifier_1417.py` + `tests/test_architecture_enforcement.py`: 62 passed (includes the #1433 reachability ratchet with the new POINTER).
- `tests/unit/services/intent_service/` (full dir): 1987 passed, 1 skipped.
- `pytest -m smoke` (gating subset): 539 passed, 31 skipped.
- Pre-existing failures (verified identical on stashed unmodified tree, NOT mine): `tests/domain/test_file_reference_detection.py` classification tests → `ContainerNotInitializedError` (live-LLM tests in the keyless lane). Filed #1506.
- Not run: `tests/intent/test_classification_accuracy.py` (marked `llm`, keyed lane) and `tests/e2e/test_canonical_conversations.py` (LLM-gated) — per task instruction.

## Discovered issues filed

- #1505 — multi-intent path drops integration-connect asks paired with a greeting ("hi piper, connect my github" → greeting only); pre-existing #1417-era gap, calendar now covered only via the 1471 substitution.
- #1506 — `tests/domain/test_file_reference_detection.py` classification tests fail keyless (need `llm` marker or container stub).

## Commit

- `fix(1471): connect-verbs out-rank the temporal calendar pattern in the pre-classifier` — see report to Lead for sha. No push (per task); Lead merges.
