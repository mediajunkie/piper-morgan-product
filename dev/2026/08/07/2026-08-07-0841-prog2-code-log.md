# Session Log — 2026-08-07 08:41 — prog2 (Coding Agent, Claude Code)

**Role**: Coding Agent (prog2)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a815d8c22190fe59b (worktree; NOT pushing to origin/main per task hard rule)
**Task**: #1491 — Due-reminder fetch crashes on naive-vs-aware datetime comparison; saved reminders never surface (live on v30)

## Plan
1. Read #1491, locate due-reminder fetch in todo_handlers + repository query
2. Find #1429 standup guard shape; mirror it (aware-UTC normalization at comparison boundary)
3. Failing-first regression test (naive + aware stored rows)
4. End-to-end handler-level surfacing test
5. Swallow: keep try/except but ensure warning has reminder-count context; note bare broad swallows for #1423
6. Class check: grep for other naive/aware comparisons

## Work
- 08:41 — session start, read issue #1491
- 08:45 — located the bug: `services/intent_service/todo_handlers.py::get_due_reminders` (line 105) used naive `datetime.now()` against `todo.reminder_date` (aware from Postgres timestamptz) → TypeError → warning-swallow → None sentinel → reminders never surface. Delegation path (list_todos → TodoRepository.get_todos_by_owner) has no datetime comparison — the bug is entirely in the handler.
- 08:50 — found the #1429 guard shape (webhook_router.py:1637-1650): aware-UTC `now` + per-row `tzinfo is None → replace(tzinfo=utc)` coercion. Codebase canonical helper: `services/utils/datetime_utils.ensure_utc` (same semantics).
- 08:55 — failing-first: added `TestDueReminderTZGuard1491` (4 tests) to `tests/unit/services/intent_service/test_reminders.py`; all 4 FAILED pre-fix (naive/aware rows → None sentinel; warning lacked count context).
- 09:00 — fix: `get_due_reminders` now uses `datetime.now(timezone.utc)` + `ensure_utc(reminder_date)` per row (#1429 shape); return annotation corrected to Optional[List[str]]; swallow retained (#1425 sentinel) but warning now carries `todo_count` + `reminders_considered`.
- 09:05 — verification: 4/4 new tests pass; test_reminders.py 36/36; #1425 sentinel tests 14/14; todo-adjacent files 85/85; full tests/unit/services/intent_service/ 1923 passed, 1 skipped (pre-existing RuntimeWarning not from my file).
- 09:10 — class check (todo_handlers + todo repositories + adjacent):
  - todo_handlers.py: no other Python naive/aware comparisons (only the fixed site).
  - services/repositories/todo_repository.py: naive LOCAL `datetime.now()` at lines 130 (SQL-side due_date cutoff in get_due_todos — NO callers found, dead-ish), 232/279/281/305/425/460/517 (naive-local writes to completed_at/updated_at timestamptz), 337 (recent-todos cutoff). SQL-side/writes — cannot raise the TypeError shape, but tz-drift class. Reported, not fixed (out of scope per AC).
  - services/intent_service/temporal_utils.py: naive-local now throughout (lines 41, 91); internally consistent (naive-vs-naive, no crash) but produces naive LOCAL reminder_date stored to timestamptz — the upstream source of ambiguous stored times. Reported, not fixed.
  - services/api/todo_management.py:526-527,706: naive `datetime.now()` writes. Reported.
  - canonical_handlers.py: existing sites already guarded (`datetime.now(dt.tzinfo)` shape).
- 09:12 — #1423 awareness: commented on #1423 with the datapoint (broad except on core path, #1425-sanctioned sentinel shape, but #1491 proves the cost — suggest narrowing exception type during inventory).

## Discovered work
- tz-drift class findings above (todo_repository naive-local now, temporal_utils naive-local reminder storage) — reporting to Lead in wrap report rather than filing blind; Lead/PM to decide whether one class issue covers them.


## Wrap
- Commit: b9775a4cd on worktree-agent-a815d8c22190fe59b — NOT pushed to origin/main per task HARD RULE (Lead merges after composition audit).
- Discovered work filed: #1493 (naive-local datetime class in todo layer). #1423 comment added (datapoint).
- git status clean after commit; no unstaged files in services/, tests/, web/.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (worktree/commit discipline, discovered-work discipline, GitHub auto-close-keyword gotcha — shaped commit message wording); issue #1491 body (AC + guard-shape pointer); #1429 fix site in webhook_router.py (guard shape mirrored); #1425 sentinel pattern (kept swallow semantics); services/utils/datetime_utils.py docstrings (ensure_utc contract).
- **Loaded but not referenced**: MEMORY.md index, briefing table, mailbox protocol (subagent task — no mailbox lane).
- **Wanted but not found**: a canonical "tz discipline" doc naming which layers store naive vs aware — reconstructed it from #768/#750/#1079/#1429 breadcrumbs; #1493 now records the todo-layer picture.
