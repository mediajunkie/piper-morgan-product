# Session Log — 2026-08-01 09:10 — prog2 (Coding Agent, Claude Code)

**Role**: Coding Agent (prog2), subagent worktree `worktree-agent-a7f73fae62169594a`
**Model**: Fable 5 (claude-fable-5)
**Task**: #1430 — Learning dashboard acts on phantom user 'current_user' (F19, census 2026-07-16)
**Branch**: worktree-agent-a7f73fae62169594a (0 behind origin/main at start)

## Investigation (verify-first, whole-artifact read)

Read all of `web/api/routes/learning.py` (1540 lines), both dashboard HTML files, `ui.py` serving route, startup template config, existing test idioms.

**Live state differs materially from the issue's framing:**

1. The issue's named routes (`learning.py:571, :604, :638` — `/controls/learning/enable|disable|status`) are **dead code**: their decorators were commented out in `4824ddf6e` (Nov 2025, #300 deprecation of Sprint A5). Never registered; requests to them 404. Same for `/controls/data/clear`, `/controls/export`, `/controls/privacy/settings`, `/analytics`, `/health` (lines 358, 530, 667, 733, 816, 867).
2. The **served** dashboard is `templates/learning-dashboard.html` (via `ui.py:384` + Jinja2Templates rooted at `templates/`), NOT `web/assets/learning-dashboard.html`. The served page's status/toggle/metrics already use the authed `/settings` (GET/PUT) and `/patterns` (GET) routes — auth binding completed by #1250/#1252 (75%-complete situation).
3. Remaining phantom-user surface in the SERVED page: `exportData()` → `GET /controls/export?user_id=current_user` and `confirmClearData()` → `DELETE /controls/data/clear?user_id=current_user`, plus the `USER_ID = 'current_user'` constant. Both endpoints unregistered → the Export and Clear buttons are broken (404) today.
4. `web/assets/learning-dashboard.html` is an **unserved orphan duplicate** (old 8-fetch version; no mounts or references in web/, main.py, services/). It is what the census read — source of the stale line numbers.

## Plan

- Complete the partial auth-binding (per task: "If partial auth-binding exists, complete it"):
  - Add production authed `GET /controls/export` + `DELETE /controls/data/clear` to learning.py (#300 section idiom: `JWTClaims = Depends(get_current_user)`, `AsyncSessionFactory.session_scope()`, error-response helpers). No `user_id` parameter declared → client-supplied value ignored.
  - Served template: drop `USER_ID` constant, point the two fetches at the authed routes.
  - Delete the orphan `web/assets/learning-dashboard.html` (grep-verified unreferenced).
- Tests: failing-first registration test; two-user isolation (settings toggle + clear) against dev Postgres per the `test_learning_cycle_phase3_phase4.py` fresh-user idiom; client-user_id-ignored via TestClient + dependency override; real `template.render()` UI test per house discipline.

## Work

- **Failing test first**: `tests/integration/test_learning_dashboard_identity_1430.py::test_controls_export_and_clear_routes_registered_1430` FAILED before implementation (routes absent from router map). Evidence: `assert ('/api/v1/learning/controls/export', 'GET') in {...}` AssertionError.
- **learning.py**: added production `GET /api/v1/learning/controls/export` (`export_learning_data`) and `DELETE /api/v1/learning/controls/data/clear` (`clear_learning_data`) in the #300 section — `JWTClaims = Depends(get_current_user)`, `AsyncSessionFactory.session_scope()`, error-helper idiom; no `user_id` parameter declared (client value ignored). Deprecated Sprint A5 handlers left as-is (dead, decorator-commented, removal already planned post-MVP).
- **templates/learning-dashboard.html**: removed `USER_ID = 'current_user'` constant; Export/Clear fetches now hit the authed routes without `user_id=`; download filename no longer embeds the phantom id.
- **web/assets/learning-dashboard.html**: DELETED (git rm). Unserved orphan duplicate — zero references in web/, services/, main.py, tests/, scripts/ (grep evidence); only doc mentions were the Sprint-A5-era verification doc (annotated) and a 2025 UX-audit historical inventory (left as historical record).
- **docs/features/learning-system-verification-tests.md**: supersession note added to header.
- **Tests**: 12 added across 2 files, all passing: 7 in `tests/integration/test_learning_dashboard_identity_1430.py` (registration, no-user_id-param, export scoped to principal, client user_id ignored on the wire via TestClient+override, two-user clear isolation, invalid data_type 422, two-user settings-toggle isolation — real dev Postgres, fresh users per test) + 5 in `tests/unit/web/api/routes/test_learning_dashboard_template_1430.py` (real `template.render()` through app_shell: no USER_ID/'current_user'/user_id=, authed endpoints present, /learning route registered).
- **Regression**: `test_learning_cycle_phase3_phase4.py` + `test_principal_threading_lint.py` (21 passed), `test_phase3_phase4_learning.py` + `test_architecture_enforcement.py` (29 passed).
- **Discovered work**: filed #1465 — `execute_pattern` success path NameErrors on missing `datetime` import (learning.py:1272); successful proactive executions recorded as failures (failure_count += 1, confidence *= 0.9).
