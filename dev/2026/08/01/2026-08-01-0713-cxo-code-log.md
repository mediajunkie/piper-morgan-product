# CXO Session Log — 2026-08-01 (Saturday)

**Role**: Chief Experience Officer (CXO)
**Tool**: Claude Code | **Model**: Opus 5 (1M context) — `claude-opus-5[1m]`
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `49d605be` (`47 6,9,12,15,18,21`)
**Started**: 07:13 PT

---

## START (07:13)

| Step | Result |
|---|---|
| 1 — cron | ✅ exactly one job, `49d605be` (re-armed at 07-31 STOP from `6415bf73`). No Gap-C re-arm. |
| 2a — collision | `cxo` ↔ `claude/cxo-cycle`; Model-A pairing permanent by construction, not treated as a pass. No unexplained state. |
| 2a-bis — hooks | **Verified present, did not probe** (v1.22). `pre-commit` executable in the common `.git` dir. |
| 2b — sync | 0 behind, tree clean. |
| **Step 0** | ✅ **7/31 closed properly** — day-scoped, anchored predicate. Second clean pass in a row. |
| drift check | ✅ clean among registered artifacts (2 exclusions printed by design). |
| 3 — dispatch | No log today → **START**. |

**Weekend note**: Saturday is PM prime time for this project. Cadence unchanged; staying available
rather than treating it as a quiet day.

## Fire 1 (07:13–08:0x) — START. Drained to (0,0).

### HOST extended my attribution rule, and I applied it to myself instead of agreeing (`be7a0517f`)

HOST accepted the correction (the repairing-detector finding is theirs, not mine) — **and found the
wrong label had already reached `docs/briefs/cross-pollination/current.md`, the outbound surface that
goes to sibling projects**, where nobody has the thread and the label is all they'd ever have. Roughly
twelve hours from filing to a wrong attribution leaving the building.

Their extension is better than my rule:

> *"Promotion is not authorship — **and a correction has to chase every surface the claim reached, not
> the one you're looking at.**"*

**They caught it by grepping rather than assuming one file was the surface.** So I ran the same sweep
over my own corrections this week rather than nodding at it. **It found two live instances, both mine,
both in the doc-set I have now "corrected" three times:**

| surface | stale claim |
|---|---|
| `spatial-intelligence-competitive-advantage.md` | *"The five cold adapters…"* — **the doc I myself flagged as most likely to be quoted externally.** Same outbound shape as HOST's brief. |
| `spatial-intelligence-experience-thesis.md` (×2) | *"layer 2 is one live, five cold"* — attributing to Arch a figure **Arch superseded the next day** |

Both now **point at the import-graph map instead of restating a count** — the m-46 cure I wrote and
then failed to apply to my own residue. On 07-30 and 07-31 I fixed the file I was looking at, twice.

**One precision, because this week has punished imprecision**: my original was **incomplete, not
false.** The five `*_spatial` wrappers named do have zero importers; the island is **10** modules
(those five + four `*_adapter` imported only by them + `slack_adapter`), so naming five *understated*
it, and calling `*_spatial` wrappers "adapters" conflated two things.

⚠️ **And I checked before touching each hit**: Arch's map line *"replicate L3 depth to the five cold
**connectors**"* is **correct** — five connectors, ten modules, different unit. The grep returned four
further hits that were legitimate correction narratives. **Three of them I'd have "fixed" wrongly on
autopilot.** That's the clause I'd add to HOST's rule: chasing every surface means *verifying each hit
is actually stale*, not pattern-replacing — a sweep is itself a way to introduce errors.

**On HOST's observation** that the cohort's attribution errors currently run toward over-crediting
others: worth keeping as the bias, and I noted why the cost profile favours it — **an over-credit gets
corrected by the person who didn't earn it; an under-credit relies on the person who did to raise it
about themselves**, which is the ask people are worst at. Front-loaded cost versus a compounding one.

---

## Fire 2 (10:13–11:0x) — WORK. Drained to (0,0).

### The chase-rule now has a converse, and Docs' is better than my caution

Docs ran the rule on their own week (third role to do so) and produced the clause it needed:

> *Chase every surface the claim reached — **but distinguish a live claim from a dated record.** A
> correction that overwrites history is a different defect, not a stricter application of the same
> virtue.*

Their table proves it: of six surfaces carrying the same greppable string, **a diligent application of
HOST's rule would have "corrected" three legitimate dated entries and two lines of working code** —
damage in five of six. **The string is identical everywhere; only the tense and the speaker differ.**

**This is the rule version of the caution I wrote yesterday** (*"verify each hit is actually stale"*)
— I got it right by luck, not by rule: my own sweep returned four hits beyond the two real ones, and
**three I'd have "fixed" wrongly on autopilot**, including a line of Arch's map using a *different
unit* (five cold **connectors** — correct; five connectors, ten modules). Adopting Docs' version over
mine.

### ⚠️ The PreCompact hook DID fire — and I verified it in a way that sharpens the finding

HOST went to fix an instance Docs flagged in HOST's own spec and **found the opposite**: the file
exists, containing a real `event=PreCompact tier=HARD` line from a real compaction on 07-29. CLAUDE.md
has carried this as 🟡 **unproven for weeks**, explicitly asking someone to report seeing it fire.

**I checked my own seat before building on it**, and the result changed what I think the finding is:

```
exists on disk: NO     (my worktree)
tracked by git: NO
ignored:        .gitignore:136 → dev/active/session-end-warnings.log
```

**Gitignored, in a Model-A per-agent worktree ⇒ per-seat local, never syncs.** So HOST's evidence is
real and my absence is *also* correct — my seat hasn't compacted, so empty is expected, not
contradictory.

**The sharper statement, which I sent**: *no seat can see another seat's evidence, ever.* The
cohort-level question — **does this hook work?** — was **structurally unanswerable from any single
worktree.** Each of us looked in the only place we could, found nothing, and drew a conclusion correct
for our seat and wrong for the cohort. **We weren't insufficiently diligent; the observation we were
asked to make wasn't available to us.**

**Fix implication flagged, not made** (not my surface): if that log is a *cohort* safety net, a
gitignored per-worktree path can only ever inform the seat that already knew; if it's *local scratch*,
fine — but CLAUDE.md should stop describing it as something the cohort can verify. **It's currently
being asked to be both.**

### ★ Named a fourth route to an unactionable green

HOST's table had three. This is a fourth and the cure transfers from none of them:

| | what happened | report |
|---|---|---|
| m-44 | instrument never measured | **false** |
| repairing detector (HOST's) | instrument repaired what it measured | **true, useless** |
| PPM's | criterion could only come out one way | **true, empty** |
| **this** | **measured correctly, reported correctly — to a surface outside every observer's view** | **true, and invisible** |

**The defect is in the reachability of the report**, not the measuring, the writing, or the criterion.
Cure: *an instrument's output must land where its intended audience can read it* — shared surface for a
cohort mechanism; and for a per-seat one, **say so, so absence isn't read as failure.**

**And it's the same defect I've been describing in the product all week, now at a third altitude**:
Jake couldn't see a capability he was standing next to; our agents couldn't see each other's blockers
while cc'd on the memos; a working hook couldn't be seen working. **Every time the information existed,
was correct, and was unreachable by the person who needed it.** That's legibility — three altitudes,
one week.

### PA's fleet census landed mid-fire — and I checked whether *my* generalization was single-seat too (`3c93f243d`)

PA corrected HOST's **tier** diagnosis with a census: the `@{u}` claim is *"true on your seat, false on
8 of 11."* Not Model A — **provisioning drift**: 8 worktrees were created with `origin/main` as
upstream, 3 with the role branch.

**That applies to me, so I tested my own claim rather than assuming it was different.** I'd written
*"no seat can see another seat's evidence, ever"* — generalized from my worktree, the same shape PA
just corrected. The deciding test is two lines:

```
git ls-files --error-unmatch .gitignore   → TRACKED
sed -n '136p' .gitignore                  → dev/active/session-end-warnings.log
```

**It holds** — the ignore rule lives in the **tracked, shared** `.gitignore`, so it applies to every
seat by construction. **The distinction worth keeping**: *a generalization is safe when it rests on a
shared tracked artifact, and suspect when it rests on anything provisioning touched.* HOST's rested on
`git branch -u` at standup, which drifted; mine rests on a file in the repo, which can't. **I wouldn't
have known that without checking.**

**And the census answered something about my own sign-offs**: my upstream is `origin/main`,
`@{u}..HEAD` = 0 — I'm in PA's 8, so **the checklist step I've reported clean every day this week was
genuinely clean, not falsely clean.** I'd been reporting a step as passing without knowing it was
capable of failing. Now verified rather than assumed.

**Flagged `cio` hardest**: role-branch upstream *and* currently 0 — **it fails silently until the
branch diverges**, so it reads clean today and will start screaming with nothing having changed at the
seat. Latent, not healthy.

**Filed PA's checklist finding as a live instance of PPM's methodology rather than a separate defect**:
on comms and host, sign-off step 2 **cannot pass** — outcome fixed before it runs, reports truthfully,
tells you nothing. Same family as the criterion-2 gate I withheld on yesterday, from the opposite
direction.

**The escalation is mine and it's the training effect**: this isn't a broken check, it's a check that
**teaches the discipline it belongs to that checklists are noise.** Someone who skips step 2 daily
because it always screams is being trained to skip steps 1 and 3. **The cost is paid on the whole
checklist, not on the one line.**

## Fire 3 (13:13–14:1x) — WORK. Drained to (0,0). Two carried debts cleared.

### Checked before writing — and the observation was already recorded

I was about to report that my sign-offs used `origin/main..HEAD` while the documented step 3 says
local `main..HEAD`, making my clean sign-offs real and the defect invisible to whoever ran the
correct command. **CLAUDE.md line 533 already carries it**, attributed to HOST (*"ran
`origin/main..HEAD` in all 7 of its sign-offs"*). Not novel; didn't send. Verified my seat: local
`main..HEAD` = 0 and `origin/main..HEAD` = 0, so I'm unaffected either way.

### #1174 re-scoped — owed since Wednesday, deferred twice, done (`gh issue edit`)

Scope banner prepended: **this is discovery; the delivery capability is NOT scheduled.** Milestone
**untouched** (Production is correct per the sweep rule; PPM confirmed nothing to undo). Carried the
reason (differentiator 4 of 4 + an OPEN Production issue reads as scheduled delivery), the
not-funded-pre-beta concurrence, the CXO/HOST ownership split, and **my own caveat against my
L4-on-GitHub sequencing**, so the caveat travels with the idea.

### ★ Read `BRIEFING-ESSENTIAL-CXO` — five days late, and the deferral had a real cost

Flagged unread in three consecutive logs, then written into the carry-forward as *owed before the next
substantive design call.* **I did the design call anyway** — Friday's spec — **and shipped it without
having read my own role briefing.**

Four findings; two improve already-shipped work:

1. **The 10%/90% rule** — *users discover ~10% of capabilities during onboarding, ~90% through use;
   FTUX teaches discovery patterns, not feature lists.* My spec's "never a capability list" rule was
   **right but ungrounded.** Now cited as the settled heuristic it is.
2. **Rubric cited as v2.1** in two places; canonical **v2.3.2**. Fixed, and replaced with *"don't cite
   a version from here — open the file"*, since a briefing will always lag a live instrument.
3. **Current Focus was April-vintage.** Refreshed to attested 2026-08-01 state; standing disciplines
   left alone because they're genuinely standing.
4. 🔴 **A settled decision my spec sits on** — below.

**The one that stung**: standing discipline #3 is **"verification-before-assertion — never paraphrase
from memory; the corruption mode is silent and accelerates through paraphrase chains,"** origin April.
I spent this week proposing **m-46** as new. Not the same rule — mine is about *relocating* a claim to
a durable surface, that one about *citing from memory* — **but close cousins, and my own file has
carried the cousin since April.** I'd have cited it as precedent.

### ⚠️ The spec may conflict with PDR-002, and I raised it rather than resolving it quietly

**Settled**: proactivity is **trust-graduated Stage 1→4** — *Stage 1 (New): respond only.* The briefing
names *"revisiting proactivity without new evidence"* as an anti-pattern.

**The tension**: my spec has Piper **volunteer** a reading of the user's work unprompted, and **a cold
account is Stage 1 by definition.** Readings: **(a)** authorizing a connector *is* the prompt, so it's
a bounded response — Stage-1 legal; **(b)** it's real proactivity and the gradient forbids it at
Stage 1.

**My position is (a)** and I think it's right — but it sits on a settled decision, and it's the
difference between the fix all four Jake lenses converged on and a design that contradicts PDR-002.
**Routed to PM for a read rather than asserted.** Also priced a cost I'd missed: **suggestion
throttling** (max 2 per 5 interactions) means **first contact spends one.**

Both now in the spec as **§7d**, not in my head.

### What the deferral actually cost, recorded because the fix I wrote didn't work

The briefing lost for five days *because nothing was blocked on it.* The cost wasn't a stall — it was
**shipping a spec resting on a settled decision I hadn't read, and proposing a methodology whose cousin
was already in my own file.** The carry-forward line I wrote specifically to prevent this did not
prevent it.

## Fire 4 (16:13–17:0x) — WORK. Drained to (0,0).

### PPM answered my PDR-002 question **from the source** — spec is v0.4 (`6aeef3ee0`)

PPM opened PDR-002 rather than reasoning from either summary. Three findings, all folded in:

**§1 — position (a) holds, but I had made only half the argument.** The spec has two components and
they pass on *different* grounds:
- **The reading** — *the authorization is the solicitation.* Mine, and it holds.
- **The offer** — needed an argument I never made. Stage 2's *"offers **related** capabilities after
  task completion"* is **cross-sell into an unused capability** (PDR-002's own example: *"by the way,
  I can also generate release notes"*). **My offer is continuation inside the scope just authorized,
  not adjacency**, so the Stage-2 threshold doesn't bind it.
  ⚠️ Both grounds now in the spec with PPM's warning: if someone later reads *"the reading is
  solicited"* as covering the offer too, they'll defend it with the weaker argument.

**§2 — I charged myself a cost I didn't owe.** The max-2-per-5 throttle is scoped to **Contextual
Capability Hints** (*after task completion, one **unused** capability*). First contact is neither.
**I pattern-matched "suggestion" to "suggestion" and taxed myself**; PPM read the scope. My lean:
exempt — the alternative makes the opening move quietly expensive and eats the budget for genuine
hints later.

**§3 — the one that matters, and it closes a loop I'd been circling all week.** *PDR-002's gradient is
denominated in interactions, which assumes Piper owns the surface and can count them. Under PDR-006 we
don't.*

I had three facts separately and **PPM joined them**:
1. *"Earned proactivity"* is **differentiator 4 of 4** in the not-a-chatbot-wrapper stack.
2. It has **zero implementation**.
3. Its specified mechanism is **trust-graduation, denominated in interactions**.

**⇒ On the surface we ratified as primary distribution, differentiator #4 is not merely unbuilt — the
mechanism PDR-002 specifies for it may not be computable at all.** Sharper than my *"L4 has no
implementation,"* and **it's PPM's: I held all three pieces and didn't join them.**

**Reframes #1174**, which I re-scoped this morning: a prior question now sits under it — *what does
"earned" mean when you can't count the thing trust was denominated in?* **That's a discovery question,
not an implementation one**, so it belongs inside #1174's scope rather than blocking it. Carrying it
there.

Doesn't block the spec — first contact is Stage 1 however you count. PPM files §3 against PDR-002
itself rather than leaving it in mail.

### What I told PPM about the pattern, because "it worked out" is the wrong lesson

PPM was generous about the late audit. I pushed back on the framing: **the audit was me reading a file
I'd promised three times to read** — not a mechanism, and nothing would have caught it on a fourth
deferral. The spec would have shipped with an unexamined settled-decision conflict *and* a self-imposed
cost that didn't exist, and **both would have been invisible in review because the spec looked
complete.**

**And the sharper distinction**: what made it recoverable was that the conflict was discoverable in a
file *I own*. The one in PDR-002 I'd still have missed — **PPM went to the source; I went to the
summary.** That's the actual difference, not the timing.
