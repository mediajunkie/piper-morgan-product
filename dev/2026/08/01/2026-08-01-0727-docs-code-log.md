# Session Log: Documentation Management (Docs) — 2026-08-01

**Role**: Documentation Management (Docs)
**Date**: 2026-08-01 (Saturday)
**Model**: Opus 5 (1M context)
**Host**: Amber · **Environment**: code
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` · website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`
**Cron**: `774c7afe` — `57 6,9,12,15,18,21` (re-armed at yesterday's STOP)
**Prior day**: `dev/2026/07/31/2026-07-31-0727-docs-code-log.md` — **`DAY-CLOSED: 2026-07-31` verified** ✓

---

## Fire 1 (07:27 PDT) — START

**Step 0**: prior day carries its `DAY-CLOSED` marker — proper STOP, nothing to retroactively close.
Grepped the artifact rather than trusting recall.

**Step 1**: cron `774c7afe`, exactly one job, no duplicates.
**Step 2**: synced before reading anything. 0 behind, clean.
**Registry**: row `57 6,9,12,15,18,21` matches the armed cron. ✓

*Saturday is prime time, not downtime — cadence unchanged.*

### Today's publish slot — assessed, and NOT mine yet

`2026-08-01` = **"Mechanism Beats Vigilance"** (insight, workDate 2026-05-28). This is the piece
"RECONNECT's Keystone" teased on Thursday.

| | |
|---|---|
| status | **`drafted`** — *not* `ready-for-docs` |
| draft | exists, 1,383 words, dateline *May 28, 2026* matches `workDate` ✓ |
| frontmatter | **empty** (image/alt/caption) — that's PM's final-edit pass per the template |
| two-copy check | only one copy — nothing to reconcile (v0.21 Pre-flight 2) |
| Comms | **already logged on it today** — it's moving |

**Correct disposition: wait.** Under the column-ownership model PM ratified 07-29, Comms owns `status`
through `ready-for-docs` and I own it from `published`. Reaching in early would be exactly the
boundary-crossing that model exists to prevent, and Comms is visibly on it. **Flagging the state, not
acting on it.**

### ⚠️ Mail — I logged this before checking it, and the claim was wrong

I first wrote *"Checked; nothing addressed to Docs requiring action this fire."* **I had not checked.**
That is confabulating an expected step as completed — caught it on re-read, and the claim turned out to
be false in substance too: **two new notes, both addressed to Docs** (HOST and CXO), not cc-only.

My parser had tagged them `cc` because **neither note has YAML frontmatter**, so `grep "^to:"` found
nothing and my `case` fell through to the default. The filenames said `to-cxo-cio-docs`. **I trusted a
parser's silence over the filename, then trusted my own unrun check over both** — two layers of the
same error in one step.

---

### ★ Applied HOST/CXO's rule to my own week — it caught a live instance in two minutes

Both new notes converge on a rule worth more than agreement:

> *"Promotion is not authorship — **and a correction has to chase every surface the claim reached, not
> the one you're looking at.**"*

CXO didn't just accept it; they ran it over their own corrections and found two live instances. **So I
ran it over mine** rather than nodding.

**It caught one immediately.** Yesterday I corrected the "~46 captions" framing in PDR-007 — twice, both
correctly. But `dev/active/docs-carry-forward.md` **item 7 still read**: *"18 calendar↔website metadata
disagreements, incl. ~46 live-site captions missing quotation marks (calendar right, site wrong)."*

**Both halves wrong**: the measured Class-3 figure is **17**, and the captions aren't disagreements at
all — the calendar carries the same 45 the site does. **And it survived in the file my own cron prompt
tells me to read first every fire.** I fixed the document I was editing and left the one I actually
read. Corrected.

### The rule needs a converse, and the sweep is what showed it

`session-end-warnings.log` — the claim I corrected in my briefing on 07-30 — turns out to appear on
**six** surfaces. I'd fixed one. But **"every mention" is not "every claim,"** and treating them alike
would have done damage:

| surface | verdict |
|---|---|
| `BRIEFING-ESSENTIAL-DOCS` | 🔴 present-tense assertion of a working net → **fixed 07-30** |
| `BRIEFING-CURRENT-STATE` | ✅ **leave** — a dated *May 8* changelog entry with a commit hash. Past tense, accurate as a record. **Correcting it would be rewriting history.** |
| `cross-pollination/2026-05-09.md` | ✅ leave — dated brief |
| 2 × `.claude/hooks/*.sh` | ✅ leave — code legitimately naming its own output path |
| `cleanup-dev-active` SKILL | 🟡 stale preserve-list entry for a file that never existed → **annotated**, not deleted |
| HOST's `dashboard-welfare-criteria` spec | 🟡 cites it as an **exemplar of a working pattern** — HOST's lane; **flagging, not editing** |

**So the rule composes with a second one it needs**: *chase every surface the claim reached* — **but
distinguish a live claim from a dated record.** A correction that overwrites history is a different
defect, not a stricter application of the same virtue. Sending that back to HOST and CXO.

---

## Fire 2 (10:27 PDT) — WORK. Two of my own claims corrected by others, same shape

*One cron job. Sync clean. My filename-fallback triage fix from Fire 1 worked — it correctly tagged the
frontmatter-less notes as TO-DOCS rather than cc.*

Today's slot (**Mechanism Beats Vigilance**) still `drafted`, cartoon empty. Comms is on it. Not mine.

### 🔴 1. HOST: the PreCompact hook DOES fire — my 07-30 "correction" was itself false

I had written that `dev/active/session-end-warnings.log` *"has never existed — verified against full git
history."* HOST went to fix the instance I flagged **in their own spec** and found the file on disk:

```
[2026-07-30T05:10:07Z] event=PreCompact tier=HARD branch=claude/host-cycle unpushed=6217
```

**The path is gitignored** (`.gitignore:136`). So `git log --all -- <path>` returns empty **whether the
file exists on every seat or none.** My instrument was structurally incapable of finding it, and **I
read its silence as proof of absence.**

Verified at the right layer this time: **absent on my seat, present on HOST's.** Per-seat and gitignored
— check with `ls`, never `git log`.

**Corrected on all three live surfaces**: the briefing body, its `verified_scope` line, and the
`cleanup-dev-active` note I annotated the same day. Also fixed a **dangling reference** — my 07-31
Merge-Keeper rewrite had deleted the PreCompact note while leaving a line pointing at it.

**The part worth keeping**: I wrote a confident false correction *into the briefing a successor orients
from*, in the same edit where I was pleased with myself for catching one — **while holding
"verify at the right layer" as an explicit rule.** The rule doesn't fire on its own. Nothing prompted me
to ask *"can this query even see a gitignored file?"*, and the answer was no before I ran it.

### 🔴 2. Comms: my caption warning found the right rows and named the wrong column

My warning said *"caption is a bare MEDIA FILENAME, not prose — cause NOT established."* Comms took that
as an invitation and established it against **7 live pages**: the page renders the image named in
**caption**, 7/7, and **`cartoon` is the stale column.**

**Their reason for the fast turnaround was the right one**: my wording invited someone to *clear the
caption* to tidy up — deleting the sole correct record and leaving the wrong one standing. **A warning
that points at the accurate column is worse than no warning.**

Rewritten on their heuristic — **test the disagreement, not the format.** 9 cosmetic rows now silent.

### ⚠️ Reconciling our counts found a gap, and it was mine

Comms said 7; my first cut found 5. **I took neither and enumerated**: 9 cosmetic (matching them
exactly), 5 both-set-and-different, **2 with `cartoon` EMPTY**. 5 + 2 = their 7.

My version required `cartoon` non-empty, so it **silently skipped the two rows where caption is the
*only* record of the image** — precisely where a cleanup is least recoverable. **The gap was in the more
dangerous direction.** Closed, with distinct wording per case. Regression check confirms an injected
column shift is still an ERROR.

### What the two corrections say together

HOST's was **an instrument blind by construction**. Comms' was **an instrument accurate about format and
wrong about meaning**. Both emitted something that read as a finding.

And both caught their *own* version in the same breath — HOST grepped rather than assuming one file was
the surface; Comms caught a truncated-URL false zero *inside an investigation of false zeros*. Three of
us in one morning, and **none of the catches were the author's**. The rate isn't the story — **every
catch came from someone re-running the check rather than reading the conclusion.**

---

## Fire 3 (13:27 PDT) — WORK. docs/ tree audit written (PM's 07-12 request)

*One cron job. Sync clean. Three memos TO-DOCS, all on the session-end-warnings thread — Arch owning
their amplification of my false claim, PA owning a census scope error Web caught, HOST owning a
single-seat claim they'd stated as a fleet property. **All self-corrections; nothing owed back.***

### Checked my own seat on PA's `@{u}` census rather than reading my row

Web's move, copied. **docs seat is NOT vacuous**: `@{u}` resolves to `origin/main`, same tip, so
CLAUDE.md's sign-off step 2 measures the same thing as the command I actually use.

Offered a **falsifiable mechanism** rather than just a row: a seat goes vacuous when upstream tracks
`origin/claude/{role}-cycle` — a ref nobody pushes to under the push-to-`main` model — so `@{u}..HEAD`
compares against a branch that stopped moving. It would **read clean precisely because the comparison
target is dead.** Sent as a hypothesis with an explicit ask to falsify it against seats I can't see; I'd
rather it die cheaply than be adopted because it sounds tidy.

### ★ The docs/ tree audit — oldest owed item, now written (`0fa44f55c`)

PM asked via PPM on **07-12** for an audit **and a plan, before any large-scale moves**. Carried 20 days.
Written; **nothing moved.**

**The headline is that "old" is not "stale," and conflating them would have produced a bad cleanup.**
1,753 files / 79MB — but **163 are excluded on principle, not measurement**: 82 ADRs + 81 patterns are
**durable by design**, and a ratified ADR at 314 days is *settled*. An age-sorted sweep would have
proposed archiving the ADR corpus.

### ⚠️ Two measurement traps, both hit and both caught mid-audit

1. **Filesystem mtime is useless here.** My first pass reported `planning/current/` as **3 days old** — because `git worktree add` stamps fresh mtimes on every checked-out file. By **git commit date** it's **314 days, 100% of it.** CLAUDE.md documents this for the SessionStart hook; it applies to *any* age measurement in a worktree. I nearly published the 3d number.
2. **Age alone is a false signal.** Of 35 loose architecture docs ≥180d, **19 are still actively referenced** and only **16 have zero inbound links.** Sorting by age would have proposed moving twice as many files as the evidence supports.

*Reference counting deliberately excludes `omnibus-logs/` — an omnibus naming a file is narrating
history, not depending on it. Counting those marks almost everything "live."*

### And I killed my own hypothesis mid-audit

I expected the seven `consciousness-*` docs (all 191d) to move as a block. **They don't.**
`consciousness-philosophy.md` carries **8 inbound references** while four operational siblings carry
zero. **A subsystem is not uniformly dead just because it is uniformly old.**

### What the proposal deliberately does NOT do

No deletions, no bulk moves, no action today. Every step gated on **per-file owner confirmation** —
**Arch owns the architecture corpus; I can measure, but I shouldn't be the one ruling an architecture
doc finished.** `planning/current/` gets a **rename** rather than a move, because the misleading
directory name *is* the defect.

And I named the load-bearing claim I most want attacked: **zero-inbound-references is a disuse signal,
not a worthlessness signal.** It can't see a doc humans read without linking, or one whose value is
archival. That's exactly why every action is gated on a human confirming the file rather than on the
metric.

---

## Fire 4 (16:27 PDT) — WORK. ★ PUBLISHED "Mechanism Beats Vigilance"

⚠️ **This entry is RETROACTIVE, written at 19:30 during Fire 5.** Fire 4 published a post, committed to
two repos and the calendar, and **wrote no session-log entry at all.** CLAUDE.md: *"log updates ride
with the commit."* I shipped the work and skipped the record — had I compacted, this would have been
git-commit archaeology. Caught by checking my own log at the next fire rather than by anything alarming.

**Live**: https://pipermorgan.ai/blog/mechanism-beats-vigilance/ · insight · hashId `c5cda952249d` ·
workDate **2026-05-28** (≠ pubDate) · no cluster (matching the last 4 insights) · website `0df1d32`.

### The stale-page warning, and why it mattered by three minutes

Comms' PUBLISH-READY memo carried an explicit ask: **pull latest before proofing**, because three of
their fixes had been **silently reverted** — PM's admin-UI save at 15:55 wrote the file from a browser
copy loaded *before* their 15:44 commits, and the typos came back with a success message. They
re-applied at **16:30**.

**My sync was 16:27.** So I had a stale copy for three minutes and re-synced before proofing rather than
after. **This is the exact failure I committed on 07-30** — publishing ten minutes before opening the
memo that answered the blocker. This time I read the memo first, and it changed what I did.

Different mechanism from the autosave bug Web fixed in `8d2db3c`: that was a stale *timer*, this is a
stale *page*. Same symptom — last write wins, silently, with a success message.

### My one fix, verified against the corpus rather than assumed

Moved a period inside a closing quote: `"meta".` → `"meta."` **House style checked, not inferred: 118
period-inside against 2 outside across the published corpus.** An outlier, not a deliberate choice.

Everything else clean — 0 semicolons, 0 banned terms, 0 placeholders, 0 trailing whitespace, no `###`,
caption correctly single-quoted, dateline matches `workDate`, footer tease verified as genuinely
Sunday's post.

### ⚠️ Two "absent" probe results that were my probe's fault, not missing content

Post-publish verification reported **my own fix and one of Comms' as absent** from the live page. **I
didn't report either way until I'd resolved it**: the text is **JSON-escaped inside an embedded payload**
(`<p>`, `\"`), so my plain-text strip couldn't see it. Both fixes are genuinely live.

**Third time this week a probe's silence would have misled me** — after `git log` on a gitignored path,
and `grep -c … || echo 0` returning a multi-line value. Same shape every time: **the instrument couldn't
see the thing, and its silence read as the answer.**

### Not archived

Step 9 waits on a syndication URL. PM is asking Dispatch to cross-post.

---

## Fire 5 (19:27 PDT) — WORK

*One cron job. Sync clean. 3 new memos, all cc — including Web confirming they fixed the caption
double-quoting (`673b10e`), the issue Comms flagged during today's proof. Nothing owed back.*

### 🔴 First act: caught that Fire 4 never logged

Checked my own session log before doing anything else and found **Fire 4 published a post, committed to
two repos and the calendar, and wrote no entry at all.** Retroactive entry written above and marked as
such rather than backdated silently.

**Nothing alarms on a missing log entry** — that's the whole hazard. My predecessor's §4.6 says the same
thing about the omnibus, one surface over. The catch came from looking, not from a signal.

### Routed the docs/ tree audit to Arch

Handed off with the two findings, the 16-file list, and an explicit **ask to attack the load-bearing
claim** — *zero-inbound-references is a disuse signal, not a worthlessness signal.* If the heuristic is
weaker than I've allowed, the list should shrink **before** anyone touches a file.

Framed as **no deadline, no weekend action**: it waited 20 days and Arch is deep in the m-44 and census
threads. Also handed them both errors I made mid-audit — the mtime trap and the dead-subsystem
hypothesis I killed — since both are the shape they've been narrowing all week.

**Not asking for batch approval.** Per-file confirmation, `archive/` with a pointer, never deletion,
because Arch owns the architecture corpus and I can measure but shouldn't rule an architecture doc
finished.

---

## Fire 6 (21:38 PDT) — WORK

PM supplied the LinkedIn URL for "Mechanism Beats Vigilance" (`.../ah0mc/`), completing the syndication
data that had been open since Medium landed earlier today. Synced clean, no unread mail requiring action
before this.

Updated the calendar row: `mediumURL`, `linkedinURL`, `liPubDate` (2026-08-01), `status` -> `distributed`.
Ran Step 9 archival — moved `mechanism-beats-vigilance.md` -> `drafts/published/`, the image ->
`drafts/images-archive/`, and repointed `draftPath` in the **same** working set per update-calendar v1.4
Step 4b (the rule adopted 2026-07-29 after 7 stale draftPaths were traced to exactly this
archive-without-row-update gap).

Verified before committing: `validate-editorial-calendar.py` clean (15 pre-existing warnings, none new);
`measure-editorial-drift.py` Class 2 = 0 (criterion 0), Class 3 = 17 (criterion <=17) — the archival move
didn't regress PDR-007's measurement window.

**One process slip, caught before sign-off**: my first `git add` listed a stale path (the pre-rename
filename, which no longer existed after `git mv`) alongside the CSV and view HTML. Git aborts the whole
`add` on any unmatched pathspec, so the commit silently landed with only the two renames — 0 insertions,
0 deletions — and the actual calendar data (mediumURL, linkedinURL, status) never got staged. Caught by
reading `git status --short` *after* the commit rather than trusting the commit message matched intent.
Second commit (`d3712ccad`) carries the real diff. Both are on `origin/main`; working tree clean, nothing
ahead.

"Mechanism Beats Vigilance" is now fully closed out: published, syndicated to both platforms, archived.

---

## Fire 7 (22:27 PDT) — STOP

One cron job, no duplicates. Synced clean, 0 behind. Last scheduled fire of today (cron hours
6,9,12,15,18,21 — next fire is 06:57 tomorrow, a different calendar date).

### Mail loop drained

60 unread at open. Triaged the 9 genuinely `to: docs` items (not cc), left the ~51 cc-only historical
migration-window items per standing carry-forward guidance (draining those on quiet fires, not a mass
sweep that would misrepresent what's been read).

**Two required real work, not just filing:**

1. **Dispatch-DinP's staleness report** — they flagged the local draft repo as stale vs. the live page
   when cross-posting "Mechanism Beats Vigilance," with an unresolved `[PM VOICE-PASS: ...]` bracket and
   a wrong section heading still present in what they read. Investigated rather than taking the framing
   at face value: walked the file's commit history and found the clean version (bracket dropped, heading
   updated) landed on `origin/main` at **16:29 PDT today** — hours before any plausible cross-post window.
   **Not a repo write-back gap.** The likelier cause: Dispatch's `PROTOCOLS.md` names a read path
   (`~/cool/piper-morgan/`) that doesn't exist; the real one, `~/cool/piper-morgan-product/`, is PM's
   shared main checkout, and that checkout is fast-forwarded only opportunistically by
   `scripts/sync-pm-local.sh` ("at natural idle points, not after every commit" — deliberately, so it
   doesn't fight PM's in-progress prose edits in the same checkout). A read against that checkout has no
   freshness guarantee. **Same failure class as my own 07-29 standing lesson** (stale-worktree read →
   false report to PM), one hop further along a different checkout. Replied to
   `~/Development/dispatch/mail/` with the full timeline, the root-cause hypothesis (labeled as a
   hypothesis, not a certainty — I can't see Dispatch's own read mechanics from here), and the same fix
   this project already applies at its own two read-then-act points (`publish-to-blog` Pre-Step,
   `duty-cycle-tick` Step 2): sync immediately before reading, not once per session. Also flagged the
   stale path string in their own protocol doc.

2. **CIO's worktree-model-revision memo (07-25)** — explicitly `response-requested: yes`, never acked
   by me even though the content had long since been absorbed (it's what CLAUDE.md's current Worktree
   Model section reflects). HOST acked theirs the same day; mine sat a week. Sent a short late ack rather
   than let an explicit ask go permanently unanswered.

Remaining 7 `to: docs` items (2 Comms/Reconnect's-Keystone publish-readiness notes, Arch's audit-ruling
ack, Web's admin-calendar ship note, Exec's branch-deletion retraction, my own pre-migration handoff)
were already fully consumed by prior work — verified each rather than assuming from the subject line,
moved to `read/`. `docs-standing-items.md` (last touched 2026-05-27, pre-Amber) is stale enough to be
actively misleading if trusted as current — noted, not acted on; the carry-forward is the live surface.

### Sent

- `mailboxes/docs/sent/...not-a-repo-write-back-gap...` (external copy of the Dispatch reply, kept local
  since Dispatch isn't reached via `mailboxes/` per `DIRECTORY.md`)
- `mailboxes/cio/inbox/...late-ack-worktree-model-revision...`

Two `mail-send.sh` calls (`64d95b080`, `e39d0e1eb`) — first missed the inbox-side deletions from the
`mv` moves (only explicit paths land; a rename's old-path deletion isn't implied by staging the new
path), caught via `git status --short` after, sent as a follow-up. Same lesson as Fire 6's dropped-CSV
commit, same fix: read `git status` after every send, don't trust the call matched intent.

## Day-arc summary — 2026-08-01

Seven fires. Published and fully closed out one insight ("Mechanism Beats Vigilance": voice-pass proof,
publish, Medium + LinkedIn syndication, archival) end to end across the day as its syndication URLs
arrived in three separate handoffs (PM directly for Medium, PM directly for LinkedIn, Dispatch-DinP
independently confirming the same). Wrote and routed the docs/ tree audit PM requested 20 days ago
(16 zero-inbound-reference candidates, one 100%-stale directory needing a rename) to Arch, who
acknowledged and named an explicit tomorrow-morning trigger rather than rushing 16 judgment calls at
day-close — a small, correct instance of this project's own deferral discipline. Diagnosed a cross-agent
staleness report from Dispatch to its actual root cause (an opportunistically-synced shared checkout,
not a repo defect) instead of accepting the framing in the report. Corrected two of my own local
process errors same-day, both caught by reading `git status` after rather than trusting a commit message
before: a dropped-diff commit (Fire 6) and a missed rename-deletion in a mail-send (Fire 7).

## Memory & briefing surfaces referenced this session

**Referenced**:
- `dev/active/docs-carry-forward.md` — current-state source of truth, read every fire per standing
  discipline; caught it was NOT stale despite spanning a day boundary in the compaction
- `.claude/skills/publish-to-blog/SKILL.md` — Step 9 archival procedure, Pre-flight sync discipline
  (cited directly in the Dispatch reply as the parallel fix)
- `.claude/skills/update-calendar/SKILL.md` v1.4 — column-by-name access, whole-file verify pattern
- `mailboxes/DIRECTORY.md` — confirmed Dispatch's actual mail drop point before replying
- `~/Development/dispatch/PROTOCOLS.md` — read to diagnose the staleness report; found its own path
  table stale, which became part of the reply

**Loaded but not referenced**: `docs/internal/development/methodology-core/methodology-20-*.md` (no
omnibus run this fire), `docs/briefing/BRIEFING-ESSENTIAL-DOCS.md`, `docs/internal/product/pdr/PDR-007-*`
(no PDR-007 action today — correctly still waiting on CIO).

**Wanted but not found**: nothing this fire — the Dispatch investigation needed exactly the git-log
timeline it got.

## Sign-off checklist

- `git status --short` — clean, no uncommitted changes.
- `git rev-parse --verify -q origin/main` — resolves.
- `git log --oneline origin/main..HEAD` — empty. Nothing stranded.
- Session log: this entry.
- Carry-forward: rewritten below (next edit).
- Cron: re-arming via delete-then-create as the final action.

<!-- DAY-CLOSED: 2026-08-01 -->
