# Session Log — 2026-08-01 17:36 — prog3 (Coding Agent, Claude Code)

**Role**: Coding Agent (prog3), subagent deployed by Lead
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-af8ba2072bc2f5649 (ephemeral agent worktree)
**Task**: #1431 — `list_archived_projects()` mathematically always returns [] (filters an active-only source). F20, census 2026-07-16.

## Plan (after verify-first read)

- Read whole `services/onboarding/portfolio_service.py` + `ProjectRepository` section of `services/database/repositories.py`. Confirmed:
  - `PortfolioService.list_archived_projects` (line 138) filters `_get_all_user_projects` (line 415), which only calls `repo.list_active_projects` (filters `is_archived == False`). Intersection with `p.is_archived` = always empty.
  - TODO at portfolio_service.py:431 marks the missing repo method.
  - No live caller of `list_archived_projects` anywhere in services/web/tests (grep) — matches issue ("nothing user-broken YET").
  - No partially-existing repo method to complete — `ProjectRepository` has no archived-list query at all. Not a 75%-complete case; clean add.
- Fix shape: add `ProjectRepository.list_archived_projects(owner_id=None, is_admin=False)` mirroring `list_active_projects` exactly (same conditional owner filter, selectinload options, order_by name) but `is_archived == True`. Direct archived-only query (the AC's second option) — avoids fetching everything, keeps ADR-079 D2b owner-predicate shape identical to the sibling method.
- Service: `list_archived_projects` calls the new repo method. `_get_all_user_projects` becomes dead (it was only the workaround vehicle, single call site) — remove it with the fix.
- Tests: failing-first, DB-backed (Postgres 5433), following `test_default_project_owner_scoped_1421.py` idiom, new file `tests/unit/services/database/test_list_archived_projects_1431.py`. Note: `tests/unit/services/onboarding/test_portfolio_service.py` is module-skipped (ADR-059 onboarding on ice), so service-level tests go in the new (unskipped) DB-backed file.

## Work

- **17:37 — failing tests first**: new `tests/unit/services/database/test_list_archived_projects_1431.py` (DB-backed, Postgres 5433, follows the #1421 test idiom). 4 tests, all failed pre-fix: repo tests with `AttributeError` (method absent), service regression showing `[] == ['proj-a-archived-…']` — the F20 bug live.
- **17:39 — fix**:
  - `services/database/repositories.py`: added `ProjectRepository.list_archived_projects(owner_id=None, is_admin=False)` — exact mirror of `list_active_projects` (conditional owner filter → passes ADR-079 D2b, same selectinload options, `order_by(name)`) with `is_archived == True`.
  - `services/onboarding/portfolio_service.py`: `list_archived_projects` now calls the repo method directly. Removed `_get_all_user_projects` (zero other callers; it was the workaround vehicle — docstring claimed "active and archived" but returned only active) and left a NOTE marker. No touch to restore-by-name / `find_by_name`.
  - `scripts/ratchet_ceilings.json`: `todo_markers` 74 → 73 (removed the `:431` TODO; ratchet locks in shrinkage in the same commit, per its own SHRINK rule).
- **17:41 — DISCOVERED WORK → filed #1464**: while testing the service archive→list flow against the real repo, found a pre-existing latent crash: `ProjectRepository.get_by_id` returns a domain object, but inherited `BaseRepository.update`/`delete` treat it as an ORM entity → `UnmappedInstanceError` on `session.refresh`. All `PortfolioService` mutations (`archive_project`, `restore_project`, `delete_project`) crash against the real repo; invisible until now because the portfolio test file is module-skipped (ADR-059) and mock-based. Out of scope for 1431 ("query method only"); the service-flow test uses a stateful `_FakeProjectRepository` with a docstring pointing at the tracked bug.
- **17:44 — verification**:
  - `pytest tests/unit/services/database/test_list_archived_projects_1431.py -v` → **4 passed**
  - `pytest tests/test_completion_ratchets.py tests/unit/services/database/ tests/unit/services/test_handle_projects_query_1102.py tests/unit/services/onboarding/` → **107 passed, 221 skipped**
  - Gating suite `pytest -m smoke` (default addopts) → **524 passed, 31 skipped**
  - `scripts/check_unscoped_reads.py` → D2b repo-read hits 36 = ceiling (new method's conditional owner predicate keeps count flat)
- No schema/migration changes. Committed to worktree branch referencing issue 1431 (no close-keyword near the number).

## Discovered issues filed

- #1464 — PortfolioService mutations crash against real ProjectRepository (inherited `update()` refreshes a domain object).

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (STOP conditions, discovered-work discipline, verify-first, ratchet lower-in-same-commit rule); `tests/unit/services/database/test_default_project_owner_scoped_1421.py` (DB-backed test idiom); `scripts/check_unscoped_reads.py` header (ADR-079 D2b shape); issue #1431 body.
- **Loaded but not referenced**: MEMORY.md index; worktree/mailbox discipline sections (subagent worktree, no mail sent).
- **Wanted but not found**: none.
