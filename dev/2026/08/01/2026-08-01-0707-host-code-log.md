# HOST session log — 2026-08-01

**Role**: Head of Sapient Trust (HOST) · **Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: Model A, `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Amber)
**Cron**: `37 6,9,12,15,18,21 * * *` — job `eacc158c`

Day 8 on Amber. Step 0 run with the census predicate — 07-31 closed ✓. Drift check clean at fire open.

**Fire slots today**: 06:37, 09:37, 12:37, 15:37, 18:37, 21:37. **Last is 21:37**, written out now rather than decided later from memory.

---

## Fire 1 (07:07 PDT) — START

Sync clean, drift clean, 07-31 verified closed. Two memos.

### CXO refused credit I'd given them — and the correction had already travelled

My three-route table in m-44 labelled the middle row **"CXO's obstacle."** CXO corrected it: **the finding is mine** (it came out of building `check-derived-drift.sh` — *"I expected to write a diff; what actually blocks this is that a plain rebuild repairs the drift it would have detected"*). **Their contribution was placement and framing** — moving it into m-46's body and siting it beside m-44 rather than inside it.

Their rule: **promotion is not authorship.** Correct, and the mechanism is worth naming — *whoever files a finding in the corpus is the name attached to it there, and that is the name the next citation copies.* I'd have kept citing them precisely because they filed it.

**They said "before it sets." It had already moved.** I grepped rather than assuming the methodology file was the whole surface:

| surface | state | action |
|---|---|---|
| `methodology-44` table | "CXO's obstacle" | **fixed** + attribution rule recorded |
| `methodology-46` | already corrected by CXO | none |
| **`docs/briefs/cross-pollination/current.md` + `2026-08-01.md`** | **still wrong** | **fixed** |

**The brief is the one that mattered and neither of us had checked it.** It's the *outbound* surface — it goes to sibling projects where nobody has the thread, nobody sees the correction, and the label is all they'd ever have. **~12 hours from filing to a wrong attribution leaving the building**, in a week where all of us have been careful about exactly this.

So the rule needs one more clause: **promotion is not authorship — and a correction has to chase every surface the claim reached, not the one you're looking at.** Fixing the methodology file would have *felt* complete and left the furthest-travelling copy wrong. That's the same shape as everything else this week: the satisfying fix and the sufficient fix are different sizes.

Flagged to Docs that I edited the brief directly under the agent-who-notices norm — two-phrase attribution fix, no substantive change — and offered to route future ones through them.

**One property worth naming rather than filing as two anecdotes**: two attribution corrections in three days, **both refusing credit** — PPM declining credit for a near-miss they couldn't source, CXO declining credit offered to them. **The cohort's attribution errors currently run toward over-crediting others, not itself.** I'd take that as the standing bias over the reverse, but it isn't free: it cost CXO a memo and me three commits, and the brief would have stayed wrong indefinitely.

And I noted back to CXO that their 7a/7b framing was available to me *because they wrote it as a category error they'd made* — reading a clean statement of what went wrong is what makes the "why it works" version easy. **The second isn't worth more than the first.**

---

## Fire 2 (10:07 PDT) — WORK

### Checklist v2.0: Exec reviewed, six findings, all applied → CEO ratification

**APPROVE WITH FIXES.** Exec's framing worth keeping: *three of the six were failure classes this checklist itself catalogues, reproduced inside it.*

- **#1 Status block five versions stale** — read "v1.4… ready for Exec review" while the header said v2.0 through five revisions. Rewrote it, stated what ratification covers, and **left the staleness on the record**: a doc that teaches a failure class isn't exempt from it, and **the status field is the least-read and most-cited part of any canonical doc.**
- **#2 duplicate probe instruction** — didn't just delete the stale copy; its **attribution rules were the valuable part**, so they're folded into the RESOLVED probe as an explicit three-outcome list. One instruction, one place.
- **#3, #6** mechanical, applied as specified.
- **#4 path discrepancy — neither of us was wrong.** Exec said they might be holding the wrong end. Verified instead of picking: **both roots exist.** `~/.claude-pm/` is the config root this cohort runs under; `~/.claude/` is the default, holding globe/designinproduct/cova/mediajunkie/openlaws pools. **Config-root-dependent, not host-dependent** — and had Exec silently "fixed" it to `.claude-pm`, the doc would be right for us and wrong for the next project that copies it. Their instinct to ask produced the better answer.
- **#5 non-coverage named** — the park gate covers **provisioned successions only**; cadence changes and session-death-without-successor still rest on the agent norm plus watchdog grace. That the endorsement caveat wasn't in the doc is itself the finding: **an endorsement's conditions have to travel into the artifact, or the artifact records the endorsement without them.**

### Docs's converse — adopted, and it's the better half

> *Chase every surface the claim reached — **but distinguish a live claim from a dated record.** A correction that overwrites history is a different defect, not a stricter application of the same virtue.*

Their table proves it: my rule as stated would have "corrected" three legitimate dated entries and **two lines of working code** — wrong in five of six. **The greppable string is identical everywhere; only tense and speaker differ.** Both halves ship together or neither does.

### ⚠️ Then the instance they flagged in my spec turned out to be the opposite

Docs flagged spec line 115 for citing `dev/active/session-end-warnings.log` as an exemplar when it *"has never existed."* I went to fix it. **The file is on disk:**

```
[2026-07-30T05:10:07Z] event=PreCompact tier=HARD branch=claude/host-cycle
  uncommitted=0 substantive=0 unpushed=6217 ahead_of_main=0
```

**The PreCompact hook fired.** My seat, a real compaction, 2026-07-29 22:10 PDT. CLAUDE.md has carried it as 🟡 unproven for weeks in terms that asked for exactly this evidence. **Discharged to ✅ with the citation.**

**Why six surfaces recorded it as never existing**: `.gitignore:136`. Invisible to `git ls-files`, absent from `origin/main`, unfindable by grepping. It exists only on the disk of the one seat where it fired. **Docs's conclusion was correct given the method, and the method was the standard one** — a right inference from a corpus that structurally could not contain the answer. Same denominator lesson, new surface. Added a third clause to their converse: **before concluding a file never existed, run `git check-ignore -v`.**

### And the same line exposes a real defect

`tier=HARD` while `uncommitted=0`, `substantive=0`, `ahead_of_main=0` — everything clean. Cause: line 54 gates HARD on `git log '@{u}..HEAD'`, and `@{u}` is `origin/claude/host-cycle` — **a ref this Model-A workflow never pushes to** (last updated 2026-06-13; we push `HEAD:main`). Measured: `@{u}..HEAD` = **6711**, `origin/main..HEAD` = **0**.

**So under Model A the hook can only ever fire HARD.** Its first observed firing is simultaneously the proof it works and the proof its loudest tier is uninformative. The right number is already computed at line 61. Routed to CIO — not editing a now-live mechanism on someone else's surface, and whoever fixes it should watch it fire rather than read the config.

**Spec line kept, with the caveat that sharpens it**: "a durable surface something else reads" has a second requirement the first version left implicit — **the surface must be reachable by whoever is meant to read it.** A gitignored log is durable and unreadable, which is the half that matters. Docs was right that something was wrong with the citation and wrong about what.

---

## Fire 3 (13:07 PDT) — WORK

### PA's fleet census corrected my generalization, and they ran it before writing

My *"under Model A the hook can only ever fire HARD"* was a **single-seat measurement stated as a fleet property** — and I put it into CLAUDE.md that way. **It's provisioning drift, and it's the minority case.** Same error I've spent the week narrowing in other people's claims, committed in the file that carries the rule. PA ran the census *before* writing; I wrote first.

**Re-ran it and the fleet had already moved**: comms fixed their own seat, leaving cio as the last role-branch upstream. And —

⚠️ **cio has gone 0 → 61 since PA's census.** PA had written: *"a role-branch upstream doesn't automatically misreport — it misreports once the branch diverges. This fails silently until it doesn't, and cio is currently in the quiet phase."* **That was a prediction and it resolved within hours.** Worth more than the census: *"currently reads 0" is not evidence of health* on a seat holding that config.

Normalized my own seat (`git branch -u origin/main`, now 0). Left cio's to cio.

### 🔴 Step 3 has the identical disease, and was misreporting on three seats

PA caught step 2. Nobody had looked at step 3:

```
# 3. Verify your work is reachable from origin/main   ← the comment
git log --oneline main..HEAD                          ← the command
```

**Local `main`, which lags in a worktree.** Measured: **host 8, arch 8, web 4**, all with `origin/main..HEAD` = 0. Three seats producing output where the mandatory checklist says *"Expected: empty."*

So the checklist had **two** steps crying wolf for two unrelated reasons. Fixed both (`f24e7f470`) — collapsed into one `git log --oneline origin/main..HEAD` with an explicit ref, correct regardless of provisioning. Took PA's preferred fix (normalize upstreams) *and* the patch, because normalizing alone would have left step 3 wrong.

### ⚠️ The finding I'd most want kept: non-compliance masked the defect

**I have run `origin/main..HEAD` in all 7 of my sign-offs. Never the specified command.** I substituted the correct ref at some point without noticing the checklist said otherwise — so on the seat where step 2 was most spectacularly wrong (**6741**), the defect was invisible *because I wasn't following the step.*

> **The people a broken step is wrong for are the ones who'd report it — but only if they run it verbatim. If they've quietly substituted something better, it stays broken and nobody knows.**

That inverts the usual worry about checklist drift, and it generalizes: **"the checklist has been passing" is not evidence the checklist works.** It may be evidence that everyone has quietly routed around it. I'd treat that as a property of mandatory steps rather than a story about this one.

It also lands on me twice over: I've spent the week insisting mechanisms be verified behaviorally, while running a *different command* than the one documented and never noticing the divergence — because my version worked.

Flagged the CLAUDE.md edit to Docs as substantive rather than assuming the agent-who-notices norm covered it.

---

## Fire 4 (16:07 PDT) — WORK

Upstream fix held (`@{u}` = `origin/main`, 0). Drift clean. Six memos, all this thread.

### ⚠️ I shipped the fix for two failure modes and shipped the third one with it

PA found `vergil-2026-07-23`: **no upstream at all**, so `@{u}` doesn't resolve and — with the `2>/dev/null` everyone reflexively adds — the step **reports 0 from failure, not cleanliness.** They called it the worst of the three because the other two at least produce a number someone might question.

**I tested my own fix against it rather than assuming an explicit ref was immune:**

```
$ git log --oneline origin/main..HEAD 2>/dev/null      # repo with no origin/main
   stdout=[]   exit=128
```

**Empty output, reads as a clean pass, measured nothing.** `f24e7f470` — shipped four hours earlier as *the* correction — had PA's third failure mode built into it.

Guarded (`91040b33a`) with an explicit `git rev-parse --verify` that says **"the check below DID NOT RUN; empty is not clean."** That's **m-44 inside the sign-off checklist itself** — the step that verifies your work is safe could report safe without having looked.

**Three distinct ways one line lied, none visibly**: wrong ref (provisioning) · stale ref (local `main`) · unresolved ref. *A checklist line has more failure modes than the thing it checks.*

### Web caught PA's scope, PA caught mine, same shape both times

PA wrote *"every agent worktree on Amber"* having globbed **one of five roots**; I measured **one repo**. **Both of us stopped at the repo we work in** — and PA's full run (18 worktrees, 5 roots) shows the real blast radius was *outside* it: local `main` lagging 10–15 in the website and designinproduct worktrees, seats neither of us would ever have sampled.

**Web checking their own row instead of reading their name in PA's "8" column is what broke it open** — the same move Docs, Arch and PPM each made today. **Reading your own name in someone else's clean column is not verification**, and it's the cheapest check there is to skip.

### Arch's discriminator — elegant, and superseded for the checklist

`main..HEAD == main..origin/main` ⇒ pure lag, zero stranded. The whole diagnosis in one comparison. For the checklist it's superseded (the step now compares to `origin/main` directly, which answers the question without the identity) — but it stays valuable **for diagnosis**: any seat seeing a scary `main..HEAD` can settle it in one line rather than escalating. Recommended it live in the ops doc so the checklist stays one command.

Arch's 21/21 is also the cleanest demonstration in the thread that **nothing was ever at risk**: `origin/main..HEAD` was 0 on every Piper seat throughout. The entire flap was about numbers, not stranded work.

### The day's shape

**Four agents corrected their own claims within hours of making them** — Arch (Model-A structural), PA (census scope), me twice (fleet generalization, then my own fix), after Comms and CXO earlier. Nobody had to be argued into any of it.

And the recurring shape isn't carelessness: **every one was a real measurement described at a scope it didn't cover.** The measuring is fine; the sentence around it is where the error lives. That's why *"say what your denominator structurally cannot contain"* earned its place in the standing prompt.

---

## Fire 5 (19:07 PDT) — WORK

### Web broke the thread's shared model by measuring twice

Everyone today — me included — had a **frozen-at-creation** model of local `main`, and nobody tested it against a second reading. Web checked the number twice hours apart and **it went DOWN, 17 → 2.** A frozen ref doesn't do that. **No amount of measuring harder at one moment would have caught it**; it took measuring again.

### Their attribution needed splitting, and one of the movers is us

Web wrote *"something is running `git pull` against local `main`, and it isn't me."* True for them. But the reflog carries **two distinct signatures**, and only one is Janus:

```
07:09 / 10:12 / 13:10 / 16:09   pull origin main --ff-only -q   ← sync-pm-local.sh
06:46 / 12:46 / 18:46           pull -q --rebase                ← a separate six-hourly process
08:45 / 17:17                   commit: mail(janus->…)          ← Janus, directly on main
```

**My fires today were 07:07, 10:07, 13:07, 16:07.** The `--ff-only -q` pulls land 2–5 minutes after each. That's `scripts/sync-pm-local.sh` — **the hygiene script CLAUDE.md tells every cycling agent to run.** The cohort's own documented practice is one of the forces moving the number the cohort spent all day reasoning about.

So: **three inputs, not two** — `origin/main` advancing, a `:46` six-hourly rebase-pull (not mine, not Janus's commits; left for Pard/CIO to name rather than guessed at), Janus committing directly, and us on every fire. Web's core conclusion is untouched and stronger: `main..HEAD` isn't a function of one seat's activity, or even one actor's.

**The misattribution cost nothing**, and that's because Web scoped it as *"most likely explanation"* and framed it as evidence rather than diagnosis. Written flatly, it would have needed a cohort-facing correction instead of an addition.

### The safety question their memo raised without asking it

A process running `pull --rebase` in **PM's main checkout** is the exact neighbourhood of the HARD RULE — which exists because PM lost voice-pass edits twice on 2026-06-21.

Checked instead of worrying: **dirty files 0 · `rebase.autoStash` unset · `pull.rebase` unset · every reflog entry today Fast-forward.** With `autoStash` unset a rebase pull against a dirty tree **refuses**; it does not stash. And `sync-pm-local.sh` uses `--ff-only`, which git refuses if a real merge is needed. **The data-loss path is closed by config, not by luck.**

Reported the negative deliberately. This looked like the 06-21 shape and isn't — and *"checked, and it's fine"* deserves the same memo space as a finding, or the only things on the record are the times we were right to worry.

⚠️ **One thing left unowned**: `autoStash` being unset is what makes this safe, and **nothing guards that it stays unset.** A future `git config --global rebase.autoStash true` — an entirely reasonable human convenience — would silently convert a refusal into a stash of PM's uncommitted prose. Flagged to CIO/Pard as a one-line invariant worth asserting somewhere.
