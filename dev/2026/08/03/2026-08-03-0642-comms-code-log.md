# Session Log: 2026-08-03-0642-comms-code

**Role**: Communications (Comms)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Monday, August 3, 2026
**Start Time**: 6:42 AM PT
**Host**: Amber.local · **Model A** stable worktree `~/Development/piper-morgan-worktrees/comms` · branch `claude/comms-cycle`
**Cron**: `e37fa867` · `12 6,9,12,15,18,21 * * *` (verified live via `CronList`)
**Prior session**: `dev/2026/08/02/2026-08-02-0642-comms-code-log.md` — closed ✓ (anchored predicate, day-scoped glob)

**Active pattern families this session**: Investigation (check the artifact, not the record about it), Completion Theater.

## Session Objectives

**Monday is a gap day — nothing publishes today.** Next slot is **Tue Aug 4, "The List That Lies"** (building). Per the carry-forward: **get ahead of it today rather than discovering it tomorrow morning.** The last two days both found an unstaged post at START; doing the pre-pass a day early is the fix rather than the reaction.

1. **Pre-pass tomorrow's post** — expect `drafted`, no art, possibly unvoiced. Surface what PM needs while there's a full day of runway instead of hours.
2. **PM-gated, hold**: Beats 24-28 steer (**the dated item** — queue dry after Aug 18; era question entangled) · Beats 21-23 voice-pass + art · `/hooks` open · compose-UI restore-banner check.
3. **Web/PM**: era split · featuring the current post on the blog index.

## Work Log

### 6:42 AM — Session Start (START fire)

- Step 0, **anchored** predicate + day-scoped glob: Aug 2's log carries a real column-0 marker. No retroactive close needed.
- `CronList`: one job, `e37fa867`, matching the registry row. Survived overnight. **Auto-expires ~2026-08-09.**
- Sync clean: `@{u}..HEAD` 0, 0 behind, tree clean. **Inbox empty.**
- **Calendar checked** — the whole near queue is `drafted` with **no art**: Aug 4, 6, 8, 9. Nothing publishes today.

### 7:15 AM — Pre-pass on tomorrow's post, a day early — and it found a hole in the publish gate

**No post today.** Next is **Tue Aug 4, "The List That Lies."** Did the pre-pass now rather than tomorrow morning, which is the carry-forward's standing instruction and the fix for two consecutive days of discovering an unstaged post at START.

**State**: never voice-passed (last touch Jul 9, agent commits only), no art, **1,901 words** against the 800–1,300 target and above `template-audit`'s 1,600 flag.

**Two things for PM**, recorded on the calendar row:
- Frontmatter `image`/`alt`/`caption` all empty.
- An **open `[PM: …]` bracket at line 39** asking a genuine factual question only PM can answer: whether the Jun 29 gate removal on the live server was PM's own. The logs establish only that it was *undocumented and not the Lead's*. If it was PM's, there's a stronger first-person beat about owning an untracked change to a live server; if not, it stays attributed to no one.

**On length — the structure is already right, which is the interesting part.** The post is A-plot/B-plot **by design** (the milestone confabulation; the server door) with a synthesis, exactly what §1.5 asks for. The fat isn't structural: it's that both plots run at full length **plus a third story**. My read on the strongest single cut is the three-agent collaboration paragraph — *"nobody defended their first draft"* — which is lovely and is a separate story. Left it for PM; §1.5 makes the cut PM's call, and I'd rather hand over a named candidate than a 1,901-word wall.

⚠️ **And the pre-pass found a real hole in my own gate.** I reported *"open brackets: 0"* from an ad-hoc grep, then noticed a `[PM: …]` bracket while reading. Checking properly: **`template-audit` check #5 — the check that BLOCKS the publish-ready signal on placeholders — could not see `[PM: …]` or `[PM VOICE-PASS: …]`**, which are the two bracket forms these drafts actually use, present in **5 drafts**.

**It scored 0 against yesterday's draft, which had a live open bracket.** So the gate has been passing drafts with unresolved PM questions in them, and the only reason yesterday's got caught is that my ad-hoc grep happened to use a different pattern than the skill's own — **the two disagreeing is what surfaced it.** The skill's check #13 already *referenced* `[PM: ...]` brackets, so the form was documented inside the very file whose check couldn't see it.

Fixed in **v1.3**: widened to `\[PM\b`, verified zero false positives across the drafts directory — every match is a genuine editorial bracket. **Bumped frontmatter and footer together**, which is precisely the defect v1.2 shipped with and CXO's thread caught last night.

**Fifth instrument-measures-the-wrong-thing instance in two days**, and the first where the faulty instrument was a *gate* rather than a report. The tell was the same as always: two checks of the same thing disagreeing.

### 9:12 AM — Follow-through on the gate fix: what the blind check had been missing (quiet fire)

Inbox empty, tree clean, cron `e37fa867` alive. **The fire's work came from the morning's finding rather than from mail**: check #5 had been blind to `[PM: …]`, so **nobody had been surfacing those brackets** — I went looking for what was actually sitting unanswered.

**4 unanswered PM questions across 3 scheduled posts** — all genuine factual questions only PM can answer, not placeholders I could fill:
- **Aug 4 *The List That Lies*** — was the Jun 29 gate removal on the live server PM's own? (**publishes tomorrow**)
- **Aug 6 *Drained on Paper*** — who declared the backlog drained; does the dishes/baby line land as written?
- **Sep 12 *No Undo*** — is the babysitter/baby analogy PM's own framing?

Recorded as an Active row in standing-items so they're answerable **in one pass** rather than rediscovered post-by-post at each publish. That's the actual cost of the blind gate: not a leak, a **drip**.

⚠️ **Then the question that mattered: had the blind gate ever let a bracket SHIP?** `the-airport-corrections.md` is **`distributed`, published Jul 2, and its draft still contains an open `[PM VOICE-PASS: …]`**. That looks exactly like a live defect.

✅ **It isn't. Checked the live page rather than reasoning from the draft: `https://pipermorgan.ai/blog/the-airport-corrections` → HTTP 200, `[PM` occurrences: 0.** PM resolved it in the admin UI at voice-pass time. **Nothing leaked to readers.** (`assume-it-was-you.md` also carries one but is `drafted` for Sep 13 — future work, not a leak.)

**What checking the page found instead — a different defect, and a real one.** The draft is a **stale pre-voice-pass copy** that diverged from the published post four weeks ago because **Step-9 archival never ran**. Swept it: **16 of 42** distributed posts since Jun 1 were never moved to `drafts/published/`. Every file exists — which is why **Docs' Jul 30 `draftPath` check passes all 16**. That check asks *does this path resolve*; the unasked question is *does it resolve where the status implies*.

**The cost is legibility, and it's measurable in my own numbers**: my open-bracket scan returned **6** hits, of which **2 were resolved weeks ago** and survive only in unarchived drafts. Without the live-page check I'd have handed PM **6 outstanding questions when the real number is 4** — a 50% overcount on a list whose entire job is telling PM what still needs them.

Memo to Docs (cc PM) — the 16-file `git mv` + 16 `draftPath` rewrites are **theirs**, not mine to do unilaterally; suggested the one-line strengthening of their existing check rather than prescribing the sweep.

**Same instrument-measures-the-wrong-thing family as the morning's finding, sixth instance in two days** — but a new variant worth naming: **the artifact that is technically present and quietly no longer authoritative.** A draft that still exists, still parses, still passes every path check, and stopped being the truth a month ago. Same shape as the caption-column finding sent to Docs on Aug 1.

**The generalizable move**: *I found the near-miss by checking the rendered page instead of the record about it* — the same discipline that caught the post-signal amendment on Ship #053 and the revert risk last week. **Checking the artifact rather than the record about it has now paid out three times in five days.**
