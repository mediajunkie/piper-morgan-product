# Session Log — 2026-08-03 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: `.claude/worktrees/agent-a133d207f6e3da2a9`, branch `worktree-agent-a133d207f6e3da2a9`
**Task**: #1428 — "What can you do?" understates capabilities (rail-descriptions-only) + leaks internal markers (F8)
**Constraint**: commit to worktree branch only; NO push to origin/main (Lead merges after composition audit)

## Plan

Route traced: "what can you do?" → pre-classifier (`DISCOVERY_PATTERNS`) → DISCOVERY/get_capabilities →
floor-routed (`_FLOOR_ROUTED_CATEGORIES`) → `ContextAssembler._gather_identity_context` builds
`context["capabilities"]` from workflow-rail entry descriptions (leaks "(#1124)" etc.) →
`ConversationalFloor._format_domain_context` renders it into the floor prompt.

Fix (per ratified #1433 design §6 step 3 — the answer derives from the ledger):
1. Move CHAT_POINTERS ledger (POINTER/CHAT_INVISIBLE/UNTRACKED_BASELINE/CHAT_POINTERS) from
   `tests/test_architecture_enforcement.py` to new `services/intent_service/chat_pointers.py`;
   test imports it — ratchet gates identically (single source).
2. Module exposes `capability_answer_lines()` deriving user-register lines from POINTER rows
   ('you can ask me: "<utterance>"'), deduped, CHAT_INVISIBLE never claimed, plus the two core
   conversational lines. No internal markers possible by construction (guard test).
3. `_gather_identity_context` uses it instead of rail descriptions; floor renders per-line bullets.
4. D4 respected: no classifier/prompt changes.

## Work

- Failing-first: `tests/unit/services/intent_service/test_capability_answer_1428.py` written before the
  module existed — collection ImportError (module missing), then after module creation the two wiring
  tests failed exactly as intended (`_gather_identity_context` still rail-derived), then green after wiring.
- `services/intent_service/chat_pointers.py` NEW — ledger moved verbatim from
  `tests/test_architecture_enforcement.py` (POINTER, CHAT_INVISIBLE, UNTRACKED_BASELINE, CHAT_POINTERS)
  + `CORE_CAPABILITIES`, `pointer_utterances()`, `capability_answer_lines()` (pure, dependency-free).
- `tests/test_architecture_enforcement.py` — ledger definitions replaced with imports from the module;
  ratchet class untouched; all 8 ratchet tests pass identically.
- `services/intent_service/context_assembler.py` — `_gather_identity_context` capabilities now
  `capability_answer_lines()` (was rail entry descriptions w/ "(#1124)" leakage); provenance source map
  updated to `ChatPointersLedger`.
- `services/intent_service/conversational_floor.py` — capabilities render one bullet per line (comma-join
  mangled quoted example asks).
- `scripts/ratchet_ceilings.json` — `silent_death_core` 227→226: removing the old broad
  `except Exception` around `get_registered_workflows` shrank the silent-death count; ratchet forced the
  lock-in (caught by smoke run, correct behavior).
- `docs/internal/architecture/current/intent-routing-stack.md` — consult-rule same-commit update: ledger
  location + #1428 derivation noted.

## Verification

- New tests: 11/11 pass. Ratchet: 8/8 pass unchanged. Full `tests/unit/services/intent_service/`:
  1919 passed, 1 skipped. Gating smoke (`-m smoke`, ignoring tests/archive which the default addopts
  also ignores): 531 passed, 31 skipped after ceiling lock-in.
- D4 respected: no changes to classifier, prompts.py, or pre_classifier.

## Discovered work

- `CanonicalHandlers._get_dynamic_capabilities` (services/intent_service/canonical_handlers.py:63) has
  NO production callers (only its own tests) and carries the same rail-description defect — dead-ish
  code, candidate for delete-module-safely treatment or ledger-derivation. Reported to Lead in wrap
  report (issue filing left to Lead per composition-audit flow; flagged loudly here).

## Memory & briefing surfaces referenced this session

- **Referenced**: intent-routing-stack.md (mandatory consult — routing chain map), chat-pointers ratchet
  design doc (#1433, the ratified derivation intent), CLAUDE.md ratchet discipline (lower ceiling in same
  commit), test env invocation from task brief.
- **Loaded but not referenced**: memory index (no per-slug files opened).
- **Wanted but not found**: none.

