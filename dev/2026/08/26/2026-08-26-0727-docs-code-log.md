# Docs Session Log — 2026-08-26

**Role**: Documentation Management (Docs) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` ·
website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`

---

## Fire 1 (07:27, the 06:57 slot) — START

**Step 0**: verified 08-25 closed properly — `<!-- DAY-CLOSED: 2026-08-25 -->` present at column 0
in `dev/2026/08/25/2026-08-25-0727-docs-code-log.md`. No self-heal needed.

`CronList`: one job (`b8037424`, STOP re-arm from last night), matches expression. Session log
for today did not yet exist — created per this fire.

Sync: 31 behind / 0 ahead on `origin/main` — fast-forward merged clean (mailbox triage from
several roles, a new mail-send-orphan-move-guard question from Lead to CIO). Inbox: 0 unread.

**Wednesday, no day-of-week trigger** — Weekly Docs Audit is Mondays, Monthly Housekeeping Audit
is first-Monday-of-month. Today's editorial slot is Weekly Ship #057, still `drafted` per
yesterday's carry-forward (hero image fixed by Exec, `f619b5ff7`) — not chasing, PM/Comms-gated.

Heartbeat: START (unconditional write).

Merge-keeper sweep: clean, no `claude/*` branches ahead of main. Standing-items reviewed — nothing
unblocked (PreCompact hook awaits deliberate scoping, frontmatter upgrade awaits PM check-in,
#1486 not due until ~09-01). Website worktree synced clean (2 commits, blog-page + compose-UI
changes, none Docs-touching). Re-checked mail loop after — inbox stayed at zero. (0,0), returning
to idle.

## Fire 2 (10:27, the 09:57 slot) — Ship #057 status noted, PM-gated

**Mail loop, 1 CC memo**: Exec's note to PM (cc Comms, me) that Ship #057's pubDate is today
(Wed) but the draft hasn't had PM's fact-check/voice pass yet — draft still `drafted`, untouched
since Exec's Monday-night hero-image fix. Exec laid out three options (pass+publish today,
pass-today-publish-tomorrow, slip a full week) without pushing one, and flagged the one thing to
avoid is the day ending with no decision either way. **No action for Docs** — this is squarely
PM's call and Comms/Docs only act once PM clears a pass, consistent with the established
Ship-review handoff pattern (PM/Exec-initiated, not chased). Triaged to `read/`
(`388216290`).

Website worktree already current (0/0), no action needed. Nothing else unblocked. (0,0).

Heartbeat: self-suppressed (covered by a commit within 6h).

## PM engaged — "The weekly ship is ready for proofreading and publication"

**Ship #057 "A Checked Claim Has a Shelf Life"** — draft still showed no PM edits on disk when I
started (0 commits since Exec's Monday-night hero-image fix), so ran a full independent audit
rather than assume PM's pass meant nothing left to check.

**Mechanical audit** (Ship calibration applied — caption/footer-tease/word-count N/A by
convention): frontmatter clean, 0 semicolons, 0 "load-bearing"/"cohort", 0 AI-tic constructions,
0 typographic residue, acronym sweep clean.

**Fact-checked the load-bearing claims against primary sources rather than trust the synthesis**:
publications list (5/5 exact match against the live calendar — dates, titles, URLs, "zero slots
missed" confirmed against the actual Sat/Sun/Tue/Wed/Thu cadence); issues-closed=19 (live `gh
issue list --search "closed:2026-08-14..2026-08-20"` returned exactly 19); the v52→v60 deploy arc
(traced through Lead's session logs 08-15/16/18, each version bump located); memory-index headroom
"twelve lines... to over a hundred" (12 on 08-15, 109 on 08-17, matches).

**Found and fixed one real defect**: the watchdog-investigation paragraph claimed the verification
chain "ran through four people" — checked against the 08-18 CIO/HOST/Exec logs and the actual
chain is **three distinct people** (CIO escalates → HOST verifies → Exec finds root cause → HOST
re-verifies, catching a 10-vs-9-days precision error in the process), with HOST checking twice,
not four separate people. Fixed to "three agents, one of them twice" — an irony worth catching
given the piece's own theme is verification discipline. **PM's voice pass (3 admin-UI commits)
landed concurrently** with my fix, producing a merge conflict on the exact same sentence — reset
to PM's voice-passed base and reapplied the correction cleanly rather than resolve it by hand
mid-conflict. Re-ran the full mechanical audit against the final merged version — still clean,
1322 words.

**Published**: dry-run clean (no rendering defects in the markdown→HTML conversion), then real
publish via `publish-post.js` — slug `weekly-ship-057-a-checked-claim-has-a-shelf-life`,
`--category ship` (reuses `piper-ship.webp`, no per-post image prep), `--work-date 2026-08-14`
(window start). Website commit `097c7e8`, hashId `e5ec30dfede7`.

**Calendar updated**: status→published, blogURL/blogPath/canonicalSite set, `draftPath`
repointed to `published/` (matching the established Ship-archival convention). Whole-file scan
clean both times (431 rows). Draft archived via `git mv` to
`docs/public/comms/drafts/published/`. Commits `7c59f5dfe` (archive) + `523e47093` (calendar,
recovered from a failed compound `git add` that silently dropped the calendar files — caught by
checking the commit diff rather than assuming the add succeeded).

**Live-verified, not just committed**: page HTTP 200 (after a 308 redirect-follow), title present,
the corrected "three agents, one of them twice" text confirmed live and the old "four agents"
text confirmed absent, "Issues closed: 19" and "nine consecutive days" both present, hero image
(`piper-ship.webp`) live 200.

Notified Exec (who drafted it and was tracking PM's decision) with the full verification trail,
cc Comms + PM (`cbf6d6a11`).

Heartbeat: WORK (covered by commit volume).

## Fire 3 (13:27, the 12:57 slot) — quiet

Both worktrees synced clean (0/0), inbox empty, merge-keeper sweep clean. Standing items
unchanged since last check — nothing new unblocked. Genuinely quiet fire after the morning's Ship
publish. Heartbeat self-suppressed (covered by recent commits).

## PM engaged — "it should have said 'three agents,' not people"

**The published post itself was never wrong** — re-verified the live page: "three agents, one of
them twice," no trace of "people" anywhere, exactly what landed in commit `bf025bcd4`. The
imprecision was mine, in my own chat recap and in the Exec publish-notice memo, which both said
"three distinct people" when they should have said "agents" — this project's vocabulary
distinguishes the two, and blurring them in a piece that's literally about verification discipline
is worth naming rather than letting slide. Sent a correction to Exec (cc PM) naming the mistake
precisely and confirming the published text was never affected (`ccf3fb631`).

## Fire 4 (16:27, the 15:57 slot) — Ship #057's LinkedIn leg, plus a real skill defect found via Dispatch-PM's flag

**Mail loop, 1 direct memo**: Dispatch-PM's second cross-post report — Ship #057's LinkedIn leg
(Building Piper Morgan newsletter), URL/date/legs-ran table, well-evidenced (heading structure,
link count, in-body illustration+caption survival all checked on the live page). Verified before
applying rather than trust the report: LinkedIn URL live 200. Applied `status`→`distributed`,
`liPubDate`=2026-08-26, `linkedinURL` set, `mediumURL` correctly left empty (Ship theme routes
LinkedIn-only). Whole-file scan clean (431 rows). Commit `ca2e7da84`.

**The real find**: Dispatch-PM flagged that this row's `canonicalSite` had been set to
`distributed` at this morning's blog publish, hours before the LinkedIn leg that just ran — the
same class of inconsistency as #1683, opposite direction. Traced it past "another data point" to
the actual mechanism: `.claude/skills/update-calendar/SKILL.md`'s own Common Updates section
instructed setting `canonicalSite→distributed` at blog-first publish, directly contradicting its
own Field Reference table's definition ("on blog **+** syndicated") two sections above. **I
followed that exact wrong instruction verbatim publishing Ship #057 this morning** — very likely
the same mechanism behind the 2026-07-19 migration's 145-row undercount at scale. Fixed the skill
(moved the `canonicalSite` action to the cross-post step, matching its own field definition;
`5ec3111ca`) — deliberately did NOT touch historical rows, which stay #1683's separate, scoped
remediation. Posted the finding to #1683 as a new comment (not just mail) since it materially
refines the issue's root-cause understanding.

**Replied to Dispatch-PM** via the ratified relay protocol (real recipient in `to:`, cc `exec`,
delivered to `exec/inbox/`) confirming the calendar update and crediting the catch for surfacing
the actual mechanism, not just a symptom. Source memo triaged, MANIFEST regenerated, all sent in
one push-to-ref batch (`e9d001fa2`) — one benign mail-send.sh warning about a MANIFEST path not
registering as changed, self-resolved on the next fast-forward merge, verified final state correct
(inbox genuinely empty).

Heartbeat: WORK (covered by commit volume).

## Fire 5 (19:27, the 18:57 slot) — quiet

Both worktrees synced clean (0/0), inbox empty, merge-keeper sweep clean. Noted a
freeze-watchdog stall alert for arch/cxo/pa (3 roles) — correctly routed to CIO, not mine to act
on. Noted CIO+Lead shipped a `mail-send.sh` fix today for exactly the half-pushed-inbox-move
warning I hit last fire (benign, self-resolved at the time) — no action needed, confirms the read
was right. Nothing unblocked. Heartbeat self-suppressed.

## Fire 6 (22:27, the 21:57 STOP slot) — Exec's error-trace reply, a same-day mail-send.sh finding, day close

**Mail loop, 1 direct memo**: Exec's reflective reply on the Ship #057 fact-check — they traced
their own error past "miscounted" to the actual mechanism: their internal report (08-21) correctly
said "a four-link cross-role chain" (CIO→HOST→Exec→HOST, HOST appearing twice, four *links*), but
rendering that into the Ship draft became "four people" — the unit changed (links → people) while
the count (four) carried over unchanged, and the information that would have caught it (HOST's
name appearing twice in their own prior sentence) was sitting in a source they'd written
themselves five days earlier. Named it as the piece's own theme happening to them directly. Also
owned that they wrote "people" in the draft first — PM caught it in my recap, but the vocabulary
error originated with Exec, not me. Nothing owed back; triaged to `read/`.

**A second same-day mail-send.sh false-positive**, this time investigated and reported rather than
silently worked around: sending a triage batch hit the new (`67dcb5d00`/`ae33827cb`, both landed
earlier today) "half-pushed inbox→read move" warning on `docs/inbox/MANIFEST.md`. Checked before
accepting it — `git show origin/main:...` showed the file's content was already correct
(`(empty)`), byte-identical to my local copy, and a direct resend confirmed "nothing to send,
already matches origin/main." **Not a real strand** — the warning fires when a path has no diff
for a given commit, without checking whether the content already matches. Reported to CIO with the
exact evidence and shape rather than just noting it privately, since they shipped the check the
same day and would want to know it has a false-positive case (`b3589e38f`).

Heartbeat: STOP.

**Stale-cleanup dry-run**: 0 cycle-log files ≥7 days, 0 `.tmp` files ≥1 day. Nothing to remove.

Cron re-armed via delete-then-create: `b8037424`→`8bddb70d`, `CronList`-verified sole job,
registry row updated.

### Day-arc summary — 2026-08-26

The day's center of gravity was Weekly Ship #057, published, syndicated, and — after PM's own
catch — corrected twice over on a single sharp point. The morning: full independent fact-check
against primary sources caught a real defect (a verification-chain miscount) that PM's concurrent
voice pass hadn't touched, resolved cleanly through a merge conflict on the exact same sentence.
The early afternoon: PM caught a subtler error in my own recap of that fix — I'd used "people"
where the actual text (and Exec's original source material) correctly said "agents," a vocabulary
distinction this project treats as load-bearing rather than stylistic. The late afternoon: Dispatch-
PM's LinkedIn report on that same post surfaced a `canonicalSite` timing inconsistency that traced,
on inspection, to a genuine self-contradiction inside `update-calendar`'s own skill file — one I'd
followed verbatim that same morning, and very plausibly the same mechanism behind #1683's 145-row
undercount from July. Fixed the skill rather than just the one row. The evening closed the loop
twice more: Exec traced their own share of the "people" error to its precise origin (a unit change
during report-to-draft translation, not a simple miscount) rather than accept "sloppy" as the
explanation, and a second mail-send.sh warning today turned out to be a false positive, checked and
reported rather than dismissed or blindly trusted.

The throughline across all four threads, named explicitly by more than one participant today: a
claim that was correct when written can go stale, change units, or need re-checking by the time
someone else relies on it — literally Ship #057's own subject, playing out in real time in the
process of publishing and correcting it.

**Sign-off checklist**:
```
git status --short                                    → clean (both worktrees)
git fetch origin main && git rev-parse --verify -q origin/main   → resolves
git log --oneline origin/main..HEAD                    → empty (product AND website worktrees)
```
Nothing stranded. All work today reached `origin/main` same-fire, not batched to sign-off.

## Memory & briefing surfaces referenced this session

**Referenced**: `update-calendar` skill (read in full before editing, both to apply the LinkedIn
update and to diagnose + fix its own internal contradiction); `close-issue-properly` skill's
evidence-first discipline (informed the #1683 comment); the `draft-weekly-ship` skill's Ship
calibration table (template-audit checks 1/6/7/12 as N/A-by-convention); CLAUDE.md's mail-vs-GH-
comment distinction (governed posting the `canonicalSite` finding to #1683 directly, not just in
mail); "never guess at facts" (governed every fact-check pass today — publications, issues-closed,
deploy arc, memory-index headroom, the watchdog-chain count, the LinkedIn URL, and the mail-send.sh
false-positive all independently verified before acting or reporting).

**Loaded but not referenced**: cross-pollination brief; `docs-standing-items.md`'s dormant
frontmatter-upgrade item (checked, still not this fire's priority).

**Wanted but not found**: nothing material. Today's dominant lesson wasn't a missing surface — it
was that verification has to happen at the point of restatement, not just the point of original
research, which is the exact discipline every fact-check pass today (mine and Exec's) had to
practice rather than just read about.

<!-- DAY-CLOSED: 2026-08-26 -->
