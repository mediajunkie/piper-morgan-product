# Docs Session Log — 2026-08-19

**Role**: Documentation Management (Docs) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` ·
website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`

---

## Fire 1 (07:11, the 06:57 slot) — START

`CronList`: one job (`2967db0e`), matches expression. Sync clean. Inbox: 0 unread. Yesterday's
log carries `DAY-CLOSED: 2026-08-18`.

Wednesday triggers: none Docs-owned. Calendar: **Weekly Ship #056 "Fundamentals First"** has
pubDate today — still `status=drafted`, no publish-ready signal yet. Not chasing, per standing
discipline. "The Dead Code That Wasn't" is queued for tomorrow (08-20).

**Cron watch**: `2967db0e` was created ~08-14 with a fresh 7-day window, so it's due to expire
~08-21 — 2 days out. Not rotating yet (no need to rotate early), but flagging so it doesn't lapse
silently; will rotate proactively at a STOP fire in the next day or two, same pattern as the last
rotation (ahead of expiry, not waiting for it).

## PM engaged — "Today's weekly ship is ready for proofreading" — Ship #056 proofread + publish

PM's signal came in with the calendar row's own notes field still ending "Awaiting PM fact-check +
voice pass" — no PUBLISH-READY language, and git history showed only the original 08-15 draft
commit with no subsequent edit-pass commit. Rather than either blindly trusting the calendar note
over PM's live statement or blindly trusting PM's statement over the note, ran a full first-read
proofread pass myself and treated PM's message as authoritative for readiness.

**Template-audit** (Ship calibration applied — #1/#6/#12/#14 recalibrated per the skill's table):
frontmatter clean (caption empty = N/A by Ship convention), title/dateline/headings clean, zero
placeholders, footer-tease N/A for Ships (confirmed against #054/#055 precedent — no tease chain,
no closing reader-question either, matching precedent), zero semicolons, zero AI-tics, word count
1568→1571 (within Ship norm 1,100–1,900), acronym sweep clean, zero typographic residue.

**2 real defects found and fixed**:
- Check #10 ("no cohort"): 1 instance at the learning-pattern's Why-it-matters line. Fixed →
  "team."
- Check #14 (no bare issue numbers in narrative prose): `#1536` cited directly in the Product &
  experience section. Checked precedent — 0 of the last 6 published Ships (#050–#055) cite a real
  GH issue number this way; `#NNN` is reserved for Ship self-references only. The underlying claim
  was accurate (see fact-check below) but the citation style broke an established convention.
  Fixed → "The alpha-tester coldstart fix" (role-functional description, matching how every other
  bullet in the piece cites its subject).

**Fact-checked 5 load-bearing claims against primary session logs** (not just trusting the
drafting notes' own verification chain) — all confirmed accurate:
- `#1536`: Lead's 08-10 log (`2026-08-10-1549-prog-1536-log.md`) — merged, 29/29 new tests + 2510
  total passed. CXO's 08-12 log — independent conformance review posted against the §7a gate,
  confirming "second reviewer" framing.
- CI dark-workflows: Lead's 08-12 log — "First run: 7 flagged of 18 checked... not the 2 known,"
  CI itself "NO successful run in retained history." Exact match to the draft's "seven... zero
  recorded successes."
- Issues closed = 53: live `gh api search/issues` query for the exact window, exact match.
- Deploy v41: Lead's 08-08 log confirms `v41 = afternoon batch` (redeploy go given), real tag near
  window start.
- #1510 trust-fork: Lead's 08-13 log — "the #1510 arc COMPLETE same-day (ruling → rail → 3
  consumers)," matches "tested by three consumers, in one day" exactly.

**Published**: `weekly-ship-056-fundamentals-first`, hashId `245f61c02146`, workDate 2026-08-07,
pubDate 2026-08-19. Dry-run clean before real publish. Website commit `a44abc4`. Calendar updated
(status→published, blogURL/blogPath, canonicalSite→distributed), whole-file scan clean (430 rows),
view rebuilt. Draft archived to `drafts/published/`. Product commits `0d03fe6a` (proofread fix),
`a7b69c0` (archive), `e8d153d8` (calendar metadata — caught and fixed a dropped `git add` from a
bad pathspec in the same push). **Live-content-verified**: HTTP 200 (following a 308 redirect),
title confirmed, both fixes confirmed present in the served HTML ("alpha-tester coldstart fix"
present, "cohort"/"#1536" absent) — not just source-file confirmation.

**PM follow-up**: confirmed the archived local markdown matches the live published content exactly
(diffed the archived draft against the actual `blog-content.json` entry, not assumed from having
edited the source file). Notified Exec of the 2 corrections made to their draft — memo sent
`mailboxes/exec/inbox/2026-08-19-docs-ship-056-corrections.md` (`02b5c5a16`), with the reasoning
and precedent check, framed as a heads-up for future Ship drafts rather than a complaint. PM taking
LinkedIn syndication directly — no calendar action from me until the URL lands.

## Fire (10:11, the 09:57 slot, arrived late) — cron rotation + real 2-day omnibus gap found+closed

**Cron rotation** (Step 1, 48h-proactive-expiry rule): `2967db0e` was created 08-14, fresh window
to ~08-21 — within 48h of expiry at this fire. Rotated proactively rather than waiting for STOP:
`2967db0e` → `c707c6a0`, `CronList`-verified sole job each time. Registry row updated with the
transition + reason (`d6bf6ff4f`).

**Standing-items + carry-forward review** surfaced a real gap: omnibus logs exist through 08-16 but
**08-17 and 08-18 had none** — 27 session logs (12 + 16, including 5 prog-agent logs on 08-18)
sitting unsynthesized, and the activity-log CSV had zero rows for either date. This is owed,
unblocked Docs work (daily omnibus cadence), so per the spine's "drain it all" — deleted the cron
(Rule 1, substantive multi-step build) and dispatched two subagents sequentially (08-17 then
08-18, to avoid the CSV-write race the earlier 3-day backfill was careful about).

- **08-17 omnibus**: `docs/omnibus-logs/2026-08-17-omnibus-log.md`, 250 lines, HIGH-COMPLEXITY:
  EXECUTION (11 role logs, cohort's quietest Monday since the reboot — 7 of 11 roles fully idle
  all six fires; three independent non-interacting content threads rather than PM-mediated
  coordination). 11 activity-log rows appended (2047→2058). Commits `1c55741c2` + `6a33f5dda`.
  **Discrepancy preserved, not resolved**: Exec's log dates Lead's `issue_intelligence.py`
  disposal (#1633) as "today" (08-17); git history + `decisions.log` show it actually landed
  2026-08-16 12:47 PT (`8c5dbb322`) — a one-day slip in Exec's account, named explicitly in the
  omnibus rather than silently corrected.
- **08-18 omnibus**: `docs/omnibus-logs/2026-08-18-omnibus-log.md`, 456 lines, HIGH-COMPLEXITY:
  COORDINATION (16 logs incl. 5 prog; the day's real shape was agent-to-agent interaction — the
  CIO→HOST→Exec→Docs→HOST/CIO heartbeat-gap verification chain, Lead's evening brief answered
  independently by both PA and CXO, Comms's mailbox-regression incident). 16 activity-log rows
  appended (2058→2074). Commits `e9e6634ae` + `47876c3ca`. **Discrepancy preserved**: Web's log
  attributes a Medium cross-post gap-catch to Comms/Docs's own session records, but neither log
  actually narrates it — the real record is a direct PM commit (`bf90a2804`), not either agent's
  session flow.

Both subagents ran the full skill (methodology-20 read fresh, Step 2.5/2.6 cross-reference gates
run for real, not skipped) rather than a shortcut synthesis. Re-armed cron at idle:
`c707c6a0` → `09681b21`, `CronList`-verified sole job. Registry updated (`c1cf5675b`).

Heartbeat: WORK (`scripts/duty-cycle-heartbeat.sh docs WORK --if-quiet`).

## Fire (13:00, the 12:57 slot) — quiet mail/task loop; standing-items full refresh

Sync clean, inbox empty (0,0), no day-of-week trigger for Wednesday. Cron `09681b21` correct,
sole job, not touched.

**Real (if low-priority) owed work found, not manufactured**: `docs-standing-items.md` hadn't been
refreshed since 2026-05-27 despite carry-forward moving on substantially. Checked live via `gh
issue view` before touching anything — 6 of its cited issues (#1058, #974, #972, #1127, #1128,
#1206) are all CLOSED; spot-checked 2 more claims (website#31 closed, ADR/Pattern frontmatter
still genuinely absent) rather than assuming either way. Full rewrite: dropped resolved sections,
kept the 2 items that checked out as still genuinely open (frontmatter upgrade — dormant 2.5
months, flagged for a direct PM check rather than silent resumption; the daily cycle-ops list),
consolidated "watch surfaces" against current carry-forward state, added a "Recently completed"
section reflecting this week's actual work instead of June's. Committed `7952f66cd`.

Heartbeat: WORK.

## Fire (19:00, the 18:57 slot) — mail: blocking hero-image 404 on Ship #056 + #054, fixed

**Mail loop**: 1 direct memo (a general-purpose session, CC'd to PM, reporting a live 404 blocking
PM's Ship #056 LinkedIn cross-post — LinkedIn fetches referenced images at post time, so a broken
hero image posts silently dropped or broken); 0 cc.

The report was unusually well-evidenced (live curl output, `git log --all --diff-filter=ADR`
trace, root-cause to the frontmatter-copy convention) but per this week's standing discipline,
independently re-verified rather than acting on the report alone: live HTTP confirmed both the
404 (`alpha-launches-before-opening.png`) and the correct live asset (`alpha-launches.webp`, 200);
`git log --all` confirmed the source `.png` was never committed to the website repo at any path,
and the `before-opening` string entered the repo in exactly the Ship #056 publish commit
(`a44abc424`) — matches the report exactly.

**Root cause**: the Ship hero-teaser convention hand-copies another post's calendar/frontmatter
image name, which is the *pre-conversion source filename* — `publish-post.js` actually deploys
images as `${slug}.webp`, a different name the frontmatter never reflects.

**Scanned the whole `blog-content.json` corpus myself** for the same defect class (not just
trusting the report's stated count) — confirmed exactly 2 hits, matching the report:
- **Ship #056** — `alpha-launches-before-opening.png` (404) → `alpha-launches.webp` (200).
- **Ship #054 "Clear Is Not a Measurement"** — `reconnects-keystone-keystone-arch.png` (404) →
  `reconnects-keystone.webp` (200). **Live-broken since #054 published 2026-08-05 — 14 days
  uncaught**, found only by scanning for the class rather than the single reported instance.

Fixed both via direct `blog-content.json` edit (hashIds `245f61c02146` + `ff0c023840c8`), website
commit `8801bfb`. Re-scanned after the fix: zero remaining non-`.webp` `blog-images` references in
the corpus. **Live-verified both**, not just the commit: asset HTTP 200 for both `.webp` files,
served page HTML for both Ships confirmed referencing the correct name and the old broken name
gone. Calendar rows for both Ships noted with the post-publish fix (`793b1065c`), whole-file scan
clean (430 rows).

**Discovered-work issue filed**: `piper-morgan-website#33` — the report's own suggestion (a
mechanical link-check guard against this whole class, since prose discipline has now missed it
twice) is a real, worth-tracking proposal, not something to silently drop after the immediate fix.

**Replied to PM** (CC'd on the original, and the party actually blocked) confirming both fixes
live-verified and that the LinkedIn cross-post is clear to proceed —
`mailboxes/xian (ceo)/inbox/2026-08-19-docs-ship-056-054-hero-image-404-fixed.md` (`10f4e12e6`,
push-to-ref, source memo moved to read/, own MANIFEST regenerated).

Heartbeat: WORK.

## Fire (22:07, the 21:57 slot) — STOP: mail drain + day close

**Mail loop, 2 direct memos**, both fully actioned (no reply required — direct-memo rule is
substantive response OR explicit action, both memos got the explicit-action half):

- **Exec's ack** on the Ship #056 corrections memo: independently re-verified the fix in the
  archived draft (not just trusted my memo), confirming "cohort" gone and `#1536` now reads "The
  alpha-tester coldstart fix." Adopting both checks into their own pre-handoff sweep for #057.
  No action needed from me — triaged to read/.
- **LinkedIn syndication report** (general-purpose session, PM's cross-post): requested a
  calendar-row update for Ship #056 (linkedinURL + liPubDate + status). Verified the report's
  stated current state against the live row before editing (matched exactly — clean first write,
  no conflict). Applied: `status`→`distributed`, `linkedinURL` set, `liPubDate`=2026-08-19,
  `mediumURL` correctly left empty (Ship-theme = LinkedIn-only, no Medium leg, per routing rules).
  Whole-file scan clean (430 rows). Committed `6de2ca67f` (rebased once on a concurrent push, then
  landed `15d183b5c`). The report also independently re-confirmed the hero-image 404 fix from
  Fire 5 is deployed and closed (their own separate verification, not just trusting my earlier
  fix) — no action needed, noted for completeness.

Both memos triaged to read/, MANIFEST regenerated, sent via push-to-ref (`5f12abcb3`). Re-checked
mail loop after — inbox stayed at zero.

**Stale-cleanup dry-run**: 0 cycle-log files ≥7 days, 0 `.tmp` files ≥1 day in `dev/active/`.
Nothing to remove this STOP.

Cron `09681b21` re-armed via delete-then-create at day-close (see below); heartbeat: STOP.

### Day-arc summary — 2026-08-19

Opened on a genuine cron-expiry watch (`2967db0e` due ~08-21) that turned into the day's first
real action: rotated proactively per the 48h rule rather than waiting for STOP. PM then engaged
directly with "Today's weekly ship is ready for proofreading" — the calendar's own note still
read "Awaiting PM fact-check + voice pass" with no git evidence of an edit pass, so ran a full
first-read audit rather than either trusting the note over PM or PM over the note. Found 2 real
defects (a bare GitHub issue number breaking established Ship convention; one "cohort"), fixed
both, fact-checked 5 load-bearing claims against primary session logs (all accurate), published,
live-verified, and — per PM's own follow-up ask — confirmed the local markdown matched the live
content and notified Exec of the corrections directly.

A quiet-looking WORK fire surfaced a real, self-found gap rather than a flagged one: the omnibus
cadence had slipped for 08-17 and 08-18, 27 session logs unsynthesized. Closed same-day via two
sequential subagent dispatches, each running the full skill for real and each preserving a genuine
cross-role discrepancy rather than resolving it silently. A later quiet fire found
`docs-standing-items.md` itself had gone stale since 2026-05-27 — verified live that 6 of its
cited issues were already closed before rewriting it, rather than assuming staleness meant
wrongness in either direction.

The day's most consequential single find came from someone else's report: a general-purpose
session working PM's LinkedIn cross-post hit a live 404 blocking the post. The report was
unusually well-evidenced, and it still got independently re-verified rather than acted on
directly — live HTTP + git history, exact match. Scanning the whole post corpus for the same class
(rather than stopping at the one reported instance) caught a second, older casualty: Ship #054 had
been broken the same way for 14 days, unnoticed. Both fixed, live-verified, a discovered-work issue
filed for the systemic guard, PM notified directly since they were the blocked party. The day
closed with the loop closing itself: Exec independently re-verified the corrections rather than
trusting the memo, and the cross-post session independently re-confirmed the 404 fix rather than
trusting mine — the same discipline running on both sides of every handoff today, not just mine.

**Cron discipline this day**: 3 rotations, each logged with reason and registry-updated —
`2967db0e`→`c707c6a0` (48h-proactive-expiry), `c707c6a0`→`09681b21` (Rule-1 drain/re-arm for the
omnibus backfill), and the STOP re-arm below. No duplicate jobs at any point, `CronList`-verified
each time.

### Sign-off checklist

```
git status --short   → clean (after this commit)
git fetch origin main && git rev-parse --verify -q origin/main   → resolves
git log --oneline origin/main..HEAD   → empty (after this commit lands)
```
Nothing stranded. All work today reached `origin/main` same-fire, not batched to sign-off.

## Memory & briefing surfaces referenced this session

**Referenced**: `template-audit` skill's Ship calibration table (checks #1/#6/#12/#14) — applied
correctly, caught 2 real defects the calibration doesn't cover (#10 "cohort", #14 bare issue
numbers, both still live checks on Ships); `create-omnibus` skill + methodology-20, read fresh by
both backfill subagents rather than from memory; `update-calendar` skill's csv-module-by-name +
whole-file-scan discipline, applied on every calendar edit today (4 separate edits, zero drift);
CLAUDE.md's "never guess at facts" — governed the Ship #056 readiness call, the hero-image report
verification, and the standing-items refresh equally; the duty-cycle-tick skill's 48h-proactive-
expiry rule (new this week) and Rule-1 delete/re-arm, both exercised for real today.

**Loaded but not referenced**: cross-pollination brief, docs-standing-items.md's now-removed
Agent-360 section (correctly dropped, not referenced because it was genuinely closed out).

**Wanted but not found**: nothing material. The omnibus-cadence slip (twice in one week now,
08-14/15/16 then 08-17/18) is worth watching for a third instance — flagged in standing-items,
not yet a pattern worth escalating.

<!-- DAY-CLOSED: 2026-08-19 -->
