# Session Log — 2026-08-19 15:35 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1666 — delete_todo has no WorkflowEntry, bypasses the #1124 consent-gate rail, deletes immediately with no confirm
**Worktree**: `.claude/worktrees/agent-acdd21b6c0146a761` (isolated; do NOT push — Lead merges after review)

## Plan (after investigation)

1. `destructive_confirm.py`: delete-todo family constant + async `build_todo_delete_confirmation`
   (title-binding confirm builder — positional target means an async list read is the only honest
   "confirm WHAT" ask; clear-family shapes pass through so #1605 keeps first claim).
2. `intent_service.py` rail gate call site: branch delete-todo family to the async builder;
   REMOVE the legacy `elif mapped_action == "delete_todo"` branch.
3. `workflow_entries.py`: `run_delete_todo_workflow` entry point (auth → clear-family seam →
   handle_delete_todo) + `delete_todo_entry` (DESTRUCTIVE, PRIVATE, action_triggered) registered
   under delete_todo / remove_todo / cancel_todo (the ActionMapper alias family).
4. `todo_handlers.handle_delete_todo`: honor gate-time resolved binding on the confirmed yes
   (delete the row the user confirmed, never a positional re-resolve).
5. Tests: new `test_delete_todo_confirm_1666.py` (unit e2e), new integration
   `test_todo_delete_chat_path_1666.py` (real Postgres, yes→gone / no→present);
   update 1190 denominator test + 1605 explicit-delete test to the new contract.
6. Doc: `intent-routing-stack.md` same-commit.

## Findings so far

- **Ratchet delta is 0→0**: `MAX_DISPATCH_SITES` is already 0; its regex counts
  `if/elif intent.action in [` sites only. The delete_todo branch is
  `elif mapped_action == "delete_todo"` inside `_handle_execution_intent` — never a counted site.
  Removing it shrinks the forward-guard token set (shrink-tolerant), lowers nothing.
- **Alias family**: ActionMapper maps delete_todo / remove_todo / cancel_todo → delete_todo.
  No pre-classifier pattern emits delete_todo (LLM lane). Slack path only uses the mapping for
  principal checks. normalize_action prefixes don't produce it.
- **Denominator test** `test_destructive_tier_scope_with_denominator` (1190) pins the exact
  destructive rail-key set — must be updated same commit.
- **1605 test** `test_explicit_delete_passes_through` pins the OLD immediate-delete contract —
  must be updated to the two-turn confirm contract (that change IS #1666's point).

## Work record

- **Shipped** (one commit, this worktree, not pushed — Lead merges after review):
  - `destructive_confirm.py`: `_DELETE_TODO_FAMILY` + `is_delete_todo_action` +
    `RESOLVED_TODO_CONTEXT_KEY` + async `build_todo_delete_confirmation` (three-legged
    `TodoDeleteGate`: confirm / passthrough / honest-error — clear-family shapes pass
    through so #1605 keeps first claim; question binds real todo text; resolved row
    stashed into intent context; lookup failure = honest no-op, never number-only
    confirm, never ungated passthrough).
  - `intent_service.py`: rail gate branches the delete-todo family to the async
    builder; legacy `elif mapped_action == "delete_todo"` REMOVED (comment tombstone
    explains the migration; else-branch honest-decline is the wiring-gap default).
  - `workflow_entries.py`: `run_delete_todo_workflow` (auth → #1605 seam DESTRUCTIVE →
    handle_delete_todo) + `delete_todo_entry` (DESTRUCTIVE, PRIVATE, action_triggered)
    registered under delete_todo / remove_todo / cancel_todo.
  - `todo_handlers.handle_delete_todo`: honors gate-time resolved binding on the
    confirmed yes (deletes the row the user confirmed; requires BOTH
    `delete_todo_resolved` and the `destructive_confirmed` marker).
  - Tests: NEW `test_delete_todo_confirm_1666.py` (26 tests: registry pins, builder
    unit incl. error leg, e2e confirm/yes/no/aside/alias/category-independence/
    list-shift WHAT-binding, boundary both ways, read-only legs); NEW integration
    `test_todo_delete_chat_path_1666.py` (real Postgres through real process_intent:
    row gone on yes, present on no); 1190 denominator updated (+3 delete keys);
    1605 explicit-delete test updated to the two-turn contract.
  - Docs: `intent-routing-stack.md` (surface-3 #1190 tier, known-boundary paragraph,
    1605 seam location) + `unwired_writes.py` docstring.

## Evidence

- New 1666 unit suite: `26 passed in 3.84s`
- Adjacent suites (1666+1605+1190+1650+1665+todo_handlers+action_mapper): `244 passed`
- Ratchets + capability suites (architecture_enforcement, routing_vocabulary_1283,
  capability_legibility_1509, floor_capability_honesty_1517, consent_gate_1509):
  `205 passed`
- FULL intent_service sweep (tests/unit/services/intent_service + tests/intent_service):
  `3345 passed, 3 deselected in 147.39s`
- Integration (1666 + 1603 + persistence + cross_user, real Postgres 5433): `39 passed`

## Findings / discovered issues

- **Ratchet delta 0→0** (task step 2 assumption corrected): `MAX_DISPATCH_SITES` was
  already 0 and its regex counts `if/elif intent.action in [` only; the delete_todo
  branch was `elif mapped_action ==` inside `_handle_execution_intent` — never a
  counted site. Elif still removed as instructed; forward-guard (1283) and elif-token
  derivations are shrink-tolerant and stay green. Nothing to lower.
- **No new tracking issues filed**: no test failures, bugs, or missing features
  discovered beyond the task's own scope. The ADR-075 first-response personalization
  notice appending to confirm asks for brand-new users (seen in the integration test)
  is existing designed behavior (FIRST_RESPONSE_PERSONALIZATION_NOTICE), not a defect.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-stripping for pytest, evidence discipline,
  intent-routing-stack same-commit rule); `intent-routing-stack.md` (4-surface chain,
  mandatory pre-read); #1605/#1650/#1190/#1665 test suites as contract sources;
  `feedback_verify_at_the_right_layer` (drove the real-Postgres integration test);
  `feedback_split_related_issues_for_testing` idiom via the 1603 test model.
- **Loaded but not referenced**: memory index at large; briefing docs (not consulted —
  task fully specified by Lead's delegation + issue #1666).
- **Wanted but not found**: none — the #1560/#1411 migration comments in
  workflow_entries.py were sufficient precedent documentation.
