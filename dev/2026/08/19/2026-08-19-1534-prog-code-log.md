# Session Log — 2026-08-19 15:34 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Task**: #1595 Inversion Phase 2.2 flip-1 — live inversion routing behind a
default-empty per-category flag set (zero-armed-state READ categories only,
per the #1663 contract addendum in `dev/active/inversion-phase2-kickoff-plan.md`).
**Model**: Fable 5 (claude-fable-5)
**Worktree**: isolated agent worktree off `claude/lead-cycle`; commit, no push (Lead merges after review).

## Plan

- New module `services/intent_service/inversion_live.py` — the ONE sanctioned
  consumer of `inversion_router` outside the shadow observer (named-allowlist
  ratchet amendment in `TestInversionShadowNoExecutionBoundary`).
- Consult seam: `_process_intent_internal`, after ethics/KG, immediately before
  the `classify_multiple` block — replaces the classifier consult only when the
  constrained call emits an in-set, READ-effect, rail-dispatchable operation at
  confidence ≥ threshold on an UNARMED turn; every other outcome falls through
  to the legacy chain unchanged.
- Flags (call-time env reads, the `shadow_enabled()` idiom):
  `PIPER_INVERSION_LIVE_CATEGORIES` (default-empty),
  `PIPER_INVERSION_LIVE_MIN_CONFIDENCE` (default 0.8).
- Telemetry: one `inversion_live_decision` structured line per consult; legacy
  comparison = deterministic pre-classifier only (no extra LLM).
- Tests: `tests/unit/services/intent_service/test_inversion_live_1595.py`
  (default-empty pin, live-dispatch same-handler e2e, armed guard, fallback
  reasons, error path, telemetry, threshold override) + ratchet amendment.
- `intent-routing-stack.md` updated same-commit.

## Work log

- 15:34 — session start; read kickoff plan §2.2 + addendum, inversion_router,
  inversion_shadow, session_snapshot, snapshot_assembly, intent-routing-stack,
  ratchet tests, dispatch seam in intent_service.py (pop seam 1024, classify
  1618, rail 2061). Mapped operation→category via ACTION_REGISTRY (the scorer's
  `_op_category_map` idiom); confirmed QUERY category contains WRITE/DESTRUCTIVE
  rail entries → effect==READ guard is load-bearing, not belt.
- 15:45 — shipped the flip:
  - NEW `services/intent_service/inversion_live.py` — consult module
    (flag resolution mirroring `shadow_enabled()` env-at-call-time idiom:
    `PIPER_INVERSION_LIVE_CATEGORIES` default-empty,
    `PIPER_INVERSION_LIVE_MIN_CONFIDENCE` default 0.8; armed guard both
    halves; four dispatch conditions incl. declared-READ rail entry;
    one `inversion_live_decision` telemetry line per consult; deterministic
    pre-classifier divergence comparison).
  - `services/intent/intent_service.py` — consult inserted after ethics/KG,
    immediately before `classify_multiple`; the classification block wrapped
    under `if intent is None:` (pure re-indent, no logic change); belt-catch
    logs `inversion_live_consult_failed` loudly and falls to legacy. No
    router tokens in this file (boundary preserved); no new
    `if/elif intent.action in [` site (dispatch ratchet still 0).
  - `tests/test_architecture_enforcement.py` — TestInversionShadowNoExecutionBoundary
    relaxed by NAMED allowlist entry (`inversion_live.py`) with reasoning in
    the comment + docstring; added m-44 vacuity asserts (live lane must
    actually reference the router; dispatch layer must actually call the
    consult).
  - NEW `tests/unit/services/intent_service/test_inversion_live_1595.py` —
    28 tests: default-empty zero-work + e2e, same-handler-result e2e
    (classifier consult explosive in the flip leg), armed guards incl.
    armed+in-set e2e, all fallthrough reasons (incl. WRITE-under-QUERY pin),
    error-path e2e, divergence telemetry, threshold override.
  - `docs/internal/architecture/current/intent-routing-stack.md` — flip-1
    section added same-commit (consult placement, conditions, telemetry,
    coverage notes).
- Evidence: flip suite 28 passed; neighbors (snapshot/router/vocab/1650)
  121 passed; architecture + completion ratchets 46 passed; silent-death
  count 198 == ceiling (unchanged); full intent_service unit suite exit 0
  (first run), summary re-run pending.

- ~16:1x — **Session resumed** after a server-side API 500 mid-task (Lead
  relayed). Worktree state intact (no commit lost); background runs recovered:
  full intent_service unit suite **3317 passed, 3 deselected in 150s**; smoke
  exit 0 (summary re-captured below). Per Lead's resume note: re-ran BOTH
  ratchet files together (completion + architecture) and re-checked my new
  code for unannotated broad excepts.
- Final evidence battery:
  - `test_inversion_live_1595.py`: **28 passed**
  - neighbors (snapshot_assembly / inversion_router / routing_vocabulary /
    crisp_accept): **121 passed, 1 deselected**
  - `tests/test_completion_ratchets.py` + `tests/test_architecture_enforcement.py`
    (BOTH ratchet files, one run): **46 passed**
  - full `tests/unit/services/intent_service/`: **3317 passed, 3 deselected**
  - CI gating subset `pytest -m smoke`: **542 passed, 1 skipped, 12856 deselected**
  - silent-death: **198 unannotated == ceiling 198** (unchanged; my three new
    broad excepts each carry `# silent-ok:` + a loud structured log)
- **#1666 interaction check (looked, not assumed)**: this worktree PREDATES
  #1666's landing — `delete_todo` here is still the legacy
  `elif mapped_action == "delete_todo"` branch (intent_service.py:8232, inside
  `_handle_execution_intent`) with no rail entry. No interaction with flip-1:
  (a) textually, my edits are the ~1600–1760 classification region + the
  post-turn shadow block; #1666's surface is the EXECUTION chain at ~8232 —
  disjoint regions, clean merge expected; (b) semantically, flip-1 can never
  dispatch delete_todo in EITHER world — pre-#1666 it's not a rail key
  (consult reason `not_rail_dispatchable`), post-#1666 it's rail-registered
  DESTRUCTIVE (consult reason `not_read_effect`); both fall to the legacy
  chain where #1666's gate does its job. The `elif mapped_action ==` shape is
  also not what the dispatch-site ratchet counts (`if/elif intent.action in [`),
  so MAX_DISPATCH_SITES stays 0 on both sides.

## Discovered issues (for Lead to triage/file)

1. **Flip coverage gap — rail READ ops with no ACTION_REGISTRY category**:
   `show_standup`, `list_projects`, `list_archived_projects`, `search_documents`,
   the analysis family etc. have no registry category, so no category flag can
   address them (consult falls through `no_registry_category`). Registry-only
   canonicals (`get_identity`, `get_project_status`) fall through
   `not_rail_dispatchable`. Flipping the STATUS/IDENTITY classes named in the
   kickoff plan's wave 1 needs either rail registrations for those canonicals
   or a reviewed category source for rail-only ops — flip-2 prep work.
2. **Shadow double-call under live dispatch**: when the flip dispatches AND
   `PIPER_INVERSION_SHADOW` is on, the post-turn shadow re-routes the same
   utterance (second Haiku call). Harmless (sampled, default OFF) but worth a
   skip-if-live-dispatched guard when the shadow flag is next touched.

## Memory & briefing surfaces referenced this session

- **Referenced**: `dev/active/inversion-phase2-kickoff-plan.md` (binding scope
  + addendum); `intent-routing-stack.md` (chain map, consult rule);
  CLAUDE.md §Intent dispatch (no-new-elif), §STOP conditions;
  `test_snapshot_assembly_1595.py` (#1621 test idioms);
  `scripts/inversion_phase1_shadow_score.py` (`_op_category_map` idiom);
  m-44 (vacuity guards in the ratchet amendment).
- **Loaded but not referenced**: memory index entries beyond the above;
  mailbox/briefing surfaces (subagent task, not a role session).
- **Wanted but not found**: a declared effect/category surface for
  ACTION_REGISTRY-only canonicals (had to derive the coverage gap by dumping
  the grammar against the rail).
