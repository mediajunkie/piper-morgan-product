# Session Log — 2026-08-16 22:51 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: GitHub #1567 — repo clarification dead-ends: the "repository not
specified" refusal can't consume the answer, and natural phrasing
("in the test-Piper-Morgan repository") is never extracted.
**Branch**: isolated agent worktree (commit here; Lead merges — do NOT push)

## Work

- 22:35 Pre-read: `docs/internal/architecture/current/intent-routing-stack.md`
  (mandatory), `gh issue view 1567`, the #846/#1190 carrier idioms
  (`destructive_confirm.py`, `reminder_clear.py`, `drafted_issue.py`), the
  #1411 update slot-fill + `_resolve_default_repository`, `repo_resolver.py`
  (#1042/#1590), `search_user_repositories` (#1327).
- 22:40 Shipped `services/intent_service/repo_clarification.py`:
  - `extract_natural_repo_name` (in-the-X-repository / repo-called-X, quoted,
    stoplisted so "in my default repository" stays the #1411 resolver's);
  - `extract_repo_answer` (owner/name, URL, natural, `use X`/`it's X`,
    quoted-alone, bare token — bare token suppressed when the turn already
    reads as accept/decline);
  - `resolve_repo_name` (default-name match first, then the user's actual
    repos, case-insensitive; resolved/ambiguous/not_found/unavailable — the
    unavailable copy never claims a search that didn't run);
  - `build_repo_question_offer` (kind `issue_repo_question` on the #1190
    action-agnostic carrier) + `handle_repo_question_turn` at the pop seam
    (binds answers incl. same-operation restatements, re-dispatches the
    ORIGINAL intent via `run_confirm_pending_action_workflow`; different
    issue numbers / off-family anchored imperatives fall through → off-intent
    routes normally; failures re-arm — retry never loses the operation);
  - `strip_trailing_repo_clause` (titles never swallow the routing clause).
- Wired `_handle_update_issue`: natural extraction on the ORIGINAL ask
  (short-circuits the question); named-but-unresolved asks (never silently
  falls to the default); no-repo-at-all arms the carrier (session) or keeps
  the honest teaching refusal (no session). `_ask_for_repository` helper;
  closed "use your default X?" form when a name lookup fails and a default
  exists.
- Wired `_handle_close_issue_query`: session threading (via
  `run_close_issue_workflow`), explicit/natural repo honoring (threads
  owner/repo_name; unnamed closes keep the exact old router-internal call
  shape), and the router's "no repo could be resolved" RuntimeError becomes
  the bindable question instead of a generic error.
- Belt fixes in passing: `_apply_soft_offer` clobber-guard now lists
  `issue_repo_question_pending` AND the previously-missing
  `unmapped_field_clarification_pending` (#1411's ask could be clobbered by
  a soft offer).
- Doc sync (consult rule): intent-routing-stack.md Stage-0 row updated with
  the #1567 paragraph in the same commit.
- Tests: new `test_repo_clarification_1567.py` (49 tests — extraction units,
  resolution units, handler-level, PM's pinned transcript e2e through the
  REAL `process_intent` with an explosive LLM). Updated 4 existing tests for
  deliberate spec changes (close entry passes session_id; the
  with-session resolution-failure refusal is now the ask).

## Evidence

- `tests/unit/services/intent_service/` full: **2960 passed** (112s)
- `tests/test_completion_ratchets.py tests/test_architecture_enforcement.py`:
  **46 passed**
- `tests/intent/`: 229 passed, 6 failed — **all 6 A/B-verified pre-existing**
  on the clean tree (git stash → same failures → pop): test_bypass_prevention
  middleware-registered, enforcement monitoring endpoint, future-nl middleware
  config, handler-error-handling empty projects, no-cli-bypasses import,
  no-timeouts query fallback. Environment-shaped, untouched by this diff.

## Discovered issues (for Lead triage — not filed by me; isolated worktree,
Lead to decide `bd create` vs fold-in)

1. `_handle_reopen_issue_query` and `_handle_comment_issue_query` share the
   close handler's old shape: no explicit-repo honoring, repo dead-end is a
   generic error, no session threading — same #1567 treatment applies.
2. The three ANALYSIS dead-ends ("Cannot analyze commits / generate report /
   analyze data: repository not specified") still refuse without arming the
   carrier and don't consult the default repo.
3. Create path: `_handle_create_issue` extracts only `owner/name`; natural
   "create an issue in the X repository" phrasing isn't resolved there
   (deliberately out of #1567 scope — update/close only).
4. Fixed in passing (noting per discipline): `unmapped_field_clarification_pending`
   was missing from `_apply_soft_offer`'s clobber-guard flag list since #1411.

## Memory & briefing surfaces referenced this session

- **Referenced**: intent-routing-stack.md (mandatory pre-read; carrier +
  moratorium constraints), CLAUDE.md (worktree/commit rules, m-43/m-44
  layer-honesty phrasing in copy), #846/#1190/#1605/#1571 module docstrings
  (carrier contract), ab-a-isolation idea (stash A/B for pre-existing
  failures).
- **Loaded but not referenced**: memory index entries, mailbox protocol
  (subagent — no mailbox), glossary.
- **Wanted but not found**: a canonical list of which `tests/intent/` items
  are known-red in the unit environment (had to A/B them myself).
