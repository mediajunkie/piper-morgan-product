# Session Log — 2026-08-16 09:53 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1624 — chat document-summarize repair per PM ruling (2026-08-15, relayed by Exec):
delete the dead summarize vocabulary honestly + wire the EXISTING REST summarize path into
chat via the workflow-dispatcher rail. Option D (file-analysis sibling) stays deferred.
**Worktree**: isolated agent worktree, branch from claude/lead-cycle lineage; commit, no push.

## Pre-reads (done)
- `gh issue view 1624` — full body
- docs/internal/operations/summarize-intent-forensics-2026-08-15.md (the spec)
- docs/internal/architecture/current/intent-routing-stack.md (mandatory consult)

**Noted discrepancy** (flagged for Lead, not blocking): the Lead's parenthetical glosses
("option C = delete dead vocabulary", "option A = wire REST path into chat") do not match the
forensics doc's letters (doc-C = finish #1187 document branch, doc-A = corpus honesty). The
Lead's build constraints are concrete and self-consistent; building to those.

## Design (from recon)
Wiring chain (all pieces verified in source):
1. prompts.py:222 — restore `document` + `conversation` to source_type vocab (undoes Break 4,
   the #1432/08-02 re-land regression); teaching example :254 emits `summarize_document`.
2. action_registry.py — `_VERB_SOURCE_TO_ACTION[(Verb.SUMMARIZE, "document")] = "summarize_document"`
   (the shim's own comment names this exact re-add condition: "Re-add a mapping here only when a
   fetch-augment-then-floor handler exists to point it at"). (SUMMARIZE, anything-else) stays
   unmapped → #1187 issue/commit/floor paths untouched. New registry canonical
   ("SYNTHESIS","summarize_document") WORKFLOW + Verb + description.
3. workflow_entries.py — `run_summarize_document_workflow`: FileResolver (owner-scoped post-#1312
   FileRepository) resolves "the document" → file_id; then calls
   `document_handlers.handle_summarize_document(file_id, format, user_id)` — the SAME function
   the REST route calls (web/api/routes/documents.py:198). effect=READ, outwardness=PRIVATE
   (explicit, #1509). Guards: issue/commit-shaped requests return None (rail falls through to
   the #1187 floor path); no resolvable upload → deterministic honest no-document reply;
   ambiguity → clarification with candidates. LLM key already bound at the chat request
   boundary (web/api/routes/intent.py:488), same as REST (documents.py:197).
4. Option C deletions: `_handle_summarize` + `_summarize_with_llm` + `_format_summary` +
   `_extract_text_content` (dormant since #1158; renderer ruled never-to-be-built) + its
   dead-path tests; IntentEnricher module + test (orphaned since 2025-10-01/2026-05-15;
   FileResolver un-orphaned by consumption); templates.py:33-34 vestige rows; stale
   comments/docstrings; prompts.py:186 file_id theater line.
5. Corpus rows re-expressed: routing_corpus_1283.yaml "summarize the document" →
   action:summarize_document (+ regenerate inversion phase0 via builder).
6. Doc sync: intent-routing-stack.md same-commit update; decisions.log entry.

## Work log
- 09:10–09:53 — recon (all files above), design settled. No blockers.
- 09:53–10:10 — build complete:
  - **Lettering discrepancy resolved**: decisions.log 2026-08-15 22:2x confirms PM's own
    ruling used the Lead's letters ("C = delete the dead vocabulary honestly, A = wire the
    existing REST summarize path into chat") — the forensics doc's letters were re-assigned
    upstream of me; what I built matches the ratified ruling verbatim.
  - Option A: prompts.py (5-set source_type restored + summarize_document teaching example +
    issue-summarize example), action_registry.py (SYNTHESIS/summarize_document canonical,
    WORKFLOW, Verb.SUMMARIZE, shim cell (SUMMARIZE,"document"), example row),
    workflow_entries.py (run_summarize_document_workflow + entry READ/PRIVATE + 4 rail keys).
  - Option C: deleted _handle_summarize + _summarize_with_llm + _format_summary +
    _extract_text_content (intent_service.py, ~330 lines), IntentEnricher module + test,
    templates.py summarize rows, ~395 lines of dead-path tests in test_synthesis_handlers.py;
    stale docstrings/comments updated (fetch dispatcher, synthesis handler, 1187 tests).
  - Corpus: routing_corpus_1283.yaml row re-expressed → action:summarize_document (dated
    comment); phase0 regenerated via builder (+_ACTION_CATEGORY row).
  - Docs: intent-routing-stack.md surface-3 cell (+4 keys #1624 entry), decisions.log entry.
  - Ratchet delta: silent_death_core 200 → 198 (two silent-death sites left with the dormant
    handler); ceiling lowered same commit.
  - Tests: new test_summarize_document_rail_1624.py (21 tests: wiring, same-path identity,
    dispatch honesty, e2e through real process_intent with explosive LLM both directions,
    deletion pins). Suites: 21/21 new; 2986 passed intent_service+synthesis+ui_messages;
    70 resolver/template neighbors; 46/46 ratchets+enforcement after ceiling fix.
  - Env note: sandbox refused `env -u`; used the Lead-sanctioned empty-assignment form
    (ANTHROPIC_API_KEY= etc.) — harmless for -m "not llm" runs.

## Discovered work (reported to Lead, not filed — Lead merges/decides)
1. The TemplateRenderer family (services/ui_messages/templates.py + personality_templates.py)
   appears to have NO live (non-test) consumers — fix-or-delete candidate (#1432 family).
   Only the two summarize rows were deleted here (scope discipline).
2. FileResolver.resolve_file_reference's second parameter is still named `session_id` while
   the value flowing through is owner_id (post-#1312) — misleading naming debt on a
   freshly-un-orphaned module.
3. shared_types.TaskType.SUMMARIZE_DOCUMENT ("Document processing tasks Issue #290") — LLM
   task-type vocabulary, not touched; worth a look in any TaskType audit.

## Memory & briefing surfaces referenced this session
- **Referenced**: summarize-intent-forensics-2026-08-15.md (the spec — every build decision),
  intent-routing-stack.md (4-surface chain, moratorium, ratchet pointers), CLAUDE.md
  (#1124 no-new-elif rule, evidence discipline), decisions.log (PM ruling letters),
  test_reminder_clear_verb_1605.py + test_archived_projects_rail_1570.py (test idioms).
- **Loaded but not referenced**: memory index (MEMORY.md) entries broadly; glossary.
- **Wanted but not found**: none — the forensics doc was an exceptionally complete spec.

