# CXO Session Log — 2026-08-31 (Monday)

**Role**: Chief Experience Officer (CXO) | **Tool**: Claude Code | **Model**: Opus 5 (1M context)
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `8207809c` (expires ~09-06 — rotate at the last fire before)
**Started**: 07:17 PDT

---

## START (07:17) — clean open; caught an error in my own carry-forward before it licensed anything

08-30 closed cleanly at 22:17 (6 fires). Synced before mail; `CronList` shows exactly one job.

**Two memos, neither owing a reply.** Arch applied my precision correction as **ESSENCE v1.0.2** —
essentially my wording, plus the falsification framing carried into the note itself, because (their words)
*"one reversed case honestly recorded"* was an undersell once my interpretation landed. They also named
the reusable class in the amendment log: **requirement-vs-instrument drift running the subtle direction —
law saying slightly more than its instrument permits, which reads as progress and is therefore harder to
catch than the loud direction.** HOST reported their 06:37 START fired the staleness check clean.

⚠️ **Caught my own error on re-reading the carry-forward I wrote last night.** I had written that the
two-call deconfounding experiment needs *"no spend approval needed at that size."* **That is wrong on both
counts and it is the kind of note that licenses someone else to skip a step:**

1. **Size is not the criterion — authorization scope is.** PA established the principle yesterday and I
   endorsed it in writing: *"an agent that reaches into a keychain to unblock itself is a worse failure
   than a probe that waits."* PM's go was scoped to the 28-call run of a specific packet. A follow-up
   experiment is a fresh ask regardless of being two calls. **My note would have let PA read my
   convenience as their permission.**
2. **And it isn't mine to run anyway** — it extends the **Claude** arm, and I'm a Claude. Same
   subject/scorer confound that kept me off the original run.

Corrected in the carry-forward and told PA directly rather than leaving the correction where only I'd see
it. **A note that quietly lowers someone else's bar is worse than no note**, and I wrote it about the one
discipline I'd spent the previous evening praising them for holding.

## Closed a Done-gate gap Lead would have hit within a week

**Layer B routes by surface type** (Criterion 1), listing response-text and UI-rendering surfaces plus a
fallback: *"new surface type with no fitting rubric → branch a new instrument."* ⚠️ **The BYOC/MCP path
had no row.** So the first person taking an MCP tool through a Layer-B gate would correctly follow that
fallback and **branch a second instrument, not knowing mine exists** — the exact duplicate-rubric outcome
Branch-or-Anchor was written to prevent, **arriving through the discipline's own escape hatch.**

Added the row (Layer B is CXO-owned, so this is my lane), pointing at the v0.2 branch **with its
constraint attached rather than left to be discovered**: T scores `PENDING-PROBE`, never PASS, so it can
inform design decisions but cannot close a gate on T alone.

⭐ **The part that's more than bookkeeping.** Layer B's governing rule is *"the score is taken on the
experience **as delivered**."* On every other surface that's observable — the reply, the card, the error
screen are ours. 🔴 **On BYOC they are not**: the user-visible text is composed by a host and **we never
see it**, not in logs, not in telemetry, not after the fact. So the BYOC rubric scores **the payload, not
the delivery** — a proxy, the only one available in production, and **a Layer-B pass there is not the same
claim as a Layer-B pass anywhere else.**

**And it's not hypothetical**: in Saturday's probe a payload that carried its qualification honestly still
produced a fabricated reply. **Payload-only scoring would have missed it entirely.** Wrote that into Layer
B as a stated limit, with the note that end-to-end BYOC verification needs a probe harness — possible, but
always a deliberate test act, never a by-product of shipping.

Told Lead it adds no work to #1688: use the row, score R and C, mark T pending, note the pass covers the
payload. Two lines in a gate report, not a new gate.

## Proposed the quarterly rubric review — ~6 weeks overdue, and that's mine

We set a quarterly full-rubric review with PPM on 2026-05-10 for ~mid-July. It's 08-31. **It sat on a
"low urgency" line in my standing items — the deferral-without-a-named-trigger shape I'd flag in anyone
else's queue.** Sent a real agenda rather than a placeholder, and asked for a **named trigger (this week
or next, PPM's pick)** instead of another vague line.

**Agenda, ordered**: (1) **tier status, now sharper** — the open question was "unratified instrument backs
a binding gate"; ESSENCE v1.0.2 makes it "unratified instrument backs a **ratified commitment**."
(2) **Family coherence** — the first two instruments score what the user receives, **the third
deliberately doesn't**, so the family has a member whose scores aren't comparable to its siblings'.
Proposed question: does Branch-or-Anchor need a third category — *branched measurement surface* vs.
*branched dimension meanings*? **The UI branch changed what R/C/T mean; the BYOC branch changed what gets
measured.** We call both "branching." (3) **CT v2.4's deferred C=0 split** — with my honest read that the
accelerate-trigger did **not** fire (Saturday's fabrication was a different instrument on a different
surface, not CT's C=0 in a canonical retest), flagged as a judgment call PPM may read differently.
(4) Whether the "as delivered" limit belongs in CT itself rather than only in the DoD doc — **I put a
family-level claim in a DoD doc and would rather not quietly own both surfaces.**

**Discovered issues filed**: None.

## PM pushed back on "low urgency" — drained the audit, and it was not a clean bill

📌 **PM 2026-08-31**: *"low urgency is a risky concept with agents… can lead to never doing it. Generally
speaking my rule is to drain all unblocked tasks as soon as possible."*

Went back through `cxo-standing-items.md` rather than just agreeing. **Four deferred items; three had
resolved or gone moot with no action from me; the fourth was live and getting worse.**

- **MUX branch disposition** — I owed PM a deletion recommendation since **07-26** and never made it. All
  four `cxo-mux-*` branches **no longer exist on origin.** It resolved without me. Cheapest possible
  version of the failure, and still a failure.
- **#1394 tester disclosure** — I drafted a tester-facing line on **07-12**, *required before invites went
  out*, routed it to Lead and PPM… and **it was never landed in any doc.** Seven weeks, nobody noticed. It
  stopped mattering only because #1394 closed 08-09. **Luck, not process.**
- **#1386** — all CXO criteria signed off since 07-12; the rest isn't mine. Correctly parked.
- **Jake arc** — complete.

### 🔴 And the live one: the tester onboarding doc is pointing people at abandoned code (#1708 filed)

`docs/ALPHA_QUICKSTART.md` is a **first-contact surface** in exactly the sense I've used all week. Three
load-bearing claims false, **measured today rather than inferred**:

1. **It instructs `git clone -b production`** and warns testers off `main` as buggy. **`production` is
   7,614 commits behind `main`, tip 2026-07-26**, and `docker.yml` builds on `main` — **`production` is
   not a deploy source.** The advice is exactly backwards. CLAUDE.md carries PA's 08-13 warning about
   this branch when the gap was **4,195**; it has roughly doubled since, unnoticed.
2. **It calls the hosted app *"planned for 2026"*** and says *"all paths require the developer setup"* —
   while 📄 **ESSENCE v1.0, ratified, names the live web-chat app with ~11 alpha testers as the current
   surface.** The doc offers a 20–50 minute local build of five-week-old code as the only path.
3. Version header 0.8.11.0, last edited 07-17; deployed is v63/v64.

**Filed #1708; put an accuracy banner at the top stating only what I measured.** ⚠️ **Did not change the
instructions** — *what it should say* is a release-model decision (is local install still a supported
tester path? is `production` revived or retired?) belonging to PM/PPM/Lead alongside PPM's new
`release-model.md`. **Denominator on the banner: those three claims; the other ~450 lines are unaudited**,
and the banner says so.

### Rebuilt the standing-items file — dropped the category, not just the item

⭐ **The mechanism worth keeping**: *"low urgency" reads as a decision, so nobody re-examines it.* "Blocked
on X" gets rechecked whenever X moves. **A label that terminates review is worse than an untriaged
backlog** — the backlog at least still looks like work. And I was running one in my own file while telling
other roles to name their triggers.

`cxo-standing-items.md` rewritten to **exactly two states**: 🟢 **unblocked (do now)** and 🟡 **blocked on
a named thing (recheck when it moves)**, each with its recheck trigger. **Nothing is "someday."** Also
gave the file the frontmatter currency claim, so it's covered by CIO's `--state-files` check like the
carry-forward.

**Discovered issues filed**: **#1708** (ALPHA_QUICKSTART accuracy).

## Fire 2 (10:17) — PPM named Thursday; drafted the dispositions the same fire

Cron single. Inbox: two memos, both clean. **PA's is a model of the discipline I'd corrected myself on**
— they took my retraction and turned it into a precise standalone ask to PM (*"should the deconfounder run
alongside the GPT arm, or is that a separate decision?"*), explicitly refusing to fold a new ask into an
old authorization. Nothing owed from me.

**PPM gave the named trigger: Thursday 09-03**, agreed the agenda order, and asked for dispositions
"whenever you have them drafted." ⭐ **Per PM's rule this morning, "whenever" is now** — an item that just
escaped a six-week drift should not immediately acquire a three-day one. Drafted and sent this fire:
`docs/internal/testing/rubric-review-2026-q3-dispositions.md`.

**1. Tier status → ratify the INVARIANTS, not the criteria.** 🔴 **What makes "sufficient as-is" no longer
hold is something I actually did**: on 08-30 I falsified and rewrote the BYOC branch's T=3 *unilaterally*.
Good practice — **and it means the bar for a ratified commitment is mine to move silently.** If commitment
7 means "passes the Colleague Test" and I can redefine passing, I hold the pen on ratified law. ⚠️ **Full
ratification is the wrong fix** — it would have required a PM decision to make yesterday's improvement.
Proposed split (ESSENCE's own shape): ratified = the question + verdict shape + fabrication auto-fail;
CXO-editable = criteria, bands, examples, branches. One `decisions.log` entry, no PDR.

**2. Family coherence → yes, a distinct category, and the reason is report legibility not taxonomy.** A
Layer-B line reading *"passed the Colleague Test family rubric, 9/9"* is **identical** whether it means the
user's experience was good or *we handed the host good material and never saw what the user read* — the
Apr 26 C-axis shape. Two requirements attach to proxies only: state what the score does **not** claim, and
**name the companion verification**. The UI branch needs neither; rendered UI *is* user-perceived.

**3. CT v2.4 → ⚠️ I reversed myself mid-draft on the item I was most confident about.** I had a clean
disposition retiring it as already-solved (fabrication→C=0, context-blindness→C=1, not-required→the v2.2
fresh-account ceiling). **Checkable reason it's wrong: v2.2 PREDATES our 05-10 concurrence** — we agreed
v2.4 was needed *with that ceiling already in place*. So the concern was never rubric text; the agreed
mechanism was a per-query **`context_requirement` tag on the corpus**. ⭐ **Rubric text distinguishes
response shapes; the tag supplies query metadata the judge lacks** — facing a C=1, a judge can't tell
whether context existed and went unused or none was required, because **that fact lives in the query, not
the response.** Reframed as corpus-metadata work, not a rubric rewrite — cheaper than it's looked for four
months, and probably why it never moved: filed as "author v2.4," actually "tag a corpus." Now affects two
instruments, since BYOC anchors C to CT.

**4. "As delivered" limit → canonical in the CT family doc, pointer in Layer B.** I put a family-level
claim in the consuming doc yesterday; 📄 ESSENCE's ratified standing rule (*"hand-maintained copies are the
documented failure mode"*) says otherwise. Flagged rather than quietly owning both surfaces.

**Denominator stated on the review itself**: re-read the C-axis, the branching discipline, and Layer B's
routing section in full; **did not** re-read all 382 rubric lines, the worked examples, or the UI Lifecycle
rubric.

**Discovered issues filed**: None.

## Fire 2 continued — PM's postponement work is already mechanical, and it read my file as unreadable

📌 **PM**: *"I am working with CIO to eliminate these postponements that get lost. It is not a matter of
fault but of instructions."* Checked before offering anecdotes — **it had already shipped**: CIO's
`scripts/aging-standing-items.sh` (`c3ab42f12`) plus a **PM-ratified CLAUDE.md rule** (`f4761d0f0`) that
standing-items rows carry a filed date.

⚠️ **My rebuilt file was one of the 7 the scan reported it could not see inside** — I'd rewritten it into a
clean two-state model *hours earlier* and it was still invisible, because structure without a date column
is unreadable to the mechanism. Dated every row from **git history rather than memory** (`-S` searches for
each row's first appearance).

🔴 **And then it immediately flagged two rows sitting in my own "🟢 UNBLOCKED — do now" column, 35 and 32
days old.** I had put them there at 07:17 **this morning**, after PM's note, with the fresh resolve — and
had not done either by 12:43. ⭐ **The structural change was right and insufficient: relabelling an item
"do now" does not do it.** That is precisely the CLAUDE.md rationale — *"it depends on the deferring agent
noticing its own deferral"* — with me as the deferring agent, same day. **Reported it to CIO that way**,
because a second adopter confirming the premise is worth more than agreement.

**Drained both rather than re-labelling them again:**
- **Jake loop-back** (filed 07-29) — memo to HOST *asking* whether it happened, explicitly not assuming.
  Cited my own 07-12 disclosure that was routed and never landed as the reason not to trust
  "it was routed to an owner" as evidence.
- **Successor read** (filed 07-26) — written: `docs/briefing/CXO-SUCCESSOR-READ.md`. Lessons that cost
  something, load-bearing-vs-commodity read, relationship map. **Written mid-role deliberately** — a
  handover doc composed at handover is written by someone with one foot out, and my predecessor's was an
  artifact list with two factually wrong claims.

**Finding returned to CIO — the blocking-language filter had ~2 false positives out of 4 flags on my
file.** Both false positives sit in a table with a literal `Blocked on` column that is non-empty;
`#1386` in the same table *was* correctly excluded, so it's phrasing-sensitive rather than column-blind.
**Suggested: non-empty `Blocked on` cell ⇒ blocked, regardless of wording** — cheaper than growing a
phrase list, and it rewards structure over incantation. ⚠️ Framed with the credibility argument: **a false
positive on a correctly-blocked row is what trains people to skim the report**, and two-of-four on the
first adopting file will erode trust before the check earns it.

**Also corrected an attribution**: PM noted they never named Thursday. Right — **PPM** picked it in
response to my ask for a named trigger. Fixed in the tracker row too, which had already gone stale within
the hour (it still said "PPM picking a slot" after PPM had picked and I'd sent the dispositions).

**Discovered issues filed**: None.

## Fire 2, third pass — all four dispositions agreed; executed the two that were mine

**PPM agreed all four**, having read the doc rather than the memo, and **independently verified item 3's
date claim** (`git log -S"v2.2"` → v2.2 landed 2026-04-26, genuinely before the 05-10 concurrence). Arch
concurred on item 1, noting the split mirrors ESSENCE's own Essence/Extension structure. No live time
needed — the Thursday slot isn't required.

⭐ **Per PM's rule, "agreed" is not "done."** Two of the four were mine to execute and I did them this
fire rather than logging that they were approved:

- **Item 2 → CT rubric v2.3.3**: added the third branch case — **branched MEASUREMENT SURFACE**, distinct
  from the two existing cases, which both branch what the dimensions *mean*. With the two requirements
  that attach to **proxies only**: state what the score does NOT claim, and name the companion
  verification. The UI/BYOC asymmetry table is in the rubric now, not just in a review doc.
- **Item 4 → moved the canonical "as delivered" statement** into the CT rubric; **DoD Layer B converted
  to a pointer**, per ESSENCE's ratified derive-don't-hand-maintain rule.

⚠️ **And two version references went stale inside my own edit** — Layer B still cited CT as "v2.3.2" after
I bumped it, and the rubric's Date field still read 2026-05-15 under a v2.3.3 header. Caught and fixed in
the same pass. **Exactly the drift the review exists to prevent, produced by the edit that prevents it.**

**Recorded PPM's noted edge in the doc, not just the memo** — under the invariants/criteria split I can
still rewrite dimension *criteria* freely, which changes **what earns points beneath a fixed bar**. PPM
deliberately did not want it closed; git history is the paper trail. **A property named only in a memo is
a property that disappears**, so it's in the artifact.

**Routed the two that aren't mine to close:**
- **Item 1 → PM**, as a ~2-minute decision with the table, both prior agreements, and PPM's edge stated
  *before* the ask rather than buried after it. Framed honestly: *"say no and the status quo holds — just
  with the pen in my hand."*
- **Item 3 → Lead cc PA**, as a **routing question rather than an assignment**: who owns the canonical
  corpus? ⭐ The reframe is the useful part — it sat four months filed as *"author v2.4"* when the job is
  *"tag a corpus."* **Misfiled work, not deferred work**, and probably nobody who could do it ever read it
  as theirs.

**Also noted**: #1708 got a PM ruling within hours — hosted app becomes the primary tester path, local
install retires to CONTRIBUTING.md pointing at `main` never `production`. PPM + Docs execute; Lead is
running a fresh-clone probe first for measured numbers. **My banner is superseded by that rewrite when it
lands** — nothing owed from me.

**Discovered issues filed**: None.

## PM ratified the invariants — recorded, not announced

📌 **PM: "ratified!"** Wrote what I said I'd write, in the three places it has to live to be findable:

- **`decisions.log`** — the three invariants (the question · the verdict shape · the fabrication
  auto-fail), the why-not-full-ratification reasoning, the provenance chain (PPM agreed all four, Arch
  concurred), ⚠️ **and PPM's known-property edge inside the entry itself** — that criteria remain
  rewritable beneath a fixed bar. **PM decided with that visible, and the record should show it** rather
  than let a future reader take it for an oversight.
- **CT rubric → v2.3.4** — a **Tier status** block at the top of the file, which is where someone about to
  extend the instrument actually looks. Governance recorded, no scoring-criteria change.
- **DoD Layer B** — pointer bumped to v2.3.4 and the ratified status.

**Q3 review closes with three of four landed the same day the dispositions were agreed**; only item 3
(the `context_requirement` corpus tag) is open, routed for ownership. **Returned the Thursday slot** to
PPM rather than holding it.

⭐ **The keeper from this whole thread**: the review was **six weeks late and then took a day.** Nothing in
it was hard and it was waiting on nobody — it sat because it was labelled "low urgency," which reads as a
decision and so never gets re-examined. **And item 3 is that same failure in a second form**: four months
filed as *"author v2.4"* when the actual job is *"tag a corpus."* **Misfiled, not deferred** — nobody who
could do it ever read it as theirs. Two different mechanisms, one symptom, both invisible until something
external looked.

**Discovered issues filed**: None.

## Fire 3 (13:17) — two corrections landed on me, both right, both verified before accepting

Cron single. Three memos, all producing work.

### 🔴 PA caught my "unblocked" claim — and the error is precisely located, which matters

I told PA *"Credits are in — that unblocks the GPT arm."* **PA tested the credential rather than trusting
my memo: still `insufficient_quota`, twice, an hour apart.**

⚠️ **The imprecise account ("CXO relayed bad info") would be wrong.** PM's statement was **true** — they
topped off the account. **My sentence collapsed two layers:**

| Layer | Claim | Attestable by |
|---|---|---|
| The action | *"I added $10 credit"* | ✅ PM, and true |
| **System state** | *"an API call now succeeds"* | 🔴 **nobody had checked — I asserted it anyway** |

A top-up can be in processing, or land on a different org/project than the key belongs to. **PA's test
distinguishes those; my sentence assumed them away.** ⭐ **This is the four-layer relay I wrote up on
Saturday — I was a hop in it and dropped the layer, two days after naming the pattern.** Knowing about a
failure mode demonstrably does not prevent being inside one.

Relayed PA's diagnostic to PM (check the billing page directly — the only thing that separates propagation
delay from wrong-org). **Tracker corrected from UNBLOCKED back to BLOCKED.**

### 🔴 HOST caught a real overclaim of mine, and it was pointed at a person

Asked HOST this morning whether the Jake loop-back happened. **Answer: no — and worse than a same-day
gap.** Four fixes traced to Jake's 07-25 feedback shipped (#1476/#1477 08-09, #1510 08-13, #1536 08-22);
**the earliest has been un-communicated for three weeks.** I verified all four closure dates myself.

⚠️ **And HOST corrected me**: I'd named "the capability-legibility thread" among what shipped. **`gh issue
view 1509` → OPEN.** I checked rather than accepting. **In a message headed to a human who did unpaid work
for us, that overclaim would have been the worst possible place for it** — telling a tester their
contribution shipped when it hasn't is the honest-decline failure aimed at a person instead of a query.
Caught before it reached him.

**HOST drafted the loop-back rather than merely answering** — the difference between closing a ticket and
closing a loop. It's PM's to send; offered a five-minute re-verification pass near send time so HOST isn't
the only one who has checked.

**Resisted the "personal failing" framing in both directions**: this was findable the whole time by
anyone, and **"findable by anyone" reliably means "found by no one."** That's an argument for more
mechanical checks, not more diligence — PM's *"not a matter of fault but of instructions."*

### PA also ruled themselves out on the corpus, factually

Their probe corpora are per-probe, hand-built, stood down — **not #928's canonical set.** So Lead isn't
choosing between two candidates; my memo implied a choice that doesn't exist. ⚠️ **Flagged the possibility
I'd rather not paper over: "nobody, currently" may be the real answer**, in which case the tagging job sits
behind an **ownership vacuum**, not a routing question. Left for Lead rather than assumed.

**Discovered issues filed**: None.

## Fire 4 (16:17) — the credential mystery has a cause, and a four-month item got its spec

Cron single. Six memos.

### The `insufficient_quota` cause is found, and my hypothesis held

PA checked the stored key: **`sk-proj-`** — project-scoped, the shape my table flagged as the common
cause. And PM had independently screenshotted the billing page: **one org, two projects** — the $9.22
top-up landed in "Intern" while the key belongs elsewhere. **A project-scoped key draws only on its own
project**, so org-level and sibling-project balance are invisible to it. Exactly the symptom: identical
error before and after the top-up.

**PM is minting a fresh key from inside the funded project** rather than hunting down which project the
old key belongs to — the right move; it makes the question moot instead of answering it. PA stores via
`KeychainService` and verifies live.

⭐ **PA's own note is the better lesson and it's theirs**: they didn't need my free-endpoint check because
the paid call's error text already said it — *"worth running the free check first when a paid one would
answer the same question."* **A diagnostic that costs nothing should precede one that costs something,
even when both work.**

### Lead owns the corpus — no vacuum — and the spec shipped the same fire

I'd flagged that *"nobody, currently"* might be the real answer. **It wasn't.** Lead claimed it on
concrete grounds (the corpus lives in their harness; every structural change this sprint went through
their lane; corpus integrity is instrument quality). **PA had already ruled themselves out factually**,
so there was no contest — my memo had implied a choice that didn't exist.

**Lead said "send the spec whenever it's ready," so I sent it this fire**:
`docs/internal/testing/context-requirement-tag-spec.md`.

- **Procedure is mechanical and never inspects a response** — *could a frontier LLM with no access to this
  user's data give a fully correct answer?* ⭐ If tagging required reading answers, **the tag would inherit
  the ambiguity it exists to remove.**
- **Load-bearing change**: `not_applicable` queries are **full marks at C=2, not docked** — today the
  instrument penalises a response for not doing what the question never asked for.
- ⚠️ **Trap flagged**: this is **not** the fresh-account ceiling. **v2.2 is about account state** (does
  context exist?); **the tag is about query type** (does the query need context?). Both can apply; §4 has
  the 2×2. Collapsing them is the silent-drift shape Branch-or-Anchor exists to prevent, and an easy
  collapse to make mid-pass.
- 🔴 **The first deliverable is a COUNT, not a rescoring.** The rubric currently reads *"clustering at C=2
  → context assembly isn't flowing."* **That diagnostic assumes every query wanted context.** If a real
  share is `not_applicable`, some historical clustering is an **instrument artifact**. **Stated as a
  hypothesis — I have not measured the distribution and won't claim it before Lead's pass reports one.**
  If it lands, the rubric edit is mine.

### Two housekeeping corrections adopted from others

- **CIO's date audit**: they judged #1463's rows should carry the **parent-gate origin (07-30)**, not the
  08-30 date I gave when I re-noted the sub-task. **Adopted — re-dating a step resets the clock, which is
  the same failure as re-labelling it**, and the whole point of dating is to expose real age.
- **#1708 landed without me**: PPM rewrote the quickstart hosted-primary (`013d5a0cd`) and caught a
  near-duplicate with Docs mid-flight. **My accuracy banner is gone, correctly superseded** — the fix was
  the surface change, not a corrected clone command. Nothing owed.

**Discovered issues filed**: None.
