# Session Log — 2026-08-31 07:36 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle, merged origin/main at start)
**Assignment**: Two sequential items — #1689 (replace two native dialogs; quiet the Code Quality native-dialog gate) and #1676 (canonical retest runs record serving provider/model).

## Plan
1. #1689: migrate `templates/settings_github.html:590` alert() and `templates/settings_llm_keys.html:161` window.confirm() to the house `Dialog` primitive (`web/static/js/dialog.js`, F1 #1170 self-contained API, loaded via app_shell). Render-path tests per m-43, mirroring `tests/unit/templates/test_native_dialog_migration_1170.py`. Verify with the exact CI invocation: `python scripts/native_dialog_lint.py --baseline .native-dialog-lint-baseline.txt`.
2. #1676: find the canonical-retest harness + history CSV; capture serving provider/model from the SERVING client at run time; add named column(s) to `docs/internal/operations/canonical-retest-history.csv` with honest `unrecorded` backfill.

## Work

### 1689 — native dialogs migrated to Dialog primitive (07:36–07:50)
- House pattern found: `Dialog` service (`web/static/js/dialog.js`), F1 self-contained API (`Dialog.alert({title, body})`, promise-style `Dialog.confirm({title, message})`), shell-owned assets via `templates/layouts/app_shell.html:79`. Both target templates extend app_shell, so Dialog is in scope — no asset wiring needed.
- `templates/settings_github.html`: `alert('Could not start GitHub OAuth: ' + e.message)` → `await Dialog.alert({title: 'Could not start GitHub OAuth', body: e.message})` (containing fn already async). Copy split title/body, no words invented.
- `templates/settings_llm_keys.html`: `window.confirm("Delete your …")` → `await Dialog.confirm({title, message, confirmText: 'Delete'})`, gate `if (!confirmed) return;` (handler already async). #1482 honesty copy preserved verbatim (its pin suite passes).
- Tests: new `tests/unit/templates/test_native_dialog_migration_1689.py` (9 tests) mirroring the 1170 file — renders both pages THROUGH app_shell (m-43: asserts dialog.js/css reach the rendered page), positive Dialog.* assertions, no-native negatives, and a subprocess run of the EXACT CI invocation asserting exit 0.
- One iteration: my own migration comment contained the literal `window.confirm(` and tripped my no-native source assertion (the lint strips comments; my test doesn't) — reworded the comment rather than weakening the test.
- Evidence: `python scripts/native_dialog_lint.py --baseline .native-dialog-lint-baseline.txt` → "no new native dialogs (0 baselined)." exit=0 (was: 2 NEW, exit=1). `pytest tests/unit/templates/ -m "not llm"` → 998 passed. Token-lint ratchet also green ("no new violations (0 baselined).").
- Commit: 364e94add.

### 1676 — canonical retest records the serving provider/model (07:40–07:57)
- Truth source: ALL LLM traffic funnels through `services/llm/clients.py` `_complete_raw` (container's "llm" = LLMDomainService delegates to the module singleton `llm_client`), where the primary-vs-fallback decision lives. Added module-level `SERVING_MODEL_RECORD` Counter + `_record_serving()`, incremented ONLY at a successful `_call_provider` return, keyed `provider:resolve_model_alias(model)` — the exact wire id the request sends (the alias-resolver pin in test_model_aliases.py updated 3→4 sites with rationale). `# silent-ok:` annotation on the defensive except (silent-death ratchet caught the bare form — ceiling 198 held).
- Harness: `tests/e2e/test_canonical_conversations.py` — module-scoped autouse `serving_llm_report` fixture snapshots the Counter around the run; teardown builds a CSV-ready report (`build_serving_report`: none/single/mixed vocabulary, comma-free values), prints a `=== CANONICAL RUN SERVING LLM (#1676) ===` block, and writes `dev/active/canonical-retest-serving-llm.json`. Judge calls deliberately NOT counted (direct SDK client, outside the serving path) — documented.
- CSV: `canonical-retest-history.csv` gained `serving_provider,serving_model` appended after `notes` (existing columns untouched); all 14 existing rows backfilled `unrecorded,unrecorded` via by-NAME csv script. The script also repaired a pre-existing defect it surfaced: Run 10 and Run 11 rows carried a stray 13th empty field (between quality_pct and env_errors) that shifted by-name parsing; dropped with shape verification, all values preserved. `canonical-retest-history-update.md` documents the new columns, the vocabulary, and where values come from.
- Live-wire proof (m-43): one env-stripped probe through `llm_client.complete("intent_classification", …)` reproduced the issue's exact confound — openai primary 429 `credit_balance_exhausted` → silent anthropic fallback — and the recorder captured `{"anthropic:claude-sonnet-4-6": 1}`, matching the `llm_usage` log line.
- Tests: new `tests/unit/services/llm/test_serving_record_1676.py` (5 tests: primary records, fallback records the FALLBACK not the failed primary, total failure records nothing, never-raises) + `TestServingReportShape` (3, non-llm-marked, runs in keyless sweeps). Suites: tests/unit/services/llm 65 passed; both ratchet files + llm units 114 passed; canonical module `-m "not llm"` 8 passed; full tests/unit/services sweep 8479 passed (one known-failing place test deselected — pre-existing, listed in scripts/known_failing_backlog.tsv).
- Discovered work (committed separately, 4bdd64ab3): two tests still read pattern-073 at the pre-fold path `docs/internal/architecture/current/patterns/` (reorg 217eee9b7 moved it; link-repair batches missed test-file readers) — FileNotFoundError since the fold. Repaired both.
