# Session Log — 2026-08-31 18:44 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Task**: Context-requirement tagging pass over the canonical query corpus, per
CXO spec `docs/internal/testing/context-requirement-tag-spec.md` (supports 1674/1676-adjacent
instrument work; spec closes CT v2.4's C=0 disambiguation).

## Plan

1. Read spec FIRST (done) — three tags (required/optional/not_applicable), query-only judgment
   (never inspect responses), no prescribed storage mechanics → extend the corpus tuples.
2. Tag all queries in `CANONICAL_QUERIES` (tests/e2e/test_canonical_conversations.py) —
   6th tuple field `context_requirement`.
3. Update the 5 parametrize decorators + signatures for the new field; add a free
   (non-llm) validation test so the tag stays complete and valid.
4. History-doc note per spec §6 (per-bucket C reporting) in
   docs/internal/operations/canonical-retest-history-update.md.
5. Verify: collection green, `-m "not llm"` green for the file, both ratchet files green
   (test_completion_ratchets.py, test_architecture_enforcement.py).
6. Commit (bare issue numbers, no close keywords), NO push per delegation.

## Work

- **Tagging pass complete — 61 of 61 queries tagged** (denominator: the full
  `CANONICAL_QUERIES` corpus; no query skipped, none unresolved). Judged from the query
  alone per spec §2 — no responses inspected.
- **Distribution (the spec's §5 finding)**: required=49 · optional=2 · not_applicable=10.
  - not_applicable (10): Q1–5 (identity), Q6 ("What day is it?" — needs a clock, not user
    data; environment context ≠ user context), Q26–28 (about Piper itself), Q50 (/piper help).
  - optional (2): Q23/Q24 (risks/opportunities) — the spec's §7 borderline shape, tagged
    per its tie-break (ambiguous → optional + flag). FLAGGED for CXO adjudication.
  - required (49): everything reading or mutating the user's actual state. Action queries
    uniformly `required` — a fully correct answer truthfully confirms a real change to this
    user's world, which no access-less LLM can produce (principle documented in the corpus
    header comment).
- **Storage shape**: 6th tuple field `context_requirement` (spec vocabulary) on every row;
  `VALID_CONTEXT_REQUIREMENTS` constant; new free shape-guard `TestContextRequirementTags`
  (no llm marker — runs in keyless sweeps; guards completeness/validity, deliberately does
  NOT pin per-row semantics). Updated 5 parametrize decorators + signatures for the field.
- **History doc**: added "C-axis reporting is per-bucket, never pooled" section to
  `docs/internal/operations/canonical-retest-history-update.md` with the spec's §6 notes
  line and the distribution.
- **Verification** (env-stripped, POSTGRES_PORT=5433):
  - `pytest tests/e2e/test_canonical_conversations.py --collect-only -q` → 246 collected
  - `pytest tests/e2e/test_canonical_conversations.py -m "not llm" -q` → **9 passed, 237 deselected**
  - `pytest tests/test_completion_ratchets.py tests/test_architecture_enforcement.py -q` → **49 passed**
  - Distribution cross-checked mechanically via AST literal_eval (counts match comment block).
- Committed on claude/lead-cycle, NOT pushed (per delegation — Lead reviews + pushes).

## Discovered issues filed

None.

## Memory & briefing surfaces referenced this session

- **Referenced**: `docs/internal/testing/context-requirement-tag-spec.md` (the whole task's
  authority); CLAUDE.md env-strip + bare-issue-number + state-the-denominator disciplines;
  `docs/internal/operations/canonical-retest-history-update.md` (placement of §6 note).
- **Loaded but not referenced**: mailbox/worktree discipline sections (no mail this session).
- **Wanted but not found**: none.
