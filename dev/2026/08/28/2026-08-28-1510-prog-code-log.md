# Session Log — 2026-08-28 15:10 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree; verification-only task, no code changes expected)
**Task**: #1661 carve-out check (PM-accepted at 2026-08-28 triage sitting) — EMPIRICAL probe:
after a fresh upload, does "summarize the file" / "summarize the file I just uploaded" (no filename)
(a) resolve to the just-uploaded doc, (b) ask which file, or (c) fabricate?
Only (c) returns the sub-case to MVP.

**Layer (m-43)**: LOCAL server from this worktree (local HEAD, includes #1657 v63 file-resolution
path), real process_intent via tests/live/ harness (PIPER_LIVE=1) — real server subprocess,
real login, real /api/v1/files/upload, real /api/v1/intent turns. NOT live prod.

## Work

- 15:09 — Preflight: Postgres 5433 up (docker compose, healthy), venv Python 3.11.15,
  KeychainService resolves a real anthropic key (len 108) with Claude Code env vars stripped.
- Plan: temporary probe test `tests/live/test_1661_probe_tmp.py` reusing conftest fixtures
  (turn_driver = fresh throwaway user per rep). 3 reps per phrasing, single fresh upload each,
  unique marker content per file to detect fabrication; one two-file case. Probe file deleted
  after run (not committed). Deliverable = verdict report to Lead, no fixes.
- 15:11 — Evidence run 1 (7 probes, all measured, no degradation; server via harness, port 50168):
  - **A: "summarize the file"** (fresh account, one fresh .md upload) — 3/3 resolved to the
    just-uploaded file: response named the exact uploaded filename, intent context carried the
    correct file_id, `resolution_confidence: 0.95`, `requires_clarification: false`. Verdict (a).
  - **B: "summarize the file I just uploaded"** — 3/3 identical: correct filename + file_id. Verdict (a).
  - **C: two uploads then "summarize the file I just uploaded"** — asked which file, listing both
    candidates verbatim (`reason: ambiguous_file_reference`, `requires_clarification: true`). Verdict (b).
  - Discovered defect (already tracked, NOT new): every .md "summary" body was
    "• Unable to analyze PDF document" — `DocumentAnalyzer.analyze()`
    (`services/analysis/document_analyzer.py:31`) runs pypdf on every upload regardless of
    file_type. **Already filed as open issue 1659 (2026-08-18)** — supersession gate honored,
    no duplicate filed. Resolution layer is unaffected (correct file chosen every time).
- 15:15 — Evidence run 2, PROBE D (real PDF with unique marker facts, "summarize the file I just
  uploaded"): faithful genuine summary of the correct just-uploaded PDF — filename, marker hex,
  "cassowary", "83 acoustic beacons", "9,410 units" all present. No fabrication.
- Cleanup: probe file deleted (never committed); both harness servers torn down by fixture
  (verified `pgrep` — only the pre-existing Aug-13 dev server remains, untouched); throwaway-user
  DB rows count-verified clean by the harness teardown; uploads/ byte residue for the 8 throwaway
  users removed. Transcripts: scratchpad `probe-1661-run1.txt` / `probe-1661-run2.txt`.

## Verdict (for Lead relay to issue 1661)

**(a)/(b) confirmed — PUB stands.** Zero fabrication in 8/8 measured turns. Single-file: always
resolves to the just-uploaded document (deterministic across 6 reps, both phrasings). Two files:
graceful clarify listing candidates. Successful analysis (PDF) is faithful to the actual content.
The only blemish — non-PDF uploads unsummarizable with wrong error copy — is missing-answer, not
wrong-answer, and is already tracked as issue 1659.

**Layer caveat (m-43)**: local worktree HEAD (includes 1657/v63 file-resolution), real server
process + real login + real upload + real /api/v1/intent turns over TCP — NOT live prod, and not
the rendered UI.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-var strip prescription, supersession gate, pause-before-irreversible
  — checked PID 67615 start date before deciding not to kill), tests/live/conftest.py docstring
  (m-43 layer framing, harness usage), tests/live/test_files_upload_1656_live.py (upload request
  shape), tests/live/test_summarize_files_view_1657_live.py (PDF seeding pattern, LLM-lane note),
  feedback_supersession_gate_before_fixing (searched issues before filing → found 1659).
- **Loaded but not referenced**: memory index at large, mailbox/worktree discipline sections
  (no mailbox writes this session).
- **Wanted but not found**: none.

