# Session Log — 2026-08-28 09:04 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Task**: #1635 — ambient-presence "coming soon" placeholder as a Radar card, per CXO design memo (binding rules + copy)

## Plan

- Radar surface found: `services/radar/feed.py` (domain assembly) → `web/api/routes/radar.py` (JSON API) → JS renderers in `templates/components/history_sidebar.html` (`renderRadarCard`) + `templates/home.html` (aside, reuses same renderer). Dashed style = `radar-card--example` (history_sidebar.html:306), driven by `provenance === 'example'`.
- Implementation: new `EntityType.COMING_SOON` (`"coming soon"` — the pill string), `_coming_soon_entity()` in feed.py with CXO's verbatim copy, appended AFTER the attention sort in the populated branch only → structurally last + suppressed at zero real entities. No template change, no route change, no new JS.
- Tests: new `tests/unit/services/radar/test_coming_soon_1635.py` (string-literal copy pins, both suppression directions, route serialization); template-render assertions added to `tests/unit/templates/test_history_sidebar.py`; update existing exact-list assertions in radar domain/route tests to account for the trailing placeholder.
- No old ambient-presence placeholder copy exists anywhere in the Radar surface (grepped) — nothing to assert removed.

## Work

- **#1635 implemented** (09:04–09:15):
  - `services/radar/models.py` — `EntityType.COMING_SOON = "coming soon"` (the pill string, CXO verbatim).
  - `services/radar/feed.py` — `_coming_soon_entity()` (CXO copy verbatim, `lifecycle_state="preview"`, `Provenance.EXAMPLE`, no ref, not pinned) appended after the attention sort in the populated branch only → structurally last + suppressed at zero real entities. Both CXO rules are structural, not conditional logic.
  - No template/route/JS change — the placeholder rides the existing `renderRadarCard` path; EXAMPLE provenance → dashed `radar-card--example`.
  - New `tests/unit/services/radar/test_coming_soon_1635.py` — 9 tests: suppression both ways (empty + seed-only), always-last (incl. vs pinned + zero-attention entities), copy string-literals verbatim (title/meta/pill/lifecycle), future-tense clauses, EXAMPLE provenance + no ref, route serialization end-to-end, empty route response carries no placeholder.
  - `tests/unit/templates/test_history_sidebar.py` — new `TestComingSoonPlaceholder1635`: example→dashed class mapping through the template surface, real `template.render()` of home.html (aside container + `/api/v1/radar` fetch + shared renderer), and negative assertions that NO template hardcodes the 1635 copy (server owns suppression).
  - Updated 6 exact-list assertions across test_radar_domain.py, test_document_source_1238.py, test_radar.py to `[:-1]` with #1635 comments.
  - No old ambient-presence placeholder copy existed (grepped) — nothing to assert removed; noted in the new test file docstring.
- **Evidence**: targeted suites 171 passed; ratchets (test_architecture_enforcement.py + test_completion_ratchets.py) + standup 446 passed; full tests/unit/templates/ + tests/unit/web/ + tests/unit/services/radar/ → **1721 passed** in 22.87s.
- **Discovered issues**: none filed — none found.

