# Session Log — 2026-08-28 12:57 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: Lead's (`~/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle`)
**Task**: Architecture Enforcement CI red since ~2026-08-23 — #1436 mypy signature-drift
ratchet over ceilings (PPM's find via #1386 criterion 4). Fix the drift, not the ceilings.

## Work

### Reproduction (CI-exact)
- Local full venv gave inflated counts (arg-type 434) — the full venv resolves more
  imports than CI's bare toolchain. Built a CI-equivalent clean py3.11 venv with only the
  pinned toolchain (`mypy==2.3.0 sqlalchemy==2.0.23 pydantic==2.12.5 fastapi==0.115.14`)
  in the scratchpad; that reproduced CI's exact failure: arg-type 411>390,
  assignment 267>266, attr-defined 250>245, name-defined 3>0, operator 78>76,
  return-value 69>68, union-attr 173>172, type-var 1 (no ceiling),
  var-annotated 81<82 (shrink-lock).
- Baselined against e7939bb65 (2026-08-12, the commit that set the current ceilings;
  gate verified green there in the same env). Content-diff green→current isolated the
  exact drift: **36 new errors across 11 files** (21 arg-type, 5 attr-defined,
  3 name-defined, 2 operator, 2 assignment, 1 each union-attr/return-value/type-var).
  Notable: the gate was already red at 3caa87594 (08-16) — part of the drift predates
  the last ceiling-touching commit, which itself shipped over-ceiling.

### Fixes (all real code, no casts, no ignores — house style per e7939bb65)
- `services/intent_service/consent_gate.py` — the 3 name-defined: quoted annotations
  (`WorkingMode`, `VerificationMetaMode`, `VerificationDecision`) carried `noqa: F821`
  with no import anywhere → TYPE_CHECKING import block (runtime unchanged; the runtime
  imports stay function-local for the cycle). NOT live NameErrors (string annotations).
- `services/intent_service/capability_legibility.py` — 5 attr-defined:
  `_unique_rail_entries` typed `Tuple[str, object]` → `Tuple[str, WorkflowEntry]`
  (TYPE_CHECKING import).
- `services/intent/intent_service.py` — 10 arg-type + 1 assignment + 1 union-attr:
  - `_handle_generate_report` / `_handle_analyze_data`: explicit `repository is None`
    narrow after the `_resolve_analysis_repository` consult (contract: `(None, result)`
    is the only None shape) returning the honest refusal — clears 5 arg-type.
  - close/reopen `update_issue(**_repo_kwargs)`: `_repo_kwargs` now a
    `_RepoRouteKwargs(TypedDict, total=False)` — mypy unpacks it correctly AND the
    #1042 call shape is preserved (first attempt passed owner=None explicitly and
    broke `test_unnamed_close_keeps_the_router_resolution_call_shape`; reverted to
    TypedDict). Clears 2 arg-type; comment-path dict typed too for symmetry.
  - `_handle_generic_query` / `_handle_update_issue`: implicit-Optional defaults
    (`session_id: str = None`) → `Optional[str]` (2 assignment).
  - standup: `_stored` initialized + `(_stored or {}).get("source")`; empty-branch
    interview invite now requires `session_id` before arming (the #1532 class — an
    offer keyed to no session); abandon-name lookup `_vi_payload.get("kind") or ""`;
    reminder `requires_clarification=bool(...)`.
- `services/intent_service/repo_clarification.py` — walrus narrow in the repo-match
  set comprehension (type-var + candidates arg-type); `resolved and full_name` guard
  before `_bind_and_dispatch`; sessionless dispatch takes the existing honest retry
  path (structurally unreachable — armed questions are session-keyed).
- `services/intent_service/inversion_live.py` — explicit
  `op/intent_category/decision.confidence is None` narrow before constructing the
  Intent (each None sets a reason upstream; dispatch ≡ reason-is-None).
- `services/intent_service/standup_todo_offer.py` + `services/file_context/file_resolver.py`
  — `ensure_utc` call sites narrowed with walrus guards (operator ×2, arg-type,
  return-value; sort key `ensure_utc(t.due_date) or now` with unreachable-fallback note).
- `services/intent_service/snapshot_assembly.py` — D1a gate mirrored inside
  `_read_ledger_head` (closures can't carry call-site narrowing).
- `services/intent_service/workflow_entries.py` — summarize resolver view: real
  detached `Intent(category=SYNTHESIS, action="summarize_document", ...)` instead of
  `SimpleNamespace` (same detachment property, honest type).
- `services/intent_service/collaboration_gate.py` — `bool(message and _RE.search(message))`.
- `services/analysis/document_analyzer.py` — `metadata: Dict[str, Any]`.

### Ceilings (`scripts/ratchet_ceilings.json`)
- **No ceiling raised.** Per shrink-lock: `mypy_assignment` 266→262 (fixes cleared 5,
  net below ceiling), `mypy_var_annotated` 82→81 (pre-existing improvement locked in).
- New class ratcheted: `mypy_type_var: 0` (post-fix count; extinct stays extinct,
  mirroring `mypy_name_defined: 0`).

## Verification
- CI-exact gate: `python scripts/check_mypy_gate.py` (pinned clean venv) →
  `mypy gate: all 24 ratcheted codes at ceiling (total=1578; ...)` exit 0.
- `tests/test_architecture_enforcement.py` + `tests/test_completion_ratchets.py`:
  46 passed.
- Workflow's second invocation `test_routing_vocabulary_1283.py`: 12 passed.
- Touched-module tests: full `tests/unit/services/intent_service/` +
  file_resolver/file_scoring/inversion-gate: **3498 passed, 3 deselected**;
  document_analyzer + analyzer_factory: 28 passed.

## Discovered issues
- None filed. Two observations for Lead: (1) the gate was already red at 3caa87594
  (2026-08-16) — that commit adjusted ratchet ceilings but shipped over-ceiling in the
  same push, so CI-red predates the ~08-23 estimate; (2) local full-venv mypy counts
  don't match the CI gate (more imports resolve → more errors) — the gate is only
  meaningful under the pinned bare toolchain.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (worktree rules, evidence discipline, commit-message
  auto-close hazard — issue numbers written without `#`); e7939bb65's commit message
  (house style for mypy drift fixes: honest guards, never casts, CI-pin reproduction);
  `scripts/check_mypy_gate.py` docstring (ratchet semantics).
- **Loaded but not referenced**: memory index (MEMORY.md), mailbox protocol sections.
- **Wanted but not found**: a documented note that the mypy gate's counts are
  toolchain-sensitive (now recorded here and in the report to Lead).
