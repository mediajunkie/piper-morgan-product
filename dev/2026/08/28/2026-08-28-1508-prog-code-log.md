# Session Log — 2026-08-28 15:08 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Claude Fable 5
**Task**: Issue 1687 — diagnose (and fix where cheap) the four standing-red CI workflows: Code Quality, Docker Build, Configuration Validation, Router Pattern Enforcement
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)

## Diagnoses (all four)

### 1. Router Pattern Enforcement — stale gate config (FIXED)
- Failing step: "Check Direct Adapter Imports" (`scripts/check_direct_imports.py`), 8 violations, all in `services/mcp/consumer/notion_adapter.py` — the module that *defines* `class NotionMCPAdapter` (class statement, docstrings, log strings).
- First-red: green 18c12d5c5 (07-04 17:52Z) → red 4fde49bc9 (07-04 23:34Z). Range contains 5050ed024 "feat(notion): port onto the 1232 Connector contract" which moved the canonical adapter from `services/integrations/mcp/notion_adapter.py` (which IS in EXCLUDED_FILES) to `services/mcp/consumer/notion_adapter.py` (which was not).
- Fix: added the new defining module to EXCLUDED_FILES. Verified with exact CI invocation: `python3 scripts/check_direct_imports.py $(find services -name "*.py" -type f)` → "No direct adapter imports detected (545 files checked)". The workflow's grep steps (Verify Router Imports Present) also verified passing.

### 2. Docker Build — stale gate config (FIXED)
- Failing step: "Build Docker image" — `docker build -f ./services/orchestration/Dockerfile .` → `lstat services/orchestration: no such file or directory`.
- Two red eras: first-red overall was 12a4b1a77 (green, 03-16 17:17Z) → d8d872c63 (red, 03-16 17:23Z; the 905 requirements sync) — red 5.5 months. The *current* error dates from addb61c99 (1436 Tier-3 Family-2, orchestration island deleted).
- Fix: point at root `./Dockerfile` — which is the actual deploy artifact (`fly.toml: [build] dockerfile = "Dockerfile"`; v63 deployed from it today), so the gate now tests the real image.
- Verification layer (named): local docker lacks the buildx plugin, so the full build isn't reproducible here. Verified: legacy `docker build -f ./Dockerfile .` resolves the context/Dockerfile (the exact failing layer) and completes steps 1–5 (base pull, apt, COPY requirements.txt), dying only at the BuildKit-only `--mount` in step 6 — which CI provides via docker/setup-buildx-action@v3. Indirect: today's v63 fly deploy built this same Dockerfile remotely.

### 3. Code Quality — real drift (FIXED) + one real defect (FILED as 1689)
- Failing step: "Check formatting with ruff" (`ruff format --check --diff .`), 325 files drifted; behind it "Lint with ruff" had 124 auto-fixable I001 unsorted-imports.
- First-red: green 867c809d1 (04-23 04:12Z) → red fcd44c51d (04-23 04:16Z, the 992 ethics merge). Red 4 months; drift accumulated across ~325 files.
- Fix: `ruff format .` (324 files) + `ruff check --fix .` (116 I001) with the CI-pinned ruff 0.6.9. Re-verified: `ruff format --check .` → "1792 files already formatted"; `ruff check .` → "All checks passed!".
- Knock-on caught and fixed: the format rewrapped 5 long annotated `except Exception as e:  # silent-ok: ...` headers into multi-line form, moving the annotation off `handler.lineno` — silent-death ratchet went 198→203. Fixed `scripts/check_silent_death.py::_annotated` to scan the whole except header (recognition-only change; cannot hide an unannotated swallow). Count back to exactly 198 unannotated / 103 annotated.
- Remaining red, real and correct: native-dialog gate catches 2 post-baseline native dialogs (`templates/settings_github.html:590` alert, `templates/settings_llm_keys.html:161` window.confirm). Filed as issue 1689 — template dialog migration needs render-level verification, out of scope here. Code Quality stays red at that gate (later steps in the job) until 1689 lands; its ruff steps are green again.

### 4. Configuration Validation — false gate, ported from ci.yml (REPORTED, recommend PM ruling)
- Job-level: "Validate Service Configurations" job is GREEN; only the `behavioral-validation` job fails — "Invalid configuration incorrectly accepted", exit 1.
- First-red: green 836c5a188 (08-13 18:49Z) → red ac3a4ed44 (08-13 19:51Z). The range contains 64fac495c, which retired ci.yml and ported this behavioral test in — it has NEVER passed in this workflow.
- Root cause: the test targets `services/config_validator.py`, a CI stub (created 191187ed0, 2025-10-13, "fix(ci): Fix 3 failing CI workflows") whose `validate_all_services()` unconditionally returns `{"status": "ok"}` and `is_startup_allowed()` returns True. The invalid-config assertion is structurally unable to pass — and has been since 2025-10-13, including inside ci.yml. The port comment's claim that the audit "found it live-green inside the otherwise-red workflow" is refuted by the stub's content (m-44: the green was never a measurement).
- No fix committed: making the stub actually validate would be inventing validation logic; rewiring the job to the real validator (`services/infrastructure/config/config_validator.py`, different API, already covered by the green job) is test design, not repair. Recommendation to PM/Lead: remove the `behavioral-validation` job (and consider `delete-module-safely` for the stub + its only caller, `tests/performance/test_config_performance.py`) — a gate that can only fail while testing a stub nobody ships is a false gate.

## Verification summary
- `scripts/check_direct_imports.py` over all 545 services files: clean.
- `ruff format --check .` / `ruff check .` (ruff 0.6.9 = CI pin): both clean.
- Gates: token 0, principal 0, mailbox 0; native-dialog 1 (pre-existing, = issue 1689; identical before/after format).
- Ratchets: `tests/test_architecture_enforcement.py` + `tests/test_completion_ratchets.py` → 46 passed.
- mypy gate: full `check_mypy_gate.py` output byte-identical between HEAD tree and formatted tree (A/B via git-archive copy) — the format diff is proven mypy-neutral. (The gate is over-ceiling in THIS venv both before and after — pre-existing local-env-vs-CI-pinned-env discrepancy, same one the 1436 fix worked around with a clean pinned venv; not caused by and not touched by this work.)
- Smoke suite (CI's gating suite): `pytest -m smoke` → 542 passed, 1 skipped.
- Docker: see layer-named verification above.

## Discovered issues filed
- 1689 — native-dialog gate red on two post-baseline callers (evidence + introducing commits in the issue).

## Process note
- One self-inflicted scare: ran `git stash --keep-index` mid-verification, which stashed the entire 406-file working diff; recovered completely with `git stash pop` (diff stat re-verified identical). Lesson re-learned: stash is on the destructive-git list — should have used the git-archive comparison from the start (which is what the final A/B used).
