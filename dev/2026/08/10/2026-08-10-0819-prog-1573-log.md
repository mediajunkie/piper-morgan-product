# Session Log — 2026-08-10 08:19 — prog (Coding Agent) — #1573

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a403d606a85be9134 (isolated worktree; commit local only, no push)
**Task**: GH #1573 — context_assembler deadline-proximity naive-vs-aware TypeError swallowed as warning; pending todos vanish from floor context. Fix per #1491 pattern (normalize aware-UTC), un-swallow to logger.error + exc_info + counters, preserve #1425 honesty contract (source_failed). Sweep file for other naive-vs-aware comparisons. Failing-first test.

## Entries
- 08:19 — Session start. Log created (recreated 08:28 — initial heredoc write did not land on disk). Read audit doc F5/F7 + current context_assembler.py on this branch (post-#1570/#1566).
- 08:23 — Baseline (pre-change): 2,426 passed, 2 deselected (tests/unit/services/intent_service/ + tests/unit/services/intent/ + tests/test_architecture_enforcement.py, -m "not llm", env-stripped, lead venv). Matches the given 2,426 baseline.
- 08:24 — Failing-first: added TestPendingTodosNaiveAwareFix1573 (8 tests) BEFORE the fix. Result: 7 failed, 1 passed. Red includes the exact F5 TypeError at context_assembler.py:125 ("can't compare offset-naive and offset-aware datetimes") and the gather-vanish repro ("due-dated todo made the whole gather vanish" — _compute_pending_todos returned None via warning-swallow). The 1 pre-fix pass is the naive-vs-naive shape, which never raised. NOTE: the briefing said "naive-stored due-dated todo row (the live data shape)" — the audit (F5) and the red run show the live mismatch is the inverse: AWARE timestamptz due_date vs naive datetime.now(). Test uses the aware shape as primary repro, plus a naive-shape test (both green post-fix via ensure_utc).
- 08:26 — Fix implemented in services/intent_service/context_assembler.py + services/intent_service/conversational_floor.py:
  - `_compute_deadline_proximity` (audit :124-125): both sides aware-UTC via `ensure_utc(due_date)` + `utc_now()` (services/utils/datetime_utils, #1491 pattern). "Today" = UTC day until per-user tz exists (audit root cause / #586).
  - `_compute_pending_todos` (audit :1048-1050): `logger.warning`+None swallow → `logger.error(..., error, user_id, fetched_count, exc_info=True)` + return `{"source_failed": True}` (#1425 honesty — never fake-empty).
  - `_get_pending_todos_cached`: translates compute's `source_failed` → `{"pending_todos_source_failed": True}` (distinct key: the generic `source_failed` renders as a REMINDER-check failure on the floor).
  - `_gather_status_priority_context`: propagates the flag (its `"pending_todos" in pending_data` guard would have dropped it). Temporal path propagates via existing `context.update()`.
  - conversational_floor.py `_format_domain_context`: renders "Todo check FAILED: could not load the user's pending todos…" for the new key.
  - In-file sweep hardenings: `_parse_updated` (GitHub activity) and `_parse_event_time` (calendar activity) wrap `fromisoformat` in `ensure_utc` — an offset-less string would raise naive-vs-aware against the aware cutoff/now.
  - Test infra: `_freeze_noon()` now freezes module-level `utc_now` too (compound patch); wall-clock-offset proximity tests moved from naive `datetime.now()` to `datetime.now(timezone.utc)` fixtures (naive local fixtures reinterpreted-as-UTC would skew by host UTC offset — deterministic in any tz now).
- 08:27 — test_context_assembler.py: 78 passed (70 baseline + 8 new). Full specified suite running in background.
- In-file sweep results (context_assembler.py): :744 `now = datetime.now()` in `_gather_temporal_context` is strftime-only (display face, no comparison) — audit F3d territory, left as-is. Slack activity sites compare float epochs (safe). Four activity-window `datetime.now(timezone.utc)` sites already aware-correct. Milestones sort ISO strings (safe).
- Out-of-file residuals (F7, confirmed still present on this branch, NOT fixed here — report-only per task): services/database/repositories.py:2820 naive cutoff vs timestamptz; services/repositories/file_repository.py:114/132/151 naive cutoffs; services/intent_service/canonical_handlers.py "yesterday's accomplishments" naive SQL cutoffs (~:2707-2721 region) + the `datetime.now(dt.tzinfo) if dt.tzinfo else datetime.now()` idiom (:561/:3232/:3342/:3391/:3749); services/feedback/response_context.py:118/142; services/domain/user_preference_manager.py:161.

## Memory & briefing surfaces referenced this session
- **Referenced**: docs/internal/operations/time-handling-audit-2026-08-10.md (F5 site + F7 residual list — drove the fix and sweep scope); CLAUDE.md (env-stripped test invocation, evidence discipline, failing-first); services/utils/datetime_utils.py (#750 helpers — ensure_utc/utc_now); #1425 honesty contract (in-file comments at reminder gather — the pattern mirrored); #1156 freeze-noon test pattern (extended for utc_now).
- **Loaded but not referenced**: memory index (MEMORY.md); briefing role table.
- **Wanted but not found**: none.

## Discovered issues filed
- None (no `gh` in this sandbox). Out-of-file F7 residuals reported to caller for routing; they are already enumerated in the audit doc.
