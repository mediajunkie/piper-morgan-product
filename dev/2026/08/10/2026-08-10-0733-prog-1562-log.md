# Session Log — 2026-08-10 07:33 PT — Coding Agent (prog) — #1562 + #1566

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a0b17214918f623ec (isolated worktree; commit local only, no push/mailboxes/gh)
**Tasks**:
- #1562 — "today" + clock time silently becomes tomorrow (parse_reminder_time has no "today" branch; past-roll overrides explicit day word; server-clock past-check). Fix: explicit today-branch, honest-ask on genuinely-past, keep bare-clock roll-forward. Also "(scheduled for at 9:41am)" copy doublet in todo_handlers.
- #1566 — due reminders surface only on CONVERSATION-category turns (context_assembler gates _gather_reminder_context on category == "CONVERSATION"). Fix: gather for every floor-bound category; verify floor renderer; align save-confirmation copy.

## Work

- 07:33 — Session start. Read temporal_utils.py, context_assembler.py, todo_handlers.py, conversational_floor.py, intent_service._assemble_guidance_context + the 1490/1542/1425/1491 suites.
- 07:40 — Verified both diagnoses:
  - #1562: `parse_reminder_time` has no "today" branch; the generic clock branch (whose own comment claims "today at 9:41") rolls past times +1 day silently; past-check on server clock. Confirmation copy renders "(scheduled for at 9:41am)" — label already starts with "at".
  - #1566: `gather_context` gathers `_gather_reminder_context` ONLY under `category == "CONVERSATION"` — AND a second, worse break: `_format_domain_context` in conversational_floor.py never rendered `due_reminders`/`reminder_count`/`source_failed` at all, so even CONVERSATION turns dropped the gathered keys before the LLM saw them. GUIDANCE uses a separate `_assemble_guidance_context` that also never gathered reminders.
- 07:43 — Failing-first RED evidence (before any impl change):
  - `TestTodayExplicitDayWord1562::test_pm_verbatim_binds_today_when_time_is_future`: `AssertionError: assert 'at 9:41am' == 'today at 9:41am'` (no today branch)
  - 1566 assembler: `due reminders missing from STATUS context` (+ PRIORITY/TEMPORAL/MEMORY/IDENTITY/DISCOVERY/TRUST/UNKNOWN)
  - 1566 renderer: `assert 'check in with the Lead Developer' in ''` (floor renders nothing)
  - Full red run: 31 failed, 7 passed (the 7 = pre-existing behavior kept: CONVERSATION gather, bare-clock roll, possessive "today's", empty renders)
- 07:46 — Implementation:
  - temporal_utils.py: `PAST_TODAY_PREFIX`, `_TODAY_WORD_RE` (excludes "today's"); today+clock branch (binds TODAY; past → `(None, past-today:label)` honest-ask; server-tz limitation noted, #1535/#747); bare "today" no clock → `(None, "today")` ask instead of tomorrow-9am default; bare-clock branch keeps roll-forward.
  - todo_handlers.py: past-today ask copy ("…has already passed on my clock. Did you mean tomorrow?…"); `schedule_label` strips leading "at" (kills "(scheduled for at 9:41am)"); promise copy → "I'll surface this in conversation once it's due."
  - context_assembler.py: reminder gather hoisted OUT of category dispatch — rides every gather_context call in its own try; CONVERSATION branch now documented no-op; `_KEY_SOURCES` entries for due_reminders/reminder_count.
  - conversational_floor.py: `_format_domain_context` renders DUE REMINDERS block (cap 5 + m-44 "…and N more") + honest "Reminder check FAILED" line for source_failed (#1425 preserved).
  - intent_service.py `_assemble_guidance_context`: gathers reminder context too (GUIDANCE pathway bypasses the assembler dispatch).
  - Tests updated to new semantics: 1490 today rows frozen-now (were time-of-day dependent), matrix frozen at 08:00; test_reminders today-label test → "today at 4pm"; pattern-073 copy pin → new copy.
- 07:47 — GREEN: targeted suites 182 passed. Full battery (tests/unit/services/intent_service/ + tests/unit/services/intent/ + tests/test_architecture_enforcement.py, -m "not llm", env-stripped, lead venv): **2391 passed, 2 deselected, 0 failed**.

## Discovered issues filed
None (no `gh` in this worktree). One noted gap for the report: reminders still do not surface on action-rail turns (canonical handlers never run context assembly) — copy softened accordingly rather than filed, per task instruction.
