# Session Log — 2026-08-10 07:37 — prog (Coding Agent) — #1411b live-path fix

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: .claude/worktrees/agent-a7522efe509ce8959 (isolated; commit local, no push)
**Task**: #1411 LIVE FAIL despite green pinned tests — find the real live path for
"change the status of issue #108 to in progress" (production got DOCUMENT handler copy),
wire the Stage-0 explicit-#N seam onto that path. Shared seam per #1555 rule; no
pre-classifier regex additions (moratorium). Failing-first via the real live entry.

## Plan
1. Read docs/internal/architecture/current/intent-routing-stack.md (MANDATORY)
2. Trace real entry: web POST chat → IntentService → classifier entry actually used
3. Reproduce PM's failure keylessly via direct execution
4. Failing-first test through real entry; fix via shared seam; agreement test hash vs no-hash
5. Full env-stripped test run, zero regressions

## Entries
- 07:37 — session start, log created.
- 07:40 — read intent-routing-stack.md (mandatory). Traced live entry:
  POST /api/v1/intent (web/api/routes/intent.py:300, calls intent_service.process_intent
  at :489) → IntentService.process_intent (services/intent/intent_service.py:513)
  → _process_intent_internal → intent_classifier.classify_multiple (:1271, session_id
  as own kwarg) → Stage 0 _resolve_issue_referent FIRST (classifier.py:1009). The
  Stage-0 seam IS on the live path — the entry-point hypothesis was wrong.
- 07:43 — root cause found by direct execution (keyless surface probe):
  PM's 08-10 sentence says "STATUS"; `_ISSUE_FIELD_WORDS` (classifier.py:87) had
  only "state". Stage 0 declined BOTH forms of the status sentence. From there:
  no-hash → surface-1 doc pattern `change …[\w\s]+ to` claims it (update_document_query
  @1.0, deterministic); hash → '#' breaks that span → fell to LLM classifier (surface 2),
  which in production chose the document lane. The pinned suite's PM_MSG is the 08-09
  "title" sentence — probe-shape ≠ live-shape at the VOCABULARY level, not the seam level.
  PM's asymmetry (no-hash "title of issue 108" worked; hash "status" doc-claimed) is
  title-vs-status, surfaced as hash-vs-no-hash.
- 07:45 — live-entry keyless repro via IntentService().process_intent:
  hash status → INTENT_CLASSIFICATION_FAILED (LLM leg — misroute NOT keyless for this
  form; said so per brief); no-hash status → update_document_query @1.0 (keyless repro
  of the doc lane); "change the title of issue 108" → update_issue (matches PM's
  "repository not specified" observation).
- 07:46 — failing-first: added PM's status sentences to test_explicit_issue_update_1411.py
  detection params (RED: None != 108) + new live-entry suite
  test_explicit_issue_update_live_entry_1411.py driving the REAL process_intent with
  LLM boundary explosive + GitHub boundary stubbed (RED: explosive tripped at
  classifier.py:1121 self.llm.complete — proof the LLM leg decided). Agreement tests pin
  hash/no-hash forms route identically (both sentence pairs).
- 07:47 — FIX at the shared seam: added "status" to `_ISSUE_FIELD_WORDS`
  (classifier.py) — the ONE constant both detectors (_detect_issue_referent and
  _detect_explicit_issue_update) compile from; no copied branch (per #1555 rule), no
  pre-classifier pattern (moratorium respected). All targeted suites green (48), live
  probe now routes all three sentences to update_issue @1.0.
- 07:48 — full mandated run env-stripped: tests/unit/services/intent_service/ +
  tests/unit/services/intent/ + tests/test_architecture_enforcement.py -m "not llm":
  **2360 passed, 2 deselected, 0 failed**. Zero regressions.

## Discovered issues
- OBSERVATION (probe env only, unconfirmed): re-running the probe against the same
  session_id served msg-1 a response with `floor_hit: True` and stale floor copy while
  intent_data showed the freshly-routed update_issue — possibly a response-cache /
  turn-hydration artifact of the dirty local Redis/DB from earlier dev-server runs, not
  reproducible in the unit harness (fresh sessions). Not filed (no `gh` in this lane;
  weak evidence) — flagged in report for Lead to triage.
- NOTE for #1555: tests/conftest loads ANTHROPIC_API_KEY from Keychain, so "-m not llm"
  runs are not key-free; the explosive-LLM boundary mock is the real no-LLM guard.
- NOTE for #1555: "milestone" is likewise absent from `_ISSUE_FIELD_WORDS`; the
  milestone-divergence case wants the same shared-vocabulary treatment when #1555 is
  fixed (reported, not implemented — out of scope per brief).

## Memory & briefing surfaces referenced this session
- **Referenced**: docs/internal/architecture/current/intent-routing-stack.md (the
  4-surface chain + Stage-0 placement — decisive for the trace); CLAUDE.md verify-first
  + evidence discipline; #1555 shared-seam rule (from brief); pre-classifier moratorium
  (from brief + classifier.py comments).
- **Loaded but not referenced**: memory index, Amber worktree/hook sections of CLAUDE.md
  (isolated worktree lane, no mailbox/push).
- **Wanted but not found**: a written note anywhere that PM's 08-10 transcript sentence
  used "status" — the brief asserted the exact sentence was in the pinned suite; it was
  not (the suite pins the 08-09 "title" sentence). One diff of the two sentences was
  the whole diagnosis.
