# PPM Session Log — 2026-08-10 (Monday)

**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` — job `e4f3b4e8`, CronList-verified exactly one at START

---

## Fire 1 (07:22, START)

Heartbeat `START`. 0 behind, 0 unpushed. Inbox **4**.
✅ **Step 0 backward check passed** — 08-09 carries its `DAY-CLOSED` sentinel.

**Owed (named trigger)**: merge the first-contact criterion — §7a's *discipline* + #1536's *coverage*
into one list. Neither is canonical-ready: §7a's binary-only constraint excluded AC2/AC3/AC4; my ACs
were never audited and carry two holes (AC2 unfalsifiable; AC3 scoped to the empty state).

**Fire 1 work — the owed merge, delivered as the fire's first item.**
`docs/internal/product/first-contact-criterion-merged-2026-08-10.md`

**CXO withdrew the inversion's reasoning**, taking my point: *"I used a correction count as a
defect-density measure. Corrections are evidence of attention, not of fault."* ⭐ **And they paired it
with my earlier finding**: *convergence is not importance* and *corrections are not defects* — **both
counting proxies mistaken for the thing they proxy.**

**⭐ The structure was already in CXO's own spec and the argument walked past it**: **§7a = binary
gate; §7b = conformance, required for done, beyond the gate.** So this was never pick-a-list — it's
**per-property placement.** ① GATE · ② **§7b** (a judgment; cannot fail cleanly) · ③ GATE by citation.

**⛔ AC4 deleted, not placed.** *"Works from the first session, not after warm-up"* is **entailed by
item ①**, which already says *cold account* and *first exchange*. **CXO's placement table carried it
as a fourth gate row.** Three items, not four.

**🔴 Item ③ cites Arch's contract instead of restating it — CXO's catch, and it's the good one.** My
fix to their item 4 and the fix to my AC3 were **the same predicate**, which would have made it
**guard six** in the same week Arch documented *"we have solved fabrication five times, never
generally."* ⭐ ***One predicate closing two holes in two documents isn't elegance — it's the signal it
belongs in neither.***

**⚠️ But I checked whether the contract reaches the case rather than assuming it does because it's
general. It may not.** `floor-honesty-contract-1517-spec.md` **H1 governs whether a READ OCCURRED;
item ③ needs whether the named ENTITY IS REAL.** **A read can occur and the particulars still be
fabricated** — fetch the issue list, hallucinate a title onto it: **H1 satisfied, item ③ violated.**
Secondarily H1's enumeration is **storage-flavoured** while first-contact fabrication concerns an
**external connected source**. **Flagged to Arch as a consumer they may not have counted.**

**Deleted the owed block from the cron prompt the same fire it completed** — the rule I earned
yesterday, now written into the prompt as a rule rather than a one-off. Cron `e4f3b4e8` → `1ab65620`,
CronList-verified one. Also added two standing lines: **corrections are not defects**, and **check a
general contract actually reaches your case before citing it as closing your hole.**

### Fire 2 (10:01, WORK) — item ③ unblocked; my flag was half a misreading; routed the pattern

Heartbeat `WORK`. Cron `1ab65620`, one. Inbox 3 → 0.

**✅ Arch answered the block I raised this morning, and I got half of it wrong.**

**Wrong half — mine**: I objected that H1's enumeration was *"storage-flavoured"* (saved/exists/deleted
= Piper's own persistence). ⚠️ ***exists / doesn't exist* is literally in H1's list.** I read it as
save-state because the enumeration **leads** with *saved / not saved*. **Arch: *"a named entity IS a
state claim."*** They fixed their wording rather than asking anyone to patch around it.

**Right half — the real objection**: H1 governs *whether a read occurred*; item ③ needs *whether the
named entity is real*. **Arch's carrier closes it**: propositions carry a `StateFact(read_at, source)`,
**entities carry a CITATION** — ***you cannot cite a read you did not perform.*** ⭐ **Because
citations are PER-ENTITY, a fabricated `#1234` has nothing to cite even when a genuine read of the
issue list happened in the same turn.** *(The per-entity property is what makes it hold; a per-turn
citation would let a fabrication ride on a real read. Noted as implementation-level, Arch's/Lead's.)*
**Item ③ gates on *every entity named in a user-facing claim carries a citation*. No fourth patch.**

**⚠️ And I left a stale summary line for an hour** — "What PM would be blessing" still said *"item 3
is pending one architectural answer"* after the block cleared. **Third time in three days I've left a
summary contradicting its own updated body** (Arch's map 08-08, my register 08-08, this). **Fixed, and
the correction says so.** *The edit that updates a body and not its summary is apparently my
signature defect.*

**⭐ Routed the counting-proxy pattern to CIO rather than endorsing it.** Arch: *"worth a pattern
entry… CIO's catalog, not mine to file, but I'd support it."* ⚠️ **Three of us now agree it should
exist and none had filed it — the shape where a good finding dies of mutual endorsement.** Sent CIO
the assembled material (my *"unaudited is not sound"* + CXO's *"I used a correction count as a
defect-density measure"* + Arch's *"vacuity family applied to artifact SELECTION"*), with the family
it belongs to — **convergence≠importance, corrections≠defects, aggregate≠per-category, total≠parts**
— **each found separately, by a different role, in a different artifact.**
**The specific danger of this one: it operates at SELECTION time**, deciding which document becomes
canonical, **so it propagates into everything downstream before anyone reviews content.**
⛔ **Did not draft it** — CIO's catalog and numbering; offered to if they'd rather.

### Fire 3 (13:01, WORK) — FTUX five ruled, hold ended; #1511 spec filed

Heartbeat `WORK`. Cron `1ab65620`, one. Inbox 9 → 0.

**✅ THE FTUX FIVE ARE RULED and my hold state ends.** **#1536 → MVP + Beta Blockers; #1537–#1540 →
Production + PUB.** **Verified on the board** rather than take the relay. **PM deferred explicitly to
CXO's position** — *"if CXO feels that one of the issues should be kept in MVP, I am inclined to defer
to that and err on the side of including"* — and **CXO's on-record argument was exactly one issue.**
**PM resolved the tension CXO named by clarifying their own earlier statement**, not by us
adjudicating it: ***"out of alpha" = the PUBLIC beta.*** ✅ **#1536 now gates on the converged three.**
**Awaiting-decision population 7 → 2.**

**✅ #1511 is my spec lane** with PM direction. **Accepted Lead's MVP/Production slice rather than
redoing it**, plus three additions:

**1. The MVP slice is a DISAMBIGUATION, not a feature — which shrinks it.** The issue's own title is
the finding: ***"Two standups wear one name."*** **Same family as production/trust/Notion/primary.**
**The interview already exists and works** — ⚠️ **anything touching its behaviour is OUT of the MVP
slice; it isn't broken, it's unaddressable.**

**2. 🔴 The merge: #1511's preference capture IS #1510's declared mode in a different domain.** Lead
spotted the shared **storage**; **the mechanism matters more — how a preference is declared, how it's
REVOKED, and how the user sees what Piper believes about them.** *Two surfaces inventing two
revocation stories is how you get a preference nobody can find to change.* **Per Arch's own line: a
property re-derived per surface wants one home.** Ordering already right — #1510 is MVP, this half is
Production.

**3. ⚠️ The first-run fallback risks PM's OWN anti-goal** (*"I don't want to dictate how people
work"*): asking *"what standup do you want going forward"* on first run **asks at the moment of least
information**, and a guess then becomes a decision. **Demonstrate then ask** (the #1536 principle) and
**make it visibly revisable** — ⭐ *an unfindable preference is the dictating the anti-goal is about,
arriving by accident.* Also counted the mechanisms against *"don't overcomplicate"*: four already;
**resisted a fifth (no per-user standup templating — different product).**

**Also landed**: CIO **filed the counting-proxy pattern as m-48**, with the provenance note included
*"because it is part of the finding."* **Routing it beat endorsing it.**

### Fire 4 (16:01, WORK) — ruled CXO's two flagged items; the audit is converging; a mail-send silently failed

Heartbeat `WORK`. Cron `1ab65620`, one. Inbox 2 → 0.

**CXO restructured §7a to the ruled three and flagged two items from their original list rather than
letting them vanish** — *"a restructure is exactly where an author quietly re-adds their own
preferences."* **Tested both the way AC4 was tested.**

**(i) *"no request for scope before the reading"* — ⛔ REDUNDANT with item 1.** Both directions:
- **Scope request BEFORE data** → no data in the first exchange, **item 1 already fails.** (i) adds no
  gating power, only a *reason*.
- **Scope request AFTER data** → *"Here are your 12 open issues. Which repo?"* → **item 1 passes, and
  CXO's wording correctly lets it pass.**
⚠️ **The second case is the one to pause on: that is the behaviour we WANT** — *demonstrate, then
ask*, the #1536 principle I argued for on #1511 this morning. **A looser (i) that dropped "before the
reading" would gate against the thing we're building.** CXO's wording avoids it; a future editor's
might not. **Ruled: diagnostic note ON item 1, not a fourth gate item** — ⭐ *a criterion that only
fires when another already fires is a label, not a gate*, and it would inflate apparent thoroughness
(**Arch's phantom-denominator point about AC4**).

**(ii) *"an offer or an opinion"* — ✅ §7b, agreed.** Two flags: it's **more binary than item 3**, and
*being more binary than the least binary item isn't the bar*; and 🔴 **it may be #1539's binary
shadow** — a status reduces no uncertainty, an offer does. **So (ii) may be to #1539 what the citation
is to AC2.** Flagged so it isn't built twice; **not proposing to move it** (#1539 is Production, this
criterion serves MVP).

**⭐ Put on the record that the audit is CONVERGING**, because *"still finding things"* and *"finding
smaller things"* look identical from outside and only the second means stop soon:
**R1 three missing properties → R2 a fabrication hole → R3 two holes of mine → R4 AC4 redundant →
R5 (i) redundant.** **Three properties down to one label.**

**Credited CXO's proximity finding** — *"I never wrote that the storefront finding was H1. I put it in
a memo that opened by taking the H1 clarification… **proximity does attributive work.**"* ⭐ **Same
family as my own repeated defect** (a summary contradicting its updated body): **the document
asserting something its sentences don't.**

**⚠️ OPERATIONAL — a mail-send FAILED and the failure was visible only in its output**: `mail-send:
fetch origin/main failed`. **The memo did not send.** Verified origin was reachable, confirmed the
memo had NOT landed, resent, then **checked for a double-send: one commit, nine recipient copies,
clean.** ⭐ **A transient fetch failure produces an unsent memo with no other signal** — the sender
sees nothing unless they read the tail. **Worth knowing: "I sent it" is a claim about output I may
not have read.**

**Also landed**: **Lead shipped the #1511 MVP slice** (`feat(standup): name-addressable interview via
token branch`) — **my spec from three hours earlier, built the same afternoon.**

**🔴 And the mail-send failure led somewhere else — a 21-memo misfiling I'd never have found otherwise.**

Chasing the two stranded triage deletions, I compared local and main and got **inbox = 0 locally, 23
on main.** That didn't add up, so I stopped rather than "fixing" it.

**Cause: a nested `mailboxes/ppm/inbox/read/` directory**, created 2026-07-26 by commit `729f4ed27` —
**a PPM triage that used `inbox/read/` instead of `read/`.** My globs (`inbox/*.md`) never saw it;
**anything counting recursively would have reported 21 phantom unread for PPM.**

**21 files. Only 2 overlapped the real archive — so 19 memos existed ONLY in a directory nobody
reads**, dating to 07-10/07-12 (Arch on #1386 scenarios, ADR-078 ledger feasibility, and others).
**Not lost — in git, and out of reach.**

✅ **Checked whether it was cohort-wide before fixing: PPM only.** *(State the denominator — it would
have been tempting to file this as a mailbox-tooling defect on the strength of one instance.)*

✅ **Repaired**: 19 moved to `mailboxes/ppm/read/`, 2 duplicates dropped, nested dir removed,
MANIFESTs regenerated. **Verified on main**: nested dir **0 files**, `read/` **768 → 787** (768+19 ✓),
inbox **0**.

⭐ **The chain worth noting**: a transient `fetch origin/main failed` → an unsent memo → two stranded
deletions → a count that didn't reconcile → **a three-week-old misfiling**. **I only got there because
the mail-send printed a failure I actually read, and because I refused to reconcile a discrepancy by
assuming.** *The failure was the useful part.*
