# Session Log — 2026-08-10 07:54 — prog (Coding Agent) — #1570

**Role**: Coding Agent (prog)
**Tool**: Claude Code
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a05803576ef2a4387 (isolated worktree)
**Task**: #1570 [MVP] two-headed floor-query defect from PM live testing
  (a) "[Available context: no todo data returned this turn]" leaking into user copy
  (b) empty todo/project data on floor turns (principal threading suspect)
  (c) "show me my archived projects" capability denial

**Constraints**: commit locally only; no push, no mailboxes/, no gh.

## Work

- 07:54 — Session start. Reading intent-routing-stack.md (mandatory first read).
- 08:10 — Diagnosis complete (direct execution + code trace):
  - **(a) Annotation source: MODEL-ECHOED, not template-rendered.** The literal string
    "[Available context: no todo data returned this turn]" exists NOWHERE in the codebase
    (grep across services/ + web/, all string families). The only carrier is the floor's own
    prompt machinery: `conversational_floor.py` names "[Available context]" ~11 times in
    FLOOR_SYSTEM_PROMPT_ADDENDUM (L74-135, L232) and injects bracketed scaffolding blocks in
    `_build_prompt` (L529-562: "[Reference binding: ...]", "[Context: ...]", "[Redirect
    context: ...]") + `_format_domain_context` (L573: "[Available context about ..."). #1393
    (commit 67538afe7) already added a prompt-side prohibition (L82-85) — PM's live transcripts
    prove instruction alone doesn't hold. Class-kill goes renderer-side: strip bracketed blocks
    opening with OUR OWN scaffolding headers from floor output (carrier fix, not phrase-list —
    the header family is defined by our prompt builders).
  - **(b) Empty-data mechanism (todos lane)**: "what todos are pending?" is NOT claimed by the
    pre-classifier (verified by direct execution: `PreClassifier.pre_classify` → None) → LLM
    classifier → paraphrase action missing the rail's 3 todo-read keys (mode 4) → category
    routing (QUERY) → `_handle_generic_query` → `_handle_unknown_intent` — which NEVER calls
    ContextAssembler (`domain_context=None` default, intent_service.py L12483-12533). The floor
    gets zero user data on this entry, structurally. user_id IS threaded post-#1394 (recovery at
    L12511) — principal threading is NOT the hole; the missing gather is.
  - **(b) projects lane**: both offer-acceptance paths converge on the same hole —
    pending-offer dispatch returning None → `_handle_unknown_intent` (L979-993, no context);
    contextual-offer "yes" → hint to classifier → unrailed emission → generic → same entry.
  - **(c) archived projects**: "show me my archived projects" IS deterministically claimed at
    surface 1 — but by STATUS/get_project_status (probe: conf 1.0), NOT PORTFOLIO. The
    PORTFOLIO list pattern `\b(?:show|list|view)\s+(?:my\s+)?(?:all\s+)?(?:archived\s+)?projects\b`
    rejects the "me" token ("show ME my archived projects"); STATUS_PATTERNS' `\bshow.*projects\b`
    + `\bmy projects\b` then claim it. STATUS floors always (`_requires_canonical_handler` L12123
    → False) → floor with status context (no archived data) + no manifest key → denial. The
    #1431 branch (canonical_handlers.py L4070) is unreachable for this phrasing. Disposition:
    rail key (sanctioned #1560 pattern) + corpus report for the pattern half (moratorium).
- Plan: failing-first tests → (A) renderer-side scaffolding strip; (B) gather_context at
  `_handle_unknown_intent` when caller supplied no domain_context (floor-ENTRY-independence,
  the #1566 pattern one level up); (C) `list_archived_projects` rail key, effect=READ
  (+manifest coverage via wired_chat_actions derivation).
- 08:12 — Failing-first evidence (all three red for the diagnosed reasons):
  - (a) `test_floor_scaffolding_strip_1570.py` — ImportError: `strip_scaffolding_artifacts`
    does not exist (respond() returns LLM text verbatim).
  - (b) `test_floor_entry_context_1570.py` — "floor received NO domain context on the
    generic-query entry" (`FloorContext(..., domain_context=None, ...)` captured).
  - (c) `test_archived_projects_rail_1570.py` — `'list_archived_projects' not in` the 106
    rail keys.
- 08:15 — Implemented:
  - **(A)** `conversational_floor.py`: `strip_scaffolding_artifacts()` + `_SCAFFOLDING_BLOCK_RE`
    (headers: available context / context: / reference binding / redirect context — the exact
    family our prompt builders define). Wired into `respond()` after `llm.complete`, with a
    `floor_scaffolding_stripped` WARNING log so prompt-discipline regressions stay visible.
    Full-scaffolding responses fall back to an honest non-empty line. Carrier fix, not a
    phrase list: content paraphrases pass through; only the bracketed machinery grammar is
    unrenderable.
  - **(B)** `intent_service.py::_handle_unknown_intent`: when the caller supplies no
    domain_context, run `ContextAssembler.gather_context(category, user_id, session_id,
    action)` (+ provenance threading, #1030 R4 parity). Caller-curated context (#1187
    summarize) skips the gather; gather failure degrades to the pre-#1570 contextless floor.
    This covers BOTH live lanes: generic-QUERY fall-through (todos) and the offer-acceptance
    fallback (projects).
  - **(C)** `workflow_entries.py`: `run_archived_projects_query_workflow` (self-contained,
    owner-scoped `PortfolioService.list_archived_projects`, mirrors the #1431 canonical
    branch) + WorkflowEntry(effect=EffectClass.READ, action_triggered=True) registered under
    `list_archived_projects` / `show_archived_projects` / `archived_projects_query` /
    `list_archived`. Manifest coverage verified (wired_chat_actions lists it exactly once).
  - **Corpus report (c pattern half, #1559 moratorium respected — no pre-classifier regex
    touched)**: 3 rows added to `tests/fixtures/routing_corpus_1283.yaml` — "show me my
    archived projects" (REVIEW; today STATUS/get_project_status, verified by direct
    execution), "list my archived projects" (regression pin, PORTFOLIO), "what projects have
    I archived?" (falls to LLM, expected action:list_archived_projects — now railed).
  - Doc sync: `intent-routing-stack.md` surface-3 count 106→110 + surface-4 note (both floor
    doors gather; renderer-side scrub).
- 08:16 — Green: 28/28 new tests pass; full suites `tests/unit/services/intent_service/ +
  tests/unit/services/intent/ + tests/test_architecture_enforcement.py -m "not llm"`:
  **2,426 passed, 2 deselected, 0 failures** (baseline 2,398 + 28 new). Dispatch-site
  ratchet untouched (no new elif; MAX_DISPATCH_SITES unchanged).

## Discovered issues filed
- None filed (no `gh` in this sandbox). Two candidates for Lead to file/route:
  1. Pre-classifier pattern gap: PORTFOLIO list pattern rejects the "me" token, so
     "show me my ..." project phrasings are claimed by STATUS — corpus rows added; pattern
     work is moratorium-gated.
  2. `_gather_status_priority_context` surfaces projects only via `_get_user_context_cached`
     (user_context_service), not the `projects` table read the TEMPORAL gather uses — worth a
     coherence look if STATUS floor turns still under-report projects.

## Memory & briefing surfaces referenced this session
- **Referenced**: `docs/internal/architecture/current/intent-routing-stack.md` (mandatory
  read — 4-surface chain, mode-4 taxonomy, #1560/#1521 precedents); CLAUDE.md (env-strip
  prescription, verify-first, failing-first discipline); commit 4293d8a4a (#1560, the
  sanctioned rail-key pattern mirrored); `test_floor_history_principal_1394.py` +
  `test_reminder_surfacing_1566.py` + `test_create_reminder_rail_1560.py` (test-shape
  templates); memory pin `feedback_verify_at_the_right_layer` (m-43 layer-honesty notes in
  the new test docstrings).
- **Loaded but not referenced**: mailbox/worktree/sign-off discipline sections (isolated
  worktree, no push); Amber keychain notes (this venv's conftest loads keys fine).
- **Wanted but not found**: the live LLM classifier's actual emission for "what todos are
  pending?" — no way to run the classifier deterministically; diagnosis is at the
  structural layer (any unrailed emission floors contextless), stated as such.
