# Session Log — 2026-08-10 08:07 — prog (Coding Agent) — #1568
Model: Fable 5 (claude-fable-5)
Branch: worktree-agent-a7526ca4d0780889c (isolated worktree; commit local only, no push)

## Task
GitHub #1568 [MVP] — finish /todos page:
1. Inline title editing (Edit → input + Save/Cancel → PUT, honest error path); remove coming-soon stub
2. Priority chip (TodoPriority low/medium/high/urgent)
3. Humanize ISO due dates (aware-ISO from server → browser toLocale*, date-only for midnight-UTC)

## Plan
- Verify-first: read todos.html, todos.py routes, 1548 PUT tests, 1512 template tests, TodoPriority, toast keys. DONE.
- Key contract finding: PUT /api/v1/todos/{id} takes title as a QUERY param (no Pydantic body).
  A JSON body {title} would be silently ignored → #1541 lie pattern. Page must send ?title=.
- Failing-first: new render tests (tests/unit/templates/test_todos_edit_priority_dates_1568.py) red
  before template change; route-level TestClient test proving the page's query-param PUT shape
  resolves into the real route (autospec repo).
- Then implement template; full suite env-stripped.

## Entries
- 08:07 session start, investigation complete (files above)
- 08:11 FAILING-FIRST evidence: new render suite red against current template —
  `10 failed, 4 passed` (all 10 template.render() tests failed:
  coming_soon stub present, no edit input, no PUT shape, no priority chip,
  no CSS coverage, raw ISO dates, no toLocale*, no midnight-UTC branch).
  The 4 passing were the route-level call-shape tests (TestClient + autospec
  repo) — correct: #1548 already fixed the route; they pin the contract the
  page adopts (query-param title; JSON body silently dropped → the trap).
- 08:13 template implemented (templates/todos.html):
  * editTodo/cancelEditTodo/saveTodoTitle inline flow; coming-soon stub removed.
    Save PUTs `?title=${encodeURIComponent(title)}` (query param — the route
    has NO body model; a JSON body would be silently dropped = #1541 lie).
    Honesty path: success toast ONLY after response.ok; on failure the row
    STAYS in edit mode and ToastMessages.error('update_error', detail) fires;
    network error → error toast; empty title → validation_error, no request.
  * priority chip `priority-chip priority-${priority}` in the title row;
    CSS: low/medium share the subtle secondary treatment, high amber,
    urgent error-red — classes are the real TodoPriority .value strings.
  * formatDueDate(): aware-ISO → browser toLocale* in user tz (audit's
    23-correct-sites pattern). Midnight-UTC = all-day-ish → date only,
    read in UTC (so the picked day never shifts west of UTC); timed values →
    local date + time; unparseable → raw string (beats "Invalid Date").
  Targeted suites (new + 1512 + 1541 template + 1548 route): 31 passed.
- 08:14 full mandated suite env-stripped (tests/unit/web/ +
  tests/unit/templates/ + tests/test_architecture_enforcement.py,
  -m "not llm" -p no:cacheprovider): **1609 passed, 0 failed** — zero
  regressions.

## Discovered issues filed
None (no `gh` in this sandbox; nothing discovered warranting one — note for
Lead: renderTodos still interpolates `${todo.text}` unescaped into the h3,
pre-existing; the new edit input DOES escape via escapeAttr()).

## Files
- templates/todos.html (edit flow, priority chip + CSS, formatDueDate)
- tests/unit/templates/test_todos_edit_priority_dates_1568.py (10 render tests)
- tests/unit/web/api/routes/test_todos_put_call_shape_1568.py (4 route tests)
