# Session Log — 2026-08-10 08:16 — prog (Coding Agent) — #1578

**Role**: Coding Agent (prog)
**Model**: claude-fable-5 (Fable 5)
**Branch**: worktree-agent-a636142c8c212daea (isolated worktree; commit local only, no push, no gh)
**Task**: GitHub #1578 [SECURITY] — escape all dynamic interpolations in templates/todos.html renderTodos + sibling render functions (stored XSS via shared todo titles).

## Plan
1. Read current todos.html state (post-#1568: inline edit, priority chip, toLocale dates — do not disturb)
2. Enumerate ALL interpolation sites in the client-side render path
3. Check Dialog component title handling (`Share ${resourceName}`) — innerHTML vs textContent
4. Check for JS runtime harness (jest/node) — verify, don't assume
5. Failing-first tests (source-level pins naming the layer honestly)
6. Fix with escapeHtml()/escapeAttr() applied consistently; shareTodo stops passing todo.text through onclick JS-string context (HTML-escaping cannot protect that layer)
7. Full suite: tests/unit/web/ + tests/unit/templates/ -m "not llm" (1,570 baseline, zero regressions)

## Work
- 08:16 log created; todos.html read (633 lines, #1568 edits present)
- 08:18 Interpolation sweep of the full client-side render path (renderTodos, renderCurrentShares, openShareModal): ~20 sites, three contexts (HTML text, HTML attribute, JS-string-inside-onclick). Dialog title verified a textContent sink (dialog.js:78,338) — `Share ${resourceName}` safe by sink.
- 08:19 JS harness VERIFIED to exist: tests/frontend/ (jest 29 + jsdom, own package.json/jest.config.js; loads web/static/js sources). node_modules absent → `npm ci` succeeded; baseline 94 passed. (Root package.json is the Next.js site — unrelated.)
- 08:20 Failing-first tests written:
  - tests/unit/templates/test_todos_xss_escaping_1578.py — SOURCE layer, docstrings state "verifies the JS source escapes, not runtime DOM". Includes an innermost-interpolation ratchet sweep over renderTodos + renderCurrentShares.
  - tests/frontend/unit/todos-page-xss.test.js — RUNTIME DOM layer (jsdom): extracts the inline script from the template, evals with real permissions.js, renders hostile todos.
- 08:22 Failing-first evidence: jest 7 failed / 1 passed (the pass = #1568's already-escaped edit input); pytest failed at first assert (escapeHtml missing; suite maxfail=1).
- 08:25 Fix applied to templates/todos.html:
  - escapeHtml() added (escapes & < > " '), escapeAttr() now delegates to it (superset of its #1568 behavior)
  - every HTML text/attr interpolation wrapped: todo.text (title branch), owner_username||owner_id, lifecycle_state + phrase (attr+text), priority (class+label), role badge class + roleDisplay, status, formatDueDate output, edit-input id, share list (user_email||user_id, role badge/label), addShare/removeShare onclick args
  - JS-string-in-onclick context: HTML escaping cannot protect it (entities decode before JS parses) — so todo.text REMOVED from Share onclick; shareTodo(todoId) now resolves the title from currentTodos. Only server-generated ids/types cross the onclick boundary (escapeAttr'd for the attribute layer; provenance note added in the file).
  - #1568 work (inline edit, priority chip, toLocale dates) untouched — its suite still green
- 08:26 Post-fix: new pytest file + todos neighbors = 45 passed; full jest = 102 passed (94 baseline + 8 new)
- 08:27 Full regression: tests/unit/web/ + tests/unit/templates/ -m "not llm" -p no:cacheprovider → 1592 passed (baseline 1570 + 22 new), zero regressions. Env-stripped via `unset` (sandbox refused the `env -u` wrapper form).

## Discovered issues filed
- None (no `gh` in this sandbox). Candidate for Lead: templates/files.html has its own inline render path with local isOwner/canDeleteFile — worth the same interpolation sweep (not touched here; out of #1578 scope).

## Wrap
- Commit local only per constraints (no push, no mailboxes, no gh).
