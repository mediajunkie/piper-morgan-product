# Session Log — 2026-08-10 15:49 — Coding Agent (prog) — #1536 FTUX-COLDSTART

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a647924dac3dc4786 (isolated worktree)
**Task**: GitHub #1536 [MVP, Beta Blockers] — FTUX-COLDSTART: user's own data in the FIRST exchange, unprompted.
**Constraints**: commit locally; NO push, NO mailboxes/, NO gh.

## Plan
1. Read design spec §7a + surroundings (dev/active/design-spec-first-contact-plugin-surface-2026-07-31.md)
2. Read intent-routing-stack.md
3. Read context_assembler.py + conversational_floor.py (heavily modified today: #1566, #1570, #1517, #1571)
4. Identify first-exchange assembly path; IntegrationStatusService (#1547); feed_factory binding-first provider (#1547-F4); #1327 default-repo rail
5. Failing-first tests, then implement first-contact gather
6. Full test run env-stripped; commit

## Entries
- 15:49 — Session start. Log created.
- 16:05 — Verify-first investigation complete. Findings:
  - **First-exchange paths**: (a) pure pleasantry greeting → canonical `_handle_conversation_query` → `ConversationHandler._respond_to_greeting` → `format_greeting_conscious` (deterministic, NO ContextAssembler); (b) everything else floor-bound → `_handle_floor_with_context` / `_handle_unknown_intent` → `ContextAssembler.gather_context` (both pass session_id).
  - **Page auto-greeting**: client-side static JS in `templates/home.html` (`initializeGreeting()` — "Good afternoon, Name · date"). NOT an assembled turn — never touches the intent stack. Honest layer naming: the first *assembled* exchange is the user's first chat message.
  - **First-exchange signal**: `process_intent` hydrates turns from DB when empty (#1122) then records the in-flight turn BEFORE routing — so at handler/gather time, a new conversation has ≤1 turn and no turn with `response is not None`. That's the honest signal.
  - **Connector source**: `IntegrationStatusService.is_configured(user_id, "github")` (#1547, binding-first, Demo structurally excluded).
  - **Data read**: `resolve_repo(user_id=...)` (#1042/#1327 default-repo rail; UnresolvedRepoError → no demo, never "which repo?") + `GitHubIntegrationRouter.get_open_issues(owner=, repo=)`; item dicts carry number/title/updated_at/is_pull_request. `WorkItemProvider` (feed_factory, #1547-F4) conflates error→[] with empty→[], so a dedicated compute keeps failure distinct (#1425 fail-honest).
  - **Design**: new `services/intent_service/first_contact.py` — `is_first_exchange`, `gather_first_contact_demo` (cached #984, TTL 300), deterministic `render_first_contact_block` (pure string formatting over payload = structurally cannot name entities outside it), `first_contact_demo_block` for the canonical greeting seam. Assembler rides it outside the category dispatch (the #1566 pattern); floor renders `first_contact_demo` inside the `[Available context]` block with a name-ONLY-these directive (#1570 strip + fabrication guard as allies). Empty read → NO demo ([] conflates adapter-swallowed errors with genuine zero; asserting emptiness would be an unverified stored-state claim, m-44).
- Next: failing-first tests (`test_first_contact_1536.py`), then implement.
- 16:00 — **Failing-first evidence**: wrote `tests/unit/services/intent_service/test_first_contact_1536.py` (29 tests) BEFORE any implementation. Red run: `1 failed — ModuleNotFoundError: No module named 'services.intent_service.first_contact'` (conftest maxfail=1 stopped after the first; every test was red-by-construction — module absent → import failure; the floor-formatter tests were red because `_format_domain_context({"first_contact_demo": ...})` returned `""` with no handler).
- 16:01 — Implementation:
  - NEW `services/intent_service/first_contact.py`: `is_first_exchange` (fail-closed; ≤1 turn, none completed), `gather_first_contact_demo` (#1547 status gate → #984 cache TTL 300 → `_compute_first_contact_github`: resolve_repo (#1327 rail; UnresolvedRepoError → None, CXO item (i)) → router read (fail → source_failed, #1425) → recency-ranked top-3 + row-derived open_count (m-44)), `_humanize_recency` (lived time, #1573 ensure_utc), deterministic `render_first_contact_block`, `first_contact_demo_block` (greeting seam).
  - `context_assembler.py`: first-contact rail outside category dispatch (the #1566 pattern), own try, provenance-attributed; `_KEY_SOURCES` rows added.
  - `conversational_floor.py::_format_domain_context`: `first_contact_demo` demonstration directive (name-ONLY-these, no scope question, marker-free copy per F8) + `first_contact_source_failed` couldn't-check line.
  - `conversation_handler.py`: `_respond_to_greeting` appends the deterministic block; principal fallback fix (`context.get("user_id") or user_id`) in both `respond` and `_respond_to_greeting`.
  - `intent-routing-stack.md` surface-4 row updated in the same commit (consult rule).
- 16:03 — **Green**: new suite 29/29. Full run: `tests/unit/services/intent_service/ tests/unit/services/intent/ tests/test_architecture_enforcement.py -m "not llm"` → **2510 passed, 2 deselected, 0 failed** (env-stripped, lead venv). Adjacent greeting/conversation suites: 79 passed.

## Discovered issues filed
- None filed (no `gh` per constraints). Candidates noted for Lead: (a) `WorkItemProvider` (feed_factory) conflates read-failure with empty ([] both ways) — same m-44 class the first-contact compute had to route around; (b) `ConversationHandler.respond`/`_respond_to_greeting` were silently DROPPING the caller-threaded user_id when intent.context lacked one (fixed here as part of principal threading, but other context-over-param overrides in that file may deserve an audit).

## Memory & briefing surfaces referenced this session
- **Referenced**: design-spec-first-contact-plugin-surface-2026-07-31.md §3/§4/§7a (gate items, boundedness-inside-the-claim, empty-state rule); intent-routing-stack.md (floor doors, consult rule → same-commit doc update); CLAUDE.md verify-first + m-43/m-44 (layer naming for the page auto-greeting; empty-read honesty); #1566/#1570 in-file comments (rail placement pattern); #1425 honesty pattern; #984 cache pattern; #1279 router close; census F8 (no internal markers in prompt copy).
- **Loaded but not referenced**: memory index entries (Amber ops, mailbox discipline — N/A for isolated worktree task).
- **Wanted but not found**: the #1536 issue body itself (no `gh` allowed — worked from the spec §7a as directed).
