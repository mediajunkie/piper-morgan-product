# Session Log — Coding Agent (prog) — 2026-08-20

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: claude-opus-5 (Claude Code)
**Branch**: `claude/lead-cycle` (Lead's own Model-A worktree, per delegation)
**Task**: #1667 / #1595 Inversion Phase 2 — flip unit onto the rail entry, flag semantics widened, coverage audit

---

## Binding inputs read first

- `dev/active/inversion-phase2-kickoff-plan.md` — especially the final section,
  "FLIP-UNIT DESIGN DECISION (#1667, Lead, 2026-08-20)". Implemented as decided; not re-litigated.
- `services/intent_service/inversion_live.py` (flip-1's four-condition dispatch + flag resolution)
- `services/intent_service/workflow_dispatcher.py` (`WorkflowEntry`), `workflow_entries.py`
  (effect/outwardness declaration style — mirrored exactly)
- `gh issue view 1667`

## What shipped

1. **`WorkflowEntry.flip_group: Optional[str]`** declared beside `effect`/`outwardness`, with
   `FLIP_GROUPS` (closed wave-1 vocabulary) above the dataclass. `__post_init__` rejects, loudly and
   at construction: (a) an unknown group name (a typo is otherwise doubly silent), (b) **any
   flip_group on a non-READ entry** — so a flippable write is unconstructible, not merely guarded.
2. **Wave-1 assignments**, each with its reasoning in the comment above it: `read_status` (50 keys),
   `read_referent` (18), `read_synthesis` (4). 72 of 93 rail READ keys grouped; 21 deliberately
   ungrouped (temporal hold: `changes_query` + calendar cohort; class-mismatch: `strategic_planning`,
   `learn_pattern`, `prioritize`, `generate_content`).
3. **Flag semantics widened** in `inversion_live.py`: `PIPER_INVERSION_LIVE_CATEGORIES` (name kept)
   resolves group names, individual operation names (incl. canonical alias), and registry categories.
   New `resolve_live_match()` (reports WHICH surface matched) + `unrecognized_flag_tokens()` (a typo'd
   wave name is now loud). Four dispatch conditions unchanged; default-empty still zero-work.
   Ops flipped by group/op-name with no registry category get `IntentCategory.QUERY` on the Intent —
   documented as shape-required, chosen after the READ guard, and dispatch-irrelevant (#1124 rail
   dispatches on action before category).
4. **`--audit` mode** on `scripts/inversion_phase2_gate.py` (sibling of `--dry`, no LLM): denominators,
   per-group membership, the full unassigned list BY NAME split into "reachable only by naming the op"
   vs "still swept by its registry category", plus a re-measurement of the READ-only invariant.

## Finding (reported, not silently corrected)

`--audit` re-ran #1667's own measurement and got **33 of 93** READ keys addressable by a category flag,
not 23. Cause: the decision counted ACTION_REGISTRY's *direct* action names; the live path
(`_category_by_operation`) also back-maps through `grammar.alias_to_canonical`. Both numbers are now
printed side by side with the mapping each measures. The decision's conclusion is unaffected (60 of 93
still unreachable by category).

## Evidence

- `tests/unit/services/intent_service/test_inversion_live_1595.py` — 28 passed, **unchanged**
- `tests/unit/services/intent_service/test_inversion_flip_groups_1667.py` (new) — 33 passed
- Combined inversion + `test_architecture_enforcement.py` + `test_completion_ratchets.py` — 146 passed
- Whole `tests/unit/services/intent_service/` — 3376 passed, 3 deselected
- `intent-routing-stack.md` updated in the same commit (mandatory per CLAUDE.md)

## Discovered work

- Telemetry bucket names `category_not_live` / `no_registry_category` are retained for corpus
  continuity but now under-describe the widened check (they say nothing about the group surface).
  A rename is a corpus migration, not a code change — filed for the Lead to prioritize.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-stripped test invocation, evidence discipline, m-43/m-44 framing);
  `dev/active/inversion-phase2-kickoff-plan.md` (binding decision); `intent-routing-stack.md`
  (mandatory before touching routing).
- **Loaded but not referenced**: mailbox/sign-off sections (subagent scope — no push, per delegation).
- **Wanted but not found**: nothing.
