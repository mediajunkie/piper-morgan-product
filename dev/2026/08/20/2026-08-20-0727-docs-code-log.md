# Docs Session Log — 2026-08-20

**Role**: Documentation Management (Docs) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` ·
website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`

---

## Fire 1 (07:27, the 06:57 slot) — START

**Step 0**: verified 08-19 closed properly — `<!-- DAY-CLOSED: 2026-08-19 -->` present at column 0
in `dev/2026/08/19/2026-08-19-0711-docs-code-log.md` (an initial glob-quirk false-negative was
re-checked directly against the file and confirmed present; no self-heal needed).

`CronList`: one job (`f6271697`, STOP re-arm from last night), matches expression. Session log
for today did not yet exist — created per this fire. Registry row current (`f6271697` already
recorded from last night's STOP re-arm, verified rather than re-written).

Sync clean. Cohort-freeze check (START-only step): `rc=0`, ordinary wake — window [03:28..07:28],
6/6 scheduled fires emitted, no cohort-wide gap.

Inbox: 0 unread. Today's queued post, **"The Dead Code That Wasn't"** (pubDate 2026-08-20), is
still `status=drafted`, notes say "Needs PM voice-pass + art" — not chasing, same pattern as
Ship #056 yesterday morning before PM engaged directly. No day-of-week trigger for Thursday
(not Monday/first-Monday/Friday/first-Tuesday).

Heartbeat: START (unconditional write, `dev/heartbeats/2026-08-20/docs.tsv`).

## PM engaged — "Did you get the memo about yesterday's Ship missing an illustration?" + today's post ready

PM's opening question referenced last night's hero-image 404 fix (Fire 5, 08-19 log) — checked
whether PM's own inbox copy of my confirmation memo had been read yet: still sitting unread
(`mailboxes/xian (ceo)/inbox/2026-08-19-docs-ship-056-054-hero-image-404-fixed.md`), so PM was
asking cold rather than having seen it. Re-verified fresh rather than just repeating last night's
claim: pulled every image reference directly from Ship #056's live `blog-content.json` entry
(the actual render source, not the static HTML shell) — exactly one, `alpha-launches.webp`, HTTP
200. Answered PM directly in-conversation with the full account (both fixes, the #054 discovery,
website#33 filed) rather than just pointing back at last night's memo.

**"The Dead Code That Wasn't"** — PM: "ready for proofreading and publishing." Draft frontmatter
was filled (image/alt/caption) though the calendar row's cartoon/altText/caption fields hadn't
been synced — read the draft file directly rather than trust the calendar's lagging fields.
Pre-flight verified the image file actually exists on disk (2.6MB PNG, added same day). Full
template-audit: 14/14 clean, including footer-tease target algorithmically verified against the
calendar (correctly targets "The Trust Gate That Wasn't," Aug 22) and acronym sweep clean (one
advisory ROLE-GLOSS note, correct per the first-person-narrative register convention).

**Fact-checked the piece's load-bearing numeric claims** against the primary Lead Dev logs the
drafting notes cited (07-16/07-17/07-18), not just trusting the notes' own prior verification —
all exact verbatim matches: census figures (1,233 broad excepts / 244 core-path / 274 classified /
66 unswallow), the `protocol/` near-deletion catch (absolute-import-only sweep missing a live
relative-import consumer, restored pre-commit, sweep method fixed), the classifier `NameError`
pre-flight-gate catch, and the "a day earlier" sequencing between the two catches (protocol/ catch
on 07-18, NameError catch on 07-17 — confirmed one day apart, not just asserted).

Dry-run clean. Published: `the-dead-code-that-wasnt`, hashId `1d60801a6bdf`, workDate 2026-07-16,
pubDate 2026-08-20. Website commit `4916b0d`.

**Caught and fixed a real defect during the calendar update**: copied the frontmatter's YAML-
escaped caption (`"Don''t cut the live wood!"`, doubled apostrophe — correct only inside
single-quoted YAML) straight into the CSV caption field without normalizing it. Caught by
checking convention against other rows with apostrophes in captions (all use a single `'`) before
moving on, not after — fixed to `"Don't cut the live wood!"` in the same fire, before commit.

Calendar updated (status→published, blogURL/blogPath/cartoon/altText/caption), whole-file scan
clean (430 rows). Draft + image archived. Product commits `53e8b8e91` (archive rename — same bad-
pathspec pattern as yesterday's Ship #056 archive, where the compound `git add` command names both
new and nonexistent-old paths and aborts before staging the real changes; caught via `git status`
immediately after, not assumed clean) + `82caa008d` (calendar metadata, completing the drop).

**Live-content-verified**: unlike this morning's Ship #056 check (client-rendered shell, no body
content in raw HTML), this post's page **does** serve real content server-side — confirmed title,
body text (`protocol/`), and the corrected single-apostrophe caption all present in the actual
served HTML, not just the source commit.

Heartbeat: WORK.

## Fire (22:27, the 21:57 slot) — STOP: mail — frontmatter-image defect reframed + root-caused

**Mail loop, 1 direct memo**: a Medium-syndication report with a second, more significant finding
— PM had flagged today's post's frontmatter `.png` filename 404ing as a likely third instance of
yesterday's Ship #054/#056 hero-image bug. **Independently re-verified before accepting either
framing**: pulled Dead Code's actual `blog-content.json` content and confirmed zero images
embedded in it — the frontmatter filename never appears anywhere in the live rendering, so today's
post was never actually broken. The reporting session had gone further and measured the whole
corpus (81 published posts) before writing: **0 of 81 frontmatter `image:` values match a
deployed asset, 81 of 81 are `.png` while every deployed asset is `.webp`** — a 100%-structural
naming convention (the field is the pre-conversion source name `publish-post.js --image`
consumes), not a spreading bug. "Third instance" was the wrong frame from the start.

**Root-caused and fixed at the actual source**, not just the two symptom sites: the
`draft-weekly-ship` skill's Step 4c explicitly instructed drafters to pull the Ship hero-teaser
image URL "verbatim from frontmatter" — that sentence is what produced both real breakages.
Corrected (`b4838696d`): alt text and caption stay verbatim (they render as-is, never the
problem); the image URL is now derived from the post's own slug, with a required live-HTTP-verify
step before shipping a draft. Also updated `piper-morgan-website#33` with the corrected diagnosis
(comment posted) — my own original filing yesterday implied "audit the frontmatter values," which
would have been the wrong fix (breaking the field's actual job across 81 files); reframed the
issue's remaining scope to the publish-time link-check alone.

Applied the requested calendar update after independently verifying current state (Medium URL,
status→distributed) — whole-file scan clean (430 rows), committed `c1ee4a571`.

**Replied to PM** (who raised the original "third instance" alarm) with the corrected account —
`mailboxes/xian (ceo)/inbox/2026-08-20-docs-frontmatter-image-defect-corrected-framing.md`
(`da58db002`, push-to-ref, source memo triaged to read/, own MANIFEST regenerated).

**Stale-cleanup dry-run**: 0 cycle-log files ≥7 days, 0 `.tmp` files ≥1 day. Nothing to remove.

Cron re-armed via delete-then-create: `f6271697`→`67b8f23f`, `CronList`-verified sole job,
registry row current. Heartbeat: STOP.

### Day-arc summary — 2026-08-20

Opened on a genuine START (verified 08-19's close marker properly, session log created) with a
quiet cron-and-mail check — today's queued post was still awaiting PM's voice-pass, not chased,
same discipline as the day before. PM engaged directly with two threads at once: a question about
last night's hero-image fix (re-verified fresh rather than repeating the prior claim, since the
confirmation memo was still sitting unread in PM's own inbox) and a request to proofread and
publish "The Dead Code That Wasn't." The publish itself was clean — 14/14 template checks, four
load-bearing technical claims fact-checked verbatim against primary Lead Dev logs — with one
small self-caught defect along the way (a YAML-escaping artifact nearly carried into the CSV
caption field, caught by checking convention against other rows before committing).

The day's more consequential thread arrived at STOP: PM read today's publish as a possible third
instance of yesterday's hero-image bug. Rather than accept that framing or dismiss it, checked
directly — today's post was never actually broken, and the report that prompted PM's concern had
already measured the real shape underneath: 81 of 81 published posts share the same frontmatter-
image-name-never-matches-deployed-asset pattern, by design, not by drift. The two real breakages
weren't a spreading bug; they were the one place — the Ship's hand-built hero teaser — where a
skill instruction told drafters to copy that frontmatter value verbatim into a public URL. Fixed
at that instruction directly (not just the two symptom sites), corrected the framing on the
tracking issue so a future guard gets built against the right shape, and closed the loop with PM
holding the corrected account rather than the alarmed one.

**Pattern across both PM-engaged threads today**: each time, the instinct was to re-verify fresh
rather than lean on a prior claim (even my own from the night before) or accept an incoming report's
framing at face value — the same discipline that found the actual root cause was a skill
instruction, not a scattered bug, and that today's post was never broken at all.

### Sign-off checklist

```
git status --short   → clean (after this commit)
git fetch origin main && git rev-parse --verify -q origin/main   → resolves
git log --oneline origin/main..HEAD   → empty (after this commit lands)
```
Nothing stranded. All work today reached `origin/main` same-fire, not batched to sign-off.

## Memory & briefing surfaces referenced this session

**Referenced**: `template-audit` skill (14-check audit on Dead Code, applied fresh); CLAUDE.md's
"never guess at facts" — governed both PM-engaged threads (the illustration re-verification, the
frontmatter-defect reframing); the `update-calendar` skill's csv-module-by-name + whole-file-scan
discipline, applied on 3 separate calendar edits today; the `draft-weekly-ship` skill, read fully
before editing Step 4c rather than patching blind — found the exact sentence that caused both
breakages, not just the symptom.

**Loaded but not referenced**: cross-pollination brief.

**Wanted but not found**: nothing material. The frontmatter-image-URL defect is worth a memory
pin or methodology candidate if it recurs after today's fix — "the field named for the thing looks
like it should hold the deployed reference, but its actual job is upstream of that" is a shape
worth naming if it shows up again elsewhere in the pipeline.

<!-- DAY-CLOSED: 2026-08-20 -->
