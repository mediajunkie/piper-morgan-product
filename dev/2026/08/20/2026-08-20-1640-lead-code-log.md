# Lead Developer session log — 2026-08-20 (Thursday)
Role: Lead Developer (lead-code) · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron 28fef16b `17 6,9,12,15,18,21 * * *` (expires ~8/25)

## ⚠️ MODEL CHANGE MID-SESSION (PM, 2026-08-20 ~16:40)
**Fable 5 → Opus.** PM: weekly Fable credits exhausted (reset tonight ~22:00); Opus has headroom.
Recording per CLAUDE.md's header rule. **Three duty-cycle fires (06:31 / ~09:17 / ~12:17) fired
into the blocked window and produced no work** — honest gap, not a silent one: no heartbeat, no
log entries, no mail drained for ~10 hours. Resuming at 16:40 with one consolidated fire.

## Fire (16:40) — RESUME on Opus
State at resume: inbox ZERO, 61 commits behind (cohort churn), v60 live, Phase 2.2 scaffold
merged dark. Queue head per carry-forward: #1667 flip-2 coverage prep (banked last night with
"tomorrow's fires" as the named trigger — trigger has arrived).

**#1667 — measured before building, and the measurement changed the design.** The issue said "a
few ops"; the actual number is **70 of 93 rail READ keys have no ACTION_REGISTRY category** — the
category-keyed flag addresses 23 of 93. Rejected bulk-registering 70 ops into ACTION_REGISTRY (a
canonical-vocabulary subsystem bent to serve routing policy, absorbing 70 rows of pressure for an
unrelated reason). DECIDED: the flip unit is declared ON THE RAIL ENTRY (`flip_group`), exactly
where effect/outwardness already live (#1509 precedent), and the flag accepts group names OR
single op names OR registry categories. Ungrouped = unaddressable BY DESIGN, with the audit
output required to LIST the unassigned so it's never a silent remainder (m-44 — the same shape as
everything else this month: a mechanism reporting coverage it didn't have). Decision written into
the kickoff plan; mechanics delegated.

## ~17:1x — #1667 CLOSED; the build corrected MY figure

Merged + closed. flip_group on the entry (non-READ + group is UNCONSTRUCTIBLE, not merely
guarded); flag accepts group/op/category; wave-1 groups assigned with every borderline call
reasoned in-code; `--audit` names the 21 unassigned READ keys INCLUDING the 4 that a category
name still sweeps in — the m-44 clause made real. 61 targeted / smoke 542 / ratchets 46, push as
its own call after reading (discipline holding).
**The agent caught an error in my decision doc**: my "23 of 93 addressable" counted direct
registry names only; the live path back-maps through alias_to_canonical, so the governing figure
is 33/93. Conclusion unchanged, but I corrected the doc VISIBLY (kept the wrong number and said
why) — a decision doc that edits its own evidence silently is the exact shape we keep catching in
the product. Good catch by a subagent on its own Lead; worth more than the fix.
Filed #1670 (telemetry bucket names under-describe post-widening; corpus migration) and #1671
(pytest -o addopts drops pytest.ini's archive ignore → false red; our own run-sweep does it).
Phase 2 state: flip-2 prep DONE; next flip is a config change on PM's word.

## Fire (18:31) — #1671 fixed personally; inbox zero, 0 behind

Took the false-red trap myself rather than delegating: a broken instrument undermines every
verification claim in the project, and it was my own run-sweep.sh triggering it. Repro'd first
(24 archive tests collected under an addopts override), fixed at the layer no flag can reach
(conftest collect_ignore_glob) rather than patching the caller — patching run-sweep fixes ONE
caller and leaves the trap armed for every other one-off run. addopts fast path kept. Pinned,
because deletion is the realistic rot and CI would never see the symptom. Batteries 3376/542/46,
push as its own call. #1671 CLOSED. The ruff I001 in the same issue is pre-existing — deliberately
NOT smuggled into a test-infra commit.

## Fire (21:47) — day close: readiness page written, #1668 banked WITH its design call

Inbox zero, 12 merged (housekeeping). Chose prep over build at this hour, and said why: #1668's
real answer is REPURPOSE (shadow computes the legacy counterfactual on inversion-routed turns —
converting a wasted call into the flip's safety instrument), not a skip-guard; that's the
instrument the flips lean on, so it gets a fresh session, trigger named. Recorded the lean ON the
issue so it can't be reopened cold and quietly get the cheap version.
Wrote dev/active/inversion-flip-1-readiness.md: current state (dark by construction), what one
flip actually changes (rail/handlers/consent untouched; four conditions), the RECOMMENDED first
flip (read_status — failure mode is 'wrong list shown', never 'wrong thing written'; explicitly
NOT the QUERY category, which sweeps 4 ungrouped calendar ops the audit names), what to watch in
harm order, the one-line revert (unset — no code change, nothing to unwind), and both honest gaps.
PM's first-flip decision now costs a minute instead of an archaeology session.
Day totals (Opus half): resumed after a 10h blocked gap owned honestly · #1667 measured
(70/93 → design decision → merged, and the build corrected MY figure, visibly) · #1671 false-red
trap in our own instrument fixed at the flag-proof layer · #1670/#1671 filed · readiness page.
