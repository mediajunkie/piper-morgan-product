# Session Log — 2026-08-14 15:39 PDT — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, reporting to Lead Developer)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
**Task**: #1568 (todos inline edit + humanized dates) and #1615 formatting half (first-contact demo run-on). Commit locally; no push, no issue closure, no mailboxes.

## Verify-first finding: #1568 already shipped

Investigated before building (the flywheel's first move). #1568's full scope —
inline title edit wired to the real PUT (query-param shape), coming-soon stub
removed, honest error via `err.detail`, `loadTodos()` re-fetch on success
(refresh-survival by construction), priority chip (scope addition), humanized
due dates (UTC-safe per #1572 caveat) — was already built and merged to main:

- `4217b5843` feat: finish /todos page — inline title edit, priority chip, humanized due dates (1568)
- `362b4ef35` fix(security): escape every client-side interpolation on /todos (1578)
- Tests already in place: `tests/unit/templates/test_todos_edit_priority_dates_1568.py`
  (10 render pins incl. honest-error toast + PUT query-param shape),
  `tests/unit/web/api/routes/test_todos_put_call_shape_1568.py` (4 real-repo
  route pins incl. the JSON-body-silently-dropped trap),
  `tests/frontend/unit/todos-page-xss.test.js` (jest).

Built nothing for Fix 1 — duplicating it would have been the anti-pattern the
verify-first rule exists for. Verified the suites green instead (below).

## Fix 2 — #1615 formatting half (built this session)

**Root cause, layer named**: `render_first_contact_block` joined `•`-prefixed
lines with single `\n`. The chat frontend renders success messages through
`marked.parse` (`web/assets/bot-message-renderer.js:66`, default options), and
markdown collapses single newlines inside a paragraph → PM's run-on line.
Determination: **emit markdown** (evidence: `renderBotMessage` line 63–66
parses content with `marked` for type 'success'; greeting responses flow
through it via `element.innerHTML = renderBotMessage(...)`).

**Changes**:
- `services/intent_service/first_contact.py` (~line 262): block now emits a
  markdown list — blank line after the intro, `- ` items, blank line before
  the closing question. Display-only; payload/gather untouched.
- `services/intent_service/conversational_floor.py` (~line 720): the floor
  reply is LLM-composed, so the lever there is the directive — it now
  instructs "Present the items as a markdown bullet list, one item per line —
  never inline in a sentence." (No `#1615` marker in prompt copy — census F8,
  and it would trip the entities-subset-of-payload fabrication pin.)
- New pins: `tests/unit/services/intent_service/test_first_contact_demo_formatting_1615.py`
  — 9 tests: exact input→markdown-block pin, no-`•`-glyph pin, one-line-per-item,
  paragraph-break-before-list (the marked-breaks-it-out condition), singular
  noun, empty-payload nothing, floor-directive instructs bullet list, #1536
  guarantees untouched. Failing-first: exact-block pin red pre-fix, all green post.

**Deliberately NOT built**: the day-part / elapsed-focus-time half of #1615
(#1572-gated — needs the user's clock). Issue stays open. Also left the floor
prompt's internal `    • #N` item lines as-is (LLM input, not user-facing).

## Evidence

- New suite: 9/9 green (red-first confirmed on the exact-block pin).
- First-contact family (1536 + 1590 + 1615): 39 passed.
- `tests/test_architecture_enforcement.py`: 41 passed (no dispatch-site growth).
- Full `tests/unit/templates/ + tests/unit/web/ + tests/unit/services/intent_service/`:
  **4354 passed**, exit 0 (env-stripped, `--import-mode=importlib`).
- Jest (`tests/frontend`): 7 suites, **112 passed**.
- No new try/except (silent-death ceiling untouched); no String-column or
  user_id surfaces touched; no pre-classifier/routing changes (the floor edit
  is copy inside an existing directive, not routing).

## Discovered issues filed

None.
