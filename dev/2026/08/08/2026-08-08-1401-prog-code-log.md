# Session Log — 2026-08-08 14:01 — prog (Coding Agent, Claude Code)

**Role**: Coding Agent (Task-tool subagent, dispatched by Lead)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: isolated agent worktree (piper-morgan-product), branch `worktree-agent-a3c913c502a708e75`
**Task**: #1522 false-trails cauterization STEP 1 — disarm fix-magnets in LIVE files (S1 home.html excision, C1 app.py comment, S2 places.py docstring, live-file sweep). No gh (quota). No push, no mailboxes.

## Precondition finding

`docs/internal/operations/false-trails-audit-2026-08-08.md` (the §Tier-1 /
§cauterization-order source) does NOT exist — not on `origin/main` (worktree
verified 0-behind), not in the lead worktree, not in any sibling agent
worktree. The audit agent's output evidently hasn't landed. Proceeded on the
dispatch prompt's enumerated scope, which is self-contained and matches the
lead session log (`dev/2026/08/08/2026-08-08-0647-lead-code-log.md`).

## S1 — home.html legacy renderer excision (render-test-gated)

**What renders (verified before cutting)**: home extends
`layouts/app_shell.html` → includes `components/nav_rail.html` (populated by
`web/static/js/nav.js`, #1280 conv-list-everywhere) = the visible conversation
list; `components/history_sidebar.html` (own `.history-item` renderer) = the
archive slide-out. home's own `aside#sidebar` was display:none'd by #1280 —
hidden-not-removed. `archiveConversation`/`deleteConversation`/`editConversationTitle`
callers: ONLY the dead renderer's onclick strings (nav rail and history sidebar
have no delete/archive actions; #1482's Dialog.confirm copy lived inside the
dead `deleteConversation`). So the whole legacy surface was excisable.

**Excised** (templates/home.html, 2003 → ~1409 lines): the #1280 hide rule,
all sidebar CSS (`.sidebar*`, `.new-chat-btn*`, `.conversation-list`,
`.conversation-loading`, `.no-conversations*`, `.conversation-group-header`,
`.conversation-item` family, `.conversation-title*`, `.conversation-actions`,
`.conversation-*-btn`, `.conversation-meta`, media-query `.sidebar`), the
hidden `aside#sidebar` + expand-button markup, and dead JS: `toggleSidebar`,
`sidebarCollapsed`, `renderConversationList`, `renderConversationItem`,
`formatConversationDate`, `escapeHtml`, `editConversationTitle`,
`updateConversationTitle`, `archiveConversation`, `deleteConversation`,
`loadMoreConversations`, initSidebar's collapsed-restore,
setActiveConversation's `.conversation-item` highlight loop,
createNewConversation's sidebar-refresh call. Tombstone comments point at the
live surfaces.

**Kept (live)**: `loadConversations` (simplified to data-only; returns
most-recent id for #583 auto-select; chat.js calls it typeof-guarded),
`initSidebar` (?new=1 + auto-select), `switchConversation` (+#1418 guards),
`setActiveConversation`, `createNewConversation`, `clearChat`.

**Test pins updated** (each pinned the dead surface):
- `test_home_sidebar_surface_1.py` — rewritten as the GATING render test:
  real jinja2 `render()` of home.html, rail + history includes survive,
  dead surface stays dead (regression pin), auto-select fetch survives.
- `test_delete_copy_honesty_1482.py` — home.html removed from SOFT_SURFACES.
- `test_home_history_render_1489.py` — specific-string apostrophe pin removed
  (regression CLASS still covered by the broader parse pin).
- `test_native_dialog_migration_1170.py` — home section repointed at live
  toasts (load_error/create_error) + no-native-dialog pin.
- `test_app_shell_migrations_1171.py` — home marker "New Chat" → "chat-inline".

## C1 — web/app.py #1427 comment

Reworded to name `services/api/todo_management.py` as the unmounted module
explicitly and `web.api.routes.todos` as the live mounted one (git history
verified: mounted by 8528a8d, unmounted by 7de9c4011).

## S2 — places.py docstring

Corrected: panel consolidated into Radar (#1236, PlaceEntitySource), route has
no UI caller (verified: zero template/JS references to `place_window.html` or
`/api/v1/places`), keep-or-delete tracked in #1522.

## Sweep — other live-file false pointers fixed

- `web/app.py` Places mount comment ("What I'm seeing" panel backend → retired).
- `services/intent_service/todo_handlers.py` docstring (names the unmounted vs
  live todos modules).
- `tests/unit/web/api/routes/test_places_1192.py` renderer-contract comment.
- `tests/unit/templates/test_place_window.py` — dead-twin notice header.

**Discovered (step-2 candidates, not filed — no gh per quota constraint;
listed for Lead)**: `web/static/js/home-modules.js` is itself dead (zero
includers; serves the #1236-retired ambient modules); `place_window.html` +
its test suite are deletion candidates.

## Evidence

`pytest tests/unit/web/ tests/unit/templates/ tests/test_architecture_enforcement.py`
(env-stripped, lead venv): **1531 passed, 0 failed** (22.56s).
Template suite alone: 894 passed.

## Wrap

Committed in worktree (no push, per dispatch). Discovered issues filed: none
(gh embargo — step-2 candidates listed above for Lead to file/route).
