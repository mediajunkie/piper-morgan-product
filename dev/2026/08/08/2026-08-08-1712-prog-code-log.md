# Session Log — 2026-08-08 17:12 — Coding Agent (prog-code)

**Role**: Coding Agent (Task-tool subagent, dispatched by Lead)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: `.claude/worktrees/agent-a762f33d280ce0a31` (isolated; no push, no mailboxes per brief)
**Task**: #1480 — Slack deep-link params (#1466) don't survive an unauthenticated visit; login redirects to '/' unconditionally.

## Work

- **Diagnosis** (17:12): traced the full login round trip.
  - `services/auth/auth_middleware.py:_unauthorized_response` built `/login?next={path?query}` **unencoded** — the #1466 deep-link carries TWO query params, so the raw `&` split `slack_team_id` out of `next` into a stray `/login` param (server-side data loss before the client even ran).
  - `web/static/js/auth.js:43` — the primary drop point: `window.location.href = '/'` unconditionally on successful login.
  - `web/api/routes/ui.py:login_page` — already-authenticated visits to `/login?next=…` redirected to `/` (dropped next).
  - `web/static/js/chat.js` — #1520 (`handleSessionExpired` fallback) and #840 expiry redirects went to bare `/login` (no next).
  - Fragment note: `#link-slack` never reaches the server; `templates/settings_slack.html` `initSlackLink()` already auto-scrolls (`card.scrollIntoView()`) when slack params are present, so path+query fidelity is the server-side contract. auth.js additionally re-attaches the browser-carried hash.
- **Failing-first** (17:13): wrote `tests/unit/services/auth/test_login_next_redirect_1480.py` + `tests/unit/web/test_login_next_1480.py` before the fix — ImportError (`sanitize_next_path` absent) + `auth.js still redirects to '/' unconditionally` assertion failure captured.
- **Fix** (17:14–17:15):
  - `sanitize_next_path()` added to auth_middleware (relative-path-only allowlist: refuses absolute/protocol-relative/backslash/scheme/control-char/auth-loop targets).
  - Middleware percent-encodes full path+query into a single `next` param (expired-token TokenExpired path flows through the same `_unauthorized_response`, so #1520's server-side variant is covered automatically).
  - `login_page` honors sanitized `next` on the already-authenticated redirect.
  - auth.js `safeNextUrl()` (mirrors the server guard) + hash re-attach; success redirect uses it.
  - chat.js both expiry redirects carry `next` via `loginReturnTarget()` (path+search+hash).
- **Verification** (17:15): 21/21 new tests pass; `tests/unit/web/ tests/unit/services/auth/ tests/test_architecture_enforcement.py` → **714 passed, 0 failed** (env-stripped); plus `tests/test_exempt_list_boundary_1308.py` → 126 passed.

## Discovered issues filed

None filed (no `gh`/`bd` per brief). Two observations for Lead to triage, recorded in report: (1) `SessionTimeout.logoutUrl = '/logout'` targets a GET route that doesn't exist — expired sessions reach `/login?next=%2Flogout` via middleware (now sanitized to land `/`), authenticated "Logout Now" clicks get a 404; pre-existing G52 behavior, out of #1480 scope. (2) `login_page`'s bounce for the placeholder `user_id == "user"` sentinel untouched.

## Wrap

Committed in worktree only (per brief): fix(1480) commit; no push, no mailbox writes.
