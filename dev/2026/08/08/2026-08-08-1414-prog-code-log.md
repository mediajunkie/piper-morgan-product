# Session Log — 2026-08-08 14:14 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, dispatched by Lead)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1394 — session continuity gap: prior turns never reach context on the chat path
**Worktree**: isolated agent worktree (`.claude/worktrees/agent-a538f05acfbfe7e1a`), commit-only, no push, no mailboxes

## Work

- Mandatory reads done first: `docs/internal/architecture/current/intent-routing-stack.md`
  (Stage-0/B3, ADR-078 D2, D4 constraint) + `understanding-layer-inversion-proposal-2026-08-08.md`
  (moratorium: no new routing patterns — this fix is context-assembly plumbing only).
- Traced the full chain: chat route → `process_intent` outer seam (#913/#953/#1122
  hydration + turn recording) → `_process_intent_internal` → floor helpers →
  `build_recent_history` → `FloorContext.conversation_history` → floor prompt.
- Live service-layer diagnostic against the dev DB (scratchpad `diag_1394.py`,
  LLM stubbed, real ConversationManager + Postgres): the plumbing WORKS for the
  unauthenticated shape and for post-restart hydration — and returns `history=[]`
  for the AUTHENTICATED generic-QUERY shape. Root cause isolated:
  **principal dropped en route to the floor** — registry key is
  `f"{user_id or 'anonymous'}:{session_id}"` (#817); outer seam records turns
  under the authenticated key; `_handle_generic_query` (QUERY),
  ANALYSIS/SYNTHESIS/STRATEGY/LEARNING fall-throughs, and the soft-offer
  unknown-workflow path all called the floor with `user_id=None` → floor read the
  empty `anonymous:` context. Same split-brain for the #852 last_offer pair
  (write/read anonymous; #953 persist + floor flags user-scoped).
- Why never caught: every pipeline test drove these paths with `user_id=None`,
  where the keys coincide — probe shape didn't match the live (always
  authenticated) chat shape. Hydration itself (#1122/#1423) is NOT the break.
- Failing-first: `tests/unit/services/intent/test_floor_history_principal_1394.py`
  — 9 failed / 1 passed pre-fix; 10/10 post-fix. Asserts on the actual captured
  `FloorContext.conversation_history` AND the built floor prompt.
- Fix (services/intent/intent_service.py): principal recovery
  (`user_id = user_id or _principal_from_intent(intent)`) in the three floor
  entries; explicit threading through `_handle_query_intent → _handle_generic_query`
  and the soft-offer site; user-scoped `get_or_create_context` for the last_offer
  write/read pair. No routing surface touched.
- Suites green env-stripped: intent_service + intent units 2076 passed / 3 skipped;
  test_architecture_enforcement.py 27 passed.

## Discovered issues filed

None filed (gh quota embargo per dispatch) — findings for Lead to triage in report:
`classify_conscious` (classifier.py:595) still anonymous-keys its conv context but
has zero production callers; four category handlers (`_handle_analysis_intent` et
al.) still lack a user_id parameter (recovery via intent.context covers them).
