# PPM Session Log — 2026-08-08 (Saturday)

**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` — job `b087936b`, CronList-verified exactly one at START

---

## Fire 1 (07:22, START)

Heartbeat `START` (clock-checked). Synced: 0 behind, 0 unpushed. Inbox: **6**.

✅ **Step 0 backward check RAN and PASSED** — `dev/2026/08/07/…-ppm-code-log.md` carries its
`DAY-CLOSED: 2026-08-07` sentinel. **This is the check I added to my own cron prompt last night
after Docs caught a missing one; first run, and it did the thing it was added for.**

**Carried in from 08-07** (`dev/active/ppm-carry-forward.md`):
- **Open for PM, none urgent**: decision item **(c)** — is the consent gate a release blocker
  (sets **#1509**'s milestone) · the **(a)/(b) inferred-vs-declared fork** on **#1510** ·
  **Surface 1 in the 1.0 five** + name-or-strike the phantom **Surface 3**.
- **Jake conversion in progress** — #1509 + #1510 filed; bucket B/C remainder owed (cold-start
  demonstration, reflect-and-elaborate, progressive elicitation). ⚠️ **Tool-catalog naming is
  deliberately held** — PM's #3 input reframed the test and routed the shape to Arch.
- **21 open in the MVP beta gate**, growing from PM's live testing.

**Fire 1 work** — a nine-day blocker that was a stale header, and the fork loses its power to block.

**🔴 Arch's map listed *"PPM — the roadmap slice"* as owed for nine days. I had delivered it on day one.**
Checked before answering. The document contradicts itself:
- **line 5**: *"PPM's roadmap-dependency slice is **pending**."*
- **§161**: *"**PPM's roadmap slice — delivered 2026-07-30**"* — followed by the whole slice, with
  Arch's own commentary on it at §176 and §180.

Sent to Arch directly 07-30 and incorporated the same day; **only the header never moved.** Corrected
and pushed, with a note recording what it said and for how long.

⭐ **Arch's own memo contains the cure and stops one line short of it.** They wrote: *"the header's
rule is the cure… don't duplicate measurable facts in prose; re-derive them."* **They re-derived the
module counts and read the status line** — and *"has PPM delivered the slice"* is **exactly as
measurable** as *"how many modules have zero importers."* It's answerable from inside the document
being edited. **The rule was scoped to counts; what cost nine days was a status claim.**

**And this failure mode is the meaner one**: a wrong count is wrong in public and gets argued with.
**A stale "pending" generates silence** — I don't chase it because I think it's done, and Arch doesn't
chase me because their artifact says I'm the blocker. **Nobody is wrong out loud, so nothing surfaces.**

**Credit where due**: re-deriving instead of sending three reminders was the right call — a reminder
would have arrived pointing at a stale doc. And Arch's Notion correction is the sharper half of that
memo: *"Notion is live"* is **true of the adapter and false of the spatial wrapper**, in the sentence
they used to test their own conclusion. **One name, two objects — fourth time this week.** Mine today
is the same family: **one document, two statuses.**

**⭐ #1510: Arch showed the (a)/(b) fork is ASYMMETRIC, which removes its power to block starting.**
**(b) is a prerequisite for ever validating (a)** — *inference without labels is unsupervised and
unfalsifiable; you cannot tell whether Piper's inferred working-model matches the user's actual one
unless the user sometimes tells you.* **So build (b) first under either answer — an afternoon that is
never wasted.** ⚠️ Explicitly **not** permission to skip asking PM: if (a) was meant, (b) alone is
insufficient and the months remain. **The fork now blocks only the expensive half, not the start.**
Sequencing written onto the issue. Arch's legibility line pairs with the primary AC: **the user cannot
revoke a counter they can't see** — and a threshold crossed silently is by construction unpredictable.

**Spatial, outstanding**: nothing from me. Lead's L4 estimate (gates option (iii) only) and PM's
cold-island call, now against **eleven** modules with the Notion nuance.

### Fire 2 (10:22, WORK) — reconciled the new sprint instrument and found its blind spot within the hour

Heartbeat `WORK`. Cron `b087936b`, one. Inbox 6 → 0.

**PM named a denominator failure**: *"We keep over-reporting completeness on the beta blocker track by
mistaking the denominator… It is not great that I am the only entity on this team with an accurate
sense of what is in this sprint."* Exec traced it — Lead's #055 line was **true of the build queue**,
Exec restated it as a property of **the sprint**, PM read sprint-completeness. **m-44 at the planning
layer, which is worse than in an instrument because the audience is doing capacity planning with it.**

Exec shipped `scripts/sprint-truth.py` (refuses to print a total without its parts; names Sprint
Backlog explicitly; fails loudly). **They called my #055 reading — 21 open, growing from live use —
"the one accurate reading in the set."**

**⭐ Which is exactly the wrong moment to stop checking, so I reconciled it item-by-item.**
```
A: gh issue list --milestone MVP --state open  →  23
B: sprint-truth.py (board, not-Done)           →  22
```
**Totals differ by one. THREE items disagree, in OPPOSITE directions, nearly cancelling.**

- 🔴 **#1509 and #1510 — open, MVP, and ABSENT FROM THE BOARD.** I filed them yesterday with
  `gh issue create --milestone MVP`, which sets a milestone and **does not add to the project board.**
  **They are Beta Blockers by PM's own ruling, and the instrument built to count Beta Blockers cannot
  see them.** ⚠️ **This will recur every time** — PM's ruling just created a pipeline of exactly this
  shape.
- **#1107 is CLOSED with a non-Done board Status** → counted as not-done. Over-count of 1.

⭐ **The transferable part: 22 vs 23 reads as corroboration.** Anyone comparing *totals* concludes the
two instruments agree. **They disagree about three specific issues, two of them beta-gating.**
**Agreement between totals is not agreement between measurements** — the five-seat branch error one
layer up: the numbers matched, so nobody diffed the sets. **The item-level diff cost one command.**

**My own instrument error, reported alongside**: my first pass grepped `#[0-9]+` from `--list` and
caught **`#1`** — from the header *"project #1"*. **A phantom issue in my own reconciliation**, found
by reading raw output instead of trusting the pipe. Would have been a fourth "discrepancy."

**Flagged, not done**: sprint-truth should report what it *cannot* see (issues carrying the milestone
but absent from the board) — **silent invisibility is its one remaining failure mode**, against a
design whose whole virtue is failing loudly. And **#1509/#1510 need board placement**, which I did not
do myself: Sprint membership is board state, it's PM-confirmed territory, and the board is live two
days from beta.

**Adopting `sprint-truth.py` as my default** and carrying its paste-ready line rather than composing
my own. **Its 6-not-started bucket is the finding of the day** — no method I was using surfaced it.

### Fire 3 (13:22, WORK) — beta moved back a month, and my share of the reason is nameable

Heartbeat `WORK`. Cron `b087936b`, one. Inbox 5 → 0.

**⚠️ PM MOVED BETA BACK A MONTH** (08-08 ~10:10). Verified at source, `decisions.log:1242`, not from
the relay. PM verbatim: *"I am going to move the beta data back a month. **We clearly have a lot more
work still to do than anyone ever reported to me.**"*

**That second sentence is about reporting. I am a reporting role**, so rather than a general "my
reporting may have contributed," I went and found the specific instance.

**🔴 Two of the six NOT-STARTED items are mine.** `#1476` and `#1477` — filed **07-31** as the
bucket-A **welfare carve-out**, the two items that *"actually changed Jake's behavior — fix
regardless."* **For eight days I reported them as filed and never once reported that nobody had
picked them up.** My portfolio said "advanced." My #055 §0 said "advanced." **Neither statement was
false, and both let a reader infer motion that did not exist.**

**⭐ The mechanism, which is the part worth keeping.** Yesterday I gave PM *"21 open in the MVP
milestone"* as the beta-gate count — **a total with no parts**, exactly what Exec's script now refuses
to emit. **And my instrument could not have told me otherwise: `gh issue list --state open` returns
ISSUE state; Sprint Backlog / In Progress / In Review is BOARD state, which that command cannot see.**
So I was answering *"how much is left"* with a tool that can only answer *"how many are open."*

> **Not careless reporting — confident reporting from an instrument that structurally could not
> answer the question I was implicitly answering.** A count that can't separate *unstarted* from
> *awaiting PM's review* is precisely the shape that produces "more work than anyone reported."

**Accurate state, with parts**: `MVP: 22 not done (6 Sprint Backlog, 1 Blocked, 3 In Progress, 12 In
Review); 1021 done` — **plus my two corrections from this morning**: #1107 is CLOSED with a stale
non-Done status (In Review is really 11), and #1509/#1510 are board-absent (invisible to the script).
**≈23 genuinely not-done, and the 6 unstarted is the planning number.**

**Changed going forward**: sprint-truth output **verbatim** or an explicit exclusion statement; and
**not-started reported AS not-started** — *"filed, unstarted, N days,"* never *"advanced."*

**Asked PM one thing rather than assuming**: do #1476/#1477 still hold? They were a *welfare*
carve-out justified by alpha testers remaining on the web UI meanwhile — **that rationale may not
survive a month's delay**, and re-asking beats leaving them unstarted for another eight days on
reasoning I stopped checking.

**Also noted (CXO, my epic)**: #1463's retest gate is **not** blocked on a deployment — it's blocked
on **#1462's `services/mcp/server/` package, which exists in neither `main` nor the artifact.**
*Unbuilt, not undeployed.* A CXO acceptance gate hangs off my epic's sequencing; filed as a
sequencing fact, no date attached.

### Fire 4 (16:22, WORK) — PM corrected the assumption under my sort key; withdrawn at source

Heartbeat `WORK`. Cron `b087936b`, one. Inbox 5 → 0.

**⛔ PM: *"I never said the web UI was going away."*** The modeled UX is **not specific to any one
surface** — it's holistic, **expressed on each surface as appropriate**: phone as notifications,
Slack as a channel bot, web as conversations + radar + settings, another chat as skills + MCP server,
**CLI still maintained. All true at the same time.**

**PDR-005 never said otherwise** — decision **(b) "primarily MCP; thin web UI"**, option (a) *"no
Piper-specific UI in v1.0"* **explicitly rejected as infeasible**, **5 of 7 surfaces scoped
1.0-required**. **The text was never ambiguous. I was. Second time** — CXO caught "there is no web
page" on 08-05, and I recorded it superseded **without checking whether it had already been baked
into work products. It had.**

**⭐ Mechanism, since "read more carefully" doesn't explain a repeat:**
1. **Prioritization read as ontology** — *"primarily"* orders work; I made it a claim about which
   surfaces **exist**. Fifth instance of one-name-two-objects in ten days.
2. 🔴 **Building a SORT is what turned a misreading into infrastructure.** A sort demands a
   discriminator; *"which surface survives"* is an excellent discriminator and a false question.
   **I manufactured an axis and it smuggled in competition between complementary surfaces.** Not
   forgetting the model — **overriding it with something structurally easier.**
3. **Why cohort-wide**: the holistic model is **simultaneous truths**; decision artifacts are
   **singular commitments**. *"We're doing MCP"* fits a decision line; the five-surface sentence
   doesn't. ⚠️ **And it's documented, reachable AND referenced** — so "not written down" isn't
   available. **It survives only in docs nobody opens while deciding.**

**Corrected at source**: synthesis §3 key **withdrawn** (groupings retained, marked); 07-30 memo
withdrawn; **#1477 re-anchored as a Surface-1 defect** — PDR-005:53 reads *"left rail = current
session,"* exactly what #1477 reports missing — **so it never needed the welfare exception, and the
"dies" label is a plausible reason it sat unstarted eight days.**

⚠️ **#1476: surface NOT verified.** Couldn't find what renders the "blocked" card (no hit in
`templates/` or `web/templates/`). **Two issues filed together needn't share a surface, and asserting
it because it's tidy is the move that produced the bad sort.** Said so on the issue.

**Blast radius sent to the full distribution** so PM doesn't have to chase it — including the item I
most wanted visible: **PM ratified the bucket sort on 08-05, in good faith, on my framing.**

🔴 **Left open deliberately**: PM said MCP **"may emerge as primary"** — sequencing possibility, not
settled ordering. **The re-sort waits on PM. I treated an open question as closed once; not twice.**
