# Session Log — 2026-08-08 12:43 — Coding Agent (prog-code)

**Role**: Coding Agent (subagent, isolated worktree `agent-aa75d6fe2098c0ac2`)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1521 — "what reminders do I have?" misroutes to the temporal/calendar answer
**Constraint**: no `gh` (quota), no push, no mailboxes; commit in worktree only

## Work

- Read `docs/internal/architecture/current/intent-routing-stack.md` first (mandatory).
- **Pinned the misroute mechanism**: probed the pre-classifier — every reminder-QUERY
  shape ("what reminders do I have?", "list my reminders", …) returned **None** on both
  entry surfaces (`pre_classify` AND `detect_multiple_intents`), so the message fell to
  the LLM classifier (surface 2), which misroutes to the temporal lane. REMINDER_PATTERNS
  (#903) only cover CREATION phrasings.
- **Failing-first**: wrote `tests/unit/services/intent_service/test_reminder_query_preclassifier_1521.py`
  (33 tests) and ran on pre-fix source: **18 failed, 15 passed** (the 15 passers are the
  unchanged-behavior regression + blocker rows). First failure message pins the live bug.
- **Fix** (mirrors #1471's shape — shared blocker-guarded helper, both entry surfaces):
  - `pre_classifier.py`: `REMINDER_QUERY_PATTERNS` + `REMINDER_QUERY_BLOCKERS` +
    `_reminder_query_match()`; `pre_classify` check placed above the #903 creation check
    (and therefore far above TEMPORAL); `detect_multiple_intents` pattern group before
    the todo group + TEMPORAL, guarded by the same helper.
  - `todo_handlers.py`: `handle_list_reminders` — owner-scoped `list_todos`, filters
    `reminder_date is not None`, due/upcoming split, aware-UTC via `ensure_utc`
    (#1491/#1429 shape), honest empty + honest failure copy (#1425), `# silent-ok:`
    annotated broad except.
  - `intent_service.py`: `_handle_list_reminders_query` (4-arg) — NO new elif dispatch
    site (#1124); auth-guard via `_coerce_todo_principal` (#1466).
  - `workflow_entries.py`: rail entry via `_make_query_dispatch_entry_point(...,
    pass_session_id=True, pass_user_id=True)`; keys `list_reminders_query` +
    mode-4 aliases `list_reminders`/`show_reminders`/`get_reminders`.
  - `action_registry.py`: ("QUERY","list_reminders_query") WORKFLOW + example + Verb.LIST.
  - `chat_pointers.py`: new **`pin:` namespace** — `pin:reminder-query` POINTER
    ("what reminders do I have?" → query/list_reminders_query). Reminders have no
    page/integration surface, so the #1471 ride-an-existing-surface trick wasn't
    available; the ratchet's stale check now exempts `pin:` keys (and asserts pins are
    POINTERs) while `test_every_pointer_resolves_deterministically` enforces them forever.
  - `tests/test_architecture_enforcement.py`: the stale-check pin exemption.
  - `tests/unit/services/intent_service/test_action_registry.py`: known-handled set +1.

## Evidence

- `test_reminder_query_preclassifier_1521.py`: 33/33 passed post-fix.
- `tests/unit/services/intent_service/` + `tests/test_architecture_enforcement.py`:
  **2051 passed, 1 skipped** (pre-existing skip).
- Smoke gate (`pytest -m smoke`, CI ignores): **539 passed, 31 skipped** — includes the
  silent-death ratchet (227→ceiling respected after `# silent-ok:` annotation).
- Post-fix probe: all 5 query shapes → (query, list_reminders_query) on BOTH surfaces;
  3 creation shapes unchanged → (execution, create_reminder).

## Discovered work (cannot file — gh quota; flagged to Lead in report)

- "delete my reminders" pre-classifies to PORTFOLIO/manage_portfolio via the greedy
  `\bdelete\s+…(.+)` portfolio pattern (pre-existing; my blocker keeps it out of the
  reminder-listing lane but the portfolio claim itself is a latent misroute class).
