# Docs Session Log — 2026-08-13

**Role**: Documentation Management (Docs) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` ·
website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`

---

## Fire 1 (07:27, the 06:57 slot) — START

`CronList`: one job (`e47bd40c`), matches expression, expires ~08-18 (rotation queued before
then). Sync clean. Inbox: 0 unread. Yesterday's log carries `DAY-CLOSED: 2026-08-12`.

Day-of-week triggers: Thursday — no Docs-owned scheduled trigger (Weekly Docs Audit is Monday
08-17). Daily omnibus for 08-12 due (11 source logs) — dispatching per the one-day-per-subagent
recipe PM endorsed during the backfill. Staleness pass on the curated keep-list continues in
parallel (my dimension; Comms handles register/tone separately per the agreed split).

**Omnibus 08-12 dispatched** to a background subagent (full skill+methodology, Janus's
Pages-build memo included as a cloud-agent source). Results on notification.

**Staleness pass, batch 1 — the five ALPHA_* docs** (highest-traffic visitor pages; Comms's
register commit `9f6ab1732` this morning touched 2 of them on the tone dimension, merged before I
started — parallel-different-dimensions per the agreed split):
- **ALPHA_FEATURE_GUIDE.md — the significant find**: v0.8.6 / April 11, **4 months and 8 releases
  stale**, entirely M1-era (localhost-first setup, "What's New in M1," no hosted alpha, no
  RECONNECT/standup/Radar) — and it's the doc README + the user-guide banner point testers at.
  A content rewrite needs capability-truth I shouldn't fabricate (the user-guide.md lesson), so:
  honest interim banner applied (points at hosted alpha + Quick Start + release notes, says a
  refresh is in progress), and a refresh-offer memo to PA (cc Lead/CXO/PM) proposing: I draft
  mechanically from release notes v0.8.7–0.8.11 with per-claim source tags, PA attests capability
  claims against the live alpha, CXO optionally frames (`d44b547b4`).
- **ALPHA_TESTING_GUIDE.md**: its five setup-wizard screenshots were **never added to the repo**
  — no files anywhere, no deletion in git history; the assets README described an intended state
  as if actual (m-49's little sibling, found by the link check). All five embeds commented out
  with an honest pending-capture note; assets README annotated. Capture needs someone with a
  running setup wizard — included in the PA memo.
- QUICKSTART / KNOWN_ISSUES / AGREEMENT: link-checked clean; version headers current (0.8.11,
  July 17) — a month old but release-stamped, not stale-class.
- Committed `5f019daed`, pushed `614df0f59`.

**Omnibus 08-12 complete** (subagent report, spot-verified): 137 lines, HC:COORDINATION justified
on the day's three same-day cross-role chains (Ship #055 six-hands · scoping propose→ratify→apply→
verify in ~6h · silent-red root-cause→fix→methodology in ~12h across four agents, one
cross-project). All 11 logs read in full; Janus credited via the cloud-only-agent provision with
a reconstructed activity-log row (12 rows, 1971→1983 exact); cross-reference gate passed with 4
non-gap names documented; m-49/m-44 canonical references verified verbatim. Commits `8373746de` +
`3786e5dd8`, both on origin/main, tree clean. Operational note from the subagent, corroborating
my own push blip this fire: GitHub SSH auth flapped intermittently on Amber this morning
(fail/succeed alternating within seconds, same key) — transient, resolved on retry, matches
CIO's 08-12 observation; watch, don't chase.

## Fire 2 (10:27, the 09:57 slot) — PA yes + Comms tier-3 findings, both drained

Two memos in:

**PA accepted the feature-guide split** — I draft from sources with per-claim tags, PA verifies
claim-by-claim against the live hosted alpha (their framing sharpens mine: "source vocabulary
isn't source verification"), plus they'll flag true-but-fragile claims separately. **Draft
dispatched to a background subagent** with strict sourcing rules (release notes 0.8.7–0.8.11 +
README + BRIEFING only; every claim tagged; unverifiable → explicit PA-check list, never smoothed
over; delivered as `dev/active/draft-...-for-pa-review.md`, live guide untouched until
attestation).

**Comms's tier-3 register pass** surfaced 3 my-lane items:
1. **18 links on KEEP pages → now-excluded content — all repointed** to absolute GitHub URLs
   (README-scrub precedent; scope changes can't re-break them) same fire (`dc200c524`). Bonus
   find their sweep shook loose: guides/README's "Documentation Home" pointed at the repo-root
   README, not docs/README — a wrong-but-existing target invisible to existence checks.
2. **#1610** (legal `[contact email]` placeholder, Oct 2025, ×3 files) — needs PM's actual
   address; queued for the next PM-touchpoint rollup.
3. **#1611** (mac-dock two-process architecture) — **verified genuinely ambiguous rather than
   assumed stale**: contradicted by CLAUDE.md's port table + all current docs, but 8081 is still
   LIVE in code (`port_configuration_service.py` WEB_PORT default + 3 scripts). Evidence posted
   on the issue, routed to Lead for attestation; file deliberately stays in KEEP (excluding on an
   unverified theory = the user-guide error inverted).

Comms replied-to (`5bed262a7`), both memos filed.

**Feature-guide draft complete + handed to PA** (subagent, spot-verified): 347 lines at
`dev/active/draft-alpha-feature-guide-v0.8.11-for-pa-review.md` (`b5a51df04`), 85 tagged claims
(65 release-notes, 8 explicitly-least-trusted v0.8.6 RETEST carryovers, rest
README/QUICKSTART/KNOWN-ISSUES), 11-item PA-check appendix. Two judgment calls worth the record:
the subagent correctly found PM's #1481 Slack hold in decisions.log when no release note carried
it, led the Slack section with it, and noted it supersedes the 0.8.7/0.8.8 inbound claims; and it
surfaced a genuine source contradiction (RN 0.8.9 "OAuth not started" vs the July briefing's
live-OAuth description) as check-list item 1 rather than picking a side — only live observation
settles it, which is PA's seat exactly. Handoff memo to PA sent (`c58844715`). Live guide stays
bannered until PA's attestation; then I fold verdicts, strip tags, ship with PA named in the
commit.

**Push-retry lesson, own error, logged honestly**: my six-attempt retry loop used `2>/dev/null`,
which disguised a plain non-fast-forward rejection (my own mail-send push-to-ref, unmerged
locally) as the morning's SSH flap. Never suppress stderr in a retry loop — the retry's whole job
is to distinguish failure modes, and I built one that couldn't.

**Staleness pass, batch 2 — `guides/` (15) + `public/getting-started/` (8)**: 17 link-check hits
→ 3 false positives (code-in-unfenced-block matching the link regex, checked before flagging),
2 real fixes (guides/README's never-filed GREAT-4C error-handling doc removed honestly with the
nearest-real-doc named as narrower-not-equivalent; mac-dock's off-by-one relative path), and the
remaining 12 all inside `legacy-getting-started/` — pre-reorg directory targets that never
existed post-reorg (the #1584 flagged-residual class). Resolution: **both legacy-* archives
excluded from the site** (post-ratification scope refinement, same consistent-with-principle +
flag-not-silent basis as NAVIGATION: self-described historical docs with dead links are
worse-than-absent on a curated visitor surface; they stay on GitHub). Proposal updated.
Committed `d4e688227`.

## Fire 3 (13:27, the 12:57 slot) — 5 memos drained

**PA — blocked on live verification, did a code-level pass instead** (no browser exists on their
Amber seat; they named the broken premise before quietly substituting a lesser check — the exact
right move, and my original memo's "tester-eye access" assumption was mine, never checked; owned
that in my reply). Their code-level findings folded into the draft with layer-honest tags
(`[PA code-level 08-13: file:route]`): GitHub = OAuth-first-recommended + PAT fallback (RN 0.8.9's
"not started" is stale — the draft now states it directly instead of ducking); Notion = pasted
API key, not OAuth; Calendar OAuth structurally present; health-dashboard omission upgraded to
confirmed cut; /standup route still renders (editorial question remains). Check-list header now
carries PA's verification-layer note near-verbatim. **4 items remain that only a browser
settles** — queued for PM as a ~5-minute click-through on the awaiting-PM list. PA continuing
code-level on the rest at my yes. (`03540e35b`, reply `80d8064ff` — caught my own reversed
from/to frontmatter before sending.)

**Comms tier 4** (user-guides): 6 links repointed (targets verified to exist first — a GitHub-URL
repoint to a nonexistent file is just a prettier 404); **PM-NNN investigated**: 2025-era internal
ticket scheme, pre-GitHub-Issues, no current lookup surface, one mapping proof on record (PM-126
= GitHub #132) — historical-IDs gloss added to the 3 files rather than guessed mappings. Priority
signal sent for their remaining ~150 (features/integrations/configuration first, compounds with my
queue).

**Exec — audit-logging.md `your-org` boilerplate**: mechanical half fixed (real repo URL; #249
verified real + closed, annotated as historical reference); security@-vs-support@ left as PM's
call with an inline pending-note, on the awaiting-PM list. Exec also confirmed **#1610 resolved**
(support@/privacy@/xian@ across the legal docs).

**Lead — ci.yml retired** per PM's ruling; two live pieces ported not lost; **#1616 filed: a
Windows user cannot clone this repo** — cohort memo filenames exceed Windows path limits, detected
correctly for weeks by a red check inside an always-red workflow (the silent-red family's
compounding case). Docs note: historical ci.yml references deliberately left as records — agreed,
no scrub action. **Behavior change adopted: keeping my own memo filenames shorter from here.**

**Awaiting-PM list as of this fire** (surfaces next PM engagement, not chased): (1) the 4-item
feature-guide click-through; (2) security@ dedicated address or support@ (audit-logging); (3)
#1611 mac-dock attestation is Lead's, FYI only; (4) stale `claude/fix-docker-migration-setup`
branch deletion (from 08-12).

## Fire 4 (16:27, the 15:57 slot) — publish day + 6-memo drain + PM engaged

**"Alpha Launches" PUBLISHED** (Beat 22, pubDate today): Comms's publish-ready signal actioned
first as time-sensitive. Step-0 calendar check, pre-flight (draft+image present, frontmatter
filled incl. correctly-escaped spoken-line caption), step-5 mechanical sweep clean (0 semicolons,
0 residue, 0 tics, acronyms clean, 683 words), dry-run rendered perfectly (list→<ol>, no
emphasis defects), published with explicit --work-date 2026-07-10 / --pub-date 2026-08-13.
Live-content-verified via background wait (anaphoric-parsing paragraph present). Calendar
updated, draft+image archived, both repos pushed. PM confirmed in chat mid-fire.

**PM engaged mid-fire**: answered the blog question (already live), gave PM the prepared 4-item
click-through brief + the 2 optional deeper items; PM taking it. PA notified with heads-up +
my dispatch-grep result.

**PM's website#31 decisions executed**: (1) regex fix + back-catalog regen dispatched to a
website-worktree subagent with control-tests-before-regen requirements (dateline preserved,
standalone bold → <strong>, mixed paragraph → zero stray asterisks; regen scoped to the emphasis
fix only — the Metrics retrofit deliberately NOT applied to the back-catalog since PM holds it
less firmly; scoping to be stated in the #31 close). (2) Template updated: Metrics = real smaller
heading, with the drift history and PM's less-firm framing recorded (`8034311f5`).

**PA's correction folded**: all draft tags retagged origin/production→origin/main (PA re-verified
all 4 prior findings against the right ref before asking — their own 08-06-class mistake, caught
by themselves, corrected exemplarily). New findings folded: #11 upload limits CONFIRMED exact
(10MB + 5 extensions verbatim from files.py); #5 sharing CONFIRMED all three surfaces with the
"Email or User ID" nuance; #4 Slack outbound resolved narrow — my dispatch-grep confirmed PA's
trace (`slack_domain_service` has exactly one caller: standup skill; no generic send handler).

**Comms tiers 5+6 drained**: 2 ADR-038 + 1 windows-setup links → GitHub URLs; integrations/README
stale count removed; **their 64-file Documentation-Home pattern swept in KEEP scope — 7 more
wrong targets fixed via computed relative paths** (16 others verified already-correct);
**the Amber billing-hazard warning Comms pulled from the visitor setup doc relocated verbatim to
internal gotchas** with provenance + the distinction from CLAUDE.md's existing (different)
ANTHROPIC_API_KEY warning. Their two installation bugs (broken clone steps — fixed by them;
missing steps 9-10 — honestly left as a gap needing live-install verification) noted; steps 9-10
queued as needing whoever next does a real install.

**Still open end-of-fire**: website#31 subagent running; PM's click-through pending; Comms
continuing to api/+api-reference next.

## website#31 executed and closed (16:30–16:40, dedicated task agent)

PM's ratified decisions on mediajunkie/piper-morgan-website#31 implemented, in the website
worktree, commit `fecd178` pushed to website origin/main; issue closed with full evidence.

- **Regex fix** (`scripts/publish-post.js`): bold-first standalone check (`^\*\*(.+)\*\*$`, no
  other `*` inside → `<p><strong>…</strong></p>`) + the standalone-italic capture now rejects
  inner `*` (mixed `**bold**…*italic*` paragraphs fall through to renderInline — the literal-
  stray-asterisk manifestation is gone).
- **Three controls passed pre-regen** through the real converter (dry-run edit-pass): dateline
  `*July 10–12, 2026*` unchanged as `<em>`; `**Thanks,**` → `<strong>`; Ship #055 mixed
  paragraph → proper `<strong>`+`<em>`, zero literal asterisks.
- **Back-catalog: 15 entries** carried the `<em><em>` fingerprint (Ships 043–055 + Five Whys +
  Triad Model). 13 regenerated from published source drafts via edit-pass; per-entry before/after
  diff audit confirmed only expected changes (plus one same-root-cause bonus repair in #048's
  "Maybe check the ruler?" line). **Ships 043 + 047 needed targeted surgery instead** — their
  on-disk drafts diverged from live content post-publish (regen would have dropped #043's linked
  image and reverted #047's post-draft editorial pass incl. title). Live content kept, em/em →
  strong substituted.
- **Verified**: `grep -c '<em><em>'` = 0 (and `</em></em>` = 0); titles asserted byte-equal
  across all 375 entries; CSV/slugs/hashIds untouched; git diff = script + the 15 content lines.
- **Scoping noted in close comment**: Metrics-heading retrofit deliberately NOT applied to
  back-catalog (forward convention only, PM holds decision 2 less firmly).
- One self-caught error: close comment initially said "178 entries" (number contamination from
  an adjacent context — the feedback_adjacent_story_number_contamination pattern, live);
  measured 375, comment edited in place.

Discovered issues filed: none (the draft-vs-live divergence on 043/047 is recorded in the #31
close comment; drafts are not the canonical surface for published content, so no tracking issue).

**website#31 COMPLETE + CLOSED** (subagent report, independently re-verified: em/em fingerprint
0, #054 footer real-bold, issue CLOSED): regex fixed with 3 pre-regen control tests; 15 entries
regenerated/repaired (13 via the converter from published drafts, 2 — Ships #043/#047 — via
targeted surgery after a per-entry diff audit caught their on-disk drafts had DIVERGED from live
content post-publish; regenerating would have silently reverted real editorial work including
#047's title). Metrics retrofit deliberately excluded from regen, stated in the close. Subagent
self-caught a number contamination in its close comment (178→375, measured then edited).
Evidence memo to Exec (cc Comms, PM) incl. the draft↔live divergence class now at 2 confirmed
instances (`9589a7e5a`). Website commit `fecd178`.

## Fire 5 (19:27, the 18:57 slot) — 4-memo drain

**PM ×2** (both relayed via a general-purpose agent — PM in Cowork, no repo access):
1. **Alpha Launches Medium cross-post** — applied mediumURL + status published→distributed. The
   memo's "current state" table was a stale snapshot (read before this afternoon's publish);
   verified actual row state first (blogURL/canonicalSite already set at publish — the "gap" PM
   flagged was already closed), then applied only the genuinely-new fields. Whole-file scan
   clean, view rebuilt.
2. **LinkedIn image-upload automation dead** (both paths confirmed dead by PM 08-12; manual OS
   file picker is the documented default in PM's cross-post skill) — forwarded to Comms + Web per
   PM's route-as-you-see-fit, with the operative facts extracted and a belt-and-suspenders
   pointer for Comms's run-of-show doc.

**CIO ×2** (cc-awareness): #1616 disposed via delegation pilot then CLOSED — filename-length CI
lint landed (180-char cap, CI-only because commit-tree bypasses pre-commit, 1724 existing
offenders grandfathered), subagent's work reviewed-not-trusted before landing. CIO's adjacency
note: #1616's red-inside-red is a *check* substituting for its referent, one level more specific
than m-49's *description* case. No Docs action; my shorter-filenames practice from Fire 3 stands.

All four filed; FWD + triage in one push (`dd6b18ba0`).
