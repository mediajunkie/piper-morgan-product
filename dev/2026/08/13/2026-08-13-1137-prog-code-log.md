# Session Log — 2026-08-13 11:37 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), reporting to Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Task**: #1510 verified-inference preference mechanism (shared rail for #1591 / #1509 /
collaboration_gate working-mode surface). PM ruling relayed by Exec 2026-08-13 on #1510's
latest comment: low trust-gradient inference → read back for verification; verified → stored,
not re-inferred; meta-feedback about the verification process is a DISTINCT signal with its
own handling. Build the mechanism only, NOT the three consumers. Commit locally, no push.

## Required reading (done)
- #1510 full issue + last 3 comments (ruling + Exec investigation + recommended shape)
- #1591 latest comment (implementation path: extend preference_detection confidence gate;
  resurrect `is_low_confidence()`)
- services/personality/preference_detection.py (NOTE: task memo said services/learning/ —
  actual location is services/personality/; flagged for Lead)
- services/intent_service/collaboration_gate.py (persistence seam: _load_preferences /
  _save_preference → users.preferences JSONB)
- services/intent_service/destructive_confirm.py (#1190 offer-carrier pattern)
- services/intent/intent_service.py pending-offer block (~lines 869–1045)
- services/intent_service/workflow_entries.py + workflow_dispatcher.py (EffectClass, #1557)
- docs/internal/architecture/current/intent-routing-stack.md (mandatory pre-read)
- tests/unit/services/intent_service/test_destructive_confirm_1190.py (test idiom)
- tests/integration/test_retrospective_todos_1472.py (real-Postgres seeding idiom)

## Findings pre-build
- `is_low_confidence()` named in the ruling does NOT exist anywhere (grepped services/ +
  tests/). Nearest live analogs: `UserStandupPreference.needs_confirmation()` (standup) and
  `PreferenceHint.is_ready_for_auto_apply()` / thresholds 0.9/0.4 (personality). Plan:
  hoist the 0.9/0.4 thresholds into named constants in preference_detection.py (extending
  the existing gate, not paralleling it), define `is_low_confidence()` in the shared
  mechanism from those constants, and add the resurrected `is_low_confidence()` method on
  UserStandupPreference delegating to the shared threshold for #1591's consumer.
- Offer carrier: reuse the #1190 pending_action shape (workflow_type + pending_action +
  decline_message) with kind="verify_inference"; register an offer-only WorkflowEntry
  (action_triggered=False, effect=WRITE — it writes users.preferences on accept; #1557
  defaultless effect).
- Meta-feedback detection lives in the confirmation flow's own turn handling (pending-offer
  block), NOT the pre-classifier (routing moratorium).
- "stop asking me every time" / "don't make assumptions" / "just do it" match neither
  ACCEPT_PATTERNS nor DECLINE_PATTERNS nor the #1510 mode-declaration regexes (no durative
  marker) — no collision with existing surfaces; meta check runs before generic
  accept/decline for verification offers anyway.

## Work log

### 11:37–12:00 — Built the shared verified-inference mechanism (#1510 inferred half)

**Shipped** (single unit; committed locally on claude/lead-cycle, NOT pushed per task):
- `services/intent_service/verified_inference.py` (NEW, ~470 lines) — the shared rail:
  - Confidence gate `decide()` EXTENDS preference_detection's thresholds (0.9 auto-apply /
    0.4 suggestion floor, hoisted to named constants there); `is_low_confidence()` resurrected
    as the read-back trigger. DEFAULT: ≥0.9 auto-apply, 0.4–0.9 read back, <0.4 discard.
    TRUST_INFERENCES meta-mode: ≥0.4 auto-applies. ALWAYS_ASK: everything ≥0.4 reads back.
  - Store: `get_verified_inference` / `store_verified_inference` (provenance: value, source
    user_verified|meta_auto, confidence_at_verification, verified_at) +
    `get_meta_mode` / `set_meta_mode` — all through collaboration_gate's
    `_load_preferences`/`_save_preference` seam into users.preferences JSONB (keys:
    `verified_inferences`, `inference_verification_meta` — distinct, per the ruling).
  - `build_read_back_offer` → #846/#1190 pending_action carrier (kind="verify_inference");
    session-scoped decline memory (in-memory, capped, NOT a preference store) so a declined
    read-back is never re-asked in-session.
  - `detect_meta_feedback` (small pattern vocabulary, handler-internal only — no
    pre-classifier changes, routing moratorium honored) + `handle_verification_turn_meta`
    (the distinct-signal turn handler; handles "no, stop asking me every time" co-occurrence).
  - #1532 principal checks: offer-user vs turn-user mismatch never writes.
- `services/intent_service/workflow_entries.py` — `run_verify_inference_workflow` entry +
  registration `verify_inference` (effect=EffectClass.WRITE explicit per #1557,
  action_triggered=False — offer-acceptance only, rail-unreachable). Honest
  persisted=False copy.
- `services/intent/intent_service.py` — pending-offer block: meta-feedback check for
  verification offers (before generic accept/decline), mark_declined on decline, honest
  log name on off-intent abandonment. No new elif dispatch sites.
- `services/personality/preference_detection.py` — thresholds hoisted to
  AUTO_APPLY_THRESHOLD / SUGGESTION_THRESHOLD constants (values unchanged).
- `services/standup/preference_models.py` — `UserStandupPreference.is_low_confidence()`
  added (the ruling named it as existing; it did not — flagged), delegating to the shared gate.

**Tests** (failing-first not practical for the new module itself — it's new code; the e2e
tests were written against the required ACs and all behaviors verified green):
- NEW `tests/unit/services/intent_service/test_verified_inference_1510.py` — 41 passed.
  Covers all five required ACs incl. e2e through REAL process_intent (explosive-LLM idiom).
- NEW `tests/integration/test_verified_inference_persistence_1510.py` — 4 passed against
  real dev Postgres (5433): store round-trip, meta key visible in raw JSONB, entry point
  writes real row, shared-store tenants don't clobber (working_mode + inferences + meta).
- Ratchets: tests/test_completion_ratchets.py + tests/test_architecture_enforcement.py →
  44 passed. silent_death count 200 before AND after (ceiling not raised).
- tests/unit/services/intent_service/ -m "not llm" → 2475 passed, 2 deselected.
- Adjacent: destructive_confirm_1190 + collaboration_gate_1510 + standup preference_models
  + personality tree → 230 passed. tests/unit/services/intent/ + tests/intent_service/ →
  83 passed.
- All runs env-stripped (env -u ANTHROPIC_* POSTGRES_PORT=5433).

**Deliberately NOT built** (consumers' own wiring): #1591 standup preference capture,
#1509 consent/legibility surfaces, any call site that generates inferences and arms
read-backs. No classifier/pre-classifier/prompt changes (moratorium).

**Ambiguities resolved (flagged for Lead review)**:
1. Task memo said services/learning/preference_detection.py — actual file is
   services/personality/preference_detection.py (used the real one).
2. `is_low_confidence()` "already-defined-but-unused" does not exist anywhere; nearest is
   `needs_confirmation()`. Resurrected it as intended: shared-module function + standup
   dataclass method delegating to the shared threshold.
3. "stop asking me every time" said WHILE being asked: chose apply+store current inference
   with source=meta_auto (distinct provenance), UNLESS the same message also declines
   ("no, stop asking me…" → decline current + set meta). "don't make assumptions" →
   decline current + ALWAYS_ASK.
4. Threshold shift semantics: TRUST lowers ask-gate to the suggestion floor (0.4), not to
   zero — sub-0.4 inferences are discarded in every mode (is_ready_for_suggestion parity).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (intent-dispatch rule, silent-death ratchet, effect-typing #1557,
  verify-first, evidence discipline); intent-routing-stack.md (mandatory pre-read — offer
  seam ordering, moratorium scope); #1510 + #1591 issue threads (the ruling chain);
  collaboration_gate.py + destructive_confirm.py module docstrings (seam contracts);
  test_destructive_confirm_1190.py + test_retrospective_todos_1472.py (test idioms);
  memory: feedback_verify_at_the_right_layer (m-43 raw-JSONB assertion in integration test),
  feedback_no_confabulating_expected_steps_as_completed (honest persisted=False copy).
- **Loaded but not referenced**: Amber worktree/hooks sections of CLAUDE.md, mailbox
  discipline (no mail sent per task), glossary.
- **Wanted but not found**: a canonical doc for the #846 pending-offer record schema
  (reconstructed from destructive_confirm.py's docstring + intent_service.py reading).

## Wrap
Committed locally (no push, per task). Working tree clean apart from nothing — see commit.
Discovered issues filed: none (no untracked defects found; naming drift in the ruling
comment reported to Lead in the report-back rather than filed, Lead to decide).
