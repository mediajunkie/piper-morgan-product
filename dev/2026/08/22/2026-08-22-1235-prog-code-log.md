# Session Log — 2026-08-22 12:35 — prog (Coding Agent, Claude Code)

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead's worktree, branch claude/lead-cycle)
**Task**: GitHub #1654 — arm a carrier for handle_create_reminder's no-task clarify ask, mirroring the #1648 time-question carrier with a `clarify_reminder_task` kind. Two-question chaining: task answer binds → time question (already armed per #1648) → real save.

## Plan
1. Read #1648 carrier implementation (todo_handlers.py) + registration (workflow_entries.py)
2. Mirror with task-question kind: arm on no-task clarify; turn handler binds answer as TASK; chain into time question or save if time known; #1631 discrimination; #1665 rendered-ask storage
3. Tests: e2e transcript shape, chaining test, existing suites, ratchets
4. intent-routing-stack.md same-commit; commit on claude/lead-cycle referencing 1654 (no close keywords); NO push

## Work
- 12:35–12:55 — #1654 implemented, carrier-only, mirroring the #1648 template:
  - `todo_handlers.py`: `REMINDER_TASK_QUESTION_KIND` / `CLARIFY_REMINDER_TASK_WORKFLOW` / `build_reminder_task_offer` (payload = original message, strings only); `_arm_task_question` beside the no-task clarify ask (rendered ONCE, stored per #1665, returned); `_TIME_ASK` factored (one render shared by the #1648 branch and the #1654 chain); `handle_reminder_task_turn` (bind answer as TASK → save if time known from answer or original message, else chain into the EXISTING #1648 time question); `run_clarify_reminder_task_workflow` generic-accept landing; `_strip_trailing_time_expressions` factored from `_extract_reminder_text` (shared strip); `_is_pure_time_expression` (a pure-time answer re-asks, never binds "at" as the task).
  - **Deliberate deviation from the 1648 template, probe-driven**: off-intent discrimination uses the pre-classifier's DETERMINISTIC claim, not `is_command_shaped` — the shape-read's verb heads (check/get/set/…) claim legitimate task answers ("check in with the team" is the ask's own example copy). Probe data in this session: `pre_classify` claims every product command tried, no bare task phrase.
  - `intent_service.py`: offer-seam elif for the kind (the sanctioned #1605/#1648 handler-internal seam); `reminder_task_question_pending` added to `_apply_soft_offer` no-clobber flags; create_reminder rail belt extended to peek both kinds.
  - `workflow_entries.py`: `clarify_reminder_task` registered offer-seam-only (READ, action_triggered=False).
  - Tests: NEW `tests/unit/services/intent_service/test_task_clarify_1654.py` (21 tests: PM's transcript shape e2e w/ explosive LLM — arm, bare-task bind→chain, full two-question recovery, answer-with-time direct save, time-already-known rare path, unbindable-original honest echo, imperative-phrase-binds discrimination pin, off-intent release, decline, bare-yes re-ask, pure-time re-ask, restatement release; seam-level: leading-"to" strip, principal mismatch, save-failure holds time question w/ task bound, arm-failure honest copy; wiring + #1664/#1665 pins). `test_rendered_ask_1665.py` drift-pin extended (kind NOT confirm).
  - Doc: intent-routing-stack.md row-3 #1648 companion passage extended with the #1654 carrier (same commit).
  - Evidence: new file 21 passed; 1648+1665 suites 94 passed; both ratchet files 46 passed; full intent_service unit suite 3418 passed (2:45); adjacent todo/reminder suites 64 passed. All env-stripped, POSTGRES_PORT=5433, -m "not llm", --import-mode=importlib.
- Discovered work filed: **#1679** — `_extract_reminder_text` returns a pure-time residue as the task ("set a reminder for tomorrow at 3pm" → saves a reminder literally titled 'tomorrow'); #1606 family; fix suggestion rides `_is_pure_time_expression` + the now-armed clarify, kept out of #1654 per scope discipline.
- 12:55 — Commit `7fea73764` (amended from 948ab9674, message-only). ⚠️ Shared-worktree race: a concurrent prog session staged its files (config/PIPER.md, conversational_floor.py, test_prompt_seed_guard.py, its 1236 log) between my `git add` and `git commit`, so the 1654 commit carries them too. Nothing lost — tree verified identical across the amend (`git diff HEAD@{1} HEAD` empty); the amend NOTE attributes the extra files to that session. Lesson for shared-worktree seats: stage-and-commit is not atomic against a peer session; the mailbox-hook idiom (stage one call, commit bare the next) actually WIDENS this window — for code commits in a shared worktree, verify `git diff --cached --stat` matches intent immediately before committing, and expect to attribute honestly when swept.

---

# Parallel prog session — GitHub #1675 (canonical ground-truth wrong-empty)

*(Separate prog subagent, same day/role — sharing the one daily log per CLAUDE.md; sections delimited to avoid clobbering the #1654 lane above.)*

**Task**: #1675 — seeded todo (P1GT-life-*) invisible to list_todos_query in TestCanonicalGroundTruth (Run 14, both sibling tests). Investigate suspects (harness owner-scoping / rail read regression / timing), fix at fault layer, prove.

## Work (#1675 lane)
- 12:35 — Session start. Issue read; git status clean apart from Lead's unstaged mailbox-inbox deletions (untouched). Branch claude/lead-cycle, 22 behind origin/main; verified NO upstream changes to any file in the fault surface (`git log HEAD..origin/main -- <paths>` empty), so investigated in place.
- 12:38 — Reproduced. NOT the reported shape: the SEED turn itself misroutes — `Add a todo: P1GT-<hex>` → LLM classifier → `create_ticket` → "GitHub isn't connected yet" → no row written. The list read was honestly empty.
- Suspect (a) RULED OUT: seed and read are both HTTP turns on the same module-scoped user/cookie (one `canon_e2e_<hex8>` user) — same JWT sub both turns; owner mismatch impossible by construction.
- Suspect (b) RULED OUT by trace: pre-classifier claims 'Show my todos' (conf 1.0) → rail → `_handle_execution_intent` list_todos → `_coerce_todo_principal` → `TodoManagementService.list_todos` → `get_todos_by_owner` (owner-scoped, correct). Tell: unauthenticated read yields the AuthenticationRequired copy, not the empty-list copy Run 14 recorded.
- Suspect (c) N/A — the row never existed.
- 12:41 — Classifier probe (real classifier, cache off, 3 samples/variant): `Add a todo: P1GT-life-<hex>` drew create_ticket 1/3; `add todo buy oat milk` (colonless) drew create_ticket 2/3; plain `Add a todo: buy oat milk` stable 3/3 create_todo. Root cause: todo-CREATE has no pre-classifier claim + classifier prompt teaches create_ticket with no create_todo example + ticket-shaped marker. Probe also caught the openai primary 429ing (credits exhausted) on EVERY classification → Anthropic fallback serving (evidence added to #1676).
- FIX (harness layer — production never wrong): `tests/e2e/test_canonical_conversations.py` — `e2e_auth_state` module fixture now exposes user_id + cookies; `e2e_auth_headers` derives (same shape, zero blast radius — TestCanonicalGroundTruthMocked 5/5 pass); new `seed_ground_truth_todo()` seeds via `TodoManagementService.create_todo` AS the authenticated principal (the exact call handle_create_todo makes). Complete + show turns stay real chat turns (both pre-classifier-claimed).
- PROOF: both ground-truth tests PASS (env-stripped, POSTGRES_PORT=5433): `2 passed in 5.64s` (was ~28s — the tier now makes zero LLM calls; dropping its `llm` marker to run every PR is a cheap follow-up, left for Lead).
- Recorded: #1675 closing-evidence comment; Run 14 CSV row notes cell — pure append recoloring the two failures as harness artifacts (no existing values changed).
- Discovered work filed: **#1677** ('add (a) todo' shape needs a deterministic pre-classifier claim — production change, Lead/Arch call); evidence comment on **#1676** (live provider fallback).

