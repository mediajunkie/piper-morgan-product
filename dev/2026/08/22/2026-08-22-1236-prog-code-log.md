# Session Log — 2026-08-22 12:36 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Issue #1655 — systematic prompt-hygiene sweep: example REPLY sentences in prompt guidance become live replies (four incidents: #1544 x2, #1648 x2). Enumerate all prompt surfaces, classify example occurrences, rewrite reply-sentence examples as rules, add seed-sentence regression guard.
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead's worktree, branch claude/lead-cycle). Commit referencing 1655 (no close-keywords), no push.
**Note**: A separate prog log exists today (1235, issue #1675) — different subagent, left untouched.

## Plan
1. Enumerate every prompt surface (conversational_floor + all other LLM-bound guidance assemblers)
2. Classify each example occurrence: (a) reply-sentence (hazard) / (b) context-line-format (safe) / (c) sanctioned exception — decide #1544 verified-empty licensed-claim sentence explicitly
3. Rewrite class (a) as rules; update prompt pins only where the sentence itself was the hazard
4. Regression guard: test asserting the four incident seed strings never reappear in any prompt surface
5. Run: floor/1544/1639/1648/first-contact pin suites + both ratchet files

## Entries
- 12:36 — Session start. Read #1655, #1544 + #1648 evidence comments. git status: claude/lead-cycle, 23 behind origin/main (upstream commits touch no services/ or tests/ paths — safe to work from HEAD). Unstaged mailbox deletions are Lead's, not touching.
- 12:50 — Sweep complete, all suites green. Details:

### Enumeration (the review artifact — every LLM-bound or named surface, with verdict)

Denominator: every `llm.complete()` call site in services/ + web/ (grepped `\.complete(` and `system=`), plus every surface the issue named, plus deterministic copy builders checked for LLM-boundness. 22 surfaces.

| # | Surface | LLM-bound? | Verdict |
|---|---------|-----------|---------|
| 1 | conversational_floor.py — FLOOR_SYSTEM_PROMPT_ADDENDUM | yes (reply-generating) | **class (a) x7 fixed**; sanctioned (c) exceptions reasoned in header comment |
| 2 | conversational_floor.py — FLOOR_DENIAL_ADDENDUM | yes | **class (a) x2 fixed** (decline sentences; invented-referent redirect examples) |
| 3 | conversational_floor.py — _CAPABILITY_MANIFEST_STATIC | yes | sanctioned (c): "file it in owner/repo" anti-example deliberately pinned by test_floor_canonical_phrasing_1571 (user-utterance register, grounds the rule) |
| 4 | conversational_floor.py — _build_prompt blocks ([Reference binding], [Context], [Redirect context]) | yes | clean (rules only) |
| 5 | conversational_floor.py — _format_domain_context directives | yes | **class (a) x1 fixed** (insights invite-correction quoted sentence); verified-empty renderer lines sanctioned (c) — conditional-on-true, third-person indirect; "Also due:" = format token (b) |
| 6 | piper_config_loader.py + config/PIPER.md (base system prompt) | partially (six section names extracted; none exist in current file) | **class (a)-LATENT x1 fixed** (line 197 example reply); loader drift filed as #1678 |
| 7 | personalization_service.py | yes (routes to loader/DB row) | clean (no guidance text of its own) |
| 8 | prompts.py INTENT_CLASSIFICATION_PROMPT + ENTITY_EXTRACTION_PROMPT | yes (JSON out) | clean (b): user-message→JSON examples only |
| 9 | classifier.py (prompt assembly + continuation hint) | yes (JSON out) | clean |
| 10 | inversion_router.py _SYSTEM_PROMPT + build_routing_prompt | yes (JSON out, shadow-only) | clean (b): catalog user-utterance examples |
| 11 | session_snapshot.py serialize_for_prompt | yes (via router) | clean (rules + state labels) |
| 12 | lens_inference.py LENS_DECODER_PROMPT | yes (JSON out) | clean |
| 13 | document_handlers.py (QA / comparison / conversational_reference) | yes (reply-generating) | clean (rules only, no example replies) |
| 14 | services/prompts/summarization.py (all variants) | yes | clean (b): format/JSON-shape placeholders |
| 15 | slot_extractor.py | yes (JSON out) | clean (b): JSON format example |
| 16 | semantic_boundary_detector.py (v0.1 + v0.2) | yes (classifier; states "You are NOT generating a response") | clean |
| 17 | work_item_extractor.py | yes (JSON out) | clean (b) |
| 18 | knowledge_graph/ingestion.py | yes (JSON out) | clean |
| 19 | analysis/text_analyzer.py + document_analyzer.py | yes (use #14) | clean |
| 20 | project_context.py | yes (name inference) | clean |
| 21 | llm_domain_service.py | yes (health ping "Hello") | clean |
| 22 | context_assembler.py + chat_pointers.py capability lines | data into floor prompt | clean (b): POINTER utterances are user-register example ASKS by design (#1428), not reply sentences |

NOT LLM-bound (deterministic copy builders — the strings ARE the reply, imitation mechanism doesn't apply): collaboration_gate.py, first_contact.py render block, standup conversation_handler.py + preference_feedback.py, slot_prompts.py, onboarding narrative_bridge/helpers/portfolio_handler, FLOOR_FALLBACK_* constants, mux/push_mode format_push_for_chat, slack link_copy.

### Class-(a) fixes (all in conversational_floor.py unless noted)
1. Agency pillar: `"Would you like me to close that?"` → rule (one short direct question offering the action)
2. Prediction pillar: `"I'm noticing several PRs waiting — might be worth a nudge"` / `"Alert: PR count exceeds threshold"` → rule (name what you're noticing, grounded in context; never alert-register)
3. Grammar contrast pair: `"I noticed a blocker in the sprint — the auth migration PR has been waiting for review since Tuesday"` / `"Alert: PR #847 status=pending_review, age=3d, priority=high"` (fabricated specifics incl. invented PR number) → shape description, no complete sentences
4. Warmth prohibition: quoted banned sentences → indirect description
5. Empty-data honesty: `"I couldn't pull up your todo list just now — want me to check again?"` → indirect rule (say in your own words + offer to check again)
6. **Verified-empty licensed-claim sentence (the named judgment call)**: `"Your todo list has no pending items right now."` → REWRITTEN, not sanctioned. Reasoning: the license CONDITION is the load-bearing content, and the concrete licensed phrasing already lives in the conditional renderer lines ("state it as a plain account-level fact: their todo list has no pending items right now"), which render ONLY on turns where the claim is true by construction (read ran, zero rows). Static example deleted; prompt now points the model at the checked-empty context line's framing. Seed exists only when true — strictly better than a sanction. Renderer lines themselves = sanctioned (c) (conditional-on-true, third-person indirect).
7. Doubt default: `"I don't have that information here"` → indirect
8. #1197 promises: quoted banned promises + positive examples ("Noted for this conversation", "you can set that as a preference") → indirect (note: 1591's docstring records "Noted for this conversation…" fabricating live — the positive example had already leaked)
9. How-to-engage framing stems → indirect
10. Express-investment pair: `"I've been tracking the migration — the last commit landed yesterday"` (fabricated specifics) → shape description
11. Denial voice: `"That's not something I want to get into"` / `"I'd rather steer us back to..."` → indirect first-person-discretion rule
12. Denial redirect: `"Let's look at the sprint board instead"` / `"Want to think through the roadmap question you mentioned earlier?"` (invented referent) → rule: door drawn from redirect context or actual conversation content, never invented
13. config/PIPER.md:197 (latent — unreachable per #1678): example reply → rule

### Pins updated
NONE — every pinned string survives (verified: 1544/1639/1648/1571/1517/1331/1293/1393 pins all assert on rules, context markers, banned-phrase names, or sanctioned anti-examples, none on the removed example sentences).

### Regression guard
tests/test_prompt_seed_guard.py (new): repo-wide ban on the four incidents' distinctive seed cores ("I don't see any todos", "creating that now", "Reminder set for") across services/ + web/ *.py (comment-exempt) + config/*.md. Deliberately NO NEVER/Do-NOT line exemption (#1570: prohibited vocabulary still seeds). "in this conversation" too generic for repo-wide — stays section-pinned by 1544/1639.

### Test evidence (all env-stripped where DB-bound, POSTGRES_PORT=5433, -m "not llm", --import-mode=importlib)
- Pin suites + guards (13 files incl. new guard, honesty guard, floor/1544/1639/1648 x2/1571/1517/1331/first-contact x2/1030): **209 passed**
- Ratchets: tests/test_architecture_enforcement.py + tests/test_completion_ratchets.py: **46 passed**
- tests/integration/test_pending_todos_query_1544.py (real Postgres): **3 passed**
- Full tests/unit/services/intent_service/: **3418 passed, 3 deselected**
- Smoke (gating subset): **542 passed, 1 skipped**

### Discovered issues filed
- #1678 — PIPER.md content never reaches the system prompt (loader extracts six section names the v3.0.0 file no longer has; verified live, 439-char effective base prompt)

intent-routing-stack.md: NOT touched — no dispatch-adjacent surface changed (prompt guidance text only).
