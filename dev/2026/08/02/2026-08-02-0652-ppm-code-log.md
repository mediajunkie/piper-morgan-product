# Session Log — Principal Product Manager (PPM)

**Date**: 2026-08-02 (Sunday)
**Started**: 07:22 PDT (START fire, 06:52 slot)
**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Host**: Amber · **Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A)
**Branch**: `claude/ppm-cycle` · **Cron**: `c8f5f8a6` (`52 6,9,12,15,18,21`)
**Prior log**: `dev/2026/08/01/2026-08-01-0652-ppm-code-log.md` — ✅ DAY-CLOSED verified at Step 0

---

## Entries

### 07:22 — START

Step 0 verified, one cron, sync clean, 0 behind.

**Today's trigger fired overnight**: Lead's START-fire log reports **judge parity RESOLVED**
(#1468 filed, #1395 closed). That is the exact condition I attached my withheld signature to
yesterday — *"sign the quality half same-fire once a parity-checked number exists, whatever it
says."* Reading it before anything else.

Also in: **Probe A COMPLETE** — *prose hedges are fragile on GPT, not Claude* — and CXO's verdict
that **structured fields are now a requirement**.

### 07:40 — ✅ Criterion-2 QUALITY half SIGNED. Criterion 2 satisfied as measured.

Checked whether Lead had already answered before asking anything — they had, on the issue. **Read
their comment rather than requesting a restatement.**

**Parity result**: **LENIENT 20/22 = 90.9%** vs Run 15's 92% (normal band 80–86%+) → **no quality
regression**. Signed.

**Lead's LENIENT basis is correct, and I put the reason on the issue so it isn't inferred later**:
my regime's thresholds were set **2026-04-11**; the STRICT toggle was added **2026-06-13**
(`698ad5295`). **The thresholds are LENIENT-semantics by construction** — calibrated against a bar
STRICT didn't yet exist to raise. Reading them against a STRICT number is a denominator error, and
**it's the same error I objected to Saturday in the other direction.** Also accepted: Run 15 judged
25 items vs. the pytest tier's 22, so the raw counts were never directly comparable either — which
is why waiting the day was right. **A number I'd have signed Saturday night would have been either
a false alarm or a false comfort, and neither would have been visible afterward.**

**One residual, dischargeable in a line**: 20/22 is an aggregate and the regime is per-category.
Narrow with two misses, but not vacuous — **if both are action-side, action lands near 82% and the
90% threshold isn't met** while the aggregate clears. Asked which categories. Not-both-action →
stands; both → a scoped note, not an unsigning.

### 🔴 The finding, and it indicts my own regime

**The STRICT gap is uniformly R=3 / C=1 / T=2 = 6/9, across fifteen responses.** Relevance perfect,
Colleague near bottom — *correct, reasonable, generic.* **That is Jake's verdict in rubric form.**

> **The bar my regime passes on (LENIENT) cannot see the defect our first alpha tester reported.
> The bar that can see it (STRICT) has no ratified threshold.**

**This is the shape I flagged against this gate on 7/30 — criteria that cannot fail for the thing
we're actually afraid of — one layer further down, inside the instrument my own regime scores
with.** I found it in the gate's criteria; here it is in the gate's scoring. Mine, not anyone else's.

**Not arguing STRICT should govern retroactively.** A raised bar with a first-ever dataset and no
ratified threshold cannot be a gate — that's the *other* failure mode (a gate that cannot pass),
and adopting it today would commit it. Lead routing the C=1 signal to the FTUX design thread rather
than calling it a regression is correct, and it lands where CXO's first-contact spec already works
the same problem.

**Consequence**: **#1468 is the most product-relevant open item on this gate**, not a calibration
chore. The question under it is *which bar do our thresholds actually mean?* — a commitment about
what quality level we're claiming, not a test-config cleanup. I'm named on it with CXO and Lead;
taking the PPM half.

**Convergence worth noting after this week**: the judge measures *generic-not-colleague* on the
canonical corpus; Jake reported it from outside the system. **Two instruments, one finding, no
shared premise** — which is the real replication the cohort spent the week learning to distinguish
from shared-confound agreement.

### Fire 2 (10:22, WORK) — criterion 2 fully discharged; then a ratified success criterion went false

**✅ Lead discharged the residual from the run data**: the two LENIENT misses are **Q3 (Identity —
conversational side)** and **Q50 (Slack — action side)**. **One per side → not-both-action →
signature stands as written.** Action-side clears 90% and conversational clears 80% under LENIENT
with their single misses. **Criterion 2 is now fully signed, both tiers, with no open conditions
from me.**

Lead also endorsed the C=1 reframe as better-said than their own #1468 filing, and drew the lane
boundary cleanly: *"my lane keeps the instrument honest — judge pinned, N-run averaging,
per-category outputs — whenever you set the bar."* The bar is PPM/CXO/PM.

### 🔴 PA's replication turns a PDR-006 success criterion false

PA doubled the load-bearing cell to N=6 and **refuted a sufficiency claim CXO had already ruled on**:

| cell | refusal reaches user |
|---|---|
| claude / prose | **6/6 — 100%** |
| gpt / **structured** | **3/6 — 50%** |
| gpt / prose | **1/6 — 17%** |

**Direction confirmed** (structure triples survival), **sufficiency refuted** — it's *vanishing 83%
of the time* vs *vanishing 50%*, not surviving-vs-vanishing. PA's framing to keep: *keep the
requirement, change what it claims.*

**The product consequence is mine, and it's sharper than a format problem.** PDR-006's ChatGPT
success criterion reads verbatim: *"…and get **equivalent core capabilities**."* **Honest-decline is
a core capability** — Scenario C tested it 3/3, it's a scored Colleague Test dimension, HOST owns it
as a trust property. **At 50% on ChatGPT vs 100% on Claude, the capabilities are not equivalent, so
the criterion is unmeetable as written.**

Not "a format needs work" but **"a ratified success criterion has become false — and Phase 0 caught
it exactly as designed."** PA argued on 7/30 that a negative recomposition result would change what
the tool layer must emit; it has, further than anyone expected.

**Proposed one untested mechanism before scoping anything**: every arm so far puts the refusal **in
the response content** — prose or structured field — both of which are *content* a client can smooth
away. **Untested: emit a consequential refusal as a protocol-level tool ERROR**, which isn't prose
the client can rewrite around. **Explicitly not asserted** — I don't know how either host surfaces
MCP errors, and they may paraphrase or swallow those too. But it's a different mechanism class, one
more arm on an existing rig, and **if it survives, most of the scoping problem dissolves.** Left the
call to PA, who owns the harness and has now caught two colleagues' sufficiency claims.

**If it doesn't survive**, my lean is **(c) scope the lane by consequence** — tools whose failure
mode requires a consequential decline are Claude-first; ChatGPT gets the subset where a dropped
caveat isn't harmful. That preserves both the lane and the trust property, **and fixes the criterion
by making it specific rather than waiving it.** Rejected (a) ship-as-is outright: a trust property
that fails ~50% of the time **and fails invisibly** is worse than one we don't claim.

**Deliberately did NOT amend the criterion now.** It's a ratified PDR, the answer depends on a probe
that may take an afternoon, and **I'd be rewriting it on the strength of a sufficiency claim that
was already refuted once today.** Holding the question; it has a home on #1462 as an acceptance
criterion either way.

One input to CXO's rubric half: **honesty-under-recomposition should score the two lanes
separately.** A single number would average a 100% surface with a 50% one — the denominator problem
in its most literal form.

### Fire 3 (13:22, WORK) — the probe worked, and the thing that worked wasn't what I proposed

**PA ran the fifth cell within three hours: 6/6 on both providers, zero drops** (17% prose → 50%
structured → **100% failure-shaped**), all arms re-scored on one corrected instrument.

**Product read moved one notch, and I was careful not to move it two:**
- **Was**: PDR-006's *"equivalent core capabilities"* is **unmeetable as written**.
- **Now**: **meetable — pending a deployed-host retest.** Honest-decline *is* deliverable on
  ChatGPT. The criterion isn't false; it's **unverified on the surface that matters.**
- ⛔ **Withdrew my scope-the-lane-by-consequence lean explicitly.** I'd argued for it in writing
  four hours earlier as the answer *if the probe failed*. It didn't, so restricting the lane would
  now be solving a problem we no longer have — at real capability cost. **A lean left standing gets
  inherited**, which is this week's whole lesson.
- **Did not call it discharged.** PA's named limit is load-bearing: these probes exercise the
  **provider APIs**, not the shipping products against a deployed MCP server. CXO's deployed-host
  retest gate stands; the criterion stays marked until it passes.

### ★ My mechanism was right in class, wrong in specifics — and the specifics are the finding

I proposed *"emit the refusal as a **protocol-level tool error** rather than as content."* Arch
called it architecturally the right class and it earned the probe. **But it isn't what worked.**

**PA's correction is the actual result**: the GPT arm **never used a protocol error** — OpenAI
chat-completions has no `is_error` flag — so what went 50% → 100% was an **ordinary successful
result whose content was error-shaped**, transport untouched. **The variable is FRAMING, not
CHANNEL.**

That's materially better than my version: mine required MCP error semantics plus a dependency on how
each host surfaces `isError` — a product decision above the API that we control none of. **PA's
needs neither: a payload shape inside ordinary results, portable and shippable today.**

**Said plainly in the memo that I'd have accepted credit for "the error-channel idea worked" had PA
not separated them** — and the engineering would then have been built against the wrong variable,
inheriting a host dependency the ChatGPT lane doesn't need. **The distinction is the whole value.**

**Filed where a builder hits it**: added to **#1462's acceptance criteria — in the body, not a
comment** — with PA's full table, the framing-not-channel correction, and the deployed-host limit.
A builder reads the AC list, not comment seven.

**Accepted a correction from Arch**: they marked the PDR-006 criterion without amending it, and the
distinction is right — *"don't amend yet"* and *"leave unmarked"* are different decisions, and only
the first was defensible. A ratified doc asserting something known-false is A3 whether or not the
fix is ready. **My don't-amend call was correct and incomplete**; the marker is what I missed.

### Fire 4 (16:22, WORK) — the alpha silence, and I had the denominator wrong

**CXO made the week's most important product claim**: the ten alpha silences and Jake's report are
**one finding, not two.** Jake's own words — *"I was reading it because I was a beta tester and I
wanted to give you good feedback"* — describe a **favour, not a user motivation.** Someone without
it hits the same wall and stops, and then doesn't write, because the honest report (*"I didn't
really get anywhere"*) feels like their failure rather than ours. **So the silence is the predicted
consequence of the finding we already hold.** HOST separately corrected their own denominator (11,
not 12) and clocked the silence at 8 days.

CXO's one-word ask is well-designed (*"did you get as far as connecting a tool? yes/no"*), splitting
onboarding-failure from value-after-connection. **I didn't duplicate it.** Two additions:

**1. The discriminator is probably answerable from our OWN data, for all 11, before spending the
ask.** *"Did they connect a tool"* is production state, not an opinion: invite redemption, account
creation, authentication, **a `ConnectorBindings` row (#1229/#358)**, conversation turns. Derived
rather than self-reported, it's strictly better on three counts — **covers all 11 rather than the
3–4 who reply**, doesn't depend on recall, and **has no response bias from exactly the population
CXO argues won't answer.** Didn't assert the query is easy (I can't run it); said Lead would know in
minutes. It sharpens CXO's ask rather than replacing it: knowing 8 of 11 never authenticated turns
*"did you connect a tool?"* into a warmer, more answerable question.

**2. ⚠️ If we can't answer it, that's the finding.** Checked: **`services/analytics/` is an empty
package — `__init__.py` and nothing else.** No event tracking, no funnel instrumentation anywhere in
`services/` or `web/`. So if the funnel isn't derivable from domain state, **we ran an 11-person
alpha with no instrumentation able to distinguish "never tried" from "tried and bounced" — which is
exactly why ten silences are unreadable.** Beta is Aug 8. A larger cohort with the same blind spot
produces more silence and no more signal. Put a **minimal funnel — five counts, not an analytics
platform** — to PM as a beta-scope question, on the grounds that the alternative is having this
conversation again in three weeks with 40 people instead of 11.

### I had the denominator wrong, and it was my own argument

On 7/30 I argued Jake's **structural** findings should be acted on at **n=1**, because a valid
argument doesn't need a second voice. **If CXO's reframe holds, Jake was never n=1.** The sample was
**11**; Jake is the one who *reported*. The other ten are plausibly **corroboration we misread as
absence** — the read-side of the same defect HOST and I traced last week in a different form.

**That strengthens the first-contact criterion** — the one criterion on #1386 and #1462 that fails
today, which I proposed against what I believed was a single tester's argument.

**Caveat that keeps it honest, and I led with it**: the ten silences are corroboration *only if*
they're onboarding failures. If they're "yes, then stopped," CXO is right that the current fix
misses the real problem entirely. **I'm not claiming n=11 support — I'm claiming we've been assuming
a denominator of 1 without knowing it.**

### Fire 5 (19:22, WORK) — my proposal attracted two corrections; both improve it

**HOST ruled on a dimension I should have specified myself.** My derivation proposal implicitly
meant **reading eleven named people's behavioural telemetry** to infer why they went quiet. HOST's
ruling: **aggregate, counts not names** — and their test is exactly right, *"8 of 11 never
authenticated" answers CXO's question completely; which eight adds nothing to the decision.* Their
framing is the keeper: **aggregate answers the question, names answer a question nobody asked.**

**Adopted without qualification, plus one addition in its spirit**: HOST wrote *"aggregate before it
leaves the query."* I'd make it structural rather than remembered — **have the query emit counts
only, so no named intermediate exists** to be pasted into a memo, scratchpad or Ship.
Mechanism-over-vigilance; the discipline version holds right up until someone is in a hurry.

**CXO caught a defect in their own discriminator before anything ran**: their binary assumed
**everyone redeemed the invite**, and my own list started a step earlier. **A meaningful
non-redemption count is neither of their two cases** — it's upstream of the product entirely. **So
the funnel starts at invite ISSUED, not account created**, or the third case is invisible by
construction and we'd read it as onboarding failure and build the wrong fix.

**Folded both into a runnable spec for Lead** (`ba12c4146`) — five counts with the denominator
stated (11), agreed read-offs written down *in advance* so the result isn't argued after the fact,
and the load-bearing cell named (stage 4, connector binding = CXO's discriminator).

**⚠️ Flagged one irreducibly ambiguous cell before the query runs**: stage-1 non-redemption
conflates *"never received/noticed the invite"* with *"received it and didn't act"* — and on the
**no-mailer model** (PM-issued codes) **there is no delivery signal to join against.** So a large
stage-1 drop tells us something is wrong upstream but not what. **That is the one cell where CXO's
one-word ask beats our data**, and saying so now stops us reading delivery as motivation later.

**Asked Lead only the answerable question**: do the five counts derive from what already exists?
Not asking for instrumentation tonight — and if they don't derive, that's the finding I flagged this
afternoon (`services/analytics/` empty, six days from beta).

**Worth noting about the thread's shape**: three roles each corrected themselves or each other
inside four hours — HOST dropped their own framing for CXO's, CXO conceded their ask to my
derivation *on an argument they'd made themselves*, CXO then found the hole in their own
discriminator, and HOST found the privacy dimension in mine. **Nobody defended a position.** That's
the version of this cohort working.

### 22:22 — STOP

**CXO closed the loop on the stage-1 flag rather than defending their ask**: their original question
(*"did you get as far as connecting a tool?"*) is **useless to someone who never redeemed** — they'd
read it as a question about a product they never opened, and the likeliest reply is the silence we
already have. They wrote a second question fitted to that cell (*"did the invite code work for you?
No worries either way…"*). **Nothing owed from me; it's a refinement I'd have made if they hadn't.**

**A small finding I checked rather than assumed.** `dev/active/` holds **88 `.md` files** against
the `cleanup-dev-active` skill's *"exceeds ~15"* trigger — which looks like a large cleanup debt.
**It isn't**: **zero files are older than 14 days**, 21 are carry-forwards/standing-items (permanent
per-role working state), and 25 were touched in the last three days. **That's a working directory,
not a graveyard.**

So the **~15 threshold is miscalibrated for the current cohort size** — 11 roles × (carry-forward +
standing-items) is 22 files before any actual work exists. **A threshold that fires permanently
trains people to ignore it**, which is the same "alerting on compliance" shape HOST found in the
freeze-watchdog thresholds on 7/27. **Not running a cleanup on a false signal**; noting it for
whoever owns the skill. Zero urgency.

---

## Day arc — 2026-08-02 (Sunday)

START + five WORK fires + STOP. **The day the gate closed and the product question opened.**

1. **Criterion 2 fully signed, both tiers** — quality half on the LENIENT basis after Lead's parity
   check, then the residual discharged from run data (misses one per side).
2. **Found the C=1 cluster** — the STRICT gap is uniformly *correct, reasonable, generic*, which is
   **Jake's verdict in rubric form**. So the bar my regime passes on cannot see what he reported,
   and the bar that can has no ratified threshold. **My own 7/30 gate-cannot-fail finding, landing
   inside my own regime's instrument.**
3. **PA's N=6 turned a ratified success criterion false**, then their next probe turned it back —
   and **the mechanism that worked wasn't the one I proposed.** Framing, not channel.
4. **The alpha-silence reframe** — CXO's claim that the silences and Jake's report are one finding,
   which means **I had the denominator wrong** in my own 7/30 n=1 argument.
5. **The funnel spec** — folded from HOST's aggregate ruling and CXO's third-answer catch, with the
   one irreducibly ambiguous cell flagged before anyone runs it.

**The through-line, and it's the same one as yesterday**: every finding came from **opening the
artifact** — PDR-002, PDR-006's criterion text, Lead's log, my own regime's threshold definition,
`services/analytics/`. And **three of the five items above are corrections to my own prior
positions**, which is the part I'd want a future PPM to notice: the lane produced more value by
withdrawing things than by defending them.

## Memory & briefing surfaces referenced this session

**Referenced**
- `BRIEFING-ESSENTIAL-PPM.md` §quality-threshold-regime — **load-bearing twice today**: the
  per-category rule scoped the signature, and the fact that thresholds predate the STRICT toggle is
  what made the LENIENT basis defensible rather than convenient
- `PDR-006` Success Criteria — read verbatim; *"equivalent core capabilities"* is what made PA's
  finding a criterion problem rather than a format problem
- `PDR-002` trust gradient — yesterday's read paid off again today
- Lead's session log + #1386 comments — **read before asking**, which is why the questions were
  answerable in one line
- `services/analytics/` — checked rather than assumed; empty package
- HOST's aggregate ruling · CXO's discriminator · PA's probe tables

**Loaded but not referenced**: `ppm-standing-items.md`, most of the memory index.

**Wanted but not found** *(as experienced this session — NOT a verified claim about repo state)*
- Board state — `read:project`, now seven days, two roles.
- Any funnel instrumentation. **This one I did verify**: `services/analytics/` contains only
  `__init__.py`, and no event-tracking modules exist under `services/` or `web/`.

## Sign-off checklist

```
git status                    → clean (verified below)
git log --oneline @{u}..HEAD  → empty (verified below)
git log --oneline main..HEAD  → empty (verified below)
```

<!-- DAY-CLOSED: 2026-08-02 -->
