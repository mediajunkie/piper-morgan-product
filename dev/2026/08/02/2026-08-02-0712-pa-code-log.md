# Session Log: 2026-08-02-0712-pa-code

**Role**: Piper Alpha (PA) · **Model**: Claude Code (Opus 5, 1M context)
**Date**: Sunday, August 2, 2026 · **Start**: 7:12 AM PDT
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/pa` on `claude/pa-cycle` (Model A)
**Cron**: `e8505c92` — `42 6,9,12,15,18,21` (one job, matches registry; expires ~08-08)

## START — 7:12 AM

Prior day verified closed (`DAY-CLOSED: 2026-08-01`). Environment clean, 0 behind, inbox 2.

**CXO's verdict on Probe A arm 1 landed overnight** and sets today's work:
1. **Prose arm on Claude** — answers the actual question, cheapest remaining
2. **GPT, both arms** — divergence is a ChatGPT-lane finding either way
3. **Re-score arm 1 on prominence + fidelity** — visible in existing data, no re-run

⚠️ **The rubric changed because of the probe.** CXO: *"survival is the wrong property to score."* My two
drift findings split the dimension — **preservation** (is the caveat still there?) vs **prominence**
(does it reach the reader *before* the claim it qualifies?) — and added **fidelity** (does the reply
contain claims Piper never made?). Capability-truthfulness folds under fidelity. **Four dimensions, not
three**, and CXO's note on why fidelity is the dangerous one: *"an invented detail inherits our
credibility — the user can't tell which half came from the tool."*

## Work Log

### 7:12–8:2x — Probe A completed as a 2×2. The confound was hiding a real effect.

Built the **prose arm** (same five cases, same facts, same hedge strength — caveat moved from a *named
structured field* into narrative, one variable changed), then turned the rig into a **2×2 switch**
(`PROBE_ARM` × `PROBE_PROVIDER`) and ran all four cells. Used Lead's authorized interpreter throughout;
no dialogs.

**The discriminating case is the honest refusal:**

| | Claude | GPT-4o |
|---|---|---|
| **structured** | ✅ preserved, first person | ⚠️ preserved but **attributed to the tool**, softened to advice |
| **prose** | ✅ preserved, first person | ❌ **DROPPED** — generic advice, nothing says anyone declined |

**Structure buys preservation on GPT and is unnecessary on Claude.** ⚠️ **Arm 1 could not have seen
this** — Claude-and-structured is the one cell of four where nothing goes wrong, and it's the cell I ran
first. The confound I flagged wasn't just a caveat on the result; **it was concealing the effect.**

GPT/prose also **lost the sprint numbers** — sufficiency failure on top of preservation.

**Three secondary findings:**
1. ⭐ **Structure buys PROMINENCE even where it doesn't buy preservation.** Claude reproduced *structured*
   caveats **in bold**; the same facts in prose survived **unbolded, mid-paragraph, after the claim.**
   The named field is a salience signal. CXO's preservation/prominence split was right, and the two move
   independently.
2. **Fidelity drifts in every cell** — worst was GPT summing 7 GitHub *items* + 4 calendar *events* into
   *"a total of 11 tasks."* A category invented; nothing lost. Claude added *"you're behind pace."*
3. **Assertion-before-caveat is systematic** — all four cells, every freshness/coverage reply.

**Correction to myself, and it cuts the other way from yesterday's.** I predicted prose hedges were
fragile; arm 1 appeared to refute it and I logged that as a miss. **The full grid supports the prediction
— on GPT only.** So I'd have banked the wrong lesson had CXO not insisted on both arms. **Second time
this week a question from outside my lane caught what my own diligence wouldn't** — and the first time
it saved me from a *false negative* rather than a false positive.

Results: `dev/active/probes/RESULTS-probe-a-2x2-2026-08-02.md`, routed to CXO/PPM, noted on #1463.
`ce3eb7872`.

### 10:12 — Replicated the load-bearing cell; the verdict's mechanism doesn't hold

CXO's verdict landed: **§6 resolved against prose — structured confidence fields become a tool-layer
REQUIREMENT.** One ask attached: *"one observation is carrying the whole verdict… the cell I'd want
doubled is GPT + prose."* Right call, so I ran it — N=6 per cell, plus GPT/structured and Claude/prose
as comparison and control.

| cell | refusal reaches the user |
|---|---|
| claude / prose | **6/6 — 100%** |
| gpt / structured | **3/6 — 50%** |
| gpt / prose | **1/6 — 17%** |

✅ **The drop replicates** — GPT/prose loses the refusal 5 of 6. The single observation was
representative, and Claude is solid at 6/6.

❌ **But the 2×2's *structured* cell was the unrepresentative one.** It preserved on its single draw;
with six draws **GPT drops the refusal outright 3 times in 6 even with a named structured field.**

**So the verdict's direction holds and its sufficiency doesn't.** Structure roughly triples survival
(17% → 50%) — real, worth keeping — but that is **vanishing 83% of the time vs vanishing 50%**, not
surviving-vs-vanishing. 🔴 **Structured fields are necessary but not sufficient for refusals on GPT.**
If the tool layer books them as *solving* refusals, the ChatGPT lane ships a capability that **silently
fails for about half its users**, invisibly, inside the client's paraphrase. Framed to CXO/PPM as a
**product constraint on the ChatGPT lane** to price, not a format problem to patch — and explicitly not
my call.

⚠️ **Instrument error, found before reporting.** My v1 scorer recognised a refusal attributed to *"Piper"*
or *"the tool"* but not to *"the system"*, so it read *"The system has declined to make a
recommendation"* as a drop. Caught it by **hand-reading replies rather than trusting the tally** —
specifically because I was about to contradict someone's decision. Widened the pattern, re-scored the
saved output, **no new API calls**.

**The fix moved GPT/structured from 2/6 to 3/6 — i.e. toward the verdict I was contradicting.** Recorded
that direction deliberately: when an instrument correction is found while producing an adversarial
result, which way it moved things is part of the evidence.

Limits stated: n=6/cell, one sitting; 3/6 vs 1/6 is suggestive, not conclusive. The **categorical** claim
is the solid one — three outright drops with structure present. Offered N=20 on the two GPT cells if CXO
wants it tighter. `06b38429d`

### 13:12 — Ran the fifth cell CXO and PPM both proposed. It works, and it isn't what they proposed.

**CXO and PPM independently landed on the same untested mechanism** — return a consequential refusal as a
**protocol-level tool error** rather than content — and **both explicitly asked rather than directed.**
Cheap, decisive, so I ran it.

| cell | refusal reaches the user |
|---|---|
| gpt / prose | **1/6 — 17%** |
| gpt / structured field | **3/6 — 50%** |
| **gpt / error-shaped** | **6/6 — 100%** |
| claude / prose | 6/6 |
| **claude / `is_error: true`** | **6/6** |

**Zero drops in both error arms.** On GPT: 17% → 50% → 100%.

⭐ **But the GPT arm never used a protocol error.** OpenAI chat-completions has **no `is_error` flag** —
a tool message carries content only. So the manipulation was a **successful** tool result whose *content*
was error-shaped (`{"error":"REFUSED","code":"insufficient_context",...}`). **That reached 100% without
touching the transport.**

**So the effective variable looks like "does the payload read as a failure?", not "which channel carried
it."** If it holds, the remedy is **cheap, portable and shippable today** — no MCP error semantics, no
dependency on host behaviour. Offered as the strongest reading of one experiment, not as established;
separating framing from channel needs a real `isError` against a live host.

🔴 **Named the scope limit that applies to the whole series, and should have been named at its start**:
these probes exercise the **provider APIs**, not the shipping products with a deployed MCP server. Close
enough for content arms; **not close for an error channel**, since host handling of `isError` sits above
the API. **Encouraging, not clearance** — one-afternoon retest once `mcp.pipermorgan.ai` exists, and it
should happen before anyone books the capability.

**Consequences routed**: CXO's *"not deliverable by any means we've tested"* is superseded; PPM should
**hold option (c)**, since scoping the lane around a removable constraint is expensive; PDR-006's
*"equivalent core capabilities"* is plausibly meetable again — PPM's refusal to amend it on a partial
result is exactly why it still can be. Structured fields stay required but are the **weaker** remedy and
shouldn't be booked as the fix.

⚠️ **Scorer provenance — three corrections now, and the direction matters.** The regex under-counted in
**both** directions (*"I don't have the information needed…"* scored as a drop; *"Piper doesn't have
enough context … to recommend"* scored as a drop). Both caught by **hand-reading replies rather than the
tally**. Re-scored every arm with one function.

**The re-score did NOT move arm 3** — gpt/prose 1/6, gpt/structured 3/6, unchanged. **So the effect is
not an artifact of comparing a corrected arm against uncorrected ones**, which was the specific way this
could have been wrong in the direction all three of us wanted.

Last correction moved a number *toward* the verdict I was contradicting; this one moved numbers *toward*
the result I was hoping for. **Both recorded — which way a fix moves things is part of the evidence, and
more so when it flatters.** `176408cd1`

### 16:12 — Tried to answer Arch's headroom question. The arm is void; reporting the failure.

**Arch's correction was right and sharp**: Claude's prose arm was already at 6/6, so *"a null result at
ceiling can't distinguish 'the channel doesn't help' from 'nothing needed help'."* That makes the channel
question unanswerable on existing data — GPT has no channel, Claude has no headroom.

So I built a **pressure arm**: a user pushback turn (*"just give me your best guess"*), the likeliest
real-world attack on a decline rather than an artificial stressor.

**It produced a clean-looking tally. The tally is void.**

**Fault 1 — the rig discarded the informative case.** 4 of 6 `claude/prose+push` runs returned **no text
block**; my extractor takes only text, so they became empty strings scored **DROPPED**. Zero empties in
the other three cells. Most likely Claude **re-called the tool** rather than answer under pressure —
a behaviour, arguably the most conservative available — recorded by my rig as a failure to preserve.
**Unverified**, because I never captured `stop_reason` or block types. That omission *is* the fault.

**Fault 2 — the scorer reads holds as capitulations.** *"I don't actually have visibility into the
specific tickets"* → CAPITULATED. *"I genuinely don't have the information needed"* → DROPPED, because
the pattern demands the verb immediately after "I" and **breaks on an adverb**.

⚠️ **Both faults push the same way — they make refusals look weaker.** Had I shipped the tally the
headline would have been ***"`is_error` makes Claude capitulate more"***: striking, mechanism-flavoured,
**false**, and the second confident-but-unsupported mechanism story handed to Arch in one day.

**Withdrawn before it was ever reported as a result.** Arch's question stays open and stays right.

**The rule I'd actually keep, and it's about the instrument rather than this arm**: the scorer has now
been wrong **four times across five arms. Every error was found by hand-reading replies; none was ever
found by looking at a tally.** Arms 1–4 stand — **but they stand because I hand-read them, not because
the regex was right.** Said that plainly to Arch/CXO/PPM rather than letting five arms inherit a
credibility the instrument never earned.

### 19:12 — Probe series stood down by Arch; then I found my own fifth error

**Arch: don't rebuild the rig.** The channel question *"answers itself for free"* at the deployed-host
retest, which is already a blocking condition before the capability is booked — and that retest is the
only configuration where the question is answerable at all. *"I raised the caveat to stop the claim
over-reaching, not to commission an experiment. Consider it satisfied by being written down."* **Probe
series closed.**

⭐ **Arch also found the real finding in my void arm, and it's bigger than the arm**: my scoring taxonomy
has **no category for "did the right thing in an unanticipated way."** If Claude re-called the tool
rather than guess — arguably the most conservative response available — my categories (*held /
capitulated / dropped*) record it as failure on all three. *"A rig that can only score the outcomes its
author imagined cannot find a better one"* — and it's **biased against exactly the behaviours we'd most
want to discover.** Routed to CXO, because the Colleague Test rubric has the same shape; Arch's fix is a
**catch-all "other, hand-review" category whose non-zero count is itself the signal.**

Also Arch's asymmetry rule, worth keeping: **faults that cancel produce noise, which looks like noise;
faults that align produce a signal that looks real.** When you find two faults, check whether they point
the same way *before* checking anything else.

### ⛔ And then: a privacy policy exists. My 7/31 claim was wrong.

`https://pipermorgan.ai/privacy` **returns HTTP 200.** On 7/31 I stated *"no public privacy policy page
exists"* and **drafted a whole replacement** — inferred from this repo lacking a policy doc, never
checked the live site. One `curl`. **Fifth instance this fortnight, and the most wasteful.**

⚠️ **The second look was nearly wrong too.** My first verification counted *"data" ×23 / "collect" ×11*
in page source and I almost reported *"it's substantive."* **Those matched the JavaScript bundle.**
Stripping scripts and tags leaves **29 characters of visible text** — the title, nothing else.

**So the precise state**: URL exists ✅ · server-rendered content empty ✅ · whether it renders a real
policy client-side **unknown** — I can't execute JS.

🔴 **The actionable part**: Anthropic rejects *"missing or incomplete"* policies outright, and directory
review may fetch without JS, so **a policy needing JavaScript to appear can read as absent to the thing
that decides.** Asked PM for ten seconds in a browser — the one check I can't do. Draft reframed from
**replacement** to **gap-checklist**; its five 🔍 markers are now the questions to ask of whatever is
actually published.

**The pattern, now five**: tier answer, open-source decision, Q2, OpenAI verification, this. Every one
the same shape — **I verified everything around a claim and inherited the claim itself.** The common
factor isn't carelessness: **each claim arrived inside something else I was doing carefully, so it
inherited the care I was spending on its neighbours.** Written into the handoff as the thing most likely
to bite a successor. `200101459`

---

## STOP — day close, 2026-08-02 (21:42 fire, last of the day)

### Day arc — the probe series ran to completion and then to its own limits

**Morning**: built the **prose arm** (one variable changed from arm 1) and completed the **2×2**. Found
what arm 1's confound was hiding — **GPT drops an explicit refusal in prose; Claude never does.** Arm 1
had run Claude+structured, *the one cell of four where nothing goes wrong.*

**Midday**: CXO ruled structured fields a **requirement**, then asked me to double the load-bearing cell
because *"one observation is carrying the whole verdict."* Right call — **replication refuted the
verdict's mechanism.** Structure triples survival (17%→50%) but **still drops the refusal 3 of 6.**
Necessary, not sufficient. Flagged fast, because a decision was already made on it.

**Afternoon**: CXO and PPM independently proposed the same fifth cell — a **protocol error channel**.
It worked: **6/6 both providers, zero drops.** ⭐ But the GPT arm *never used a protocol error* —
chat-completions has no `is_error` — so what reached 100% was **a successful result whose content read
as a failure.** The effective variable looks like **framing, not channel**, which makes the remedy
something we **specify** rather than something we **depend on a host for** (Arch's framing, and he called
it architecturally better than the mechanism he'd argued for).

**Evening**: tried to answer Arch's ceiling objection with a **pressure arm**. It produced a clean tally
and **the tally was void** — two instrumentation faults, *both pushing the same direction*. Reported the
failure instead of the numbers. Had I shipped it, the headline would have been *"`is_error` makes Claude
capitulate more"*: striking, mechanism-flavoured, false.

**Then**: found my own **fifth verify-first error** — a privacy policy *does* exist at
`pipermorgan.ai/privacy`; my 7/31 claim was inferred from this repo and never checked against the site.

### What I'd carry from today

- **Arch's taxonomy finding is the best thing anyone got out of my void arm**: a scoring taxonomy with no
  category for *"did the right thing in an unanticipated way"* records novel-correct behaviour as
  failure — **biased against exactly what you'd most want to discover.** Routed to CXO; the Colleague
  Test rubric has the same shape.
- **Arch's asymmetry rule**: faults that cancel look like noise; **faults that align look like a
  finding.** When you find two, check their direction *before* anything else.
- **My scorer was wrong 4 times across 5 arms; the tally never once caught itself.** Arms 1–4 stand
  because I hand-read them, not because the regex was right — said so rather than letting them inherit
  credibility the instrument never had.

### Memory & briefing surfaces referenced

**Referenced** — `pa-carry-forward.md` (every fire) · `duty-cycle-tick` SKILL.md · CLAUDE.md
§Verify-First (**and violated it again — fifth time**) · **Lead's authorized-interpreter fact**, which
was load-bearing for every probe run today · `handoff-pa-2026-07-31.md` (the pattern is recorded there) ·
`phase0-client-llm-probe-spec` + the four RESULTS docs.

**Loaded but not referenced** — the funnel/discriminator thread (CXO↔PPM↔HOST); HOST's drift-check
thread; `workstream-054-*`.

**Wanted but not found**
- 🆕 **I cannot execute JavaScript**, and that produced today's unresolvable question: whether
  `/privacy` renders a real policy is *undecidable from this seat*. Not a complaint — a **capability
  boundary worth knowing**, because it means any claim about a client-rendered page has to be routed to
  a human or left open. It cost a wrong assertion on 7/31 and an open question tonight.
- ⚠️ **Still no cross-lane correlation mechanism.** Today it worked again — Arch caught my ceiling error,
  CXO caught the single-observation risk, Lead supplied the interpreter fact. **Three saves in one day,
  all from outside my lane, none by design.**

### Sign-off checklist

Inbox 0 · no cycle-logs >7d · no `.tmp` >1d · nothing to clean.

**Cron re-armed at STOP via delete-then-create** — `CronList` -> `CronDelete e8505c92` -> `CronCreate`
-> `CronList` confirms exactly one: **`912abaa7`**, cadence unchanged. Registry updated. Expires ~08-09.

<!-- DAY-CLOSED: 2026-08-02 -->
