# Session Log — 2026-08-02 18:48 — Coding Agent (prog-code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Branch**: worktree-agent-a8c939a9433b33edf (worktree at .claude/worktrees/agent-a8c939a9433b33edf)
**Task**: #1460 — original_message instance fix: backfill attribute on detect_multiple path + make 6 live attribute-only reader sites two-surface-safe + fix 2 half-safe Slack fallback keys
**Constraints**: NO push to origin/main (Lead merges after composition audit). No classifier prompt changes. Do not touch #1459 class-fix scope.

## Plan
1. Read issue #1460, #1459 trace, intent-routing-stack.md (done)
2. Verify cited line numbers against current code (locate by content)
3. Failing-first regression tests (writer-side backfill + reader-side idiom B)
4. Writer-side cure in detect_multiple_intents
5. Reader-side idiom B at 6 sites; Slack fallback-key fix x2
6. Runtime-style verification via in-process e2e app (POST /api/v1/intent)
7. Commit to worktree branch; report

## Entries
- 18:48 — Session start. Read #1460 body, #1459 first comment (trace), intent-routing-stack.md.
- 18:55 — Verified all cited line numbers by content. All 5 named reader sites located: `_detect_setup_request` (:1967), `_detect_agenda_request` (:2286), `_detect_retrospective_request` (:2585), `_detect_last_activity_request` (:2798), `_detect_duration_request` (:3532). Writer site confirmed at pre_classifier.py:1819-1824 (Intent built with dict-only original_message; classify_multiple returns it before the classify() backfill — confirmed at classifier.py ~916-950).
- 19:00 — **6th-site audit result: NO additional LIVE attribute-only reader exists.** Exhaustive sweep of `.original_message` attribute reads + all external CanonicalHandlers entry points (`handle` callers: intent_service :1396, orchestrator :204, Slack webhook subcommands :1236/:1278/:1320 — all Slack subcommands construct Intents WITH the attribute set; issue_intelligence.py:88 has zero live callers; commands/definitions.py handler_name is declarative metadata, never executed). Remaining attribute-only detectors: 4 STATUS detectors (:433/:478/:506/:594) + `_detect_priority_recommendation_request` (:3498) — all reachable today only via attr-setting Slack subcommands. Converted `_detect_priority_recommendation_request` as the audited 6th (nearest-risk: PRIORITY is a category detect_multiple_intents emits); left the 4 STATUS detectors for #1459's class accessor (documented).
- 19:01 — Failing-first: 11 of 14 new unit tests failed pre-fix for exactly the traced reasons (attribute empty; detectors dead on dict-only intents; both Slack sites bailing "No message available"). Implemented: writer-side backfill (pre_classifier), 6 reader sites → idiom B (canonical_handlers), 2 Slack fallback keys corrected (`context.get("message")` → `original_message`; verified NOTHING ever writes context["message"]). 14/14 pass.
- 19:02 — Runtime e2e (in-process ASGI, POST /api/v1/intent, real Postgres 5433): AC-1 'help me setup my projects' → action `provide_setup_guidance`, not floor. PASSES. AC-2 exemplar finding: **'What's my schedule today and show my todos' cannot reach the orchestrator's TEMPORAL handler at all** — 'my schedule today' matches CALENDAR_QUERY_PATTERNS, #919 subsumption drops TEMPORAL, both halves are QUERY, orchestrator `can_handle` has no QUERY → generic fallback regardless of plumbing. Pinned AC-2 with variant 'What does my schedule look like today and show my todos' (genuinely TEMPORAL+QUERY, probe-verified). Filed **#1473** for the QUERY+QUERY orchestrator gap.
- 19:05 — AC-2 e2e exposed TWO latent downstream bugs in the newly-reachable agenda path: (1) `get_todos_by_owner(status=TodoStatus.PENDING)` → asyncpg DataError (String column vs raw Enum — filter has likely never worked; only caller was gate-dead); (2) `"todo_count": len(todos)` crashes on the #1425 None sentinel → whole agenda response became error_fallback. Fixed both minimally (repo .value normalization in get_todos_by_owner only + None guard); filed **#1472** for the remaining raw-enum comparison sites (:101/:116/canonical :2663/:3025).
- 19:10 — Regression sweep: tests/unit/services/intent_service (full) + pre_classifier + multi_intent + Slack suites = 1976 passed/1 skipped. Smoke suite (`-m smoke`, --ignore=tests/archive as pytest.ini does) = 531 passed. Architecture enforcement + routing vocabulary ratchets = 38 passed. Both e2e tests pass.
- 19:15 — Committing in two scoped commits (wiring fix; discovered-work unblockers + e2e). NOT pushing per task HARD RULE — Lead merges after composition audit.

## Discovered issues filed
- #1472 — raw TodoStatus/TodoPriority enum comparisons vs String columns (class; 2 sites fixed here as unblockers, 4 remain)
- #1473 — orchestrator can't handle QUERY-category intents; QUERY+QUERY multi-intent turns (incl. #1460's AC exemplar) return generic fallback

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (worktree/commit/mailbox rules, auto-close gotcha, test-env stripping), docs/internal/architecture/current/intent-routing-stack.md (4-surface chain — shaped the 6th-site audit), #1459 first comment + mailboxes/arch/read Lead trace memo + dev/2026/07/30 arch/lead logs (evidence base), tests/e2e/test_canonical_conversations.py (fixture pattern for the e2e harness), memory: feedback_verify_at_the_right_layer (runtime e2e not just mocks), feedback_no_flattened_commands_without_referents (read whole issue+trace before acting).
- **Loaded but not referenced**: MEMORY.md index bulk, briefing table.
- **Wanted but not found**: an explicit enumeration of the "6 live reader sites" (the trace names 5; the 6th had to be audited from scratch — resolved, but the issue text implied a list existed).
