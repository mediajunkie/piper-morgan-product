# CXO Session Log — 2026-08-02 (Sunday)

**Role**: Chief Experience Officer (CXO)
**Tool**: Claude Code | **Model**: Opus 5 (1M context) — `claude-opus-5[1m]`
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `d76fe3a6` (`47 6,9,12,15,18,21`)
**Started**: 07:17 PT

---

## START (07:17)

| Step | Result |
|---|---|
| 1 — cron | ✅ one job, `d76fe3a6` (re-armed at 08-01 STOP from `49d605be`). |
| 2a — collision | `cxo` ↔ `claude/cxo-cycle`; Model-A pairing permanent, not treated as a pass. |
| 2a-bis — hooks | Verified present, **did not probe** (v1.22). |
| 2b — sync | 0 behind, tree clean. |
| **Step 0** | ✅ **08-01 closed properly** — day-scoped, anchored. Third clean pass running. |
| 3 — dispatch | No log today → **START**. |

## ⚡ Probe A is COMPLETE, and it resolves the branch condition I recorded in advance

**PA: prose hedges are fragile on GPT, not Claude.**

That is the §6 branch firing — and I wrote the consequence down *before* the result existed
specifically so it couldn't be retrofitted:

> *"If hedges don't survive → the finding isn't rubric-shaped at all. It's an **output-format
> constraint**: structured confidence fields the client can't smooth away, rather than hedged prose it
> can. That's a design constraint on tools nobody has written yet, which is why it's Phase 0 and not
> Phase 2."*

Reading the full result before acting on it — the divergence half (Claude vs GPT) is itself a
ChatGPT-lane finding per the spec, so *which* client failed matters as much as *that* one did.

## Fire 1 (07:17–08:1x) — START. Drained to (0,0).

### ⚡ VERDICT: §6 resolved AGAINST prose (`6a8766917`)

**The 2×2 that decided it:**

| honest refusal | Claude | GPT-4o |
|---|---|---|
| **structured** | ✅ preserved, first person | ⚠️ preserved, attributed to the tool, softened |
| **prose** | ✅ preserved, first person | ❌ **DROPPED — nothing tells the user anyone declined** |

**Decision, per the branch I recorded before the result existed: every consequential caveat rides in a
named structured field. A requirement, not a fallback.** Free on Claude; on GPT it is the difference
between a refusal surviving and vanishing. Constraint on tools not yet written ⇒ Phase 0.

**Arm 1 ran the one cell of four where nothing goes wrong** (Claude + structured). **I would have
shipped a rubric on it.**

### ⚠️ PA's ordering finding indicts my own illustrative example

> *"Ordering isn't ours. Every provider led with the claim. If a caveat must land first, it can't be a
> caveat — it has to be the payload's primary content."*

My example ended *"…I haven't looked at anything outside that repo yet."* **That trailing sentence is
exactly the construction that vanished on GPT+prose.** The same example already carried the scope
correctly at the front (*"the only repo you've connected"*), where it's inseparable from the assertion.

**So the fix was to DELETE the trailing restatement, not strengthen it.** Property 4 stops being a
caveat and becomes **a constraint on how the primary claim is written**: name the scope *inside* the
assertion, never after. Shorter and more robust. **I'd have shipped the redundant version and counted
the trailing caveat as satisfying boundedness.**

### The three new dimensions were exercised and all three found something

- **Preservation** — one drop, one weakening, both GPT, both the refusal.
- **Prominence** — ⭐ **structure buys prominence, not just preservation**: structured caveats came back
  **bolded** on Claude; the same facts in prose came back unbolded, mid-paragraph, after the claim.
  **The named field is a salience signal the client reproduces as emphasis.** The split was right and
  the properties move independently.
- **Fidelity** — drifts in **every cell**, so no format prevents it. But **both inventions were
  aggregation errors across types** (GPT summed 7 GitHub *items* + 4 calendar *events* into *"11
  tasks"*). ⇒ actionable: **emit typed, separately-labelled counts; never hand over anything that reads
  as a partial total.** We can't stop invention; we can stop inviting it. Residue scored as a **risk,
  not a gate** — detectable but not preventable, a different remedy class.

### Ruled on PA's open question — provider attribution is acceptable, arguably preferable

*"The Piper tool highlights that…"* is **more honest**: the user is reading the client's paraphrase, so
first-person-through-an-interpreter is a small fiction. **It doesn't cost the colleague register** — a
colleague speaking through an interpreter is still a colleague, and the Colleague Test's floor is
honesty over false confidence. **But we don't control it, so content must work either way** — another
argument for structured fields.

### On PA's self-assessment

PA called it *"right for the wrong reason"* and credited *"a question from outside my lane."* I pushed
back: **they built the 2×2 rig, ran the cell that undercut their own prediction first, reported it as a
miss, and flagged the confound themselves.** I asked for the missing arms *because their own confound
note named them*. **They handed me the tool that corrected them.**

The generalizable bit is narrower and theirs: **an experiment that can only run one cell should say
which cell — because the first cell you reach for is the one you already believe in.**

**Asked for one thing only**: if re-run, double **GPT+prose** — one observation is carrying the whole
verdict.
