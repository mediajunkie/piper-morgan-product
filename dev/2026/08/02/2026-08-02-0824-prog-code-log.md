# Session Log — 2026-08-02 08:24 — Coding Agent (prog, Claude Code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1464 — PortfolioService archive/restore crashes against real ProjectRepository (inherited BaseRepository.update refreshes a domain object → UnmappedInstanceError)
**Branch**: worktree-agent-a938a8a5e3d68f13f (worktree, 0 behind origin/main at start)

## Investigation (verify-first)

- Read issue #1464 in full + Lead's triage comment (LIVE via chat: `canonical_handlers.py:4111` archive / `:4198` restore; broad `except Exception` at `:4297` is the #1423 swallow shape — confirmed present).
- `services/database/repositories.py`:
  - `BaseRepository.update` (:151) calls `self.get_by_id(id)` then `setattr` + `session.refresh(entity)` — crashes when subclass `get_by_id` returns a DOMAIN object.
  - `BaseRepository.delete` (:163) has a SECOND latent bug: `self.session.delete(entity)` is **un-awaited** — in SQLAlchemy 2.0.23 `AsyncSession.delete` is a coroutine, so base delete has been a silent no-op (returns True, row survives) for every subclass, not just domain-returning ones.
  - `ProjectRepository.get_by_id` (:260) returns `domain.Project` → both inherited mutations broken.
- Callers of the broken paths: `services/onboarding/portfolio_service.py` (:261 archive, :320 restore, :396 delete — chat-reachable) and `web/api/routes/projects.py` (:467 PATCH update, :534 DELETE — REST-reachable; also `:1228` integration delete hits the no-op-delete bug).
- Neighboring idiom for mutations: `session.get(ORMClass, id)` + `await session.delete(row)` (ArtifactRepository.delete/update_title, ConversationRepository.save_turn, RepositoryRepository.unlink_from_project).
- Fix shape chosen: base-level — issue's own sketch offers "split the base's fetch into a private `_get_db_row` used by mutations". Fixes ProjectRepository (live), RepositoryRepository (latent, same shape), and the un-awaited delete for everyone. No subclass overrides needed; REST route only reads scalar columns off the update return, so an ORM row satisfies it.

## Class sweep (AC)

Repos overriding `get_by_id` to return domain objects:
- `ProjectRepository` — LIVE (chat + REST callers). Fixed by base change.
- `RepositoryRepository` (:776) — same shape, NO current callers of inherited update/delete (latent). Fixed by base change.
- `ConversationRepository` (:1587) — domain return, but `model = None` and it has its own `delete_conversation`/`update_title`; inherited mutations unreachable/unusable either way. No action.
- `ArtifactRepository` (:2727) — domain return, but defines its OWN `delete`/`update_title` (correct idiom); no `model` set so inherited `update` is unusable and uncalled. No action.
- `services/repositories/*` (Todo/UniversalList/File/UserTrustProfile) — no `get_by_id` overrides; use their own `update_*`/`delete_*` methods. No action.
- `StandupConversationRepository` — not a BaseRepository subclass; own update/delete. No action.

## Discovered work

- Restore-by-name chat path (`canonical_handlers.py` restore → `find_project_by_name(include_archived=True)`) can NEVER find an archived project: `ProjectRepository.find_by_name` hard-filters `is_archived == False` regardless of the service flag. Distinct bug (query filter, not domain-vs-ORM) — filing tracking issue, not fixing here (scope discipline).

## Work

1. **Failing-first tests** — `tests/unit/services/database/test_project_archive_restore_1464.py` (8 tests: repo update/delete, service archive/restore/delete + not-owner guard, chat-path archive via `CanonicalHandlers._handle_portfolio_query`). Pre-fix run: **6 failed, 2 passed** — failures were exactly UnmappedInstanceError (chat test showed it swallowed by the #1423 broad except into "I'm having trouble managing projects right now.", confirming Lead's triage hypothesis) + `RuntimeWarning: coroutine 'AsyncSession.delete' was never awaited` (base delete was a silent no-op returning True for EVERY subclass — second latent bug).
2. **Fix** (`services/database/repositories.py`, base-level per the issue's own "_get_db_row" sketch, matching the ArtifactRepository/save_turn `session.get` idiom):
   - New `BaseRepository._get_db_row(id)` = `await self.session.get(self.model, id)`.
   - `update`/`delete` use it instead of `self.get_by_id(id)` (which subclasses override to return domain objects).
   - `delete` now `await`s `session.delete(entity)` — fixes the universal silent no-op (also un-breaks `DELETE .../integrations/{id}` via ProjectIntegrationRepository).
   - No schema changes, no subclass overrides needed; REST route (`projects.py:467`) reads only scalar columns off the update return, so the ORM-row return satisfies it.
3. **1431 fake-repo docstring updated** (AC 2): `_FakeProjectRepository` in `test_list_archived_projects_1431.py` now notes the blocker is fixed and real-repo service-flow coverage lives in the 1464 file; fake kept as fast no-DB sanity check.
4. **Post-fix verification**: 1464 + 1431 files → **12 passed**. Regression subset (tests/unit/services/database/, test_projects.py routes, file/workflow repository migration tests, test_handle_projects_query_1102.py, test_portfolio_service.py) → **146 passed, 59 skipped** (skips pre-existing: portfolio service file is module-skipped per ADR-059, as the issue notes), 0 failures.
5. **Class sweep posted** as comment on #1464 (AC 3): ProjectRepository (live, fixed), RepositoryRepository (latent same-shape, fixed by same base change), Conversation/Artifact repos (domain-returning get_by_id but inherited mutations unreachable/unused — no action), all others no override.
6. **Discovered work**: filed **#1470** — restore-by-name chat path can never find an archived project (`ProjectRepository.find_by_name` hard-filters `is_archived == False`; the service's `include_archived=True` only post-filters). Distinct shape, out of #1464 scope.

## Evidence

- Failing-first: `6 failed, 2 passed, 2 warnings in 1.72s`
- Post-fix: `12 passed in 1.51s`; regression: `146 passed, 59 skipped, 7 warnings in 4.42s`
- Command: `POSTGRES_PORT=5433 …/lead/venv/bin/python -m pytest <paths> -o addopts="--import-mode=importlib" -v` (ANTHROPIC_* vars unset)

