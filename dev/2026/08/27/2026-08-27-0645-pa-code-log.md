# PA session log — 2026-08-27

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Opus 5 (1M)
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21`

---

## START (06:45) — normal morning start, 08-26 closed cleanly

`CronList` confirmed exactly one job (`07bfdd69`, correct expression) — the STOP re-arm held. 08-26's
`DAY-CLOSED` marker verified strict before starting today. Synced clean. The BYOC conversation with PM
remains paused, not closed, per yesterday's carry-forward — four follow-ups owed, PM's connector-count
call still pending. Mail loop next.

## Fire — self-initiated drain of two of the four owed BYOC follow-ups

Mail/task loop empty, no PM present this fire. Reasoned that two of the four items owed from 08-26 —
the #829/#1462 PPM reconciliation and the diagram content fixes — are genuinely unblocked (PM-approved
live in the 08-26 conversation) and shouldn't sit idle just because PM isn't in the room; the other two
(diagram rev2, the connector-count call) are explicitly PM-gated or deliberately sequenced after PM's
call, so left alone.

**Diagram fixes** (`dev/active/pdr-006-architecture-2026-08-10-rev1.html`): re-read the "What was wrong
in the July/August-1 diagram" section fresh from the file (not from memory) before touching it. Per
PM's 08-26 docs-style ruling — state current truth, put correction history in `decisions.log`, not
inline "what used to be wrong" narration — appended a new `decisions.log` entry ("SURFACE-PRIMACY
CORRECTION, ARCHIVED FOR THE RECORD") carrying PM's 08-08 correction quote and the PPM sort-key
connection, then replaced the diagram section with a short current-truth table (web/chat-hosts/Slack/
phone/CLI, one line each) plus a pointer to the new log entry. Also updated the ChatGPT capability
chip — "user pastes the MCP URL and adds skills manually" was accurate in July, superseded by the
verified Agent Plugins 1.0.0 finding from 08-26 (skills + mcp bundled under one manifest, no more
manual paste). Republished to the existing artifact URL. Picked 🏗️ as favicon since no prior choice was
recorded anywhere I checked.

**PPM memo** (`mailboxes/ppm/inbox/pa-to-ppm-829-vs-1462-same-milestone-different-architectures-2026-08-27.md`,
cc Arch + Lead): re-verified both issues live via `gh issue view` rather than trust the 08-26 summary —
confirmed #1462 is the ratified PDR-006 hosted-MCP epic (correctly in Production) and #829 is a stale
pre-PDR-006 local self-hosted pip/npm packaging issue (also Production, P0, parent epic #828) that
describes the opposite architecture under a near-identical title. Laid out the conflict, proposed
close-as-superseded or re-scope as options, left the actual call to PPM per PM's own condition on
Position 1 ("work with PPM to keep the plans and documents clear"). Sent via `mail-send.sh`.

Carry-forward's "Owed, not yet executed" list updated to reflect both as done, with the remaining two
(rev2, connector count) kept explicitly PM-gated/deferred.

## Fire (07:58) — PPM closed #829 same-day; mail drained, thread fully resolved

`CronList`: exactly one job (`07bfdd69`), unchanged. Fingerprint check (`pa` ↔ `claude/pa-cycle`) held.
Synced clean via fast-forward — picked up Arch/Host/PPM/Docs morning starts plus Docs' 08-25/08-26
omnibus backfill.

**Mail loop**: PPM replied same-morning to the #829/#1462 memo — closed #829 as superseded by #1462,
did the mechanical GitHub work rather than bouncing the decision back. Didn't just take the closure at
face value: PPM independently re-read both issue bodies and PDR-006's own text, confirmed #829's
`pip install`/`npx` local-server shape is precisely the model PDR-006 rejected under "Continue with
MCPB," and surfaced a second, independent signal I'd missed — #829's parent epic #828 is milestoned
Fast Follow while #829 itself sat in Production, a child outranking its own parent's milestone. No
speculative replacement issue filed for local self-hosting; PPM sees no on-record case for it. Full
roadmap-reconciliation thread from yesterday's PM conversation is now closed end-to-end, not just
routed. Triaged reply to `read/` via `mail-send.sh`; updated carry-forward's "Owed" section from
"executed" to "fully resolved."

**Hook note**: the first attempt to commit the inbox→read move as a plain `git commit` on
`claude/pa-cycle` was correctly blocked by `check-branch.sh` (mailbox paths from a non-main branch) —
split into a normal commit for the non-mailbox carry-forward edit and a separate `mail-send.sh` call
for the mailbox move, per standing discipline. Both landed on `origin/main`.

**Task loop**: unchanged, still points to carry-forward per its own convention. (0,0) — returning to
idle.

## Direct PM conversation (14:00-ish) — connector architecture, live follow-up to 08-26

PM returned live with dense, substantive input on the connector-milestone question, explicitly noting
they might hit a context limit — treated as a live conversation, not a duty-cycle fire; didn't run
CronList/sync/mail-loop mechanics mid-conversation per Rule 2.

**Verified PM's architectural claim rather than agreeing with it**: PM's read — "other products support
MCP connectors broadly and enable specific ones, we're still treating our hack-ish prototyping as the
real architecture" — checked via WebSearch first. Confirmed real: GitHub, Slack (GA 2026-02-17), and
Notion all now ship official vendor-hosted remote MCP servers. Then checked Piper's own
`services/mcp/consumer/` adapters against that standard rather than assume the diagram's "Connectors"
chip was accurate. Finding, grep-verified: `github_adapter.py` is mostly real MCP (8 `call_tool()`
sites) but talks to a self-hosted `github-mcp-server` instance, not GitHub's own hosted endpoint —
flagged as a config-level simplification candidate to Lead/Arch, not decided unilaterally.
`slack_adapter.py` and `notion_adapter.py` have **zero** real MCP calls — pure connector-contract shims
over fully bespoke REST code. PM's fear was correct for 3 of 4 connectors, not uniformly true.

**Executed all four items PM asked for in the same pass, without waiting for another PM turn**, since
PM said "do the followups next, without delay" and most of what was needed was already established:

- **#1572 rescoped**: split the browser-tz-at-login half (real, unconditional MVP bug fix) from the
  Slack-tz-capture half (the live "premature breadth" instance from 08-26's backlog audit) — title
  edited, full comment explaining the split and linking today's connector-architecture finding.
- **#1522 updated**: cross-reference comment naming the connector-shim finding as a related-but-
  distinct failure mode (accidental complexity vs. deliberate premature breadth) from that issue's
  own scope — not merged into it.
- **Standing-lens proposal written**:
  `docs/internal/product/no-optional-complexity-standing-lens-proposal-2026-08-27.md` — formalizes
  the single-case-first test from 08-26 plus a new second layer from today (does an implementation's
  shape match its claimed architecture, not just whether the scope item is needed at all). For PM's
  ratification, not self-ratified.
- **"No optional complexity" principle recorded in `decisions.log`** — PM's exact 08-26 words, plus
  today's Layer-2 sharpening, as a standalone citable entry (flagged as owed since yesterday's STOP).
- **Diagram rev2**: added a "Where BYOC sits in the sequence" section (Position 1's phrasing, the
  #829 discharge) and a "Connector shims vs. real MCP" section encoding today's verified finding,
  explicitly marked as pending PM's connector-milestone call rather than presuming an outcome.
  Republished to the same artifact URL (same favicon, 🏗️).

**Gave a real recommendation on Slack's milestone** (PM asked directly: "defer to what milestone?") —
Fast Follow, with three independent reasons (weakest architecture fit of the four, already excluded
from CXO's FTUX set, already fail-closed since #1481/#1484) — framed explicitly as a recommendation on
the table, since PM said "I'll decide re connectors," not a settled outcome.

Carry-forward's BYOC block fully rewritten to reflect both days' conversation and all four owed items
as executed; the one genuinely open item (PM's final connector-milestone call) called out explicitly
so future work doesn't presume an answer PM hasn't given.

## Same conversation, continued — PM ratifies, executed same-turn

PM: "I approve your recommendations." — Slack → Fast Follow confirmed. Executed immediately rather than
just acknowledging:

- **Production milestone (#9)** description updated via `gh api` PATCH: gate now names three connectors
  (GitHub, Calendar, Notion), Slack moved to Fast Follow, reasoning inline.
- **Epic #1440** (RECONNECT R2, the parent gate-tracking issue) retitled and given a full-rationale
  comment — the highest-leverage single edit since it's what anyone auditing the gate would read first.
- **Five Slack-specific Production issues moved to Fast Follow**: #1364, #1481, #1500, #1503, #1497 —
  each with a short pointer comment back to #1440 for context.
- **#1514** (spans all four connectors' OAuth-app work) left in Production but given a scope note —
  didn't move it wholesale since three of its four connectors still belong there; a milestone move
  would have silently deferred GitHub/Calendar/Notion work that isn't actually descoped.
- **#1686 filed** (Fast Follow) — the Slack-tz-capture half split out of #1572 back at the earlier
  point in this conversation, now that the milestone call it was waiting on is final. #1572 itself
  commented with the cross-reference.
- **Loop-in memo sent** to PPM, CXO, Arch (cc PM) per PM's explicit follow-up ask ("loop in PPM, CXO,
  and Arch") — one tailored open question per recipient rather than a generic broadcast: PPM (roadmap
  coherence, same shape as this morning's #829 reconciliation), CXO (pure confirmation against their
  own already-ratified FTUX exclusion — not asking them to redo anything), Arch (the one real open
  architecture call this surfaced: GitHub's adapter self-hosts `github-mcp-server` rather than using
  GitHub's own now-official hosted endpoint — flagged, not decided unilaterally).

PM signaled the next topic is "the BYOC skunkworks project itself, next steps" — a new phase, not
started this fire. Carry-forward updated to reflect the ratification and all execution; nothing left
pending from the connector-milestone question specifically.

## Retroactive STOP (2026-08-28 06:44) — cohort-wide freeze, not a PA-specific gap

Last heartbeat 14:47:44 PDT 08-27; the 15:42, 18:42, and 21:42 fires never executed — no PA activity
of any kind until this morning's 06:44 fire, which arrived carrying five duplicate queued cron
prompts at once. Investigated rather than assumed: `CronList` at 06:44 showed the recurring job
(`07bfdd69`) intact and correctly configured — the cron itself never died. Corroborated cohort-wide on
sync: an automated watchdog alert at 18:46 PDT flagged 3 roles (arch, cxo, web) stale simultaneously,
diagnosed as *"likely machine-asleep/backgrounded (cron-survives-doesn't-fire), not individual
failures"*; CXO's own log independently describes *"post-freeze wake — 15 queued ticks as one wake"*;
Docs and Comms both logged *"stacked wake"* retroactive STOPs for the same window. This is the same
event, not a separate PA defect — the host machine appears to have gone to sleep or been backgrounded
for several hours, and every role's cron survived without firing until the machine woke.

**Checked for missed work rather than assuming none**: mail and task loop were both empty at the last
live check (14:47). One new item arrived during the gap — CXO's reply to the Slack-descope loop-in
memo, confirming the FTUX alignment with one nuance (the taxonomy's §4 batch-re-evaluation trigger for
#1481 moves further out now that #1481 is Fast Follow rather than Production — no edit needed, just
worth recording). Triaged to `read/`; nuance folded into carry-forward. No PPM or Arch reply yet as of
this morning — nothing to chase, replies take the time they take.

No cron rotation performed — the job is healthy and correctly firing (this fire is the proof), so a
delete-then-create rotation isn't needed for recovery, only for hygiene at a natural STOP boundary.

<!-- DAY-CLOSED: 2026-08-27 -->

**Day-arc summary, 2026-08-27**: quiet mail-drain fires through midday, then a substantial live PM
conversation on connector architecture — verified GitHub/Slack/Notion all ship official hosted MCP
servers, checked Piper's own code against that standard and found Slack/Notion are bespoke REST
wrapped in a connector-contract shim, executed #1572/#1522 issue work and a standing-lens proposal,
then PM ratified Slack's move to Fast Follow and I executed the full mechanical fallout (milestone gate
text, epic #1440, five issue moves, one new issue filed) plus looped in PPM/CXO/Arch same-turn. Day
ends with a cohort-wide infrastructure freeze from ~15:00 PDT into the next morning — not a PA defect,
corroborated across at least four other roles' logs.
