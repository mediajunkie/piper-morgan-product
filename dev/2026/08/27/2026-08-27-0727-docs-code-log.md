# Docs Session Log — 2026-08-27

**Role**: Documentation Management (Docs) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` ·
website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`

---

## Fire 1 (07:27, the 06:57 slot) — START

**Step 0**: verified 08-26 closed properly — `<!-- DAY-CLOSED: 2026-08-26 -->` present at column 0
in `dev/2026/08/26/2026-08-26-0727-docs-code-log.md`. No self-heal needed.

`CronList`: one job (`8bddb70d`, STOP re-arm from last night), matches expression. Session log
for today did not yet exist — created per this fire.

Sync: 30 behind / 0 ahead on `origin/main` — fast-forward merged clean. Inbox: 1 direct memo —
CIO's confirmation they fixed the `mail-send.sh` false-positive same-day (`626316ad1`), root cause
precisely diagnosed (a no-op write-tree delta looked identical to "path never passed"), new test
added, verified my report was correct. Purely informational, nothing owed back. Triaging.

**Thursday, no day-of-week trigger** — Weekly Docs Audit is Mondays. Today's editorial slot is
"The Detector That Notified Nobody" (Beat 3, per the footer tease verified in The Burn-Down's
publish) — checking calendar status before assuming.

Heartbeat: START (unconditional write).

Merge-keeper sweep clean. "The Detector That Notified Nobody" still `drafted` — not chasing.
Standing items unchanged. CIO's mail-send.sh false-positive fix (`626316ad1`, same-day) confirmed
and triaged.

## A genuine 2-day omnibus gap found and closed — 2026-08-25 + 2026-08-26

No omnibus existed for either date (12 and 10 session logs respectively, both well over
threshold). Delegated to two sequential subagents (day-by-day, matching the established pattern
for avoiding a shared-CSV write race on `agent-activity-log.csv`) rather than compress the work
myself — each ran the full `create-omnibus` skill for real (methodology-20 read fresh, Step
2.5/2.6 cross-reference gates actually run).

**08-25**: 12 sessions, HIGH-COMPLEXITY:COORDINATION, 442 lines. Commits `1518b736d` (omnibus) +
`e06452231` (activity-log, 12 rows, 2151→2163). **Found and fixed a real orphaned duplicate**:
Arch filed #1684 and Lead independently filed #1685 for the identical `create_todo`
consent-gate-coverage finding, ~2 hours apart, neither aware of the other. #1685 was implemented
and closed same-day; #1684 sat open as an orphan. Closed #1684 as duplicate (body updated with
status banner, closing comment citing #1685's shipped fix, `--reason "not planned"`) rather than
leave two open tracking threads for one piece of work.

**08-26**: 10 sessions, HIGH-COMPLEXITY:COORDINATION, 551 lines (first draft came in
under-length at 200 lines — the subagent caught its own gap against the methodology's 30%-capture
floor and reflowed before I ever saw it, a good sign the skill's own quality gates are being
followed rather than rubber-stamped). Commits `3dfcb029a` (omnibus) + `d6001430a` (activity-log,
10 rows, 2163→2172, whole-file scan clean both times). **Flagged a real cross-role finding**:
#1462 vs #829 milestone conflict from a PM/PA architecture conversation, PM-approved for
reconciliation but unexecuted as of 08-26. **Checked before treating as still-open**: PPM had
already closed it this morning (08-27) — #829 closed superseded by #1462 (`gh issue view 829`
confirms CLOSED), PPM's own reply shows full independent verification (read both issue bodies +
PDR-006 directly, found a second corroborating signal — #829's milestone outranked its own parent
epic). No action needed from me; correctly already resolved by the time I checked.

Both omnibus files verified on `origin/main`, activity-log whole-file scan clean (2172 total
rows, 0 field-count mismatches).

Heartbeat: WORK (covered by commit volume).

## PM engaged — "what was #829 about?" + "today's blog post is ready for proofing and publishing"

**#829**: answered from the fresh `gh issue view` I'd already run this morning (PPM closed it as
superseded by #1462 — a locally-run MCP package model PDR-006 had explicitly evaluated and
rejected in favor of hosted MCP support).

**A real, stale-sync incident, caught by PM directly**: started auditing "The Detector That
Notified Nobody" against my local checkout — 33 commits behind `origin/main` since this morning's
Fire 1, unnoticed because a duty-cycle sync earlier in the session felt like a durable fact rather
than a timestamped one. Reported the draft's frontmatter as empty (a real blocker, correctly
identified given what I was looking at) and asked PM how to handle missing art. **PM asked "are
you synced with origin main?" rather than answer the art question** — caught the actual root
cause. Fetched: 33 commits behind, including the image PM had already added and a full 5-commit
voice-pass on the very file I was auditing. Re-synced, re-ran the entire audit against current
state.

**Fixed durably, not just apologized**: added a concrete instance to CLAUDE.md's existing
"Never guess at facts" mechanical-form section (`60ad50267`) — the general principle already said
"don't let a check you ran five minutes ago stand in for one you'd run now," but didn't name git
sync specifically as a category of stale check. Named this exact incident as the evidence.

**Then discovered the "missing art" question had an odder cause than staleness alone**: the
freshly-synced calendar row already carried a full review-and-resolution trail dated hours earlier
that morning — a claim flagged, PM resolving it, "PUBLISH-READY." Checked whether this was a
worktree collision (another live session in the same directory) before assuming — found no second
session log, no lock, and Comms' own session log for today confirmed it: **PM had reviewed the
piece directly with Comms in a separate, parallel conversation this morning**, not a collision at
all, just ordinary concurrent activity across two different roles' threads.

**Re-audited independently rather than trust "PUBLISH-READY" on the label alone**: full mechanical
checklist clean (frontmatter now complete, 0 semicolons, 0 AI-tics, acronym sweep clean, 662
words); fact-checked the load-bearing claims fresh against `docs/omnibus-logs/2026-07-27-omnibus-log.md`
(nine instances/four agents/72 hours, the 3½-hour dead-output window, the suppressed-confirmation-
line detail — all exact matches); spot-checked all 4 of Comms' mechanical fixes were present in
the final text. Dry-run clean, no rendering defects.

**Published**: `publish-post.js`, slug `the-detector-that-notified-nobody`, category `building`,
`--work-date 2026-07-26`. Website commit `3579c60`, hashId `b5e2e504a807`. Calendar updated
(status→published, art wired in, `canonicalSite` correctly left empty per yesterday's skill fix —
no syndication leg has run yet). Draft + source image archived via `git mv`. Commit `f1d171084`.

**Live-verified**: HTTP 200, title/caption/key-facts text present, hero image 200.

Notified Comms with the full verification trail and credit for the review (`13b379d2d`).

Heartbeat: WORK.

## Fire 2 (10:27, the 09:57 slot) — quiet

Synced first per today's new standing practice (2 commits behind, just a heartbeat — nothing
missed). Both worktrees clean, inbox empty, merge-keeper sweep clean. Nothing unblocked. Heartbeat
self-suppressed.

## Fire 3 (13:27, the 12:57 slot) — Detector's Medium leg, a real heading-level defect found+fixed on 2 live posts

**Mail loop, 3 items**: Dispatch-PM's Detector Medium-syndication report; Dispatch-PM's flag to
Comms (cc me) of a heading-level defect found during cross-post pre-flight; Comms' reply
confirming, widening scope, and root-causing it.

**Detector Medium leg**: recorded (`status`→`distributed`, `mediumURL` set). Could not
independently HTTP-verify the URL — both `curl` and `WebFetch` returned 403 from Medium, confirmed
against a known-good prior Medium URL too (also 403), so this is a structural tooling limit, not a
doubt about the report — noted honestly in the row rather than either fabricate a "verified" claim
or silently skip the discipline. **Also caught my own gap applying yesterday's skill fix**: first
pass left `canonicalSite` empty, then caught that this Medium leg *is* the syndication event my
own fix says should set it — corrected same-fire before committing.

**The heading-level defect**: Dispatch-PM found, during Detector's cross-post pre-flight, that its
two subheads render as `<h2>` when the site's real convention (confirmed against a known-clean
post, The Burn-Down) is `<h1>` for subheads — a template quirk the cross-post skill documents
explicitly. Traced back a matching defect in *The Dead Code That Wasn't* (published 08-20, also
live-wrong for a week uncaught). Comms verified the live defect independently, found the same
pattern in 4 of their own not-yet-published drafts (fixed those, `75b13c33c`), and root-caused it
to inconsistent application during the Aug 16-18 drafting window — not a skill/template defect —
then flagged the 2 already-live posts as squarely publish-pipeline territory, not theirs to fix
solo.

**Verified independently before touching anything**: pulled both hashIds directly from
`blog-content.json` (2 `<h2>` in Detector, 3 in Dead Code — exact match to Dispatch-PM's counts)
rather than trust either report on its own. **Fixed both halves**: the archived source markdown
(`##`→`#`, matching Comms' already-fixed drafts, product-repo commit `5be0b7738`) and — the actual
live artifact, which the source-markdown fix alone doesn't touch — the rendered HTML in
`blog-content.json` itself (website commit `ee3e597`). Deploy lagged ~20 seconds; polled rather
than assume the push alone was sufficient. **Live-verified both pages fully**: all 5 subheads
across both posts confirmed rendering as `<h1>` in the actually-served HTML, not just the commit.

Replied to Comms directly (cc Dispatch-PM via the relay protocol, Exec, PM) with the full
verification trail, credited Comms' root-cause trace and the 4-draft scope widening. All 3 source
memos triaged, MANIFEST regenerated, sent in push-to-ref batches (`2987c2386`, `aed5e4e65`).

Heartbeat: WORK.

## Fire 4 (22:27) — stacked wake (15:57/18:57/21:57 slots), STOP

Three identical cron-fire prompts arrived stacked at once — treated as one wake per the skill's
idempotency design rather than run the fire procedure three times. 22:27 is past the 21:57 slot,
the last of today, so this wake closes as STOP.

Synced (12 behind, clean fast-forward — mostly CXO/PA/Arch mail threads, none Docs-addressed).
Both worktrees clean, inbox empty, merge-keeper sweep clean. Nothing unblocked. Noted a
freeze-watchdog stall alert routed to CIO (18:46) — not mine to act on.

**Stale-cleanup dry-run**: 0 cycle-log files ≥7 days, 0 `.tmp` files ≥1 day. Nothing to remove.

Cron re-armed via delete-then-create: `8bddb70d`→`8f5e9099`, `CronList`-verified sole job,
registry row updated. Heartbeat: STOP.

### Day-arc summary — 2026-08-27

A day with one dominant thread: the editorial pipeline, worked end-to-end twice and each time
surfacing something beyond the immediate publish. The morning answered a PM factual question
(#829) cleanly from state already checked, then hit a real process failure mid-audit — reading
"The Detector That Notified Nobody" against a checkout that was 33 commits stale, reporting a
false blocker as a result. PM caught it with one precise question rather than accept the wrong
answer, and the fix went beyond apologizing: a durable addition to CLAUDE.md naming exactly why a
sync from earlier in a long session reads as a fact when it's really a timestamp. Once actually
synced, the piece turned out to have already had a full independent review from Comms working
directly with PM in a parallel thread — not a collision, just two roles' ordinary concurrent work
overlapping — and publishing proceeded clean after an independent re-verification.

The afternoon's thread picked up from there: syndicating that same post surfaced a real,
independently-confirmed defect (subheads rendering one heading level too deep) that turned out to
affect a second, week-old published post too. Comms had already scoped and root-caused it on their
own drafts; the two already-live posts needed a fix at the actual rendering layer, not just the
source markdown, which is exactly the kind of thing that's easy to think you've fixed when you've
only fixed the input to the fix. Both layers got fixed, the deploy got polled rather than assumed,
and both pages got checked in their actually-served form before calling it done.

Underneath both threads, the same discipline kept paying for itself: check the thing itself, not a
report about the thing, and don't let a check from earlier stand in for one you'd run now.

**Sign-off checklist**:
```
git status --short                                    → clean (both worktrees)
git fetch origin main && git rev-parse --verify -q origin/main   → resolves
git log --oneline origin/main..HEAD                    → empty (product AND website worktrees)
```
Nothing stranded. All work today reached `origin/main` same-fire, not batched to sign-off.

## Memory & briefing surfaces referenced this session

**Referenced**: CLAUDE.md's "Never guess at facts" section, both consulted and extended today —
the stale-worktree addition is now load-bearing for every future fire, not just noted once;
`update-calendar` skill's corrected canonicalSite timing (applied and self-caught a lapse
applying my own fix mid-fire); the site's `<h1>`-for-subheads convention, verified fresh against a
known-clean post rather than assumed from memory; the ratified cross-project relay protocol,
applied cleanly for the Comms/Dispatch-PM reply.

**Loaded but not referenced**: cross-pollination brief; `docs-standing-items.md`'s dormant items
(checked, unchanged, still not this fire's priority).

**Wanted but not found**: nothing material. Today's dominant lesson was procedural and now
durable rather than a missing surface: a fact checked once in a long session stops being current
the moment anyone else acts, and the fix for that is a habit (re-sync before reading), not a
one-time apology.

## Addendum before close — PDR-007's own measurement window closed today

Caught while rewriting carry-forward's "Awaiting others" section: PDR-007 (Docs-authored,
07-29) pre-registered a 4-week measurement window ending exactly today, with a falsifiable
threshold and two shipped, reproducible instruments. Ran both rather than let a self-authored
window expire unmeasured while writing about other people's stale-fact incidents in the same
day-arc summary above. **All three criteria held**: Class 1 (column-shift) 0, Class 2 (stale
`draftPath`) 0, Class 3 (field disagreements) 17 — exactly the baseline, zero growth over 4 weeks.
Per the pre-registered rule, **Option A is sufficient — PDR-007 closes as adopted-without-
migration.** Recorded on the PDR itself (`8464c6f4a`) as a measurement result, not a self-declared
ratification — Arch and Web already signed off; CIO's outstanding boundary-question review
(§Implications, whether m-44 extends to a new class) is what's left, not mine to skip. Notified
CIO directly, cc PM/Arch (`edf99b7a5`).

<!-- DAY-CLOSED: 2026-08-27 -->
