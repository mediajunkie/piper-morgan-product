# Session Log — Coding Agent (prog) — 2026-08-25 21:59

- **Role**: Coding Agent (prog), delegated by Lead Developer
- **Model**: Claude Opus 5 (claude-opus-5)
- **Branch**: `claude/lead-cycle` (Lead's Model-A worktree, `~/Development/piper-morgan-worktrees/lead`)
- **Issue**: #1685 — create_todo has NO WorkflowEntry; dispatches via the legacy
  elif into handlers with zero `consent_gate` calls (Arch's find, 2026-08-25)

---

## What shipped

create_todo + its ActionMapper alias family onto the #1124 action-dispatch rail,
`effect=EffectClass.WRITE` / `outwardness=PRIVATE` / `action_triggered=True`;
the legacy elif removed in the same change.

### 1. Alias enumeration (done FIRST, not assumed)

Grepped `ActionMapper.ACTION_MAPPING` for keys canonicalizing to `create_todo`:

| raw key | → canonical |
|---|---|
| `create_todo` | `create_todo` |
| `add_todo` | `create_todo` |
| `new_todo` | `create_todo` |

Three keys — and it does **not** mirror the delete family (`delete_todo` /
`remove_todo` / `cancel_todo`). All three registered, canonical key first.
`test_alias_family_is_exactly_the_mapper_family` re-derives the set from the
mapper at test time, so a future mapper alias cannot silently land off-rail —
which is #1685's own failure mode wearing a different name.

### 2. Ratchet-shape verification — **delta NONE, and the reason**

`MAX_DISPATCH_SITES` stays **0**. Verified rather than assumed:

- `DISPATCH_RE = r"^[ \t]*(?:if|elif) intent\.action in \["`
  (tests/test_architecture_enforcement.py:523).
- The removed branch was `elif mapped_action == "create_todo":` — a
  **`mapped_action ==` shape**, which that regex does not match.
- Measured both sides: counted sites **0 before, 0 after**. Same finding as
  #1666's lane, re-verified here rather than inherited.

Second-order catch: the *replacement comment* originally contained the literal
`mapped_action == "create_todo"`, which the #1411/#1666 elif-token derivations
(`re.findall(r'mapped_action\s*==\s*"([a-z_]+)"')`) read with a regex that cannot
tell comment from code. Reworded so prose can't read as a live dispatch site.

### 3. Consent-consulted proof (A/B against the real pre-#1685 tree)

Backed the working files up to scratchpad, `git checkout HEAD --` the two
production files, ran the suite, restored from backup. Result:

- pre-#1685: `TestNoNewCeremony` imperative-create tests **PASS**; the
  `TestConsentGateIsConsulted` spy asserts **FAIL** ("evaluate_consent was not
  consulted").
- post-#1685: all 26 green.

That is the m-44 shape stated as an experiment: the transcript is identical on
both sides, so only a seam assertion can tell "evaluated and waved through" from
"nothing evaluated it."

### 4. No new ceremony — pinned

`PRIVATE × WRITE × execute framing → PROCEED`, and every natural create phrasing
is verb-initial imperative (`_EXECUTE_RE` carries `add`/`create`), so a create
turn still writes the row in one step, asks nothing, and leaves nothing pending.

**Honest scope note, reported up rather than buried**: an AMBIGUOUS-framed
create emission (`"todo: buy milk"`, `"new todo - ship the thing"`) is now held
for a consent check. That is the ratified #1509/#1510 matrix, identical to the
already-registered create_reminder sibling — but it IS a behavior delta for
non-imperative phrasings and Lead/Arch should see it named.

### 5. Reminder boundary, both directions

- #1654 no-task carrier still arms; the bare-task answer `"buy milk"` (exactly
  the phrase a classifier would read as create_todo) still binds at the offer
  seam, which runs **before** classification; the full two-question recovery
  still ends in the real save.
- Registry: `create_reminder` entry is a distinct object, not aliased or
  re-pointed.
- Inverse: a create_todo turn arms no reminder carrier and leaks no reminder copy.

---

## Files

- `services/intent_service/workflow_entries.py` — `run_create_todo_workflow`
  (carries the removed elif's exact body) + `create_todo_entry` + 3 rail keys
- `services/intent/intent_service.py` — legacy elif REMOVED
- `services/intent_service/unwired_writes.py` — docstring: create_todo moved
  from "elif chain" to "#1685 rail WorkflowEntry"
- `docs/internal/architecture/current/intent-routing-stack.md` — surface-3 key
  count (re-measured live: 123 keys / 43 unique entries) + the consent-gate
  known-boundary paragraph, same commit
- `tests/unit/services/intent_service/test_create_todo_rail_1685.py` — 26 tests

## Evidence

- 1685 suite: 26 passed
- both ratchet files + 1685: 72 passed
- full intent_service unit sweep: 3477 passed, 3 deselected, 12 warnings
- adjacent (tests/intent + todo service + web routes + both ratchets):
  **6 failed, 700 passed, 2 skipped, 76 deselected**

### The 6 failures are NOT mine — A/B verified, and already tracked

Ran the exact 6 node-ids twice: once on my tree, once after
`git checkout HEAD --` of my three production files (backed up to scratchpad
first, restored after). **Identical 6 failures on both sides**, in 2.2s each —
so they are pre-existing, not a side effect of the migration:

```
FAILED tests/intent/test_bypass_prevention.py::…::test_middleware_is_registered      (assert 13 == 12)
FAILED tests/intent/test_enforcement_integration.py::…::test_monitoring_endpoint_accessible
FAILED tests/intent/test_future_nl_endpoints.py::…::test_all_nl_routes_in_middleware_config
FAILED tests/intent/test_handler_error_handling.py::test_status_query_empty_projects  (assert None == 'configure_projects')
FAILED tests/intent/test_no_cli_bypasses.py::…::test_all_commands_import_intent
FAILED tests/intent/test_no_timeouts.py::…::test_query_fallback_handles_misclassifications
```

Already tracked: **#1637** — "tests/intent has 6 standing failures + poisons 6
unit tests when run combined — pre-existing, verified on two commits". Same
count, same directory. No new issue filed (checked before filing, per
verify-first).

## Discovered issues

- Ambiguous-framed create_todo now gets a consent check (§4 above) — reported to
  Lead in the handback rather than filed unilaterally, since it is the ratified
  matrix applied to a newly-registered action, not a defect.
- No new tracking issues filed.
