# Session Log — 2026-08-18 07:38 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: GitHub issue 1639 — sibling gathers (projects, completed todos) conflate verified-empty with never-gathered; apply the fix shape proven by 1544
**Worktree**: isolated agent worktree, branch `claude/lead-cycle`; commit only, no push (Lead merges after review)

## Work

- Read 1544's implementation-evidence comment and commit `5a200b2b8` first (per brief) — the proven shape: compute returns the verified-empty dict with a real zero count instead of None; renderer gains a distinct "none — checked this turn" state; prompt licenses the empty-list claim only from an explicit verified-empty context line.
- `services/intent_service/context_assembler.py`:
  - `_compute_projects`: zero rows → `{"projects": [], "project_count": 0}` (zero from a LIMIT-capped read is exact); error path unchanged (None — never verified-empty).
  - `_compute_completed_todos`: zero completed rows → `{"completed_todos": [], "completed_todo_count": 0}`; error path unchanged.
  - `_get_projects_cached`: passes `project_count` through when present (only the empty case carries one — the populated query is LIMIT-capped, so no truncated count is invented, m-44).
- `services/intent_service/conversational_floor.py`:
  - Projects renderer: populated / verified-empty (`PROJECTS: none — checked this turn…`, account-level framing) / absent-silent; the 1530 `Active project count` line skips the redundant bare zero beside the verified-empty line.
  - Completed-todos renderer: same three states (`COMPLETED TODOS: none — checked this turn…`); populated header now states the row-derived `completed_todo_count`, not the display-slice length (m-44 fix found in-lane).
  - Never-fabricate guidance: extended the context-line examples only (`"PROJECTS: none"`, `"COMPLETED TODOS: none"`) — no example reply strings reintroduced (1544's root cause).
- `docs/internal/architecture/current/intent-routing-stack.md`: synced surface-4 row in the same commit.
- Tests: `tests/unit/services/intent_service/test_sibling_verified_empty_1639.py` — 22 pins (prompt, per-lane three-state renderer, compute/cache/gather pipeline, error-path never-verified-empty, transcript-phrase regression).

## Evidence

- New suite: 22 passed.
- 1544 + 1570 + assembler + cache + 1530 suites: 154 passed.
- Full `tests/unit/services/intent_service/` + `tests/unit/services/intent/`: 3051 passed, 3 deselected.
- 1544 real-Postgres integration: 3 passed.
- Ratchets (`test_completion_ratchets.py` + `test_architecture_enforcement.py`): 46 passed.
- Deterministic renderer proof script: three distinguishable states per lane (populated line / verified-empty line / no line).

## Discovered issues (reported to Lead, not filed — Lead to triage)

1. Populated temporal projects lane has no true total: `_compute_projects` is LIMIT 5 with no COUNT, so a user with >5 projects gets 5 "tracked" lines and no denominator from this lane (the user_context lane has 1530's count; this lane doesn't). m-44 adjacent, pre-existing.
2. Sibling lanes have no source-failed state: a compute error returns None, indistinguishable from never-gathered at the floor (1573 covered pending todos only). Both read as "no visibility" — honest but undifferentiated.
