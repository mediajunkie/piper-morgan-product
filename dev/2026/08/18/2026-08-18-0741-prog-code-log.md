# Session Log — 2026-08-18 0741 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1640 — login_page's already-authenticated next-bounce (a #1480 fix surface) structurally unreachable: /login was auth-middleware-excluded, so the cookie was never parsed and `request.state.user_id` was never set on /login requests.

## Work

### Layer decision
Fixed at the **middleware** layer (option a): moved `/login` from `EXEMPT_AUTH_AND_SETUP_PATHS` into a new `OPTIONAL_AUTH_UI_PATHS = ("/", "/login")` handled by the existing optional-auth dispatch branch (the #1399 "/" mechanism, generalized). Rejected option (b) (route parses cookie itself) because state population is the middleware's job, every other UI route gets user_id from the middleware, and "/" already solved this exact failure shape in the same file — one mechanism, two paths.

Robustness addition: the optional-auth branch now runs **before** the exclude-path check, so re-adding /login to any exclude list (constructor arg or default) can't silently re-kill the cookie parse. Pinned by `TestMechanismPins::test_optional_auth_wins_even_if_reexcluded`.

Invariants preserved: /login never BLOCKS — missing/invalid/expired cookie falls through as anonymous (form, 200, no error); `sanitize_next_path` guards the bounce target (was already in login_page; now pinned at the real layer).

### Files
- `services/auth/auth_middleware.py` — /login out of exempt list, `OPTIONAL_AUTH_UI_PATHS` added, dispatch branch generalized + reordered before exclude check
- `web/api/routes/ui.py` — reachability comment on the bounce branch (no logic change)
- `tests/live/test_login_next_1480_live.py` — strict-xfail marker removed per its own instruction (flipped to pass live); stale "currently dead" comments refreshed; probe-skip retained as regression guard
- `tests/unit/web/test_login_optional_auth_1640.py` — NEW: real AuthMiddleware + real login_page + real JWT crypto through TestClient; no mocked `request.state` (that mock hid the bug); 11 tests incl. open-redirect pins + mechanism pins

### Evidence
- New unit file: 11 passed
- Auth-adjacent suites (1640 + 1480 + unit/services/auth + exempt-boundary 1308): 85 passed
- Ratchets (test_completion_ratchets, test_architecture_enforcement): 46 passed
- Full tests/unit: 9988 passed, 1 failed = pre-existing `test_github_place_has_name`, verified via stash A/B (fails identically without my diff); already tracked as **#1349**
- Smoke (`-m smoke`, CI-gating): 542 passed, 1 skipped
- **LIVE** (PIPER_LIVE=1, real server subprocess, real login): `test_authenticated_login_visit_honors_next` PASSED (was strict-xfail — the flip), `test_open_redirect_guard_live` PASSED (previously self-skipping as unmeasurable), unauthenticated-encoding test PASSED, browser-half deliberately SKIPPED (unrunnable by HTTP harness, by design)

## Discovered issues
- None new. Pre-existing unit failure already tracked (#1349).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-var stripping for server/test runs, commit discipline, no close-keywords near issue numbers); issue #1640 body (layer analysis + "/" precedent pointer); tests/live/conftest.py docstring (harness usage, PIPER_LIVE gate); m-43/m-44 framing in the live test file (kept its layer-naming discipline in the new unit file); ab-a-isolation skill concept (stash A/B for the unrelated failure)
- **Loaded but not referenced**: memory index entries, ROSTER/briefing docs
- **Wanted but not found**: none
