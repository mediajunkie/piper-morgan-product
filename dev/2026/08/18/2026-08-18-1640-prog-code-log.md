# Session Log — 2026-08-18 16:40 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Issue #1657 — #1624's happy path failed live: PM's Files listing shows one
document ('artifact-8b029c94.md') but chat 'summarize artifact-8b029c94.md' answered
the honest-empty reply.
**Branch/worktree**: isolated agent worktree (Lead merges after review; NOT pushed)

## Investigation — the exact divergence (16:40–16:47)

Table divergence, the third hypothesis in the brief — not owner-column drift:

- **Listing** (`web/api/routes/files.py:279` + `:291`): reads
  `uploaded_files WHERE owner_id == current_user.sub` **∪**
  `ArtifactRepository.list_for_owner(current_user.sub, source_type="generated")`
  (`services/database/repositories.py:2873-2876`), projecting artifacts through
  `_artifact_filename(title, id)` → `artifact-{id[:8]}.md` when untitled.
- **Resolver** (`services/repositories/file_repository.py:66-75` / `:147-163`):
  `uploaded_files WHERE owner_id == :owner` only. Artifacts: never read.
- PM's "document" is a generated **artifact** (the filename IS the untitled-artifact
  projection), so the resolver's read was correctly-empty for uploaded_files and
  wrong-empty for "the user's documents."
- information_schema check (per the #1629-style audit): `uploaded_files.owner_id` is
  **uuid**, `artifacts.owner_id`/**id** are **varchar** — real drift, but not the cause
  here; both reads compare against the same principal string successfully.
- Secondary latent miss: nothing matched exact stored filenames — a verbatim
  'summarize <filename>' ran through fuzzy keyword scoring (hash-shaped names lose to
  recency noise; ambiguity falsely triggered between artifact-*.md siblings).
- Aged-row shape (m-44 education): plaintext (pre-encryption) content passthrough,
  `reference_count=0`, `last_referenced NULL`, year-old timestamps.

## Fix (16:47–16:52) — both reads now share one document set + one projection

- `services/file_context/artifact_view.py` (new): single-source
  `artifact_filename()` (moved from the route) + `artifact_as_file_view()` projection.
- `web/api/routes/artifacts.py`: `_artifact_filename` delegates to the shared function.
- `services/file_context/file_resolver.py`: optional `artifact_repository`; candidates =
  uploads ∪ owner's generated artifacts (owner-scoped `list_for_owner`, never widened);
  exact-filename pin (verbatim stored filename in message → 0.98, boundary-safe);
  named-but-absent filename → honest None (never summarize the best-scoring OTHER doc).
- `services/intent_service/workflow_entries.py`: #1624 rail hands the resolver an
  `ArtifactRepository(session)`.
- `services/intent_service/document_handlers.py`: `_get_owned_artifact` (owner-scoped
  `get_by_id`, no admin bypass) + artifact branch in `handle_analyze_document` → text
  path; cross-owner id still raises FileNotFoundError.
- `services/analysis/document_analyzer.py`: `analyze_text()` — same LLM JSON-summary
  path for DB-resident content (artifacts have no storage_path/PDF bytes).

## Evidence (16:47–16:53)

- Unit: new `tests/unit/services/intent_service/test_summarize_files_view_1657.py`
  15/15 pass; prior surfaces 26/26 (resolver edge cases + 1624 rail) and 135/135
  (artifacts_355, scoring weights, analysis, both summarize files).
- **Live (#1621 harness, real server + real login + real LLM turns)**: new
  `tests/live/test_summarize_files_view_1657_live.py` 2/2 pass —
  (1) year-old untitled plaintext artifact: /files lists `artifact-c6765fcd.md`,
  turn "summarize artifact-c6765fcd.md" → real summary, resolution_confidence 0.98;
  (2) year-old plaintext-storage uploaded_files row: "summarize q3-roadmap-2025.pdf"
  → real summary through handle_summarize_document → DocumentAnalyzer → pypdf.
  Count-verified teardown clean.
- Ratchets: `tests/test_architecture_enforcement.py` 41/41 (dispatch-site ratchet,
  byte-seam, chat-pointers). Smoke: 542 passed, 1 skipped. Wider sweep:
  tests/unit/services/intent_service/ + tests/unit/web/api/routes/ +
  file-repository tests → 3679 passed.

## Discovered issues filed

- #1659 — non-PDF uploads unsummarizable (pypdf run on every file_type)
- #1660 — key_findings read from always-empty `recommendations` ('detailed' format empty)
- #1661 — temporal references ('the file', 'last month') cap at 7 days → wrong-empty on aged docs

## Wrap

Committed in agent worktree referencing issue 1657; NOT pushed (Lead merges after
review). Working tree clean post-commit.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md intent-routing-stack pointer (4-surface routing model —
  informed rail vs. classifier analysis); m-43/m-44 sections (shaped the live test's
  precondition assertion and evidence framing); #1306 byte-seam rule (kept the seed
  write in tests/, checked the enforcement scan's scope first); discovered-work
  discipline (three issues filed immediately); no-close-keywords-near-#N gotcha
  (commit message wording).
- **Loaded but not referenced**: Amber worktree/mailbox discipline sections;
  memory index.
- **Wanted but not found**: a doc stating which chat actions are pre-classifier
  deterministic vs. LLM-lane (had to derive from action_registry.py comments).
