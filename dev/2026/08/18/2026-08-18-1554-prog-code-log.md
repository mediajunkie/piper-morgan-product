# Session Log — 2026-08-18 15:54 PDT — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: GitHub #1651 — standup offered "mark that overdue todo done?", PM
accepted verbatim, and the handler text-matched 'overdue' as a title and
failed. Bind the offer's referent into the #846 pending-offer carrier.
**Branch**: isolated agent worktree (Lead merges after review; not pushed)

## Work

- **Pre-read**: issue #1651; `docs/internal/architecture/current/intent-routing-stack.md`
  (mandatory); studied the #846 store (`soft_invocation.WorkflowOfferService`),
  the #1190 carrier (`destructive_confirm.py`, `run_confirm_pending_action_workflow`),
  and the reminder-clear (#1605) / drafted-issue (#1571) / standup-invitation
  (#1591) consumers as the idiom to mirror.
- **Root cause confirmed in code**: "Yes mark the overdue todo done." →
  `handle_complete_todo` → `_extract_completion_text` → "overdue" →
  `_find_best_matching_todo` fails → "I couldn't find a todo matching
  'overdue'" (`services/intent_service/todo_handlers.py:568`). The standup's
  closing copy had no armed referent for the acceptance to bind to.
- **Fix (composition-side, moratorium honored — no pre-classifier or prompt
  changes)**:
  - NEW `services/intent_service/standup_todo_offer.py` — overdue-todo
    resolution (owner-scoped `TodoManagementService.list_todos`, aware-UTC
    due-date compare, most-overdue-first), the bound offer builder (kind
    `standup_todo_offer`, todo id bound at offer time, copy always names its
    referent; multiple overdue → single strongest bound + count stated), and
    the offer-only acceptance entry `run_standup_complete_todo_workflow`
    (completes the BOUND id; principal-mismatch no-op; honest failure copy).
  - `services/intent_service/workflow_entries.py` — registered
    `standup_complete_todo` (WRITE, PRIVATE, action_triggered=False).
  - `services/intent/intent_service.py` — `_handle_standup_query` arms the
    offer on the non-empty report path (trailing = the bound question; #1591
    mode asks stay quiet that turn; empty branch untouched per PPM's rule;
    todo-read failure isolated), `standup_todo_offer_pending` flag +
    `_apply_soft_offer` no-clobber guard, off-intent abandon name.
  - Doc updated in the same commit: intent-routing-stack.md #1651 entry.
- **Tests**:
  - `tests/unit/services/intent_service/test_standup_todo_offer_1651.py` —
    19 tests: referent resolution, offer builder, acceptance entry (bound-id
    dispatch, principal mismatch, vanished row, foreign payload), and e2e
    through the REAL `process_intent` (explosive LLM): arm, PM's verbatim
    acceptance, crisp yes, decline, no-referent, multi-overdue, todo-read
    isolation, #1631 prose turn.
  - `tests/integration/test_standup_todo_offer_1651.py` — real-Postgres AC:
    transcript turn-for-turn with a REAL seeded todo (title shares no word
    with 'overdue'), bound id asserted in the armed offer, completion
    asserted by re-reading the ROW; crisp yes + decline; no-referent case.

## Evidence

- New unit file: `19 passed`
- New integration file: `3 passed` (real Postgres, port 5433)
- Standup + todo unit suites: `527 passed`; full intent_service unit dir:
  `3055 passed`; standup/todo integration set: `52 passed` (re-run after
  comment rewording: `411 passed` combined sweep)
- Ratchets: `tests/test_completion_ratchets.py tests/test_architecture_enforcement.py`
  → `46 passed` (first run tripped the case-insensitive todo-marker ratchet
  on my own wrapped comments — reworded, not ceiling-raised; A/B/A confirmed
  the baseline was clean)

## Discovered issues

- The #1591 interview invitation / mode read-back arm the one-slot #846 store
  via the direct-return standup path but carry NO `*_pending` intent_data
  flag — on the rail-dispatched `get_standup` path `_apply_soft_offer` could
  clobber them with a soft workflow offer. Pre-existing gap, out of scope
  here; flagged in my report to Lead rather than filed (no `bd`/gh write from
  this worktree without direction).
