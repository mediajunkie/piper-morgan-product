# Session Log — 2026-06-14 16:00 — code-opus (general-purpose audit agent)

**Role**: General-purpose Claude Code agent (no leadership role assigned). Task: read-only audit of RECONNECT-sprint Config & Credentials issues (#1226, #1199, #1229) against actual code/docs/DB.

## Task
Adversarial grounding audit of 3 GitHub issues filed today by Lead Dev for the connector-refactor ("RECONNECT") sprint, cross-checked against `docs/internal/architecture/connector-refactor-sprint-scope-2026-06-14.md` (§2a/§2b/§3). READ-ONLY — report back to PM, no edits.

## What I verified (evidence trail)
- KeychainService `_api_key` suffix: `services/infrastructure/keychain_service.py:24,207-220` — service `"piper-morgan"`, account `{provider}_api_key` / `{username}_{provider}_api_key`. GROUNDED.
- 4-connector cred conventions: integration_config_service.py (OAuth-app creds global), github/config_service.py:161/183 (`github_token` username-scoped), settings_integrations.py:811 etc (`google_calendar_{user_id}` name-interpolated), slack socket_mode_runner/oauth_handler/config_service (`slack_bot` username-scoped, `slack_app_token`/`slack_client_id`/_secret global), notion/config_service.py:192 (user-scoped) + config/notion_config.py:28 (GLOBAL) → "ambiguous both ways" confirmed.
- Flat files: settings_integrations.py:211/237/263/289 — all four `data/*_preferences.json` cwd-relative; repo_resolver.py:45. Only `data/github_preferences.json` exists on disk (222B, created today 09:03 — the band-aid; contains BOTH a25db09c + 009afc8c identities → mediajunkie/piper-morgan-product).
- #1199 two stores: UserPreferenceManager (`services/domain/user_preference_manager.py:190-258` — purely in-memory dicts, comment "would be replaced with database persistence") vs JSON file. `set_default_repo` has ZERO non-test callers. repo_resolver `_resolve_from_user_default` (line 279) reads JSON, not UPM; its own docstring confirms the two-disconnected-stores thesis. Live `get_default_repo` readers exist (morning_standup.py:262) but no writer → that path dead too.
- #1226 "0 project_repository_links DB-wide": confirmed via `docker exec piper-postgres psql` — project_repository_links=0, repositories=0, projects=3.
- resolve_repo order (repo_resolver.py:118+): actually 5 paths (explicit → project → default-project → user-default → env), #1226 collapsed to 4.
- Ancestry: #1192 (CLOSED) is genuine parent — its gap (a) names the in-memory two-store problem. #1042 (CLOSED, shipped 2026-05-04) is genuine UserPreferenceManager default_repo origin.

## Verdicts
- #1226: SOUND (minor: resolve_repo order simplified; "json absent in both checkouts" now STALE — worktree file exists as of today's band-aid).
- #1199: SOUND — strongest-grounded of the three; code docstrings self-confirm.
- #1229: SOUND — inherits scope §2a table, which is accurate. One nuance: GitHub has a stale 3rd reader `get_api_key("github")` (bare, no username) in intent_service.py:6254/6416 that the table doesn't mention.

Report delivered to PM in-conversation (not committed as a doc per instructions).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (KeychainService `_api_key` foot-gun section — informed claim-1 verification; env-stripping guidance — informed DB-access approach); MEMORY.md (investigate-before-extending — informed reading whole issue bodies + scope doc, not fragments).
- **Loaded but not referenced**: most role-briefing tables (no role assigned); the bulk of MEMORY.md feedback entries.
- **Wanted but not found**: none.

<!-- DAY-CLOSED: 2026-06-14 -->
