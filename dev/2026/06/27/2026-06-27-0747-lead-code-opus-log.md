# Lead Developer — Session Log 2026-06-27

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Sonnet 4.6
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B) · Sole lead.
**START**: 07:47 PDT Sat Jun 27 — PM kicked off: close Jun 26, open today, check mail, resume RECONNECT. Jun 26 retroactively closed (busy-signal interrupts prevented a live STOP; all Jun 26 work was already on origin/main).

## Carry-in (RECONNECT Chunk 2 — the ports)
- ✅ **WS-2 (#1229) CLOSED** 6/26 — `ConnectorBinding` storage foundation. ✅ **GitHub port inc.1** — adapter connect()/status() read the binding store.
- ⚠️ **inc.2 (OAuth callback) is BLOCKED** behind the real transport: the MCP-consumer transport is simulation-only (`services/mcp/protocol/protocol_client.py:179` `NotImplementedError`) + no github-mcp-server. **#1220 (real MCP-consumer transport) is the prerequisite.**
- **▶ TODAY: build #1220 — the real MCP transport.** Buildable+testable NOW against `scripts/mcp_file_server.py` (no github-mcp-server needed for the transport itself). github-mcp-server provisioning (stdio-local vs hosted) = a later infra call. Gameplan: `dev/2026/06/26/1317-1220-mcp-ports-gameplan.md`. Exec green-lit "keep draining the ports — pre-authorized" (6/26).
- **Gated (don't touch)**: #1312 (queued AFTER alpha bundle, PM-approved timing); #1320/#1162 (Caddy-gate = PM+Arch).

## Work

- **07:47 — START.** Closed Jun 26 (DAY-CLOSED). Synced; all prior work on origin/main. Triaged the 2 Exec memos (1312-timing + keep-draining-ports) → read/. Mail clear.

- **~08:00 — #1220 investigated + gameplan written (the spine).** Investigate-before-extending on the transport: **the official MCP SDK is already a dep (`mcp==1.26.0`)** — provides `ClientSession`, `stdio_client`+`StdioServerParameters` (real subprocess transport), `create_connected_server_and_client_session` (in-memory test transport), `FastMCP` (test-server fixtures). Also found: the hand-rolled sim stack (`MCPProtocolClient`/`consumer_core` `simulation_mode:True`) is **LIVE** in `query_router.py:109`; and `scripts/mcp_file_server.py` is itself a POC sim (not real MCP). **Design decision (Lead-lane, flagging for PM/Arch): Shape B — build a NEW SDK-based `MCPClient`, don't retrofit the hand-rolled `_send_message`.** Rationale: don't hand-roll MCP JSON-RPC when the SDK is installed; the legacy sim stack is live in query_router → a new client leaves it untouched (zero regression); legacy cutover = a separate follow-up, OUT of #1220 scope. Gameplan + audit self-check: `dev/2026/06/27/1220-real-mcp-transport-gameplan.md`. **Concern to surface**: this is a notable transport-mechanism + architecture call — proceeding per Exec's keep-draining green light, but PM/Arch may want to weigh in on the legacy-cutover sequencing. Building inc.1 (the SDK `MCPClient` + in-memory round-trip test) now.

- **~07:57 — #1220 inc.1 SHIPPED: the real MCP transport (`MCPClient`).** New `services/mcp/consumer/mcp_client.py` — a thin client over the SDK `ClientSession`: `list_resources`/`read_resource`/`list_tools`/`call_tool` (real calls) + a `connect_stdio(StdioServerParameters)` async-context factory for production (spawn-subprocess→initialize→yield). **5 new tests** (`test_mcp_client_1220.py`) prove a REAL protocol round-trip via the SDK's in-memory transport against a `FastMCP` fixture server (list+read a resource, list+call a tool) — no simulation, no subprocess. **Verified**: 5/5 new green; **83/83 full consumer suite green** (was 78 — zero regression; legacy sim stack untouched per Shape B). The `NotImplementedError("Real transport not yet implemented")` is now superseded by a real client. Next: inc.2 (stdio subprocess integration test).

- **~08:00 — #1220 inc.2 SHIPPED: stdio subprocess integration.** New real MCP server fixture `tests/fixtures/mcp/stdio_fixture_server.py` (FastMCP over stdio; a genuine MCP server, unlike the POC-sim `scripts/mcp_file_server.py`). New `@pytest.mark.integration` test spawns it via `StdioServerParameters(command=sys.executable, ...)` and round-trips the **full production transport path** through `MCPClient.connect_stdio` (spawn→initialize→list/read resource + list/call tool over OS stdio). **Verified**: 6/6 #1220 tests green; **84/84 consumer suite + 11/11 queries (live legacy path) green** — zero regression (Shape B confirmed: legacy sim stack untouched, MCPClient is purely additive). **#1220's transport build is complete** (inc.1 in-memory + inc.2 subprocess both prove real MCP). Remaining for close: file the legacy-cutover follow-up + verify #1220 AC + close properly. Consumption (github resolve) = #1317 inc.3.

- **~08:10 — SCOPE CORRECTION (close-issue-properly read-the-whole-issue caught it): #1220 is the UMBRELLA, not "the transport" — kept OPEN.** Before closing, read the full #1220 body: it's the **§0 anchor/umbrella** for the connection/auth → MCP migration ("Move integration connection/auth layer to MCP"), with AC like *"bespoke OAuth flows retired,"* *"per-user user-scoping preserved (ADR-058),"* *"Arch cross-validation,"* + the note *"PM closes issues after approval."* My carry-forward/the chunking had compressed it to "build #1220 = the transport" (a fragment-framing that crept in during yesterday's inc.2-blocker discovery). The issue body is authoritative → corrected toward it. **Disposition**: (a) **#1220 stays OPEN** — posted a progress comment (`#issuecomment-4818596294`) recording hard-blockers cleared (#1232 ✅, ADR-070 ✅) + the shipped transport infra + the remaining umbrella work (the #1317 ports + bespoke-flow retirement); (b) filed **#1322** (legacy sim-transport cutover — genuine discovered work, my gameplan's AC); (c) the transport itself = shipped infrastructure under the umbrella (commits `69fc4f8de`/`40b8b1821`). **The real RECONNECT bulk is the per-connector auth migration (#1317 ports), which #1220 closes behind.** Surfacing the scope clarification to PM (sprint-accounting-relevant). Next drain (per keep-draining-ports): **#1317 github inc.3 — resolve() via MCPClient**, partially buildable now against a FastMCP fixture (real github-mcp-server hookup = the flagged provisioning decision).

- **~08:12 — #1317 github inc.3 SHIPPED: resolve() honest-degrade rail + real-MCP wiring (folds #1231).** Replaced the resolve() stub with the binding-aware degrade rail (server-agnostic, fully real): no binding → ConnectRequired-miss; stale → STALE_TOKEN-miss; unreachable-stored → UNREACHABLE-miss; **bound-but-unprovisioned → honest UNREACHABLE** (never a fake success); bound+reachable → real MCP round-trip via `MCPClient` → ResourceHandle (hit) | RESOURCE_NOT_FOUND-miss. The transport seam (`_mcp_client_ctx`/`_server_params_for`/`_resolve_via_mcp`) isolates the **two provisioning-gated points** (the github-mcp-server command + the resolve tool name, `_RESOLVE_TOOL`) so the rail is testable against a FastMCP fixture without a live server. **7 new tests** (`test_github_resolve_1317.py`) — 5 degrade-rail + 2 real-transport (hit/miss via in-memory round-trip); **91/91 consumer suite green** (84→91, zero regression). **The GitHub connector is now fully protocol-wired** (all 4 methods binding-aware + honest-degrade). Remaining github-port work (inc.2 OAuth callback, the concrete #1230 resolution tool, live cutover) is **all gated on the github-mcp-server provisioning decision** — the actual next critical-path unlock. Bringing PM the provisioning options next (per my memo: "when the live hookup is the actual next step" = now).

- **~08:25 — github-mcp-server provisioning DECISION-MEMO to PM cc Arch/Exec/PA.** Researched the real github-mcp-server (web): two deployments — **A hosted-remote** (`api.githubcopilot.com/mcp/`, OAuth 2.1+PKCE, server owns token = **D3-aligned**; needs an HTTP transport) vs **B local-stdio** (Docker/binary + `GITHUB_PERSONAL_ACCESS_TOKEN` = matches my stdio transport but **re-introduces per-user raw-token custody**, against D3's intent). Recommended **A**. Routed as the Arch/CIO architecture-direction call the #1220 issue names. Memo `memo-lead-to-pm-cc-arch-exec-pa-github-mcp-provisioning-decision-2026-06-27.md`.

- **~08:30 — MCPClient transport COMPLETED: `connect_http` (streamable-HTTP) shipped.** Per the memo's "meanwhile" — added the second standard MCP transport (`connect_http(url)` over the SDK's `streamable_http_client`), the companion to `connect_stdio`. Justified independently (most hosted MCP servers use HTTP) + **pre-pays option A's main cost**. New `@pytest.mark.integration` test serves the FastMCP fixture in `--http` mode (subprocess) + round-trips via `connect_http` — **stable 3×**; 92/92 consumer suite green. Used the non-deprecated `streamable_http_client` (the deprecated `streamablehttp_client` took `headers=`; the current one takes a pre-built httpx client — so auth/OAuth headers are the inc.2 follow-on, transport-level wire proven now). MCPClient now covers BOTH standard transports (stdio + HTTP).

- **~08:45 — #1317 CALENDAR port (connector #2): binding-aware Connector-protocol rail.** Per keep-draining "move to the next unblocked thing": ported `GoogleCalendarMCPAdapter` to the Connector protocol — `IMPLEMENTS_CONNECTOR=True` + all 4 methods (connect/status binding-aware; resolve honest-degrade rail + the real-MCP seam; degrade), mirroring github. Established **`_CALENDAR = "calendar"`** as the connector-name convention (matches territory_id + RECONNECT scope; flaggable). The live MCP resolution stays provisioning-gated (bound-but-unprovisioned → honest UNREACHABLE), same as github. **7 new tests** (`test_calendar_connector_1317.py`); **115 passed** (consumer + arch-enforcement) — the **m-41 contract guard now covers calendar too** (accepts it: all 4 methods, no credential leak); existing calendar data-ops tests green (zero regression). **#1231 (honest-degrade) is now connector-WIDE for github + calendar.** Filed **#1323** (dedup the connector-protocol logic into a shared `BindingBackedConnector` mixin at the 3rd connector — rule of three; deliberately deferred over refactoring just-shipped live github code mid-run + the m-41-guard interaction).

- **~09:00 — ARCH RULED (in parallel): A (hosted-OAuth) + Shape-B ratified + #1322 elevated to critical-path. Acked.** Sync pulled in Arch's rulings (Arch was working the same thread): (1) **provisioning = A** (hosted-OAuth, D3-realizing — exactly my recommendation; ruled on architecture-integrity, not toss-up), gated only on **PM's one business checkpoint** (cost/Copilot-seats/data-policy on `api.githubcopilot.com`); (2) **Shape-B ratified** (clean GO — my new-MCPClient approach is textbook m-40 layer-then-migrate); (3) **#1322 elevated from follow-up to CRITICAL-PATH** — Arch verified `simulation_mode` is hardcoded `True` (`services/mcp/client.py:93`, a Pattern-073), so the live MCP query path serves **simulated** data today; #1322 is what makes #1220's transport reach query routing. Arch's #1322 sequencing: behavioral coverage (real-data assertion) BEFORE deleting the sim stack + the m-36 sim-test-only guard. **Acked all three** (`8da17e768`): HTTP transport already shipped (A's cost paid), github+calendar ported, #1322 sequenced as the umbrella close. Both arch memos → read/. **THE GATE is now PM's single business-checkpoint yes/no** — everything buildable without it is shipped; github inc.2 (OAuth callback via `connect_http` to the hosted endpoint) is GO the moment PM clears it.

- **~09:30–14:00 — PM cost convo → A→C RE-RULE (the afternoon arc).** PM engaged the cost/data-policy dimension: (a) corrected my over-attribution (the API spend is PM's own testing, NOT tester Jake who hasn't installed) → saved memory `feedback_cost_token_efficiency_paramount` (token/cost-efficiency paramount across product + Flywheel + DinP-OS); (b) **requiring testers to have Copilot = non-starter** → A's hosted endpoint is out. I surfaced **option C (self-host the official github-mcp-server + per-user OAuth, no Copilot)** which Arch hadn't formally weighed (A-tree's fallback was B/PAT). **Arch RE-RULED A→C** + confirmed the token-custody model (binding → #358-encrypted OAuth grant; D3 precised to "no raw PAT," not "no token") + owned the "no token touches Piper" imprecision. Filed **#1325** (GitHub-App installation-token = D3-ideal end-state, m-36 ratchet, when the server supports it). Also: acked the Exec inbox-proxy ratification; fixed a cwd-bug slip (a decisions.log append landed in PM's main checkout — surgically removed my lines only, PM's draft untouched). decisions.log records C.
- **~14:30 — inc.2 build STARTED (option C). Gameplan + slice A shipped.** Gameplan `dev/2026/06/27/1317-inc2-github-oauth-gameplan.md` — key correction: C's GitHub auth is a **plain GitHub OAuth App flow** (mirror the calendar handler), NOT MCP-PKCE; the user's GitHub token forwards to the self-hosted server via the Authorization header (so `connect_http` needs header support back — slice C). All build surfaces located (routes=settings_integrations.py, grant store=`user_api_keys`+`user_api_key_service`, transport=`connect_http`). **Slice A**: `services/mcp/consumer/github_oauth_handler.py` — `GitHubOAuthHandler` mirroring calendar (authorize-URL + user-bound/single-use/expiring state CSRF #734 + code-exchange w/ GitHub's 200-with-error quirk; token never logged). **9 tests** (`test_github_oauth_handler_1317.py`); 108 consumer suite green. PM registering the OAuth App in parallel (callback `…/api/v1/settings/integrations/github/callback`).

- **~15:30 — inc.2 slice C shipped: `connect_http` auth-header.** Re-added `headers=` to `MCPClient.connect_http` (via the SDK's `create_mcp_http_client(headers=...)` + `AsyncExitStack` for clean lifecycle) so the user's forwarded GitHub OAuth token reaches the self-hosted server (ADR-070 C). 1 network-free test (asserts the auth header is wired into the http-client factory — security-critical); 8/8 #1220 transport tests green (the no-header real-HTTP round-trip still passes). **Slices A + C done** (the two foundational/independent pieces: OAuth handler + transport-auth). **Remaining (focused next pass): B grant-storage (`user_api_keys`), D web routes, E adapter live-wiring** — these tie together + the LIVE path also needs PM's OAuth-App registration + the Docker server deploy.

- **~15:50 — PM cost/work-pace correction + sprint-status review + inc.2 slice B.** (1) PM corrected two things: I over-flagged the secret-in-chat (I'd *asked* for it via chat) + I **over-applied "economy" to unilaterally decide to stop — that's PM's call, not mine** (fixed `feedback_cost_token_efficiency_paramount`: efficiency = less waste per unit, NOT deciding when to work). (2) **Sprint status (pulled the board Sprint field myself):** RECONNECT = 23→**26 items, 10 Done / 16 Sprint Backlog / 0 In Progress**. **Triaged the 4 new untriaged** per PM: all → assignee `mediajunkie`; #1322/#1323/#1325 → RECONNECT sprint (verified); #1324 (config audit) → PM, sprint left for PM. Stored PM's GitHub OAuth App creds in the keychain (rotate-before-alpha flagged). (3) **inc.2 slice B**: `services/mcp/consumer/connector_grant_store.py` — `ConnectorGrantStore` over the #358 `user_api_key_service` (grant at `(user,"<connector>_mcp_oauth")`, encrypted, validate=False; binding=pointer via the (owner,connector) convention, no schema change). 4 tests; 113 consumer green. **Slices A+B+C done; D (routes) + E (adapter wiring) remain.**

- **~16:00 — inc.2 slice D shipped: the OAuth web routes + connection-persist orchestration.** `persist_github_connection(session, user_id, access_token)` in `github_oauth_handler.py` — stores the grant (ConnectorGrantStore) + marks the #1229 binding BOUND with the self-hosted server ref (`GITHUB_MCP_SERVER_URL`); binding holds no token (D3). Added `/github/connect` + `/github/callback` routes to `web/api/routes/settings_integrations.py` (mirror the calendar routes: connect→authorize-url; callback→exchange→persist→redirect). Routes register cleanly (no collision w/ the native `/github/*` settings routes); 1 persist test (in-memory binding store); 114 consumer green. **Slices A+B+C+D done; E (adapter live-wiring) remains** — then the LIVE deploy (github-mcp-server Docker + creds + code on the Droplet) + browser round-trip.

- **~16:10 — inc.2 slice E shipped: github adapter live-wiring. inc.2 is CODE-COMPLETE (A–E).** (E1) connect() unbound now offers the connect-URL `action_hint` (`/api/v1/settings/integrations/github/connect`), routed through `degrade()` (DRY). (E2) `_mcp_client_ctx` rewired stdio→**HTTP**: fetches the user's grant (`ConnectorGrantStore`) + `connect_http(binding.mcp_server_ref, headers={Authorization: Bearer <grant>})`; retired the stdio `_server_params_for` stub. 3 new tests (action_hint + the HTTP-grant seam); **130 green** (consumer + m-41 + arch-enforcement); inc.3 resolve tests unaffected (the seam stays patchable). **The GitHub connector is fully wired for option C** (connect/status/resolve all binding-aware + honest-degrade + live HTTP transport). **inc.2 CODE-COMPLETE.** Remaining for LIVE = (1) PM registers the OAuth App [done — creds stored], (2) deploy `ghcr.io/github/github-mcp-server` Docker + push code + set env (`GITHUB_OAUTH_REDIRECT_URI`=alpha, `GITHUB_MCP_SERVER_URL`) + creds on the Droplet, (3) browser OAuth round-trip → grant stored → binding bound → resolve returns real GitHub data, (4) confirm the concrete `_RESOLVE_TOOL` name against the real server (provisional).

- **~18:00–19:00 — LOCAL STAGING GO-LIVE of the GitHub connector + badge fix + #1322 de-risk (the evening arc).** PM chose "go live from main in a staging sense without cutting a release" (NOT prod — production is 536 commits behind; an alpha release is deferred). (1) **Stood up** `ghcr.io/github/github-mcp-server` (v1.5.0, `http` on :8082) + restarted the local app from the worktree on inc.2 code with `GITHUB_MCP_SERVER_URL=http://localhost:8082/mcp`. (2) **PM did the browser OAuth round-trip** → callback exchanged the code (rotated secret) → grant stored encrypted (#358) → **binding BOUND**. (3) **DATA PATH PROVEN LIVE** via `connect_http` + PM's real grant: `get_me`→mediajunkie; `search_issues("assignee:@me is:open is:issue")`→**179 real issues** (user-wide, no repo-resolution → sidesteps #1230 for my-issues); `list_issues(owner,repo)`→repo issues. Connector + per-user fetch end-to-end proven, zero remaining unknowns. (4) **Badge fix**: PM saw "disconnected" because `GET /github` status is native-PAT + system-scoped (can't see the per-user OAuth binding). Added user-aware **`GET /github/oauth-status`** (reads the per-user `connector_binding`; returns no token, D3) + the settings page checks it on load → "✓ Connected with GitHub via OAuth." Committed + pushed (badge fix commit on main). (5) **#1322 cutover de-risked + scoped** (this is the **B** fork PM chose over polishing the vestigial native `resolve_repo`/#1230 — "we should not polish our vestigial pathways"). Findings: the live github-query consumer (`federated_search`, `query_router:879`) is **system-scoped / no user context** AND only reachable via the **MCP-server tool surface** (`server_core`) + a demo — the **web chat has no github-read path at all** (grep of `services/intent*/` + `web/` for the github adapter is empty). So #1322 = **D4 principal-threading** (thread `user_id` from request origin → `federated_search` → fetch) + **decide/wire WHICH user-facing surface reads github** + transport swap. The full finding + proven data path (`search_issues`/`list_issues`) + 5-phase cutover seed captured on **#1322** (comment 4824054374). **Next: a focused #1322 pass — PM to pick the first surface (chat = the natural "ask Piper about my issues").** Staging stack left UP (container `piper-ghmcp` :8082 + app :8001 on inc.2) for PM to keep exercising the connect.
