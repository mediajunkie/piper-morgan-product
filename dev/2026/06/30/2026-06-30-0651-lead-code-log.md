# Lead Developer — Session Log 2026-06-30

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Opus 4.8
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B). Sole lead. Continues the 6/27–29 RECONNECT arc (6/29 DAY-CLOSED). Carry-forward: `dev/active/lead-carry-forward.md`.
**Mode**: RUN LEAN through Wed Jul-1 ~9pm (usage). PM doing UAT.

## START 6/30 (06:51)
- **Step-0**: 6/29 DAY-CLOSED ✓ (proper STOP; no self-heal needed). Synced origin/main (current). Lead inbox clean. **Staging health 200** (up — PM tested this morning).
- **State carried in**: #1327 now-buildable scope COMPLETE as of 6/29 (set-default GUI+conversational · connector reads [branches/releases/issue; labels+milestones native] · repo-config — all live, several Lead-verified live). #1329 closed (PM-verified). #1331 (write-confabulation → honest-degrade) live, **PM-verify pending**.
- **PM reports a testing finding this morning** — message arrived truncated ("…something is still [off]"); awaiting specifics before acting (no guessing — anti-confabulation). **Hypothesis to hold (NOT asserted)**: the #1331 milestone honest-degrade keys on the classifier emitting a *covered* action string; if the live classifier emits a different one for PM's phrasing, the milestone could STILL confabulate — the one live risk I flagged 6/29. Will confirm against what PM actually saw.

- **(~07:10) PM UAT finding: confabulation PERSISTS → systemic floor-hardening (the real fix).** PM tested "can you add a milestone to my repo?" → Piper **confabulated** "the test milestone is sitting there" (trusting yesterday's fake "✓" still in the conversation history) + offered to add another; the honest-degrade fired only several turns later ("I don't actually have the ability to create milestones"). **Diagnosis**: #1331's action-coverage fix is INCOMPLETE — `conversational_floor.py::FLOOR_SYSTEM_PROMPT_ADDENDUM` had "Never fabricate user DATA" but **NO rule against claiming ACTION-SUCCESS / RESOURCE-EXISTENCE**, and nothing told it to distrust prior "✓" claims in history → it re-asserted the fake milestone as real. (The reads were honest: "show milestones" → "no milestones" = correct.) **Fix (systemic)**: added a CRITICAL floor rule — never claim an action happened / a resource exists unless verified THIS turn; distrust prior success claims in history; never simulate/pre-announce success; affirm existence only from [Available context]. + guard test `test_floor_no_action_fabrication_1331.py` (44 green incl. existing floor tests). **This is the real fix vs #1331's action-coverage stopgap; it's a trust-property change → flagging HOST/Arch for ratification.** **Behavioral verify = LIVE** (PM, fresh conversation — a prompt change doesn't guarantee LLM adherence; if it still confabulates we escalate to a deterministic guard). **SEPARATE BUG**: "your message came through empty" (×2) — PM's messages arrived blank to the classifier (`llm_classifier.py:171` correctly rejects empty; content lost UPSTREAM = chat-input/transport bug). Filing separately.

- **(~07:30) #1332 empty-message — traced + INSTRUMENTED (PM curious about cause).** Trace: backend `/intent` (`intent.py:238`) defaults `message` to `""` if the payload's "message" is missing/blank → empty payload → the floor's "came through empty." The main chat send (`web/static/js/chat.js:494`) captures `message` BEFORE clearing + returns early on empty → it CANNOT send blank; `processPermissionIntent` returns null for normal text → not the source. So the empty payload is an **intermittent edge** (a race during the rapid back-and-forth fits: 2 long messages blanked, the short "yes" got through), not the happy path. Can't reproduce from code alone → **instrumented**: added a diagnostic WARNING in `/intent` (`intent_empty_message_payload_1332`) capturing payload-keys / message-repr / content-length / content-type / UA / session when a message arrives empty — the next occurrence root-causes it (distinguishes frontend-sent-blank `{message:""}` vs no-"message"-key vs empty-body/transport). Committed + restarted (live). The real fix follows the captured cause (likely a frontend race hardening); the backend already degrades gracefully (no crash). #1332 updated.

- **(~07:35) ⭐ #1331 floor fix LIVE-VERIFIED by PM (fresh conversation) + new default-repo-knowledge gap.** PM re-tested "Please add a milestone to my repo" in a FRESH chat → Piper said upfront "I can't directly push changes to GitHub from this conversation", then on details → **"I can't create milestones from chat yet — add it directly in GitHub."** **#1331 = LIVE-VERIFIED, NO confabulation.** 🎉 The systemic floor-hardening holds. **New gap** (PM "one step back"): "what is my default repo?" → Piper "I don't have it in context" — the floor's new honesty rule WORKING (refused to guess) but **unhelpful**: the default IS set (`ConnectorConfigService.get_default_repo`, connector_configs); the floor's selective context-render (`_format_domain_context`) doesn't include it. **Fix DELEGATED**: a **get-default-repo query handler** — the INVERSE of build #1's set-default (pre_classifier + action + handler reading `get_default_repo`) → Piper reports its default deterministically. → review + merge + restart on completion. Part of the #1327-meta "Piper knows its config/rules" layer.

- **(~07:50) get-default-repo query handler MERGED + LIVE.** Reviewed before trust: re-ran **53 tests** myself (get-default + set-default collision-guard + architecture ratchet); feature scope = +454 additive (handler + 324-line test); main-checkout clean. Merged → on main; staging restarted (health 200) → Piper now answers **"what is my default repo?"** deterministically (reports the repo if set; prompts to set one if not). **⚠️ Agent-deviation caught + handled**: the isolated agent **PUSHED its commits to `origin/claude/interesting-beaver-7ee19c`** (my origin branch) — a process deviation (isolated agents should commit to their worktree branch and let me integrate, NOT push to origin). **Verified NOT on origin/main** (main safe) + main checkout clean. Reconciled origin/claude (fast-forwarded my merged HEAD) + cleaned up the worktree/branch. **Lesson → future agent prompts must explicitly forbid `git push` (commit to the worktree branch only); and verify a delegated agent's push/branch claims, never trust them** (the agent's report mislabeled its branch as `claude/...` — it was on the isolated branch but had pushed to origin/claude). #1327-meta self-knowledge advanced.

- **(~08:05) #1330 CLOSED — disconnect now clears the OAuth binding + revokes the #358 grant (not just the PAT).** Verify-before-extend: traced the full chain — `disconnect_github` only cleared the native PAT (keychain+env+cache); the OAuth binding (`ConnectorBindingRepository`) stayed BOUND + the #358 grant (`ConnectorGrantStore`) persisted → badge/health/reads all gate on `status==BOUND` → Disconnect left "Connected via OAuth." **Fix** (TDD, mirrors the inverse of `persist_github_connection`): added `ConnectorGrantStore.delete` (the store-inverse, idempotent) + `disconnect_github` now `set_status(UNBOUND)` + `grant.delete` (best-effort — a missing binding/grant never fails the disconnect). **186 tests pass** (mcp/consumer + connectors + settings_github + keychain_scoping). **Drive-by**: fixed pre-existing test-theatre in `test_keychain_scoping_849::test_save_github_token...` (mocked the obsolete `test_connection` → real `verify_github_token`; it 401'd on real validation). Surfaced by the regression — **NOT my change** (diff isolated to `disconnect_github`; the file's disconnect tests all passed; verified before fixing). Commit `4cb71d528` on origin/main; staging restarted (health 200); #1330 closed with evidence.

- **(~08:30) PM's "point-fix vs general" meta-thread → 4 structural artifacts + handoff prep.** PM pushed twice (rightly) on whether today's fixes were band-aids. **(a) Fabrication five-whys** → root cause: the architecture trusts the LLM floor to be honest by default; an LLM confabulates by default. General fix = the QUERY-vs-ACTION split: unhandled ACTIONS need a DETERMINISTIC decline (generalize the `unwired_writes` LIST → a CATEGORY rule), QUERIES keep prompt-honesty + promote-to-handler (get-default was a fine point-fix). Filed **#1333** (ADR-worthy; Arch/HOST). **(b) Disconnect sweep** → verified github is the ONLY connector that *creates* a binding+grant (so #1330 was the only LIVE instance; calendar reads-but-doesn't-create; notion not on the model) → the risk is STRUCTURAL (the next OAuth connector reintroduces it). Surfaced a real bug: **duplicate `/slack/disconnect` route** (lines 440+2146; the live one may not revoke Slack-side). Filed **#1334** (connector-agnostic disconnect helper + slack-dup). **(c) Gate matrix** (PM-directed): 8 considerations × connectors; audit the closed RECONNECT issues against it before declaring connector-refactor done — github row verified, calendar/slack/notion to-audit. Filed **#1335**. **(d) Canonical-regression** expand/refactor sketch (connector-state coverage bound/unbound/unreachable + trust-property assertions; split judged-quality from deterministic-behavioral). Filed **#1336** (placeholder; triage later). **(e) Handoff prep** (PM may hit the usage wall → backup-account resume): wrote `dev/active/lead-handoff-2026-06-30.md` (standalone resume memo); inbox confirmed empty; session log current; all pushed to origin/main. PM → OpenLaws ~08:32. **The day's theme: turn point-fixes into structural guarantees.**

- **(~08:50) #1335 gate audit DONE (agent-audited + Lead-VERIFIED) → 1 new bug, 1 corrected agent-error, 1 Arch decision surfaced.** Ran a read-only Explore audit of calendar/slack/notion vs the 8-consideration matrix, then verified its load-bearing claims. Findings: **(1)** the #1330 disconnect-leak is **github-ONLY and FIXED** — only github creates a binding+grant; calendar/slack/notion are keychain-model (no binding to leak). **(2) ⚠️ corrected an agent error**: the audit claimed github disconnect doesn't clear the binding ("Gap 2") — **FALSE** (verified `set_status`+`grant.delete` at `settings_integrations.py:1838/1841` + test `test_settings_github.py:245` per #1330); did NOT propagate it. **(3) NEW real bug — notion health is env-var-only** (`integrations.py:435`), missing the user-scoped keychain `save_notion_key` writes to → UI-configured notion reads "not configured" (parallel #1329). Filed **#1337**. **(4) Big-picture**: the binding model is **github-only** → the binding-specific gate cells are **N/A-until-migrated** for calendar/slack/notion → the open question is an **Arch roadmap decision** (migrate the keychain connectors onto the binding model, or accept the two-model split), NOT a set of leak-bugs. Corrected matrix recorded on #1335 (comment). **Gate done-when**: #1337 + #1334 fixed + Arch binding-migration decision. **Lesson reinforced: VERIFY agent findings — the audit's Gap 2 would have falsely reopened #1330.**

- **(~09:00) #1337 (notion health) FIXED + CLOSED — first gate cell turned green.** Verify-before-extend caught a nuance: notion stores via `UserAPIKeyService` (#358 DB store, provider "notion"), NOT `KeychainService` (which slack/calendar use) — so the fix mirrors the user-scoped *intent* via notion's actual path, not a literal slack-copy. `_get_integration_config_status` notion branch now checks the user-scoped store after the env check. **38 tests** (test_integrations + #1329 health); +2 new (configured-via-user-key / not-configured-no-key). Commit `818880596` on main; staging restarted (health 200); #1337 closed with evidence; #1335 notion cell → ✅. **Gate done-when now: #1334 (slack-dup, Slack-is-last) + an Arch binding-model-migration decision** — i.e., the autonomous-buildable RECONNECT queue is drained; what remains is Arch-gated (#1333, the binding-migration call), ordered-last (Slack/#1334), or usage-paced (inc.4, Wed). Clean checkpoint.

- **(~09:10) PM moved SLACK UP (team on slow cycles + autonomous queue drained, ~99% usage) → wrote the Slack approach plan.** `dev/2026/06/30/slack-reconnect-plan-2026-06-30.md`. Scope = 4 keychain-model (ADR-058) items: **#1110** (multi-tenancy bug — `SlackClient.get_config()` without `user_id` at slack_client.py:77/89/116), **#1334** (dup `/slack/disconnect`; the Slack-side-revoke impl is shadowed), **#1109** (OAuth state class-level dict → Redis), **#1201** (inbound Socket-Mode onboarding, CXO-gated). Recommended sequence #1110→#1334→#1109→#1201; **first step on resume = #1110** (thread user_id, TDD; check #1085's masking). Carry-forward + handoff both point at the plan. **PM will resume on the backup account at the 100% cutover to execute.** This is a PLAN deliverable (PM asked to plan the approach first); no code yet.

- **(~09:20) BRIEFING-CURRENT-STATE refreshed (PM-asked — it was stale).** Frontmatter said 6/27 but the entire 6/27–6/30 connector arc was missing. Per the mandatory staleness-refresh discipline, refreshed **targeted to my lane (RECONNECT)**: banner June 27–30 entry + Current Focus rewrite (+ superseded-marker on the old WS-2 note) + Last Updated attest + frontmatter `last_updated`/`last_verified` → 6/30 + a structured **June 30 Recent Progress** entry. Captures: connector LIVE + #1327 config + trust fixes (#1331/#1329/#1330/#1337) + the point-fix→structural issues (#1333–#1336) + Slack-now. Left sections outside my visibility alone (Inchworm WS-2-next line + pre-6/27 banner log flagged historical). Committed + pushed.

- **(~09:30) #1110 (Slack multi-tenancy bug) — investigated + DELEGATED to a bg worktree agent (TDD, no-push).** Verify-before-extend: `SlackClient.get_config()` called w/o `user_id` at 3 sites (slack_client.py:78/90/117); `get_config(self, user_id)` is REQUIRED (config_service.py:92); `__init__` (:58) stores no user_id. **It's a real multi-tenancy threading task, not a param-add**: the router (`slack_integration_router.py:70/84`) constructs `SlackClient(config_service)` **EAGERLY in `__init__`** while user_id is per-request → an **eager-vs-lazy construction design question** (handed to the agent to decide from the actual construction context); `get_slack_channels` (`settings_integrations.py:499`) is **doubly broken** (`SlackClient()` — no args at all). Agent has the caller map + design latitude + "flag any caller with no user_id rather than passing None." Agent ID `ae9eef98...`, branch `worktree-agent-ae9eef98...`. → review (re-run slack tests + main-integrity) + merge on completion, then #1334 → #1109 → #1201.

- **(~09:50) #1110 MERGED + properly CLOSED (after correcting a close-discipline miss) + Phase-3 split → #1338.** Agent's work: threaded user_id through SlackClient (required `__init__` param) + the 3 get_config sites; **decided LAZY per-operation router construction** (`_get_client(user_id)`) after proving the router is a startup singleton (#784); + a **real multi-tenancy catch** — the webhook path's `user_id` is the SENDER's Slack ID, but creds key on the connector-OWNER's Piper user_id (#759) → added `connector_user_id` threading. **Lead-VERIFIED (not trusted)**: re-ran 216 slack+settings+arch tests green post-merge in my worktree; spot-read the two-namespace diff (real + sane, not narrative); main-integrity clean. Merged `36682f217` → main; restarted (health 200); agent worktree cleaned. **⚠️ Anti-confabulation catch**: the agent reported the integration-test failures as "4 spatial_adapter/MagicMock failures" but they're actually a **stale-import collection error** (`services.domain.models` has no `models.py`) — inferred-not-observed; pre-existing + unrelated to #1110 either way. **⚠️ MY close-discipline miss + correction**: I closed #1110 then saw it had 44 unchecked AC boxes (comment-only-close anti-pattern — I grepped the count but didn't *gate* on it). Corrected: the agent did the CORE (threading + multi-tenancy) but **Phase 3 (user-token request path + `_fetch_slack_mentions_items` migration) was DEFERRED** → filed **#1338** + updated #1110's description (32 `[x]` core, 12 `[ ] *Deferred → #1338*`, status banner). **Lesson: gate the close on the checkbox-count, don't just print it.** **Next: #1334 (slack-dup) → #1109 → #1201.**

- **(~10:15) #1334 Part 1 (duplicate /slack/disconnect) FIXED + live.** Found a subtle **two-def split**: FastAPI routed to the FIRST `disconnect_slack` (440: user-scoped keychain-delete, NO Slack-revoke) while Python's imported symbol resolved to the SECOND (2176: revoke, but it **leaked the keychain creds** — env-only). Neither a superset → **MERGED** both into the canonical def (keychain-delete `slack_bot`+`slack_user` #849 + Slack-side OAuth revoke + env-clear) + removed the dup. +1 test (`test_clears_user_scoped_keychain_and_revokes_1334`) asserting both behaviors (the gap — prior tests imported the symbol = the revoke-only def). **221 passed**; commit on main; staging restarted (health 200). **#1334 kept OPEN for Part 2** (the connector-agnostic `disconnect_connector` helper — gated on the Arch binding-migration decision, #1335). **Next: #1109 (Redis OAuth state) → #1201 (CXO-gated).**

- **(~10:25) #1109 (Slack OAuth state → Redis) DELEGATED to a bg worktree agent (TDD, no-push).** Scoped first: `_oauth_states` (class-dict, `oauth_handler.py:46`) used at :109/:209/:252/:290-299/:602-625; reuse `RedisFactory` (`services/cache/redis_factory.py`); store nonce→JSON keyed `slack:oauth:state:{nonce}` with TTL=`expires_at` (→ Redis TTL **replaces the manual cleanup** :602-625). **The real lift = sync→async** (`generate_authorization_url` :58 is sync; `redis.asyncio` needs await → the OAuth routes must go async) — agent has a **STOP-if-blast-radius-exceeds-the-OAuth-routes** guardrail. Agent `af55e861...`, branch `worktree-agent-af55e861...`. → on completion: review (re-run slack tests + main-integrity + scrutinize the sync→async decision) + merge. **Then #1201 (CXO-gated, last Slack item).** **IN FLIGHT as of this entry** — if the session hits the wall, the resume should check this agent's branch for committed increments.

- **(~10:50) #1109 MERGED + live; KEPT OPEN (Phase 2 + a deviation, close-gated on the checkbox-count this time).** Agent's core merged (`27b5dda`): `_oauth_states` class-dict → Redis (`RedisFactory`, `setex` TTL **replacing** the manual cleanup, `GETDEL` single-use, `scan_iter` status). **Lead-VERIFIED**: re-ran **250 tests green**; class-dict gone (0 refs); scope sane. Merged → main; restarted (health 200); agent worktree cleaned. **Kept #1109 OPEN** (gated on its 43 boxes — the #1110 lesson applied): **(a) Phase 2 (env-var-gated in-memory fallback) NOT done** — always-Redis; YAGNI given Redis is always provisioned (:6379)? → **PM call**; **(b) deviation**: the "invisible to callers" AC was infeasible (`redis.asyncio` → 5 methods made `async` + callers `await`) → **ratify**. Status comment posted; #1109 OPEN for PM's Phase-2 + deviation call. **Discovered (pre-existing)**: webhook `_get_oauth_authorization_url` calls `generate_authorization_url` w/o `user_id` (ValueError) → filed **#1339**. **⚠️⚠️ PATH-SAFETY INCIDENT (2nd this session)**: the agent's file tools resolved to PM's **MAIN CHECKOUT** (live workspace, 435 uncommitted drafts) — it caught it pre-commit, transplanted to the worktree, surgically reverted its files in main. **Lead-VERIFIED main intact** (435 untouched; agent files clean). Real infra risk — flagging to PM. **Slack lane: only #1201 (CXO-gated) remains.**

- **(~13:20, backup account, post-wall RESUME) Wall-recovery + #1339 fixed inline.** The previous-account wall stranded one uncommitted carry-forward edit (the SLACK-lane status) — local disk persisted it; committed + pushed it first thing (continuity priority). Then drained the smallest genuinely-unblocked Slack item **inline** (not a bg agent — the 2 path-safety incidents + token burn make agents contraindicated under RUN-LEAN/rate-limit-fragility): **#1339 FIXED + closed** — webhook `_get_oauth_authorization_url` now threads `_get_connector_user_id()` (connector OWNER, not sender Slack id #1110) into `generate_authorization_url` + `except HTTPException: raise` to preserve the clear "SLACK_CONNECTOR_USER_ID not configured" error past the generic 500. +2 tests; **201 slack-unit passed**; commit on main; staging restarted (health 200). **Slack lane now: #1110 ✓ #1334-P1 ✓ #1109 ✓(open for PM Phase-2) #1339 ✓ — remaining = #1338 (Phase 3, unblocked but meatier: user-token request path + mentions migration) + #1201 (CXO-gated).** **HELD #1338**: it's a multi-file design-bearing change (bot-vs-user-token path on `_make_request`); matching work-unit size to runway, it wants a clean window (rate-limit resume ~2:20, both accounts fragile) — NOT a fragile-partial-budget start. Teed up as the clear next unblocked step.

- **(~15:25, backup account) ⭐ #1338 (Slack user-token path + search.messages migration) COMPLETE + closed — the meaty Slack item, done inline across 4 layers (TDD).** PM said proceed; did it inline (sole agent + agents contraindicated). Verify-before-extend first: `SlackConfig` had no user_token, `search_messages` didn't exist on the router, `_fetch_slack_mentions_items` (context_assembler.py:1729) used direct-aiohttp. **Built**: (L1 config) `SlackConfig.user_token` + `slack_user` keychain load (4-layer, user-scoped); (L2 client) `_make_request(use_user_token, params)` with honest-degrade when no user token + `search_messages()` + `test_auth(use_user_token)`; (L3 router) `search_messages(query, user_id)` + `test_auth(use_user_token)` passthrough (mirrors the lazy `_get_preferred_integration` pattern); (L4 assembler) `_fetch_slack_mentions_items` now routes through `router.test_auth(use_user_token=True)` (handle) + `router.search_messages()` — aiohttp workaround retired, docstring updated. **Tests**: +8 new (`test_user_token_path_1338.py`, all 4 layers incl. token-selection + honest-degrade); **rewrote the 6 existing mentions tests** onto router-mocks (they broke as expected — impl-coupled to the old aiohttp; preserved time-window/missing-ts/fail-graceful coverage); removed 2 now-dead aiohttp helper fns. **2007 passed / 2 skipped** (slack + settings + intent_service + architecture). Commit on main; staging restarted (health 200). **Closed properly**: 4/5 AC `[x]`, the 5th (live manual verification) **deferred-with-note** — gated on a real Slack user token + `search:read` re-auth (PM Slack workspace re-authorization; honest-degrades to [] until then). **★ SLACK clean-autonomous lane now FULLY DRAINED — everything remaining is gated** (see below).

- **(~15:40) Arch brought over by PM → sent the consolidated Arch decision-request memo** (`mailboxes/arch/inbox/2026-06-30-lead-to-arch-reconnect-gated-decisions.md`; cc PM/HOST/PA; on main `95d873d73`). Three gated items, leverage-ordered: **① binding-model migration (#1335 keystone)** — github-only binding model; (A) migrate calendar/slack/notion onto it or (B) accept the two-model split + justified-N/A the gate cells (my lean: B for the MVP window, Arch's call); **② #1334-P2 connector-agnostic disconnect helper** — shapes around ①; **③ #1333 fabrication list→category rule** — Arch+HOST trust-property ADR bless. ① unblocks the #1335 gate-close + ②; ③ unblocks the deterministic fabrication build. CXO next (PM bringing over for #1201). **HOLD point: only-active-build-agent, Slack drained, awaiting Arch decision; remaining autonomous chunk = inc.4 (PM-paced Wed).**

- **(~15:55) inc.4 STARTED (PM said go) → STOP-condition #1: the removal-set gameplan is FALSIFIED. NO deletes made.** Did the verify-before-delete pass (destructive work). Findings that contradict the carry-forward's "federation spatial POC is DEAD, delete ~10 files":
  - **`github_spatial.py` (`GitHubSpatialIntelligence`) is LIVE** — it's the github router's graceful **FALLBACK** (`github_integration_router.py:117` `self.spatial_github = GitHubSpatialIntelligence()`; router is live via `context_assembler.py:1178`). The plan's own "verify github_spatial isn't live first" → **it IS live.** Removing it = deleting a fallback path (a behavior change), not dead-code cleanup.
  - **`GitHubMCPSpatialAdapter` (mcp/consumer/github_adapter.py) is LIVE** — the DEFAULT github adapter (`query_router.py:106`, `github_integration_router.py:107`). Near-identical name to the dead-POC `GitHubSpatialIntelligence` → the plan's "remove the spatial adapters" is dangerously ambiguous.
  - **`services/integrations/spatial_adapter.py` is LIVE shared infra** (`BaseSpatialAdapter`/`SpatialContext`/`SpatialPosition`/`SpatialAdapterRegistry`) imported by github/calendar/notion adapters. NOT the federation dir.
  - **`mcp/consumer/__init__.py` couples dead+live**: line 8 imports `consumer_core` (MCPConsumerCore) at the same package level as line 9's live `github_adapter` → deleting consumer_core needs the live-path __init__ edited; MCPConsumerCore is also instantiated by gitbook/devenvironment/cicd adapters.
  - **The "non-sim" tests** (test_calendar_integration / test_complete_integration_flow / test_notion_integration) reference the LIVE `spatial_adapter`, not the federation POC → must NOT be deleted.
  - **Net**: inc.4 is NOT a clean bottom-up delete. The real question is architectural — *is the `GitHubSpatialIntelligence` MCP-fallback path still wanted now that the RECONNECT connector is the live default, or is it obsolete?* That's an **Arch** call (folds into the binding/connector-architecture thread already open with Arch), not autonomous dead-code cleanup. **Held all deletes; corrected the carry-forward removal-set premise; flagging to PM + Arch.**
  - **PM RULING (6/30): spatial intelligence is a PROTECTED key architectural innovation — never remove without consult.** General principle: only redundant *connection methods* are removable; *domain-model meaning-representation* is NEVER removable even if incomplete. Encoded in memory `project-spatial-intelligence-protected` + carry-forward. inc.4-as-written is dead; any future sim-TRANSPORT-only cleanup is mechanism-only, re-verified, PM/Arch sign-off.

- **(~17:10) Arch+CXO returned (4 memos) → built #1333 (trust keystone) end-to-end + CLOSED.** Arch ruled all 3 RECONNECT decisions + ratified #1331; CXO gave #1201 full design + decline-voice. **#1333 SHIPPED** (2 increments, live): inc.1 — `_handle_execution_intent`'s else-branch (the real confabulation vector — routed unhandled EXECUTION actions to the floor → fake "done ✓") now **deterministically honest-declines, never floors**, deriving the set by construction; inc.2 — retired the `UNWIRED_WRITE_ACTIONS` list + registration + dead `_handle_unwired_write` (kept the curated copy map). Signal refinement (decisions.log): the no-handler signal is the else-branch, not `get_action_workflows()` alone (2nd dispatch surface = ActionMapper). +3 tests, rewrote the #1331 test, **1775 green**, health 200, closed w/ evidence. Follow-ons: behavioral-corpus fixture (Arch drafts→I wire), HOST transparency, ADR-073 fold @M5. **Still buildable + unblocked: #1334-P2 (uniform disconnect helper), #1335 gate-close (B+discipline), #1201 (CXO go-ahead).**

- **(~17:30) #1335 gate-CLOSED + #1334-P2 built + CLOSED (PM-approved sequence).** **#1335**: finalized gate matrix w/ Arch's disciplined-fork wrapper (binding cells justified-N/A-with-rationale + named (B)→(A) trigger); done-when met → closed. **#1334-P2**: built `services/connectors/disconnect.py::disconnect_connector(user_id, connector)` — one call surface, per-model dispatch (github binding+grant #1330 / slack keychain+revoke / calendar key / **notion keychain + #358 store — bonus #1337 fix: disconnect had left the real token behind**); all 4 endpoints delegate. **Caught+fixed a real bug**: helper used stdlib logging w/ structlog kwargs → revoke-fail path crashed → switched to structlog (the existing revoke-raises test caught it — verify-with-real-behavior win). +6 helper tests; repointed #849 source-inspection + relocated endpoint tests; **97 disconnect tests green**; live (health 200); #1334 CLOSED (both parts). **RECONNECT connector-refactor essentially complete — only #1201 (Slack inbound, a full feature) remains → fresh focused block.**