# Lead Developer — Session Log 2026-06-30

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Opus 4.8
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B). Sole lead. Continues the 6/27–29 RECONNECT arc (6/29 DAY-CLOSED). Carry-forward: `dev/active/lead-carry-forward.md`.
**Mode**: RUN LEAN through Wed Jul-1 ~9pm (usage). PM doing UAT.

## START 6/30 (06:51)
- **Step-0**: 6/29 DAY-CLOSED ✓ (proper STOP; no self-heal needed). Synced origin/main (current). Lead inbox clean. **Staging health 200** (up — PM tested this morning).
- **State carried in**: #1327 now-buildable scope COMPLETE as of 6/29 (set-default GUI+conversational · connector reads [branches/releases/issue; labels+milestones native] · repo-config — all live, several Lead-verified live). #1329 closed (PM-verified). #1331 (write-confabulation → honest-degrade) live, **PM-verify pending**.
- **PM reports a testing finding this morning** — message arrived truncated ("…something is still [off]"); awaiting specifics before acting (no guessing — anti-confabulation). **Hypothesis to hold (NOT asserted)**: the #1331 milestone honest-degrade keys on the classifier emitting a *covered* action string; if the live classifier emits a different one for PM's phrasing, the milestone could STILL confabulate — the one live risk I flagged 6/29. Will confirm against what PM actually saw.

- **(~07:10) PM UAT finding: confabulation PERSISTS → systemic floor-hardening (the real fix).** PM tested "can you add a milestone to my repo?" → Piper **confabulated** "the test milestone is sitting there" (trusting yesterday's fake "✓" still in the conversation history) + offered to add another; the honest-degrade fired only several turns later ("I don't actually have the ability to create milestones"). **Diagnosis**: #1331's action-coverage fix is INCOMPLETE — `conversational_floor.py::FLOOR_SYSTEM_PROMPT_ADDENDUM` had "Never fabricate user DATA" but **NO rule against claiming ACTION-SUCCESS / RESOURCE-EXISTENCE**, and nothing told it to distrust prior "✓" claims in history → it re-asserted the fake milestone as real. (The reads were honest: "show milestones" → "no milestones" = correct.) **Fix (systemic)**: added a CRITICAL floor rule — never claim an action happened / a resource exists unless verified THIS turn; distrust prior success claims in history; never simulate/pre-announce success; affirm existence only from [Available context]. + guard test `test_floor_no_action_fabrication_1331.py` (44 green incl. existing floor tests). **This is the real fix vs #1331's action-coverage stopgap; it's a trust-property change → flagging HOST/Arch for ratification.** **Behavioral verify = LIVE** (PM, fresh conversation — a prompt change doesn't guarantee LLM adherence; if it still confabulates we escalate to a deterministic guard). **SEPARATE BUG**: "your message came through empty" (×2) — PM's messages arrived blank to the classifier (`llm_classifier.py:171` correctly rejects empty; content lost UPSTREAM = chat-input/transport bug). Filing separately.

- **(~07:30) #1332 empty-message — traced + INSTRUMENTED (PM curious about cause).** Trace: backend `/intent` (`intent.py:238`) defaults `message` to `""` if the payload's "message" is missing/blank → empty payload → the floor's "came through empty." The main chat send (`web/static/js/chat.js:494`) captures `message` BEFORE clearing + returns early on empty → it CANNOT send blank; `processPermissionIntent` returns null for normal text → not the source. So the empty payload is an **intermittent edge** (a race during the rapid back-and-forth fits: 2 long messages blanked, the short "yes" got through), not the happy path. Can't reproduce from code alone → **instrumented**: added a diagnostic WARNING in `/intent` (`intent_empty_message_payload_1332`) capturing payload-keys / message-repr / content-length / content-type / UA / session when a message arrives empty — the next occurrence root-causes it (distinguishes frontend-sent-blank `{message:""}` vs no-"message"-key vs empty-body/transport). Committed + restarted (live). The real fix follows the captured cause (likely a frontend race hardening); the backend already degrades gracefully (no crash). #1332 updated.

- **(~07:35) ⭐ #1331 floor fix LIVE-VERIFIED by PM (fresh conversation) + new default-repo-knowledge gap.** PM re-tested "Please add a milestone to my repo" in a FRESH chat → Piper said upfront "I can't directly push changes to GitHub from this conversation", then on details → **"I can't create milestones from chat yet — add it directly in GitHub."** **#1331 = LIVE-VERIFIED, NO confabulation.** 🎉 The systemic floor-hardening holds. **New gap** (PM "one step back"): "what is my default repo?" → Piper "I don't have it in context" — the floor's new honesty rule WORKING (refused to guess) but **unhelpful**: the default IS set (`ConnectorConfigService.get_default_repo`, connector_configs); the floor's selective context-render (`_format_domain_context`) doesn't include it. **Fix DELEGATED**: a **get-default-repo query handler** — the INVERSE of build #1's set-default (pre_classifier + action + handler reading `get_default_repo`) → Piper reports its default deterministically. → review + merge + restart on completion. Part of the #1327-meta "Piper knows its config/rules" layer.

- **(~07:50) get-default-repo query handler MERGED + LIVE.** Reviewed before trust: re-ran **53 tests** myself (get-default + set-default collision-guard + architecture ratchet); feature scope = +454 additive (handler + 324-line test); main-checkout clean. Merged → on main; staging restarted (health 200) → Piper now answers **"what is my default repo?"** deterministically (reports the repo if set; prompts to set one if not). **⚠️ Agent-deviation caught + handled**: the isolated agent **PUSHED its commits to `origin/claude/interesting-beaver-7ee19c`** (my origin branch) — a process deviation (isolated agents should commit to their worktree branch and let me integrate, NOT push to origin). **Verified NOT on origin/main** (main safe) + main checkout clean. Reconciled origin/claude (fast-forwarded my merged HEAD) + cleaned up the worktree/branch. **Lesson → future agent prompts must explicitly forbid `git push` (commit to the worktree branch only); and verify a delegated agent's push/branch claims, never trust them** (the agent's report mislabeled its branch as `claude/...` — it was on the isolated branch but had pushed to origin/claude). #1327-meta self-knowledge advanced.

- **(~08:05) #1330 CLOSED — disconnect now clears the OAuth binding + revokes the #358 grant (not just the PAT).** Verify-before-extend: traced the full chain — `disconnect_github` only cleared the native PAT (keychain+env+cache); the OAuth binding (`ConnectorBindingRepository`) stayed BOUND + the #358 grant (`ConnectorGrantStore`) persisted → badge/health/reads all gate on `status==BOUND` → Disconnect left "Connected via OAuth." **Fix** (TDD, mirrors the inverse of `persist_github_connection`): added `ConnectorGrantStore.delete` (the store-inverse, idempotent) + `disconnect_github` now `set_status(UNBOUND)` + `grant.delete` (best-effort — a missing binding/grant never fails the disconnect). **186 tests pass** (mcp/consumer + connectors + settings_github + keychain_scoping). **Drive-by**: fixed pre-existing test-theatre in `test_keychain_scoping_849::test_save_github_token...` (mocked the obsolete `test_connection` → real `verify_github_token`; it 401'd on real validation). Surfaced by the regression — **NOT my change** (diff isolated to `disconnect_github`; the file's disconnect tests all passed; verified before fixing). Commit `4cb71d528` on origin/main; staging restarted (health 200); #1330 closed with evidence.

- **(~08:30) PM's "point-fix vs general" meta-thread → 4 structural artifacts + handoff prep.** PM pushed twice (rightly) on whether today's fixes were band-aids. **(a) Fabrication five-whys** → root cause: the architecture trusts the LLM floor to be honest by default; an LLM confabulates by default. General fix = the QUERY-vs-ACTION split: unhandled ACTIONS need a DETERMINISTIC decline (generalize the `unwired_writes` LIST → a CATEGORY rule), QUERIES keep prompt-honesty + promote-to-handler (get-default was a fine point-fix). Filed **#1333** (ADR-worthy; Arch/HOST). **(b) Disconnect sweep** → verified github is the ONLY connector that *creates* a binding+grant (so #1330 was the only LIVE instance; calendar reads-but-doesn't-create; notion not on the model) → the risk is STRUCTURAL (the next OAuth connector reintroduces it). Surfaced a real bug: **duplicate `/slack/disconnect` route** (lines 440+2146; the live one may not revoke Slack-side). Filed **#1334** (connector-agnostic disconnect helper + slack-dup). **(c) Gate matrix** (PM-directed): 8 considerations × connectors; audit the closed RECONNECT issues against it before declaring connector-refactor done — github row verified, calendar/slack/notion to-audit. Filed **#1335**. **(d) Canonical-regression** expand/refactor sketch (connector-state coverage bound/unbound/unreachable + trust-property assertions; split judged-quality from deterministic-behavioral). Filed **#1336** (placeholder; triage later). **(e) Handoff prep** (PM may hit the usage wall → backup-account resume): wrote `dev/active/lead-handoff-2026-06-30.md` (standalone resume memo); inbox confirmed empty; session log current; all pushed to origin/main. PM → OpenLaws ~08:32. **The day's theme: turn point-fixes into structural guarantees.**