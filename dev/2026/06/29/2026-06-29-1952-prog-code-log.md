# Session Log — Coding Agent (prog) — 2026-06-29

**Role**: Coding Agent (Lead Dev's cohort)
**Model**: claude-opus-4-8 (Opus 4.8)
**Tool**: Claude Code
**Branch**: `worktree-agent-a86f7c641dfcf1de9` (ephemeral isolated worktree)
**Task**: #1327 gap 3 — repo-config cutover (Settings → GitHub) native-PAT → OAuth connector

---

## Goal

The Settings repo-config still uses the dead native PAT:
- `GET /github/repositories` (repo dropdown — lists user's repos) — native PAT via keychain/aiohttp → `api.github.com/user/repos`
- `GET/POST /github/preferences` (load/save selected repos + default)

Make them work for OAuth-connected users. GUI half of set-default (conversational half = build #1, shipped).

---

## Investigation findings (pre-build)

- `GET /github/repositories` (settings_integrations.py ~:1840) — the ONLY native-PAT path. Keychain `github_token` (user-scoped) → env fallback → aiohttp to `https://api.github.com/user/repos`. THIS is what to cut to the connector.
- `GET /github/preferences` (~:1930) + `POST /github/preferences` (~:1956) — already DB-backed via `_load_github_prefs_db`/`_save_github_prefs_db` → `ConnectorConfigService.get_config`/`set_config` over `connector_configs` (ADR-070 D4). **NO native-PAT gating** — already work for OAuth users. Confirmed by reading. So gap 3's real work = the repositories dropdown only; prefs just need a confirming test.
- Connector rail to mirror: `github_adapter.py` `_search_via_connector` (user-wide search, no repo resolution — exact fit for "list the user's own repos") + honest-degrade (`degrade()`, `_bound_binding_or_degrade`, `_mcp_client_ctx`).
- Chat-read fallback pattern (intent_service.py ~:4380, `_handle_list_issues_query`): connector first; native-PAT fallback ONLY on `CONNECT_REQUIRED`; degraded-but-connected → honest message (never silent PAT fallback). This is the decision logic to replicate in the route.
- Env up: github-mcp-server :8082 (401=up+auth), DB :5433.

## Plan

1. LIVE de-risk: confirm `search_repositories` exists + find the query that lists the bound user's OWN repos (PM: a25db09c-..., default repo mediajunkie/test-piper-morgan). **labels lesson — verify before building.**
2. TDD adapter method `search_user_repositories` (mirror `_search_via_connector` rail).
3. TDD route cutover of `GET /github/repositories` (connector-first, native-PAT fallback only on CONNECT_REQUIRED).
4. Confirming test for prefs endpoints (OAuth users).
5. pytest (my tests + architecture enforcement) green.

## Work completed

- **LIVE de-risk (labels lesson)**: confirmed `search_repositories` EXISTS in github-mcp-server
  (44 tools; repo-ish: create_repository, fork_repository, list_repository_collaborators,
  search_repositories — NO `list_repositories`). Query `user:@me` → bound PM user's OWN repos:
  total_count=18, payload `{total_count, incomplete_results, items[]}` (same shape as
  search_issues). Item keys include id/name/full_name/description (+private/fork/…).
  `user:@me fork:true` → 24 (includes forks); chose `user:@me` (non-fork default; matches
  native /user/repos intent).
- **Fire 1 (commit a4a8009dd)** — adapter: `search_user_repositories` + `GitHubReposResult` +
  `_parse_repo_search` (normalizes to {id,name,full_name,description}, null desc → ""). Mirrors
  the #1322 `list_open_issues` binding-aware honest-degrade rail. 8 TDD tests (FastMCP fixture).
- **Fire 1 (commit 64c3e39fc)** — route: cut `GET /github/repositories` to connector-first;
  native-PAT fallback ONLY on CONNECT_REQUIRED (extracted `_list_repos_native_pat`, 401/502
  preserved); connected-but-degraded → honest HTTP 502 (never silent PAT/empty, #1231).
  Module-level import for patchability. 6 TDD route tests.
- **Fire 1 (commit f17873559)** — `/github/preferences`: ALREADY DB-backed (connector_configs,
  ADR-070 D4), NO native-PAT gating → works for OAuth users unchanged. NO code change needed;
  added 2 confirming tests (prefs touches neither PAT nor connector binding).
- **LIVE round-trip verified**: `search_user_repositories(PM_uid)` → 18 real repos, all
  normalized to {id,name,full_name,description}. End-to-end binding→grant→search_repositories→parse.

## Test results
`pytest test_github_repositories_1327 + test_settings_github_repositories_1327 +
test_settings_github_preferences + test_architecture_enforcement -q` → **37 passed**.
No regressions: existing connector adapter tests (test_github_issues_1322 +
test_github_repo_scoped_reads_1327) → 21 passed.

## Discovered work
- None requiring a tracking issue. (Unrelated worktree noise: `skunkworks/klatch/...csv` modified
  pre-session — not mine, excluded via explicit-path adds.)

## Memory & briefing surfaces referenced this session
- **Referenced**:
  - CLAUDE.md "Verify First, Create Second" + the gap-2 labels lesson (prompt) — drove the LIVE
    de-risk before building; confirmed search_repositories exists, didn't assume.
  - CLAUDE.md ANTHROPIC_* env-strip warning — used the `env -u …` invocation for all server/SDK
    scripts + pytest.
  - `services/mcp/consumer/github_adapter.py` (#1322 `list_open_issues` / `_search_via_connector`
    rail) — the honest-degrade pattern to mirror.
  - `services/intent/intent_service.py` `_handle_list_issues_query` (~:4380) + `_handle_list_branches`
    (~:4979) — the canonical connector-first / PAT-fallback-only-on-CONNECT_REQUIRED decision rail.
  - Existing tests (test_github_issues_1322.py, test_settings_github_preferences.py) — fixture +
    mocking conventions.
- **Loaded but not referenced**: most of MEMORY.md index; the comms/publishing feedback items.
- **Wanted but not found**: nothing — the prompt's de-risk recipe + the existing #1322/#1327-gap-2
  code gave a complete pattern.
