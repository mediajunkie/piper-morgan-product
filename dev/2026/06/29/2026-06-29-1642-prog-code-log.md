# Session Log — Coding Agent (prog) — 2026-06-29

**Role**: Coding Agent (Lead Dev's cohort)
**Model**: claude-opus-4-8 (Claude Code)
**Branch**: worktree-agent-a3acee545deb46257 (ephemeral worktree)
**Task**: RECONNECT #1327 gap 1 — conversational "set my default repo"

## Feature
User types in chat "set my default repo to owner/name" (+ variants) → validate,
persist as that user's default repo (connector_configs store), confirm in chat.
Conversational counterpart to the GUI default-repo setting.

## Verify-before-extend findings
- `services/connectors/config_service.py::ConnectorConfigService.set_default_repo(owner_id, value)`
  — persister; writes `default_repository` key in the github connector config blob.
  Flushes but does NOT commit (caller owns txn).
- `services/integrations/github/repo_resolver.py::parse_full_name(value)` raises ValueError
  on bad "owner/name". `resolve_repo` path 3 (`_resolve_from_user_default` →
  `_read_user_default_repo_from_db` → `ConnectorConfigService.get_default_repo`) READS the
  same key my setter writes → round-trips. Confirmed.
- Handler pattern mirrors `_handle_list_issues_query` / `_handle_close_issue_query`:
  `_principal_from_intent(intent)` for user, parse from `intent.context["original_message"]`.
- Dispatch: `MAX_DISPATCH_SITES = 0` (all elif chains migrated). MUST use
  `WorkflowEntry(action_triggered=True)` in `workflow_entries.py` — zero new dispatch sites.
- Classifier: pre_classifier.py is deterministic regex → emits Intent(action, context). I add
  a SET_DEFAULT_REPO pattern block CHECKED BEFORE repo-management/portfolio (more specific).
  Action registry requires consistent entries: ACTION_REGISTRY + ACTION_EXAMPLES + ACTION_TO_VERB
  (added Verb.SET) + the example must pre-classify to its key. session_scope() commits on clean exit.

## Plan (TDD)
1. Tests first: handler (mock ConnectorConfigService), validation (bad shape graceful),
   pre-classifier patterns, dispatch routing, action-registry coverage.
2. Pre-classifier: SET_DEFAULT_REPO_PATTERNS → (QUERY, set_default_repo).
3. action_registry: add entry + example + verb (Verb.SET).
4. Handler `_handle_set_default_repo` in intent_service.py.
5. WorkflowEntry registration in workflow_entries.py.

## Progress
- TDD red: wrote test_set_default_repo_1327.py (16 tests) — failed as expected.
- Green increment 1: pre_classifier SET_DEFAULT_REPO_PATTERNS + check. Found ordering
  bug: "change my default repo to X" matched DOCUMENT_QUERY's "change ... to" pattern →
  moved the SET_DEFAULT_REPO check ABOVE the DOCUMENT_QUERY check (patterns require the
  literal "default repo[sitory]" so they steal nothing).
- Green increment 2: action_registry — entry + example + Verb.SET + ACTION_TO_VERB.
- Green increment 3: _handle_set_default_repo in intent_service.py (parse owner/name from
  original_message, validate via parse_full_name, persist via ConnectorConfigService inside
  session_scope(), confirm). Graceful on bad shape.
- Green increment 4: WorkflowEntry(action_triggered=True) "set_default_repo" in
  workflow_entries.py via _make_query_dispatch_entry_point.
- Updated the action_registry test's known_handled_query_actions allowlist (legit handler).

## Results
- Target run: `pytest test_set_default_repo_1327.py test_architecture_enforcement.py` →
  30 passed. (Architecture ratchet MAX_DISPATCH_SITES stays 0 — zero new dispatch sites.)
- Regression: `pytest tests/unit/services/intent_service/ tests/unit/services/connectors/`
  → 1732 passed, 0 failed.
- `pytest tests/unit -k "pre_classif or repo_resolver or repo_management"` → 257 passed.
- Pre-existing unrelated collection error: tests/load/test_cache_effectiveness.py
  (ModuleNotFoundError tests.load.setup_real_system) — NOT my change.
- NOTE: fix-newlines.sh re-normalized CRLF in skunkworks/klatch/klatch data model.csv
  (pre-existing line-ending noise); LEFT UNSTAGED, not part of this change.

## Wiring (classifier → action → handler → set_default_repo)
pre_classifier.pre_classify("set my default repo to owner/name")
  → Intent(category=QUERY, action="set_default_repo", context={original_message})
  → process_intent action-dispatch rail: intent.action in get_action_workflows()
  → dispatch_workflow("set_default_repo", ...) → WorkflowEntry.entry_point
  → IntentService._handle_set_default_repo(intent, workflow_id)
  → parse_full_name(candidate) → ConnectorConfigService(session).set_default_repo(user_id, "owner/name")
  → connector_configs github.default_repository  (SAME key resolve_repo path-3 reads)

## Memory & briefing surfaces referenced this session
- Referenced: CLAUDE.md "Intent dispatch — no new elif" (dispatch architecture);
  CLAUDE.md sign-off + explicit-path-add discipline (commit hygiene).
- Loaded but not referenced: most of CLAUDE.md cycling/mailbox protocol (not a cycling role here).
- Wanted but not found: none.
