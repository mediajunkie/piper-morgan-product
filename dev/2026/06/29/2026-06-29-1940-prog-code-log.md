# Session Log — Coding Agent (prog) — 2026-06-29

**Role**: Coding Agent (Lead Dev's cohort)
**Model**: claude-opus-4-8
**Branch**: worktree-agent-aedaa55b7ce7e8652 (ephemeral worktree, nests in main)
**Task**: Fix defect in just-merged #1327 gap-2 work — revert labels read to NATIVE (full TDD)

## The defect (live-verified by Lead Dev)
gap-2 cut `_handle_list_labels_query` to the connector via `list_labels_connector` →
`_LABELS_TOOL = "list_label"`. But github-mcp-server has NO list-labels tool (only `get_label`
for ONE label). Live → `unknown tool` → labels UNREACHABLE-degrade for OAuth users. Labels
must stay NATIVE, exactly like milestones (also no MCP tool). Other gap-2 reads
(branches/releases/review-issue) are live-verified GOOD — DO NOT TOUCH.

## Plan
1. Revert `_handle_list_labels_query` → native (mirror `_handle_list_milestones_query`).
2. Remove `list_labels_connector`, `_LABELS_TOOL`, `_parse_labels` from github_adapter.py
   (verified `_parse_labels` used ONLY by labels path — safe to remove).
3. Tests: remove labels cases from the two #1327 connector test files; adjust the #1040
   labels/branches test fixture so labels exercises the native path with no connector patch.

## Verify-before-extend findings
- Native template: `_handle_list_milestones_query` (intent_service.py ~4720) — straight
  `GitHubIntegrationRouter().list_milestones_via_mcp()`, no connector branch.
- Connector reads to PRESERVE: `_handle_list_branches_query` (~4984), `_handle_list_releases_query`
  (~4786), `_handle_review_issue_query`. Leave exactly as-is.
- `list_labels_via_mcp` exists in github_integration_router.py:569 (native path target).
- `_parse_labels` / `list_labels_connector` / `_LABELS_TOOL`: only non-test refs are inside
  the labels path itself → safe to remove.

## Progress
- TDD RED: updated the three test files to the target (native-labels) state first; ran
  test_handlers_labels_branches_1040.py → FAILED as expected (handler still called
  list_labels_connector, now unpatched → real connector returned REPO_UNRESOLVED "which repo?").
- TDD GREEN: reverted `_handle_list_labels_query` to native (mirror milestones — straight
  `GitHubIntegrationRouter().list_labels_via_mcp()`). #1040 test → 11 passed.
- Removed dead connector code from github_adapter.py: `list_labels_connector`, `_LABELS_TOOL`,
  `_parse_labels` (+ updated 3 stale gap-2 docstrings/comments that listed labels as connector).
- Full required suite GREEN: 49 passed
  (test_repo_scoped_reads_connector_1327 + test_github_repo_scoped_reads_1327 +
   test_handlers_labels_branches_1040 + test_architecture_enforcement).
- Verified branches/releases/review-issue connector methods, parsers, tool constants UNTOUCHED
  (git diff grep → none). py_compile OK on all four code files. fix-newlines run.
- Commit: fix(#1327): revert labels read to native — no github-mcp-server list-labels tool.

## Memory & briefing surfaces referenced this session
- Referenced: CLAUDE.md (path safety, sign-off discipline, TDD); task prompt (defect spec).
- Loaded but not referenced: most of MEMORY.md index.
- Wanted but not found: n/a.
