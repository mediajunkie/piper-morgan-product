# Lead Developer — Session Log 2026-06-29

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Sonnet 4.6
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B) · Sole lead.
**Mode**: PM away/busy until afternoon — authorized autonomous RECONNECT work. **RUN LEAN — usage at 89%** (resets Wed Jul-1 ~9pm); pace the burn, leave headroom for PM's afternoon review. Continues the 6/27–28 RECONNECT arc (6/28 log DAY-CLOSED). Carry-forward: `dev/active/lead-carry-forward.md`.

## Work

- **START 6/29** — autonomous, run-lean. Prior fires (00:06/00:25, logged in the 6/28 log before its close): #1322 sim-retirement inc.2 (federated_search + dormant mcp_consumer) + inc.3a (connect_to_mcp).

- **Fire (~07:50–08:30, autonomous) — #1322 sim-retirement inc.3 COMPLETE (b+c).** github_adapter is now **sim-free**:
  - **inc.3b**: removed the caller-less dead methods `list_issues_via_mcp` + `get_issue_via_mcp` + the `MCPConsumerCore` import/attr + the `mcp_consumer` reference lines in `get_mapping_stats`/`disconnect` (kept those 2 methods). Deleted 2 pure-sim tests (`test_github_adapter_demo_fallback_1088.py`, `test_mcp_consumer_demo.py`). Commit on origin/main.
  - **inc.3c**: the 2 integration tests turned out to **keep** real `list_github_issues_direct` coverage — so I *updated* (not deleted) them: dropped `test_router_methods_delegate_correctly` (mocked the removed method) + the `test_mcp_fallback_integration` script-fn + its `__main__` runner call. Real coverage preserved.
  - **Verified**: 168 unit green; integration collects **886 clean** (the `--collect-only` gate caught the dangling `__main__` caller). Only remaining sim ref = a *comment* in `consumer_core.py` (removed with inc.4).
  - **Paced for usage** (superseded — see next fire): inc.3 was a substantial chunk; wrapping and letting the **next cron fire take inc.4** (the orphaned sim classes — `MCPConsumerCore`/`MCPProtocolClient`/`PiperMCPClient`/`PiperMCPServer` + `start_mcp_server.py` + `test_dual_mode.py`), then inc.5 (m-36 guard). Spreads the burn so there's headroom for PM's afternoon.

- **Fire (~15:23, autonomous) — inc.4 investigated → SCOPE EXPANDED; PACED for usage; shifted to LEAN MAIL-CHECK IDLE.** Mapping `MCPConsumerCore`'s importers showed it's NOT just orphaned classes: still used by `linear_adapter` → `LinearSpatialIntelligence` → the whole **spatial-federation POC** (linear/cicd/gitbook/devenvironment/github spatial). **Confirmed DEAD** (none on `web/`/`main.py`/`services/intent|queries/` paths; `PiperMCPServer` only via the unrun `scripts/start_mcp_server.py`). So inc.4 = a **~10+-file dead-subsystem removal**, not a few classes. At **89% usage**, grinding that low-urgency cleanup is poor economy → **PACED** to usage-reset (Wed Jul-1 ~9pm) or PM direction (full removal set + bottom-up approach scoped in carry-forward). **No quick/clean unblocked RECONNECT work remains** (inc.4 paced; #1327 = PM/CXO-gated) → per PM's guidance, **shifted to lean mail-check idle** (cron continues 3×/day doing cheap mail-checks; won't grind inc.4). Mail clean.

- **Fire (~14:25, PM-engaged afternoon) — RECONNECT sprint-board correction (PA's GitHub-Project mistake) + #1235/#1299 readiness assessment.** PM flagged the board showed **1 Done** in RECONNECT vs the real ~9-10; PA had cleared the **Sprint field** on the closed issues during a board-command mishap (PA+PPM rebuilt but lost the Done tags). **Mined the records** (per PM — don't guess): `dev/2026/06/25/reconnect-sprint-chunking-proposal-2026-06-25.md` line 8 = *"Done (9): #1199/#1226 (WS-1), #1232 (WS-5 contract), #1233 (WS-9), #1227 (Slack mrkdwn), #1291 (ADR-071 D4), #1294 (BYOC bridge), #1308/#1311 (security)"* + #1229 (closed 6/26, after the doc) = **10 Done** — matches PM's "9 then 10." My initial title-search missed #1291/#1294/#1308/#1311 (no "RECONNECT" in title) — corrected by reading the planning doc.
  - **Diagnosis (board query)**: 9 closed Done issues had `Sprint=None`; only #1229 kept its tag → that's why RECONNECT showed 1 Done.
  - **Fix (PM-approved retag)**: batched `updateProjectV2ItemFieldValue` mutation set `Sprint="RECONNECT - Connector Refactor"` on the 9 (#1199,1226,1227,1232,1233,1291,1294,1308,1311) → **Done 1→10**. Added **#1327** to the board (`Sprint=RECONNECT`, `Status=Sprint Backlog` — not-yet-started per PM's rule). Field/option IDs verified before mutating (PM: "be careful").
  - **#1235/#1299** (the 2 "Review for accuracy"): **#1235** = *unfixed* conversation-/turns OLDEST-50 display bug (#1223-family) — needs a **PM fix-approach decision** (newest-first viewer vs paginated-from-start), NOT closeable; **likely mis-scoped into RECONNECT** (body: "no milestone/sprint — for PM triage"). **#1299** = 0.8.8 deploy DONE (live 6/20) but **2 hardening items remain unchecked**: (a) `alembic.ini` env-driven DB URL + (b) `deploy.sh` migrate hardening — buildable (NOT blocked on PM; (a) is literally a RECONNECT "config has no stable home" instance). Neither is close-ready.
  - **2 In Progress** = #1220 (WS-8 umbrella) + #1317 (WS-5 ports) — both legitimately active. **Slack** (#1109/#1110/#1201) goes **LAST in the sprint**, not deferred-after (PM correction).
  - **PM unblocks**: #1327 default-repo = **GUI + conversational** set-default (PM confirmed conversational connect is part of the BYOC vision — verify current state when building). Test project **"Test Piper Morgan"** = the testing default + the **safe write fixture** ("make/close/delete issues all day long") → this also unblocks the **writes cutover**. **Next: proceed to #1327 build.**
  - **Sent PA+PPM memo** (sprint corrections) via mail-send.sh → origin/main (`8a8dbd37c`). mirror in lead/sent.

- **Fire (~15:45, PM UAT) — #1329 filed + fixed: status surfaces now OAuth-connector-aware (UAT Test 1 finding).** PM's UAT Test 1 showed contradictory status: Settings→GitHub showed BOTH "✓ Connected via OAuth (MCP connector)" (my badge) AND a scary "Connection Issue — Invalid/expired token (401)" banner; Integration Health showed "GitHub: unknown / 0 of 4 healthy". **Root cause**: after the #1322 chat-reads cutover, the STATUS/HEALTH surfaces still polled the legacy native PAT (expired) — they were never cut over (the reads + badge were). **Filed #1329.** **Fix (TDD)**: shared `_github_oauth_bound(user_id)` helper (mirrors `GET /github/oauth-status` binding check) → (a) `_check_integration_health` short-circuits GitHub to **healthy** when the connector is BOUND, before the native config/health check (`web/api/routes/integrations.py`); (b) `settings_github.html` `checkGitHubOAuthStatus` now also overrides the **top status card** to "Connected via OAuth" and runs **LAST** in the load flow so it wins over the stale native banner. **3 tests green** (`tests/unit/web/api/routes/test_integrations_oauth_health_1329.py`: bound→healthy / not-bound→fall-through / non-github→unaffected). **NOT yet live** — needs a staging restart to apply; **deferred to a PM UAT break** (restart drops PM's live session). Connector itself unaffected (chat reads work on the current build). #1329 stays OPEN until staging-verified.
  - **Broader-pattern sweep (PM-requested "don't keep hitting it naively")**: dispatched an Explore inventory of ALL GitHub surfaces classified native-PAT vs OAuth-connector. Result = a **clean layered cutover in progress**, no surprise iceberg — the remaining latent surfaces map exactly onto existing roadmap items: **READS** cut over ✓; **WRITES** (close issue `intent_service.py:3723`, create issue `github_issue_command.py`/`github_domain_service.py`) all native = **#1322 Q3 writes cutover**; **Settings repo-config** (`/github/repositories` dropdown + `/github/preferences` load/save) native = **#1327 scope**; **status/test** surfaces native = **#1329** (health + banner fixed; **folded in the `_test_github` "Test" button too** → success-when-bound; **4 tests green**). Also noted: `/github/disconnect` only clears the native PAT, not the OAuth binding (separate disconnect-completeness gap — noted on #1329). Full table posted to #1329.

- **Fire (~16:00, PM-engaged) — staging restarted (PM "restart now") + #1327 kicked off → verify-before-extend WIN.** Restarted staging from the worktree so the #1329 fixes are live (`/health` 200, clean startup, `piper-ghmcp` container up 47h); PM re-testing Test 1. **#1327 kick-off**: investigating before building found **`services/integrations/github/repo_resolver.py::resolve_repo()` (#1042) already implements PM's hierarchy's now-buildable tiers** — explicit → project/default-project/user-default (connector_configs, #1226) → env → `UnresolvedRepoError`. So #1327 is **NOT a from-scratch resolver build** (avoided a duplicate); it's **wire/extend** the existing resolver. Real remaining gaps: (1) **conversational set-default** intent → `ConnectorConfigService.set_default_repo`; (2) **connector repo-scoped reads** (branches/labels/milestones) through `resolve_repo()` + honest-degrade; (3) **repo-config cutover** (`/github/repositories` + `/preferences`, native→connector — #1329 sweep sibling); (4) explain-rules meta (later). Trust-gated tiers 2–3 (infer-suggest / ask) = **M4** (OQ-2, Arch). **#1230 overlaps heavily** (same resolver) → recommend PPM reconcile #1230 into #1327. **Doc-of-record written**: `docs/internal/architecture/current/github-repo-resolution-rules.md` (the #1327-requested durable rules doc + as-built mapping). Recommended next code increment: conversational set-default (lets PM set the test project conversationally).

- **Fire (~16:30, PM UAT) — build #1 (conversational set-default) DELEGATED → reviewed → MERGED.** Delegated #1327 gap-1 to a background general-purpose agent (isolated worktree, TDD). **Reviewed before trusting** (anti-completion-bias): re-ran **179 key tests myself** (the new 16 + pre_classifier theft-risk + action_registry consistency + dispatch + the architecture ratchet) — all green; reviewed the pre_classifier (tightly-scoped regexes requiring the literal "default repo[sitory]", ordered before DOCUMENT_QUERY with documented rationale → no query-theft) + the dispatch wiring (`WorkflowEntry` via `_make_query_dispatch_entry_point` + `action_triggered`, ratchet stays 0). **Merged** (commits 77ddab165 + 727b64f23) → on main; agent worktree/branch removed. **Ships**: "set my default repo to owner/name" (+ change/update/make, "use X as my default repo", "my default repo is X") → pre_classifier → `set_default_repo` action → `_handle_set_default_repo` → `parse_full_name` → `ConnectorConfigService.set_default_repo` (writes the `default_repository` key `resolve_repo` reads). Works whether or not GitHub is connected (preference, not binding). **UAT item (the real gate)**: the deterministic pre_classifier covers the canonical phrasings; **off-pattern phrasings fall to the LLM classifier, which doesn't yet know this action → would miss**. PM tests the phrasings they actually use in staging; broader coverage = a follow-up (LLM-classifier vocabulary). **Needs a staging restart to test the chat command.** **#1327 gap 1 done (pending UAT); next: gap 2 (connector repo-scoped reads) + gap 3 (repo-config cutover).**

- **Fire (~18:00, PM UAT) — #1327 gap 2 (connector repo-scoped reads) DELEGATED → reviewed → MERGED.** Delegated to a bg agent (isolated worktree, TDD, pattern-first). **Reviewed before trusting**: (1) verified **PM's main-checkout integrity FIRST** — the agent flagged its Write tool stray-wrote to main before relocating; confirmed the gap-2 tracked files (connector.py/github_adapter.py/intent_service.py) are **CLEAN in main** + my worktree's 3 untracked files intact → **no data loss**; (2) re-ran **116 tests** post-merge (gap-2 new 30 + legacy-cutover + architecture ratchet) — all green; (3) reviewed the `REPO_UNRESOLVED` reason + the label/assignee **str-or-dict render fix** (a real latent-bug fix — old code was dict-only, would crash on the string shape both paths emit). **Merged** (9e831252b adapter + 142951b10 handler cutover) → on main; agent worktree removed. **Cut over to the connector**: branches (`list_branches`), labels (`list_label`), releases (`list_releases`), review-issue #N (`issue_read` get) — each via `resolve_repo()` → connector → honest-degrade (`REPO_UNRESOLVED` "which repo?" / `CONNECT_REQUIRED`→native-fallback / `UNREACHABLE`). **Milestones STAYS NATIVE** (no milestone tool in github-mcp-server — agent correctly did NOT fake one). **UAT items (the real gate, like #1322)**: (a) connector payload shapes are **parser-assumed, NOT live-verified** (unlike #1322's search_issues, de-risked live at 179) → a live chat test of each read is the key UAT; (b) branches loses the "(default: main)" annotation on the connector path (minor cosmetic); (c) `issue_read` method coupling (if the deployed server predates the consolidated tool → honest UNREACHABLE, not silent). **Needs a staging restart to test.** **⚠️ HARNESS NOTE**: an `isolation:worktree` agent's Write tool can **stray-write to PM's main checkout** (a data-loss vector vs the HARD RULE) — caught + cleaned this time, no damage; future agent launches must emphasize worktree-relative paths, and I verify main integrity post-merge. **#1327 gap 2 done (pending UAT); remaining: gap 3 (repo-config cutover) + milestones-stays-native + explain-rules meta (later).**

- **(~18:05) build #1 LIVE-VERIFIED — PM "bingo".** PM tested **"Piper, please set my default repo to mediajunkie/test-piper-morgan"** in staging chat → Piper: *"Done — your default repo is now mediajunkie/test-piper-morgan. I'll use it whenever you don't name a repo explicitly."* The pre_classifier recognized the (polite, "Piper, please…") phrasing, parsed the repo, persisted to connector_configs, confirmed. **Build #1 = LIVE-VERIFIED ✓.** Then **restarted staging w/ gap 2** (health 200; LISTEN-only kill to protect PM's browser; ~90s slow boot) so PM can now test the **repo-scoped reads** (branches/labels/releases/issue#N) against that default — gap-2 UAT in progress (watch the connector payload shapes — the key gate). Next after gap-2 verify: gap 3.

- **(~18:15) ⚠️ CRITICAL UAT FINDING — Piper CONFABULATED a write-success (filed #1331, HIGH).** PM tested "add a milestone to my default repo" → Piper gathered details + claimed **"Milestone created ✓"** (full title/desc/due/repo). **VERIFIED FAKE**: `mediajunkie/test-piper-morgan` has **0 milestones** (`gh api …/milestones?state=all` → `[]`; repo exists+accessible); `create_milestone` is **recognized by the classifier** (`llm_classifier.py:537`) but has **NO handler** → falls to the floor (LLM) → **confabulates success**. **Trust-property violation** — claims an action that didn't happen (worse than a read gap). **Reframes priority**: the write-confabulation fix (unwired write intents must **honest-degrade**, not floor-confabulate) is the **urgent floor of the writes cutover (#1322 Q3)** — recommend it **OVER gap 3**. **Genuine wins** (PM's "behaving remarkably well" holds for these): set-default LIVE-VERIFIED ✓, conversational recognition + connector reads behave honestly. **Still untested**: gap-2 connector READS (branches/labels/releases — PM tested a write instead) → reads UAT pending. **Self-awareness gap** ("one repo at a time") = the explain-rules meta (#1327 later layer); the **explicit-mention override (tier 1) IS built** + testable via "show X in owner/repo".

- **(~18:40) #1331 confabulation fix DELEGATED → reviewed → MERGED → LIVE (PM verify pending).** Bg agent (isolated worktree): NEW `services/intent_service/unwired_writes.py` (`UNWIRED_WRITE_ACTIONS` = create_milestone/create_release/create_label/create_branch/create_pull_request/update_status + per-action honest decline copy + generic fallback) + `_handle_unwired_write` (honest-degrade, **NO write**, `success=True` graceful) + `workflow_entries` registration on the ADR-059 rail (**intercepts BEFORE the floor**; no elif; ratchet 0). WIRED writes (create_issue/update_issue/close/reopen/comment/todos/generate_report) explicitly EXCLUDED (they error honestly, not confabulate) + a test asserts they're not hijacked. **Reviewed before trust**: main-integrity clean (no stray-write — path caution held), **34 tests re-run green** (incl. the `floor_not_reached_for_unwired_write` crux), code review (decline copy honest + points to GitHub; performs no write). Merged (6411f9ce6) → on main; agent worktree removed. **Restarted staging** (health 200). **PM live-verify pending**: "add a milestone" must now **honest-decline**, not fake "✓". Risk: if the live LLM classifier emits a non-covered action string for that phrasing → one-line add to `UNWIRED_WRITE_ACTIONS`. **Next: gap-2 reads live-verify (branches/labels/releases) + gap 3.**

- **(~19:40) GAP-2 READS LIVE-DE-RISKED (the key UAT — done WITHOUT PM, via a read-only script against the live connector + PM's binding) → found + fixing a real defect.** PM said "work on anything in the sprint" → the highest-value unblocked thing was de-risking the gap-2 connector reads the agent flagged as parser-assumed-not-live-verified. Ran the repo-scoped reads against `mediajunkie/test-piper-morgan` with PM's bound grant (READS ONLY): **branches ✓** (count=1, `main`), **releases ✓** (count=0, clean empty), **review-issue ✓** (`issue_read` get; #100 parses — its content is a malformed JSON blob but the parser handled it). **labels ✗ — BROKEN + UNFIXABLE via connector**: `_LABELS_TOOL="list_label"` is a NONEXISTENT github-mcp-server tool → live `unknown tool` → UNREACHABLE-degrade for connected users. Listed the server's **44 tools** authoritatively: label tools = ONLY `get_label` (one by name) — **no list-labels tool**; **no milestone tool** (confirms milestones-stays-native was correct). So labels must revert to native, exactly like milestones. **Delegated the focused revert to a bg agent** (remove `list_labels_connector`/`_LABELS_TOOL`/`_parse_labels`; revert `_handle_list_labels_query` to native mirroring `_handle_list_milestones_query`; update tests) → **review + merge + restart on completion**. The live de-risk caught the labels bug before PM did — exactly its purpose. (Scratch: `scratchpad/derisk_gap2.py`, `list_tools.py`, `verify_reads_final.py`.) **Next after labels: gap 3.**

- **(~19:50) labels revert MERGED + LIVE; gap 3 DELEGATED.** Reviewed the labels revert (main-clean, 49 tests, diff net-negative = a proper revert), merged (0643adee3) → on main; agent worktree removed; **staging restarted** (health 200) — labels now native like milestones (no connector tool). **Delegated gap 3** (repo-config cutover: `/github/repositories` + `/github/preferences` native→connector — the **GUI half of set-default**) to a bg agent **with the labels lesson baked in** (verify `search_repositories` LIVE before building; STOP-and-report if the connector can't cleanly list a user's repos). → review + merge + restart on completion. **#1327 status**: resolution doc ✓; build #1 (conversational set-default) LIVE-VERIFIED ✓; gap-2 reads (branches/releases/issue) ✓ + labels reverted-to-native ✓; **gap 3 in-flight**. Remaining after gap 3: explain-rules meta (later) + trust-gated infer/ask tiers (M4, OQ-2/Arch). **Stretch summary (PM away)**: this afternoon/evening shipped #1329 (status surfaces), #1331 (write-confabulation honest-degrade), #1327 build #1 + gap 2 + the labels-bug catch+fix — all reviewed + live. Pending PM verify: #1331 ("add a milestone" → honest decline).

- **(~20:05) gap 3 (repo-config cutover) MERGED + LIVE-VERIFIED → #1327 now-buildable scope COMPLETE.** Gap-3 agent DID the live de-risk (labels lesson applied — no assumptions): confirmed `search_repositories` + `user:@me` lists the user's repos. Built `search_user_repositories` (binding-aware honest-degrade) + `GET /github/repositories` connector-first (native PAT fallback ONLY on CONNECT_REQUIRED; honest 502 on UNREACHABLE; never silent) + confirmed `/github/preferences` already OAuth-agnostic (connector_configs). Reviewed (main-clean, 30 tests, scope), merged → on main, restarted (health 200). **Independently live-verified myself** (the labels discipline): `search_user_repositories(PM)` → **18 repos**, clean parse. (First got count=0 → a bug in my OWN verify script — read `items` not the result's `repositories` field; fixed → 18. The discipline worked; the false alarm was my tool, not the code.) **#1327 NOW-BUILDABLE SCOPE DONE**: resolution doc ✓ · build #1 conversational set-default ✓ (live) · gap-2 reads ✓ (branches/releases/issue + labels-native) · gap 3 repo-config ✓ (live). **Remaining #1327 = later layers only**: explain-rules meta + trust-gated infer/ask (M4, OQ-2/Arch). **RECONNECT backlog**: #1330 (disconnect-binding, small) · inc.4 (sim removal, paced) · Slack #1109/#1110/#1201 (last) · #1230 reconcile (PPM) · systemic floor-confabulation deepening (#1331 follow-up, HOST/Arch).

---

## STOP — DAY-CLOSE 2026-06-29 (21:54 PT)
<!-- DAY-CLOSED: 2026-06-29 -->

**Day-arc** — a major RECONNECT day (PM-engaged afternoon → autonomous evening):
- **Board correction**: PA's GitHub-Project Sprint-field clear fixed — RECONNECT Done 1→10 (9 closed retagged + #1327 added); PA+PPM memo sent.
- **#1329** (status/test surfaces OAuth-connector-aware) — built, **PM-verified ("github success"), CLOSED**; #1330 filed (disconnect-binding gap).
- **#1331** (write-confabulation → honest-degrade) — **CRITICAL trust catch**: Piper faked "Milestone created ✓" for an unwired write; verified fake (0 milestones); fixed (honest-degrade rail) + live. PM-verify pending.
- **#1327 NOW-BUILDABLE SCOPE COMPLETE**: resolution doc-of-record · build #1 conversational set-default (**PM-verified "bingo"**) · gap-2 connector reads (branches/releases/issue; labels+milestones reverted-to-native — no server tool) · gap-3 repo-config cutover (18 repos live-verified). Remaining #1327 = later layers (explain-rules meta + M4 trust-gated infer/ask, Arch/OQ-2).
- **Method**: 6 background agents (sweep · build #1 · gap 2 · #1331 · labels-revert · gap 3), each rigorously reviewed (re-ran tests, **main-checkout-integrity verified after each** per the HARD RULE, **live de-risk**). The live-de-risk discipline caught **2 real bugs** unit-green/agent-reports missed: the #1331 confabulation + the gap-2 labels tool (`list_label` doesn't exist). Staging restarted ~5× (current).

**Remaining RECONNECT** (next fire / PM): #1330 (small) · inc.4 (sim removal, paced) · Slack (last) · #1230 reconcile (PPM) · floor-confab deepening (HOST/Arch) · #1327 later layers.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md HARD RULE (never touch PM's main checkout → drove the post-agent-merge main-integrity checks, which confirmed clean each time despite one agent's stray-write); `duty-cycle-tick` skill (STOP dispatch + last-fire rule); `close-issue-properly` (#1329 close); the carry-forward + #1327/#1322/#1329 issues (gap sequencing); the live github-mcp-server tool set (the de-risk source of truth).
- **Loaded but not referenced**: most memory-index entries (comms/PA/editorial-specific) — not load-bearing for a connector-build day.
- **Wanted but not found**: an in-repo authoritative github-mcp-server tool-name/payload reference — had to query the live server's `tools/list` to find the labels-tool truth; a committed connector-tool-surface doc would have pre-empted the gap-2 labels bug. (Worth filing.)

## Sign-off
- `git status`: clean (all work on origin/main) ✓ · `@{u}..HEAD`: empty ✓ · `origin/main..HEAD`: empty ✓
- Cron: armed, 3×/day (`23 9,15,21`); next fire 09:23 tomorrow — STOP leaves it armed (no teardown).
