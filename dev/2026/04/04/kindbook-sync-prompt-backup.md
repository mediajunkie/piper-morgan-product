# Sync Agent Prompt — Kindbook

**Purpose**: Sync kindbook's local piper-morgan repo with origin/main. Faoilean (Christian;s other laptop) has pushed all its work. Your job is to pull that, then push any local-only files that kindbook has but origin doesn't.

---

## Steps

### 1. Save any uncommitted work

    git stash

### 2. Get on main and pull

    git checkout main
    git pull origin main

### 3. Check for stranded branches

Look for local branches with commits not on main:

    for branch in $(git branch --list | grep -v "^\*" | grep -v "main"); do
      commits=$(git log main..$branch --oneline 2>/dev/null | wc -l | tr -d ' ')
      if [ "$commits" -gt "0" ]; then
        echo "$branch: $commits ahead"
        git log main..$branch --oneline
      fi
    done

Report any you find. Do not merge them — just report.

### 4. Find local-only files

Check these locations for files that aren't tracked by git:

    git status docs/public/comms/drafts/
    git status dev/active/
    git status mailboxes/

**Critical file we need**: `draft-are-we-doing-it-backwards.md`

If it's not in `docs/public/comms/drafts/`, also check:

    ls dev/active/*backward* ~/Downloads/*backward* 2>/dev/null

### 5. Stage and commit local-only files

    git add docs/public/comms/drafts/ dev/active/ mailboxes/
    git commit -m "docs: sync kindbook local files to origin — drafts, dev/active, mailboxes

    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
    git push origin main

### 6. Restore stash if needed

    git stash pop

Report what was stashed and whether the pop was clean.

### 7. Report back

List:
- Files pushed to origin
- Branches with stranded commits (if any)
- Whether `draft-are-we-doing-it-backwards.md` was found
- Any conflicts or issues

---

*Generated by Docs agent on faoilean, March 31, 2026*
