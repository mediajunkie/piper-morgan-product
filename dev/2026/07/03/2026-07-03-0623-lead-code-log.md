# Lead Developer — Session Log 2026-07-03

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B). Sole lead. Carry-forward: `dev/active/lead-carry-forward.md`.
**Mode**: PM-initiated morning session (not a duty-cycle fire). Continues directly from 7/2's clean day-close (`#1343` closed properly, `#1344` handed to HOST).

## START 7/3 (~06:23, log created ~06:50 — process gap, noted below)

**⚠️ Process gap**: PM opened with "Good morning" at 6:23 — that's a session start per CLAUDE.md ("ANY first message... create the session log before responding to anything"), and I went straight into the MCP/skunkworks investigation instead. Caught it when PM explicitly asked me to keep the log current. Creating it now, retroactively but completely — nothing was lost, just late.

- **Step-0**: 7/2 DAY-CLOSED ✓ (verified last night before signing off — #1343 closed properly, #1344 handed to HOST, sign-off clean). No self-heal needed.
- **Date correction**: PM opened stating "Friday, July 23rd" — actual date is Friday, **July 3rd** (a same-session digit typo, not a real date confusion — flagged plainly since it's about to matter for date-stamped work).
- **PM's context**: day off from regular work, spending it reconnecting the cohort (bringing agents back into position); **HOST is now up and running** (relevant — HOST owns #1344's invite-code/list-issuance lane, previously the blocker).
- **Sync**: origin/main clean, 0/0 both directions. Lead inbox checked — genuinely empty (verified via disk + MANIFEST + fresh fetch), despite PM saying "you have mail" — noted honestly rather than assumed/fabricated a finding.

## PM's question — MCP bundle (MCPB) vs. the hosted alpha
PM's actual question: once #1344 (registration limits) is resolved and testers are invited in, is the *hosted* `alpha.pipermorgan.ai` also what the MCPB/plugin (Piper Alpha's work) connects to — i.e., are the web-hosted alpha and the MCP-distributed plugin the same backend, or two separate things?

**Investigated rather than answered from memory** (I had a vague recollection from yesterday's runbook read, wanted to verify current, not stale):
- No MCPB source, manifest, or `alpha.pipermorgan.ai` reference anywhere in `piper-morgan-product` (this repo) — checked via targeted `find`/`grep`, confirmed genuinely absent, not just unsearched.
- The `piper-morgan:ask-piper` / `consult-piper` skills (which relay through the plugin's own `ask_piper` MCP tool) explicitly document requiring "the local Piper Morgan server running (`python main.py`, port 8001)" — direct, first-hand evidence the plugin is built for local-only operation today.
- `alpha-deployment-runbook.md` names the *actual* hosted-MCP effort as a separate, distinct thing: **#1278** ("host piper-morgan server on Fly.io for beta launch," `server.pipermorgan.ai`, a different auth pattern). Checked its live GitHub state: **still OPEN, not built.** Its stated preconditions (Caddy gate removal + #1162 BYOC cred-decoupling) are BOTH now done (this week, largely my own work) — so it's unblocked but not started.
- **Answer given to PM**: today, the MCPB/plugin and the hosted alpha are two disconnected paths. Unlocking #1344 only affects the browser/web path. Flagged the one real gap in my own investigation honestly: I couldn't find the MCPB's actual source, likely because it lives in `piper-morgan-skunkworks` (a location I don't have checked out here) — so this is strong circumstantial evidence, not a first-hand code read.

## PM directive: full MCPB briefing from PA
PM: "send Piper Alpha a memo asking for a full briefing on the MCPB. Its source may be only in piper-morgan-skunkworks so far." Sent (`dca196d2b`, to pa cc xian (ceo)) — laid out exactly what I'd already found + verified (so PA isn't re-deriving it) and asked 5 concrete questions: where the source actually lives, current version/distribution state, the *actual* (not inferred) connection architecture, the relationship between the existing MCPB and #1278, and anything in this repo the MCPB depends on that I should know I'm the steward of. No urgency stated; awaiting PA's reply.

## Memory & briefing surfaces referenced this session (so far)
- **Referenced**: `alpha-deployment-runbook.md` (re-consulted for the #1278/hosted-MCP gating language — this is the second time today I've relied on having read it fully yesterday); the `piper-morgan:ask-piper`/`consult-piper`/`meet-piper` skill descriptions (direct evidence source, not previously used this way); `dev/active/lead-carry-forward.md` (confirmed 7/2's close state before starting).
- **Wanted but not found**: the actual MCPB bundle source/manifest — confirmed absent from this repo, presumably in `piper-morgan-skunkworks` (unverified, now the explicit subject of the PA memo).

## Fire (~09:23) — drained 6-memo mailbox: #1344 contract, #1235 board fix, #1322 gate recorded, 2 ACKs

**Sync first**: 5 commits behind origin/main (HOST + Docs had been active) — pulled clean after the usual CRLF-only false-positive on the klatch CSV (`--ignore-all-space` confirmed 0 real lines, as documented in the runbook). Read all 6 memos in the inbox in full before acting on any of them (investigate-before-extending applies to mail same as code).

**#1344 — replied to HOST with the token format + validation contract** (`mailboxes/host/inbox/…1344-token-format-validation-contract…`, commit `9080f6b68`). Landed on Crockford Base32, 24 chars (avoids visually-ambiguous characters, normalizes to uppercase for the case-insensitive requirement HOST wanted). The substantive part: HOST's own question offered a "static published set" option for validation, but **Arch's atomicity requirement (7/3 09:55 memo) rules that out** — a static set can't atomically mark-used across concurrent requests, which is exactly the double-spend TOCTOU race Arch flagged. Proposed instead: a Lead-owned `invite_tokens` DB table, single conditional `UPDATE … WHERE used_at IS NULL RETURNING token` as the atomic primitive (verified against the actual `create_user` handler at `web/api/routes/setup.py:809` — it already uses `AsyncSessionFactory.session_scope_fresh()`, so the token-burn can co-locate in the *same* transaction as the user INSERT, avoiding a second smaller TOCTOU between a separate validate-and-consume call and account creation). Checked first that no invite/token table already exists (grepped `services/domain/`, `services/infrastructure/`, `web/`, `alembic/versions/` — genuinely nothing to extend, confirmed new work not a duplicate). Timing: confirmed unambiguously — #1343 Gap-B is not just "in prod," it's resolved/deployed/live/closed since 7/2 evening (v0.8.9.1) — no blocking dependency, told HOST to proceed.

**#1333/#1231 copy — flagged to HOST that both already shipped**, not pending as both HOST's and Arch's memos assumed ("once Lead drafts," "before they ship"). Separate memo with exact file/commit pointers (`unwired_writes.py` @ `a5ddf3be4`, `degradation_copy.py` @ `d2aa4fbe0` — the latter already carries an inline "CXO voice pass 2026-07-01" credit). Self-checked the shipped copy against HOST's own 3 D5 trust properties before sending (honest-gap distinction, actionable-next-step, once-per-response) rather than just pointing and leaving it to them cold — verified the "once-per-response" property at the actual call site (`canonical_handlers.py:1414`, docstring states the contract explicitly, 3 call sites all append-once-to-message, none in a per-item loop). One property (`RESOURCE_NOT_FOUND` has no explicit next-step) flagged as a maybe-gap, explicitly not pre-judged — HOST's call.

**PPM's #1322 write-gate — recorded durably, since PPM's own stated action hadn't landed.** PPM's #1331 alpha-trust-lens memo said "I'm flagging #1322 as dependent on the floor guard (code-level) in tracking" but `gh issue view 1322` showed nothing from PPM as of this morning — the ruling existed only in the memo. Since I'm the one who'll eventually build #1322 Q3 and need to see this gate, wrote it down myself rather than wait: [GH comment](https://github.com/mediajunkie/piper-morgan-product/issues/1322#issuecomment-4878205511) quoting PPM's ruling verbatim + a `decisions.log` entry (commit `a8dc1cbd1`). Gate: no user-facing write actions on #1322 until a deterministic code-level guard verifies the action executed before Piper claims success — distinct from and stricter than #1331's general alpha yellow-flag (which is contingent on PM's re-test and doesn't block M3).

**#1235 — Sprint field moved RECONNECT → M3-Quality per PPM's request.** Verified via GraphQL first (`gh api graphql`) that the `M3-Quality` option existed on the Sprint field before mutating (`ba398391`), found #1235's project item id, applied `updateProjectV2ItemFieldValue`, verified the read-back showed `M3-Quality`. Issue itself is closed (unrelated to the Sprint-field metadata fix — a board-hygiene move, not a reopen).

**2 lightweight ACKs sent**: HOST's #1331 ratification (no changes needed, registered the third-failure-class watch-item) + PPM's #1331 lens memo (told them directly the #1322 gate was recorded, with the link, so it's not a silent fait accompli) — combined into one memo (`to: host, ppm`) rather than fragmenting into two near-identical sends.

**Mail**: all 6 inbox memos triaged to `read/` (one `mail-send.sh` batch, 6 renames, 0 content diff — verified via `git show --stat`). 3 outbound memos sent this fire, all verified via `git show --stat` on the actual pushed commit (not just trusting the script's success text) — commits `9080f6b68`, `d375459be`, `e5e684214`. `decisions.log` entry pushed separately as `a8dc1cbd1` (plain commit, not mail — architecture record, not agent signaling). Inbox confirmed empty (MANIFEST only) after a pull to resync local view with the 4 mail-send pushes.

**No blockers, nothing pending from me** — HOST has what they need to proceed to #1344 step 2 (I draft `create_user`'s invite-gate) whenever the token/contract reply lands for them; that draft is my own next concrete build item, not yet started this fire.

## PM conversation (~10:40–11:46) — drift check, #1235 correction, briefing refresh

PM asked directly whether the drift I'd just caught (HOST/Arch assuming shipped copy was pending; PPM's #1322 flag not landing anywhere) meant `BRIEFING-CURRENT-STATE.md` needed a refresh too — **and separately caught that I'd moved #1235 (a CLOSED issue) into M3-Quality (a not-yet-started sprint) without confirming with PM first**, given PA's prior sprint-board damage and PM's standing lack of trust in agent sprint-judgment.

- **Briefing refresh**: checked `docs/briefing/BRIEFING-CURRENT-STATE.md` directly rather than assume — not clock-stale (edited 7/2), but content-stale in exactly the PM-relevant ways (still said #1343 "NOT YET DEPLOYED" and #1344 "PM decision pending," both now resolved). Ran the `update-current-state` skill: refreshed STATUS BANNER, Version (v0.8.9.1 hotfix vs. main's unbumped v0.8.9), Current Focus, added a Recent Progress July 2–3 entry, updated frontmatter + footer dates. Commit `93373ac0f`.
- **#1235 corrected**: reverted the Sprint field back to RECONNECT (its pre-edit state) via GraphQL, verified via read-back. Sent PPM a heads-up that the move was held, not done. Saved a feedback memory (`feedback_sprint_field_changes_require_pm_confirmation.md`): sprint/board field changes are PM-gated by default going forward, regardless of requesting agent.
- **PM's follow-up nuance** (important correction to my own framing): closed-issue-into-not-started-sprint isn't *always* wrong — a legitimate cherry-pick/early-completion pattern exists (item pulled forward from a future sprint's backlog, completed ahead of that sprint's formal start; the tag then correctly records *where it belongs*, not *when it was worked*). PM's actual point was narrower: the mechanical state alone doesn't tell you which case you're in, and PM couldn't tell PPM's intent from the memo. **Revised the memory file to reflect this precisely** — the lesson is "ambiguous, ask" not "always incoherent, revert."
- **Asked PPM directly** which they'd meant (cherry-pick vs. topical-only), per PM's "yes please." **PPM answered**: topical-only (Option 2) — confirmed my inference was right, called the revert correct, and escalated final disposition (clear field / leave in RECONNECT / move with PM sign-off) directly to PM with a stated lean (Option A, clear the field). Nothing further from me here — properly PM-gated now, PPM did the escalation themselves.
- Proposed (did not unilaterally add) a `CLAUDE.md` rule codifying "Sprint/Milestone field changes require PM confirmation" cohort-wide, since PM named this as an operating-model gap, not just a personal lesson — awaiting PM's call on whether/how to word it.

## Fire (15:53, KEEP-tier cron 23 9,15,21) — #1231 completeness guard shipped, #1235 resolution mail triaged

Cron check: exactly 1 registered for the expression, no self-heal needed. Sync clean (0/0). Today's session log existed, prior day (7/2) closed properly (`DAY-CLOSED` confirmed) → dispatched to WORK PARTS, not START.

**3 new memos, all processed**:
- **PPM's #1235 escalation to PM** (cc me): confirmed topical-only intent, PM decision now pending among 3 options (PPM leans "clear the field"). No action from me — properly escalated, correctly gated.
- **Arch's `_NUDGES` completeness-guard request** (following HOST's watch-item from the earlier trust-lens pass) + **HOST's timing concur**: both proposed landing a `DegradationReason`-completeness test alongside the `NOT_CONFIGURED` enum add. **Verified before building**: `NOT_CONFIGURED` already has its `_NUDGES` entry, shipped 2026-07-01 — so nothing to "land together"; the guard is retroactive coverage, not a same-commit companion. Built it anyway (small, well-specified, genuinely unblocked): `test_every_degradation_reason_has_nudge_copy` added to `tests/unit/services/intent_service/test_priority_honest_degrade_1231.py`. 11/11 file tests green, 1772/1772 intent_service suite green, no regressions. Commit `7b0491f98`. Replied to Arch/HOST with the timing correction (commit `b17441b10`) — same "verify before assuming shipped-vs-pending" discipline as this morning's #1333/#1231 catch.

All 3 memos triaged to `read/`. Checked `dev/active/lead-standing-items.md` for Task Loop pickup — it's dated 2026-06-21 and describes RECONNECT as still in-flight (stale; the doc itself says carry-forward wins when they disagree, and carry-forward is unambiguous that RECONNECT's buildable queue is fully drained). Not treating it as live work; flagging the staleness rather than acting on outdated premises. No further unblocked work found — quiet-hold is honest.

Cron: exactly 1 registered, left armed (not mid-substantial-build). Next fire 21:23 today (same calendar date) — not a STOP dispatch.

## PM conversation (~16:00) — #1235 pagination question → #1347 filed

PM asked what "the pagination question" on #1235 actually was. Explained it from the issue body (oldest-50-vs-newest-50 fix options) — **then caught my own mistake**: that description was the ORIGINAL (6/14) bug text, not current code. Checked before going further and found **the newest-first half was already fixed** 2026-06-30 (`903d6db13`, explicitly `fix(#1235): conversation /turns restore returns newest window, not oldest 50` — `get_conversation_turns(..., most_recent=True)`, reusing #1223's machinery). That commit's own comment had already deferred exactly what PM was about to ask for: *"If a 'scroll back to older turns' viewer is needed later, add offset pagination then."* Corrected the record with PM directly rather than let the stale description stand — same discipline as this morning's #1333/#1231 catch, this time on my own words a few messages earlier in the same conversation.

PM's actual ask, once the record was straight: **full offset-based backward pagination** (not just the newest-first default) + **a failsafe against silently losing transcript** if the paginated UI hits some practical ceiling, with newest-first remaining the default scroll-back-from-here direction. Verified before scoping: no retention/TTL/archival exists on `conversation_turns` today (turns stored indefinitely in Postgres — the failsafe is forward-looking, not a fix for a live risk); no frontend scroll-back UI exists; no duplicate issue exists (searched).

**Filed #1347** (`https://github.com/mediajunkie/piper-morgan-product/issues/1347`) per PM's explicit "file the new feature" — full body covers requirements, current-state evidence, a fix-shape sketch (not a committed design), and refs to #1235/#1223/#565. **Deliberately left Sprint/Milestone unset**: PM's own timing was "M5 polish pass or even maybe... post-0.9 beta" — genuine ambiguity between two windows, PM's words not mine to resolve; given this morning's sprint-field lesson, didn't pick one myself. Labeled `enhancement, component: ui, component: api, priority: low` — skipped a size label (the failsafe design is undefined, sizing it would be a guess). Cross-linked from #1235 with a comment pointing to the follow-on.

## #1344 build — invite-gate implemented, tested, on main (~19:00–19:30)

PM: "proceed in the way you see fit. I trust you to keep track of the open issues" — both HOST and Arch had confirmed the token/validation contract this fire already, so this was the standing next-build item, not a fresh decision.

**Investigated before building** (per discipline): read the actual `AUTH_EXEMPT_JUSTIFIED`/`DEFAULT_EXCLUDE_PATHS` mechanism (`auth_middleware.py`) to understand precisely what "the route loses exempt-writable status" (Arch's phrase) means mechanically — concluded it can't mean removing JWT-exemption (a not-yet-existing user can't hold a JWT; chicken-and-egg), so it means: keep the route JWT-exempt (structurally necessary), but stop relying on the vague blanket `/api/v1/setup/` "pre-account-creation" justification for THIS route specifically, replacing it with a specific entry naming the real gate. Checked the migrations directory + a recent migration (`b1229bindings`) for the exact revision-chain/style convention before writing a new one. Checked for any pre-existing invite/token infrastructure (none). Checked the existing `update().where().values().returning()` idiom already used in `todo_repository.py`/`universal_list_repository.py` before choosing the atomic-UPDATE mechanism, rather than inventing a new pattern.

**Built** (6 tasks, all TDD): `invite_tokens` migration + `InviteToken` model; `services/auth/invite_token_service.py` (generation/normalization/atomic consume); wired `invite_token` into `CreateUserRequest` + the `create_user` handler (co-located in the existing `session_scope_fresh()` transaction — empirically confirmed via a failing test that `session.execute(update(...))` autoflushes the pending user INSERT first, so the FK write is valid within the same uncommitted transaction, matching the atomicity contract exactly); added the specific `AUTH_EXEMPT_JUSTIFIED` entry (kept the blanket prefix entry — 5 other setup-wizard write routes still need it); `scripts/mint_invite_tokens.py` for HOST.

**Caught and fixed my own exception-handling bug before it shipped**: the outer `except Exception as e:` in `create_user` would have caught and remapped my `HTTPException(400, "invalid token")` into a generic 500 — added `except HTTPException: raise` as the first, more-specific clause.

**Caught a real pre-existing bug via my own test-writing** (not this issue's scope): `CreateUserRequest.email` is `Optional` but `User.email` is DB `NOT NULL` — filed #1348 rather than silently work around it in the actual fix code (worked around it only in my *test* fixtures, which is legitimate).

**Full regression discipline**: ran the complete `tests/unit/` suite (8152 tests) rather than just the new/adjacent tests, given the security-sensitive scope and the shared `services/database/models.py` file touched. Found 4 failures across 2 unrelated causes; verified EACH is pre-existing by `git stash`-ing my changes and reproducing identically before filing (#1349: a `services/place` GitHub-naming assertion; #1350: 3 `settings_projects.html` template tests, likely stale against an `app_shell` migration) — didn't just assume unrelated-sounding test names meant unrelated causes.

**The atomicity proof is empirical, not just asserted**: `test_concurrent_registrations_cannot_both_consume_the_same_token` fires two real `create_user` calls via `asyncio.gather` at the actual Postgres DB with the identical token, asserting exactly one succeeds — the literal double-spend scenario Arch's requirement exists to prevent, not a mocked approximation.

**Recorded**: `decisions.log` entry (~19:20 PT); GH evidence comment on #1344 (issue left OPEN — not deployed to production/alpha yet, so the live vulnerability isn't actually closed regardless of what's true on `main`, matching the #1343 precedent of not closing until deploy+live-verify); memo to HOST+Arch+PM with the same evidence. Commit `04c9f7601` (9 files, 479 insertions), verified on `origin/main`.

**Not done**: deployment. This is a separate, later action per the release-cut model established this week — flagged to HOST/Arch as their trigger to request when ready.

## #1344 DEPLOYED — v0.8.9.2, PM-directed, live-verified (~19:30–20:15)

PM: "let's deploy it as a dot release since it makes production more secure and unblocks alpha testing." Arch had already ratified the code implementation clean, no reservations (read after the fact — arrived while I was mid-deploy).

**Cherry-pick, adapted for production's real state (not assumed from main's diff)**: created `release/v0.8.9.2-1344-invite-gate` off `origin/production`. Checked production's actual DB state first via SSH — `alembic_version` = `000baa96d800` (found the hard way: `docker compose exec app alembic current` failed with a connection-refused error I initially couldn't explain; querying postgres directly, `docker exec piper-postgres psql`, gave the authoritative answer — this turned out to be the exact same known `alembic.ini` hardcoded-localhost bug the runbook documents, not a new mystery). Cherry-picking `04c9f7601` hit 2 real conflicts: `decisions.log` (kept production's version — that file's canonical home is `main`, no need to merge history) and `services/database/models.py` (my diff would have also introduced the unrelated `ConnectorBinding` class, #1229 WS-2, which was never released to production — excluded it, kept only `InviteToken`). Repointed the new migration's `down_revision` from `b1229bindings` (main's chain) to `000baa96d800` (production's actual head), with a comment explaining the divergence for a future reader. Version bumped to 0.8.9.2, manually verified VERSION==pyproject.toml (the `check-version-consistency.py` script itself isn't on production's branch — didn't cherry-pick unrelated tooling just to use it once). Full test suite run against the release branch directly, not just assumed from main's earlier pass: found 1 more pre-existing-and-unrelated failure specific to production's older snapshot (`test_keychain_scoping_849.py`, an unmocked live GitHub-API call) — verified via a temporary separate worktree against a completely clean `origin/production` checkout before concluding it was unrelated, not just assumed from the test name.

**Two real deploy-time catches, both resolved before any user-facing impact**:
1. **The `PIPER_HOST` fix never made it to production.** Checked (out of habit, given last week's incident) whether the fix was still present after `git archive` transferred the new code — it wasn't. Production's committed `docker-compose.yml` never had it; only `main` did. This `git archive` was about to silently reproduce the EXACT SAME 502 outage from last Tuesday, a second time, on the same droplet. Caught it before running `deploy.sh` or restarting any container — backported the identical fix + comment from `main` verbatim, committed, re-transferred, verified present, then proceeded.
2. **A stray untracked migration file** (`b1229bindings_create_connector_bindings_table.py`) was sitting on the droplet's filesystem — not part of production's git history at all, timestamp matching my own #1343 deploy session from Tuesday evening (near-certainly a leftover I didn't notice at the time). Caused an alembic `MultipleHeads` error on the first migrate attempt. Verified via direct DB query that its migration had never actually run (no `connector_bindings` table, `alembic_version` unaffected) before deleting the stray file — did not assume, checked.

**Followed the documented runbook's migrate workaround** (`_run_migrate.py` overriding the real engine URL) rather than trusting `deploy.sh`'s own broken automatic migrate step — `deploy.sh` wrote `BUILD_FAIL` exactly as the runbook predicts for this known bug; `docker compose ps` confirmed the app itself was healthy throughout, consistent with the documented "not a real failure" pattern.

**Live-verified against the real API, not just the code**: `POST /api/v1/setup/create-user` with no token → 422; with an invalid token → 400 "Invalid or already-used invite token"; confirmed zero orphaned accounts from either attempt via a direct query against production's database. Also caught myself nearly mis-reading a `401` on `/` as a possible gate-related problem — checked the actual response headers/body before reacting, confirmed it's an unrelated, normal app-level "not logged in" response (`WWW-Authenticate: Bearer`), nothing to do with Caddy or this change.

**Closed out**: tagged `v0.8.9.2`, GitHub Release published, release notes written (mirroring v0.8.9.1's format), `versioning.md`/`VERSION_NUMBERING.md` updated on BOTH `production` and `main` (main stays unbumped at 0.8.9, same pattern as last time). `decisions.log` entry recording the full deploy narrative including both catches. **#1344 closed with full evidence** — code ratification (Arch) + actual live deploy + live API verification, not just "the code merged." Notified HOST/Arch that it's genuinely live now (my earlier "not yet deployed" memo from this afternoon is now superseded) and flagged the practical question of where the mint script needs to run to issue tokens against the real production DB, rather than asserting a mechanism I hadn't verified.

## Memory & briefing surfaces referenced this session (running)
- **Referenced**: `BRIEFING-CURRENT-STATE.md` (read fully + refreshed, first time this session going beyond a citation); `update-current-state` skill (first invocation this session); `duty-cycle-tick` skill (this fire's dispatch); `decisions.log` (read tail for format + appended, then appended to again for #1344); memory `feedback_sprint_membership_is_project_board_not_labels.md` (checked before writing a new, distinct memory) + the new `feedback_sprint_field_changes_require_pm_confirmation.md` (written, then revised mid-session per PM's correction); `services/repositories/todo_repository.py` + `universal_list_repository.py` (checked for the atomic-UPDATE idiom before inventing one).
- **Loaded but not referenced**: `lead-standing-items.md` (checked, found stale, did not act on it).
- **Wanted but not found**: nothing this section.
