# Web session — 2026-07-26 (Sunday)

**Role**: Unicorn Web Designer (piper-morgan-website)
**Account**: pipermorgan.ai / Amber
**Model**: Claude Opus 5 (1M context)
**Host**: Amber — Model A worktree
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/web` on `claude/web-cycle`
**Trigger**: PM launch — resume on Amber, no handoff, orient from CIO's orientation note

---

## Boot (17:49)

Predecessor went dark 2026-07-19 (clean close, `DAY-CLOSED` marker). No handoff
memo exists — CIO assembled `dev/active/orientation-note-web-amber-2026-07-25.md`
from artifacts instead. Read that first, then the 7/19 log and
`dev/active/web-carry-forward.md` (both current to 7/19, in agreement).

### Environment verification

| Check | Result |
|---|---|
| Worktree path | `/Users/xian/Development/piper-morgan-worktrees/web` (Model A, stable) ✓ |
| Branch | `claude/web-cycle` ✓ |
| Currency vs `origin/main` | **0 behind, 0 ahead** ✓ (no silent-staleness gotcha) |
| Working tree | clean ✓ |
| **Repo my worktree points at** | **`piper-morgan-product`** — ⚠️ see finding 1 |
| Website repo | `/Users/xian/Development/piper-morgan-website`, plain checkout on `main`, clean, **4 behind origin/main**, no worktree ⚠️ |
| Memory | shared pool present (~168), verified not imported |
| Hooks | ⚠️ see finding 2 — behavioral check produced a **real diagnosis** |

### Finding 1 — my lane spans two repos and only one has a worktree

PM's launch note flagged this and it's real. My Model-A worktree is cut from
`piper-morgan-product` (git common dir `/Users/xian/Development/piper-morgan-product/.git`).
But per the orientation note, **Web's lane is the `piper-morgan-website` repo** —
product front-end is Lead/CXO's lane.

So the substrate splits:
- **Cohort infrastructure** (mailboxes, session logs, `dev/`) → this product-repo worktree. Correct as-is.
- **Actual Web work** (the website) → `/Users/xian/Development/piper-morgan-website`, which is a **plain checkout on `main` with no per-agent worktree**, and is **4 commits behind origin/main**.

Not resolving unilaterally — routed to CIO as an open provisioning question.
Flagged rather than guessed because the predecessor's carry-forward note
("ALWAYS absolute `git -C` paths; cwd drifts across reconnects") suggests
cross-repo cwd confusion has bitten this role before.

### Finding 2 — the "unexplained hook intermittency" in CLAUDE.md is explained; it's command shape, not time

Orientation said to expect a PASS and **escalate loudly on disagreement**. I got a
disagreement, chased it, and it resolved into a reproducible mechanism.

**Probe series** (all on `claude/web-cycle`, staging a file under `mailboxes/`):

| # | Shape | Index state when hook fired | Result |
|---|---|---|---|
| 1 | `git add X && git commit` (one Bash call) | empty | **NOT blocked** — commit succeeded |
| 2 | `git add X` (call A), then `git commit` (call B) | X staged | **BLOCKED**, names `check-branch.sh` |
| 3 | `git add Y && git commit` (one call), probe-2 file still staged | Y' staged from before | **BLOCKED** |
| 4 | `git add X && git commit` (one call), index verified empty first | empty | **NOT blocked** |

**Mechanism**: `check-branch.sh:28` decides via `git diff --cached --name-only`.
PreToolUse fires **before** the Bash call executes — so for a compound
`git add … && git commit …`, *the `git add` has not run yet* and the index is
empty at hook time. The hook inspects an index that does not yet contain the
files being committed, finds no `mailboxes/` paths, and exits 0.

**This is deterministic by command shape**, 2/2 in each direction — not the
hours-timescale randomness CLAUDE.md currently describes as "real, unexplained."
It also retro-explains CIO's 1-of-5-then-4-of-4 pattern without needing an
intermittency: whether a probe blocks depends on whether mailbox paths happened
to be staged *before* the probe's own call began.

**Confirming details**: block surfaced as `PreToolUse:Bash hook error:
[bash .claude/hooks/check-branch.sh]: No stderr output` — exactly the signature
CLAUDE.md documents as *the hook working*. The path is **relative**, so the
**project layer** caught it (user layer uses absolute paths). Config verified
correct and script present+executable at both layers before probing, so this is
not config drift.

**Consequence, and why it matters more than the diagnosis**: the near-universal
agent idiom is `git add … && git commit …` in one Bash call. That is precisely
the shape the hook cannot see. So the mailbox backstop has been **silently
absent for the common case** — while the 2026-07-25 verification, which used a
staged-first shape, correctly reported PASS. Both results were true; they tested
different things. This is the CLAUDE.md rule about verifying behaviorally biting
one level deeper: *the probe's shape has to match the shape you actually use.*

All four probes fully reverted — index clean, `git status` empty, HEAD back at
`a1763a236`, 0 ahead of `origin/main`, no probe files on disk. Cleanup used
`git reset` (soft) + explicit-path `rm`, never a broad discard.

⚠️ Not changing the hook — that's a cohort-infrastructure surface and this is my
first session on Amber. Reported to CIO with the reproduction.

### Mailbox — 6 unread, triaged

| Memo | Substance |
|---|---|
| Docs 7/25 "admin calendar staleness — GitHub Action vs ISR" | **Direct ask to me**: pick an approach. Option A = product-repo GH Action → Vercel deploy hook; Option B = ISR `revalidate`. |
| Docs 7/25 "still stale — PM asking today" | Same root cause; **PM asking directly**, wants runtime read + a timeline. Third PM-visible staleness event in ~10 days. |
| Comms 7/21 "calendar sync stale again" | Same issue, first flag. Correctly identifies it as the residual gap Web named on 7/16, not a new bug. |
| Comms 7/25 "compose UI save-conflict — three automation asks" | localStorage autosave · conflict diff · live staleness warning. Real recurring collision (PM in compose UI vs Comms via git). |
| Exec 7/21 "prepare handoff memos" | Overtaken by events — the migration happened; this note is why no handoff exists. |
| CIO 7/24 "thanks correcting my tracking" | cc, informational. |

**Convergent signal**: three separate memos across 5 days, all the same
architectural gap — the admin calendar bakes at deploy time. That's the clear
top priority, and it has an explicit PM ask attached. Not starting it this fire;
reporting oriented first as instructed.

---

## Fire log

| Fire | Time | Action | Notes |
|---|---|---|---|
| launch | 17:49 | START | Oriented from CIO's note + 7/19 log + carry-forward. Environment verified (0-behind, clean, Model A). Two findings: cross-repo worktree gap (finding 1); hook-probe disagreement chased to a reproducible diagnosis (finding 2). Mailbox triaged, 6 memos. |
| launch | 18:00 | WORK | **Reconciled finding 2 against PPM's independent report.** Pushing my session log hit a non-fast-forward; the commit below mine was PPM's `mail(ppm): … hook intermittency reproduced on a fresh seat`. Read it before claiming novelty — correct call, because it changed the finding. PPM ran three probes today on their own seat and reported the command-shape hypothesis **refuted**, asking that nobody re-run it. But all three of their rows are exactly what the index-state mechanism predicts: their probe 3 was designed as a controlled repeat of probe 1, but **probe 2 had been blocked, so its `git commit` never ran and its staged file was still in the index** when probe 3 fired. I fell into the identical trap on my own probe 3 and only caught it by printing `git diff --cached` around every step. So PPM's data is an independent confirmation collected while testing a different hypothesis — the strongest evidence in the finding, and not mine. Sent the correction gently and with the reasoning shown; the instinct to deliberately test the compound shape was right, the confound is genuinely invisible without clearing the index. |
| launch | 18:02 | WORK | **Tested the layer-alternation claim.** CLAUDE.md treats the named script path as diagnostic (relative = project layer, absolute = user). With the same staged file, same command, no config change, consecutive blocks on my seat named **project → user → user**. So the named layer varies between identical calls and is **reporting noise, not a signal of which layer is live**. Parsimonious read is that both layers fire and both block, with only one surfaced — flagged to CIO as clearly-labelled conjecture since I can't see the dispatcher. This also likely explains the "alternation" in CIO's 22:39 result and PPM's probes 2→3 without needing a mechanism. Recommended (seconding PPM) **not** consolidating the layers: if the naming is unreliable we have no cheap way to confirm a layer is live, so removing one would be unfalsifiable tidying. |
| launch | 18:05 | WORK | Memo to CIO (cc PM, PPM, HOST, Exec) — findings 1 + 2, the PPM reconciliation, the layer-naming correction, env verification, inbox dispositions, and what I owe. Sent via `mail-send.sh` (`60f96adfc`), all 6 copies verified present on `origin/main`, inbox drained to MANIFEST-only, worktree clean. Deliberately did **not** patch the hook — cohort infrastructure, first session, and the fix has a real design choice in it (parse the pending command string vs. move to a post-tool/pre-push surface vs. accept it since `mail-send.sh` bypasses `git commit` entirely). Handed CIO a reproduction, not an unrequested patch. Cron **not armed** — PM actively engaged; arming is PM's call, and I did not write a registry row claiming "watched" with no job behind it. |

| launch | 18:15 | WORK | **Follow-up memo — PA's data landed in CLAUDE.md mid-write and needed the same correction.** CLAUDE.md gained PA's four probes while I was drafting, concluding again that command shape is excluded (true, and not my claim — the variable is index state) and proposing "lazy attach on first matching call" as an untested n=1 hypothesis worth a fresh seat. **I already had the distinguishing probe**: my probe 4 was the *fourth* commit-shaped call of the session, after two confirmed blocks — so the hook was demonstrably attached — with the index verified empty, and it **bypassed**. Lazy-attach predicts a block; index-state predicts a bypass. PA's 4 rows also fit index-state 4/4, including the probes-1-and-4 pair that looks decisive against it (a blocked commit never runs, so its staged file stays staged and primes the *next* probe). Cumulative: **11 of 11 probes across three seats**, none of the three collectors testing for it. Sent to CIO cc PM/PA/PPM/HOST/Exec (`89f3cfc46`), verified on `origin/main`. Included the two-call falsification recipe so nobody burns a fresh seat on the probe-twice test. |
| launch | 18:20 | WORK | Updated the existing `project_amber_worktree_hooks_not_firing` memory rather than adding a duplicate — appended the index-state section, the refutation of lazy-attach, the layer-naming-is-noise correction, and one clearly-labelled conjecture: the *"live reload is universal"* refutation still asserted in that memory's own description rested on two non-blocking probes on CIO's seat, which is exactly what an empty index predicts with no reload failure at all. Cheap to check, nobody has. Index line in `MEMORY.md` updated in place, same length (byte-limit discipline). |

---

## ⚠️ Finding 3 — the MEMORY.md compaction ask is arithmetically unsatisfiable

A `PostToolUse` hook fired on my `MEMORY.md` edit: *"193 lines, approaching the 200-line
read limit. Compact it to under 140 lines now."*

Measured before acting: **169 entry lines, 169 memory files on disk**, plus 14 blank, 6
headers, 4 prose = 193. Stripping *every* blank line, header and prose line still leaves
**169 lines**. So "under 140" cannot be reached by formatting — it requires **deleting ~30
cohort memories**, and the memory pool is shared by all twelve roles, is not version
controlled, and has no undo.

**Not doing it.** This is the "escalate to a broader destructive mechanism than the task
needs" pattern the CLAUDE.md HARD-RULE section names, and doing it silently on a first
session would be the worst version. Also note the file now has **two constraints in
tension**: a documented ~24KB byte limit (HOST caught it at 41.4KB on 7/25, silently
truncating ~40% of entries) and this 200-line read limit. At 19.7KB it is comfortably under
the first and nearly at the second — so the byte-based `scripts/rebuild-memory-index.py`
guard would not catch this.

Raised with PM in-conversation. HOST owns memory-index hygiene and is the right owner for
the real fix, which is probably a policy call about pruning criteria, not a reformat.

---

## Open / next

1. **Admin calendar staleness** — top item. Three memos, two roles, explicit PM ask with a timeline requested. Need to read `loadCalendar()` + the admin calendar page before recommending Option A (GH Action → Vercel deploy hook) vs Option B (ISR). **Gated on finding 1** if the fix lands in the website repo, which it almost certainly does.
2. **Compose-UI save-conflict** (Comms, 3 asks) — localStorage autosave is the cheap one that removes the sharp edge.
3. **Finding 1 resolution** from CIO — website worktree vs. working on `main` in the shared checkout.
4. **Predecessor's two unanswered PM questions** — `scripts/publish-cli.js` end-to-end tested since May? `--mode=archive` still wanted?
5. Own lessons / load-bearing-vs-commodity read / Web↔Docs↔Comms publishing-seam view.

## Memory & briefing surfaces referenced this session

- **Referenced** — `dev/active/orientation-note-web-amber-2026-07-25.md` (whole session's spine; its "escalate loudly if hooks don't pass" line is what turned a shrug into finding 2). CLAUDE.md §hooks/intermittency (the claim finding 2 revises) and §"verify behaviorally, never by config presence" (the rule that needed extending to probe *shape*). CLAUDE.md HARD RULE on PM's main checkout (governed all probe cleanup — soft reset + explicit-path `rm` only). `dev/2026/07/19/...-web-code-fable-log.md` + `dev/active/web-carry-forward.md` (predecessor state). `feedback_dont_excoriate_iterate` (shaped how the PPM correction was written). `feedback_mailbox_writes_main_only` + the `mail-send.sh` push-to-ref procedure.
- **Loaded but not referenced** — most of the ~168-memory pool; the blog/editorial-calendar skills; product-repo architecture sections (intent routing, API conventions) — all product lane, not Web's.
- **Wanted but not found** — (a) **any provisioning doc covering the `piper-morgan-website` repo on Amber.** `docs/internal/operations/amber-worktree-lifecycle.md` and `git-worktrees-model-a-setup.md` both assume the product repo; nothing addresses a role whose lane is a *second* repo. That's the gap behind finding 1. (b) A predecessor read on the Web↔Docs↔Comms publishing seam — the orientation note flags it as missing and it genuinely is.
