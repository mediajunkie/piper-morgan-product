# Session Log — Principal Product Manager (PPM)

**Date**: 2026-07-26
**Started**: 12:47 PDT
**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code
**Model**: Opus 5 (1M context)
**Host**: Amber (`pipermorgan.ai`) — first PPM session on Amber
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/ppm` (Model A)
**Branch**: `claude/ppm-cycle`

---

## Session purpose

Migration orientation. **No handoff exists** — predecessor session went dark 2026-07-19
mid-day with no `DAY-CLOSED` marker; Exec's 7/21 "prepare handoff memos" ask arrived after
it was already gone. Working from `dev/active/orientation-note-ppm-amber-2026-07-25.md`
(assembled by CIO from artifacts, explicitly *not* predecessor's words).

Tasks: verify environment (not assume it), drain 12-message inbox, report oriented to CIO.

---

## Entries

### 12:47 — Session start, substrate read

Created this log (no PPM log existed for today; 6 peer roles already logging).

Environment first-pass:
- `pwd` → `/Users/xian/Development/piper-morgan-worktrees/ppm` ✅ Model A stable path
- `git branch --show-current` → `claude/ppm-cycle` ✅ not main
- `git rev-list --count HEAD..origin/main` → **0** ✅ not stale (the gotcha that bit the
  first migrant with 5,393 commits behind)

Read `dev/active/ppm-carry-forward.md` — current to 7/19 and genuinely substantive
(unusual among the dark roles per CIO). Carries 9 active PM threads, a live escalation
history, and 8 process notes including the stale-tree-object rule.

Registry (`dev/active/duty-cycle-registry.tsv:49`): ppm row present, marked
`parked: awaiting Amber migration`, last-seen 2026-06-18. Needs updating once cron is armed.

### 12:50 — Hooks behavioral check: FAILED on probe 1, then passed. Escalated.

Per duty-cycle-tick v1.15 Step 2a-bis. Orientation note said expect a PASS, escalate loudly
if not. **Probe 1 did not block** — the fail case.

| # | Shape | Result | Path named | Layer |
|---|---|---|---|---|
| 1 | compound (`mkdir && echo && git add && git commit … \| head`) | ❌ **NO BLOCK**, commit `588b69960` created | — | none |
| 2 | bare `git commit -m …` | ✅ BLOCK | absolute (`…/piper-morgan-product/.claude/hooks/check-branch.sh`) | **USER** |
| 3 | compound (`echo && git add && git commit … \| tail`) | ✅ BLOCK | relative (`.claude/hooks/check-branch.sh`) | **PROJECT** |

Same branch, same staged `mailboxes/` path, same session, ~4 min apart, no config change.

**Hypothesis formed and refuted in-session**: I thought probe 1's bypass was `if:
"Bash(git commit*)"` prefix-matching, with `git commit` buried mid-chain escaping the match.
Probe 3 was deliberately compound to test it — and blocked. **Command shape is not the
variable.** Recorded so nobody re-runs the dead end.

Config checked afterward for characterization only (config presence proves nothing, per
CIO's own rule): both layers well-formed, `matcher: "Bash"` + `if: "Bash(git commit*)"`,
three hooks each; `check-branch.sh` present + executable at both paths.

Could NOT confirm the provisioner's headless PASS — `amber-agent` not on PATH in my shell.

**Probe hygiene**: `588b69960` removed via `git reset HEAD~1`, never pushed; probe file
deleted; working tree verified clean before proceeding.

**Operating assumption for this session**: mailbox discipline and log maintenance have no
reliable automated backstop. Enforcing by hand.

### 13:05 — Inbox drained (12 → 0), reported oriented to CIO

All 12 read and moved to `read/`. Live obligations extracted:
- **#1386 gate run** — unblocked since 7/20 (beta v25 carries both Scenario-B fixes); mine +
  CXO's to schedule with Lead, ~half a day. Top live item.
- **PDR-006 review + Q2 addendum** (PA, 7/19) — my slice is sprint/roadmap implications +
  alpha-vs-later capability split; Q2 wants milestone implications of client-vs-server-side
  colleague-model inference. Gating an implementation epic.
- **Spatial committed-theory review** (CXO, 7/19) — CXO voted (b) park cold adapter tier as
  wave-2. **My lane was accepted by my predecessor and never delivered.** Arch flagged 7/19
  that it's *coupled* to PDR-006's colleague-model design → these are one piece of work.

Closed loops: CIO's 7/24 correction on the revert root-cause; the 7/19 fleet-audit +
data-loss pair (superseded by Model-A revision); Model-A revision (now in CLAUDE.md); v1.15;
Exec's 7/21 handoff-prep ask (overtaken by events — no handoff was ever written).

**Live-state verification, not taken on faith from 6-day-old carry-forward**:
- `#1386` **OPEN** ✅ — predecessor's reopen held, no silent re-close
- `#1278` **OPEN** ✅ — gate criteria genuinely still unmet

Report sent to CIO (cc PM, HOST, Exec) — commit `83fe1c8bb`.

⚠️ **The send hit a non-fast-forward and retried** — the exact path that caused my
predecessor's 7/19 data-loss incident. Applied the carry-forward's hardest-won rule: diffed
the new commit against its immediate parent and read the **FULL** file list, not just
confirmed my own file. 17 files, all mine, **zero collateral**. Spot-checked one rename on
`origin/main` by byte count (4336 in `read/`, absent from `inbox/`) because `--stat`
rename-collapse shows pure moves as "0 changes".

### 13:30 — PA's independent corroboration → lazy-attach hypothesis now n=2

A `git merge origin/main` brought in PA's memo, written ~25 min after mine, no coordination.
**PA ran the same probes on their own fresh seat and got the same shape**: probe 1 bypass,
probes 2–4 block, identical layer alternation. PA also ran a probe 4 *identical in shape to
probe 1* which blocked — a stronger refutation of command-shape than my probe 3.

PA raised "lazy attach on first matching call" as a hypothesis, explicitly n=1, and asked the
next fresh seat to test it. **My seat is that test, run before their memo arrived.** Verified
the precondition held on my side: my pre-probe-1 Bash calls were `pwd`, `git branch`,
`git rev-list`, `date`, `ls`, `git status` — none `git commit`-shaped, so probe 1 was
genuinely the first call that could trigger a hook evaluation.

**2 of 2 fresh seats, 7 of 7 probes fit lazy-attach.** It's the only model on the table not
yet refuted (CLAUDE.md's four all have been). Sent the corroboration to PA + CIO — commit
`43441ee09`, verified clean, no collateral.

Two consequences argued in that memo: (1) it would explain the headless 6/6 — if
`verify-hooks` makes any tool call before its probe, it structurally cannot test the
first-call case, making the provisioner PASS incapable of catching the failure that bites;
(2) it reframes CIO's "timescale of hours" as a session-boundary effect, not a clock effect.

### Gaps flagged, not routed around

1. **Cannot read the project board** — `gh project list` fails, token missing `read:project`.
   So the Beta Blockers recount (carry-forward's 21-open is 7/16 and flagged stale) is
   **not done**. Refused to substitute a label count: sprint membership is board state, not
   labels. Needs `gh auth refresh -s read:project` — PM's call.
2. **Cron not armed.** Left the registry row parked with an explicit note rather than
   flipping it active — a row means *watched*, and `arch`'s row shows the cost
   ("alerted 3x in 20h while known-dark"). Arming while PM is engaged also cuts against the
   cron-off-when-engaged norm. PM's call.

### Owed next, in order

1. Schedule the #1386 gate run with Lead + CXO.
2. PDR-006 review + Q2 milestone read **together with** the spatial product-value slice
   (Arch established they're coupled).
3. Own lessons / load-bearing-vs-commodity / PPM-vs-PA boundary — the orientation note names
   this the highest-value early act and it's the one thing no artifact could hand me.

## Memory & briefing surfaces referenced this session

**Referenced**
- `dev/active/orientation-note-ppm-amber-2026-07-25.md` — the whole session's spine
- `dev/active/ppm-carry-forward.md` — live threads + the push-retry rule that caught the
  non-fast-forward verification; genuinely load-bearing, not ceremonial
- CLAUDE.md §hook intermittency / §Model A / §Sign-Off — framed the hook probes and the
  "verify behaviorally, never by config presence" discipline
- `feedback_never_reuse_stale_tree_object_on_push_retry` (predecessor's pin) — directly
  applied at the retry
- `feedback_sprint_membership_is_project_board_not_labels` — stopped me laundering a label
  count into a sprint claim when the board read failed
- `duty-cycle-registry.tsv` header + `arch` row — the row-means-watched semantics

**Loaded but not referenced**
- `docs/briefing/BRIEFING-ESSENTIAL-PPM.md`, `dev/active/ppm-standing-items.md`, most of the
  memory index's feedback bucket

**Wanted but not found**
- A canonical `ROLE-PORTFOLIO-PPM` doc — my predecessor flagged this same gap on 7/19 and
  noted it's "worth actually asking about rather than continuing to route around." Two
  sessions have now wanted it. Asking.
- Predecessor's lessons / load-bearing self-assessment — known-absent by construction; mine
  to write.
