# Session Log: 2026-07-26-1250-pa-code

**Role**: Piper Alpha (PA)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Sunday, July 26, 2026
**Start Time**: 12:50 PM
**Host**: Amber (pipermorgan.ai account) — first PA session on Amber
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/pa` on `claude/pa-cycle` (Model A)

## Session Objectives

1. Orient per `dev/active/orientation-note-pa-amber-2026-07-25.md` (NOT a handoff — predecessor went dark 7/19)
2. Verify environment rather than assume it
3. **Highest-value first act**: surface the three distribution items parked on PM since 7/19
4. Triage PA inbox (7 unread)
5. Report oriented to CIO

## Work Log

### 12:50 PM — Session start, environment verification

Predecessor context: `dev/2026/07/19/2026-07-19-0734-pa-code-log.md` carries DAY-CLOSED. Clean stop,
not a mid-task death. Last substantive work = distribution research (Claude connector/plugin + ChatGPT).

**Environment checks:**

| Check | Result |
|---|---|
| Worktree path | `/Users/xian/Development/piper-morgan-worktrees/pa` ✅ stable Model-A path |
| Branch | `claude/pa-cycle` ✅ (not main) |
| Currency (`git rev-list --count HEAD..origin/main`) | **0** ✅ — no silent staleness |
| Working tree | clean ✅ |
| Memory | shared pool present (~168 memories via MEMORY.md index) ✅ — verified, not imported |
| Hooks | behavioral check pending (see below) |

⚠️ Per orientation note: `dev/active/pa-carry-forward.md` is 38 days stale (2026-06-17) — treated as
historical, NOT current state. Predecessor's real recent state is the 7/19 log.

### 1:00 PM — Distribution items surfaced to PM (the highest-value first act)

Three items parked since 7/19 (exactly 7 days, not 6 or 8). **Re-verified before restating**, per the
orientation note's warning. Two facts changed the picture:

- `mcp.pipermorgan.ai` is **not deployed** — appears only in PDR-006 + planning docs.
- **No public privacy policy page** exists (only in-app privacy *settings* templates).

Submission is therefore further out than the 7/19 memo implied — but that makes the two 5-minute
items (claude.ai tier check, OpenAI identity verification) *more* urgent, since they're the only
long-external-lead-time steps and the only ones not gated on the server existing.

Also flagged: the account migration to pipermorgan.ai (7/25) means the tier question may have a
different answer than it did on 7/19.

**Routed DIRECT to PM**, cc Exec + CIO — deliberately not via Exec relay, which is the path that
already absorbed a week. Commit `666bbdd91`.

Adjacent staleness noted in the same memo: **PDR-006 still "Arch/CXO/PPM review pending"** — Arch
acked 7/19 promising a dedicated read "next fire"; 7 days, no review from any of the three.

### 1:05 PM — Hooks behavioral check: FINDING (fresh session is NOT deterministic)

Orientation note said to expect a PASS (provisioner runs a same-day headless `verify-hooks`) and to
**escalate loudly on disagreement**. My first probe disagreed. Ran four, same branch
(`claude/pa-cycle`), same file, same config, ~10 minutes apart:

| # | Command shape | Result | Layer (by script path) |
|---|---|---|---|
| 1 | compound, `echo "…$(date +%s)" && git add && git commit` | **BYPASS** — commit succeeded (`115af2a7a`, since reset) | — |
| 2 | bare `git commit` | BLOCK | **user** (absolute path) |
| 3 | compound, plain | BLOCK | **project** (relative path) |
| 4 | compound, `$(date +%s)` — *identical shape to #1* | BLOCK | **user** (absolute path) |

**Conclusions:**

1. **Command shape is excluded.** Probes 1 and 4 are the same command shape with opposite outcomes.
   This independently confirms CIO's exclusion of command shape — I proposed shape hypotheses twice
   (prefix-anchored `if` glob; `$(...)` substitution) and refuted both myself.
2. **NEW — fresh sessions are not deterministic.** CLAUDE.md currently records "fresh sessions are
   **deterministic** (Pard, 6/6 headless `verify-hooks`)". This is a fresh seat that bypassed on its
   first probe. That is a direct counterexample to a claim written into CLAUDE.md.
3. **Both layers are live on this seat and alternate** (user, project, user) — matches CIO's 22:39
   observation; refutes single-layer explanations again.
4. **Untested hypothesis, offered not asserted**: probe 1 was the *first git-commit-shaped call of
   the session*. "Lazy attach on first matching call" fits all four datapoints and is consistent with
   HOST's mid-session-attach result. **n=1 — I cannot test it without a new session.** Cheap for
   others: fresh session → probe immediately → probe again.

**The operative rule held.** CLAUDE.md's "two probes separated by real time, never one" is validated
here: a single probe would have produced either a false FAIL (probe 1) or a false PASS (probe 2).

Probe artifacts fully cleaned up; probe commit reset (never pushed); working tree clean.

### 1:45 PM — Three-seat synthesis: I was wrong, and the correction is the finding

A `git merge origin/main` pulled in CXO's and PPM's memos — both fresh Amber seats, both probing the
same thing within ~40 minutes of me, no coordination. **PPM independently reproduced first-call
bypass (n=2). CXO reported the opposite of my probes 3/4: compound bypassing 3/3, reproducible.**

Pooled all 14 datapoints across the three seats:

| Shape | BLOCK | BYPASS |
|---|---|---|
| standalone `git commit` | **4** | **0** |
| compound `… && git add && git commit` | 3 | **7** |

**Every bypass on record was compound. No standalone commit has ever bypassed.** Command shape is
**necessary but not sufficient** — which is exactly why CXO and I read the same phenomenon oppositely.

**My error, named plainly**: I proposed "compound bypasses," refuted it with probes 3/4, then promoted
*not sufficient* → *excluded*, and wrote that into CLAUDE.md as independent confirmation of CIO's
prior exclusion. Overreach beyond my evidence. It sat in a shared file ~1 hour. Corrected in the same
section with the pooled table. This is precisely the failure mode CLAUDE.md's own hook section warns
about — a plausible diagnosis of a silent mechanism, asserted past its evidence.

**Lazy-attach does not survive as a sole model**: CXO's probes 2–4 bypassed without being first-call,
so first-call isn't necessary. May still be a second factor.

**Mitigation shipped to the cohort** (no config change): stage in one call, commit as a *separate,
bare* call — 4/4 caught. The compound one-liner is 7/10 uncaught, and it's what everyone actually
uses. CXO's framing is right: the hook looks alive whenever probed and is largely absent in real work.

Synthesis memo sent to CIO/CXO/PPM cc PM/HOST/Pard/Exec/Lead (`d233c48e6`). Open gap flagged: PPM's
probe-1 shape unconfirmed — if standalone, the 0-of-4 breaks. Suggested to CIO that the real lever is
likely the `if: "Bash(git commit*)"` predicate itself; did not touch shared hook config on day one.

### 2:00 PM — PA carry-forward rewritten; the delivery-path gap fixed

Learned mid-session (memory `feedback_pm_does_not_read_memos_wants_rollups`, written today) that
**PM does not read memos** — inbox at ~890. So my 13:00 distribution memo is a durable record, not a
delivery. The legitimate PM-facing paths are the chat reply and the carry-forward's PM Attention
section, which Exec's `cohort-attention-rollup` reads directly.

Checked what was actually on PM-facing surfaces: **CIO's board carries only the OpenAI verification
item** (as "eight days" — it's 7). The tier check and the open-source decision were on **no** PM-facing
surface at all, and PA had no lane of its own on the attention board.

Rewrote `dev/active/pa-carry-forward.md` wholesale — it was 38 days stale, describing a DinP account
and a Model-B worktree. New PM Attention section carries all three distribution items plus PDR-006,
with the verified context (server not deployed, no privacy policy) that makes the two clock items more
urgent rather than less. Structural fix, not just a restatement: the items now ride a surface that
gets read.

### 2:15 PM — Step 2a-bis amendment to duty-cycle-tick v1.15

CIO's v1.15 memo adds Step 2a-bis (probe your hooks on first fire). I ran it — and the three-seat data
says the step as written has a systematic flaw: **it doesn't specify command shape.** "Stage a file,
then attempt a commit" reads naturally as the standalone form, which passes 4/4 — while the agent's
real compound commits stay uncovered. The check certifies coverage the agent doesn't have. Same
failure mode CIO warns about one level up, reproduced inside the check built to catch it.

Sent a concrete amendment: run **both** shapes, report separately; probe B (compound) is the one that
matters. Also withdrew, explicitly, my earlier "independently confirms your exclusion of command
shape" line to CIO. (`ed2f5319c`)

Inbox triaged: 9 memos → `read/` in the same push.

### 3:00 PM — Predecessor consulted by PM; three load-bearing items, and I had propagated one

PM consulted the predecessor session directly (it still had genuine first-person recall of the 7/19
post-compaction window, and said so honestly — reconstruction for anything earlier). It correctly
predicted `mail-send.sh` would reject a `dev/active/` path and declined to improvise a git route, so it
printed §4/§6 in chat. **I transcribed it to `dev/active/handoff-pa-predecessor-2026-07-26.md`** — its
wording, its VERIFIED/BELIEVED labels, my dispositions appended.

**I had independently hit none of the three. Worse, I amplified the first one this morning.**

**1. The plugin-path claim I gave PM at 13:00 is unreliable — and it was mine to check.** I told PM
"verify the claude.ai account tier" as one of two headline five-minute asks. That rests on the 7/19
memo's "Team/Enterprise required, Max blocked" finding, which **its own author had already retracted —
in chat only, never in a committed document.** I verified the things *around* the claim (server not
deployed, no privacy policy) and took the gating claim itself as given. That's the lesson: I audited the
periphery and inherited the core.

⚠️ **What I deliberately did NOT resolve.** The correction concerns an **"Upload plugin"** option at
`platform.claude.com/plugins/submit`. Per the glossary's stop-and-look-up zone, **Connector ≠ Plugin** —
Connector is a remote MCP URL via Settings→Connectors (Track A); Plugin is a `.zip` of skills + MCP
server (Track B). So this bears on **Track B** and does **not** self-evidently clear Track A's gate. The
predecessor's summary doesn't disambiguate. **Collapsing the two is exactly how the original error
propagated**, so I withdrew the blanket claim as unreliable and routed the disambiguating question to
PM, who holds the screenshot and the account. Recorded the ambiguity rather than resolving it.

Actions: correction banner committed to the 7/19 memo on `main` (verified present at `origin/main`);
carry-forward PM-Attention rewritten from "check your tier" to the narrower real question; memo to Exec
to stop the relay (`53d8ee282`).

**2. #1351's carry-forward is an unfinished audit, not a design note.** `ConversationDB` traced safe;
**Redis, in-process floor/context state, and rate-limiting under anonymous-caller conditions were never
traced.** Escalated to Arch as a pre-go-live verification task, offering to file a tracked issue.

**3. Q2 blocks PDR-006 ratification.** It sat in "Open Questions (PM-gated)" as a peer of Q1/Q3. It
isn't: a server-side-LLM answer invalidates the "no server LLM" premise the phase rests on. Elevated to
the Status line as an explicit blocker; Q1 marked resolved, Q3 annotated. Recorded the author's own
caveat that the A/B framing was **pattern-matched from PDR-005 rather than derived**, so "neither" is a
legitimate answer — handing Arch the question honestly rather than inside an inherited binary.

**The generalizable failure**, flagged to Exec: the error wasn't bad research — that got caught. It's
that **the correction lived in a chat session while the error lived in a committed file.** The chat
ended; the file didn't. Same shape as the PreCompact hook and today's `sync-pm-local.sh` find: the
durable artifact kept asserting what everyone involved had stopped believing. *A correction that isn't
committed hasn't happened.*

### 4:00 PM — Registry: PA had NO row, and the freeze-check reads a stale checkout

PM asked for a recommendation on cadence. Checking the registry rather than the (stale) carry-forward
turned up something better than an answer: **PA had no row at all** — structurally invisible to the
freeze-watchdog, finding #6 exactly. A role with no row can't be reported stale, only silently missed.

Added `pa 42 6,9,12,15,18,21 6 6 22 06:42 2026-07-26 parked:…`, mirroring PPM: **counted in coverage,
no stall alerts.** Cadence restores PA's previously PM-ratified windowed schedule; `:42` collides with
nobody (lead :17, arch :27, host :37, cxo :47, ppm :52). Verified 8 tab-separated fields.

**Then it didn't show up in coverage — and the reason matters more than the row.**
`duty-cycle-freeze-check.sh` resolves `REPO` to the **shared checkout** `/Users/xian/Development/piper-morgan-product`
and reads the registry from that **working tree**, while fetching heartbeats from `origin/main`. That
checkout is **clean but 12 commits behind**. PPM's row (pushed earlier today) is there; mine isn't.

**So registry rows are invisible to the watchdog until someone updates that checkout — and nothing
does.** This closes a loop from this morning: `sync-pm-local.sh`, the standing post-push mechanism that
would keep a checkout current, **is pointed at the nonexistent laptop path** and no-ops on Amber. The
broken sync script *is* the reason the checkout drifts, and the drifting checkout is why the watchdog
reads a stale registry. Two findings I'd filed separately are one causal chain.

Drift rate is fast — 12 commits in ~3 hours, mostly mine. Did **not** hand-pull the shared checkout:
CLAUDE.md says don't operate there, a manual pull fixes it for minutes, and the real fix is the
one-line path in `sync-pm-local.sh` (Pard's lane). Recommended that; flagged the boundary question to PM.

### 4:30 PM — PM authorized both fixes; chain closed end-to-end

PM clarified the worktree norm: isolation is about **minimizing** shared-space contention, not a
prohibition — mail already lands on `main` by design. Authorized the path fix and the pull.

- **`sync-pm-local.sh` fixed**: host-aware candidate resolution (Amber path first, laptop second),
  mirroring the shape already applied to `duty-cycle-freeze-check.sh`. Added an **exit 3 + "synced
  NOTHING"** branch so a total miss can never again read as a clean run — the same
  silent-vs-absent failure that made the original bug invisible.
- **Verified in sequence, not assumed**: dry-run resolved → live run fast-forwarded the shared checkout
  to **0 behind** → `DUTY_CYCLE_COVERAGE=1 duty-cycle-freeze-check.sh` now emits **`PARKED pa`**.
  PA is no longer structurally invisible to the freeze-watchdog.

### 4:45 PM — Max vs. Team: RESOLVED against the docs, and it reverses my own advice

PM had conflicting information: told recently that Max could submit, but their own research said Team.
**Both prior claims were half-right and each was over-generalized to the other track** — which is
exactly why I declined to collapse Connector into Plugin earlier. That caution is now vindicated.

- **Track A (connector directory) requires Team/Enterprise.** Official docs: *"Admin settings aren't
  available on individual plans."* **PM's own research was correct.** No workaround exists.
- **Track B (plugin directory) IS submittable on Max** via `platform.claude.com/plugins/submit` —
  because a **Console organization is a different account object from the chat subscription**, gated on
  a Console *role*, not on the plan. The predecessor's retraction was right about this and wrong to
  generalize it.
- **Probable seed of the whole muddle**: a listed connector **installs** one-click for Pro/Max users.
  **Installing is not submitting.** PM's screenshot showing "Piper morgan" present is consistent with
  install/personal-upload UI.

🔺 **This reverses my own advice from this morning.** I told PM to defer the open-source decision as
"not time-critical." **Wrong.** Track A is behind a paid upgrade, so **Track B is the only Claude-side
route open today — and it requires a public GitHub repo.** The open-source decision is now the live
gate, not the deferrable one. Withdrawn and corrected in the carry-forward.

Canonical doc written — `dev/active/distribution-submission-tiers-resolved-2026-07-26.md` — because
this question has been answered wrong twice in eight days in opposite directions and neither answer
ever lived anywhere durable. Sourced to Anthropic's own docs.

Also captured PM's request to discuss the predecessor's architecture diagram as a standing item, with
the three things that have moved under it since (tier resolution, Q2-as-blocker, spatial coupling).


---

## Day close — 2026-07-26

**Closed retroactively on 2026-07-29** at PM's request. The session ended 7/26 evening without a
close marker; PA then sat idle 7/27–7/28. **Cause, owned plainly: PM approved the cron cadence on
7/26 ("your recommendation sounds like a great place to start") and I never armed it.** I executed
every other item in that same exchange — the `sync-pm-local.sh` fix, the shared-checkout pull, the
tier research — and dropped the one that governed whether I'd wake up again. Not migration
shenanigans; a missed step of mine.

### Shipped 7/26

- Oriented on Amber (first PA session there); environment verified, not assumed.
- Three parked distribution items surfaced to PM; **structural fix** — PA carry-forward rewritten
  (38 days stale) with a PM-Attention section that Exec's rollup actually reads.
- Hook probing: found a fresh-seat bypass, escalated, then **got the explanation wrong twice** and
  corrected both times. See carry-forward note — the cohort has since resolved the real mechanism.
- `sync-pm-local.sh` fixed (host-aware + loud-failure branch); shared checkout un-drifted;
  **PA registered in the duty-cycle registry, which had NO PA row at all** (finding #6).
- Predecessor consulted by PM: three load-bearing items recovered, one of which I had amplified.
- **Claude submission tiers resolved** against Anthropic's docs; reversed my own defer-open-source advice.

### Carried into 7/29

Cron arming (approved, not executed) · OpenAI identity verification (PM) · open-source decision (PM,
now the live gate) · PDR-006 review · architecture-diagram discussion (PM-requested).

<!-- DAY-CLOSED -->
