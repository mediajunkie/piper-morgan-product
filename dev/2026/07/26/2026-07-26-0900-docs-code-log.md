# Session Log: Documentation Management — 2026-07-26

**Role**: Documentation Management (Docs)
**Date**: 2026-07-26 (Sunday)
**Model**: Claude Sonnet 4.6 → Claude Opus 5 (PM switched mid-session)
**Environment**: code
**Worktree**: `.claude/worktrees/admiring-elion-ad18c4` (Model B ephemeral)
**Session start**: ~09:00 PT

---

## Session Objectives

PM: "Today's blog post is ready for your proofreading and publication."
Target: "The Meta-Observation Pattern" (insight, pubDate 2026-07-26).

---

## Work Log

- (~09:00 PT) — **Template audit run** on `docs/public/comms/drafts/the-meta-observation-pattern.md`.
  13 of 14 checks PASS. One FAIL:
  - **Check 11 (AI-writing-tics)** — 4 instances of the negation-reveal cliché
    (lines 33, 43, 45, 55). Comms' Jul 26 review had not flagged these.
  - Check 6 verified against calendar: footer teases "The Trust Architecture
    Hardens", which IS the next scheduled post (Jul 28). Correct.
  - Check 13 advisory only: NO-GLOSS warnings on "Comms"/"Docs" — both are
    contextually clear in-prose, not hard failures.
  - Frontmatter complete (image/alt/caption). CSV altText/caption were empty —
    noted for post-publish backfill, not a draft defect.

- (~09:25 PT) — **PM approved affirmative rewrites**; applied all 4:
  1. "documentation isn't standing outside… The documentation is *part of*"
     → "the documentation is *part of* the system"
  2. "The drift wasn't a bug in any single document, it was the system reading…"
     → "The drift came from the system reading… not from a bug in any single document"
  3. "communication isn't just reporting. It's intervention."
     → "communication becomes intervention"
  4. "The pattern isn't deep. But it is operational"
     → "The pattern is shallow but operational"

  Left line 59 ("isn't necessarily wiser than the system that doesn't") — plain
  comparative negative, not the deny-then-reveal construction.

- (~09:30 PT) — **Held before committing** on PM's report of a compose-UI save
  error (the Jul 25 SHA-optimistic-concurrency collision pattern). Verified
  `origin/main`'s draft was byte-identical to my base before proceeding — no PM
  save had landed, so my rewrites were the only delta. PM confirmed in sync.

- (~09:32 PT) — 🔴 **FOUND AND FIXED: `pre-commit-reconcile-drafts.sh` was
  hard-blocking every `docs/public/comms/drafts/` commit despite documenting
  itself as warn-only.**
  - The hook prints *"Commit is NOT hard-blocked (warn-first mode)"* and then
    `exit 2`. In Claude Code hook semantics **exit 2 blocks**; exit 0 warns.
    Message and behavior contradicted each other.
  - Surfaced when it blocked this publish on a **false positive**: it flagged
    `the-triad-model.md` as a "TRUE ORPHAN", but that file is *deliberately
    parked* per `d67e7d5c1` — "preserve PM-edited triad-model draft (awaiting
    republish decision)". Repointing that calendar row's `draftPath` would have
    been actively wrong: the row belongs to the already-distributed Jun 27
    version.
  - PM chose "fix the hook's exit code" over routing around it via commit-tree.
    Changed `exit 2` → `exit 0` with a comment naming what to change to promote
    it to a real hard-block later.
  - **Behaviorally verified**: the identical commit that failed twice before the
    fix succeeded immediately after.
  - Commit `2d2d60e60`, pushed to origin/main as `61a8c852c`.

- (~09:33 PT) — **Published** to pipermorgan.ai.
  - Pre-flight: draft + image both confirmed on disk.
  - Dry-run first (mandatory): HTML clean, all 4 rewrites reflected, headings
    render as `<h1>`, image path resolving, `workDate=2026-04-18` correctly
    passed (NOT defaulted to today).
  - `publish-post.js --slug the-meta-observation-pattern --category insight
    --work-date 2026-04-18`
  - hashId `82a94eaff7b0`, workDate 2026-04-18, pubDate 2026-07-26
  - Website commit `d253bf85a8` → pushed to `piper-morgan-website` origin/main
  - Staged 5 files explicitly; deliberately did NOT commit
    `src/data/alt-text-backfill-batches.json` (untracked stray dated Jun 17,
    unrelated to this publish).

- (~09:34 PT) — **Editorial calendar updated** (`65703af4b5`):
  status `ready-for-docs` → `published`; blogURL, blogPath, canonicalSite=
  distributed, cartoon slug set; **altText + caption backfilled** (both empty —
  PM spotted this). Whole-file verification passed (417 rows, all 18 fields,
  semantic anchors clean).

- (~09:36 PT) — **Live verification**. Initial curl returned HTTP 200 with
  correct `<title>`/`<meta>` but a 404 body — deploy propagation, not a data
  defect (confirmed blog-content.json entry well-formed dict, `guid:
  blog-first-82a94eaff7b0` carrying the hashId correctly). Resolved on poll.
  All 4 rewrites confirmed serving live; all 3 old constructions confirmed gone;
  image + caption rendering.

---

## Findings Flagged

1. 🔴 **`pre-commit-reconcile-drafts.sh` exit-code defect — FIXED this session.**
   Warn-first message, blocking exit code. Had been silently hard-blocking every
   drafts commit since introduction.

2. 🟢 **Project hooks DO fire on this seat** — a live data point for the open,
   explicitly-unresolved question in CLAUDE.md ("verify behaviorally, never by
   config presence"). Observed deterministically: blocked twice pre-fix, passed
   immediately post-fix, same command shape. Model-B ephemeral worktree,
   Claude Code desktop.

3. 🟡 **`the-triad-model.md` reconcile false positive** — filed as discovered
   work. The reconcile script classifies a deliberately-parked draft as drift.
   Not fixed here: resolving it requires a PM scheduling decision about whether
   the PM-edited rewrite republishes.

4. 🟡 **Caption `''` bug reconfirmed live** — `don''t` / `we''re` render
   literally on the published page, exactly as PM predicted. Per PM's standing
   instruction (Comms log Jul 26), NOT acted on. Web owns the encode/decode fix
   plus a back-audit of already-published posts.

---

## Issues Filed This Session

- Triad-model reconcile false positive (spawned as background task)

## Issues Closed This Session

- None (publishing work only)

---

## Memory & Briefing Surfaces Referenced This Session

### Referenced
- `template-audit` skill — the 14-item checklist; check 11 is what caught the post's only real defect
- `feedback_negation_reveal_cliche_and_claude_isms.md` — the specific pattern family; directly load-bearing for check 11
- `publish-to-blog` skill — mandatory dry-run, `--work-date` discipline, pre-flight file existence check
- `update-calendar` skill — csv-module-by-name access, whole-file verification
- `feedback_csv_edit_by_name_never_position.md` — used throughout the calendar edit
- `feedback_commit_only_own_files.md` — kept the stray `alt-text-backfill-batches.json` out of the website commit
- `feedback_pause_before_irrevocable_actions.md` — held on the compose-UI save error; verified origin/main state before committing
- `feedback_diff_head_before_editing_shared_file.md` — the byte-identical check that made it safe to proceed
- CLAUDE.md hooks section — framed the exit-code finding as a behavioral verification, not a config claim

### Loaded but not referenced
- Cross-pollination brief
- BRIEFING-ESSENTIAL-DOCS.md
- Most non-comms-tagged MEMORY.md entries

### Wanted but not found
- **A durable record of Claude Code hook exit-code semantics** (0 = warn/allow,
  2 = block). This was the crux of the session's real finding and I reasoned it
  from the hook's own contradictory output rather than from any project doc.
  Worth a line in the CLAUDE.md hooks section, which currently documents hook
  *reliability* extensively but not hook *exit-code contract*.

---

## Sign-off checklist

- `git status`: clean (verified at wrap)
- `git log --oneline @{u}..HEAD`: empty
- `git log --oneline main..HEAD`: empty — all work on origin/main
- Product commits: `2d2d60e60` (rewrites + hook fix), `65703af4b5` (calendar)
- Website commit: `d253bf85a8`
- Post live + content-verified: https://pipermorgan.ai/blog/the-meta-observation-pattern/

<!-- DAY-CLOSED: 2026-07-26 -->
