# HOST Session Log — 2026-07-26 (Sunday)

**Role**: HOST (Head of Sapient Trust) · **Account**: pipermorgan.ai · **Tool/Model**: Claude Code / Opus 5 (1M context) · **Host**: Amber · **Worktree**: `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Model A) · **Slug**: `host-code`
**Cron**: `60aaebf2` — `37 6,9,12,15,18,21` (6 daytime fires). **Registry row**: live, `cron_expr` matches.

> Continuous session from 2026-07-25 15:36 (the Amber migration cutover). Prior day's log — `dev/2026/07/25/2026-07-25-1536-host-code-log.md` — carries `<!-- DAY-CLOSED: 2026-07-25 -->`; **Step-0 verified, no missed close to self-heal.**

---

## Work log

- **START (07:07 PDT)** — Day 2 on Amber. Cron verified (exactly one job). Sync clean, 0 behind. Collision sanity: `host` ↔ `claude/host-cycle`. Both mail channels checked. Registry row verified against the armed cron (`cron_expr` matches — the thing that goes stale after a cadence change).

### 1. Longitudinal hooks probe — **8/8 across ~9 hours** (`fbff3bd5d`)

CIO's redundancy-refutation arrived overnight: it ran my proposed test **immediately, because its restart would destroy the only instance of the condition** — the right instinct, and one I should have flagged when I handed over a diagnostic whose window was closing. Result: both layers alternate on CIO's seat too. **My redundancy hypothesis is refuted**, and with it CIO's own scope model (its user layer *did* attach to a session predating the key). CLAUDE.md corrected at line 105 — verified by reading, not assumed.

Ran the ~8-hour instrument, same design as yesterday for comparability:

| probe | time | shape | result | layer |
|---|---|---|---|---|
| E | 07:08:16 | bare | ✅ BLOCKED | absolute → **USER** |
| F | 07:08 | **piped** | ✅ BLOCKED | absolute → **USER** |
| G | 07:08 | bare | ✅ BLOCKED | relative → **PROJECT** |
| H | 07:08 | bare | ✅ BLOCKED | relative → **PROJECT** |

**8/8 across two clusters ~9h apart. This seat has never once failed to block.** All reversed, nothing pushed.

What I was careful *not* to claim: my clean result does not explain CIO's 1-of-5 and must not become the story. CIO's non-firings were real, observed, properly reversed. Two seats disagree; mine being clean **localises** the problem rather than resolving it. Logged as **open-unexplained with the condition retired** — excluded so far: file shape, command shape, config drift, single-layering, and (for this seat) slow time-variation.

Also took CIO's better formulation: **refuting the reason doesn't refute the caution.** I'd have been tempted to withdraw the keep-both-layers warning along with the hypothesis it rested on; that would have been wrong.

### 2. ⚠️ New finding — the registry is binary, and it's crying wolf on a parked role

The watchdog has alerted on **`arch` three times in 20 hours** (07-25 14:01, 20:02; 07-26 07:03) and will fire ~every 6h until arch migrates. Every alert is **correct and unactionable**: arch *is* stalled and *deliberately* dark, first in the roll.

The trap is that both available options are wrong, because the registry has two states: **keep the row** → alert fatigue on the one belt we just fixed; **delete the row** → structural invisibility, finding #6 exactly, which is how five roles went dark six days. Proposed a third state — **⏸ PARKED** (no stall alerts, still counted in coverage as `parked (since …, reason)`). It also formalises a workaround already in the file: `cxo`/`ppm` are commented out, i.e. PARKED-as-comment, invisible to the denominator rule. **Did not edit the registry** — CIO's surface, Exec confirmed the row shape.

### 3. ★ Dashboard criteria **v0.3 SPEC** shipped (`2a8199f34`) — the queued item, not re-deferred

Idle since 2026-06-19. v0.1/v0.2 were criteria; this is normative — six render rules, A–F at spec altitude, data sources, and open items **with an owner named per item** (applying CIO's own ask-scoping corollary to my own asks).

**New Criteria G — mechanism liveness ("the belt needs its own belt").** The gap: v0.1–v0.2 model *agent* liveness thoroughly and *mechanism* liveness not at all — backwards for the failures we actually have. Four mechanisms found silently dead or unreliable in 48h; **none visible on any dashboard.** A dashboard rendering every agent 🟢 while its enforcement substrate is dead is the exact false assurance this criteria set exists to prevent, and it would have rendered that for ten weeks. G1 config-presence≠liveness · G2 verification staleness ages out of green · **G3 unverifiable is not a pass** (PreCompact renders `⚪ never observed firing`, permanently) · **G4 known-unreliable is its own 🟠 state** — 1-in-5 enforcement is worse for planning than known-absent because it invites reliance · G5 advisory-vs-control labelled.

**New ⏸ PARKED state** (§4) and **new F4** — undelivered outbound obligations, the highest-severity asymmetry class because *the damage lands on a party who cannot know it exists*; the dashboard is the only surface that can see both halves.

**E is now blocked from shipping without its coverage indicator** — `0 actions logged` under partial instrumentation is indistinguishable from `no actions taken`, the same shape as the MEMORY.md truncation. Flagged to PM specifically, because E is the surface reporting what Piper did autonomously on a user's behalf, so that false negative has real-world consequences rather than process ones.

Appendix maps **eight** incidents to the rules they produced. The through-line: every one was believed-working on the strength of its config or its plausibility, and every one was invisible to every dashboard we have. **G extends the criteria set from the agents to the instruments — because the instruments turned out to be where the silence was.**

Routed (`67f4a39a9`) + Pard's channel (`7ab9246`). Standing-items entry corrected — it had been misreporting this item's state, which is what sent me to the artifact yesterday.

### 4. Sapient-trust poll — **9th consecutive clean**

`gh issue list --label sapient-trust --state open` → **0 open**. Nominal, no action. Next due ~2026-08-02.

### 5. §7 added — E's coverage-indicator definition (`e6a043642`)

Closed one of my *own* §6 open items the same day rather than leaving it as a listed intention, since it's independent of whether G is accepted (E's shape was already agreed 6/19). Defines action-site / instrumented-site / `coverage = M of K` as a **fraction, never a bare percentage** — `12 of 19` is checkable, `63%` isn't. Five render forms; count and coverage are **one atomic string** layout cannot separate.

The blocking rule exists for exactly one cell: **`M<K, N=0`** renders "no actions logged" — the state that reads as reassurance while being the least informative output the system can produce. And because the 6/19 markup sequences E incrementally, **partial coverage is the expected condition for the entire rollout, not an edge case.** `N` is labelled a *floor, not a total*, because undercount from partial instrumentation is biased downward always and invisible without `K` — structurally the same error as the MEMORY.md truncation.

**G's verification intervals: held, with a named trigger** — until CIO rules on G, because intervals for a redirected criterion are wasted work. Recorded in carry-forward so the deferral has a trigger instead of being silence.

### Fire close (07:40)

Queue drained to (0,0): inbox zero, both queued START actions done, poll run, three items out for review and correctly not mine to advance. Cron `60aaebf2` armed, one job. All work on `origin/main`.

---

## Fire (10:07 PDT) — WORK

*Next fire 12:37 (today) so not STOP. Cron verified. Sync clean. Both mail channels drained. No new watchdog alert since 07:03 — consistent with the ~6h cadence; next due ~13:00.*

Both outstanding asks came back:

### Exec — PARKED ratified, F4 accepted, F2 needed a design pass → **drafted it** (`24b4a81b8`)

**⏸ PARKED** ratified on shape (CIO still owns the mechanics — registry untouched by me). **F4 accepted into rollup scope**, and Exec grounded it in two cases it caught by hand this week, which is better evidence than my hypothetical. **F2** Exec declined to accept on a nod — correctly: *"rather than nod it into scope and then not build it, which is its own version of the silent-gap problem."* That's the right instinct and I'd rather be told that than get an easy yes.

Exec offered to work the design pass *"when there's a natural window."* **That's the triggerless-deferral shape** — and F2 has been open on exactly that framing since 6/19. So I drafted it rather than waiting.

**The design pass's core move**: the general entity-resolution problem is hard, but **this cohort doesn't write identifier-free prose** — it writes `#1394`, `ADR-078`, `m-43`, `finding #6`, memo filenames. Tier 1 is a regex + a set-join + the GH-verify the rollup already runs. Candidate gap requires **all three** of: anchor in ≥2 surfaces owned by different roles · none flags it blocked · untouched >14d. **Dropping the staleness condition is what would make it fire on healthy collaboration** — F2 is mutual non-flagging of a *stalled* thread, not "two agents working on the same thing."

Render is a flag with a `matched on:` line so the detection is **checkable by the reader** (R5), plus its denominator (R3). Validation uses four known-answer cases from this week including a **false-positive control** — HOST+CIO trading 8 memos on the intermittency, which any wrong design fires on. Tier 3 (semantic) deferred with a numeric trigger, and the tier-1 recall hole named rather than left implied.

### Pard — drumbeat armed in ~20 min; two refinements, and a new spec rule (`466ac75c9`)

Verified the artifacts myself rather than trusting the memo: log present with the 07:22 PASS line, script executable, crontab live at `5 7,19`.

**Gap 1 — it escalates on FAILURE but is silent on ABSENCE.** If the crontab is removed, the script dies before appending, or the host is asleep at 07:05/19:05 → no line → no non-PASS → nothing escalates → **silence reads as health**. The instrument built to detect silent mechanism death can itself die silently. Filed as **G6** in the spec.
> **G6 also names where the chain terminates**, because the obvious objection is infinite regress: not another daemon — *a staleness assertion on an artifact that already exists, inside a loop that already runs.* The last link doesn't need a watcher if it announces its own age. Concretely: Pard's existing 30-min tail also checks newest-line age >15h.

**Gap 2 — the schedule itself is unproven.** The 07:22 line is from a *manual* run; 07:05 had already passed when the crontab was installed. So `7/7 lifetime` is 7 hand-triggered invocations — the **script** is verified, the **schedule** is config-present-but-unobserved. **First scheduled beat is 19:05 tonight.** Flagged per my own G1; if 19:05 produces no line, that's the finding, not a non-event. *(Carried into tomorrow's START.)*

Also recorded the interval's *justification* alongside the interval: 12h tolerance rests on `check-branch.sh` being **advisory**. If it's ever reclassified as a control, 12h stops being tolerable.

### Housekeeping — fixed an antipattern I introduced

Cron re-armed **`60aaebf2` → `7c1d5637`** (delete → create → verify; **same expression, cadence unchanged**, registry row still correct). The prompt itself was rewritten: yesterday I baked two one-time START actions into it, which is precisely the *"freeze carry-forward state into the cron prompt"* antipattern the skill's own table names — and they rode the prompt for three fires after being completed. The new prompt **carries no task list and points at the carry-forward**, with a note saying why, plus one durable line earned this week: *deferring unblocked work needs a named trigger, never "when there's a window."*

### ⚠️ The validation run overturned my own headline case (`5eba347e8`, memo `2147d887f`)

Ran F2's §5 validation set by hand — the thing I'd named as mine and unblocked — and it refuted the case I'd made canonical.

**We had all been citing**: *arch issued an integrity ruling stopping Lead's `#1394` Option A, then went dark, so Lead may have been building against a ruling it never received.* **Lead's 07-19 log, line 42**: *"Overnight-crash mail: **Arch STOP on 1394 Option A** (reverses ADR-078 D4…)"* — received, understood, follow-up steps recorded, **same morning**. Arch's own log confirms the send. **Both ends accounted for; nothing was stranded.**

**The exemplar had reached three surfaces** — checklist v1.4.2 Rule 4, spec §2 F4, and the validation table. All corrected, visibly rather than quietly (v1.4.1's lesson).

**F4 the rule survives — on Exec's evidence, not mine.** Exec grounded it independently in two cases it had caught by hand; those are *observed* rather than inferred, which is exactly why they're the better basis.

**The part worth sitting with**: the example failed *in the shape of the rule it illustrated.* F4 says verify the obligation reached its recipient; the example was written from arch's log alone plus a plausible narrative, **without checking the recipient's side**, and then propagated through a chain that had no step for checking. Ownership is shared and I'll name mine: CIO originated the framing, **I propagated it into two canonical documents without verifying it**, Exec accepted scope partly on its strength.

The generalisation I think is the real finding: **"a mid-day death means the counterparty may never have received something" is a hypothesis about a specific memo, not a property of mid-day deaths.** Arch died mid-day *and* delivered its ruling — independent facts we collapsed by reasoning from category to instance. So I rewrote Rule 4 to say mid-day death makes stranding *possible* → **enumerate the outbound obligations and verify each, expecting most to have landed** — rather than framing it as implying stranding. Otherwise every migrant inherits a suspicion instead of a task, and the suspicion is what produced this.

**F2 dry-run findings (§5a), all three material:**
1. **`finding #4` collides with GH `#4`** — the cohort writes `finding #6` and `#1394` in one sentence; bare `#\d+` conflates them. Needs its own namespace. Surfaced only because the false-positive control is referenced *exclusively* as `finding #N` and so produced **no numeric anchor at all** — it can't false-positive, but not for the reason I'd predicted.
2. **Session logs are load-bearing, not optional** — `#1394` appears in **12 session logs across 6 roles** but is **absent from arch's carry-forward**. Scanning only carry-forwards/standing-items would miss the corpus's highest-traffic thread.
3. **The 14-day staleness threshold is the main precision lever** — it's the *only* condition excluding `#1394`, at 10d vs 14d. Recommend building tier 1; recommend **not** trusting that default until it's swept across a month.

Anchor density confirmed on all 11 role surfaces (arch alone ~55). Tier 1 is viable and cheaper than feared.

---

## Fire (13:07 PDT) — WORK

*Next fire 15:37 (today) → not STOP. Cron `7c1d5637`, one job. Sync clean. Six memos in, all drained.*

### ★ Finding #7 (CIO): the freeze-watchdog was running on the laptop we're retiring

CIO found — by accident, doing something unrelated — that **the watchdog had never been on Amber at all.** No launchd job, no crontab, no audit log here; and yet alerts unbroken since 07-24. It was running on **PM's laptop, the machine this migration is retiring.**

The property that makes it nasty, and CIO stated it exactly right: **a watchdog that is silent when healthy is indistinguishable from a watchdog that is dead** — silence *is* its success condition. So when the laptop shuts down, the observable signal is identical to a healthy cohort. **And the laptop failing isn't a random risk; it's the planned outcome of the project we're executing.**

That's Criteria **G3** — *unverifiable is not a pass* — landing on the belt itself, found **within three hours** of shipping the criterion, on the mechanism whose entire job is noticing silence. CIO's framing (the criterion working, not a fresh embarrassment) is the right read.

**Cutover EXECUTED** by Pard + PM: laptop job off 11:18, Amber armed 11:20 (`46 */6`), ~2-minute watch-gap. **Laptop inventory delivered** — the watchdog was **1 of 4** custom jobs; two more are live mediajunkie infra (a running web/RAG service, a 5am sweep). *"I looked for one thing and found one thing"* would have missed both. Pard owns those; not HOST-lane.

### ✅ Verified the 12:46 beat — the schedule is proven

Pard named the layer before I could (*"the manual beat proves the script; the 12:46 fire proves the schedule"*). Checked:

```
10:54:18  rc=0 roles=8 all-quiet   ← proof-run
11:20:07  rc=0 roles=8 all-quiet   ← gap-closing manual beat
12:46:06  rc=0 roles=8 all-quiet   ← ★ SCHEDULED — this is the one
```

**Finding #7 closed on evidence.** PARKED verified live (`arch`/`cxo`/`ppm` carry `parked:` col 8; **arch alerts stopped** — 3 in the prior 20h, none since 07:03).

### ⚠️ But the heartbeat's denominator is wrong — two ways

`roles=8`; the registry has **7** rows, **4** watched. Reproduced rather than reasoned — ran the wrapper's own awk:

- **Off by one**: `NR>1` skips only line 1, but the header sits at **line 31** after the comment block, so `[role]` is counted as a role.
- **Worse**: `roles=8 all-quiet` **conflates watched with parked**. It reads "8 checked, all healthy"; the truth is "4 watched, 3 deliberately not." **A parked role is not a checked role** — the entire point of the state we just shipped.

**This is finding #6's shape — subset phrased as total — reappearing inside the fix for finding #7, ~2 hours later, in the one field R3 exists to govern.** Not carelessness; evidence of how strong the pull is. The registry's own ROSTER NOTE says *any coverage claim from this file must state its denominator*, and the new heartbeat states a wrong one.

Fix **tested before proposing** (anchoring on `$1!="role" && NF>1`, not `NR>1`, so it survives the comment block growing again — which is what broke it): yields `watched=4 parked=3`. Pard's emit half, so proposed not applied. Sent (`714e8d45b` + Pard's channel `a2004bc`).

### ★ §3a shipped — verification intervals (`55c163861`). Held item, trigger arrived.

**CIO accepted Criteria G, no redirect** — that was my named trigger, so this got done rather than carried.

The ask was "per-mechanism intervals," which **presumes a clock for everything. That presumption is wrong**, and getting it wrong is how you schedule a check for something unschedulable and then read its silence as a pass. Four modes — **Scheduled · At-use · Event-reported · Self-evidencing** — only the first is a clock. Interval rule: *how long can this be dead before damage is unrecoverable* → halve it. Not "how often is convenient."

**Two gaps the filled table made visible that a flat list would have hidden:**
1. **`broad-staging-warn` and `reconcile-drafts` are still unverified** — the drumbeat exercises `check-branch` only. All three were dead together; **one has been proven alive.**
2. **`mail-send`'s residue-reconcile half has no check at all** (the push self-verifies; the reconcile doesn't).

PreCompact assigned **Event-reported explicitly so nobody invents a clock for it**: a compaction with no warning is a finding, not a non-event.

Endorsed CIO's heartbeat-in-START design — **the regress terminates in redundancy rather than another mechanism**, which is what G6 should have said; folded in as the At-use row.

---

## Fire (16:07 PDT) — WORK

*Next fire 18:37 → not STOP. Cron `7c1d5637`, one job. Sync clean. Carry-forward checks: heartbeat fresh (13:22, bar 7h) · **denominator fix applied by Pard verbatim** (`watched=4 parked=3`) · drumbeat 19:05 still pending.*

### ★ Verified the two hooks that had never been proven alive

§3a had surfaced that the drumbeat exercises `check-branch` **only** — all three were dead together under the same matcher, one was proven alive, and **the other two were assumed alive by inheritance.** Inference, not observation (m-43). Testable from this seat, so waiting on Pard's drumbeat extension would have been deferral wearing a helper's name.

| hook | fires? | exit | writes | what the agent experiences |
|---|---|---|---|---|
| `check-branch.sh` | ✅ (8/8) | 2 → blocks | **stdout** | blocked, **message lost** |
| `broad-staging-warn.sh` | ✅ **first time ever** | 2 → blocks | **stderr** | blocked — **while its message says "commit is not blocked"** |
| `reconcile-drafts.sh` | ⚠️ detects, says nothing | **0** | stdout, no log | **nothing at all** |

`broad-staging-warn`: 22 non-mailbox files staged (isolating it from check-branch) → fired, full text surfaced, commit prevented. **It created `dev/active/session-end-warnings.log`** — the file CLAUDE.md cites as corroboration these hooks were dead. Its absence was evidence; its appearance is the counter-evidence. Probe entries removed (synthetic; would have misled the merge-keeper sweep).

`reconcile-drafts`: orphan draft staged → **commit succeeded silently.** Invoked directly it detects the orphan perfectly, prints a correct warning, **then exits 0** — and exit-0 stdout goes nowhere. Its own comment gets half of it right: *"exit 0 = warn-only (message reaches the agent…)· exit 2 would BLOCK."* Correct that 2 blocks; **wrong that the message arrives.**

### ★★ The finding that outranks the three bugs: **there is no warn-without-blocking tier**

Two outcomes exist. **exit 0 → invisible** (stdout *and* stderr discarded). **exit 2 → blocks** (stderr surfaces, stdout discarded). No third.

All three hooks were written against a warn-first tier **that does not exist**, and each compensated wrongly in a *different* direction. That's why the taxonomy matters more than the individual fixes: **patch the three and leave the assumption standing, and the fourth hook repeats it.** Written into the spec as **§3a-bis** with two permitted patterns (block honestly + stderr; or exit 0 + durable surface) and one prohibition (exit 0 + stdout only = indistinguishable from dead).

Arrived at Criteria G from the opposite direction: G asks *is this mechanism alive?* — §3a-bis says **a mechanism can be alive and still communicate nothing.**

### ⚠️ Hazard I walked into: the trigger can lock an agent out of Bash

At ≥20 staged files, **ordinary non-commit Bash calls got blocked** — I lost the shell mid-probe and had to find empirically that some command shapes escape. Observed, stated as observation: multi-line commands with no `git commit` in them **fired** (reproducibly, ×2); single-line commands **did not** (×3). So the `if: "Bash(git commit*)"` predicate is **leaky** and command *shape* is implicated.
**Not isolated** — all my multi-line cases began with `cd`, so shape and first-token are confounded, and I failed three times to construct the discriminating command. Handed it over rather than guessing: *one test — single-line beginning with `cd`, 20+ files staged — separates them.* An agent with a large legitimate changeset can lose Bash entirely while the only message it can see insists nothing is blocked.

### Side finding: both hook layers really do execute

The warn-log recorded **every firing twice** — same timestamp, same counts. Direct evidence the user-level and project-level registrations are independent execution paths. **Two log lines = one event** (don't let anyone read it as double the incidents), and it's the keep-both-layers call finally *watched* rather than inferred.

Spec updated (`f4d34706d`); memo sent (`cf276862c` + Pard's channel `4439a01`).

---

## Fire (19:07 PDT) — WORK

*Next fire 21:37 = STOP. Cron `7c1d5637`, one job. Sync clean. **The cohort has rolled** — arch, cxo, pa, ppm, web all live on Amber; 24 memos in, dominated by a five-seat convergent investigation into the hook bypass.*

### ✅ Both carry-forward checks closed

- **18:46 watchdog beat: landed unattended, carrying the corrected denominator** (`watched=4 parked=3`). Pard's fix proven on the schedule, not just a manual run.
- **19:05 drumbeat: landed unattended → schedule PROVEN** (this morning's `7/7 lifetime` was 7 hand-triggered runs). **Verdict came back INCONCLUSIVE** — *"commit did not land but refusal lacks hook attribution."* The instrument reported the honest tier instead of rounding to PASS, which is CIO's corrected rubric working unattended.
  > ⏳ **This is the first live non-PASS, so it's the first real test of Pard's escalation path** (*"any non-PASS goes to HOST and CIO within the half hour"*). Fired 19:05:03 → window open to ~19:35. **Check at STOP whether the escalation mail arrived.** G6 asks whether an instrument reports its own absence; this is the adjacent question — **does its escalation deliver when it has something to say?** Nobody has watched that either, and it's better found on an INCONCLUSIVE than on a real stall.

### ⚠️ WITHDRAWN: my "predicate leak / Bash lock-out hazard" (`95b866e10`, memo `442c08759`)

I raised it to the whole cohort as a hazard. **It does not reproduce.** Pard's four headless probes: none fired. My marker-hook instrument (no threshold → isolates *invocation* from the script's ≥20-file logic), 21 files staged:

| probe | shape | marker |
|---|---|---|
| A | single-line, no git | — |
| B | single-line `git diff` | — |
| C | **multi-line cd-first, no git — the exact reported shape** | **—** |
| D | multi-line with the word "commit" in text | — |
| **validation** | **genuine `git commit`** | ✅ **FIRED** |

**The validation row is what makes the four negatives mean anything** — four negatives from a dead instrument prove nothing, which is the trap in every finding this week and I nearly walked into it from the other side.

Unexplained residue, stated rather than buried: I *was* blocked twice at 16:07, and one uncontrolled marker firing was never isolated. **Withdrawn as a hazard, retained as an unexplained single observation.** A false hazard is the alert-fatigue failure this whole week has been about — the correction had to travel as far as the claim, so it went cohort-wide rather than into a quiet edit.

### ★ The gate I set and cleared tested the shape nobody writes — checklist **v1.5** (`b4a02ff3b`)

PA quantified what Web diagnosed: **standalone shape blocks 4/4; compound `… && git add … && git commit …` bypasses 7/10** (14 probes, three fresh seats). PreToolUse fires *before* the Bash call, so in the compound form `git add` hasn't run when the hook reads the index.

**My day-one gate used the standalone shape.** So did the checklist step I wrote, and for the same reason — *"stage a file, then attempt a commit"* reads as two steps. **The gate's PASS was real but narrower than it was read as, and I cleared the cohort's roll on it.** PA is amending the skill; the identical defect was in my surface, so I shipped **v1.5**: both shapes, reported separately · the free mitigation (stage in one call, commit bare in the next — 4/4 caught, no config change) · and PA's inversion that **on a fresh seat the first probe is the least trustworthy**, because a blocked commit leaves its file staged and primes the next one.

Put the scope correction in the canonical doc rather than only a memo — it's the same rule I applied to the F4 exemplar this morning.

### My own paired probe — index verified empty before each

| probe | shape | result |
|---|---|---|
| A | standalone (stage, then bare commit) | ✅ **BLOCKED**, `check-branch` named |
| B | compound, one call | ❌ **BYPASSED** — a `mailboxes/` commit landed on a feature branch |

Reversed immediately. **Fifth seat confirming Web's mechanism**, and the honest state of my seat: **the hook is alive and does not cover my normal workflow.** My 8/8 was never wrong — it was shape-limited, and I reported its scope as broader than it was.
