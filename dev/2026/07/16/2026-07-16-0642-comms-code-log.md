# Communications Director Session Log

**Date**: July 16, 2026 (Thursday) · **Start**: 6:42 AM PT
**Role**: Communications (Comms) · **Model**: Claude Sonnet 5
**Branch**: worktree-vivid-gathering-wreath (ephemeral auto-worktree — Option B)
**Cron**: `c526cc02` · `12 6,9,12,15,18,21 * * *`
**Prior session**: `dev/2026/07/15/2026-07-15-0630-comms-code-log.md` (DAY-CLOSED: 2026-07-15 ✓)

## Carry-forward from Jul 15

- **Open question awaiting PM's answer**: 38 pre-existing calendar rows have stale `canonicalSite` values (from an old schema drift), tripping the semantic-anchor check I added to `update-calendar` v1.2 yesterday. Verified all 38 are genuinely `status=published` with real `blogURL` values — the correct fix is `canonicalSite=distributed` on all 38, not the "dead backlog" characterization I'd wrongly given them in yesterday's #1406. Held off writing pending PM's go-ahead (Code's memo still sitting unread in inbox).
- Beat 14 "Into Production" — **pubDate is TODAY** — fully fact-checked and mechanically clean, frontmatter/art still pending PM.
- Insight "Mechanical First, Then Read" (pubDate Jul 18) — drafted, fact-checked, awaiting PM voice-pass.
- BYOC marketplace narrative — still awaiting PM direction, now several weeks stale.

## Work Log

- **(06:42 PT — START)** — Jul 15 confirmed DAY-CLOSED ✓. Cron: 1 job (`c526cc02`), correctly armed, no duplicates. Inbox: still holds Code's memo from yesterday (unread, pending PM's canonicalSite decision). Beat 14's pubDate is today; frontmatter still empty, same state as I left it. Created this log.
- **(07:22-08:15 PT — PM's admin-UI edit on Beat 14, full proof pass, two substantive editorial questions resolved)** — PM edited today's post ("Into Production") via the new website-team-built admin editing UI, added frontmatter (image/alt/caption), and asked for a voice/accuracy review. Diffed against my last-known-good baseline. PM's edit reworked several passages into fuller first-person voice, added the real image, and made two substantive content changes I didn't unilaterally act on:
  - **Fixed 3 real mechanical errors** from the admin-UI edit itself: `image: 'real-world.pn'` — truncated file extension (same failure shape as yesterday's "sprin" truncation from the same UI — worth watching if it recurs); `caption: '""Hello, world!""'` — doubled quote marks that would have rendered with stray extra quotes (confirmed the correct convention, `'"text"'`, against multiple published posts); "went out to **an** tester" → "a tester."
  - **Flagged rather than silently resolved**: (1) PM's edit had removed Beatrice's name entirely, generalizing to "a tester"/"she" — asked whether this was deliberate. PM confirmed: yes, both privacy AND accuracy — **Beatrice never actually tested the hosted-alpha plugin**, despite my own Jul 14 fact-check having flagged-but-left-in a PA log line stating otherwise ("alpha package sent to Beatrice, first external tester"). This means a real primary-source log citation was itself wrong, not just ambiguous — saved a new memory (`feedback_primary_log_can_misattribute_a_named_person.md`) on this exact failure mode: a log records what was believed at the time, not independent truth, and named-person claims need extra scrutiny beyond "the log says so." (2) PM's edit changed "Claude Desktop extension" to "Claude Desktop plugin" — per the project glossary these are genuinely different formats, so I asked rather than assumed. PM asked me to verify against the actual primary source rather than guess; checked PA's own Jun 7 log directly (`dev/2026/06/07/2026-06-07-0540-pa-code-opus-log.md:108`, "PM installed the alpha plugin on Claude Desktop" — a `.zip` DISTRIBUTION file, not an `.mcpb`) — confirmed PM's edit was correct and my *original* "extension" framing from Jul 14 was the actual error. Good reminder that "I already fact-checked this" doesn't mean past-me got everything right.
  - Updated Beat 14's calendar note with full detail, flipped status to `ready-for-docs`. Committed (`87db957ed` mechanical fixes, `dbcd2ccf4` calendar update), pushed.
- **(08:15-08:30 PT — calendar-staleness report, traced and routed to Web)** — PM separately noticed the new admin editing UI showed Tuesday's post as unpublished and Ship #051 as missing entirely. Checked the product-repo `editorial-calendar.csv` I own directly — confirmed it's fully current (both rows `published` with URLs, verified via git history). Found the likely explanation in `building-narrative-method.md`'s documented two-repo pipeline: the actual website publish path writes to a *separate* `blog-metadata.csv` in the `piper-morgan-website` repo via the `publish-to-blog` skill, and the new UI almost certainly reads that file, not mine. Since I don't have that repo checked out here, sent a memo to Web (cc PM) via `mail-send.sh` laying out what I'd confirmed, my hypothesis, and specific questions for them to trace (does the UI read `blog-metadata.csv`, is there a sync step, did it run for these two posts) — rather than guess further into a repo I can't inspect.
- **(Fire, 12:42 PT — duty-cycle-tick, WORK)** — Cron: 1 job, correctly armed. Not last fire of today (15:12/18:12/21:12 remain) → WORK PARTS. Synced clean. Beat 14 confirmed published; PM confirmed syndication done. **Web replied to the calendar-sync memo** with a real root-cause fix, more precise than my own hypothesis: the admin UI reads a *build-time copy* (`data/editorial-calendar.csv` in the website repo) made by `scripts/copy-editorial-calendar.js` from the canonical CSV — not `blog-metadata.csv` as I'd guessed. The actual bug: that copy script only worked when a sibling `piper-morgan-product` checkout existed on disk, which is true in local dev but never true on Vercel/GitHub Actions — so every real deploy silently kept a stale snapshot (last refreshed Jul 12) instead of erroring visibly. Web shipped a fix (GitHub Contents API fallback when no sibling checkout exists, using already-configured Vercel env vars) and verified live: 413 rows fetched, matching my own independent row count. Triaged the memo to `read/`, regenerated MANIFESTs, sent via `mail-send.sh`. One residual gap Web flagged and I'm noting for awareness, not action: the calendar is only as fresh as the last deploy, not live-read per page view — a future calendar edit won't show until something triggers a rebuild. Not urgent per Web's own framing; nothing further needed from me on this thread.
