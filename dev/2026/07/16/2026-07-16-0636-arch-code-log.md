# Session log — Architect (Chief Architect) — 2026-07-16

**Role**: Chief Architect (arch) · **Tool**: Claude Code Opus 4.8 · **Account**: PM backup · **Worktree**: `arch-backup-0630`. Mailbox via `mail-send.sh`. HARD RULE honored.

---

## Thursday July 16 — START ~06:36 PT (autonomous)

**Identity verified**: `arch-backup-0630`, synced. **Inbox empty.** Cron `1b4d6ef2` live. July 15 day-closed.

**Continuity**: yesterday = the #1394 B-arc (B4 ratified · B3 plan ratified · owned my §4 rail-check error → OQ-3 emit-directly · #1411 ratified + the create_issue-cohort reachability finding → ADR-077 scoped-gap documented). My scoped-gap finding traveled to today's cross-poll brief. **Parked on Lead (none blocked on me)**: create_issue migration (I ratify — the live-write reachability step), B3 TDD (emit-directly + N1/N2/N3 guards + D5 P1/P2=update_issue), #1395 rev (I ratify, fold #1410).

### Fire — 06:36 START WATCH — quiet new-day start, IDLE

Inbox empty; nothing actionable. 2 overnight commits (cross-poll brief carrying my reachability-lint scoped-gap + one other). No builds to ratify yet. All parked on Lead's next moves. No manufactured work. IDLE.

### Fire — 09:36 WATCH → #1394 B3 RATIFIED → ARCHITECTURE COMPLETE

Lead built B3 (emit-directly referent resolution) + the D5 corpus rows + filed #1412 (create_issue migration, per my ruling). **Build-ratified from code + suite-run 27 green myself.** B3 conforms to ADR-078 D2/OQ-3 on every dimension: deterministic detection (field-word = the N2 guard, elegant); emit-directly (create_issue-duplicate impossible-by-construction, test_n3 pins it); D1a owner-scoped (no-principal→None); raw original_message preserved (#1332); Stage-0 before pre_classify (surface-1 correction, D4 held — classifier sees resolved intent not state). All guard tests green (N1/N2/N3/D1a). **The #1394 arc is COMPLETE**: determination (7/12 gap-not-wiring) → ADR-078 → B4 → B3; both symptoms resolved from one primitive, classifier stateless throughout. → decisions.log + #1394 comment (architecture-complete, ready-to-close-on-probe) + memo Lead cc PM/PPM/HOST. **One pending gated step**: the D5 out-of-CI probe (confirms explicit-form live routing post-#1411; rides canonical-retest cadence, not per-fire). Next reachability step: **#1412** (create_issue migration, I ratify). Also still pending: #1395 rev (fold #1410). Inbox drained; back to IDLE.

### Fire — 12:36 WATCH → #1412 RATIFIED (create_issue reachable) + cohort-close ruling

Lead built #1412 (create_issue → rail+registry, mirrors #1411). **Build-ratified — verified the registry entry EXPLICITLY** (a passing #1283 ratchet doesn't prove registry membership, only that registered canonicals are reachable → confirmed ("QUERY","create_issue")=WORKFLOW :85 + Verb.CREATE :254 + ACTION_TO_VERB :300 + rail + 6 aliases directly; #1412 tests + verb-coverage green, run myself). **#1412 CLOSED.** Both live-write paths (create/update) now rail-reachable + lint-covered — the beta-critical exposure closed. **Cohort-close RULED**: enumerate the rest of the EXECUTION mapped_action cohort → migrate remainder (#1124 batch, non-urgent) → **the real by-construction close = a FORWARD-GUARD test** (fail if any mapped_action-dispatched handler is registry/rail-absent — stops a NEW elif-only handler reopening the ADR-077 scoped-gap; that's what lets me retire the scoped-gap note). → decisions.log + memo Lead cc PM + #1412 closed. Open threads: D5 probe (Lead sequences, closes #1394) + #1395 rev (I ratify, fold #1410). Inbox drained; back to IDLE.

### Fire — 15:36 WATCH → 5 rulings (forward-guard + both FtU enforcement lints + sequencing)

Two Lead memos, both architecture-ratification in my lane. Ruled all:
- **(A) EXECUTION forward-guard Q1 = registry-only** — the rationale is the ruling: the guard's job is MEMBERSHIP (bring mapped_action handlers into the registry), which composes with the existing D4 reachability-lint (membership→reachability). Migrate all 6 into the registry; shrink-only ratchet; retire the ADR-077 scoped-gap note once landed. Enumeration accepted (4 mode-4 todos, variant-emission-only, non-urgent).
- **(B) Lint 1 check-unscoped-reads (#1419) RATIFIED** — the owner-scoping impossible-by-construction spine (ADR-071/#1366/075-D1a/078-D1a) made systemic. 2 refinements: DERIVE the owner-bearing table set (don't hand-list — the lint would drift, make-drift-impossible one level up); indirect-scoping calibration in warn-mode + allowlist-rationale names HOW scoped.
- **(C) Lint 2 check-silent-death (#1423) RATIFIED** — the honest-degrade/no-silent-failure spine (ADR-060/#1331/#1269) made mechanical. 1 refinement: gate CI-flip on Census-A's LEGIT/NARROW/UNSWALLOW triage; # silent-ok: rationale states why the swallow is honest.
- **(D) ADR-tier**: lints ride existing ADRs; offered to author an "Owner-Scoping Integrity Contract" ADR IF #1419's audit is a systemic contract (ADR-077-for-routing analog) — Lead/PM call.
- **(E) Sequencing**: #1415 mirrors PersonalizationService (after lint-1 warn-mode); #1416/#1417 (route "connect my github"→real link_repo/OAuth not #1333-decline) = my lane, ADR-070∩ADR-077 reachability class, send-me-the-vocabulary before touching classifier vocab.
→ decisions.log + memo Lead cc PM/PA. CI-blocking flip on both lints cleared (after refinements land in warn-mode). Ping-me-to-ratify each guard/lint from code as it lands. Inbox drained; back to IDLE.

### Fire — 18:37 WATCH → #1417 ratified + ADR-079 AUTHORED (Owner-Scoping Integrity Contract)

Lead's memo: lint-refinements ack + ADR-YES (audit is systemic) + #1417 vocabulary proposal. Two deliverables:
1. **#1417 RATIFIED** (route "connect my github" → guidance lane). Verified reuse-vs-new-canonical: `get_contextual_guidance` IS a registry CANONICAL (action_registry.py:59, pre_clf-reachable) → routing to it is lint-covered, NOT another mode-4 target → **reuse confirmed clean**. Same architectural move as B3 (deterministic surface-1 fixes an LLM category-luck). Approved verb/noun sets + anti-collision (owner/repo→link_repo, the N2-discipline) + D5 rows (positive+collision+regression) + a collision unit test. **#1426 concur** (honest-decline names Piper's own surface not "e.g. GitHub" misdirect — #1331/#1333 principle). **#1415 fail-closed affirmed** (consent must never fail-open on keychain error). → memo Lead cc PM/PA (unblocked Lead's build).
2. **ADR-079 AUTHORED** (ACCEPTED v0.1) — Owner-Scoping Integrity Contract. Promotes the owner-scoping discipline (ADR-071/075/078-D1a/#1366) from case-by-case rulings to ONE systemic contract + mechanical enforcement: D1 scoped-by-construction / D2 check-unscoped-reads lint / D3 DERIVE-the-table-set (make-drift-impossible on the enforcement itself) / D4 allowlist-names-how / D5 fail-CLOSED / D6 indirect-scoping-allowlisted. Scope boundary: check-silent-death (#1423) is the PARALLEL honest-degrade contract (ADR-060), separate. Same spine as ADR-077. → decisions.log + notice to Lead (build against the D-numbers) + HOST (trust-lens welcome on D4/D5).
Inbox drained; back to IDLE. Open: ADR-079 lint build-ratifications (per rule as they land, I run the ratchet) + #1395 rev + D5 probe.

### Fire — 21:56 STOP → #1417 build verified + original_message observation banked + day-close

Inbox empty. Lead built+closed #1417 per my design-ratification (no ratify-ping needed — design was the gate). **Spot-verified the build matches**: INTEGRATION_CONNECT_PATTERNS + the collision guard labeled "Arch ruling (a), the load-bearing one" + D5 rows + routes to the get_contextual_guidance canonical. Loop closed. **Banked an architecture observation**: Lead's #1417 build flushed out the 3rd instance of the Intent.original_message contract-drift (pre-classifier Stage-1 handoff left it empty → GUIDANCE setup requests were flooring; #1332's fix never covered this path). 3 instances of "a construction path didn't set it" → the systematic fix (single authoritative setter / required constructor arg, SSOT discipline) is worth a small follow-up so the 4th doesn't ship. Not urgent; I'll raise with Lead. Day-close.

## Day arc — July 16 (Thursday; #1394 completed, then a reachability + enforcement-contract day)
The densest Lead-support day of the stint. **#1394 architecture COMPLETED** (B3 ratified AM — B4 ledger + B3 resolution, classifier stateless). Then the reachability + enforcement thread: **#1412 ratified** (create_issue reachable) + the cohort-close **forward-guard ruling** (registry-only, D4-bridge). **Both Finish-the-Unfinished lints ratified** (check-unscoped-reads + check-silent-death, with derive-the-table-set + census-gate refinements). **#1417 ratified + built + verified** (deterministic route to the guidance lane, reuse confirmed lint-covered). **ADR-079 AUTHORED** — Owner-Scoping Integrity Contract, promoting the owner-scoping discipline from case-by-case rulings to one mechanically-enforced contract. Plus #1426/#1415 affirms. The make-drift-impossible spine ran through all of it: ledger owner-scoping, reachability membership-guard, owner-scoping lint, all "vigilance → construction."

## Memory & briefing surfaces referenced this session (per #974)
**Referenced**: ADR-078 (B3 conformance) · ADR-077 + intent-routing-stack.md (reachability, the mapped_action cohort, #1417) · action_registry/workflow_entries/pre_classifier (grounding for #1412/#1417/forward-guard) · ADR-071/075/#1366/ADR-078-D1a (the owner-scoping family → ADR-079) · ADR-060/#1331/#1269 (honest-degrade → check-silent-death + #1426) · ADR-076-D4/#1382 (fail-closed → ADR-079 D5 / #1415) · #1308 (allowlist discipline → ADR-079 D4) · the multi-tenancy audit #1419 + census docs · Lead's memos (dense thread) · decisions.log (continuity).
**Loaded but not referenced**: Comms/Docs/PPM planning commits.
**Wanted but not found**: nothing — the audit + census docs gave ADR-079 its inventory.

## Sign-off
✓ All July 16 work on origin/main. PM's main checkout untouched. Worktree `arch-backup-0630`; cron `1b4d6ef2`. Inbox empty. Open (all on Lead, I ratify each): ADR-079 lint builds (per rule), forward-guard batch, #1395 rev, #1416 (github-connect routing), the #1394 D5 probe, + the original_message systematic-fix follow-up to raise.

<!-- DAY-CLOSED: 2026-07-16 -->

— Architect (Opus 4.8, PM backup), Thursday July 16 closed 21:58 PT. #1394 completed + ADR-079 authored + reachability/enforcement thread advanced. Tomorrow: build-ratify the ADR-079 lints + forward-guard as they land; #1416 vocabulary; #1395 rev; raise the original_message single-setter fix.
