# Lead Developer — Session Log — 2026-07-21 (Tuesday)

**Role**: Lead Developer | **Slug**: lead-code | **Model**: Fable 5 (claude-fable-5)
**Worktree**: lead-1452-harness (build) / main checkout mail-logs-only | **Cron**: e1106eb5 (17 6,9,12,15,18,21)
**Continuity**: 2026-07-20 DAY-CLOSED ✓ (first-green-CI day; backlog 570)

## Context at START
CI green + gate live. Queue per carry-forward: temporal re-pinning (16) → conv-manager pool cure (9) → pg15/16 diagnosis (1) → triage glances. External: Arch #1432, Exec #1386 window, PM #1424/#1427.

## Fires
- START (06:47) — inbox empty; opening wave 5 (temporal_context re-pinning).
- Fire 1 cont. — **Wave 5: temporal_context rewritten to current contracts, 7/7** (was 16 failed of a 460-line PM-034-era file testing removed machinery: get_focus_guidance, _format_calendar_context, the never-shipped dynamic-calendar/MCP path, and STATUS/PRIORITY temporal-awareness pins whose honest replacements deliberately answer differently). Live contracts captured first (service: Current Time/Day/Week + static PIPER.md patterns; TEMPORAL handler: "Today is <date>."; GUIDANCE: Right-Now framing). Pruned-test record in the file docstring per delete-discipline. Backlog −16.
  - **Wave 6: conversation_manager immunized test-side (8/8)** — autouse fixture binds the file's factory scope to a fresh per-test engine; the app-level question (should the manager itself use fresh scopes on the live path? real latency tradeoff) explicitly NOT decided here — flagged for Arch. Backlog −9 → 545.
  - **Wave 6 REVERTED (honest)**: the fresh-factory fixture passed standalone but failed in CI's sweep — pytest-asyncio 0.21.1 finalizer-loop mismatch ("Future attached to a different loop" in teardown; same version both envs → loop-STATE environmental). My process error: wave 2 was validated in-sweep before pushing, wave 6 only standalone. 6 entries restored as fixture; the standup shrink-demand taken (+transitively-fixed test off). Redo plan: NullPool engine (kills the carrier entirely) WITH full-sweep local validation first. Gate discipline held: revert before experimenting, never leave it red.
  - **🟢 Green restored** @ 9a7595480, backlog 553. Morning fire net: wave 5 held (−16), wave 6 honest-fail→revert-in-one-run (validation rule now explicit: in-sweep before push), +1 bonus standup removal.
- Fire 2 (09:47) — **Wave 7 (the big one): 222 backlog entries were ONE missing marker.** The canonical-conversations live tiers (Routing/Structure/Quality/MultiTurn/GroundTruth) drive the real classifier (LLM) but carried no `llm` marker — every keyless sweep ran and failed the whole corpus; 41% of the backlog was this single omission. Five classes marked (`pytestmark = pytest.mark.llm`); the Mocked class deliberately unmarked (its 5 entries stay — 5 pass/1 error standalone now, CI arbitrates). Keyless verification: 247 deselected ✓. Also: pg15/16 diagnosis parked (postgres:16 pull stalls locally; noted harness idea — gate reruns NEW failures with tb=short for free diagnostics). NullPool full-sweep validation running.
  - **🟢 Wave 7 CI-confirmed** @ 666f90a84 — backlog 331. 48h arc: 634→331, all CI-arbitrated.
  - **Wave 6-redo SHIPPED with validation met**: root cause was TEARDOWN-side — the async cleanup fixture collided with pytest-asyncio 0.21.1 cross-loop finalization ('another operation is in progress' during finalizer). Cure pair: NullPool async engine for the manager's internal calls + a SYNC-engine cleanup fixture (sync can't interleave with the loop — immune by construction). Validated in the sweep-order prefix that reproduced both prior failures (20s repro loop — the killed 27-min sweep was the wrong instrument). Manager file green in-context; backlog −6 → 325.
  - **🟢 Wave 6-redo CI-CONFIRMED** @ 0539b4deb — backlog 325, green. Fire 2 total: wave 7 (−222, the marker), wave 6-redo (3-attempt saga closed: teardown-side diagnosis, sync-cleanup cure, prefix-repro validation), pg15/16 parked with harness idea.
- Fire 3 (12:47) — **Wave 8 = the #1438 PRODUCT FIX**. The learning-cluster diagnosis found the dead learning loop's mechanism: `find_similar_pattern` compared the JSONB `->` rendering ('"execution"', WITH quotes) against the plain string — similarity NEVER matched, every capture created a new pattern, the upsert was dead. One-char fix (`->>`), proven live against Postgres. Riding: get_automation_patterns' lying Optional-session signature made honest; the EncryptedJSON leaf-split whitelist did its job all along (action_type deliberately plaintext for exactly this query). Test suite restructured to the LIVE thresholds (0.7 suggest gate + usage/10 volume factor are deliberate — unproven patterns stay quiet; the cycle test now EARNS suggestibility). Learning pair 22/22; live wiring confirmed (intent_service:1250). Design note flagged: ContextMatcher unknown-trigger-keys match-all (permissive default — misfire hazard for ≥0.9 auto-patterns; for the #1438/Arch design leg, not unilateral). Smoke 527. Backlog −20 → 305.
  - Wave 8 CI round: learning file failed in-sweep only — the route functions the tests call (provide_pattern_feedback) hit the poisoned shared pool INTERNALLY (error JSONResponse in-context). Applied the proven wave-6-final recipe (NullPool scope patch, no async teardown) — prefix-repro proof: zero learning lines under the poison context. Plus the diagnose harness step shipped (gate reruns NEW failures with tb=short on failure — the free-diagnostics idea, now real).
  - Wave-8 CI round 2 (with the diagnose step's first haul): the 8 learning nodes PASS in CI's isolated rerun but still fail in CI's sweep (a poison layer beyond my local prefix — third round-trip stopped per cost discipline; 8 restored as fixture, designated CI-sweep-pathology cluster; the PRODUCT fix stands proven). Diagnose also unmasked the standup test as a day-part oscillator ('Good morning' asserted, afternoon CI got 'Good afternoon') — root-fixed to the stable content assert. The diagnose step's first run already paid for itself twice.
  - Learning-pair verdict: OSCILLATING CLUSTER (different subsets fail per CI run — settings-table + run-order interference between the two files sharing TEST_USER_ID). Whole pair tagged `flaky` — the mechanism built for exactly this; stops the churn honestly. De-flake = the designated pathology session (likely: per-file unique user ids + settings isolation).
  - **v26 DEPLOYED + verified (health 200) — the learning loop is live on beta. #1438 CLOSED evidence-first** (description banner, root-cause comment, design note for Arch). Exec/PM memo sent. Fire 3 final: the #1438 product fix found+proven+deployed, diagnose harness shipped + first haul (2 oscillators), flaky mechanism absorbed the learning-pair cluster, backlog 323.
- Fire 4 (15:47) — **Wave 9: todos-persistence 12/12** (UUID-hardening fixture rot: short string ids vs the #484/#1312 UUID FK; seeded-user helpers extracted, fixture hoisted module-level, two-user isolation test seeds real users, a default-arg-masking-the-fixture bug fixed). Backlog −9 → 314. Batch glances done on the other 8 clusters (all real drift, various shapes — taxonomies recorded for next waves; connection_pool = held spatial-cascade zone, parked).
  - **methodology/ package: fix-or-delete proposal to Arch** (zero production importers, both styles; only its own rotted test tree references it — the PM-033-era coordination/verification framework, superseded by the cohort's process-level operating model). 21 backlog entries ride the eventual ruling. #1432 nudged in the same memo.
  - **THE ROOT CURE LANDED + FULL-SWEEP PROVEN**: tests/conftest.py autouse fixture backs session_scope with a session-scoped NullPool engine (mirroring the #1193 contract exactly: commit/rollback/close) — the poisoned-pool class is dead at the root. Sweep proof: pathology cluster 40+→1 line, total failures 373→250, errors 71→48, and the sweep runs in 10 min (was 27 — no pool contention). The one survivor solved the "pg15/16 mystery" with a better answer: sweep-residue crowding a generic search token (never versions at all) — unique-token fix, 11/11.
  - **Waves 10+11**: security_framework 19/19 (protocol-portability + federation suites had been erroring on a missing module-level test_client fixture — zero effective coverage); perf-indexes 13/13 (FK seeding ×11 + shared-DB-residue scoping on the analytics assert).
  - De-flaked wholesale: learning pair + todos + conversation_manager + the search test — all pathology entries off (root cure + fixes make them deterministic; CI arbitrates). Backlog → 270.
