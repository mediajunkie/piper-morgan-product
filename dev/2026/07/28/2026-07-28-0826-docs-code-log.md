# Session Log: Documentation Management — 2026-07-28

**Role**: Documentation Management (Docs)
**Date**: 2026-07-28 (Tuesday)
**Model**: Claude Opus 5
**Environment**: code
**Worktree**: `.claude/worktrees/admiring-elion-ad18c4` (Model B ephemeral)
**Session start**: 08:26 PT
**Prior session**: `dev/2026/07/26/2026-07-26-0900-docs-code-log.md`

---

## Session Objectives (PM-assigned, priority order)

1. **Today's blog post** — proof + publish ("The Trust Architecture Hardens", building, pubDate 2026-07-28)
2. **Weekly doc audit** — overdue (was due Mon Jul 27)
3. **Reply to Dispatch** — skip the syndication note; ask them to log the stale-local-checkout catch
4. **Backfill missing calendar metadata** — PM approved the 186-row altText backfill

---

## Carry-forward from Jul 26

- Caption `''` bug root-caused and fixed (3 chained defects in our own pipeline, NOT Web's). 8 posts backfilled. Website `f49d763683`.
- Triad-model correction recorded in `decisions.log` (`c1264d4a7`) — I had misread a stale commit message and reported the opposite of the truth.
- `pre-commit-reconcile-drafts.sh` exit-code fixed (`2d2d60e60`) — was hard-blocking while claiming warn-only.
- "The Ritual Becomes a Skill" altText/caption backfilled (`857c97946`).
- Meta-Observation Pattern fully distributed (Medium + LinkedIn, Dispatch, Jul 26).

---

## Work Log

- (08:26 PT) — **SESSION START**. Synced to origin/main. No Docs log existed for today (Comms 06:19 and HOST 07:07 already up). Created this log. Today's post identified from calendar: "The Trust Architecture Hardens" (building, workDate 2026-06-15→06-19, status `ready-for-docs`). Docs inbox: 17 unread — deferred until after the PM-priority blog work per explicit sequencing.

- (08:40 PT) — ❌ **Misreported Comms' status to PM, corrected within the turn.** Read Comms' session log (START-only), inferred "no editorial pass yet," told PM. Wrong: Comms committed the full pass at 08:10 (`cb66cfb00`), 16 min before my session. The calendar's structured `status` field said `ready-for-docs` the whole time — I printed those notes one command later. Comms confirmed independently and fixed their log.
  - **PM's ruling on the remedy**: my suggestion (ask Comms to log tighter) is *"just a crutch for you"* — the fix is mine. PM: *"You need a stronger rule about consulting the sources of truth and not relying on hearsay."*
  - Wrote **`feedback_read_the_artifact_not_testimony_about_it`** (memory) + indexed in MEMORY.md. The rule: for state questions read the artifact, never a description of it; structured field beats prose; when a mechanical check disagrees with a narrative-derived belief, the check wins until the artifact is consulted. Names the mechanism too — **follow a skill's step order literally**: `publish-to-blog` Step 0 is "check the calendar first," and inverting that order *is* the bug.
  - Second instance in 3 days (triad model 7/26 was the same shape), which is what makes it a pattern rather than a slip.
  - PM also ruled: **website-repo main-direct publishing is by design and correct** — the asymmetry I flagged is a non-issue, dropped.

- (09:05 PT) — **"The Trust Architecture Hardens" — template audit + Docs final proof.** 12 of 14 checks PASS. Found 4 unambiguous mechanical errors Comms' pass missed, fixed and pushed (`1101223a9`):
  - L11 tense break: `fixes` in an otherwise past-tense sequence → `fixed`
  - `chief experience office (CXO)` → `officer`. **Verified against house convention rather than assuming**: only "office" across all drafts, vs 5× "chief innovation officer (CIO)"
  - L41 stray commas turning a 2-item appositive into a 3-item list
  - L15 missing commas around "if ever"; trailing whitespace on 5 lines
  - Two judgment calls escalated rather than decided silently — PM cleared both: word count 2,319 (over the 1,600 flag; PM noted creep to tame at drafting stage) and the L45 negation-reveal (earned in this case).

- (09:30 PT) — **PUBLISHED** `https://pipermorgan.ai/blog/the-trust-architecture-hardens/` (`d317291bddb8`, workDate 2026-06-15, pubDate 2026-07-28). Dry-run first; website `4d21bcccb8`; calendar → `published` + altText/caption backfilled (`0e1133d1c`). **Live-verified all three fixes serving.** Caption landed correct on first pass — `"I haven't shouted 'Look out!' all week"` — the first publish since Saturday's parser fix, confirming it end-to-end on new content.

- (09:45 PT) — **Weekly docs audit #1456 executed and closed** (one day late; generated Mon, run Tue). All mechanical checks run directly. **PASS**: briefing 2 days fresh · 0 broken links across ADRs/patterns/briefings/methodology · 12/12 ROSTER briefings present · version sync · 0 backup files · 0 beta blockers. **FINDINGS**: 🔴 omnibus gap Jul 24–27 (31 session logs unsynthesized — Docs' own deliverable) · 97 docs >30d asserting current-state language · 7 issues without milestone · INDEX↔NAVIGATION one-way xref · 4 real duplicate filenames · 1 stale published draft · `BRIEFING-piper-alpha.md` unlisted in ROSTER.
  - **Near-miss worth recording**: first pass flagged the pattern count as off-by-one (75 files vs README's "74"). Re-checked before "fixing" it — the README is right; 75 files = patterns 001–074 **plus** the 000 template. A naive `ls | wc -l` produced a false finding. Same rule as above, caught this time.
  - **#1402 (Jul 13) closed as superseded** — never executed, 69 boxes marked `N/A:superseded` per close-issue-properly Example 5. Flagged the recurrence: 2 of the last 6 weekly audits went unexecuted; if that holds, the cadence generates orphans faster than findings.

- (09:52 PT) — **Replied to Dispatch** (`~/Development/dispatch/mail/`, flat dir per DIRECTORY.md, no git commit). Recommended **skipping** the syndication note — it would duplicate four structured fields in prose, and that column already carried a stale pairing claim for weeks. Asked them to **log their 270-commits-behind catch** properly (session log + decisions.log): a stale checkout edits cleanly and reverts silently, and it's the write-side instance of the same rule PM just gave me. Corrected the caption-bug attribution for them.

- (10:05 PT) — **Calendar metadata backfill: 333 empty altText/caption fields filled** from the published website CSV (`296aaf523`). Fill-empty-only; checked clobber risk before running (333 fillable, 18 populated-but-disagreeing left untouched, 0 unmatched). Whole-file verified: 417 rows before and after.

- (10:15 PT) — 🔴 **Found and repaired a column shift on the Weekly Ship #050 row** (`fcfc95039`) — surfaced by the 18 disagreements. Three fields shifted one right: `notes` held a duplicate draftPath, `altText` held 1,000+ chars of editorial prose, `caption` held the real alt text. **Field count stayed at 18, so no count check would ever have caught it** — this is exactly the positional-index failure `update-calendar` v1.2 documents from the 2026-07-14 incident. Verified direction against the website's `imageAlt` before writing rather than inferring it. Scanned all 417 rows; 3 others tripped a naive >320-char heuristic and were individually cleared as genuinely long alt text.

- (10:25 PT) — **Memo to Comms (cc PM)** on the role-gloss drift (`3d1efc325`), per PM's deferral. Nine variants of the role gloss in circulation across four axes (suffix/capitalization/acronym/whether-glossed). The point that matters: the `office`/`officer` typo survived a full editorial pass *because* there's no canonical form to check against — the drift disables the check. Suggested lowercase + `officer` (plurality, 5 uses) written into the voice guide so `check-acronyms.py` can enforce it. Also flagged 2 stale carry-forward items (caption bug is fixed and was never Web's; log-lag).

---

## Findings Flagged

1. 🔴 **Omnibus gap Jul 24–27** — 31 session logs unsynthesized, 4 days. Docs' own core deliverable. Historical coverage is otherwise excellent (409 logs, 2 gaps >2d in 14 months, both Jun 2025), so this is a recent lapse, not chronic. **Awaiting PM's call on priority.**
2. 🔴 **Weekly-audit orphan rate** — 2 of last 6 unexecuted (#1402, and #1049 in May). Cadence or ownership worth revisiting.
3. 🟡 **18 calendar↔website metadata disagreements** left untouched by the backfill. Three classes: substantively different alt text (4), smart-vs-straight quotes (3), and **website captions missing their quotation marks** (~46 total, house style says captions keep them — the calendar is right and the live site is wrong).
4. 🟡 **97 docs >30 days asserting current-state language**; `docs/internal/planning/current/` is itself now a misleading directory name (oldest items 310d).
5. ✅ **Column-shift class is live** — found one real instance today. Field-count checks cannot detect it; only semantic checks can.

## Issues Closed This Session

- #1456 (weekly docs audit — executed with evidence)
- #1402 (weekly docs audit — closed as superseded, never executed)

- (10:40 PT) — **START-routine fix shipped** (`68ce7d0eb`): weekly docs audit now surfaced by the SessionStart hook. **Fires Mon–Thu, not Monday-only** — PM asked for a Monday check, but Monday-only would not have caught the case that prompted it (the 7/27 audit went unrun Monday; PM raised it Tuesday). The failure mode is silent drift *past* Monday. Stops Thursday, when the right move becomes close-as-superseded. No network call, consistent with the hook's other sections. Trimmed the wording after measuring stdout at 495 chars against a 500 budget — the first draft risked pushing the ROLE line off the end. Verified behaviorally: `bash -n` clean, exit 0, line renders, ROLE line survives.

- (10:50–14:30 PT) — 🔴 **OMNIBUS GAP CLOSED — all four days backfilled.** ~75,000 words of source across 31 session logs.

  | Day | Sessions | Type | Result |
  |---|---|---|---|
  | Jul 24 | 5 | EXECUTION | `cf6682da1` |
  | Jul 25 | 10 | COORDINATION | `cac36d28b` |
  | Jul 26 | 11 | COORDINATION | (roll day, full cohort) |
  | Jul 27 | 5 | COORDINATION | `152 lines, 80 entries` |

  Coverage verified after: **413 omnibus logs, zero gaps since June 2025.**

  - **Jul 24** — migration prep becomes real work; PM reframes the risk from path to account; three portability boundaries named as distinct; PM ratifies worktree isolation.
  - **Jul 25** — the Amber cutover. 10 logs cover 8 roles (CIO and HOST each crossed mid-day). Seven findings, six sharing one shape. The day's durable output is a rule: *a safety net you haven't seen fire is a claim, not a mechanism*, plus HOST's extension that the **diagnosis** of a silent mechanism carries the same evidentiary burden.
  - **Jul 26** — the roll (2→7 of 10). The hook-intermittency investigation resolved by distributed work, with every rival hypothesis **refuted by its own proposer** before Web found the mechanism. Lead pulls the 43% methodology lever same-fire (backlog 94→56).
  - **Jul 27** — a day about instruments, where every fix shipped contained the defect it was written to fix. m-44 filed. The watchdog found to be **alerting on compliance**.

  **Shape-B activity-log reconciliation**: 31 rows appended (1727→1758), whole-file verified.

  **Two honest notes on method**:
  1. **All four land under methodology-20's line targets while meeting its content targets.** Jul 25 has 97 timeline entries against the "100+" COORDINATION rule and every exec-summary bullet band at or above range, but 179 lines against a 450–600 target. The gap is formatting density — the line budget assumes ~2.5–3 lines per entry, mine are one dense line each. **Flagged in each file rather than padded.** The two targets measure the same thing in incompatible units; worth a methodology-20 refinement.
  2. **Jul 27's gate flagged a real source defect rather than papering it**: Lead's log is 8 lines and ends after Fire 2 with no `DAY-CLOSED` marker. Content corroborated from Exec's and CIO's independent verifications, and the gap named in the omnibus.

- (14:40 PT) — **Dispatch reply sent** (`~/Development/dispatch/mail/`, flat dir, no git commit per DIRECTORY.md). Recommended **skipping** the syndication note — it would duplicate four structured fields in prose, and that column already carried a stale pairing claim for weeks. Asked them to **log the 270-commits-behind catch** properly: a stale checkout edits cleanly and reverts silently, and it's the write-side instance of the rule PM gave me this morning. Corrected the caption-bug attribution for them.

- (14:50 PT) — **Calendar backfill: 333 empty altText/caption fields filled** (`296aaf523`) from the published website CSV. Fill-empty-only; clobber risk checked before running (333 fillable, 18 populated-but-disagreeing left untouched, 0 unmatched). 417 rows before and after.

- (15:00 PT) — 🔴 **Column shift found and repaired on the Weekly Ship #050 row** (`fcfc95039`), surfaced by those 18 disagreements. `notes` held a duplicate draftPath, `altText` held 1,000+ chars of editorial prose, `caption` held the real alt text. **Field count stayed at 18, so no count check could ever have caught it** — the positional-index failure `update-calendar` v1.2 documents from 2026-07-14, still live. Direction verified against the website's `imageAlt` before writing. Scanned all 417 rows; 3 others tripped a naive >320-char heuristic and were individually cleared as genuinely long alt text.

- (15:10 PT) — **Memo to Comms (cc PM)** on role-gloss drift (`3d1efc325`), per PM's deferral. Nine variants across four axes. The point that matters: the `office`/`officer` typo survived a full editorial pass *because* there's no canonical form to check against — **the drift disables the check.** Also flagged two stale carry-forward items.

---

## Findings Flagged

1. ✅ **Omnibus gap closed** — was the audit's headline finding; 4 days, 31 logs, resolved same day.
2. 🟡 **Weekly-audit orphan rate** — 2 of last 6 unexecuted (#1402, #1049). Now mitigated by the Mon–Thu hook reminder, but the cadence may still deserve review.
3. 🟡 **methodology-20's line targets vs entry-count targets are in incompatible units** — an omnibus can satisfy one and fail the other while being correct. Candidate refinement.
4. 🟡 **18 calendar↔website metadata disagreements** left untouched. Three classes: substantively different alt text (4), smart-vs-straight quotes (3), and **website captions missing their quotation marks (~46)** — house style says captions keep them, so the calendar is right and the live site is wrong.
5. 🔴 **Column-shift class is live**, not historical — one real instance found today. Field-count checks cannot detect it; only semantic checks can.
6. 🟡 **97 docs >30 days asserting current-state language**; `docs/internal/planning/current/` is itself now a misleading directory name.

## Issues Closed This Session

- #1456 (weekly docs audit — executed with evidence)
- #1402 (weekly docs audit — closed as superseded, never executed)

## Memory & Briefing Surfaces Referenced This Session

### Referenced
- **`feedback_read_the_artifact_not_testimony_about_it`** — written this session at PM's direction, then immediately load-bearing twice: caught a false pattern-count "finding" (README was right; my `ls | wc -l` counted the template) and three false column-shift positives (genuinely long alt text).
- `template-audit` skill — check 11 caught the post's only real defect
- `publish-to-blog` skill — Step 0 ordering (the thing I inverted this morning), mandatory dry-run, `--work-date` discipline
- `create-omnibus` skill + `methodology-20` — format selection, line limits, the cross-reference and cross-role-assertion gates
- `close-issue-properly` skill — Example 5 (close-as-superseded) applied to #1402
- `update-calendar` skill v1.2 — the positional-index incident it documents is exactly what I found on Ship #050
- `feedback_csv_edit_by_name_never_position`, `feedback_commit_only_own_files`, `feedback_pause_before_irrevocable_actions`

### Loaded but not referenced
- Cross-pollination brief; BRIEFING-ESSENTIAL-DOCS.md; most non-comms-tagged MEMORY.md entries

### Wanted but not found
- **A cheap way to detect column shift in a CSV.** Field count can't; semantic anchors caught this one only because a path in `notes` is obviously wrong. A per-column type/shape assertion (draftPath matches a path pattern, altText under N chars, caption starts with a quote) would make the class detectable rather than lucky.

<!-- work log continues -->


