# Session Log: 2026-07-07-0647-lead-code

**Role**: Lead Developer
**Model**: Claude Code (Sonnet 5 → **Fable**, PM-switched ~10:41 AM — experimental boost, subsidized-access window ending today)
**Date**: Tuesday, July 7, 2026
**Start Time**: 6:47 AM PT

## Session Objectives

1. Duty-cycle autonomous operation — drain mail, advance unblocked owed work.
2. Epic A (#1304): implement + close once/if PM confirms the visible-only variant (CIO's recommendation, awaiting PM's explicit go-ahead — see 7/6 carry-forward).
3. Candidate: refresh `dev/active/lead-standing-items.md` (flagged stale 6/21, if nothing more urgent is queued).

**Active pattern families this session**: Multi-Agent Coordination (029/059/010/021/037) — cross-role memo/mail threads; Completion Theater (045/046/047/049) if any issue-closure work comes up.

## Work Log

### 6:47 AM - Session Start (START, duty-cycle-tick)

Cron fired at the 06:17 slot (running ~30 min late — normal jitter/idle-wake timing). Exactly one cron job found (`773e1e46`), no dedup needed. Synced clean (`git fetch` + `merge origin/main`, fast-forward, no conflicts) — pulled in 3 new commits since last night's close: a watchdog "duty-cycle stall — arch" alert (not addressed to lead, not actioned), Docs's cross-pollination brief, and Docs's Jul-6 omnibus.

**Step-0 self-heal check**: verified 7/6's log has the `<!-- DAY-CLOSED: 2026-07-06 -->` marker (`grep -l` confirmed) — prior day STOPped properly, no retroactive close needed.

Lead inbox empty (0 unread) at session start. Creating this log now per START procedure.

### 7:05 AM - Standing-items refresh (task loop, (0,0) low-pri item)

Mail empty; checked #1304 directly on GitHub (not just memory) — unchanged since 2026-07-05, still 4/5 AC done, 1 PM-gated. No new PM message this fire. Per last night's carry-forward ordering, picked up the flagged-stale `dev/active/lead-standing-items.md` refresh (last touched 2026-06-21).

**Verified every referenced issue against live GitHub state** (`gh api .../issues/N --jq '.state'` for 21 issues) rather than trust the doc's claims. Found the doc was significantly stale in exactly the way flagged: **#1232 (WS-5 connector contract), #1185 (identity core), and #1286 (D2 design-system) had all closed** since the 6/21 refresh — #1232 on an unrecorded date, #1185 likewise, #1286 confirmed closed 2026-06-21T20:22:00Z (same day as the doc's own "last refreshed" stamp — the doc was written just before that closure landed, not after). The doc's "Sprint position" section still described Phase-1 as Arch-ratify-gated, when in fact the entire foundation phase closed and the sprint has moved into connector-port execution (4/8 ported as of yesterday).

Rewrote the doc section-by-section: Sprint position (now reflects port-execution phase, not foundation-gating), Open-issue surface (closed items removed from "open" framing, connector-port status folded in, Beta Blockers epic summary added since that's now the live front), Cross-agent threads (the 3 stale threads — Arch/#1232, CXO/Slice-2, PM/#1286-UAT — all resolved/moot now that #1232/#1286 closed; replaced with the 2 threads that are actually current: Epic A's PM-gate, #1278's board-Status-field question), Recurring infrastructure-health checks (cron ID/expression updated to current, added a note that this doc's own staleness went 16 days uncaught — no enforced trigger catches it, flagging that gap rather than pretending this refresh closes it permanently). Added 2 new pinned discipline reminders from yesterday's incidents (GitHub Projects v2 field-option full-replace danger; commit-message keyword-adjacency-to-#N danger) — both already in CLAUDE.md, cross-referenced here since this doc is what a future fire's task-loop actually reads.

Kept the doc's own structure (durable recurring-signals checklist, distinct from carry-forward's per-session queue) rather than restructuring — the shape wasn't the problem, the content staleness was.

### 7:20 AM - Found and fixed a live doc-vs-GitHub drift in PPM-owned beta-blockers.md

While cross-checking standing-items against GitHub, opened `docs/internal/planning/beta-blockers.md` (PPM-owned, not mine) to check for any other unblocked Lead-owned work before treating the queue as drained. Its Epic D section and total-open-issue count (19) still described #1278 as "shows CLOSED... flagged, not confirmed legitimate" — text written 2026-07-06 ~17:30, **before** last night's PM-directed reopen. Verified #1278's actual live state (`gh api .../issues/1278` → `open`) rather than trust either the doc or my own memory of having reopened it.

Corrected in place (agent-who-notices-updates-stale-info discipline — PPM owns the doc, but the drift was live and I held the answer): Epic D's #1278 paragraph rewritten to describe the full 4-hop timeline (auto-close → PPM reopen → unexplained second close → last night's PM-directed reopen) and to state plainly this is real unfinished hosting work, not a bookkeeping gap. Total-count line corrected 19→20 open. Two new change-log entries added (2026-07-06 documenting the reopen action itself, 2026-07-07 documenting this doc-correction pass) rather than silently editing history — the change log's own discipline throughout this doc is to show the correction, not just the corrected state. "Last updated" banner bumped with an explicit attribution note.

Did not touch the board's Status field (still shows "Done" post-reopen, per last night's carry-forward) — that stays PM-gated, only the prose description was corrected.

### 9:47 AM - Fire 2 — Arch's ADR-076 BUILD ratification (clean D1-D6); filed #1374 (mail-send.sh reconcile bug); closed #1370 with live verification evidence

Synced clean, pulled in several commits from exec/host/arch overnight — nothing conflicting. One unread: Arch's ADR-076 build ratification (`web/middleware/usage_cap_middleware.py`, #1370) — **clean against D1-D6**, one documented-and-blessed deviation (fixed-window rate limiting instead of D1's named sliding-window/token-bucket, correct call for alpha). The one open item Arch flagged: "verification-not-architecture... your AC, not mine" — staging/live load verification. Triaged the memo to read/.

**Hit the same mail-send.sh reconcile edge case as yesterday, second occurrence in two days, same exact trigger.** Traced the actual mechanism this time instead of just working around it again: the push uses `commit-tree` directly (never touches local `HEAD`), so when the caller passes both halves of an in-progress `git mv` (old path + new path) in one call, the reconcile loop's two branches — "restore from index if in HEAD" / "rm if not in HEAD" — each make an assumption that only holds for a *pure* add or a *pure* modify, not a same-invocation rename split across both. Filed **#1374** with the full trace (reproduction steps, root-cause hypothesis read from the actual script lines, the manual fix that's worked both times, suggested fix directions) rather than silently work around it a third time — two occurrences with an identical, precisely-characterized trigger is exactly the kind of thing "not my problem" discipline says to file, not bury.

**Did the flagged live-verification AC properly, not superficially.** Started a real local server (env-var-stripped per the documented gotcha) + real Redis (was not previously running — confirmed via `docker compose ps` before starting, so nothing pre-existing was disturbed). Found the safe zero-cost probe empirically before hammering anything: `GET /api/v1/intent` is auth-exempt (`EXEMPT_OPTIONAL_AUTH_PATHS`) so unauthenticated requests reach the usage-cap middleware, and it's POST-only so it 405s at the router — verified with one sanity request (405, no LLM cost) before running the real test.

Wrote `adr076_load_verify.py` (scratchpad) and ran 3 tests against the live server:
1. **Rate limit**: 105 sequential requests, one principal — 1-100 pass (405), 101+ return 429 with the exact documented JSON shape + `Retry-After: 60` header.
2. **Concurrency cap**: 12 requests, each spoofing a distinct `X-Forwarded-For` (12 distinct principals) — tripped at the 11th *overall* distinct principal, not the 11th within this specific test. My test script's own assertion was wrong (assumed isolation from Test 1), but the middleware wasn't: Test 1's own principal (`ip:127.0.0.1`) was still a legitimately-active session in the *same* Redis-backed gauge, correctly occupying 1 of the 10 slots. Confirmed via direct `ZRANGE usage_cap:active_sessions 0 -1` — exactly 10 members, matching `MAX_CONCURRENT_SESSIONS`. Didn't just accept the "failed assertion" at face value — traced it to ground truth before concluding anything, and it turned out to be a *better* proof (cross-request-sequence, instance-wide correctness) than an artificially isolated test would have given.
3. **Fail-closed**: `docker stop piper-redis` mid-test, one request, `docker start` after — 503 with the distinct `capacity_check_unavailable` message (not conflated with either cap-exceeded message), matching D4/D5 exactly.

Updated #1370's AC checklist (the one remaining unchecked item, now `[x]` with the full evidence inline — close-issue-properly discipline: description first, comment second), added the evidence comment, closed the issue. **Caught my own get-ahead-of-myself mistake**: the closing comment said "Redis flushed of test keys, no state left behind" before I'd actually done it — caught immediately on re-reading my own comment, did the actual flush (`DEL usage_cap:active_sessions` — the rate-limit keys had already TTL-expired on their own) before moving on, so the claim was true by the time anyone would read it, but the sequencing itself was a real near-miss worth naming.

Cleanly tore down the test environment after: killed the local server, `docker compose stop redis` (restoring exactly the pre-test state — only `piper-postgres` running, matching what `docker compose ps` showed before I started).

**Closed the loop on ADR-076's own status line**, which still read "staging/live verification not yet done" — stale the moment #1370 closed. Updated to "implemented and closed... fully closed, no open items," folding in both Arch's morning BUILD-ratify and this morning's live-verify. No other stale staging-verification references found elsewhere in the doc.

### 10:00 AM - New mail mid-fire: Arch's Component B (#1373) BUILD ratification, "impossible-by-construction." Did the same live verification, hit a real learning along the way.

Second memo arrived while wrapping up: Arch ratified Component B (ADR-075 personalization store) — clean against D1-D5 + OQ-1/2/3, and structurally "impossible-by-construction" for the #1366 privacy leak (the FK-constrained, always-scoped repository methods mean an unscoped read/write literally can't be expressed). Same closing line as ADR-076's memo: "Open = the usual staging/live verify (your AC)." Triaged to read/ — **3rd occurrence of the mail-send.sh reconcile edge case this session, same exact trigger/symptom/fix** — added a confirming comment to #1374 rather than a new issue (3-for-3 on every triage-move this session is strong signal this isn't rare).

**Did the live verification properly, and it surfaced a real methodological correction along the way — worth naming precisely.** First attempt used a bare `uuid4()` as the test principal and `str(pm_id)` where `pm_id` was already `None` — both wrong in ways that would have produced a false-positive "pass" if I hadn't checked the actual mechanism: the bare UUID correctly failed the schema's real `owner_id` → `users.id` foreign key (confirming the FK constraint works, which is itself useful evidence, but not what I intended to test), and `str(None)` = the literal string `"None"`, which isn't a real test of PM's principal at all — it would have "passed" for the wrong reason (malformed input correctly degrades to file-based, not "PM's real identity resolves to file-based"). Investigated why `resolve_pm_owner_id()` returned `None` before working around it: this local dev Postgres has no real PM identity row at all — only test/fixture users from prior test runs (`test_user_a`, `test_user_b`, various isolation-test fixtures). Confirmed this via a direct `users` table query, not assumption.

Rebuilt the test correctly: created one real, throwaway user via `INSERT INTO users` (satisfying the actual FK, not working around it), ran the verification against that real distinct principal, and **honestly skipped** (not faked) the PM-specific sub-cases (D3 no-regression, notice-suppression for PM) with an explicit "SKIPPED, no PM identity resolvable in this local DB" message rather than silently passing something that wasn't really tested. 5 of 7 planned assertions ran for real and all passed: `user_id=None` → file; real distinct principal → scoped + lazy-seeded (real, inspectable DB row, `is_seeded_default=True`); first-response notice fires the exact CXO text once, `None` on repeat. Cleaned up fully after (deleted both the personalization row and the throwaway user; verified 0 remaining in both tables via a fresh query, not just trusting the script's own claim).

Updated #1373's AC checklist precisely: checked the live-verification item with the honest scope note (what was and wasn't tested, and why) rather than overclaim "full multi-user request flow" verification when it was actually a direct-service verification with one legitimately-untestable sub-case in this environment. Marked Component C's deferred item `[⏸]` (not `[ ]`) per the deferred-AC discipline. Added the evidence comment, closed #1373. Updated ADR-075's own status line the same way as ADR-076's — "implemented, BUILD-ratified, live-verified, and CLOSED," noting the one legitimate scope caveat rather than smoothing over it.

**The server-owned-state family (ADR-070/071/075) is now fully ratified AND fully implemented** — all three completed as of this fire.

### 10:35 AM - Fire 3 (arrived early — ~50 min after Fire 2, not the scheduled 12:17 slot; ran the procedure faithfully anyway per idempotent-by-design). Picked up a genuine IDLE-time low-pri item: #1105 (LLM keychain UI), confirmed NOT a regression, found+fixed a real dead-code bug via live browser verification.

Mail empty, no new commits since my own last push. Per "IDLE means do low-priority work, not nothing," checked the M5/backlog items from this morning's standing-items refresh rather than reflexively declare quiet-hold. #1105 stood out: `beta:auth-lifecycle` labeled (part of Epic E, not pure backlog, despite standing-items filing it under "M5/later"), and a prior investigation (May/June, same shared git identity so unclear which agent) had already done real legwork — ruled out #1104, confirmed keychain state was fine, traced the wizard's check flow, reframed from "regression" to "possibly UX/wizard-flow issue," demoted P2→P3, and left a clear next step: reproduce live, observe button behavior.

**Read the code first, found a real, precise, confirmable bug**: `checkKeychainAvailability()` (fires once on entering setup step 2) loops over `['openai','anthropic','gemini','notion']`, querying `.keychain-btn[data-provider="${provider}"]` for each. But openai/anthropic/gemini share ONE dynamic button (`#keychain-llm-btn`) whose `data-provider` starts empty and is only set by a SEPARATE mechanism (the LLM dropdown's own `change` handler, which sets the attribute AND calls its own direct-check function `checkKeychainForProvider`). The loop's query for those 3 providers can never match — confirmed this is real dead code, not a hypothesis.

**Did NOT stop at the code trace — went to a real live browser reproduction**, since a UI question needs real rendering, not just static analysis. Created `.claude/launch.json` (new, reusable infra — `env -u ANTHROPIC_*` wrapping the venv python, matching the documented server-restart gotcha) and used the Preview tools for the first time this session: started the real server, navigated the actual setup wizard (intro screen → step 1 system-check → programmatically clicked past ChromaDB/Temporal not being required for this check, since starting those was out of scope), reached step 2, and inspected the LLM keychain button's real DOM state before and after selecting a provider from the dropdown. **Result: the dropdown-driven path works correctly** — selecting Anthropic or OpenAI correctly reveals the "Use Keychain" button (`hidden=false`, `data-provider` correctly set), backed by a real, working keychain read (independently confirmed via direct curl to the endpoint: `exists:true` for openai/anthropic/notion, matching real `security find-generic-password` state). Screenshot captured as visual confirmation.

**This flips the original report's framing entirely**: the keychain integration was never broken. PM's May 19 "regression" is most likely a discoverability gap (the button is invisible until a provider is picked from the dropdown — the whole key-input section is `display:none` before that), not a functional bug — exactly the prior investigation's own hypothesis #1, now confirmed rather than speculated.

**Fixed the one real, confirmed bug**: removed the 3 dead entries (`openai`/`anthropic`/`gemini`) from the loop, leaving only `notion` (the one provider that loop actually affects), with a comment explaining why and pointing at the real mechanism. Zero behavior change for anything that was actually working — pure removal of misleading dead code. Re-verified live post-fix: Notion's button still shows correctly, the dropdown-driven path still works for both LLM providers, zero console errors, screenshot re-confirmed. Ran the full frontend Jest suite (94 tests, none of which cover this specific function) — clean, no regressions.

Updated #1105 with a STATUS banner + comprehensive evidence comment (the full reproduction steps, the code trace, the fix, before/after verification) and closed it — the original P2 "regression" claim is resolved as not-a-regression, with the one real bug found along the way fixed and verified, not just asserted.

**Environment discipline held throughout**: killed the manually-started server before switching to `preview_start` (to avoid two competing processes on the same port), stopped the Preview-managed server + `docker compose stop redis` after, restoring exactly the pre-investigation state (only `piper-postgres` running). Kept `.claude/launch.json` as durable, reusable infra for future UI verification work rather than treating it as scratch — it's exactly the kind of thing the Preview tool's own docs expect to persist in the repo.

### 10:41 AM - PM engaged directly (model switched to Fable mid-conversation). Three questions answered; two real misses owned.

**PM's "you have mail" — both attached memos confirmed STALE**, not missed mail: `memo-arch-...1366-componentA-RATIFIED...` and `memo-host-...1344-mapping-complete...` both live in `mailboxes/lead/read/` on origin/main (triaged 7/6, commits `19341fefe` / `acb3a2310`); PM was seeing them in `inbox/` on a local main checkout that's **170 commits behind origin**. The #1344 memo is stale in *content* too — its "Rebecca Refoy email" blocker cleared the same day (HOST → Exec cc PM, `rrefoy@gmail.com` recorded, "batch 1 now 10/10 READY TO SEND").

**Sync-pm-local: ran it, it skipped, diagnosed why precisely (read-only — never touched PM's tree).** PM's checkout has 24 uncommitted changes: 18 are MANIFEST regen-noise (the documented known-limitation class), 3 are PM's real draft prose (`docs/public/comms/drafts/` — untouchable), 3 untracked (a memory file + 2 new drafts — also PM's real work). The script's conservative any-uncommitted-changes→skip means it's currently no-oping *every* time. **Owned my own miss**: CLAUDE.md says run `sync-pm-local.sh` once per fire — I ran it zero times across today's three fires; running it would have surfaced the no-op warning days earlier. Offered PM the two-part fix: (immediate) authorize the surgical explicit-path MANIFEST clear (the exact form the HARD RULE permits) + re-run sync; (durable) CIO refines the script — either the PM-blessed MANIFEST special-case, or attempt-ff-and-let-git-refuse-only-on-real-collisions, which is less conservative than blanket-skip but still ff-only-safe.

**PM's "what is this about M5?" — owned two layers of error.** (1) The label: "M5" is dead; my standing-items doc inherited the bucket name from its 6/21 version and this morning's refresh kept it. GitHub-verified now: #1144/#1131/#1300 = Production, #1162 = Ongoing, and **#1105 = MVP milestone** — i.e., the item I actually worked WAS current-sprint (Beta Blockers Epic E), mislabeled by my own doc as backlog. Fixed the doc. (2) The bigger miss: **I was never blocked on the sprint.** My "(0,0) queue" reports reflected the carry-forward's active-threads view (Epic A PM-gated, Epic C's remaining ports gated), not the sprint board — Epic F (#1279/#1285/#1216-interim/#1256), Epic E (#441+#1261), and Epic G (#1283/#1324) are full of unblocked, well-scoped items. Added a warning block to standing-items so the task-loop checks `beta-blockers.md` before ever declaring (0,0) again. **Epic F drain starts immediately** — no PM input needed for it.

**Movement requested from PM** (relayed in-conversation): (1) one-word go/no-go on #1304's visible-only required-check variant (CIO's recommendation — closes Epic A); (2) confirm the 7/5 "not in scope for 1.0" ruling on cicd/devenvironment/gitbook/linear applies to #1317 so I can descope + close it at 4/4 real connectors ported, leaving #1220 as Epic C's true remainder; (3) #1278's board Status field (shows "Done", issue is open) — PM flips it or authorizes me the one edit; (4) rider: #358's final step travels with the next alpha deploy.

### 10:50 AM - PM answered all five — executed the batch. Epic A CLOSED, #1317 descoped+CLOSED, PPM memo sent, #358 deploy-reminder rig installed. Beta Blockers: 20 → 17 open today.

**(1) #1304 — "Go" received, implemented, CLOSED.** `Security Test Suite (Postgres)` (literal check-run name confirmed from real run 28757694382, not guessed from the workflow file) is now a required status check on `main`, `strict: false`, `enforce_admins` untouched at false. The mechanics were the exact full-replace trap class from the 7/5 Projects-v2 incident: `required_status_checks` didn't exist yet, so the narrow sub-endpoints weren't available and the full `PUT /branches/main/protection` (documented full-replace) was the only path. Handled per the standing guardrail: captured the complete pre-change protection object, reproduced every existing setting in the PUT body, verified field-by-field after — **the only delta is the new required-check block**; all four PR-review settings and every branch flag byte-identical, `enforce_admins` still false. Closed per close-issue-properly (5/5 AC checked in the description FIRST, evidence comment, close, state verified). **Epic A is fully done.**

**(2) #1317 — descope confirmed by PM, executed, CLOSED.** PM's ruling recorded on the issue in plain terms: cicd/devenvironment/gitbook/linear are not in scope for 1.0 and never were — not even the next priorities, never PM-ratified scope (riffed into lists at some point without a ratification decision). Descoped ≠ deferred; a future want starts as a fresh product decision. All 4 real connectors (GitHub/Calendar/Notion/Slack) ported — 4/4 of actual scope. **PPM memo sent** (`memo-lead-to-ppm-cc-pm-pa-1317-descope...2026-07-07.md`, cc PM + PA, via mail-send.sh — clean send, no reconcile edge case: all-new-files, consistent with #1374's rename-only diagnosis) so sprint/roadmap artifacts record this as always-the-intent. Epic C's remainder = **#1220 only** (write-path credential migration — workable by me without further input).

**(3) #1278 — no board move.** PM moved it to Sprint Backlog and said move to In Progress *if I'd started it*. I have not started it (I only reopened it last night) — Sprint Backlog is the correct placement; told PM so rather than performing a move that would misrepresent state.

**(4) #358 — answered honestly: NOT automatic.** Two manual actions ride the next alpha deploy (set `ENCRYPTION_MASTER_KEY` on the droplet; run `scripts/backfill_encrypt_content_358b.py`). Existing backstops verified in code before answering (one-time plaintext-fallback warning at write; backfill refuses to run keyless; `DecryptionError` on keyless reads of encrypted rows) — good, but none is a deploy-time prompt, and deploy.sh lives on the droplet (out of in-repo reach, PM-gated infra anyway). Installed the proportionate mechanism instead: a flagged **NEXT ALPHA DEPLOY checklist comment on #358** + a **cross-link on #1299** (whose own remaining item requires the same next real deploy — coupling them saves a second deploy touch).

**(5) "M5" title cleanup** — PM removed the stale prefix from the issue title; acknowledged.

**Same-session beta-blockers.md update** (the doc's own discipline: every removal is a same-session edit): Epic A marked CLOSED, Epic C down to #1220 with the descope banner, Epic E down to #441+#1261 (#1105's closure now reflected), the stale #1366 note refreshed (Component B is built/ratified/live-verified/closed — the note still said "blocked on ADR-075, not yet authored"), total 20 → 17 open, Recommended-sequencing item 1 corrected, changelog entry added. Committed with this log update.

**Next: Epic F drain begins — #1279 first** (GitHubIntegrationRouter missing `close()`, per-request aiohttp session leak).

### 11:25 AM - Epic F #1: #1279 FIXED + CLOSED (session leak). Real blast radius found to be ~10x the issue's list — follow-up filed as #1376. Beta Blockers: 17 → 16.

**The fix**: `GitHubIntegrationRouter.close()` added — the MCP adapter already had an idempotent `disconnect()` that closes and nulls its aiohttp `_session`; the router just never called it. `close()` delegates to it, never raises (cleanup must not mask the request's outcome), resets `_initialized` for clean reuse. Verified the spatial fallback holds no session before deciding the adapter was the whole leak (grep for aiohttp/httpx/requests state — none).

**Three call sites, not the issue's two** — investigate-before-extending found `PlaceProvider` in `feed_factory.py` (postdates the issue) constructs a candidate router that, worse than the other two sites, initialized BEFORE its `is_configured` check. All three now close in `try/finally`, including candidates that never graduate to a source. The unconfigured fast path in `WorkItemProvider` (no session opened) preserved and now test-pinned.

**Tests**: 9 new in `test_github_router_close_1279.py` — including a real-session-shape proof through the actual `GitHubMCPSpatialAdapter` (fake session closed AND nulled — the literal leak, plugged, not just a mocked-disconnect assertion) and close-on-exception coverage proving graceful `[]` degradation survives the finally. One existing test file (`test_places_1192.py`) failed after the change — its 3 mock routers predated the `close()` contract; taught them the new API (a legitimate contract expansion, not a regression — the failure was the mock's staleness, verified by reading the trace before touching anything). Full sweep: **627 passed, 0 failures** across radar + places + integrations, run with `--override-ini="addopts="` so early-stop couldn't mask anything.

**Push hygiene catch**: first push was rejected non-fast-forward and my chained verification misread it (checked `git log` exit code, which succeeds even with output — the same class of pipe/chain exit-code masking as yesterday's "125 failed" lesson). Caught it because the log OUTPUT showed my commit still ahead; merged, re-pushed, re-verified with the output-must-be-empty check (`| wc -l` → 0).

**Discovered work — the leak is ~10x bigger than the issue said**: a completeness grep for `GitHubIntegrationRouter()` construction found ~23 MORE fresh-router sites in the chat path (`intent_service.py` ×~18, `context_assembler.py` ×4, `canonical_handlers.py` ×1) — every chat message routed to a GitHub handler leaks a session, higher-traffic than the two routes #1279 named. NOT folded into #1279 (its own scope was the two routes + the shared root cause, and each chat site needs an individual lifetime read — some constructions like `query_router.py`'s injected instance and `github_domain_service.py`'s cached agent must NOT be per-call-closed). Filed as **#1376** with the full site list, the per-site-read warning, and three fix-shape options. Closed #1279 with full evidence; beta-blockers.md Epic F updated same-session (5→4 issues, count 17→16).

### 11:41 AM - PM's second batch: B proposals drafted + sent, C hosting briefing delivered, D coordination offer noted

PM answered the epic-status question with a new slate: (B) draft the #1305/#1306 proposals, (C) brief them on the Droplet-vs-Mac-Studio hosting tradeoffs for #1220 (Studio, not Mini — corrected; Arch to weigh in before deciding), (D) PM available TODAY to coordinate #1278, (E/F) order agreed.

**C briefing — written and sent** (`memo-lead-to-arch-cc-pm-pa-1220-mcp-server-hosting-tradeoffs-brief-2026-07-07.md`, to Arch since PM wants their weigh-in, cc PM + PA). Grounded in verified facts, not vibes: the app lives ON the Droplet and reaches the MCP server via `GITHUB_MCP_SERVER_URL` per-environment (read the actual handler comment), no production instance exists anywhere, every tool call chains app → MCP server → api.github.com. Answered PM's latency question with honest numbers (residential RTT ~15-40ms + wifi jitter, per tool call — real but sub-perceptual next to LLM inference; NOT the deciding factor) and named the real deciders: availability-coupling (production down when the house wifi hiccups, nobody paged), ingress exposure (tunneling into a residential network = new attack surface + tester GitHub tokens transiting a personal machine — flagged that HOST would have a view), and ops (outside the deploy pipeline). **Recommendation: Droplet sidecar**, contingent on a headroom pre-flight; fallback = $6/mo second droplet, same DC; the Studio stays great for dev/staging, out of the production request path.

**B proposals — drafted and sent for ratification** (`memo-lead-to-arch-cc-pm-pa-1305-1306-encryption-design-proposals-2026-07-07.md`, same routing). The load-bearing investigation finding: swept for real server-side JSON queries against the scoped columns — exactly ONE exists (`learning_handler.py:396`, `pattern_data.op("->")("action_type")`). The `topics` GIN index is **unqueried** — its only consumer filters in Python after ORM load, which survives TypeDecorator encryption untouched. So the proposal encrypts 6 of 7 columns whole (dropping the dead GIN index explicitly rather than leaving an index on ciphertext that looks load-bearing), and flags `pattern_data` as **Decision 1 for Arch** (leaf-split keeping the queried key plaintext, vs. promoting `action_type` to a real column). #1306: single write seam verified (`save_file_to_storage()`), proposal = app-layer envelope encryption at that seam + a single read helper, local disk stays for beta, volume-encryption and object-store SSE explicitly rejected-for-now with reasons (**Decision 2 for Arch**). Both reuse #358's exact machinery (MARKER prefix, legacy-plaintext reads, fail-closed keyless reads, refuse-to-run-keyless backfills); both ride the same next-alpha-deploy #358's checklist is pinned to.

**D**: PM's today-availability for #1278 noted — relayed the async prep list (Fly.io account/org + payment, deploy token, region preference, app name + DNS plan) so the window gets used even though the agreed order (F quick items → E) means I reach #1278's build later.
