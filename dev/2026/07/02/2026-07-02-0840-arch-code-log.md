# Session log — Architect (Chief Architect) — 2026-07-02

**Role**: Chief Architect (arch)
**Tool**: Claude Code — Opus 4.8 (`claude-opus-4-8`)
**Account**: PM's **backup account**. Worktree `arch-backup-0630`. HARD RULE honored (never touch main checkout).
**Mailbox**: `scripts/mail-send.sh` (push-to-ref).

---

## Thursday July 2 — START at 08:40 PT (light-available cron `25 8-22` fired)

**Step-0**: July 1 closed (`DAY-CLOSED: 2026-07-01`) → no retroactive close.

**START state**: inbox empty; sync clean; no RUN-LEAN restore broadcast overnight (still light-available per PM's direct instruction).

**Overnight — two SECURITY findings from Lead (PM-decision-pending), and they're in my architecture-integrity lane**:
- **#1343** — anonymous `/api/v1/intent` could silently bill the server's own Anthropic key (Caddy gate removed June 29 without the paired server-side fix). Code fix shipped, NOT deployed. PM Q: deploy?
- **#1344** — alpha registration is fully open; the June-29 gate removal **reversed a 2026-06-25 PM-ratified decision** to keep the gate as the invite mechanism. Lead correctly did NOT act unilaterally. PM Q: restore / accept-risk / build-real-invite-control.

Both are the **exact failure class my 2026-06-20 gate-removal read predicted** (`memo-arch-to-lead-cc-pm-gate-removal-concur-plus-exempt-list-as-security-boundary`): gate-removal safe ONLY paired with app-layer control + the auth-exempt list IS the attack surface → fail-closed lint. The June-29 removal shipped without the prerequisite. **Proactive architectural-read memo below** (framing for PM's pending decision + the durable prevention mechanism — NOT making the restore/accept call, that's PM's).

### Fire — proactive architectural read on #1343/#1344 (gate-removal integrity) — 09:10

Both findings are the failure class my 6/20 gate-removal read named — but **Verify-First sharpened it materially**: the exempt-list lint I recommended 6/20 **IS built + working** (`test_exempt_list_boundary_1308.py`, #1308 — exempt+WRITE+prod impossible-by-construction). So these are **two coverage gaps in a working guard, not a missing guard.** (Almost wrote "my recommended lint would prevent this" — it exists; the real finding is the gap. Verify-First paid off again.)

- **Gap A (#1344 crux)**: the lint checks a justification EXISTS, not that it's still TRUE. `create_user` is writable-exempt + "justified" as `"wizard"` → lint passes — but that justification was only safe because the Caddy *perimeter* gate gated reachability (the exact 6/25 keep-the-gate ratification). June-29 perimeter removal silently falsified it; lint still passes. Verified: `setup.py:772 create_user(req)` — no `Depends`, open by construction.
- **Gap B (#1343)**: lint's dimension is auth+WRITE; the billing exposure is auth+PAID-SIDE-EFFECT (different dimension). Lead's `AnonymousLLMKeyRequiredError` fix (verified on main) closes it; lint should ratchet it.
- **Meta-principle**: a security invariant must not be load-bearing on a perimeter control any infra change can silently remove — put it in the app layer where the lint enforces it independent of the perimeter.
- **#1344 reframe for PM** (framing, not the call): restore/build aren't either/or — restore = reversible bridge (restores the 6/25 invariant now), build app-layer invite control = durable end-state (Caddy → optional defense-in-depth); accept-risk counseled-against for open *registration* specifically (unbounded signup ≠ the now-fixed #1343 magnitude).
- **Durable fix**: extend #1308 two dims — (A) create_user requires app-layer invite → not-exempt at all; (B) flag anonymous-reachable server-key-billing routes unless fail-closed.

→ memo to PM cc Lead/HOST (`3d39985ae`) + decisions.log. **PM owns disposition; I draft the lint extensions + invite-gate shape on PM's pick.** This is the architecture-integrity mandate at work — a ratified precondition (6/25 keep-gate; my 6/20 gate-removal-needs-paired-fix) was silently bypassed by the undocumented June-29 change; my job = name it + durable fix, PM decides.

Highest-value unblocked Arch work this fire (vs. a no-op WATCH) — proactive read on a live security-boundary decision, in-lane, verified. Back to light-available hold otherwise; queue awaiting PM's #1343-deploy + #1344-disposition calls, and Lead's #1342/#1231 builds.

### Fire — 10:45 — #1343 Gap-B ratchet RATIFIED (Lead built it to the read)

Lead built the Gap-B ratchet I recommended in ~1.5h (`test_anonymous_llm_key_boundary_1343.py`, `fecc2942a`, on main) + verified both gaps against live code first. **Ratified from the artifact** (read the test, not the summary — the ratify half of the seam, and it's a *security* lint so worth confirming it's not vacuous). **RATIFIED**: correctly encodes the cost-dimension invariant (anonymous-reachable + touches raw resolver → must handle `AnonymousLLMKeyRequiredError`), appropriately scoped (route-introspection like #1308, m-40), non-vacuous. **Named 2 honest limits** (not blockers): (1) indirection blind-spot — reads endpoint's own source, a helper-wrapped billing path would slip; (2) mention≠handle — textual `"...Error" in source` proves the name appears, not that it's caught (the same vigilance-vs-deterministic gap as #1331). **Named the structural end-state** (m-36): route all billing through a fail-closed-by-construction wrapper so the raw resolver has no anonymous bypass → lint becomes backstop not primary guard (the derive-don't-lint / make-drift-impossible move, same as #1333). Not now — the target the ratchet points at; pursue with the Gap-A/#1185 work. **Gap-A split**: Lead holding correctly (inside PM's open #1344); confirmed **Lead-drafts / Arch-ratifies** the invite-gate, shape already on record in my PM memo (create_user requires invite token → removes it from exempt-writable entirely — strongest fix), ready the moment PM picks "build."

→ memo to Lead cc PM/HOST (`53b2f20e9`). Gap B done + ratified; Gap A staged + shaped, pending PM's #1344 disposition. Back to light-available hold.

### Fire — 17:39 — PM's #1344 direction landed → invite-code = staged Gap-A; usage-cap read (Arch-routed)

After ~6h of empty holds, PM's #1344 direction arrived (Janus→HOST, `e2c3fd3fd`; also a #1343 deploy go-ahead to Lead): HOST owns the canonical tester list; **invite codes** = primary app-layer gate; **usage-cap/circuit-breaker** explicitly routed to Arch; obscurity = interim. PM chose build-invite-control (not restore-Caddy) — the durable path. Proactive architectural memo to HOST cc PM/Lead (in-lane: my staged thread + Arch-tagged, without stepping on HOST's list ownership):
- **Invite-code = the Gap-A fix I already staged** — `create_user` requires an invite token → removes it from auth-exempt-writable entirely (strongest form; a not-exempt route can't be silently-un-justified when a perimeter's removed — the #1344 root). No new design; Lead-drafts/Arch-ratifies, HOST owns issuance+list.
- **Usage-cap read (PM routed to Arch)** — SEPARATE concern from invite-code (who-registers vs how-much-load; don't conflate). Shape: global ASGI middleware, **Redis-backed shared-state NOT in-process** (the #1109 lesson — per-worker counters → N×workers, cap silently fails), fail-closed honest-`503` "at capacity" (the #1231/#1333 honest-degrade throughline). Connects to my 6/20 rate-limiting note. **Arch owns the shape; product (HOST+PM) owns the thresholds** — bring me the requirement (concurrent / rate / both) and I detail the enforcement.

→ memo `0e046b5fc`. Both pieces are app-layer — the #1344 lesson (invariants stop being load-bearing on the Caddy perimeter). Ready to ratify Lead's invite-gate build + detail the usage-cap on HOST's coordination. Back to hold.

### Fire — 19:39 — Lead ack (thread closure); #1345 filed; #1343 deploying

Lead's ack: agreed on both ratchet limits; **filed my m-36 structural end-state as #1345** (route all billing through one fail-closed-by-construction wrapper → raw resolver has no anonymous bypass; lint becomes backstop — good durability, tracked not evaporated; builds with Gap-A/#1185). PM confirmed → **#1343 deploying to alpha now**. #1344 — Lead standing by for HOST's coordination (not preempting the list-ownership lane). Informational/no-ask → triaged. **#1343 thread closed on my side** (read → Gap-B ratified → m-36 tracked as #1345 → deploying). #1344 now with HOST (invite-code=staged Gap-A + usage-cap read delivered). Hold.

---

### STOP (20:39) — day-close

All my threads landed or staged; HOST's #1344 coordination is likely tomorrow. Day-closing (light-window fires remain at 21:25/22:25 — post-close/skippable).

## Day arc — July 2 summary (backup account, day 3; gate-integrity arc — highest-value Lead-support day)

The security-boundary work PM moved me here for came to a head today: I turned Lead's two overnight findings into a full architectural arc, and everything I contributed landed or staged.

| Fire | Time PT | Deliverable |
|---|---|---|
| START + gate read | 08:40 | **#1343/#1344 gate-integrity architectural read** — verified the exempt-lint (#1308) I recommended 6/20 EXISTS + works → these are 2 COVERAGE GAPS not a missing guard (justification-truth-decay + cost-dimension); #1344 reframe (restore=bridge / build=durable); durable fix = extend #1308 |
| Gap-B ratify | 10:45 | **ratified Lead's Gap-B ratchet** from the artifact — correct + named 2 honest limits (indirection / mention≠handle) + the m-36 structural end-state |
| holds | 12:39–16:39 | light-available WATCH no-ops (~6h; flagged the cost-vs-availability trade + offered dial-back) |
| #1344 direction | 17:39 | PM's direction landed → **invite-code = the staged Gap-A fix; usage-cap read** (global/Redis-backed-not-in-process #1109-lesson/fail-closed-503; Arch-shape, product-thresholds) → HOST |
| Lead ack | 19:39 | #1343 thread closed (Gap-B ratified, m-36 → #1345, deploying); #1344 with HOST |

**Load-bearing of the day**: the whole #1343/#1344 gate-integrity arc. The sharpest single move was Verify-First catching that my 6/20 exempt-lint recommendation was **built** (#1308) — so the finding shifted from "missing guard" to "two precise coverage gaps in a working guard," which is a far more useful + accurate read. The through-line (now 4 days running): make the security invariant hold at the app layer where a lint enforces it, not load-bearing on a perimeter any infra change can silently remove. Lead built Gap-B to it same-day; PM chose the durable invite-code path (= my staged Gap-A); the m-36 wrapper end-state is tracked (#1345).

## Memory & briefing surfaces referenced this session (per #974)

**Referenced**: `test_exempt_list_boundary_1308.py` (the exempt-lint — my 6/20 rec, built; the Verify-First catch) · `test_anonymous_llm_key_boundary_1343.py` (Lead's Gap-B ratchet — ratified from the artifact) · `setup.py:772 create_user` (verified open-by-construction, no Depends) · `services/llm/request_key.py` (the #1343 fix) · my **6/20 gate-removal read** (`memo-...-exempt-list-as-security-boundary` — the predicted class + the rate-limiting note the usage-cap builds on) · the #1109 in-process-vs-Redis rate-limit lesson (the usage-cap load-bearing constraint) · ADR-058/071 (auth anchoring) · m-36 (structural end-states → #1345) / m-40 (lint scope match #1308) / m-41 (make-drift-impossible — the arc's spine) · CLAUDE.md HARD RULE (never touch PM's main checkout — honored; worked only in `arch-backup-0630`).
**Loaded but not referenced**: xpoll; broad cohort delta.
**Wanted but not found**: nothing notable — every artifact (the lints, the routes, the fix, my prior memo) was present + verifiable.

## Sign-off

```bash
$ git log --oneline origin/main..HEAD   # 0 — all July 2 work on origin/main (per-fire)
$ git status --short                     # clean apart from this close
```

✓ All July 2 work on `origin/main` (gate-integrity read + Gap-B ratify + #1344-direction/usage-cap memo + decisions.log). PM's main checkout untouched all day.
✓ Cron `7d001ae9` (`25 8-22`, LIGHT-AVAILABLE) armed — leave as-is (PM-directed for Lead support; the dial-back offer stands if PM prefers). Restore to normal on PM/Exec signal.

<!-- DAY-CLOSED: 2026-07-02 -->

— Architect (Opus 4.8, PM backup account), Thursday July 2 closed at 20:39 PT. Backup-account day 3: the #1343/#1344 gate-integrity arc — architectural read → Gap-B ratified → #1344 direction connected to staged Gap-A + usage-cap read; all landed/staged, Lead fully supported. **Tomorrow**: HOST's #1344 coordination (ratify Lead's invite-gate build; detail the usage-cap on requirement); Lead's #1342/#1231 builds; watch the RUN-LEAN restore.
