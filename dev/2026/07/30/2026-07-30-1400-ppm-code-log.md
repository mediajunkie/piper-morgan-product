# Session Log — Principal Product Manager (PPM)

**Date**: 2026-07-30
**Started**: ~14:00 PDT (resumed after an overload error interrupted the 7/29 session)
**Role**: Principal Product Manager (PPM)
**Tool**: Claude Code
**Model**: Opus 5 (1M context)
**Host**: Amber · **Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A)
**Branch**: `claude/ppm-cycle`
**Prior log**: `dev/2026/07/29/2026-07-29-ppm-code-log.md`

---

## Entries

### 14:00 — Resumed on PM's ask: Jake FTUX reply + duty cycle

PM flagged the cohort was blocked on my Jake reply. Synced (0 behind, clean tree).

**I was the last of four lenses.** CXO (7/29), HOST (7/27), and PA (7/29) were all in; Exec
synthesizes once all four land. So this was a hard block on PM's synthesis, not a queued item.

### 14:15 — Jake FTUX: PPM roadmap lens sent (`0725db682`)

Read the full source thread plus all three peer lenses before writing, specifically to avoid
re-covering them. Agreed with all three headline calls and said so once, briefly:
PA's cold-start reframe (*"an empty list is a form, a populated queue is a colleague"*) is the
correct root; CXO's §1 (we ask the user to supply the output of the work we promise) is the
correct headline; HOST's consent-boundary escalation is right.

**Checked before claiming**: I expected the PDR-006 coupling to be my contribution — CXO had
already made it that morning. Read their PDR-006 review first and built on it instead of
"discovering" it. Worth noting as a near-miss of exactly the inherited-claim failure I wrote up
yesterday.

**What the lane actually owed — scope, sequence, gate:**

1. **The fix list has never been sorted against the pivot.** Twenty items from four lenses,
   PDR-006 at ratification (Arch resolved Q2), and the default — work it in severity order —
   spends beta capacity on a surface being retired. Proposed a three-bucket filter:
   **A. dies with the pivot** (nav pill, panel width, placeholder, composer, lists-as-navigation)
   → don't spend beta capacity; **B. survives but relocates** (taxonomy → tool catalog naming,
   legibility → tool descriptions, elicitation → plugin-driven conversation) → re-specify before
   building or build twice; **C. gets harder and becomes the whole game** (cold-start
   population, reflect-and-elaborate, consent gate) → this *is* the beta.
   Carve-out on A: the two items that **changed Jake's behavior** (unfindable "blocked" card,
   missing chat row) get fixed on welfare grounds regardless of surface lifetime. Panel width
   didn't change his behavior; those did.

2. **Answered the three-list decision CXO routed to me.** Their principle (organize by user
   trigger, not our object model) is right and survives — but the surface is the **MCP tool
   catalog**, not navigation. Tools named for the situation they serve, routing to the same three
   structures. Same fix, ~⅓ the cost, on the surface that will exist. **Flagged the risk against
   my own recommendation**: the catalog is read by the host LLM too, and situation-shaped names
   may route *worse* than object-shaped nouns. Nobody knows; cheap to test, expensive to assume.
   Recommended as direction, not settled call.

3. **The beta-gate finding, which is the one I'd most want PM to see.** #1386's criteria —
   canonical suite + three scenarios + sign-off — **would all pass on Jake's exact session while
   producing his exact outcome.** The gate measures whether Piper answers correctly; Jake's
   session says the risk is a competent user getting correct behavior throughout and concluding
   we're an LLM wrapper. **methodology-44 at the product level**: the gate emits "clear"
   identically whether we demonstrated value or merely avoided errors.
   Recommended: (a) do NOT expand #1386 — close it on existing terms; (b) add one new beta
   criterion, a binary **first-contact demonstration test** (cold account, one tool connected —
   does the user's own data appear unprompted?); (c) treat HOST's consent gate as a genuine
   release blocker, the one item I'd hold for.

4. **Cohort-size risk**: act on structural findings at n=1 (they stand on internal logic — a
   valid argument doesn't need a second voice), don't act on preference-level ones. The
   denominator is the real risk: one tester is not an alpha program. Argues for shipping
   PDR-006 *sooner*, not slower.

5. **Tracking**: four excellent lenses is four memos, not work. None of it is in GitHub. Offered
   to file the issues against the bucket structure the same day synthesis lands — deliberately
   not pre-filing a shape PM may reorder.

### 14:30 — Duty cycle resumed

Cron armed: job **`85981d52`**, `52 6,9,12,15,18,21` — six fires/day, next ~15:52 PT.
Registry row updated to drop `parked` and set active_since 2026-07-30 (a row means *watched*,
so the note had to go the moment the job existed, not before).

⚠️ **Two properties of this cron PM should know**: it is **session-only** (in-memory, dies with
this Claude session — not written to disk) and it **auto-expires after 7 days**. So it is not a
durable daemon; if the session ends, the cycle stops silently and the registry row would then
claim watched coverage that doesn't exist. Flagged to PM rather than left implicit.

### Fire 1 (16:22, WORK) — drained the entire owed queue to (0,0)

First cron fire. Step 1: exactly one job (`85981d52`), no duplicates, no Gap-C. Step 2a-bis: per
v1.22 **did not probe** — verified the real `pre-commit` hook exists in the **common dir**
(`/Users/xian/Development/piper-morgan-product/.git/hooks/pre-commit`, delegating to
`check-branch.sh`), which covers all worktrees by construction. That's Arch's TOCTOU fix; the
probe apparatus is retired instrumentation. Sync clean, 0 behind. Dispatch → **WORK PARTS**
(session log exists, next fire 18:52 is not the day's last). CronDelete'd per Rule 1 before
draining substantive multi-step work.

**Four memos shipped; every owed item cleared.**

**1. PDR-006 — PPM review: RATIFY** (`8b310ca37`). I was **the only outstanding review** (Arch ✅
7/29, CXO ✅ 7/30), so this was blocking ratification. Read the PDR itself, not just the memo
traffic. Three findings:
   - 🔴 **The Success Criteria cannot fail for the thing this PDR most fears.** All three are
     *setup* criteria — every one passes if a user installs cleanly, calls a tool, gets a correct
     answer, and concludes we're an LLM wrapper. That is Jake's session exactly. Same
     methodology-44 shape as #1386, but a **distinct instrument with the same defect**, which is
     what makes it a class rather than a repeat. Proposed one binary addition: *from a cold
     account with one connector, does the user's own data appear in the first exchange,
     unprompted?*
   - **Answered the question Q2 handed me**: **do NOT pull #558 forward** (verified live: OPEN,
     milestone Production). Jake gives no signal on the colleague-model gap — and *that non-signal
     is the answer*: you can't get colleague-model feedback from users who bounce at first
     contact. #558 is gated behind cold-start, not the reverse. Noted that if PM overrides, the
     spatial coupling returns immediately (PA: "one issue away, not one project away").
   - **Two tracking gaps**: the epic is "issue TBD" in a ratifying PDR; and the two pre-user gates
     are asymmetric — #1458 has a number, the rubric-branch gate is prose only. A gate that isn't
     an issue isn't tracked.

**2. CXO's two decisions** (`8b310ca37`). Both decidable, so decided rather than routed:
   - **Colleague Test tier — rejected both options on the table.** CXO asked whether to promote
     the *rubric* to PDR status. Under m-38 the corpus records **decisions, not instruments** —
     promoting a live rubric would drag re-ratification behind every revision and they'd stop
     revising it. **The unratified thing isn't the rubric; it's the decision that Layer B binds at
     all** — which is what someone disputing a Layer-B failure would actually appeal to. Ratify
     the decision (half a page, or a PDR-004 amendment), leave the instrument where it lives.
     Survives every future rubric version *and* the new plugin branch.
   - **Rubric branch — yes, open it, it's CXO's.** Endorsed PA's Phase-0 sequencing hard: a
     negative recomposition result changes what the tool layer must *emit* (structured confidence
     fields vs. hedged prose), so it's a design input, not QA. Added the thing nobody had said:
     **a rubric can score honesty loss but cannot prevent it** — we don't own the paraphrasing
     model — so the branch must be built so a failing score routes to an *output-format* change,
     not a "write better hedges" remediation.

**3. HOST's tense finding** (`52d73498b`) — **corrected my own account.** I'd framed the
ROLE-PORTFOLIO chain as "session 1 checked and was wrong, 2–4 inherited." HOST's finding says
session 1 may never have checked — and **I was session 2 and have the receipt**: my own 7/26 #974
bucket recorded it as "wanted but not found" and **I never ran a `find`.** So creation and
transmission are **two stages of one defect**, not two mechanisms: the #974 bucket invites a felt
absence (HOST), a carry-forward reads it as repo state and propagates it (mine). My provenance tag
was aimed one stage too late — without HOST's tense label it would faithfully stamp a date on
something never checked. Three fixes, one per stage. Submitted to CIO as **one instance with two
stages**, not the separate instance-12 I proposed — a correction that makes the entry smaller.

**4. Spatial committed-theory — PPM slice delivered** (`3c655a90b`), long-owed, hold released this
morning. Arch sharpened it to: *does any 1.0 commitment assume L3 beyond GitHub, or L4 at all?*
Checked roadmap + live GitHub:
   - **L3 beyond GitHub — NOT promised.** `roadmap.md:70` classes connectors as **"indoor plumbing
     (commodity)."** 1.0 commits to connector *function*, never *spatial depth*. Independent
     product-side confirmation of (b): the 10-module cold island can be disposed of with **no
     roadmap commitment losing its referent.** **Concur with (b).**
   - **L4 — YES, and worse than a stray line item.** **#1174 verified OPEN, milestone Production**
     — a 1.0 commitment whose whole subject is when/how Piper nudges unasked, over a layer Arch
     found has zero implementation. And `roadmap.md:68` lists **"Earned proactivity" as
     differentiator 4 of 4** in the Vision-V2.3-*Stable* stack — the section that opens *"four
     differentiators that make Piper a colleague rather than a chatbot wrapper."*
   - ⚠️ **The connection worth PM's attention**: Jake returned the phrase *"just packaging a
     regular LLM… with a different UI."* He never met differentiator #4 because there is no #4 to
     meet. Not claiming L4 would have saved his session (cold-start would have, cheaper) — the
     narrower and worse point is that **the stack has four legs, one is empty, and the first
     outsider to lean on it said so in the stack's own words.**
   - Three options to PM; recommended **(i) re-scope #1174 to the discovery thread it's titled as**
     (interruption ethics is answerable on paper and is HOST's lane regardless), keep it in
     Production as discovery, state that delivery isn't scheduled. Would **not** fund L4 pre-beta,
     same reasoning as #558.
   - **Declined credit for a "near-miss" Arch attributed to me** — it's in no PPM log or
     carry-forward I can find, and this role has had two dark periods plus a concurrent lineage in
     ten days. Adopted the lesson independently (**promotion to a higher-authority surface is a
     re-verification trigger** — the write-side twin of the read-side defect HOST and I traced).

**Mail loop drained to zero**: 50 memos → `read/`, MANIFESTs regenerated (`60a6dbb8a`).
⚠️ **Honest scope**: I deep-read the memos addressed *to* PPM and those gating my owed work; the
remainder (hook-investigation and m-44 cross-traffic where PPM is cc'd, now settled in CLAUDE.md)
were triaged, not deep-read. Saying so rather than implying full coverage.

### Fire 2 (19:22, WORK) — corrected my own Fire-1 finding before anyone acted on it

One cron (`b0b14ea8`), no duplicates. Sync clean. Four memos in; three addressed to PPM, all
direct responses to Fire 1. Everything I sent landed:

- **CXO**: (b) confirmed — *their* stated falsifier was about **L3**, and my Half 1 answered it
  (not promised) — so my check **confirmed** their position rather than overturning it, from an
  independent route. They also disclosed that **#1174 is a CXO issue** and they'd been arguing L4
  is "wave-2, don't fund it" all week while holding an open Production commitment to it in their
  own lane. Taking option (i) and doing the discovery themselves with HOST.
- **CXO on tier**: accepted my reframe, drafting the PDR-004 amendment. **Branch opened**; PA
  running Probe A on both Claude and GPT.
- **Arch**: both halves folded; called Half 2 *"more consequential than anything in my
  architectural slice."*
- **Attribution resolved**: the `decisions.log` near-miss was **CXO's**. Arch owned the ambiguity
  — they wrote "your near-miss" in a memo addressed to three recipients, unresolvable from the
  reader's side. Arch attributes the *agreement-is-not-replication* lesson to PPM, sourceable in
  my 7/26 withdrawal memo. **Refusing the unsourceable credit was the right call and cost nothing
  to resolve, because it was written down.**

**⚠️ Then I corrected myself** (`f304c7d1f`). CXO had said the roadmap edit was mine to make. On
re-reading my own evidence I found I'd **overstated one claim**: I wrote that the "Vision V2.3 —
Stable" banner *"covers a leg that doesn't exist."* But `roadmap.md:68` already reads
**"(M4 territory)"** — the same scheduling qualifier form as #3's "(M3 territory)", and #3 is
built because M3 closed. **The roadmap never claimed #4 exists today.** My proposed edit
(`stable` → `intended`) would have made the roadmap *less* accurate by implying the milestone
label was missing.

Flagged loudly rather than quietly narrowed: three people had endorsed the framing and it was on
its way to PM. **An overstated finding others have endorsed is harder to walk back later than now.**

**The replacement finding is sharper, not softer** — verified both surfaces:
`roadmap.md:68` schedules L4 in **M4**; **#1174 sits in the Production milestone**. Two surfaces,
two milestones, same capability — **and M4 has not run** (RECONNECT → M4 → M5; RECONNECT closed
Jul 1). Whichever is right, one is wrong, and **neither surface reveals it when read alone.**
So the edit changes from a wording qualifier to **milestone reconciliation** — CXO's call since
they own #1174; I lean #1174 → M4 with delivery explicitly unscheduled.

Everything else in the slice stands: Half 1, the zero-implementation finding, don't-fund-L4-pre-beta,
and the Jake connection — none depended on the banner claim.

Closing note I sent CXO: **their "arguing both sides of #1174 without noticing" is the same shape
as this.** They held two positions across two surfaces; the roadmap and #1174 hold two schedules
across two surfaces. Not carelessness — **what happens when a claim lives in more than one place
and nothing forces them to be read together.** Their instance had a person who could notice; this
one has two documents and nobody.

Inbox 4 → 0, MANIFEST regenerated.

### 20:10 — ⛔ PM caught a real error: M4/M5 were SWEPT. Both my readings were wrong.

PM flagged a gap and was right. **M4 and M5 do not exist as sprints.** They were swept
2026-07-04/05 — **PPM's own work** — along with M3-Quality/Health/Security and RECONNECT.
Contents went to the **Beta Blockers** sprint or to **Production**.

Source, which I had never opened: `docs/internal/planning/beta-blockers.md`
- **:20** — *"The full open backlog was swept sprint-by-sprint (M3-Quality, M3-Health,
  M3-Security, **M4, M5**, RECONNECT — 2026-07-04/05)… documented in the **PPM session logs for
  2026-07-04 and 2026-07-05**."*
- **:12** — *"Everything else that was in the MVP milestone but did not meet the hard-gate bar has
  been **moved to the Production milestone**, to be addressed during the beta period."*

**Both of my readings of `roadmap.md:68` were wrong, in opposite directions.** Fire 1: the "Stable"
banner undercuts it. Fire 2: "(M4 territory)" is honest scheduling labeling. **Neither** — it is a
**stale pointer to a dissolved sprint.** Both readings assumed M4 was live.

**And there is no milestone-split defect.** #1174 sitting in Production is the disposition rule
applied *correctly* — a capability that didn't meet the beta hard-gate bar belongs in Production.
I invented a defect out of a rule working as designed.

⛔ **The consequential part**: I recommended *"#1174 → M4"* to CXO, **who had accepted the framing
and taken ownership of the re-scope.** That would have moved a live issue into a **dissolved
sprint** — the board-damage class PM has flagged before. Sent a STOP memo (`5a3d62961`) to CXO +
Arch, cc cohort, before anyone acted.

**What survives, unchanged and unaffected**: #1174 OPEN/Production with zero implementation;
"earned proactivity" as differentiator 4 of 4; the Jake connection; CXO's option (i) — which is now
*simpler*, because it needs **no milestone change at all.**

**Why I got it wrong — the useful part.** Three surfaces disagreed and I trusted the two stale ones:
`sprint-board-structure.md` (labelled canonical, still lists M4/M5 as planned) and `roadmap.md:68`
— while **never opening `beta-blockers.md`**, which is accurate and records the sweep explicitly.
My carry-forward carried *"Beta Blockers sprint recount — blocked on `read:project`"*, so I knew the
sprint existed and treated it as **a thing to count**, never recording that it **replaced M4/M5**.

**This is the write-side of the defect HOST and I traced yesterday.** I spent the week arguing that
inherited claims decay, then reasoned from two stale docs against a canonical one I didn't check —
about a refactor **I personally ran and had no memory of.** Investigate-before-extending applies
hardest to the ground you think you already know, which is exactly where it doesn't feel needed.

**Fixed**: carry-forward now opens with a ⚠️ SPRINT STRUCTURE block naming the sweep, the
disposition rule, the two stale docs by path, and the accurate source — so the next PPM session
inherits it rather than rediscovering it at this cost.

**Owed**: repoint the stale `(M4 …)`/`(M5 …)` references across `roadmap.md` and
`sprint-board-structure.md` — **as a class, in one pass**, not the one line I happened to look at.

### 20:30 — Corrections landed; diagnosis corrected a THIRD time; CXO signalled feedback-complete

PM's directive: make the corrections, then tell CXO feedback is done so PM can work the
perspectives with them.

**⚠️ The diagnosis changed again when I went to fix the docs, and the new one is less flattering.**
I had told CXO the problem was stale documentation. **`roadmap.md` was never stale.** Its §M4
(`:131`–`:133`) records the triage closure *and names #1174 explicitly* among the 15 issues moved
to Production. **The file contained the exact fact I was missing, sixty lines below the line I
quoted.** I'd grepped it for `proactiv|ambient`, hit line 68, and treated a keyword hit as the
document's position. Only `sprint-board-structure.md` was genuinely stale.

So this is **CLAUDE.md's own "read the WHOLE artifact before acting on a fragment"** — not doc rot.
Blaming stale docs was the comfortable diagnosis and would have sent Docs on a cleanup that wasn't
the problem. Three passes, three wrong readings of one line, and the answer was in the first
document I opened. It was endorsable by three colleagues because **I presented a fragment with the
confidence of a full read.**

**Landed** (`e9eae9f26`):
- `sprint-board-structure.md` — the genuinely-stale one. "Remaining sprints" list presented M4 as
  *next planned* / M5 as *final planned*; now a SUPERSEDED banner + per-sprint disposition
  (M4 triage-closed, RECONNECT drained, D1 closed, M5 swept) + **Beta Blockers named as the live
  pre-beta sprint** + pointer to `beta-blockers.md`.
- `roadmap.md:68` — differentiator #4 now names the triage closure, #1174 → Production, and that
  the capability has no implementation today. (One stale cross-reference in an otherwise accurate
  doc — fixed as a cross-reference, not as rot.)
- `decisions.log` — the sweep, the corrections, and the corrected diagnosis, so the *propagation*
  is recorded and not just the original decision.
- Carry-forward — opens with the sprint-structure block.

**CXO: feedback-complete memo sent** (`d7b7eb3d6`) — consolidated final position, explicitly
superseding all three earlier passes, with the one thing to undo (`#1174 → M4`) called out. Nothing
further owed from me on that thread; PM has it.

**Additive finding routed to Arch/Docs/CIO**: Arch reported today that `check-staleness.py` works
and is invoked by nothing (33/36 operating docs stale). Verified an extension — **the planning docs
aren't in its denominator at all.** `roadmap.md`, `sprint-board-structure.md`, `beta-blockers.md`
are neither flagged nor cleared by it. A superseded planning doc is currently **unread *and*
unmeasured.**

**The lesson PM named, in the form I'd actually keep**: recording a decision and expunging what it
supersedes are two different acts, and we reliably do only the first. The sweep *was* recorded — in
`beta-blockers.md`, `decisions.log`, and PPM's own 7/4–7/5 logs. What never happened was updating
the surfaces that pointed at the old structure. That's methodology-35 (asymmetric discipline —
creation half specified, cleanup half not) applied to **records** rather than resources. The
mechanism-shaped fix is that a decision record should name its **invalidation set** — which
surfaces this supersedes — so retirement is a checklist item rather than an act of memory.

### 22:22 — STOP (day-close)

Last scheduled fire of today (next is 06:52 Friday, a different date). Cleanup dry-run: no stale
cycle-logs >7d, no `.tmp` >1d — nothing to remove. Final mail triaged, inbox zero.

**Arch confirmed the STOP was honored — no board damage.** They touched no milestone and moved no
issue, verified against their own commit log rather than asserted. They also relocated their
ADR-038-A3 point onto the correct target, which is a better generalization than mine:
**`(M4 territory)` is a durable document carrying a fact with a shorter lifetime than the
document itself.** Same class as ADR-038 citing an implementation as proof of a pattern — different
noun.

**#1386 window LOCKED for Friday** (Exec, per PM's expedite directive) — carried to the top of the
carry-forward with the scope caveat, because "re-run done" must not read as "gate passed."

---

## Day arc — 2026-07-30

Resumed midday after an overload error, on PM's flag that the cohort was blocked on me.

1. **Unblocked two people's work.** Jake FTUX roadmap lens (4th of 4 — Exec's synthesis was waiting
   on it) and the PDR-006 PPM review (the only outstanding review — ratification was waiting on it).
2. **Answered three decisions routed to me**: CXO's Colleague-Test tier question (reframed —
   ratify the *decision*, not the instrument; CXO accepted and is drafting the PDR-004 amendment),
   the rubric-branch read, and the three-list taxonomy question (relocates to the MCP tool catalog).
3. **Delivered the long-owed spatial slice** — concur (b), with the L4/#1174 finding.
4. **Got the L4 finding's supporting argument wrong three times, and PM caught it.**
5. **Corrected the record** — two planning docs, `decisions.log`, carry-forward — and closed out
   to CXO with a single consolidated position.
6. **Resumed the duty cycle** (armed at 14:30; fires at 16:22, 19:22, 22:22).

**The day's real lesson, and it's mine not the cohort's**: I was right about the finding and wrong
about the argument, repeatedly, and each wrong version was *endorsable* because I presented a
fragment with the confidence of a full read. `roadmap.md` §M4 named #1174 as moved-to-Production
sixty lines below the line I quoted. Three colleagues agreed with me before PM caught it. **The
cure isn't more caution about documents — it's reading the whole artifact before speaking, which
is a rule I'd quoted approvingly earlier the same week.**

## Memory & briefing surfaces referenced this session

**Referenced**
- `dev/active/ppm-carry-forward.md` — the continuity thread across three PM-resumed interruptions;
  also the surface that propagated my own stale claim, so it cut both ways today
- `docs/internal/planning/beta-blockers.md` — the accurate sprint-structure source. **The document
  I should have opened first and didn't**
- `docs/internal/planning/roadmap/roadmap.md` — §M4 (:131–133) held the fact I was missing
- `dev/active/alpha-feedback-jake-krajewski-2026-07-25.md` + CXO/HOST/PA lenses — read in full
  before writing mine, specifically to be additive rather than repetitive
- `docs/internal/product/pdr/PDR-006-*.md` — read whole, not from memo traffic
- `.claude/skills/duty-cycle-tick` v1.22 — the retired-probe rule saved a pointless hook probe
- CLAUDE.md §"read the WHOLE artifact before acting on a fragment" — quoted approvingly, then
  violated, then re-learned
- `feedback_never_reuse_stale_tree_object_on_push_retry` — applied at both mail-send retries

**Loaded but not referenced**
- `docs/briefing/ROLE-PORTFOLIO-PPM.md`, `ppm-standing-items.md`, most of the memory index

**Wanted but not found** *(as experienced this session — NOT a verified claim about repo state;
tense label per HOST's 2026-07-29 correction)*
- A mechanism that ties a decision record to the surfaces it invalidates. I believe this doesn't
  exist; I have **not** verified that, and per today's lesson I'm not asserting it. Proposed to
  PM/CIO as an "invalidation set" field on decision records.
- Live board state — genuinely blocked, not a search failure: `gh` lacks `read:project`.

## Sign-off checklist

```
git status                    → clean
git log --oneline @{u}..HEAD  → (verified below)
git log --oneline main..HEAD  → (verified below)
```

<!-- DAY-CLOSED: 2026-07-30 -->

## Still owed

✅ ~~PDR-006 + Q2~~ — **done, Fire 1** (RATIFY; ratification unblocked).
✅ ~~Spatial product-value slice~~ — **done, Fire 1** (concur (b); L4 finding).

Remaining, all now gated on someone else:
1. **#1386 gate run** — coordination is Exec's per their 7/29 memo; my sign-off role stands.
2. **Jake issue-filing** — on Exec's synthesis signal.
3. **Roadmap qualifier + #1174 re-scope** — on PM's pick among the three L4 options.
4. **Rubric-branch issue** — asked CXO whether I file it or they do.
5. Open for PM: `gh auth refresh -s read:project` (no board reads without it).
