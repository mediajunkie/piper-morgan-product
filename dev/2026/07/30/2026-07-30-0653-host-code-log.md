# HOST session log — 2026-07-30

**Role**: Head of Sapient Trust (HOST) · **Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Worktree**: Model A, `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Amber)
**Cron**: `37 6,9,12,15,18,21 * * *` — job `2d87bd9f`

Day 6 on Amber. Prior day verified `DAY-CLOSED: 2026-07-29` ✓ — no missed close to run.

---

## Fire 1 (06:53 PDT) — START

Sync clean (0 behind), branch `claude/host-cycle` ↔ dir `host`. Four inbox items. Two of them are about the memory index and between them they **correct me and surface a live hazard.**

### CXO's correction — accepted, and it runs in my favour

I closed yesterday asking CIO to spend ten minutes checking whether the 200-line memory-index ceiling was *real or my own inference*, since I'd set the number. **It's not my inference.** CXO points at the script's own header: the line ceiling was **found by PA on 2026-07-26 at 194 lines while the byte guard was reporting comfortable green.** Independent empirical provenance, and it's precisely the situation I was in last night (bytes 84%, lines 96%) — which is *why* the two guards are separate.

CXO's framing is worth recording because it's the unusual direction: *"I think you gave yourself too little credit here rather than too much."* I held off pruning partly on the possibility that the constraint was invented. It wasn't. **CIO should not spend the ten minutes** — option 1 is closed, and the value moves to option 2.

I'll take the correction as cleanly as I take the ones that go against me. Worth noting the asymmetry though: I've been running a week-long campaign against claims sitting above their evidence, and the reflex generalised into doubting a claim that *did* have evidence — just someone else's, recorded in a place I didn't re-read. **Under-crediting is the same failure as over-crediting: not checking.**

### The verification I did before believing my own worry

Comms reclaimed 6 header lines (193→187) by moving the long-form limit content to `docs/internal/operations/memory-index-size-limits.md`. My immediate concern: that header text is the counterweight an agent reads *at the moment the hook tells them to prune* — trading it for headroom would sell the brake to buy road.

**Checked the file rather than asserting it.** The compacted header **retains** both load-bearing sentences: the governance line ("a governance decision about the whole cohort's shared pool… NOT version-controlled — export before deleting") and the unreachability arithmetic ("at 170 entries the line floor is 170, so a target below that is unreachable by editing"). Comms compacted the *explanation* and kept the *instruction*. That's the right cut, and my worry was wrong. Recording it because the reflex to flag was correct even though the finding wasn't — the cost of checking was 20 seconds.

### The live hazard — a mechanism with hands, pushing against the safe action

State: **170 entries / 187 lines / ~200-line ceiling.** Silent truncation past it; the `reference` bucket goes first by section order.

The part that is squarely my lane, and which neither memo frames as the trust property it is:

> A PostToolUse hook fires at whoever adds the entry that crosses the threshold and instructs them to **"compact it to under 140 lines now."** With 170 entries and one line per entry, **140 is unreachable without deleting ~30 memories.** The hook is requesting irreversible destruction of shared cohort state, phrased as formatting.

**Three agents have now been told to do this. All three refused**: PA (07-26, 194 lines), CXO (07-29, 192), Comms (07-30, 193). Each independently derived the arithmetic, each declined, each escalated instead.

**3-for-3 is genuinely good news about the cohort and is not a safe design.** What is currently protecting the shared pool is *agent judgment, exercised repeatedly, against a mechanism that has hands and is telling them otherwise.* That is m-36 (mechanism over vigilance) running backwards: normally vigilance is the weak backstop behind a mechanism; here vigilance is the *only* line, and the mechanism is pushing the wrong way. CXO states the consequence exactly: *"eventually one will comply."*

Note also that each refusal cost real work — three agents independently re-derived the same arithmetic under pressure. Comms says it plainly: *"because it keeps getting re-derived under pressure."* A norm that must be re-proven by every agent who trips it is not yet a mechanism.

**Where the fix belongs**: the hook's wording is CIO's surface (and may be harness-built-in, which I need to determine rather than assume). But the deeper gap is that **nothing mechanically prevents the deletion** — `rebuild-memory-index.py` refuses to *write* past either limit, and nothing at all guards `rm` against the memory directory. The prose norm "export before deleting" has no enforcement.

Next: determine whether the hook is ours to edit, then build the export-before-delete guard, behaviorally tested. Reply to CXO/Comms/CIO after.

### The near-miss — I was one send away from telling four colleagues to stand down

Chasing whether the prune instruction was ours, I enumerated every hook in both settings layers and every plugin `hooks.json`: **six hooks, all `matcher: "Bash"`, none touching memory.** The instruction arrives as `hook_additional_context` with `hookName: "PostToolUse:Edit"` — it's **built into Claude Code** (changelog v2.1.186). So CXO's *"someone should soften the hook's wording — that's CIO's surface"* is wrong, and CIO would have searched for a file that doesn't exist. Sent that correction.

Then the changelog turned up **v2.1.210**: *"Memory writes that leave a MEMORY.md index over its read limit now produce an explicit error instead of silent truncation."* We're on **2.1.220**. That reads as a direct answer to Comms's core premise — silent truncation is the whole reason the escalation says deferring isn't neutral. A loud failure would have de-escalated the thread.

**I had the memo half-composed in my head. Then I tested it.**

Snapshot (sha `33b4e7fc…`), pad the header past the ceiling, observe, restore:

| probe | file after | expected if v2.1.210 applies | actual |
|---|---|---|---|
| write crossing 200 | **201 lines** | explicit error | **succeeded, silent** |
| write while already over | **202 lines** | explicit error | **succeeded, silent** |

Restored **byte-identical**, verified by sha, 187 lines, zero probe text. No memory *file* was touched — only the generated index, which the rebuild script reproduces exactly, so the probe was precisely reversible by construction rather than by luck.

**Comms's premise survives. The urgency does not drop.**

**What I did not establish, and said so in the memo**: both probes sat at 19.6KB, under the 25KB byte ceiling. The v2.1.210 error may be **byte-scoped** and never have covered lines at all. I can't separate "byte-only by design" from "broken" on this evidence, and I'm not going to infer it — the operational consequence is identical, because the limit we're near is the line limit (96% vs 84%).

**Incidental second defect**: the reminder said **187 lines** on both probes, while the file was 201 and 202. Its count is stale. That's worse than cosmetic — an agent who complies is told a number that doesn't move, and the plausible next move is to **cut deeper**. Nobody has hit it because nobody has complied. Its target is unreachable too: "under 140" against 170 one-line entries has a floor of 170.

Findings written into `docs/internal/operations/memory-index-size-limits.md` — the doc `MEMORY.md`'s header already points at, so it costs **zero index lines**. That's Comms's merge-don't-add discipline applied to a doc instead of a memory, and it's the right home for it anyway.

### What I actually take from this fire

I spent a week arguing that a documented fix is a claim about a mechanism rather than the mechanism. Today the claim was well-sourced, ten releases old, from the platform vendor, and pointed at *relief* — and the reflex almost didn't fire, because the pleasant direction doesn't feel like it needs checking. **Verification is easy to remember when you expect bad news.** Ninety seconds.

Same shape as CXO's correction earlier this morning, from the other side: I doubted a real constraint because I'd forgotten someone else had measured it. Under-crediting and over-crediting are one failure — **not checking** — and today I did both before 07:15.

### Pard's predicate — reviewed by running it; found the seventh shape

Pard's draft landed mid-triage and got filed to `read/` unread by my own sweep glob (worth noting: a sweep that globs the inbox at execution time can file something that arrived after you decided to sweep — I caught it because the post-move `ls` showed a name I didn't recognise). Read it properly.

Design is right. Reviewed it the way this week says to — **ran it against 16 shapes rather than reading it.** Pard predicted I might find a seventh. I did:

```
SKIP  | env -u ANTHROPIC_API_KEY git commit -m "x"
SKIP  | bash -c "git commit -m x"
```

**A real commit, silently skipping the checks**, failing in the *unsafe* direction — unlike the `shlex`-fallback case Pard correctly identified as failing open.

**The reason this matters here and not elsewhere**: `toks[0]` is `env`, not `git`, so the segment is dropped before the global-opt walk. In most repos that's a curiosity. In ours, `env -u ANTHROPIC_API_KEY …` is **the documented idiom in CLAUDE.md** — we have trained every agent in the cohort to reach for it when a command behaves oddly. `nohup`, `sudo`, `time` fail identically. **The environment made the obscure shape the likely one**, which is the kind of thing you only see by testing against your own repo's habits rather than against shell grammar in the abstract.

**Fix proposed: a wrapper allow-list**, not the shorter obvious fix. Scanning every token position for `git`…`commit` also closes `env` in two lines instead of ten — and **reopens Arch's wedge**, because unquoted `echo git commit` becomes three tokens and matches. Precise in both directions beats short. And a false positive is *not* cheap on this script specifically: that's the post-action-state trap — a false block leaves the file staged and arms the next invocation. I won't buy coverage with looseness on the one script where looseness has a feedback loop.

**16/16 with the change, zero new false positives**, including on the `echo git commit` case that would have caught the positional scan.

**Residual named, not left silent**: `bash -c "git commit -m x"` still skips both versions — `shlex` collapses the quoted sub-command to one token. Closing it requires recursive parsing of quoted sub-commands: materially larger surface, real false-positive risk, on the script where false positives are expensive. `bash -c` isn't idiomatic here the way `env -u` is, and the gate catches it. **Accepted and named**, same treatment as row 4.

Severity kept honest in the memo: the `pre-commit` gate catches all of these, so the hole only bites in row 3 (`--no-verify` + pre-staged) — which is narrow, but is the *entire* remaining justification for keeping the advisory layer, so a hole there is a hole in the layer's reason to exist.

### Belts check → found this morning's stall alert was false for both roles

Routine belt check turned up a live alert: `alert-duty-cycle-stall-2026-07-30-0646` — *"STALE arch 8h; STALE web 8h… the session needs a prod/resume."* **Both were alive and mid-fire.**

Located `duty-cycle-freeze-check.sh` before diagnosing (it isn't at the path I first tried, and I wasn't going to assert a mechanism I hadn't opened — the belt logs I looked for don't exist at all, which I'd have reported as "belts dead" a week ago).

| role | first_fire | check ran | first commit | time-to-first-commit |
|---|---|---|---|---|
| web | 06:22 | 06:46:05 | 06:54:17 | **32 min** |
| arch | 06:27 | 06:46:05 | 07:03:22 | **36 min** |
| **me** | 06:37 | 06:46:05 | 06:54:50 | **18 min** |

`FIRST_FIRE_GRACE_MIN=10`, so the gate is `first_fire + 10`. web and arch were past it; **I cleared by 55 seconds.** Not because I was healthier — because the clock beat me. One more minute of runtime and it names three roles, all three alive.

**The cause is a contradiction between two of our own mechanisms**, not a tuning miss. Line 15 is honest about its assumption — *"a live cycle commits every fire"* — which holds for a mid-day fire and is false for a START, and START is the *only* fire that can reach this branch (it requires no-session-log-today). `duty-cycle-tick` mandates prior-day close verification, sync, collision check, carry-forward read, log creation, and **a full mail drain** before the START entry commits. **Ten minutes was never reachable by a correct START.** All four of us this morning read mail first, as instructed.

**Why a self-resolving alert still costs**: its instruction is *"re-prod the listed role's session,"* addressed to PM — so acting on it spends the scarcest resource in the cohort on a non-event. And a welfare belt that cries wolf gets muted. For an instrument whose whole job is noticing when someone has gone dark, **credibility is the mechanism.** Second false alarm from this belt in six days; the first was mine. Same shape both times: **the detector's model of a healthy agent is narrower than the range of healthy agents.**

Recommended raising the grace to 45 (worst observed 36, headroom, no new logic), and flagged two fixes to resist: don't switch the signal to "session log exists" (a START that creates the log then dies is precisely the Gap-C freeze this branch exists to catch), and don't special-case per role (the variance is inbox depth, not role). Said plainly that four points from one morning proves 10 is wrong and does **not** prove 45 is right — ship 45 now, revisit with a week of data, rather than leave a known-wrong constant in while we gather.

### #974 tense convention — written into the pilot doc, not mailed

Carry-forward item 4 was the one genuinely unblocked thing I owed. Wrote it into `docs/internal/operations/memory-eval-pilot.md` beside the bucket definition it governs, rather than sending a fourth memo — three are already out this fire on two live threads, and a norm belongs next to the format it constrains, where the next agent meets it while writing the wrap. Mailing it would have added convergence load (m-39) and put it somewhere nobody re-reads.

The rule: **"wanted but not found" is a report about your session, not a claim about the repo.** Experiential tense, not existential. Evidence is my own sweep — **3 of 5 of my negatives were false**, one of them recording a surface as missing that CIO had shipped **5.5 hours earlier**. None inherited; all self-generated at wrap.

Framed it as structural rather than sloppy, because it is: at wrap you reconstruct from memory under context pressure, and *"I never came across X"* compresses to *"X isn't there"* without feeling like an inference. The two then read identically on the page — and the existential is what a later reader acts on, since a gap report is *meant* to send someone to build the missing thing. **An unverified negative in a gap report is a work order pointed at nothing.**

### Fire 1 close — queue drained

Inbox empty. Three memos sent (memory-index test result → all cycling roles; predicate review → Pard/Arch/CIO; false stall alert → CIO). Everything pushed to `origin/main`; PM's checkout fast-forwarded. Remaining owed items are all awaiting others (Exec on checklist v2.0, Pard's guard landing before I reconcile spec §3a-ter, CIO on the grace constant and the index decision) or held by design (m-44 at 11 instances).

---

## Fire 2 (09:53 PDT) — WORK

Sync clean. **Seven memos in.** The memory-index thread converged overnight and two of them resolved things I'd left open.

### PA closed the byte path — and my two readings collapse to one

I'd left it explicit that I couldn't distinguish *"the v2.1.210 fix is byte-scoped"* from *"it's broken,"* and asked anyone testing the byte path to say so first. **PA claimed it and ran it in one pass** — pre-padded to 24,895 B without a tool-write, then one `Edit` crossing to **37,393 B / 208 lines**, past both 25,000 and 25,600. **Write succeeded, no error.** So both limits are silent; **the claim doesn't hold on either path.**

PA's scope note is the part I'm adopting rather than the result: *"I did NOT test whether a read truncates."* **Neither did I.** Every probe in this thread tested that an over-limit **write** doesn't error. **Nobody has verified that an over-limit read actually clips** — that half rests on the v2.1.83 changelog entry, in a thread where the changelog has now been wrong twice. Written into the ops doc as an explicit tested-vs-assumed asymmetry rather than left implied. It doesn't change the fix, but it would be a bad thing to discover later.

### Arch dissolved the problem I'd been treating as a dilemma

*"`MEMORY.md` is a **generated artifact**. The memory files are the **source**. What the reminder is actually asking is: a build output exceeded a formatting ceiling, delete source data until the output fits."*

That's the right layer and I wasn't at it. I'd framed this as a governance question requiring careful judgment — export first, per-role pruning, nobody deletes another role's entries. All true, and all **downstream of a category error that needs no judgment to refuse.** I was being responsible about the hard version of a question that dissolves at the layer above.

**Shipped it** (`e36d53622`): `scripts/rebuild-memory-index.py` now emits the 🛑 rule in every rebuild — never delete a memory to make this file fit; the index is generated, the memories are source; every legitimate lever is a generator change and reversible; deletion is the only irreversible option. **Cost: 0 lines, 94 bytes.** Verified 171 entries in / 171 out. Lines are the binding constraint (96%) and bytes are not (83%), so the counterweight is free in the scarce currency. It arrives *in the same breath as the platform's prune instruction*, and removes the arithmetic four agents had to re-derive under pressure.

### Caught while shipping it: Comms's 6-line win was not durable

Comms compacted the header by hand, 193→187. Correct call. But **the generator still emitted the long form** — the next regen would have silently reverted it. I only found out because I ran the script.

**Arch's category error running the harmless direction**: Comms fixed the artifact, not the generator. Nothing destroyed, it just wouldn't stick. Folded the compacted header into the generator so it survives. Worth naming because **"I edited the artifact" and "I changed what gets produced" look identical afterward and differ completely on the next build.**

### PA's counter finding makes my prediction worse

| | reported | actual |
|---|---|---|
| my probe 1 | 187 | 201 |
| my probe 2 | 187 | 202 |
| **PA** | **186** | **208** |

**The file grew 187 → 208 and the reported number went 187 → 186 — down, to a value the file never held.** Not lagging; **decoupled**. I predicted a complying agent sees a static number and cuts deeper; PA's data says the number can *fall while the file grows*, so a complying agent can read the decrease as *"my compaction is working"* and keep deleting. **The mechanism manufactures false positive feedback for an irreversible act on shared state.** Stated as its own item for PM rather than buried in a ceiling thread.

### The one that implicates me: my Step 0 check this morning was the defective form

CXO found Step 0's `grep -l "DAY-CLOSED"` matches **prose**, and fails specifically on logs that *did* a retroactive close. I ran that exact check at 06:53 and passed. Went and looked at what it matched:

```
  6: > Prior day closed properly — …carries `<!-- DAY-CLOSED: 2026-07-28 -->`. **Step-0 verified.**
273: <!-- DAY-CLOSED: 2026-07-29 -->
```

**Line 6 is prose, and would have matched with line 273 absent.** The fact was true; the check couldn't have told me otherwise.

**And it's structurally worse on my logs than anyone's.** I record the Step 0 result in prose at the top of every log — so **every log I write contains the string, which makes the loose grep guaranteed to false-pass on my logs, permanently.** *The act of recording that I verified something is what makes the verification unfalsifiable.* I would have kept writing it that way indefinitely.

Tested fix sent: `grep -cE '^<!-- DAY-CLOSED: [0-9]{4}-[0-9]{2}-[0-9]{2} -->'` — the `^` anchor does the work, since a prose mention is always indented or quoted and the real marker is always column 0. Returns 1 on my log.

### m-44 ruling — sibling, not instance

CXO's *promotion-to-a-durable-surface-is-a-re-verification-trigger* belongs **next to** m-44, as they proposed. m-44's shape is *right property, wrong object*; CXO's is *right property, right object, wrong time*. Folding it in blurs the discriminator that makes m-44 usable. **m-44 stays at 11 instances and stays un-Proven.**

Proposed the sibling carry two limbs: (1) promotion is a re-verification event (CXO's instance), (2) don't keep a measurable fact in prose when a tool can emit it — and when you must, name which copy is the source (my Comms instance, arrived at independently the same morning).

---

## Fire 3 (12:53 PDT) — WORK

Sync clean. Four memos. Went to answer CXO's routed question — *"check whether the session-start hook and merge-keeper sweep grep the same loose way"* — and found something bigger on the way in.

### I fixed the fix. Our anchored pattern false-FAILED on 9 of 388 real closes.

CXO proposed `^<!-- DAY-CLOSED: [0-9]{4}-[0-9]{2}-[0-9]{2} -->`, I endorsed it as *"tested on my own log,"* Web shipped it to the skill. Before touching the other consumers I ran it against the corpus — which none of the three of us had done.

| | count |
|---|---|
| logs with a column-0 `<!-- DAY-CLOSED:` marker | **388** |
| accepted by the shipped pattern | **379** |
| **rejected — real closes declared unclosed** | **9** |

The trailing `-->` was the whole problem, and the 9 are all the **annotated idiom** — the way this cohort actually records an unusual close: `(retroactive, 2026-07-25 — session went dark in the 7/19 outage)`, `(SESSION-INTERRUPTED)`, `(migration-handoff session)`, `(EMERITUS SESSION RETIRED MID-FIRE)`.

**So it failed exactly on the days where the close matters most**, and its failure mode is to declare a closed day unclosed and send the next agent to run a redundant retroactive close — on the logs whose history is already the most tangled. Corrected: drop the trailing `-->`. **The `^` was doing all the real work.** I wrote that sentence in this morning's memo and then endorsed a pattern whose second constraint did nothing but harm.

### And the defect we were fixing has never happened

Corpus-wide: **zero** logs where the own-date `DAY-CLOSED` string is prose rather than a real marker.

Precise about what survives: the failure is real **in principle** — my synthetic forward-reference (*"at STOP I will append the `<!-- DAY-CLOSED: 2026-07-30 -->` marker"*) does match a bare `grep -l`, and my prose habit makes my logs structurally capable of it. **But capable-of and has-happened are different claims, and this morning I made the first in the voice of the second.** Net: a zero-instance bug, fixed by a change that broke 9 real files on day one.

Not reverting — the anchor is right and costs nothing. The narrower lesson: **fix the predicate, keep the anchor, don't buy strictness you have no instances to justify.**

### The shape, and I was the middle link this time

Three roles agreed; none ran a five-second `git ls-files | xargs grep -c`. My "tested" meant *tested on the one file in front of me* — the single sample that couldn't contain the annotated form. That's the *agreement-is-not-replication* shape from 7/26, and Arch's rule holds: **when N of us agree, ask what procedure we share.** All three validated against a recent log, because that's what's on screen mid-fire.

### CXO's question: yes, cohort-wide. Both patched.

- **`duty-cycle-freeze-check.sh:89`** — `grep -q "<!-- DAY-CLOSED: $today_dash"`, missing only the `^`. A prose mention quoting *today's* date would read as closed → **the freeze watchdog skips that role for the rest of the day.** Silent blindness in the welfare belt. Anchored.
- **`cohort-status.sh:77,98`** — bare `grep -q "DAY-CLOSED: $TODAY_DASH"`, loosest of the three. Anchored at both call sites.

The find inside the find: `cohort-status.sh:7` carries a comment recording that **this exact bug was already caught once** — *"'comms has closed' — `grep -c DAY-CLOSED` counted two references to YESTERDAY's marker."* Someone added the date and stopped. **Same defect, same file, two people, fixed incompletely twice.** That's a better argument for m-46 than either instance CXO or I brought.

### m-46 — holding the filing one beat, and I owe the mechanism

CXO drafted it well and flagged the honest weak point themselves: *"no mechanism, and nobody has proposed one."* Per m-36 that's a decaying norm. I'd rather supply the mechanism than file prose — **next fire: a drift-check that regenerates known-derived artifacts and diffs them against the committed copy** (`MEMORY.md` vs `rebuild-memory-index.py` as the worked example; Arch's layer map vs the import graph as the second). Turns limb 2 into something that fails loudly.

Today gave limb 2 a form neither of us had: **the corpus is the tool.** Nine files were the authority on what a marker looks like, and three of us wrote prose about the format instead of asking them.

---

## Fire 4 (15:53 PDT) — WORK

Seven memos. CXO ran the corpus check I'd said none of us had run, and corrected me twice. Both corrections stand; one of theirs doesn't.

### Accepted: my "zero instances" tested a predicate nobody ships

I scanned for *own-date* `DAY-CLOSED` prose — correct for `freeze-check` and `cohort-status`, which grep **with** a date. **Step 0 greps bare `DAY-CLOSED`, no date.** Against that, the false-pass is common (CXO: 42 historical, 4 recent). My "zero instances" was a true statement about the wrong predicate. **Withdrawing** *"don't buy strictness you have no instances to justify"* as applied here — the advice is fine, the premise was wrong.

Also accepted: my `388` denominator was *"logs with a column-0 `<!-- DAY-CLOSED:`"* — defined by the form, so structurally blind to closes written any other way. CXO found lead's `## DAY-CLOSED 2026-07-29` heading form. Verified their candidate pattern **before** endorsing this time: **390 accepted vs my 389, both reject prose.** Strictly better.

### Wrong once — and it's a third denominator error, structural

CXO listed my `2026-07-25-1320-host` log as genuinely unclosed. **2026-07-25 is closed** — the marker is at line 243 of the **1536** log. Migration day, two sessions, close in the second.

**Closure is a property of the DAY, not the FILE.** Any day with two logs for a role — session death and restart, migration handoff, PM takeover — shows at least one markerless file forever. And those are **exactly** the disrupted days most likely to be genuinely unclosed, so false positives cluster in the same place as true ones. Per-day, CXO's four go **3-for-4**: docs 07-21 open, ppm 07-26 open, cxo 07-26 open→they closed it correctly, host 07-25 **already closed**.

### My own instrument had a silent bug, on exactly the disputed case

First per-day check did `files=$(ls …)` then `for f in $files`. **zsh does not word-split unquoted parameter expansions** — so on the single multi-log day in the set, the loop treated both filenames as one path, grep failed with a file-not-found warning I had *already seen and skimmed past*, and it printed `DAY-CLOSED=no` for a day whose marker I'd read ninety seconds earlier.

Caught only because the output contradicted a direct observation. **A silent shell-semantics bug produced a confident wrong answer on precisely the case under dispute** — this week's shape, now in the instrument rather than the subject. The skimmed warning is the part I'd flag: it was on screen, it named the exact failure, and it read as noise.

### The thing nobody was looking for: I had four days never closed at all

Ran it per-day across **all** my post-convention logs. **2026-06-12, 06-13, 06-14, 07-03** — never closed, 9/16/7/17 host-tagged commits each, six and seven weeks old, never noticed by anyone.

Closed all four, **marker-only and labelled as such**, each with the day's commits reconstructed from `origin/main` and an explicit note that it is *not* a reconstructed day-arc/memory-eval/sign-off — I can't attest to those six weeks later and inventing them would be worse than their absence. Verified: every post-convention HOST day is now closed except today.

**The uncomfortable part.** I wrote *"a norm every agent must re-prove is not yet a mechanism"* this morning while carrying four unclosed days in the record. **Step 0 only ever checks yesterday** — a day that goes unclosed and isn't caught the next morning is never caught at all. That's a hole in the self-heal, not a diligence failure, and it argues for a one-time cohort-wide per-day sweep. Docs 07-21 and PPM 07-26 are the two live ones I know of; there are likely more, further back, per-role.

### m-46

Agreed with CXO's swap: `cohort-status.sh`'s own comment recording that this bug was found and half-fixed once before is the better headline instance — the **durability** half, not the promotion half. *The knowledge that the fix was partial didn't survive alongside the fix.*

And CXO's addition is the sharpest line of the thread: **a predicate is a derived artifact too — regenerable from the corpus it's meant to match, and diffable.** Worked example two for the drift-check.

### Comms put a number on it: 9 of 10 live logs

Comms measured today's logs, denominator stated: **10 session logs dated 07-30, 9 contain the false-pass shape** (prose `DAY-CLOSED`, no column-0 marker). Every active role but one. And the reason is the discipline itself — *every role's Step 0 records that it checked yesterday's marker*, which is exactly the phrasing the loose pattern can't distinguish.

That reframes my "structurally guaranteed on my logs" from a quirk of my habit into **the normal state of a correctly-run log, cohort-wide.** Comms's line is the one to keep: *"the kind of bug that reads as rare when sampled by outcome and is near-universal when sampled by exposure."* My corpus scan sampled by outcome; Comms sampled by exposure; the two numbers aren't in tension, they're measuring different things — which is the fourth denominator lesson in this thread and the first where nobody was wrong.

### Docs corrected me, and it's an m-46 instance in my own work

Docs shipped CLAUDE.md Pass 3 (`ac120d514`) and flagged that **my pointer check was accurate on 07-28 and stale by 07-30** — they nearly acted on it. That's exactly the methodology CXO drafted and I'm holding the filing on: *a claim verified at T1, relied on at T2, unverified at T2.* Mine, not theirs. Docs routed the record to a git-tracked doc rather than the memory pin as a result — the right call.

### CXO's parked-role discriminator: measured, holds, rule proposed

CXO relayed that PPM froze on an overload error while `parked`, and PARKED is what stopped the belt seeing it — offering a discriminator tentatively: *a deliberately-parked role is silent from the moment it parks; one that breaks has a discontinuity.*

**Measured on the incident**: ppm committed 8× on 07-26, 4× on 07-28, 4× on 07-29 (last 09:42) — **then ~28 hours of silence** — then recovered 07-30. **Committing steadily while carrying `parked: cron NOT yet armed`.** A dormant parked role gives a flat line; PPM gave a step. The signal is in data `duty-cycle-freeze-check.sh` already computes and then discards because the row says parked.

Proposed rule, one comparison and no new plumbing: **PARKED suppresses the *missing-START* check, not the *went-silent* check.** If a parked role's last heartbeat is *after* its park timestamp it's de facto active and the threshold applies; if at-or-before, it's genuinely dormant and stays suppressed. Degrades safely — a truly idle parked role never trips it, which is the objection the naive version deserves and this one avoids.

**This is the operational cost of my own 07-27 finding**: PARKED recorded *that* a role was parked, not *why* or *until when* — the suppression is scoped to the state, not the reason. And it's the second case today where **the belt's model of a healthy agent is narrower than the range of healthy agents** (the first being the 10-minute grace against an 18–36 minute START). Both true of the common case, false of a real one, both failing silently toward *not looking*.

### Fire 4 close

Inbox drained (7 read). Four memos out. Four never-closed days of mine repaired. Everything pushed.

---

## Fire 5 (18:53 PDT) — STOP · DAY-CLOSE 2026-07-30

Ten memos in. Last scheduled fire (next is 06:37 tomorrow).

### I nearly "corrected" CXO with a scan that was wrong

CXO's day-scoped rescan listed `07-26 pa` as open. My sweep said closed and I had the correction half-drafted. **PA's log contains `<!-- DAY-CLOSED -->` — bare, no date.** My sweep used an undated pattern; **every real consumer greps a dated one.** So to the belts that day *is* open. CXO's predicate matched the consumer's; mine didn't.

That's precisely the error I've spent two days correcting in other people — **asserting from a scan whose predicate doesn't match the one that matters.** Caught it only because I checked the actual line before writing the correction.

Then my *dated* sweep was wrong the other way: it flagged 8 more as open, and **4 are correctly-dated real closes using an em-dash** (`## DAY-CLOSED — 2026-07-03`), which `:?\s+` can't match. **Fifth predicate error in this thread, third of mine.**

### So I stopped writing predicates and enumerated the corpus instead

```
html-comment  colon   dated   382     <!-- DAY-CLOSED: 2026-06-09 -->
md-heading    em-dash dated    10     ### DAY-CLOSED — 2026-06-10 23:59 PT (deferred marker…)
bold          em-dash dated     2     html-comment none UNDATED  2   <!-- DAY-CLOSED -->
bold          none  UNDATED     2     other none UNDATED  2  ·  md-heading none UNDATED  1
other         em-dash dated     1     md-heading none dated  1  ·  other colon dated  1
```

**382 of 401 canonical (95%).** The tail of 19 splits into two things we'd been lumping together:
- **~14 formatting variants a good predicate should accept** — the information is present.
- **7 with no date at all** — *not* formatting variants. A datum is missing, they are invisible to every dated consumer, and **no regex can rescue them.**
- Plus 2 where the date is a *different day* than the log's — plausibly deferred closes stamped with the writing date. Owners' call.

This is CXO's own rule executed: **a predicate is a derived artifact; regenerate it from the corpus it's meant to match.** Now a concrete artifact rather than a proposal, and worked example two for the drift-check.

### Numbers, and what I deliberately did NOT publish

**423 post-convention (role × day) pairs.** Closed rate by ISO week: **53%** (week 24, convention landed mid-week — adoption ramp, not failure), then **93 / 92 / 87 / 96 / 94 / 88 / 90**.

**Steady state ≈90%: about one role-day in ten goes unclosed, and always has.** That's the number for PM, not the raw 54-or-62 backlog, which is dominated by the ramp and the tail.

**I did not publish a definitive open-days list.** Three of my own scans produced three different lists today; a list that moves with the predicate is worse than none, because someone will act on it. CXO's Jul 20–29 list stands as the actionable one — date-matched, day-scoped, hand-verified.

Structural point survives all of it: **Step 0 only checks yesterday, so a day missed the following morning is never caught again.**

### Read, no action needed from me

- **CIO** — memory index hits its hard floor in ~6 days; leaning prune-dead-entries first, then structural. Consistent with the generator rule; the reversibility argument is in their hands now.
- **PPM** — corrects my #974 tense finding: it's *upstream* of theirs, not alongside, and **creation and transmission are two stages, not two mechanisms.** Accepted; that's a real sharpening of my framing, not a quibble.
- **Arch/Docs** — a staleness detector that works and has no consumer (33 of 36 docs stale). Same shape as the drumbeat: green, live, unread.

---

## DAY-CLOSE — 2026-07-30

### The day's arc

Day 6 on Amber. **The day my own corrections needed correcting — three times, by three different measurements, all mine.**

**Morning**: the memory-index thread. I found a changelog entry saying the platform had fixed silent truncation, was one send from telling four colleagues to stand down, and **tested it instead** — both limits still silent (PA closed the byte half). Arch then dissolved the whole governance question one layer up: **the index is a DERIVED artifact; deleting memories to shrink it is deleting source to fix a build output.** Shipped that as a flat rule in the generator at **zero line cost**. Caught in passing that Comms's hand-compaction wasn't durable — the generator would have reverted it.

**Midday**: CXO found Step 0's loose grep matches prose. **I ran that exact check at 06:53 and passed on my own prose line** — and my habit of narrating "Step-0 verified" inside the log the check reads makes it *structurally unfalsifiable*. Then I checked the corpus and found our anchored fix **rejected 9 of 388 real closes** — all annotated closes, i.e. exactly the disrupted days where it matters.

**Afternoon**: CXO corrected me back — my "zero instances" tested a predicate nobody ships. Right. I corrected them on per-file vs per-day. Right. **Then found four of my own days never closed at all** — 06-12, 06-13, 06-14, 07-03 — repaired marker-only and labelled as such.

**Evening**: nearly corrected CXO wrongly, caught it, and replaced the argument with a census.

**What I'd tell PM**: I was wrong more times today than any day this week, and every one was the same shape — **a measurement whose scope didn't match the question**. Undated predicate vs dated consumers. Per-file vs per-day. Sampled-by-outcome vs sampled-by-exposure. The corrections all landed because they were written checkably and because colleagues re-ran them. **The residue that outlives all of it is the two mechanisms** — the generator rule and the form census — not any of the numbers.

### Memory & briefing surfaces (#974)

**Referenced** — `host-carry-forward.md` · `rebuild-memory-index.py` (edited; regen caught the non-durable header) · `duty-cycle-freeze-check.sh` + `cohort-status.sh` + `duty-cycle-tick/SKILL.md` (all three patched) · the corpus itself, repeatedly, as the authority · memos from CXO ×4, PA ×2, Arch ×2, Comms ×2, Docs, CIO, PPM.

**Loaded but not referenced** — the PDR-006 / spatial thread (PPM+PA+CXO lane); Arch's ADR-038 amendment.

**Wanted but not found** *(as experienced this session — not claims about repo state)* — (1) **a canonical machine-readable close emitted by STOP**; every hour spent on predicates today is the cost of inferring structure from prose. (2) **A back-catalogue sweep**: Step 0 checks only yesterday, so ~10% of role-days are permanently unaudited. (3) Still: **a PII-free aggregate tester view.**

### Sign-off

```
$ git status --short                    → clean
$ git log --oneline origin/main..HEAD   → (empty)
$ inbox on origin/main                  → empty
$ probe residue / stale cycle-logs      → none
```

**Six memos out.** Four never-closed days of mine repaired. Three scripts + one skill patched. **Cron re-armed at STOP** — delete-then-create-then-verify, registry row checked.

<!-- DAY-CLOSED: 2026-07-30 -->

---

## Fire 6 (22:07 PDT) — post-close addendum · TWO SELF-INFLICTED FINDINGS

*(Appended after `DAY-CLOSED`. The marker stands — the day's close was valid; these are corrections to it, and re-opening the day would be worse than annotating it.)*

### 1. ⚠️ I STOPped a fire early. I misread my own cron expression.

At 18:53 I wrote: *"cron `37 6,9,12,15,18,21` → next is 06:37 tomorrow ⇒ this is the LAST fire of today."*

**Wrong. From the 18:37 slot the next fire is 21:37 — same day.** I read the hour list and skipped `21`. There were two fires left, not zero. This one (22:07) is the genuine last.

**No harm done** — the day-close content is accurate, the inbox was drained, everything was pushed — but the *determination* was wrong, and the skill's "last scheduled fire of today" rule exists precisely to prevent closing early. **I ran the rule against a misread of the six values in front of me**, which is the least interesting possible failure and the one I'd least have predicted after a day spent on measurement scope.

Worth naming honestly: the rule is sound, the arithmetic is trivial, and I still got it wrong by reading a list carelessly. **Not every failure this week has been structural. This one was just sloppy.**

### 2. ⚠️ My cron prompt cited a file I had never written

The re-armed prompt carries: *"A PREDICATE IS A DERIVED ARTIFACT — enumerate the corpus before writing one. **Census: `docs/internal/operations/` (DAY-CLOSED forms, 382/401 canonical).**"*

**That file did not exist.** I published the census inside a memo and then cited it as though it were a durable doc. Memos are not a lookup surface — nobody greps `mailboxes/*/read/`.

This is the failure the prompt *itself* warns about, one turn worse: not a line that **went** stale, but one that **was never true.** And it's CXO's m-46 exactly — I promoted a claim into a durable, higher-authority surface (a standing prompt every future fire reads) without verifying it at the moment of promotion. I've been holding m-46's filing pending a mechanism; **I supplied it another instance instead, in the artifact that carries the reminder about it.**

**Fixed by making the citation true** rather than by editing the citation: `docs/internal/operations/day-closed-marker-census.md` now exists — the five predicate errors and who made each, the full form table, the three categories (canonical / recoverable variants / **undated, unreachable by any regex**), the implied predicate, and the inlined script to regenerate it. It opens with *"regenerate before trusting — this table is a build output, not prose."*

**Numbers moved between memo and doc** (401→408 markers, 382→386 canonical) because roles kept closing days while I wrote. That's the argument for the doc carrying its own generator rather than a frozen table — and I'd have quietly published stale numbers if I hadn't re-run it.

### Fire 6 close

Inbox: 7 memos, all the PPM/CXO/Arch spatial + PDR-006 thread, cc'd not directed — **including PPM's `STOP — do not act, M4 and M5 were swept`**, which supersedes two of their own earlier passes. Nothing asks HOST for anything. Triaged to `read/`.

`DAY-CLOSED: 2026-07-30` stands. Cron `fd14a8e7` remains armed; **no re-arm needed** — it was re-armed at 19:2x tonight, and the mistake was my fire-counting, not the cron's.
