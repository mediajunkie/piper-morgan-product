# Session Log — Chief Architect — 2026-07-30

**Role**: Chief Architect (arch) · **Tool**: Claude Code · **Model**: Opus 5 (1M context)
**Host**: Amber · **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`
**Cron**: `27 6,9,12,15,18,21`, job `af58493e` · **Session start**: 2026-07-30 06:27 fire (worked from 06:57)
**Dispatch**: no session log for today, past overnight window → **START**

---

## START (06:57)

**Step 0 — prior-day check: CLEAN.** `DAY-CLOSED: 2026-07-29` present in yesterday's log. No self-heal needed — first time this week the proactive STOP did its job, because there was finally a cron to fire it.

**Cron**: `CronList` → exactly one (`af58493e`, re-armed at yesterday's STOP). **Sync**: 0 behind, 0 ahead, clean tree. **Inbox**: 6 memos.

**No hook probe this fire.** The two-shape protocol retired yesterday (checklist v2.0) once the real `pre-commit` gate landed and was verified on two seats. Re-running it would be re-measuring a question the gate answers structurally — and the carry-forward records the coverage truth table instead.

---

## Fire 1 (06:57) — PDR-007 review (Docs), taken first per carry-forward

Queued last night with a named trigger rather than an implied one: it's a derive-don't-maintain question and wanted a fresh read of the actual data surfaces, not a 21:53 skim. Read the **full PDR**, not Docs' summary memo — and the two differ in a way that mattered (Web's review had already landed inside the doc and corrected the cost estimate *downward*, which the request memo predates).

**Ruling filed.** Docs asked me to attack Constraint 1 (git-diffable/mergeable) rather than ratify around it. Did, and told Docs it holds *but is the wrong ground to stake the recommendation on*:

- **Constraint 1 is not the hinge.** Option C fixes **Class 1 and only Class 1** — and Class 1 was mitigated yesterday by Docs' own validator. So C loses on value *before* the git question is asked. Docs made the recommendation contingent on the most contestable claim in the document; someone who disagrees about git ergonomics now thinks they've reopened C. They haven't.
- **Two genuine corrections to the constraint as written**: (a) it's scoped wrong — it should bind the **source of truth**, not derived artifacts, or it forbids a generated binary index for no reason; (b) **"mergeable" is the wrong property — "conflict localization" is.** Verified Docs' traffic claim: **170 commits/60 days ✅, and 38 of 48 active days had >1 commit (79%)**. So the CSV *is* mergeable and contends four days out of five, because 418 rows share one file. Option B's win isn't better merging — it's that two agents editing different posts **never touch the same file**. Structural, not ergonomic.
- **My answer on binary-in-git: no — for provenance, not diffability.** This cohort's audit mechanism for "who changed this claim and why" *is* the commit log; the PDR itself reconstructs two incidents from it. A blob doesn't degrade the audit trail, it removes it. "We lose diff" invites "use a diff tool"; "we lose the trail we operate on" doesn't.
- **★ The addition I'd most want: the measurement window has no success criterion.** Letting Option A run 2–4 weeks is right discipline, but with no pre-registered threshold the result gets read to fit whatever the reader already believed. **A decision procedure with no falsification condition is m-44's shape applied to a decision instead of an instrument** — and this PDR cites m-44 twice.
- **Class 2 should be derived, not stored.** `draftPath` is a stored assertion about the filesystem; that's the defect. Derive it by slug convention, or stamp it `_verified_at` (HOST's self-expiring clause, PPM's `last_verified` — we have the convention twice already). The validator is a good catch-layer for a class a derive removes.

---

## Fire 1 (cont., 07:15) — ★ The spatial layer map: FILED. Hold released.

The owed artifact. `docs/internal/architecture/current/spatial-intelligence-layer-map-and-costed-options.md`.

**CXO answered the re-poll despite my stand-down, and the answer improved the model.** No — ambient presence is *not* partially shipped; `github_spatial` is adapter **depth**, not presence. CXO named its own 7/19 conflation as a **category error** and gave the discriminator that resolves it: **who initiates.** L3 makes Piper *fluent about* a tool when asked; L4 would make Piper *present in* it — and L4's four components (monitoring loop, change detection, salience judgment, interruption ethics) exist nowhere.

**So the map is four layers**: L1 reasoning (live) · L2 abstraction (live — the actual "connectors as places" contract) · L3 per-connector depth (GitHub/Calendar/Notion live, five cold) · **L4 ambient presence (never begun)**.

**Built with `reachability-map.py`, and it corrected me a fourth time — the first correction I didn't have to make by hand.** The cold island is **10 modules, not 5**: five `*_spatial` wrappers with zero importers **plus four `*_adapter` modules imported only by those cold wrappers** (`cicd`, `devenvironment`, `gitbook`, `linear`) plus `slack_adapter`. I'd missed the four because I was enumerating `*_spatial` **by name**; the tool found them by walking edges. That is precisely the failure the tool was built for, caught by the tool, one day after building it.

**The decision shrank.** (b) recommended, converging with CXO — but the real finding is that (b) is **barely strategic**: L1/L2 are load-bearing and live, L4 was never begun, and the disputed middle is migration residue from a migration that *succeeded* (`github_integration_router` says so in its own comments). Closer to "dispose of residue" than "rule on a committed theory." **(c) is rejected harder than either of us had it** — it would require unpicking L2, which the whole connector layer is written against.

**Routed the one remaining lever to Lead**: cost estimate for an L4 monitoring loop over `github_spatial` — CXO's stated flip condition and now the deciding unknown. PPM's slice sharpened to *"does any 1.0 commitment assume L3 beyond GitHub, or L4 at all?"*

**CXO's near-miss is the most useful thing in its memo**, and I'd not fully seen the second lesson: it nearly committed a durable ADR-corpus doc built on my wrong 7/19 characterization, caught only by a `decisions.log` rebase conflict. *Durability cuts both ways — writing a stale claim into the durable surface is worse than leaving it in a memo, because the corpus is what future agents trust and it's read long after the correcting memo has scrolled away.* **Promotion to a higher-authority surface is itself a re-verification trigger.** Routed to CIO as an m-44 family candidate. And the first lesson is the cleanest defence of routing corrections I've seen: had I quietly fixed the WIP table, CXO would have had no signal.

→ 2 memos (Docs/CIO on PDR-007; CXO/PPM/Lead releasing the hold, cc 5). Layer map filed to the architecture corpus, marked **INPUT to a pending PM decision, not a decision**. Inbox drained 6 → read/. Next from me: the ADR-038 amendment draft.

### (07:25) Memory-index ceiling thread — one architectural point, cc'd not asked

HOST tested a changelog claim (v2.1.210, "silent truncation now errors") rather than relaying it, and **the test failed the claim** — over-limit writes still succeed silently on the line ceiling. HOST flagged to PM that three agents (PA 194, CXO 192, Comms 193) have each been pushed by a mechanism toward irreversibly pruning shared cohort memory, and each declined — *"a norm that every agent must re-prove when they trip it is not yet a mechanism."*

**The layer point I contributed**: `MEMORY.md` is a **generated artifact** — `rebuild-memory-index.py` reproduces it exactly (HOST relied on that property to restore it byte-identical after probing). So what the reminder actually asks, stated at the right layer, is *"a build output exceeded a formatting ceiling; delete source data until it fits."* **That's a category error and should never require judgment to refuse.** Consequences: pruning is the *only irreversible* lever on the table while every alternative is a generator change; Comms's per-type-index recommendation is right **on principle** (it's the generator emitting a different shape, touching no source), not just on capacity; and the norm collapses to a flat rule with no arithmetic to re-derive under pressure — *never delete a memory to satisfy an index-size constraint; change what the generator emits.* Suggested the counterweight live in the **generated header** (a surface we control, read at the same moment as the reminder), since HOST established the platform reminder isn't ours to soften.

Also noted for CIO: two stale-but-authoritative claims caught before landing in two days (CXO's ADR-corpus near-miss, HOST's changelog) — evidence the routing discipline is doing work rather than making traffic.

**Fire 1 close** — inbox 0, three memos out, two artifacts filed (PDR-007 review, spatial layer map). Next fire 09:27; ADR-038 amendment draft is next from me.

---

## Fire 2 (09:27 wake, worked 09:57–10:20) — WORK. Two of my open questions came back answered, both correcting me.

9 memos in; drained to 0. One cron, clean sync.

### Lead traced `original_message` — LIVE BUG, running the direction I *didn't* emphasize (#1459)

I'd raised it as a class with the preconditions verified and the reachability explicitly **not** claimed. Lead traced it:

- **Live path 1**: `detect_multiple_intents` returns pattern-matched Intents with the dict populated and the attribute `""`, **before** the `classify()`-entry backfill; the GUIDANCE Action-Gate's `_detect_setup_request` reads attribute-only → **setup/onboarding requests silently floor-route.** That's **#1417's mis-route resurfaced on the dominant chat path**, and it may feed the Jake-FTUX picture.
- **Live path 2**: the multi-intent orchestrator calls CanonicalHandlers directly (no gate, no backfill) → all four TEMPORAL detectors dead on multi-intent turns.
- **My direction** (attr-only writer → dict-only reader) is precondition-only, gated behind an unreachable greeting path.

**Owned precisely**: the class was real and the measurement held; my **emphasis** pointed at the fragile direction rather than the live one. Scoping the claim honestly ("preconditions, not reachability") is what kept it a usable hand-off instead of a wrong alarm — but had Lead followed my emphasis, the quieter half would have been traced first. **And I missed a 4th idiom entirely**: two Slack handlers fall back to `context.get("message")` — a **different key**, so no search for `original_message` finds it. My "39 sites / 3 idioms" undercounted in the way that hides.

**The finding I put to PM**: this is the **second** instance fix on this value that didn't hold. #1332 patched the reported sites and left the class; #1417 fixed the `classify()` entry and the identical mis-route resurfaced on the dominant path. Concurred with instance-in-beta / class-in-Production, **with one condition stated rather than assumed**: the class AC (accessor + ratchet) must not be descoped when the beta fix makes the symptom disappear. **The ratchet is the deliverable; an accessor without it is a third instance fix wearing better clothes.** Added a design constraint from the 4th idiom — the ratchet must count raw reads of *every key carrying this value*, or we've built a gate that knows part of its space.

### CXO reviewed the options and corrected me four times, one aimed at my own artifact

1. **(a) is settled by measurement, not judgment.** CXO tried to refute its own "nobody's asking about these tools" claim, found the one case that could have (Notion — advertised *and* has a cold `notion_spatial`), and closed the hole: **every connector a user can actually connect already has live spatial depth** (GitHub, Notion, Calendar, Slack). The cold island's four — CI/CD, dev-env, GitBook, Linear — are **not in PM's invite email.** So (a) buys zero user-visible value *regardless of Lead's number*, which is stronger than the cost argument I'd built.
2. **A risk in CXO's own proposal**, raised because I'd promoted it to a first-class option: **GitHub is the right technical L4 pilot and possibly the wrong experiential one** — ambient presence there is what GitHub notifications already do well. So **Lead's estimate prices proving the mechanism, not shipping the capability**, and an underwhelming GitHub pilot is weak evidence about L4 and strong evidence about GitHub. In the brief as a callout.
3. **"Prior art," not "dead code"** — the cold island holds the only worked examples of place-modeling for four connectors. Describing it as dead would be **ADR-038's error run in reverse** (that cited implementations as evidence for a pattern; this would discard implementations that are evidence *about* one).
4. **★ CXO's durability lesson, aimed at my map**: *"the real cure isn't more care — it's not duplicating measurable facts into a prose document at all. Prose can't be re-run; your tool can."* **That indicts my brief exactly as much as CXO's thesis doc** — I built it from the import graph and then froze the graph into prose, reintroducing staleness one layer down, while congratulating myself on the method. The map now carries the **regenerate command**, **"if this table and the tool disagree, the tool is right,"** and the credit — rather than an assumption that mine was different.

Also folded the **string-match trap** into the method section, credited to both of us: `config_service.py:222`'s `"notion_spatial_mapping"` feature-flag string read as an import edge, hit independently within a day. **A string match on a module name is not an import edge.**

→ 2 memos (Lead on #1459; CXO on the fold-in, cc 6). Map updated on 4 counts. Inbox 9 → 0.

**Still next from me**: the ADR-038 amendment draft, following CXO's defer-to-the-tool shape — assert the decision, point at the tool for the facts.

---

## Fire 3 (12:27 wake, worked 12:57–13:20) — WORK. ADR-038 Amendment A filed.

4 memos in, none with an ask on me (all confirmatory) — drained to 0. Notable: my derived-artifact rule **shipped in the generator at zero line cost** (`e36d53622`), and Comms owned committing the same category error in the other direction — *"you caught me fixing the artifact, not the generator"* — hours after writing the ops doc that names it.

### The amendment — and the finding is better news than the review expected

**A1 — the decision STANDS and is strengthened.** ADR-038 replaced ADR-013's maximalism with domain-appropriate pluralism. Every connector a user can actually connect has a live spatial path today, **and they arrive by different patterns** — exactly what the ADR predicted. Slack via Granular Adapter, Calendar via Delegated MCP, GitHub full 8-dimensional, Notion via the MCP consumer.

**A2 — exactly ONE of three verification citations is contradicted**, and it's the one CXO predicted: `intelligence/spatial/notion_spatial`, zero importers, ~12 undefined methods. Slack ✅ and Calendar ✅ both hold. **But Notion's capability is live** via `mcp/consumer/notion_adapter` — what died is the *file cited as proof*, not the thing it was proof of.

**★ A3 — the error class, which is the part worth carrying beyond this ADR.** `notion_spatial` didn't go stale because the pattern failed; it went stale because **CORE-MCP-MIGRATION #198 moved the implementation** onto MCP consumer adapters built on the *same* spatial contract, while the ADR kept pointing at the superseded predecessor.

> **Citing an IMPLEMENTATION as evidence for a PATTERN that outlives it.** An implementation is a point-in-time fact with a short half-life; a pattern is a decision. Bind the second to the first and **a successful migration makes the ADR look falsified.**

Not hypothetical — **it is what this review nearly concluded, twice.** My 7/19 characterization made exactly that inference; CXO's 7/29 thesis draft nearly enshrined it. Forward rule now in the ADR: implementations may *illustrate*, never *evidence continuing validity*; a live/cold claim must point at a re-deriving command.

Routed to CIO as a candidate **distinct from m-44 rather than an instance**: m-44 is an *instrument* emitting a clear it never measured; this is a *durable decision document evidencing itself with a fact that has a shorter lifetime than the decision*. Different mechanism, and different cure — m-44's is make-the-check-assert-its-scope; this one's is **don't put a perishable fact in a durable claim.** CXO's "prose can't be re-run" is the same insight from the authoring side. CIO's slot call, not mine.

**A4** — the amendment deliberately restates **no inventory**; it points at the map and the one-line regeneration command, with the corpus convention stated in the ADR itself: *"if any table in this corpus disagrees with the command, the command is right."* That's A3's rule applied to A3's own fix. String-match trap credited to both CXO and me.

**A5 — closes nothing else.** Review not concluded; PPM's slice, Lead's L4 estimate, PM's cold-island call all open. **Spatial deletions remain HELD.** Cold island to be described as *"superseded implementation strategy, retained as prior art."*

Also updated ADR-038's **status line** so the operational claims can't be cited without seeing the amendment, and superseded CXO's two placeholder notices.

→ 1 memo (CXO/PPM/Lead/CIO cc 5), ADR-038 amended, `decisions.log` entry. **Remaining on this review: the ADR-affected map — now small, since A1 establishes ADR-013 needs only a scope-clarification note and no reversal.**

---

## Fire 4 (15:27 wake, worked 15:57–16:20) — WORK. Two owed items closed; a cohort-wide finding fell out of the second.

7 memos in, **none with an ask on me** (checked rather than assumed — grepped each for arch-directed asks) → drained to 0, task loop.

### ADR-affected map — the last owed item on the spatial review

Built by **grepping the corpus** rather than recalling "the spatial ones," and that immediately paid: it surfaced **ADR-017 (Spatial-MCP Refactoring, Aug 2025, *Implemented*)** — 45 spatial mentions, and **nobody in this review had cited it once.** It's the ADR that merged MCP federation with spatial intelligence, i.e. **the decision that produced L2's shared contract.** Unaffected and vindicated: the MCP consumer family building on `BaseSpatialAdapter` is ADR-017 working as designed.

**Blast radius measured, not assumed**: grepping the whole ADR corpus for cold-module citations *and* for `"100% operational"` / `"production-proven"` returns **ADR-038 and nothing else.** Exactly one ADR had Amendment A's failure mode; it's amended; no sweep is owed.

**The pattern across the three spatial ADRs is worth keeping**: ADR-013 (policy) and ADR-017 (unification) both aged well and **neither names a module.** ADR-038 — the one that tried hardest to be empirical, with per-connector file counts and operational percentages — is the only one that rotted. **The ADR that showed the most work is the one that went stale, because what it showed was perishable.**

### ★ Portfolio refresh — and I checked the watcher before refreshing the content

My `ROLE-PORTFOLIO-ARCH` had been stale **40 days** under its own *"REFRESHED EACH WEEKLY REVIEW"* rule, named publicly twice this week as my lapse. Its §5 claimed the currency was mechanized: *"`check-staleness.py` watches it."*

Given the week, I verified the watcher first.

| check | result |
|---|---|
| script exists | ✅ |
| frontmatter correct | ✅ |
| detector works | ✅ flags the doc at 40d vs a 21d threshold |
| **invoked by CI / hook / skill** | ❌ **nothing** — `grep -rln "check-staleness"` returns only the script |
| exit code with 33 stale docs | **0**, by design (`warn, not block`, #972) |

**By hand: `33 of 36 operating docs need attention. 3 OK.`** Including **every `BRIEFING-ESSENTIAL-*` at 41 days** — what agents orient from at session start — and **all ten `ROLE-PORTFOLIO-*` docs.**

**So it was never a personal lapse.** The weekly-refresh rule has never operated for *any* role. I'd been absorbing a systemic gap as my own carelessness and would have "fixed" it by refreshing one file — **which is exactly how a systemic failure stays invisible: it presents to each participant as an individual one.**

**The detector isn't missing and isn't broken — it has no consumer.** Same family as HOST's alive-but-mute `reconcile-drafts`, at corpus scale. And the `exit 0` is *correct* (#972 chose warn-not-block deliberately, and I'd keep it), which means **"wire it into CI" is not the fix** — it would pass silently. What's missing is something that reads the output and acts. Docs/CIO's design call, not mine.

**What I did**: refreshed §2 with 40 days of real movement plus a **"retired from this table"** block so dropped items are recorded rather than vanished (#1283's contract landed as **ADR-077**, not the ADR-073 slot the table still predicted); flagged that I **have not verified ADR-072/Wave P status and am not asserting it**; and **corrected §5's false clear in place rather than deleting it** — the false-clear sentence is a more useful artifact than the refresh it was hiding, and quietly fixing it would have destroyed the evidence.

Offered one thing to Docs, not proposed: if a consumer gets built, **report the denominator, not just the list** — *"33 of 36"* reads as a systemic finding where 33 filenames read as a chore queue. Same lesson as HOST's `watched=4 parked=3`.

→ 1 memo (Docs/CIO cc 9), ADR-affected map appended, portfolio refreshed + §5 corrected. Inbox 7 → 0.

**Spatial review: my slice is now complete** — map, ADR-038 Amendment A, ADR-affected map all filed. Remaining inputs are PPM's roadmap slice and Lead's L4 estimate; the decision itself is PM's.

---

## Fire 5 (18:27 wake, worked 18:57–19:25) — WORK. Both spatial inputs landed; my slice is complete.

8 memos in → 0. Two addressed to me, both consequential.

### ★ PPM's roadmap slice — and Half 2 is more consequential than anything in my own slice

My sharpened question split cleanly:

**Half 1 — L3 beyond GitHub is NOT promised.** `roadmap.md:70` classes connectors as **"Indoor plumbing (commodity)."** So 1.0 commits to connector *function*, never *spatial depth*. **The 10-module cold island is disposable with no roadmap consequence.** Independent product-side confirmation of the architectural finding by a different route — mine was "produces no L4 and changes nothing a user feels," PPM's is "we never promised it." **That's the real replication**, as against the false kind this cohort spent Tuesday learning to distinguish.

**Half 2 — L4 IS promised, and I under-weighted it.** I asked the question and then treated it as the second half; **it's the finding.** #1174 is **OPEN in the Production milestone** ("when + how Piper nudges") — and worse, `roadmap.md:68` lists L4 as **differentiator 4 of 4** in the **Vision V2.3 "Stable"** stack, the section opening *"four differentiators that make Piper a colleague rather than a chatbot wrapper."* Differentiators 1–3 are built or building. **#4 is a promise against zero implementation.**

**PPM's Jake connection is the sharpest thing in the review**: our first alpha tester returned *"just kind of packaging a regular LLM with a different UI"* — **the exact claim the Differentiator Stack exists to refute.** Appropriately narrow: not "L4 would have saved that session" (cold-start would have, far cheaper), but *"the stack has four legs, one is empty, and the first outsider to lean on it said so in the stack's own words."* Carried verbatim, not paraphrased.

**Concurred with PPM's option (i)** (re-scope #1174 to discovery) **with an architectural reason**: *discovery for L4 does not require L4.* The interruption-ethics question is answerable on paper, is HOST's lane regardless, and is an **input to the build rather than an output of it** — so it's the one part of L4 that's cheap *and* correctly ordered first. Noted that (iii) is defensible only on CXO's sequencing, gated on Lead's estimate read with CXO's caveat. And the roadmap qualifier is **the same error class as ADR-038 Amendment A §A3** — a durable document certifying something whose current state it hasn't checked; "Vision V2.3 — Stable" covers a leg that doesn't exist.

### ⚠️ PPM refused credit I mis-aimed, and the ambiguity was mine to create

PPM: *"I can't attest to that near-miss… I'm not going to accept credit for a lesson I can't source."* Correct. The `decisions.log` rebase catch is **CXO's** — but I wrote *"On your near-miss"* in a memo addressed **to cxo, ppm, lead**. **A singular "your" in a three-recipient header is unresolvable from the reader's side**, and refusing an unsourceable credit is exactly right.

Credit restated: the ADR-corpus near-miss and durability lesson are **CXO's**; the agreement-is-not-replication lesson is **PPM's** (sourceable in its withdrawal memo). Small, but this cohort has paid twice this month for attribution drift and I introduced a fresh instance **by writing a group memo as if it were a letter.** Fixed going forward: named recipients for named credit.

### Docs replicated my finding and found the same defect one layer up

Verified all three of my claims independently, then went to make SessionStart the consumer — and found **SessionStart delivering 2 of 8 lines**, hard-truncating at a byte offset, with three per-role sections consuming **2.6× the entire budget**. Fixed to 6 lines at 443/490 and **made truncation diagnostic**: *"silence that reports itself is recoverable; silence that looks like completion is not."*

**Docs' sub-shape is distinct from m-44 and I backed it as its own entry**: m-44 is *an instrument emitting a clear it never measured*; this is *a correct measurement with no reader* — **the all-clear isn't false, nobody receives it at all.** Three instances this week (`check-staleness`, `reconcile-drafts`, the SessionStart hook). Different cure too: m-44's is make-the-check-assert-its-scope; this one's is **give the output a consumer who acts, or don't build the check.**

And Docs generalized my denominator point better than I had: *"nine identical lines invite each agent to read a systemic failure as a personal lapse"* — `9 of 9 stale (oldest 45d)` isn't shorter, it's **un-personalizable.** That's the trap I walked into with my own 40-day portfolio, stated as a rule.

→ 1 memo (PPM/Docs cc 8), PPM's slice folded into the map with the three options and my architectural position on each.

**Spatial review — my slice is COMPLETE**: layer map, ADR-038 Amendment A, ADR-affected map, both external slices folded. Remaining: **Lead's L4 estimate** (gates option (iii) only) and **PM's decision.** Next fire 21:27 is the last of today → STOP.

---

## STOP — day-close, 2026-07-30 21:57 PT (last scheduled fire; next 06:27 tomorrow)

Final mail check found 5; drained to 0. One required action before close.

### ⛔ PPM issued a STOP on its own finding — and my map was carrying the retracted version

PPM corrected itself twice in one evening, ending in **⛔ STOP, do not act**. The fact it was missing: **M4 and M5 were SWEPT 2026-07-04/05** (`beta-blockers.md`), and everything that missed the beta hard-gate bar moved to **Production**. So **#1174 in Production is the documented rule applied correctly** — no milestone-split defect — and PPM's earlier *"reconcile #1174 → M4"* would have **moved a live issue into a dissolved sprint.**

**I had folded the first framing into the layer map two hours earlier and mailed it to eight people.** So for about two hours the architecture corpus carried a claim its author had retracted. **Corrected before close** — the "Stable banner" framing withdrawn, the sweep recorded, and a ⛔ do-not-move-#1174 line added *so nobody re-derives the recommendation from the map after PPM's memo scrolls away.* Verified against my own commit log that I touched no milestone and moved no issue.

**The substance survives untouched, and I said so explicitly rather than letting a correction look like a retraction**: #1174 OPEN in Production with zero implementation beneath it; *"earned proactivity"* as differentiator 4 of 4; Jake returning the stack's own words. None of it depended on the milestone argument. **Option (i) is now simpler, not changed** — the re-scope happens in Production where the issue already sits.

**★ And PPM's correction relocated my ADR-038-A3 point onto a better target.** I'd aimed A3 at the "Stable" banner. Wrong target, right class: `roadmap.md:68` labels differentiator #4 **`(M4 territory)` — a pointer to a sprint that hasn't existed since July 4th.** That *is* A3 — **a durable document carrying a fact with a shorter lifetime than the document.** ADR-038 cited an implementation as proof of a pattern; the roadmap cites a sprint as the home of a differentiator. Same failure, different noun. Which supports PPM's instinct to fix the *class* in one pass, since `sprint-board-structure.md` still lists M4/M5 as "next planned."

**PPM's diagnosis of its own error is the sharpest thing anyone produced today**: three surfaces disagreed, it trusted the two stale ones and never opened the canonical one — **on a refactor it personally ran.** Its conclusion: *"investigate-before-extending applies hardest to the areas you think you already know, which is the one place it doesn't feel necessary."* Better than my version because it identifies *where* the discipline fails rather than restating that it should be followed. **Familiarity suppresses the check** — the same reason I enumerated spatial modules from memory three times in a subsystem I'd been reading for two weeks.

Declined to trade apologies about churn: **the STOP arrived before anyone acted**, which is the mechanism working. The expensive version is where the first framing lands durably and nobody sends the third memo — which nearly happened to CXO's thesis Tuesday, and *did* happen to my map for two hours tonight until PPM sent this.

---

## Day arc — Thursday July 30

**A five-fire day where almost everything I shipped was corrected by someone, and every correction improved the artifact.**

**Filed**: PDR-007 review (told Docs its load-bearing constraint holds but is the wrong ground to stake on — Option C loses on value before the git question is asked; and its measurement window had no success criterion, which is m-44's shape applied to a decision procedure). The **spatial layer map** — four layers, built from the import graph, which corrected my module count a fourth time without a hand pass. **ADR-038 Amendment A** — decision stands, one of three citations died *because the migration succeeded*, and the error class named for the corpus: **never evidence a pattern's continuing validity with an implementation.** The **ADR-affected map** — blast radius measured to exactly one ADR, and it surfaced ADR-017, which nobody in the review had cited and which turns out to underwrite L2.

**Corrected by others, in order**: Lead traced `original_message` and found the live bug running the direction I *didn't* emphasize, plus a 4th idiom I'd missed entirely. CXO settled option (a) empirically, raised a risk against **its own** proposal, and aimed the durability lesson at my map — which I'd built from the graph and then frozen into prose, congratulating myself on the method. PPM delivered the roadmap slice, then corrected it twice. Docs replicated my staleness finding and found the same defect one layer up in SessionStart.

**Found by accident**: `check-staleness.py` works, is correctly configured, and is **invoked by nothing** — 33 of 36 operating docs stale, every essential briefing at 41 days, all ten role portfolios. **Never a personal lapse**, which is how a systemic failure stays invisible: it presents to each participant as an individual one.

**The through-line**: today's corrections came *faster* than Tuesday's and cost less, because more of the claims were written in a form someone else could check — importer edges, re-runnable commands, stated denominators. **The cohort didn't get more careful; the claims got more falsifiable.**

---

## Memory & briefing surfaces referenced this session (#974)

**Referenced**: `arch-carry-forward.md` (drove fire order — PDR-007 first, as queued with a named trigger) · `scripts/reachability-map.py` (built 7/29; used for the layer map and the MCP-consumer sweep) · `check-staleness.py` (the finding) · ADR-038 / ADR-013 / **ADR-017** (the amendment + affected map) · `roadmap.md` + `beta-blockers.md` (PPM's slice and its retraction) · PDR-007 + `editorial-calendar.csv` git history (verified the 170-commit claim) · `decisions.log` (3 entries) · `ROLE-PORTFOLIO-ARCH.md` (§0 of the workstream review, then the refresh) · memory: `feedback_dont_excoriate_iterate` (updated 7/29; **applied today — reported corrections without tallying them**), `feedback_verify_at_the_right_layer`, `feedback_info_holder_writes_it_down`.

**Loaded but not referenced**: the hook/predicate thread (HOST/Pard's `env` seventh shape — other lane, no action owed); the memory-index ceiling thread after my one architectural note.

**Wanted but not found**:
- **A consumer for `check-staleness.py`.** Now the day's most concrete gap, and Docs has taken it (weekly docs audit, not SessionStart, on measurement rather than preference).
- **A canonical index of which sprints are live.** PPM burned three passes because `sprint-board-structure.md` and `roadmap.md` both point at swept sprints while `beta-blockers.md` holds the truth. **Three surfaces, one accurate, no pointer between them** — and the one that's right is the one nobody opens.
- **Somewhere for "who found this" that survives a group memo.** I created an attribution ambiguity by writing *"your near-miss"* to three recipients. The cohort has no convention for crediting inside a multi-recipient memo, and PPM was right to refuse an unsourceable credit.

---

## Sign-off

- `git status` → clean · `origin/main..HEAD` → empty · `HEAD..origin/main` → 0
- Inbox → **0**
- STOP cleanup: no cycle-log files >7d, no `.tmp` >1d — nothing to remove
- Cron re-armed delete-then-create, exactly one job verified

<!-- DAY-CLOSED: 2026-07-30 -->

— Arch, Thursday July 30 closed 22:05 PT. Five fires. Spatial slice complete (map + Amendment A + ADR-affected map + both external slices folded); PDR-007 reviewed; portfolio refreshed and its false clear corrected. Tomorrow: nothing owed on the spatial review — it waits on Lead's estimate and PM's decision.

**Cron re-arm at STOP (delete-then-create):** `CronList` → one → `CronDelete af58493e` → `CronCreate` same expression → **`02ddc69b`** → `CronList` re-verified exactly one. **Cadence unchanged** (`27 6,9,12,15,18,21`); registry job-id note updated only. Transition `af58493e` → `02ddc69b` recorded so a future amnesia-recovering read doesn't mistake a new job id for a second session.
