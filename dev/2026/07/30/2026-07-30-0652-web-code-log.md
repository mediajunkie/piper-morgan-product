# Web session — 2026-07-30 (Thursday)

**Role**: Unicorn Web Designer (piper-morgan-website)
**Account**: pipermorgan.ai / Amber
**Model**: Claude Opus 5 (1M context)
**Prior session**: `dev/2026/07/29/2026-07-29-0924-web-code-log.md` — **DAY-CLOSED: 2026-07-29** (retroactively, at this fire's Step 0 — see that log's day-close section for the full arc: five fires, admin-calendar runtime read shipped, compose autosave ask #1 shipped, the cohort's hook-mechanism saga resolved architecturally, PDR-007 reviewed).
**Trigger**: cron fire `fafad118`, `22 6,9,12,15,18,21 * * *`, first fire of the day (06:52)

---

## START (06:52)

### Step 0 self-heal

7/29 ended without a STOP (last scheduled fire landed with PM actively present; correctly didn't
day-close mid-conversation, but that left no `DAY-CLOSED` marker). Ran the missed close first, before
creating this log — day arc, lessons pointer, sign-off checklist, cron state, `<!-- DAY-CLOSED:
2026-07-29 -->` all written to yesterday's log (commit `8ff8e6bac`). Full detail lives there, not
duplicated here.

### Environment

| Check | Result |
|---|---|
| Cron | one job, no duplicates, `fafad118` |
| Product worktree | `claude/web-cycle`, 0 ahead/behind, clean |
| Website worktree | `claude/web-cycle`, 0 ahead/behind, clean |
| Pre-commit hook (common dir) | confirmed present, unchanged from yesterday's install |
| `node_modules` (website) | present (installed 7/29; cron prompt's "not installed yet" note is stale boilerplate) |

### Carry-forward — open threads inherited from 7/29

- **PDR-007** — Arch/CIO's ruling pending; my reply sent, nothing further owed unless the ruling
  raises something specific.
- **Compose autosave** — shipped, **not yet browser-verified**; clears only when PM or Comms observes
  the three-step check.
- **Admin calendar** — shipped, spot-checked unauthenticated; the authenticated live-success path
  still needs PM's first post-deploy load.
- **Two Docs-flagged gaps** (publish-queue staleness class, `copy-editorial-calendar.js` sibling-path
  break) — awaiting Docs' preference on the latter.
- **Predecessor's two unanswered questions** (CLI B tested-since-May?, `--mode=archive` still wanted?)
  — no rush, still open since 7/19.

---

## Fire log

| Fire | Time | Action | Notes |
|---|---|---|---|
| cron | 06:52 | START | Step 0 self-heal run for 7/29 (see above). Environment verified, both worktrees synced clean. |
| cron | 06:55 | WORK | Mail loop: 1 memo — **CIO's closing-the-four-owed items wrap.** Nothing actionable for Web directly, but it referenced `dev/active/handoff-web-predecessor-2026-07-29.md` as confirmed-on-`origin/main` — a file I had no memory of creating. **Read it before triaging the memo, not after.** It's the actual predecessor handoff PM offered on 7/29 to pull from the designinproduct.com account — genuinely their first-person account of the Vercel migration week (7/12–19), with an honest scope statement (context intact only for that window, zero for 7/20–29) and every claim marked VERIFIED (session-log-dated) or BELIEVED (single-datapoint). **This resolves the open question from my own 7/29 chat exchange with PM** ("did we ever get a handoff" → I'd said no and PM offered to check). Extracted the two load-bearing lessons worth carrying: (1) a green signal after fixing one layer of a multi-layer problem doesn't mean the problem is solved (their DNS cutover was 3 sequential bugs, each looking like "still propagating" — same shape as this week's own hook saga); (2) a size/limit check is only as correct as the units it's measured in — the predecessor's own 413-bug postmortem, directly relevant if I ever touch the compose upload path's size logic again. Updated carry-forward to replace the stale "confirmed genuinely absent" framing with what's actually now known. CIO memo triaged to `read/`, sent via `mail-send.sh` (`e14296a85`), verified drained to MANIFEST-only. |
| cron | 09:55–10:35 | WORK | ⚠️ **LIVE DATA-LOSS BUG in code I shipped 7/29 — traced and fixed.** Two memos from Comms: PM's alt text on a Weekly Ship was silently blanked 28 seconds after being correctly saved, with git history showing three consecutive admin-UI commits (correct save at `:15`, pure-deletion overwrite at `:43`) and no agent involved. Comms diagnosed precisely to the timer ("28 seconds = your autosave interval") and posed the decisive question themselves: is the autosave PUT snapshot-based or diff-based? **Read the exact code before answering, twice** — confirmed `getPayload = useCallback(() => ({image,alt,caption,body}), [...])` closes over React state at ARM time, not fire time (a real closure-staleness bug, not merely "sends a full snapshot"), and separately confirmed the manual "Save now" button (`SaveIndicator`'s `onClick={onSave}`) never cleared the pending autosave timer the way `handleFormBlur` does — the two compounding exactly: field's first edit/paste arms a timer holding the PRE-edit value → manual save succeeds with fresh data moments later, correctly updating `shaRef`/`lastSavedRef` → the never-cancelled leftover timer fires ~28s later with its stale closure and silently overwrites. Neither existing guard catches it: `lastSavedRef` only dedupes an identical repeat, and the sha check only detects an EXTERNAL writer — this is self-inflicted, sha-consistent, so it sails through. **A genuinely different bug from what ask #1 (7/29) scoped**, confirming Comms' own read. |
| cron | 09:55–10:35 | WORK | **Fixed at the mechanism, not the trigger**: `getPayload` now reads a `fieldsRef` kept live by the same effect that already syncs localStorage on every field change, instead of closing over state — any timer, however stale its arm-time was, now reads current values when it fires. Also defensively fixed the manual-save button to clear the pending timer (`handleManualSave`), matching blur's behavior, so the common case doesn't even reach the ref-fix's safety net. **Verified by reproducing the actual mechanism, not by reasoning alone**: no test runner exists in this repo and no browser is available on this host, so wrote a standalone Node script simulating the exact incident timing with closure-capture vs. ref-read — the old design reproduces the bug exactly (correct save, then a silent stale overwrite); the new design produces no second write at all (the stale timer's payload now matches current state, caught as a no-op dedup). Both confirmed before landing. `tsc`/lint/build all clean. Website `8d2db3c`, pushed. |
| cron | 10:35 | WORK | Memo to Comms (cc PM, Docs, CIO) with the precise mechanism (closure-vs-ref, not quite their snapshot-vs-diff framing, though directionally the same instinct), the fix, and the verification. Confirmed ask #2 (genuine concurrent-editor conflict) is untouched and still open — this fix is narrower and doesn't do anything for an actual external-writer collision. Confirmed ask #3 stays declined, independently agreeing with Comms' own reasoning before rereading it. Sent via `mail-send.sh` (`9b889c73a`), verified on `origin/main` (5 copies). Rebased 13 commits from other agents that landed while I worked the bug. |
| cron | 10:40–10:50 | WORK | Drained remaining mail loop: 7 memos, all cohort-wide governance threads (PDR-007 review chain: Arch's thorough review concurred with my cost correction and Q2 answer, no objection to Option B, sequencing deferral held even harder with a pre-registered threshold — Docs applied all five corrections same day; memory-index governance: Arch's "the index is a DERIVED artifact, never prune source to fix a build output" reframe, HOST's/PA's changelog-claim testing that disproved a plausible "already fixed" story on both the line and byte limits). Nothing actionable for Web in any of the seven beyond noting outcomes already confirmed. |
| cron | 10:45 | WORK | ⚠️ **Found and fixed a live defect in the duty-cycle-tick skill's own Step 0 — one that demonstrably affects my own logs.** CXO's memo showed the `grep -l "DAY-CLOSED"` self-heal check matches prose *mentioning* the sentinel, not just the sentinel itself — and the logs most likely to contain that prose are exactly the ones that just self-healed correctly (a day's header routinely narrates the prior day's close). **Verified this against my own actual logs before acting**: my 7/29 log has both the real anchored marker AND a prose mention in its header; my 7/30 log (still open) has only prose mentions and no real marker yet. A loose grep can't tell these apart — checked precisely, this is a live, present risk to my own duty-cycle mechanism, not a hypothetical. Applied CXO's exact fix (line-anchored, date-shaped pattern) to `.claude/skills/duty-cycle-tick/SKILL.md`, then **verified the fix behaviorally against both of my own real logs** (correctly matches 7/29's real marker, correctly does NOT match 7/30's prose-only mentions) before committing — matching this week's own standing rule of verifying a fix against real state, not just re-reading the diff. Committed and pushed (`634204fa2`) as an editorial-surface (non-mail) change, separately from the mailbox triage batch. |

## Fire (12:52) — DUTY CYCLE TICK, WORK

- **(12:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Synced both worktrees, clean, current.
- **(12:55)** — Mail loop: 1 memo — **Comms closing the compose-bug thread with a self-correction.** They noted plainly that their own proposed fix ("make the save diff-based") would NOT have worked — a diff computed from a stale closure is still stale, so it would have diffed the pre-paste empty alt against the server's populated alt and written the wipe just as faithfully, possibly looking more deliberate. Credited the scoping-question discipline ("ask before building") as what stopped a wrong hypothesis from becoming a wrong afternoon. Explicitly closed the thread from their side — nothing owed back. Triaged to `read/`, sent (`e5c658dee`), verified drained.
- **(13:00)** — Task loop: updated carry-forward's PDR-007 section from "pending" to effectively settled — Arch's 7/30 review concurred on my Q2 answer and cost correction, found Constraint 1 survives but was staked on the wrong ground (replaced with stronger conflict-localization + audit-trail arguments), and caught that the measurement window had no falsification condition; Docs pre-registered a real threshold same day and shipped it as a runnable script rather than a described criterion. Nothing further owed on that thread. Two other carry-forward items (Docs' sibling-path preference, predecessor's two no-rush questions) remain genuinely open, no new movement.
- **(13:02)** — Both worktrees re-verified clean, 0 ahead/behind. Heartbeat self-suppressed (commit this fire serves as it).

**Cron**: unchanged, armed (`fafad118`) — no substantive multi-step build warranted a delete this fire.

## Fire (15:52) — DUTY CYCLE TICK, WORK

- **(15:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Sync surfaced a skill-file change mid-fire: HOST corrected my earlier anchored `DAY-CLOSED` fix as **over-strict** — required a bare `-->` right after the date, which false-FAILED 9 of 388 real closes (the *annotated* idiom the cohort actually uses for unusual closes: retroactive, session-interrupted, migration-handoff). Verified HOST's corrected pattern against my own two logs before syncing further — both still correct.
- **(15:55)** — Mail loop, part 1: **CXO found a further, sharper correction to HOST's own correction.** HOST's "zero historical instances" claim tested the wrong predicate (dated, not the bare one Step 0 actually ships); against the real predicate, CXO found 42 historical false-passes, 4 in the last 10 days — including HOST's own 7/25 log. CXO also found a real close in a form neither denominator could see: a `## DAY-CLOSED {date}` markdown heading with no `<!--` at all (Lead, 7/29), invisible to a corpus scan defined by the comment form. CXO's proposed pattern accepts both forms while still rejecting prose (389 accepted / 4-of-4 correctly rejected). **Verified all of this myself against the actual cited files** (Lead's heading close: matches; the three still-open cited logs: correctly rejected) rather than trusting the memo's table — one discrepancy surfaced (CXO's own 7/26 log matched when the memo implied it shouldn't) and resolved immediately: CXO had already retroactively closed it themselves, exactly as their memo said, so the match was correct against current file state, not a bug. Applied CXO's pattern to all three call sites (skill Step 0, `duty-cycle-freeze-check.sh`, `cohort-status.sh`) and corrected HOST's now-refuted "zero instances" claim in the skill file itself, rather than leave a refuted claim standing in the artifact — the exact failure this correction chain is about. Ran `cohort-status.sh` end-to-end afterward to confirm no regression.
- **(16:00–16:15)** — Mail loop, part 2: **HOST independently verified CXO's pattern and endorsed it ("Web — take it")** — already done by the time this landed. But HOST's memo also surfaced a deeper structural finding: **closure is a property of the DAY, not a single FILE.** Their own ad-hoc analysis tool used `head -1` to pick "the" log file per role/day, and on their own multi-log migration day it picked the wrong (unclosed) file, misreporting an actually-closed day as open — caught only because it contradicted a direct observation 90 seconds earlier. **Checked whether the same `head -1` pattern existed in the shipped scripts I'd already touched today — it did, at three separate call sites** across `cohort-status.sh` and `duty-cycle-freeze-check.sh`. Fixed all three to check every matching file for the role/day, treating the day as closed if any file carries the marker (mirroring the semantics Step 0's own glob already had). **Built a real test fixture** (two log files, one open, one with a genuine close) to verify both the bug (old `head -1` logic reports closed=no) and the fix (new logic reports closed=yes) — not reasoned through, actually run.
- **(16:15)** — ⚠️ **Introduced and caught my own bug while writing this fix.** A `#` comment containing an apostrophe, placed inside a `$(...)` command substitution, broke bash's paren-matching — even though the identical comment is inert at the top level of a script. Confirmed by bisecting the actual file precisely (binary-searched line ranges, isolated to one specific comment, reproduced in a minimal standalone script) rather than guessing at the cause; fixed by removing the apostrophe. Both scripts syntax-check clean; `cohort-status.sh` run end-to-end with no regressions.
- **(16:20)** — Committed and pushed both rounds (`f63f85371` pattern-correction, then `072b3658e` day-not-file fix) with real verification evidence in each commit message, not just a description of intended behavior.
- **(16:25)** — Mail loop, part 3: **Arch found `check-staleness.py` works correctly and is invoked by nothing** — all 10 role portfolios were stale, including Arch's own (40 days). Checked my own portfolio rather than assuming this was someone else's problem: `ROLE-PORTFOLIO-WEB.md` was 41 days stale, written by my predecessor's DinP/Sonnet account before the migration, describing work (#998's FastAPI phase, CLI B, June's obs-pass) largely superseded or irrelevant now. **Refreshed it following Arch's own example**: rewrote Section 2's priority table to reflect this week's actual work (admin calendar, compose autosave bug, PDR-007, the DAY-CLOSED predicate chain), corrected two specific stale claims in Sections 3/4 (GitHub Pages retired during the Vercel migration; the Lead-Dev seam's "FastAPI templates" reference superseded by the Next.js compose system), and added an honest note in Section 5 about the consumer gap Arch found rather than implying the refresh mechanism works. Did **not** attempt to fix the systemic consumer gap itself — Arch named that as a Docs/CIO design call, and I agree it's out of my lane.
- **(16:35)** — Both worktrees re-verified clean, 0 ahead/behind. Heartbeat self-suppressed (commits this fire serve as it).

**Cron**: unchanged, armed (`fafad118`) — no substantive multi-step build warranted a delete this fire, despite the volume of work; each unit landed and pushed individually.

## Fire (18:52) — DUTY CYCLE TICK, WORK

- **(18:52)** — Step 1/2a: one cron job, no duplicates; both fingerprints correct. Synced both worktrees, clean, current.
- **(18:55)** — Mail loop: 2 memos closing out the DAY-CLOSED thread and the doc-staleness thread from this afternoon, both informational. **CXO's day-scoped rescan**: accepted HOST's day-not-file correction fully, re-ran the scan properly scoped and found it surfaces *more* genuinely-open days than the file-scoped version (one false positive dropped — HOST's 7/25 — four new true ones found: docs ×3, pa, lead, ppm). Confirmed explicitly that **Step 0's own glob was already day-correct** — the defect was in the measurement tooling, not in the skill I already fixed, so nothing further needed there. **Docs' SessionStart investigation**: went looking for a home for Arch's staleness-consumer finding and discovered SessionStart itself has the identical shape — was delivering 2 of 8 intended lines, silently truncated at a hard byte offset, with per-role sections eating the whole budget on other roles' detail. Fixed and verified behaviorally (2→6 lines delivered). Named a third instance of the same family (`check-staleness`, HOST's `reconcile-drafts`, this hook) — "a check whose output has no reader" as its own sub-shape. Worth noting: Docs' memo confirms my portfolio refresh from the last fire is what moved their "33 of 36" count to "31 of 36" — direct, small confirmation the detector is genuinely live. Nothing actionable for Web in either; triaged to `read/`, sent (`fc056b5fc`), verified drained.
- **(19:00)** — Task loop: no movement on the two remaining open items (Docs' sibling-path preference, predecessor's two no-rush questions) — both still genuinely gated on others, not self-imposed pacing.
- **(19:02)** — Both worktrees re-verified clean, 0 ahead/behind. Heartbeat self-suppressed (commit this fire serves as it).

**Cron**: unchanged, armed (`fafad118`) — quiet fire relative to the last several; no substantive multi-step build in progress.
