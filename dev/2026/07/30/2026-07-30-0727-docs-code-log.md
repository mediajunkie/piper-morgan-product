# Session Log: Documentation Management (Docs) — 2026-07-30

**Role**: Documentation Management (Docs)
**Date**: 2026-07-30 (Thursday)
**Model**: Opus 5 (1M context)
**Host**: Amber · **Environment**: code
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle` · website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`
**Cron**: `a37a1eb5` — `57 6,9,12,15,18,21` (re-armed at yesterday's STOP from `26805e13`)
**Prior day**: `dev/2026/07/29/2026-07-29-0948-docs-code-log.md` — **`DAY-CLOSED: 2026-07-29` verified present** ✓

---

## Fire 1 (07:27 PDT) — START

**Step 0 self-heal check**: prior day carries its `DAY-CLOSED` marker — proper STOP happened, nothing
to retroactively close. Verified by grepping the artifact rather than trusting my own memory of having
written it, which is the whole point of the check.

**Step 1** — cron: exactly one job, `a37a1eb5`, `57 6,9,12,15,18,21`. No duplicates.

**Step 2** — synced *before* reading anything (yesterday's standing lesson: a stale worktree read is
what produced a false report to PM). 0 behind, working tree clean. Collision check: `docs` ↔
`claude/docs-cycle` pairs correctly — though on Model A that pairing is permanent by construction and
proves little; the real gate is the tmux-side check at standup.

**★ Registry row verified against the armed cron** (START owns this):

| | |
|---|---|
| row `cron_expr` | `57 6,9,12,15,18,21` |
| armed job | `a37a1eb5`, same expression |
| `threshold_h` / `first_fire` | 7 / 06:57 |

Match. Row already records last night's `26805e13` → `a37a1eb5` transition.

**Inbox**: 36 total, **7 new overnight**. Two live threads, both with Docs in the recipient list.

---

### ★ Arch reviewed PDR-007 — five corrections, one of them structural

`3a3dea60a`. Arch attacked Constraint 1 as I'd asked, and the useful answer was **"it holds, and you
shouldn't have put the weight there."**

**§4 was the catch that mattered: my measurement window had NO success criterion, so it could not
fail.** Arch — *"a decision procedure with no falsification condition is m-44's shape applied to a
decision instead of an instrument."* A PDR citing m-44 twice contained one. And I'd have been the
reader most motivated to read an ambiguous result as "Option A is fine," since Option B spends Web's
time to save mine.

**Registered before the window runs** (2026-07-30 → 08-27): Class 1 = **0** · Class 2 = **0** · Class 3
**≤ 17** (no growth over baseline). Zeros for 1 and 2 because both have a known cause and shipped
countermeasure — recurrence means the countermeasure isn't holding. Class 3 is no-growth *deliberately*:
Option A's job is to stop drift being generated, and healing the existing 17 is a backfill I could run
in an afternoon, which would otherwise disguise a still-leaking mechanism.

**Went one step past the ask**: shipped `scripts/measure-editorial-drift.py` rather than describing the
measurement. **A pre-registered threshold nobody can reproduce measuring is decoration in a different
costume** — the same defect one layer down. Verified to reproduce the 07-29 baseline exactly (365
matched, 0 stale, 17 disagreements); `--json` and `--verbose` both work; reports rather than gates.

**The other four, all applied**:
- **Option C's rejection now leads with the class analysis** (C fixes only Class 1; Class 1 was mitigated 07-29) instead of the git argument. Staking it on the most contestable claim handed a reopening to anyone who merely disagreed about git ergonomics.
- **Constraint 1 scope**: binds the *source of truth* only, not derived artifacts — as written it forbade a generated binary index that Constraint 3 makes fine.
- **"Mergeable" → conflict LOCALIZATION.** The correction I'd have been least likely to find. Arch verified **38 of 48 active days had >1 commit (79%)** — the CSV *is* mergeable and contends anyway. B's win is that two agents editing different posts never touch the same file: impossible, not resolvable. I had an ergonomic preference; Arch supplied a structural argument.
- **Binary-in-git rejected for PROVENANCE, not diffability** — a blob doesn't degrade the audit trail this cohort operates on, it *removes* it. *"We lose diff"* invites "use a diff tool"; *"we lose `git log -p` on editorial claims"* doesn't.
- **Class 2 should be DERIVED, not stored** (or stamped `draftPath_verified_at`, per ADR-079 D4a / #972). Arch is right that it's **a better illustration of the PDR's thesis than anything I wrote**: the reconciliation problem and the staleness problem have the same cure — *stop maintaining what you can generate.* Validator kept as a catch-layer for a class a derive would remove.

Q1 and Q2 both closed (Arch + Web concur independently on the product repo; Arch's reason is stronger —
the website repo would **invert an existing dependency, and inverted dependencies are how you get two
sources again**). Arch's m-44 sub-shape read recorded for CIO's ruling rather than adopted.

**A routing defect I owned back to Arch**: they noted that reviewing from my request memo alone would
have priced the PDR wrong, because Web's correction had already landed in the doc and the memo predated
it. **I sent a summary of a document that then changed underneath the summary** — the same
read-the-artifact-not-the-testimony failure, reproduced from the *sending* side. Future reviews get the
artifact plus a one-line pointer, not a summary that can go stale.

### Mail — CIO closed both my findings; registry now 11/11

CIO's memo (to exec/docs/web) confirms findings 1 and 2 from my arrival report are resolved, and
**"registry is now 11/11 — no role is structurally invisible to the belt for the first time."** Nothing
owed back.

### The overnight memory-index thread — not mine, but it sharpens my largest owed item

HOST/PA/CXO/Arch worked a live thread: the `MEMORY.md` line limit **still writes silently** despite a
changelog claiming otherwise (HOST and PA each tested a different path and both found it silent).
Arch's contribution is the one that matters structurally: **the index is DERIVED — nobody should prune
source to shrink an artifact.**

That is the *same shape* as my largest owed item. HOST's Pass 3 proposes a **load-time / record
separation for CLAUDE.md**; the memory index needs exactly that separation and for the same reason.
Both are "one surface is doing two jobs — the thing you load and the thing you keep." Doing the
CLAUDE.md compaction now would produce the worked example the memory question needs. Assessing scope
next.

### ★ PUBLISHED — "RECONNECT's Keystone" (building, 2026-07-30)

**Live**: https://pipermorgan.ai/blog/reconnects-keystone/ · hashId `7857761de4b1` · cluster
**`reconnect`** · workDate **2026-06-20** (source-work-period, ≠ pubDate) · website `1b7ecf7`.

**Proofread outcome**: Comms' 14 fixes held, fact-check clean, footer teaser verified against the
calendar. **Nothing I'd fix silently** — everything remaining was voice territory or in the area PM
ruled on this morning, so I escalated all of it. PM approved 4 fixes:

1. Unstacked a double colon (second → em dash).
2. Repaired not-only/but-also parallelism, and dropped a doubled "now" (restatement).
3. + 4. Normalized `my Lead Dev agent` (hybrid) and a late `my Lead Developer agent` (full
   re-expansion after three bare uses) to the bare acronym, per gloss-once. **This is the first piece
   published under the register-scoped role-gloss ruling PM made this morning, so it sets precedent.**

**Length**: 1,680 words against the 800–1,300 guard. PM cleared it with durable guidance worth
recording: *"we are deliberately covering larger swathes of time now, which is leading to longer posts.
We can both allow for this somewhat but also start imposing more discipline about which narrative
threads belong."* Plus: *"I also reduced some redundancy from the first draft so we can be more
vigilant about restating what we just restated again again all the time."*

The two `[ROLE-GLOSS?]` lint advisories were **expected false positives** — the voice guide states
outright that `check-acronyms.py` cannot see register and will flag the correct first-person "agent"
form. Verified against the table, not the linter.

### 🔴 Publishing was BLOCKED by the toolchain again — and I fixed it at the mechanism

`prepImage` failed: **both documented rungs of the image-prep chain are missing on this host.** cwebp
absent, Pillow absent, and **`sips` cannot emit webp** — verified behaviorally by trying it on a copy,
not by reading its help text. Clean failure, no partial state (image prep runs before any mutation).

Meanwhile **`sharp` — a direct dependency of the website repo, present by construction whenever
`node_modules` is installed — sat unused.**

Shipped two changes to `publish-post.js` (website `1b7ecf7`, committed separately from the publish so
Web can review it independently):

1. **The Pillow branch was gated on `which('python3')` and THREW on failure.** python3 exists on every macOS host, so that branch was always entered and always fatal when PIL was absent — **a fallback that could not fall back.** Now falls through.
2. **Added sharp as a third rung**, run via `spawnSync` in a subprocess so `prepImage` stays synchronous. My first attempt returned a promise from a sync function, which would have handed the caller a Promise where it expects a path string — caught before running it.

Existing behavior is unchanged wherever cwebp or Pillow works; sharp is only reached when both fail.
Verified `node --check`, the dry-run path, and a real publish producing a correct 1200×675 webp at 94KB.

### ⚠️ HTTP 200 that lied

First liveness check returned **HTTP 200 with a body reading "Post Not Found"** — deploy lag. Verified
the three artifacts were correct on `origin/main` (CSV row, content entry, image) to rule out a data
problem, then polled until content appeared. **A status code is not a liveness check**, which is the
same shape as everything else this week: the reassuring signal and the real one are different fields.

Confirmed on the served page: all four fixes present, the hybrid and re-expanded gloss forms both gone,
webp referenced, and the caption round-tripped correctly through the YAML `''` escape to *"It's
holding!"* — first new-content confirmation of that parser fix since the Ship.

### First live reading in the PDR-007 window

`measure-editorial-drift.py`: matched rows **365 → 366**, Class 3 **holding at 17**, Class 2 at 0.
**This publish added a matched row without adding a disagreement** — which is precisely the behavior
the pre-registered criterion exists to detect. Day 1 of 28.

**Not archived**: Step 9 gates archival on a confirmed syndication URL. Building narratives syndicate
to Medium — awaiting PM's URL, then `status=distributed` + `mediumURL` + archive.

---

## Fire 2 (13:27 PDT) — WORK. ★ CLAUDE.md load-time/record separation SHIPPED

*Tick arrived **twice**; `CronList` showed exactly one job (`a37a1eb5`), so it was a delivery duplicate,
not a stacked cron. Worth noting because a doubled tick is otherwise easy to misread as a duplicate job.
Sync clean, 0 behind. 4 new memos, all cc, none addressed to Docs for action → straight to the task loop.*

**The largest item I held is done** (`ac120d514`). PM-greenlit since 07-12; HOST did the analysis on
07-28 and explicitly left the structural edit to Docs.

| | |
|---|---|
| CLAUDE.md | **58,262 → 55,303 bytes (−5.1%)** — *while adding four norms* |
| the hooks item | 8,979 → 2,850 bytes (**−68%**) |
| HOST's catalogued norms present | **8/8**, up from 4 |
| full record | `docs/internal/operations/amber-hooks-investigation-2026-07.md`, verbatim |

Placed the four missing norms **by concern rather than in a heap**: named-trigger deferral and
m-43/state-the-denominator into Core Principles beside verify-first; park-before-dark into Sign-Off
Discipline, since it's a going-dark rule.

### Two things I found by verifying HOST's memo instead of executing it

**1. The item had GROWN — and the reason is the argument.** HOST measured 6,923 bytes / 12.8% on 07-28;
it was **8,979 / 15.4%** on 07-30, **+30% in two days**. Cause: `b67abad65`, the commit that *resolved*
the investigation. **Concluding it meant writing more record into the load-time surface.** The item
flagged as bloat grew *because it got solved* — which is structural, exactly HOST's point, and a
stronger case than the one HOST made.

**2. ⚠️ HOST's pointer check was correct on 07-28 and had gone stale by 07-30 — I nearly inherited it.**
HOST verified the memory pin doesn't dangle. True then. But 2,407 bytes landed 07-29 and the pin was
last written 07-26 17:57. Re-checked: the pin carries matcher / index-state / PreToolUse, but **NOT**
the 07-29 five-seat validation, **NOT** "clear the index between probes" (which is *operative*), and not
"advisory" or the free mitigation. **A naive point-at-the-pin would have dropped operative material.**

Not a criticism of HOST — their check was right when run. It's the investigation's own lesson turned on
itself: **a verification has a timestamp, and the artifact kept moving after it.**

⚠️ **And my first re-check was broken.** `grep -ci … || echo 0` produced a multi-line value, so my
comparison reported PRESENT for *everything* — including "advisory", which HOST had already established
was absent. **A check emitting a false all-clear, inside the verification for an investigation about
checks emitting false all-clears.** Caught only because `advisory: PRESENT` was implausible on its face.
Redone in Python. That's twice this week a convenience shell idiom silently inverted a result (the other
being `rebase 2>&1|tail -1` masking exit status in a `&&` chain).

### Where I departed from HOST's proposal, deliberately

HOST proposed pointing at the memory pin. **I sent the record to a git-tracked ops doc instead**:

1. The pin is a **partial** record (above) and would have needed updating to carry the rest.
2. **Memory isn't version-controlled** — no diff, no blame, no `origin/main` copy, deletion irreversible. A poor home for the cohort's most-cited investigation.
3. **`MEMORY.md` is 8 lines from a silent 200-line ceiling** (HOST's own measurement last night). Pointing future record *at* the pool while it's at capacity is the wrong direction.

Precedent exists: `duty-cycle-self-attribution-drift-2026-07-06.md` is the same shape — an investigation
record extracted from CLAUDE.md that CLAUDE.md points at. The new doc names the pin as *partial* so
nobody mistakes it for complete. HOST's disclosure (that they wrote much of the cut prose, and every
edit was correct when made) is carried forward verbatim — dropping it while keeping the compaction it
justifies would have been dishonest.

**The structural rule is now in the file**: *CLAUDE.md holds operative rules and pointers; the reasoning
that produced a rule lives in the ops doc / methodology entry / session log. Corrections keep landing
here — that must not change — but they land as rules, and the narrative goes where narratives go.*

Flagged to CIO that the same shape is live on the memory index right now, with Arch's *"the index is
DERIVED — nobody should prune source to shrink an artifact"* being the identical insight one surface
over. This is the worked example if it helps.
