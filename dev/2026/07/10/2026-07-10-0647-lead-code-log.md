# Session Log — 2026-07-10 — Lead Developer (Claude Code)

**Role**: Lead Developer | **Model**: Fable 5 | **Worktree**: interesting-beaver-7ee19c (Model B ephemeral) | **Cron**: 17 6,9,12,15,18,21

## Context at START (06:47 fire)
Prior day DAY-CLOSED ✓ (the 9-release tester-loop close-out day). Sprint: **2 open, both gated** — #1278 (Fly, PM coordinates), #1332 (soaking since v0.8.10.7, 7/9). Invites READY TO SEND (HOST confirmed; PM has go/no-go). Pending next release cut: h1312recon migration + #1382 NullPool. Unblocked non-sprint queue: #1383 (gate-threading, priority-high), #1384 (timeout-modal buttons).

## Fires
- Fire 1 (06:47) — START. Inbox empty. Morning checks: CI on last night's pushes, #1332 soak evidence, then #1383.
  - **Morning CI check found the security workflow RED since v0.8.10.1's push** (7/9 15:10Z) — my breakage, caught a day late (the push-time "required check expected" banner prints on every direct push; it is not a run result. Lesson: check `gh run list` after substantive pushes). Root cause: the #1382 fail-closed path working as designed — CI's headless-ubuntu keyring is dead (same as droplet), KeychainService routed to the encrypted-DB store, no ENCRYPTION_MASTER_KEY in workflow env → constructor refused → every keychain-touching test down (12+).
  - **Fix**: TEST-ONLY committed 32-byte key in security-tests.yml env (deliberately not a secret — ephemeral CI DB; decodes to "ci-test-only-1382-never-prod!!32"). CI now exercises the REAL hosted shape (dead keyring + encrypted-DB store + Postgres) — strictly better coverage than the accidental pre-#1382 nothing. Plus one stale assertion: test_multi_user_key_isolation pinned "keychain was called," but #358's retrieve_user_key PREFERS encrypted_secret when an encryptor exists → mode-aware assertion (user-scoped IF consulted, else encryptor must exist). Proven both shapes locally (36/36 db-mode, 12/12 keychain-mode). CI run 29097860424 adjudicating.
  - ci.yml chronic red (96/100 failures, predates everything) = known #1365; not today's problem.
  - **CI run 29097860424: SUCCESS** — required check restored; the autogen-empty guard + hosted-shape coverage now live in CI. #1332 soak day 1: **0** "came through empty" in 14h on droplet 0.8.10.9 (not closing yet — a few clean days per criteria).
- Fire 1 continues → #1383 (Notion/Calendar gates don't thread user_id, priority-high) — the next unblocked queue item.
- Fire 1 (cont., ~07:00–07:30) — **#1383 built, shipped, live-verified, CLOSED (v0.8.10.10)**:
  - **Notion (the real half)**: router grows `is_available(user_id)` (per-user config chain: env > user config > user-scoped keychain → #1382 store on hosted; global fallback principal-less; never raises) + `connect_for_user(user_id)` (threads the user's token to the client). All 3 handlers gate + connect through `_principal_from_intent`; degrade messages modernized (Settings → Notion). 10 new tests + 10 migrated; touched surfaces 2410 passed.
  - **Calendar (the claimed half): NOT A BUG** — traced end-to-end: rail user_id → handler → router(user_id=) → adapter reads ONLY `google_calendar_{user_id}` (#917 removed global fallback). The 7/9 audit line was a call-shape misread; issue corrected honestly.
  - **v0.8.10.10 cut + deployed solo** (temp-worktree cherry-pick, archive-push, deploy.sh): payload = #1383 + **h1312recon (ran clean on droplet: g1382creds → h1312recon)** + NullPool + CI key. Container-verified 0.8.10.10.
  - **Live verification**: reversible fake-key round-trip as PM's user (gate False→True→False with key lifecycle; resolved token == stored; `credential_stored_db`/`credential_deleted_db` in the real hosted store; zero residue) + authenticated chat probe returns the NEW per-user degrade with Settings guidance.
  - Next-cut payload note in carry-forward: CLEARED (h1312recon + NullPool now live).
- Fire 1 (cont., ~07:30–08:00) — **#1384 investigated, fixed, shipped, CLOSED (v0.8.10.11)**:
  - **Empirical repro discipline**: served the LIVE authenticated rendered home.html + byte-identical droplet assets in a preview harness. The literal incident didn't reproduce — but the hunt surfaced FOUR real defects: (1) `transition: all` = pointer-dead fade-in window (elementFromPoint at the button returned chat-window at t=0 — the proven click-eating mechanism); (2) Continue Working = placebo (client timer only; JWT marched on; #857 refresh endpoint existed, never wired); (3) copy promised mousemove, never tracked + `'touch'` isn't a DOM event (dead listener since birth); (4) inline onclick wiring. All fixed; extend now hits /api/v1/auth/refresh awaited with honest failure toast.
  - Verified interactively (extend/[x]/mousemove all work; click-target-at-t=0 probe passes) + 8 pinning tests (render-based). Closed with the honest unreproduced-incident caveat + reopen criteria.
  - One PM-permission hiccup mid-build (test-file Write bounced "user declined" — PM confirms unintentional; resumed clean).
  - **Self-trip pattern noted twice now**: my own explanatory comments tripping my own content asserts (models.py excise 7/9, CSS test today) — fix = strip comments before asserting, not reword the prose.
- **Sprint + queue after fire 1: everything actionable is DONE.** #1278 (PM), #1332 (soak day 1 clean), #1385 (test-lane, unsprinted). Releases today: v0.8.10.10 + v0.8.10.11 both live.
- Fire 2 (09:47) — inbox empty; queue at (0,0). **BRIEFING-CURRENT-STATE refreshed** (was a month stale — banner led with M2-close/June 3, Last-Updated June 10, nothing from July): Version → v0.8.10.11, Current Focus → tester-loop-closed/invites-ready/sprint-2-gated, Recent Progress Jul 8–10 week block added (Lead-attestable content only; other lanes untouched per skill rules). Committed + pushed.
- Fire 2 (cont., ~10:00) — PM directives executed:
  - **#1386 BETA-GATE filed** (DRAFT): 5 criteria — sprint-clear, canonical suite fresh-run (≥90 routing / ≥75 expected-pass quality), THREE CXO+PPM-defined multi-turn scenarios (PM's new verification layer), 3-day stability window, PM sign-off. Review memo → Arch/CXO/PPM (cc PM) with per-role asks; scenario definition explicitly CXO+PPM's.
  - **Briefing false-staleness loop → CIO+Exec memo** (cc PM, PM-requested): evidence contributed — updates ARE landing (PM's `git pull`: up to date); the header-date-never-advances append convention + possibly stale local reads are the loop; suggested resolution shapes (authoritative date line + fetch-before-check), theirs to own.
  - Answered PM: #1332 soak + #1278 needs.
- Fire 3 (12:47, absorbed mid-PM-conversation — #1278 walkthrough in progress, waiting on PM's item-1 Fly-account answer). Mail: **CIO root-caused + fixed the staleness loop same-morning** (`76f6b5dd4`) — a THIRD mechanism: the SessionStart hook checked filesystem mtime (worktree checkouts stamp fresh mtimes → structurally unreliable), not git; fixed 3 instances via `git log -1 --format=%ct`, + killed a dead pre-6/29 glob whose fix exposed a 4-5s per-file stat loop (rewrote: hook 6.5s→1s). No action on me; loop closed.
- Fires 3–4 window (~11:24–14:20) — **#1278 PM walkthrough, all four decision items closed**: (1) account = personal org + adopt the `piper-morgan` app shell (wizard residue; token stored via keychain house convention, auth-smoked — sees personal org; app-scope caveat flagged); (2) DB = Fly Postgres co-located (privacy posture preserved; 124KB dump = trivial migration); (3) Redis→Upstash / Chroma→tiny-Fly-app+volume (0 log lines in 48h noted; beta features kept whole) / github-mcp→own Fly app pinned v1.5.0 / Caddy dissolves (Fly edge TLS); (4) DNS = beta.pipermorgan.ai, cutover-time, PM owns CNAME + OAuth callback. Cost ≈$20–35/mo PM-approved. decisions.log ×2; **#1278 body REWRITTEN** to real six-service architecture + build/cutover AC split. Build starting: target = working piper-morgan.fly.dev, droplet untouched.

## Fires 4–5 window (~14:30–17:00) — #1278 BUILD: the full stack is LIVE on Fly (piper-morgan.fly.dev)
**Provisioned + deployed**: Fly PG (attached; fresh dump restored MACHINE-SIDE after flyctl-proxy DNS proved unreliable — sftp + local psql; ownership reassigned to app user; pinned always-on after auto-stop caused resets), Upstash Redis (PM created interactively — flyctl prompt not scriptable; password rotated by PM mid-build, caught via the capacity-guard fail-close + rewired), Chroma (+1GB vol), gh-mcp (renamed `piper-morgan-gh-mcp` — Fly abuse filter blocks 'github'), all private-only; secrets piped droplet→Fly (never chat/disk).
**Five real defects fixed en route (all on main, all tested)**:
1. Dockerfile heredoc needs BuildKit syntax directive for Depot (+.dockerignore).
2. `get_sync_migration_url` didn't normalize `postgres://` (SQLAlchemy 2.x dropped the alias) — migrate died.
3. **`_get_database_url` never read DATABASE_URL at all** — app would crash-loop on localhost defaults.
4. My own normalizer DROPPED `sslmode=disable` → asyncpg defaulted to TLS-prefer → Fly proxy hard-reset (translate-not-drop: sslmode→ssl).
5. **connection.py's duplicate builder** — app ran HALF-connected (chat worked via factory; reads/turn-saves/learning hit 127.0.0.1). One convention, both engines now. (8+ pinning tests.)
**Migrated-state gotcha**: `connector_bindings.mcp_server_ref` carries the literal compose hostname → repointed to .internal (added to cutover runbook items on the issue).
**Smoke (all green)**: health 200 / unauth 401 / real chat replies / GitHub read end-to-end (JWT → binding → encrypted-store grant → private sidecar → live issues). **Parity check vs droplet: identical** — incl. discovering #1388 (issue-list reads ignore named repo; pre-existing both sides, filed with fix-shape).
**Remaining on #1278**: cutover session (PM: CNAME beta.pipermorgan.ai + OAuth callback; me: binding repoint in runbook, quickstart update, verified write, parallel-run window, decommission check). Build AC all checked on the issue.
- Fire 5-late (16:35, arrived mid-idle post-build) — **both Arch reviews drained on arrival**:
  - **#1387 FIXED + CLOSED same-day as flagged** (Arch's #1278 boundary-check find: field-encryption silently wrote PLAINTEXT on unset master key — "non-prod fallback" nothing enforced). `_no_key_fallback_or_raise`: production + no key = RuntimeError on writes, both types; dev keyless preserved; 6 tests. Rides next deploys (both live envs have the key — this guards the future mis-boot, exactly the cutover window).
  - **#1386 P1 CONFIRMED REAL by checking**: the ADR-077 D4 reachability lint ran ONLY in chronically-red ci.yml → not gated anywhere green. Wired into the (green) Architecture Enforcement workflow. P2 folded as criterion 5 (boundary-in-artifact: security suite vs shipped build + live-DB at-head/autogen-empty + master-key-present-via-guard); P3 folded as 3a (federated queries OUT of beta surface; scenario constraint for CXO/PPM; if-IN → #1322 gate-blocking).
  - Reply memo to Arch (cc PM) incl. the mcp_server_ref literal-URL design nudge from the migration.
