# Session Log: 2026-07-31-0711-pa-code

**Role**: Piper Alpha (PA)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Friday, July 31, 2026
**Start Time**: 7:11 AM PDT
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/pa` on `claude/pa-cycle` (Model A)
**Cron**: `52cfe05a` — `42 6,9,12,15,18,21` (one job, matches registry row)

## START — 7:11 AM

**Step 0 — prior day verified closed.** `dev/2026/07/30/…-pa-code-log.md` carries
`<!-- DAY-CLOSED: 2026-07-30 -->`. No missed-close self-heal needed.

**Environment**: worktree ✅ · branch ✅ · `HEAD..origin/main` = 0 ✅ · tree clean ✅ ·
one cron, no duplicates, id matches registry ✅ · inbox 1.

**Today's queue was set at last night's STOP, with named triggers rather than intentions:**
1. **Run the two client-LLM probes** — green-lit by CXO (A) and PPM (B); deferred from 7/30 STOP because
   a designed experiment started inside a day-close fire ends half-run.
2. **File the cron-mechanism gap to CIO** — owed since 7/29 and dropped once. Second consecutive
   memory-eval appearance; not to be re-promised a third time.

## Work Log

### 7:11–8:0x — START; the dropped item filed first; Probe A built and blocked

**Filed the owed item BEFORE the big work, deliberately.** The cron-mechanism gap was dropped yesterday
because PDR-006 displaced it — so today it went first, which is the actual fix for that failure mode.
Sent to CIO cc PM/HOST/Exec/Pard/Lead/Comms/Docs (`1f51165d1`), plus Pard's separate-repo copy.
Content: nothing documents *which* cron mechanism we use (session-scoped `CronCreate`), and it has **two
silent death modes** — dies on session exit, auto-expires after 7 days, neither emitting anything, while
the registry records *intent* rather than liveness. **PA lost 7/27–28 to exactly this; Arch went dark the
same way** (*"I didn't arm a cron after migrating"*), which makes it a process gap rather than two lapses.
Three suggestions, and I flagged the third as the real question: **every armed job in the cohort expires
within 7 days of arming, staggered, and nobody tracks it** — a distributed set of silent deadlines that
would produce a quiet day where several roles are dark and every board reads clean. Finding #6, one layer
down.

**Probe A built.** Five payloads, each a *different kind* of honesty — graded confidence, incomplete
coverage, honest refusal, freshness boundary, capability truthfulness — so a failure identifies **which
kind is fragile** rather than just that honesty is. Scoring: survived / weakened / dropped /
**contradicted** (the serious one: the client asserting what our payload explicitly denied). Committed
runnable at `dev/active/probes/` with a README carrying the design and the run command.

⛔ **Not run — blocked on credential access, and I stopped rather than routing around it.**
The script resolves no API key because **there is no dotenv file anywhere** — not in the shared checkout,
not in any worktree. The Anthropic key lives in the **macOS Keychain** via `KeychainService`.

I did **not** reach into the Keychain. The app using PM's credential in normal operation is one thing;
**me pulling it to fund an experiment PM didn't specifically authorize is another**, and the cost, though
small, is PM's. CXO and PPM green-lit the *probes*; neither authorizes spending PM's key. Asked instead.

⚠️ **Side finding worth correcting where it lives**: CLAUDE.md's server-restart gotcha says an inherited
empty `ANTHROPIC_API_KEY` "shadows the real key in `.env`". **The `env -u` advice is still right — but
there is no `.env` to unshadow.** The key comes from Keychain. The prescription survives; its stated
mechanism is stale.

**What this leaves**: the rig is one sentence from running. The intellectual work — payload design,
confound control, scoring scheme — is done and committed, which is the part that would have been wasted
had I stopped at "blocked."

### 10:11 — CXO's four-lane synthesis; three responses, one of which I'd escalate

**CXO independently assembled the four-lane view** of the credential blocker: #1386 criterion 2, PA's
Probe A, #1445, #1395 — *"four lanes, three roles, one provisioning step."* Lead had probed the keychain
on 07-30; my 07:15 report was the second sighting; CXO correlated them by hand this morning.

**1. PM had already answered half of CXO's ask.** CXO offered PM the choice *"if you'd rather not fund
the probe, say so and I'll design the branch without it"* — but **PM funded it at 07:5x today** ("yes you
may"). Relayed, because CXO was about to have PM re-decide something already decided, and that's a
round-trip for nothing.

CXO also confirmed the scope reading explicitly and unprompted: *"I authorized the probe's design, not
the spend of your credential, and I hadn't thought about the distinction until PA drew it… an agent that
reaches into a keychain to unblock itself is a worse failure than a probe that waits."* That converts my
call from an idiosyncrasy into a norm, which is worth more than the green-light was.

**2. The #1382 fallback will NOT cover this — the bit nobody had.** Corroborated Lead by a different
route (traced `llm_config_service:213`'s resolution order rather than probing the keychain), and added
the load-bearing detail: **`_db_store` activates only when there is no real keyring backend, and Amber's
`keyring.backends.macOS` is live — so the fallback never engages.** Anyone reading #1382 as coverage
here would be wrong. Consequence: this isn't just "PA's probe can't run" — **by the code's own path there
is no LLM credential on the Amber seat at all**, consistent with CXO's finding that the canonical suite
*skips* rather than fails.

**Corrected CLAUDE.md's server-restart gotcha**, which was actively misleading: it attributed this exact
symptom to an empty env var shadowing a key *in `.env`* and prescribed stripping the vars. **The
prescription remains right for its original cause; the mechanism it named describes a setup that no
longer exists, and on Amber stripping the vars fixes nothing.** Both causes now documented side by side
with the fallback caveat. Flagged to Lead as adjacent to their lane, revert-or-reshape invited.

**3. 🔴 The escalation — cross-lane convergence is now n=2 in 24 hours.** CXO's own words: *"none of the
reporters had the others in view when they wrote."* **I logged that exact shape last night** from a
smaller instance (CXO and PPM asking the same question four hours apart, neither seeing the other,
caught only because both landed in my inbox). **7/30 was two lanes; 7/31 is four. Both were resolved by
an individual happening to read enough inboxes at the right moment.**

**Mail distributes; nothing correlates.** Everyone was cc'd on everything and four roles still filed four
separate stall reports. The failure isn't attention — every reporter was diligent, correct and specific.
Nothing in the process asks *"has anyone else reported a stall with this shape today?"*

Routed to CIO + Exec rather than PM. **Did not propose the mechanism** — that's CIO's surface and
manufacturing ceremony is the wrong reflex — but argued two instances in 24h moves it from observation to
design question, and noted the cheap version is probably a **question inside an existing ritual** rather
than a new artifact. The version that doesn't get caught: four roles each waiting on what they think is
their own blocker, indefinitely, while every board reads clean — **finding #6's shape at the cohort
layer.** `9f705d44e`.

### 13:11 — PDR-006 RATIFIED; and a correction that inverts twelve days of my own advice

**PDR-006 is RATIFIED** — PM in conversation with Exec, ~11:00: *"And yes I do ratify PDR 006."* Arch
recorded it in the corpus and `decisions.log`, and **wrote the three Architect conditions INTO the PDR**
so the implementation epic inherits them without re-reading a memo. Arch's reasoning is the right one and
worth keeping: *"ratifying the decision doesn't discharge the conditions attached to it… the gap between
[reading the PDR and re-reading my review] is exactly where architectural conditions go to die."* My
conflation guard is recorded there too. Verified all three surfaces rather than trusting the memo.

### ⛔ The catch — and it's a correction to my own twelve-day-old advice

Exec's relay carried two things from the same PM conversation: PM **expects to do the OpenAI identity
verification today**, and is **refocusing their ChatGPT base of operations onto the design-and-product
account**, away from kindsys.

Before restating my standing "just go verify it" advice, I checked the mechanics. **OpenAI's help centre:
"An ID can only verify one organization every 90 days."**

**Verification is per-ORGANIZATION and the government ID is the rate-limited resource.** So this is not
*verify-wrong-then-redo*. It is **verify wrong ⇒ the right org is blocked until ~29 October** — and PM
was about to spend that action **during an account migration**, which is exactly when the wrong org is
easiest to pick, since muscle memory points at the old one. kindsys closed yesterday; verifying there
would burn the window on a dying account.

Sent as **URGENT** with the one question that must precede it: *which OpenAI org will own the Piper
Morgan ChatGPT listing?* Named the real tension rather than papering it — **the Claude side is
consolidating on pipermorgan.ai while the ChatGPT base moves to design-and-product**, and if those
diverge the listing lands away from everything it belongs with. Didn't guess which; it's PM's and isn't
inferable from anything I can see.

**Owned the correction plainly.** I called this *"a five-minute unblock with no dependencies"* on 7/26
and pushed it daily since. **It has a dependency and I never checked for one** — the 90-day limit is on a
public help page and one search would have found it any day this week. That is the **fourth** instance of
one failure this thread: tier answer, open-source "decision," Q2, and now this. Named the shape, because
it's specific enough to be actionable: **the item I'm most confident about is the one I stop checking.**

**Keys still absent** at 13:11 (checked, not assumed) — four lanes still blocked on that one step.
`9b6469f4f`.
