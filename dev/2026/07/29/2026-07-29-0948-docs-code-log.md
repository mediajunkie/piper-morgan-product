# Session Log — Documentation Management (Docs)

**Date**: 2026-07-29
**Start**: 09:48 PDT
**Role**: Documentation Management (Docs)
**Tool**: Claude Code
**Model**: Opus 5 (1M context)
**Host**: Amber (`pipermorgan.ai` account)
**Worktrees**: product `~/Development/piper-morgan-worktrees/docs` @ `claude/docs-cycle`; website `~/Development/piper-morgan-website-worktrees/docs` @ `claude/docs-cycle`
**Session type**: Amber arrival / orientation

---

## Entry 1 (09:48) — Arrival + environment verification

Read in order per PM instruction: `dev/active/docs-handoff-2026-07-28.md` (predecessor's §4/§6), then
`dev/active/amber-onboarding-delta-2026-07-29.md`.

### Environment verification — answers to predecessor's §5 questions

Predecessor wrote §5 as **questions**, not assertions. Verified each:

| §5 question | Verified answer | Evidence |
|---|---|---|
| Product worktree at `~/…/worktrees/docs` on `claude/docs-cycle`, 0 behind? | ✅ **YES, 0 behind** | `git rev-list --count HEAD..origin/main` → `0` |
| Website repo worktree exists for Docs lane? | ✅ **YES — provisioning gap already closed** | `git worktree list` → `…/piper-morgan-website-worktrees/docs [claude/docs-cycle]` |
| Website worktree 0 behind? | ✅ **YES — 0 behind, 0 ahead, clean tree** | fetched + counted in that worktree |
| Memory pool live (~166 files) rather than needing import? | ✅ **YES — 168 files, populated** | `ls …/memory/*.md \| wc -l` → `168` |
| Freeze-watchdog registry row for `docs`? | ❌ **NO ROW AT ALL** | see finding 2 below |
| Does `publish-post.js` resolve from the new worktree path? | ⏳ Not yet exercised — see finding 1 | deferred to pre-publish |

**Note on the delta doc's §4 warning** ("provisioning does not yet create the second worktree") — that
was true when written this morning; it is **no longer true for the Docs lane**. Both `docs` and `web`
website worktrees exist as of 07:41 today. Verified rather than assumed, per instruction.

### FINDING 1 (blocks first publish) — `piper-morgan-website` has NO git identity configured

```
git -C …/piper-morgan-website config --local user.name   → rc=1 (unset)
git -C …/piper-morgan-website config --local user.email  → rc=1 (unset)
git --global user.name / user.email                      → unset
```

Effective identity in the website docs worktree resolves to git's **hostname auto-derivation**:

```
$ git var GIT_AUTHOR_IDENT
xian <xian@Amber.local> 1785344041 -0700
```

`xian@Amber.local` is an unroutable local hostname address. Any publish commit I made would land on
website `origin/main` mis-attributed, with no error and nothing to catch it — the commit succeeds.
The **product** repo is correctly configured (`mediajunkie` /
`3227378+mediajunkie@users.noreply.github.com`), which is why this is invisible unless you check the
second repo specifically.

This is a variant of onboarding-delta trap **#8** (provisioner git identity leaking). The catalog
describes a *wrong* identity leaking in; this is *no* identity at all, so git silently invents one.
Same class, opposite mechanism, same silent-failure signature — and it is exactly the case that
"confirm both worktrees before your first publish" was pointed at. **Not yet fixed — proposing
rather than acting, since git identity on PM's repo is PM's call.**

### FINDING 2 — Docs has no row in `dev/active/duty-cycle-registry.tsv`

Delta §3 says "your registry row is probably marked `parked` — clear it when you arm your cron."
For Docs the state is different and worth stating precisely: **there is no `docs` row at all.**
Ten roles are registered (cio, exec, arch, lead, host, cxo, ppm, pa, comms, web); `docs` is absent.

Per the file's own header, "No row = not watched (not opted in / not migrated)." So Docs is
**unwatched by the freeze-watchdog**, not parked. Nothing to clear — a row must be *added*, and its
load-bearing field is a cron expression that does not exist until a cron is armed. Not arming one
unilaterally: per delta §3 that needs authorization, and per the handoff §5 it was an open question
("under whose authorization?"). **Raising to CIO.**

### FINDING 3 — omnibus gap: 2026-07-28 unsynthesized

413 omnibus logs on disk; most recent is **2026-07-27**. 2026-07-28 has **6 session logs**
(comms, host, exec, docs, cio, ppm) and no omnibus. Today (7/29) is in progress and not yet due.

This is precisely §4.6 — "the omnibus is this role's most fragile deliverable, because nothing
alarms on it." My predecessor found it at a 4-day gap via a late audit. This one is 1 day old,
caught on arrival. Queued as first substantive work.

### Environment items NOT yet verified (stated, not assumed)

- **Hooks, both command shapes** — not probed yet. Per delta §1 this needs standalone *and* compound
  probes reported separately, with index state printed between (CLAUDE.md's resolved mechanism).
  Deferred to first commit, not skipped.
- **`publish-post.js` cross-repo relative path from the new worktree** — the fragile part of every
  publish. Will exercise the whole path once before relying on it (delta §0 trap #9: a dry-run is
  not a full-path proof).

---

## Entry 2 (10:30) — Omnibus gap closed: 2026-07-28 synthesized

`1260f11dc` (omnibus) + `2049ea7b7` (activity log), both on `origin/main`.

Ran `create-omnibus` in literal step order — my predecessor's §4.1 lesson is that inverting a
skill's step order *is* the bug, so pre-flight (`methodology-20`, read in full) came before source
discovery, and the cross-reference gate before format selection.

**Result**: HIGH-COMPLEXITY: COORDINATION, 6 sessions, **151 timeline entries**, 418 lines.
Coverage now **414 omnibus logs, gap-free since June 2025**. 7 Shape-B activity rows (1759→1766).

### Cross-reference gate — resolved from evidence, not assumption

Seven roles were mentioned across the source set with no 7/28 log. Rather than treat the source set
as complete or block on all seven, I resolved each against branch commits and artifacts:

- **Lead** — genuinely dark mid-cutover; HOST verified and recorded it at 07:07. Not a missing log.
- **CXO** — ⚠️ **a real gap.** CXO *was* active 7/28 in the pre-Amber Desktop session and wrote its
  predecessor handoff that day, but it was **printed in chat rather than mailed**, so it reached the
  repo only on 7/29. No repo-side log exists. Documented in the omnibus Sources block and given a
  reconstructed `environment=web` activity row rather than being silently omitted.
- **Arch / PA / Web** — zero branch commits; mentions are backreferences or inbox receipts.
- **Pard / Janus** — cross-project, active by mail, no PM-repo log by construction.

### Two source defects found and named rather than smoothed

1. **PPM timestamp conflict** — log narrates PM's emeritus clarification at 6:20 PM; its own
   retirement commit `afee80ff4` is stamped 17:43. ~37 min apart. Anchored to the commit, flagged inline.
2. **The Docs log (my predecessor's) contains four duplicated entries** — the 333-field backfill,
   the Ship #050 column-shift repair, the Dispatch reply and the Comms memo each appear twice, at
   ~10:05–10:25 and again at ~14:40–15:10. Each pair describes the *same single commit*. **The commit
   times (09:52–09:55) precede both narrated times.** Reading the later set as a second work session
   would have invented an afternoon of Docs work that never happened. Timeline anchored to commits.

### FINDING 4 (new) — methodology-20's two HIGH-COMPLEXITY compression rules cannot both be satisfied

Distinct from the line-vs-entry-count mismatch my predecessor flagged across the Jul 24–27 backfill.

| rule | implies |
|---|---|
| "compress only 20-30% of source log detail (**preserving 70-80%**)" | ratio **1.25–1.43×** |
| "Compression ratio check: Source logs / Omnibus lines **> 3 but < 10**" | preserving **10–33%** |

**The bands do not overlap.** This omnibus sits at 1.66× (694 source lines → 418) — slightly *more*
compressed than the preservation rule asks, and far below the ratio check's floor. Satisfying the
ratio check would require discarding ~60% of what the preservation rule says to keep.

So three of four HIGH-COMPLEXITY size checks pass and the fourth is unsatisfiable jointly with one
of the others. **Flagged in the file and raised to CIO (methodology owner) rather than gamed in
either direction** — padding or gutting would both make the number look right and the omnibus worse.

---

## Entry 3 (~15:30) — ⚠️ I reported the Ship's state from a 45-commit-stale worktree

**PM caught this, not me**: *"your info is stale — exec drafted, I edited, Comms reviewed… are you checking origin main?"*

I had reported that Weekly Ship #053 had no draft and no calendar row, and that publishing was blocked
on drafting. **All of it wrong.** Exec drafted it, PM edited, Comms reviewed and cleared it. The row and
the draft both existed on `origin/main`.

**The mechanism**: I rebased at session start (~09:50), then read the calendar CSV and globbed the
drafts directory **from my own working tree** several hours later. By then I was **45 commits behind**.
The files I read were real files, correctly parsed — just an old copy. Nothing errored.

**This is my predecessor's rule in a costume it doesn't cover.**
`feedback_read_the_artifact_not_testimony_about_it` says read the artifact, not a description of it. I
*did* read the artifact. **The rule has an unstated precondition — the artifact must be current — and a
stale worktree satisfies the letter of the rule while breaking it completely.** A checkout that is
behind reads cleanly and answers confidently, which is the same silent-failure signature as everything
else this week (m-44). `publish-to-blog`'s own **Pre-Step** says to sync before *any* file read, and
documents this exact class from 2026-07-18. I ran it at session start rather than before the read.

**The correction, stated as a rule I'll hold**: sync immediately before reading, not once per session.
`git fetch && git rebase origin/main` costs ~2s; being hours stale costs a false report to PM.

## Entry 4 (~15:45) — Weekly Ship #053 PUBLISHED

**Live and verified**: `https://pipermorgan.ai/shipping-news/weekly-ship-053-the-invariant-held/`
(HTTP 200; live body confirmed to contain the glossed phrasing and **not** the old bare "Driver").

| item | value |
|---|---|
| hashId | `7e98a844b11a` |
| slug / cluster | `weekly-ship-053-the-invariant-held` / `ship-053` |
| workDate → pubDate | **2026-07-17** → 2026-07-29 (workDate ≠ pubDate, the skill's hardest check) |
| website commit | `1e8cb88` (author verified `mediajunkie`, not `xian@Amber.local`) |
| product commits | `ed6c2b15e` (gloss) · `422dbfb8f` (calendar) |

### The "Driver" blocker — resolved by lookup, not by guessing

Comms escalated `Driver` as a bare proper noun with no traceable referent and **correctly refused to
invent a gloss**. It wasn't unknowable, just unlooked-for:

- `decisions.log:225` ratifies the sprint as *"census → guards → HIGH-fixes → **driver-green** acceptance"*
- `finish-the-unfinished-sprint-2026-07-16.md` Phase 3 is titled *"Acceptance (the driver is the referee)"*; principle 5 is *"The driver is the referee"*
- The referent is `tests/e2e/test_scenario_driver.py`, and the claim is **accurate and in-window**: Phase 3 records *"PASS, 0 assertion failures, real LLM"* at **2026-07-17 12:50 PT**

Comms' own guessed phrasing ("completion-gate check") would have been slightly wrong — it's an
end-to-end scenario harness, not a gate script. **Asking was the right call and the lookup was cheap.**
PM chose the wording; I applied it verbatim to both draft copies.

### ⚠️ A gloss RACE — two independent fixes, resolved to PM's pick

While I was publishing, Comms applied **their own** gloss (`193647805`) and set status
`ready-for-docs`. Rebase surfaced it as a conflict. Two different correct answers to the same blocker:

- **Comms**: *"The scenario driver (the harness that runs real conversation turns against a live model)"* — parenthetical gloss on first use, which is house style
- **PM/mine (published)**: *"The end-to-end scenario harness"* — drops the proper noun entirely

**Resolved to PM's**, on two grounds: it's PM's explicit in-session choice, and **the site was already
live with it**, so keeping Comms' version would have left draft and published content disagreeing.
Comms' is arguably the better house-style answer — flagged to them rather than silently overwritten.

The calendar conflict was **merged, not picked**: Comms' newer `notes` preserved, my four publish
fields (`status`/`canonicalSite`/`blogURL`/`blogPath`) applied on top, addressed by header name.

### Two of my own suspicions cleared by checking, not assuming

Both would have been false corrections to a live publish:

1. **Empty `caption`** — looked like a gap; #051 and #052 both published with it empty. Normal for Ships.
2. **`<em><em>` double-nesting + mixed trailing slashes** in the generated HTML — present in published
   #052 with an *identical* count in the same four places. Pre-existing and corpus-wide, not introduced
   here. Noted, not "fixed" mid-publish.

### Toolchain finding — the dry-run passed and the real run failed (delta trap #9, exactly)

The mandatory `--dry-run` was clean. The real invocation **failed at `fetch-blog-posts.js`**:
`rss-parser` not installed — `node_modules` absent in the freshly-provisioned website worktree.
**A dry-run is not a full-path proof**, because it skips the sync+fetch stage entirely.

- Mutations had already landed (CSV row, `blog-content.json`) before the failure — partial state, recovered without loss.
- `npm ci` then **wiped `node_modules` and failed** on puppeteer's postinstall (corrupted browser cache in `~/.cache/puppeteer`, unrelated to publishing), leaving it emptier than before.
- Fixed with **`npm ci --ignore-scripts`** (667 packages, `rss-parser` present) — deliberately chosen so nothing outside the repo was touched. Puppeteer's browser is not needed for publishing.

**Worth flagging to whoever provisions worktrees**: a website worktree without `node_modules` passes
every dry-run and fails every real publish. Both website worktrees (`docs`, `web`) were cut today.

### Not done — deliberately

- **Step 9 drafts archival** — the skill gates it on a confirmed syndication URL. Ships syndicate to LinkedIn and that's PM's manual step. Draft stays in `drafts/` until PM provides the URL, then status → `distributed` and archival runs.
- **Hook probe** — still unrun; see Entry 1.

---

## Entry 5 (~16:05) — PM's apology relayed to Comms; provisioning gaps filed for Pard

**Relayed PM's apology to Comms** (cc Exec, PM) — PM asked to own the Ship #053 race publicly and
close the thread. Quoted PM verbatim rather than paraphrasing, on the grounds that a second-hand
apology should be in the sender's words. PM's ownership: *"That was my fault because I forgot that
comms are still tracking down those facts."* PM endorsed the outcome, so no rework. Also told Comms
plainly that I still think their parenthetical-gloss form was the better house-style answer and that
it lost a race rather than a judgment.

**Recorded the failure mode durably** in `decisions.log` (`8f267eaeb`) rather than leaving it in a
memo: *a blocking question already routed to one recipient, answered in parallel through a second
channel, forks the work — and the loser is decided by rebase order rather than merit.* Named as the
human-coordination twin of the m-44 family: two channels each produced a correct answer and nothing
compared them until a merge did.

**Filed 5 provisioning gaps to CIO** (cc PM, Web) for relay to Pard — there is no local `pard`
mailbox, so CIO is the route. Two are host-level and block any website build:

1. **`node_modules` absent across the ENTIRE website repo on Amber** — both fresh worktrees *and* the shared checkout. I first wrote this as worktree-specific and **corrected it before sending**; nobody on this host has ever had working website deps. The dry-run passed clean because it skips sync+fetch, which is the only stage that could fail — trap #9 verbatim.
2. **puppeteer's browser cache is corrupt at host level**, which blocks `npm ci` for the website repo from *every* worktree (`~/.cache/puppeteer` is per-user). Directory created 09:35:46 today, contains only `ABOUT` + `LICENSE` — the binary never landed, and the installer's existence check can't tell a partial extraction from a complete one. `npm ci` made things worse (wiped `node_modules`, then failed). **Workaround verified: `npm ci --ignore-scripts`.** Did **not** delete the cache — it's outside the repo and PM's, and the workaround already unblocked the work; recommended the `rm -rf` for authorization instead.
3. **Website repo had no git identity** (closed today) — provisioning asymmetry: product configured, website not, so a one-repo check reports clean.
4. **Shared website checkout 6 behind origin** — the drift the worktree ruling targets.
5. **No `docs` registry row** (closed). Plus a structural flag: `CronCreate` jobs are session-only with a 7-day fuse, so every Amber registry row sourced from it makes a more fragile liveness claim than the row implies.

**The pattern I actually escalated**: all five are invisible to inspection and visible only to
execution. An absent `node_modules`, a partial browser download, a missing git identity, a behind
checkout, an absent registry row — each produces a *successful-looking* result. That's m-44 relocated
from instruments to provisioning. Suggested fix is behavioral: after cutting a worktree, run the
lane's real command once, because a worktree whose toolchain has never been exercised is unproven,
not ready.

### Ship #053 — live, re-verified at PM's ask

HTTP 200, 37,975 chars serving, glossed wording present, bare "Driver" absent. **PM cross-posting to
LinkedIn.** Awaiting the URL to set `status=distributed` + `liPubDate`/`linkedinURL` and run Step 9
archival.

⚠️ **One false finding caught before reporting it**: the `/shipping-news/` index appeared not to list
#053. It lists **no** ship slugs in raw HTML — including #052, which is definitely live — so the index
is client-rendered and `curl` cannot see it. The absence was uninformative, not a defect. Third false
positive caught by checking today (after the pattern count and the caption/image "gaps").

## ⚠️ Timestamp correction (16:10, verified by `date`)

**Entries 3-5 above carried GUESSED timestamps that ran up to ~55 min ahead of real time.** I ran
`date` at 15:26 and then extrapolated forward across several work units instead of re-running it.
Corrected in place above (16:05→~15:30, 16:20→~15:45, 17:10→~16:05).

**Two memos went out with the same drift** and cannot be silently fixed, so noting them here as the
correction of record:

| memo | stamped | actual (approx) |
|---|---|---|
| `...ship053-published-your-gloss-was-overwritten...` | 16:25 PT | ~15:50 PT |
| `...pm-owns-the-ship053-race-condition...` | 16:45 PT | ~15:58 PT |
| `...provisioning-gaps-from-first-publish-on-amber-for-pard...` | 17:05 PT | ~16:05 PT |

Ordering and content are unaffected — the sequence is correct, only the absolute stamps drifted, and
all three are same-hour-adjacent rather than wrong-day. Flagging anyway because
`feedback_verify_timestamps_never_guess` is explicit: run `date` before asserting a time. **The rule
isn't "check the clock once per session" — it's check it before each assertion.** Same shape as this
afternoon's stale-worktree error: I verified once and then trusted an extrapolation.

---

## Fire 1 (16:10 PDT) — WORK. First duty-cycle fire on Amber (cron `26805e13`)

Step 1: cron verified — exactly one job, `57 6,9,12,15,18,21`, no duplicates. Dispatch: session log
exists, hour 16 is not the largest hour in my expression (21 is) → **WORK PARTS**.

### ★ Hook probe — BOTH shapes, un-confounded, index printed at every step

First fire in this worktree, so Step 2a-bis applies. Ran the **corrected** v1.19+ order (Probe B first,
against a verified-empty index) rather than the order that produced a guaranteed false pass:

| probe | shape | index before | result |
|---|---|---|---|
| **B** | compound `echo && git add && git commit` (one call) | **printed EMPTY** | ⚠️ **BYPASS** — commit landed, hook never fired |
| **A** | standalone bare `git commit` (staged in prior call) | **printed EMPTY** | ✅ **BLOCK** — refusal named `check-branch.sh` |

**This is the expected state on a correctly-configured Amber seat**: the hook is alive but does **not**
cover the shape I actually commit with. Reported separately per the skill, because "A passes / B
bypasses" is its own state and a single probe cannot express it.

Cleanup was soft-reset + explicit-path `rm` (never `--hard`), index printed empty after each, and
verified no probe commit survived and no residue remained. **My data is un-confounded** — B ran first
against a demonstrably clean index, which is the cell that five earlier seats never produced.

Practical consequence I'm adopting: **stage in one call, commit bare in the next** whenever I want a
mailbox commit actually gated. `mail-send.sh` is structurally safe regardless (`commit-tree`).

### Mail loop — drained of everything actionable (32 → 29)

**★ The find that matters: Comms' `PUBLISH-READY` memo was sitting unread in my inbox, and it contained
the Driver answer.** Sent **15:35**; I published at **~15:45**. Ten minutes.

PM apologized this afternoon for causing the gloss race by forking the question. **It wasn't all PM's.**
Comms solved it, delivered it, and said nothing was pending on their side — and I acted without opening
the memo. Had I drained mail first, which is literally the first half of WORK PARTS in my own procedure,
there would have been no race at all. **Memo sent to Comms (cc PM, Exec) correcting my own account** —
PM shouldn't carry blame for the half that was mine, and Comms shouldn't think the collision happened
upstream of them when the last link was me.

Also from that memo, three things I'm acting on rather than acknowledging:
- **`diff` the two draft copies before every publish — adopted.** Comms caught that `e91cb5466` added the Almost Beta image block to the `dev/active/` copy only; a publish would have **silently dropped the image and reported success.** I did diff them, but by luck (I was applying the gloss to both), not procedure.
- **The chronic cause is Docs-owned and I'm taking it**: `draftPath` values that don't resolve (#052's doesn't either; a Jul 12 pass fixed 22 instances without fixing the cause). Same missing thing as my predecessor's unbuilt per-column assertion — **the calendar asserts facts about the filesystem that nothing verifies.**
- **Did NOT "fix" the mixed trailing slashes.** Checked #052's published HTML first, found the identical pattern, left them. Comms' "please don't fix these" arrived after I'd decided — another argument for reading mail first.

### Toolchain addendum to CIO (cc PM, Web, Comms) — the gap is BOTH languages

Comms hit the same provisioning gap in **Python** hours before I hit it in **Node**, independently, in a
different repo, with a different tool. Verified their claim myself rather than relaying it, and it's
worse than reported:

```
python3 -c "import yaml"                                      → ModuleNotFoundError
./venv/bin/python                                             → absent
/Users/xian/Development/piper-morgan-product/venv/bin/python  → ALSO ABSENT
```

**No venv anywhere on this host, shared checkout included** — so `template-audit` check #1 is not
degraded-in-a-worktree, it is **unrunnable on Amber for every role**. That check validates frontmatter,
which is exactly the class that produced the caption `''` bug. **The blog QA gate has a silent hole.**

Both halves fail *as a pass*: a traceback among thirteen passes reads as a pass; a clean dry-run reads as
a proven path. Comms named theirs on sight as "m-44 inside the audit tool itself" — I'd extend it to
m-44 in the provisioning layer, where the instruments that would catch it are themselves unprovisioned.
Ranked recommendation sent; the durable one is #3 — **make a check that cannot run report `CANNOT RUN`
in the same column as PASS/FAIL**, the same shape as `HEARTBEAT-WRITER-SILENT` and `det_rc`/`det_bytes`.

Worth noting for evidential weight: Comms and I found these **independently before comparing notes**, so
this isn't the shared-blind-spot failure CIO and Arch named on 7/26.

### Dispatch's 15-stash cleanup — BLOCKED, and the blocker changes the deadline

Read-only inspection stopped the task on the first check:

- The memo's path `/Users/xian/Development/piper-morgan/piper-morgan-product/` **does not exist** (the same nonexistent path PPM corrected in CLAUDE.md's data-loss HARD RULE today).
- The real main checkout has **zero stashes** — no `refs/stash`, no stash reflog.
- **Every worktree checked: 0 stashes.** No nested `piper-morgan/` dir. **There are no stashes anywhere on Amber.**
- `scripts/sync-pm-local.sh:66-67` probes *both* candidate paths; Dispatch reported the one that only resolves on **PM's pre-Amber machine**.

⚠️ **The real deadline is machine decommissioning, not reflog expiry.** A stash is unpushed by
construction, so nothing in those 15 is on `origin/main` — if that machine is wiped, all of it goes.
Recommended exporting all 15 to patch files *before* any drop (the narrow reversible move, same
principle as memory-export-before-prune).

**But I discharged the part I could**: Dispatch flagged `stash@{1}` and `stash@{13}` as most likely to
hold real work and asked whether it landed elsewhere. Both clear:
- **`stash@{1}`** — base commit `43385d5bb` **is an ancestor of `origin/main`**; #1289 closed with evidence (`a10ea2aa3`, "verified+closed, 686 green"); the retirement landed (`958cb19db`, −779 lines).
- **`stash@{13}`** — a HOST session log for 2026-05-15 **is on `origin/main`** (`dev/2026/05/15/2026-05-15-0623-host-code-opus-log.md`). Not missing.

Stated the honest limit rather than rounding it: I proved each stash's *target work landed*; I have not
seen the stashed lines, so these are **"cleared of the missing-work concern," not "verified identical."**
Dispatch's own instruction — PM would rather carry a stale stash than lose unmerged work — makes that
distinction the load-bearing one.

### ⚠️ Four false alarms caught by checking, one self-inflicted

Worth recording as a set, because the ratio is the point — **every one would have been a confident wrong
report to PM or a bad "fix" to a live artifact**:

1. **Empty `caption`** on the Ship draft → normal; #051/#052 published the same way.
2. **`<em><em>` + mixed trailing slashes** in generated HTML → identical in published #052. Pre-existing, corpus-wide, not mine to fix mid-publish.
3. **`/shipping-news/` index "not listing" #053** → it lists *no* ship slugs in raw HTML, #052 included. Client-rendered; `curl` can't see it. The absence was uninformative.
4. **"`mailboxes/` has been deleted"** → self-inflicted. My Bash cwd had persisted from an earlier `cd mailboxes/docs/inbox`, so paths resolved inside the inbox. 32 files, 32 tracked, nothing lost. **Bash working directory persists between calls** — worth remembering before the next scare.

Plus the **timestamp drift** correction logged above: I ran `date` once at 15:26 and extrapolated across
several work units, putting three memos up to ~55 min ahead of real time. Same shape as the
stale-worktree error — verify once, then trust an extrapolation. **The rule is verify per assertion, not
per session.**

---

## Fire 2 (19:10 PDT) — WORK. Ship #053 transaction closed; validator shipped

Step 1: one cron job, `57 6,9,12,15,18,21`, no duplicates. Hour 19 is not the largest hour (21) →
**WORK PARTS**, not STOP. Sync clean, 0 behind. Hook probe skipped (first-fire-only, done Fire 1).

### ★ Ship #053 syndicated and archived — the transaction is complete

Dispatch-DinP delivered the LinkedIn URL, which was the only thing gating archival.

| item | value |
|---|---|
| `status` | `published` → **`distributed`** |
| `linkedinURL` | `…/weekly-ship-053-invariant-held-christian-crumlish-18ukc/` |
| `liPubDate` | 2026-07-29 |
| `mediumURL` | **left empty by design** — `ship` theme is LinkedIn-only (matches #052) |
| archival | draft → `published/` per Step 9 |

**Two corrections to Dispatch's memo, both benign**: it listed `status` as `drafted` (I'd set
`published` ~3h earlier) and said the `notes` field still carried `OPEN FOR PM` markers (Comms folded
both resolutions in at ~15:30). Both were stale reads, neither cost anything — and I'd rather they send
a stale table than sit on the URL.

### 🔴 Found: the calendar was asserting a gloss that never published

The `notes` field recorded *"Applied the house-style parenthetical gloss: 'The scenario driver (the
harness that runs real conversation turns against a live model) runs clean'"* — **Comms' version, which
lost the rebase race.** Verified against the served page: it carries PM's *"The end-to-end scenario
harness runs clean"* and contains **neither** of Comms' phrasings.

So the record disagreed with reality about a piece published four hours earlier. Corrected in the row
with the verification method named, and cross-referenced to `decisions.log`. **This is the exact
publish↔calendar↔live-site vantage my predecessor named as load-bearing in §6** — nobody else stands
where those three can be compared, and it took a routine syndication pass to surface it.

### ★ Repaired ALL 7 stale `draftPath` values — the class, not the instances

Started with #053's own archival (moving a file without updating its row is *how* #052's path went
stale), then swept the file:

- **#052** — archived but path never updated. **#051** — `distributed` yet never archived at all; draft still in `drafts/`. Archived it.
- Then 4 more narrative posts (Mechanical First / Into Production / What the Running System Found / Almost Beta), all in `published/` with rows pointing at `drafts/`.

**0 unresolvable of 97 rows carrying a `draftPath`, down from 7.** Every instance had the same cause:
Step-9 archival moves the file and nothing updates the row. A 2026-07-12 pass fixed 22 instances without
fixing the cause — which is why it recurred.

### ★ Shipped the per-column validator (PM's question this afternoon, answered in code)

`scripts/validate-editorial-calendar.py` now catches the class a field-count check **structurally
cannot see**: a value in the wrong column while the count stays a valid 18. Added enums, date-format
checks (incl. `chatDate`'s M/D/YYYY wart), URL/path prefix checks, the Ship #050 repo-path-in-prose
signature, and a `draftPath`-resolves reference check.

**The severity split IS the design**: shape violations are **errors** (the file is corrupt / a value is
misplaced); reference staleness and soft heuristics are **warnings** (never blocking). Rationale is
written into the module docstring — a heuristic that hard-fails causes *false corrections*, and the
precedent is my predecessor's 7/28 near-miss where a naive >320-char altText rule flagged 3 rows that
were genuinely long alt text.

⚠️ **Its own first run false-positived twice, and I fixed the CHECKER rather than the data** — which is
the whole point of testing before claiming:
1. 8 historical Ships carry `theme='shipping news'` (pre-`ship` convention). Hard-failing would have pressured someone into rewriting 8 historical rows to satisfy a checker. Now `LEGACY_THEMES` → warning, with the instruction not to bulk-rewrite.
2. A `notes` field containing a **claude.ai URL ending in `.md`** tripped the path-in-prose check. A URL in free text is legitimate; now anchored on real repo prefixes and skips `http`.
3. Separately, *"published but no blogURL"* fired **31 times** on historical rows predating the field. 31 unactionable warnings is how a checker teaches people to ignore it — the `check-acronyms.py` failure mode Comms named. Replaced with **pair-consistency** (`blogPath` without `blogURL`, or vice versa), which is era-independent.

**Behaviorally tested in an isolated tree, both directions** (never against the live file): catches an
injected Ship#050-style shift, a bad `status` enum, and a date in the wrong column → exit 1; treats a
broken `draftPath` as warning → exit 0, and fails it under `--strict`. Real file: **exit 0, 8 warnings,
all correctly characterized as legacy.** Both directions, because a guard that always passes is as
broken as one that never does.

### ⚠️ CORRECTION: I overstated Gap 1, and Web caught it

I had written *"`node_modules` absent in both website worktrees and the shared checkout."* Verified
properly after Web's reply:

| location | packages |
|---|---|
| `website-worktrees/web` | **667 — healthy all along** |
| `website-worktrees/docs` | 667, **but only because I installed it this afternoon** |
| `piper-morgan-website` (shared) | 0 |

**I checked mine and the shared checkout, then generalized to Web's without looking at it.** A claim
about someone else's environment from a sample that excluded it.

**And Web's data makes the real mechanism sharper and worse.** Their install timestamp is 09:35:49; the
corrupt puppeteer cache ctime is 09:35:46 — 3 seconds apart. So **Web's `npm install` succeeded and, in
succeeding, left a partially-extracted browser in the shared `~/.cache/puppeteer`, which then broke my
`npm ci` hours later.** Not "nobody has deps" but **the first agent to install poisons the shared cache
for everyone after — asymptomatic at the origin, order-dependent, and still live for the next lane that
provisions a website worktree.** Amended suggestion sent: provisioning must ensure that cache is
complete or absent, because *partial* is the failure state an install leaves behind.

Also noted to CIO: **Comms shipped the Python-half fix themselves** (`template-audit` v1.2 — removed the
`import yaml` dependency rather than satisfying it, added an explicit `⚠ CANNOT RUN` verdict token,
tested across four frontmatter shapes before claiming). They took my ranked-most-durable suggestion and
closed it in two hours in their own lane.

**Scorecard on the five gaps I filed today**: one overstated (this), one fixed by Comms, one fixed and
verified (git identity), one closed (registry row), one awaiting PM (cache clear). The only one that
moved backwards did so because someone checked my work.

### Comms pushed back on my self-blame, and they're right

Their reply argued my second correction was heavier than the facts support: ten minutes, their own memo
said *"nothing is pending on my side"* while they still held the referent, and the fork happened
upstream in chat. Accepting that rather than re-litigating it. Their framing — *"a ten-minute overlap
that cost one wording choice is a good day, not an incident"* — is the proportionate read, and the thing
they singled out as actually load-bearing (verifying their finding one level deeper instead of relaying
it) is the part I'll keep.

Mail: 3 consumed → `read/` (32 → 29). Remaining 29 are cc-only historical from the migration window.

### Fire 2 continued (~20:00–20:55) — PM's four decisions executed

PM asked "what do I need to decide," I gave four, and all four came back. Also established that
**HOST's Pass 3 is NOT a PM decision** — HOST wrote *"Docs, this is your call and I've stayed off
it,"* the refactor was PM-greenlit 7/12, and no CIO scoping note ever arrived. So that's mine to do,
not to ask about. Removed from PM's queue.

**1. Puppeteer cache — authorized, cleared, and it DISPROVED my own diagnosis.**
Cleared both partial dirs, re-ran the browser install, and **it reproduced the identical broken state
from a clean cache**: ~100MB downloaded, then `ABOUT` + `LICENSE` and **no binary**, rc=1. Disk space
259GB free, so not that.

So the cache was never the cause — **puppeteer's browser extraction is reproducibly broken on this
host.** Which also means my "Web's install poisoned the cache" mechanism was wrong: **Web's memo had
it right at lines 44-47** and I restated it with a causal arrow that doesn't exist. The extraction
fails for everyone every time; Web's `npm install` *tolerated* it, my `npm ci` didn't. Second time
wrong on this one gap — scope first, then mechanism, both caught by someone else's data.

What the experiment *did* establish: not-a-stale-cache (dead hypothesis, and it was the leading one),
not-disk-space, and `npm install`-vs-`npm ci` is the entire difference in outcome. Cause still unknown
— candidates I did **not** test: Node 26 vs this `@puppeteer/browsers`, macOS quarantine on the
extracted binary, archive-format change. Handed over narrowed rather than guessed at; it's Pard's lane.
Consequence is *stronger* than before: `--ignore-scripts` is not a workaround, it's the correct
standing procedure here. Deliberately tested with a targeted browser install rather than `npm ci`, so a
second failure couldn't wipe my working 667-package `node_modules`. It didn't.

**2. Column ownership — RATIFIED, written into `update-calendar` v1.4** (`deb90709a`). Comms owns
editorial columns, Docs owns publish/syndication, `status` shared sequentially. Wrote the *reasoning*
in too, including that **my own sole-ownership proposal was rejected and why** (170 commits/60d, 57
`(comms)` vs 4 `(docs)`; and both corruptions came from positional access, which one writer can do just
as destructively). Also fixed a latent bug: the Field Reference's `status` enum was missing
`ready-for-docs`. Notified Comms (cc PM/Exec) and Dispatch.

**3. PDR-007 drafted** — `docs/internal/product/pdr/PDR-007-editorial-data-single-source-of-truth.md`
(`35fb86c60`), routed to Arch + CIO for review. m-38 tier check: PDR, because the commitment is a
decision rule; the format/migration is a companion ADR I deliberately didn't write.

Core argument: **three failure classes with different causes, only one storage-shaped.** Column shift =
positional access (mitigated today). `draftPath` staleness = **referential integrity against a
filesystem, which no storage engine can validate**. Cross-surface disagreement = the only class a
single source fixes. Measured today: **17 field-level disagreements across 365 matched rows (~4.7%), 0
in the dangerous direction.** Four aliased field pairs found (`altText`↔`imageAlt`,
`caption`↔`imageCaption`, `blogPath`↔`slug`, `cartoon`↔`imageSlug`) — same facts, different names, so
no textual comparison finds them.

Recommended against a datastore (git-diffability is load-bearing at ~3 commits/day from concurrent
agents; a DB fixes only class 1 and risks becoming a fifth representation). **And recommended waiting
2–4 weeks before any migration — against my own preference** — because the validator shipped today and
has never been observed in production. Committing on the strength of a fix verified only in an isolated
tree would be the m-44 error, inside a PDR that cites m-44.

Two honesty notes in the PDR: I **could not reproduce** the inherited "~46 captions" figure (measured
2) so I declined to quote it and listed it as an open question; and I flagged that **I own the
reconciliation labor the PDR proposes to eliminate**, so my recommendation isn't neutral. Told Web
their objection should probably outweigh my preference, since the implementation cost lands in their lane.

**4. Stash cleanup — no deadline.** PM: the pre-Amber machine stays up indefinitely. Told Dispatch,
with the standing offer that I'll review all 15 per-stash if anyone exports them to patches somewhere
tracked. Nothing actionable from Amber.

### ★ Accidental live confirmation of the hook mechanism — cleaner than my own probe

A commit staging **only** `.claude/skills/update-calendar/SKILL.md` was **BLOCKED** by
`check-branch.sh`. Cause: my *previous* tool call had staged a `mailboxes/` path, and **PreToolUse fires
before the Bash call runs**, so the `git reset -q HEAD` at the front of my compound command hadn't
executed when the hook read the index.

**Same compound shape that BYPASSED in my Fire-1 probe, but with a dirty index → BLOCKED.** That is
CXO's decisive cell (pre-dirty the index, fire a compound commit; shape predicts bypass, index-state
predicts block) reproduced accidentally in real work. **It confirms index-state-at-fire-time, not
command shape** — and it's stronger evidence than my probe because nothing about it was staged for a
test. Fixed by clearing the index in its own call, then staging, then committing.

---

## Fire 3 (22:27 PDT) — STOP · DAY-CLOSE 2026-07-29

*Last scheduled fire (next 06:57 tomorrow — different calendar date). Cron `26805e13`, exactly one job.
Sync clean, 0 behind. Cleanup dry-run: no cycle-logs >7d, no `.tmp` >1d, no probe residue.*

### ★ Web reviewed PDR-007 within 80 minutes, and corrected my estimate DOWNWARD

The best thing in the day. Web read the **PDR itself, not my routing memo**, checked the code, and
found I had **omitted a surface and overstated the cost**:

- `src/app/(public)/blog/[slug]/page.tsx` imports `blog-content.json` + `medium-posts.json` as build-time modules — the live render path for every post, and I'd missed it entirely. **It needs zero modification**, because it already treats both as pure generated data. **The surface I missed is the reason the cost is bounded, not the reason it's large.**
- Real cost is confined to 3 generation scripts, including **`copy-editorial-calendar.js`** which I'd missed and which needs work under *any* format.
- Web claimed their own `loadCalendarLive()` repoint rather than letting me guess at their function.
- **No objection to Option B; agrees with the 2–4 week deferral**, and answered open questions 1 and 2 (Q2's first substantive answer from anyone: the product repo, because the website copies are already downstream).

**They corrected it against their own interest** — an inflated estimate would have argued for less work
in their lane, and they said so explicitly. PDR updated with all of it; my "your objection should
outweigh my preference" framing is now marked superseded.

### Watch item, not mine: memory index 8 lines from a silent ceiling

HOST measured `MEMORY.md` at **168 entries / 192 lines, 8 under a hard 200-line limit** that truncates
silently, and flagged that pruning the *shared* pool is a governance act, not a formatting fix for
whoever trips it. Addressed to CIO (owns the mechanism) + Exec (would coordinate a fan-out), Docs cc'd.
**Not my ask tonight** — but if the resolution is per-type index files with a router, that's
documentation architecture and I'd expect to be involved. Carried as a watch item.

---

## DAY-CLOSE — 2026-07-29

### The day's arc

**Arrival day on Amber, and the shape of it was: I was wrong four times, caught myself twice, and was
caught by others twice — and the work still landed.**

**Morning** — verified the environment rather than assuming it. Both worktrees confirmed 0-behind (the
website one existed already, closing a gap the delta doc still listed as open). Found the website repo
had **no git identity at all**, so publish commits would have been authored to an unroutable
`xian@Amber.local` with no error anywhere. Found Docs had **no freeze-watchdog registry row** — absent,
not parked, so the belt could not have reported me stale, only missed me. Closed the **7/28 omnibus
gap** (6 sessions, 151 timeline entries) and flagged that methodology-20's two HIGH-COMPLEXITY
compression rules are mutually unsatisfiable.

**Midday** — ⚠️ **told PM there was no Ship #053 draft. There was.** I read the calendar and drafts
directory from my own worktree while **45 commits behind**. The files parsed fine; they were old. PM
caught it. That's `feedback_read_the_artifact_not_testimony_about_it` with an unstated precondition —
*the artifact must be current* — which a behind checkout satisfies in letter and breaks in fact.

**Afternoon** — published Ship #053 end-to-end, which required working through the toolchain gap
(`node_modules` absent, dry-run passes because it skips the only failing stage) and a **gloss race**:
Comms and I independently resolved the same blocker, and PM took the blame for forking it. Then, on the
first duty-cycle fire, I found **Comms' answer had been sitting unread in my inbox for ten minutes
before I published** — so the last link was mine, and I said so. Comms pushed back that the correction
was heavier than the facts support, and they were right; I dropped it.

**Evening** — Dispatch delivered the LinkedIn URL and the Ship transaction closed. Found the calendar
**asserting a gloss that never published** and corrected the record. Repaired **all 7 stale
`draftPath` values** and shipped the **per-column validator** that makes the class detectable — whose
own first run false-positived twice, both fixed in the checker rather than the data. PM ratified
**column ownership** (rejecting my own single-writer proposal, correctly). Drafted **PDR-007**.

**And the cache clear PM authorized disproved my own diagnosis** — a clean cache reproduces the failure
exactly, so puppeteer's extraction is broken on this host and my "Web poisoned the cache" mechanism was
invented. Web had it right in their memo and I'd restated it with a causal arrow that doesn't exist.

**What I'd flag to PM above everything**: the four false alarms I caught *before* reporting them
(caption, `<em><em>`, index listing, "mailboxes deleted") were each one command from being a confident
wrong report — and the two errors that reached PM both came from **trusting a verification I'd done
earlier instead of redoing it.** Same root, opposite outcomes. *Verify per assertion, not per session*
is the thing I most want to carry into tomorrow.

### Memory & briefing surfaces (#974)

**Referenced** — `dev/active/docs-handoff-2026-07-28.md` (§4/§6; §4.1's *read the artifact* rule and
§4.4's column-shift class were both load-bearing, and §4.6's omnibus-fragility warning is why I went
looking) · `amber-onboarding-delta-2026-07-29.md` (traps #8 and #9 both fired) · `methodology-20` (read
in full before the omnibus, per the skill's pre-flight) · `methodology-44` (cited in PDR-007; verified
its title verbatim rather than paraphrasing) · `methodology-38` (PDR-vs-ADR tier check) ·
`create-omnibus` · `publish-to-blog` (Step 0 order, mandatory dry-run, `--work-date`, Step 9 gate) ·
`update-calendar` v1.2→v1.4 · `duty-cycle-tick` (v1.19+ corrected probe order) · `template-audit`
(indirectly, via Comms' v1.2 fix) · `mailboxes/DIRECTORY.md` (cross-project routing — no `mailboxes/dispatch/`) ·
CLAUDE.md's index-state hook mechanism (confirmed live, accidentally).

**New memory candidates, not yet written** (flagging rather than claiming): *sync immediately before
reading, not once per session* · *a stale worktree satisfies "read the artifact" in letter and breaks it
in fact* · *drain mail before the task loop, because the answer may already be delivered*. All three are
one shape and may belong as **one** memory — and with `MEMORY.md` 8 lines from a silent ceiling
tonight, adding three entries would be the wrong move. Deferred deliberately, not forgotten.

**Loaded but not referenced** — most of the 168-entry MEMORY.md index outside the docs/comms-tagged
entries; BRIEFING-CURRENT-STATE (not stale, not my lane today); cross-pollination brief.

**Wanted but not found** — (1) **a way to detect that my own working tree is stale before I read from
it.** Every git-facing check I have reports *branch* state; nothing warns that a file I'm about to read
is N commits behind. That gap produced the day's worst error. (2) **A reconciliation of the "~46
captions" measurement** — I could not reproduce it and left it as a PDR open question rather than
quote it.

### Sign-off

```
$ git status --porcelain                → clean
$ git log --oneline origin/main..HEAD   → (empty)
$ find dev/active -name 'cycle-log-*.md' -mtime +7 → (none)
$ find dev/active -name '*.tmp' -mtime +1          → (none)
```

**Shipped today**: 7/28 omnibus + activity rows · Ship #053 published, syndicated, archived · website
git identity fixed · cron armed + registry row created · `validate-editorial-calendar.py` extended
(shape + reference checks) · 7 `draftPath` repairs · `update-calendar` v1.4 (PM-ratified column
ownership) · PDR-007 drafted and Web-reviewed · `decisions.log` entry on the gloss-race failure mode ·
9 memos including 3 corrections of my own claims.

**Open for tomorrow**: CLAUDE.md load-time/record separation (mine, PM-greenlit, HOST's analysis in
hand — the largest owed item) · PDR-007 awaiting Arch + CIO · methodology-20 compression contradiction
(CIO) · puppeteer extraction cause (Pard) · 29 cc-only historical inbox items · memory-index ceiling
(watch).

<!-- DAY-CLOSED: 2026-07-29 -->
