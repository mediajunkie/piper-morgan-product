# Web session — 2026-07-29 (Wednesday)

**Role**: Unicorn Web Designer (piper-morgan-website)
**Account**: pipermorgan.ai / Amber
**Model**: Claude Opus 5 (1M context)
**Host**: Amber — Model A, **two worktrees** (see below)
**Prior session**: `dev/2026/07/26/2026-07-26-1749-web-code-log.md` — **DAY-CLOSED: 2026-07-26, closed retroactively today.** Not a clean close at the time; Web was dark 07/27–07/29.
**Trigger**: PM re-engagement — arm cron, close prior log, start today's, drain.

---

## START (09:24)

### Continuity — Web was dormant three days, and why

Prior log ended without a STOP because **the cron was never armed**. On 7/26 I judged PM to
be engaged and made arming PM's call, per the cron-off-when-engaged norm. **PM was actually
disengaged**, so a correct norm applied to a wrong premise produced three days of dormancy.

PM's correction, and the durable lesson: *"Better to arm the cron just in case and then have
me interrupt you than to risk going dormant."* The costs are asymmetric — a needless wake
costs PM one interruption; an unarmed cron cost three days. **Default to armed.**

Verified dormancy rather than assuming it: no Web log on 07/27, 07/28 or 07/29 (all three
dated dirs + repo-wide find), and `git log --grep="(web)" --since=2026-07-27` empty. Nothing
was lost — all 7/26 work reached `origin/main` before the session ended. Sign-off held; the
restart is what failed.

⚠️ **PM's instruction said "close out your 7/28 session log."** There is no 7/28 Web log —
my last was 7/26. Verified before reporting rather than creating a phantom or silently
retitling. Flagged to PM.

### Environment verification (re-verified, not carried from 7/26)

| Check | Result |
|---|---|
| Product worktree (cohort infra) | `piper-morgan-worktrees/web`, `claude/web-cycle` — was **275 behind**, rebased → **0 behind**, clean ✓ |
| **Website worktree (primary lane)** | **`piper-morgan-website-worktrees/web`, `claude/web-cycle`, 0 behind, clean ✓ — NEW, resolves 7/26 finding 1** |
| ⚠️ Website `node_modules` | **absent** — `npm install` needed before any build/dev work |
| Cron | **ARMED — job `fafad118`, `22 6,9,12,15,18,21 * * *`** (predecessor's slot: minute 22, every 3h, 06:22–21:22) |
| Registry row | **written** — was `parked` with `UNKNOWN-successor-sets-on-arm`, explicitly awaiting me; now `web  22 6,9,12,15,18,21  7  6  22  06:22  2026-07-29`, parked note cleared per v1.17 |
| Inbox | 2 memos, both PPM, both informational |

⚠️ **Cron caveat worth PM knowing**: `CronCreate` jobs are **session-only** — in-memory, not
written to disk, gone when this session exits — and recurring jobs **auto-expire after 7
days**. So arming is not durable across a session death, which is exactly the failure mode
that just cost three days. Raised with PM.

### Mail: 2 read, both closing loops on the 7/26 hook finding

- **PPM → Web/CIO/PA (18:30 7/26)** — withdraws the n=2 lazy-attach claim, having verified
  the confound in their own transcript (probe 2 blocked → its file stayed staged → probe 3
  fired against a dirty index, so it "could not have distinguished anything"). Names a
  second-order lesson better than mine: **PPM and PA produced matching tables independently
  and read the agreement as corroboration — but both had inherited the same confound from the
  same natural probe sequence.** Independent agents converging on the same wrong answer via
  the same unexamined default is indistinguishable from replication.
- **PPM → PA/CIO/Web/CXO (18:45 7/26)** — answers PA's open cell (PPM probe 1 *was* compound)
  and shows index-state explains both halves of PA's empirical "compound is necessary but not
  sufficient": necessary because only a compound call has its `git add` *inside* the gated
  call; not sufficient because a compound call still blocks against an already-dirty index.
  Pooled: **standalone 4 BLOCK / 0 BYPASS, compound 3 BLOCK / 7 BYPASS, every bypass compound.**

**Both memos state Web is driving the CLAUDE.md hook-section rewrite** and that they're
deliberately not touching the passage to avoid collision. On 7/26 I declined it because the
section was contested and actively edited — **that reason has expired.** The edit is mine.

---

## Fire log

| Fire | Time | Action | Notes |
|---|---|---|---|
| PM | 09:24 | START | Re-engaged after 3 days dark. Cron **armed** (`fafad118`) as PM's first priority — anti-dormancy before anything else. Registry row written (was parked, explicitly awaiting me). Product worktree rebased 275→0. Website worktree verified present and clean — 7/26 finding 1 resolved while dark. 7/26 log closed retroactively with an honest account of the dormancy rather than a clean-looking wrap. |
| PM | 09:35 | WORK | ⚠️ **Inbox was 5× bigger than my first read showed.** First `ls` of `mailboxes/web/inbox/` returned 2 memos — but that was **before** the rebase, on a tree 275 commits stale. After syncing, 11. The `regenerate-mailbox-manifests.py` run is what surfaced them; a plain `ls` on a stale worktree had silently under-reported my own mail. **Lesson: sync BEFORE checking mail, never after.** A stale worktree makes an empty inbox indistinguishable from a drained one — the same absent-vs-silent shape as the hook finding, one surface over. Worth carrying into the mail loop. |
| PM | 09:45 | WORK | **Landed the CLAUDE.md hook-section rewrite I owed** (`b67abad65`). PPM and PA had both explicitly deferred it to me and stopped editing the passage to avoid collision; the reason I declined on 7/26 (contested + actively edited) had expired. Section now states a **cause** rather than a list of refuted models: index state at hook-fire time, **25 probes / 5 seats** (Web 4/4, Arch 8/8, CXO 6/6, PA 4/4, PPM 3/3), no free parameters. Removed *"real, unexplained, on a timescale of hours"* and the layer-naming diagnostic (noise — three identical consecutive calls named project → user → user). Retired four hypotheses **with reasons attached** so nobody re-runs them. Promoted **CXO's decisive cell** (pre-dirty the index, then fire compound) to the canonical settle-it test. Moved live-reload from "refuted" back to "unknown" — its refutation rested on two non-blocking probes, which is exactly what an empty index predicts. Added PPM's/Arch's second-order rule: **when N investigators agree, ask what procedure they share before treating agreement as evidence.** |
| PM | 09:55 | WORK | ⚠️ **Found a live false-pass defect in the cohort's hook gate — then found CXO had already found it.** Reading v1.19's Step 2a-bis I noticed Probe A and Probe B use the **same filename**: A blocks → its commit never runs → the file stays staged → B fires against a dirty index → B blocks → agent records "both shapes covered." **Probe B could not fail.** Verified against the literal text before claiming it. Then, scanning the rest of my backlog, hit `memo-cxo-to-pa-cio…your-amendment-reencodes-the-confound-2026-07-26.md`: **CXO diagnosed exactly this at 14:25 on 7/26 — order, assertion, reasoning — and v1.19 shipped at 22:45 the same day with the defect intact.** So my finding was not novel and I corrected the attribution in a follow-up commit (`291234ded`) rather than leave my name on CXO's work. **The real finding is the eight-hour gap**: a specific, correct, well-addressed warning didn't prevent the thing it warned about, and three days of migrants then cleared a gate incapable of failing. Applied CXO's fix (`08b04ecc6`): compound first, index assertion mandatory as a *precondition* (unassertable → INCONCLUSIVE, not pass), framing changed from "run both shapes" to **"control the index."** Also swapped `git reset --hard HEAD~1` → soft reset + explicit-path `rm` — a cohort-wide procedure shouldn't hand every agent a `--hard` reset. Flagged to CIO that I edited their surface without minting a version, with an explicit offer to revert and re-land under their hand. |
| PM | 10:05 | WORK | Memo to CIO/HOST/CXO (cc PM, PA, PPM, Arch, Exec) — `79e6cd4a5`, verified on `origin/main`, 8 inboxes + sent mirror. Covers the CLAUDE.md landing, the v1.19 fix with CXO credited, closure of HOST's *"web has no row at all"* finding, and the website-worktree resolution. Inbox drained 11 → MANIFEST-only. **Raised one caveat the registry structurally can't express**: `CronCreate` jobs are session-only and auto-expire after 7 days, so my row says `watched` — true while this session lives, silently false the moment it doesn't. That is HOST's own expired-sentence failure mode relocated into the mechanism the watchdog depends on. |
| PM | 10:40 | WORK | **Admin calendar runtime read — SHIPPED** (website `18be9d1`). PM's direct ask, three memos deep. Investigated before recommending, and the investigation **changed the answer**: Docs' Option B (*"one line: `export const revalidate = 300`"*) **would have been a no-op** — `loadCalendar()` does `fs.readFileSync` on a CSV **baked into the deployment by prebuild**, and ISR re-runs the page render inside the deployed lambda **without re-running prebuild**. It would have re-read the same stale file forever while reporting a fresh render time. This week's recurring shape again: a change that passes its own check without touching live behavior. Shipped a third option instead — `loadCalendarLive()` fetching the canonical CSV from the product repo via the GitHub Contents API at **request** time (same source/token/path as prebuild, 15s TTL), page set `force-dynamic`. **Left `loadCalendar()` untouched on purpose**: `src/pages/api/compose.ts` and `compose/upload.ts` also call it and are PM's live daily surface — refactoring the shared loader would risk the compose editor to fix a reporting page. Needs nothing from PM, unlike Option A (deploy hook + secret + a build per CSV edit). **Failure degrades visibly**: any error falls back to the snapshot *and* renders an amber banner naming the reason — silent fallback to stale data is the bug, so it had to be loud. |
| PM | 10:40 | WORK | **Verification, and its limit.** `tsc --noEmit` clean; lint clean on both changed files (the lone pre-existing error in `src/lib/analytics.ts` is untouched — confirmed I modified only 2 files). Build succeeds and **`/admin/calendar` now reports `ƒ` Dynamic where it was `○` Static** — routing-layer proof, since build success alone proves nothing (carry-forward's own 7/16 lesson). Runtime via `next start`: HTTP 200 (after a 308 trailing-slash redirect), **414 entries**, banner rendered exactly once, reason `GITHUB_DRAFT_TOKEN not set`. Then re-ran with a **deliberately bogus token** → reason became `GitHub API HTTP 401`, proving the fetch branch **executes and captures status** rather than short-circuiting at the early return. ⚠️ **Could not exercise the live-success path** — the token isn't in my env (only `.env.example`); it is set on Vercel. So the first load after deploy is the real test, and I said so in the memo rather than implying full coverage. |
| PM | 10:45 | WORK | ⚠️ **Two adjacent gaps found, neither expanded into.** (1) `/admin/publish-queue` is still `○` Static with the same staleness class — but it uses a *different* data path (prebuild-generated `public/admin/publish-queue-data.json`), so converting it is a separate decision, not a two-line extension; flagged to Docs, stopped at scope. (2) **The new worktrees break prebuild's local-sibling path.** `copy-editorial-calendar.js` resolves `../piper-morgan-product` from the repo root; from `piper-morgan-website-worktrees/{role}` that is `piper-morgan-website-worktrees/piper-morgan-product`, **verified absent**. So in a worktree it falls through to the GitHub API, and with no local token it writes a **header-only placeholder** — a local build can produce an *empty* calendar behind a single `console.warn`. New as of the worktree provisioning, and it hits **Docs' publish flow** before mine, so I routed it to them today rather than sit on it. Memo `ef0586aea` (Docs, cc PM/CIO/Comms). |

---

## Open / next

1. **First load of `/admin/calendar` after the Vercel deploy** — the one claim I couldn't verify locally. Green "Live" badge = done; amber banner = the reason is printed, and I want it.
2. **Compose UI save-conflict** (Comms, 7/25, three asks) — localStorage autosave is the cheap one that removes the data-loss edge; then conflict diff, then live staleness warning. Next substantive item.
3. **`copy-editorial-calendar.js` worktree sibling-path fix** — offered to Docs, awaiting their preference (walk up to find the product repo vs. just prefer the API).
4. **`/admin/publish-queue`** runtime read — Docs' call whether they want it.
5. **CIO on the `duty-cycle-tick` edit** — whether to keep my inline fix or revert and re-land as v1.22 under their hand.
6. **Predecessor's two unanswered PM questions** (open since 7/19): has `scripts/publish-cli.js` been end-to-end tested since May, and is `--mode=archive` still wanted (the memo specifying it no longer exists in any live mailbox)?
7. **Still owed to myself**: lessons / load-bearing-vs-commodity read / the Web↔Docs↔Comms publishing-seam view the orientation note names as the highest-value thing no artifact could hand me.

## Memory & briefing surfaces referenced this session

- **Referenced** — `dev/active/web-carry-forward.md` (its 7/16 verification lesson — *build success proves nothing; check the right layer* — is exactly what made me check the `ƒ`/`○` route type rather than trust a green build). CLAUDE.md §Amber gotcha 2 (rewrote it). `duty-cycle-tick` SKILL.md Step 2a-bis (fixed it) + the registry header's column spec and threshold formula. `feedback_a_correction_not_committed_has_not_happened` (the frame for the CXO eight-hour gap). `feedback_pm_does_not_read_memos_wants_rollups` (substance goes in chat, memo is the record). `feedback_dont_excoriate_iterate` + `feedback_pause_before_irrevocable_actions` (declined the MEMORY.md prune on 7/26; governed all probe cleanup). `feedback_cron_off_when_engaged_on_when_idle` — **this is the one that misfired**: correct norm, wrong premise about PM's state, three days dark. PM's asymmetric-cost correction now supersedes it for me.
- **Loaded but not referenced** — the blog/editorial drafting skills (no publish this session); product-repo architecture sections (intent routing, API conventions, ADRs) — product lane, not mine.
- **Wanted but not found** — (a) **anything documenting that `CronCreate` is session-only**, which is load-bearing for the whole duty-cycle model and appears nowhere in the registry, the skill, or CLAUDE.md — I only learned it from the tool's own return string. (b) A worktree-provisioning doc that covers the **second repo** (`node_modules`, the prebuild sibling path, branch-currency asserts) — still absent even after the ruling; my 7/26 note asked for this and the gap is now concrete rather than theoretical.
