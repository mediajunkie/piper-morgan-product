# Lead Developer — session log 2026-07-29

- **Role**: Lead Developer (`lead-code`)
- **Model**: Opus 5 (1M context)
- **Host**: Amber (`Amber.local`) — first Lead session on Amber
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` on `claude/lead-cycle` (Model A)
- **Started**: 07:50 PDT

---

## Session start — Amber onboarding

Read in order per PM's instruction: `dev/active/lead-handoff-2026-07-21.md` (incl. the 7/26 REFRESH §4/§5/§6), `dev/active/lead-carry-forward.md`, `dev/active/amber-onboarding-delta-2026-07-29.md`.

### Environment verification (verified, not assumed)

| Claim | Result |
|---|---|
| Worktree path stable, Model A | ✅ `~/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle` |
| `git rev-list --count HEAD..origin/main` | ✅ **0** — not stale |
| Working tree clean | ✅ clean |
| Memory pool populated | ✅ **168** files (delta says ~170) |
| `gh` auth | ✅ `mediajunkie`, ssh, active |
| Heartbeat script present | ✅ `scripts/duty-cycle-heartbeat.sh` (executable) |
| Session cron | ❌ **none** (`CronList` → "No scheduled jobs") — expected; Gap-C |
| Registry row | `parked` — clearing condition is "a cron job is actually armed" |

### ⚠️ The Lead build stack is ABSENT on Amber — this is the material finding

My handoff's §5 asked whether the venv / Postgres / Redis discovery carries. Answer: **it does not, and the gap is wider than the question assumed.** All verified by direct probe, not inference:

- **No venv anywhere.** The handoff's path `~/Development/piper-morgan/piper-morgan-product/venv` does not exist — and neither does that *parent directory*; Amber's checkout is `~/Development/piper-morgan-product` (one level shallower than the laptop). No venv there either, none in the worktree.
- **Python is 3.14.6 (homebrew); the project pins 3.11.** `.python-version` = `3.11`, `requires-python = ">=3.11.0"`, and every CI workflow pins 3.11 (e2e-aaxt pins 3.12). A venv built on 3.14 would not reproduce CI.
- **No container runtime.** `docker`, `colima`, `orbstack` all absent — so `docker compose up -d` cannot bring up the dev stack.
- **Postgres 5433 closed, Redis 6379 closed.** Confirmed by port probe, not by absence of docker.
- **No `fly` / `flyctl`, no `~/.fly/bin`.** Beta deploys cannot be driven from this seat.

**Consequence for the lane**: the #1452 burn-down method depends on the local full-sweep instrument (standalone glance → fix → validate *in-sweep* → CI arbitrates), and beta work depends on `fly deploy`. Neither is executable here today. Per handoff §4.2 and lesson #2, I will not substitute a weaker instrument and call it equivalent — a local sweep I cannot run is not replaced by reading CI logs.

**What IS executable from this seat**: mail/triage, GitHub board + issue work (`gh` is live), doc/design-record work, backlog TSV bookkeeping, review of CI runs on the wire, and the Arch/Exec/PM coordination items below.

### Carried-forward items still standing (from carry-forward 7/23 + 7/25/7/26 notes)
- **Arch**: #1432 orphan-pair delete; ContextMatcher permissive unknown-trigger note. (methodology ruling — executed 7/26, confirmed received by Arch.)
- **Exec**: #1386 gate re-run window (beta v25+ carries both Scenario-B fixes).
- **PM**: #1424 close-vs-keep (lean: close); #1427 PROD-RECONNECT bucket confirm.
- **#1452 backlog at 94** (7/25); remaining composition is parked/gated — spatial-held, flaky oscillators, load tests (need a quiet box), ~45 small glances.
- **llm-lane gap** (BELIEVED, flagged 7/23, still standing): keyed lane accumulating tests with no scheduled executor.

### Notes
- `sync-pm-local.sh` hard-codes the laptop checkout and no-ops on Amber (PA's finding; CIO/Pard own the fix). Amber's main checkout `~/Development/piper-morgan-product` is on `main`, **1 behind** origin.
- 2a-bis hook probe (both command shapes) — pending, per onboarding delta §1.

## Hook probe — both shapes, first Lead-on-Amber datapoints (~07:55)

- **Compound** (add && commit, one call): **BYPASS** — probe commit `4b268c6ac` landed unchallenged; reversed with `git reset --hard HEAD~1`.
- **Standalone** (staged prior call, bare commit): **BLOCK** — refusal named `check-branch.sh` at the **absolute path (user layer)**; surfaced as `No stderr output` (= the hook working). Probe file unstaged + removed.
- Reading: modal Amber state — hook alive, doesn't cover the compound shape agents actually use. Cohort tallies: standalone 5/5 blocked, compound bypasses 7→8. Also resolves handoff §5 Q1: the permission classifier did NOT intercept here — 2a-bis is observable on Amber, unlike the old seat.

## Mailbox triage (~08:00)

7 unread → `read/` (watchdog-threshold thread ×6 + comms readiness; all cc/context, no open Lead ask — outcomes already codified in the onboarding delta). MANIFESTs regenned (`--role lead`). Two mail-sends landed (triage moves; read/ MANIFEST follow-up).

## Arrival report to CIO (~08:05)

`memo-lead-to-cio-cc-pm-lead-on-amber-arrival-report-env-verified-build-stack-absent-2026-07-29.md` → cio inbox, cc PM, sent mirror; pushed `01204bee9`. Headline: seat clean, probe matches modal state, **build stack absent** (no venv, python 3.14 vs 3.11 pin, no container runtime, 5433/6379 closed, no flyctl) → #1452 sweep instrument and beta deploys not executable from this seat; provisioning ownership question asked (Pard/host-level vs my in-repo parts). Cron deliberately NOT armed (PM engaged); registry row stays parked per its own clearing condition until armed.

---
