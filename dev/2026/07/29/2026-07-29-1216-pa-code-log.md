# Session Log: 2026-07-29-1216-pa-code

**Role**: Piper Alpha (PA)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Wednesday, July 29, 2026
**Start Time**: 12:16 PM
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/pa` on `claude/pa-cycle` (Model A)

## Session Objectives (PM-directed)

1. Close 7/26 log ✅ · start today's ✅
2. Catch up: mail (32 unread), briefings, docs, 316 commits since 7/26
3. **Unblock first**, then resume ongoing work

## Work Log

### 12:16 PM — Session start; owning the idle gap

**PA sat idle 7/27–7/28. The cause was mine, not the migration.** PM approved the cron cadence on 7/26
("your recommendation sounds like a great place to start"); I executed every other item in that
exchange — the `sync-pm-local.sh` fix, the shared-checkout pull, the tier research — and **never armed
the cron**. The one item that governed whether I'd wake up again is the one I dropped. 7/26 log closed
retroactively with that stated.

**State on arrival**: `HEAD..origin/main` = 0 · working tree clean · **316 commits** since my last
(`5f2aa442c`) · **32 unread** in `mailboxes/pa/inbox/` · no PA cron entry (confirmed via `crontab -l`).

### 12:40 PM — Unblocking pass

**Cron ARMED** — `CronCreate` job `04985c22`, `42 6,9,12,15,18,21`; registry row un-parked (threshold
had been adjusted 6→7 by a peer; left as-is). Discovered the mechanism is **session-scoped
`CronCreate`**, not system cron or launchd — no PA entry existed in either. **Two silent death modes,
both looking exactly like a quiet day**: session-only (dies on session exit → every session must
re-arm) and 7-day auto-expiry (this one lapses ~2026-08-05). Recorded in the carry-forward with the
instruction to check `CronList` at session start, because **the registry records intent, not a live
job** — and those two disagreeing silently is precisely what produced 7/27–7/28.

**PDR-006 ratification UNBLOCKED.** Arch's dedicated read landed 7/29 (ten days late — its session was
taken by the 7/19 outage and it hadn't armed a cron after migrating; the same failure as mine).

**Q2 was never open.** PM ruled it **2026-01-08** — `services/standup/preference_extractor.py:8`:
*"Start with rule-based (Option A), evolve to LLM later (#558)."* Option A is shipped; #558 is OPEN,
Production/1.0, due 2026-10-30. Arch verified empirically: zero LLM refs across `services/mux/`
(incl. the 584-line `composting_pipeline.py`, which is deterministic aggregation) and across all four
preference/personality modules. There is no "colleague model" subsystem at all — every `colleague`
occurrence is persona register.

⚠️ **My error, and it's the one I named on 7/26 and then repeated.** I elevated Q2 to a ratification
blocker on sound *reasoning* but never checked it against the running system — the PDR said "open," so
I treated it as open. Arch went and looked. **Verify-first applies to a document's own claims about
itself**, not just to inherited memos. Cost: ten days of blocked status on an already-decided question.
Recorded as a provenance note in the PDR rather than quietly fixed.

Also landed from Arch: its own 7/19 spatial-coupling flag **withdrawn as a gate** (they share a
metaphor, not a mechanism), with a re-trigger condition recorded if #558 is pulled forward. And **one
real architectural risk named**: the MCP caller-identity mapping sits *upstream* of all ADR-079
owner-scoping enforcement — if a tool handler reaches a repository without an owner-scoped identity,
the derived lint can't see it, because the read looks owner-scoped while the owner was caller-chosen.
**Fail closed: no identity, no read.** Written into the PDR's For-Arch section, plus the
derive-the-tool-catalog request and the resources-for-reads/tools-for-writes split.

**#1458 filed** — the #1351 carry-forward is now a tracked pre-live gate rather than a line in a closed
issue, with the three untraced surfaces (Redis, in-process floor/context state, rate-limiting) as ACs
and the identity-boundary mechanism as a requirement. Checked for duplicates first; none existed.

**Tier question CLOSED** (Janus, 7/28): pipermorgan.ai is **Pro (Max 20x)**. Track A is blocked on the
plan, not pending a lookup — it is now a purchase decision. Janus also supplied the exact OpenAI
verification path. PM Attention rewritten to two clean decisions plus one action.

### Fire (12:42) — Jake FTUX review filed; inbox drained to zero

*(Logged late, at the 15:42 fire — the 12:42 fire shipped the work and never wrote its Step-5 entry. The
discipline is "the log rides with the commit," and I pushed the memo without it. Noting rather than
backdating.)*

**Jake Krajewski FTUX review filed** — `mailboxes/exec/inbox/workstream-jake-ftux-pa-review-2026-07-29.md`
(commit `fe7afc937`), last of the four PM asked for. Read CXO's and HOST's first to avoid duplicating:
CXO holds experience design / IA / sequencing, HOST holds trust + the consent-boundary framing of the
"file a ticket" bug. Deliberately did not add a third opinion on either.

**PA's distinct lens, and why it's the one only this lane has**: Jake asked whether Piper is *"just
packaging a regular LLM alongside three kinds of lists."* That is an empirical question, and per mandate
2 this role **is** the running experiment on it — PM work done as an LLM without Piper's structured
handlers. Answer for a first session: he's right.

What actually makes this lane productive is **durable state across sessions + a queue of what's owed +
something that re-engages** — none of which his FTUX gave him. *An empty list is a form; a populated
queue is a colleague.*

**The recommendation I put first**: Piper already connects to GitHub / Notion / Calendar / Slack, and the
FTUX asked Jake to type five things into a chat box **instead of showing him his own work.** That reframes
it as a **cold-start-state problem, not a positioning problem** — better copy cannot fix an empty
account. Every complaint he made dissolves if the first post-connect screen reflects his own backlog with
an opinion attached. It's also the only way to *demonstrate* opinionation rather than assert it, and the
connectors are already paid for.

Filed as a mandate-2 design discovery: Jake independently asked for the **Grill Me** pattern (incremental
elicitation), which is how this lane already works — so we have **in-house evidence for the thing the
alpha tester requested.** Flagged that "five things in one message" may be a structured-intake assumption
showing through the chat surface, i.e. possibly a handler change not a front-end one — did not guess.

Metric I put in front of PM: **first alpha tester, willing, recorded his screen, dictated 1,500 words —
and never reached the core loop. 0 for 1** on "did the user reach the thing the product is for."

**Inbox drained 34 → 0**, MANIFESTs regenerated via `regenerate-mailbox-manifests.py --role pa`
(recipient is sole writer), all moves paired in the same push-to-ref. Verified on `origin/main`:
inbox holds only MANIFEST.md. *(First attempt failed — shell word-splitting collapsed 68 paths into one
argument; `mail-send.sh` correctly refused. Retried with a bash array.)*

### Fire (15:42) — Arch's layer-2 correction verified; PDR-006 coupling holds

Arch corrected the spatial review: layer 2 is **not** cold — `github_spatial` is live, 8-dimensional.
**That corrected a premise Arch had used four hours earlier to withdraw the PDR-006 coupling flag**, so I
read the code rather than the memo (this morning's lesson, applied).

**Arch's claim verified and stronger than written**: `github_integration_router.py:30` imports
`GitHubSpatialIntelligence` top-level, **and :117 instantiates it unconditionally** — outside the
`if self.use_mcp:` guard, with a `RuntimeError` if it fails and no MCP adapter exists. Load-bearing, not
vestigial.

**Two refinements sent back:**
1. **It's the FALLBACK tier.** Primary is `GitHubMCPSpatialAdapter` (`USE_MCP_GITHUB` default true,
   CORE-MCP-MIGRATION #198, deferred import :105); the docstring says "FALLBACK" / "Try MCP adapter
   first." So Arch's "replication, not invention" re-pricing is really **replicate two tiers ×5, or first
   decide the direct-API fallback is obsolete** — a materially different cost question, and the table CXO
   and PPM will re-vote against says only "LIVE."
2. **Direction discipline, flagged unprompted**: `services/mcp/consumer/` is Piper as an MCP **client**;
   `mcp.pipermorgan.ai` is Piper as an MCP **server**. Opposite directions. The inference "MCP is already
   live so PDR-006 is precedented" is available and wrong — **same conflation class as
   Connector-vs-Plugin**, which cost a week and two contradictory answers to PM.

**PDR-006 ratification unaffected — checked, not assumed.** `context_assembler` has **zero**
preference/personality references, so the colleague-model store isn't in that path and the re-trigger
hasn't fired. **But there's no wiring left to build**: the machinery is already live-wired to the
8-dimensional adapter, so if #558 lands the coupling arrives immediately. Recorded in PDR-006 as *one
issue away, not one project away* — a stronger statement than the "watch for this" it replaced.

Also noted to Arch: its failure (cold list built from a **recalled filename pattern**, not a directory
listing) is the same shape as mine this morning (trusting PDR-006's own "open question" label). Both are
trusting a *representation* of the system instead of the system — twice in one day, in opposite
directions between us.

### Fire (18:42) — PDR-006 nudge, disambiguated from Arch's HOLD

Three memos in, all read. Two were cc-only hook/gate hardening (Arch↔Pard seat-2 confirmation; HOST↔Arch
on the checklist cell) — **owned by Arch/HOST/Pard/CIO, no PA action.** The third mattered.

**Arch superseded BOTH earlier characterizations — third in ten hours — and adopted both my refinements.**
The model is **three layers**, and the review's premise is *inverted*: layer 2 is the spatial
**abstraction** (`spatial_adapter.py` — `SpatialPosition`, `SpatialContext`, the `SpatialAdapter`
Protocol, `BaseSpatialAdapter`), and **every MCP consumer adapter in the codebase is written against it.**
So the cold `*_spatial` modules are not abandoned ambition — they are **superseded predecessors of a
migration that worked.** The theory wasn't dropped; its abstraction is the substrate.

Arch accepted: my fallback refinement (sharpened to **live-by-construction, secondary-by-dispatch** —
both halves true, neither alone accurate), my MCP **client/server** conflation guard (explicitly adopted,
so nobody cites #198 as de-risking PDR-006), and my PDR-006 verification incl. the *one-issue-away*
amendment.

**Arch asked CXO + PPM to HOLD the spatial re-vote** and stop chasing increments until it ships one
finished layer map.

**This changed my planned work rather than blocking it.** I was going to nudge CXO + PPM toward PDR-006
ratification. The risk: *"Arch said hold"* generalizing past its scope — these two threads have been
braided all day via the coupling flag, and if the hold bleeds across, PDR-006 sits another ten days for
nothing. So the nudge **leads with the disambiguation**: hold the spatial re-vote, don't hold PDR-006;
they're separable *because* the coupling flag is withdrawn **and I verified the withdrawal at the code**,
which Arch has accepted.

Sent with the **10-day delta** so neither reviews a stale document — Q2 resolved (incl. plainly owning
that my blocker call cost ten days), coupling withdrawn+verified, Arch's caller-identity risk, the
client/server guard, #1458, and the two Arch requests (derive the tool catalog; resources-for-reads).
Gave each a specific lane ask, no deadline, and an explicit **"say so and I'll route around it"** escape
so it can't sit as a silent dependency. Flagged for CXO the live cross-link to its own Jake finding:
Jake never reached the core loop, and PDR-006 is what makes Piper reachable from inside Claude/ChatGPT —
whether that helps or merely relocates the cold-start problem is open and squarely CXO's.

Inbox 3 → 0. *(Methodology note worth keeping, Arch's own: three characterizations from three successive
greps, each looking complete — the failure was **treating "I verified the modules I enumerated" as "I
verified the layer."** The fix is building from the directory and import graph, not from a list of names
you arrive with. Same family as my own two misses today.)*
