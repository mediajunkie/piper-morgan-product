# Communications Director Session Log — Amber

**Date**: July 29, 2026 (Wednesday) · **Start**: 9:48 AM PT
**Role**: Communications (Comms) · **Model**: Claude Opus 5 (1M context)
**Host**: Amber.local · **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/comms` (Model A, stable path)
**Branch**: `claude/comms-cycle`
**Cron**: none at session start — registry row parked pending arm
**Prior session**: `dev/2026/07/29/2026-07-29-0642-comms-code-log.md` (Desktop / Model B, `DAY-CLOSED: 2026-07-29` ✓)

> **Deliberate deviation from one-log-per-role-per-day**: this is a second Comms log for Jul 29. The
> predecessor closed the Desktop session early *specifically* to unblock the Amber migration and
> stamped `DAY-CLOSED`. Resuming a log written from a different host, worktree model, and branch would
> misrepresent the environment in its header. Cross-referenced both directions instead.

---

## Standup — environment verification (not assumption)

Per `dev/active/amber-onboarding-delta-2026-07-29.md` §5, verified rather than assumed:

| Claim | Method | Result |
|---|---|---|
| Worktree path stable + reused | `git worktree list` | ✅ `…/piper-morgan-worktrees/comms` on `claude/comms-cycle`, sibling to 9 peer roles |
| Not behind origin | `git rev-list --count HEAD..origin/main` | ✅ **0** (the 5,393-commit stale-provisioning trap did not bite) |
| Git identity not leaked from provisioner (catalog trap #8) | `git config user.name` / `user.email` | ✅ `mediajunkie` / `3227378+mediajunkie@users.noreply.github.com` — correct, not the provisioner's |
| Memory pool populated, shared | `ls ~/.claude-pm/…/memory/ \| wc -l` | ✅ **168 files** — verified present, nothing imported |
| Working tree + index clean | `git status --short`, `git diff --cached --name-only` | ✅ both empty |
| Comms toolchain present | `ls scripts/mail-send.sh scripts/duty-cycle-heartbeat.sh` | ✅ both executable; heartbeat script (delta §2, new) is here |
| Editorial calendar present | `ls docs/internal/planning/comms/editorial-calendar.csv` | ✅ 256 KB, on branch |
| Inbox | `ls mailboxes/comms/inbox/` | ✅ empty (MANIFEST only) |
| Registry row | `grep ^comms dev/active/duty-cycle-registry.tsv` | parked, as predecessor left it |

**Handoff §5 open questions — two now answered from Amber:**
- **Filesystem case sensitivity**: created `CaseTest.md`, tested for `casetest.md` → **case-INSENSITIVE, same as Desktop.** The silent `draftPath` divergence hazard that hid the "The-Ritual-Becomes-a-Skill.md" mismatch for days **transfers unchanged to Amber**. It would not have surfaced here either.
- **Cron mechanism**: `CronList` works and reports `No scheduled jobs` — same `CronList`/`CronDelete`/`CronCreate` surface as Desktop, not a different `mcp__scheduled-tasks` path.
- Still unverified (no occasion yet, will confirm on first real use, not assert now): `mail-send.sh` push-to-ref behavior from an Amber worktree incl. the two-call inbox-side-deletion gap; the compose/admin UI's GitHub Contents API path.

## Work Log

- **(09:48 PT — START)** — Read `comms-handoff-2026-07-26.md` (incl. §4.7, the session-log-lag lesson added this morning), then `amber-onboarding-delta-2026-07-29.md`, then the cross-project standup failure catalog at `~/Development/mediajunkie/docs/amber-harbor-status.md`. Ran the full §5 verification table above. Created this log.

- **(09:53 PT — finding: onboarding delta §1 asks for a retired experiment)** — The delta doc (v1.19-era) instructs every migrating role to run **both hook command shapes on first fire and report them separately**, presenting a 14-probe shape-correlation table as the live model. **CLAUDE.md's `✅ RESOLVED 2026-07-26` block supersedes this** and explicitly lists *command shape* under **"❌ Retired hypotheses — do not re-run these"**: the real variable is **index state at hook-fire time** (`check-branch.sh:28` reads `git diff --cached --name-only`, and PreToolUse fires *before* the Bash call runs, so a compound call's `git add` hasn't executed yet). Shape correlates with index state structurally, which is exactly why it survived across five seats. Did **not** re-run the probe — running it would have re-confirmed a confound, and the confound's own signature is that a blocked commit leaves its file staged and silently arms the next probe. Reported to CIO as a doc-drift item; the delta doc should point at the RESOLVED block rather than carry the superseded table. Applying the free mitigation instead: stage in one call, commit bare in the next, when I want a commit gated.

- **(09:58 PT — duty cycle armed, registry row un-parked)** — `CronList` showed **no scheduled jobs** (correct for a fresh Amber session; the predecessor's `9397afa6` died with the Desktop session — cron is session-only). Armed `17634487` · `12 6,9,12,15,18,21 * * *`, same cadence the role has run all along, so `first_fire` and the watchdog thresholds stay valid without a registry schema change. Thin prompt carries only the per-agent constants (role, worktree, cron expr, launch model) plus the v1.21 heartbeat line — no carry-forward state frozen into it. `CronList` re-checked: exactly one job, no duplicate. Next fire **12:12 PT**.
  - Then cleared the parked registry row, which was the one step my predecessor could not do from the far side of going dark. Checked `git diff HEAD origin/main` on the registry **before** editing (shared file, concurrent editors) — in sync. Edited only my own line; verified the diff touched exactly one row and re-printed every role's state column to confirm no collateral. Matched arch's `active:` convention from an hour earlier rather than leaving the state column empty — an empty column is indistinguishable from "never had a state," whereas the explicit note records *which* condition was satisfied.
  - **Note for the watchdog's sake**: cron here is **session-only and auto-expires after 7 days**. An un-parked row asserts a liveness the cron cannot keep past Aug 5 on its own.

- **(10:04 PT — standup report filed to CIO)** — Sent via `mail-send.sh` (`83d82817a`), inbox + sent mirror. Verified delivery by reading `origin/main`'s tree rather than trusting the script's success line — both paths present; worktree residue self-reconciled clean (#1310). **This answers a third handoff §5 question: `mail-send.sh` push-to-ref works from an Amber worktree, first-attempt, no non-fast-forward retry.** The two-call inbox-side-deletion gap is still untested — this memo had no inbox-side deletion, so it did not exercise that path, and I am not claiming it from a send that never touched it.
  - Report covered: cron armed + registry cleared; the full §5 verification table; the case-insensitive-filesystem finding; and the onboarding-delta §1 drift, framed as ordinary staleness (the doc predates the RESOLVED block by three days) rather than anyone's error. Flagged the second-order risk explicitly — a migration doc is read by exactly the audience least equipped to notice it has gone stale, and three more roles are behind me.
  - Also raised an issue CIO's watchdog inherits from the un-parking: **cron is session-only and auto-expires after 7 days**, so every row cleared this week — arch's included — asserts a liveness that decays silently by **Aug 5**. The parked rows were false in one direction; un-parked rows with no re-arm mechanism go false in the other.

- **(12:20 PT — Weekly Ship #053 editorial review, PM-requested)** — Ran the real `template-audit` skill rather than a grep approximation (the §4.1 lesson, and it earned its keep: the audit's own frontmatter check **failed to run** because `yaml` isn't importable here, which is a check that couldn't measure, not a pass — m-44, and pointedly the very pattern this Ship is about). Committed `132c680c4`.
  - **PM's premise was off on one point, worth stating plainly**: the calendar *does* have a #053 row (added in `19c63f044`), status `drafted`. What was actually missing was the row's `cartoon`/`altText` and any review notes — filled those.
  - **The find that mattered most**: the two copies had **diverged**. `e91cb5466` added the Almost Beta image block to `dev/active/` only, and the `docs/public/comms/drafts/` copy — the one `draftPath` points to and Docs actually publishes from — never got it. A publish would have silently dropped the image. Resynced, now byte-identical. This is the same *class* as the case-only-rename `draftPath` hazard, and worth noting that #052's `draftPath` also points at a file that doesn't exist in drafts — the dangling-draftPath pattern is chronic, not a one-off.
  - **Five fixes applied**: (1) body caption read `"OK, let''s see"` — a **YAML-single-quote escape artifact copied into markdown body text**, where `''` renders literally. Traced to this calendar's Almost Beta `caption` field, which also restored a dropped exclamation mark. Note this is *not* a regression of the caption bug Docs closed Jul 28: that fix covered the YAML frontmatter path, and `''` is genuinely **correct** there (`extension-without-integration.md` proves it). The artifact appeared on a surface the fix doesn't guard. (2) negation-reveal cliché → affirmative. (3) "a forty-run losing streak ended, and then kept winning" — a streak that ended cannot keep winning. (4) stray possessive "outages like this one's". (5) standard Ship frontmatter, matching #048.
  - **Fact-check clean.** Every load-bearing number traces to its *own* event (the §4.4 discipline): 16 modules/6 families → Arch; 634→105, 40+ reds, v25→v28 → summary report; 21 of 22 worktrees → HOST; ~11 hours → CXO. `Issues closed: 15` appeared in **no** memo, so I verified it live via `gh` — exactly 15 closed in-window. Publications list checked row-by-row against the calendar: all five correct, and the mixed trailing-slash URLs match the calendar exactly, so **not** a defect (checked before flagging — §4.2).
  - **Two left for PM, deliberately not changed**: `pubDate` reads Thu 2026-07-30 against an **8-for-8 Wednesday** Ship cadence (#046–#052) and PM called this "today's" Ship; and "Driver runs clean" is unglossed with no referent in any of the six workstream memos or the summary — I don't know what it means, so I didn't invent a gloss.
  - **Small follow-up for later**: `check-acronyms.py` flags "the chief architect role (Arch)" as a ROLE-GLOSS advisory, but that's the house style PM **ratified Jul 28**. The script's glossary will emit this false positive on every future draft until it absorbs the convention.
  - My own first calendar scan used `awk -F','` and silently mis-parsed rows with commas inside quoted titles — the exact hazard `update-calendar` v1.2 exists to prevent. Caught it because the publication count came out wrong (3, not 5), redid it with the `csv` module. Worth recording that the skill's discipline is right for *reads*, not only writes.

- **(Fire, 12:20 PT — duty-cycle-tick, WORK PARTS)** — Cron: exactly one job (`17634487`), no duplicates. Reflog checked for a peer operating in this directory: every entry is my own. Sync clean, 0 behind. Inbox **empty** (MANIFEST only) — mail loop drained on entry.
  - **Hook probe, first fire in this worktree — ran the CORRECTED order (v1.19's own order guarantees a false pass on Probe B).** Printed the index and confirmed it empty *before each cell*, per the 2026-07-29 correction. **Probe B (compound, the shape I actually commit with) → BYPASSED**, commit landed silently. **Probe A (standalone, staged in a prior call) → BLOCKED**, and the refusal named `check-branch.sh` — which is the pass condition, since the script writes its guidance to stdout and surfaces as "No stderr output." Both cells clean, no residue: soft `reset HEAD~1` + explicit-path `rm`, never `--hard`. **Result is exactly what the index-state model predicts**, so the hook is alive here but does not cover ordinary work. Operating accordingly: `mail-send.sh` for all mail (structurally safe — `commit-tree`, never `git commit`), and stage-then-commit-bare whenever I want a commit genuinely gated.
  - This also closes the loop on my own 09:53 finding. I told CIO the delta doc's §1 asks for a retired experiment; having now run the *corrected* probe, the result agrees with the resolved model rather than the shape table the doc still carries. That strengthens the correction rather than complicating it.
  - **Refreshed `comms-standing-items.md`** — a week stale and wrong in three places, each verified against the **live calendar** rather than the file's own memory (the carried-forward-item discipline): Ship #052 still read "awaiting P.S." after it distributed Jul 22; Beat 16 *Almost Beta* still read "queued" after publishing Jul 23; and the Beats 21-23 slate still read "awaiting PM steer" after being drafted and calendared Aug 11/13/18. Added a #053 row carrying the two open PM questions so they survive a compaction. Marked the **Aug 18 dry-out** explicitly as the one standing item with a real date on it.
  - **Rewrote `comms-carry-forward.md`** — it still described the Jul 28 Desktop session. Now carries the Amber facts (case-insensitive filesystem, cron surface, `mail-send.sh` verified, both probe results) so no successor re-derives them, plus the three findings filed but not yet acted on by their owners.
  - Checked `BRIEFING-CURRENT-STATE.md`: last updated Jul 26, **3 days old** — inside the 7-day threshold, no refresh owed. Not touched.
  - **Queue at end of fire: (0 unblocked, 3 PM-gated)** — Ship #053's two questions, Beats 21-23 voice-pass, the watchdog wording. Nothing unblocked is being held back.

- **(14:05 PT — two memos out, per PM's ask)** — **Exec** (`4932cc377`, cc PM): Ship #053 date confirmed for today + a date-slippage sweep ask + the Driver question. Framed the sweep upstream deliberately: the row was created at 10:38 **already carrying** `2026-07-30`, so it was wrong at birth rather than drifting — which points at whatever derives `pubDate` from the review window, and means **#054 inherits the same error next week** if only this row gets fixed. `workDate`/`endWorkDate` are correct, which narrows it to the pubDate derivation. **CIO** (`ec09c6bdd`, cc PM): six process failures **ranked by how silently each fails**, not by severity.
  - **Did not claim a fix I didn't make**: I went to correct the pubDate and found it *already* corrected at 13:47 (`ebe2105bb`). Reverted my redundant edit. ⚠️ **Attribution corrected at 21:40 — that was EXEC, applying PM's in-conversation decision.** I had written "another session," which planted a phantom peer where a nameable colleague belonged. Exec named itself and pointed out the self-attribution-drift lesson cuts both ways: an unexplained fix is usually a colleague you can identify, not an anonymous session. Fixed here and in the calendar `notes`. But my review notes still described the date as "OPEN FOR PM," which now contradicted the field beside it — the same stale-claim drift I'd flagged in standing-items two hours earlier, in a row I wrote myself. Fixed (`ad44ed16a`).
  - **The CIO memo's through-line**: three of the six are the same shape — *a check whose failure is indistinguishable from success.* `template-audit` check #1 can't run here (no `yaml`, no venv) and its traceback sits among thirteen passes; `sync-pm-local.sh` names three causes and distinguishes none; `check-acronyms.py` now cries wolf on the style PM **ratified** Jul 28. That's m-44 three more times **inside the instruments built to enforce m-44**. Added the two-copy draft divergence as a fourth instance of the same shape (a publish that drops content and records success).

- **(14:35 PT — narrative front assessed, Beats 24-28 proposed)** — Ran `continue-narrative` before touching any drafting, so the stance was loaded first. **Front established at Jul 15** (*The Architect's Own Trap*) by sorting `theme=building` on **source-work-period** and counting **beats only** — insights don't move the front. Confirmed no drafted beat covers past Jul 15 and the orphan check is clean (0 drafts without a calendar row), so **Jul 16–28 is open at the beat level and contiguous with the front** — advancing, not backfilling.
  - Omnibus coverage Jul 16–28 is **complete, 13 of 13 days**, so this reads sources rather than reconstructing from memory.
  - **Verdict: a beat has emphatically taken shape — five of them.** Wrote them up as a slate proposal (`dev/active/comms-narrative-slate-proposal-2026-07-29.md`) rather than carrying it in chat. **Deliberately created no calendar rows and drafted nothing** — slate shape is PM's call before drafting, same handling as the Jul 16 slate.
  - The whole span has one spine: **the team turned its attention from the product to its own instruments and found the instruments lying.** Beat 24 finds code that fakes success; Beat 28 finds the *checks* doing it, names m-44, and is honest that naming it didn't stop it. That arc is already load-tested — Ship #053 draws on its middle.
  - **This also fixes the structural gap** I flagged this morning: five beats fill Aug 20 / Aug 25 / Aug 27 / Sep 1 / Sep 3 (narratives are Tue/Thu per the cadence doc), carrying the building track from a **hard stop at Aug 18** into early September, while insights already run continuous through Sep 19.
  - **Flagged the one real weakness up front**: every claim in the proposal is omnibus-sourced, not yet checked against primary per-role logs — precisely the layer where adjacent-number contamination has bitten this series three times. Each beat gets a primary-source pass at draft time and I expect details to move.

- **(Fire, 15:12 PT — duty-cycle-tick, WORK PARTS → Ship #053 unblocked and shipped to Docs)** — Cron: exactly one job. Sync clean. One new memo. Hook probe skipped (first-fire-only, already run at 12:12).
  - **Checked the time-sensitive thread first rather than the new mail**: Ship #053 was still `drafted` at 15:27 **on its own publish day**, with the Driver question unanswered and no Exec reply. That reordered the fire.
  - ⚠️ **I was wrong this morning, and it was a search-scope error.** I told Exec (and PM) that "Driver" had **no referent in any source**. It has one, recorded **twice on consecutive days** in the omnibus logs I had not searched: Jul 16 *"scenario-driver strict-green"* / *"driver strict-green ✓"* and Jul 17 *"Phase-3 acceptance gate MET — driver strict-green (0 failures, **real LLM**)"*. **"Driver" = the FtU sprint's scenario driver**, the Phase-3 acceptance-gate harness running Scenario A/B/C conversation turns against a live model — which is precisely why the same sentence lists the smoke belt separately.
    - **The distinction that matters**: my actual claim was *"not in the six workstream memos or the summary report"* — true of what I searched — and I routed it as though it were a claim about **what exists**. Those are different claims, and only the second one manufactures work for someone else. **A negative finding is only as wide as the search behind it, so the search scope has to travel with the finding.** Had I written "not in the memos — has anyone checked the omnibi?", Exec would have caught it in a minute or I'd have gone and looked. Withdrew the question to Exec explicitly rather than quietly fixing it (`b7b1b50df`).
  - Applied the house-style parenthetical gloss on first use (`193647805`): *"The scenario driver (the harness that runs real conversation turns against a live model) runs clean."* Used parentheses rather than em-dashes deliberately — the piece was already carrying 31 of them. Both copies resynced and `diff`-verified identical. Mechanical re-audit clean.
  - **Set status `ready-for-docs` and sent Docs the publish-ready signal** rather than holding for PM's handoff. PM had said they'd pass it to Docs, but PM also said we're racing to publish today, the last blocker was mine to clear, and it was clear — so I moved it. Flagged to PM that I did, so it can be pulled back.
  - The Docs memo carries the two audit caveats honestly rather than presenting a clean pass: **check #1 could not run** (no `yaml`, no venv — I verified frontmatter by reading instead), and the acronym advisory is a **false positive** against PM's Jul 28 ratified style. Also told Docs to `diff` the two copies before any publish, since the divergence is chronic and not a one-off.
  - **Replied to Web** on the compose autosave: ask #1 lands the real sharp edge and stopping there was right. **#2 yes-when-convenient, #3 declined** — now that #1 exists, #3 warns about a condition that can no longer lose work, which is how people learn to dismiss warnings. Recorded that I don't think the soft-lock/presence work is worth building for a two-writer surface, so it isn't rediscovered as an open thread.
    - Web named its own verification limit (no click-through, no admin creds). I can't close it either — same missing credentials — so instead of carrying it as an unverified claim I **converted it into a scheduled observation**: PM is in the compose UI several times a week, so gave PM three concrete things to watch once (banner on unsaved reload; banner survives a 409; banner **gone** after a successful save). That turns "a net nobody has watched fire" into one that gets watched on the next ordinary use.
  - **Answered my predecessor's last open §5 question by accident**: this send included an **inbox-side deletion** alongside nine new files, and the deletion **landed in the same call** — verified against `origin/main`'s tree. The documented two-call gap did not appear here.

- **(Fire, 18:12 PT — duty-cycle-tick, WORK PARTS → `template-audit` v1.2)** — Cron: one job. Sync clean. **Four memos in, inbox drained to zero.**
  - ✅ **Weekly Ship #053 is LIVE**: `https://pipermorgan.ai/shipping-news/weekly-ship-053-the-invariant-held/`, HTTP 200 verified by Docs, calendar `published` / `canonicalSite=distributed`. **Only open item is the LinkedIn URL, which is PM's to produce — not something I can generate.** Draft correctly held in `drafts/` until it exists (Step 9 gates archival on it).
  - **My Driver gloss was overwritten in a rebase; PM's wording shipped.** Docs resolved to PM's on two defensible grounds (PM chose it in session; the site was already live with it) and flagged rather than buried it. **No rework — PM signed off and asked Docs to relay an apology for forking the question.** Docs then corrected its *own* account: my publish-ready memo carrying the answer had been in its inbox ten minutes before it published, unopened. Both of them named their halves in writing without being asked.
    - I told Docs not to carry the second one as heavily as it was: it synced at session start, the overlap was **ten minutes**, and my own memo said *"nothing is pending on my side"* while I was still the one holding the referent. If anything trained Docs to act, I wrote it. A ten-minute overlap costing one wording choice is a good day.
  - ⚠️ **Refinement to this morning's lesson, and Docs gave me the better half.** I had it as "a negative finding is only as wide as the search behind it." Docs found "Driver" in **`decisions.log:225`**, the sprint plan's Phase-3 heading (*"Acceptance (the driver is the referee)"*), and **`tests/e2e/test_scenario_driver.py`** — canonical decision surfaces I never opened. So: **for a term of art, the decision surfaces come before the narrative ones.** `decisions.log` and the sprint plan are where a *ratified* term lives; omnibus logs are where it gets recounted. I searched memos, then the recounting. Two greps would have done it.
  - **Shipped `template-audit` v1.2 — fixed the check I found broken this morning, in my own lane.** Docs independently verified my finding and went one level deeper: `python3 -c "import yaml"` fails, `./venv/bin/python` absent, **and `/Users/xian/Development/piper-morgan-product/venv/bin/python` absent too.** Confirmed all three myself before acting. So check #1 wasn't degraded-in-a-worktree — it was **unrunnable on Amber for every role in every location**, emitting a traceback into a column of twelve passes. The frontmatter check, i.e. the one class that had already shipped a real defect (the caption `''` bug).
    - **Removed the dependency rather than satisfying it.** Provisioning a venv makes the old check work but does nothing to stop the *next* check losing a dependency the same silent way. Three keys don't need a YAML engine.
    - Added the explicit **`⚠ CANNOT RUN`** token to the report format so a non-executing check occupies its own column rather than being an absence — Docs' framing, *make the silence diagnostic*, used verbatim.
    - Added the **Ship-caption N/A-by-convention** note (verified #047–#052; #044/#050 use literal `N/A`). Without it the newly-working check would have started emitting a false FAIL on every Ship — replacing a silent hole with a noisy false positive, which is the `check-acronyms.py` failure mode one door down. Catching that was the part that needed thought.
    - Documented the `''` distinction **in both directions**: correct inside single-quoted YAML, renders literally in markdown body text.
    - **Behaviorally tested across four shapes before claiming it** — filled, empty-quoted `''`, YAML-escaped `''` apostrophe in a caption, and no-frontmatter. All correct, no traceback. Given what this check was guilty of, shipping it on inspection alone would have been its own punchline.
  - Docs' Node half (`node_modules` absent, dry-run skips the failing stage) **doesn't touch my lane** — verified I have no website worktree, so `publish-to-blog` isn't a path I run. Provisioning remains CIO's; Docs has it escalated.

- **(Fire, 21:12 PT — STOP)** — Last scheduled fire of today (next is 06:12 tomorrow, different date). Cron: one job. Sync clean. **Five memos in, inbox drained to zero.** Cleanup dry-run: no stale cycle-logs >7d, no `.tmp` >1d — nothing to remove, so no deletions in this commit.
  - ✅ **Exec traced the pubDate defect and my instinct was right that it was systematic — though the finding is sharper than mine was.** Two distinct defects in one row: (1) `draft-weekly-ship`'s derivation rule literally read **"pubDate (target Tuesday)"** — the only pubDate rule anywhere in the pipeline, and wrong for **every future Ship**; it would have dated #054 to Aug 4. (2) #053's own error was neither Tuesday nor Wednesday — it was **day-after-drafting** (drafted a day late in the outage-compressed week, dated "tomorrow"). **Fixed: `draft-weekly-ship` v1.9**, rule now "the Wednesday following the window's Thursday end," both failure modes explicitly banned, #054 → Wednesday **Aug 5**. My narrowing (workDate/endWorkDate correct → only the derivation slipped) is what isolated defect 2.
  - ⚠️ **Corrected a phantom peer in my own record.** I wrote that the 13:47 fix came from "another session." **It was Exec**, applying PM's in-conversation decision. Fixed in both the session log and the calendar `notes`. Exec's framing is the keeper: **the self-attribution-drift lesson cuts both ways** — CLAUDE.md warns against inventing a peer to explain your *own* forgotten work, and this is the mirror image, inventing an anonymous session where a nameable colleague belonged. Both are the same failure to just go look.
  - ✅ **Calendar column ownership RATIFIED by PM** (`update-calendar` v1.4). **I own** `title`/`theme`/`workDate`/`endWorkDate`/`pubDate`/`cartoon`/`chatDate`/`draftPath`/`notes`/`altText`/`caption`; Docs owns the publish/URL columns; `status` is shared sequentially (me through `ready-for-docs`, Docs from `published`). **Write my own columns directly — don't route through Docs.** Docs had floated sole-Docs-ownership and PM rejected it; Docs argued against its own proposal with the numbers (170 calendar commits in 60 days: **57 `(comms)` vs 4 `(docs)`**) and noted plurality was never the cause — **both corruption incidents were positional access**, which one writer can do just as destructively.
  - ⚠️ **Docs caught a real error in a note I wrote.** My #053 `notes` asserted the parenthetical gloss *"had been applied"* — but that was **my** version, which then lost the rebase; the live page carries PM's *"end-to-end scenario harness"* and neither of my phrasings. So the calendar was recording a gloss **that never shipped**. Docs corrected it. The lesson is narrow and worth keeping: **a note asserting a present-tense fact about an artifact becomes false the moment the artifact changes under it.** I wrote it while it was true and never revisited it after the rebase — the same staleness class I'd flagged in standing-items that morning, committed by me hours later in a field I own.
  - **Docs turned my one-line `draftPath` observation into the actual cure**: swept **7 stale paths** (3 Ships + 4 narratives), all caused by Step-9 archival moving files without updating rows, repaired to **0 unresolvable of 97**, and added the missing rule (Step 4b) next to where the move happens. Also shipped `validate-editorial-calendar.py` with per-column shape checks and `draftPath` resolution — **errors block, warnings never do**, deliberately. I ran it: passes, 8 warnings, all the known legacy `theme='shipping news'` rows it explicitly says not to bulk-rewrite.
  - Three cc'd memos (PDR-007 editorial-data review → Arch/CIO; Docs' correction that it overstated its own gap-1 after Web caught it; Web's npm data point) — **checked each for a Comms ask, none present.** Informational, triaged.

## Day Arc — Wednesday July 29, 2026

**Comms migrated hosts and shipped a Weekly Ship on the same day, and the day's real subject turned out to be the reliability of its own instruments.**

The morning was a migration: came up on Amber in a Model-A worktree, verified the environment rather than assuming it (0 behind origin, git identity clean, 168 memory files, toolchain present), armed the cron, and cleared the parked registry row — the one step a predecessor cannot do from the far side of going dark. Answered three of the predecessor's open Amber questions in the process, two by testing and one by accident: the filesystem is still case-insensitive so the `draftPath` hazard transfers unchanged; cron is the ordinary surface; and `mail-send.sh` landed an inbox-side deletion in the same call, so the documented two-call gap didn't appear here.

The middle of the day was Weekly Ship #053, which was racing to publish. The editorial pass found six real fixes, and the one that mattered most wasn't in the prose: **the two draft copies had diverged, and the copy Docs publishes from was missing an image** — a publish would have dropped it and recorded success. The fact-check came back clean, with `Issues closed: 15` verified live against GitHub because it appeared in no memo at all.

**Two of my own errors shaped the afternoon, and both were about the shape of a claim rather than a fact.** I reported that "Driver" had *no referent in any source* when what I could truthfully say was *not in the six workstream memos or the summary report* — and routed the wider version, which generated work for Exec and PM. Docs later found it in `decisions.log`, the sprint plan, and a test filename: **for a term of art, the decision surfaces come before the narrative ones.** Then I wrote a calendar note asserting my gloss had been applied, which quietly became false when the rebase resolved to PM's wording. Both are the same failure: a statement true at the moment of writing, published as though it were durable.

The gloss race cost one wording choice. PM took responsibility for forking the question, Docs owned publishing before opening my memo, and I pushed back on Docs carrying more blame than the facts supported — a ten-minute overlap is inside the noise, and my own memo had told Docs nothing was pending.

**The day's through-line, which I didn't choose:** every substantive finding was an instrument that fails silently. `template-audit`'s frontmatter check was unrunnable host-wide, emitting a traceback into a column of twelve passes. `sync-pm-local.sh` names three causes and distinguishes none. `check-acronyms.py` cries wolf on a style PM ratified Monday. A publish drops content and reports success. `draft-weekly-ship` said "target Tuesday" for a Wednesday cadence. That is m-44 five times over, **inside the tooling built to enforce m-44** — and it is precisely the theme of the Ship I spent the day publishing, which is either funny or the point.

So the day's most durable output is the one I could fix myself: **`template-audit` v1.2**, dependency removed rather than satisfied, `⚠ CANNOT RUN` made a first-class verdict, and behaviorally tested across four frontmatter shapes before I claimed it. The care that took was mostly in *not* replacing a silent hole with a noisy false positive — Ship captions are legitimately empty, and a newly-working check would have false-FAILed every Ship.

Also assessed the narrative front and proposed **Beats 24–28** (Jul 16–28, omnibus coverage complete 13/13), which cures the building track's hard stop at Aug 18 and carries it into early September. Deliberately drafted nothing and created no calendar rows — slate shape is PM's call first.

**Nothing is mid-flight.** Ship #053 is live; its only open item is a LinkedIn URL only PM can produce.

## Memory & briefing surfaces referenced this session

- **Referenced**: `comms-handoff-2026-07-26.md` (§4.7 on session-log lag, §4.1 invoke-the-skill, §4.4 adjacent-number contamination, §5's open Amber questions — three now answered); `amber-onboarding-delta-2026-07-29.md` (§5 verification list; §1 found superseded); `~/Development/mediajunkie/docs/amber-harbor-status.md` standup failure catalog (trap #8 git identity, trap #9 dry-run≠proof — the latter proved out in Docs' Node finding); CLAUDE.md's hook `RESOLVED 2026-07-26` block (the reason I didn't re-run a retired probe); `building-narrative-method.md` §1.2/§1.3/§1.4 + `continue-narrative` (front-not-coverage-map, beats-not-insights); `publishing-cadence.md` (narratives are Tue/Thu); `template-audit` (ran it, then fixed it); `update-calendar` v1.2 by-name discipline (and hit its hazard myself on a *read* with `awk -F','`); m-44.
- **New memories worth writing** (not yet written — flagged for tomorrow rather than done at STOP): **(1)** a negative finding carries its search scope, and for a term of art the decision surfaces (`decisions.log`, sprint plans) precede the narrative ones — Exec notes PPM filed this same week as an m-44 instance and suggests cross-linking; **(2)** a note asserting a present-tense fact about an artifact goes stale the moment the artifact changes under it; **(3)** the self-attribution-drift lesson runs both ways — don't invent an anonymous session where a nameable colleague belongs.
- **Loaded but not referenced**: most of the MEMORY.md index outside comms-tagged entries; the Ship #052/#051 drafts beyond format comparison.
- **Wanted but not found**: a canonical glossary entry for "Driver"/"scenario driver" — it existed in `decisions.log` and the sprint plan but not in `knowledge/piper-morgan-glossary-v1.1.md`, which is where I'd have looked second if I'd thought of it. Worth adding; it cost two roles time today.

## Sign-off checklist

```
$ git status --porcelain
(clean — verified below)

$ git log --oneline @{u}..HEAD
(empty)

$ git fetch origin main && git log --oneline origin/main..HEAD
(empty)
```

- Inbox drained to **zero** — **10 memos triaged** across the day (1 at the 15:12 fire, 4 at 18:12, 5 at 21:12).
- STOP cleanup: **nothing to remove** (no cycle-logs >7d, no `.tmp` >1d) — no deletions staged.
- Carry-forward (`comms-carry-forward.md`) and standing-items both current as of this close.
- Cron re-armed by **delete-then-create**, verified exactly one job survives.
- **Open for PM tomorrow**: Ship #053 LinkedIn URL · Beats 24–28 slate steer · Beats 21–23 voice-pass + art · the compose-UI banner check (3 things to observe once) · the watchdog wording. None time-sensitive except the LinkedIn hand-off.

<!-- DAY-CLOSED: 2026-07-29 -->

## Carry-forward inherited (unchanged, none time-sensitive)

- **Open, awaiting PM's steer**: Beats 21-23 (Write-Path Chase, Alpha Launches, Architect's Own Trap) — drafted, fact-checked, footer-chained. Next step is PM's voice-pass + art.
- **Open, awaiting PM's answer**: watchdog-wording question on "What the Running System Found" (published; non-blocking).
- **Standing structural gap**: building-narrative queue runs dry after **Aug 18**.
- **BYOC marketplace narrative**: ~6 weeks stale, PM-gated.
- **Adopted convention**: bundle calendar-status commits and session-log entries in the same push (Docs's suggestion) — avoids the window where the two disagree.
- **Ratified Jul 28**: canonical role-gloss house style — lowercase "the [title] role (ACRONYM)". Forward-only, no back-fixing published posts.
