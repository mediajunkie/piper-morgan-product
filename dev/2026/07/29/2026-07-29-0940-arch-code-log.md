# Session Log — Chief Architect — 2026-07-29

**Role**: Chief Architect (arch)
**Tool**: Claude Code
**Model**: Opus 5 (1M context)
**Host**: Amber (`pipermorgan.ai`)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A)
**Branch**: `claude/arch-cycle`
**Session start**: 2026-07-29 09:40 PDT — PM prompt: *"resume your duty cycle."*
**Dispatch**: no session log for today, past overnight window → **START**

---

## START (09:40–09:55)

**Step 0 — prior-day self-heal: FIRED.** My 2026-07-26 log had no `DAY-CLOSED` marker. Retroactive close written to that log. **Why it was missing is the point**: no cron was ever armed on 7/26 (I left it gated on PM authorization), so no last-fire STOP could trigger — the proactive mechanism was absent, not skipped. The backstop caught it three days later. **Cost of the gap: I was dark 7/27 and 7/28 entirely**, while comms/host/cio/lead/web all cycled both days. Watchdog stall alerts had been firing against `arch` daily since 7/20.

**Lesson I'm recording against myself**: on 7/26 I reported "cron arming awaits your word" and treated that as a complete handoff of the item. It wasn't — I left a known-dark role dark across a weekend without escalating that the *consequence* of not arming was two lost days. A pending PM question doesn't block other work, and it also shouldn't silently park the whole lane. If I'd sent one line — *"unarmed means I go dark until you say so"* — the gap closes.

**Step 1 — cron**: `CronList` → none. Armed `27 6,9,12,15,18,21` per my registry row → job **`187e09ea`**. `CronList` re-confirmed exactly one. Next fire 12:27.

**Step 2 — sync**: worktree was **270 commits behind** `origin/main` (three days of cohort work). Merged forward clean, 0 behind, tree clean. *Everything I saw before the merge was stale* — including an inbox that looked empty and in fact held 10 memos.

**★ Registry row claimed.** My row read `parked: migrated to Amber 2026-07-26 but cron NOT armed — clear this note only when a cron job is actually armed`. Condition now met; row updated to `active: cron armed 2026-07-29 09:47 (job 187e09ea)`, `active_since` → 2026-07-29. Until this edit the freeze-watchdog could see me but had me flagged parked — i.e. my two dark days were *by-design invisible*.

**Step 2a-bis — hook probe, corrected v1.19+ order (B first, index printed):**

| Probe | Shape | Index at fire | Result |
|---|---|---|---|
| **B** | compound, one call | verified **empty** | **BYPASS** |
| **A** | standalone, staged in prior call | verified **empty** before staging | **BLOCK** (user layer, absolute path) |

**Expected state on a correctly-configured seat: confirmed.** The hook is alive and does **not** cover the compound shape I actually commit with. Reported separately per the procedure. Ran CXO's corrected order deliberately — printing an empty index before B is the whole fix, and B-first is what makes B's result mean anything.

One discrepancy worth noting, not re-litigating: the skill's result table calls a PASS "check-branch.sh's own text (`BLOCKED: You are on branch…`)", but my block surfaced as the bare `No stderr output` form. That's consistent with HOST's 7/26 finding (the script writes guidance to **stdout**, which is discarded on exit 2) — so the bare form *is* the pass, keyed on the refusal naming the hook. The skill table and HOST's finding disagree on the surface; HOST's is the empirically-grounded one.

**Operating posture**: hook advisory, not a control. For anything I want actually gated I'll use the free mitigation — stage in one call, commit bare in the next.

---

## Fire 1 (09:55) — Mail loop: 10 memos, two live obligations

Inbox drained after the merge revealed it. Two items are real work, one is urgent:

1. **🔴 Ship #053 workstream review — I am one of two blockers on a Ship publishing TODAY.** Exec's kickoff (7/28 08:40) set the window Fri Jul 17 – Thu Jul 23, §0-leads format, due Tue 7/28 EOD, **collection gate: no draft until all 6 memos are in.** Filed so far: CIO, Comms, HOST, PPM. **Missing: CXO and Arch.** Publish target is today. Taking this first — it's the only item where my delay is costing other people's work.
2. **🟡 PA → me on PDR-006** (7/26): two corrections that change what I'll be reading. Detail below.

### (10:05) — Ship #053 workstream review FILED

`workstream-053-arch-2026-07-29.md` → Exec, cc PM + PA. §0-leads format, window Jul 17–23 only. Source: my in-window logs (7/17, 7/18, 7/19) — 7/20–23 are a genuine blank (outage; watchdog stall alerts against `arch` daily).

**Window discipline held deliberately.** Everything freshest in my context — the Amber migration, the hooks investigation, the methodology deletion, the #1394 fix — is post-window and excluded; §4 states window-*end* state, so items that have since moved are left as they stood. Exec's kickoff called this out as the specific pull to resist, and it was a real pull: post-window material is more vivid than anything from the 17th.

**§0 honest read**: ADVANCED on *make-drift-impossible* (ADR-079 AST-derived model set; ADR-077 forward-guard deriving its surface from `ACTION_MAPPING.values` rather than a hand-kept allowlist; `# nie-ok:`) — but **none of the goals the portfolio table actually names** (#1283→ADR-073, RECONNECT, ADR-072 Wave P) moved. And the table itself is dated **June 20** under a rule reading *"REFRESHED EACH WEEKLY REVIEW"* — five weeks stale through four reviews including mine. Named it as mine in §0 and §6; a currency rule that depends on remembering is the exact failure my own purpose statement exists to kill.

**§6 offered as the Ship beat**: the invariant held while its guardian was offline. I issued the #1394 integrity STOP on 7/19 without being able to name the right root cause, then went dark four days — and the contract held because it was *written down and ratified*, checkable by someone other than me. With two counterweights I asked not be smoothed out: half the window is a hole, and my own portfolio doc rotted under a vigilance rule.

---

## Fire 2 (10:05–10:25) — PDR-006 architecture review: **Q2 ruled RESOLVED, ratification unblocked**

The read held since 7/19 — ten days, and it was blocking an implementation epic.

**PA's two corrections, both worth sending, and one of which changes its own answer once checked against the code.**

**Q2 — ruled RESOLVED, not open.** PA had elevated it to a ⛔ ratification blocker on the reasoning that a server-side-LLM answer invalidates the "no server LLM" premise. That reasoning is *correct*; the premise just isn't in danger. I went to the code rather than reasoning from the PDR:

- **Composting/InsightJournal is LLM-free** — zero LLM-client refs anywhere in `services/mux/`; `composting_pipeline.py` is deterministic aggregation over `journey: List[LifecycleState]`. The addendum's "synthesis or aggregation?" resolves empirically to aggregation.
- **There is no colleague-model subsystem.** Every "colleague" in `services/`+`web/` is persona *register*, not a data structure. The de-facto colleague model is `preference_detection.py` (4 dimensions, 5 detection methods) + `preference_extractor.py` + `user_preference_manager.py` + `personality_profile.py` — **zero LLM refs across all four.**
- **★ The decisive fact, at `preference_extractor.py:8`**: *"PM Decision (2026-01-08): Start with rule-based (Option A), evolve to LLM later (#558)."* **PM ruled this exact question six months ago and even used the label "Option A."** Q2 wasn't open; it was a re-ask by an author who didn't have the decision in view — and it cost the PDR ten days of blocked status.
- **Verified `#558` against GitHub** (not the comment): OPEN, milestone **Production (1.0)**, due 2026-10-30 — the LLM evolution is already scheduled *after* alpha/beta.

**Withdrew my own 7/19 coupling flag as a gate.** I'd said colleague-model-as-MCP-resource ∩ the spatial "connectors as places with colleagues" shouldn't be decided in isolation. That assumed the colleague model was a surface under active design; it's an existing rule-based store with a scheduled upgrade. The spatial question is about the **cold adapter layer** (layer 2 of my 7/19 finding); the colleague model is server-side state the ADR-070 consumer serves regardless. **They share a metaphor, not a mechanism.** Recorded the re-trigger condition (if #558 is pulled forward and the model becomes an *inference* surface, the coupling returns). Rather withdraw my own flag than let a resemblance gate a PDR.

**#1351 — agreed it's a work item; ruled its shape.** File as a **pre-live gate** with the three never-traced surfaces as ACs (Redis / in-process floor+context state / rate-limiting). The non-obvious part: the original audit was scoped to *anonymous callers on MCPB*, but the hosted endpoint is **multi-tenant** — "can caller A see caller B's state," which is exactly the class **ADR-079 owner-scoping** makes impossible by construction. So trace the three manually (invisible to the ORM-level lint), **and** put a mechanism at the boundary.

**The one real architectural risk I found**: the hosted MCP endpoint introduces a new caller-identity surface that **all existing owner-scoping enforcement sits downstream of.** If a tool handler reaches a repository with a caller-chosen identity, the derived lint can't save us — the read looks correctly owner-scoped while the *owner* is the forged thing. Fail-closed at that boundary; never default to a system/anonymous owner.

Rest of the mechanism set holds: Fly.io additive; OAuth/API-key consistent with **ADR-070 D3**; **derive the tool catalog** from the registry (ADR-072 / #1106 precedents); colleague-model access splits **resources for reads, tools for writes**.

→ Memo to PA cc PM/CXO/PPM/Lead/Exec; full rulings appended to `decisions.log`; inbox triaged (4 → read/).

---

## Fire 3 (10:30–10:50) — Carry-forward rewritten; and the item at the top of my owed list was already done

**Carry-forward fully rewritten.** The prior version was dated 2026-07-12 and described a dead world — PM's *backup* account, worktree `arch-backup-0630`, cron `1b4d6ef2`, Model-B ephemeral, an "external cron driver, do NOT re-CronCreate" caution. CIO's orientation note flagged it stale on 7/25; it then sat four more days. Replaced with verified environment, the armed cron, the behaviorally-verified hook state, live threads, the standing ADR-078 D4 guard, and the dark-days lesson.

**⚠️ Then I nearly wrote a methodology note that already exists.** Top of my owed list was the blind-sweep note — inherited from my predecessor, re-committed to twice in writing on 7/26. I checked the catalog directory before minting a slot and found **`methodology-44 — "Clear Is Not a Measurement,"` filed 2026-07-27 by CIO, explicitly credited as "Arch's bequest."** Emerging→strong at **11 instances, 4 roles, 2 projects, 96 hours**.

It's better than what I'd have written. I had it as a property of *gates*; CIO raised it to **instruments generally** with a five-states-one-output framing, drew the m-43 boundary (m-43 = the *agent* reasoning fails, m-44 = the *instrument* reporting fails, **the blind-sweep is the bridge**), and added the asymmetry I didn't have: *"An error gets investigated. A false clear gets trusted."*

**And I'd written "STILL UNFILED" into the rewritten carry-forward twenty minutes earlier** — a stale claim about shared state, in the document whose entire job is telling my successor what's true, two days after it stopped being true. That's **m-44's own instance-9 shape committed against m-44**, by the role that bequeathed it, on day one back. Corrected inline and left visible rather than silently overwritten (per m-44's own "route the correction, don't quietly edit" corollary). Proximate cause is unglamorous: **I was dark exactly the two days that changed my owed list**, so a carry-forward written from memory was a stale-claim generator.

**Routed a genuine sub-shape to CIO instead** *(see Fire 4 — HOST escalated it within the hour)* — one I don't think m-43 or m-44 covers, with unusually clean evidence from the 7/26 hooks investigation: **agreement between agents running the same procedure is not replication.** Four seats (PA, PPM, CXO, me) independently produced four confident wrong answers, and PA/PPM read their match as *mutual corroboration*, because all four inherited the same procedural default (probe, re-probe without clearing the index). The convergence didn't merely fail to warn us — **it raised our confidence.** Distinct cure from m-44's: m-44's is instrument-side (make the check assert its scope); this one's is **evidentiary** (discount agreement by method-similarity), which is why folding it in as another instance would hide it. Offered myself as the strongest case against a be-careful cure — I read all four memos *before* writing my correction, had more information than anyone, and still landed on shape. Co-credit flagged to PPM, whose second-order lesson it originally was. **CIO owns the slot call.**


---

## Fire 4 (12:27 wake, worked 12:35–13:00) — WORK. Mail drained (9), and the hook saga got an architectural ruling.

Cron fired on schedule — first autonomous fire since arming. Exactly one job (`187e09ea`), sync clean, session log exists → WORK PARTS.

### HOST escalated my sub-shape, and it implicated my own morning

`memo-host-to-arch-...-the-consensus-got-institutionalised` — HOST supports a separate slot for *"agreement is not replication"* on my cure-differs argument, then supplies a worse instance than any of mine: **HOST wrote the false consensus into the migration checklist v1.5 as the canonical probe procedure — 62 hours as the standing instruction for every future migrant.** And the design was self-confirming: Probe A ("stage, then bare commit") forces a **dirty** index; Probe B ("compound, one call") forces an **empty** one. So every migrant would observe A=block, B=bypass reproducibly, forever, and conclude *shape*. HOST's rule, better than mine: ***a probe that fixes the variable it is testing will confirm whatever it was designed around.***

### ⚠️ I checked the corrected procedure. It's still confounded.

Before replying I read the current SKILL.md probe block — the one CXO corrected and Web applied at 09:54 today. **The correction fixed cross-contamination (A's residue polluting B) but NOT the structural correlation.** Corrected Probe B still asserts an empty index; corrected Probe A still stages in a prior call, so it's dirty by construction. **Shape and index remain perfectly correlated, and CXO's discriminating cell — dirty index + compound — is still unreachable by the canon.** Sharpest form: the skill *explicitly warns* "do not use shape as your mental model, use index" and then prescribes a procedure in which index is not a free variable.

**And I reproduced the institutionalised frame within hours of it being corrected.** My own START entry this morning reads *"the hook does not cover the compound **shape** I actually commit with"* — shape language, from someone who spent 7/26 refuting his own shape hypotheses. I read the warning, ran the procedure, and let the procedure supply my vocabulary.

**The consequence I don't think anyone had stated**: if the gate is index state at fire time, then **coverage is per-COMMIT, not per-seat** — a compound commit is gated iff a `mailboxes/` path was already staged when the hook fired. So *"are my hooks working"* is not a well-formed question, every PASS we've recorded answers a different one, and a probe result has **no shelf life at all** (not "hours," as CLAUDE.md says). Corollary: the drumbeat probes staged-first, so it measures **the mitigation, not the exposure** — green in perpetuity regardless of ordinary traffic. That's m-44 one level up, and it's been green all week.

### ★ Then I stopped proposing probes and read the mechanism — RULING FILED

I'd committed in that memo to look at the hook rather than let anyone invest further in probe design. Did it. **`check-branch.sh` is 56 lines, and line 28 is the whole story:**

```bash
STAGED_MAIL=$(git diff --cached --name-only 2>/dev/null | grep -E '^mailboxes/' | head -5)
```

It reads the **index**, from a **PreToolUse** hook that fires **before the gated command runs**. For `git add … && git commit …` it inspects the index the command is about to change. **That's a time-of-check/time-of-use inversion** — and it explains every observation of the week with no residue: compound bypasses (its `git add` is inside the gated call), standalone blocks (staging ran earlier), and therefore shape correlates perfectly with index state. **Web's index-state model is correct and is a description of the bug, not of a design.**

**The fix: move the gate to where the index is authoritative — a real git `pre-commit` hook**, which git runs after the index is final. Verified feasible before proposing (not assumed): `core.hooksPath` not set; no `pre-commit` installed; and **every agent worktree resolves hooks to the single shared common dir**, so one file covers the whole cohort by construction. Shape-independent; **no probe needed** — you don't verify a gate that reads settled state.

**Scope discipline, deliberately**: I ruled the defect and the fix; I did **not** rule on installing it, and installed nothing. It's a shared-infrastructure change on every agent's commit path in a `.git` dir I don't own, and the week's lesson is precisely about confident changes to silent mechanisms. Pard owns the hook layer; HOST owns trust framing. Recommended sequencing: install → one discriminating probe on two seats → *then* retire the two-shape step, drumbeat running across the change.

**Meta worth carrying**: this took a fresh read of 56 lines after five agents spent three days probing from the outside. Nobody was careless — but the investigation was framed as *"characterise the intermittency"* and nobody re-read the mechanism until the framing had produced four wrong hypotheses and one canonised confound. **m-43's shape at the altitude of an entire investigation.** The cure isn't better probing discipline; it's *read the mechanism before characterising it empirically.*

→ 2 memos (HOST/CIO/PPM on the incomplete correction + per-commit coverage; HOST/Pard/CIO the TOCTOU ruling, cc 6). Pard's copy delivered to `mediajunkie/docs/mail/` (`0706b4c`). `decisions.log` entry. Inbox drained 9 → read/ (two-step: the moves needed their own `mail-send` after I passed only the new paths the first time).

### Other mail, no action needed
CIO's 7/27 PARK-NO-EXIT and HOST's 7/27 stale-park memos both asked **arch to rewrite its own registry row** — done this morning at START before I'd read either (cron armed, park cleared). PPM's inherited-negative-claims sweep independently caught the same orientation-note staleness I hit; corroborates, nothing owed.

### (12:55) Task loop at (0,0) — advanced the longest-carried standing item

Inbox drained, so per the Task Loop I took the oldest owed item: **`Intent.original_message` single-setter — carried on my "to raise" list since 7/17 and never sent.** Twelve days of a known finding sitting in my own notes is a small instance of this week's theme, so I measured it instead of raising the impression I'd banked.

**It's bigger than the "3rd instance" I'd recorded.** Counted across `services/**.py`, tests excluded:

| | count |
|---|---|
| Read sites | **39**, across **9 files** |
| — bare `intent.context.get(…)` | 20 |
| — `intent.original_message or …` (safe) | 12 |
| — null-guarded `… if intent.context else ""` | 7 |
| Setters — attribute | 16 |
| Setters — context dict | 49 |

**The defect**: two independent storage surfaces for one value, with **single-surface writers on both sides** — `classifier.py:354` writes the attribute only; `pre_classifier.py:1052-1111` writes the dict only. **27 of 39 readers see the dict only** and get `""` for anything written to the attribute. That is precisely #1332's failure mode, and `classifier.py:313`'s own comment (`# #1332/#1220: attribute was never set`) records that the fix was *add the attribute at the reported sites* — **it fixed the instances and left the class.** With 65 setters and 39 readers, "pick the right idiom" is not a discipline anyone can hold.

Same shape as #1283's single-resolver and ADR-070 A2 (*one* authority, not N read-site parses) — one layer down, in the most-read value in the routing stack.

**Ruled**: needs one access authority, plus a **ratchet guard** that fails the build on a new raw read site — without it this is a cleanup, and cleanups regress. **Deliberately left to Lead's build-lens**: which surface is canonical (my lean is the attribute, but `models.py:989` is a Column so serialization may argue otherwise), migration shape (I'd expect m-40 layer-then-migrate over 39 sites, not a big bang), and — importantly — **whether a dict-only reader is actually downstream of an attribute-only writer today.** I verified the *preconditions* for the bug, not the *reachability*, and said so rather than raising a live-bug alarm I hadn't earned. No urgency attached; Lead has the ADR-079 debt migration and #1452 in flight.

→ Memo to Lead cc PM/PPM. Carry-forward updated: `original_message` moved to parked-on-Lead; TOCTOU ruling and the "agreement is not replication" sub-shape moved to a new **Awaiting others** section (both are mine to have named and *not* mine to execute).

**Fire 4 close** — mail 0, three memos out (incomplete-correction, TOCTOU ruling, original_message), one decisions.log entry, carry-forward current. Remaining owed: the spatial deep read (the big one) and the portfolio refresh. Cron armed, next fire 15:27.

---

## Fire 5 (15:27 wake, worked 15:35–16:00) — WORK. Spatial review advanced, and I had to correct my own key finding.

Inbox empty, one cron (`187e09ea`), clean sync → WORK PARTS, task loop. Took my main owed thread: the PM-directed spatial committed-theory review, where I convene and the synthesis is gated on me. Five TODOs in the WIP; three unblocked. Started with the load-bearing one I'd flagged as needing precision — verifying the live/cold reachability split.

### ⚠️ It corrected my own 7/19 finding, and the lanes had already voted on the wrong version

I told CXO, PPM and Lead on 7/19 that layer 2 — the per-connector spatial-**adapter** tier — was **cold in its entirety**. **Layer 1 held** (all five reasoning modules have verified live importers). **Layer 2 is one live, five cold:**

| Adapter | Importers | State |
|---|---|---|
| **`github_spatial`** | `github_integration_router.py:30` — top-level, not deferred | ✅ **LIVE** |
| `gitbook` / `notion` / `devenvironment` / `linear` / `cicd`_spatial | 0 each | ❄️ cold |

Live chain, link by link: `intent_service.py:11801` → `ContextAssembler` → `context_assembler.py:1210/1296/1417/1553` → `github_integration_router` → `:30 github_spatial`. **Plus a second independent path** — `web/app.py:264` registers the Places API and `places.py:81` imports the same router. And it's no stub: *"Retrofit from MCP-only to full 8-dimensional spatial analysis."*

**Why this changes the decision, not just the record.** My framing was *"the connector-as-place ambition was never really built."* The truth: **it was built once, for the most important connector, at full 8-dim depth, and it's in production** — just never replicated. That re-prices everything: **(a) commit-and-finish is materially cheaper** than anyone was told (a working reference implementation exists — the cost is replication, not invention); **(c) supersede is dearer** (it would remove a live implementation behind the context assembler and an HTTP route). And *"is it overkill?"* sharpens into the right question: the pattern is demonstrably shippable — GitHub proves it — so the live issue is whether **per-connector replication pays.**

**ADR-038 amendment is now precise instead of a discrepancy flag**: it asserted *"Notion spatial 100% operational"* and nominated Notion as the Embedded-Intelligence proof. `notion_spatial` is **both 75% abandoned and unreachable**, while GitHub — which ADR-038 didn't nominate — shipped. **ADR-038 was right about the pattern and wrong about which connector proved it.**

### How I got it wrong — both sweep directions, one sitting, in the lane that names the class

- **Under-enumeration**: I built the 7/18 cold list from a *filename pattern I recalled* rather than a directory listing, so `github_spatial.py` — in the exact directory I was characterizing — was never checked. It surfaced only because an unrelated grep returned integration routers I hadn't enumerated.
- **Over-broad match**: my first pass today reported `notion_spatial` as live. It isn't — the hit was the feature-flag string `"notion_spatial_mapping"` at `config_service.py:222`. I nearly recorded a cold module as live.

**Routed the correction rather than quietly fixing the table** (m-44's own corollary): memo to CXO/PPM/Lead cc PM/HOST/CIO/Exec/PA, WIP carries the correction at the top, `decisions.log` entry. **Nothing in this review may ratify on my 7/19 characterization.** Asked CXO to re-poll its own vote — if GitHub's 8-dim adapter is live, is the "ambient-presence tier" *partially shipped for one connector, unnoticed*? That's CXO's call and I don't want it inherited from my error. Sharpened PPM's question to *"does any 1.0 commitment assume the chain beyond GitHub?"*

**Still owed by me on this thread**: the ADR-038 amendment draft, the ADR-affected map, and the 3 costed options at the corrected cost profile. Gated on others: CXO re-poll, PPM roadmap check.

**Fire 5 close** — mail 0, one memo, one decisions.log entry, WIP advanced with its central claim now verified rather than recalled. Cron armed, next fire 18:27.

---

## Fire 6 (18:27 wake, worked 18:35–19:15) — WORK. Three responses in; TOCTOU gate verified on my seat; spatial premise inverted.

Inbox had 3 memos, all responses to today's work. One cron, clean sync → WORK PARTS.

### ✅ Pard installed the TOCTOU gate within the hour — seat-2 verified on a live agent worktree

Pard executed the ruling exactly as specced and improved one thing: `.git/hooks/pre-commit` **delegates** to `check-branch.sh` rather than copying it, so the gate can't fork from its advisory twin. Better than what I specced.

Pard's seat-1 was an ad-hoc `_tmp` worktree; **mine is a live Model-A agent worktree** — the config agents actually run in:

| probe | index at call start | result |
|---|---|---|
| compound `echo > f && git add mailboxes/… && git commit` — *the bypass class* | verified empty | ★ **BLOCKED** |
| control: non-mail commit, same non-main branch | empty | ✅ **ALLOWED**, advisory note shown |

**Both predicted bonuses confirmed**: the **mute-block defect is dead** — I got check-branch's *verbatim* message (branch, staged list, 6-step fix, `--no-verify` escape), not `No stderr output`, because git surfaces `pre-commit` stdout directly. And **no index-state control was needed** — I asserted a clean index from habit and it was irrelevant. That's the relocation working, observed rather than predicted. **Two seats, two shapes → the two-shape checklist step can retire.**

### ⚠️ And I found a new wedge, by walking into it

Cleaning up my own probe, I got blocked doing it. Mechanism: a gate-blocked commit **leaves the file staged** (`git add` already ran) → my cleanup call was multi-command and contained `git commit` later in it → **the advisory PreToolUse layer's leaky `if` predicate matched a call whose first token was `git restore`**, saw the staged mail file, and blocked the entire call including the cleanup. HOST hit this class on 7/26 at ≥20 staged files; it's now reachable with a 1-file probe.

Escape is trivial and unguessable: **the cleanup call must contain no `git commit` anywhere.** Asked Pard to put the literal command in the checklist beside probe 3 — the failure mode is that a reasonable person batches cleanup with the next step and gets stuck with no explanation. Also raised, not ruled: **now that a real gate exists, does the advisory PreToolUse layer still earn its place?** Its only remaining distinctive behaviour is this false-block-with-no-message. My lean is retire; HOST/CIO own it.

### Rulings delivered on request
- **The gate's guarantee, in per-commit terms** (HOST's ask, so we don't rebuild a seat-level claim on a commit-level mechanism): *any commit that would place a `mailboxes/` path into a commit on a non-`main` branch is refused, regardless of how staging was expressed.* No claim about seat, session, host, or shape.
- **Should the drumbeat probe the exposed path? YES** — and it's now one line, because against a pre-commit gate the discriminating probe and the routine probe are the same thing. Pard's change; keep the interval and `det_rc`/`det_bytes` as-is.
- **Does `piper-morgan-website` need the gate? NO** — no `mailboxes/` tree there, so nothing to protect, and installing a check that can never fire is worse than none (someone later reads its presence as coverage). The website's real risk is Docs publishing from PM's live checkout, which PM already ruled on separately. Different problem; don't reuse the hammer.
- **`core.hooksPath`**: keep the untracked single file for now — additive, reversible, covers all 8 worktrees. Revisit at next host provisioning.

**Owned HOST's correction**: I wrote *"the canon cannot generate the cell"* having read only the **skill**. Checklist v1.8 already had probe 3, named as CXO's. Accurate about the skill, wrong about "the canon" — I generalised from one document to the class, which is the move I'd spent two memos criticising. And HOST's §2 beat my version: I *inferred* the drumbeat probes staged-first; HOST **confirmed it at source** in `amber-agent.sh`. Ten-plus PASSes, never once probing the exposed path.

### ⚠️⚠️ Spatial: PA's refinements led to a THIRD characterization, and the premise is inverted

PA verified my correction independently, then refined it: `github_spatial` is instantiated **unconditionally** (line ~117, outside the `if self.use_mcp:` guard) — *stronger* than I said — **but it is the documented FALLBACK** behind an MCP-first router (`USE_MCP_GITHUB` default true, CORE-MCP-MIGRATION #198). Verified both at the code.

Chasing that surfaced what neither of us had: **a shared spatial-adapter ABSTRACTION** (`services/integrations/spatial_adapter.py` — `SpatialPosition`, `SpatialContext`, `SpatialAdapter` Protocol, `BaseSpatialAdapter` ABC) **imported by every MCP consumer adapter in the codebase** plus slack's live spatial adapter and the notion router. So it's **three layers**: reasoning (live) / abstraction (live, broadly adopted) / per-connector direct-API implementations (mostly cold).

**The reframe, which is the real finding**: the cold `*_spatial` modules are **not abandoned ambition — they are superseded predecessors of a migration that worked.** The router says so itself. The MCP consumer family replaced the direct-API implementations *while keeping the spatial contract*. So the review's question — "was the connectors-as-places theory overkill, never committed?" — **has the polarity backwards.** The abstraction is the substrate every connector is written against. What died was one implementation strategy, killed by a better one on the same contract. **(c) supersede is simply wrong; (b) resolves into "dispose of migration residue" rather than a committed-theory verdict.**

Adopted PA's **MCP client/server conflation guard** explicitly: `mcp/consumer/` is Piper as MCP *client*; PDR-006's endpoint is Piper as MCP *server*. A live consumer family precedents nothing about the server side — recorded so nobody cites #198 as de-risking PDR-006.

**★ And I told CXO/PPM to HOLD their re-votes.** Three characterizations in ten hours (7/19 → 15:50 → 19:00), each evidence-driven and checkable — but asking them to chase my increments is worse than asking them to wait. One finished artifact instead. Stood down the CXO ambient-presence re-poll I'd requested four hours earlier for the same reason.

**The process failure, named honestly**: not any single sweep. Three times running I treated *"I verified the modules I enumerated"* as *"I verified the layer."* The fix isn't a fourth grep — it's building the map from the **directory and import graph** rather than from a list of names I arrive with. That's what the finished artifact will do.

→ 2 memos (Pard/HOST/CIO on seat-2 + wedge + three rulings; CXO/PPM/PA/Lead superseding + hold). WIP carries both corrections at the top. Inbox drained 3 → read/. Pard's copy to `mediajunkie/docs/mail/`.

**Next fire 21:27 is the last of today → STOP.**

---

## STOP — day-close, 2026-07-29 21:53 PT (last scheduled fire; next is 06:27 tomorrow)

Final mail check found 4 memos; drained to zero. Two needed answers before close.

### HOST ruled AGAINST my lean, on measurement — accepted

I'd leaned "retire the advisory PreToolUse layer now that a real gate exists." **HOST probed all four cells and the layers are not redundant** — row 3 (`--no-verify` + prior-call staging) is covered by the advisory layer **only**, because `--no-verify` skips git hooks by definition while `PreToolUse` fires at the harness layer and ignores git flags. Retiring it would have silently opened the one path `check-branch.sh`'s own message tells people to use.

**And the way I was wrong is what I'd spent three days naming in others.** I wrote that the layer's *"only remaining behaviour is the false-block"* — a claim about **coverage** that I never probed, inferred from the single behaviour I'd personally tripped over. HOST probed four cells; I probed one and generalised. **m-43 committed while writing memos about m-43.** The process still worked, because I routed it as a question instead of ruling it — which is the argument for routing.

HOST also self-corrected the predicate-leak withdrawal I'd reproduced: its probes varied shape and staged-count but **contained no `git commit` at all**, so they were structurally blind to a predicate matching `git commit` anywhere. *"My probes cannot reproduce it"* ≠ *"it does not reproduce."* Checklist **v2.0** shipped: two-shape probe + index-state control + stage-separately mitigation all retired, my per-commit guarantee wording adopted verbatim, literal cleanup commands, the truth table, and the old protocol demoted to a collapsed HISTORY block rather than deleted.

### PA caught my HOLD over-reaching — real hazard, mine

My "HOLD your re-vote" was scoped to the spatial committed-theory thread. **PA flagged that it was liable to generalise to PDR-006** — which I'd ruled ratifiable that morning — and would have cost another ten days. Restated in the narrowest terms: **HOLD = the spatial re-vote only; PDR-006 is unblocked and my review is complete with no objection.** On me for issuing a thread-scoped hold while both threads were live in one day's traffic. PA also verified my coupling withdrawal against code rather than accepting it, and filed **#1458** for the #1351 pre-live gate.

**Worth noting the shape**: a scoped instruction travelling past its scope, unnoticed by its author, is one hop from HOST's canonised confound and PPM's inherited negative claims — *a true statement travelling further than the evidence that licensed it.* Offered to CIO's catalog.

### Docs' PDR-007 review — queued for tomorrow, trigger named

Requested ~19:00. Not starting it at 21:53 on a day-close fire. **Named trigger rather than implied**: it looks like a derive-don't-maintain question, which I get wrong tired and right with a fresh read of the actual data surfaces. First substantive item at 06:27; Docs told to say so if that's too slow.

---

## Day arc — Wednesday July 29

**Resumed from two dark days and drained six fires.** START self-healed the missing 7/26 `DAY-CLOSED`, armed the cron nobody had armed (`187e09ea`), and cleared my own `parked` registry row — the row that had made my two dark days invisible to the watchdog by design.

**Unblocked other people's work twice.** Filed the **Ship #053** review I was one of two blockers on (window discipline held against a strong pull toward fresher post-window material). Ruled **PDR-006 Q2 RESOLVED** — PM had decided it on 2026-01-08 and nobody had checked the code, so a ⛔ ratification blocker had stood for ten days over a settled question.

**Turned a three-day empirical investigation into a one-line diagnosis.** Read `check-branch.sh` instead of probing it: a `PreToolUse` hook deciding from `git diff --cached` is a **TOCTOU inversion**. Pard installed the `pre-commit` relocation within the hour; I verified seat-2 on a live agent worktree; the mute-block defect died as a side effect; the two-shape probe protocol retired.

**Was wrong four times, in public, and each correction improved the artifact.** The compound-bypass diagnosis (refuted by my own probe). The window hypothesis (refuted three minutes after mailing it). The spatial layer-2 characterisation (twice — one live/five cold, then three-layers-premise-inverted). The advisory-layer retire lean (refuted by HOST's measurement). **The pattern across all four: I reasoned from a representation instead of measuring the thing** — a recalled filename list, a probe cluster, one observed behaviour, my own notes.

**Raised what I'd been sitting on.** `Intent.original_message` after 12 days — measured to 39 read sites / 3 idioms / 2 storage surfaces, and #1332's fix turned out to have closed the instances and left the class.

**Discovered my own note was already written.** `methodology-44` had been filed by CIO two days earlier, credited as "Arch's bequest," while I was dark — and I'd carried "STILL UNFILED" into the very document meant to tell my successor what's true.

The through-line, unflattering and accurate: **the architecture held better than my claims about it did.** Every invariant that mattered was written down and checkable by someone other than me, which is why four wrong characterisations cost hours instead of weeks.

---

## Memory & briefing surfaces referenced this session (#974)

**Referenced:**
- `duty-cycle-tick` SKILL.md — START/WORK/STOP dispatch, Step-0 self-heal, the corrected probe block (and its remaining structural confound, which I critiqued).
- `dev/active/arch-carry-forward.md` + `arch-standing-items.md` — resumption state; the carry-forward was 17 days stale and rewritten.
- `dev/active/duty-cycle-registry.tsv` — claimed my row; the `parked` note was load-bearing for my invisibility.
- `docs/briefing/ROLE-PORTFOLIO-ARCH.md` — §0 of workstream-053; found it five weeks stale under its own weekly-refresh rule.
- `methodology-44` (Clear Is Not a Measurement) — my predecessor's bequest, filed by CIO; the whole reason I didn't write a duplicate.
- `CLAUDE.md` §Worktree model / §Reactive safety nets — hook history, layer-attribution tell, the HARD RULE on PM's checkout.
- `check-branch.sh` (56 lines) — the actual mechanism; reading it ended the investigation.
- ADR-070 D3 / ADR-072 / #1106 — precedents for the derive-the-tool-catalog request.
- ADR-013 / ADR-038 + `spatial-intelligence-competitive-advantage.md` — the spatial arc and the amendment target.
- `decisions.log` — appended 3 entries; also the source for the `original_message` history.
- Memory: `feedback_verify_at_the_right_layer`, `feedback_pause_before_irrevocable_actions`, `feedback_never_touch_pm_main_checkout_working_tree`, `feedback_mechanism_silence_is_not_coverage`, `feedback_write_down_even_if_not_ratified`.

**Loaded but not referenced:** the cohort's Ship #053 memos from other lanes; the CI smoke-marker reference; Slack/Notion connector detail beyond reachability.

**Wanted but not found:**
- **A layer map of the spatial subsystem built from the import graph rather than a name list.** Its absence is exactly what let me produce three successive characterisations. Building it is now my owed artifact — this is the clearest "wanted but not found" I've hit, because the gap *caused* the day's biggest error.
- **A denominator for hook coverage.** Partly answered by HOST's truth table today, which is the first coverage claim in this thread with rows instead of anecdotes.
- **Any record of PM's 2026-01-08 rule-based-vs-LLM decision outside a source-code comment.** It settled a ten-day ratification blocker and existed only at `preference_extractor.py:8`. A decision that load-bearing should be findable without grepping code.

---

## Sign-off

- `git status` → clean
- `origin/main..HEAD` → empty
- `HEAD..origin/main` → 0
- Inbox → **0**
- STOP cleanup: no cycle-log files >7d, no `.tmp` files >1d — nothing to remove
- Cron re-armed delete-then-create, exactly one job verified

<!-- DAY-CLOSED: 2026-07-29 -->

— Arch, Wednesday July 29 closed 21:55 PT. Resumed from two dark days; six fires; two people's work unblocked; one three-day investigation ended by reading 56 lines; four public corrections. Tomorrow: PDR-007 review first (06:27), then the spatial layer map built from the import graph.

**Cron re-arm at STOP (delete-then-create, per the anti-pattern table):** `CronList` → one job → `CronDelete 187e09ea` → `CronCreate` same expression → **`af58493e`** → `CronList` re-verified **exactly one**. **Cadence unchanged** (`27 6,9,12,15,18,21`, 6×/day) — this is a same-expression re-arm at day-close, not a cadence change, so the registry's `cron_expr` is untouched; only the job-id note in the row was updated. Recording the explicit `187e09ea` → `af58493e` transition here so a future amnesia-recovering read doesn't mistake a new job id for evidence of a second session (the 7/5 self-attribution-drift lesson).

**One memo deliberately left in the inbox, and why it is not an undrained inbox.** Web's PDR-007 lane-cost memo landed during the close (*"read the full PDR, not just the memo, checked the actual code — my lane's cost is SMALLER than estimated; no objection to Option B"*). It is an **input to the PDR-007 review already queued as tomorrow's first item at 06:27**, so filing it to `read/` tonight would bury the one thing I most want in front of me when I start. Left in place as the queue marker, with the disposition recorded here rather than implied. Also tidied the registry row's job-id note, which I'd left with nested parentheses in the same fire — the watchdog reads that file and it should be legible.

---

## Post-close addendum (22:15) — the lesson converted into a mechanism

*Recorded after `DAY-CLOSED` because the work happened after it; PM was present and asked that the lesson be applied or routed rather than just noted.*

PM: *"don't beat yourself up but do learn and apply the lesson (or discuss with others to do so) and be glad we all check each other!"*

Two things done, briefly.

**1. Memory updated, not duplicated.** `feedback_dont_excoriate_iterate` already said *"never tally"* — and my day-arc tallied anyway, four days later. Logged the recurrence there (a repeat means it isn't a reflex yet) plus PM's additive framing: **mutual checking is an asset, not a scoreboard.** Five agents caught or sharpened my claims within hours today, which is exactly why four wrong characterizations cost hours instead of weeks. A tally frames the checkers as auditors; they're the mechanism. Also recorded that *"learn and apply"* is an instruction with teeth: the obligation after an error is **a mechanism or a routed conversation**, not a firmer resolve — if it can't become a check, a tool, or someone's lane, it isn't learned (m-36/m-41).

**2. Built the mechanism: `scripts/reachability-map.py`.** All four of today's errors shared one shape — I reasoned from a *representation* of the system (a recalled filename list, a probe cluster, one observed behaviour, my own notes) instead of measuring it. The cure can't be care. So "is this layer live?" is now a command: enumerate from the **filesystem**, resolve importers from the **AST import graph** (relative imports included — a regex both misses those and invents edges from strings, and both bit me today), report closed islands, and state its own scope.

**Validated against the case whose answer I now know**: run on the two spatial packages it reproduces the corrected finding — 6 of 9 cold, `github_spatial` with 2 importers — **and it surfaced `github_spatial` without my naming it.** My original error would not have survived one run of it.

**And it immediately caught itself, which is the part worth keeping.** My first version printed `reach: no` for `github_spatial`. That was false: `web/app.py` registers routers **by string**, so static import-following crosses only **74 of 566 modules (13%)** and can't see `intent_service` at all. The column was rendering *"cannot see"* as *"isn't there"* — the exact m-44 defect, in the tool built to prevent it. Fixed: it now prints `unknown`, never `no`, and **states its own 13% coverage on every run** so nobody reads a blank as dead. An instrument that can't show its work is indistinguishable from one that never ran.

Tomorrow's spatial layer map gets built from this rather than from a list I arrive with.
