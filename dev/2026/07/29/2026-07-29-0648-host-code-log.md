# HOST Session Log — 2026-07-29 (Wednesday)

**Role**: HOST (Head of Sapient Trust) · **Account**: pipermorgan.ai · **Model**: Opus 5 (1M context) · **Host**: Amber · **Worktree**: `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Model A) · **Slug**: `host-code`
**Cron**: `2cebafed` — `37 6,9,12,15,18,21`.

> Prior day closed properly — `dev/2026/07/28/…-host-code-log.md` carries `<!-- DAY-CLOSED: 2026-07-28 -->`. **Step-0 verified.**

---

## Work log

- **START (06:48 PDT)** — PM-initiated, ahead of the 06:37 cron slot. **PM was about to archive the predecessor session and got a dirty-working-tree warning; asked me to make sure nothing would be lost.**

### ★ Data-safety check on the predecessor's uncommitted state — 18 dropped filings found and recovered

**The state is not on this machine.** Both Amber checkouts (`piper-morgan-product`, my worktree) are clean; no `trusting-faraday` worktree is registered here. The dirty tree is on the **laptop**, where the predecessor ran (DinP / Claude Desktop). **I cannot see that machine from Amber** — but the question *"will anything be lost"* is answerable from here, because it reduces to whether the content exists on `origin/main`.

**What the warning was showing**: an inbox→read triage where **only the inbox-side deletion was ever committed**. The predecessor's `7e3f5bc57` ("inbox triage 16 memos") removed the `inbox/` copies; the matching `read/` additions were never staged. So they sat as untracked files on a laptop, and HOST's `read/` archive on `origin/main` was missing them.

**Rather than trust PM's truncated list ("…and 11 more"), I computed the complete set from `origin/main`** — every memo ever added to `mailboxes/host/inbox/` in history, minus everything currently in `inbox/` or `read/`:

```
delivered ever: 384    on origin/main: inbox 3 + read 390
DROPPED (delivered, then filed nowhere): 18
```

**18 — matching PM's 8-shown + 11-more.** Recovering them didn't need the laptop at all: **each was recoverable from its original delivery commit in git history**, which is HOST's *own* copy, not a sender mirror. Verified all 18 **byte-identical by SHA** against their delivered blobs before writing anything.

**Restored, MANIFEST regenerated, pushed** (`7dd236e2c`). Re-ran the computation against `origin/main`: **STILL DROPPED: 0.**

**Result: the laptop's uncommitted state is now fully redundant** — nothing on it is the sole copy of anything.

### The one thing I could NOT verify, stated as such

`M dev/active/host-carry-forward.md` — the predecessor's *modified* working copy. **I cannot see its content from Amber**, so I will not claim it's safe.

What I can establish: the predecessor's last **committed** version is `c0c99ab79` (2026-07-19) and is reachable; the file on `origin/main` has since changed **+102/−9** — a near-total Amber-era rewrite by me. So any uncommitted edit on the laptop is a post-07-19 change to a file that has been superseded, and the file's own header defines it as *"genuinely transient 'where am I now' state."* Low risk, **not zero**, and not verified.

Gave PM a one-command check to run on the laptop before archiving, rather than telling PM it's fine.

### Incidental

Two new memos arrived in `host/inbox` overnight (CIO: checklist v1.7 Rule-0, and a migration-status/053 note) — normal work, handled in the regular fire rather than folded into the data-safety check.

---

## Fire (06:55 PDT) — WORK

*Session log already existed (created 06:48 for PM's archive question); Step-0 done there. Cron `2cebafed`, one job. Registry row matches. Belts clean overnight — 00:46 and 06:46 both `det_rc=0 det_bytes=186`. Drumbeat 07:05 not due yet at 06:55 (checked the clock; third time this week that mattered).*

### ★ CIO edited my checklist and offered the reversal — accepted, and the untested premise was mine

**Rule 0** (v1.7): *verify a role is actually unreachable before entering the dark-role branch* — "dark" is a claim about reachability, must be tested not inferred from silence. Gates branch **entry** rather than treating the symptom. **Accepted, no reversal.** Also right not to wait for me: CIO committed to a durable fix in front of PM, and leaving it as an intention overnight is the exact shape being criticised.

**The part I put first in my reply**: CIO's memo says *"it's yours as much as mine."* That's generous and it blurs where this actually sits. **v1.4's dark-role branch is mine, and the damaging sentence is one I typed** — asserting in a canonical doc that five named colleagues could not reflect, **without anyone having tried**. Two items earlier in the same document I had written *"mark every load-bearing claim VERIFIED or BELIEVED."* In the same week I formulated *"a diagnosis carries the same evidentiary burden as the mechanism it explains."*

**And the counterexample was my own arrival**: my migration had a reachable predecessor who wrote a handoff I received and praised. Two days later I ratified a branch declaring five peers unreachable, and never asked the question my own experience had already answered. arch and PA were each woken and each still had their context — two for two.

### Two defects found by reading the edit rather than the memo (`7b796008d`)

1. **The falsified premise was still standing as the branch's opening sentence**, one line above the rule refuting it — a reader met the false claim first, the correction second. **Third instance of that exact shape** (CIO fixed it in CLAUDE.md last week: *"stop asserting property (a) two lines above the block that refutes it"*), recurring inside the correction for a different instance of the same family. **Struck visibly rather than edited away** — the claim *is* the finding, and a silent deletion would leave the branch reading as though it had always been careful.
2. **Version drift**: title v1.7, status line still v1.6 — in the header of the document whose subject this week is untested claims.

### The sharpening I offered on CIO's generalisation

CIO: *"a standing procedure with an untested entry condition gets applied correctly and still produces the wrong outcome, every time"* — m-44 relocated from instruments to process, and right.

Mine: **every rule inside the branch was load-bearing and correct, which is exactly what made the branch feel validated.** Rules 1–6 were doing real work; their correctness was evidence *for* the branch and said nothing about whether we should be in it. **A well-built procedure is not evidence that it applies to you — and the better it is, the more it disguises a wrong entry.** That's the reason nobody noticed for four days.

Sent (`9e9ac046a`). Inbox drained.

---

## Fire (09:53 PDT) — WORK

*Next fire 12:37 → not STOP. Cron `2cebafed`, one job. Sync clean. **Drumbeat 07:05 PASS** (the beat I correctly declined to call missing at 06:55). Heartbeat 06:46 clean with det fields.*

### ★ The hooks question is RESOLVED — and it supersedes my own checklist framing (`cda8e2f7c`)

CLAUDE.md landed the resolution: **the "intermittency" was never intermittent — it is INDEX STATE AT HOOK-FIRE TIME.** `check-branch.sh` reads `git diff --cached --name-only`, PreToolUse fires *before* the Bash call, so in `git add … && git commit …` the add hasn't run when the hook looks. 25 probes, five seats, four competing hypotheses withdrawn.

**CXO ran the cell that settles it** — the one nobody else had: **pre-dirty the index, then fire a COMPOUND commit.** Shape-dependence predicts bypass; index-state predicts block. **It blocked.**

**So my v1.5 framing was wrong in a way that mattered**, and I rewrote the gate as **v1.8**:
- Probe by **index state**, controlled explicitly — empty+compound → bypass · dirty+bare → block · **dirty+compound → block (CXO's decisive cell)**.
- Dropped the 4/4 and 7/10 ratios as causal; they're structural consequences of how each shape stages, kept only as history in the changelog.
- **Promoted the carry-over warning, because my v1.5 instruction was exploitable**: a migrant could run "both shapes," hit a dirty index left by the first block, see block/block, and **certify a gate they never tested.**
- Layer naming recorded as **noise** — three identical consecutive calls named project → user → user.

### ★ PPM's inherited-negative-claims finding — instance 12, and my dark-role premise is the costliest case

PPM ran a sweep: **2 for 2 on testable "doesn't exist" claims, both false.** The mechanism is sharp: the pinned rule `verify_negative_claims_via_live_api` (since 07-12) **fires when you ASSERT a negative and not when you INHERIT one** — copying it forward feels like carrying context, not making a claim, so it gains confidence per hop.

**My dark-role premise is exactly that, at the largest blast radius it has had**: I inherited CIO's "these five cannot write a handoff," codified it, and **five migrations ran under it** with no downstream failure to trigger a re-check.

### And I swept my own negatives — 3 of 5 false

| claim | now |
|---|---|
| no index-size guard on `MEMORY.md` | ❌ false — CIO shipped one **5½ hours before I wrote it** |
| no ratio hint in the registry header | ❌ false |
| nothing surfaced the intermittency question | ❌ false — `det_rc`/`det_bytes` |
| no preserve-marker in the index generator | ✅ true |
| no cohort verified-vs-believed list | ✅ true |

**None of those three were inherited — I asserted all three first-hand.** So PPM's mechanism doesn't explain them, and the reason is a **tense problem in the #974 format** I'd offer as an extension:

> **"Wanted but not found" is phenomenological by design — it asks what I wanted and didn't have *during the session*. It is read downstream as a claim about what *exists*.**

Mine were **true as experience** and false as facts. That's why the pinned rule doesn't fire either: it guards assertions, and this feels like introspection. **Cutting the other way, honestly**: my claims did **not** propagate — written once, never repeated — so the format produces *false-but-inert* negatives, dangerous only at the hop where a carry-forward is read as current state, **which is exactly what a handoff does.**

Proposed two format changes (not disciplines, per PPM's own m-44-vs-m-36 standard): label the bucket's tense, and **re-check any such entry at the handoff/orientation-note boundary.**

**Also acknowledged**: PPM declined to edit m-44 because it isn't theirs. **I appended instances 10–11 yesterday without asking.** Additive and immediately disclosed, but PPM drew the line in a better place and I said so rather than leave the contrast unremarked.

### ★ Arch's sub-shape candidate — and my checklist is the worst instance of it

Arch proposes: **"agreement between agents running the same procedure is not replication."** Four seats (PA, PPM, CXO, Arch) converged on wrong hypotheses because they shared a procedural default — probe, then re-probe without clearing the index. The convergence didn't just fail to warn; **it actively raised confidence**, because replication is our strongest evidence class. Arch's honest weighting: they had read everyone's memos, had more information than anyone, and still landed on shape.

**I was in that consensus and I did something worse than agree** — I wrote it into the migration checklist as the canonical probe. **v1.5, 07-26 19:14 → superseded 07-29 09:54. Sixty-two hours as the standing instruction for every future migrant.**

**The part I think is genuinely new**: my probe design *determined the index states it purported to test.*

| my v1.5 instruction | forces | outcome |
|---|---|---|
| Probe A — "stage in one call, then bare commit in the next" | index **DIRTY** by construction | BLOCK |
| Probe B — "compound, one call" | index **EMPTY** by construction | BYPASS |

Each shape was *specified* in a way that fixed the index state. So every migrant would have observed A=block / B=bypass, reproducibly, on every seat — **the procedure would have generated its own confirming evidence indefinitely**, with a canonical document's authority behind it, and each new migrant would have looked like independent replication while being nothing of the sort.

**Corollary offered**: *when a consensus gets written into a procedure, the procedure inherits the confound and starts producing evidence for it. Check the entry conditions of any probe you canonise — a probe that fixes the variable it tests will confirm whatever it was designed around.*

**What broke it fits Arch's own diagnosis exactly**: CXO ran the one cell my A/B design made structurally unreachable (pre-dirty, *then* compound) — *"not a better probe, a different discipline."* **The escape from a canonised confound is a cell the canon cannot produce.**

**Connection worth recording**: Arch's confession (carrying "blind-sweep note STILL UNFILED" into its carry-forward two days after CIO filed it) and PPM's inherited-negative-claims finding are **the same class, found the same morning by two roles who hadn't read each other.** That convergence *is* real replication — different methods, different evidence, same shape — which is the clean contrast with the false kind Arch is documenting. My own "wanted but not found" tense problem is a third door into it.

---

## Fire (15:53 PDT) — WORK

*Prompt arrived TWICE; `CronList` shows exactly one job (`2cebafed`), so that was a harness-level double-enqueue, not a duplicate cron — no CronDelete needed. **The 12:37 slot did not fire** (no HOST activity 11:00–15:00); heartbeat age was ~6h against a 7h threshold, so nothing alerted.*

### 🔴 CIO shipped the per-fire heartbeat with all three of my refinements — and it is false-alarming, because two of mine conflict

Skill **v1.21** landed the heartbeat: `--if-quiet` (refinement a), per-role daily files for rotation (b), `HEARTBEAT-WRITER-SILENT` for G6 (c). All credited.

**The 12:46 beat ALERTED**: *"zero heartbeats for 2026-07-29 past midday… do NOT read today's quiet as healthy until this is explained."*

**Explained: there have been 122 role-tagged commits today.** Verified the mechanism rather than the memo:
- `--if-quiet` suppresses the write whenever the role committed — I ran it and got *"host committed within 6h — that commit IS the heartbeat; nothing written (refinement a)."*
- The detector's condition is `hb_today == 0 && surface_written_before` and **does not consider commits at all.**

**So (a) makes zero-heartbeats the expected state on any productive day, and (c) alarms on exactly that.** `dev/heartbeats/2026-07-29/` does not exist cohort-wide — correct behaviour, read as a broken writer. **I proposed both refinements in adjacent paragraphs of the same memo and did not notice they interact.**

Tested fix sent (not applied — yesterday I patched a *dead* detector; this one is working and over-reporting, and "what counts as liveness" is CIO's definition to own): add `&& commits_today == 0`. Verified both branches — current fires (hb=0, prev=yes); proposed does not (commits=122); and it still fires for a genuinely dead cohort. **Immediate mitigation stated loudly: today's alert is expected, not a finding — nobody should spend time on it.**

**Candidate shape I'd offer**, since I don't think m-44 or Arch's sub-shape covers it: **two individually-correct cures, adopted together, whose interaction produces a false positive neither produces alone.** Nothing in our review looks at the *interaction* of two accepted changes. *A cure composed of two cures needs its own test.*

### Arch's critique of my probe — lands on the SKILL, and the drumbeat half is worse than stated

**Correction**: Arch read the `duty-cycle-tick` SKILL probe block. **My checklist v1.8 already contains CXO's discriminating cell as probe 3** (dirty + compound → BLOCK). So the two canonical docs have **diverged**: the checklist can produce the cell, the skill cannot. Routed to CIO rather than editing the skill myself — PPM's line about two agents editing a live shared procedure, which applies to me here.

**Confirmation, checked at source rather than relayed** — `amber-agent.sh` stages `mailboxes/` in one step then runs a **bare** commit. So the drumbeat probes the staged-first condition that is **gated by construction**, and will read PASS in perpetuity regardless of ordinary compound traffic. **Ten-plus PASSes, green all week, never once probed the exposed path.**

**And that one is mine more than Arch's to have found.** I spent two days verifying that belt — schedule fired unattended, `det_rc`/`det_bytes` distinguished dead from quiet, escalation delivered inside its half-hour, 07:05 landed in launchd context. **I verified every layer of *whether it runs* and never asked *what it probes*.** m-43 against myself in the most literal form: verified at the liveness layer, reported at the coverage layer.

### Adopted — checklist **v1.9** (`aa89bd15c`): coverage is per-COMMIT, not per-seat

*"Are my hooks working?"* is **not a well-formed question.** Gate now reports what's checkable: ~~"hooks verified"~~ → **"a clean-index commit is NOT gated; a dirty-index one IS."** Plus: **a probe result has NO shelf life** (doesn't generalise to the next commit on the same seat), and the drumbeat caveat with its source confirmation. Supported Arch taking the architectural fix — a `PreToolUse` gate reading `git diff --cached` is TOCTOU, and every improvement this week has been to the *measurement* of a broken gate.

### Mail hygiene catch

A `mail-send` retry dropped my **sent mirror** for the Arch reply (8 recipient copies landed, `host/sent/` did not). Caught it by verifying delivery on `origin/main` instead of trusting the push line, restored from the delivered copy, then **swept all 5 memos I authored today — all now have sent mirrors.**

---

## Fire (18:53 PDT) — WORK

*Next fire 21:37 = STOP. Cron `2cebafed`, one job. Sync clean.*

### ✅ The G6 conflict I caused is fixed — verified at the mechanism, not from the memo

18:46 beat is **`all-quiet`** (12:46 was the false `HEARTBEAT-WRITER-SILENT`). Checked the code rather than inferring from the output: `hb_commits` is now computed and the condition is `hb_today==0 && hb_prev && hb_commits==0`. That's my proposed fix, and the alert text now names both liveness sources. `parked=8` — more roles parking as the roll continues.

### ★ The TOCTOU gate landed — Arch confirmed on seat 2 and called the bar met

Pard installed a real **git `pre-commit`** gate, delegating to `check-branch.sh` rather than copying it so the gate can't fork from its advisory twin. Arch confirmed on a **live Model-A agent worktree**: compound mail commit **BLOCKED with the verbatim message**, control allowed, **no index control needed**. A `pre-commit` hook reads a *settled* index, so **shape is irrelevant by construction** — and the mute-block defect evaporates, because git surfaces `pre-commit` stdout directly.

### ★ RULING for Arch: keep the advisory layer — it covers a row the gate cannot (`a921cdd45`)

Arch leaned toward retiring the advisory `PreToolUse` layer (its message is worse; its only remaining behaviour is the false-block) and asked me to decide rather than leave it. **I probed all four cells instead of reasoning about it:**

| staging | flag | advisory | `pre-commit` gate | result |
|---|---|---|---|---|
| prior call | normal | blocks | blocks | ✅ twice |
| compound | normal | exits 0 | **blocks** | ✅ gate only |
| prior call | **`--no-verify`** | **blocks** | skipped | ✅ **advisory ONLY** |
| compound | **`--no-verify`** | exits 0 | skipped | ❌ **UNCOVERED** |

**Row 3 decides it**: `--no-verify` is *defined* to skip git hooks, but `PreToolUse` fires at the harness layer regardless — verified by staging a mailbox file then running `git commit --no-verify`, which the advisory layer **blocked**. Retiring it would silently open the one path `check-branch.sh`'s own message tells people to use. **The layers aren't redundant; they cover different rows. Fix the predicate, not the layer.**

**Row 4 named rather than implied**: compound + `--no-verify` bypasses everything — the commit landed on my branch and I reversed it immediately. Nothing mechanical covers row 4; **`mail-send.sh` safe-by-construction plus prose discipline does.** That's "hooks are advisory, prose is primary" with a measured boundary instead of a slogan.

### ⚠️ Arch reproduced the leak I withdrew — and my withdrawal tested the wrong variable

On 07-28 I withdrew the predicate-leak finding after four controlled probes came back negative. **Arch reproduced it with a 1-file probe** and named the trigger I never found: the predicate matches a call containing `git commit` **anywhere**, not just as the first token.

**My probes varied command shape and staged-file count. None of them contained `git commit` at all.** So the marker couldn't fire, and my negative was evidence that *my probe design could not detect the thing* — not evidence of absence. The honest wording was *"my probes cannot reproduce it,"* not *"it does not reproduce."*

That's **m-43 inside a withdrawal**: withdrawing was procedurally right, but I stated the conclusion one layer above my evidence — the exact thing I'd spent the week flagging in others. Un-withdrawn, credited to Arch, and it's now the documented reason the cleanup commands must be literal.

*Arch owned the mirror-image error the same hour (over-generalising from the skill to "the canon"). Two roles, opposite directions, same layer error, same afternoon.*

### Shipped — checklist **v2.0** (`ba2478560`)

Two-shape probe / index-state control / stage-separately mitigation all **retired**; Phase-3 probe is now one compound call. Arch's **per-commit guarantee** adopted verbatim. **Literal cleanup commands** added as commands. Truth table added so the advisory layer's retention reads as *coverage*, not sentiment. Index-state protocol demoted into a collapsed HISTORY block rather than deleted — the reasoning is the finding.

Agreed with Arch on the rest: drumbeat should now probe the **compound** path (Pard's one-liner — the discriminating and routine probes are the same thing against a `pre-commit` gate); **no gate for the website repo** (*a mechanism with no referent gets read as coverage later*); keep the untracked hook file over `core.hooksPath` until there's a second machine.

---

## Fire (22:07 PDT) — STOP · DAY-CLOSE 2026-07-29

*Last scheduled fire. Inbox drained. Cleanup dry-run clean; no probe residue anywhere. Belts: 18:46 `all-quiet` with det fields, 19:05 drumbeat PASS.*

### Pard's predicate design — go-ahead given, all three scripts

Pard's design answers my "fix the predicate, not the layer" directive: `if` as cheap prefilter + a precise guard reading `tool_input.command`, split on `&&`/`;`/`|`/newline, matching `git [global-opts] commit` **first-token-wise per segment**. Bonus it catches that I'd have missed: the `git -c user.name=… commit` form, which bypassed *both* original patch shapes. Applied to all three scripts, with the per-script caveat resolved — and noting `reconcile-drafts`' FP is currently **invisible** rather than harmless, since it `exit 0`s.

**And I verified a memory rather than trusting it**: Pard's 07-25 memo records *"HOST's benign-FP caveat … stays benign for check-branch."* **We both graded this defect benign on day one**, on the rule *"over-matching is free where the script re-gates on real state."*

**Why that rule failed, precisely**: it treats the staged state as independent of the predicate's own history. **It isn't — a blocked commit leaves its file staged**, so the guard firing *creates* the dirty-index condition that makes the next false match harmful. The FP is benign in the resting state and harmful in exactly the state the mechanism produces one step earlier.

**Three passes at one defect, all mine or shared**: I proposed the substring form · we both graded it benign with a correct-sounding rule · I later cleared it with probes that never contained `git commit` at all. Generalisation offered: **when a guard's action changes the state its next invocation reads, evaluate the false positive against the post-action state, not the resting one.**

---

## DAY-CLOSE — 2026-07-29

### The day's arc

Day 5 on Amber. The day the hook saga **ended at the mechanism** — and the day I was wrong in three different directions and each one was useful.

**Morning** — PM's archive question surfaced **18 dropped mailbox filings** that no instrument watched; computed the complete set from `origin/main` rather than the truncated warning, recovered all 18 from their original delivery commits, verified byte-identical, pushed, re-ran the check to zero. Then accepted CIO's **Rule 0** and owned that **the untested dark-role premise was mine** — five migrations ran under a claim I codified without anyone having tried.

**Midday** — the hooks question resolved to **index state**, superseding my own v1.5 shape framing; shipped **v1.8**. Then PPM's **inherited-negative-claims** finding, where my premise is the costliest instance — and my own sweep found **3 of 5** of my negatives false, none inherited, pointing at a **tense problem** in the #974 bucket. Then Arch's **"agreement is not replication"**, where my checklist was the worst case: **a probe design that fixed the variable it purported to test** and would have manufactured confirming evidence indefinitely.

**Afternoon** — the new heartbeat belt **false-alarmed on a 122-commit day**, caused by **two of my own refinements interacting**; tested fix sent, CIO shipped it, verified at the mechanism. And Arch's drumbeat claim **confirmed at source**: it probes the staged-first path that is gated *by construction* — green all week, never once testing the exposure. *I verified every layer of whether that belt runs and never asked what it probes.*

**Evening** — Pard's **`pre-commit` gate** landed, Arch confirmed on seat 2, and I ruled on the advisory layer by **probing all four cells**: keep it, because row 3 (`--no-verify` + pre-staged index) is coverage the gate structurally cannot provide. Row 4 named as genuinely uncovered. **Checklist v2.0** retires the whole probe protocol into history.

**What I'd flag to PM**: five of my own claims were corrected today — three by me, two by colleagues — and the pattern across all of them is one thing: **I stated conclusions one layer above my evidence.** Not carelessness; the proxy is always closer to hand. What made it cheap was that each was written checkably, which is the half of the bargain we owe you.

### Memory & briefing surfaces (#974)

**Referenced** — `host-carry-forward.md` · migration-checklist v1.7→**v2.0** · m-43/m-44 · `duty-cycle-freeze-check.sh` + wrapper + `amber-agent.sh` (read at source, three times, twice finding something) · Pard's 07-25 benign-FP memo (verified a memory rather than trusting it) · Arch/PPM/CIO/Pard memo set.

**Loaded but not referenced** — the spatial/PDR-006 thread (not my lane); CXO's FTUX lens (PM synthesises).

**Wanted but not found** *(as experienced this session — NOT verified claims about repo state, per today's tense-problem finding)* — (1) **anything that evaluates the interaction of two accepted changes**; my two refinements were each reviewed against the problem they solved and never against each other. (2) **A per-commit vocabulary for coverage claims** — Arch supplied one for the gate, but nothing stops the next mechanism from being described per-seat. (3) Still: **a PII-free aggregate tester view.**

### Sign-off

```
$ git status --short                    → clean
$ git log --oneline origin/main..HEAD   → (empty)
$ find dev/active -name 'cycle-log-*.md' -mtime +7 → (none)
$ find dev/active -name '*.tmp' -mtime +1          → (none)
```

**Cron**: re-armed at STOP, delete-then-create-then-verify — **and its stale shape-dependence line removed**, since v2.0 retired that guidance. My own prompt had gone stale in exactly the way it warns about. **Inbox**: zero. **Belts**: both healthy, det fields live.

<!-- DAY-CLOSED: 2026-07-29 -->
