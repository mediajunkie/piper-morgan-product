# Lead Developer — Session Log 2026-07-06

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B). Sole lead. Carry-forward: `dev/active/lead-carry-forward.md`.
**Mode**: PM-initiated Monday morning session, resuming after 7/5's session was interrupted mid-task (see closing entry appended to `dev/2026/07/05/2026-07-05-0801-lead-code-log.md`).

## START (~06:22)

- PM opened by circling back to yesterday's PIPER.user.md discussion with a sharp catch: "this didn't smell right" — correctly identified that the PIPER.md/PIPER.user.md pattern is single-user-prototype-era and hasn't evolved for multi-tenancy, AND that `alpha.pipermorgan.ai` is a real, live, currently-running shared instance — not a hypothetical future concern. Asked directly: should we loop in Chief Architect?
- Then gave the standard session-start instructions: close 7/5 log, start 7/6 log, check mail, resume duty cycle, continue Beta Blockers.
- **Closed 7/5 honestly**: appended the un-logged #358/ADR-074 work (dimension A/B verification, the stale-comment contradiction resolved via direct code-check, ADR-074 written) and marked `<!-- SESSION-INTERRUPTED: 2026-07-05, resumed 2026-07-06 -->` — not a false DAY-CLOSED, since ADR-074 was written but not committed and #358 wasn't yet closed.

## Session objectives (from PM, this morning)

1. Take PM's live-shared-instance catch seriously — this is a real, present gap on a real, running alpha deployment, not a future hypothetical. Confirm/verify precisely, then answer PM's direct question (loop in Arch?) with a real opinion, not just agreement.
2. Check mail, resume normal operation.
3. Continue Beta Blockers — finish #358's closure (interrupted mid-task), then continue Epic B (#1305/#1306/#542) and Epic C.

## #1366 filed — PIPER.user.md's single-instance/unscoped design, confirmed live on alpha (~06:30)

Mail: empty. Duty-cycle registry re-checked: `lead` still not a registered row — same reading as before, "resume your duty cycle" is plain-English, not a literal skill invocation.

Took PM's catch seriously and verified precisely before responding, rather than just agree. Traced `piper_config_loader.get_system_prompt()`'s actual call site (`conversational_floor.py::_get_system_prompt()`) — confirmed it's called with **zero arguments**, no `user_id`, nothing. The module-level global `piper_config_loader` singleton reads one fixed file path, full stop. Also confirmed `load_github_config()` (the GitHub default-repo setting) has the identical unscoped pattern.

**This makes it concrete, not just "weird personalization"**: on a shared instance, every tester's Piper conversation gets PM's actual personal system-prompt context AND PM's GitHub default-repo setting — meaning a tester's GitHub-related actions could default toward PM's real repo. Confirmed via issue search this is a genuinely new gap (nothing existing tracks it) — ADR-071 covers *database* content stores; this is a *filesystem config* layer ADR-071 never touched.

Filed **#1366** with full evidence (exact call sites, why ADR-071 didn't cover this, 3 candidate architectural shapes named without prescribing one). Sent Arch a direct memo (cc PM/PPM) — answered PM's "should we loop in Arch" question with an unambiguous yes in the memo itself, not just a verbal agreement: this is cross-cutting, architectural, and live on a real shared instance, not something to improvise a patch for myself.

## #358 closed out; #542 fully implemented and closed (~06:35-06:45)

Updated #358's description with a precise status banner (both encryption dimensions code-complete + tested; dimension B live-verified on alpha 6/25; dimension A's live-alpha check is the one remaining item, needs droplet access) rather than mechanically tick 30+ stale checkboxes referencing columns that no longer exist in the real schema. Left it open — one real, named gap remains.

**#542** (token revocation on disconnect): read the actual current code before touching anything — Slack's `revoke_workspace_access()` was a documented stub (comment literally said "This would typically: 1. Call Slack's auth.revoke API" — never did); Calendar's disconnect had no revocation call anywhere. Implemented both for real: Slack now calls `auth.revoke` for bot_token and user_token independently; Calendar's new `GoogleCalendarOAuthHandler.revoke_token()` makes a real call to Google's revoke endpoint.

**Caught a real sequencing bug while implementing, not just theorizing**: `_disconnect_slack` deleted the keychain tokens BEFORE calling revoke — but revoke resolves tokens via `SlackConfigService.get_config()`, which reads those exact same keychain entries. Verified this precisely (read `_load_config()`'s actual keychain key names, confirmed exact match) before concluding revocation would always have found empty tokens post-deletion, silently never revoking anything. Reordered: revoke first, clear locally after (matching the existing best-effort principle already used for github/notion in this file). Also renamed `revoke_workspace_access`'s misleading `workspace_id` parameter (sourced from an env var with no relationship to which user was disconnecting) to `user_id` (the actually-correct scoping key).

4 test files touched/added; 161 tests pass across the full affected surface (connectors, calendar, slack settings routes). Closed #542 with full evidence.

## Arch ruled on #1366 — cleaner than I expected, and I owe Component A now (~06:47)

Arch verified my finding independently (didn't rule from my writeup alone) and **reframed it**: this decomposes into two genuinely different components, not one build.

**Component A (GitHub default-repo leak)** — NOT greenfield. There's already a correct, user-scoped mechanism (`ConnectorConfigService.get_default_repo(owner_id)`, shipped via RECONNECT #1327) that the real request path already uses correctly. The leak is specifically **coexisting unscoped callers that bypass it**: `pm_number_manager.py` (8 sites), `canonical_handlers.py`, `intent_service.py`, plus a stale `UserPreferenceManager` still instantiated. Arch's ruling: this is a migration-completion in the make-drift-impossible family (repoint the unscoped readers, retire the legacy path, add an enforcement lint), bounded, severable, **doesn't gate on any ADR — start now**.

**Component B (system-prompt personalization)** — genuinely new, no existing scoped home, live on every request. Arch is authoring **ADR-075** (next free number; 073/074 taken) with a CXO/HOST trust-lens, extending ADR-071's `owner_id`/`is_global_pm_domain` pattern to configuration. I build against it once ratified — not before.

**Verified Arch's specific caveat before starting anything**: Arch asked me to confirm the caller inventory against the *deployed* alpha SHA, not just origin/main, since alpha deploys from `origin/production` (a separate branch that can lag main). SSH'd into the alpha droplet (read-only: `docker exec piper-app grep -m1 '^version' /app/pyproject.toml`) — confirmed deployed version `0.8.9.2`, tags commit `255c27cfd`. Checked all of Arch's named call sites against that exact commit (not current HEAD) — all confirmed present (line numbers shifted slightly due to normal commit drift, same underlying finding). Sent Arch a precise confirmation before starting Component A's implementation.

## Component A implemented — 2 of 4 named sites were genuine, verified before fixing (~06:50-07:15)

Rather than mechanically repoint all 4 sites Arch named, read each one directly first (the same discipline that surfaced the #1366 mechanism correction to CIO yesterday, and the #1241 stale-issue finding before that). Found a real, precise split:

- **Genuine**: `canonical_handlers.py::_get_priority_metadata` (`github_config.default_repository`) and `intent_service.py::_handle_create_issue` (same field, plus a separate `default_labels` read). Both fixed — repointed onto `get_user_default_repo(user_id)` (promoted `repo_resolver.py`'s `_read_user_default_repo_from_db` to a public function for this reuse).
- **Not genuine**: `pm_number_manager.py`'s 8 sites read `pm_prefix`/`pm_start`/`pm_padding` (numbering-format prefs), never `.default_repository`/`.owner`. The "stale `UserPreferenceManager`" call sites (`canonical_handlers.py:1383`, `preference_handler.py:50`) are for calendar-offer-state and personality-preference detection respectively — neither touches GitHub config, and `UserPreferenceManager.get_default_repo()` itself already correctly delegates to `ConnectorConfigService` (fixed 2026-06-21, #1226/#1199, predating this investigation). Couldn't find the "models.py:602 supersession note" Arch's ruling cited — likely a citation slip, doesn't change the finding.
- Full codebase sweep for `.default_repository`/`.owner` (not just Arch's named files) confirmed no other callers exist — the 2-site fix is complete, not partial.

Wrote 2 new regression tests (one per fixed call site) and confirmed BOTH fail against pre-fix code before accepting them as real coverage (not tautologies that'd pass regardless). Added `TestGitHubDefaultRepoScopingEnforcement` (tests/test_architecture_enforcement.py) — same family as #1283/#1307 — matching the established `TestPreFloorDispatchSiteRatchet` shape/conventions in that file. Arch's follow-up memo (read mid-work, after a merge pulled it in) proactively flagged scoping precision I hadn't yet covered: `.owner` alongside `.default_repository`, and two exemptions to verify (`load_pm_identity_config()`/#1260 Component C, and non-repo `load_github_config()` fields). Checked both: `.owner` has no current reader (forward guard only); Component C is structurally separate (different method, never produces a `github_config` variable, can't collide); `pm_number_manager.py` passes today by virtue of the regex being field-scoped, not file-scoped. Broadened the lint regex and shipped as a second commit.

**Discovered incidentally, filed**: #1367 — `test_execution_intent_no_placeholder` is a pre-existing stale-mock test failure (mocks `.classify`, code calls `.classify_multiple`), confirmed via stash-and-rerun to fail identically without any of my changes — predates this session, unrelated.

**Test evidence**: 234 passed across the full affected surface (architecture enforcement, canonical_handlers, repo_resolver ×2, execution_analysis_handlers) + the 1 pre-existing #1367 failure. Both new regression tests and the new lint independently confirmed to fail against pre-fix code.

**Housekeeping caught mid-task**: found 2 mailbox inbox→read moves from earlier this session that were never pushed via mail-send.sh (the inbox-side deletion for the arch-ruling memo, and both sides for the CIO guardrail-ack memo) — sent via mail-send.sh before continuing. Also found `data/github_preferences.json` untracked in the working tree — the legacy flat-file GitHub-prefs store `ConnectorConfigService`'s own docstring says is already superseded (#1226/#1199) — gitignored it (runtime artifact, not source).

**Git housekeeping**: push hit the documented SSH-port-22-blocked issue; used the CLAUDE.md port-443 workaround successfully. Branch had diverged 14-16 commits from origin/main; a pre-existing, unrelated CRLF/LF renormalization churn on `skunkworks/klatch/klatch data model.csv` (confirmed whitespace-only, last real edit March) kept blocking `pull --rebase` even after stash/checkout — used `merge origin/main` instead (tolerates dirty-but-untouched files) rather than fighting the renormalization further; left the CSV's churn as pre-existing, out-of-scope noise. A stray `.git/worktrees/.../rebase-merge` directory survived a failed `rebase --abort` — removed it directly (git-internal bookkeeping metadata, not tracked content, standard recovery step) after confirming my commit was intact throughout.

Committed as `f04cbeea6` (fix) + `1784ae017` (lint scope broadening) on `main`. Sent Arch the completion report with both precision corrections (`mailboxes/arch/inbox/memo-lead-to-arch-cc-pm-ppm-1366-componentA-complete-plus-two-precision-corrections-2026-07-06.md`, cc PM/PPM). Updated #1366: description now shows the A/B/C status split (A done, B blocked on ADR-075, C out-of-scope-not-a-leak), added the Component A evidence comment. Issue stays OPEN for Component B.

## Duty cycle resumed; mailbox drained; Beta Blockers re-verified fresh against GitHub (~17:11-17:45)

PM returned after a multi-hour gap, named the gap directly ("we've lost hours of potential productivity"), and asked me to resume duty cycle, check mail, update the log, and report Beta Blockers status.

**Duty cycle actually set up, not just acknowledged.** `lead` had never had a registered row in `dev/active/duty-cycle-registry.tsv` or a live cron — checked and confirmed both. Set up `CronCreate` (`17 6,9,12,15,18,21 * * *`, 6x/day matching Arch's shape/hours with a distinct :17 offset to avoid collision) invoking `duty-cycle-tick` on each fire. **Honest limitation, not overclaimed**: passed `durable:true` but the tool confirmed session-only ("dies when Claude exits") — matches the skill's own documented Gap-C caveat (durable is a no-op in this environment). Registered in the registry (`threshold_h=4`, `wake_start=6`, `wake_end=22`, `first_fire=06:17`) so the freeze-watchdog also covers lead going forward. Committed `0897740d5`.

**Mailbox fully drained** — 8 items total across two waves (1 pre-existing + 7 that arrived mid-investigation via a merge). Notable ones: Arch's ratification of #1366 Component A (owns both corrections gracefully, confirms "A is done," notes ADR-075 now incorporates the pm-numbering/default_labels 3-category refinement my correction surfaced) — and **two memos addressed directly to me carrying a live, unblocked, already-authorized build task**: ADR-076 (Usage-Cap Enforcement — 2 mechanisms, Redis-backed rate limit + concurrency gauge, fail-visibly per HOST's trust-lens) is fully ratified with an explicit "Lead: go." Not started yet — surfaced to PM in the status reply rather than silently begun, since PM's ask this turn was specifically for a status report. Regenerated own MANIFESTs post-triage; all sent via `mail-send.sh`.

**Beta Blockers: re-verified fresh against live GitHub rather than trust this morning's doc snapshot** — direct payoff of exactly the discipline this whole session has been running on (verify before reporting, don't confabulate completeness). Queried all 25 `beta:*`-labeled issues' live state directly. Found 2 real discrepancies:
- **Epic A (#1304)**: the doc's own "Recommended sequencing" line wrongly claimed this was closed. It is genuinely OPEN — 4/5 AC done (gating CI job live + proven with teeth, 2 real bugs fixed along the way), the 1 remaining item deliberately held pending PM's explicit go/no-go on flipping the repo-wide required-status-check setting (a hard-to-reverse-without-notice change, correctly not done unilaterally).
- **Epic D (#1278)**: shows CLOSED on GitHub since 2026-07-05T23:18:37Z — discovered only now, predates today's session (missed in this morning's Epic-B-focused edit pass, not a new development). Traced the full timeline: accidentally auto-closed 19:51 (the commit-keyword incident already documented in CLAUDE.md) → reopened 21:55 (PPM) → closed again 23:18 via a direct API/CLI call, NOT another keyword accident. But this second closure has **zero comments, zero session-log entries, zero decisions.log entries anywhere**, and the issue's own AC checklist still shows the actual Fly.io hosting work unchecked. Two equally-plausible explanations (a real PM scope call made verbally and never written down, vs. an undiagnosed second accidental/premature closure) — flagged precisely in the doc and to PM rather than asserting either way.

Corrected both in `beta-blockers.md` directly (info-holder-writes-it-down discipline) + updated the total count (20→19 open) + change-log entry. Committed `13690a19d`, pushed clean after a routine non-fast-forward merge.

**Memory & briefing surfaces referenced this session** (per #974 pilot):
- Referenced: `dev/active/duty-cycle-registry.tsv` (cron cadence precedent), `duty-cycle-tick` skill (setup procedure + Gap-C caveat), CLAUDE.md's SSH-port-443 workaround (used again), the close-issue-properly discipline (informed how precisely to flag #1278 rather than assert).
- Loaded but not referenced: most of `lead-carry-forward.md`'s historical entries (6/27-7/4) — read for continuity but none of today's decisions drew on them; the file is long overdue for compression (noted, not actioned this fire).
- Wanted but not found: no durable record anywhere (mail/decisions.log/session-log) explaining #1278's second closure — a real gap, now flagged to PM.
