# Session log — Architect (Chief Architect) — 2026-07-09

**Role**: Chief Architect (arch) · **Tool**: Claude Code Opus 4.8 · **Account**: PM backup · **Worktree**: `arch-backup-0630`. Mailbox via `mail-send.sh`. HARD RULE honored.

---

## Thursday July 9 — START ~06:57 PT

**Identity verified** (drift-prevention): `arch-backup-0630`, one session. **Step-0**: July 8 retroactively closed this START (was mid-ADR-numbering work at the 21:57 fire). **Inbox**: Docs ADR-073-collision memo (addressed below).

**This fire — author the routing-integrity contract as ADR-077** (NOT ADR-073 — Docs caught that 073 is the accepted no-destructive-git ADR; my #1283 memos + 6/18 scope reserved "073" but the git-rule ADR took it 6/27 unnoticed). Next sequential free = 077 (Docs confirmed; cleaner than backfilling 067/068 gaps). The #1283 AC-4 ruling (decisions.log 7/8) is the substance; the probe validated the approach → the trigger is met.

### Fire — 06:57 START drain — ADR-077 (Routing-Integrity Contract) AUTHORED

Docs #1375 sweep caught the "ADR-073" collision pre-authoring (073 = the no-destructive-git ADR; I'd reserved 073 for routing in the 6/18 scope, git-rule ADR took it 6/27 unnoticed). **Authored the Routing-Integrity Contract as ADR-077** (`adr-077-routing-integrity-contract.md`) — formalizes the 7/8 #1283 AC-4 ruling verbatim: D1 registry-canonical SSOT / D2 derive-the-prompt / D3 normalization-shim-additive-to-aliases (aliases stay = mode-4 defense) / D4 CI reachability-lint across the 4-surface predicate (rail∪category∪pre_classifier∪floor-internal∪intentional-floor) / D5 behavioral golden-corpus. + the mode-1..4 taxonomy. Two-altitude enforcement (static lint modes 1/2/3 + behavioral corpus mode 4). Refines ADR-059 (capability→reachability) + ADR-060 (floor honest-degrade covers unhandled-action). Probe-validated (Lead's 24/29 run). → decisions.log number-correction (all 7/8 "ADR-073" refs → ADR-077) + reply to Docs/Lead cc PM (`588129713`; Docs to fix omnibus+briefing refs). **The #1283/routing-integrity arc — scoped 6/18, resolver-ratified 6/19, M5-deferred, AC-4-ruled 7/8, now contract-authored 7/9 — is complete on the architecture side; Lead builds the enforcement.** Inbox drained; back to IDLE.

### Fire (cont.) — #1382 tier-2 credential store CONCUR (critical-path Lead-support)

Lead consult landed mid-fire (drain-until-empty). #1382: droplet has no python-keyring backend → per-user OAuth/slack/calendar keychain ops fail (killed the GitHub connect callback); gates the tester-loop GitHub leg → PM's invite decision. **CONCUR all 3**: (1) A-now (KeychainService encrypted-DB fallback = general floor) / B-per-port (binding-rail = connector end-state) — m-40, ADR-070-D3-consistent; (2) encryption design — per-name HKDF (#358), **no-plaintext-column = leak impossible-by-construction**, fail-closed endorsed as THE invariant; (3) sync-engine wrinkle acceptable (rare, off-request-path; +connect-close/keep-off-request-path notes). Lead builds same-day → completes tester-loop GitHub leg. → memo Lead cc PM (`28ed0d127`) + decisions.log.

**Fire net (large)**: July 8 retro-closed; ADR-077 (Routing-Integrity Contract) authored + number-corrected + Docs/Lead notified; #1382 credential-store concurred. The #1283/routing arc complete architecture-side; the tester-loop credential blocker cleared. Inbox drained; back to IDLE.

### Fire (cont.) — ADR-077 build CONFORMS (D1–D5) + #1382 shipped ack

Lead's memo: (1) #1382 credential store SHIPPED v0.8.10.1 — all 3 invariants held (no-plaintext-column / fail-closed / per-name HKDF); honestly reported my build-note (a) was initially missed (lazy singleton `pool_size=1` parked an idle connection) then FIXED to `poolclass=NullPool`, 9/9 green. (2) ADR-077 build LANDED, #1283 CLOSED. **Conformance-checked the ADR-077 build from the code (`test_routing_vocabulary_1283.py` + `workflow_entries` derivation) — CONFORMS, D1–D5 all realized**: D4 4-surface reachability lint verbatim (`reachable = rail ∪ pre_classifier ∪ FLOOR_ALLOWLIST; gaps = canon-reachable`); D1/D2 SSOT+derive-the-prompt; D3 shim additive; D5 corpus frozen. **STRONGER than spec in 2 places**: (1) `test_floor_allowlist_carries_no_freeloaders` ratchet enforces my "keep allowlist small" D4 caveat by-construction; (2) the 19-item hand-ledger RETIRED because the derived pre_classifier surface subsumed it (derive-don't-maintain realized) + a derivation-is-alive canary (≥20) closing the vacuous-pass hole. One nit flagged: test docstring still says "ADR-073-bound" → should read ADR-077 (fold into number-correction sweep). **Folded Lead's `Intent.original_message` two-reader contract-drift into ADR-077's motivation** — a value with two hand-maintained access idioms + no single authority = the same class one layer down (retroactively explains #1332). → memo Lead cc PM (`7c7b29eda`) + ADR-077 edit (`6b1ea4717`) + decisions.log (`3f169a4ad`). The seam ran honest both directions today (I flagged connect-close, Lead caught the NullPool miss; Lead built the ratchet past my spec, I credited it). Inbox drained; back to IDLE.
