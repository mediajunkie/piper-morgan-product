# Session Log: 2026-07-09-0647-lead-code

**Role**: Lead Developer
**Model**: Claude Code (Fable — per PM 7/7, header-tracked)
**Date**: Thursday, July 9, 2026
**Start Time**: 6:47 AM PT

## Session Objectives

1. **#1382 tier-2**: per-user OAuth token storage on hosted (the GitHub/Slack/Calendar connect unblock + #1220's tail) — design memo to Arch, then build on ruling.
2. **Arch's overnight rulings** (both landed): #1283 AC-4 SSOT + corpus RATIFIED → step-3 CI enforcement; #1312 three rulings → reconciliation work.
3. Deploy-runbook as-run annotations (owed from last night).
4. Deploy-watch aftermath: PM's invite go/no-go pending; #1220 first-real-write pends GitHub connect (#1382 tier-2).

## Work Log

### 6:47 AM - START (06:17 slot, ~30 late). Step-0: wrote 7/8's retroactive DAY-CLOSE (21:17 fire was absorbed mid-deploy — no STOP ran). Two Arch memos in inbox (both #1283 + #1312 rulings) — mail loop next.

### 7:00 AM - Mail drained: BOTH Arch rulings absorbed; #1374 fix LIVE-VALIDATED on its own triage; #1382 tier-2 design memo sent.

- **#1283 ruling**: corpus v2 RATIFIED (both REVIEW rows resolved as probed); AC-4 = registry-canonical SSOT + (a) derive-the-prompt + (b) shim additive-to-aliases + (c) 4-surface reachability lint. ADR-073 = Arch's next pass, not gating.
- **#1312 ruling**: (1) UNIFY Base (re-confirms Arch's own 6/25 call — personality/models.py is a stale accidental duplicate; delete + repoint + one-Base lint); (2) EXCISE todo_lists architecturally, **gated on PM's product confirm**; (3) PARK-WITH-MODEL the MUX family (additive; NOT drop, NOT suppress — protected meaning-representation).
- Triage ran through the FIXED mail-send: clean reconcile, zero warnings — the exact 3-for-3 failure flow. Noted on closed #1374.
- **#1382 tier-2 design memo → Arch (cc PM)**: KeychainService grows an encrypted-DB fallback backend (`secure_credentials`, per-name HKDF contexts, fail-closed keyless, sync-engine wrinkle flagged) NOW; binding-rail = the per-connector future. Fast concur requested (gates GitHub connect → invites).

### 7:15–8:00 AM - #1283 STEP 3 BUILT (the full ratified AC-4) — probe run 3: 26 PASS / 0 FAIL.

- **(c) lint**: hand-ledger RETIRED — pre_classifier surface DERIVED from source (30 emissions, covers all 19 former entries) + empty FLOOR_ALLOWLIST + extractor canary. 
- **(a) v1**: 7 action-routed prompt examples canonicalized (the mode-1 SOURCE — the drift-cluster's literal origin lines); category-teaching names kept deliberately (probe-verified) under an explicit lint allowlist; +stale_prs_query example from the registry's own phrase (killed run-2's third-variant chaos at source). Full generation = ADR-073 conformance follow-up.
- **(b) shim**: `normalize_action` — conservative prefix-strip, exact post-strip match only, wired pre-rail in process_intent; structlog idiom caught pre-commit. get_stale_prs/fetch_stale_prs→stale_prs proven; get_pull_requests passes through honestly.
- **Probe → ratified predicate** (4-surface + alias-equivalence-by-handler-identity + corpus seam annotations): run 3 = **26/0/2-REVIEW/1-SEAM** (run-1 raw was 15/12). Lint 9/9; intent_service unit 1792 passed; tests/intent/ 124-fail cluster verified PRE-EXISTING via stash-compare (125 unmodified; my changes fix one).

### 8:00–8:20 AM - #1283 CLOSED (all 4 ACs; description-first per the skill). Beta Blockers: 4 open.

### 8:20–9:00 AM - #1312 rulings 1+3 EXECUTED — autogen residual 41 → 30 ops (all judgment/PM-gated).

- **Ruling 1 (unify Base)**: `services/personality/models.py` DELETED — a fully-orphaned pre-#262 duplicate (String user_id vs the DB's UUID; ZERO absolute importers; one relative importer). `personality/repository.py` repointed to the canonical `database/models.PersonalityProfileModel`; canonical `id` gains the `uuid.uuid4` default the create path relied on. **Live-proven against real Postgres**: save(create)/load/update(0.42 round-trip)/delete through the repointed repository (no unit coverage exists for this path — noted; the live round-trip is the evidence). New `TestSingleDeclarativeBase` enforcement lint (one Base per DB — Arch's invariant, now mechanical).
- **Ruling 3 (park-with-model)**: `ConversationLinkDB` declared matching the live DB exactly (information_schema-verified columns/FK-names/ondelete/5 indexes/column COMMENTS — the comments were a fun find: migration 601 wrote them, autogen flagged their absence); `conversation_turns.parent_id` (+FK SET NULL +index), `features.lifecycle_state`, `todo_items.lifecycle_state` declared. All additive, zero DDL.
- **Ruling 2 (excise todo_lists)**: WAITING on PM's product confirm (Arch: excise iff no distinct todo-list feature is owed). Surfaced to PM in fire status.
- Sweeps: enforcement 18 (incl. the new lint), db/security/repos + api-key suite 242, upgrade head clean. tests/unit+security -k personality collection errors verified pre-existing (33 on unmodified code, stash-compare).

### ~7:20 AM (PM's clock) - PM's morning batch: #1380-1382 added to Beta Blockers; GO on GitHub-connect fix + v0.8.10.1 before invites; PA/HOST hold notice SENT.

### #1382 tier-2 BUILT + LIVE-PROVEN LOCALLY — the hosted credential store.

- Migration `g1382creds` (`secure_credentials`: name PK, encrypted_value, timestamps — no plaintext column exists).
- `EncryptedDBCredentialStore`: FieldEncryptionService per-name HKDF contexts; keyless REFUSES (fail closed; keyrings.alt-style plaintext rejected); small dedicated sync engine (settings-time ops — the memo'd wrinkle).
- KeychainService seam: probes for the fail.Keyring null backend at init (the droplet's state — init "succeeds" there, ops raise); auto-routes store/get/delete through the DB store; `PIPER_CREDENTIAL_STORE=db|keychain` explicit override; dead-backend+no-encryptor = hard refuse. **Mac path proven unchanged** (db_store=None locally).
- **Live forced-db round-trip on real Postgres**: store→get exact, ciphertext at rest (no plaintext leak), None-on-missing, delete/re-delete semantics.
- Tests: 9/9 new (selection matrix incl. fail-closed + forced-legacy; round-trip; upsert; per-name context isolation — cross-name ciphertext refuses to decrypt; keyless refuse). Security+mcp sweep 392 passed (3 pre-existing cross-user failures, same set verified 7/8 on unmodified code).

### ~1:30 PM - Session resumed after a ~3h dormant gap (PM woke it; brief Opus interlude ~13:25-13:30 during PM's model check, back to Fable). Gap anatomy owned: an interrupted turn's continuation went degenerate AND my cron was down (deleted for the 7:15 build, never re-armed during the interactive stretch — armed-by-default violation). Corrective: cron stays armed through PM conversations from now on.

### 1:35–2:15 PM - PM directives: (1) FIX the chat write path now, (2) same-oversight audit across all four connectors. "Building a feature Piper can't use is not done" — owned as Completion Theater (045): my #1220 "write cutover" migrated router methods no chat handler called.

**The GitHub fix (v0.8.10.2)**:
- `GitHubIntegrationRouter.is_available()` — the binding-aware gate (BOUND connector binding OR legacy PAT; the same check Settings' Connected status uses), replacing PAT-only `config_service.is_configured` at ALL chat gates.
- `_handle_create_issue` + `_handle_update_issue` REBUILT: legacy env/keychain GITHUB_TOKEN gates removed; writes now go through the ROUTER (connector-first, #1322 read-back guard) instead of the legacy GitHubDomainService that bypassed it.
- 8 read/query gates swapped to `await github_router.is_available()`; 5 stale "add GITHUB_TOKEN" degrade messages modernized to "connect GitHub in Settings → Integrations".
- `_unverified_write_result` helper: the fired-but-unverified honest-uncertainty message now surfaces verbatim from all four write-capable handlers (create/update/close/comment) instead of being eaten by generic error wrapping.
- Tests: 8 new (gate matrix + router-wiring + uncertainty surface + is_available truth table); 39 existing tests' mocks updated to stub the new gate (mechanical, per-file intent preserved); 2 legacy-contract tests repointed from GitHubDomainService to the router. **intent_service suite: 1800 passed, 0 failed**; legacy handler file back to its 7 pre-existing failures (stash-compare-verified before/after).

**The audit (PM's #2)**: Slack = no chat surface (nothing to fix). **Notion + Calendar = same class, different flavor — gates don't thread user_id** (Notion's plugin docstring admits it, #781; Calendar authenticates unscoped while data calls are user-scoped) → filed **#1383** (priority-high) with full traces; per-connector careful fixes, not invite-gating.

### 2:30–3:30 PM - The first-real-write failure ROOT-CAUSED + FIXED (v0.8.10.3): github-mcp-server consolidated its tool contract under our floating `latest`.

**The win first**: PM's retry on v0.8.10.2 produced my honest-uncertainty message VERBATIM — the binding gate passed, the guarded write was attempted, verification failed, and Piper told the truth instead of claiming success or inventing a reason. The #1322 guard's first live firing. ALSO: **#1332 reproduced live** in the same session ("your message came through empty") — instrumentation evidence to collect.

**Root cause (confirmed via release history after PM's log greps came back empty — itself diagnostic)**: github-mcp-server v1.2.0 "Promote issue fields and deprecate legacy issue write tool" — create_issue/update_issue → `issue_write` (+method), get_issue → `issue_read` (+method); the droplet's deploy pulled fresh `latest` (≈v1.5.0) while the adapter + my in-memory fixture both spoke the deprecated dialect (the fixture faithfully mirroring the OLD contract is exactly why 11/11 tests stayed green through a live break). add_issue_comment + all read tools (search_*) unchanged — reads were never broken.

**The empty greps' second finding**: the failure was silent BY CODE — an in-band tool-error response parses to None and returned unverified without logging the response text anywhere.

**Fix (main a-commit + production v0.8.10.3)**: adapter constants + method args onto the consolidated contract (with the bump-together map in a comment); `github_write_unparseable_response` structlog line preserving the response head (the missing evidence) + structlog mirror on the unreachable path (stdlib logger provably didn't surface in docker logs); **compose image PINNED to v1.5.0** — floating `latest` on production is how this happened; version bumps are deliberate now (pin + constants + fixture together). Fixture rewritten to the v1.5.0 dialect with the legacy names deliberately ABSENT + a drift regression test: old constants vs new server = fired-but-unverified, never success, never PAT-fallback. Consumer suite 175 passed.

### 3:40–4:00 PM - v0.8.10.3 retry: same honest-uncertainty — but the NEW evidence line caught the real story. Root-caused to TWO stacked findings + fixed (v0.8.10.4).

`github_write_unparseable_response` (shipped an hour earlier) captured the server's actual reply: **410 Issues disabled on mediajunkie/7cups-testbot** — the tool contract fix WORKED (issue_write accepted, real authenticated API call as PM), but the write went to PM's stale DEFAULT repo, not the explicitly-named one. Why: **live experiment on PM's exact sentence proved the classifier returns NO entities** — the classification JSON schema has no repository/title/body fields, and ENTITY_EXTRACTION_PROMPT has ZERO callers (written, never wired — 75% pattern). So `intent.context`'s issue-write keys were a consumer contract with no producer: every chat-created issue ever would have used fallback-title + default-repo. First time anyone checked output against intent. Flagged for ADR-073 (entity-bearing schema / wiring the orphaned stage = the general fix).

**Fix (deterministic, house #1066 pattern)**: `_slotfill_issue_request` — owner/repo (URL-form first, letter-in-owner guard against fractions/dates, .git suffix strip), titled "…", body "…" — wired into create + update handlers ahead of the default-repo fallback. Adversarial smoke: fraction inside a real request loses to the real repo; date-talk yields {}. 5 new tests incl. the exact live sentence + slotfilled-repo-beats-default (the live failure pinned). Suites: 13/13 file, 1833 passed overall (7 pre-existing). PM also fixing the stale default-repo setting (7cups-testbot, lol — their words).

**Cron discipline note**: 15:17 fire absorbed mid-build in one line — armed-through-everything working as intended after last night's lesson.

### 4:00–6:00 PM - THE WRITE-PATH CHASE, CONCLUDED: five layers, five point releases, first VERIFIED connector write live. #1220 CLOSED. #1332 ROOT-CAUSED.

Full chain (each layer found by the previous one's evidence, never by guess):
1. **v0.8.10.2** legacy PAT gates + unguarded legacy writes → binding-aware `is_available()` + router writes (built earlier today).
2. **v0.8.10.3** sidecar tool-contract drift: floating `latest` had consolidated create/update/get → issue_write/issue_read(+method). Image PINNED to v1.5.0; fixture rewritten to the real dialect; drift regression test.
3. **v0.8.10.4** no entity extraction existed (classifier schema carries none; ENTITY_EXTRACTION_PROMPT had zero callers) → deterministic slot-fill (URL-precedence, letter-guard).
4. **v0.8.10.6** v1.5.0's write response is a minimal {id,url} envelope (no number) → number derived from URL; WARNING-level breadcrumbs (INFO provably doesn't surface in hosted logs — the "invisible diagnostic" mystery).
5. **v0.8.10.7 — THE ROOT**: `Intent.original_message` NEVER set by any of the main classifier's five construction paths (field born with the newer pre_classifier/multi-intent machinery; old classifier used context["original_message"]; no retrofit). Starved the slot-fill (→ default repo → the 7cups 410s) AND the floor (→ "your message came through empty" = **#1332's root**, intermittency explained by WHICH surface classified). All five sites fixed + belt-and-suspenders readers. 1984 tests.

**Autonomy milestone (PM-invited)**: PM's ssh-agent held the key → I ran deploys, log forensics, in-container X-ray repros (monkeypatched spies proving slot-fill/router/MCP args correct → narrowed to the live-path delta), minted a JWT via the app's own jwt_service, and drove the REAL route myself. **Proof: test-piper-morgan#104** — "Created issue #104..." — read-back verified, authored by PM's OAuth identity. Repro artifacts #101-103 closed with notes.

**Paperwork**: #1220 CLOSED (checkbox-first, full evidence chain; Calendar/Slack/Notion ports deferred post-beta per Arch's 3-layer ruling; #1383 tracks gate-threading). #1332 updated with root cause + soak-based closure criteria. **PM's directives executed**: HOST all-clear memo (invites GO — PM's word, ~5:45 PM) + PA resume memo (alpha-hosted-MCP+skills/plugin path, with the substrate summary). Dormancy note: one more mid-turn cut ~5:30 PM (PM pasted the frozen state); cron-armed recovery + PM nudge resumed it.

**Also observed live tonight, tracked**: session-timeout modal's dead Continue/[x] buttons (to file), #1332 reproduced 3× pre-fix (evidence on the issue).

### 6:15 PM - Continuing freely (PM's word): #1384 filed (dead timeout-modal buttons); #1381 FIXED (v0.8.10.8).

**Archaeology answer for PM**: original_message was born with the NEWER machinery (pre_classifier/multi-intent/llm_classifier all set it); the original classifier.py predates the field and used context["original_message"] — no retrofit, two reader populations, #1332's "intermittency" = which routing surface classified the message. The #1283 story in dataclass form.

**#1381**: #1150's helper read the SINGLE config file's timezone (or naive-UTC fallback) — every hosted user got the wrong clock. Now: per-user personalization timezone (ADR-075 D1) → file config (single-tenant path preserved) → **OMIT the flourish entirely** (never the server clock in user clothing; the floor's renderer already skips absent keys). 6 new tests (precedence/degrade/bogus-tz/omit); 1150 test file rewritten to the new contract; intent_service suite 1810 passed.

### 6:30 PM - #1380 BUILT (v0.8.10.9): the Settings LLM-key page — /api/v1/keys finally has a consumer.

`settings_llm_keys.html` (app_shell blocks; provider select + write-only key entry + stored-keys table with validated badges + remove; XSS-escaped rendering; the store/list/delete routes wired), `GET /settings/llm-keys` in ui.py, card on settings-index. **Tests per the house UI discipline**: real Jinja render through the actual shell (5 tests — the block-name lesson caught live: content vs main; a grep-based test would have passed while the page rendered empty). Routes sweep: 16F/4E vs 18F/4E baseline (stash-compared — net -2, all remaining pre-existing).
