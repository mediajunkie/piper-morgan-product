# Session log — Architect (Chief Architect) — 2026-07-12

**Role**: Chief Architect (arch) · **Tool**: Claude Code Opus 4.8 · **Account**: PM backup · **Worktree**: `arch-backup-0630`. Mailbox via `mail-send.sh`. HARD RULE honored.

---

## Sunday July 12 — RESUME (PM present, remote-control) ~16:26 PT

**Context**: laptop reboot killed the cron (CronList empty = Gap-C mode-1a); PM resumed the session and flagged "crons need to be reset" + "you have mail." First arch fire since Jul 10 (Jul 11 was a no-work day; Jul 10 day-closed). **Identity verified**: worktree `arch-backup-0630`, synced to origin/main `0ad88c8e7`. Shell had homed to PM's **main checkout** on resume — moved to the worktree for all substantive work (HARD RULE: no work in PM's main checkout tree).

**Cron re-armed** (PM-authorized): CronList was empty post-reboot → re-CronCreate'd the canonical `27 6,9,12,15,18,21` (6×/day). Session-only per the CronCreate tool (dies when Claude exits) — flagged to PM that the durable/external driver may also need restarting on PM's side.

**Inbox: 4 memos, all from Lead (Jul 12)** — two need my action:
1. `adr070a-built-a2-a4-ratification-ping` — Lead BUILT ADR-070 Amendment A (my Jul 10 ruling) same-cycle; wants A2/A4 code-ratification (the two I called "hold-or-rot"). **← priority (I promised this)**
2. `1395-corpus-rev-ratification-request` — corpus revision ratification.
3. `1386-scenario-results-b-blocked-c-pass` (cc) — informational.
4. `1386-title-update-answer-scenarios-absorbed` (cc) — informational.

### Drain — ADR-070-A ratified (A4 gap found → #1398) + #1395 corpus-rev ratified

**ADR-070-A (my Jul 10 mcp_server_ref ruling) — BUILD-RATIFIED from code.** A2 PASS: `resolve_server_ref` (server_ref_resolver.py) is the single authority; all 6 github_adapter read-paths route through it via `_mcp_client_ctx` L986; no raw-ref read-site; `_KEY_TO_ENV` one map; bind-time stores logical `'github'`. A3/A5 PASS. **A4 HALF-BUILT → filed #1398**: the resolver correctly RAISES `ServerRefResolutionError` with the config-naming message (tested), but `github_adapter` catches it specifically NOWHERE → it falls into every site's generic `except Exception → UNREACHABLE`; the user sees static "server unreachable," the config message buried in `exc_info` under a log line that also says "unreachable/unprovisioned." That's the exact config-masquerades-as-outage failure A4 exists to prevent, still live one deployment away (the beta-cutover case) — precisely the "hold-or-rot" seam I flagged Jul 10. Contract tests assert the resolver raises, not that the adapter surfaces it → integration point uncovered. Fix (#1398): catch distinctly inside `_mcp_client_ctx`, new `DegradationReason.MISCONFIGURED`, log as CONFIG-error naming the var, keep end-user msg generic. NOT blocking (both envs have the var set). → memo Lead cc PM + decisions.log (`1d3de3cf7`).

**#1395 D5 corpus-rev — RATIFIED (7 rows).** All `expected floor → real registered handler` for capabilities shipped after the corpus was written (stale-floor, not regression) — the ADR-077 D5 maintenance case per the #1283 corpus-v2 precedent (ratified rev, not silent edit). Spot-confirmed destinations are real registered entries (didn't take "observed in Run 15" on faith). Guardrail on record: ratifiable only where observed==intended (else teaching-to-the-test); all 7 clear it. Q45 ruled PASS-with-clarification (close-issue asking for a missing issue number = intended ADR-060 honest ask). Harness graceful-degrade on malformed principal affirmed (degrade-not-crash, ADR-071). → memo Lead cc PM + decisions.log (`fcab92e97`).

**Triaged (informational, cc):** #1386 scenario results (C PASS 3/3; B blocked turns 3-4 by pre-existing continuity gap #1394 — CXO/PPM's joint scoping call) + the title-update answer. **#1394 has an architecture angle in my lane** (cross-turn antecedent machinery exists — passing pronoun-retention corpus tests — but isn't wired to the `/api/v1/intent` session path; Lead says scoping is uncertain until that's mapped) — offered to PM as an available next step, not started unprompted.

Inbox drained (0). Both Lead-support ratifications done. Cron re-armed (`1b4d6ef2`). Everything on origin/main.

### Fire — 18:36 WATCH (autonomous) — light: cron confirmed + carry-forward post-reboot refresh

Inbox empty; queue dry; nothing new in my lane (24 commits, all other roles). **#1394 mapping parked pending PM's steer** (offered last turn take-it/leave-with-Lead; not starting unilaterally — it's a fork I put to PM + Lead's named owner). Cron confirmed healthy (CronList shows `1b4d6ef2` firing — this fire is proof). **Idle hygiene: carry-forward post-reboot refresh** — cron id `9c0b0550`→`1b4d6ef2` (stale-id = the 7/4 self-attribution-drift hazard class) + reconciled the pre-reboot "external driver / don't re-CronCreate" model with the 7/12 PM-authorized in-session re-arm regime + a self-attribution note (a new cron id post-reboot is THIS session's work, not a phantom peer). Awaiting-Lead items: #1398 (A4 fix), #1395 rev commit+re-run. Back to IDLE.

### Fire — 21:56 STOP (autonomous, last fire of day) — day-close

Inbox empty; queue dry; nothing new in my lane (17 commits, all other roles — notable: PPM closed #1386 criterion 3, Scenario B rescoped 4/4 PASS). #1394 mapping still parked pending PM's steer; #1398 (A4 fix) + #1395 (corpus rev commit+re-run) still Lead's to build — nothing to ratify yet. Day-close.

## Day arc — July 12 (Sunday; post-reboot resume + two Lead-support ratifications)

A short, high-value day. PM resumed the session after a laptop reboot (~16:26); cron re-armed (`1b4d6ef2`, session-only, PM-authorized). Drained the Lead-support queue: **ADR-070 Amendment A build-ratified** — A2/A3/A5 clean (the single `resolve_server_ref` authority exactly right), **A4 half-built → filed #1398** (the resolver raises the config-naming error but the adapter flattens it into UNREACHABLE at all 6 call sites — the config-masquerades-as-outage failure A4 exists to kill, the precise "hold-or-rot" seam I flagged 7/10; not blocking since both envs have the var set). **#1395 D5 corpus-rev ratified** (7 rows, corpus catching up to shipped capability, observed==intended guardrail applied; Q45 ruled PASS-with-clarification). Carry-forward post-reboot refresh (stale cron-id → `1b4d6ef2`, regime reconciled). The author/ratify seam held: Lead built A same-cycle, I ratified from the code and caught the one real gap.

## Memory & briefing surfaces referenced this session (per #974)

**Referenced**: my **carry-forward + Jul 10 session log** (drift-recovery first move post-reboot — cron/worktree/account state) · **ADR-070 Amendment A** (the ruling I authored 7/10 → ratified the build against 7/12; A2/A4 the hold-or-rot conditions) · **ADR-077 D5** (the corpus-as-contract discipline governing #1395; ratified-rev-not-silent-edit) · **#1283 corpus-v2 precedent** (the D5 rev procedure) · **ADR-060 floor-first** (Q45 ask-don't-fabricate ruling) · **ADR-071 owner-scoping** (harness graceful-degrade affirmation) · **the 7/4–7/5 self-attribution-drift lesson** (carry-forward cron-id refresh rationale) · **#1387/#1382 fail-closed pattern** (the mirror the #1398 fix should follow).
**Loaded but not referenced**: the #1386 scenario memos (cc; CXO/PPM's joint scoping lane, decision theirs).
**Wanted but not found**: nothing — carry-forward + logs carried the post-reboot resumption cleanly (the drift-recovery infra worked).

## Sign-off
✓ All July 12 work on origin/main (through the day's last push). PM's main checkout untouched (HARD RULE). Worktree `arch-backup-0630`; cron `1b4d6ef2` (session-only, re-armed post-reboot). Inbox empty; queue dry. #1394 parked pending PM; #1398/#1395 awaiting Lead builds.

<!-- DAY-CLOSED: 2026-07-12 -->

— Architect (Opus 4.8, PM backup), Sunday July 12 closed 21:56 PT. Post-reboot resume + ADR-070-A ratified (A4 gap #1398) + #1395 corpus-rev ratified. Tomorrow: ratify Lead's #1398 fix + #1395 re-run when they land; #1394 mapping if PM says go.

### Session REOPENED ~22:1x (PM instruction) — #1394 architecture mapping: wiring lapse or architectural gap?

PM said "go" on the #1394 mapping + directed the method: review domain models + architecture docs myself, dispatch a subagent to audit the code, determine wiring-vs-gap. (DAY-CLOSED marker above was premature — reopening per one-log-per-day.) Work follows.
