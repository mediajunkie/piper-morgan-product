# Session Log — 2026-07-12 — Lead Developer (Claude Code)

**Role**: Lead Developer | **Model**: Fable 5 | **Worktree**: interesting-beaver-7ee19c (resumed session) | **Cron**: 17 6,9,12,15,18,21 * * * (re-armed at resume)

## Context at START (12:31 PM, PM-initiated resume)
Friday session froze ~19:00 7/10 (no STOP; retroactive close written). Jul 11: no session (PM attended blog posts + ALPHA INVITATIONS — sent, per PM). Sprint state: #1332 soak (now day 3 if logs clean — CHECK), #1278 cutover in progress (Fly stack live; cert pending PM's DNS — PM doing NOW), #1386 awaiting CXO/PPM scenarios, #1388 queued. 5 unread memos.

## Fires
- Resume (12:31) — retroactive 7/10 close; today's log; cron re-arm; PM doing CNAME; mail drain next.
- Resume (cont.) — cron re-armed (d050c607). **5 memos drained**: Arch ratified #1387 fix + confirmed P1-D4 close + RULED mcp_server_ref → **ADR-070 Amendment A** (Option B + single-resolver authority; build queued as next-after-cutover, task #108); CXO delivered scenarios A/B/C; PPM co-signed + asked title-update question (**answered from code: WIRED — B turn 3 = edit path**) + recommended gate-against-Fly-artifact (endorsed operationally, PM's call). Reply memo sent (ppm+cxo+arch, cc PM). #1332 soak check: droplet logs since Fri (66h window) — **0 occurrences. Soak days 1-3 ALL CLEAN → closure criteria met**; closing #1332 next.
- (~13:00–13:30) **Cutover milestone: beta.pipermorgan.ai LIVE** (PM's DNS landed, cert issued; PM created "Piper Morgan Beta" OAuth app + set client secrets from own terminal; I flipped PIPER_BASE_URL + REDIRECT_URI to the beta domain). Health 200 / unauth 401 on the real hostname.
- **#1390 found live minutes after public exposure**: ten scanner IPs filled the ADR-076 session gauge → authenticated "Instance at capacity". Fixed (gauge counts user:* only; ip:* keeps the rate limit), 15/15 middleware tests, filed + deploying. Exactly the class #1386 criterion 5 exists for — found before testers did.
- **#1332 CLOSED** (soak 66h clean). Sprint issue-list fully drained.
- **Deploy-environments doc** written (PM-requested): phases 0-1 current practice, 2-4 proposal (canary split → public-beta flip → 1.0 three-tier).
- Exec ledger memo sent (PA read-no-reply on MCP-resume; all other threads current).
- (~14:00–14:45) **#1386 gate execution (PM pre-authorized away-window)**:
  - **Criterion 5 PASS both envs**: Fly + droplet both at h1312recon (head), autogen-diff EMPTY against live schemas, ENCRYPTION_MASTER_KEY present (both; #1387 guard silent = passing). Security suite = green CI on the deployed SHAs.
  - **Criterion 2 launched**: canonical retest Run 15, judge enabled, in-process at Fly-deployed code (background).
  - **Scenario B run 1 = productive FAIL at turn 2, two live finds** (the gate earning its keep): (a) sidecar READ path HTML-entity-escapes text (`Let's`→`Let&#39;s`) vs raw-stored writes → #1322 verify mismatch → honest-uncertainty on every apostrophe'd title; (b) colon-introduced quoted titles (CXO's natural phrasing) missed extraction → garbage fallback title. Both FIXED (entity-normalized comparison + fixture mirrors live escaping; colon-quote extraction) + 30/30 write suites + deploying. Also noted: `[Available context] (none)` scaffolding leaked in B1's reply (to file). Artifacts #105/#106 closed on test repo.
- (~14:45–15:00) **Scenario B run 2 + C executed on live beta**:
  - B1 ✓ B2 ✓ (both same-day fixes proven: #107 created, exact title, verified confirmation). **B3 ✗ misroute→Notion / B4 ✗ no-recall — alpha parity probe BIT-FOR-BIT IDENTICAL → pre-existing continuity gap, filed #1394** (turns save; history never loads into subsequent context on the session path). Honesty held throughout (no fabricated edit/recall).
  - **Scenario C: PASS 3/3** (honest declines, zero simulation, accurate capability description).
  - #1386 updated with the full criterion 3+5 report; **memo → CXO/PPM (cc PM, Arch): fix-before-gate vs re-scope-B is their joint call**; #1394 investigation queued next regardless.
  - Run 15 first pass: 242P/10F/1E — my own `tail -3` ate the failure names (output-capture error, owned); Run 15b re-executing with full capture.
- (~15:15–15:50) **Criterion 2 verdict: Quality PASS 92% (23/25, band-high) · Routing 88.5% nominal = CONDITIONAL** — all 7 misses triaged as corpus-expectation drift (`expected floor, got real-handler` — capability growth from #1220/#1383/Slack/canonicals outpacing the frozen contract); Q51's extra error = harness non-UUID identity crashing a UUID-typed owner query (prod-unreachable). **No product routing regression.** Runs 15/15b byte-identical (stable). Filed: **#1395** (corpus rev + harness UUID fix), **#1396** (vestigial User.preferences loader). History row appended. **D5 ratification memo → Arch** with the 7 draft row changes (the #1283 corpus-v2 precedent: reviewed, never silent). Verdict + full triage posted on #1386. (Own error owned: Run 15's first capture was truncated by my tail-pipe; 15b re-ran with full output.)
- (~16:00–16:30) **#1394 investigation COMPLETE** (task #110): persistence works; the floor's history surface = last-4 user messages @80 chars + Layer-4 state — **assistant responses never reach any context surface** (B4 structurally impossible), and **classification never sees history on this path** (B3's misroute; the passing pronoun-retention path is the structured-dispatch QUERY follow-up reader, not classification). Fix = real build (response-aware history slice + correction-verb antecedent rule touching ADR-077 surfaces): days-not-hours. Posted to #1394 with a rescope-for-this-gate recommendation input; the call stays CXO/PPM's.
- (~16:35–17:05) **Q51's "harness artifact" framing was WRONG — two layered PRODUCT bugs, both fixed** (own-correction posted on #1395): (1) `_handle_productivity_query` passed **session_id as owner_id** (#734/ADR-071 principal-confusion class) — productivity stats NEVER returned real data for any user; now `_principal_from_intent` + honest unauth degrade. (2) Underneath: `get_completion_stats` compared VARCHAR status vs raw TodoStatus enums (asyncpg rejects) — `.value` ×3. Q51 fingerprint+quality now PASS; routing row rides the corpus rev. Committed + deploying to beta.
- (~17:10–17:45) **#1388 built** (read-path named-repo): `list_open_issues` gains repo-scoping (`repo:owner/name is:issue is:open`); the list handler slot-fills the named repo and threads it — named beats default, count + empty-state phrasing name the scope. 2 pinning tests; productivity tests aligned to the #1395 principal contract (they drove the handler user-less and asserted the pre-fix behavior). Touched surfaces 1444+ green. Deploying; live-verify + close on landing.
- (~17:50) **#1388 CLOSED** — live-verified both directions on beta (named: "102 open issues in mediajunkie/test-piper-morgan" w/ that repo's issues; unscoped: 169 user-wide intact). PAT-fallback omission recorded as chosen (retires with D6).
- (~18:00–18:25) **ADR-070-A BUILT (task #108)**: `resolve_server_ref` single authority (A2) w/ key→env map in-module; bind-time stores logical key 'github' (A1); BYOC shape-discrimination centralized (A3); config-naming degrade error (A4); `i070abackfill` data-only migration for the closed managed-literal set (A5; autogen-empty guard still green). 7 contract tests + 66 write-suite green. Deploying to beta (migration in release_command); droplet at next cut. **Arch pinged for the A2/A4 code ratification** (his hold-or-rot pair).
- (~18:35) **ADR-070-A verified live**: binding row backfilled to `github|github` by the release migration; chat-driven GitHub read resolves logical-key → deployment env → sidecar → real issues. **The away-window queue is FULLY DRAINED** — every pre-authorized item executed: gate criteria 5+2+scenarios, four same-day bug-pairs fixed+deployed, #1388 closed, ADR-070-A built+verified, 5 issues filed, 3 ratification/decision memos out (Arch corpus-rev, Arch A2/A4, CXO/PPM scenario call).
- (~19:00, PM-nudged wave) **CXO+PPM JOINT SIGN-OFF landed**: re-scope B for this gate (their memos predate my scope-read; days-build → their default holds); **#1394 = committed pre-wave-2 P1** (they ask PM to set it explicitly); quickstart disclosure = load-bearing, CXO drafts from my scope contour; original B3/B4 stays the intended test. Their note: "the gate design is working" (B caught 2 same-day bugs + a product gap no suite tested).
- **Re-scoped B3'/B4' first run caught TWO MORE**: B3' update fired with "no fields" (the 'to-form' new-title phrasing missed extraction — the SUBSTITUTE phrasing itself!); B4' displayed raw HTML entities (Let&#39;s) from the sidecar's escaped reads. Fixed at the right layer (parse-boundary unescape → display+verify+recall all clean; to-form extraction) + 33/33 write suites; deploying; re-run on landing.
- (~19:45) **Re-scoped Scenario B: PASS 4/4 on live beta** (B3' verified explicit-reference title update — GitHub truth confirms; B4' clean-text GitHub-truth recall after the THIRD parse-site fix — _parse_issue_detail had its own json.loads; meta-debt noted: 3 issue-parse copies in one adapter IS the drift pattern, consolidation candidate). Joint-signed execution recorded on #1386 with the remaining-to-close list. **Day total: 8 defects found+fixed+deployed, 1 gap scoped, all before tester exposure.** Arch session was frozen — PM resumed it; corpus-rev + A2/A4 ratifications now processable.
- (~20:00–21:15) **#1399 — PM's Scenario A attempt found the FRESH-TESTER WALL (both envs, alpha invitations in flight!)**: `/` + `/register` 401'd (smart redirect never ran — `/` not exempt), only reachable surface = self-host wizard failing localhost checks (no /.dockerenv on Firecracker; DATABASE_URL vs POSTGRES_*; CHROMA_HOST vs CHROMADB_HOST). **Fixed the full chain**: `/` exact-match exempt (never prefix!) → smart redirect works; managed-infra sentinel (FLY_APP_NAME + PIPER_ENVIRONMENT); DATABASE_URL-first db check; CHROMA_HOST honored; login names the invite path ("Have an invite code? → Create your account" — the wizard's Account step already carries #1344's invite field); 4 tests. THEN the residual chroma-red was REAL: Rust-era chroma binds IPv4-only vs IPv6-only 6PN (tcp6 = ssh only) → **private flycast** bridge. **Beta wizard: ALL GREEN ("Core services ready"); / → 302 /login. Scenario A UNBLOCKED.** Droplet cut = PM decision pending (recommended full-parity tonight; invitees blocked until shipped).
- (~21:00–21:25) **v0.8.10.12 CUT + DEPLOYED TO ALPHA (PM-approved full-parity dot release)**: 45 files, zero residual delta vs main on product surfaces (the parity diff also revealed the 7/9-morning items — Base unify, routing SSOT — had never made ANY production cut; now shipped). Droplet migrate ran h1312recon→i070abackfill clean; VERSION verified 0.8.10.12. **Alpha verification all green**: / → 302 /login; invite path named; wizard checks "Core services ready"; GitHub rail through backfilled binding + resolver + named-repo read (corrected #107 title visible). **#1399 CLOSED — alpha invitees unblocked.** Both environments identical (true parity restored). (Build note: a zsh `read` loop using `path` as a var name clobbered $PATH — zsh ties it to PATH; renamed. Worth remembering.)
- (~21:30–21:55) **PM-requested local-setup audit (the #1399 same-oversight sweep)** — 7 pattern classes swept (localhost defaults / disk state / compose literals / subprocess / uploads / self-reference URLs / sentinels). **Two real findings filed**: **#1400** (per-user connector prefs in local JSON — vanish on every Fly deploy, invisible to pg_dump, outside DB isolation; fix = personalization_contexts or ConnectorConfigService rail; calendar token-file fallback folded in) + **#1401** (uploads on ephemeral FS — active data loss on Fly, doc-upload IS in the beta surface; DB rows survive while blobs vanish = confusing-not-honest reads; storage decision wanted: volume / Tigris object store / DB-bytea — my lean Tigris... stated DB-bytea-viable too). Everything else triaged clean: env-first-with-local-default (correct pattern), gated/unprovisioned connectors (ngrok, devenv, calendar), in-container self-probes (fine). Both = pre-wave-2 class, not gate-blocking.

---

## Day Close (retroactive — written 2026-07-14 at PM/Docs request)

This day ran continuously past the normal STOP window (deep #1386-gate away-window drain + the v0.8.10.12 full-parity cut), then the session compacted into 2026-07-14 without emitting a formal STOP. Reconstructed and closed here so Docs can build the omnibus. All content above is complete and contemporaneous; this section adds only the day-arc + memory-eval + sign-off.

**Day-arc summary**: The #1386 beta-gate away-window turned into the day's spine — PM pre-authorized an unattended drain and the gate earned its keep, surfacing **8 product defects (all found+fixed+deployed same-day) plus 1 architectural gap scoped** before any tester saw them: sidecar HTML-entity escaping on three separate issue-parse sites (the consolidation-debt tell), colon-quoted + to-form title-extraction misses, the `session_id`-as-`owner_id` principal bug (productivity stats never worked for anyone) layered over a VARCHAR-vs-enum stats query, and the fresh-tester wall (`/` not exempt → smart-redirect dead; wizard localhost checks failing on Firecracker; chroma IPv4-vs-IPv6-6PN → private flycast). Gate criteria 5 (PASS both envs) + 2 (Quality 92% PASS / Routing 88.5% conditional = corpus-drift, no product regression) executed; Scenario B re-scoped per CXO/PPM joint sign-off → PASS 4/4, Scenario C PASS 3/3. Shipped #1388 (named-repo reads), built+verified ADR-070-A (`resolve_server_ref` single authority), and cut **v0.8.10.12** to alpha restoring true alpha/beta parity (#1399 closed, invitees unblocked). Filed #1394 (continuity gap), #1395/#1396 (retest follow-ups), #1400/#1401 (hosted local-setup audit). Three ratification/decision memos out (Arch corpus-rev, Arch A2/A4, CXO/PPM scenario call).

**Memory & briefing surfaces referenced this session**
- *Referenced*: ADR-070 / ADR-070-A (connector resolution — built against it); ADR-071 (owner-scoping — the principal-confusion fix); ADR-077 D5 (corpus-rev discipline — the "reviewed never silent" precedent for the ratification memo); #1386 gate criteria (execution spine); `close-issue-properly` skill (#1388/#1399 closes); canonical-retest-history doc (Run 15 append); `github-and-tooling-gotchas` (auto-close-keyword hazard avoided in commits).
- *Loaded but not referenced*: most role-briefing docs; cross-pollination brief.
- *Wanted but not found*: a single shared issue-payload parser — the adapter has 3 near-duplicate parse sites (the escaping bug had to be fixed in each); a consolidation-candidate memory/issue would have surfaced it earlier. (Meta-debt noted in-log; worth a tracking issue.)

**Sign-off (retroactive attestation)**: all 7/12 work reached `origin/main` and `production` contemporaneously — v0.8.10.12 deployed and live-verified on both environments same-day; issues filed/closed on GitHub; no work stranded on a branch. Verified 2026-07-14: no uncommitted 7/12 artifacts remain.

<!-- DAY-CLOSED: 2026-07-12 -->

