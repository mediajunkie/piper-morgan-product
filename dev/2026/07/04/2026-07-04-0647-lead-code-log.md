# Lead Developer — Session Log 2026-07-04

**Role**: Lead Developer (role-slug: lead) · **Tool**: Claude Code · **Model**: Sonnet 5
**Worktree**: interesting-beaver-7ee19c (ephemeral, Model B). Sole lead. Carry-forward: `dev/active/lead-carry-forward.md`.
**Mode**: PM-initiated morning session. Continues directly from 7/3's clean day-close (`#1344` deployed live as v0.8.9.2, closed with full evidence).

## START (~06:47)

- **Step-0**: verified 7/3 closed properly — `<!-- DAY-CLOSED: 2026-07-03 -->` marker present in yesterday's log. No missed-STOP repair needed.
- PM opened with "Good morning" + a direct status-update request (where things stand / what's next / blockers) — this is a session start per CLAUDE.md; log created before responding to anything else.
- **Note on session hiccup**: the first two attempts to create this file were blocked by a tool-denial ("user doesn't want to take this action") even after PM explicitly said to proceed. A `SessionStart:resume` hook then fired, confirming a genuine session resume/interruption event (not an intentional PM block) — third attempt succeeded cleanly. No work was lost; flagging only for continuity in case anything looks odd in retrospect.
- **Mail check**: the resume hook's own summary said my inbox was empty; a direct `ls` found 2 memos anyway (HOST's #1344 trust-lens PASS, Arch clarifying to HOST that the architectural ratification already landed 7/3 evening) — checked directly rather than trusted the hook's count. Both memos are informational/confirming: both gates on #1344 (HOST trust-lens + Arch architecture) are now cleanly met; minting is unblocked pending HOST sending an actual tester count. Triaged both to `read/`.
- **Cross-pollination brief** (fresh, 06:07 this morning) featured yesterday's #1344 atomicity pattern and the #1231 completeness-guard timing pattern as cross-project insights for Klatch/sibling projects — no action needed, just confirms yesterday's work propagated correctly.
- Sync clean (0/0). Nothing else pending. PM asked for a status update — answering directly in conversation rather than duplicating it here at length; this log has the day-arc from 7/3 for full detail.

## #1235 resolved — PM ruled Option A (~08:00)

PM: "#1235 - clear" (Option A, matching PPM's own lean from their 7/3 escalation). Verified current state first (still `RECONNECT - Connector Refactor`), applied via `clearProjectV2ItemFieldValue` (the GraphQL mutation for genuinely unsetting a single-select field, not just re-assigning it), verified read-back returns `null`. Notified PPM the thread is closed. PM also asked me to check for a HOST update on #1344 minting — checked directly, inbox is clean, HOST hasn't sent a tester count yet.

## PA's MCPB briefing arrived — correcting my own 7/3 answer to PM + a real finding filed (~08:20)

PA replied in full to my 7/3 briefing request (`piper-morgan-skunkworks` source location, version state, connection architecture, #1278 relationship, my stewardship boundary). Two things worth logging precisely:

**I gave PM a wrong answer on 7/3.** I said the MCPB and hosted alpha were "two disconnected paths," reasoning from the plugin's skill descriptions ("requires local `python main.py`"). PA confirmed those descriptions are stale — `manifest.json` bakes in `PIPER_BASE_URL=https://alpha.pipermorgan.ai` as the install-time default, overriding the local-only framing entirely. **The MCPB already talks to the same production backend I deploy to.** Correcting this with PM directly rather than letting it stand uncorrected in the record.

**Real security finding, contextualized rather than just relayed.** PA found `connect(credential)` sends a shared password as Basic Auth that authenticated against Caddy's basic-auth — removed 6/28/29, so nothing checks it now ("connect" is non-functional as a gate). Before reporting this further I checked one specific mechanism myself rather than take the framing at face value: does the shared literal `session_id: "byoc-poc"` mean different anonymous testers' conversations get persisted into one shared DB row? Traced `intent.py`: the `ConversationDB` persistence branch is gated on a real `user_id` (`current_user.sub if current_user else None`), which anonymous MCPB callers don't have — so that specific worst-case mechanism doesn't fire. Also confirmed #1343 (already live) already refuses anonymous+keyless callers honestly, so the *billing* exposure isn't re-opened by this finding. What remains genuinely unverified: whether Redis or in-process state might still be keyed on the shared `session_id` in a way that crosses users — didn't claim this was safe, filed it as the explicit open question.

Filed **#1351** with the full finding + my verification + what's still open, rather than let it live only in mail. Sent PA a short ack owning the correction. Both memos triaged to `read/`.

## RECONNECT status check — real board query, not memory (~09:00)

PM asked "anything unblocked in RECONNECT you can work on now?" Rather than repeat my days-old "fully drained" recollection, paginated the actual project board (38 RECONNECT-tagged items total; naive `first:100` query initially returned 0 matches — the board has 1175 items total, needed a proper cursor-paginated walk across 12 pages to get an honest count: 26 closed, 12 open). PM independently confirmed the count matched their own rough memory and mentioned they can pull the board themselves + floated defining a skill for this query pattern — extended my sprint-tracking memory to cover *reading* status, not just *changing* it (I've now hand-written this same pagination logic twice with nothing reusable between).

Traced each of the 12 open items rather than assume any were pickup-able:
- #1315 (project↔repo linking) and #1314 (auto-default repo) both explicitly need a PM/CXO decision before there's code to write.
- #1323 (extract connector mixin) is explicitly gated on a 3rd connector port existing — only 2 exist.
- #1317 (more connector ports) is real but needs its own scoping pass, not a cold start.
- **#1231 and #1320 turned out to be stale, not open** — verified against current code rather than the (6/14 and 6/25-dated, respectively) issue bodies:
  - **#1231**: the GitHub #1226-class silent-`{}` sites the issue names are all fixed and tested (11/11 tests green). But the issue's broader "apply to every connector + general enforcement test" (Phase 3) is genuinely not done — calendar's `calendar_offer_policy.py` was never migrated onto the shared contract it inspired, no other connectors were audited, and only a narrower copy-completeness guard exists where a general enforcement test was called for. **Split the honest remainder into #1352** rather than either falsely mark 100% complete or leave the whole thing open indefinitely. Closed #1231 with a full checkbox-by-checkbox accounting (including two AC items I could NOT honestly check: no distinct `EMPTY` reason exists, and the copy was never routed through #876's `UserFriendlyErrorService` as literally specified).
  - **#1320**: **caught my own mistake mid-investigation.** I told PM I'd verified the two secondary bugs (`setup.js` missing `/api/v1` prefix; Slack/calendar status routes not exempt) were "still present" — I had misread my own grep output, mistaking "found in the exempt-list" for "confirmed missing from it." Re-checked properly: both were already fixed, on different days, by different commits that each cited #1320 but never went back to update the issue (`fb63b827c`, 6/25, same day as filing; `f7e20df28`, 7/1). The primary cause (Caddy dialog loop) is also resolved, though not by the clean mechanism the issue anticipated — the gate's removal was undocumented and is what created #1343/#1344, not a deliberate fix. Closed with the correction stated plainly in the closing comment, not smoothed over.

Net: no new RECONNECT feature work was cleanly unblocked, but 2 stale-but-actually-done issues got closed with full evidence and 1 genuine follow-up (#1352) got properly scoped and filed — real, honest progress, just not the shape PM's question anticipated.

## PM rules on #1315/#1314, asks #1323, defers #1317 (~09:15–10:05)

PM's decision closed exactly the gap the prior section flagged — both #1315 and #1314 needed a PM/CXO call before code; PM made both calls in one message, plus asked #1323 and set #1317 as "when you get to it."

**#1315 — retire the dead paths.** Removed `_resolve_from_project()` and `_resolve_from_default_project()` from `repo_resolver.py` entirely; `resolve_repo()`'s decision tree collapsed from 6 paths to 4 (explicit → user_default → env_var → unresolved). Updated module/function docstrings with explicit "RETIRED (#1315, PM-directed 2026-07-04)" notes pointing at git history if project↔repo linking is ever rebuilt. Rewrote `test_repo_resolver_paths_1230.py` (removed path2/2.5 proof-tests, renumbered the rest) and removed `TestDefaultProjectResolution` (4 tests) from `test_repo_resolver_1042.py`. Deliberately did NOT touch the underlying `ProjectRepositoryLinkDB` table/model — confirmed it's now fully orphaned (only consumer was the removed functions) but that's a bigger, separate action than "retire the dead paths" asked for; flagged as a future cleanup candidate instead. Carefully verified `Project.is_default`/`is_archived` (used broadly — todos, conversations, onboarding, intent_service) were untouched; only `ProjectRepositoryLinkDB` (repo_resolver-only) lost its consumer. Committed `04358751c`, pushed clean.

**#1323 — connector-port master list.** Investigated and answered conversationally (informational, not a code task): 8 named connectors total (github, calendar, slack, notion, cicd, devenvironment, gitbook, linear); only github + calendar have been ported onto the #1232 4-method Connector protocol (`connect`/`status`/`resolve`/`degrade`) so far — 2 of 8. No committed timeline exists for a 3rd port; #1317 (the next port) is real but "needs its own scoping pass, not a cold start" per the prior section. Deliberately distinguished this from the older pre-RECONNECT spatial/MCP-consumer adapter files (which exist for all 8 by an older, thinner shape) so as not to overclaim completeness PM didn't ask about.

**#1317 — noted, not actioned.** PM: "when you get to it run a complete audit cascade and we can discuss." This is standing guidance for whenever #1317 is picked up, not a request to start now — carried forward, not worked this session.

**#1314 — implement "default default."** PM's rule verbatim: "default is easy if one exists. if multiple exist and none is designated as primary/default by user then first/oldest active one should be system default." Implemented as two functions in `repo_resolver.py`: `compute_default_default(repos)` (pure selection — single repo wins outright; multiple → oldest non-archived; all-archived edge case PM didn't name falls back to oldest-overall rather than returning nothing) and `apply_default_default_if_unset(user_id, repos)` (persists via `ConnectorConfigService.set_default_repo`, but only after confirming no existing preference — never overwrites an explicit user choice). Extended `github_adapter._parse_repo_search()` to carry `created_at`/`archived` through (previously dropped fields) as the rule's inputs. Wired into `handle_github_callback` right after `persist_github_connection` succeeds — the natural first-run moment — wrapped in its own best-effort try/except so a failure here can never break an OAuth connection that already succeeded.

11 new unit tests (`test_repo_resolver_default_default_1314.py`): empty/single/multiple/archived-skipped/all-archived-fallback/missing-created_at cases for the pure function, plus never-overwrite/no-op-on-empty/sets-when-unset for the persistence wrapper. Full `integrations/` + `mcp/consumer/` + `connectors/` regression: 742 passed, 3 skipped (pre-existing), 0 failed. Committed `5d071137e`.

**Push hit a real conflict this time** — `git push origin HEAD:main` was rejected (non-fast-forward; Arch had pushed a session-log commit in the interim). `git rebase origin/main` mis-behaved: it created an autostash for an unrelated pre-existing dirty file (`skunkworks/klatch/klatch data model.csv` — not mine, already dirty before this session per the worktree's git status) but then errored out of the rebase before finishing, leaving `rebase-merge/` in a half-initialized state and the autostash orphaned (created but not listed in `git stash list` — recoverable directly by its SHA, confirmed byte-identical to the working tree via `git diff <sha>:<path> <path>` before doing anything else). `git rebase --abort` cleanly restored HEAD with zero data loss (re-verified the same diff-identical check post-abort). Abandoned rebase entirely in favor of `git merge origin/main --no-edit`, which succeeded with no conflicts (the incoming commit only touched an unrelated arch session-log file) and never touched the pre-existing dirty file. Pushed clean (`8eddd41ae`); verified `git log --oneline origin/main..HEAD` and `HEAD..origin/main` both empty post-push. Noting this in case the rebase-vs-merge friction recurs for another agent on this repo — `merge` was the lower-risk path here given unrelated dirty files already sitting in the tree.

Closed #1314 via close-issue-properly: status banner + AC checkbox added to description first, then closing comment with full evidence (commit, test counts, file list) including an explicit **honest gap disclosure** — the `handle_github_callback` route itself has no dedicated test (pre-existing gap, not introduced here; the new logic is fully unit-tested in isolation but the route wiring itself was verified by signature-reading + a clean-import check, not an executed route test).

## #1317 audit cascade — PM corrected my framing first (~10:20–10:45)

I'd carried #1317 forward as "PM-deferred, standing guidance for whenever it's picked up, not actioned this session." **PM corrected this immediately: "when you get to it just means after preceding work is done."** The preceding items in that same instruction batch (1315, 1314, 1323) were done — so #1317 was actually due right then, not indefinitely parked. Saved this as its own feedback memory (`feedback_when_you_get_to_it_means_sequentially_next.md`) since it's a genuinely non-obvious reading of how PM sequences batched instructions, distinct from the existing deadline/idle memories.

Ran the actual audit cascade PM asked for on #1317 (implement the #1232 4-method contract onto all connectors). Code-verified, not memory-based:

**2 of 8 connectors ported** (github, calendar) — both declare `IMPLEMENTS_CONNECTOR`, both have all 4 methods, both pass the m-41 AST-guard + runtime-Protocol check. **But neither actually passes its integration test suite** — I ran `tests/integration/` for both (not just unit tests, which were already green) and found 14 failures, all pre-existing and unrelated to today's #1314/#1315 work (verified by file — none touch anything I edited). Root-caused into 3 groups and filed separately: **#1353** (1 test, `test_github_spatial_temporal` — hardcoded fixture date from ~Aug 2025 asserting "2 days old", now literally 328 days off; a test-hygiene bug, explicitly NOT a spatial-intelligence logic bug, flagged that distinction directly in the issue given spatial's protected status); **#1354** (8 tests in `test_calendar_config_loading.py`, single root cause — `CalendarConfigService.get_config()` requires `user_id` now, tests never updated, all 8 fail identically); **#1355** (5 tests — 3 more in github_spatial + 2 in calendar_integration — each looks like benign drift from separate unrelated refactors, e.g. one stale test still checks for a `CalendarIntegrationRouter` reference in the morning-standup module, which now says its own docstring is "back-compat re-exports" after an apparent standup-feature refactor; needs individual triage, didn't guess at fixes blind).

Also found calendar has a **vestigial dead `MCPConsumerCore()` instantiation** (line 178) — imported, constructed, never called again anywhere in the file. Leftover from before the #1232 port, never cleaned up — exactly the "bespoke code" #1317's own AC says should be retired, and it wasn't, even for the one connector otherwise fully ported.

**The 6 unported connectors split into two distinct blockers, neither of which is "just write the 4 methods":**
- **cicd/devenvironment/gitbook/linear**: all 4 still depend on `MCPConsumerCore`/`MCPProtocolClient` — the *same* simulation-transport stack #1322 already flagged as dead code and partially retired from github (inc.1-3, 6/29). Confirmed for all 4: no `*_MCP_SERVER_URL` config exists anywhere in the repo; `initialize_connection_pool()` is defined but never called from any live path; their spatial wrappers are unreferenced by anything outside themselves/tests (unlike github_spatial, confirmed live). Zero test coverage. This means porting these to the contract isn't really unblocked work — it's gated on a prior, non-coding question: does a live MCP server for these 4 services even exist or is one planned? Flagged this as PM's call, not mine to resolve by writing code.
- **slack/notion**: live in entirely different directories (`services/commands/adapters/`, `services/integrations/mcp/`), use the keychain credential model (ADR-058) rather than the binding model the contract assumes, and notion's existing `connect()` has a completely different signature already. Porting these raises an architectural question (does the binding-model contract even apply to a keychain-model connector) rather than being a mechanical gap-fill.

Also noticed, flagged but explicitly did NOT chase down further (scope discipline — this is a #1317 audit, not a full architecture sweep): gitbook and notion each have a *second*, older adapter copy under `services/integrations/mcp/`, and a whole parallel `services/intelligence/spatial/` tree (2 files) that IS referenced by some live code (`cli/commands/notion.py`, `services/features/notion_queries.py`) — so real duplication exists between `services/intelligence/spatial/` and `services/integrations/spatial/`, unclear which is canonical. Noted in the #1317 comment as a separate, unattempted investigation.

Posted the full findings as a structured comment on #1317 (table + AC-by-AC assessment + the "bigger structural finding" framing) rather than just reporting conversationally, so the evidence has a permanent home. Issue stays open — this was an audit, not a build; PM said "we can discuss" and that's still pending.

## PM refocuses the sprint — one connector at a time, GitHub first (~10:45–11:15)

PM read the #1317 audit and pushed back on the shape of the work, not the facts: "we can never close this sprint until we get those eight connectors done, and we've only done two of them, and neither of those two are working yet." Direct ask: am I losing sight of the forest for the trees? Answered honestly — no contrary read on the core call, and I named a concrete data point supporting it: a real share of "RECONNECT" effort this cycle went into the Slack lane (#1109/#1110/#1201/#1334/#1335), which today's audit found isn't even architecturally on the same model as github/calendar (different base class, different directory, keychain credentials not the binding model) — work that didn't compound toward the 8-connector goal. Proposed github=connector#1/calendar=connector#2 as my reading of PM's numbering (unconfirmed but low-risk to proceed on).

**New execution model**: one connector driven to fully, literally done — contract methods + integration tests green + bespoke code retired + latency checked + coherent onboarding — before starting the next. Sequence: GitHub → Calendar → TBD. PM also answered the #1317 open question directly: cicd/devenvironment/gitbook/linear having no live MCP server today is *expected*, not a red flag — creating/connecting those servers IS the sprint's point (this reframes what "porting" those 4 connectors actually means: it's not just a coding task, it includes building/connecting real MCP servers, plus per-connector onboarding UX at signup, conversational or simple config).

**Actions taken**: (1) memo to Arch (`mailboxes/arch/inbox/memo-lead-to-arch-cc-pm-pa-connector-architectural-alignment-2026-07-04.md`, cc PM+PA) on the architectural-divergence findings from #1317 (slack keychain-model vs binding-model, notion's incompatible `connect()` signature, a gitbook adapter duplicate, the live duplicate `services/intelligence/spatial/` vs `services/integrations/spatial/` tree) — PM's explicit ask of Arch: one shared domain model, exceptions only when genuinely justified by connector nature/job-to-be-done. Sent via `mail-send.sh`, landed clean. (2) Recorded the refocus decision in `docs/internal/architecture/decisions/decisions.log` (2026-07-04 ~10:45 PT entry). (3) Rewrote the carry-forward's top section to lead with this as the governing frame, marking the prior "PRIOR CURRENT" section as superseded-but-kept-for-detail.

## GitHub connector-1 completion pass (~11:15–12:15)

Drove GitHub to fully-working per the new model, rather than treating today's audit findings as a report-and-move-on. Fixed all 3 github-specific failures bundled in #1355 plus #1353 itself — `tests/integration/test_github_spatial.py` is now 12/12 green (was 8/12 this morning):

- **#1353** (`test_github_spatial_temporal`): the `sample_issue` fixture hardcoded an absolute `created_at` date (~Aug 2025) with a comment claiming "2 days ago" — 328 days stale by wall-clock time now. Rewrote the fixture to compute `created_at`/`updated_at`/`milestone.due_on` relative to `datetime.now(timezone.utc)`.
- **`test_github_spatial_quantitative`**: same root cause (the shared fixture) — `analyze_metrics`'s `complexity_estimate` also keys off `age_days`, which the same date fix resolved for free.
- **`test_github_spatial_mcp_adapter_integration`**: asserted `hasattr(adapter, "resolve_from_position")`. Checked `git log --all -S "resolve_from_position"` across every commit in this repo's history — zero hits. This method never existed; the real, designed, implemented inverse is `map_from_position` (part of the actual `SpatialAdapter` Protocol). Fixed the test to check the real name — a stale/typo'd assertion, not a missing feature.
- **`test_github_spatial_backward_compatibility`**: calls `get_issue()` with a bare repo name (no owner) and no resolution context, so `resolve_repo()` correctly raises `UnresolvedRepoError` before `_call_github_api` is ever reached. Verified this is NOT a regression from this morning's #1315 change — checked out the pre-#1315 version of `resolve_repo` via `git show` and confirmed its paths 2/2.5/3 all ALSO required `project_id`/`user_id`, neither of which this test provides either; only the always-present env-var/unresolved paths were ever reachable for this test, both before and after #1315. Mocked `resolve_repo` in the test to isolate its actual concern.

**Also found and removed**: `tests/integration/test_github_deprecation_infrastructure.py` (12 more failures, discovered while verifying the full suite — not originally in scope). This tested a "4-week phased legacy/spatial feature-flag switch" (PM-033b) whose own target router (`GitHubIntegrationRouter`) has a docstring literally stating: *"Week 4: ✅ Legacy code removed (October 15, 2025)"* — confirmed the feature flags it manipulates (`USE_SPATIAL_GITHUB` etc.) aren't read anywhere in the current router, and the "legacy" class it mocks (`GitHubAgent`) doesn't exist anywhere in the codebase. Deleted the whole file — testing infrastructure for a migration that finished 9 months ago is not a judgment call the way removing live/protected code would be.

**Two more issues discovered and filed, deliberately NOT chased to resolution** (tangential to github's own scope, per PM's just-stated "don't lose the forest" instruction): **#1356** (`tests/archive/` isn't actually excluded from pytest collection despite its name — at least one file has a broken import; plus an orphaned test-runner script referencing deleted files) and **#1357** (`test_github_api_connection.py`'s 4 live-credential tests fail with real GitHub 401s only when run in sufficiently large combined test batches — reproduced with several different unrelated file combinations, ruled out env-var pollution and leaked-mock theories via direct empirical checks, landed on "likely GitHub rate-limiting from real API traffic volume, not confirmed" as the honest, bounded conclusion after intentionally stopping the investigation rather than continuing to chase it unboundedly). Corrected my own earlier, too-specific theory on #1357 openly in a follow-up comment rather than let a wrong diagnosis stand.

Closed #1353. Updated #1355 with partial-progress status (3/5 done, github-side; 2/5 — calendar token-filename + stale morning-standup reference — deliberately left for Calendar's turn as connector #2, not forgotten).

**What's left for "GitHub/connector-1 100% done"**: the latency AC ("connect/status latency no worse than current" — no benchmark exists, not measured) and confirming the onboarding UX is coherent (conversational or simple-config at signup — github already has a live OAuth "Connect with GitHub" flow from earlier RECONNECT work, not re-verified today as part of this specific pass). Flagging this rather than claiming full completion — checking in with PM before continuing further given how much ground this turn already covered.

## Correction: this morning's "connector completion" work was scoped to the wrong class (~12:30–13:15)

PM relayed a real, well-founded PPM concern: "we have never really tested with the new connector — all the testing has been with the older infrastructure." Investigated properly rather than either defend or capitulate reflexively — the answer turned out to be genuinely nuanced, and I own a real miss in it.

**What I found, verified against live infrastructure (staging app on :8001 up since Wed 7/1 05:45 — 3 days stale, predates ALL of today's commits; `piper-ghmcp` MCP-server container up 6 days on :8082; real DB `connector_bindings` row for PM's account, `status=bound`, created 6/28, never refreshed since):**

1. **This morning's whole test-fixing pass (#1353/#1355, `test_github_spatial.py`) targeted `GitHubSpatialIntelligence`** (`services/integrations/spatial/github_spatial.py`) — which `GitHubIntegrationRouter`'s own docstring explicitly labels **"direct API + spatial intelligence" — FALLBACK**, not the new MCP connector. I read that docstring multiple times today without connecting that fixing ITS tests wasn't the same as verifying the real connector. That's the real miss — not "PPM is wrong," but "I was testing the wrong class and didn't catch it."

2. **The actual new MCP connector (`GitHubMCPSpatialAdapter`, `services/mcp/consumer/github_adapter.py`) genuinely does work, live, right now** — ran a direct script from this worktree (today's code, bypassing the stale 7/1 server process) calling `status()` + `search_user_repositories()` against PM's real bound account (`a25db09c-...`). Got back `ConnectorStatus(state=BOUND)` and 5 REAL repos (`mediajunkie/piper-morgan-product`, `optilisten`, `designinproduct`, `openlaws`, `piper-morgan-website`) with real creation dates — a genuine live MCP round-trip through the real `github-mcp-server` container, using the real stored OAuth grant. This directly contradicts the *strongest* form of "we've never tested the new connector."

3. **But the class is split down the middle, and that's the real source of the confusion.** `GitHubMCPSpatialAdapter` contains TWO distinct method families in the same file: (a) real MCP-transport methods (`_mcp_client_ctx` + `call_tool`) backing `connect`/`resolve`/`search_user_repositories`/`list_open_issues`/`list_open_prs` — confirmed wired into chat via `_handle_list_issues_query` in `intent_service.py`, connector-first with native-PAT fallback only on `CONNECT_REQUIRED`; and (b) direct-REST-API methods (`_call_github_api`, hitting `api.github.com` directly) backing `list_github_issues_direct`/`get_github_issue_direct`/milestones/releases/labels/branches/repo-info — all still called by `GitHubIntegrationRouter` for those specific capabilities, EVEN for OAuth-connected users. So: issues/PRs/repo-search ARE on the real connector and chat-verified; milestones/releases/labels/branches/single-issue-lookup are NOT — they're still on the old direct-API rail, just living inside a class whose name says "MCP."

4. **The 6/28 live-verification (in carry-forward history) was real** — that's where today's proof-of-life traces back to (the binding's `created_at` matches) — but it's 6 days stale and has not been re-confirmed since, across everything that's changed in the interim (my own #1315/#1314 work this morning included).

Told PM this plainly rather than smoothing it over. Next: send PPM the requested sprint-status update, with this correction as the headline finding (more consequential to their beta-blocker evaluation than test-pass counts), then continue RECONNECT work — reframing "what's unlocked" to include a proper systematic audit of which GitHub chat capabilities are real-MCP vs still-direct-API, since that's now clearly the load-bearing open question, not a side detail.

## PPM's own memo arrived independently — reconciled two conflicting factual claims (~13:15–13:35)

Before I could send PPM anything, their own memo landed (`memo-ppm-to-lead-cc-pm-reconnect-validation-gap-2026-07-04.md`, cc PM): PPM ran their own investigation and concluded **"PM has no ConnectorBinding row"** and **"#1317 increment 2 — OAuth redirect-orchestrator + callback... not yet built."** This directly contradicted what I'd just verified myself minutes earlier (a real, live, working binding + a real live MCP round-trip for PM's account). Rather than pick a side or silently defer to whichever came first, verified both claims precisely:

- Queried the DB directly for PM's real account (confirmed via `users` table: `xian@pobox.com`, last login 6/30 — not a test fixture): a `connector_bindings` row DOES exist, `status='bound'`, created 6/28. PPM's claim doesn't hold **for local dev/staging**.
- BUT: confirmed (from my own 7/3 decisions.log entry, written during yesterday's #1344 deploy) that **production's database doesn't have the `connector_bindings` table at all** — the #1229 migration never shipped there. PPM's claim holds completely **for production** — the table structurally doesn't exist to query, so any interaction with the real hosted product can never see a binding, full stop.
- Checked #1317 inc.2's actual build status directly: `web/api/routes/settings_integrations.py` has `/github/connect` + `/github/callback`, both explicitly commented `# ── GitHub connector OAuth (#1317 inc.2 / ADR-070 option C) ──` — I edited the callback route myself this morning for unrelated #1314 work. **The code exists and works** (verified live against local staging) — PPM's "not yet built" doesn't hold as literally stated; the accurate framing is "built, verified locally, never deployed to a production DB that has the table it needs."

**Net reconciliation**: PPM's structural conclusion (production can't validate RECONNECT) is correct and is the real beta blocker — but the mechanism is a **deploy/migration gap**, not a **build gap**. That's a materially different, better-bounded remaining-work estimate (ship an existing migration + release cut, not build an OAuth flow from scratch) — important for PPM's evaluation to get right.

Sent PPM a single reconciliation + status memo (`mailboxes/ppm/inbox/memo-lead-to-ppm-cc-pm-reconnect-status-and-validation-gap-reconciliation-2026-07-04.md`, cc PM) covering: the reconciled facts above, today's full RECONNECT/GitHub-connector-1 status, and the two now-separate blockers (deploy gap vs. the milestones/releases/labels/branches/single-issue capabilities still on the old direct-API rail even inside the "new" adapter class). Did not characterize this as "PPM was wrong" — their finding was accurate for the one environment that actually matters for beta (production); I just had visibility into local-staging state they didn't have reason to check separately.

## Self-caught overclaim in the memo I'd just sent — corrected within the hour (~13:50)

Moving into "continue RECONNECT work," went to actually migrate the "coverage gap" I'd just told PPM about (milestones/releases/labels/branches/single-issue still on direct API) — and immediately found I'd only checked `GitHubIntegrationRouter`'s own methods, not whether `intent_service.py` bypasses the router entirely for these (the way it already does for issues/PRs/repos, which I HAD verified). It does: `_handle_list_branches_query` → `list_branches_connector()`, `_handle_list_releases_query` → `list_releases_connector()`, `_handle_review_issue_query` → `get_issue_connector()` — all real MCP, all already built (#1327 gap 2), just not methods I'd checked before writing the memo. Live-tested `list_branches_connector` myself to be sure rather than trust the grep alone: 30 real branches back from the real repo, real MCP round-trip.

Only labels and milestones are genuinely native-only — and that's correct, not an oversight: the adapter's own code comment documents that `github-mcp-server` has no list-tool for either (labels exposes only `get_label`, one at a time; a `list_label` connector cutover was tried once and reverted after confirming live it returned "unknown tool"). Sent an immediate correction to PPM/PM (`mailboxes/ppm/inbox/memo-lead-to-ppm-cc-pm-correction-branches-releases-issue-lookup-ARE-on-connector-2026-07-04.md`) fixing this before it could shape their evaluation on a wrong number — the real remaining gap is much smaller than what I'd just told them. Net: GitHub's own application code is in materially better shape than my last two hours of framing suggested; the deploy gap (production missing the migration) remains the one real, large, separate blocker.

## Checked Calendar (connector #2) with the same rigor — found something significant, stopped to report rather than push forward alone (~13:55–14:15)

Applying today's lesson (trace to what `intent_service.py` actually calls, don't stop at the adapter class's structural declarations), started characterizing Calendar the way I'd just properly done for GitHub. Found something that changes the "connector #2" picture materially, not just a smaller version of GitHub's gaps:

- **Zero calendar connector bindings exist in the DB** — queried directly: `SELECT * FROM connector_bindings WHERE connector='calendar'` returns 0 rows. Never bound, not even once, not even locally (unlike GitHub's real 6/28 binding).
- **No calendar-mcp-server container exists or has ever run** — `docker ps -a` has no calendar-related container at all (only `piper-ghmcp` for github, plus redis/chromadb/postgres).
- **`GoogleCalendarMCPAdapter`'s real, live event-fetching (`get_todays_events`, the thing chat actually calls via `CalendarIntegrationRouter`) is built entirely on `Credentials.from_authorized_user_file()` / `_authenticate_from_keychain()` → a direct `googleapiclient` `build("calendar", "v3", ...)` service object** — the classic direct-OAuth2-API path, with ZERO MCP transport involved. Of the adapter's 4 declared #1232 contract methods, only `resolve()` even references `_mcp_client_ctx`/`call_tool` — `connect()`/`status()`/`degrade()` and every real data-fetching method are untouched by MCP transport.

**Net**: Calendar's `IMPLEMENTS_CONNECTOR = True` declaration (which satisfies the m-41 AST-guard and is why this morning's #1317 audit counted it as "2/8 ported" alongside GitHub) is structurally present but not backed by any real external MCP server or any exercised binding — closer in actual architecture to the keychain-model connectors (Slack/Notion, already flagged to Arch) than to GitHub's genuinely-live MCP setup. This is NOT "connector #2, smaller version of GitHub's gaps" — it's a different starting condition that may need the same architectural reconciliation I already asked Arch about for Slack/Notion, not just test-fixing + coverage completion.

Stopped here rather than push further alone — this is a significant enough finding (changes the shape of "connector #2," not just its completion percentage) to report to PM directly before continuing, rather than keep independently investigating or start "fixing" it on an assumption that might not hold.

## PM: send PPM the main-vs-production distinction explicitly, keep characterizing Calendar (~14:20–15:15)

PM confirmed the Calendar catch, asked me to make the main-vs-production release model unambiguous for PPM specifically (not just implicit in the earlier memos), and to keep going on Calendar's real scope in parallel with the architecture question, not blocked on Arch.

**Main-vs-production memo sent** (`mailboxes/ppm/inbox/memo-lead-to-ppm-cc-pm-main-vs-production-release-model-2026-07-04.md`): pulled real numbers rather than round them — `origin/main` is 1,211 commits ahead of `origin/production` right now; production's last update was yesterday evening's #1344 deploy; production carries 10 commits of its own (version bumps/release notes, normal for this model). Framed the general principle PPM needs for every future readiness question, not just this one: "built and verified on main" and "live in production" are independent claims, ask both separately.

**Calendar characterization — found this is a known, already-tracked gap, not a surprise, and it's more bounded than my first-pass framing suggested.** Read #1220 (RECONNECT WS-8, still OPEN, P1) in full — this is the umbrella issue that EXPLICITLY names exactly the split I found empirically: "data/operations → already MCP-consumer, default-on... connection/auth → still bespoke" for calendar specifically, dated 2026-06-14. So this isn't new information I discovered — it's the precise gap WS-8 exists to close, and GitHub (via #1317/#1322/#1327) is the one connector where that closing work has actually happened so far.

**More importantly**: `docs/internal/architecture/connector-refactor-sprint-scope-2026-06-14.md` (Arch's own planning doc) states directly: **"GitHub/Calendar are well-served; Slack/Notion need a maturity check"** for MCP-server maturity, and separately: **"GitHub + Calendar first (the M3/M4-relevant pair) with Slack/Notion to follow."** This means Arch's own 6/14 assessment already believed a viable external MCP server option exists for Calendar (unlike Slack/Notion, which genuinely need investigation) — what's missing isn't "does a server exist," it's provisioning one locally (parallel to how `piper-ghmcp` was stood up) and doing the same binding+OAuth-flow build #1317 did for GitHub. This also confirms GitHub-then-Calendar was ALREADY the planned sequencing before today, not an arbitrary PM call this morning.

**Also confirmed Calendar's existing bespoke auth is genuinely mature, not a hack**: `_authenticate_from_keychain()` uses a per-user keychain key (`google_calendar_{user_id}`) via a real web-OAuth setup wizard (#529); a prior bug where User B could silently inherit User A's token via a shared fallback key was found and fixed (#843/#917) by removing the global-key fallback entirely. So Calendar already has real, working, correctly-isolated multi-tenant OAuth — it's just on the pre-#1317 architecture, not broken or single-user.

**Test status**: ran calendar's own connector-contract test suite (`test_calendar_connector_1317.py`) — clean, including its explicit test that an unprovisioned binding honest-degrades to UNREACHABLE (the provisioning gap is intentionally tested-for, not accidentally undiscovered). The only failures in calendar's integration suite are the already-tracked #1354 (8 tests, `get_config()` signature) and 2 of #1355's original bundle (token-filename + stale standup reference) — nothing new.

**Net characterization for PM/PPM**: Calendar's remaining scope to reach GitHub's level is the SAME SHAPE of work #1317+#1322+#1327 already did for GitHub — provision a real MCP server, build the binding-creation OAuth flow, migrate event-fetching off direct googleapiclient calls onto `_mcp_client_ctx`/`call_tool`. Not a bigger, novel undertaking — a repeat of an already-proven playbook, on a connector Arch already assessed as viable for it. And because Calendar's current bespoke auth is real and properly multi-tenant, this is an architectural-consistency goal more than a "beta is broken today" risk, unlike the GitHub production-deploy gap.

## PPM's follow-up questions — answering them surfaced a genuine gap in my own earlier framing (~15:30–15:50)

PPM replied to the reconciliation memos with two specific, sprint-blocking questions before finalizing Beta Blockers: (1) #1317 inc.2's actual issue status/AC/sprint placement; (2) whether production's `github-mcp-server` provisioning (stdio-local vs. hosted) is decided/implemented, or whether a real beta tester would even have a server to connect to. Investigated both properly rather than answer from memory.

**Q1 answer**: there is no separate "#1317 increment 2" issue — it's a code-comment convention inside the single #1317 (OPEN). #1317's own AC is scoped entirely to code/tests, not deployment. #1229 (the migration) is CLOSED but on local-only verification. Checked #1299 (the other candidate) — unrelated, it's 6/20's deploy-tooling hardening issue. **No existing issue currently tracks "ship this to production"** — told PPM this plainly and left the sprint-mechanics call (file new vs. repurpose vs. something else) to them.

**Q2 investigation surfaced a real correction to my own earlier "bounded deploy gap" framing.** Read #1220's full body (not just the top half I'd read this morning) and ADR-070's D6 (MCP-server maturity tiers): GitHub + Calendar are both "Tier 1: well-served, published, stable" architecturally — the ADR-052-vs-"MCP server owns auth" tension IS cleanly resolved (D2: two distinct boundaries, Piper-as-host vs. Piper-as-consumer, no real conflict). But **the practical production hosting/provisioning decision has NOT been made or implemented** — checked directly: `github-mcp-server` isn't defined in `docker-compose.yml` on either `main` or `production`; the only running instance anywhere is a manually-started local container on this dev machine, outside any codified config. #1220 itself says "Status: In Progress (blocked on #1232 + governing MCP-consumer ADR)."

**Net**: the deploy gap I described this morning as "bounded — ship an existing migration + release cut" is actually TWO pieces: (1) that migration/release cut, still accurate and bounded; (2) deciding + implementing how production actually hosts or reaches an MCP server at all — genuine infrastructure work I hadn't checked before calling the whole thing bounded. Sent PPM a direct correction (`mailboxes/ppm/inbox/memo-lead-to-ppm-cc-pm-answers-plus-a-refinement-deploy-gap-is-two-pieces-2026-07-04.md`) rather than let the rosier framing stand — didn't attempt to size piece 2 without being asked, flagged it as unscoped.
