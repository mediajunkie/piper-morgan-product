# Session Log: 2026-07-08-0647-lead-code

**Role**: Lead Developer
**Model**: Claude Code (Fable — carried over from PM's 7/7 switch; correct per the log-header discipline until PM changes it)
**Date**: Wednesday, July 8, 2026
**Start Time**: 6:47 AM PT

## Session Objectives

1. **#1306 build** (file-content encryption) — this fresh session is its named quality-banking trigger; complete Arch-ratified spec in the carry-forward (single seam + exhaustive read-site inventory + drift guard).
2. **Support PM's alpha deploy** whenever it starts (runbook committed last night: `docs/internal/operations/deploy-runbook-alpha-2026-07.md`) — then close #358 on dimension-A evidence + update #1299b.
3. Then per the agreed order: #1258 → #1220 build → Epic G audits.

**Active pattern families**: Completion Theater (045/046/047/049) — issue closes with evidence; Investigation (006/041-043/060) — the read-site inventory.

## Work Log

### 6:47 AM - Session Start (START, duty-cycle-tick)

06:17 slot fired ~30 min late (normal). One cron (`fcda3c19`), no dedup. Step-0: 7/7's log has its `DAY-CLOSED` marker (grep-confirmed) — clean close, no retroactive work. Synced clean; overnight commits are Docs's omnibus/audit/merge-keeper work + a watchdog "duty-cycle stall — arch lead" alert (my cron is armed and firing — the alert likely predates this fire's late wake; noting, not chasing). One unread in lead inbox: Docs's merge-keeper escalation (6 stranded branches, disposition to PM, cc me) — triaging in the mail loop next. No deploy signal from PM yet.

### 7:00 AM - #1306 BUILT + CLOSED — Epic B fully built. 9 open.

**The inventory (Arch's load-bearing condition) found 7 sites — one more WRITE than the design memo knew**: the upload route bypassed `save_file_to_storage` with a raw `open(...,"wb")` — the "single write seam" was already false before encryption entered. All routed: `write_file_to_storage` (encrypt) + `read_file_from_storage` (sniff-decrypt, legacy read-through, keyless-fail-closed) are now the only byte paths (2 writes: save-helper + upload route; 5 reads: document_handlers ×3, the analyzer, files.py preview + zip). Added bytes-native `encrypt_bytes/decrypt_bytes` to `FieldEncryptionService` (binary files; no base64 detour) — smoke-proven round-trip + context isolation before wiring.

**The drift guard is real**: `TestUploadedFileByteSeamEnforcement` — raw byte IO in any storage-path-handling file fails the build with exact offender+line. Injected-regression-proven (planted `read_bytes()` → guard named `files.py:687`; restored → green). Backfill: DB-known files, in-place, idempotent, refuses keyless — joins the deploy checklist (now FOUR commands; runbook + #358 comment updated).

**Verification**: 5 seam tests + guard; 523-test sweep (security/enforcement/routes/document-handlers/file-upload) — only #1350's 3 known template checks fail (3rd confirmation this week); `web.app` imports. One vestigial test stub caught + deleted before commit (a fixture-ordering experiment I'd left as `pass`).

Deploy watch: no PM signal yet — runbook Phase 5 now lists all three backfills.

### 7:10 AM - #1258 FIXED + CLOSED (same fire, drained while hot). 8 open.

`strip_empty_anthropic_vars()` (`services/utils/env_hygiene.py`, called in main.py before load_dotenv) — the issue's own proposed shape, extracted testable. Deliberately empty-only: a non-empty BASE_URL may be a legitimate hosted gateway; the local Claude-Code-shell non-empty-proxy nuance stays on CLAUDE.md's `env -u` (now belt-and-suspenders). **Proven live in the exact pre-fix failure mode**: launched with `ANTHROPIC_API_KEY=""` in the environment → `anthropic: Valid … 1/1` (previously a guaranteed APIConnectionError on every call). 2 unit tests. Beta-blockers: Epic D → #1299b only (+#1278 backlog); count 9 → 8.

**Remaining 8**: #358 (deploy dimension-A check), #1220 (next build), #1299b (the deploy verifies it), #1278 (backlog), #1332 (reproduction-gated), #1283/#1312/#1324 (Epic G audits). Next: #1220's build — headroom pre-flight needs droplet access, so its LOCAL half (compose service + env template + the write-path credential migration investigation) can start; the droplet half rides PM's deploy-day access.

### 9:19 AM - Fire: #1220's hosting half IMPLEMENTED (compose sidecar, live-smoked); write-path migration banked as its own unit.

**Compose sidecar shipped**: `github-mcp` service in docker-compose.yml mirroring the proven 6/28 container EXACTLY (image/command/port read from `docker inspect piper-ghmcp` — verified, not invented); app env gains `GITHUB_MCP_SERVER_URL=http://github-mcp:8082/mcp` (compose DNS). No token env on the sidecar BY DESIGN (ADR-070 D3/OQ-5: per-user OAuth per connection, never a shared credential — the comment in compose says so). Compose file validates; started it live from compose: container up, and a tokenless MCP initialize gets **"Unauthorized" — the HEALTHY response** (per-connection auth enforced; connection-refused would be the sick one). One narrow removal en route: the old EXITED manual dev container held the name — verified stopped + superseded-by-identical-compose-definition before `docker rm` (the narrow thing, not a sweep). Stopped the sidecar after (local baseline restored; the compose definition persists). Droplet enablement is now AUTOMATIC on deploy (deploy.sh's compose up starts it) — runbook Phase 3 updated with the Unauthorized-is-healthy smoke; #1220 progress comment posted.

**Write-path credential migration banked as its own fresh-window unit — named trigger + named reasons**: it carries the standing hard gate (deterministic action-success guard BEFORE any user-facing write cutover, #1322's 7/1 ruling — a real design piece, not a mechanical swap), the PM-present first-real-write rule, and 4 call surfaces (router create/update/add_comment + the direct-REST summarize fetch at intent_service.py:9165). Not bite-sizing: this fire completed the hosting half whole; the migration is a genuinely separate multi-hour unit with a PM-interactive tail.

### 9:46 AM - PM batch: "handle #1220 and Epic G while I do morning business; deploy later today with undivided attention." #1220's write-path migration BUILT.

**The #1322 hard gate, designed then implemented**: `GitHubWriteResult.verified` = True ONLY on a `get_issue` read-back through the SAME connector session confirming the artifact's expected state — never from the write response alone. Comments additionally require a GitHub-minted comment `id` (caught my own first draft overclaiming: issue-existence would have "verified" a comment that never attached). **The double-write hazard is typed, not commented**: `attempted=False` (pre-call rail degrade — the write never fired) is the ONLY state licensing native-PAT fallback; fired-but-unverified RAISES honest uncertainty — no retry through different credentials (a mid-flight timeout may have landed the write; a second write through the PAT would duplicate it). Router create/update/add_comment: connector-first via the stashed user_id, native unchanged otherwise.

**Verification the strong way**: 11 tests against a REAL in-memory MCP round-trip (the #1322 FastMCP fixture pattern, extended with a STATEFUL fake repo — create→readback genuinely round-trips; every failure mode comes from breaking the repo, never from mocking the guard's own logic). Fixed a real bug the tests caught: stdlib-logging kwargs (structlog idiom in a logging module) made the except-handler itself raise — the two "failures" were the guard working while its logger blew up. 776-test sweep clean. Committed + pushed; #1220 comment posted. **Remaining on #1220: the PM-present first REAL write (post-deploy, throwaway) + the deploy starting the sidecar — both land today.** (Fable re-confirmed by PM ~10:00 after a rate-limit blip.)

Now Epic G: #1283 → #1324 → #1312-start.

### 10:30 AM - Epic G part 1: #1283 static audit DONE (corpus + Arch memo sent); #1324 audit WORKED — both blockers fixed.

**#1283 (routing integrity)**: three-vocabulary reconciliation run as code, not eyeball — prompt-vocab (17 actions) vs `ACTION_REGISTRY` (43) vs dispatch rail (86 keys). Key reframe: the 61 rail-only aliases are **load-bearing mode-4 defense** (they catch undocumented LLM emissions), not dead code — so the SSOT recommendation is *generate/validate aliases from the registry*, never prune. Deliverables: `tests/fixtures/routing_corpus_1283.yaml` (~29 phrase→expected rows, the behavioral-probe contract) + memo to Arch (cc PM) with the AC-4 SSOT design ask. Behavioral probe is LLM-cost — runs post-deploy after Arch's corpus pass.

**#1324 (hardcoded-config audit)** — every finding verified at the site, fixed-or-dispositioned:
- **Blocker 1 FIXED**: `main.py` `PIPER_BASE_URL` env-overridable (health-check + printed URLs were silently wrong off-box; localhost default preserved for local runs).
- **Blocker 2 FIXED, better shape than the issue asked**: the 3 OAuth redirect fallbacks (Slack ~382, Google ~997/~1063 in `settings_integrations.py`) now **derive from `PIPER_BASE_URL`** via `_base_url()` instead of hardcoded localhost — one env var makes all three correct on the droplet, no new hard-fail mode (the issue's "require in non-dev" would have added a crash path; derive-from-base is correct-by-default).
- **SHOULD-FIX password guard SHIPPED**: `warn_if_prod_uses_dev_password()` in env_hygiene, called at startup — CRITICAL log when production runs `dev_changeme_in_production`. Deliberately warn-not-crash (compose may inject the real password at a layer the check can't see; false-positive hard-exit would take the service down). **Aligned to the codebase's canonical env pair** (`PIPER_ENVIRONMENT`/`ENVIRONMENT` — same read order as jwt_service's prod guard; the issue said "APP_ENV" but that name appears nowhere in the codebase — verify-before-extend caught it). +3 tests (20/20 utils).
- **SHOULD-FIX dedup DEFERRED**: the DB-URL duplication is *deliberate* (documented in-code: avoids circular import); consolidation = real DB-layer refactor, wrong day for it (deploy-day churn). → follow-up issue.
- **SHOULD-FIX setup.py N/A — finding doesn't hold**: those port fallbacks feed the wizard's connectivity *probes*; misconfiguration fails visibly in the UI. Detection already exists.
- **SHOULD-FIX byoc N/A for this repo**: `byoc/mcp/server.py` doesn't exist here (no `PIPER_AUTH_USERNAME` in any .py on the whole tree) — it's the skunkworks POC surface; noted on issue.
- **MINOR (GITHUB_API_URL, gpt-4o fallback) DEFERRED**: GHE enablement (zero beta users) + LLM-fallback behavior change — wrong day; → follow-up issue.
- **Runbook Phase 3 updated**: `PIPER_BASE_URL=https://alpha.pipermorgan.ai` added to the env block, PLUS the coupled-behavior warning I found while verifying: setting `ENVIRONMENT=production` arms jwt_service's hard-fail on missing `JWT_SECRET_KEY` — the runbook now says check-don't-improvise.

Tests: utils+routes sweep 396 passed; only #1350's 3 known template checks fail (pre-existing, 4th confirmation this week).
