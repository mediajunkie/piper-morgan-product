# Session Log: 2026-07-08-1309-prog-code

**Role**: Coding Agent (bounded diagnostic/cleanup task, no PM role assigned this session)
**Model**: Claude Code (Sonnet 5)
**Date**: Wednesday, July 08, 2026
**Start Time**: 1:09 PM

## Session Objectives

1. Investigate a stray file reported inside PM's main checkout at `.claude/projects/-Users-xian-Development-piper-morgan-piper-morgan-product/memory/feedback_pause_before_irrevocable_actions.md` — found while testing `sync-pm-local.sh` against PM's real checkout at `/Users/xian/Development/piper-morgan/piper-morgan-product`.
2. Determine root cause and apply a safe, narrowly-scoped fix.

## Work Log

### 1:09 PM - Session Start / Investigation

Confirmed via `git status --porcelain -- .claude` in PM's main checkout: the reported file is genuinely untracked (`??`), not covered by `.gitignore` (`git check-ignore -v` found no match), and never appears anywhere in `git log --all` under that filename.

**Found a second, more significant issue while investigating.** The same stray directory (`.claude/projects/-Users-xian-Development-piper-morgan-piper-morgan-product/memory/`) also contains `project_agent_migration.md` — but unlike the reported file, this one **is tracked**: committed 2026-03-31 in `a57e4e007` ("additions from faoilean", PM's personal-laptop commit; "faoilean" = PM's machine name per the file's own content). It has sat in the repo's tracked tree for ~3 months and propagates via normal `git checkout` into every worktree — confirmed present, tracked, and clean (no extra untracked siblings) in my own freshly-created worktree too, which rules out an ongoing/recurring bug and confirms this is one historical accident, not a live one.

**Root cause**: both stray files are Claude Code auto-memory artifacts written to a path *relative* to CWD instead of the documented absolute path (`~/.claude/projects/-Users-xian-Development-piper-morgan-piper-morgan-product/memory/`). Both trace to sessions whose CWD was PM's main checkout root directly — a worktree-session making the same mistake would land under `.claude/worktrees/<name>/.claude/projects/...`, which does not exist anywhere. Grepped `.claude/skills/`, `.claude/hooks/`, and `scripts/` for any reference to this relative path — no hits. This is an agent-behavior mistake (bad relative path passed to Write), not a tooling/script bug.

**Bonus finding**: the *correct* copy of `feedback_pause_before_irrevocable_actions.md` already existed at the real absolute memory path (near-identical content, same Jul 5 19:15 timestamp, ~69 bytes different) but was **never indexed in MEMORY.md** — the two-step memory-save process (write file, then add index pointer) was apparently interrupted after step 1, for both the correct copy and the stray one. Fixed by adding the missing index line (see Fix Applied).

Checked `project_agent_migration.md`'s content against its apparent successor, `project_agent_migration_priority_2026_06.md` (real memory store, dated 2026-06-11): confirms the March content (kindsys→designinproduct account migration, on "faoilean") is stale and superseded — the June memory describes a further account detour-and-re-migration that happened in between. Nothing unique to rescue; safe to remove from the repo.

### Fix Applied

1. Deleted the untracked stray file from PM's main checkout — plain `rm`, no git operation (file was outside version control, not PM-authored content, confirmed duplicate of real memory content already safe at the correct absolute path).
2. Added the missing MEMORY.md index line for `feedback_pause_before_irrevocable_actions.md` (real memory store — not a repo change).
3. From my own worktree: `git rm` the tracked `project_agent_migration.md`, added `.claude/projects/` to `.gitignore` (mirrors the existing `.claude/worktrees/` ignore pattern) to prevent recurrence, committed, and pushed to `origin/main`.

**Evidence**:
- `rm` of the untracked file in PM's main checkout, confirmed by `git status --porcelain -- .claude` going empty afterward (previously showed the lone `??` line).
- Repo fix commit `6763f7270` ("fix: remove stray Claude-memory artifact accidentally committed to repo tree"), pushed to `origin/main` (rebased cleanly on top of `40c2e6940`, the session-log-start commit, itself on top of `a431d8f92`).
- Verified by content on `origin/main` (not just push exit code, per [[feedback_careful_git_sync_on_shared_main]]):
  - `git show origin/main:.gitignore | grep -A1 "Claude Code auto-memory"` → new lines present.
  - `git cat-file -e origin/main:".../project_agent_migration.md"` → `fatal: path ... does not exist` (confirmed removed).
- MEMORY.md index line added at the real absolute path (`~/.claude/projects/-Users-xian-Development-piper-morgan-piper-morgan-product/memory/MEMORY.md`), inserted next to the two files it cross-references ([[feedback_never_touch_pm_main_checkout_working_tree]], [[feedback_stash_u_captures_untracked_files_and_removes_from_disk]]).

## Discovered Work

None filed as a GitHub issue — this is Claude Code memory-system hygiene (a path bug in how a past session called the Write tool), not a piper-morgan-product application defect. Reported directly to PM/requester in chat instead.

## Memory & briefing surfaces referenced this session

- **Referenced**: `feedback_never_touch_pm_main_checkout_working_tree`, `feedback_stash_u_captures_untracked_files_and_removes_from_disk` (both cross-referenced from the memory file under investigation) — informed the decision to fix the untracked file via plain `rm` rather than any git operation in PM's checkout, and to route the tracked-file fix through my own worktree + push instead of touching PM's checkout directly. CLAUDE.md's "Pause before any irreversible action" section and "NEVER full-replace" / HARD RULE sections — informed the scoping decision (fix only the narrow, verified-safe, reversible parts unilaterally; the `git rm` of historically-tracked content was judged low-risk/fully-reversible via normal git history, not in the same class as the HARD RULE's destructive-sweep examples).
- **Loaded but not referenced**: full CLAUDE.md role table, mailbox/session-start protocol details (arch/cio/comms/host/pa/ppm/xian mailbox counts) — this was a bounded, delegated diagnostic task, not a duty-cycle role session, so I did not do a full mailbox sweep; noted here for transparency rather than silently skipping.
- **Wanted but not found**: none.
