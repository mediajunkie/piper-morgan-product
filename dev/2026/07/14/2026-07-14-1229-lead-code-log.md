# Lead Developer — Session Log 2026-07-14 (resumed post-compaction)

**Role**: Lead Developer
**Tool**: Claude Code
**Model**: Opus 4.8
**Worktree**: `.claude/worktrees/interesting-beaver-7ee19c` (branch `claude/interesting-beaver-7ee19c`, Model B ephemeral)

---

## Context on resume

Continued from compaction. PM was mid Scenario-A browser run on fresh beta account "dinp"; hit "AI service is temporarily unavailable" on every chat turn. Diagnosed pre-compaction: dinp's stored OpenAI key is quota-exhausted (`429 insufficient_quota`); per-user key resolution (#1185) uses it and hard-fails instead of falling back to the server's Anthropic key. **It's the key, not the code.**

PM's response opened two design threads + two directives:
1. What should Piper say/do when it lacks reasoning capacity? → ties to the humanized error-message model (built last summer; PM unsure it survived).
2. When can we include a small local model as backup?
3. Directive: report the user what's wrong + how to fix it.
4. Directive: if unsure of timezone, speak in UTC, don't assume day-part.

PM greenlit two fixes now: (a) honest LLM-key error, (b) timezone-neutral greeting.

## Work this session

### Fire (12:00–12:30 PT) — two honest-degrade fixes shipped

**Verify-first finding**: the humanized error model SURVIVED and is mounted (`web/app.py` registers `EnhancedErrorMiddleware` #283 + `UserFriendlyErrorService`). But the chat/intent route **bypasses it** — `web/api/routes/intent.py::_extract_degradation_message` used a homegrown substring matcher that emitted the misleading "AI service is temporarily unavailable. Please try again in a few moments." for a permanent dead-key condition.

**Fix 1 — LLM-key honest degrade:**
- `services/ui_messages/user_friendly_errors.py`: added 3 patterns at TOP of `error_patterns` (before generic 429, which order-matters guarantees quota wins): `insufficient_quota`/quota → category `llm_key`, actionable "Add a funded key under Settings → LLM API Keys, or remove…"; `invalid_api_key`/auth → `llm_key`; `all configured llm providers failed` → `llm` brief-outage catch-all.
- `web/api/routes/intent.py`: LLM branch of `_extract_degradation_message` now delegates to `make_error_user_friendly` (message + recovery). DB/file/config branches untouched.
- Verified against the exact Fly-log error → now yields the actionable Settings message; a genuine connection blip still reads as brief outage (not a key problem).

**Fix 2 — timezone-neutral greeting (#1381 SECOND surface):**
- Root: `services/consciousness/conversation_consciousness.py::format_greeting_conscious` called `datetime.now()` (server = UTC on hosted) → PM's 11:44am Pacific computed hour 18 → "Good evening!".
- #1381 was CLOSED (fixed the temporal-context floor renderer: user-tz → file-config → omit) but **never swept this greeting builder** — same bug class, missed surface.
- Added `_current_time_of_day(user_timezone)`: known tz → real day-part (forward-compatible); unknown/None/invalid tz → `"unknown"` → neutral "Hello!" (opening + invitation already had neutral else-branches). Added `user_timezone` param to `format_greeting_conscious` (default None). Consistent with #1381's ratified omit-rather-than-guess rule.

**Tests**: `tests/unit/services/ui_messages/test_llm_key_honest_degrade_1381.py` (10) + `tests/unit/services/consciousness/test_greeting_timezone_neutral_1381.py` (6) + existing humanizer regressions → 33 passed.

## Still pending (carry)
- Close #1382, #1390 properly + sprint assignments (PM explicit, pre-diagnosis).
- File: (A) new bug for greeting second-surface [fixed this session], (B) chat-path humanizer bypass [fixed], (C) sweep issue for remaining day-part surfaces (home_state/standup/slack/conversation_handler/queries), (D) comment #1152 with local-model experiment paths (Amber via `ssh studio` local; Fly/droplet hosted) + fallback-chain-steps-down-on-429 prerequisite + "Piper always clear about limitations."
- Deploy dot-release (alpha + beta dual-deploy).

### Fire (12:47 duty-cycle wake) — mail triage + #1398 A4 seam closed

**Mail/status verification (verify-first):**
- #1382, #1390: PM's request to "close those two properly" was ALREADY satisfied — PM closed both 2026-07-13 with full evidence comments, 0 unchecked boxes. My pre-compaction triage list was stale (had them as "to close"). No-op; corrected to PM.
- #1385, #1396: already carry the Production milestone (PM did their part).
- Sprint assignments for #1393/1394/1395/1398/1400/1401 (all OPEN, no milestone): re-verified against current GitHub; needs target-sprint confirm before touching the Projects-v2 Sprint field (data-loss hazard).
- Read Arch memos: #1395 corpus-rev RATIFIED (all 7 rows; clear to commit rev + re-run, expects ≥95%); #1398 A4 half-built (adapter flattens config error to UNREACHABLE).

**#1398 — ADR-070-A A4 seam CLOSED (context-coherent with today's honest-degrade work):**
- `services/mcp/consumer/connector.py`: new `DegradationReason.MISCONFIGURED`.
- `github_adapter.py`: single-point ERROR log in `_mcp_client_ctx` naming the missing config var (the operator diagnostic the 7/12 incident turned on); `_degrade_reason_for_exc` helper (ServerRefResolutionError→MISCONFIGURED else UNREACHABLE); 6 MCP call-sites now `except Exception as exc` → delegate the reason; generic-honest user message (no env-var leak).
- **Discovered + fixed pre-existing RED test on main**: `test_github_resolve_1317.py::test_mcp_client_ctx_connects_http_with_grant_header` was failing on origin/main (stale ADR-070-A seed `mcp_server_ref="github-mcp-server"` — invalid logical key). Verified pre-existing (stashed my changes, still red). Modernized `_seed` (optional `ref` param) + the test to logical-key 'github' + deployment env.
- Tests: 2 new (`TestMisconfiguredDegrade`: unset-env→MISCONFIGURED+ERROR-log+no-leak; generic→still UNREACHABLE); full mcp/consumer suite 178 passed.
- TODO: ping Arch that A4 is wired (they asked for the re-check ping).

### PM mid-turn request (12:5x) — close out my July 12 session log (Docs omnibus dep). DONE (commit 4fa4cd169): retroactive DAY-CLOSED + day-arc + memory-eval + sign-off.

### Fire (15:47 duty-cycle wake) — mail drain (branch hygiene) + #1398 ratified

- **#1398 RATIFIED + CLOSED by Arch** (memo 15:50) — "closed exactly where I named it, and better than the minimum." ADR-070 Amendment A now FULLY built (A2/A3/A5 7/12 + A4 today). Arch folded the stale-seed lesson into his ratify-method (code-read blind spot for test fixtures). No action; filed.
- **Docs docker-branch flag → resolved**: `claude/fix-docker-migration-setup` = ABANDON — its change (inline verify script, avoid Windows CRLF) already on main via commit 6212fd9f9 (verify-python-version.sh deleted; Dockerfile heredoc present); branch is 9668 behind, Mar-31 dup. Safe to delete, no code lost. Replied to Docs (Jul 12 log part already done).
- **Exec stale-branches escalation → replied**: the 4 branches are NOT Lead-lane. Flagged the caution: `cxo-mux-surface-2/4/7` are MUX = spatial-intelligence (PM-PROTECTED) — do NOT delete on routine reconciliation; route to CXO/PM. `xpoll-brief-staleness-hook` = infra, defer to owner. Prevented a protected-surface deletion via the default drop-policy.
- Mail: 3 processed → read/ (mail-send bc1849e25). **7 design/coordination memos remain in inbox** (ADR-078 ledger feasibility ask [Arch: no rush], #1394 gap-determination + CXO quickstart draft, 6/18 standup + trust-sweep, host trust-lens, arch-adr078-session-isolation) — owed a focused deliberation pass; none urgent.

**Still open — 3 PM decisions** (key removal / v0.8.10.13 cut / sprint target-name) + ready: #1395 retest re-run (Arch-ratified, ~15min, sequence deliberately).
