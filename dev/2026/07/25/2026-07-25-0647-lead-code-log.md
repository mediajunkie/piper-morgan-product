# Lead Developer — session log 2026-07-25
- Role: Lead Developer (lead-code) · Model: Fable 5 (claude-fable-5) · Worktree: .claude/worktrees/lead-1452-harness
- Cron e1106eb5 (`17 6,9,12,15,18,21 * * *`, session-only)
- Prior days closed properly (DAY-CLOSED 7/23 + 7/24). State: CI green era (backlog 104), beta v28, beta band gated (#1393/#1394 on the #1386 re-run, #1395 on ratification), methodology on Arch.

## Fires
- Fire 1 (06:47) — START. Prior STOP verified, inbox empty, CI green at 23dfc3127.
  - The banked learning-complex de-flake, taken in the Saturday-morning quiet window: root cause confirmed — module-level TEST_USER_ID was xian's LIVE UUID, shared with the manual phase1 script and any app activity as that user; foreign patterns/settings rows cross-contaminated similarity + settings checks in sweeps. Cure: autouse fixture rebinds the module globals to a FRESH user per test (learned_patterns FK-CASCADEs on user delete; teardown via delete_test_user_fully). 11/11 standalone. Manual phase1 script renamed check_* (pytest collected it by name; one function's parameter read as a missing fixture) — same wave-42 class; its entry delisted (103). Full validation sweep running for the 9 learning entries.
  - Validation sweep: learning 0 FAILED in-sweep (the de-flake HOLDS; 85 total local failures, down from 100) — all 9 learning entries delisted. Standup still 8 in-sweep (its cumulative poison is elsewhere; flaky stands). **Backlog 103→94 — under 100.** The learning complex, quality-banked three days ago, is fully drained.
- Fire 2 (09:47) — quiet otherwise; sent Arch (cc Exec) the updated burn-down math: the methodology ruling now gates 40 of 94 remaining (43%) — the single largest lever; executable same-day either way it lands.
- Fire 3 (12:47) — CIO's branch-audit memo (risk inverted: the four "protected" MUX branch refs are empty; the real content was MY stranded 2026-05-19 session log, 59 lines, adrift two months on worktree-mux-ui-lane-scoping). Landed the log's final state on main (single commit — the two branch commits' file end-state; cherry-pick order conflicted on an interim deletion); branch deleted after landing (recovery handle in reflog + this note: tip was 879286d79). ACK to CIO/Exec/Docs riding this push.
- Fire 4 (15:47) — CIO's duty-cycle-tick v1.15 memo (awareness: Step-2a false-pass under Model A; 2a-bis hook-fire probe; PreCompact hook dead ten weeks → manual sign-off is the only backstop). Ran the 2a-bis probe here: INCONCLUSIVE — the session's permission classifier intercepts the mailbox-commit attempt before git hooks could fire, so hook liveness can't be observed from this seat (no workaround attempted, per the denial's intent). Mitigations already my practice: mailbox writes go exclusively via mail-send push-to-ref (never interactive mailbox commits), and sign-off runs by hand every STOP. Will re-probe on first Amber fire where the classifier layer differs. Memo triaged to read/.
