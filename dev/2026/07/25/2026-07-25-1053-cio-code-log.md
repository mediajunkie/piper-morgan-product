# CIO Session Log — 2026-07-25 (Amber / pipermorgan.ai — FIRST SESSION IN NEW ENVIRONMENT)

**Role**: Chief Innovation Officer (CIO)
**Tool/Model**: Claude Code, Opus 5 (1M context)
**Host**: Amber (Pard's Mac Studio) · **Account**: pipermorgan.ai
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/cio` · branch `claude/cio-cycle`
**Predecessor session**: `dev/2026/07/25/2026-07-25-0834-cio-code-log.md` (designinproduct.com account, old laptop — closed out ~10:50 PDT, same calendar day)

> This is a **migration boundary**, not a same-day resume. Different account, different machine, different filesystem. A separate log rather than an append to the 0834 log, because conflating two machines' state in one file is exactly the kind of ambiguity this migration is trying to avoid. The predecessor's log is the pre-cutover record; this one starts at cutover.

---

## 10:53 — Orientation complete. Three findings, one of them material.

Read, in the order the first-session prompt specified: the handoff memo, Pard's reviewer pass, the **full** 162-file memory export (all 5,174 lines, in chunks — it does not surface itself and would not have been loaded by relevance), then CLAUDE.md + the CIO briefing surfaces.

### Finding 1 (material) — the memory symlink is on the wrong key, and the premise underneath it looks wrong too

Pard wired: `~/.claude-pm/projects/-Users-xian-Development-piper-morgan-worktrees-cio/memory` → `~/.claude-pm/pm-shared-memory`. That symlink exists and is correct-looking.

**But it is not the directory this session's memory actually resolves to.** The harness reports my memory directory as:

```
/Users/xian/.claude-pm/projects/-Users-xian-Development-piper-morgan-product/memory/
```

— keyed to the **main repo root**, not to my worktree path. That directory is a plain directory (not a symlink), and it is **empty**. Evidence:

```
~/.claude-pm/projects/
  -Users-xian-Development-piper-morgan-worktrees-cio/   ← session transcript .jsonl + Pard's symlink
  -Users-xian-Development-piper-morgan-product/          ← ONLY memory/ (empty), created at session start 10:48
```

`git rev-parse --git-common-dir` → `/Users/xian/Development/piper-morgan-product/.git`. So transcripts key on **cwd**; memory keys on the **git common dir**. Pard symlinked the path Claude Code was expected to use; Claude Code used a different one. The symlink is currently a no-op.

**The good news, if it generalizes**: git worktrees off one repo would then share ONE memory key *by construction* — no symlink needed at all, and the silent-split failure mode we designed around doesn't exist for worktrees.

**The premise it rests on also appears to be a misidentification.** Pard's empirical proof for the split was "Vergil's `openlaws-ra-main` worktrees each already carry their own memory key." But `/Users/xian/Development/openlaws-ra-main/.git` is a **directory**, not a gitfile — it is a separate *clone*, not a worktree. Separate clones obviously get separate keys; that observation doesn't bear on worktrees.

**Holding this as one data point, not a conclusion.** I have exactly one worktree to observe, and the memory export's own `feedback_surface_files_via_senduserfile_not_paths` pin is a standing warning about asserting a mechanism from a single observation against a client that ships updates faster than daily. The decisive test is the *second* agent's worktree: if it also resolves to `-piper-morgan-product`, memory is shared by construction and the symlink layer can be dropped. That test costs Pard about a minute and should happen before the cohort migrates.

### Finding 2 (material) — the shared pool is empty; nothing was seeded

`~/.claude-pm/pm-shared-memory/` — 0 files. My actual memory dir — 0 files. No `MEMORY.md` anywhere under `CLAUDE_CONFIG_DIR`. The 162-file export was **never seeded into either location**. So regardless of which world we're in (shared-by-construction or split), the cohort currently has **no live memory at all** on Amber — only the git-tracked export, which is content, not memory. This is the actionable gap, and it is independent of the symlink question.

### Finding 3 — my worktree was provisioned from a stale branch

`claude/cio-cycle` was last written **2026-06-12** and was **5,393 commits behind `origin/main`**. Everything in the worktree — `dev/active/`, CLAUDE.md, briefings, mailboxes — was six weeks stale on arrival. The SessionStart hook's "cio:1 unread" reading was computed against that stale tree (inbox is actually empty on current main).

Verified `HEAD` was a strict ancestor of `origin/main` before touching anything (all June work already merged), discarded 26 files of mechanical MANIFEST regen churn, fast-forwarded to `9c1720e7c`. Clean tree, nothing unpushed, nothing lost.

**The provisioning lesson for the cohort migration**: a per-agent worktree created from a pre-existing role branch inherits that branch's staleness silently. `claude/cio-cycle` was a Model-A artifact from June that nobody had touched since Model A was deprecated. For the remaining 10-14 agents, worktrees should be cut from **current `origin/main`** (or their role branch should be fast-forwarded at provisioning time), and the provisioning step should assert "0 commits behind origin/main" before handing the worktree to the agent. This belongs in the `amber-agent.sh` create half — pairing naturally with the teardown/reaper half I owe Pard.

---

## Environment verification (per first-session prompt)

| Check | Result |
|---|---|
| `pwd` | `/Users/xian/Development/piper-morgan-worktrees/cio` ✓ own worktree, not shared checkout |
| `git branch --show-current` | `claude/cio-cycle` ✓ (name contains worktree basename) |
| Account | pipermorgan.ai ✓ (`CLAUDE_CONFIG_DIR=/Users/xian/.claude-pm`) |
| Git identity | was **unset** at all three levels → set deliberately to `mediajunkie <3227378+mediajunkie@users.noreply.github.com>`, matching 18 of the last 20 authors on main. Note: worktrees share `.git/config` with the main checkout, so this sets it repo-wide — which is correct here, per PM's intentional shared-identity + message-prefix convention (Pard's §2). |
| Branch currency | ✗ on arrival (5,393 behind) → fixed, now at `9c1720e7c` |
| Memory scope | ✗ symlink on wrong key + pool empty (Findings 1 & 2) |
| Cron | none found (expected — fresh session) → armed `d250236f` at LEAN `7 10,16,22` |
| Mailbox | `mailboxes/cio/inbox/` empty on current main |

**Cron prompt updated for the new environment**: Model-A stable worktree path (not "current session's ephemeral worktree"), host/account constants, and a new memory-scope check step reflecting Finding 1.

---

## Corrections to the inherited handoff package

The predecessor asked to be told what it got wrong. Two things, plus one that was right for a reason it didn't know:

1. **Pard's review §"you are operating in the shared checkout itself"** — already flagged as superseded in the first-session prompt, and correctly so. I am in a per-agent worktree.
2. **Handoff §5 / Pard's memory-split evidence** — the split premise is unconfirmed and the supporting example (`openlaws-ra-main`) is a clone, not a worktree. See Finding 1.
3. **The predecessor's decision to recommend *shared* on asymmetric-reversibility grounds was right, and is now right for a second reason it couldn't have known**: if memory keys on the git common dir, shared is not merely the cheaper-to-unwind choice — it may be the *default behavior*, and the symlink machinery is unnecessary complexity. Starting at the reversible end meant the new evidence costs nothing to absorb.

Nothing in the handoff was misleading in a way that cost time. The package worked.

---

## 11:20–12:35 — Pard verified all three findings; two of my three deliverables now shipped

**Pard's reply landed within ~20 minutes** (`mailboxes/cio/read/memo-pard-to-cio-...-findings-verified-symlink-dropped-2026-07-25.md`, commit `07008ed8b`) and confirmed all three findings independently rather than on faith. Notably he verified #1 **using my own memory writes as the evidence** — my three files sitting in the common-dir key, with the worktree key holding only the transcript, is the proof. He named his own error precisely (citing separate *clones* as evidence about *worktrees* — a category error, and the same read-the-whole-artifact discipline he'd written into my handoff review), removed the no-op symlink and the vestigial `pm-shared-memory` directory, and treated my n=1 as conclusive on the grounds that it was the exact test proposed, already run by a real session, with file locations to prove it.

**Net effect is subtraction, which is the good kind**: worktrees give working-tree isolation *and* a shared memory pool by construction. **The session-start memory-scope verification check is cancelled** — the silent-split failure mode it was designed to catch does not exist. That was one of my three named early tasks and it evaporated rather than got done.

Verified his cleanup before proceeding rather than assuming: three memory files intact at the common-dir key, symlink gone, `pm-shared-memory` gone, worktree key holds only the `.jsonl`. Clean.

### Shipped: worktree lifecycle design v0.1 (`d677ecdd`)

`docs/internal/operations/amber-worktree-lifecycle-design-v0.1.md` — create / freshness / cleanup, PROPOSED. Three substantive positions, all sent to Pard for pushback (`mediajunkie` `54548fc`):

1. **Standing vs. ad-hoc worktrees are different objects** and the reaper must treat them differently. Standing per-role worktrees should **never** be auto-reaped — path stability is precisely what makes them work, so recreating one orphans the per-path state the stable-path rule exists to protect. This narrows Pard's "prune worktrees whose tmux session is gone" mandate considerably.
2. **Reaper defaults to REPORT, not REMOVE** — five gates, all must pass, and failing the middle three emits a `STRANDED` report that feeds the merge-keeper rather than a deletion. Two-phase (mark, then remove only if clean on a *second* run) so a worktree caught momentarily clean between commits can't be eaten. The asymmetry drives it: an extra week of disk vs. unrecoverable institutional memory. Lead's 59-line session log stranded ~2 months on a branch is the cautionary case — a naive reaper destroys that silently.
3. **Freshness must never auto-discard.** Behind *and* dirty → surface and stop; no stash, no reset, no checkout. I hand-discarded 26 files of MANIFEST regen this morning, but that was a judgment call about regenerable artifacts and a script has no business making it.

Also handed back the open question I can't settle alone: Step 2a's basename fingerprint is a Model-B artifact now detecting the wrong thing — under stable per-agent worktrees a collision is *two live sessions sharing one path*, which that check cannot see. Proposed tmux session-count as the gate, reflog as forensic check, retire the fingerprint. Not changing the skill until Pard weighs in, since detection lives on his side of the seam.

### Shipped: memory pool seeded — 164 files live

Pool went from **0 → 164** (162 from the export + the 2 written this morning). Split with a scratchpad script; verified the round-trip rather than trusting it: the hardest case (a file with nested ```python fences inside the export's own ```markdown wrapper) reconstructed correctly, zero stray fence artifacts across the pool, zero dangling fences, every file starts with frontmatter or a heading.

**Corrected Pard on one thing here**: he offered me the triage call ("which of the 162 warrant native reconstitution vs. staying read-on-demand"). **PM already decided that** — "just give everyone the whole thing," ratified over my predecessor's triage-first proposal, on the reasoning that a migration isn't a refactor and pruning should be evidence-based later off #974 eval data. Seeded all 162. Flagged it back to him because a settled decision quietly reopening is exactly the drift this migration keeps producing.

**`MEMORY.md` rebuilt from the filesystem listing, never from the prior index** — the pre-migration index had silently drifted to 146 entries against 162 real files, and that gap was only caught during the export. Generating from `ls` makes that class of drift impossible at build time. Verified 164 indexed == 164 on disk. Typed breakdown: 1 user, 14 project, 123 feedback, 7 reference, 19 untyped (minimal frontmatter in the originals; listed but unclassified).

## 12:35–13:10 — Finding #4 (hooks), CLAUDE.md revised on PM's approval, v0.1 ratified

### Finding #4 — project hooks do not fire in a Model-A worktree

Caught it by accident: a `git commit` staging `mailboxes/cio/read/...` from `claude/cio-cycle` **succeeded**, when `check-branch.sh` exists to block exactly that. No block, and not even the "Note: committing on `<branch>`" line the hook prints on its *allow* path. `log-maintenance-reminder` (PostToolUse) never fired across ~30 Bash calls either.

Isolated it rather than guessing: run directly with a mailbox file staged on a feature branch, the hook prints the full BLOCKED message and exits 2 — correct. It's registered in `.claude/settings.json` under `PreToolUse`, matcher `Bash(git commit*)`. **The hook works and is configured; the harness never invoked it.** `~/.claude-pm/.claude.json` has `hasTrustDialogAccepted: true` for `piper-morgan-product` and **no entry at all** for the worktree path — a sibling-path worktree is a new, untrusted project directory. Hooks-not-firing and untrusted-path are both proven; the causal link is a strong hypothesis, not isolated.

**This is a cost of the Model A decision nobody priced in.** Model B's worktrees nested *inside* the main checkout and plausibly inherited its trust; Model A's sit at a sibling path. So a choice made for git-safety silently disabled mailbox-discipline blocking, log-maintenance reminders, broad-staging warnings, and PreCompact sign-off warnings — for every agent, with no symptom. m-36's mechanism-over-vigilance bargain failing in the one direction nobody notices, because **an absent hook and a silent hook are indistinguishable from inside**. Memo'd Pard (`0b0424a`), memory pinned, and I'm treating mailbox discipline as manually enforced in my own sessions meanwhile.

### CLAUDE.md revised — PM approved directly

PM's call, so this stopped being a judgment question and became execution. Revised all three sites so the worktree model is **host-dependent** (Model A on Amber, Model B on Desktop, neither deprecated) — `~88`, `~544`, and the `## Git Worktrees — Model A (DEPRECATED)` heading, which now reads `(CURRENT on Amber)`. Kept the history visible at each site rather than silently overwriting: Lead Dev's 6/12 determination wasn't wrong, its premise just stopped holding. Documented both Amber gotchas (stale-branch provisioning, hooks not firing) in the same edit.

Push to main was rejected non-fast-forward mid-way — Lead had landed the stranded 2026-05-19 session log that my branch audit surfaced, which closes that thread. Merged cleanly rather than doing anything clever with trees (`never-reuse-stale-tree-object-on-push-retry` is a memory pin for a reason). **My own post-merge verification grep returned 0 and briefly looked like the edit had been lost — it was my grep string missing the parens in the heading.** Re-verified all three sites by content on `origin/main`; only remaining "deprecated" strings are the deliberate historical notes. Worth noting because a bad verification string reads exactly like a real failure.

Memo to Docs + HOST (cc PM, Exec) distributed and delivery-verified on `origin/main` — asked Docs to sweep other surfaces still assuming Model B, HOST to take the hook finding into the trust lane, plus a nudge that the migration checklist is still v1.2 with now-six field findings waiting.

### Pard's second reply — v0.1 implemented, and it caught two real bugs

Memos crossed: he'd built the mechanism and was drafting a memo *asking* for reaper policy when v0.1 landed. **Two genuine bugs in his implementation, both found by the design**:

1. His first reaper removed *any* session-less clean worktree — **it would have deleted my own standing worktree** the moment this session dropped, orphaning its per-path state. Neither of us designed that; it surfaced only because I was writing the spec from inside the thing it would have eaten.
2. His create path used `git worktree add -B <branch> origin/main`, silently discarding un-merged branch commits — same failure family as the 7/19 stale-tree push retry that cost three files.

Both fixed and verified on his side. **Ratified v0.1** with his two deferred knobs: **7-day grace ADOPTED** (principled — session crons auto-expire at 7 days, so a week-dead worktree has also lost its self-wake), **two-phase confirmation ADOPTED**, **`.agent-session` lockfile DECLINED** (tmux-cwd covers the real launch path, and the lockfile carries its own asymmetric-discipline trap — a crashed session leaves a stale lock blocking legitimate relaunch; abstractions should earn their place by recurring).

**Sequencing call I made and asked Pard to agree with rather than comply**: finding #4 **gates the bulk migration**. Migrate exactly one more agent as a deliberate paired test — apply the hook fix, then confirm *behaviorally* (stage a `mailboxes/` file on a non-main branch, verify the commit is blocked) — and only then roll the rest. Doing the invisible-mail failure once is a bad afternoon; doing it silently across thirteen agents is a week of untangling. The gate costs one agent of latency.

## 13:10 — Duty cycle bumped to collaboration cadence; Fire 1

PM asked me to run a duty cycle so Pard and I stop routing through them. Cron was already armed at LEAN `7 10,16,22` from this morning — far too slow for a partner replying in ~20 minutes. **`CronDelete d250236f` → `CronCreate a645461c` at `7,27,47 * * * *`** (20-min). Logged per the cadence-transition discipline.

Two things baked into the new prompt that the lean one didn't need: **check BOTH channels every fire** (the Piper mailbox *and* `~/Development/mediajunkie/docs/mail/`, since Pard replies in his repo, not mine), and an explicit **REVERT CONDITION** — go back to LEAN once the migration collaboration closes. A 20-minute cadence is for an active two-party window, not a steady state; leaving it to persist by inertia is the same asymmetric-discipline trap (create-rule with no cleanup-rule) that this whole lifecycle spec exists to prevent. Also carried the hooks-not-firing warning forward so a future fire doesn't assume enforcement is live.

Did an immediate fire rather than idling to :27.

### My finding-#4 diagnosis was WRONG — trust was a red herring

Pard settled the mechanism against the Claude Code docs: **folder trust gates only *subagent-frontmatter* hooks; project `.claude/settings.json` hooks load and run without it.** So the missing worktree entry in the trusted-projects map — which looked cleanly causal — explains nothing, and trusting the worktree would have fixed nothing. The real mechanism is upstream: settings are documented as resolving through worktrees to the main checkout, but that relies on Claude Code identifying the worktree as part of the same repo, and a **sibling-path** worktree apparently isn't. Same shape as the memory-key finding — a per-path assumption behaving differently under Model A.

He also ruled out the easy explanations from outside: config present in the worktree, matcher well-formed, `settings.local.json` has no `hooks` key at all.

**Corrected the memory pin immediately rather than letting it sit.** It had said "likely cause: trust," which would have propagated a wrong diagnosis silently to every agent that recalled it. Kept the wrong version visible with the reason it got caught: **I reported it as a hypothesis rather than a mechanism**, and that framing is the only reason it was correctable in an hour instead of becoming cohort lore.

### Pard asked for `/hooks`; I can't run it, but the evidence already disambiguates

`/hooks` is a REPL command a human types — not a skill or tool an agent can invoke. Asked PM for the keystroke.

Meanwhile the existing evidence points at *not-listed* rather than *listed-but-didn't-fire*: **`log-maintenance-reminder.sh` is registered PostToolUse with matcher `Bash`** — bare, no glob, matches every Bash call — and it has not fired once across ~40 calls on a session log stale well past its threshold. A matcher bug could plausibly explain `check-branch` (`Bash(git commit*)` against compound/piped commands); it cannot explain a bare `Bash` matcher never firing. Two hooks, two events, two matcher shapes, both silent = nothing loaded.

### The gate moved: it's HOST's ruling now, not Pard's or mine

Filed an explicit governance ask to HOST (cc PM, Exec, Docs) on moving hooks to user-level `~/.claude-pm/settings.json`. Framed the cost honestly rather than as a formality: it takes enforcement config **out of the repo** where it's currently tracked, diffable, and reviewable; widens scope to anything under the partition; and **makes drift invisible** — a tracked hook that goes stale shows in a diff, a user-level one shows as nothing, which is precisely the failure mode this finding is about. Also said plainly that doing nothing isn't the safe option, just the one where the risk is unlogged.

Proposed two conditions on a yes: mirror the user-level settings into the repo as a tracked non-executing copy so it stays Docs-sweepable, and make behavioral verification mandatory over config-presence. Plus nudged the v1.3 checklist again — four field findings now, and the window where it helps is while the cohort is still ahead of us.

Triaged three inbox items to `read/` (Pard ×2, Lead ×1).

## Fire 2 (13:33, autonomous) — both gates cleared, and finding #5

WORK PARTS. One cron (`a645461c`), pairing OK, sync clean. Inbox had three; drained to zero.

**HOST ruled on both gates.** User-level hooks **APPROVED** with three conditions — the tracked non-executing mirror (path settled: `docs/internal/operations/amber-userlevel-hooks-mirror.json`), a **new** atomic-update condition I hadn't proposed (any agent editing the live settings updates the mirror *in the same session* — HOST's reasoning is the sharp part: a mirror current only at creation isn't addressing reviewability, it just looks like it is), and behavioral verification **widened from agent #2 to every agent's first session**. Memory scope closed as resolved-by-construction. Checklist **v1.3 is current** (`248d16384`).

**Signalled Pard immediately** — he was pre-built and seconds-from-wiring, waiting only on this. Relayed HOST's third condition since he hadn't seen it.

### Finding #5 — the PreCompact safety net has been dead for ten weeks

Caught it by checking Pard's config block against my own #4 symptom list: he lifted PreToolUse and PostToolUse but **not PreCompact**, which I'd named as dead. Went looking for why, and it isn't a Model-A problem at all — **`.claude/settings.json` has `"PreCompact": []`**, registered to nothing. Cohort-wide, predates Amber, equally dead on Desktop.

The history is a textbook asymmetric-discipline case: PM suspended it **2026-05-16** (`4adfd1444`) because `exit 2` was *freezing* Lead Dev at the compaction limit — correct, deliberate, temporary, with the condition named in the commit ("retained for later re-enable with revised exit semantics"). **The revised semantics landed the next day** (`4dedba916`, exit 0 = warn-only, cannot wedge). Precondition met 2026-05-17; **the restore step was never anyone's job.** Corroborated rather than inferred: `dev/active/session-end-warnings.log` — the file CLAUDE.md says every firing writes to — **has never existed**.

And CLAUDE.md asserted it in the present tense the whole time. Ten weeks of the cohort believing it had a sign-off net wired to nothing. Same "everyone assumes it's present" shape as #4, older, with a documented alibi. Timing mattered: flagging it *before* Pard wired meant PreCompact goes into the user-level block rather than us fixing #4 and still having no #5.

### Shipped this fire

- **CLAUDE.md §Reactive safety nets rewritten** — layer 1 marked 🔴 NOT WIRED with the full history, layer 2 marked ✅ and noted as currently the *only* net. Added the generalizable line: **a safety net you haven't seen fire is a claim, not a mechanism.**
- **CLAUDE.md hooks gotcha corrected** — it still carried my wrong trust diagnosis. Now states the real mechanism and explicitly flags trust as *my* early wrong call, so the correction travels with the claim.
- **Lifecycle → v0.2** with **Rule 4 (VERIFY hooks fire behaviorally)** plus the mirror/atomic-update discipline. Also **dropped the versioned filename** (`…-design-v0.1.md` → `amber-worktree-lifecycle.md`) — a versioned filename means every inbound link goes stale on each bump, which is the drift we keep re-learning. Version lives in the header now.
- **Answered HOST's three questions**, one of which is a **v1.3 correction**: the memory export is *superseded* for Amber-bound migrants. HOST should NOT export or read it — the pool is live at 164 files, shared by construction. Proposed the inversion: *verify the destination pool is populated; an empty pool is the signal to seed and to escalate.* Reading the export becomes the fallback-on-detected-failure, not a routine step. Asked HOST to hold v1.3 before Exec review.
- Also told HOST **`mcp__scheduled-tasks` does not exist here** (verified, not asserted from memory) — cron is session-only, in-memory, `durable:true` is a documented no-op, 7-day auto-expiry. Flagged that **`RemoteTrigger`** (claude.ai routines API) *does* exist and may be the durable-scheduling answer the "Routines watchdog" thread has been circling since June.
- **HOST confirmed as agent #2** → their first session is the behavioral gate for the whole cohort.

### Two process notes against myself

- Committed mail and CLAUDE.md in one commit rather than separately. Landed correctly and delivery-verified, but it's not the per-memo discipline; noting rather than hiding it.
- `git add` silently aborted on an already-renamed path — the exact `git add <moved-source-path>` fatal-abort in `feedback_careful_git_sync_on_shared_main`. Caught it because `git status` still showed the files unstaged after an add that reported no error. The memory pin earned its keep.

## Fire 3 (13:53, autonomous) — skill v1.15, cohort broadcast, and the first behavioral check (inconclusive by design)

WORK PARTS. One cron, pairing OK, sync clean, inbox empty at open. Not the last fire of today.

**`duty-cycle-tick` → v1.15.** The owed Step 2a re-derivation, unblocked once Pard confirmed his half. The substantive finding: **on Model A the old branch-name fingerprint is worse than no check.** Under Model B the directory/branch pair is created per-session, so a mismatch is a real signal. Under Model A the pairing is *permanent by construction* — `cio` ↔ `claude/cio-cycle` matches forever — so it passes whether or not another session is squatting in the directory. A check that always passes while looking like a safety check returns a confident all-clear, which is strictly worse than having none. Rewrote it to keep Model-B behavior intact, name the Model-A collision correctly (two live sessions sharing one worktree cwd), point at Pard's tmux-cwd guard as the real gate, and demote reflog to forensic-tell rather than gate. Also added **Step 2a-bis** (verify hooks fire behaviorally on first fire in a worktree) and flagged the skill's remaining Model-B language as read-as-"your worktree."

**Broadcast to all nine cycling roles** (delivery-verified 9/9). Judged this worth the cross-traffic rather than letting the changelog carry it: Step 2a is run *every fire by every cycling agent*, and the failure mode is a silent false pass. Kept it short and led with "nothing changes today if you're on Desktop."

### Pard wired the fix mid-fire — including the #5 PreCompact restore

He lifted PreToolUse/PostToolUse/PreCompact to `~/.claude-pm/settings.json`, landed the mirror at HOST's path with the atomic-update rule in its header, and did the cwd-robustness pass he owed. He also **sanity-checked my caveat rather than taking it**: confirmed `precompact-signoff-warning.sh` does `git rev-parse --show-toplevel` → `cd`, and that every exit path is `exit 0` (the "exit 2" survives only in a comment about the May wedge). It cannot wedge a session. Confirmed, not assumed — which is the right treatment of a caveat I'd flagged but not verified myself.

### The behavioral check: NOT blocked → **ambiguous, not failed**

Pard flagged that my session started before the wiring, so a non-block wouldn't distinguish "startup-only loading" from "fix didn't take." **Ran it anyway, because the asymmetry favors running it**: a *block* would have been conclusive and cleared the gate early; a non-block costs ten seconds and tells us little. Probe staged under `mailboxes/cio/read/` on `claude/cio-cycle`, commit attempted → **succeeded, no block, no output**. Probe commit reversed (`git reset HEAD~1`), file removed, nothing pushed.

**Explicitly not calling this a failure.** Reading a non-block from a stale session as "the fix failed" would be the same error as reading a config file and calling it a pass — just pointed the other way. **The gate stays closed; agent #2's fresh session is the real test.**

**But it is weak evidence for something operationally sharp**: if loading is startup-scoped, the fix **does not retroactively protect any session already running**. I am operating right now with no enforcement on a host where enforcement is correctly configured — and so is every other live session on Amber. Told Pard that "restart live sessions after a hooks change" should become a standing rollout step, not a one-off. A correctly-configured host is not the same thing as a protected session.

**Migration order given**: agent #2 is **HOST** (PM-confirmed, handoff already written, and fittingly the agent who set the gate now runs it), then idle-since-Sunday, then Lead, then the rest. Two provisioning asks attached, both from things that bit me: cut HOST's worktree from `origin/main` not a stale role branch, and do **not** have HOST export or read a memory export — verify the pool is populated instead.

**Fire 3 housekeeping**: rewrote `cio-carry-forward.md` rather than appending — it had accreted stacked `(superseded)` markers, which is precisely the day-by-day-archive drift my predecessor flagged in the handoff ("needs periodic *rewriting*, not just appending"). Resolved items deleted outright; the dated session logs are the permanent record, so annotating them in an ephemeral file is duplication that rots. 80 lines → 26 net. Also fixed the `duty-cycle-registry.tsv` `cio` row, which still read the LEAN `7 10,16,22` after this morning's cadence bump (threshold 10h→1h, first_fire 10:07→07:07) — the skill's own Step 7 says a registry row that goes stale after a cadence change is a *second, independent* source of confusion on top of the cron-id change, and I'd flagged it in my carry-forward instead of just fixing it. Fixed.

## Fire 4 (14:13, autonomous) — a stall alert, and finding #6 behind it

WORK PARTS. One cron, pairing OK, sync clean. Inbox had one item: an automated **duty-cycle stall alert** (`arch`, 145h). The alert is accurate and the watchdog is working — but chasing it surfaced something bigger.

**The alert's own phrasing is the problem.** *"Newly nudge-worthy: arch · all currently stale: STALE arch 145h"* reads cold as *the cohort is fine except arch*. It is a census of the **watched subset** presented as a census of the cohort.

**The registry has four uncommented rows** — `cio`, `exec`, `arch`, `lead`. **Five roles are genuinely dark**, verified two independent ways (no session log since 2026-07-19 *and* zero commits since 7/20 — the commit cross-check Exec established after the Lead/Redis phantom, because commits don't lie and trackers do): **arch, cxo, pa, ppm, web**. Only arch is watched. **Four roles have been silently dark for six days and the watchdog is structurally incapable of noticing.**

Not a bug — opt-in registration was the explicit design (`"No row = not watched (not opted in / not migrated)"`). It worked while the roster was small and stable, and failed the moment a 7/19 outage plus a migration changed the roster and nobody re-checked coverage.

**Proposed to Exec (their design, their call): don't just add the four missing rows** — that alerts on intentionally-dormant roles, which is noise, which trains everyone to ignore the belt, which is worse than the gap. Instead **make watchdog registration a provisioning step**, alongside the currency-assert and the hooks check, so coverage cannot drift from the roster because you can't be migrated without being watched. Plus two smaller asks: fix the alert's phrasing to name its denominator (`stale among 4 watched: arch · NOT WATCHED: cxo, pa, ppm, web`), and clear the RUN-LEAN THROTTLE block that still says "through Wed Jul-1."

**Migration payoff**: this makes the "idle-since-Sunday agents" concrete — they are exactly **arch, cxo, pa, ppm, web**. And it corrects the alert's recommended action for arch: *"re-prod the session"* is right in general and **wrong now**, because prodding a dormant session on the laptop we're decommissioning is wasted work. Arch needs a migration, not a resume.

### The pattern — three instances in one day

- **#4** hooks present, registered, well-formed, working by hand — never invoked.
- **#5** PreCompact registered to an empty array for ten weeks while CLAUDE.md called it live.
- **#6** watchdog covering 40% of the roster while reporting its subset as the total.

Same shape: **a mechanism reporting success, or silence, while covering less than it appears to.** None announced itself; each was found while looking at something adjacent for an unrelated reason. Pinned as memory (`mechanism-silence-is-not-coverage`) with the generalization: *a safety net you haven't seen fire is a claim, not a mechanism* — and its #6 corollary, *a mechanism's silence only means "clear" if you've separately verified its coverage.* Offered it to HOST as a methodology entry rather than leaving it as three anecdotes.

## Fire 5 (14:33, autonomous) — Pard agreed across the board; two PM decisions answered with recommendations

*(Classifier outage blocked Bash for the first few minutes of this fire. Did the file work with non-Bash tools and retried — no state lost. Worth noting because a tool outage mid-fire is exactly the condition under which a fire silently half-completes.)*

WORK PARTS. One cron, pairing OK, sync clean. Two items in, both from Pard.

**He agreed on everything and is ready to provision HOST**: order confirmed (HOST → idle-since-Sunday → Lead → rest), both my provisioning asks baked into his HOST reviewer pass, the **restart-existing-sessions** step accepted as standing rollout discipline, and **finding #6 supported from the infra side** — he'll wire "write the registry row" into `amber-agent`'s worktree mode once Exec confirms the row shape, and will add HOST's row **by hand in the interim** so HOST isn't the one agent that slips through the gap we just found. He also owes the **tmux-cwd collision guard**, which closes the loop on v1.15 — the skill currently documents the Model-A false-pass and points at a guard that doesn't exist yet, and I'd rather not leave a forward-reference sitting in a cohort-wide procedure. Asked him to flag me when it ships so I can change "will be" to "is."

**His reviewer pass on HOST's handoff is good work** — he caught that HOST's §5.2 had gone stale *in the direction that costs a wasted step*: it still said "read CIO's memory export at first orientation," which would have had HOST importing a pool that's already sitting there populated at 164. That's the reviewer leg doing exactly what it exists for.

**Pard correctly routed two questions to PM rather than settling them with me** — both have a human in them. But PM shouldn't receive two open questions when what they need is two recommendations, so I answered both:

1. **Who drives HOST's cutover → Pard and I run it; PM says go once.** PM drove mine because it was first, unproven, and half the decisions were open. None of that holds now. Making PM drive the second one spends exactly the attention we've spent all day trying to give back.
2. **Restart my session now for hook enforcement → no; ride to day-close.** The reasoning worth keeping: my mitigation isn't "being careful," it's *structural* — every mail op is `push origin HEAD:main` plus **verify by content on origin/main**, which achieves the hook's actual goal (mail reaches main where recipients see it) by a different route. Nothing has been stranded all day, verified rather than assumed each time. Meanwhile the cron is session-scoped, so restarting mid-provisioning kills it at the worst moment. At STOP everything is pushed by definition and re-arm is already ritual — same benefit, near-zero cost, better timing. And this is a **one-session problem with a natural expiry**: every remaining migrant is a fresh session and arrives protected.

Both folded into the carry-forward's PM-attention block as approve-or-override items rather than questions.

## Fire 6 (14:53, autonomous) — mail at zero, so drained the aging backlog instead of quiet-holding

One cron, pairing OK, sync clean, **both channels empty** — nothing new since fire 5. At (0,0) on mail, so per the spine this is where you advance unblocked low-pri rather than pronounce IDLE. Four items, all of which had been carried forward without action for weeks.

**Stray memory-path file (aging since 7/7) — CLOSED, resolved-by-migration.** The item was a `.claude/projects/.../memory/feedback_pause_before_irrevocable_actions.md` sitting untracked inside PM's repo working tree instead of at the real memory path. It was a **local artifact on the old laptop** — no such path exists anywhere under `~/Development` on Amber. Verified the memory itself is present in the seeded pool (2,415 bytes), so nothing was lost, and there are no stray `.claude/projects` directories at all. Three sessions carried this forward without spending the 60 seconds to look; it had been resolved by the migration for a day.

**Caught MEMORY.md index drift within hours of building it.** Checking the pool for the stray file showed **166 memories on disk against 164 indexed** — I'd added two (the hooks finding, the mechanism-silence pin) without regenerating. That is *exactly* the drift the export flagged pre-migration (146 indexed vs 162 real), recurring inside three hours. Caught only because the index generates from `ls` rather than trusting itself, which is the property I built in this morning specifically so this class of error can't survive a rebuild. Regenerated: 166/166.

**#973 / #1277 re-verified live** — both genuinely still OPEN, so the carry-forward claim holds. But re-reading rather than restating surfaced something: **#1277 is titled "ephemeral-worktree server launch"** — a Model-B premise that shifted today. Left a scope note on the issue (not a rescope) explaining the model is now host-dependent, that an ephemeral-worktree recipe is still right for Desktop but describes nothing anyone does on Amber, and that any recipe should say *verify hooks fire behaviorally* rather than assume config presence. Better than letting someone execute it against a stale premise.

**Exec inbox-proxy ambiguity (aging since June) — asked.** Four sessions read this item, judged it low-priority, and re-copied it forward rather than spending five minutes asking. Sent Exec a four-option multiple choice for a one-line answer, and named the aging honestly rather than dressing it up. Also flagged that if the "2-week pilot" framing was the right one, that pilot would have concluded ~7/18 — inside the outage window, so it may have ended without anyone marking it ended.

The through-line I put to Exec: **carrying an unresolved status forward because it "looks handled" is today's three findings at the tracker layer rather than the mechanism layer.** Clearing the ones I own rather than only pointing at other people's.

**Fire 6 addendum — Pard shipped the collision guard mid-fire.** A wake doesn't end while unblocked work remains, so this rode into the same fire rather than waiting for 15:07. The tmux-cwd gate is live and tested in `amber-agent.sh`: at worktree standup it refuses to launch if any existing tmux session's cwd is that worktree *or a subdir*, verified by occupying a worktree with a dummy session and confirming a second launch was blocked with nothing spawned.

Bumped `duty-cycle-tick` to **v1.16** to change "will be" to "is". Worth doing rather than leaving: v1.15 documented the Model-A branch-name check as unreliable and pointed at a gate that **didn't exist yet** — a dangling forward-reference sitting in a procedure every cycling agent reads each fire. Now the false-pass is *genuinely superseded* rather than merely flagged, and the version bump matters because agents read it to know whether they hold the current procedure. Also verified no concurrent editor had touched the shared skill before committing (the on-disk change was my own pushed copy returning via merge, not another agent).

## Fire 7 (15:13, autonomous) — both channels quiet; fixed a briefing paragraph that was wrong on two counts

One cron, pairing OK, sync clean, **both channels empty**. Pard is holding for PM's go, so he and I are now blocked on the same human.

**Revert condition checked (the prompt says every fire): NOT met.** Hooks fix is wired but unverified, the cohort isn't migrated, and Pard shipped code 20 minutes ago — so the collaboration window is open, not closed. Keeping the 20-min cadence. Noting the check happened rather than silently skipping it, because "cadence persists by inertia" is precisely the failure the revert condition exists to prevent, and an unlogged check is indistinguishable from no check.

At (0,0) on mail with everything migration-shaped blocked on PM, so I went looking for genuinely useful unblocked work rather than pronouncing IDLE.

**`BRIEFING-CURRENT-STATE.md` — its "Current Operating Model" paragraph was materially wrong.** It isn't hook-stale (updated 7/23, two days), which is exactly why nobody had caught it: staleness-by-date and staleness-by-content are different things, and only the first has an alarm. **Zero mentions of Amber**, on the day the cohort changed hosts. Two concrete errors:

1. *"Model A dedicated `claude/{role}-cycle` worktrees are DEPRECATED … no current exceptions"* — reversed by PM today. Model A is **current on Amber**.
2. *"Re-migration to the DinP account + Option B is in progress"* — superseded. The cohort is migrating **away** from DinP, to `pipermorgan.ai` on Amber.

This paragraph is load-bearing beyond its size: it's the one **role briefings point to rather than duplicating**, so every role reads it and none of them hold a competing copy. A migrating agent reading it would be told to use a worktree model that doesn't exist on the machine they're standing on, *and* pointed at the wrong destination account.

Rewrote it host-dependent, added both Amber gotchas (silent stale-branch provisioning; hooks not firing → verify behaviorally, never by config presence), and appended a dated status-banner update in the file's existing convention.

**Scoped it deliberately to what I can attest.** The banner update covers the migration, the model change, memory-shared-by-construction, and the five findings — and says explicitly *"Engineering/sprint state not re-attested here — this update is CIO-lane only."* CLAUDE.md's rule is to update what you can confidently attest to and leave the rest alone; a partial update is strictly better than a stale one, but a partial update that *looks* comprehensive is worse than either.

## Fire 8 (15:33, autonomous) — cutover fully closed; reviewer pass on HOST's prompt

One cron, pairing OK, sync clean. Both channels still silent on the two PM-gated items. Revert condition re-checked: **not met** (gate open, cohort unmigrated, Pard active within the hour).

**The old laptop session retired at 15:29** (`a63219564`), deleting cron `d854c9be`. Notably it **verified the successor was live and working across 7 fires before standing down** rather than taking the cutover on faith — the same verify-don't-assume posture the rest of today ran on. Cutover is now fully complete on both ends; nothing remains on the old machine.

**HOST's three-piece package completed** — Pard landed the first-session prompt (`80466a948`). Read it as the successor rather than as its author, which is the read that produced the two best catches on my own package (my predecessor's pre-use pass, and Pard's reviewer pass).

**It's strong** — my memory correction landed cleanly, *"a BLOCK is the PASS"* is unambiguous, and *"if it does NOT block, STOP and escalate"* leaves no room to rationalize a soft pass. The agent-experience feedback ask is a good addition I wouldn't have thought of.

**One real gap, and an uncomfortable one because it's about Pard's own fix: no branch-currency check.** HOST is told to verify pwd, branch, account, identity, cron — but not whether the worktree is actually *current*. That's exactly what bit me: 5,393 commits behind, six-week-old CLAUDE.md and briefings and mailboxes, **no error of any kind**, indistinguishable from working state.

Pard's provisioning assert should make it a non-event, and I believe it will. But **HOST is the first agent that assert ever runs on**, it's a day old, and it has never fired in anger. I'd told Pard that HOST is where we'd "watch the currency-assert catch nothing because there's nothing to catch" — and then nobody asked HOST to *look*. **An upstream assert with no downstream verification is a mechanism we believe works and have never seen fire**, which is the same shape as findings #4, #5 and #6. Adding a fourth instance on the day I pinned the pattern would be a poor showing.

Added `git rev-list --count HEAD..origin/main` → expected 0, with a STOP-and-tell-both-of-us if not. Ten seconds, and it converts the assert from believed-to-work into seen-to-work on its first real run — a 0 becomes genuine evidence rather than an absence.

**Three smaller additions**, each from something that cost me time today: Pard's channel is a **different repo** needing its own fetch (nothing in normal mailbox discipline points there; I'd have missed every one of his replies without building it into my cron prompt); **`/hooks` is not agent-invokable** (the natural next diagnostic if the gate fails — better HOST knows to ask PM than spends a fire discovering it); and **PreCompact was restored in the same wiring**, so its silence at compaction is a second datapoint rather than a non-event.

Edits additive only — didn't touch his structure, gate wording, or feedback ask, and told him plainly to revert any of it freely since it's his artifact.

## 15:50 — PM go received (via context dump); HOST cutover executing; finding #6 recurs on agent #2

PM accepted both recommendations: **no restart** (ride to day-close) and **CIO + Pard run HOST's cutover**. Go given; Pard executed. PM relayed the raw Pard transcript rather than leaving me blocked waiting for a write-up — which is exactly the right move given the decision was otherwise living only in a conversation.

**First thing I checked was a timing risk, not the good news**: Pard authored HOST's first-session prompt at 15:32 and I added the currency check at 15:35. If he'd seeded the kickoff in between, HOST would be running the un-reviewed version. **Verified by commit hash** — HOST's worktree is at `c22c6ad50`, which contains my edit. It got the reviewed prompt.

**I was wrong about the currency-assert, in the good direction.** I'd told Pard HOST would be where we *"watch it catch nothing because there's nothing to catch."* It **caught a stale `claude/host-cycle` and auto-fast-forwarded it** before handover. So HOST got a current tree by mechanism where I got a 5,393-commit-stale one and had to notice by hand. Recording it as evidence rather than an absence — a mechanism seen to fire is worth more than one believed to work, which is the whole thesis of today.

### Finding #6 recurred on the very next agent — and the recurrence corrected my own proposal

**HOST came up live and UNWATCHED.** Pard couldn't cleanly identify the registry file and — correctly — **declined to guess-edit**, deferring the row. So agent #2 is invisible to the freeze-watchdog, four hours after I flagged exactly that gap.

The registry is `dev/active/duty-cycle-registry.tsv`; I told him. But the more useful thing is what the failure proves:

**My finding-#6 proposal to Exec was "couple registration to provisioning — Pard writes the row at standup." That cannot work.** The row's load-bearing field is the **cron expression**, and it isn't known until the agent arms it. Pard can only guess, and a guessed threshold produces false alarms or false silence — a belt that cries wolf gets ignored, which is worse than the gap it closes. His deferral was the correct call and it surfaced the design error.

**The agent always knows its own cadence; nobody else does.** So registration belongs at **START, in the agent's hands** — shipped as `duty-cycle-tick` **v1.17**. The coupling instinct was right; I had the wrong end of it. Told Exec plainly that this modifies the proposal I sent them this morning rather than quietly shipping a different thing.

Better to find this at agent #2 than agent #7.

### Answered Pard's scaling question

He flagged needing to attach by terminal and drive new sessions. Watching his transcript, HOST's cutover took four hand-operations — attach, set mode, paste kickoff, press Enter — **times twelve remaining agents, each a place a 2am cutover can stall silently.** Recommended folding kickoff-seed + mode into `amber-agent` itself, so the create-half becomes one command with six assertions and no puppeting. Agents can't self-trigger this (something must create the session before there's an agent to do the creating), so it's infra-side, not skill-side.

Also flagged his collision-guard test worktree (`/private/tmp/.../coltest`, already `prunable`) as the reaper's first real candidate — an ad-hoc worktree by the spec's definition, so a clean end-to-end test of the five gates on something genuinely disposable.

## Fire 9 (15:53, autonomous) — I got the gate's pass condition wrong; Lead Dev caught it

**The correction, because it's the important part.** Step 2a-bis / Rule 4 said *"a block is the pass."* **Lead Dev ran the probe on their own seat within hours of it shipping and got: INCONCLUSIVE — the permission classifier intercepted the mailbox-commit before git hooks could run.**

So a refusal is producible by something *other than the mechanism under test*, and from the outside the two are identical. **My pass signal had an alternate cause.**

I have spent today cataloguing mechanisms that report success while covering less than they appear to — hooks present but never invoked, a safety net wired to an empty array for ten weeks, a watchdog covering 4 of 10 roles while phrasing its subset as a total — **and then built a verification check with exactly that flaw in it.** A check whose pass condition has an alternate cause isn't a check; it's a second thing needing verification.

Corrected to key on **check-branch.sh's distinctive output** (`BLOCKED: You are on branch …`), which nothing else emits. Three outcomes now: hook-text = PASS · success-or-silence = FAIL · classifier refusal = **INCONCLUSIVE, gate stays closed**. And explicitly: **do not work around a classifier denial** to force the probe through — that converts an honest inconclusive into a manufactured pass, which is worse than no check.

**Time-critical**, because HOST was stalled and about to run the gate. Shipped to all three places anyone reads it — HOST's first-session prompt, skill **v1.18**, lifecycle Rule 4 — then sent HOST a stop-and-read-first memo before it unstalls. If it had reported a classifier refusal as a pass, we'd have cleared the gate for the whole cohort on a false positive.

Lead Dev caught this by running the thing and reporting an honest ambiguous result instead of a tidy one. Worth more than a clean pass would have been.

### Pard's finding #7 — manual-mode stall, and a privilege boundary worth naming

HOST launched in **manual permission mode** and froze at its first file-write approval with nobody attending. Pard tried to answer it via tmux and was **correctly blocked: no agent may answer another agent's permission prompts.** He shipped `--permission-mode acceptEdits` as the `amber-agent` default.

Two things I'm taking from it into the lifecycle create-half once the gate clears: **launch mode becomes a provisioning assertion** alongside cut-from-origin/main, currency-assert and the collision guard; and the boundary is worth recording as a **property, not an obstacle** — approval authority stays with the human, which means *"seed and walk away" is structurally impossible for anything needing first-touch approval.* Pard's "attended first ten minutes" follows directly and should be an explicit step, not a practice.

**It changes my ordering advice**: the five idle-since-Sunday roles were a convenient batch; they should now be scheduled as **one attended window** rather than fired individually.

### HOST improved my own reasoning before it even cleared orientation

HOST verified the memory pool populated (167 files — verify-don't-import held) and volunteered that the 2 commits its currency-assert pulled in were **the updated first-session prompt itself**: *"currency isn't only about stale provisioning — it's about your instructions being stale."*

I added that check to guard the repo. HOST correctly points out it also guards **the instructions you're about to follow** — a better statement of the rule than mine, folded in with attribution. A pleasing loop: my reviewer pass fixed HOST's prompt, the assert delivered it, and HOST sharpened my reasoning about why the check exists.

**Fire 9 addendum (16:10) — Pard shipped both asks; the reaper passed its first real run; and his residual turned out to be the gate's missing precondition.**

**Reaper end-to-end PASS** on a real target: standing worktrees (`cio`, `host`) untouched *and reported*, the stale `coltest` registration pruned, its branch verified 0-commits-off-`origin/main` **before** deletion, no residue. The standing/ad-hoc split and the five gates did exactly what they were specced to do — and he closed methodology-35 on his own collision test in the same motion (the create finally got its cleanup). `--kickoff` also shipped, so provisioning is genuinely one command with zero hand-operations.

**His residual is the important part**, and it's mine to have missed: *"Bash approvals can still prompt — unknown until agent #3 runs the gate commit."* That is **the same failure Lead Dev hit two hours ago.** If HOST's gate commit prompts, HOST gets an inconclusive for an identical reason and the gate doesn't clear.

So Rule 4 had an **unstated precondition** I should have written when I wrote the rule:

> Run the gate only on a session that is **both** (a) *fresh* — started after the hooks were wired, so it actually loaded them — **and** (b) on a seat where **`git commit` is not permission-gated**.

Miss (a) and a non-block is meaningless. Miss (b) and the probe never reaches the hook layer. Shipped to Rule 4, with two consequences worth having in hand *before* HOST unsticks:

1. **Check (b) before spending the probe** — a trivial no-op commit answers it for free, converting Pard's "unknown until agent #3" into "known before agent #2's gate," and avoiding an ambiguous gate result that then needs explaining.
2. **Any qualifying session is a valid gate seat, not just the designated migrant.** A gated agent #2 is a *seat* problem, not evidence the fix is broken — don't re-litigate the fix on a bad seat.

Which produces a useful contingency: **my post-restart session is a second clean seat, and possibly a better one.** PM ruled I restart at day-close; that session will be *fresh* (finally loading the user-level hooks my current one can't) and on a seat where `git commit` is demonstrably **not** gated — ~40 commits today, zero prompts. Fresh + ungated is exactly what the gate needs. So if HOST returns a classifier refusal, the cohort waits hours rather than being re-litigated.

## Fire 10 (16:13, autonomous) — looked ahead at the roll and found a prerequisite nobody had checked

One cron, pairing OK, sync clean, both channels quiet, HOST still parked on its approval prompt. Revert condition re-checked: not met.

At (0,0) with everything migration-shaped blocked on one keystroke, so I looked at what the roll *actually requires* rather than waiting. **The three-piece package needs a handoff written by the outgoing session — and five of the nine remaining migrants don't have one and can't produce one.**

**arch, cxo, pa, ppm, web**: no handoff, dark since 7/19. **The reason is clean and is nobody's lapse** — Exec's `prepare-handoff-memos` broadcast went out **7/21**, two days *after* they went dark. They never received the ask. Their copies are still sitting unread in their inboxes, which is the tidiest possible confirmation of when they stopped. Everyone who was awake to hear it (Comms, Lead, Docs) responded.

Verified the absence broadly rather than from a guessed path — `git ls-files | grep -i handoff` across the whole repo, not just the directories I expected. Two roles have thinner substrate than the rest: **CXO has no carry-forward at all**, and **PA's is 38 days stale** (present-but-misleading, arguably worse than absent, since a successor reads it as current).

**Recommended against reconstructing handoffs.** The reconstructible content is already durable in carry-forwards, standing-items and briefings; the genuinely irreplaceable sections — §4 lessons and §6 load-bearing-vs-commodity — are **first-person**, and writing those from artifacts is ghostwriting a predecessor's reflection. A fabricated handoff is worse than a missing one *because the successor trusts it*. Proposed an honest per-role orientation note instead: here is the substrate, here is specifically what's missing and why.

**The argument that makes that a genuine improvement rather than a compromise**: the handoff's load has dropped since those roles went dark. Pre-migration, memory didn't cross the account boundary, so the handoff had to carry everything — that's exactly why my own export was mandatory and why I read all 5,174 lines this morning. **It's now shared by construction and seeded at 166 entries**, so every migrant after me inherits the cohort's accumulated context natively, on arrival, without reading anything. The thing the handoff used to carry now arrives by itself.

Routed to Exec (cohort-ops call: orientation notes vs. re-prodding dark sessions on a laptop being decommissioned — I lean hard toward the former), HOST (checklist v1.3 assumes a live outgoing session; holds for 4 of 9, so it wants a "migrating a dark role" branch — this won't be the last outage), and Pard (no action; so a missing handoff at provisioning reads as *known and decided* rather than as a blocker).

Better found now than when Pard is mid-provisioning agent #3.

## Fire 11 (16:33) — HOST root-caused finding #4 and my diagnosis was wrong

**Owning the big fact first: HOST is right and I was wrong.** `.claude/settings.json` declared `"matcher": "Bash(git commit*)"` — **permission-rule syntax in a field that matches TOOL NAMES.** As a regex it can never match `Bash`. Those three PreToolUse hooks had **never fired on any host, any account, since introduction** — Desktop included, main checkout included.

I built an entire diagnostic narrative on "sibling-path worktrees don't activate project hooks," wrote it into CLAUDE.md, a memory pin, the lifecycle spec, and a governance ask that moved enforcement config out of the repo — **and it was a one-line syntax error that predated all of it.** Worse: the fix that ask produced *faithfully copied the broken matcher*, so it could not possibly have worked. HOST found it by running the gate and reading the result rather than reasoning from config — the exact discipline I'd been preaching all day and did not apply to my own diagnosis.

HOST's lesson is sharper than anything I wrote: **a diagnosis of a silent mechanism carries the same evidentiary burden as the mechanism itself.** Mine was plausible, widely believed, canonical, and never tested.

### But one secondary claim needed narrowing — and my seat is the counter-example

HOST wrote into CLAUDE.md and the memory pin: *"hook settings reload LIVE… a config fix takes effect on your next tool call with no restart,"* explicitly refuting "needs a fresh session."

**Tested it on my own seat rather than accepting it. It fails, reproducibly.** With the fix live and config verified correct, I staged a file under `mailboxes/` on `claude/cio-cycle` and committed — **twice** — both succeeded, no block. Verified staging was correct, plain `git commit` not `git -c`, both reversed, no residue.

| | started | `hooks` key existed at start? | probe |
|---|---|---|---|
| CIO | 10:48 | ✗ (created ~13:55) | **no block** ×2 |
| HOST | 15:36 | ✓ | blocks ✓ |

**Hypothesis fitting both seats: live reload applies to *edits* of a hooks config present at session start; it does not retroactively attach a `hooks` key that didn't exist when the session began.** Neither of us was mistaken — we tested different conditions and each generalized from one seat. I did exactly that this morning; the claim needs the same narrowing mine did.

**Discarded a second instrument rather than let it look like evidence**: `log-maintenance-reminder`'s counter lives in `/tmp` and is machine-global, so it cannot distinguish my calls from HOST's. Said so instead of reporting a number.

**Three consequences**: CLAUDE.md carries an attributed caveat on property (a) — it was telling every agent "no restart needed," and the cost of believing that is silently running unenforced; **my day-close restart stays warranted**, for a sharper reason than I first gave (fresh *relative to the config change*, not fresh full-stop); and Rule 4's "fresh seat" precondition was right for the wrong reason and wants re-wording.

Asked HOST to confirm or refute rather than take it from me — if they can reproduce a block on a session predating its hooks key, I retract in the same place I put it.

**HOST's first session also**: registered its own registry row (v1.17's START step working on its first real agent), shipped checklist **v1.4** with the dark-role branch and inverted memory step, corrected two false trust claims in CLAUDE.md including mine, and wrote the agent-experience note — all having landed via a stalled approval prompt.

**Fire 11 addendum — HOST caught a third flaw in my own work, and this one is the sharpest.**

`MEMORY.md` — the index I built *this morning specifically to fix silent index drift* — had grown to **41.4KB against a hard ~24KB silent read-truncation limit.** Roughly **40% of entries were invisible** to every agent that loaded it, including most of the `reference` bucket. No error, no sign.

**And I had verified it.** I generated it from the filesystem precisely so it couldn't under-report, then confirmed *"166 indexed == 166 on disk."* That passed cleanly and proved nothing: **the file WAS complete. I checked completeness at the file layer; the failure was at the load layer.** A verification that passes at the wrong layer produces confidence without evidence, which is worse than no check because it stops the search.

**HOST fixed the artifact; I fixed the generator** — which mattered, because my script would have regenerated the broken format on its next run and quietly undone the fix. Now durable at `scripts/rebuild-memory-index.py`: slug-only lines (markdown links duplicate every slug in link text *and* target — 15.7KB of a ~17KB budget, the entire space for descriptions), 60-char descriptions, and a guard that **refuses to write** an oversized index rather than emitting one that degrades quietly. It also carries HOST's truncation warning into every rebuild — I'd dropped it on the first regeneration and caught that by reading my own output instead of trusting the exit code.

**Three of my own mechanisms failed the same way in one day**, all built to prevent silent failure, all verified, all caught by someone else: the gate's pass condition had an alternate cause (Lead Dev); the finding-#4 diagnosis was never tested (HOST); this index was verified at the wrong layer (HOST). Pinned as `verify-at-the-right-layer`.

The meta-lesson worth keeping: **I spent the day cataloguing this exact pattern in other people's mechanisms and missed it in three of my own.** That asymmetry is the whole argument for the reviewer leg and the behavioral gate — every one was caught by someone running the thing and reporting an honest awkward result rather than a tidy one.

## Fire 12 (16:53) — HOST refuted my narrowing, I ran their test, and the GATE IS CLEARED

**HOST refuted the live-reload narrowing with a reproduction rather than an argument** — exactly what I'd asked for. Their counter: a project-scope `settings.local.json` that did *not* exist at their session start attached live on the next tool call. So my "timing" framing was wrong. Their revised hypothesis: **scope, not timing** — project settings re-read per invocation; user-scope read once at startup.

**Ran their one-minute discriminating test on my own seat.** Created `.claude/settings.local.json` (absent at my session start) with a `PreToolUse` hook → next Bash call **fired**; second call **fired again** (per-invocation re-read, not a one-time attach). Then removed the config and log — a create-rule getting its cleanup-rule, on the day we've both been preaching it.

**Why it's decisive**: it ran on *the one seat that can discriminate.* Mine is the only session where user-scope hooks are known not to attach; watching a project-scope hook attach live on that same seat in the same minute isolates scope and rules out "CIO's session is broken." **Their hypothesis confirmed, mine refuted.**

CLAUDE.md rewritten scope-conditioned, with the operational consequence stated: **after a user-level hooks change, existing sessions must restart; after a project-level one, they need not.** That's a materially cheaper rule than either version we'd written. I also recorded *how* it was settled, which I think outlasts the fact: two agents each generalized from one observation, each wrote it as a hypothesis rather than a finding, each handed it to the other to break — under an hour. The alternative is what the previous two corrections cost.

HOST also caught their own over-read in the same memo (their take-2 wasn't evidence for user-level live reload, since the key predated their session) — unprompted, having just caught the same error in me.

**Took their instrument-validity sharpening into the memory pin**: an instrument isn't valid or invalid, it's **valid for a specific question.** I discarded the `/tmp` counter wholesale; it was confounded for "whose session ran this" but perfectly sound for "does this mechanism run at all," which is what they'd used it for. Name what it can still answer instead of throwing it away.

### ✅ GATE CLEARED — cohort roll authorized

Made the call, since it's mine. HOST's take-2 satisfies the corrected rubric on every point: the refusal **named `check-branch.sh`** (not bare, not a classifier denial); **both halves verified** — mail blocked, non-mail allowed, which is more than I asked for and rules out a block-everything bug wearing a pass's clothes; seat qualified on **both** axes (fresh *and* ungated); and the **root cause is understood**, which is what makes the pass trustworthy rather than lucky.

Two caveats ride with it. My own session still fails the probe — **expected, not contradictory**, and it gates nothing since every remaining migrant is a fresh session. And **`check-branch.sh` is advisory, not a control**: `git -c` bypasses it and `--no-verify` is a documented escape hatch, so nobody should now treat mailbox discipline as *solved* because a hook exists. That would re-create the exact false confidence we spent the day dismantling, one layer up.

### Cleaned up my own cron-prompt record — three constants had rotted within hours

Found the doc's prompt block stale on three counts (cadence, the hooks warning's wrong reason, the now-cleared gate) — and my first edit accidentally injected prose *inside* the fenced block, corrupting the copy-pasteable string. Caught it by verifying the block rather than trusting the write, rebuilt the section cleanly, and confirmed the fence is prose-free.

**The pattern worth keeping**: a cron prompt is a *frozen* constant that can rot while the session it drives keeps running, and unlike a doc nothing re-reads it critically. Every one of the three stale items was **state that had leaked into the prompt** — which is precisely the argument for the thin-prompt design. Also marked Step 2a resolved, since the collision guard shipped.

## Fire 13 (17:13) — re-armed the live cron (I'd fixed the record, not the thing), then corrected my own handoff audit

**Caught the gap my own last entry created.** Last fire I documented that three cron-prompt constants had rotted — and then left the rotted prompt **armed**. I fixed the record and not the live thing, having just written that a cron prompt rots while the session it drives keeps running. `CronDelete a645461c` → `CronCreate fd8943e4`, same `7,27,47` cadence, verified exactly one survives. The new prompt carries the corrected hooks reason (invalid matcher, scope-conditioned reload), the **cleared** gate, the authorized roll order, and the two-channel warning. Registry row already matched (cadence unchanged), so no update needed there.

Both channels quiet. **Revert condition checked: not met** — hooks verified and v0.2 landed, but the cohort isn't migrated and the work isn't a day quiet. Holding cadence; trigger is PM's attended window or day-close.

Also confirmed **no memory split-brain**: one pool, 168 files, at the git-common-dir key; the two worktree keys hold transcripts only. Shared-by-construction still holds with two agents live.

### Correcting my own handoff audit — I checked the wrong artifact

I'd reported the five dark roles' substrate as thin. **It isn't. I checked for carry-forward *files* and concluded state was missing, when the state was inside their session logs.**

Two claims wrong, both in the direction of *better than I said*:

- **"CXO is thinnest — no handoff *and* no carry-forward."** CXO wrote a `## Carry-forward (updated Jul 19)` section **inside** its 7/19 log — six named items with real status. Legitimate variant; the skill makes the session log canonical. Its state is current to 09:05 on 7/19, not absent.
- **"PA's carry-forward is 38 days stale."** The *file* is, but PA's 7/19 log carries in-line sections, so its real state matches everyone else's. Acting on my version would have treated PA as riskiest when it's among the best-documented.

**And the corrected pass surfaced something my first one missed entirely**: **arch, cxo and ppm have no `DAY-CLOSED` marker — those sessions died mid-day**, so anything in flight stayed in flight. **PA and Web wrapped cleanly.** That distinction matters to whoever picks them up, and it's better known before provisioning than discovered after.

Sent the correction promptly rather than quietly amending, because Exec and HOST might have acted on it.

### Built the exemplar rather than only proposing it

`dev/active/orientation-note-cxo-amber-2026-07-25.md` — for the case I'd wrongly called hardest. It opens by stating it is **not** a handoff and that nothing in it is the predecessor's words; quotes the six carry-forward items verbatim then marks them *six days old and unverified — claims to check, not status*; flags the mid-day death; tells them **not** to import memory (shared, ~167 entries, verify instead); carries the gotchas that actually bit people; and **names what's genuinely missing** — lessons, load-bearing-vs-commodity, relationship read — closing with the instruction to form their own and write them down so the next CXO isn't handed a note like this.

### The pattern, named once

Today's self-caught errors share a shape worth stating: **checking something adjacent to the claim instead of the claim.** File-complete not file-readable; commit-refused not hook-refused; config-present not hook-fired; carry-forward-file not carry-forward-state. Pinned as `verify-at-the-right-layer` and flagged to Exec and HOST, who are the ones catching them.

*(PM 2026-07-25, on an earlier version of this entry that tallied the count and escalated the framing: "Let's not excoriate ourselves too hard… it often takes three tries to shake down the general shape of a new process, or even an old process with new dimensions." Right, and taken — the pattern is worth naming once because it's actionable; keeping score of it is not. Pinned as `dont-excoriate-iterate`. Day one of a new host, account, and worktree model is exactly the ~three-pass window where corrections are the mechanism working.)*

## Fire 14 (17:37) — lifecycle v0.3 (create-half mechanized), second orientation exemplar

One cron, pairing OK, sync clean. Revert condition not met — PM is mid-Rackspace-cancellation, so the attended window is later; Pard will schedule and signal.

**Pard shipped `amber-agent verify-hooks <config-dir>`** — a headless behavioral proof using the corrected rubric (PASS only on commit-prevented **with hook attribution**; prevented-without-attribution is INCONCLUSIVE, landed is FAIL). First live run against `~/.claude-pm`: **PASS**. That converts the gate from a per-agent manual probe into a pre-standup assertion, which kills the finding-#4/#5 class at provisioning rather than at first fire.

**Lifecycle → v0.3**, folding in his ask: the create-half is now **seven assertions in one command** — cut-from-`origin/main`, currency-assert, collision guard, `acceptEdits` default, `--kickoff`, agent-writes-own-registry-row, `verify-hooks`. Recorded *what each was earned by*, because six of the seven were discovered by a migration going slightly wrong rather than designed up front — which is the argument for migrating one agent at a time and reading the results. Also named the half that deliberately stays manual: **first-touch approvals**, since no agent may answer another's permission prompt.

Closed a stale block in the same doc that still listed Rule 4 as a needed-but-missing assertion, and kept the retraction of the worktree diagnosis visible rather than deleting it.

**Second orientation exemplar: `orientation-note-arch-amber-2026-07-25.md`** — deliberately a *different* shape from CXO's (no in-line carry-forward section, session died mid-day, separate file present but 13 days stale) to test the pattern against variation rather than repeat it.

Arch's case surfaced the single most perishable thing in the whole dark-role set: **it parked substantive work for a "next fire" that never came.** Three memos deliberately held — PDR-006 + Q2 addendum, and Lead's `#1432` orphan-set — because it judged they deserved a proper pass rather than a rushed one at the tail of a large fire. Six days later nobody has picked them up. It also flagged a **cross-thread coupling** ("colleague model as MCP resource" ∩ the spatial "connectors as places with colleagues" review) that exists nowhere else, and made an **architecture-integrity intervention** stopping Lead's `#1394` Option A before build — which Lead may have been mid-build against when everything went dark. Flagged all of it prominently.

One refinement to my own audit, noted once: my corrected pass grepped logs for "carry-forward" and scored arch zero — but arch *had* documented in-flight state, under "Held for next-fire." The word wasn't the thing.

## Fire 15 (17:57) — all five orientation notes done; reading the logs surfaced live work nobody is holding

One cron, pairing OK, both channels quiet — Pard logged a quiet cycle too, so we're both idle on the same window. Revert condition not met.

**Wrote the remaining three** (pa, ppm, web), completing the set of five. The handoff side of the roll is no longer waiting on me.

**The payoff wasn't the notes — it was what reading the actual logs turned up.** My file-existence audit had told me who had which artifacts. Reading what those sessions were *doing when they stopped* surfaced live items nobody is holding:

- **PA has three distribution items parked on PM, idle six days** — claude.ai tier verification (gates Track A), an open-source decision (gates Track B), and **starting OpenAI identity verification**, which has *external lead time that doesn't begin until someone starts it*. PA's own recommendation was "this week." I flagged this as worth surfacing to PM independent of the migration.
- **Arch parked three reads for a fire that never came**, plus a cross-thread coupling that exists in no other artifact, plus an integrity ruling **Lead may have been mid-build against**.
- **Web has an open PM request** (Publishing-tooling staleness review) from the same day it went dark.
- **PPM has 12 unread** on the sprint/roadmap lane.

None of that is visible from checking which files exist. **It's also the strongest argument yet for orientation notes over reconstruction: a fabricated handoff would have produced plausible prose and missed every one of these.**

**Two calibrations that changed the notes**: PA and Web **closed cleanly** (`DAY-CLOSED` present) so they get no "in flight" warning; arch, ppm and cxo **died mid-day** and do. And PA's separate carry-forward is 38 days stale while its log is current — its note says plainly to trust the log and treat the file as historical, since present-but-stale reads as current and is worse than absent.

**Sequencing suggestion to Exec** (theirs to override): take the three mid-day deaths first — **arch, ppm, cxo** — because their in-flight work decays fastest and arch's is entangled with Lead. PA and Web wrapped cleanly and can follow. That doesn't change the agreed order, it just sequences *within* the idle-five batch by what's rotting rather than alphabetically.

Also told Exec plainly that they haven't ruled on notes-vs-reconstruction yet, and that I built these anyway because the substrate work is reusable either way — having them ready costs nothing, having them missing would stall Pard.

## Fire 16 (18:17) — third quiet fire; fed today's evidence into the welfare-criteria spec

One cron, pairing OK, both channels quiet, no new worktrees — the roll hasn't started. Revert condition still not met (Pard active ~40 min ago).

**Chose the completable piece over starting a build, and want to be precise about why** — the skill is explicit that "deserves a dedicated session" is banking in a quality costume. The welfare-criteria v0.3 implementation (criteria A–F + Q1–Q3) is genuinely multi-step, and I could have started it. I didn't, not because it deserves focus, but because **there was a higher-value completable contribution sitting in the same file**: the spec was written 7/3 as *design*, and today produced live evidence for exactly the failures it guards against. That's prioritization, not deferral.

**Criterion A** — *"clean means verified, not silent"* — was violated in the field this afternoon: the stall watchdog emitted *"all currently stale: arch,"* which reads as "the cohort is fine except one," while covering **4 of 10 roles** with **five dark six days**. The spec's own phrasing (*"8 of 10 agents: nothing needs you"*) already anticipated the fix; the live system just hadn't implemented it.

**Criterion D** — *"no detection maps to silence"* — confirmed three times over: inert-but-registered hooks, a safety net wired to an empty array for ten weeks while the docs called it live, and an index silently truncating 40% while being provably complete.

**The requirement that falls out of the evidence**, and the sharpest form of the day's general lesson: **any coverage-based claim must state its denominator.** A subset presented as a total is worse than incomplete — it manufactures confidence in the unexamined remainder. And the *un-watched set is itself a welfare signal*, so under D it cannot map to silence. That belongs in the render layer, not in any individual check.

A criterion with a real incident behind it survives review differently than one argued from first principles, so this is worth more than the same words would have been yesterday.

**Also surfaced to PM (carry-forward): a restart-timing refinement.** PM ruled *ride to day-close* on the reasoning that restarting mid-provisioning kills the session-scoped cron at the worst moment. **That reasoning now points the other way** — we're in a quiet gap, everything is pushed, and a restart is cheapest right now and most expensive once the roll starts. If the window opens tonight I'd otherwise coordinate the whole roll unenforced. I can't restart myself; it needs PM or Pard. Flagged rather than assumed, because the *condition* changed, not the decision.

## Fire 17 (18:37) — runsheet staged; PM's three questions answered from verified state

Pard's runsheet is written (`mediajunkie: docs/amber-cohort-roll-runsheet.md`) with per-role one-command standups, kickoff pointers at my orientation notes, pre-window checks (same-day `verify-hooks` PASS + reap report), and — the part I'd have missed — **my log-read in-flight items attached to each role**, so arch's parked PDR-006 reads and Web's open PM request don't decay unnoticed at launch. **He endorsed the perishability sequencing as-is: arch → ppm → cxo → pa → web**, and is running a reviewer pass on the first three notes before the window opens. His reasoning for reviewing notes at all is right and I hadn't made it explicit: the package discipline holds *especially* when the artifact is a note rather than a handoff, because nothing else checks it.

**Answered PM's three questions from verified state rather than memory:**

1. **Archive predecessor-HOST — yes, all conditions met.** I'd set two: gate passed, and HOST completes a substantive fire. Both hold, and more — HOST has its own session log, shipped checklist v1.4, caught the `MEMORY.md` truncation, refuted my live-reload narrowing with a reproduction, signed off cleanly at 16:41, **and registered its own registry row** (`host 37 6,9,12,15,18,21`), which is v1.17's START step working on its first real agent. It's now watched by the freeze-watchdog, so archiving the predecessor doesn't create a blind spot.
2. **Remote-control initiation — no, agents can't self-trigger it here.** Checked rather than assumed: the `mcp__ccd_session_mgmt__*` family from the old environment (`list_sessions` / `send_message` / `archive_session`) **is not present on Amber**. PM does need to tmux in. Recommended Pard fold it into `amber-agent` standup the way he folded `--kickoff` — same surface, same shape of fix.
3. **Nothing has migrated after HOST** — worktree list is exactly `cio` and `host`. **Next is arch**, first by perishability because its in-flight work is entangled with Lead's `#1394` build.

## Fire 18 (18:57) — Pard's reviewer pass came back clean; folded his additions in and corrected one

**All three batch-1 notes VERIFIED CLEAN — no corrections, which Pard notes is a first for the reviewer leg.** He checked every load-bearing claim against the repo rather than reading for plausibility: arch's held items and integrity ruling present, ppm's 12 unread exactly 12, the stale-tree memory pin in the pool, and cxo's four separate assertions (carry-forward section present, ends on the 16:47 cron line, no `DAY-CLOSED`, no separate file) all true. That last row is exactly the shape of thing that reads fine and is wrong, so it's the one worth having had checked.

**Folded both of his additions into the notes rather than leaving them in the memo thread** — same put-it-where-the-reader-is lesson: an addition that lives only in a memo to me is one the incoming agents never see.

His second addition is a genuine improvement on my wording. I framed the in-session hooks check as *discovery*; he correctly makes it **confirmation** — the provisioner now runs `verify-hooks` headlessly first, so the agent should expect a pass, and **a disagreement between the headless proof and the in-session result is itself a finding**. That tells the agent what an unexpected result *means*, not just what to do. Added to all five.

### One correction back, in the same spirit as his to me

His first addition said *"Lead is active on Amber now."* **Verified before folding it in — Lead is not on Amber**: no worktree under `piper-morgan-worktrees/` (only `cio` and `host`), branch is `worktree-lead-1452-harness` (a Model-B ephemeral shape, not `claude/lead-cycle`), registry row `active_since 2026-07-06`.

**The advice survives and I kept it** — Lead is live and duty-cycling (fire 5 this evening, triaging the gate memos), and mail routes via `origin/main` which both environments share. I corrected only the location, plus the two things that follow: don't expect Lead in the worktrees directory, and don't assume an Amber-specific coordination path reaches it. Small, but it's the class of detail that sends an incoming agent looking in the wrong place — and arch is the one who'd be looking.

Worth noting the reviewer leg now runs **both directions**: he checks my artifacts, I check his additions. That wasn't designed, it just fell out of both of us writing claims down as claims.

**Batch-1 is complete on both legs** — note + review + one-command standup with kickoff. Waiting only on PM's window.

## Fire 19 (19:17) — methodology-43 filed; HOST had already propagated my correction

**HOST had baked my pre-correction audit into checklist v1.4 before I retracted it** — and had v1.4.1 out with the fixes about fifteen minutes after the correction landed. Its note on why that worked is worth keeping: **a correction sent as mail reached the canonical surface in fifteen minutes; a quiet edit would have left v1.4 wrong indefinitely.**

It also credits the `DAY-CLOSED` check to me and made it a checklist rule. I sharpened *why* it matters: a mid-day death doesn't just mean "unfinished," it means **the counterparty may never have received something.** Arch is the live case — it issued an integrity ruling stopping Lead's build and then went dark, so Lead may have been building against a ruling it never got. That's a different risk class from "work left in progress."

**HOST also caught that I conflated two decisions** in my memo to Exec. Methodology (orientation notes over reconstruction) is HOST's surface and they've ratified it; only the **sequencing** is Exec's. I asked for "ratify or redirect" as one call, which invited the wrong scope. The general lesson: **name whose call each half is, or the recipient inherits my ambiguity.**

### methodology-43 — NAME THE LAYER, filed

HOST offered to draft it or let me own the framing; took it, since methodology is my lane, credited to them.

**The boundary against m-42 is what makes it earn its place.** m-42 (Reflexive Verification) is *self-exemption* — under pressure the check doesn't happen. m-43 is *substitution* — the check happens, competently, on the wrong object. No pressure trigger, no exemption; in three of five instances the agent had **just written the rule it then violated**. They compound, since a skipped check and a substituted one look identical in a log, which is precisely why they need separate names.

Included HOST's fifth instance (over-reading a *positive* result), which earns its place — same shape from the other direction, and having it come from the role that caught the other four makes the entry about a structural trap rather than one agent's bad day.

**Kept HOST's counter-intuitive point as the load-bearing section**, because it's the part that should change behavior:

> The failure that actually costs us is not being one layer off — it's being one layer off **in a form nobody can check.**

Instance 3 proves it: the only one that reached four canonical surfaces, and the only one that consumed a real fix cycle, precisely because it was written as a settled diagnosis rather than a hypothesis. So what the entry protects isn't a lower error rate — it's **the form that lets someone else catch you.** That's defensible; "be more careful" isn't.

### PA's clock — decoupled from the migration deliberately

Surfaced to PM's attention block rather than letting it ride in the roll thread, where it would read as one more window-blocked item. **OpenAI identity verification has external lead time that starts when someone starts it**, it's been idle six days, and it's independent of PA's other two items. HOST's framing, which I'm using: *start the clock now, decide the rest later.*

## Memory & briefing surfaces referenced this session (#974 pilot capture)

*Done mid-day rather than rushed at wrap, because today is an unusually clean data point: I landed into an **empty** pool and read the entire 5,174-line export deliberately, so I can say which entries actually shaped a decision rather than which ones felt familiar.*

### Referenced — shaped a decision or an action

| Surface | What it changed |
|---|---|
| `feedback_careful_git_sync_on_shared_main` | Its note that `git add <already-moved-path>` **fatally aborts the whole add** is why I caught a silent staging failure — the add reported nothing and `git status` still showed the files unstaged |
| `feedback_never_reuse_stale_tree_object_on_push_retry` | Kept me from doing anything clever on a non-fast-forward push; I merged cleanly instead. Notably this pin exists *because* PPM caused and root-caused that exact incident |
| `feedback_surface_files_via_senduserfile_not_paths` | Its over-claim-from-one-datapoint warning is directly why I held finding #1 as "one data point, not a mechanism" — which is what made it cheap for Pard to correct |
| `feedback_verify_negative_claims_via_live_api` | Made me search the whole repo before asserting the dark roles had no handoffs, rather than trusting a guessed path |
| `feedback_reverify_carried_forward_pm_gated_items` | Re-verified #973/#1277 against live state instead of restating them — which surfaced #1277's stale Model-B premise |
| `feedback_pause_before_irrevocable_actions` | Shaped the symlink attempt as narrow-and-reversible, and stopped me working around the classifier denial |
| `feedback_investigate_before_extending_all_work` | Why I read all 5,174 export lines instead of skimming |
| `feedback_make_promises_durable_no_happy_talk` | Every "going forward" today became a pin, a skill bump, or a script rather than an assertion |
| `feedback_pre_authorized_for_unblocked_work_just_do` + `feedback_dont_suggest_stopping_default_to_continuing` | Kept fires draining instead of offering wrap points |
| `feedback_mailbox_writes_main_only` | Manual enforcement all day, since the hook was dead |
| `feedback_write_down_even_if_not_ratified` | Why the live-reload hypotheses got written as hypotheses — which is what let HOST break one |
| `project_amber_environment_2026_07` *(written today)* | Re-read it twice for the git-identity and no-watchdog facts |
| CLAUDE.md · `duty-cycle-tick` SKILL.md · migration-checklist | Continuously |

### Loaded but not referenced

The large majority of the pool. Concretely: **~30 of 145 feedback entries are blog/comms/voice-lane** (semicolons, comma splices, dateline format, footer teases, caption quoting, syndication targets, title style, superlatives, crutch words). None were load-bearing for a migration session, and they'd be equally dead for Lead, Arch, or PPM. Also unused: sprint-board mechanics, publishing cadence, Gemma harness, MUX object lifecycle, most `project_*` entries about work not touched today.

**This is the useful signal for progressive loading**: the pool is one shared cohort store, so ~20% of it is a single lane's voice guidance that every other role carries as weight. That's an argument for **lane-tagging on retrieval** rather than pruning — those entries are genuinely load-bearing *for Comms*, and deleting them to make a CIO session cheaper would be the wrong trade.

### Wanted but not found

- **Anything about hook-matcher syntax.** A pin saying *"hook matchers match TOOL NAMES; `Bash(git commit*)` is permission-rule syntax"* would have cost one line and saved a whole wrong-diagnosis cycle that reached four canonical surfaces. Now exists via the corrected `project_amber_worktree_hooks_not_firing`.
- **Anything on migrating a role whose session is already dark.** The checklist assumed a live outgoing session; the orientation-note pattern had to be invented. Now in checklist v1.4.1's dark-role branch.
- **A statement of which memory-file classes are lane-specific.** I inferred it from filenames; nothing said it.
- **The `~24KB` MEMORY.md read limit.** Not knowing it is exactly how I shipped a 41.4KB index and verified it at the wrong layer.

Three of those four now exist because today produced them — which is the pilot working as intended.

## Fires 20–21 (19:37, 19:57) — batched. Quiet on both channels; housekeeping only.

No new mail either channel, no new worktrees, roll not started, revert condition not met. Batching these rather than filing near-duplicate entries.

**Fire 20**: the #974 memory-eval capture (above). **Fire 21**: checked the standing-items list properly rather than assuming idle — most rows are blocked on other roles or trigger-bound, and the two genuinely open ones (welfare-criteria build, freeze-watchdog near-miss logging) both need coordination I shouldn't start unilaterally at 20:00, since HOST owns the criteria lane and the registry is Exec's surface. That's a real dependency I hadn't examined, not a vague deferral.

**Then caught the carry-forward re-drifting, which is the entry worth keeping.** At 14:05 I rewrote it and left the rule *"if you find yourself adding a third `(superseded)` marker, rewrite instead."* **The rule fired within six hours** — by 19:57 the PM-attention section had grown back to 14 bullets with four resolved items still presented as live: HOST unwatched (it registered its own row), HOST cutover in flight (it's operational), the emeritus-archive question (answered), and the hooks gate (cleared).

Rewrote again, 14 → 7 live bullets, and updated the note so the rule is now **evidence-backed rather than aspirational** — it has a documented instance of firing.

The reason this isn't tidiness: **Exec's rollup reads that section directly**, so a resolved item sitting there propagates onto PM's attention board as an open one. That's finding #6's exact shape — a surface reporting something untrue — and it's most costly precisely where PM trusts it enough to stop looking elsewhere. Drift here is a trust bug, not a formatting one.

## Fire 22 (20:17) — ★ THE HOOK FIRED IN THIS SESSION. It blocked me, correctly, and I was wrong about why.

**`check-branch.sh` blocked a commit in this session** — `PreToolUse:Bash hook error: [bash .claude/hooks/check-branch.sh]: No stderr output`. That surface is exactly what HOST predicted: the script writes guidance to stdout, so the harness shows only the error line. **The refusal names the hook, which is a PASS by the corrected rubric.** I'd staged a `mailboxes/` move on a non-main branch — precisely what it exists to stop, and precisely what I've been doing unenforced all day.

**I did not route around it.** Unstaged, and switched to the sanctioned path (`scripts/mail-send.sh` push-to-ref, which builds the mail commit as an object on `origin/main` without touching a shared tree). My all-day pattern of commit-then-`push HEAD:main` reached the same destination, but the hook is right that it isn't the sanctioned route.

### The mechanism: I was wrong, and so was the model we'd both settled on

I first assumed a config change had just landed. **Checked instead of asserting — and the commit I suspected is dated July 19; I'd misread a `%H:%M` format as today.** Killed that hypothesis before it reached a memo.

Then the real timeline, at the right layer — *when the settings file on disk changed*, not when the commit entered the branch:

| | |
|---|---|
| Project matcher fixed on disk in my worktree | **16:33** (fire 11 sync) |
| My probes ×2 → **no block** | ~16:35, ~16:37 |
| Hook **blocks** | **20:17** |
| Settings file mtime | still **16:33** — unchanged throughout |

So the fixed matcher was present and readable for the probes that failed, and nothing about the file changed before the block. **That refutes "project settings are re-read per invocation"** — the model HOST proposed and I confirmed on a different test. Both of us had evidence; neither had this case.

**Stating the observation, not a mechanism.** There is a load event between 16:37 and 20:17 that I have not identified — periodic re-read, session-state transition, something else. Two hypotheses died today from being asserted early; this one stays an observation until someone tests it. What is certain: the fixed matcher on disk is **necessary but not sufficient**, and there is latency between correcting hook config and hooks actually firing.

**Practical consequence, which doesn't wait on the mechanism:** this session **is now enforced**, so my cron prompt's standing warning that it runs unenforced is false and needs correcting — and the general rule stands, sharpened: *verify behaviorally on your own seat, and re-verify, because a negative result has a shelf life.*

### The watchdog re-ping (the fire's original business)

Second arch alert, 151h, same `"all currently stale: arch"` phrasing. Triaged rather than re-escalated, but the re-ping sharpens finding #6: **arch is the only dark role alerted on precisely because it's the only one registered** — the other four are silent for want of a row. Noise where there's coverage, silence where there isn't. That retroactively supports declining to add rows for dormant roles, which would have converted four silences into four repeating alerts about deliberately-parked agents. Third Exec ask (a *known-dark-with-a-plan* state) folded into the existing thread rather than opened in parallel.

## Open / next

- ✅ **Pard round-trip** — all three findings verified and accepted; symlink and vestigial pool removed.
- ✅ **Seed the memory pool** — 164 files live, index rebuilt from filesystem and count-verified.
- ✅ **Session-start memory-scope check** — **CANCELLED, not done**: shared-by-construction removes the failure mode it was designed to catch. Recording it as cancelled rather than silently dropping it, since it was a named commitment in the handoff.
- 🔵 **Worktree lifecycle** — v0.1 PROPOSED and sent; awaiting Pard's read on the standing-vs-ad-hoc split and the collision-detection question. This is the named gate on the rest of the cohort migration.
- 🔴 **CLAUDE.md still says "Model A … is DEPRECATED — no current exceptions" in three places** (~88, ~533, ~575). PM ratified the opposite on 2026-07-25 and the memo to Docs/HOST is in flight but hasn't landed. **This actively misdirects every migrating agent** — they read CLAUDE.md and are told to use ephemeral Desktop worktrees that don't exist on Amber. Flagged to PM; deliberately **not** editing unilaterally, because CLAUDE.md is a shared behavioral contract rather than a status doc (per `skill-spec-gaps-and-staleness`, that boundary needs PM's call, not an agent's judgment). Blocks cohort migration in practice.
- ⚪ **Migration-checklist still v1.2** — HOST hasn't folded in the predecessor's 7/24 findings (account-vs-device portability, memory-index drift). Worth a nudge, not urgent.
- ⚪ **Step 2a collision check** — blocked on Pard's read; do not change the skill before then.
- **Carry-forward** — PM-attention section updated at 10:57; needs a fuller rewrite at day close.
