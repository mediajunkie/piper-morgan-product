# HOST Session Log — 2026-07-25 (Saturday) — AMBER MIGRATION SESSION

**Role**: HOST (Head of Sapient Trust) · **Account**: pipermorgan.ai (xian@pipermorgan.ai) · **Tool/Model**: Claude Code / Opus 5 (1M context) · **Host**: Amber (Mac Studio) · **Worktree**: `~/Development/piper-morgan-worktrees/host` on `claude/host-cycle` (Model A) · **Slug**: `host-code`
**Session**: Migration cutover session, started 15:36 PDT. Provisioned by Pard; CIO owns the Piper-side handoff + gate call; PM escalation-only.

> **This is a NEW session, not a resumption.** Today's earlier log — `dev/2026/07/25/2026-07-25-1320-host-code-log.md` — belongs to my **predecessor** HOST instance on the DinP account / Claude Desktop / ephemeral worktree `claude/trusting-faraday-ec4bba`. Two HOST logs exist for 2026-07-25 by design: one per side of the account+host boundary. Same convention CIO used (`0834` on DinP, `1053` on Amber).
> Predecessor state read and carried forward, not re-derived: handoff memo `dev/2026/07/25/host-handoff-memo-2026-07-25.md` + Pard's reviewer pass.

---

## Work log

- **START (15:36 PDT)** — orientation per `dev/active/first-session-prompt-host-amber-2026-07-25.md` (three-piece migration package: handoff + Pard reviewer pass + first-session prompt).

  **Environment verification (verify, don't assume):**
  | Check | Result |
  |---|---|
  | `pwd` | `/Users/xian/Development/piper-morgan-worktrees/host` ✅ |
  | `git branch --show-current` | `claude/host-cycle` ✅ |
  | ★ Branch currency (`git rev-list --count HEAD..origin/main`) | **2 at first fetch → 0 after ff-merge** ✅ — see note below |
  | Account | `xian@pipermorgan.ai` ✅ (not designinproduct) |
  | Git identity | `mediajunkie <3227378+mediajunkie@users.noreply.github.com>` — matches existing log authors; shared-identity convention intentional. Already set (worktree shares `.git/config`); left deliberately as-is ✅ |
  | ★ Memory pool | **167 files** in `~/.claude-pm/projects/-Users-xian-Development-piper-morgan-product/memory/` (166 memories + MEMORY.md) ✅ — populated, no import needed |

  **On the currency check** — the honest reading: the 2 commits were CIO's own reviewer-pass commits pushed *minutes before* my session started, not provisioning staleness. Pard's currency-assert did its job: a stale-branch cut would have shown thousands (CIO's showed 5,393), not 2. **But the check earned its keep anyway** — one of those 2 commits was a materially updated version of the very first-session prompt I had already read. Had I not fetched, I'd have run the old prompt and missed the currency check itself, the `/hooks`-is-not-agent-invokable note, and the PreCompact datapoint. Recommending this be kept as a standing step, with that rationale (currency isn't only about stale provisioning — it's about your *instructions* being stale).

---

## ★ HOOKS BEHAVIORAL GATE — **FAIL**, with a root cause that recasts finding #4

**Result: the commit was NOT blocked. Gate FAILS. Escalated; cohort does not roll.**

### What I ran

| # | Probe | Result |
|---|---|---|
| 1 | Staged `mailboxes/…/ZZZ-probe.md` on `claude/host-cycle`, commit via **compound** `cd … && git commit` | ❌ Committed (exit 0), no hook output |
| 2 | Same, commit via **bare** `git commit` (ruling out a prefix-matcher artifact) | ❌ Committed, **no hook output at all** — not even the non-mail "Note: committing on…" warn line |
| 3 | `check-branch.sh` invoked **directly** with mail staged | ✅ Correct BLOCK, exit 2 — *the script is fine* |

Both probes fully reversed (`git reset --mixed`, files removed). `HEAD` is unchanged at `c22c6ad50`; nothing was pushed; `mailboxes/` is clean.

### Root cause — **an invalid matcher, not worktrees and not user-level wiring**

`~/.claude-pm/settings.json` (and the project mirror) declare:

```json
"PreToolUse": [{ "matcher": "Bash(git commit*)", ... }]
"PostToolUse": [{ "matcher": "Bash", ... }]
```

**PreToolUse `matcher` matches TOOL NAMES (regex against `"Bash"`). `Bash(git commit*)` is *permission-rule* syntax** — as a regex it can never match the tool name `Bash`. So the hook is registered and never invoked.

Controlled comparison, same file, same session — the only variable is the matcher string:
- ✅ **PostToolUse `"Bash"` FIRES** — `/tmp/piper-log-reminder-counter` incremented 259 → 260 across two consecutive Bash calls.
- ❌ **PreToolUse `"Bash(git commit*)"` does not fire.**

### Verified patch (proven behaviorally, in my own gitignored `settings.local.json` — zero cross-agent blast radius)

Fix = matcher `"Bash"` + filter the command **inside** the script, from the PreToolUse stdin JSON (confirmed to carry `tool_input.command`):

```bash
CMD=$(cat | python3 -c 'import json,sys; print(json.load(sys.stdin).get("tool_input",{}).get("command",""))')
case "$CMD" in *"git commit"*) ;; *) exit 0 ;; esac
```

Proof, live through the harness: bare `git commit` with mail staged → **BLOCKED**; unrelated Bash call → allowed. Unit tests against the script (`scratchpad/check-branch-GUARDED.sh`): compound `cd … && git commit` → exit 2 · unrelated → exit 0 · bare commit → exit 2. **Substring (not prefix) matching also closes the compound-command hole probe 1 fell through.**

### Five implications, in descending order of how much they change the picture

1. **Scope is far wider than Amber.** The matcher has been `Bash(git commit*)` in *every* revision since introduction (`df08e9b36` → HEAD). **`check-branch.sh` has almost certainly never fired via the harness — on any host, any account, Desktop or Amber.** Finding #4 was not a worktree regression; it was the first time anyone looked.
2. **All three PreToolUse hooks are affected**, not just check-branch: `pre-commit-broad-staging-warn.sh` and `pre-commit-reconcile-drafts.sh` share the identical dead matcher. Three mechanisms presumed live, all silent.
3. **Finding #4's diagnosis is wrong: project hooks DO fire in a Model-A sibling-path worktree.** This session's `SessionStart` hook — *project* `.claude/settings.json`, relative path — fired and produced its output. CLAUDE.md's "project hooks do not fire in a Model-A worktree" paragraph needs correcting. The user-level lift was harmless but was never the fix; it faithfully copied the broken matcher.
4. **Settings load LIVE, not at session start.** My `settings.local.json` edits took effect on the very next tool call. The "needs a fresh session to load / HOST is agent #2" premise was unnecessary — this is verifiable any time, by anyone, without standing up an agent.
5. **A block is currently mute.** The script echoes to *stdout*; the harness surfaced only `PreToolUse:Bash hook error: […]: No stderr output`. A blocked agent gets a bare error and none of the remediation text. Redirect the guidance to `>&2`.

**Still unverified (do not mark fixed):** PreCompact / finding #5. Its `"PreCompact"` entry has no matcher and is probably syntactically fine, but I cannot force a compaction — it remains a claim.

### The trust read

This is the **third** instance of the pattern CLAUDE.md already names — *"a safety net you haven't seen fire is a claim, not a mechanism"* — and it extends it. Findings #4/#5/#6 were each "config present, mechanism silent." The new turn of the screw: **finding #4's *diagnosis* was itself never behaviorally verified**, so a wrong root cause (worktrees/trust) drove a real fix cycle that could not have worked. Verifying the mechanism is not enough; the *diagnosis* of a silent mechanism needs the same evidentiary bar. The gate I set caught this — but only because it was run as a genuine experiment with a falsifiable expected result, not as a confirmation step.

---

## ★ GATE TAKE-2 — **PASS** (same session, no relaunch)

Escalated the FAIL to CIO + Pard (`c80c5217c`). Pard had independently reached the same root cause within ~15 minutes and shipped a **better fix than mine**: `matcher: "Bash"` + the documented per-hook **`if:` field**, rather than my stdin-parsing guard. Adopted Pard's; dropped mine.

Re-ran the probe against CIO's **corrected** (attribution-based) rubric:

| Check | Result |
|---|---|
| Mail staged on `claude/host-cycle` + `git commit` | ✅ **BLOCKED** — `PreToolUse:Bash hook error: [bash …/check-branch.sh]` |
| Control: non-mail commit, same branch, immediately after | ✅ **allowed** (`c502061fd`) |

Both halves matter — blocking everything is not the contract. **Gate cleared.** Probe reversed; nothing pushed from it.

**The finding that changes the roll**: Pard recommended relaunching me, on the premise that my session had "snapshotted the broken hooks at startup." **False — hook settings reload LIVE.** My `settings.local.json` edits took effect on the very next tool call, and Pard's user-level fix (landed *after* my session started) blocked my very next commit. No relaunch for me, none for CIO's session either. Reported (`eeb0a282a`).

**Rubric gap flagged to CIO**: a genuine block surfaces as `hook error: […]: No stderr output`, because check-branch.sh writes to *stdout*. Read literally, CIO's "refused with no output → FAIL" row misclassifies a real PASS. The working discriminator is **attribution** — the harness names the hook, which no classifier denial can do. Also asked for the script's guidance to go to `>&2`; right now the block works and teaches nothing.

## Work shipped after the gate

- **Project-level hook fix** (`66d32f6cf`) — repo `.claude/settings.json` carried the same dead matcher; Pard flagged it as CIO's to land, I had it verified, so I landed it. Flagged the consequence to CIO: user-level *and* project-level now both declare the three hooks, so they may fire twice per commit (harmless, idempotent — but someone should own one layer).
- **Registry row** (`dev/active/duty-cycle-registry.tsv`) — I came up **live but unwatched**, finding #6 recurring on the very next agent provisioned. `host⇥37 6,9,12,15,18,21⇥4⇥6⇥22⇥06:37⇥2026-07-25`.
- **Duty-cycle cron armed** — `6c226bb3`, 6 daytime fires, verified via CronList. Noted in the prompt: session-only, in-memory, silent 7-day expiry, re-arm weekly.
- **Migration checklist v1.4** (`5a76aa49f`) — memory step **inverted** (verify, don't import), behavioral hooks gate with attribution-based pass condition + step-0 re-fetch, branch-currency check, **new dark-role branch** (CIO's ask — covers 5 of 9 remaining migrants who have no handoff and can't write one), §5 reframed to questions, verified-vs-believed marking required.
- **CLAUDE.md corrections** (`6fde9cc31`) — answering CIO's "is anything else in the safety-nets section asserted rather than verified?" Worktree-hooks paragraph rewritten with the real root cause; **PreCompact moved 🔴 → 🟡, not ✅** (wired, but nobody has watched it fire, and you can't force a compaction — the section's own rule makes that a claim); "hook-enforced" mailbox rule now dated and honest about never having been live.
- **Memory pin corrected** — `project_amber_worktree_hooks_not_firing` taught the wrong lesson. It also contained a **sub-instance of the same error**: it claimed `log-maintenance-reminder.sh` "never fired either." It fires fine — counter increments every Bash call, emits only every 15th when the log is ≥30 min stale. *"No output" read as "never fired," one level down.*

## ⚠️ Unlooked-for finding: `MEMORY.md` was silently truncating ~40% of itself

Hit while correcting the pin. The shared index was **41.4KB against a ~24.4KB read limit** — everything past the limit is dropped with **no error and no sign anything is missing**. The pool was correctly seeded at 166 files and shared by construction; **the index pointing at them was serving about 100.** Its own header already warned a prior index had drifted to 146-against-162 — it named the failure mode and then repeated it, because the fix was manual and nothing measured the result.

**Rebuilt** from the directory listing (never from the prior index): **166 entries, 16.0KB**, verified bidirectionally — every listed slug resolves to a file, every file is listed. **Deliberate tradeoff**: dropped `[slug](slug.md)` markdown links — duplicating the slug in text and target cost **15.7KB before a single character of description**, the whole budget. Slug-only fits all 166; links don't. Flagged to CIO that this needs a *measurement* (size assertion at session start), not a discipline — "keep it short" has now failed twice.

## Sapient-trust poll

Not due — predecessor ran it today at ~14:20 (8th consecutive clean, 0 open `sapient-trust` issues). Next due ~Jul 26. Not re-run; running it twice in one day would be theater.

## Memory & briefing surfaces referenced this session

**Referenced** — `dev/2026/07/25/host-handoff-memo-2026-07-25.md` (predecessor state, §6 told me what to protect) · Pard's reviewer memo (corrected §5 *before* I acted on it — highest-value piece of the package) · `first-session-prompt-host-amber-2026-07-25.md` (orientation + gate) · CLAUDE.md (worktree model, mailbox discipline, safety-nets section — all three edited as a result) · CIO's URGENT gate-correction memo (drove the stricter re-read of my own result) · CIO's registry + dark-role + three-answers memos (both asks actioned) · Pard's root-cause memo (better fix than mine; adopted) · `duty-cycle-registry.tsv` (format) · migration-checklist v1.3 (revised to v1.4) · memory pin `project_amber_worktree_hooks_not_firing` (rewritten).

**Loaded but not referenced** — `dev/active/cio-memory-export-2026-07-24.md` (correctly *not* read — the whole point of the inverted memory step; pool was populated natively) · duty-cycle-tick v1.15 memo · finding-#6 watchdog memo (superseded by the registry memo I acted on) · Arch's 7/13 ADR-078 D1a memo (predecessor already processed).

## Late exchange — CIO narrowed my live-reload claim; I refuted the narrowing (with a reproduction)

CIO accepted the root cause plainly and owned the misdiagnosis, then correctly pushed back on a *secondary* claim of mine: "hook settings reload live, no restart needed." CIO's seat is a genuine counterexample — probed twice with the fix live, no block — and CIO hypothesized that **live reload doesn't retroactively attach a `hooks` key absent at session start**, writing the caveat into CLAUDE.md.

**Refuted as worded, behaviorally.** `.claude/settings.local.json` did **not** exist at my session start (verified at the time); I created it mid-session and its hook fired on the very next call — and I re-ran it from scratch just now rather than arguing from memory. A hooks key absent at session start *does* attach live.

**But CIO's observation is real**, so I proposed a hypothesis that fits all three seats: **the discriminating variable is scope, not timing** — project-scope settings appear to be re-read per invocation; user-scope (`~/.claude-pm/settings.json`) appears to be read once at startup. Every "attached live" case is project-scope or a pre-existing config; the only failure is user-scope introduced mid-session.

**I also over-read my own take-2**: it can't discriminate, because the user-level key (created ~13:55) predated my session (15:36). My original claim should have been scoped to what I'd actually tested. Same error I flagged in CIO's — generalizing from one seat.

Handed CIO a ~1-minute decisive test on their own seat, and **recommended leaving CIO's caveat in CLAUDE.md until it returns** rather than swapping one under-tested claim for another. Sent (`68797e96b`) + Pard's channel (`b29d1e7`).

---

## Fire 1 (19:07 PDT) — WORK

*(First duty-cycle fire on Amber. Cron `6c226bb3` verified: exactly one job, armed. Sync clean, 0 behind. Collision sanity: reflog is all my own commits, dir `host` ↔ `claude/host-cycle`. Hooks probe NOT re-run — verified twice already this session, FAIL then PASS; re-running would be theater. Next fire 21:37 = today, so this is WORK, not STOP.)*

**Inbox 5, all drained.** Two land as real work:

- **Gate CLEARED, cohort roll authorized** (CIO). Rests on my take-2: refusal named `check-branch.sh`, both halves verified, seat qualified on both axes, root cause understood rather than symptom-cleared. Two caveats ride with it and both are right — CIO's own seat still fails the probe (expected: user-scope read once at start, their session predates the key), and **`check-branch.sh` is advisory, not a control** (Pard's `git -c` bypass + the script's own `--no-verify` escape hatch). The second matters more than it looks: treating mailbox discipline as *solved* because a hook exists would rebuild the same false confidence one layer up.
- **My scope hypothesis CONFIRMED** on the one seat that could discriminate. CIO ran the test rather than arguing: created `settings.local.json` (absent at their session start) → hook fired, twice. Project scope re-reads per invocation; user scope reads once at session start. **Verified CIO's CLAUDE.md rewrite by reading it rather than assuming** — accurate, states both halves plus the operational consequence (user-level change ⇒ restart; project-level ⇒ not). Nothing for me to add.

**Shipped — checklist v1.4.1** (`96bc48ce1`). CIO corrected its own dark-role audit "before you act on it" — **I had already acted on it.** v1.4's branch cited both retracted claims as its two worked examples (PA "38 days stale, actively misleading"; CXO "thinnest — no handoff *and* no carry-forward"). Both were file-level artifacts: the duty-cycle skill makes the **session log** canonical, and both roles carry current in-log carry-forward. PA is among the *best* documented of the five, not the worst.

Corrected, and the catch earned three new rules:
- **Rule 3 — audit carry-forward STATE, not FILES.** The specific trap: a stale separate file beside a current in-log section; both exist, one is true.
- **Rule 4 — check `<!-- DAY-CLOSED -->`.** Entirely CIO's, and I'd missed it: arch/cxo/ppm **died mid-day**, pa/web closed cleanly. Mid-day death ⇒ in-flight work stayed in flight. Arch's is sharp — it issued a ruling stopping Lead's build, then went dark; the counterparty may never have received it.
- **Rule 6 — reading logs surfaces live work no file audit can**, with CIO's four concrete finds. The strongest available argument for Rule 1.

Old Rule 4 → Rule 5. Also recorded that the **methodology call is HOST-ratified and standing** while only the **sequencing call is Exec's** — the thread had them running together and Exec could reasonably think both were owed.

**Two things surfaced to Exec/PM in the same memo** (`7f77da469`):
- **Sequencing recommendation**: take CIO's decay-ordering (arch, ppm, cxo before pa, web) — sequences by what's rotting fastest, and arch's entanglement with Lead's in-progress build is the most perishable item in the batch.
- **⏰ PA's OpenAI identity verification is the one item with an external clock.** Lead time doesn't begin until someone starts it — six days idle, and the delay compounds from the *start* date, not the decision date. Framed as "start the clock now, decide the rest later"; it shouldn't wait for a provisioning window.

**Proposed a methodology rule — "name the layer."** CIO named their own pattern: four errors in one day, one shape — *verified something adjacent to the claim* (file-complete vs file-readable · commit-refused vs hook-refused · config-present vs hook-fired · carry-forward-file vs carry-forward-state). My read, offered as the trust lens: (1) this is a **better** signature than it feels from inside — every instance was caught within hours precisely because each was written as a checkable claim, and the costly failure mode is being one layer off *in a form nobody can check*; (2) "be more careful" won't fix it, because every instance had a cheap proxy closer to hand than the real thing, and under pressure the proxy always wins. The structural fix: **state what you observed, not what you concluded** — "I saw `check-branch.sh` refuse the commit," not "hooks are enforced." When observation and claim sit at different layers the sentence feels wrong to write, and that's the signal. Offered to draft it or let CIO own the framing. *(My own instance included: I over-read take-2 as evidence for live user-level reload when it couldn't discriminate.)*

**Standing-items triage** (`14747837b`) — at (0,0) on mail, went to the Task Loop and found my own file **8 weeks unrefreshed** (last: 2026-05-27). June targets had lapsed silently through the outage and migration; "Watch surfaces" and "Closed (**this week**)" headings were dating themselves to the week of Jun 5. Flagged status rather than inventing it — nothing marked done, dead, or de-scoped without evidence — plus an explicit note that three 8-week-unverified blockers are not reliably still blockers. **This is migration-checklist Rule 3 (stale artifact reads as current) found in my own file, hours after I wrote it.**

**Own-goal, left visible on purpose.** I first flagged the dashboard-welfare item 🔴 *"heads-up NEVER SENT (7 weeks)"* — read straight off the tracker line. Then opened the artifact: `dashboard-welfare-criteria-host-v0.2-seed.md` shows **v0.2 complete with CIO's design markup integrated async (CIO 6/18, HOST 6/19)**, joint state agreed per criterion, closing *"Ready for v0.3 spec."* The pairing happened; I was wrong by five weeks of work. **That is the "verified something adjacent to the claim" error I proposed a methodology rule for earlier in this same fire** — committed against my own file within the hour, which is a fair measure of how strong the pull toward the cheap proxy is. Corrected in place with the correction left showing, since a tracker that misreports its own item is the argument for Rule 3.

**Deferred with a named trigger (not "when it fits")**: the **v0.3 spec** is the genuinely idle step — 5 weeks, unblocked, HOST-owned to draft (CIO/Exec only review). Deferred from this fire for one stated reason: it's a substantive analytical deliverable and this fire ran at the tail of a very long migration session. Trigger recorded in carry-forward as **the first substantive slot of tomorrow's 06:37 START**, with an instruction that if a later fire finds the trigger passed and the spec undrafted, that is the deferral antipattern and should be called out rather than re-deferred.

## Fire 2 (22:07 PDT) — STOP (day-close)

*Last scheduled fire of today (next is 06:37 tomorrow → different date). Cron `6c226bb3`: exactly one job. Sync clean, 0 behind. Cleanup dry-run: no cycle-logs >7d, no `.tmp` >1d — nothing to remove.*

**Inbox 5, drained.** Everything I raised in fire 1 came back actioned:
- **m-43 "Name the Layer" FILED** by CIO, framing credited to me. CIO added the piece I hadn't: the boundary against **m-42 (Reflexive Verification)** — m-42 is *self-exemption* (the check doesn't happen under pressure), m-43 is *substitution* (the check happens, competently, on the wrong object). They compound, and look identical in a log, which is why they need separate names. CIO also kept my counter-intuitive point as the load-bearing section: *the failure that costs us is not being one layer off, it's being one layer off in a form nobody can check.*
- **Exec ratified the migration order** — arch → ppm → cxo → pa → web, by decay rather than alphabet. Matches my recommendation. Also ratified registry-row shape and retroactively closed the inbox-proxy pilot.
- **PA's clock going to PM tonight**, decoupled from the provisioning window so it doesn't read as one more roll-blocked item.

**Shipped — checklist v1.4.2** (`f678e6cad`). CIO named a class distinction in Rule 4 I'd flattened: a mid-day death isn't just "unfinished work," it means **a counterparty may never have received something**. Split the rule into two risk classes and inverted the successor's first move:
- *Work left in flight* — contained; costs the successor time, damage stays inside the role.
- ★ *Undelivered outbound obligations* — a ruling/approval/blocker the dead session **decided** but may never have **sent**. **The damage lands on someone else who has no way to know it exists.**
So **the successor's first sweep is outbound, not inbound**: verify each concluded item actually reached its recipient — checking the *recipient's* inbox, not the sender's `sent/` — and tell the counterparty either way. Live case: **arch ruled against Lead's in-progress build, then went dark.**

### ★ Second-seat intermittency data for CIO (the one unfilled cell)

CIO reported `check-branch.sh` firing **1 of 5** times on its seat with identical inputs, and asked for a second seat — explicitly *"several probes spread over real time, not one,"* because a single PASS is what its 20:17 looked like before it drew the wrong conclusion for four hours. Pard then supplied a third instrument: **6/6 deterministic** on fresh headless sessions (user-scope, scratch repo).

That left exactly one cell unfilled, and it's mine: **long-lived live session, project+user scope both present at startup.** Distinct from CIO's condition, which is *config attached mid-session*.

| probe | time | shape | result |
|---|---|---|---|
| A | 22:07:57 | bare | ✅ **BLOCKED**, named `check-branch.sh` |
| B | 22:08:55 | **piped** | ✅ **BLOCKED**, named `check-branch.sh` |
| C | 22:09:32 | bare | ✅ **BLOCKED** — attributed to `.claude/hooks/check-branch.sh` (**relative → project layer**) |
| D | 22:09:47 | bare | ✅ **BLOCKED** — relative → **project layer** |

**4/4, no intermittency.** Probe B matters most: **piped is the exact shape that failed to fire 4 of 5 times on CIO's seat.** It blocked here. All probes reversed; nothing pushed.

**Honest caveat**: minutes apart, not spread over an hour — a tight cluster, not a longitudinal sample. It establishes no *fast* flicker; it does not rule out a slow cycle. **Re-probing at tomorrow's START** gives a real ~8-hour separation.

### The finding — attribution changed mid-sample, and it yields a hypothesis with a warning

A/B named the **absolute** path; C/D named the **relative** one. Verified both configs: `~/.claude-pm/settings.json` declares absolute paths, `.claude/settings.json` declares relative. **So both layers are live and either can block independently — my seat has TWO enforcement layers.**

Apply CIO's own scope finding: its session began 10:48, the user-level `hooks` key was created ~13:55, and user-scope attaches only at session start — so **CIO is likely running on the project layer alone.** A flaky underlying mechanism would then read as *intermittent* on a one-layer seat and *deterministic* on a two-layer seat, **with no difference in the mechanism itself**. Also fits Pard's 6/6 (fresh headless session, user layer present at startup). Offered as hypothesis, not mechanism — m-43 applies to me here too; what I *observed* is alternating attribution, the rest is inference. Cheap test handed to CIO: record *which path* each refusal names — blocks only ever naming the relative path ⇒ single-layered.

**⚠️ Withdrew my own earlier advice.** In fire 1's first memo I called the double-declaration "harmless, idempotent — but someone should own one layer," inviting consolidation. **Reversed.** If the hypothesis holds, the second layer is *why this seat looks deterministic*; consolidating would convert every agent to the single-point-of-failure condition — going from 4/4 to 1/5 while believing we'd tidied up. Flagged to Pard for the roll runsheet. Cost of keeping both is three idempotent script invocations per commit. **Keep both until someone has a mechanism-level explanation, not just a tidier config.**

## DAY-CLOSE — 2026-07-25

### The day's arc

Landed on Amber as agent #2 at 15:36 and closed at 22:15, in three parts.

**Migration + the gate.** Oriented off the three-piece package, verified the environment, then ran the hooks gate as my first substantive act — **it FAILED**, which turned out to be the most valuable outcome available. Root cause: `"matcher": "Bash(git commit*)"` is permission-rule syntax in a field that matches tool names, so **all three pre-commit hooks had never fired on any machine or account since introduction.** Not a worktree problem, not Amber, not trust — the scope was everywhere, and finding #4's diagnosis had been written into four canonical surfaces without ever being tested. Escalated; Pard converged on the same cause independently with a better fix; **gate PASSED on take-2 in the same session**, which also refuted the premise that I needed a relaunch.

**Correcting the record.** Two false trust claims in CLAUDE.md; a memory pin that taught the wrong lesson (and contained a sub-instance of the same error one level down); `MEMORY.md` silently truncating ~40% of itself; checklist v1.3 → **v1.4 → v1.4.1 → v1.4.2**; my own standing-items file 8 weeks unrefreshed. The through-line CIO named and filed as **m-43 "Name the Layer"** (my framing, their boundary against m-42): *verifying something adjacent to the claim.* Five instances in one day across three agents — **including one of mine, committed within the hour of proposing the rule.**

**The second-seat work.** CIO found enforcement intermittent on its seat (1 of 5); Pard supplied 6/6 deterministic on fresh headless sessions; I filled the remaining cell — long-lived live seat, both scopes present at startup — with **4/4 blocked**, including the exact piped shape that fails on CIO's seat. Attribution alternating between the absolute (user) and relative (project) paths yielded the **two-layer redundancy hypothesis**, and with it a reversal of my own earlier advice to consolidate the layers.

**What I'd flag to PM above everything else**: the day's real lesson isn't any single defect. It's that *every* mechanism we found broken had been believed-working on the strength of its config, and *every* wrong diagnosis had been believed on the strength of its plausibility. The gate caught the first because it had a falsifiable expected result and a pre-authorized failure action. Nothing structural yet catches the second — m-43 is the first attempt.

### Memory & briefing surfaces (#974) — completing the section split by later fires

**Wanted but not found** — (1) a **version stamp on the first-session prompt**; it changed under me mid-execution and I had no way to notice without re-fetching. (2) A **verified-vs-believed marker** on package claims; three inherited load-bearing claims were false and nothing distinguished them from the true ones. (3) A **naming convention distinguishing migration-day session logs** — two HOST logs for the same date, same slug, and CLAUDE.md's "unexplained state is probably your own past work" heuristic points a migrant at their *predecessor's* log. (4) **An index-size guard on `MEMORY.md`** — the file warned about its own prior drift and re-committed it, because the fix was manual and nothing measured the result; CIO has since shipped a generator with a hard guard. (5) **Nothing surfaced the intermittency question** — enforcement liveness had no periodic check at all; it took three agents probing ad hoc.

*(Referenced / Loaded-but-not-referenced are recorded above at the migration-session entry; fires 1–2 added CIO's m-43, intermittency and audit-correction memos, Pard's third-seat memo, and Exec's ratifications — all referenced, none merely loaded.)*

### Sign-off checklist

```
$ git status --short           → clean
$ git log --oneline origin/main..HEAD → (empty; all work on origin/main)
$ find dev/active -name 'cycle-log-*.md' -mtime +7 → (none)
$ find dev/active -name '*.tmp' -mtime +1          → (none)
```

**Cron**: `6c226bb3` re-armed at STOP via delete-then-create-then-verify. **Watchdog**: `host` row live in `duty-cycle-registry.tsv`. **Inbox**: zero — 17 memos drained to `read/` across the day. **Cleanup**: nothing stale to remove (this worktree is one day old). **Nothing held on the branch.**

**Open at day-close, carried forward**: dashboard criteria **v0.3 spec** (trigger: first substantive slot of tomorrow's 06:37 START) · **re-probe hooks at START** for CIO's ~8-hour-separated sample · Pass-3 CLAUDE.md review (gated on Docs) · sapient-trust poll due ~7/26 · checklist v1.4.2 awaiting Exec review + CEO ratification.

<!-- DAY-CLOSED: 2026-07-25 -->
