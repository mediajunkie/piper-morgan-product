# Lead Developer — Session Log — 2026-07-19 (Sunday)

**Role**: Lead Developer | **Slug**: lead-code | **Model**: Fable 5 (claude-fable-5)
**Worktree**: `.claude/worktrees/interesting-beaver-7ee19c` (Model B ephemeral)
**Cron**: `17 6,9,12,15,18,21 * * *` (d9471ac8) | **Continuity**: prior day DAY-CLOSED ✓

## Context at START
Finish-the-Unfinished sprint, post-Phase-3-gate era. v0.8.11.0 / Fly v21 live on beta.
Overnight inbox: (1) Arch's Family-3 ruling; (2) Exec relaying PM's question-batch answers
(the rollup relay PM foretold). Queue per carry-forward: Family-3 execution → #1400 →
#1401-on-pick → probes.

## Fires
- START (06:57) — session log created; two memos to drain (Arch Family-3 ruling, Exec Q-batch relay).
- Fire 2 (08:36, PM greeting; PM afk today, coordinating via Exec) — **Family-3 EXECUTED per Arch's overnight ruling** + a CI-honesty excavation the residual sweep forced:
  - Mail drained: Arch's Family-3 ruling (delete-set approved incl. query_router; #1322 superseded-by-construction; LLMIntentClassifier HELD for #1432) + Exec's Q-batch relay (PM: #1401 "volume now" GREEN; #1438 back to sprint — "learning is core"; #1386 Exec coordinates; #1424/#1427 held with PM).
  - Family-3: 5 modules deleted (degradation.py conditional → DELETE, router-only confirmed), todo_management surgery (import+stub+Depends out, request models kept), 8 dedicated test files deleted, 2 live test files pruned surgically, package inits trimmed. Rider: query_response_formatter (zero live importers, purpose = deleted producer) — flagged to Arch. Collection 11,774→11,679/0. Ceilings: silent_death 244→234, todo 77→74.
  - **CI chronic-red discovered + fixed**: Tests workflow ZERO green in last 40 runs. (1) #1382 keychain hard-raise fired at IMPORT via llm_client module singleton → collection dead on keyring-less runners since Thu; moved fail-closed to operations (writes raise, reads None) — construction safe, 1382 tests 9/9. (2) check_mypy_gate read missing-mypy (exit 1, empty stdout) as ZERO errors — 6th blind-sweep instance, guard added. (3) Three fossil jobs deleted from test.yml (perf jobs importing Family-2-deleted engine; coverage job measuring deleted directory) + 8 fossil scripts; real-gates issue filed #1449. Smoke local: 520 passed/0 failed. mypy ceilings: measuring under 3.11 container (local 3.12 skew confirmed vs CI numbers).
  - Board ops per PM's relayed rulings: **#1438 → sprint (Beta Blockers) + MVP milestone** (per-item mutation, verified); **#1322 closed superseded** (description-first: banner + all 4 ACs resolved N/A:superseded/satisfied-by-construction, evidence comment, closed not-planned). #1449 filed (real perf/coverage gates replacing fossil jobs).
  - Both commits + merge pushed: main+production @ 53d2601f0 (one push race with Exec's morning traffic + one decisions.log union-merge conflict, resolved). CI watch armed on both workflows.
  - Memos: Arch (Family-3 evidence + formatter rider + CI excavation incl. #1382 semantics change flagged for review + consumer_core→cold-adapter-layer spatial datapoint); Exec (Q-batch all-three-actioned, no new PM questions). Inbox drained ×5 (spatial CXO/PPM lanes FYI-cc; PPM's #1386 autoclose-reopen noted — Arch's mail-commit subject tripped the substring parser).
  - **arch-enforcement GREEN @241a094d4** (first since Family-1) after mypy floors locked 40/405/249/209 (gate-printed). Tests still red → landmine #2 unmasked: scripts/preferences_questionnaire had a MODULE-LEVEL venv guard (os.execv/sys.exit at import!) — collection died in CI (no venv) the moment the keychain landmine cleared; guard moved under __main__. Landmine-chain lesson: -x hides the next mine behind each fix.
  - **#1401 built**: UPLOAD_DIR env (get_upload_base, call-time read), upload sites rewired, fly.toml [mounts] piper_data→/data + UPLOAD_DIR=/data/uploads; read-side honest-degrade (410 Gone + re-upload message). **#1450 found+fixed riding**: download_file served PMENC1: ciphertext (FileResponse bypassed the #1306 decrypt seam — framework-side read invisible to the seam grep); now decrypt-seam + Response. 5 new tests green (decrypt round-trip pins ciphertext-on-disk→plaintext-on-download). Discovered+filed: #1451 (pre-existing settings-template nav test failure).
  - **#1410 fixed+closed**: 5 stale productivity-handler tests stamped with the #1395 principal contract (context user_id) — file 12/12 green. Stray deploy/fly/github-mcp.fly.toml committed (#1278 infra was untracked).
  - **#1401 CLOSED with live proof**: volume piper_data mounted, UPLOAD_DIR=/data/uploads honored, probe blob ciphertext-at-rest, survived redeploy v22→v23, decrypted after. **#1450 closed riding** (download decrypt-seam fix + round-trip test). Beta health 200.
  - **CI root-cause 3 fixed**: smoke job had NO postgres service (DB-inclusive smoke by design; security suite's postgres:16 recipe mirrored into both Tests jobs + alembic step + job-level env).
  - **#1393 fixed**: floor system prompt now forbids reproducing "[Available context]"/placeholder "(none)" (the leak was model-echo — the builder already omits empty blocks); presence-guard test added (floor file 43/43). Behavioral verify rides the #1386 Scenario B re-run.
  - **CI root-cause 4**: smoke's Slack-OAuth tests WRITE tokens → the #1382 operations-raise fired (correct fail-closed) on the keyless runner; mirrored the security-suite's deliberately-committed TEST-ONLY master key into both Tests jobs (CI now exercises the real hosted config: dead keyring + DB store + Postgres).
  - **#1400 built**: slack/calendar/notion prefs → DB connector_configs (the ADR-070 D4 rail github already used); flat files + helpers deleted; one-time legacy-file migration shim (droplet case, no-op on Fly); 11 call sites migrated. Flat-file-era test suite rewritten as real-DB round-trips + isolation + shim + merge (11/11 green).
