- Fire 2 (12:47) — **#1452 harness BUILT** (worktree lead-1452-harness): `scripts/known_failing_backlog.tsv` (415 unique entries from the enumeration; 37 fixture-tagged from the category glance, 378 explicit `triage` — triaging IS burn-down work), `scripts/check_fullsuite_backlog.py` (both-direction shrink-lock; blind-spot guards: no-summary→refuse, file-level ERROR covers per-test entries, tag validation; backlog-size shrink signal printed + into CI summary), gate contract pinned 6/6 (smoke-marked). Full Test Suite job rewired: named subset steps consolidated into ONE gate-governed sweep (subsets under -x turned the job red before the gate could judge). Expect one CI calibration commit (local-vs-CI env deltas in the 415).
