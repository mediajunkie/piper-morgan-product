# Lead Developer — Session Log — 2026-07-20 (Monday)

**Role**: Lead Developer | **Slug**: lead-code | **Model**: Fable 5 (claude-fable-5)
**Environment**: resumed session re-attached in the SHARED MAIN CHECKOUT (not a worktree) — read-only + mail + careful explicit-path log commits only; builds go to a worktree. Session cron: d9471ac8 KILLED by the re-attach (Gap-C, as documented) — re-armed as e1106eb5, same shape (17 6,9,12,15,18,21).
**Continuity**: 7/19 DAY-CLOSED ✓ (evening fires ran the close).

## Context at START
PM back at ~12:02 (asked for the two standing calls: #1424 close-vs-keep, #1427 PROD-RECONNECT confirm — answered).
Overnight rulings: **#1452 harness RATIFIED** (burn-down-backlog framing + fixture-vs-regression triage refinements) and **#1394 Option A STOPPED** (reverses ADR-078 D4; B3 referent-resolution already owns "change the title"; real lane = why-didn't-B3-fire → B4 ledger/observer diagnosis).

## Fires
- START (12:02, PM present) — answered PM's standing-calls question. **#1432 confirmation for Arch turned up a significant finding**: the Phase-4 shim's only consumer AND the prompt-flip commit (1d70dfd19) live exclusively in the ORPHANED llm_classifier.py; the live classifier.py has zero verb-emission machinery → Phase-4 verb-canonicalization is NOT live on the chat path; June's gate-IDENTICAL is consistent with never-wired. Memo'd to Arch with options (lean: delete the orphan pair + record the flip's reference impl for re-landing under #1432). Inbox triaged (2 rulings → read/).

## Queue today
1. **#1452 harness build** (RATIFIED — worktree session): known-failing backlog file + shrink-lock CI step + fixture/regression triage tags + shrink-signal in CI summary.
2. **B3/ledger diagnosis** (#1394, D4-conformant lane): verify whether turn-2 create writes a session_activity row; observer seam first.
3. #1432 orphan delete on Arch's rule (with the June singleton-history archaeology as evidence).
4. Standing: #1386 (Exec), #1424/#1427 (PM — answered today?).
- Fire 2 (12:47) — **#1452 harness BUILT** (worktree lead-1452-harness): `scripts/known_failing_backlog.tsv` (415 unique entries from the enumeration; 37 fixture-tagged from the category glance, 378 explicit `triage` — triaging IS burn-down work), `scripts/check_fullsuite_backlog.py` (both-direction shrink-lock; blind-spot guards: no-summary→refuse, file-level ERROR covers per-test entries, tag validation; backlog-size shrink signal printed + into CI summary), gate contract pinned 6/6 (smoke-marked). Full Test Suite job rewired: named subset steps consolidated into ONE gate-governed sweep (subsets under -x turned the job red before the gate could judge). Expect one CI calibration commit (local-vs-CI env deltas in the 415).
- Fire 2 cont. — **#1394 B3 wiring FIXED (D4-conformant, per Arch's STOP lane)**. Diagnosis went past both of Arch's hypotheses: the observer records fine and B3's scope is fine — **the session_id never reached Stage-0** (the chat path passed no context; B3 read a null session → N1 fall-through, always). Plus two entry-order hazards found + fixed: (1) B3 sat BELOW the classification cache (a cross-session cache hit on "change the title" would bypass resolution — session-relative messages must never be cache-served); (2) `classify_multiple` ran `detect_multiple_intents` BEFORE classify — and it pattern-matches "change the title to X" → update_document_query, which IS the live Scenario-B turn-3 misroute mechanism (test-confirmed: the new wiring test caught it red before the hoist). Fix: `session_id` as its own kwarg (never in context — context injects into the LLM prompt AND disables the cache), Stage-0 hoisted above cache + above detect_multiple at both entries. 4 live-wiring regression tests added (incl. cache-bypass + call-site-kwarg pins); B3 suite 22/22, routing-vocab + multi-intent green, smoke 526 passed. intent-routing-stack doc: added the missing Stage-0/B3 row (the mandatory doc predated B3 — which is exactly how Sunday's partial-model happened).
  - **Beta v25 deployed + verified** (health 200): B3 fix live — one #1386 Scenario-B re-run now verifies #1393 (turn-1) AND #1394 turn-3; Exec notified (window unblocked). Contamination bisect parked with data: victim green in own dir AND with both a..database / domain..intent halves → poison is in intent_service/integration-internal ordering or an accumulation effect; needs a loop-harness, queued under #1452 waves. CI gate calibration run in flight.
  - **Gate calibration applied**: first CI run measured the true env delta — 236 CI-only failures (invisible for weeks: ubuntu/3.11/service-DB/no-keychain vs local Mac) added as triage, 18 local-only removed per shrink-lock → backlog 633. Larger-and-honest beats smaller-and-blind. Post-calibration run in flight — expecting the FIRST GREEN Tests workflow (both jobs) in the repo's recent history.
  - **Post-calibration delta = ONE oscillator** (a latency-claim test — inherently timing-flaky; the other 633 held deterministic). Added the minimal flake mechanism: `flaky` tag tolerated in both states (still debt — burn-down = de-flake then retag); gate contract 7/7. Backlog 634 (1 flaky).
  - **🟢 TESTS WORKFLOW FULLY GREEN @ 972b4287b — first in the repo's visible history** (zero green in the 40+ runs before this arc). Smoke ✓ Full Suite ✓, gate OK, backlog 634 visible in every summary. Day-1 protection live: new rot fails the build immediately; fixes lock in via shrink-lock. Calibration arc complete in 3 runs (415 → ±CI-delta → +flaky mechanism), set deterministic.
- Fire 3 (15:47) — inbox duplicates round 3 → root-caused (add/delete merge semantics re-adding triaged memos from stale-base worktrees) → **#1454 filed + FIXED same fire**: regen-script self-heal drops any inbox file whose read/ twin exists (authoritative-triage-state rule); functionally verified (caught both live ghosts on first run). Cohort-wide benefit: every role's mail-loop regen now self-heals this class.
  - **Burn-down wave 1 — cross-user security cluster GREEN (22/22, was 18 failed)**: fixture rot — the tests predate the #1312 FK hardening and created lists/todos/files for owner ids never inserted as users; the FK rejecting orphan owners is the product WORKING. Uniform transform seeds each test's declared users via the file's own existing helper. 18 backlog entries off (shrink-lock honored same-commit).
  - **Contamination ROOT-CAUSED**: `asyncpg InterfaceError: another operation is in progress` at fixture setup under full integration load — poisoned shared-pool connections (loop-bound, abandoned mid-op by earlier tests); no single poisoner (accumulation; all halves/subdirs clean individually). Victim-class cure = fresh-engine fixtures (the house B15 pattern); conversion of conversation_repository + db_user_history queued next wave (~30 more entries).
  - **Wave 2 — poisoned-pool victims immunized**: conversation_repository + db_user_history fixtures converted to fresh-per-test engines (the B15 house pattern); PROVEN in-sweep (0 errors under the full integration load that previously killed them; 21/21 standalone). ~22 backlog entries off. conversation_manager_integration's 9 stay: its poison is app-internal (ConversationManager's own global-factory calls — cure is session_scope_fresh-shaped app work, own thread). Poisoner-hunt disposition: accumulation effect (no single culprit; halves+subdirs all clean) — victim-side immunization is the systemic cure, per-culprit hunting unnecessary.
  - Wave-2 CI verification: 1 of the 21 removed entries fails in CI only (test_search_matches_title_preview_topics — passes locally; the pool immunization unmasked an env-dependent failure underneath, suspected pg15-local vs pg16-CI search semantics). Re-added as triage (596); the other 20 held. Diagnosis follow-up queued.
  - **🟢 Waves 1+2 CI-CONFIRMED** @ bdc6354f8 — backlog 634→596 day one; the full fix→shrink→CI-confirm loop proven including the unmasking edge. Fire 3 total: #1454 fixed+closed, 2 burn-down waves landed, contamination root-caused, security cluster restored.
- Fire 4 (18:47) — **burn-down waves 3+4**: slack_config 20/20 (get_config/is_configured grew a required user_id in the #734 multi-tenancy work; 21 call sites threaded) + capability_discovery 31/31 (tests pinned the pre-#488 taxonomy — capability queries are DISCOVERY/get_capabilities by ratified design, checked before IDENTITY; "show my projects" moved to its PORTFOLIO pin). −32 backlog entries. temporal_context (16) deferred: deeper format-contract re-pinning, own unit.
  - Waves 3+4 CI verification: **all removals HELD** (zero shrink-demands). One new node caught by the gate: the #1381 greeting day-part test failed only during evening-hour runs (the evening template opens "I'm here and ready. Good evening!" — doesn't start with "Good "). De-flaked properly (day-part frozen via patch) instead of tagging — the gate's first hour-of-day oscillator, fixed at the root. 5/5.
  - **🟢 Waves 3+4 + de-flake CI-CONFIRMED** @ d5bb7d1e9. Day-one burn-down: 634→570, 4 waves, contamination cured, 2 riders. Workflow green.

---

## STOP (21:47 PT) — day-close

### Day arc
The day CI became trustworthy. Morning (PM present): the two standing calls laid out; the #1432 confirmation for Arch flipped into a finding — **Phase-4 verb-canonicalization lives ONLY in the orphaned llm_classifier.py; the live chat classifier never received the flip** (June's gate-IDENTICAL is consistent with never-wired). Midday: **#1452 harness designed-to-green in one day** — 415-entry backlog → CI calibration (+236 invisible-for-weeks CI-env failures, the honest correction) → flaky mechanism (1 node) → **the first fully-green Tests workflow in the repo's visible history** — then FOUR burn-down waves the same day: cross-user security cluster 22/22 (FK-hardening fixture rot — the product working broke pre-FK fixtures), poisoned-pool immunization (contamination root-caused as accumulation, no single culprit; fresh-engine fixtures = systemic cure), slack_config 20/20 (#734 user_id threading), capability_discovery 31/31 (pre-#488 taxonomy pins). **Backlog 634→570 day one, every wave CI-confirmed, zero shrink-demands.** Alongside: **#1394 turn-3 continuity actually fixed** per Arch's STOP lane — the session key never reached B3, PLUS two entry-order hazards (B3 below the cache; detect_multiple_intents intercepting referent messages first — the live Scenario-B misroute, reproduced red before the fix); D4 intact, deployed beta v25, one gate re-run now verifies #1393+#1394 together (Exec unblocked). Riders: #1454 mailbox-ghost self-heal (root-caused + fixed cohort-wide, closed), the #1381 greeting hour-of-day oscillator de-flaked at root (the gate's first evening-only catch), the stranded-morning-commits repair in PM's checkout (silent push failure — merged through the worktree so PM's local fast-forwards; lesson: never silence push output), intent-routing-stack doc gained its missing Stage-0/B3 row (the mandatory doc predated B3 — the stale-doc gap behind Sunday's partial-model).

### Carry to tomorrow
Wave queue: temporal_context re-pinning (16, format-contract work), conversation_manager app-internal pool cure (9, session_scope_fresh-shaped), pg15-local/pg16-CI search diagnosis (1), 555 triage glances. External: Arch's #1432 orphan-pair ruling (my finding may reshape it), Exec's #1386 window (both scenario fixes live on v25), PM's #1424/#1427 calls. #1393 remains open pending the scenario re-run (correct).

### Memory & briefing surfaces referenced this session (#974)
**Referenced**: duty-cycle-tick (every fire; last-fire-STOP rule); intent-routing-stack doc (the consult rule caught my own gap — then the doc itself was the stale surface; updated); ADR-078 D2/D4 (the STOP + the conformant fix); delete-module-safely (authored yesterday, its sweep discipline reused in today's residual checks); close-issue-properly (#1454, #1400/#1401 evidence-first closes); assign-sprint-safely + query-github-board (board ops); pause-before-irreversible (PM-checkout repair path: merge-through-worktree instead of touching PM's branch); B15 house test pattern (reused 3×: fresh-engine fixtures, DB-backed suites); never-reuse-stale-tree memory (mail-send retry awareness).
**Loaded but not referenced**: comms/publishing lanes, PA/HOST-specific memories.
**Wanted but not found**: (1) a "which classifier is live" pointer — the classifier.py-vs-llm_classifier.py duality cost two investigations (Sunday's partial-model + today's Phase-4 finding); the routing-stack doc now carries Stage-0 but a one-line "the live classifier is the classifier.py module singleton; llm_classifier.py is orphaned" would have saved hours. Added to the doc today — noting the gap existed. (2) A CI-vs-local env-delta cheat-sheet (pg15/16, py3.11/3.12, keyring-none, service-DB) — assembled piecemeal across three calibrations; worth one reference doc if the deltas keep biting.

### Sign-off checklist
- `git status`: clean (this log commit rides the close) ✓
- `git log @{u}..HEAD`: empty post-push ✓
- `main..HEAD`: on origin/main via worktree pushes throughout ✓
- `main == production`: lockstep maintained at every push today ✓

<!-- DAY-CLOSED: 2026-07-20 -->
