# Lead Developer — Session Log — 2026-07-23 (Thursday)

**Role**: Lead Developer | **Slug**: lead-code | **Model**: Fable 5 (claude-fable-5)
**Worktree**: lead-1452-harness | **Cron**: e1106eb5 (17 6,9,12,15,18,21)
**Continuity**: 2026-07-22 DAY-CLOSED ✓ (the 15h-freeze day; migration recommended, awaiting PM's call)

## Fires
- START (06:47) — inbox empty; resuming document_processing (the 5× generic-error thread).
- Fire 1 — **Wave 14: document_processing 9/9 + TWO live beta-surface product fixes.** (1) The analyze route silently 500'd since the #1312 ownership migration: document_handlers still queried UploadedFileDB.session_id (a docstring even codified the dead convention) — owner_id at both sites. (2) The question path NameError'd on a missing `import io` (pypdf BytesIO reads) → generic-error floor. (3) The "auth gap" was the test's own cookie (login on the shared client rode into the unauthenticated call — cookie-clear + comment); endpoint auth VERIFIED intact. Yesterday's stranded fixes ride (login form-encoding, usage-cap headroom + gauge-clear). Smoke green. Backlog −9.
  - **v27 deployed + verified** (health 200): the doc-analysis surface works on beta for the first time since the ownership migration. PM-checkout repaired (14/14 twin-verified drops committed; ff to 0109f232a).
  - Wave-14 CI round: the deepened doc tests hit the usage-cap middleware's Redis dependency — CI had NO Redis service (fail-closed 503 capacity_check_unavailable at login). redis:7-alpine service added to both Tests jobs (env-parity, same pattern as the postgres fix). CI-red root cause #5 of the drain, found by the burn-down going deeper.
  - Redis parity fixed the 503 but the doc tests now 404 with FastAPI's BARE "Not Found" — the route's own 404 carries detail=str(e), so this is an UNMOUNTED router (mount_router swallows import errors into captured logs). No module-level side effects found in the import chain; added a CI probe step (direct import + app.routes assert) so the next run yields the real traceback instead of a silent 404.
  - Wave 15 (execution_analysis, 9): pure fixture rot — tests mocked the classifier with MagicMock but process_intent now awaits classify_multiple (the #595 multi-intent rail). Added _stub_classifier helper stubbing both surfaces; 35 passed 1 skipped standalone. Backlog 264→255; in-sweep validation running.
  - Triage while sweeping: classification_accuracy (7) uses the LIVE conftest intent_service → belongs under pytest.mark.llm (wave-7 class; queued as wave 16 after this sweep validates). llm_classifier_benchmarks (7) is mocked-LLM + timing asserts — likely runner-load oscillators, needs a run for failure detail. intent_wiring RecursionError trio all route through the real-lifespan full_app_client fixture — traceback needs a standalone run post-sweep. standup_performance constructors match current signatures; failures behavioral/timing, needs a run.
  - Probe verdict: document_handlers built DocumentService() at module import → chromadb's OpenAIEmbeddingFunction RAISES keyless → router import died → mount_router swallowed it → bare 404. Rewired to the service module's EXISTING get_document_service() lazy getter (verify-first: no second singleton). Real product-robustness fix too — a beta key-config hiccup would have silently dropped the whole doc surface.
  - Sweep dissection (28 "new" locally): wave 15 validated clean; 4 usage-cap-middleware failures were MY headroom fixture defeating its own test subject (RATE_LIMIT 100000 made 'exceeding' unreachable) → fixture now exempts the middleware's own module, 15/15; 9 doc tests are keyed-live tests — live class marked pytest.mark.llm, 3 keyless-viable edge cases stay in sweep; 12 remaining are local-env-shape noise (providers-list keyed to which keys exist; chromadb host cached at import) that pass in CI — CI is the arbiter.
  - Shrink-lock discipline: local claimed 20 now-pass; CI's authoritative list is 6 (canonical-mocked 4 + token_blacklist 2). Removed ONLY the CI-confirmed 6 — the 14 local-only passers are env-dependent oscillators that still fail in CI and stay listed.
  - Wave 16 delisted (15 by path-prefix — a bare substring match nearly took test_classification_accuracy_under_load from the benchmarks cluster). Backlog 264→234.
  - intent_wiring: trio passes standalone (RecursionError is sweep-order-only; entries stay listed, CI agrees they still fail in-sweep). Fixed the real defect the run surfaced: the file's inline user cleanup was a bare users DELETE → personalization_contexts FK violation at teardown — rewired to delete_test_user_fully. 19/19. Directory-scope order repro running for the RecursionError.
  - Wave 17 (llm_classifier_benchmarks, 7): #322 DI rot — classifiers built without llm_service fell back to the uninitialized container. Both fixtures inject a mock LLM (tests patch .complete anyway). 7/7.
  - Wave 18 (standup_performance, 9): create_conversation went async (9 un-awaited sites) + multi-turn loops reused a stale conversation object, replaying the INITIATED branch into same-state transition guards; the live caller (process/adapters) re-fetches per turn — tests now match. 10/10. Backlog staged 234→218; detached validation sweep running (first attempt hit the 10-min Bash cap).
  - CI at fe890b82e: probe GREEN ("documents router mounted: 6 routes") — the lazy-init cure verified keyless; it also cured the 1185 doc-route pair (removed per CI shrink-lock). 5 new failures dissected: update_issue pair = #943 pre-flight fires keyless before the missing-field validation (pinned is_available=True in both tests, 35/35); doc edge trio = CI-sweep-order oscillation (pass in CI isolation + keyed local) → re-listed flaky.
  - Wave 18 postscript: full-sweep FAILS all 9 despite standalone 10/10 (wave-6 rule earns its keep); integration+performance prefix passes → poison in earlier dirs, same neighborhood as the intent_wiring RecursionError. Prefix also exposed a vacuous pass — transition_state called un-awaited in the timing test (measured coroutine creation); now awaited, still 10/10. Test fixes kept; 9 entries re-listed flaky pending the earlier-dir poison chase. Backlog pushed at 228 (arc 634→228).
  - (08:40) **CI GREEN at c0a10e40f** — the first green Tests run under the burn-down gate, ending the 40+-run red streak. Smoke + Full Suite both pass; gate holds at backlog 228 (arc 634→228); probe confirms the doc router mounts keyless. PM notified (terminal push).
  - Wave 19 (configuration_regression, 7): pure mock-theatre — 10 of 11 tests patch()ed PiperConfigLoader and asserted their own MagicMock (Pattern-045 terminal case; the backlogged 7 were where the real class leaked through import order into a superseded contract). Real loader coverage confirmed green elsewhere (config_performance + multi_user_configuration, 20 tests). File reduced to its one real test (PM-123 CLI runner). Bonus: its teardown rewrote config/PIPER.user.md mid-sweep — a live poison source for order-sensitive tests, now gone. Backlog 228→221.
  - Wave 20 (error_message_enhancement, 6): fossil patched main.classifier (gone since the intent stack moved to services) AND pinned pre-degradation 500/502 shapes. Rewritten against the real seam (app.state.intent_service) and the real contract (Pattern-007: service failures → 200 + conversational degradation; the route parses its own body, so malformed input degrades rather than 422s). 22/22. Backlog 221→215.
  - Wave 21 (clarification_edge_cases 6 + api_query_integration 5): both drive LIVE classification (module-level classifier.classify / natural-language POSTs asserting category+action) → pytest.mark.llm lane. Noted for the llm-lane's own burn-down: api_query pins actions (count_projects) that diverge from live behavior even keyed. standup_multi_turn_1079 passes standalone → another sweep-order case, stays listed. Backlog 215→204.
  - Wave 22 (integrations_dashboard, 6): the dashboard routes grew a JWT Depends parameter after these direct-call tests were written — the Depends sentinel leaked in as current_user. Tests now pass complete fake JWTClaims explicitly. 12/12. Backlog 204→198.
  - Wave 23 (mcp_error_scenarios, 6): applied Arch's existing #1436 Tier-3 ruling — 4 tests pinned the DELETED POC simulation stack (asserting a never-dialing stub "fails to connect"; one drove the removed MCPResourceManager) → removed with a pointer to the Family-6 consumer/* follow-up. The 2 live-referent tests fixed: circuit breaker rewritten to the real call() API; resolver test to current UploadedFile/Intent signatures. 7/7 file-green. Backlog 198→192.
  - Waves 24-25 (todo_handlers 5 + todo_full_stack 5): todo_items.owner_id became UUID+FK+NOT NULL (#484/#1312) — both files passed literal "user1"/"test" owners. Real per-test users via autouse fixtures (cascaded away with delete_test_user_fully); copy-pins modernized to the conversational handler replies (ids resolve by list position — probed live). PLUS a real latent service fix: item_service reorder_items/get_items_in_list selected the polymorphic base and to_domain() lazy-loaded joined subclass tables in sync context (MissingGreenlet) — selectin_polymorphic eager-loads now. Not exposed on any live route (latent, not beta-urgent). Backlog 192→182.
  - Wave 26 (auth_integration, 5): the conftest transaction-rollback isolation became a dead letter when auth routes moved to session_scope_fresh (#442) — the app couldn't see rolled-back test users, every login 401'd. File now commits real users (fresh-engine session) with registry-based delete_test_user_fully teardown; login posts fixed json→data (Form); copy-pins updated to middleware-reshaped bodies ({"message"}). Security behavior verified LIVE again: password-change token invalidation + blacklist CASCADE had been dark since the strategy broke. 5/5 (+1 deliberate skip). Backlog 182→177.
  - Wave 27 (no_web_bypasses, 4): global auth middleware now answers 401 before routing — the bypass is still blocked (more strictly); 401 added to the accepted refusal set. 7/7. Backlog 177→173.
  - CI at 1902a3bd5: ZERO new failures across the wave 19-27 batch — only shrink-lock: the intent_wiring RecursionError trio now PASSES in CI (the wave-19 removal of the mid-sweep PIPER.user.md-rewriting teardown + the delete_test_user_fully teardown fix cured the order pathology). Removed per gate. Backlog 173→170.
  - Wave 28 (auth_endpoints, 4): middleware-reshape pins (detail → message; auth-required carries error/type). The no-user-oracle security property re-asserted against the reshaped body. 15/15. Backlog 170→166.
  - Wave 29 (data_isolation, 4): pinned the #280 migration's implementation snapshot — a generic ConfigService that no longer exists (live personalization coverage: test_personalization_*_1366) and a literal 'xian' row with 2025 preferences schema (q4_goals). Removed; the file keeps its live contract (PIPER.md carries zero personal data), 3/3. Backlog 166→162.
  - Wave 30 (error_handling_integration, 3): same main.classifier fossil + pre-degradation 422/502 shapes as wave 20 — rewritten to the app.state seam and the Pattern-007 contract. 3/3. Backlog 162→159. (Push held until the in-flight CI run completes — my push cadence was cancelling runs before they could report.)
  - Wave 31 (file_reference_detection, 3): the three live-classify tests (category/action/confidence asserts) marked pytest.mark.llm individually — the pure-regex pattern tests stay in the sweep. Backlog →156.
  - Wave 32 (user_flows_complete 4 + integration_complete 3): global-auth-era pins (admin endpoints now correctly 401 unauthenticated; nl-endpoint config read in-process; "/" probed without following the login redirect). TWO product fixes shipped en route: (1) RedisFactory pool was loop-bound-and-cached — the Redis twin of the #1193 poisoned-pool class; now rebuilds on loop change. (2) REAL masking bug in usage_cap_middleware: the anonymous-principal early return ran call_next INSIDE the fail-closed try, so downstream handler errors masqueraded as capacity_check_unavailable 503s (in production too — this very bug made the "/" template error read as a capacity failure until the restructure surfaced the truth). call_next now always outside the try. Middleware 15/15. Backlog 156→150.
  - Fire 2 (09:47, cron tick joins the running drain — cron healthy, inbox empty). Wave 33 (config_pattern_compliance, 4): get_config/get_client_configuration/is_configured became owner-scoped (required user_id, ADR-071) — compliance probes now pass a fresh principal id (the claims are user-agnostic). 37 passed. Backlog 150→146.
  - Wave 34 (plugin_registry, 3): a fifth plugin (demo) joined the registry; count pins loosened to superset semantics + demo allowed in the name whitelist. 24/24. Backlog 146→143. Spatial note: complete_integration_flow (4) + slack_spatial_adapter_integration (3) confirmed SPATIAL territory (SlackWebhookRouter spatial_adapter wiring, adapter registry) — parked with the held cascade for the committed-theory review.
  - Wave 35 (settings_projects_ui, 3): the template migrated onto app_shell (#1171) — nav/toast/tokens are the shell's job; tests now assert the two-hop contract (child extends app_shell; app_shell carries the chrome). 22/22. Backlog 143→140.
  - CI at 1e429a762 (wave 30): ZERO new failures; 1 shrink-lock (url_redaction httpx-install test now passes — possible order-oscillator; if it re-fails the gate will say so and it gets the flaky tag). Removed. Pushing the staged batch (waves 31-35): backlog 140→139 on the wire.
  - Wave 36 (pm056_schema_validator, 3): two rots — (1) the old @patch on tools....sys.modules swapped the REAL singleton sys.modules process-wide, so dataclasses defined inside the window resolved annotations against the fake module (KW_ONLY AttributeError; py3.12 exposes it) → patch.dict scoped to the loader call with classes defined outside; (2) `str = None` typing lie — the validator's type-derived nullability is correct; Optional[str] in the test. 11/11. Backlog 139→136.
  - Waves 37-38 (simulation_guard_1436 2 + setup_slack 2): the 1436 guard's patched subject (services.mcp.resources) was itself deleted by the ruling it guarded for — patches dropped, the flag-on degrade contract survives; setup_slack's get_slack_status grew the #1434 request.state user pattern — minimal request supplied. 12 green. Backlog 136→132.
