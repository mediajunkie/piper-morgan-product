# Session Log — 2026-09-12 13:21 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), deployed by Lead Dev
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Task**: #1749 — test_search_matches_title_preview_topics fails CI-only; diagnose env divergence, fix at evidenced layer. Commit, no push.

## Diagnosis

- Read #1749 + CI log of red run 34639901479 (Tests / Full Test Suite, 2026-09-11).
- Failing assertion: `tests/integration/services/test_db_user_history_repository.py:171` — `assert by_preview.id in ids`, with `ids` a ONE-element set (by_title only). So title matched; preview and topic matches both absent. Reproduced in both the full sweep and the isolated diagnose rerun → not order/bleed dependent.
- Hand-computed expectation from fixture: 4 rows, fresh user_id, unique token; search should return exactly 3 (title/preview/topic matches), exclude 1.
- Mechanism (source-verified, `services/database/models.py:1642-1645` + `services/security/encrypted_types.py`):
  - `ConversationDB.preview` = `EncryptedString`, `topics` = `EncryptedJSON` (#1305, at-rest encryption). `title` is plaintext `String`.
  - `DBUserHistoryRepository.search_conversations` (repositories.py:2031) does SQL `ILIKE` on preview and CAST(topics AS VARCHAR).
  - Encrypted types: no `ENCRYPTION_MASTER_KEY` → dev plaintext passthrough (local = green). Key present → `PMENC1:` ciphertext at rest → ILIKE on ciphertext never matches plaintext token.
  - CI sets `ENCRYPTION_MASTER_KEY` in `.github/workflows/test.yml:133` (added for the #1382 credential store). THAT is the env divergence.
- NOT the #1748 credential-pollution class; cd980af2c does not cure it (different mechanism, same CI env delta as trigger family).
- Product bug, not test bug: any keyed deployment (production) has history search matching titles only. #1305's own comment on `topics` says "no server-side SQL reads it" — this call site was missed.

## Work

- **Red-first repro (13:27)**: keyless run of the failing test = green; `ENCRYPTION_MASTER_KEY=<CI test key>` run = red with the byte-identical CI signature (same line 171, one-element set). NOT CI-only-provable — fully local.
- **Fix** (`services/database/repositories.py` search_conversations): SQL narrows on plaintext columns (user_id, lifecycle_state, is_private); title/preview/topics matching + title-first/recency ordering moved Python-side post-ORM-load (decryption transparent there), mirroring InMemoryUserHistoryRepository's contract. Removed now-unused `String` import. This was a PRODUCT bug — any keyed deployment had search matching titles only.
- **Regression test**: `test_search_matches_preview_topics_encrypted_at_rest_1749` — monkeypatched key, asserts `PMENC1:` ciphertext at rest (m-43 layer gate so it can't degrade into a plaintext rerun), then the 3-in/1-out membership. A/B stash: red against pre-fix repositories.py, green after.
- **Backlog ratchet**: removed the test's row from `scripts/known_failing_backlog.tsv` (13 → 58 lines; shrink-only rule: comes off in the same commit as the fix).
- **#1765 cross-check** (Lead's brief): NOT a common mechanism. Reproduced locally: 2 failures are `UniqueViolationError` on `lists_pkey`/`pk_items` — the tests use FIXED PKs (`33333333-…`, `44444444-…`) and residue rows with those exact PKs verified present in the persistent local dev DB (`SELECT count(*)` = 1 each); CI green because per-job DB is fresh. Mechanism recorded on #1765 as a comment; no code change there (that lane's call).
- **Verification**: test file 12/12 green keyless AND keyed; `run-sweep.sh unit` over touched neighborhood (test_user_history_helpers, test_user_history, test_standup_assembler_1269, test_command_palette) = 135 passed; `run-sweep.sh ratchets` = 54 passed; ruff 0.6.9 (ruffenv2 rebuilt — was absent) check + format clean on both touched files.
- GH artifact comments: issues 1749 + 1765 (evidence + mechanism).
- What waits on CI: only the final confirmation that the next Tests run on main shows this test green under the real workflow env (mechanism already proven locally under the identical key).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-divergence gotchas discipline, name-the-layer m-43, commit-no-closing-keyword rule); commit cd980af2c message (1748 mechanism, ruled out); .github/workflows/test.yml (the env delta itself); #1305 design note in models.py (ratified the Python-side-filtering pattern the fix uses).
- **Loaded but not referenced**: MEMORY.md index; roster/briefing tables.
- **Wanted but not found**: none — the CI log of the red run (34639901479) was retrievable and decisive.
