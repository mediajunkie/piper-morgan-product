# Session Log — 2026-09-12 13:04 — Coding Agent (prog-code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead's worktree, branch claude/lead-cycle)
**Task**: GitHub #1748 — CI test-isolation defect: tests write fake provider keys into shared per-job Postgres via #1382 DB store; conftest later loads one as real → live 401s in CI.

## Plan
1. Read issue #1748 (done — body + PPM triage comment).
2. Diagnose: (a) which tests write into DB store in CI; (b) where conftest/key-resolution reads them; (c) honest fix layer.
3. Red-first pin: fail if a test-written credential survives into a fresh resolution pass.
4. Fix + verify (run-sweep.sh unit over credential/keychain/conftest neighborhood, ratchets, ruff 0.6.9).
5. Commit on claude/lead-cycle referencing 1748 (no closing keyword). Do NOT push.

## Entries
- 13:04 — Session start. Issue read: fix shape suggested = test-scoped DB isolation for secure_credentials + conftest refusing to export test-run-provenance keys.
- 13:10 — Diagnosis complete, with CI evidence:
  - **Writers (CI: dead keyring + ENCRYPTION_MASTER_KEY → real DB-store writes to shared per-job Postgres):**
    1. `tests/infrastructure/test_keychain_timeout_1711.py` PRE-FIX (before 36cb6191b, 09-11): `_isolate` deleted `PIPER_CREDENTIAL_STORE`, so `store_api_key("anthropic", "sk-test")` / `("anthropic", "sk-x")` — written expecting a raise — auto-routed to the DB store and SUCCEEDED, no cleanup. **This was the actual polluter in the observed incident.** Already fixed (now forces `=keychain`).
    2. `tests/infrastructure/test_keychain_service.py`: real `KeychainService(service_name="piper-test")`; in CI writes `openai_api_key`/`anthropic_api_key`/`test-provider_api_key`/`integration-test_api_key` to the DB store — note the DB store IGNORES `service_name`, so "piper-test" writes land on production-identical names. Has per-test teardown deletes → residue only on crash/kill.
    3. `tests/security/test_secure_credential_store_1382.py`: unique `test1382_*` names + teardown — never collides with conftest's read names.
  - **Read path**: `tests/conftest.py::pytest_configure` → `get_keychain_service()` → (CI) `EncryptedDBCredentialStore.get("anthropic_api_key")` → exports `ANTHROPIC_API_KEY` → llm tests un-skip + live calls with garbage → 401.
  - **Smoking gun**: Tests run 34639901479 (red, 09-11) full-suite job, diagnose steps: `backend=EncryptedDBCredentialStore (#1382 hosted fallback)` then `[conftest] Loaded ANTHROPIC_API_KEY from keychain` — on ubuntu. Green run 34714785476 (post-1711-fix) shows no such line.
  - **Same-job re-resolution window**: test.yml full job runs pytest up to 3× against one Postgres (full sweep + "Diagnose env-oscillator" (always) + "Diagnose new failures" (on failure)).
- 13:13 — Fix layer chosen: **read-side provenance guard in tests/conftest.py** (test infrastructure, not production code). Rule: only a positively-confirmed OS-keyring-backed KeychainService is a developer-key source; DB-store-backed, degraded, or unrecognizable shapes refused (fail-safe: renamed internals → skip loading, never load residue). Rationale: the per-job CI Postgres is provisioned empty, so ANY key in its secure_credentials is test-written by construction — closing the read closes the class for every present and future writer. Unconditional (not CI-gated): no seat currently uses PIPER_CREDENTIAL_STORE=db as a real key home (Amber keys went into the macOS Keychain 08-01); env vars still work everywhere.
- 13:14 — Red-first pin: `tests/infrastructure/test_credential_pollution_1748.py` (5 tests). Pre-fix: 3 failed (db-store residue exported; degraded consulted; unknown shape presumed OS) / 2 passed (OS path loads; env precedence). Post-fix: 5/5. Also verified the real hook under simulated CI (PYTHON_KEYRING_BACKEND=fail + CI master key): guard message fires, nothing loads.
- 13:20 — ⚠️ Process incident (self-inflicted, recovered): attempted A/B via `git stash push` including an UNTRACKED file → push failed after... actually push failed outright (pathspec unknown to git), then the chained `git stash pop` applied the **Lead's pre-existing 08-16 stash** → 10 unmerged (UD) index entries on `mailboxes/xian (ceo)/read/*` files. Recovery: verified every UD path disk-identical to HEAD (`git diff HEAD -- <path>` = empty, per the diff-first HARD RULE), then per-path `git reset -q -- <path>` (index-only). Lead's stash@{0} preserved untouched; my changes unaffected. Lesson: never chain `stash push && ... ; stash pop` — pop can grab someone else's stash when push fails; A/B untracked files by moving them, not stashing.
- 13:25 — A/B on sweep failures WITHOUT stash (file swap): `tests/security/test_cross_user_isolation.py` 2 failures reproduce with pristine HEAD conftest + pin removed → pre-existing, not mine. 9 `tests/infrastructure/mcp/test_connection_pool.py` errors are in `scripts/known_failing_backlog.tsv` (triage).
- 13:30 — Verification: neighborhood sweep `run-sweep.sh unit "tests/infrastructure tests/security tests/config"` → 2 failed (pre-existing, A/B-proven) / 269 passed / 10 skipped / 9 errors (known backlog) — identical failure set to pre-change. `run-sweep.sh ratchets` → 54 passed. ruff 0.6.9 (scratchpad ruffenv2): check + format --check clean on both changed files.
- **Verified how:** red-first pin run (`pytest tests/infrastructure/test_credential_pollution_1748.py`, 3F/2P pre → 5P post); real-hook probe under simulated CI store-selection (dead keyring + CI master key, guard message observed); layer: local runs prove the guard logic + store-selection shape, NOT the CI end-to-end (macOS seat has a live keyring; the shared-Postgres residue path only exists in CI). Denominator: the next Tests-workflow run on a push containing this change is the true end-to-end verification — change touches tests/**, which triggers that workflow.
