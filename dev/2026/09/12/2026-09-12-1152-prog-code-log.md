# Session Log — 2026-09-12 11:52 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead's worktree `/Users/xian/Development/piper-morgan-worktrees/lead` on `claude/lead-cycle`
**Model**: Fable 5 (claude-fable-5)
**Task**: #1717 — honest-degrade directives compose additively across five sites; implement single-source composition (opens EPIC 5, honest-empty / GatherOutcome, per PPM order; acceptance-contract single-source-predicate idiom proven 2026-09-12 across five seams)

## Plan
1. Read issue #1717 + comments fully — DONE. Key state: evidence run c302e767b (6/6 five-flag runs synthesized gracefully — litany did NOT materialize); CXO's two directives landed 555db7daa (scope rule once-only + anti-reassurance rule); 14 pins exist with the all-five test docstring-marked as the seam that MUST fail when aggregation lands.
2. Mandatory reads: intent-routing-stack.md; conversational_floor.py incl. SOURCE_FAILED_FLAGS registry (AST-enforced); commit d32429975 (#1596, touched _format_domain_context).
3. Red-first repro of the additive pile (2+ flags → concatenated directives).
4. Implement minimal single-source composition: names ONLY failed checks, never reassurance, no duplication, preserves registered flag semantics.
5. run-sweep.sh unit over floor neighborhood + full tests/unit/services/intent_service; ratchets; ruff 0.6.9 (ruffenv2).
6. Commit on claude/lead-cycle referencing 1717 (no closing keyword). NO push.

## Work

### #1717 — single-source composition of the source-failed honest-degrade directives (12:00-12:10)

**Red-first repro** (current code, verbatim render): all-five-flags domain context produced the
additive pile — 5 independent "… check FAILED: …" directives, each with its own honesty guard,
plus the scope directive; two-flag turn produced 2. Existing 14 pins passed (they pinned the pile;
their docstring marked the all-five/two-of-five tests as the seam that MUST fail when the fix
lands). New-shape tests written first → red on current code (collection error: registry lacked the
NamedTuple fields the composed contract needs).

**Design chosen** (issue left freedom; contract §4 of
`docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md` is the spec):
- Registry `SOURCE_FAILED_FLAGS` extended (not forked) from `(flag, prefix)` 2-tuples to
  `SourceFailedDirective(flag, check_name, directive)` named tuples — the full single-failure
  copy moved INTO the registry.
- The five hand-placed render sites REMOVED; ONE composition site (where the wrinkle-1 scope
  gate sat) derives the armed subset from the registry:
  - N == 1 → the registered per-source directive verbatim (byte-identical to pre-fix copy —
    live-probe-tuned, individually correct per the issue's own words, pinned by 5 other files).
  - N >= 2 → ONE aggregate directive naming exactly the failed check names in registry order
    ("DATA CHECKS FAILED this turn — could not check: …"), one-sentence instruction, unified
    honesty guard (never claim empty/none, never invent items), never one caveat per check.
  - Scope directive (CXO verbatim, binding) rides once after the failure report for any N >= 1 —
    "listed as FAILED above" stays literally true.
- Why: names ONLY failed checks; adds no reassurance (wrinkle-2 anti-reassurance block
  untouched); no duplication (aggregate replaces per-source lines); keeps per-flag semantics
  (N=1 copy unchanged, flag keys unchanged, assembler untouched). Threshold matches contract §4
  ("when more than one reportable failure exists").

**Tests**: test_source_failed_composition_1717.py rewritten — 16 tests (aggregate composition 5,
single-failure-unchanged 6, copy pins 5); seam fired as designed (old additive pins → new composed
pins). test_source_failed_registry_1717.py AST invariants inverted — 4 tests: no literal
`domain_context.get("*_source_failed")` reads may remain (a hand site reappearing = registry
fork), exactly one comprehension iterating the registry, no hand flag tuples, registry
conventions (distinct flags/names/directives, "check FAILED:" marker, aggregate-safe names).

**Verified how**: red-first render evidence (pile) + post-fix render (one aggregate line, 5/2/1
flag shapes) captured this session; neighborhood sweep 212 passed; full
tests/unit/services/intent_service 4019 passed / 3 deselected; ratchets 54 passed; ruff 0.6.9
check + format clean. Doc sync: intent-routing-stack.md has no source-failed-family cell (grep
1425/1645/1717 = no hits) — the module registry comment is the living doc, rewritten same commit.
No other SOURCE_FAILED_FLAGS consumers (grepped).
