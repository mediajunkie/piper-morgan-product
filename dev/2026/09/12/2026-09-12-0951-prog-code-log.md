# Session Log — 2026-09-12 09:51 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), delegated by Lead Dev
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1596 — floor amnesia after guided-flow escape. Escape works (fixed in #1529, commit b9a1996e0); the floor reached afterward has no context. Reproduce red-first via the issue's verbatim exchange, diagnose WHERE context is dropped (escape state handoff vs. floor read), fix at the right layer.

**Constraints**:
- ONE predicate (evaluate_acceptance); no local detectors; no new extraction regex (TestExtractionPatternRatchet — corpus rows for new phrasings)
- Spec: docs/internal/design/acceptance-contract-user-facing-2026-09-10.md
- MANDATORY pre-read: docs/internal/architecture/current/intent-routing-stack.md (sync same commit if staled)
- Floor: services/intent_service/conversational_floor.py — SOURCE_FAILED_FLAGS conventions
- Extend today's idiom: d3830faa4 (#1652), d554a4e78 (#1653), c035d7a5e (#1654), 1dc8ff5aa (#1753), 40ae79298 (#1696)
- Commit on claude/lead-cycle referencing 1596 (no closing keyword adjacency). Do NOT push.

## Entries

- 09:51 — Session start. Read #1596 + parent #1529 (body + comments). Verbatim sequence understood: standup hijack → 'end standup' → 'Which todo...' → 'none' → floor with amnesia. Underlying user goal the floor forgot: "restore CoVa". Beginning mandatory reads (intent-routing-stack.md, conversational_floor.py, today's five acceptance-contract commits).

- 09:55–10:15 — VERIFY-FIRST INVESTIGATION (the long half of this lane; probe scripts in scratchpad `repro_1596*.py`, all driven through the REAL `process_intent`):
  - **History half of the filed amnesia: ALREADY FIXED** by #1394 (`acf26bbec`, 2026-08-08 14:15 — 53 min AFTER PM's 13:22 hostage transcript, 3 days BEFORE #1596 was filed at #1529 closure). Reproduced e2e: escape turn and post-escape turn both reach the floor with full authenticated history. #1596's "reproduces whenever the floor is reached" was filed as an administrative split-out (08-11 lead log confirms: "split from #1529"), not a fresh repro. Supersession gate honored: verified, pinned e2e (no prior test drove history through a real escape), NOT re-fixed.
  - **Two halves still RED on today's tree**, both reproduced through the real path:
    1. **The floor's read (wrong-empty)**: classifier rewrites vague/low-conf turns → CONVERSATION/clarification_needed (classifier.py:590) — the lane PM's 'none' takes — and `ContextAssembler.gather_context` had a deliberate `pass` for CONVERSATION ("minimal for greetings" — a rationale keyed to a caller that no longer exists; pure greetings take the canned canonical lane and never reach the assembler). Measured: post-escape 'none' floored with domain_context == {'current_time'} while the identical UNKNOWN fall-through gets the #960 baseline.
    2. **The escape's state handoff**: the #899/#1529 fall-through prefix is glued on AFTER floor composition; the in-flight turn is excluded from history → the escape-turn floor composes with the flow's open question visible and nothing saying the flow closed.
  - Also verified green (controls): escape kinds all work (#1529), residual routing works ("restore CoVa" → portfolio restore lane with honest not-found + offer), prefix rides floor returns (#1617 funnel).

- 10:15–10:25 — FIX (both layers, no routing changes, no new dispatch sites, no detectors, no extraction regex):
  - `services/intent_service/context_assembler.py`: CONVERSATION special-case removed → falls to #960 else-branch baseline.
  - `services/intent/intent_service.py`: `_check_active_guided_process` → 3-tuple (adds escaped ProcessType); classification seam stamps `intent.context["guided_flow_escape"]` (strings only, per-turn); all three floor doors merge it into domain_context.
  - `services/intent_service/conversational_floor.py`: `_format_domain_context` renders rule-stating GUIDED-FLOW-ENDED directive (#1655-clean, no sample reply sentences).
  - Shape-coupled test mocks updated to 3-tuple: test_offer_binding_1529 (1), test_ethics_denial_flow (4), test_floor_history_principal_1394 (1), test_silent_death_unswallow_1423 (unpack + new assert).
  - Acceptance-contract statement (#1739): arms nothing, consumes nothing; evaluate_acceptance not involved; §5a/§5b arm-survival untouched; note turn-scoped by construction (pinned).
  - intent-routing-stack.md surface-4 cell synced same commit (mandatory consult rule).

- 10:17–10:30 — VERIFICATION: new `test_floor_escape_amnesia_1596.py` — **red-first A/B via git stash: pre-fix 10 failed / 7 passed (the 7 = deliberate green control pins incl. the #1394 history pins), post-fix 17/17**. Neighborhood sweep (process + intent + floor/assembler/acceptance files): 493 passed / 7 skipped. Ratchets: 54 passed (extraction-pattern + dispatch-site + 1739 KNOWN_UNADOPTED all quiet). Ruff 0.6.9: all checks passed (new test file formatted). Full tests/unit/services/intent_service: **3914 passed, 3 deselected** (182s; captured `scratchpad/full_isvc_1596.log`).

- 10:42 — Wrap. Verified how: red-first A/B (git stash) on the new suite = the method; layers measured = FloorContext/prompt (unit+e2e through the real process_intent with a real IntentClassifier over a low-confidence LLM fake) — NOT a live-LLM compose test (the floor's respond was captured, not generated); denominators stated per run above. Committed on claude/lead-cycle, NOT pushed (per task instruction — Lead merges).

- 10:35 — DISCOVERED WORK filed: **#1754** — ConversationHandler's canned clarification/chitchat lane appears unreachable from the chat path (Action Gate floors every CONVERSATION non-greeting turn); verify multi-intent/REST callers + fix-or-delete. Found while tracing where post-escape fragments land. Honest framing: single-intent path verified, multi-intent path unverified.

- Commit-verification note (subagent rule): untracked `dev/active/canonical-retest-serving-llm.json` and `uploads/ac61e65c-…/` are pre-existing, not this lane's output — deliberately excluded from the commit. The 21 origin/main commits that landed mid-lane were inspected: mail/logs/heartbeats + PPM board work, zero overlap with these seams.

## Memory & briefing surfaces referenced this session

- **Referenced**: `docs/internal/architecture/current/intent-routing-stack.md` (mandatory pre-read; surface-4 map + acceptance-adoption blocks drove the diagnosis and got the same-commit sync); `docs/internal/design/acceptance-contract-user-facing-2026-09-10.md` (§5a/§5b framing for the arms-untouched statement); CLAUDE.md §Verify First (supersession gate — kept me from re-fixing the #1394 half), §stale-sync warning (prompted the mid-lane fetch that found 21 upstream commits), env-var-strip recipe (every server-adjacent run); `feedback_supersession_gate_before_fixing` (memory index — same); `test_midgather_hold_1623.py` + `test_floor_history_principal_1394.py` (harness idioms reused wholesale).
- **Loaded but not referenced**: glossary, ROSTER, cross-pollination brief, most of the memory index's feedback entries.
- **Wanted but not found**: a written record of WHICH reply PM actually received at the 'none' → floor beat (the transcript fragment in #1529 stops at "had amnesia") — would have cut ~30 min of hypothesis-testing about which lane composed the amnesiac reply.
