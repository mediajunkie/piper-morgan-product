# Session Log — 2026-09-12 07:48 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead Dev worktree `/Users/xian/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle`
**Model**: Fable 5 (claude-fable-5)
**Task**: GitHub #1652 — offer-flag gap: #1591 standup invitation + mode read-back arm the one-slot #846 store with no `*_pending` intent_data flag, so `_apply_soft_offer` can clobber them on the rail-dispatched `get_standup` path. Acceptance-contract adoption (epic 3): adopt `evaluate_acceptance` idiom per `docs/internal/design/acceptance-contract-user-facing-2026-09-10.md`, mirroring #1617/#1739 seams.

## Plan
1. Read issue #1652 (done), acceptance.py + tests, standup_todo_offer.py precedent, intent_service.py seams, spec doc, intent-routing-stack.md.
2. Reproduce filed gap in a failing test first.
3. Adopt contract at offending seam(s); no new predicate, no local yes-detector.
4. Verify: run-sweep unit targets + ratchets; ruff 0.6.9 (ruffenv2).
5. Commit on claude/lead-cycle referencing 1652 (no closing keyword). NO push.

## Work
- 07:48 — Session start. Issue read: two older arms (#1591 invitation, mode read-back) need `*_pending`-flag treatment + contract adoption.
- 08:05 — Investigation complete. Findings:
  - **Gap confirmed at 3 arm sites** in `_handle_standup_query` (services/intent/intent_service.py): read-back arm (~4098), after-report invitation arm (~4133), and the EMPTY-lead invitation arm (~3977, a third site the issue's two-site description undercounts). Only the #1651 todo-offer arm sets a `*_pending` flag (~4148). `_apply_soft_offer`'s `_pending_flags` guard (~357-383) lists neither; rail funnel at ~2712 (`get_standup` under a QUERY-category emission → rail at 2501) exposes them to the soft-offer `set_pending_offer` clobber at ~447.
  - **Consume side**: `verify_inference` + `standup_interview` are registered WRITE×PRIVATE (workflow_entries ~1566-1590) → they fall past the READ-adopted branch and the STO branch to legacy `detect_offer_response` (line 1530). LOW_CEREMONY tier by axes; adoption = mirror READ-branch shape (silent re-arm on STATE_QUESTION per contract §5a).
  - Enforcement docstring (tests/test_architecture_enforcement.py ~2489-2492) names these residues explicitly — must update in same commit.
  - Plan: failing tests first (rail-path clobber repro + state-question survival), then flags + adoption branch.
- 08:20 — **Gap reproduced red-first**: new suite `tests/unit/services/intent_service/test_standup_offer_flag_1652.py` — 8 failed / 2 passed pre-fix. The failing e2e showed the live clobber verbatim: rail-dispatched get_standup, invitation armed, then `_apply_soft_offer` replaced the store record with the soft "meeting" offer and appended "Want me to find a time?" to the report.
- 08:35 — **Fix shipped, both halves**:
  - Arm half (the filed one-line treatment × 3 sites): `standup_interview_invitation_pending` (after-report + empty-lead invitation arms) and `verify_inference_read_back_pending` (mode read-back arm) stamped onto intent_data in `_handle_standup_query`; both flags added to `_apply_soft_offer._pending_flags`.
  - Consume half (#1739 adoption, epic 3): new branch at the generic offer seam — `verify_inference` + `standup_interview` kinds consult `evaluate_acceptance` at registry-declared axes (WRITE×PRIVATE → LOW_CEREMONY), stored ask threaded (#1665); STATE_QUESTION → silent re-arm (§5a LOW-tier survival); ACCEPT/DECLINE parity with legacy (zero-widening: LOW vocabulary = legacy rows + crisp CONFIRM superset). No new predicate, no local yes-detector, no new regex (corpus discipline: nothing extraction-shaped added).
  - Bookkeeping in same commit: `TestAcceptanceSeamContractRatchet` docstring adoption table + 2026-09-12 paragraph (tests/test_architecture_enforcement.py); `intent-routing-stack.md` #1651 entry extended with the #1652 note. `detect_offer_response` baseline row REMAINS (ftux + unregistered kinds still legacy — true remaining debt, m-44).
- **Verified how**: `scripts/run-sweep.sh unit` over the new suite + 15-file acceptance-seam neighborhood → 577 passed; full `tests/unit/services/intent_service` directory → 3826 passed, 3 deselected; `scripts/run-sweep.sh ratchets` → 54 passed (layer: unit e2e through real `process_intent`, LLM/assembler/todo/prefs boundaries mocked; denominator: whole intent_service unit dir + both ratchet files). Ruff 0.6.9 (scratchpad ruffenv2): check clean + format clean on the three touched .py files.
- Discovered work: third flag-less arm site (empty-lead invitation) — fixed in-scope, noted in issue-facing report rather than a new issue (same defect class, same fix).
