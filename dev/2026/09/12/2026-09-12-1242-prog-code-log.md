# Session Log — 2026-09-12 12:42 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead Dev's worktree on branch `claude/lead-cycle`
**Model**: claude-fable-5
**Task**: GitHub #1738 — Piper reads its OWN truncated render as its data source (archived-projects list cut to 5 for display becomes "the list I got back only showed five names" next turn). Last epic-5 build item (honest-empty / GatherOutcome family: #1717, #1736 landed; #1730 remainder is a ruling with Arch).
**Spec**: docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md
**Constraints**: red-first repro; check today's #1596 commit d32429975 (history/floor context) for collisions; provenance half only if the fix splits with epic-6 rendered-deliverable cousin; commit on claude/lead-cycle referencing 1738 (no closing keyword), do NOT push.

## Work

- 12:42 — Session start. Read #1738 in full (body + triage comment). Three defects noted in the issue: (1) truncation-as-data-source [core], (2) broken `restore <name>` template slot, (3) unconditional degraded-turn rider (likely #1717 family) + truncation unconditional even with "(show all names)".
- 12:45–12:52 — **Diagnosis (m-43, layer by layer)**:
  - **Truncation site**: `canonical_handlers.py` `_handle_portfolio_query` — `list` (:4200) and `list_archived` (:4233) both render `projects[:5]` + `"...and N more"`; rail twin in `workflow_entries.py` `archived_projects_entry` (~:831). `intent.context` carries only `project_count` — the names are destroyed at render time.
  - **Next-turn context**: `build_recent_history` (#1122, conversation_context.py:640) is the single history source; it carries `turn.response` — the RENDERED string — and nothing else. No structured per-turn data record exists. So whatever the render drops is provably absent from the model's next-turn world. Exactly Arch's §5b clause: the assistant reading its own render as evidence.
  - **Restore slot (defect 1)**: copy is literally `Say "restore <name>"` at both sites — `<name>` swallowed by the web render as an unknown HTML tag → PM saw the empty slot.
  - **Rider (defect 2)**: `_aggregate_messages` (orchestrator.py:284) emits it only for a FAILED sibling intent. "list my archived projects" matches BOTH `PORTFOLIO_PATTERNS`' list pattern AND `STATUS_PATTERNS`' `\blist.*projects\b` in `detect_multiple_intents` → phantom STATUS/get_project_status sibling → multi-intent → orchestrator → sibling handler throws live → rider stapled onto the successful listing. Subsumption filter (#919/#1084) had no PORTFOLIO-subsumes-STATUS rule. Red output confirmed: `pattern_lists=['PORTFOLIO_PATTERNS', 'STATUS_PATTERNS']`. Same rider text as the #1431 screenshots — firing on this utterance for ≥1 month.
  - **No collision with d32429975 (#1596)**: that commit touched CONVERSATION-floor gather baseline + escape handoff; this work touches portfolio renders, subsumption, and no floor context assembly. `test_floor_escape_amnesia_1596.py` included in neighborhood sweep — green.
- 12:52 — **Red-first**: new `tests/unit/services/intent_service/test_truncated_render_provenance_1738.py` — 14 tests, pre-fix **12 failed / 2 passed** (the 2 are deliberate green controls: pure-STATUS ask, portfolio-WRITE+status ask). Headline red: `'Sixth Project' missing from the archived-list render`.
- 12:53–12:57 — **Fix (provenance half)**:
  - `canonical_handlers.py`: both list branches render the FULL set (no `[:5]`, no `"...and N more"`); restore hint → `"restore [project name]"`. §5b comments inline.
  - `workflow_entries.py`: rail archived entry, same fix.
  - `pre_classifier.py`: extracted `PORTFOLIO_LIST_PATTERN` (the existing literal, named — no new pattern, #1559-clean) + `_apply_subsumption_filter` rule: drops only STATUS/`get_project_status`, only when the message matches the list claim (portfolio WRITE + genuine status ask keeps both; `check_completion_status` never collateral).
  - Post-fix: 14/14 green. Neighborhood sweep (1738+1431+1570+1084+1505+1596): 72 passed. Full `tests/unit/services/intent_service`: **4037 passed, 3 deselected** (background run; ruff-format-only delta after start, neighborhood re-run green post-format: 55 passed). Ratchets: **54 passed**. Ruff 0.6.9 (scratchpad ruffenv2): check clean, format applied + clean.
  - Doc sync: `intent-routing-stack.md` surface-1 row — #1738 subsumption note added same commit.
- **Deliberately left for epic 6 (rendered-deliverable cousin)**: display policy for genuinely long lists (caps/pagination/deliverable render consuming a structured GatherOutcome it never rewrites). This fix makes render == data for the bounded portfolio lists; it does NOT build the structured-outcome channel.
- **Discovered work filed**: #1762 (render-truncation sweep — ~18 more `...and N more` sites incl. conversational_floor.py:848 domain_context render + context_assembler gather-layer `[:5]`/limit=5 caps), #1763 (get_project_status handler failing live on every v70 turn — cause unverified, rider evidence).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (m-43 name-the-layer, red-first/verify-first, discovered-work discipline, no-closing-keyword rule); docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md (§2 reportability, §5b joint invariant — the fix's spec); intent-routing-stack.md (4-surface model, moratorium context); test files 1431/1570/1084 (harness + subsumption precedent); commit d32429975 message (#1596 collision check).
- **Loaded but not referenced**: MEMORY.md index entries beyond the above.
- **Wanted but not found**: none — the spec + prior test files covered everything needed.
