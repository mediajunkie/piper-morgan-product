# Session Log — 2026-09-12 10:43 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead Dev worktree `/Users/xian/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle`
**Model**: Fable 5 (claude-fable-5)
**Task**: Batch of three corpus-deposit issues (epic 4): #1559, #1579, #1606. Deposit corpus rows only — no extraction patterns/routing code (ratified 2026-08-29 policy; TestExtractionPatternRatchet).

## Plan
1. Read all three issues (done), read corpus schema from 5ea5cd562 + existing rows
2. Verify each phrasing still fails/misroutes today (post #1696 bulk-delete, #1653/#1654 reminder seams)
3. Deposit rows with verbatim phrasing, source citations, expected routing (REVIEW-only if ambiguous)
4. Run corpus-schema/gate tests + scripts/run-sweep.sh ratchets
5. Commit on claude/lead-cycle referencing the three issue numbers (no closing keywords); do NOT push

## Work

### Verification (surface 1 = deterministic pre-classifier, run TODAY at HEAD e0fb10b81; LLM layer NOT probed — named per m-43)
- #1559: both PM verbatims ("remind me at 3pm tomorrow to review the PR", "remind me at 9:41 today to check in with the lead developer") -> `pre_classify` = None. REMINDER_PATTERNS unchanged (`pre_classifier.py:656` still `\bremind\s+me\s+(?:to|about)\b` — adjacency gap intact; #1653/#1654 did not touch it). STILL FAILS at surface 1; downstream remains the LLM coin flip the issue describes.
- #1579: "show me my archived projects" -> `status/get_project_status @1.0` — the 'me'-token rejection persists verbatim. Control "list my archived projects" -> `portfolio/manage_portfolio @1.0` (works). Matches PM's 09-09 live comment (v70 still failing, new deflection shape). STILL FAILS.
- #1606: full two-part message -> `query/set_default_repo @1.0` at surface 1 — the whole message is claimed by the set-command pattern, so request 1 (reminder-clear) is never seen and request 2 (a question) is treated as an invocation. #1696 (40ae79298) is downstream in run_delete_todo_workflow and cannot affect this surface-1 claim. Standalone interrogative also -> `set_default_repo @1.0`. First half alone -> None (LLM layer). STILL FAILS.

### Deposits (scripts/build_inversion_corpus_phase0.py HAND_ROWS -> regenerated tests/fixtures/inversion_corpus_phase0.yaml, 97 -> 104 rows)
- #1559: +1 row — turn-1 verbatim "remind me at 3pm tomorrow to review the PR" (action:create_reminder, TEMPORAL). Turn-2 row already deposited; issue says BOTH are acceptance cases.
- #1579: +1 row — 09-09 comment drift verbatim "list my archive projects" (action:manage_portfolio pin; failure is extraction-layer, noted). Core rows were already deposited (phase0 + routing_corpus_1283.yaml:134-139).
- #1606: +6 rows — the issue's two body rows already existed; deposited the comment-thread verbatims none of which had reached any corpus: "use the interview from now on" (REVIEW), "use the standup interview format by default from now on" (REVIEW; since handled by #1591, pinned there), "change the status of issue #108 to Done" (action:update_issue, #1411 family), "please mark issue #108 in the mediajunkie/test-piper-morgan repo complete" (REVIEW; still claimed by execution/complete_todo @1.0 today), "add a reminder: test the safe clarification" (action:create_reminder), 'please remind me: ask Lead how to test "outwardness disclosure" today' (action:create_reminder). Elided fragments + grammar templates NOT deposited (no full verbatim; nothing invented).
- Fixed my own first draft: builder's YAML writer escapes quotes only in `phrase` — reworded the 1579 source line to drop embedded double quotes; yaml.safe_load validates, 0 duplicate phrases.

### Findings (reported, not changed)
- Builder merge order swallows HAND_ROW expected/category for "show me my archived projects": corpus-1283's REVIEW wins and bucket(REVIEW) puts the row in the QUERY denominator, not PORTFOLIO. Left as-is (REVIEW is right — manage_portfolio #1431 branch vs list_archived_projects rail key is genuinely ambiguous), but the dead assertion in HAND_ROWS and the QUERY bucketing are worth a Lead/Arch look.

### Verified how
- Gate/corpus tests: `pytest tests/unit/test_inversion_phase2_gate_1595.py tests/unit/services/intent_service/test_routing_vocabulary_1283.py tests/unit/services/intent_service/test_lens_corpus.py tests/unit/services/intent_service/test_inversion_router_1595.py -q` -> 115 passed (layer: corpus loaders/scorers + pins, no LLM).
- `scripts/run-sweep.sh ratchets` -> 54 passed (incl. TestExtractionPatternRatchet — no patterns added, ceilings untouched).
- ruff 0.6.9 (scratchpad ruffenv2): check + format-check clean on the one touched .py.
- Surface-1 probes quoted above run this session via venv/bin/python with ANTHROPIC_* stripped.
- Denominator: intent_service unit subset (`scripts/run-sweep.sh unit tests/unit/services/intent_service`) -> 2158 passed, 4 skipped, PYTEST_EXIT=0 in 4:45. Full suite not run (fixture + builder-only change). First background attempt piped through `tail -3`, which swallowed the summary AND masked pytest's exit code behind tail's — rerun without the pipe; the quoted result is the clean rerun.

## Wrap
- Committed 6009193f6 on claude/lead-cycle (NOT pushed, per task instruction). 3 files: builder, regenerated corpus, this log.
- origin/main checked before commit: 6 ahead, all CIO housekeeping, zero overlap with corpus surfaces.
- Discovered issues filed: none (the builder merge-swallow finding is reported to the Lead in the batch report for disposition — it is a corpus-infrastructure nuance inside an open epic's tooling, Lead's call whether it warrants an issue).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-var strip for server-adjacent python, sync-before-read discipline, commit-only-own-files, Verified-how field); issue 1595 epic body (corpus schema conventions, REVIEW semantics); commit 5ea5cd562 message (flow:<op> form, corpus file locations); inversion_corpus_phase2_armed.yaml header (phase0 denominator-freeze rationale); feedback_supersession_gate_before_fixing (checked existing deposits before adding); feedback_verify_at_the_right_layer / m-43 (named the pre-classifier layer on every probe).
- **Loaded but not referenced**: memory index at large; mailbox/duty-cycle sections of CLAUDE.md (not a duty-cycle session).
- **Wanted but not found**: a written pointer that ruffenv2 lives in the session scratchpad (recovered it from prior prog logs in dev/2026/09).
