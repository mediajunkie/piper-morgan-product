# Session Log — 2026-09-12 09:07 — Coding Agent (prog-code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead Dev's worktree, branch claude/lead-cycle)
**Task**: GitHub #1753 — `_apply_soft_offer` can clobber a STATE_QUESTION-survived arm (no-clobber guard reads only result.intent_data flags; survival turns compose results with no flag)

## Plan
1. Read issue #1753 (done — store-level guard proposed by issue itself)
2. Read seam composition: `_apply_soft_offer` + `_pending_flags` in services/intent/intent_service.py, survival paths in reminder_clear.py, todo_handlers.py, standup_todo_offer.py, generic seam
3. Red-first repro: STATE_QUESTION survival turn + soft offer eligible → arm clobbered
4. Fix at store layer (peek before set) unless flags rail is load-bearing otherwise
5. Pins: 2 e2e survival paths + guard unit + no-over-block pin
6. run-sweep unit + full tests/unit/services/intent_service + ratchets + ruff (ruffenv2)
7. Commit on claude/lead-cycle referencing 1753 (no closing keyword), NO push

## Work

- Fire (09:07) — session start, issue read.
- 09:10 — Composition read confirmed the filed gap: `_apply_soft_offer`'s `_pending_flags` guard reads only `result.intent_data`; survival turns (generic READ branch ~L1420, #1652 branch ~L1574, CONFIRM branch ~L1372, seam-specific fall-throughs in reminder_clear/todo_handlers) re-arm the store and let normal processing compose a flag-less result, which funnels through `_apply_soft_offer` (UNKNOWN path L2890→L2901 among 12 sites). Store is popped unconditionally at L1107 before classification, so ANY entry present at apply time was armed/re-armed THIS turn — the store is the honest single source of truth (issue's proposed direction confirmed as the right layer; flag stamping at survival time would need per-seam copies, exactly what the task forbade).
- 09:13 — RED: `test_soft_offer_survival_clobber_1753.py` reproduced the clobber on both e2e paths: generic READ (meeting arm → 'project_setup' replaced it) and reminder verb question (pending_action gone); guard-unit red too. 4 failed / 3 passed pre-fix.
- 09:15 — FIX: store-peek guard in `_apply_soft_offer` after the flag belt (peek_pending_offer #1595 read-only; skip + log `soft_offer_skipped_live_pending_arm`). Flag belt retained (no #1652 regression). peek_pending_offer docstring updated (second sanctioned reader). First version had a defensive `except Exception` — tripped the #1424 silent-death ratchet (198>197); removed it (peek is a plain dict read; a raise there is a real defect to surface).
- 09:18 — GREEN: 7/7 new tests; ratchets 54 passed; seam neighborhood (1652/1653/1654/1739/1651/1529/soft-invocation suites) 310 passed; ruff 0.6.9 clean (check + format). Full tests/unit/services/intent_service re-running on final code (first run: 3870 passed pre-ratchet-tweak).
- Docs same-commit: intent-routing-stack.md guard description updated (flags + store peek, two-way guard); test_architecture_enforcement.py adoption narrative gained the #1753 corollary paragraph.
- 09:24 — Final verification on final code: full tests/unit/services/intent_service via run-sweep unit = 3870 passed, 3 deselected; test_architecture_enforcement.py 49 passed (after docstring edit); ratchets 54 passed; new suite 7 passed; ruff 0.6.9 check + format clean on all three changed py files. Contract design doc checked — no soft-offer mention, no update needed there.

## Wrap

**Verified how:** red-first repro (4 tests failing pre-fix with the exact clobber shape: 'project_setup' == 'meeting' assertion diff), then on final code: `scripts/run-sweep.sh unit tests/unit/services/intent_service` → 3870 passed / 0 failed (layer: unit suite, denominator: entire intent_service unit tree); `scripts/run-sweep.sh ratchets` → 54 passed; seam-neighborhood suites (1652/1653/1654/1739/1651/1529/soft-invocation ×3, accept-decline) → 310 passed; `venv/bin/ruff` (0.6.9) check + format --check clean on the 3 changed py files.

**Discovered work:** none filed. Noted in passing (not defects): the CONFIRM-tier survival path shares the same window and is covered by the same store guard; `suggestions_count = 0  # TODO` in `_apply_soft_offer` is pre-existing tracked debt.

**Not pushed** per task instruction — commit left on claude/lead-cycle for the Lead's review.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (session-start protocol, red-first/evidence discipline, ratchet rules, no-closing-keyword-near-issue-number rule); issue #1753 body (proposed store-layer direction); `tests/unit/services/intent_service/test_standup_offer_flag_1652.py` + `test_reminder_clear_verb_anchor_1653.py` + `test_acceptance_contract_1739.py` (test idioms); `docs/internal/architecture/current/intent-routing-stack.md` (guard description — updated same-commit).
- **Loaded but not referenced**: memory index (MEMORY.md), cross-pollination brief.
- **Wanted but not found**: none.
