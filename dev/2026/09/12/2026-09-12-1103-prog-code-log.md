# Session Log — 2026-09-12 11:03 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), delegated by Lead Dev
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Task**: GitHub #1505 — pre-classifier multi-intent path drops integration-connect asks paired with something else. Supersession-check first (epic-4 corpus work this week may have changed behavior), then diagnose plumbing vs coverage, fix if plumbing.

## Plan
1. Read issue #1505 (done — filed 2026-08-08, no comments). Defect claim: `detect_multiple_intents` has no INTEGRATION_CONNECT pattern group; 'hi piper, connect my github' → greeting only.
2. Mandatory read: docs/internal/architecture/current/intent-routing-stack.md
3. Reproduce verbatims today through real pre-classifier; check supersession (1e1438033 corpus notes, #1591, Inversion shadow).
4. Diagnose; fix at multi-intent layer if plumbing (red-first pins); corpus rows if coverage.
5. Verify: pins + run-sweep.sh unit + full intent_service unit suite + ratchets + ruff 0.6.9 (ruffenv2). Commit on claude/lead-cycle, NO push.

## Work
- 11:03 — Session start. Issue read. No comments on issue. Worktree synced (0 behind / 0 ahead of origin/main at start).
- 11:04 — Mandatory read: intent-routing-stack.md (full, both pages). Noted: routing is 4-surface; surface 1 has TWO entry points (pre_classify + detect_multiple_intents); pre-claim shadow probe (2026-09-02) threads pattern-list names through MultiIntentResult.pattern_lists — any fix must keep that alignment.
- 11:05 — **Supersession check: NOT superseded.** Evidence: (a) today's 1e1438033 is corpus-only (fixture rows; its 1606 multi-part note is about set_default_repo claiming a two-part message — different defect); (b) `git log --since=2026-08-08 -- pre_classifier.py` shows 4 commits, none touching the multi-intent connect gap (preclaim probe, 1527 delete narrowing, ruff format, 1521 reminder-query); (c) #1591 is standup-handler-internal; (d) inversion shadow/preclaim shadow are observers by construction (enforcement test pins non-dispatch).
- 11:06 — **Reproduced all issue verbatims TODAY** (deterministic probe through real `PreClassifier`, no LLM needed):
  - `'hi piper, connect my github'` → `[(conversation,greeting)]` — connect ask DROPPED at surface 1's `detect_multiple_intents`; since `classify_multiple` returns any non-empty detection, the LLM never sees it. Total drop.
  - `'hi piper, connect my calendar'` → greeting + guidance (survives ONLY via the 1471 TEMPORAL substitution).
  - slack/notion pairings and `'hey, link my google calendar'` (dodges the temporal `\bmy calendar\b`) also dropped.
  - `'connect my github'` alone → detect returns [] → falls to classify() → pre_classify claims (guidance, setup_target=github). Single path fine, as the issue said.
- 11:07 — **Diagnosis: PLUMBING.** The pattern list exists and the single path claims all these phrasings since #1417/#1471; the multi path's `pattern_groups` table simply omitted the group. No new regex needed — reuse `_integration_connect_match` (blockers included). Ratchet-clean by construction.
- 11:08 — Red-first pins: `tests/unit/services/intent_service/test_multi_intent_connect_1505.py` — 15 tests, **7 red pre-fix** (the drop family + pattern_lists threading + classify_multiple e2e), 8 green controls (1471 parity, blockers, temporal control).
- 11:08 — Fix in `detect_multiple_intents`: INTEGRATION_CONNECT group added to `pattern_groups` before TEMPORAL (mirrors pre_classify precedence, #1471); loop body blocker-guards it via `_integration_connect_match` (repo lane #862 + event-writes #1471 hold on this path too); the 1471 TEMPORAL substitution special-case retired — replaced by a conditional TEMPORAL skip on connect-claim (byte-for-byte parity with substitution-era output, incl. the known two-part temporal+connect limitation) + the existing GUIDANCE dedupe (flag renamed connect_substituted → connect_claimed). pattern_lists threading kept (`INTEGRATION_CONNECT_PATTERNS` name).
- 11:10 — Stale comment fixed in `test_integration_connect_preclassifier_1417.py` header (described the retired substitution). Routing-stack doc checked: no statement went stale (it never described the multi-path gap or the substitution mechanism).
- 11:12 — **Verification** (all with env-strip via run-sweep.sh where applicable):
  - 1505 pins + 1417/1471 suite: **51 passed** post-fix (was 7F/8P + 36P pre-fix).
  - `run-sweep.sh unit tests/unit/services/test_multi_intent.py tests/unit/services/test_pre_classifier.py tests/unit/services/intent_service`: **3988 passed, 3 deselected** (184s).
  - `run-sweep.sh ratchets`: **54 passed** (TestExtractionPatternRatchet untouched — no new extraction regex).
  - ruff 0.6.9 (scratchpad ruffenv2): check + format-check clean on all 3 touched .py files.
  - Probe re-run post-fix: all pairings now greeting+guidance with correct setup_target; single-ask claims at surface 1 with identical destination; temporal+connect two-part case byte-identical to pre-fix (documented limitation).
- 11:14 — **Discovered work filed: #1755** — genuine two-part temporal+connect message loses its temporal half (pre-existing under the 1471 substitution, preserved deliberately for parity; fix shape = span-overlap suppression instead of group-level skip).

## Memory & briefing surfaces referenced this session
- **Referenced**: intent-routing-stack.md (mandatory read — surface map, preclaim-shadow threading contract, ratchet policy); CLAUDE.md (corpus-vs-plumbing policy, commit discipline, discovered-work discipline, no-closing-keyword rule); prior prog logs in dev/2026/09 (ruffenv2 location).
- **Loaded but not referenced**: MEMORY.md index (no memory file opened).
- **Wanted but not found**: a durable pointer for ruffenv2's location (again — same gap the 1043 prog log flagged; it lives in the session scratchpad, recovered from prior prog logs).

Verified how: pytest runs quoted above executed this session (unit layer — pattern-detection + classifier-entry e2e; not a live-server render test); denominator: full tests/unit/services/intent_service (3988) + both pre-classifier/multi-intent suites + 54 ratchets; ruff 0.6.9 on the 3 touched files.

