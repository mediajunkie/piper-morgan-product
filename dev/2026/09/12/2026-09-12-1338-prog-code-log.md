# Session Log — 2026-09-12 13:38 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)
**Task**: Run the two live replays owed against v89 per the pre-run policy — #1736 ("show me
issue #112" renders the real description + URL line) and #1738 (archived-projects list renders
the full bounded set, no rider, follow-up answers from the true set). Read-only replays; report
transcripts + PASS/FAIL per criterion; commit session log only, no push.

## Method discovery (13:38–)

- Read #1736 + #1738 (verbatim turns + PASS criteria), fix commits 24cd313eb (#1736) and
  39e7aa864 (#1738), lead log (v87/v88/v89 deploys, replays owed).
- Yesterday's #1617 replay lane (dev/2026/09/11/2026-09-11-0935-prog-code-log.md): the #1621
  live-harness idiom — real `main.py` subprocess (env-scrubbed), throwaway account through real
  `POST /api/v1/auth/login`, turns through real `POST /api/v1/intent`. That replay ran against
  **v72's code locally**, not against fly.dev.
- Prod-drive assessment: fly access exists (`fly status` OK, v89 machine confirmed started), but
  **no prod account credentials exist on this seat** — no self-serve registration (pruned per
  #1504; /api/v1/auth has login/refresh/logout only), create-user requires an invite token that
  lives in PROD's invite_tokens table (minting = prod DB write), and the two provisioned test
  accounts (`~/.piper-shared/`) are dev-server-only. A fly-ssh env probe was also blocked by the
  permission classifier — not worked around, per instructions.
- Decision: run the replays with the proven pre-run-policy method — the #1621 harness at the
  exact code deployed as v89 (both fix commits verified ancestors of HEAD) — and report the
  prod-credential gap explicitly rather than improvise auth (per task instruction). Layer named
  per m-43 in the report.

## Replay runs (13:49–13:51) — BOTH PASS

Harness: temp never-committed `tests/live/test_replay_1736_1738_tmp.py` (deleted post-run);
real `main.py` subprocess (ANTHROPIC_* scrubbed, ephemeral port, real Postgres 5433), throwaway
account via real `POST /api/v1/auth/login` (JWT cookie), turns via real `POST /api/v1/intent`.
Server env carried `PIPER_DEFAULT_REPO=mediajunkie/test-piper-morgan` (repo-resolver env leg —
throwaway has no default_repo binding; mirrors PM's binding to the same repo).

**Replay 1 (#1736)** — PASS 2/2 criteria. First attempt degraded honestly ("GitHub isn't
configured yet"): #1461 made env GITHUB_TOKEN system-only, so a real user needs their OWN
connected PAT — which is PM's actual account state. Fixed the replay's account-state fidelity,
not the product: connected the seat's gh token through the REAL Settings endpoint
(`POST /api/v1/settings/integrations/github/save`, validated via GitHub GET /user, stored
user-scoped in keychain), then re-ran. Verbatim turn "show me issue #112" →
`**Description:**\nchecking that stated slots are used` (the issue's real body; no
"No description") + `**URL:** https://github.com/mediajunkie/test-piper-morgan/issues/112`
(uri-fallback line rendering). PAT-path fingerprint confirmed: `retrieved_via: github_api`,
labels as plain strings. Keychain entry deleted post-run (`deleted=True` in output).

**Replay 2 (#1738)** — PASS all criteria, single session, 3 turns. Seeded 6 archived projects
(Klatch, Test, Test1, Test2, Test3, Zephyr) for the throwaway user. T1 verbatim "list my
archived projects" → "You have 6 archived projects:" + ALL six names, no "...and N more", no
"wasn't able to check on project status" rider, restore hint renders `[project name]` (not the
swallowed `<name>`). T2 verbatim "what's the sixth one?" → "The sixth archived project is
\"Zephyr.\"" — answers from the true set. T3 "how many archived projects do I have?" → "You
have 6 archived projects." No mutating turn sent in either replay (#1738's verbatim had no
mutating follow-up; none was substituted in).

Note: both first-turn replies carried the trailing ADR-075 OQ-3 once-per-account notice
"(Running with a default configuration…)" — designed first-response injection, impossible on
PM's established account (same note as the 09-11 v72 replay).

## Teardown proof

- `pgrep -fl "main.py --no-browser"` → none; dev server 8001 (PID 30735) untouched.
- Count-verified fixture cleanup passed in-run; post-run sweep: `live_verify_%` users = 0,
  orphaned seeded projects = 0. Keychain entry for the throwaway deleted.
- Temp test file deleted. Untracked `ruffenv2/`, `uploads/…`, `canonical-retest-serving-llm.json`
  predate this session (mtimes checked) — not mine, left alone.

## Prod-credential gap (reported, not improvised around)

The task asked for replays "against production v89 (fly.dev)". Driving fly.dev turns directly
requires a production account this seat does not have: no self-serve registration on prod
(pruned per #1504; /api/v1/auth exposes login/refresh/logout only), `POST /api/v1/setup/create-user`
requires an invite token that lives in PROD's invite_tokens table (minting one = prod-DB write),
and both provisioned test accounts (`~/.piper-shared/`) are dev-server-only. A fly-ssh env probe
was additionally blocked by the permission classifier (not worked around). Yesterday's 3/3 "v72"
replay precedent (2026-09-11-0935-prog log) ran the same way as today: the #1621 harness against
the deployed release's code. Layer named per m-43: these replays verify v89's exact code
(24cd313eb + 39e7aa864 both `merge-base --is-ancestor` HEAD) through the full real HTTP stack
(login, cookie auth, middleware, intent dispatch, handlers, DB, live GitHub API), on a local
server process — NOT the fly.dev machine itself, and not the browser-render layer.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-stripped launch #1258, never-guess, m-43/m-44 layer+denominator
  discipline, STOP-vs-improvise on credentials); 2026-09-11-0935 prog log (the proven replay
  idiom); `tests/live/conftest.py` #1621 docstring (honesty rules); #1736/#1738 issue bodies
  (verbatim turns + criteria); fix commits 24cd313eb/39e7aa864 (expected render shapes);
  memory pins `project_web_browser_lane_test_account` + `project_web_browser_automation_pilot`
  (report-the-credential-gap precedent, dev-only accounts); #1461 gate history in
  config_service.py (explained the first-attempt degrade).
- **Loaded but not referenced**: memory index entries broadly.
- **Wanted but not found**: a documented prod-replay account/procedure for fly.dev (gap named
  above — this is the second time a lane has hit "no prod account exists for verification").

