# Session Log — 2026-09-12 12:25 — Coding Agent (prog-code)

**Role**: Coding Agent (prog)
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead Dev's worktree, branch claude/lead-cycle)
**Task**: GitHub #1736 — chat read-back of an issue reports "Description: No description" while the issue HAS a body (fabricated absence). EPIC 5 honest-empty / GatherOutcome contract.

## Plan
1. Read issue #1736 (done — body fetched via gh JSON; `--comments` view rendered oddly truncated)
2. Read spec: docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md
3. Diagnose fabrication layer: fetch (github_adapter) vs mapping vs composer vs template
4. Red-first repro through real read-back path; fix; sweep; ruff; commit (no push)

## Work
- 12:25 — Session start. Worktree 0 behind origin/main. Found candidate fabrication site: services/intent/intent_service.py:5487 `body = issue.get("body", "No description") or "No description"`.
- 12:28 — **Diagnosis (m-43, layer named)**: two-half defect, both below the floor (SOURCE_FAILED_FLAGS registry NOT involved — this handler composes its message directly, never through `_format_domain_context`).
  - **Mapping layer (native PAT path)**: `get_github_issue_direct` (services/mcp/consumer/github_adapter.py:~1779) maps GitHub's `body` into `description` and emits NO `body` key. The connector parser (`_parse_issue_detail`) emits both keys and claims to "mirror the native shape" — it didn't, until now.
  - **Composer**: `_handle_review_issue_query` (services/intent/intent_service.py:5487) read only `issue.get("body", "No description")` — conflating "field absent from payload" with "empty body" → fabricated absence. PM's live turn (labels rendered as plain strings = the native shape's format) went down the PAT fallback.
  - Same shape-mismatch family: composer read `html_url` only; native shape carries `uri` → URL line silently vanished on the PAT path.
- 12:29 — **Red-first**: 4 new pins in `TestReviewIssueHonestEmpty1736` (tests/unit/services/intent_service/test_github_query_handlers.py) — ALL RED pre-fix; the first one reproduces PM's live output character-for-character (`**Description:**\nNo description` for an issue whose payload carries the body under `description`).
- 12:31 — **Fix** (honest-empty per GatherOutcome contract §3):
  - Composer: three states, three renders — delivered+non-empty → preview; delivered+empty → definite `(none — this issue has no description)` with no hedge (verified_empty); field absent → `(I couldn't retrieve the description this turn — view the issue on GitHub to read it)`, never an empty claim. Plus `html_url or uri` URL fallback.
  - Mapping: `get_github_issue_direct` now emits the body under BOTH `body` and `description` (`""` when GitHub returns null — delivered verified-empty, distinct from absent).
  - New adapter suite: tests/unit/services/mcp/consumer/test_github_adapter_issue_body_1736.py (2 tests, mocks `_call_github_api` per the sibling-suite idiom).
- 12:33 — **Verification**: 6/6 new tests green post-fix. `run-sweep.sh unit "tests/unit/services/intent_service tests/unit/services/mcp/consumer"` → **4230 passed, 0 failed** (3 deselected). `run-sweep.sh ratchets` → **54 passed**. ruff 0.6.9 (ruffenv2, recreated in scratchpad): check clean + format clean on all 4 touched files; baseline stash-check confirmed intent_service.py was format-conformant before AND after.
- **Verified how**: commands above run this session in this worktree; layer = unit tests through the real `_handle_review_issue_query` composer with the connector mocked to CONNECT_REQUIRED (the PAT fallback PM's live turn took) + the real adapter mapping with only the HTTP transport mocked; denominator = the two touched suites in full (4230) + all ratchets (54). NOT measured: a live delivered turn over a real GitHub connection.

## Discovered work (reported to Lead, not filed)
1. `test_formats_issue_details_correctly` (pre-existing) mocks `router.get_issue` with the RAW GitHub shape (`body`, dict labels) — not the shape the real router returns. That test-shape mismatch is exactly what masked #1736. New pins cover the real native shape; the old test still passes (raw-shape tolerance is intended) but is worth a note.
2. `services/mcp/consumer/consumer_core.py:180` — `resource.get("description", "No description available")` on MCP resource listings: same fabricated-absence family (absent field rendered as a definite empty), different surface. Honest-empty candidate for the GatherOutcome epic.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (session protocol, m-43/m-44, worktree rules, no-closing-keyword rule); docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md (§3 provenance table drove the three-state render + no-hedge-on-verified-empty); prior prog logs in dev/2026/09 (ruffenv2 location convention).
- **Loaded but not referenced**: MEMORY.md index; briefing table.
- **Wanted but not found**: a durable pointer that ruffenv2 lives in the (session-scoped, hence perishable) scratchpad — recovered from a prior prog log again; second session in a row per the 09-12 1043 log.
