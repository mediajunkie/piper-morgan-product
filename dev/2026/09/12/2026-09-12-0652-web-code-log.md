# Session Log: 2026-09-12-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Saturday, September 12, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/11/2026-09-11-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-11** (reporting-and-coordination day — Step 0 self-heal for 09-10's idle evening, Ship #060 workstream report filed with live-verified issue states, chunking-thread self-check datapoint contributed, two fast-moving cohort threads drained as cc's, zero code changes).

## Session Objectives

1. Normal duty-cycle drain (mail loop → task loop → idle) — no PM-assigned task this fire, cron-triggered.
2. Open threads: WYSIWYG question with PM (their answer pending), Vercel Q1 (access-blocked, standing item #4).

## Work Log

### 06:52 AM - Session Start

- `CronList`: exactly one job (`7aca645b`, `22 6,9,12,15,18,21 * * *`) — no duplicates.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-11 -->` — no self-heal needed.
- Fingerprint check both worktrees: `web` / `claude/web-cycle` — correct, no collision indicators.
- Synced both worktrees clean: product repo, website repo.
- Cohort-freeze check: `rc=0`, INSUFFICIENT-SCHEDULE note (overnight window — not a freeze signal).
- Created this session log.
- **Mail loop**: genuinely empty.
- **Task loop — first real application of PM's 09-11 work-queue ruling**: with
  mail empty, checked role-relevant GitHub issues (`piper-morgan-website`,
  open) as a first-class queue source rather than going idle. Found six open
  issues; four were genuinely unblocked and mine. Drained them in one wake:

**1. website#33 (hero-image link-check guard) — CLOSED as already-implemented.**
The guard it requested is exactly `check-hero-image-refs.js` (shipped 08-30 for
product #1669, wired into `prebuild`). Re-ran it fresh before closing: 535 refs,
0 broken, exit 0. Closed with evidence plus the one honest delta (build-time
blocking vs. the issue's live-HTTP ideal — named, with reasoning why the
build-block closes the class at its introduction point).

**2. website#18 (alt text dropped during sync) — CLOSED, resolved long ago +
one residual gap found and fixed.** The requested pipeline fix has been in
`fetch-blog-posts.js` since the June alt-text work; all four evidence posts
verified carrying alt text. Corpus-wide check found 390/391 — the one gap
(`weekly-ship-051`) was an empty CSV source cell, not the pipeline. Filled it
with the modal alt text 11 other piper-ship rows already use (not invented
copy), synced, verified **391/391** (website `9e6b8c1`, deployed `success`).
⚠️ **Hit CLAUDE.md's auto-close-keyword gotcha on my own commit**: the message
contained "the pipeline fix #18 requested" — `fix #18` auto-closed the issue
before my explicit close ran. Benign here (I wanted it closed; banner+evidence
landed anyway) but logged as a real recurrence of the documented trap — on an
issue I didn't want closed this would have been the 2026-07 incident again.

**3. website#40 (dark-mode critical-CSS cascade bug) — FIXED, CLOSED.** The
09-03 diagnosis implemented: wrapped the inline critical-CSS block in
`@layer base` (website `5984b13`, deployed `success`). One load-bearing
refinement discovered during implementation, recorded in both the code comment
and the close: a NEW layer name (the issue's literal option 1) would NOT have
worked — the compiled stylesheet loads earlier in document order, so a
later-declared layer would outrank `utilities` and re-create the bug; merging
into Tailwind's already-declared `base` is deterministic. Verified the
compiled CSS reproduces everything the block provides before layering it
(sole-source rules like `.site-container` confirmed safe — layering only
demotes against competitors). Real-browser verification: the exact
`getComputedStyle` check that failed on 09-03 now returns `#f1f5f9` in dark
mode; light mode and layout unregressed; dark screenshot visually clean.

**4. website#32 (--pub-date UTC default) — FIXED, CLOSED.** `todayIso()` now
computes the local calendar date (website `58e180e`). Chose local-default over
the issue's fail-loud alternative deliberately: a defaulted pubDate is
semantically correct (only the UTC computation was wrong), and fail-loud would
have broken every caller that omits the flag — including the corpus harness
itself. Single call site confirmed by grep; corpus 21/21 post-change; the
honest verification limit (divergence window not demonstrable at 7am) named
in the close.

**Deferred with a named trigger**: website#35 (composer restore-blank) and
website#37 (publish-time image archival) both need genuine re-investigation —
deferring to a **fresh session** (real capacity limit at the tail of a long
multi-item wake, not pacing). Both remain visible open-queue items under the
new ruling; they cannot silently vanish.

- Emitted the START heartbeat. Fires drained as one wake per the spine.

### ~07:15 AM — PM direct (three items): WYSIWYG direction + two bug reports

**WYSIWYG**: PM clarified the real pain is readability-while-editing (markdown
conventions are not the problem) and proposed the cheapest alternative
themselves — a raw/rendered toggle that maintains cursor location. Accepted as
the direction; scoped work to pick up after the bugs.

**Bug 2 (work dates) — root-caused, repaired, deployed (website `41a795d`).**
PM: work dates "uniformly show the day after the pub date" + suspected
ordering issues. Diagnosis inverted the attribution: measured all 391 posts
against the CSV authority — **workDate string+ISO are 391/391 correct;
publishedAt strings were wrong on 153 posts and publishedAtISO on 148.** On
the 147 posts where work==pub (the old defaulted `--work-date` era), the
one-day-early pub string made the correct work date LOOK like pub+1 — the
exact reported symptom. Two stale-data corruption families (pre-UTC-fix local
formatting → strings −1; RSS-era evening-Pacific UTC timestamps → ISOs +1);
the current generator is already UTC-correct, so this was data repair, not a
code fix. Repaired both fields from CSV pubDate — with one deliberate
refinement caught mid-implementation: first pass would have normalized 126
correct-date real timestamps to midnight, discarding genuine intra-day
ordering for old same-day posts; restored and re-ran touching only
wrong-date ISOs (exactly 148 + 153 fixed, verified 0 mismatches after).
**Ordering fixes itself**: `sortByPubDate` sorts on `publishedAtISO`, so the
148 wrong ISOs were actively mis-ordering posts by a day. Deploy `success`.

**Bug 1 (phone image upload) — definite latent bug found and fixed
(website `ef08813`), exact instance pending PM's answer.** `upload.ts` still
used the build-time `loadCalendar()` snapshot — the same stale-calendar class
fixed in compose.ts on 09-02 (website#38), with this sibling endpoint
**missed in that pass** (my own miss, owned in the commit). Uploads to any
draft newer than the last deploy 404'd — matching "used to work" precisely.
Fixed with the same proven `loadCalendarLive()` pattern. Two other rejection
branches could also produce PM's error depending on the file (HEIC → 400,
>3.3MB → 413, each with distinct wording) — asked PM which error text they
saw rather than guess. Honest verification limit named: endpoint is
admin-auth'd, no credentials on this seat, so verification is build + the
pattern's prior production proof.
