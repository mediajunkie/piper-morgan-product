# Session Log: 2026-09-12-0720-docs-code

**Role**: Documentation Management
**Model**: Claude Code (Sonnet 5)
**Date**: Saturday, September 12, 2026
**Start Time**: 7:20 AM PDT

## Session Objectives

1. Standard duty-cycle floor: mail loop, standing items, day-of-week triggers as time allows.
2. Watch for "Piper Morgan Eras" (today's scheduled narrative post per the editorial calendar,
   teased in yesterday's footer) in case a publish-support ask comes through.

## Work Log

### 7:20 AM - Session Start (06:57 cron fire)

- Created session log (first fire of the day, no log existed yet for 09-12).
- Synced product worktree (60 behind → 0) and website worktree (5 behind → 0).
- Cron `433c7e09` confirmed single healthy job via `CronList`.
- Yesterday's day-close verified clean (`<!-- DAY-CLOSED: 2026-09-11 -->` present).
- Ran START heartbeat (writes unconditionally), confirmed pushed. Merge-keeper clean, omnibus
  currency correct (09-11, yesterday). Inbox empty. "Piper Morgan Eras" (today's scheduled insight
  post) still `drafted` on the calendar — watching, not chasing, no PM/Comms handoff yet. No
  day-of-week trigger today (Saturday). Nothing else unblocked. Returning to idle for the 09:57
  fire.

### 9:07 AM - PM-engaged — publish "Piper Morgan Eras," with a real self-caught mistake

- PM: today's post is ready for proofread + publish. Comms' PUBLISH-READY memo was already in
  inbox (two review passes, 6 real fixes, all 16 template-audit checks clean, era descriptions
  claimed verified against `episodes.ts`).
- Did an independent proofread rather than trust Comms' account: mechanical checks clean (0
  semicolons; the several "someone/anyone/somebody" instances in the closing paragraph are
  legitimate — a deliberately general aphorism for readers, not agents obscured), viewed the image
  directly (matches alt/caption exactly — museum-gallery scene, curator filing a labeled machine),
  footer teaser verified against the live calendar ("Who's Who at Piper Morgan," 9/13 — matches),
  and independently re-verified all 7 era descriptions **and their date ranges** against the live
  `episodes.ts` source myself rather than trust Comms' claim of having done so — all matched
  verbatim, plus cross-checked the "nearly 390 posts" claim against the live website CSV (391 rows,
  accurate).
- **Found what I believed was a real typo Comms missed**: the draft read "minimum-**valuable**-
  product," while `episodes.ts` uses bare "MVP" and Comms' own memo claimed to have verified a
  "minimum-**viable**-product" spellout. "Fixed" it to "viable," dry-ran clean, published for real
  (hashId `0547bee91100`).
- **This was wrong, and I found it myself before reporting done.** While updating the calendar,
  checked the file's git history as a matter of routine and found commit `d8124d3d5`
  ("PM's terminology: 'minimum valuable product' not viable") landed at 08:56 — **9 minutes before
  my publish, and after my session's initial sync.** PM had deliberately set "valuable" via the
  admin UI; my working copy still had my own "correction" layered on top of a stale read. Also
  found "minimum-valuable-product" already used in an older, unrelated Ship post's own content
  (grepped `blog-content.json` directly) — confirming this is an established, deliberate term
  across the blog, not a one-off choice I could reasonably have overridden.
- **Same failure shape as this whole week's "stale snapshot" incidents** (Docs BELT-INVISIBLE,
  the NO-SESSION-LOG race): synced once at session start, then acted on that snapshot without
  re-checking before finalizing, even though meaningful time and at least one relevant upstream
  edit had passed in between.
- **Fixed immediately, transparently**: reverted the draft/archived file to "valuable"; located and
  corrected the one already-live occurrence directly in the website's `blog-content.json` (verified
  via `git diff` that the fix was a clean, minimal 2-line change — no reformatting bloat, no other
  content touched); corrected the calendar's own notes to record what actually happened rather than
  let my first, wrong account stand; committed with an explicit, honest commit message naming the
  mistake rather than folding it silently into "publish complete." Live-verified the correction
  itself (not just the original publish) via actual rendered content before considering this done.
- Archived draft + image to `drafts/published/`, `draftPath` updated in the same pass, validator
  warning count dropped 16→15 confirming the fix landed.

### 9:25 AM - PM: this belongs in the blog's style guide, which should exist

- PM confirmed "valuable" is deliberate and asked for it to be captured in the blog's style
  guide — noting no such document currently exists. Checked first rather than assume: no
  dedicated blog-terminology style guide exists (`docs/guides/piper-style-guide.md` is a
  completely different document — Piper-the-product's own conversational persona, not blog
  editorial style). The fact itself was already in `knowledge/piper-morgan-glossary-v1.1.md`
  ("MVP: Minimum Valuable Product (our gloss; not 'Viable')"), added after a pre-06-10 Comms
  incident of the identical shape — meaning this is the **second** time this exact mistake has
  happened, and the gap isn't that the fact is unrecorded, it's that nothing prompts checking it
  at the moment someone's about to "fix" it.
- Created `docs/internal/planning/comms/blog-style-guide.md` — a home for house-style terminology
  decisions specific to blog content (distinct from voice/tone, structure, and the project-wide
  glossary), seeded with the MVP entry and both known incidents named plainly. Cross-linked into
  the two actual discovery points: `blog-post-template.md`'s Required Reading (Comms' draft-time
  side) and my own `publish-to-blog` SKILL.md's canonical references (my proofread-time side,
  where today's mistake actually happened — carries the fuller incident note and the generalized
  lesson: re-sync immediately before any wording edit, not just at session start).
- Notified Comms directly (mail) since the new doc lands in their required reading.

### 10:20 AM - Duty-cycle fire (09:57) — the SAME gap recurred a third time, fixed at the mechanism this time

- Synced both worktrees (product 39→0, website 1→0). Cron confirmed single healthy job. Heartbeat
  self-suppressed correctly.
- 2 items: Comms' original PUBLISH-READY (already acted on directly during the PM-engaged
  session, triaged now) and Dispatch-PM's syndication confirmation for "Piper Morgan Eras."
- **Both syndication legs live** (Medium + LinkedIn, `insight` theme obligates both). Dispatch-PM
  explicitly confirmed the timing risk on my earlier word-choice fix worked out (both platforms
  read the content after `39c0fc2`, not before). Also flagged two real issues:
  1. **altText/caption/cartoon empty on the calendar row — the THIRD time in five days** on
     effectively the same post (09-08, 09-11, 09-12). Verified Dispatch-PM's supplied values
     byte-for-byte against the website's own `blog-metadata.csv` before using them (correct this
     time). Backfilled + set status→distributed, canonicalSite→distributed.
  2. **`status` read `drafted` on a live post** — checked the actual commit timeline rather than
     assume either side was right: my calendar-status commit landed 09:11:15, ~2 minutes after
     the content-fix commit Dispatch-PM cited (09:09:34) — they almost certainly read state in
     that window. Confirmed current state is correctly `distributed`; not a persistent bug, but a
     real timing gap between publish and calendar-status-update worth naming honestly rather than
     just asserting "already fixed."
- **This is the third occurrence of the same gap, and I'd already told myself I'd fix it after
  the second** (09-11 carry-forward: "a real gap in my own publish checklist... will fix going
  forward") — and then made the identical mistake again today. Prose telling myself to remember
  isn't working, so fixed it at the mechanism instead: added a warning to
  `scripts/validate-editorial-calendar.py` for `altText` empty on a published/distributed non-Ship
  row. **Measured before shipping, not assumed**: the obvious version (also checking
  caption/cartoon) would have fired **337 times** — caption is empty on 282/385 published rows
  (73%, longstanding convention) and cartoon on 109 (28%), both would have buried the one real
  signal in noise. Narrowed to altText alone (21/385, 5.5%). First test run caught a real bug in
  my own new check (8 historical Ships leaking through because they use the legacy
  `theme='shipping news'` value, not current `'ship'`) — fixed using the file's own existing
  `LEGACY_THEMES` set rather than inventing a new exclusion. Verified against all three of this
  file's own established controls: known-positive (fires on real gaps), known-negative (silent on
  the just-fixed Eras/Mailbox-Trust-Violation rows), full-sweep (28 total warnings, no flood).
- Replied to Dispatch-PM via the correct relay-via-Exec protocol (`to: dispatch-pm, cc: exec`,
  delivered to `mailboxes/exec/inbox/`) — got this right on the first try this time, unlike
  yesterday's dead-letter-directory mistake. Addressed both findings plainly, including the
  timing-race explanation for `status=drafted` and naming the mechanical fix rather than just
  promising better discipline again.
- Triaged both items + MANIFEST regen (known false-positive, verified empty diff). Merge-keeper
  clean, omnibus currency correct. Fully drained.
