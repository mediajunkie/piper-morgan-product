# Session Log — 2026-09-12 11:40 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead Dev's worktree
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead
**Branch**: claude/lead-cycle
**Model**: Fable 5 (claude-fable-5)
**Task**: GitHub #1693 — todo task-text extraction misses 'new todo - X' and hyphenated 'to-do:' forms. Last epic-4 item (corpus/classifier cluster).

## Constraints
- Argument-extraction-by-regex is gate-side, corpus-deposit BY DEFAULT (PM-ratified 2026-08-29)
- TestExtractionPatternRatchet ceiling: 567, must not grow
- Precedent for legitimate pattern repair: #1527 (95839e7aa) — narrowing/generalization of existing element, ratchet-neutral
- Don't regress #1654 (c035d7a5e task/time clarify turns) or #1696 (delete flows)
- Commit on claude/lead-cycle referencing 1693 (no closing keyword), do NOT push

## Work

- 11:40 — Session start. Read #1693 via `gh api` (gh issue view returned empty — noted). Three verbatims, all route correctly to create-todo handler, die in task-text extraction:
  1. `new todo - water the plants` (dash separator)
  2. `one more to-do - water the plants` (deliberately unfair, "fine to miss, recorded for corpus")
  3. `add to-do: water the plants` (teach-copy's suggested form + hyphenated 'to-do' — the one that matters most)
- 11:42 — **Supersession gate fired**: #1693 is ALREADY FIXED. Commit c3c4742f8 ("fix(todo): 1693 — task-text extraction accepts dash separators and the to-do/to do token", 2026-08-29 15:45 PDT, same day as the live round) landed the extraction-only repair and is on origin/main. It rewrote `_extract_todo_text` around two named elements — `_TODO_TOKEN = r"to[-\s]?do"` and `_TODO_SEP = r"(?:\s*[:\-–—]\s*|\s+)"` (todo_handlers.py:1019/1023) — plus new/another/one-more lead-ins, and shipped 17 pins in `tests/unit/services/intent_service/test_todo_text_extraction_1693.py`. The issue stayed open as verification-queue ("PM will re-hit it next round"). So this session's work = verify TODAY through the real path, confirm no regression from the later #1654 (c035d7a5e) / #1696 changes, and produce the close-or-keep evidence.

## Per-verbatim diagnosis (reproduced TODAY, 2026-09-12, real code path)

Probe: `PreClassifier.pre_classify()` + `TodoIntentHandlers._extract_todo_text()` on today's HEAD (9d50124a2), venv python, ANTHROPIC_* stripped.

| Verbatim | Pre-classifier | Extraction today | Marker leak? |
|---|---|---|---|
| `new todo - water the plants` | None (falls to LLM classifier → action rail, matching the live round's correct routing) | `'water the plants'` ✓ | none |
| `one more to-do - water the plants` | None (same) | `'water the plants'` ✓ | none |
| `add to-do: water the plants` | None (same) | `'water the plants'` ✓ | none |
| `add todo buy oat milk` (live-passing control) | None | `'buy oat milk'` ✓ | none |
| `add a todo: check the flayrod` (live-passing control) | None | `'check the flayrod'` ✓ | none |

Where each form died AT FILING (8/29, pre-fix): claimed correctly by routing, extraction returned `""` → the handler's own didn't-catch ask. Fixed at the extraction layer by c3c4742f8. Today: all three extract the exact task text, no separator characters leaking.

## Layer justification (why pattern repair, not corpus rows, was and remains right)

- Routing NEVER failed on these phrasings — zero misroutes in 5 live draws. Corpus deposit is the default for gate-side ROUTING misses; there is no routing miss to deposit.
- The issue's own supersession-gate paragraph sanctions the handler side: "extraction of the task text from the raw message remains handler work. Sanctioned fix side."
- Ratchet-wise the fix is absorbed and tight: `TestExtractionPatternRatchet` CEILINGS `todo-create: 11` was FROZEN AT MEASURED VALUE 2026-09-01 (post-fix) including `_TODO_TOKEN`/`_TODO_SEP`; `pre-classifier: 567` untouched (no pre-classifier edits in the fix). Tightness test keeps both exact.
- Inversion corpora checked: no rows carry these phrasings, and none should — phase0 is generated (routing categories, denominators frozen), phase2_armed is armed-state pairs only. The verbatims are recorded durably in the issue + the pin file for the eventual slot-emission lane.

## Verification (Verified how)

- **Method**: `scripts/run-sweep.sh unit tests/unit/services/intent_service/test_todo_text_extraction_1693.py` → **17 passed** (includes TestReminderBoundary1693 — #1654 carriers unaffected both ways). Full `scripts/run-sweep.sh unit tests/unit/services/intent_service` → **4017 passed, 3 deselected** (covers #1654 c035d7a5e and #1696 delete-flow neighborhoods). `scripts/run-sweep.sh ratchets` → **54 passed**; targeted `pytest -k ExtractionPatternRatchet -v` → 3/3 passed (`test_no_new_extraction_patterns`, `test_extraction_ratchet_stays_tight`, `test_named_spans_still_exist`) — pre-classifier ceiling **567**, unchanged. Ruff **0.6.9** (scratchpad ruffenv2): check clean + format clean on todo_handlers.py + the pin file. Live repro of all 5 phrasings through PreClassifier + _extract_todo_text quoted above.
- **Layer**: unit/extraction + pre-classifier probe — NOT a live server round; PM's next live re-hit is the user-layer confirmation.
- **Denominator**: all 3 failing verbatims + both live-passing controls; whole intent_service unit dir (4017); both ratchet mechanisms; 2 touched-file ruff scope.

## Discovered work

- **Filed #1758** — `_extract_priority` (todo_handlers.py:1055) matches 'high'/'low'/'urgent' as bare substrings: verified live that `add todo: highlight the report` creates a HIGH-priority todo. Wrong metadata, never wrong action. Also noted in the issue: `_extract_priority` sits OUTSIDE the ratchet's todo-create span list.
- Tooling anomaly (not filed): `gh issue view 1693` returned empty output with exit 0 twice; `gh api` worked. Environmental, non-blocking.

## Recommendation

**CLOSE #1693** with this evidence — fix landed 8/29 (c3c4742f8), 17 pins green, all three verbatims extract correctly today on HEAD, ratchet tight at 567/11, no regression across the #1654/#1696 seams. Residual: PM's live re-hit next round confirms at the user layer (my verification is unit + extraction-path, not a live server round).

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md extraction-ratchet corollary (corpus-deposit default — framed the layer decision); `feedback_supersession_gate_before_fixing` (checked for an existing fix BEFORE writing one — it existed); CLAUDE.md discovered-work discipline (filed #1758 immediately); run-sweep.sh header doc (canonical test invocations); ANTHROPIC_* env-strip recipe (used on every python invocation).
- **Loaded but not referenced**: worktree Model A/B sections, mailbox discipline (no mail this session), sign-off push rules (task says do NOT push).
- **Wanted but not found**: a canonical pointer to where the pinned ruffenv2 lives — rediscovered via prior prog logs + scratchpad find.
