# Session Log: 2026-09-08-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Tuesday, September 8, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/07/2026-09-07-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-07** (four quiet fires, then CXO reversed their own "don't go looking" posture to ask for a real FTUX render-check after PM flipped `PIPER_FTUX_INTERVIEW` live; investigated and found no self-serve signup exists and the existing test account isn't cold anymore, replied precisely naming the real blocker to Lead cc CXO/PPM/Exec/PM rather than force a misleading check or sit on it silently; tracked as standing item #4, no urgency stated).

## Session Objectives

1. Normal duty-cycle drain (mail loop → task loop → idle) — no PM-assigned task this fire, cron-triggered.
2. Check for any response from Lead on the FTUX cold-account request.

## Work Log

### 06:52 AM - Session Start

- `CronList`: exactly one job (`a3675916`, `22 6,9,12,15,18,21 * * *`) — no duplicates.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-07 -->` — no self-heal needed.
- Fingerprint check both worktrees: `web` / `claude/web-cycle` — correct, no collision indicators.
- Synced both worktrees clean: product repo, website repo.
- Cohort-freeze check: `rc=0`, INSUFFICIENT-SCHEDULE note (overnight window, 0 scheduled fires in window — not a freeze signal).
- Created this session log.

### 06:52 AM — FTUX thread: one blocker resolved, replied precisely, hit and fixed a real mailbox bug

**Mail loop**: three memos, all on last night's FTUX thread. **Direct**: Exec closed their own
digest-ambiguity flag from Monday night — read the running app directly
(`flyctl ssh console -C "printenv ..."`), confirmed `PIPER_FTUX_INTERVIEW` and
`PIPER_INVERSION_SHADOW` are both genuinely `1`, found a named test
(`test_flag_vocabulary_matches_inversion_shadow`) confirming the shared value is deliberate
design, not a bug — said plainly this was a false alarm they raised without checking the codebase
first. **Two cc's**: CXO's follow-up naming a second hazard (identical values today doesn't mean
intentionally coupled — whoever next changes one may silently affect the other) addressed to Lead;
PPM self-correcting an earlier "the interview is live" claim that outran what they'd actually
checked.

Read all three in full. The flag-value ambiguity is now resolved, but that was only one of the two
blockers I'd named Monday night — the cold-account gap is untouched by any of this. Replied to
Exec cc Lead/CXO/PPM/PM: credited the close, confirmed the remaining blocker precisely rather than
let "unblocked" get inferred from one memo resolving one of two things, restated the ask to Lead.

**Hit and fixed a real mailbox bug while triaging**: a `mv` targeting a mixed-case filename
(`flag-is-on`) silently matched the real file (`flag-is-ON`) on this case-insensitive filesystem —
moved it correctly to `read/`, but under the wrong-case name, since git treats the two casings as
different paths. The original uppercase-named file was never actually deleted from `inbox/` on
`origin/main`, and `mail-send.sh`'s own "left behind" warning caught it (didn't ignore the
warning). Traced it precisely via `git ls-tree origin/main` rather than guess, resent the deletion
using the exact original filename, verified `origin/main`'s inbox tree is genuinely down to just
MANIFEST.md. Noted the gotcha in carry-forward (copy exact filenames from `ls`, don't retype from
memory) so it doesn't repeat. Regenerated own MANIFEST, hit and correctly dismissed the recurring
`inbox/MANIFEST.md` false-positive warning as in prior fires.

Updated carry-forward's FTUX thread entry and standing item #4 to reflect one-of-two-blockers
cleared. **mail: 1 direct, 1 read in full; 2 cc, skimmed.** Genuinely at (0,0) after.

### 09:53 AM — Fire (cron 09:22 slot), genuinely quiet

Both worktrees synced clean, no collision indicators. Cohort-freeze check clean,
exit 0. Mail empty. Standing items unchanged: all four gated on others (no update
from Lead on the FTUX cold account yet). Heartbeat self-suppressed. Returning to
idle.

### 12:52 PM — FTUX render-check: account arrived, ran it, captured a real finding

**Mail loop: one direct memo** — Lead's cold account (`web-ftux-cold`) was ready,
dev server restarted with the flag confirmed in its process env. Read in full.

**Verified before acting rather than trust the memo alone**: `ps eww $(lsof -ti:8001)`
confirmed `PIPER_FTUX_INTERVIEW=1` still present, same PID (30735) and start time as
Lead's report — no restart in between, safe to proceed on Lead's stated state.

**Tooling**: `npx playwright` needed a browser version bump (cache had 1234, this CLI
wanted 1243) — `npx playwright install chromium` succeeded cleanly this time (unlike
yesterday's blocked attempt). A scratch `npm install playwright` also succeeded
without friction, letting the actual check script resolve its `require('playwright')`.

**Ran the real check**: logged in via genuine Playwright browser interaction (not an
API shortcut) as `web-ftux-cold`. Landing page after login: empty chat, no
auto-greeting — the interview fires on the first user turn, not on page load. Sent a
real first message ("Hi"), captured the reply verbatim: leads with CXO's exact
two-line interview copy, asks the question, matches spec precisely.

**Found and traced a real, unasked-for detail rather than just report success**: a
third paragraph appeared in the same reply, not mentioned in any of last night's
memos. Traced it via `grep` to `services/configuration/personalization_service.py`
(`FIRST_RESPONSE_PERSONALIZATION_NOTICE`) — its own comment identifies it as CXO's
own prior ADR-075 OQ-3 direction, previously hardened once (#1604), and NOT the
`why_asking` string PPM's ruling cut. Reported this precisely as a fact for CXO to
rule on (does this combination read right, next to the new interview copy) rather
than judge it myself either way.

Sent the full report to CXO cc Lead/Exec/PPM/PM (`mail-send.sh`, `aafbe854a`) —
verbatim exchange, both questions answered directly, the layer named (local dev
server, not live production), screenshot paths for reference. Triaged Lead's memo
using its exact filename this time (yesterday's case-mismatch lesson applied),
MANIFEST regen (`812f43d04`) — same recurring `inbox/MANIFEST.md` false positive,
verified zero diff before trusting the send.

Closed standing item #4 and the carry-forward thread entry — Web's part of this is
done; CXO's design call on the third paragraph is theirs, not something to hold open
on Web's tracker. Cleaned up the one-off check script and scratch npm dir (report
already has the full verbatim record); left the two screenshots in `/tmp` since I
referenced them as available evidence.

**mail: 1 direct, 1 read in full; 0 cc.** Genuinely at (0,0) after.

### 15:52 PM — CXO confirmed the finding, asked for reachability, found a real stale exposure

**Mail loop**: two memos. **Direct**: CXO confirmed both render answers (yes/yes), validated the
third-paragraph finding as genuine promise-language leakage the phrase-pin doesn't catch by
construction (it pins specific cut phrases, not the promise class), credited naming the layer as
what made the finding usable, self-corrected a near-miss (was about to report "no real surface
exists" before widening the search and finding the route IS mounted), and explicitly asked
"whoever picks this up" to establish reachability of the preferences surface in the hosted beta.
**Cc**: PPM closed their own long-running watch item on today's render-check, independently
confirming the same result.

**Picked up the reachability question directly** rather than wait to be asked by name — squarely
browser-lane work, cheaply checkable (unauthenticated HTTP status, no account needed), and CXO's
own memo said "not filing an issue yet, I'd rather establish reachability first." Found the actual
hosted-beta domain (`piper-morgan.fly.dev`) via `docs/internal/operations/environment-status.md`
rather than guess. Checked both the real linked route (`/personality-preferences`, `401` both
locally and on the hosted beta — correctly gated) and the specific static file CXO named
(`/assets/personality-preferences.html`, `200` both places — publicly reachable).

**Went further than a yes/no answer**: confirmed the public one is genuinely the same page's
content, then found its own JS hardcodes `fetch(".../profile/default")` — a shared placeholder id,
not the real session's user — rather than assume it was a harmless duplicate. Traced its git
history: untouched since 2026-02-05, predating the real gated route entirely. The backend API is
still 401-gated even for this static shell's own calls, so this isn't a live anonymous data leak —
but a publicly-loadable authenticated-feature shell sitting in the public `/assets` mount is a real
finding, not a false alarm.

**Filed #1733 immediately rather than let it sit in mail** (Discovered Work Discipline) — full
evidence: both status checks on both environments, the hardcoded-default finding, the file's
history, and a suggested next step (remove or move the static copy) without deciding it myself.
GitHub API hit two transient TLS-handshake timeouts on the first two attempts — checked
connectivity directly (`gh api user`) rather than assume a real outage, waited briefly, third
attempt succeeded cleanly.

Replied to CXO cc Lead/PPM/Exec/Arch/PM with the precise reachability answer and the issue link;
left CXO's two design-call flags (passive-learning phrasing, pointing the notice at the real
surface) explicitly open and theirs, not folded into the bug filing. Sent via `mail-send.sh`
(`73361a6e7`), triaged both memos using exact filenames from `ls` (yesterday's case-mismatch lesson
held), MANIFEST regen (`df0d36917`) — same recurring `inbox/MANIFEST.md` false positive, verified
zero diff. Updated carry-forward's FTUX thread entry with the full follow-up.

**mail: 1 direct, 1 read in full; 1 cc, skimmed.** Genuinely at (0,0) after.

**Discovered issues filed this fire**: #1733 (stale unauthenticated personality-preferences
duplicate, hardcoded default user_id).

### 18:52 PM — FTUX thread closes: two more real defects, final copy call made

Both worktrees synced clean, cohort-freeze check clean. Mail: **two cc's**, both closing out the
day-long thread, neither addressed to Web directly. Lead dug into the mechanism behind #1733's
finding and filed two more real defects: **#1734** (SECURITY — the preferences page's save path
silently rewrites the instance-wide config overlay rather than a per-user one) and **#1735** (the
"learning" mechanism the cut promise referenced and the actual tuning code never touch — zero live
callers, confirmed independently by both Lead and PPM). CXO owned a specific, named self-correction
error in the open — their "the inverse is true" reasoning yesterday widened correctly but checked
the wrong nearby artifact (a tone-preferences page, not a role/priorities one). Final copy call:
cut the promise clause entirely rather than soften it, keep only a checkable capability claim, no
pointer to the tone page. PPM confirmed the cut matches their own 09-03 no-promise-language ruling
exactly. CXO explicitly credited the reachability check from this morning as "the right instinct."

Neither memo asked anything of Web — skimmed both, updated carry-forward with the thread's final
resolution so the full arc (render-check → reachability → #1733 → #1734/#1735 → copy fix) is
recorded in one place rather than scattered across separate entries. Committed the carry-forward
update separately from the mailbox triage per discipline (`0407379f7`). Triaged both memos using
exact filenames, MANIFEST regen — same recurring `inbox/MANIFEST.md` false positive, verified zero
diff. Standing items unchanged, back to the original three PM-gated items.

**mail: 0 direct, 0 read; 2 cc, skimmed.** Genuinely at (0,0) after.

### 21:52 PM — STOP (day-close)

Last scheduled fire of today (21:22 slot; next is 06:22 tomorrow). Both worktrees
synced clean, no collision indicators, cohort-freeze check clean. Final mail sweep:
one cc'd cohort-governance memo (CXO to Arch, flywheel-v3 challenge round) — nothing
addressed to Web, skimmed and triaged. Distributed cleanup dry-run: no stale
cycle-log files (>7d) or `.tmp` files (>1d) in `dev/active/` — nothing to remove.
Cleaned up the two FTUX screenshots from this morning now that thread is fully
closed.

**Day arc**: The FTUX render-check thread from yesterday consumed most of today and
was genuinely productive. START: Exec closed the flag-value ambiguity from Monday
night, owning their own false alarm plainly. 12:52: Lead's cold account arrived —
ran the real check via genuine browser login, confirmed CXO's interview copy
renders exactly as specified, and found an unasked-for third paragraph, traced to
its exact source rather than guessed, reported precisely for CXO's design call.
15:52: CXO confirmed the finding as real (the phrase-pin protects specific strings,
not the promise *class*), asked whoever picks it up to check reachability — picked
that up directly, found a genuinely separate stale artifact (hardcoded default
user, untouched since February) and filed #1733 immediately. 18:52: the thread
closed — Lead's deeper dig found two more real defects (#1734 security, #1735
disconnected mechanism), CXO owned a specific self-correction error in the open,
PPM confirmed the final copy call matches their own ruling.

Four real GitHub issues touched this thread (#1733 filed by Web; #1734/#1735 filed
by Lead), one design decision made (cut the promise clause, no pointer to the wrong
surface), zero half-evidenced claims anywhere in the chain — every participant
(Exec, PPM, CXO, Web) corrected their own prior claim at least once rather than let
an overclaim stand. All three original standing items (obs-pass, site walkthrough,
Buttondown) remained unchanged throughout — correctly not chased.

**GitHub issues filed today**: #1733 (stale unauthenticated personality-preferences
duplicate).
**GitHub issues referenced (filed by others, same thread)**: #1734, #1735.
**Commits today**: no code changes to either repo — this was verification and
investigation work throughout; product-repo session-log/carry-forward/standing-
items commits only.

## Memory & briefing surfaces referenced this session

**Referenced**:
- "Never guess at facts you can look up" (CLAUDE.md) — the day's clearest
  through-line: verified the flag's live process env before acting on a memo;
  checked the exact hosted-beta URL rather than assume; traced the third
  paragraph's source via `grep` rather than speculate; checked its git history
  before judging severity.
- Discovered Work Discipline — filed #1733 immediately on finding it rather than
  let it ride in mail, even mid-investigation.
- m-43 "name the layer" — explicit in every report this thread (local dev server
  vs. hosted beta vs. production; HTTP-status reachability vs. authenticated
  data-exposure).
- Established mail-send.sh MANIFEST-heuristic false-positive pattern — recognized
  and verified directly (not assumed) five times today, all five genuinely false
  positives.
- Yesterday's filename-case lesson (carry-forward Notes) — copied exact filenames
  from `ls` output before every triage move today; zero repeats of that bug.

**Loaded but not referenced**: `duty-cycle-tick` skill (surfaced explicitly in
context once, already the active procedure); no artifact/design skills needed
today.

**Wanted but not found**: nothing — the day's investigations all resolved with
tools already available (curl, grep, git log, Playwright once the browser version
was fixed).

## Sign-off checklist

- `git status` (product repo): clean, up to date with `origin/main`.
- `git status` (website repo): clean, up to date with `origin/main`.
- `origin/main` resolves in both repos: confirmed via `git rev-parse --verify -q origin/main`.
- `git log --oneline origin/main..HEAD`: empty in both repos — everything today is
  reachable from `origin/main`.
- Distributed cleanup: dry-run found nothing stale to remove.
- Cron re-arm: pending (next step, delete-then-create).

<!-- DAY-CLOSED: 2026-09-08 -->

