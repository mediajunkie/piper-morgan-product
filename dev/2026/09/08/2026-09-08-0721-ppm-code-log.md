# PPM session log — 2026-09-08

**Role**: Principal Product Manager (PPM) · slug `ppm-code` · **Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` · job `528c0261` (re-armed 09-07 22:22 STOP; unchanged today)

### 07:21 START — a busy overnight, sequencing deliverable sent

**Heartbeat + sync**: clean, one cron job (`528c0261`), 0 ahead/0 behind. Prior day's log
(2026-09-07) confirmed carrying its `DAY-CLOSED` sentinel.

**`sprint-truth.py` at wake**: `MVP: 47 not done (28 Sprint Backlog, 3 In Progress, 16 In Review,
1 Product Backlog); 1119 done`, 2 unmilestoned (later 0 after triage below, then 2 again after
today's PM round added `#1729`/`#1730`).

**#1688 digest alarm resolved**: Exec read the actual boolean live (`printenv
PIPER_FTUX_INTERVIEW` → `1`) — the shared-digest coupling was a benign, deliberate design choice
(both flags share truthy vocabulary). Only remaining blocker is Web's cold test account, Lead's to
provision, no urgency.

**The real work this fire**: PM found overnight that the duty-cycle procedure has no intake from
the sprint board — "no work" and "28 open MVP items" were both true simultaneously, and Lead's
quiet fires were the flywheel working exactly as specified, not a failure. Exec and Arch both
named PPM as owning the real sequencing. Read the finding in full, pulled the actual 28-item list
via lighter queries (the heavy full-board pull hit the same shared GraphQL burst-throttle seen
this week — worked around it with `gh issue list` + batched per-issue Status reads), and sent a
real tiered ordering with a stated principle per tier (not a re-band by title guess): six tiers —
broken-pipeline-first, fresh-evidence-today, conversational-state-structural, corpus-pattern-batch
(flagged to check against Arch's noun-audit before fixing one-by-one), tester-visible-cheap,
large-needs-own-fire. Two items (`#1635`, `#1663`) explicitly flagged as needing a real read
rather than guessed into a tier. Claim convention stated and scoped honestly — first-mutation-wins
is fine with one consumer (Lead), named that it needs a real check-then-claim if PM's fleet idea
multiplies consumers, so that gap doesn't silently recur the way the two-surface work-definition
did. Sent to Exec/CIO/Arch cc Lead/Host/CXO/PM, verified landed.

**Also caught while reading the overnight mail**: `#1201` has sat in my own carry-forward as
"PM-gated" for a while past its actual close — Exec's rollup caught it, I verified (`gh issue view
1201` → CLOSED) and fixed the line. Same "left the queue by action not answer" pattern I'd already
diagnosed in my own file and then repeated three lines later.

**Two new issues triaged from today's PM test round**: `#1729` (doc-summary rendering — literal
`• -` separators instead of a list) and `#1730` (two honesty-structural gaps: decline-asserts-
absence for any unmapped emission; floor can ask a clarify question without arming a carrier).
Both MVP/Sprint Backlog/`Beta Blockers - Hard Gates Only`, matching this week's live-round-bug
precedent. `#1730` isn't decision-ready — routed to CXO directly by mail (needs copy + a
structural call, not just board mechanics).

**Mailbox mechanics note, for future reference**: batching 17 files (new sends + inbox→read moves)
in one `mail-send.sh` call silently no-op'd the moves — a reconcile side-effect from earlier in
the same batch removed my local `read/` copies before the move half was captured, producing a
false "nothing to send" on retry until I split into smaller batches of 4-6 and verified each via
direct `git cat-file -e origin/main:<path>` checks rather than trusting the tool's success message
alone.

Committed and pushed. Nothing further owed this fire — the sequencing reply and #1730 routing are
sent; watching for responses, not chasing.

### 10:21 WORK — #1730 closed out, Q1 answered for the flywheel re-eval

**Heartbeat + sync**: 1 behind, fast-forwarded clean. `sprint-truth.py`: 0 unmilestoned, no drift.

**#1730 Gap 1 resolved**: CXO delivered the copy + scoping I'd routed for — fix is scoped to
`GENERIC_UNWIRED_WRITE_DECLINE` only, per-action declines stay as-is. **Verified against
`services/intent_service/unwired_writes.py` directly** rather than post the disposition on trust
alone — matches exactly, including the `original_message` availability claim. Posted the
disposition on `#1730` itself; it's build-ready now, already Tier 2 in this morning's sequencing.

**Q1 answered for the flywheel re-eval**: Arch (leading the re-eval PM approved yesterday) named
PPM's morning sequencing memo as the foundation for Q1 and asked for an independent take on three
sub-questions, explicitly requesting it be written before reading Arch's own framing. Did that:
role-scoping by capability (build roles pull the generic backlog, governance roles' idle-drain
surface is their own domain — landed on the same rule Arch stated independently, a good
convergence signal); the stalled-claim return path (reuse the board's existing unused `Blocked`
status rather than add new mechanism, matching PM's refactor-not-add constraint); and a firm
position on Product Backlog → Sprint Backlog promotion staying permanently PM's/PPM's act, never
agent-pull's, since it's a scope decision not a work-selection decision. Sent to Arch cc
Exec/CIO/Lead/CXO/PM, verified landed. Also triaged Docs' Q2 read (their own lane, no PPM action).

Committed and pushed. Watching for Arch's synthesis before doing more on the flywheel thread.

### 13:21 WORK — #1688 fully closed, two new triages, one self-corrected

**Heartbeat + sync**: 1 behind, fast-forwarded clean. Unmilestoned drift 0→2.

**#1688 closed for real this time**: Web ran the cold-account render capture — leads with CXO's
opening line verbatim, asks the question verbatim, no promise language. This closes the whole
multi-day thread (HOLD → PM overrule → digest scare → resolved → this observation). CXO found one
adjacent, pre-existing personalization-notice string with its own promise-shaped phrase — correctly
holding it pending reachability of a real preferences surface rather than filing prematurely; not
mine to duplicate-flag. Also read CIO's "both amendments shipped" memo: my morning's eligibility
denominator and claim convention went straight into `duty-cycle-tick` v1.32 without rework — good
confirmation the sequencing memo was actually usable, not just directionally right. Sent one ack
covering both threads to CIO cc the full list, verified landed.

**Two new issues triaged**: `#1732` (chat-render XSS, no sanitizer on `marked.parse()` →
`innerHTML`, found via the #1730 lane) — MVP/Beta Blockers, matches #1578/#1581 exactly. `#1731`
(`mail-send.sh` silently drops paths in a large batch) — **CIO hit this independently the same
morning I hit the identical shape myself** in this morning's 17-path batch; added my own account
as corroborating evidence on the issue. **Caught and corrected my own first instinct** on #1731's
milestone — reached for MVP before checking precedent, then recognized this is cohort tooling
(mail-send.sh), not a product surface, and re-set it to Ongoing/FLYWHEEL matching #1691/#1719.

Committed and pushed.

### 16:21 WORK — third-line thread resolves, three more issues triaged

**Heartbeat + sync**: clean, one cron job, 0 ahead/0 behind. Unmilestoned drift 0→3.

**The adjacent finding I'd flagged as "not mine to rule on" escalated fast and closed clean.**
Lead verified the mechanism: the promise ("I'll tune to your role and priorities") names a write
path (`PersonalizationContext.upsert`) with zero real-content callers — **verified this myself
against `personalization_repository.py`** before treating it as settled; the one caller
(`get_or_seed_default`) only ever seeds default content. CXO's copy call: cut the clause entirely,
don't soften it into a hedge — the exact shape of my own 09-03 #1688 scope ruling. Sent explicit
confirmation since Lead named my ruling directly as the frame for the call. Also verified CXO's
own honest self-correction held up: their earlier "maybe the promise IS backed" read (after
finding an adjacent preferences page) was itself wrong — that page edits tone sliders, not
role/priorities, so the original read was right all along.

**Three new issues from this lane, all triaged**: `#1733` (stale unauthenticated duplicate page,
hardcoded `user_id=default`), `#1734` (`[SECURITY]` — personality API ignores its user_id param
and PUT-rewrites the *global* config, so any authed user's save clobbers the whole instance's
overlay), `#1735` (the learning→tuning loop is disconnected at every joint — directly explains the
#1688 mechanism finding). All MVP/Beta Blockers, matching this week's live-hosted-surface
precedent. Board mechanics only; fixes are Lead's/Arch's.

**Also read Host's #1731 data point** (spot-checked their own multi-path mail-send calls today,
all clean — a useful negative data point for CIO's investigation, no PPM action).

Committed and pushed. `sprint-truth.py` at close: 0 unmilestoned. Nothing further owed.

### 19:21 WORK — flywheel v3 challenge round, #1731 case clarified

**Heartbeat + sync**: 1 behind, fast-forwarded clean. 0 unmilestoned, no drift.

**Flywheel v3 synthesis landed**: Arch's full doc, 7 decisions, challenge round open through
09-09 EOD. D6 (mine — Product→Sprint promotion stays a human act) accurately reflected and
adopted verbatim, good confirmation the Q1 answer held up under synthesis. Read the whole document
before responding, not just the summary memo. Sent a challenge-round pass: no substantive
objection to D1-D5/D7 (methodology-corpus governance calls where I don't have independent evidence
to argue either side — said so plainly rather than manufacture a challenge to participate), one
real coordination note on D7 (its milestone-close review trigger and `#1386`'s own beta-gate
checklist both key off milestone close independently — not a conflict, worth cross-referencing).
Deliberately stopped there — the thread kept generating fast-moving addenda (Docs' D5-attribution
correction, CXO's own addendum connecting it to their earlier challenge) squarely in Arch's/CIO's/
CXO's/Docs' domain; triaged each for context, didn't chase further.

**#1731 (mail-send.sh silent-drop) got more precise**: CIO retracted their own finding — root
cause was their own zsh unquoted-command-substitution splitting, not the script. CIO asked
directly whether my corroborating report this morning shared the same cause. **Checked rather than
assume either way**: confirmed I'm also on zsh, but reproduced my actual array-construction
pattern (`FILES=()` / `FILES+=(...)` / `"${FILES[@]}"`) live and it split correctly — 6 in, 6 out.
My symptom was also a different shape (a verified false no-op via direct `git cat-file -e` tree
inspection, not truncated argument count). Recommended against closing #1731 on CIO's retraction
alone since it doesn't explain my case. Watching for CIO's call on how to disposition it.

Committed and pushed.

### 22:22 STOP — quiet close, flywheel thread continues without PPM

**Heartbeat + sync**: clean, one cron job, 0 ahead/0 behind. 0 unmilestoned, no drift.

Three more flywheel-thread items (Exec's challenge-round pass, Arch's v3.0.1 amendment,
CXO's table-vs-footer verification) — all squarely Arch's/Exec's/CXO's methodology-corpus domain,
none needed PPM action. Triaged and moved to `read/`, consistent with this afternoon's decision to
watch passively rather than chase every fast-moving addendum in a thread outside PPM's lane.

## Day summary

Seven fires (START 07:21, WORK 10:21/13:21/16:21/19:21, this STOP). The day's real arc: PM found
a genuine structural gap overnight (duty-cycle had no intake from the sprint board) and named PPM
to own the sequencing — sent a real tiered ordering with a stated principle per tier, not a
re-band by title guess, and it went straight into `duty-cycle-tick` v1.32 without rework. That
same eligibility/claim-convention work fed directly into the flywheel re-evaluation's Q1, where it
was adopted verbatim in v3's synthesis (D6). Separately, the `#1688` thread closed twice over —
once on the render check itself, once on an adjacent honesty-structural finding (a promise-shaped
line that turned out to be factually false, not just risky) that resolved to exactly the same
shape as PPM's own 09-03 scope ruling. Two genuine self-corrections landed today, both mine: an
overclaim from last night ("the interview is live") caught and corrected before this morning's
fire, and a careful, evidence-based push-back on CIO's #1731 retraction rather than accepting a
plausible-sounding explanation that didn't actually cover my own case. **The throughline**: every
substantive item today was verified against its primary source (code, GitHub state, a live shell
reproduction) before being treated as settled — including when the source under question was my
own prior claim.

`sprint-truth.py` at close: `MVP: 52 not done (34 Sprint Backlog, 3 In Progress, 15 In Review);
1120 done`, 0 unmilestoned. Seven new issues triaged today (`#1729`, `#1730`, `#1731`, `#1732`,
`#1733`, `#1734`, `#1735`), all correctly dispositioned, no collateral damage on any Sprint-field
mutation. `#1386` unchanged
(criterion 6 + 2/3/4/5 fire fresh at MVP close 2026-10-30; criterion 1 alone unqualified) — no
new movement, not actionable yet. `#1688` fully closed on both threads. `#1731`'s disposition is
the one open thread carrying into tomorrow, watching for CIO's call, no urgency. No OWED items.

**DAY-CLOSED**
