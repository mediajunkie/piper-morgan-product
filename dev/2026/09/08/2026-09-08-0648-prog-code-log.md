# Session Log — 2026-09-08 06:48 — Coding Agent (prog) — Claude Code
**Model**: Fable 5 (claude-fable-5)
**Delegated by**: Lead Developer
**Task**: Two live failures from PM's 2026-09-08 ~05:15 v70 test round (Exec memo, read in full).
- Failure A: #1527 false decline — "delete my hydrate reminder" → generic capability-decline despite v67 named-target delete having shipped. Identify emitting site with evidence; classify routing-gap vs fabricated absence; fix at real site if gate-clean.
- Failure B: #1654 merged clarify question — "remind me" → "What would you like me to remind you about, and when?" (ONE question, not the armed two-question chain); bare answer "make coffee" classified as literal request. Identify emitting site; check #1648 carrier contract; fix at real site.

Constraints: extraction ratchet frozen; supersession gate (classifier-prompt patches = corpus deposit); ratchets + seed guard + intent_service suite green; pinned-ruff format+check; commits per failure with BARE issue numbers; NO push.

## Work
- 06:48 — Read Exec memo in full. Worktree synced (0 behind origin/main). Beginning Failure A investigation: grep for decline copy.
- 06:55 — Failure A emitting site CONFIRMED: `GENERIC_UNWIRED_WRITE_DECLINE` (services/intent_service/unwired_writes.py:102), emitted from `_handle_execution_intent` else-branch (services/intent/intent_service.py:8444-8451) via `get_unwired_write_decline`. Live-classifier probe (real LLM, no stub, 3/3 runs): "delete my hydrate reminder" → pre_classify None (v67 portfolio guard declines correctly) → LLM emits EXECUTION/`delete_reminder` conf 0.9-0.95 → not a rail key (rail has delete_todo/remove_todo/cancel_todo only), normalize_action no-op (prefix list is read-verbs only), ActionMapper unmapped → else-branch → generic decline. Classification: ROUTING-GAP at root (emission vocabulary missed by rail registry), presenting as FABRICATED ABSENCE (decline affirmatively claims "still on the way" for a wired capability). Exec's (b) holds. Fix: rail alias registration (delete_reminder/remove_reminder/cancel_reminder → delete_todo_entry) — same pattern as remove_todo/cancel_todo #284 aliases and the create-side add_reminder #1426 census fix. Gate-clean: registry fix, no classifier-prompt patch.
- 07:05 — Failure A FIXED at the real sites. Live raw emission captured verbatim: {"category":"execution","verb":"delete","source_type":"reminder","action":"delete_reminder","confidence":0.95}. Two-leg fix: (1) action_registry `_VERB_SOURCE_TO_ACTION` gains (DELETE, reminder/todo/task) → delete_todo cells (deliberately NO (DELETE,None) — non-todo deletes must not canonicalize); (2) rail + ActionMapper + `_DELETE_TODO_FAMILY` gain delete_reminder/remove_reminder/cancel_reminder aliases (same entry object, same #1666 DESTRUCTIVE ruling — mirrors create-side add_reminder #1426). New pin: tests/unit/services/intent_service/test_reminder_delete_live_emission_1527.py — REAL process_intent → classify_multiple → pre-classifier → JSON parse → verb shim → rail; ONLY llm.complete mocked, returning the captured emission verbatim; explosive for any other LLM consultation; todo boundary explosive-until-armed. 4 tests: title-bound confirm arms; generic decline never; yes deletes bound row; verb-less leg via rail alias. A/B verified: FAILS without fix, passes with. Denominator pin test_destructive_tier_scope_with_denominator updated deliberately (same-entry aliases, not a new destructive capability); DELETE_TODO_ALIASES tuples extended in 1190 + 1666 suites. Gates: both ratchets + seed guard green (6 passed); intent_service suite 3700 passed; pinned ruff 0.6.9 format+check clean. NOT touching classifier prompt — no corpus deposit required (registry fix, not a classifier draw: the emission was reasonable vocabulary the registry failed to recognize).
- 07:15 — Coordinator relayed PM's 07:09-07:10 live probe round (delete-the/remove-my decline identically; complete-my works). Verified against live classifier (2/2 runs each): raw emissions delete_reminder / remove_reminder / complete_reminder — NONE in ActionMapper. The asymmetry mechanism is the verb shim: (COMPLETE, None)→complete_todo existed; DELETE had no cell. Confirms + sharpens the committed fix (shim canonicalizes both delete emissions to delete_todo now — FINAL lines show it). Pinned all three probe phrasings with verbatim emissions in test_reminder_delete_live_emission_1527.py (7 tests total; complete control pins the COMPLETE cell + WRITE-class no-confirm). Amended into the 1527 commit.
- 07:18 — Failure B FIXED. Emitting site: the conversational FLOOR (merged question is not a literal string; CONVERSATION is floor-routed via _should_route_to_floor). Live trace (3/3): bare "remind me" → pre_classify None → LLM emits {"category":"unknown","action":"clarification_needed","confidence":0.6} → _seems_vague conversion → CONVERSATION/clarification_needed → floor → unarmed clarify question (#1648 carrier-contract breach) → "make coffee" orphaned into routing as literal request. Classification: UNARMED-QUESTION SITE = the floor; fix = the floor loses the turn to the arming path. One classifier-prompt example ("remind me" → execution/create_reminder 0.7, with do-not-emit-clarification_needed note) + corpus deposit per 8/29 policy (rows for both failures' phrasings in build_inversion_corpus_phase0.py HAND_ROWS, fixture regenerated). Live verified post-change 3/3: execution/create_reminder 0.7; vague-conversion does not fire. Pin: test_reminder_bare_live_emission_1654.py — real classifier path, one-draw-then-explosive LLM boundary (a second draw IS the orphan), PM's exact two turns: task ask arms carrier; "make coffee" binds + chains to time ask; full recovery saves; prompt-content pin. Gates: ratchets + seed guard + vocab lint green (29 passed with pins); intent_service suite 3704 passed; ruff clean.

## Wrap (07:25)
Both failures fixed, pinned, and committed on claude/lead-cycle (NOT pushed, per brief): 78e325c9e (1527), 75b0f2394 (1654). Final sweep: 3752 passed (intent_service + full architecture enforcement + seed guard). Held as separate findings, not fixed here: (1) the generic unwired-write decline still affirmatively claims absence ("still on the way") for ANY unrecognized emission — Exec's "a decline is a claim" point stands structurally (#1648 territory, needs its own ruling on uncertainty-honest copy); (2) the floor can still ask clarify questions without carriers for OTHER vague turns — the general #1648 carrier contract at the floor is unaddressed by this narrow fix. Discovered-work note: both are extensions of existing tracked issues (#1648), not new untracked work.

## Memory & briefing surfaces referenced this session
- Referenced: CLAUDE.md (env-var stripping for LLM-calling scripts; intent-routing-stack mandate; supersession gate; verify-at-the-right-layer), Exec test-round memo (both transcripts + site-identification warnings), test_delete_todo_named_target_1527.py (harness idiom + the stub gap), 8/29 extraction-ratchet policy text (corpus-deposit default).
- Loaded but not referenced: memory index entries beyond the above.
- Wanted but not found: Fly log retention deep enough to reach PM's 05:14 turns (had to reconstruct the live emission by probing the classifier directly — worked, but server-side evidence of the actual live turn would have been stronger).

---

## Session resumed (09:45) — second prog delegation of the day (Lead): two tasks

Task A: code verdict on issue 1431 (Exec routed — no user action reaches it). Task B: provision Web's genuinely-cold FTUX account.

- 09:48 — Read gh 1431 full comment history (08-11 NOT-closing determination + 08-29 re-verification), Exec routing memo (mailboxes/lead/read/route-exec-to-lead-cc-ppm-pm-1431-needs-a-code-verdict-not-a-user-test-2026-09-08.md), and intent-routing-stack.md in full (mandatory). Worktree 0 behind origin/main.
- 09:52 — **Task A verdict: (a) — routing now reaches the fixed branch; 1431 CLOSED with full evidence.** Live-emission trace (the 1527/1654 method: real pre-classifier + real constrained-router LLM, raw emissions captured, 3 runs/phrasing): the #1595 flip-1 consult seam (intent_service.py:1732) runs BEFORE classify_multiple (:1757), so on an unarmed turn the flipped router claims ahead of surface 1's STATUS steal. With the production flag value (fly secrets 08-29: read_status,read_referent,read_synthesis,create_todo), all 3 phrasings — including PM's exact "show me my archived projects" — drew `{"operation": "list_archived_projects", ..., "confidence": 1.0}` **9/9 runs**, live_match=group (read_status), dispatching `run_archived_projects_query_workflow` = the fixed F20 branch. Deterministic layer unchanged (STATUS steal still present in legacy patterns — 1579/1580 stay open as corpus material; armed turns + flag revert fall back to legacy). No pre-classifier or prompt patch made (none needed — gate honored). Description banner updated FIRST, evidence comment, then closed (close-issue-properly). No corpus deposits (verdict (a)).
- 09:57 — **Task B done: Web's genuinely-cold FTUX account provisioned.** Dev server on 8001 found STALE (started Aug 31 06:38, predating 9067e4382 = Sep 3 interview ship; no PIPER_FTUX_INTERVIEW). Restarted by PORT per gotchas (kill by lsof -ti:8001 → port-empty verified → relaunch env-stripped with PIPER_FTUX_INTERVIEW=1 POSTGRES_PORT=5433 → new PID 30735, start 09:54:34, flag confirmed via ps eww, /health green). Invite token minted (mint_invite_tokens.py --apply), account `web-ftux-cold` / web-cold@test.invalid created via REAL signup (POST /api/v1/setup/create-user, token consumed atomically; user_id 59bcdc4e-c43f-4310-976e-7425d9cb96c5), login verified via /api/v1/auth/login (JWT returned). SEEDED NOTHING — verified cold post-creation: 0 projects / 0 todo_items / 0 user_api_keys / 0 project_integrations (owner-scoped DB counts). Credentials at /Users/xian/.piper-shared/web-ftux-cold-credentials.txt (dir 700, file 600). Memo to Web cc cxo/exec/xian(ceo) + lead/sent mirror sent via mail-send (pushed 949010635): account ready, server flag-verified, check = CXO v0.2 §3a question verbatim, no promise language (why_asking cut per PPM 09-03).
