# Session Log: 2026-09-19-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5) — on Opus since 2026-09-14 (Fable access ceiling; per Exec 09-18, Fable is reserved for Lead Developer, everyone else Opus/Sonnet).
**Date**: Saturday, September 19, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/18/2026-09-18-0630-web-code-log.md` — **DAY-CLOSED: 2026-09-18** (outage recovery, Amber handoff GREEN, website#35 closed with test evidence, two bad checks caught).

## Session Objectives

1. Normal duty-cycle drain (mail → queue → standing items → idle).
2. Queue is **empty** of GitHub issues for the first time — so the real question
   each fire today is whether anything in the standing items has become unblocked.
3. Open, all PM-gated/access-blocked: Vercel Q1, `integration-reveals-all`
   workDate, obs-pass + site walkthrough + Buttondown.

**Context worth carrying**: Amber restart is pending (due 09-18 "today or
tomorrow", hadn't happened as of last night). If it lands, this session restarts
and the cron dies with it — `docs/handoff-web-2026-09-19` would need writing, and
the existing `handoff-web-2026-09-18.md` is dated yesterday. Also: weekends are
PM's prime working time, so direct asks are more likely today than on a weekday.

## Work Log

### 06:52 AM — START

- `CronList`: exactly one job (`580a4989`) — **survived overnight**, and today's
  06:22 slot delivered, so the fresh arm at last night's STOP took. Per the
  standing rule, that delivery — not the `CronList` reading — is the proof.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-18 -->`. Clean close,
  no retroactive self-heal needed.
- Fingerprinted both worktrees **by remote URL** (both are named `web`; basename
  cannot disambiguate). Both clean, both synced.
- **Cohort-freeze `rc=0` but `INSUFFICIENT-SCHEDULE`** — 0 scheduled fires in the
  02:52–06:52 window, so the check *cannot discriminate* and is explicitly not an
  all-clear. Expected overnight; recording it as a non-signal rather than letting
  `rc=0` read as "cohort healthy."
- **Mail**: empty. **Issue queue**: empty.

- **Reboot gate re-checked** rather than assumed: Exec's memo said the matcher
  requires "today's date," which would have made yesterday's
  `handoff-web-2026-09-18.md` read RED this morning. **It doesn't** — ran the gate:
  `web GREEN`, GREEN=21 / RED=4, and all four RED are non-cohort residents (cova,
  piper-open, vergil, zephyr). My concern was unfounded, and checking cost one
  command versus writing a redundant handoff doc.

### 07:0x AM — Site health pass, and a false positive I caught before reporting it

Queue empty, mail empty, standing items all PM-gated — so rather than invent
design work that isn't mine to decide (the obs-pass improvements are PM's call,
not mine), I ran **defect detection**, which is squarely in lane: console errors,
4xx responses, and broken images across 6 key pages in a real browser.

**Result: the site is healthy.** 0 console errors, 0 page errors, 0 responses
≥400 across `/`, `/blog`, `/shipping-news`, `/methodology`, `/about`, `/try/beta`.

⚠️ **But my check first reported "9 broken images on `/blog`" — and that was
false.** The tell was internal contradiction: 9 images with `naturalWidth === 0`
while the same run recorded **zero** responses ≥400. A genuinely missing image
produces a 404; these produced nothing. So the check was reporting a state it
could not distinguish from breakage.

Verified the two directions instead of guessing:
- `curl -sI` on two of the nine → **HTTP 200** each. The files serve fine.
- Then the actual discriminator — scroll the page and re-measure:
  **`before scroll: {total:26, unloaded:9, lazy:24}` → `after scroll: {total:26,
  unloaded:0}`.** All 26 load; 24 are `loading="lazy"`.

`naturalWidth === 0` means "has not decoded yet," and for a lazy image below the
fold that is **correct behavior, not a defect**. Had I reported the first number,
someone would have gone hunting for nine missing files that are all present — and
it would have looked like a real finding on a site whose recent history includes
actual hero-image 404s.

**Fourth instance this week of the same shape**: a check that looks conclusive but
cannot separate two states (the empty jest test, the raw-HTML `<img>` grep, the
raw-HTML `<form>` grep, now this). **The difference today is that I caught it
before publishing rather than after.** What made the difference was noticing the
result contradicted another number in the same output — the internal
inconsistency was the cheapest possible tell, and it was free.

**Note for reuse**: any future image-health check on this site must scroll to the
bottom and wait for network idle *before* counting `naturalWidth === 0`, or it
will report every below-the-fold lazy image as broken. Kept the scripts in
scratch, not committed — not worth repo weight until there's a reason to run it
on a schedule.

At (0,0). Standing items unchanged. Returning to idle.

### 08:24 AM — ARRIVAL (Wave 2, Amber fleet renewal)

**Context cleared deliberately** at ~08:23 by Pard (Wave 2 of the Amber fleet renewal;
certified by Janus, xian overseeing). This is a context clear, **not** a session exit or a
new session — same session log, resumed, per one-log-per-role-per-day.

**Identity**: Unicorn Web Designer (Web), role slug `web`, lane `pipermorgan.ai`.

**Model observed**: Opus 5 (`claude-opus-5`). Honest qualifier — that is what the harness
environment block reports, not something I measured independently; I have no way to verify
the served model from inside the turn. Consistent with the log header and with Exec's 09-18
note that Fable is reserved for Lead Developer.

**Handoff read**: `docs/handoff-web-2026-09-18.md`, in full. It is dated **yesterday**, so I
read the newer surfaces before trusting its open-items list: today's log above (through the
06:52 START), `dev/active/web-carry-forward.md`, `dev/active/web-standing-items.md`, and
`dev/active/delta-web-2026-09-19.md`. **Result: no drift.** The handoff's open-items list is
still accurate — Vercel Q1 and the `integration-reveals-all` workDate remain the only two
named items, both genuinely gated, plus standing items 1–3 (obs-pass, site walkthrough,
Buttondown), all PM-held. The 06:52 START added no new work.

**Sync**: was **44 commits behind** `origin/main` on arrival (cohort mail + peer logs landed
during the clear). Fast-forwarded f94f60015 → f4f1bd8b4 before reading anything, per the
"a worktree you synced earlier is not synced now" rule. Web inbox: **0 unread**.

**One handoff claim verified against primary source** — I picked the two-worktree/two-repo
claim, because it is the one my next hour's commits actually depend on and the handoff names
it "the single easiest mistake to make on this seat." Verified by `git remote -v`, not by
basename:

| path | basename | remote | branch | dirty |
|---|---|---|---|---|
| `~/Development/piper-morgan-worktrees/web` | `web` | `piper-morgan-product` | `claude/web-cycle` | 0 |
| `~/Development/piper-morgan-website-worktrees/web` | `web` | `piper-morgan-website` | `claude/web-cycle` | 0 |

Claim **confirmed exactly as written**, including the trap: both really do return `web` from
`basename`, so the remote URL is the only disambiguator. Both clean, both on the expected
branch.

**Cron state — `CronList` run this turn:**

- **One job, `580a4989`**, `22 6,9,12,15,18,21 * * *`, recurring, session-only. Same id the
  handoff, the carry-forward, and the registry row all name. It **survived the context
  clear** — which fits this seat's documented 09-14 finding that the cron *object* outlives a
  session restart even when pending firings are dropped.
- ⚠️ **Recorded honestly: a `CronList` entry is not evidence that fires are delivered.** That
  is this seat's own hard-won rule (three distinct dark-gap causes, all of which leave
  `CronList` looking healthy). Today's 06:22 slot *did* deliver — the predecessor's START
  above is the proof — but that was before the clear.
- **Next fire: 09:22 PDT today**, ~58 minutes out. That fire, if it arrives, is the actual
  proof of delivery across the clear. If it does not arrive by ~09:30 I will re-arm
  (delete-then-create) rather than trust the listing.
- Auto-expiry still **~2026-09-25** (7-day ceiling from the 09-18 21:24 arm). Not re-arming
  now — re-arming would reset the id and destroy the one clean observation available.
- Registry row is `active`, **not parked**, so the freeze watchdog can still see me. Left as
  is; its stated id matches what `CronList` returns, so it is not stale.

**Scheduled cycle continues as normal.** Nothing else claimed: no work done this arrival
beyond reading, verifying, and recording.

### Cron delivery across the context clear — PROVEN (09:22 fire, reached me 09:52)

The open question from the arrival block is discharged. The 09:22 tick **arrived**, which is the
only thing that proves delivery (a `CronList` entry never has). So the clear behaved like the
09-14 model switch: **session restarts, cron object survives with the same id, and — new
information — fires still deliver across it.** Third observation of the survival half, first
confirmation of the delivery half.

⚠️ Recorded without diagnosis: the fire **arrived ~30 min late** (09:22 slot, first tool call
09:52). One sample, and the scheduler isn't visible from here, so I'm not calling it latency vs.
queueing vs. anything else. Noting it because this seat has two documented dark-gap causes that
present as lateness, and a single data point is where a trend starts — not a level to read.

Cohort-freeze check run despite this being an ordinary WORK fire (the skill says START/WATCH only)
— justified by the fleet being mid-renewal, i.e. exactly the day cohort state is unusual:
`rc=0, watched_roles=11, scheduled_fires=10, emissions=10, emitters=[all 11]`. **Worth noting
against the handoff: after the 09-16 standdown this read `watched_roles=3`. It is now 11** — the
renewal restored the registry rows, so the watchdog's denominator is whole again.

### Closed the criteria-line gap — Web had no third queue source at all

PM's v1.33 ruling defines "drained" as mail **+** standing-items **+** GitHub issues matching the
role's own criteria line. I went looking for Web's and **there isn't one** — `grep` across
`web-carry-forward.md`, `web-standing-items.md`, and `BRIEFING-ESSENTIAL-WEB.md` returned nothing.

The skill says an absent criteria line is "a gap to name in your fire entry, not a blocker." True,
but naming it and stopping would be the deferral antipattern with a procedural excuse — it costs
one edit to fix. **Written into the carry-forward now**, with the denominator reasoning recorded so
it isn't re-derived every fire:

- `piper-morgan-website`, all open issues, no label filter — Web owns that repo outright.
- `piper-morgan-product`, `label:web` — Web is one lane among many there.
- **Excluded on purpose**: product issues Web merely *filed*. #1697 is mine by authorship but
  assigned to PM under milestone MVP (backend data-contract gap) — pulling it back in would
  manufacture work out of a filing credit.

Reading today: website **0** open, product `label:web` **0**. A measured zero with its denominator,
not a bare "nothing there."

### `integration-reveals-all` workDate — it was never blank, it was WRONG (09:52 fire)

The item I inherited as *"no dateline, no chatDate, nothing derivable — PM recall only."* I
re-verified it per the START refresh rule rather than re-carrying the row, and the premise is off
in a way that explains why it sat so long.

**`workDate` is `2025-06-27` — populated, and equal to `pubDate`.** Untouched since the archive
import (`58da3dd`). So it was never derived; the import mirrored pubDate into it. **A populated
wrong value is invisible to every find-the-blanks sweep**, which is the actual reason this
outlived several passes.

The control check is what made it conclusive rather than suggestive — full corpus, not a sample:

| check | result |
|---|---|
| rows with `workDate == pubDate` | **1 of 396** (this one) |
| rows with `workDate != pubDate` | 395 |
| rows with `workDate` empty | 0 |
| **`workDate` strictly before `pubDate`** | **395 of 395**, min lag 2d, median 22d |

A same-day workDate is a value this corpus *never* produces. I also checked `chatDate`, which the
original framing cited as evidence of underivability — **empty on 277 of 396 rows**, so its absence
here is ordinary and carries no signal. That's the handoff's own lesson applied to the handoff:
the cited evidence didn't discriminate.

**Live-visible too, and I verified it in a browser instead of asserting it.** `BlogPostCard.tsx:126`
renders the labeled `Work: … • Published: …` pair only when the dates differ, else a bare date
(line 135). On `pipermorgan.ai/blog?page=16`: ten controls each render two `<time>` elements; this
post renders **one, unlabeled**. The blog's oldest post is the only card of 396 missing the
treatment — and it's downstream of the data, so fixing the date fixes the render.

⚠️ **I nearly published a false version of this.** My first pass tested for the string
`Published:`, which exists only in the labeled branch — so the fallback's bare date read as *no
date at all*, and I was one step from reporting "this post renders with no date." **The control row
caught it**: the same test said `pub=NO` for a card I could see had a date. Fifth instance this week
of a check that can't separate two states, second one caught pre-publication. The habit that keeps
working is the cheap one — always measure a known-good alongside the suspect.

**Sent PM a single yes/no** (`~week of 2025-05-26, likely 05-27?`), bounded from evidence: the
corpus's earliest workDate is 2025-05-27, and the next-published posts carry 05-28/05-29. Included
the weak link explicitly — publication order is only **74%** workDate-monotonic, so "published
first ⇒ worked first" is a tendency, not proof, which is why this is a question and not an edit.
Also proposed the fallback: if PM doesn't recall, **blank the field rather than keep a wrong one** —
flagged as PM's call because it's content truthfulness, not code. Memo `6df611165`.

**Deliberately not claimed**: the sort-order consequence. `blog-utils.ts:19` sorts on `workDateISO`,
which *would* misplace the post — but the position I observed matches pubDate ordering, so I never
established which sorter that view uses. Unverified and labeled as such.

**Incidental, not filed**: `playwright` isn't resolvable from either worktree — it lives in the npx
cache (`~/.npm/_npx/6bcb61ec6d5aea22/node_modules`) and needs `NODE_PATH` set per invocation. That's
the concrete form of the "configuration is still ad-hoc" thread the carry-forward has carried since
08-29. Cost three failed invocations today. Not filing an issue for it yet — it's a papercut with a
one-line workaround, and PM has a real decision queued ahead of it.

### CIO's belt rows empty 4 days — re-checked, real, routed (09:59)

`duty-cycle-freeze-check.sh` (read in full first: `rows=11`, real denominator; no match for `web`,
so my own belt is clean) flagged **`BELT-INVISIBLE cio`**. Applied PM's v1.33 re-check rule before
treating it as real — and my arrival prompt's specific caution that wave-2 arrivals pushing in the
same seconds can manufacture false FAILUREs.

**It survived re-check at trunk.** CIO is emphatically alive (arrival 08:29, cron `f308bd35` armed,
registry unparked, a ruling shipped, 8+ commits today) — and has **0 heartbeat rows on `origin/main`
for 09-16, 09-17, 09-18, and 09-19.** Four days, not a race.

The discriminator that makes it a lapse rather than correct suppression: **`last invoked 89h ago`.**
Refinement (a) suppression still *updates the last-invoked marker* — mine did today in this same
fire ("row suppressed…, last-invoked marker updated"). A suppressed-but-invoked role reads fresh;
89h is the signature of the script not running at all.

Routed to CIO cc PM (`faf95ea7a`), with my own uncertainty stated — I'm reading the script's output,
not its source, so if suppression can leave the marker untouched then I have it backwards and said
so. Noting the structural bit for the record: Step 5b v1.34 exists *because* of this exact lapse on
this exact seat, and its self-check hasn't caught the fourth consecutive day either.

**Tooling papercut found while sending** (not filed, one line): `mail-send.sh`'s #1716 recipient
check **doesn't case-normalize** — a memo with `to: CIO` makes it look for `mailboxes/CIO/inbox/`
and warn that the recipient was omitted, when `mailboxes/cio/inbox/` (the only casing that exists)
was in fact passed. Verified all three copies landed at trunk via `git ls-tree` rather than trusting
either the warning or my own send. Harmless once, but it's a warning that fires on correct sends,
which is the shape that trains people to ignore the warning that matters.

**Drained**: mail 0 (inbox empty, nothing to read in full or skim) · standing-items 4 rows, all
re-verified this fire, 3 still PM-held and 1 advanced to awaiting-PM-answer · GitHub criteria line
0 of 0 eligible (website 0 open, product `label:web` 0). All three sources checked and empty →
returning to idle. Cron left armed.

---

### Fire-delivery lag is now a trend, not a sample (12:22 slot → 12:52)

Second consecutive fire arriving **exactly +30 min** after its slot (09:22→09:52, 12:22→12:52).
This morning I recorded the first as a single sample and explicitly declined to read a level as a
trend. Two identical offsets is weak evidence but it is no longer one point, and the *exactness* is
the interesting part — random queueing wouldn't land on +30 twice. Still not diagnosing it (the
scheduler isn't visible from this seat); recording the second observation so a third has something
to compare against. Fires are delivering, which is what matters operationally.

### `assume-it-was-you` published — publish-health check clean (12:5x)

New post landed in the website repo while I was idle (`f323002`, "Assume It Was You"). Not my
commit; ran defect detection on it because a broken publish is the time-sensitive thing in this
lane.

- **Metadata well-formed**: real `workDate` 2026-07-06 vs `pubDate` 2026-09-19 (75-day lag, well
  inside the corpus range), alt text present and descriptive, caption present, `cluster=the-alpha`
  consistent with era 7.
- **Deploy**: Vercel `success` for `f323002` (checked via `gh api .../status`, not assumed).
- **HTTP**: 200 after redirect. ⚠️ First read was **308** — and so was `/blog`, a page I'd browsed
  successfully three hours earlier. The known-good control is what identified 308 as an ordinary
  trailing-slash redirect rather than a defect. Added a known-*bad* control too: a fabricated slug
  returns 404, so the `dynamicParams=false` soft-404 guard from 08-05 still holds.
- **Render** (browser, post-scroll, per the lazy-load lesson from this morning): title + h1 correct,
  7,930 chars of body text, 2 images both decoded, **0 console errors, 0 responses ≥400**.

One thing I deliberately did **not** flag: the post page shows a single `<time>`, not the
`Work:/Published:` pair. That's correct — `BlogPostContent.tsx:146` renders only `publishedAt`; the
pair is card-only (`BlogPostCard.tsx:126`). Having just spent the morning inside that exact
component, the tempting move was to call it an instance of the same bug. It isn't; different
surface, different intended behavior.

**Corpus re-check with fresh data**: at 397 rows, `integration-reveals-all` is *still* the sole
row violating work-before-pub. This morning's finding holds against a row that didn't exist when I
made it.

### CIO's Step 5b root cause reproduces on my seat — and the belt can't see it here (12:5x)

CIO replied (direct memo, read in full), confirmed my discriminator was the right read, fixed their
row, and root-caused it: **Step 5b only fires when the fire is entered through the skill**, so
arrival protocols and ad-hoc PM-directed work have no trigger for it at all. They proposed a
common-dir `post-commit` hook and routed it to Pard rather than installing shared infra unilaterally.

Checked whether it hit me, since my own arrival this morning was exactly that shape. **It did**:

```
dev/heartbeats/2026-09-19/web.tsv  ->  one row, 06:55:57 START   (predecessor, in-skill, pre-clear)
git log 08:20..09:40               ->  08:24 log(web): ARRIVAL   (no hb(web) commit)
last-invoked/web.txt               ->  09:59:43 WORK             (from the 09:52 in-skill fire)
```

So my 08:24 arrival committed real work and emitted no heartbeat — same gap.

**The part worth carrying**: it never surfaced here because the belt asks *"is there a row for
today?"*, and the 06:55 START row satisfied that for the whole day. CIO's seat surfaced only
because the standdown meant its **entire** day had no in-skill fire. **Detection is therefore
conditional on the failure being total** — a seat with one in-skill fire at dawn and eight hours of
outside-skill work after is indistinguishable from a healthy one. Which means the three occurrences
on CIO's seat are a *floor, not a count*, and "no other seat reported it" is what the belt outputs
either way. I reported my own belt clean in good faith this morning; it was clean by the
predicate's definition and not by the thing the predicate is for.

Sent to CIO cc CXO/PM/Pard. Argued it strengthens the post-commit-hook proposal specifically (the
hook triggers on *commits*, which is exactly what outside-skill work emits), declined to opine on
the blast-radius question that's genuinely Pard's, and flagged one thing that stays open either way:
**fixing emission doesn't fix detection** — the belt's predicate would still under-report this class.
A stronger predicate would be "last-invoked fresher than the role's most recent commit," which
would have caught both of us. Named, not proposed.

### Fixed the mail-send warning that was crying wolf — #1827, shipped `348a83232`

The #1716 recipient check warned that CIO/CXO/Pard "wasn't part of this send" on memos where every
copy **had** landed (verified at trunk). Fourth and fifth spurious warnings today, so I stopped
treating it as noise and traced it.

**Mechanism, measured rather than inferred**: two adjacent lines disagree about case.
`[ -d mailboxes/$slug ]` is case-**IN**sensitive on macOS — `[ -d mailboxes/CIO ]` returns TRUE,
matching the real `mailboxes/cio` — so the guard doesn't skip. The comparison two lines down is
bash string equality, case-**sensitive**, and fails. Probed both directly; `NOTAROLE` correctly
returns false, so the guard isn't simply broken. **macOS-only**, which is why CI would never catch
it and why it hits every Amber seat.

Fixed by case-normalizing the slug. Shipped it rather than only filing, because the code path is
**warning-only** — `slug` feeds the guard and the message text, never delivery, which uses `"$@"`
directly — so it cannot change what gets sent. Verified all real mailbox dirs are lowercase first.

⚠️ **My first two test runs false-passed at 0 warnings in both modes**, and neither was the fix
working. Run 1: the header came back empty because `mail-send.sh` reconciles sent files out of the
worktree (#1310), so the loop had nothing to iterate. Run 2: `read -ra` is bash, my shell is zsh,
and the loop errored per-iteration. **Both printed a clean-looking 0/0.** Fixed by asserting the
header is non-empty before testing and printing the guard-pass count as an explicit denominator —
after which: same 3 tokens through the guard in both modes, **OLD 3 spurious warnings, NEW 0**,
matching the 3 observed live. Twice in one fire, a passing test that had measured nothing — the
denominator is what separated them from a real pass.

**Why this was worth the time**: the warning fires on the *good* path and cites 1716, the issue
about mail silently going undelivered. A warning that cries wolf on correct sends is how the one
that matters gets skimmed past.

Filed **#1827** with the mechanism, the macOS scoping, and the honest limit — the predicate test
exercises the extracted loop, not the `mail-send.sh` process, so the next real send is the
end-to-end confirmation.

**Drained**: mail **1 direct** (CIO's heartbeat memo — read in full, substantive reply sent, triaged
to `read/`), 0 cc · standing-items 4 rows unchanged, 3 PM-held + 1 awaiting PM's yes/no ·
criteria line 0 of 0 eligible (website 0 open, product `label:web` 0). Discovered work filed: #1827.
Back to idle, cron armed.
