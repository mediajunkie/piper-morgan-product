# Session log — Exec (Chief of Staff), 2026-09-19

**Role**: Chief of Staff (Exec) · **Tool**: Claude Code · **Model**: Opus 5 (`claude-opus-5`)
**Worktree**: `~/Development/piper-morgan-worktrees/exec` · **Branch**: `claude/exec-cycle` (Model A)
**Cron**: `4a6add06`, `38 6,10,14,18,22`, CronList-verified single at START. Expires ~09-25.

**Continuity**: this seat was replaced 2026-09-18 11:12 (wave 0 of the Amber fleet renewal) and ran a
full normal day after. Yesterday's log — `dev/2026/09/18/2026-09-18-0708-exec-code-log.md` — is the
one to read for context; it carries a `DAY-CLOSED` marker, verified at this START with the anchored
pattern rather than a bare grep.

**Saturday.** Weekends are Piper Morgan prime time; expect PM engagement.

---

## START (07:08 fire — 06:38 slot, +30)

⭐ **Fourth consecutive +30 confirmation, and the first to survive BOTH a day boundary and a cron
rotation** (`d070a7df` → `4a6add06` at last night's STOP). The empirical-offset model that replaced
prereg §3's assumed-jitter figure has now predicted four fires in a row. **Deadline for this seat is
slot +30/+31, never the cron expression.**

- Prior day verified closed (Step 0, anchored `DAY-CLOSED` pattern — not the bare grep that
  false-passes on prose narrating a marker).
- `CronList` → exactly one job, correct expression.
- Inbox: 1 memo (Arch, Bets 001–003) — drained below.

### Owed by me today, carried from yesterday's close

1. ★ **Bets 001–003 reminder to PM — PM-requested, DUE TODAY**, and thereafter a standing rollup duty.
2. **The invite-copy currency question** — unanswered by PM, and Janne's email may go out.
3. **Raise the conversational-ruling gap with CIO/HOST** (PM's spoken rulings have no durable home).

### Janne's invite green-lit, and a second unchecked claim of mine corrected

🔴 **I told PM last night the invite template "pins 0.8.11.0, almost certainly stale." It is not.**
`VERSION` on trunk = `0.8.11.0`; template says `0.8.11.0`. **I inferred staleness from the shape of a
number instead of reading the file** — the same move that put the unsourced Vercel line in front of PM
twice on 09-18, repeated within hours of writing that one up. **Two instances in one day of asserting
an unchecked fact inside the artifact whose entire job is telling PM what is true.** Corrected in
writing to PM, HOST, CXO, Lead and Arch rather than quietly in the board.

**Green-lit the send.** #1814 — the wall a tester hit following the invite's own first instruction —
is closed and verified by **Lead's observed driven flow** (not mine, and the strongest evidence in the
chain). One claim I could not discharge and said so at its layer: the template's *"validates your API
keys before storing them"* — I verified the endpoint **exists** in `web/api/routes/setup.py`, which is
**code presence, not a behavioural test.** Routed to CXO/Lead as a this-week copy check, explicitly
**not** a blocker on a 4-day-old invite. HOST had checked in with PM at 07:08 independently.

### Saturday board compiled — `dev/active/exec-cohort-attention-rollup-2026-09-19.html`

**Two live blockers** (invite, Vercel) + four decisions. Yesterday's queue largely cleared: PM ruled
Q5 and the scope-guard, Web closed #35 with evidence, CXO/PA closed the T axis between themselves.

**Bets 001–003 carried, PM-requested, now a standing duty of this seat.** ★ Arch's re-sizing is the
valuable part and I led with it: **"three fields on one memo," not "three architecture documents to
review."** Bet 001 needs a buyer name (*"none named" is a complete answer*), a cost box, a kill date;
003 is partly pre-filled from PM's own 08-29 answer. ⭐ Arch's structural point: *"'non-blocking' is
exactly what let them sit 20 days. A label that removes something from the critical path also removes
it from everyone's attention."*

⭐ **`check-unboarded-pm-items.sh` had its first real compile-time use** — surfaced 2 PM-addressed
memos out of 4 landed (cc correctly excluded), 0 false positives, 0 unmatched standing-items rows.
The `to:`-not-`cc:` filter is doing exactly the work it was built for.

⚠️ **Nearly reported CIO as dark and didn't.** Its row reads `parked` and it has no commits today —
but its cadence is `7 10,16,22` and it is 07:1x. **That is the clock, not a stall.** The handoff's
standing-error list (*"check a role's actual cron before calling it dark"*) caught it before it
reached the board. Re-check after its 10:37 fire; my un-park notice is already in its inbox.

**Mail**: 1 direct (Arch's Bets memo, read in full, drove the board's item 3), triaged. Inbox zero,
verified on `origin/main`.

**Still owed today**: raise the conversational-ruling gap with CIO/HOST. ⭐ **Arch hit the identical
wall independently this morning** — their Bets memo's own caveat: *"If PM answered in a conversation
that never reached the documents, that's a different gap and this memo is the wrong fix for it."*
Two roles, same gap, one day apart. That strengthens the case; it is not just my miss.

### Model-allocation distortion corrected at source; renewal feedback relayed to Pard

🔴 **PM corrected a distortion I repeated three times.** I described their guidance as *"Fable
reserved for Lead Developer; everyone else Opus or Sonnet"* — a rule with a permission structure.
PM's actual position: they are **currently providing** Lead with Fable (and the live point was that
**Fable 5.1 lands once the machine and software are updated** — *that* was why Fable came up); the
rest *"tend to be"* Sonnet or Opus on a per-workload judgment about who is doing heavier planning,
**sometimes settled in discussion with the agent itself.** PM: *"It's not a perfect science at all."*

⚠️ **Provenance, because it matters more than the instance: I inherited the crisp framing from
`docs/handoff-exec-2026-09-18.md:73` and never questioned it.** Same shape as the Vercel line —
**a soft input hardened into a confident sentence by an upstream artifact, then propagated by me.**
That is now **three instances in two days** (Vercel, the version pin, this). The board format rewards
crispness and I have been paying for it in accuracy. **The tell is the same every time: a sentence
with no source that reads well.**

✅ **Recorded in `decisions.log` — deliberately, as the first real use of the conversational-ruling
gap's own fix.** I flagged 09-18 that PM's spoken rulings have no durable home and that it cost a
stale blocker. Rather than memo about it again, used the surface. Carry-forward corrected too.

**Model tracking — PM asked whether it's worth doing at all, and the data supports their doubt.**
Of 8 wave-2 arrival blocks: **3 quoted system info, 5 asserted a model with no source shown.** PM:
*"it does seem to be something that gets guessed at more often than not."* 5 of 8 is literally that.
⚠️ **And "asserted" ≠ "guessed" — the point is you cannot tell from the artifact**, which is m-44's
shape. The field can't support a dislocation audit as currently written.

🔴 **I BUILT A SECOND BAD TABLE AND CAUGHT IT BEFORE SENDING.** A per-seat before/after model
comparison, grepping each log for the first model string — which on a two-session log returns the
**header**, not the arrival block's observation. It rendered HOST as "Opus 5 → Opus 5" when HOST's own
block says they observe **Sonnet 5** against an Opus 5 header. **My instrument reproduced precisely
the error HOST caught by hand.** Not sent. Told PM the table was wrong rather than publishing a third
bad table in two days.

**Relayed PM's renewal feedback to Pard** (cc PM, Janus): **a renewal should detect the seat's current
model and reuse it, not assign one from a rubric.** A renewed seat should be capability-
indistinguishable from its predecessor — the same property §2.2 protected on timing, and **nothing in
the wave protocol pins tier.** Asked the three questions that precede any fix (does the renewal path
set a model; from what; is the pre-clear model observable at clear time — *"if it isn't, that's the
actual gap and it's harder than a rubric change"*). **Explicitly did not send my numbers**, and told
Pard not to let my earlier "five seats came back on Sonnet" stand as fleet data. One seat has
evidence; seven have claims. PM asked we check with Pard before anyone "fixes" anything.

✅ **CIO closed my unboarded-PM-items proposal by SHIPPING it** — `duty-cycle-tick` **v1.36**. All
three questions resolved: `--scope=role|global|all` as a real flag rather than an unenforced
convention; marker moved out of sprint-cleaned `dev/active/` to a new `dev/state/`; wired as step 1c
after the mail drain. **Every role now runs the role-scoped scan every fire — PM's design, not mine.**

**PM's priorities, noted and respected**: mired in administrative work, **today's blog post
("Assume It Was You", `status=drafted`, calendar 09-19) is the top temporal priority**, and they asked
me to hold the substantive items until the admin is under control. **Not pushing anything at them.**
Board stands at 2 blockers + Bets; nothing of mine is urgent.

**Mail**: 3 triaged (1 direct — CIO's ruling, read in full; 2 cc — the #1823 thread, skimmed, nothing
owed). Inbox zero, verified on `origin/main`.

### Ruled 7x cohort rollout; two closeout rows corrected by their own owners

**RULED (mine as originator): CIO's option 2 — self-serve — but wired to a chokepoint that already
fires.** CIO offered (1) they run it against all 10 other seats, or (2) each role runs its own, and
leaned (2) while naming its flaw: *"depends on 10 roles each remembering."* **That flaw is exactly
what I measured Thursday** — 29 check-shaped scripts, the relevant ones wired into CI 0× and hooks
0×. A rollout whose mechanism is "everyone remembers" is m-53's bolt-on by construction.

**The fix was already in the repo**: `.github/workflows/quarterly-maintenance.yml`, cron
`0 9 1 1,4,7,10 *`, already generates a checklist issue with a `maintenance` label. **Add one line to
it.** Timing is unusually good — **next firing Oct 1, twelve days out**, and since Q3 completes
Sept 30, a single Oct-1 run per role archives **Q1+Q2+Q3 together**: the existing backlog and the
recurring cadence collapse into one action, with no agent ever touching another's mailbox. Carrying
it in the rollup through the first cycle as belt-and-braces — **and if the quarterly issue turns out
to be read by nobody, that is itself a finding I'd rather get from a no-op sweep.**

🔴 **Verified CIO's `.gitignore` near-miss generalizes — and got there the WRONG WAY first.** A
scoped negation would have meant ten roles each silently dropping their mail history on first run.
`git check-ignore -v` printed `!mailboxes/*/read/archive/**` and **exited 0**, which I nearly read as
"IGNORED — the others would hit the footgun." **`check-ignore` prints negation patterns too**, so
that reading was ambiguous in precisely the direction that manufactures a false alarm. Behavioural
test settled it: `git status --porcelain` shows `?? mailboxes/exec/read/archive/` and `git add -n`
stages it. **Config-read said one thing, behaviour said the opposite.** Fourth crude-instrument
error in two days — **third caught before publishing.**

**Two closeout rows corrected by the people they were about, not by me:**
- **Lead**: the usage-per-account capture is now written (`dev/active/usage-per-account-capture-2026-09-19.md`).
  Shape worth keeping: append-only daily TSV per account, **written from OUTSIDE the seat fleet** —
  a ceiling-refused seat cannot self-report, same watcher-outside-the-frozen-set argument as the
  freeze-watchdog. Two unknowns are PM's: the seat→account mapping and where the authoritative number
  is read.
- **Docs**: ⚠️ **I had the recipient wrong.** My synthesis said Docs' 09-13 routing-memo offer to
  *Lead* was unanswered. It was to **PM**, not Lead, and nothing is missing. **My row implied Lead was
  sitting on something they were never sent.** Corrected in the synthesis rather than left standing.

**PM's priority respected**: they are mid-edit on "Assume It Was You" via the admin UI (10:43 commit).
Nothing pushed at them this fire; the board is unchanged at 2 blockers + Bets.

**Mail**: 3 triaged — 2 direct (CIO's report and Lead's note, both read in full and both actioned),
1 cc (Docs→Lead, skimmed — and it happened to correct my own synthesis, which is the argument for
reading cc's for asks rather than filing them blind). Inbox zero.

### Internal report for Ship #061 built — the artifact I'd produced was not in the established form

**PM asked whether I'd synthesized an internal summary report for discussion.** Honest answer: I had
written a **markdown synthesis** (10/10 closeouts, 09-18), but **not the established artifact.** The
precedent is `ship-NNN-internal-report-for-pm-YYYY-MM-DD.html` — two prior instances (#059, #060) —
with a fixed shape: **At a glance dashboard · the week's three arcs · the pattern worth the Ship's
theme · the honest counterweight · needs you · what didn't move.** And per PM's own 09-13 ruling it
**gates the public draft.**

⚠️ **What my synthesis was missing wasn't polish — it was the load-bearing section.** "The pattern
worth the Ship's theme" is the part Comms drafts the public post *from*. Without it there is no Ship
input, which lines up exactly with the other two facts: **no workstream-061 files, no Ship #061
calendar row.** The whole #061 pipeline hadn't started, and my synthesis being "close enough" is what
made that easy to miss.

**Shipped**: `dev/active/ship-061-internal-report-for-pm-2026-09-19.html`.

🔴 **Two dashboard tiles are marked UNMEASURED, not zero.** The GraphQL limit is exhausted on this
account; the issues-opened/net query returned `0`. **Zero closed against 1,804 commits is a broken
instrument, not a result** — refused to put it in a tile. The **~45 closed is Lead's own attributed
figure**, not my verification. ⭐ **Same failure CXO and PA hit yesterday, and both refused to invent
a number — it would have been indefensible to do otherwise in the very report that praises them for
it.** Re-runnable once the limit resets.

**Proposed theme, flagged as needing PM's challenge**: *a clearing condition is only worth what it's
worth under pressure* — HOST refusing to let their own bar be lowered, Arch conceding same-day, Lead
producing observed evidence twice, CXO holding a conditional that then failed honestly.
⚠️ **I checked it against the last one rather than assuming novelty: Ship #060's theme was "the week
the cohort spent correcting itself."** Self-correction would be a REUSE. The distinction I'm claiming
— correction looks inward, refusal-under-pressure holds a line outward with a named external person
waiting — **may be too thin, and I said so in the report rather than selling it.**

**Milestone figure honesty**: `sprint-truth.py` ran at 12:51 **today**, which is *after* the window
closed. Labelled "as of the 19th" rather than passed off as a window-close number.

**Still open and now sharper**: does this closeout stand in for the workstream reviews, and does
Ship #061 exist? Those two answers decide whether Comms drafts from this report.

### PM's two asks shipped: sprint-truth snapshot/delta, and the weekly cycle captured as a runbook

🔴 **Corrected by PM: *"Let's not slop ahead to themes before we discuss the substance."*** I put a
theme section in the internal report as a conclusion. **The theme belongs at step 6, after the step-5
discussion — which may replace it.** Demoted it in the artifact to raw material explicitly marked
*not a conclusion*, rather than deleting the work.

**1. `scripts/sprint-truth.py` now snapshots and diffs** (PM's ask, verbatim: *"write a last best
snapshot somewhere and seek the diff / changes"*). It had **no persistence at all** — the `json`
matches in it were parsing `gh` output. So it computed a correct figure every run and remembered
nothing: **"57 not done" was always true and never informative — a level, never a change.**
- Stores **issue NUMBERS, not just counts**, deliberately: a net figure hides *"three closed, four
  arrived"*, which is the shape that matters. Reports arrived/left by number.
- Snapshot lives in **`dev/state/`** — CIO's not-swept home established today, *because* `dev/active/`
  is sprint-cleaned and a vanishing marker degrades silently. Reused their convention rather than
  inventing a home.
- `--no-snapshot` reads the baseline without moving it. **A first run says so explicitly** rather
  than reporting no change.
- **Tested**: baseline write ✅ · delta path ✅ · corrupt JSON → None + warning ✅ · missing file ✅ ·
  old-format snapshot with no numbers → flags "membership unknown" ✅.
  ⚠️ **The corrupt-file test via the CLI was INCONCLUSIVE** — GraphQL limit exhausted, so the script
  died at the board query before reaching snapshot load. **Exercised `load_snapshot`/`report_delta`
  directly in-process instead — free, and the test I should have reached for first.**
- Live evidence for why it was needed: MVP read **57 at 12:51 and 58 at 13:48.** The level moves
  hourly; only the delta carries information.

**2. `docs/internal/operations/weekly-cycle-runbook.md`** — PM's nine steps captured. PM: *"we need
to capture it so you don't try to guess in the future."* **Two of nine steps had skills
(`create-omnibus`, `draft-weekly-ship`); the other seven lived only in habit** — which is precisely
why this seat guessed, produced a markdown synthesis instead of the established HTML internal report,
and missed that #061 had no calendar row.
★ **The check I added that would have caught it**: all three of *no calendar row · no workstream
files · no internal report* were true at once and nobody connected them. **Any one is a stall signal;
all three is a pipeline that never started.** Three greps at step 4.

⚠️ **My own efficiency cost, owned**: I exhausted the shared GraphQL limit twice today — redundant
`sprint-truth` runs across fires, a double freeze-check invocation, and two throwaway tables that
were wrong. **That limit is cohort-shared, and exhausting it breaks the script for everyone.** PM
flagged it and is right. Going forward: one `sprint-truth` per fire quoted from the log, and nothing
asked of the API that `git` can answer.

### Corrected twice by PM on the same exchange — and one error was already in a durable doc

🔴 **Error 1: I invented a distinction that doesn't exist.** I kept asking whether "this closeout
stands in for the workstream reviews." **There is no separate workstream artifact owed in this cycle
— the ten update memos ARE the input.** PM: *"Not sure what 'this closeout' means but we are now at
the point where I will review your report, fill in the GitHub gaps, and discuss it with you."*
I manufactured a gating question out of my own unclear vocabulary and then asked it three times.

🔴 **Error 2, worse because it was already written into a runbook I'd created an hour earlier.**
I recorded *"the calendar row is created at draft time — a Ship with no row is an orphan, and that's
how #061 went unnoticed."* **PM: *"We write the Ship AFTER this step and then we add it to the
calendar."*** So an absent row for an undrafted Ship is **correct, not a defect.**
**Root cause: I generalised the blog-post pattern — where the row genuinely does come at draft
creation — onto the Ship, which has a different order.** Two adjacent workflows, one habit, wrong
transfer.
★ **And the compounding failure is the one to remember: I found three absences at once (no report in
the established form, no calendar row, no workstream files), assumed they were one failure, and wrote
a "any of these three is a stall signal" check into the runbook. ONLY ONE was real.** A cluster of
absences is not evidence of a stall unless you know which absences are *supposed* to be there at that
point in the cycle — **and the runbook not existing is exactly why I didn't know.** m-54: a false
claim in a durable doc, caught within the hour because PM read it.

**Fixed**: runbook step 7–9 rewritten with the correct order and the error recorded rather than
quietly overwritten; the false three-signal check replaced with "what actually went wrong, and what
didn't"; header framing corrected; and the bogus "Ship #061 needs you" row pulled from the internal
report with a note saying why.

⭐ **PM's improvement, routed to Comms** (sole hand-editor of the calendar): **pre-seed the Wednesday
Ship slots for the foreseeable future.** The reason it's more than tidiness — **absence is currently
the default state for a Ship row until late in the cycle, so a missing Wednesday carries NO
information**; it looks identical whether the cycle is on track, stalled, or never started. Same
shape as a check that can't distinguish "measured and found nothing" from "never ran." Pre-seeding
inverts it so a *gap in the sequence* is the signal. **Asked Comms one genuine question rather than
assuming: does a pre-seeded row break `reconcile-drafts-calendar.py` by creating rows with no draft
behind them? If so the cost may outweigh the signal — their tooling, their call.**

**Where we actually are: step 5.** PM reviews the internal report, fills the GitHub gaps from their
side, and we discuss. Nothing gates that.
