# 2026-09-19 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

### 07:22 START — a real PPM ruling: #1823 (openai-only BYOC user refused at /intent)

Heartbeat, `CronList` (one job, `619147bb`), sync clean (0/0). Prior day's log
(`dev/2026/09/18/2026-09-18-1151-ppm-code-log.md`) confirmed carrying its **DAY-CLOSED** sentinel.

**Mailbox had 2 items, both directly addressed to PPM on `#1823`** — the openai-only BYOC refusal
question flagged as a genuine product decision two fires ago. Read both in full before ruling.

**Arch's finding made the call cheap**: `resolve_request_api_key`'s ladder never knew what a
provider was — the Anthropic constraint is injected at the call site
(`web/utils/llm_key.py:117,189`), not structural. So gating on "owns at least one spendable
provider key" instead of "owns an Anthropic key" needs no restructuring, just a different
injection plus constraining provider selection to the providers actually bound. Arch flagged one
unverified hinge (whether `#1415`'s provider selection actually consults the binding before
routing, or selects first and looks up the key after) as Lead's to trace, not an assumption to
make either way. **CXO** found an independent defect while drafting copy for both branches: the
current refusal self-contradicts (states an ownership policy, then names a specific vendor) and
diverges from `conversational_floor.py:610`'s already-neutral convention — true today, regardless
of the ruling, and the fourth time this "one user question, N inconsistent answers" family has hit
CXO's copy in five days.

**Ruled: accept Arch's recommendation in full** — gate on any spendable provider, refuse by naming
the task type (not the vendor) when a task is genuinely Anthropic-tuned, per Arch's fail-closed
condition. CXO's "any spendable provider" copy branch ships and resolves their independently-found
defect in the same stroke — no separate issue needed for that half. **Sequencing**: `#1824`'s
bucket split lands first or together, not after, so the task-type refusal has a bucket to live in
rather than inventing a fifth one informally — recorded in the epic-order file. **Named Arch's
unverified hinge as an explicit precondition on implementation** rather than letting it get
assumed away mid-build.

Sent the ruling to Arch/CXO, cc Lead/Exec/PM. Folded the full ruling, sequencing note, and
precondition into `dev/active/mvp-epic-order-2026-09-09.md`'s epic 2 section.

---

## ARRIVAL — 2026-09-19 08:26 PDT (Wave 2, Amber fleet renewal)

**Identity**: Principal Product Manager (PPM), seat continuing on `claude/ppm-cycle`, worktree
`~/Development/piper-morgan-worktrees/ppm` (Model A). **Context cleared deliberately** — Pard
conducting, Janus certifying, xian overseeing. **This log is RESUMED, not replaced** — the 07:22
entry above is my predecessor's work this morning and I am not re-doing it.

**Model I observe myself running**: **Sonnet 5** (`claude-sonnet-5`). Matches this log's header.
Per the 09-18 handoff's model-allocation note, Fable is reserved for Lead Dev and everyone else
runs Opus or Sonnet — consistent, no mid-session change to record.

**Handoff read**: `docs/handoff-ppm-2026-09-18.md` in full — dated 2026-09-18, one day stale by
design. Also read the newer surfaces that outrank it where they disagree: `dev/active/delta-ppm-2026-09-19.md`
(cutoff 07:22, 4 commits + 2 memos) and this log's own 07:22 entry (the #1823 ruling).

### Handoff claim verified against a primary source — MVP board state

**Picked because it's the claim my next duty-cycle fire directly depends on** (board hygiene is
core to this seat, and any "what's left" answer starts here). The handoff quotes a line from
2026-09-18 and explicitly warns not to cite it as current: *"MVP: 56 not done (33 Sprint Backlog,
3 In Progress, 6 In Review, 14 Product Backlog); 1176 done."*

**Verified how** — ran `python3 scripts/sprint-truth.py` fresh this session (not a quoted line, a
live GraphQL pull, 1657/1657 board items):

```
MVP: 57 not done (32 Sprint Backlog, 3 In Progress, 6 In Review, 16 Product Backlog); 1179 done.
PLUS 0 unmilestoned — every open issue carries a milestone.
```

**The handoff's number is stale, as it predicted it would be** — net +3 done, composition shifted
(Sprint Backlog 33→32, Product Backlog 14→16, not-done total 56→57) between the handoff's writing
and now, consistent with real work landing (today's 07:22 fire alone touched #1823/epic 2). Not
treating either number as durable; `sprint-truth.py` is the only legitimate denominator, run fresh
each time, per the handoff's own instruction.

### Cron — CronList-verified

- **Live**: `CronList` → **`619147bb`** — `52 6,9,12,15,18,21 * * *` (recurring, session-only).
  Singular, no duplicate jobs.
- **Handoff says `4bcf1e1b`.** That job ID is gone — this morning's 07:22 log entry already shows
  `619147bb`, so the re-arm happened before my arrival (likely an Amber cold-start restart between
  the handoff's writing and this morning's first fire) and is already recorded there. Stale-handoff,
  not a lost job.
- **Next fire: 09:52 PDT today** (~86 min from this block). `CronList` proves the job object
  exists; delivery is unproven until 09:52 actually lands.

### What I did NOT verify, and am not asserting

- The epics-9/10 open PM question (handoff line 28) — not re-checked against mailboxes/decisions.log
  this arrival; carrying it as unresolved per the handoff and the 07:22 log until the next fire's
  mailbox drain.
- No heartbeat FAILURE message observed this morning for this seat. Nothing to verify at trunk on
  that front.
- Sync: `git fetch` + fast-forward brought local branch from 4 commits behind to 0/0 against
  `origin/main` as part of this arrival (four other roles' arrival-block commits landed in the gap).

---

### #1823 fully scoped: branch two ruled OUT OF SCOPE, Lead's precondition DISCHARGED (10:22 WORK fire)

`CronList` → one job (`619147bb`), unchanged. Worktree-collision fingerprint clean (`ppm` ↔
`claude/ppm-cycle`). Synced clean. Mailbox had 3 items, all direct to PPM on `#1823` — read all
three in full.

**CXO** traced whether any task type is currently Anthropic-tuned (the precondition branch two's
task-type-named refusal copy depends on) and found `resolve_model` total by construction
(`clients.py:337-357`): task type selects a model *tier* inside whichever provider selection
already picked, never a provider. 8/8 `MODEL_CONFIGS` task types × 3/3 providers, no failing path.
**Verified the load-bearing claim myself before ruling** (never guess at facts you can look up):
`config.py:74` — *"Task configurations — provider-agnostic."* — literal source comment, not
inferred. **Ruled: no work item for branch two.** The architecture is deliberately
provider-agnostic, so the task-type-refusal copy would invent a distinction the code doesn't make —
the same false-precision shape Arch's own memo warned against. CXO's reworded string stays
deposited (not shipped), licensed for if a task type ever becomes genuinely provider-constrained.
**Deliberately not filing a successor issue** — this is scope narrowing to match verified reality,
not discovered work (the epic-12 lesson cuts the other way here: inventing a issue for a
currently-empty branch would manufacture the feeling of coverage, not avoid an incomplete closure).

**Lead** traced the precondition my original ruling left open (whether `#1415` provider selection
consults the binding or selects-then-looks-up) — **discharged**: it consults the binding
structurally, so the pass-gate/fail-at-route third state CXO flagged is unreachable once the gate
change lands (the binding-expansion fix IS the gate change; Slack/`#1822` already live as
demonstration). No new string owed there either.

**Arch** self-corrected an earlier scope-clause error (claimed the gate/spend asymmetry was
"uniform across four call sites"; Slack was already fixed by `#1822`, and one cited "site" was an
import line — actually 2 of 3 affected). Already fixed at the reading surfaces (issue comment +
decisions.log) per Arch's own discipline; nothing further owed, read in full not skimmed as cc.

**#1823 is now fully scoped: branch one only.** Posted the closing ruling to the GitHub issue
(https://github.com/mediajunkie/piper-morgan-product/issues/1823#issuecomment-5743888454), a
decisions.log entry (2026-09-19 10:2x PT, ppm), folded into `mvp-epic-order-2026-09-09.md` epic 2,
and replied to CXO/Arch cc Lead/PM via `mail-send.sh` (two sends: the ruling memo with explicit
delivery paths to all four recipients + own sent mirror; own inbox→read triage + regenerated own
MANIFESTs in a follow-up send after a resync).

**Mail**: 3 direct, all read in full and substantively actioned (this entry). 0 cc-only this fire.
**Standing-items**: none owed (per the tick prompt's explicit "NO STANDING OWED WORK ITEM"). Third
queue source (GitHub criteria) — PPM doesn't have a formalized criteria line yet; noting this as
the honest gap rather than skipping silently, per the duty-cycle-tick skill's own instruction that
an absent criteria line is a fact to name, not a blocker.

**Discovered issues filed**: None.

### `sprint-truth.py` re-run: 1 unmilestoned issue found and fixed same-fire (#1825)

Per the tick prompt's instruction to re-run `sprint-truth.py` every fire, not just when mail is
empty: found `PLUS 1 open issue(s) carry NO milestone`. `#1825` (mail-send.sh's stranded-sibling
guard false-positiving on `MANIFEST.md`, filed by CXO per Discovered Work Discipline, 10:21 PDT —
one minute before this check) — new-issue-skips-filing-convention shape, same family as `#1824`
two days ago, not the `--milestone`-doesn't-board-add shape. Fixed same-fire: milestone `Ongoing`
(precedent: `#1746`, an earlier `mail-send.sh` guard bug, same milestone), board-added via
`gh project item-add`, Status `Product Backlog` set via the safe per-item mutation
(`updateProjectV2ItemFieldValue`, never the field-level full-replace — verified no collateral
damage on `#1824` post-set, option count unchanged at 6). Re-ran `sprint-truth.py`: `0 unmilestoned`
confirmed. No epic-order entry needed — this is tooling/infra (`Ongoing` milestone), not an MVP item.

---

### #1829 filed and board-fixed; Arch answers CXO's #1823 permanence question (13:22 WORK fire)

`scripts/duty-cycle-heartbeat.sh ppm WORK` emitted first (unsuppressed — first heartbeat this
fire). `CronList` → one job (`619147bb`), unchanged. Worktree fingerprint clean. Synced clean.

Mailbox had 1 item, direct to PPM (+ CXO). Arch answered the question CXO explicitly flagged and
left open this morning (is provider-agnosticism deliberate/load-bearing, making #1823's branch two
*permanently* empty rather than merely empty today): **yes — derived from BYOC (PM's #1812
ruling), not an independent preference.** Not currently law though — absent from `ESSENCE.md`'s
five standing rules and seven commitments, protected only by a source comment. Arch declined to
propose an ESSENCE amendment (would restate BYOC at lower altitude) and instead filed `#1829`
asking for a build-time ratchet, plus found a second, sharper defect while reading the function
directly: `resolve_model`'s totality is silent `.get()` fallback, not exhaustiveness — an unknown
task type silently buys the `heavy` tier, an unrecognized provider silently returns OpenAI's model
IDs (latent at 3/3 providers, live the moment a fourth is added) — same honest-empty mechanism as
`#1816`, and the thing that makes branch two empty is the same thing that can silently mis-tier a
typo.

**Board hygiene**: `#1829` had skipped the filing convention (no milestone, not board-added) — same
recurring shape as `#1824`/`#1825` this week, third instance in five days. Fixed same-fire: MVP
milestone (precedent `#1816`, same honest-empty family, not `Ongoing` — this is a real spend-
correctness risk under BYOC, not meta-tooling), board-added, Status Product Backlog via the safe
per-item mutation, verified.

**Folded into epic 5 (Honest-empty/GatherOutcome)** — Arch named the shape themselves, and it's
this epic's own class, not epic 2's. Now 8 of 16 open. Recorded Arch's branch-two-permanence answer
in the same epic entry for continuity, explicit that it doesn't change my #1823 ruling (branch two
stays out of scope regardless of how permanently empty it is — CXO's antecedent-false finding was
sufficient on its own).

Replied to Arch, cc CXO/Lead/PM, confirming both fixes and that no further PPM action is owed on
either thread. Two mail-send.sh sends (reply + own MANIFEST regen after sync), inbox drained to
zero, re-checked after.

**Mail**: 1 direct, read in full and actioned (board fix + epic fold + reply). 0 cc-only.
**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line — same
honest gap as last fire.

**Discovered issues filed**: None (Arch filed #1829, not PPM).

### `sprint-truth.py` re-run: 3 MORE unmilestoned issues found and fixed same-fire (#1827/#1828/#1830)

Re-ran after the #1829 fix, per the tick prompt's per-fire instruction — found `PLUS 3 open
issue(s) carry NO milestone`, all filed by other roles during this fire's window
(20:19/19:57/19:57 UTC): `#1827` (Web — mail-send.sh case-insensitive recipient-check false
warning, same family as `#1746`/`#1825`) → **Ongoing**. `#1828` (Docs — TZ-PROJECT parent tracker
for `#1493`'s 6 F-slice children) → checked all 6 children's actual milestones first rather than
guessing: unanimously **Production**, so the parent matches. `#1830` (HOST — alpha-onboarding
email template describes the retired local-install flow, *"nearly went out to Janne Lammi on
2026-09-19, caught by PM in review, not by any process"*) → same defect class as `#1804`
(ALPHA_TESTING_GUIDE.md's identical local-vs-hosted staleness), which is milestoned **Ongoing** —
matched the precedent rather than treating the near-miss urgency as a milestone signal. All three
board-added, Status Product Backlog set via the safe per-item mutation.

**Verification hit the shared cohort-wide GraphQL burst throttle** on the full `sprint-truth.py`
re-run (`gh api rate_limit` showed 5000/5000, confirming it's the shared throttle, not my own
quota — per the tick prompt's own diagnostic). Retried once after a 20s pause, still throttled.
**Did not block the fire on it** — verified all four fixes (`#1827`/`#1828`/`#1829`/`#1830`)
individually via a small single-item GraphQL query instead, all four milestones confirmed set
correctly. Full board recount deferred to next fire rather than spending more of the fire waiting
on a shared resource. Four board-add fixes this session in ~3 hours (`#1825`, `#1829`, `#1827`,
`#1828`, `#1830` — five, not four) is worth naming as a rate, not just individually: the filing
convention is visibly not airtight under concurrent multi-role filing during Wave 2's active
resume, matching the tick prompt's own "not airtight" caveat.

---

### Board-add convention: named the mechanism gap to Arch, closed #1785/#1743 threads (16:22 WORK fire)

`scripts/duty-cycle-heartbeat.sh ppm WORK` emitted (unsuppressed, first this fire). `CronList` →
one job (`619147bb`). Worktree fingerprint clean. Synced clean.

Mailbox had 3 items, all direct to PPM (or PPM+CIO).

**Arch traced why the board-add drift keeps recurring**: `piper-draft-issue` sets milestone
correctly (verified, 2 command blocks) but has **zero** board-add or Status-set steps at all —
grepped for `--project`/`item-add`/`Product Backlog`/`project-add`, 0 hits. So an author following
the skill perfectly still produces an off-board issue; the convention has only ever lived in my
head and my corrections, which don't propagate. Arch declined to guess the canonical values
themselves (right call, per "never guess a fact you can ask about," plus CLAUDE.md's Projects-v2
full-replace hazard). **Sent the canonical values**: project `PVT_kwHOADE-8s4A-JwA`, `gh project
item-add 1 --owner mediajunkie --url <url>`, Status field `PVTSSF_lAHOADE-8s4A-JwAzgxpGyU`,
`Product Backlog` option `e7d1c990` (all verified live this session, not from memory — this is the
exact mutation I've run 4 times today with zero collateral damage). Authorized Arch to make the
skill edit and verify behaviorally as they offered (scratch issue through the amended skill, confirm
board+Status, close it). This should retire the "5 board-add fixes in 3 hours" pattern from earlier
today at its source rather than at my correction each time.

**Lead's #1743-vs-quarterly-archival carve-out**: verified the actual lint source
(`mailbox_filename_lint.py:91-111`) before acking rather than trusting the memo's description alone
— confirmed the exemption is exactly `read/archive/…`, nothing broader, tested both directions.
No overrule; the carve-out preserves the invariant's actual intent (catching triage-move accidents)
while accommodating CIO's deliberate, documented archival design it hadn't been told about. Acked
to Lead/CIO, cc CXO/PM.

**#1785 (Lead's push-vs-nightly canonical-job decision)**: correctly routed direct-to-PM (issue was
explicitly filed "not decided unilaterally"), PPM only cc'd. Not a PPM call. Folded the
recommendation into epic 1's tracking paragraph for continuity, triaged, not chasing PM for an
answer.

**Mail**: 3 direct/cc-to-me-plus-others, all read in full; 2 got substantive replies + explicit
action (Arch's canonical values, Lead's ack), 1 (#1785) required no PPM action beyond epic-file
tracking since PM is the addressed decision-maker. 0 pure-cc-skim this fire.

**Discovered issues filed**: None.

### Large `sprint-truth.py` delta investigated before reporting: MVP "done" 1179→1127, explained

Fresh `sprint-truth.py` run showed a startling delta: `not done 58→56 (-2) done 1179→1127 (-52)`.
A 52-issue swing in "done" from closing 3 issues (`#1687`/`#1747`/`#1811`) and 1 arrival (`#1832`)
doesn't arithmetically follow — **did not report this as fact without checking it first** (m-43/m-44
discipline: a script's own delta line is not automatically ground truth).

**Cross-verified against a second, independent source**: `gh api repos/.../milestones` (raw
GitHub REST, not the project board) confirms **MVP: open=56, closed=1127** right now — matches
`sprint-truth.py` exactly. So 1127 is real; the earlier 1179 readings (mine, at both prior fires
today) were the milestone's true state *before* something moved ~52-62 issues off it this
afternoon, not a script bug.

**Found the mechanism**: searched closed issues updated in the last hour, not milestone MVP —
**62 closed issues** (`FLY-AUDIT: Weekly Docs Audit`, `ROLE-HEALTH-CHECK`, `PATTERN-SWEEP`,
`Quarterly Maintenance Sweep` — recurring auto-generated cohort housekeeping tickets going back to
2026-03) all show `milestone=Ongoing`, `updatedAt` clustered in a **~7-minute window,
23:00-23:07 UTC (16:00-16:07 PDT)** — a bulk retriage, not organic individual closures. This
matches `Ongoing`'s own milestone description verbatim (*"perpetual, parallel-running tracks...
FLYWHEEL and Skunkworks"*) — these were misclassified under MVP and got moved to where they
actually belong. **Reads as legitimate housekeeping, not data loss**: 0 unmilestoned issues
throughout, no missing board items, this is milestone reassignment (an issue-native field) not a
Projects-v2 field-level mutation (the actually-dangerous full-replace shape). **Could not find
attribution** — grepped mailboxes/read, inbox, decisions.log, today's session logs for
FLY-AUDIT/ROLE-HEALTH-CHECK/retriage/"milestone.*ongoing", zero hits. Not chasing it down further
this fire; noting the gap rather than guessing who ran it. **Going forward, `1127` is the correct
MVP-done baseline, not `1179`** — updating my own running references.

---

### PM ruled epics-9/10 after five days open, plus a real mail-storm fire: 7 direct items, 2 substantive replies, epic-order file gets its biggest single-fire update yet (19:22 WORK fire)

`scripts/duty-cycle-heartbeat.sh ppm WORK` emitted (unsuppressed). `CronList` → one job (`619147bb`).
Worktree fingerprint clean. Synced clean. Mailbox had 7 items — the biggest single-fire inbox this
session — all direct to PPM or PPM+others (0 pure-skim cc). Read all seven in full before acting.

**The big one: PM ruled the epics-9/10 question I'd had open since 09-14, five days.** Verbatim via
Exec's relay: *"Agree the mini-epics do not serve... let's not overindex on our filing rules."*
Collapsed former epics 9 (Silent-death) and 10 (Composer UX) into one catch-all epic, preserving
epic 9's mechanism-level distinctness as a labeled subgroup rather than flattening it (Exec's own
flag against over-collapsing). `#1420`/`#1422` now ride as `#1423`'s children, not separate epic
membership. **Epic count: 11 → 10** — reverses part of the 6→12 growth PM questioned 09-14. Added a
change-log entry naming the ruling and the reversal explicitly, since it closes a thread PM opened.

**Three more real corrections landed in the same file, same fire** (from Lead's PM-rulings relay,
cc'd): `#1785` corrected from "recommends splitting" to **shipped wholesale** — Lead re-verified
their own recommendation before shipping and found the premise was wrong (the canonical job's
entire selection is live-spend, measured 183/183, no free deterministic subset exists) — recorded
both the original claim and the correction rather than silently overwriting one with the other.
`#1812` step 5 **UNBLOCKED** (PM ruled their own account gets normal-account semantics, no
operator-key special case) — folded Arch's 6-site consumer enumeration into epic 2 for Lead to
build against, including the honest-empty warning on the ladder's bare-`None` third rung (same
family as `#1816`/`#1829`), and noted this dissolves the "on a BYOC deployment" qualifier on my own
earlier #1823 ruling (simpler conclusion, not a different one — not reopening #1823 for it). Slack
sponsorship question **retired** (linked-account-only, no code change). `#1818` updated to reflect
CXO's experience-half ruling (greeting passes the gate but must carry key-state — "both halves or
neither"), Arch's structural half still open.

**Arch's board-add fix, shipped and behaviorally verified**: filed scratch issue `#1833` through
the amended `piper-draft-issue`, confirmed board+Status end to end, closed it. Arch flagged a
tension with PM's "don't overindex on filing rules" ruling — **I had PM's actual words from the
same fire** (the epics-9/10 ruling above) and could resolve it with more than my own read: the
ruling is scoped to epic taxonomy specifically, not to mechanizing an already-ratified
(2026-09-12) field default. No revert needed; replied confirming.

**PA's Cross-Piper synthesis, read in full per PM's direct ask (via Exec)**: the doc's relay-
failure-class finding (*"a claim true at one layer, restated as true at another, propagating
through a relay"*) was flagged as squarely my lane. Replied with two concrete same-day instances
rather than an abstract agreement — the `#1785` correction above (Lead's own re-verification
catching a premise error before it shipped) and the MVP-done delta investigation from earlier this
fire (cross-verified before reporting rather than trusting a script's own delta line). Named
explicitly what I'm NOT claiming: both catches happened because I was already re-verifying for
other reasons, not because I have a dedicated check for this specific failure class.

**Board hygiene**: `#1834` (HOST — mechanical check for "people"/"person" describing agents) found
unmilestoned via the routine post-drain `sprint-truth.py` re-run. Checked precedent first (5 prior
`sapient-trust`-labeled issues, all `Ongoing`) rather than guessing — set milestone `Ongoing`,
board-added, Status Product Backlog, verified. `0 unmilestoned` confirmed after.

**Mail**: 7 direct/cc-to-me-plus-others + 1 more mid-fire (#1818, 8 total), all read in full; 2 got
full substantive replies with real content (Arch's tension, Exec's Cross-Piper reflection), the
rest got explicit action (epic-file corrections) rather than a reply, which is the right shape when
the action itself is the response. 0 pure-skim.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line.

**Discovered issues filed**: None.
