# Session Log: 2026-09-19-0657-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Opus 5 — observed from harness environment block; PM switch 2026-09-14)
**Date**: Saturday, September 19, 2026
**Start Time**: 6:57 AM PT
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

**First full day of the post-renewal seat.** The session was cleared 2026-09-18 ~22:00 as wave 1 of
the Amber fleet renewal; the arrival block lives at the foot of the 09-18 log, not here.

## Session Objectives

Three carried items, two of them PM-dated:
1. **Bets 001–003 reminder — DUE TODAY**, PM-requested 09-18, plus rollup surfacing for Exec/Janus.
2. **#1744** ruleset migration — PM approved option (2); prepared payload blocked by the permission
   classifier, awaiting PM's choice of path. Not blocked on the decision, blocked on the mechanism.
3. **#1824** legibility check — my predecessor filed it minutes before the clear; I said it was the
   first item on my next fire, so it is.

## Work Log

### START (06:57 fire)

**Step 1 — cron**: `CronList` → exactly one job, `a1a8e2e5`, `27 6,9,12,15,18,21`. Recorded arm-date
09-15 STOP, expiry ~09-22, so I am **~87h out, outside the ~48h proactive-rearm window** — tonight's
STOP re-arm resets it well before the threshold. No action.

**Step 0 — prior day closed properly.** The anchored pattern (column-0, date-shaped) matches
`dev/2026/09/18/2026-09-18-0657-arch-code-log.md`. No retroactive close owed. Worth noting the
anchored pattern earned its keep here: my own 09-18 log now carries a POST-CLEAR ARRIVAL block
*below* the marker, and a looser grep over a log that both closes a day and narrates a renewal is
exactly the shape that false-passes.

**Step 2a — collision check**: `arch` / `claude/arch-cycle`. Pairing matches, but on Model A that
is **permanent by construction and therefore proves nothing** — the real gate is the tmux-side
refusal at standup. Recording it as run, not as evidence.

**Step 2c — cohort-freeze, post-fetch**: `rc=0` but the detector printed **INSUFFICIENT-SCHEDULE**
(1 scheduled fire < 6 in the 4h window) — its own words: *"NOT an all-clear, this window cannot
discriminate."* So: **no freeze signal, and no clear either.** Reporting it that way rather than as
a pass, which is the whole reason the detector prints that line.

**Own tracked-state currency**: `arch-carry-forward.md` ✓ current (1d old against its own 4d claim).
`arch-standing-items.md` is **UNDECLARED** — no checkable claim at all. Honest coverage gap, not a
failure; it is a candidate for frontmatter adoption but I am not scope-creeping into it at a START.

**Belt visibility**: `duty-cycle-freeze-check.sh` header reads `rows=11` (non-zero denominator
confirmed *first*), and no match for `arch`. A measured absence, not an unmatched grep.

### Bets 001–003: PM's requested reminder delivered, and re-sized (07:0x)

PM asked on 09-18 to be reminded **today**, and for it to ride the rollup so Exec — and via Exec,
Janus — sees it. Both halves done: memo to Exec cc PM, and the direct reminder to PM in-conversation.

**The useful finding isn't the reminder, it's the size.** I read all three bet memos rather than
just citing their age, and **Bet 001's actual ask is three bracketed blanks** — buyer, appetite,
kill-date — each with Arch's recommendation already written beside it. "None named" is a *complete
and valid* answer to the buyer field; it converts the bet to labeled speculation or retires the
milestone. Bet 003 is partly pre-filled from PM's own 08-29 answer and needs confirm-or-amend, not
authorship. So the honest framing is **"three fields on one memo," not "three architecture documents
to review"** — and I suspect the perceived size is part of why 20 days passed, since each memo opens
with tripwires and context and the ask sits well down the page.

Named plainly in the memo: *"non-blocking by design" is precisely what let these sit 20 days.* Not a
complaint — the structural point. A label that removes something from the critical path also removes
it from everyone's attention, and nothing in the system was going to surface them. PM asking to be
reminded is the correction, and the dated standing-items row is now the mechanism so it doesn't
depend on him remembering to ask again.

### #1824 legibility check — passes, but it was unowned and unsignalled (07:0x)

Carried commitment from the arrival block ("first item on my next fire"). **Verdict: legible.** The
body carries file:line, all five causes verbatim, the split criterion, the four-bucket table, the
explicit do-not-cite-#1814 warning, and its own `Verified how:` line. A stranger could act on it.

I checked the body rather than the metadata — "2802 chars, `architecture` label, MVP milestone" is
not legibility, and accepting it as such would have been this seat's documented failure mode
(asserting what an artifact contains without opening it) inside the very check meant to catch it.

**Two gaps the check surfaced that the handoff didn't predict:**
1. **#1824 is UNASSIGNED.** A finding on MVP with no owner is the same class of gap as one living
   only in a memo — durable, findable, still nobody's. "It's tracked now" is not "it's owned now."
2. **CXO holds the content but not the issue number.** The 09-15 memo is in `arch/sent/`, but
   nothing in CXO's inbox references #1824. Since #1824's own title says it blocks CXO's copy, they
   should hold the durable reference, not just the thread. Memo sent, cc Lead.

Both memos verified delivered **at trunk** across all 6 paths (`git ls-tree origin/main`), never a
local `ls`.

### Third queue source — I had no criteria line; now I do (07:1x)

Step 2b requires a GitHub criteria line and says an absent one is *a gap to name, not a blocker*.
Arch had none. **Adopted: `label:architecture --state open`** — denominator **9** today.

Opened each rather than reading titles (CXO's 09-12 refinement: a row written from `gh issue list`
is a guess about the issue). 7 are known to me: my own #1723, #1824 just handled, and five dormant
post-MVP items (#1028, #1011, #688, #557, #329, #326). **#1823 is genuinely new** — created
2026-09-18 23:38, *after* my predecessor's clear, so it could not have been in the handoff.

### #1823 — the gate checks one provider while the spend is already multi-provider (07:2x)

The new issue my criteria line surfaced. Read the source rather than the issue's summary of it, and
the picture is **cleaner and cheaper** than the issue frames it.

**The ladder does not know what a provider is.** `resolve_request_api_key` takes
`fetch_stored: Callable[[str], Awaitable[Optional[str]]]` — no provider argument
(`services/llm/request_key.py:394-399`). The Anthropic constraint is *injected*:
`resolve_user_llm_key` hardcodes `"anthropic"` (`web/utils/llm_key.py:117`),
`resolve_user_openai_key` hardcodes `"openai"` (`:189`). **The same generic ladder, instantiated
twice.** So the per-surface principle the issue cites as a thing to decide is already the
implemented shape, and this decision needs no restructuring of the rungs — which materially lowers
its cost and is the single most useful thing to hand PPM before they sequence it.

**The real defect isn't the refusal rung — it's an asymmetry #1819 introduced.**
`expand_llm_key_binding` starts from `resolved` (already Anthropic) and only *then* adds the OpenAI
key (`:157-160`). So: **the gate asks for Anthropic; the spend may be OpenAI.** #1819 widened the
spend without widening the gate. Uniform across all four call sites, not an `/intent` quirk.

Shape: **#1816's family again.** `resolved` carries two meanings — "the credential for this turn"
and "proof the user may spend at all" — and #1819 split those apart in behavior without splitting
the variable.

**Ruled at the right altitude, deliberately.** #1823 is labeled `[PRODUCT]`; the call is PPM's and
the copy is CXO's. I filed the constraint set plus a recommendation (gate on *owns ≥1 spendable
key*, then constrain routing to bound providers), with one condition I'd hold: where a task type
genuinely needs Anthropic, still refuse — **but name the task type, not the vendor.** Today's copy
tells the user they have no key, which is false for the user described.

**Named the hinge I did NOT verify**: whether #1415's provider selection consults the binding or
selects first and looks up the key after. That ordering decides between the options. It is Lead's
to answer and I flagged it rather than reasoning past it — the denominator line on the memo says
3/3 resolution entry points read, **0/1 provider-selection paths traced.**

**Sequencing input for PPM**: #1823 and #1824 touch the same copy surface. #1824 first gives the
honest refusal a bucket to live in; #1823 first will likely invent a fifth bucket informally.

Distributed: memo to PPM + CXO cc PM/Lead (5 paths, verified at trunk), a pointer comment on the
issue itself (artifact record, per the mail-vs-GH-comment norm), and a decisions.log entry — the
structural finding is durable regardless of which way PPM rules, so it should not live only in a
memo. That is the exact gap my predecessor caught in themselves before the clear, applied forward
rather than re-learned.

### #1823 converged in under three hours — and I got a clause wrong (09:57 fire)

**The thread**: my 07:2x lens → CXO's copy half + an independent pre-existing defect (five
vendor-specific strings vs one neutral, same user question answered two ways) → **PPM ruled at
07:25, accepting my §4 recommendation unmodified, holding the refuse-by-task-type-not-vendor
condition as part of the ruling** → **Lead discharged my flagged precondition at 08:32**: selection
consults the binding structurally, so CXO's third state (pass the gate, fail at route) is
unreachable. Four roles, one issue, design-complete before 09:00. Mail: 4 (2 direct read in full,
2 cc; Exec's says explicitly nothing is owed by me).

**And inside that, my own error.** Lead's trace noted in passing that *"Slack already demonstrates
the target shape live — #1822, shipped this morning."* That is incompatible with a sentence in my
memo, and Lead didn't make anything of it — so flagging it was mine to do.

**I claimed the asymmetry was "uniform across all four call sites." It is 2 of 3.**
`response_handler.py:816-828` has a **second arm** fetching the sender's OpenAI key directly — the
exact shape #1823 aims at. And "four" counted `documents.py:36`, an *import*, as a call site.

**No staleness excuse available**: #1822 landed **06:57:34**; I read the file at **~07:20**. The
corrected code was in my working tree while I mis-described it.

**The mechanism, and it generalizes.** I grepped for `expand_llm_key_binding`, got line hits, and
wrote a claim about what those functions *do* from the line numbers — never opening the Slack
function. The OpenAI arm sits **twenty lines below my own citation**.

> **The rule this sharpens** — a real refinement, not a restatement. The standing rule I inherited
> is *"asserting X covers Y requires quoting X inline."* I thought I complied, because I cited file
> and line for every site. **A grep line-hit is a pointer, not a quote.** It tells you a string is
> present, says nothing about what the function does, and is *specifically blind to the branch below
> the hit.* Operative form: **a claim about what N sites DO requires opening N sites.**

**Direction matters, and it was predicted.** "Uniform across surfaces rather than an `/intent`
quirk" made the finding sound larger than it was. The handoff's failure-mode #2 says watch the
*supporting* clause, not the claim — *"my claims survive scrutiny; my rationales are where this
lives."* The ruling held. The scope clause under it didn't. First live confirmation of an inherited
failure mode, on day one of carrying it.

**Corrected at three surfaces, deliberately**: the #1823 issue comment (where an implementer looks),
an **inline marker on the original decisions.log line** plus a full correction entry, and a memo to
PPM/CXO/Lead cc PM. Appending only where history lives while the read surface stays wrong is
failure-mode #4 — repeating it in the same week would be worse than the original error.

**Concrete cost if uncorrected**: an implementer reads "all four call sites," goes to
`response_handler.py`, and either wastes the trip or collapses a working two-arm path back into the
single-arm shape #1822 had just removed.

### Branch two is permanently empty — conditional on BYOC, and nothing enforces the conditional (12:57 fire)

Mail: 3 (2 direct read in full, 1 direct ack). CXO closed #1823's copy half and **traced my own held
condition to a false antecedent**: no task type is Anthropic-tuned, `resolve_model` is total, task
type selects a *tier* not a *provider*. PPM ruled branch two out of scope. Both correct.

**But CXO explicitly flagged one question for PPM and me and declined to answer it**: is
provider-agnosticism *deliberate and load-bearing*, making branch two permanently empty rather than
merely empty today? PPM's ruling leaned on `config.py:74`'s comment. **A comment is not a
commitment**, so the question was still open and it was mine.

**Ruled, in three parts:**
1. **Load-bearing, but DERIVED from BYOC** — not an independent preference. A provider-constrained
   task type would lock out any user whose only key is another vendor's, which is #1823 generalized
   from one endpoint to the whole task surface. PM's #1812 ("we don't resell tokens") makes BYOC the
   model, so provider-agnosticism is what keeps BYOC *coherent*.
2. **So yes — permanently empty, conditional on BYOC. The conditional is the answer**, not a hedge
   appended to it.
3. **But it is NOT law.** I checked instead of assuming: it appears in **neither** ESSENCE's five
   standing rules **nor** its seven commitments. Protected today by one comment plus the incidental
   fact that all three providers define all three tiers.

**Deliberately did NOT propose an ESSENCE amendment.** First instinct, and wrong: the law already
exists as BYOC, so a new commitment restates an existing one at lower altitude — corpus growth that
makes law harder to read without making it more binding, spending a PM ratification gate on a
restatement. **What's missing is the mechanism, not the law** (m-41). Filed **#1829** for a ratchet,
with the vacuity trap named in the issue: `assert resolve_model(p,t) is not None` **passes
vacuously** given the fallbacks — the #1642 test-that-cannot-fail shape.

**Second finding, and it only appeared because I read the function rather than CXO's (accurate)
summary of it** — my own one-fire-old rule applied to myself: **`resolve_model`'s totality is silent
fallback, not exhaustiveness.** An unknown `task_type` silently becomes the **heavy** tier (a *spend*
defect under BYOC — the user pays); an unrecognized provider silently returns **OpenAI** models
(latent at 3/3, live at a fourth). Honest-empty family again: a `.get()` with a default can't
distinguish *absent* from *found the default*.

⭐ **The consequence worth carrying**: the property that makes branch two empty and the property that
silently mis-tiers a typo **are the same mechanism.** Hardening one without the other is incoherent.

**Also: the correction compounded.** Lead's ack and CXO's memo both report that this morning's
*grep-line-hit-is-not-a-read* rule changed their behavior — CXO was one step from filing
"structurally task-blind" off a partial trace and stopped. That rule exists because I broke it four
hours earlier. Said so plainly in the memo rather than accepting it as process: the most useful
thing I produced today, I didn't produce — the failure did.

First substantive read of **ESSENCE.md** (§§118-160), which I had deferred at arrival and flagged as
unread. It was load-bearing exactly here, and the deferral cost nothing only because the question
arrived after it.

### The filing convention PPM has fixed three times this week is half-unwritten (15:57 fire)

Mail: 8 — **1 direct** (PPM's #1829 ack: board-fixed, MVP milestone, folded into **epic 5**, the
honest-empty family, which is the home I'd named myself), **7 cc** on the HOST/Lead/PM alpha-droplet
thread. Criteria line: 10 now (#1829 is mine, filed last fire). Inbox drained to zero at trunk.

**On the cc thread — deliberately adding nothing.** The droplet-parity question (alpha live and
healthy, but nobody has read the live box's *version*; no deploy since July) is exactly the class
CLAUDE.md warns about — branch staleness and deployed-artifact staleness being two different
numbers — but **Lead named that distinction themselves**, PM ruled hold-and-fix-now, and an upgrade
plan is already on `origin/main`. Nothing architectural is owed. Manufacturing a contribution to a
well-run thread would be busywork with an Arch letterhead.

**The real work came out of PPM's ack.** They noted #1829 was the *third* filing-convention miss
this week (#1824, #1825, #1829) and fixed the board entry by hand. **Three in a week from three
authors is a mechanism gap, not three lapses** — so I went and read the mechanism instead of
resolving to be more careful.

**Two distinct causes, and only one is anyone's fault:**
- **Mine was mine**: I called `gh issue create` directly rather than using `piper-draft-issue`. The
  skill *does* carry milestone (lines 42, 66, 147, 165). I bypassed it and lost what it holds. Owned.
- **The board half isn't in the skill at all.** Grepped for `--project` / `item-add` /
  `project-add` / `Product Backlog`: **0 hits.** Its `gh issue create` blocks end at `--body-file`;
  the only post-create step is an optional epic-link comment. **An author who follows the skill
  perfectly still produces an off-board issue with no Status.**

That's **m-35 asymmetric discipline** — the creation half specified, the cleanup half not — and the
unspecified half silently becomes one person's recurring manual chore. Here that person is PPM,
three times in five days, and **the correction doesn't propagate to the next author**, which is
precisely why it recurred across three different people.

**Declined to edit the skill myself, and said why rather than going quiet**: I don't know the
canonical project number, Status option set, or whether the target column varies by milestone —
guessing those into shared infrastructure propagates my guess to every future author, which is worse
than the gap. Plus CLAUDE.md's Projects-v2 footgun (the field-level full-replace that wiped 1175
items) means the canonical board-mutating snippet should come from the board's owner. Offered to
make the edit and **verify it behaviorally** — file a scratch issue through the amended skill,
confirm it lands with the right Status, close it — so the fix is tested rather than described.

**Named the limit of my own story**: I did *not* check whether #1824's and #1825's authors used the
skill or bypassed it as I did. If they bypassed it too, the skill gap is real but isn't the cause of
those two, and the shared cause is something else. Flagged rather than letting a clean narrative
rest on an unchecked assumption.

### Skill edit made and behaviorally verified; #1812 step 5 unlocks a consumer enumeration (18:57 fire)

Mail: 4 — **2 direct** (PPM's canonical board values; Lead's four-PM-rulings relay), **2 cc**
(Exec's epic ruling relay; HOST's ack). Inbox drained to zero at trunk. Criteria line: 10, no new.

**PPM took up the offer and sent verified values**, with the count worse than I knew: **six**
board-add fixes in three days (#1824, #1825, #1829, #1827, #1828, #1830), the last three filed by
three *different* roles inside one three-hour window. That settles the limit I flagged last fire —
the recurrence is not about who bypassed the skill, it's the skill's missing half.

**Edit made** (`piper-draft-issue` Step 5b, mandatory, placed after creation and before the optional
epic-link comment) with the six-in-three-days history stated *in the step itself* — a step carrying
its own incident survives edits that a bare command doesn't. Inlined the 🔴 per-item-not-field-level
warning with the 1175-item number: that footgun is in CLAUDE.md, but the person copy-pasting from a
skill is exactly the one who hasn't read it that day.

**Verified behaviorally, which was the whole point of the offer.** Filed scratch **#1833** → board
(`item-add` ✅) → `updateProjectV2ItemFieldValue` ✅ → **read back `Status` = `Product Backlog`** ✅ →
closed. Option count **6 before and 6 after**. I read the count on *both* sides deliberately: "6
after" alone can't distinguish *unchanged* from *coincidentally still 6*. And I read the field back
rather than trusting the mutation's success response — **a mutation returning an id proves it ran,
not that it set what I meant.**

**Named a tension instead of stepping around it.** Exec relayed PM's ruling whose closing line is
*"let's not overindex on our filing rules"* — and I made a filing-rule edit hours later. My read:
compatible, because (1) PM's ruling is scoped to *epic taxonomy*, (2) the Product-Backlog convention
was separately ratified 09-12 so I'm mechanizing an existing rule rather than inventing one, and (3)
the edit **reduces** what a human must remember. ⚠️ **But it would be convenient for me to
rationalize that**, so I put the reasoning in the memo and cc'd PM: one line to revert, and I'd
rather be corrected cheaply than hold a quiet disagreement. *(This is failure-mode #3 inverted — the
risk isn't lowering my own bar, it's waving away someone else's constraint because it's inconvenient.)*

**#1812 step 5 unblocked — and it's the same shape as my own #1810 error, in the other direction.**
My rule from that one: *deleting a write requires enumerating that slot's readers.* Retiring a
*concept* is the same obligation, so I enumerated the operator seam's **6 consumer sites** for Lead
before build time rather than after — including the easily-missed second injection at
`llm_key.py:191` (the `/documents` ladder, not the `/intent` one).

⚠️ **The load-bearing one**: the ladder's third rung returns **`None`**, and `None` *means* "use the
server's key." If the seam retires while that rung stays reachable, `None` becomes a value with no
defined meaning — **the honest-empty family for the fourth time in one week** (#1816, #1815 Gap 2,
#1829, now this). Recommended the rung go away entirely rather than survive behind a
permanently-False checker: **a gate that can never open is dead code that reads as policy.**

**And Lead's #1823 trace is stronger than they wrote it**: their §2 caveat ("where the server DOES
still hold keys") loses its premise under PM's ruling, so selection's candidate set *is* the binding
unconditionally, not "on a BYOC deployment." Flagged so a correct trace doesn't quietly carry a dead
qualifier into next week.

**Third role today on the same shape.** Lead reported the #1785 "deterministic and free" claim came
from reading the job's *comment* instead of the marks — my pointer-is-not-a-read rule, biting their
artifact eight hours after I wrote it because I'd broken it first. Me, CXO, Lead in one day. **That's
not three careless people — it's a tooling affordance**: `grep` and a docstring both return something
that looks like an answer, fast, while reading actual behavior is slower and feels redundant. Told
Lead it may deserve a methodology entry rather than living in three session logs.

### STOP — day close (21:57 fire)

**Last scheduled slot confirmed by cron arithmetic**, not by feel: `27 6,9,12,15,18,21` → next match
is 06:27 tomorrow, a different calendar date. Cleanup dry-run found nothing (no cycle-logs >7d, no
`.tmp` >1d). Mail drained to zero at trunk on this fire as on all five before it.

**#1818 structural half ruled rather than banked.** It arrived at the STOP fire, and "it's the last
fire" is not a named trigger — it's the bite-sizing antipattern wearing a clock. I sized it first (two
greps), found it converged, and ruled. The one thing I'd have deferred for — a binding design call
made carelessly at the end of a long day — didn't apply, because the answer turned out to be *don't
build anything*: `ActionDisposition.CANONICAL` already expresses "spends nothing," and a hand-
maintained exemption list would duplicate a registry that exists, which ESSENCE's own derive-don't-
hand-maintain rule forbids verbatim. Declining to build is cheaper to get right at 22:00 than
designing would have been.

**The ordering finding is the part that would have cost a build cycle**: the gate fires *before*
`process_intent`, but disposition is determined by the pre-classifier *inside* it — so the gate
structurally cannot see what it would condition on. Cure verified at the hinge (`PreClassifier` is
regex, zero LLM calls), so moving it ahead of the gate spends nothing.

**And it unblocked CXO's copy by correcting their framing**: a CANONICAL greeting never reaches the
floor, so the `due_reminders` directive shape they'd identified as "already ships" is not on this
path. The string is fixed text in the canonical handler. Their pattern was real; it just isn't
reachable from here.

## Day arc — 2026-09-19, first full day of the post-renewal seat

Six fires, six heartbeats, **inbox drained to zero at every one**. Criteria line 9 → 10.

| what | outcome |
|---|---|
| **Bets 001–003** (PM-dated) | Reminder delivered + **re-sized**: Bet 001 is three bracketed blanks, and "none named" is a *complete* answer. Rollup-surfaced to Exec→Janus. |
| **#1824 legibility** (carried promise) | Passed on body. Surfaced two gaps the handoff didn't predict: **unassigned**, and CXO held the content but not the issue number. |
| **Criteria line** | Arch had none — "drained" was being claimed against 2 surfaces, not 3. Adopted `label:architecture`. **Found #1823 on its first run.** |
| **#1823** | Lens → **PPM ruled it unmodified** → CXO copy + an independent defect → **Lead discharged my flagged hinge**. Design-complete in <3h. |
| **My scope error** | Claimed "uniform across four call sites"; it was 2 of 3. Self-flagged, corrected at **three** reading surfaces. Earned: *a grep line-hit is a pointer, not a quote.* |
| **#1829** | Filed: provider-agnosticism is derived-from-BYOC, load-bearing, **absent from ESSENCE** — plus `resolve_model`'s totality is silent fallback, not exhaustiveness. |
| **piper-draft-issue** | Found the board step missing entirely (6 misses/3 days, 4 roles). Routed → PPM sent values → **edit made and behaviorally verified** via scratch #1833. |
| **#1812 step 5** | Enumerated the operator seam's **6 consumers** for Lead before build time — my own #1810 rule, applied in the retire-a-concept direction. |
| **#1818** | Structural half ruled (above). |

**The day's real theme, and I didn't choose it**: three roles independently hit the same failure
shape — me on a grep line-hit, CXO one step from a wrong "task-blind" filing, Lead on a job's comment
instead of its marks. CXO's mirror formulation (*a grep MISS is not an absence*) is better than
mine. Suggested to Lead it deserves a methodology entry rather than three session logs.

## Memory & briefing surfaces referenced this session

**Referenced** —
- `docs/handoff-arch-2026-09-18.md` — the failure-mode section was load-bearing all day; instance #1 fired for real within four hours.
- `dev/active/arch-carry-forward.md` + `arch-standing-items.md` — read at every fire; the #1810 reader-enumeration rule drove the #1812 work.
- `CLAUDE.md` — Projects-v2 footgun (inlined into the skill edit), mail-vs-GH-comment norm, `origin/production` warning, explicit-paths discipline.
- `.claude/skills/duty-cycle-tick` — the procedure; its Step 2b third-queue-source rule is what produced #1823.
- **`docs/internal/architecture/ESSENCE.md`** — first substantive read (§§118-160). Load-bearing twice: the standing-rules check for #1829, and derive-don't-hand-maintain for #1818.
- `decisions.log` — read for format, written five times.
- `.claude/skills/piper-draft-issue` — read, found the gap, edited.

**Loaded but not referenced** — `BRIEFING-ESSENTIAL-ARCHITECT.md` (still unread; flagged at arrival and not yet needed), the reorientation plan, `mvp-epic-order`, the cross-pollination brief.

**Wanted but not found** —
1. **A cohort-wide index of "which invariants are law vs. merely true today."** #1829 exists because provider-agnosticism is load-bearing yet absent from ESSENCE, and I could only establish that by reading ESSENCE end-to-end. A derived list of enforced-vs-asserted invariants would have made it a lookup.
2. **A criteria-line registry.** I found 5 of 11 roles have one in their carry-forward, but HOST's lives in the skill — so I could not state a true denominator for a cohort-wide property.

## Sign-off

- Cron: **re-armed via delete-then-create-then-verify** — `a1a8e2e5` → **`66baedd3`**, same expression, `CronList` confirms exactly one job. Expires ~2026-09-26.
- Registry row updated with the new job id (8 columns intact, 88 rows, no neighbour disturbed).
- Carry-forward rewritten for tomorrow.
- `git status` clean / `git log origin/main..HEAD` empty, with the `rev-parse` guard — output in the wrap commit's verification run.

<!-- DAY-CLOSED: 2026-09-19 -->
