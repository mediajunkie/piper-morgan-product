
### 09:52 fire — alpha access unblocked, a hard signup-wizard bug found and reported first, #1859 traced and visually captured

Lead's reply finally landed: an alpha invite minted, target DB verified against the token file's
own read lines before touching it. Masked form (`7TFM…2DHX`) cross-checked byte-for-byte against
the actual token before use.

**Redeeming it hit a real production bug before I ever reached account creation.** `/setup` Step 1
("System Check") fails hard for every new user — `Continue` genuinely `disabled: true`, not a
render glitch — because `POST /api/v1/setup/check-system` returns 403. Traced to source
(`web/api/routes/setup.py:294`, the `require_setup_incomplete` dependency): it's the real #1504
security fix working correctly (setup IS complete, alpha has real users), and its own detail
message is specific and helpful — but **that message never reaches the caller**. What I actually got
back was a different, generic 403, meaning something upstream is swallowing the real detail and the
frontend's fallback UI renders it as "Docker/PostgreSQL/Redis offline, run docker compose up -d" on
a hosted production instance. **Both known entry paths lead to the identical dead end** (fresh
`/setup`, and the login page's own "Create your account" link) — confirmed independent of my
browser via a bare `curl`, no cookies. **This blocks every new user on alpha right now**, not just
me — reported urgently to Lead cc PM/Host/CXO before doing anything else, since it's directly
relevant to the planned alpha-tester rollout.

**Then unblocked myself the legitimate way, told rather than done silently**: `/create-user` (line
847) carries no such dependency — a separate, ungated route. Redeemed the invite there directly via
the real API (not a DB insert, same discipline as the existing `web-browser-lane` precedent),
login-verified end to end (same `user_id` both times). Credentials saved to
`/Users/xian/.piper-shared/web-agent-alpha-credentials.txt`, mode 600. ⚠️ Hit and fixed two of my
own mistakes en route: assumed env vars would persist across separate Bash tool calls (they don't —
only cwd does; had to do secret-load-and-use atomically in one call), and assumed `/auth/login`
took a JSON body like `/create-user` when it actually wants `Form(...)` fields — found by reading
the route signature, not by guessing again. Raw token and password never printed to any output,
memo, log, or file outside the two intended 600-mode credential locations.

**#1859 (white-flash on chat switch) — traced with direct evidence, not inference.** Confirmed the
mechanism three ways: an injected DOM marker was wiped by the switch (full teardown, not a patch),
`document.readyState` cycled `loading→complete`, and real `framenavigated`/`load` events fired —
**this is architecturally a full page navigation**, answering CXO's design question directly: not
miswiring, working as built. **Captured the flash itself**: timed screenshots at 50/150/300ms show
a completely blank viewport at 50ms, fully redrawn by 150ms — PM's exact description, on camera.
**Root cause, from the network waterfall**: every switch re-fetches all **32 static assets fresh**
(14 CSS, 17 JS, the logo), **zero 304s, zero cache-control headers** — extends Lead's own 09-23
header finding (etag-but-no-cache-control) with the missing piece: nothing is cached client-side at
all, so every switch pays the full 32-asset cost before first paint. Zero console errors — rules
out Lead's hypothesis 1 (a JS error breaking partial nav). Flagged the one real gap: Playwright's
cross-navigation cache behavior in headless mode isn't independently verified against a real
browser session, so the "why zero 304s" causal read carries that caveat explicitly.

**Render sweep**: Settings and Files clean (0 errors, 0 4xx). Home page: **intermittent 503s on
static JS assets, 2 of 4 independent fresh loads**, different files failing each time — reproduced
before reporting, not filed on one hit. Filed as **#1874** (separate follow-up sent once it had a
number, since the original report said "not filing yet").

**Test-card rows 5/6**: both blocked on the same gap — this account has no LLM key, and none is
provisioned for me to add (checked `cred.sh`, nothing exists). Confirmed rather than assumed: sent
row 6's exact command and read the real reply — a clean, honest *"Piper runs on an LLM key of your
own... add one in Settings"*, not a bug. Row 5's Settings→Preferences timezone control wasn't found
under current section names; not chased further since it's blocked on the same key gap regardless.

**Cleaned up all scratch** (Playwright scripts, screenshots, saved session state) once findings were
reported — nothing sensitive left on disk outside the two 600-mode credential files.

**Drained**: mail **1 direct** (Lead's ready-notice, worked substantively — the fire's real content)
· criteria line unchanged from earlier check (website 0, product 0) — **discovered work filed: one
GitHub issue, #1874** (the wizard blocker was reported by mail, not filed separately, since Lead
owns that fix directly and already has full reproduction). Carry-forward refreshed (stale cron
reference, two days behind). Back to idle, cron armed.

### 12:52 fire — #1875's own thread resolved, verified live rather than trusted

**HOST filed #1875** for the wizard blocker (checked the tracker first, none existed) and recorded
the `web-agent` identity on the roster per #1344. **CXO independently found the exact frontend
mechanism**: `web/static/js/setup.js` never checked `response.ok` before destructuring any response
as the success shape — a 403 error body's absent fields all read as falsy, producing the false
"everything is down" display for *any* non-2xx response, not just this specific 403. Answered the
one thing my own URGENT memo had flagged as unverified.

**Went to verify CXO's source read and found it already superseded** — Lead's fix (`ce0be2b41b`)
had landed in the minutes between CXO's memo and my checking it: a `#1875`-tagged block doing
exactly what CXO asked for, plus two more layers per the commit message (the app-level 403 handler
keeps the real detail; `check-system` reclassified as a READ exempt from the #1504 write-lockout
entirely, rather than papering over the display). Neither memo was wrong — the code just moved
between writing and reading.

**Verified live rather than trust the commit message, same discipline as every other finding this
week**: raw `curl` against the exact endpoint that returned the generic 403 before now returns
**200** with the real service-check payload. Full wizard walkthrough in a **genuinely fresh,
cookie-free Playwright session** (not my logged-in `web-agent` session — the actual new-user path
that was broken): Step 1 shows real ✓/✗ per service, `Continue` is visible and enabled, clicking it
**advances to Step 2**. Deliberately didn't re-run account creation itself — no need to spend a
second invite token confirming a mechanism that isn't in question.

Confirmed to the thread. Nice shape to the whole arc: my repro + root cause, CXO's independent
frontend read, HOST giving it a tracked number, Lead fixing all three layers together, me
live-verifying the result — six roles, one bug, closed same-day.

**Drained**: mail **2 direct/cc, both on a thread I opened** — worked substantively (verified
CXO's claim against live current source, confirmed the fix end-to-end in a fresh session, reported)
· criteria line **website 0, product 0**. Back to idle, cron armed.

### 15:52 fire — Lead's fixes for #1875/#1874/#1859's network half; two re-measures done honestly

Lead shipped fixes for all three threads within the hour of my morning reports: **#1875** (three
stacked causes — the write-lockout wrongly gating a read route, the app-level handler swallowing
specific 403 details, and CXO's frontend `response.ok` finding, all fixed together), **#1874**
closed (Fly's default 25-connection limit hitting my 32-asset burst → 503s; a second layer where
`UsageCapMiddleware` was counting static fetches against the chat rate budget → 429s once the first
was fixed — both root-caused and fixed), and **#1859's network half live** (versioned assets get
`Cache-Control: immutable` for a year). CXO separately confirmed the shipped fallback copy is
correct and named one small, explicitly-not-urgent residual (a 403 title that outlived the specific
gate that used to justify it) — agreed and acknowledged, since CXO's memo had me as a direct
recipient, not just cc.

**Did both re-measures Lead asked for, and reported precisely rather than round up.**

**#1859**: network layer confirmed exactly as predicted — all 32 assets now carry
`Cache-Control: public, max-age=31536000, immutable`, 15 of 32 came back 304 in this run. **But the
visual flash is not resolved.** Same screenshot sequence with a warm cache: 50ms now shows a
designed loading-transition (gradient+spinner, better than before), but **150ms is still fully
blank**, and full settle took ~300ms — longer wall-clock than the original ~150ms cold measurement.
Per Lead's own stated fork ("if the blank frame is now one paint or less, close; if a flash persists
with zero network cost, next lever is shell-preserving navigation"), this lands on the second
branch — reported as such rather than letting "network fixed" get rounded into "#1859 closed" when
the actual visual complaint PM filed isn't resolved. Flagged the sample-size gap honestly (one run
per cache state, not a distribution).

**Wizard render walkthrough**: pushed further than the earlier account-creation confirmation,
without spending a second invite — confirmed Step 1→2 render live. Hit a real, honest depth limit:
Step 2's `Continue` is genuinely `disabled: true` without an LLM provider selected (correct, not a
bug), and I won't fabricate a key or borrow anyone else's to force past a gate working as designed.
⚠️ Caught and corrected my own bad read mid-check: my first quick test said "Continue enabled
without a key" — it had grabbed a stale hidden button left over from Step 1 (multiple steps' buttons
coexist in the DOM, several hidden at once), not the real Step 2 control. Re-checked by filtering to
currently-visible buttons only before trusting the result. Reported the genuine limit rather than
force past it: full wizard-UI coverage past Step 2 needs either a provisioned key or a second
invite token — both PM's calls, named as such.

**Drained**: mail **1 direct on #1875's confirm thread + 1 more direct from Lead's fix report**,
both worked substantively (re-measured live, corrected my own earlier mistake mid-check, reported
precisely) · criteria line unchanged. Back to idle, cron armed.
