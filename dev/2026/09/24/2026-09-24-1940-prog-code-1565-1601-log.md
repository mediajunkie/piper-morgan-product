# 2026-09-24 1940 — Coding Agent (prog) — #1565 / #1601

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated: Sonnet)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (shared — do NOT commit/push; Lead reviews and commits the explicit file set)

## Scope given

- #1565: "what day is today?" rendered a real all-day multi-day calendar event's TITLE as if it were a place ("You're currently in: Sandra and Brian"). Fix the render in `_handle_temporal_query`.
- #1601: `effective_user_id = str(ctx.user_id) if ctx else user_id` at `services/intent/intent_service.py` stringifies a real-but-None `ctx.user_id` into the literal `"None"`, which #1532 uses as an ownership principal.
- Own: the temporal render in `services/intent_service/canonical_handlers.py`, the one line in `services/intent/intent_service.py`, and my tests. Do NOT touch `services/feed_factory*` / `WorkItemProvider`.

## #1565 — all-day/timed current-meeting render

**Reproduced (before fix)**, via a scratch script driving `_handle_temporal_query` with a hand-built `temporal_summary["current_meeting"]`:

- All-day 2-day block (`is_all_day=True`, start `2026-08-10`, end `2026-08-12` exclusive):
  `'...at 7:42 PM PDT. ... You\'re currently in: Sandra and Brian'`
- Timed current meeting (`Standup`, no end shown at all):
  `'...at 7:42 PM PDT. ... You\'re currently in: Standup'`

Confirmed the bug is specific to the DEFAULT/"Standard detail" branch of `_handle_temporal_query` (`services/intent_service/canonical_handlers.py`, was ~line 379-386) — the EMBEDDED/GRANULAR branches already route through `_meeting_face` (which handles `is_all_day`) and never render "currently in".

**Fix**:
- Added `CanonicalHandlers._all_day_through_label(item)` (static helper near `_meeting_face`): given a processed event's `start_time`/`end_time`, returns the weekday name of the all-day block's last covered day (adjusting for Google's EXCLUSIVE all-day `end` date), or `None` for a single-day block.
- Rewrote the DEFAULT branch's current-meeting clause:
  - all-day → `"Today is an all-day block: {title}{through_note}."` where `through_note` is `" (through {weekday})"` only for genuinely multi-day blocks. Marked `# CXO copy pass pending (#1565)` since the exact "through Thursday" wording is my choice, not literally CXO-ruled (PM's own confirming comment on #1565 said "Today (all day): Sandra and Brian — never 'You're currently in:'"; I used the dispatch brief's fuller "Today is an all-day block…(through X)" phrasing since it captures the multi-day fact PM specifically confirmed).
  - timed → `"You're in a meeting right now: {title} (until {until_face})."` via `_meeting_face(current_meeting, user_tz, key="end_time")`. Also marked `# CXO copy pass pending (#1565)`.
- Neither branch ever again says "You're currently in:".

**Reproduced (after fix)**:
- All-day multi-day: `...Today is an all-day block: Sandra and Brian (through Tuesday).`
- All-day single-day (new edge case checked): `...Today is an all-day block: Company Holiday.` (no spurious "(through …)")
- Timed: `...You're in a meeting right now: Standup (until 3:30 PM PDT).`

**Tests** (red-first): added `TestCurrentMeetingFaceIsNotAPlace` to `tests/unit/services/intent_service/test_agenda_render_faces_1576.py` (the existing file exercising `_handle_temporal_query`'s meeting faces) — 9 new tests: multi-day all-day, single-day all-day (no spurious "through"), timed current meeting, plus 3 direct unit tests of `_all_day_through_label`'s edge cases (missing end_time, unparseable dates, single-day). Confirmed red pre-fix by temporarily reverting the line and re-running (`AssertionError: ctx.user_id=None must yield a None principal, got 'None'` — that's the #1601 repro below; for #1565 I confirmed red via the scratch script's raw output, quoted above, before any test existed).

Files: `services/intent_service/canonical_handlers.py`, `tests/unit/services/intent_service/test_agenda_render_faces_1576.py`.

## #1601 — `effective_user_id` None-vs-"None"

**Fix** (`services/intent/intent_service.py:756`, in `process_intent`):
```python
effective_user_id = str(ctx.user_id) if ctx and ctx.user_id is not None else user_id
```
(was `str(ctx.user_id) if ctx else user_id`). Preserves #1532's fail-closed principal semantics — read `services/domain/models.py`'s `RequestContext` (frozen dataclass, `user_id: UUID` typed non-Optional but with no runtime `__post_init__` guard — `from_jwt_and_request` validates, but a hand-built ctx doesn't have to go through it) and the #1532 test suites before writing.

**Red-first confirmation**: temporarily reverted the line to the old form, ran the new test — failed with `AssertionError: ctx.user_id=None must yield a None principal, got 'None'`. Restored the fix; re-ran — passed.

**Tests**: new file `tests/unit/services/intent_service/test_effective_user_id_1601.py`, 3 tests. Technique: patch `IntentService._process_intent_internal` (the very next call after `effective_user_id` is computed, and — unlike the #913 instrumentation block above it — NOT wrapped in a swallowing `except Exception: pass`) to raise a sentinel, so the actual `effective_user_id` value reaching downstream can be inspected without mocking the whole pipeline.
- `test_ctx_with_none_user_id_yields_none_principal_not_the_string_none` — pins the fix.
- `test_ctx_with_real_uuid_still_stringifies` — pins the unchanged normal path.
- `test_no_ctx_falls_back_to_the_legacy_user_id_param` — pins the no-ctx fallback is untouched.

Files: `services/intent/intent_service.py`, `tests/unit/services/intent_service/test_effective_user_id_1601.py`.

## Collateral: architecture-enforcement census line numbers

`tests/test_architecture_enforcement.py::TestGuidedProcessStartersRegistered1867::test_census_matches_measured_sites` hardcodes exact `(file, line_no, process_type)` tuples for guided-process session-start call sites. My edits shifted line numbers in `canonical_handlers.py` (+57, post `ruff format`) and `intent_service.py` (+11) around unrelated call sites already in `KNOWN_SITES`/`KNOWN_DARK_SITES` — same five sites, no new/removed site. Re-measured via the test's own scan logic and updated both frozensets in the same commit (test's own instruction). Updated file: `tests/test_architecture_enforcement.py`.

## Sweeps / gates run

- `venv/bin/python -m pytest tests/unit/services/intent_service/test_agenda_render_faces_1576.py tests/unit/services/intent_service/test_effective_user_id_1601.py tests/unit/services/conversation/test_conversation_ownership_1532.py tests/unit/web/api/routes/test_intent_conversation_ownership_1532.py tests/unit/test_census_principal_blind_tests.py tests/intent/test_handler_error_handling.py tests/unit/services/integrations/test_slack_subcommand_intents_1436.py tests/live/test_session_ownership_1532_live.py tests/integration/services/test_conversation_repository.py tests/unit/web/api/routes/test_keyless_exchange_persists_1838.py tests/test_architecture_enforcement.py -q` → **184 passed, 1 skipped, 1 xfailed**.
- `tests/e2e/test_original_message_1460_e2e.py::test_setup_request_reaches_setup_flow` FAILS in this environment — `all_llm_providers_failed` / Anthropic 401 "API key is invalid" → LLM classification falls through to floor routing. Not caused by my diff (unrelated to temporal rendering or `effective_user_id`; it's a keychain-credential/environment issue for this seat, matching the class of problem CLAUDE.md documents under "Restarting the server..."). Reported, not fixed — out of scope and pre-existing.
- `scripts/run-sweep.sh smoke` → **566 passed, 1 skipped**.
- `scripts/run-sweep.sh ratchets` → pytest ratchets: **71 passed, 1 xfailed**. mypy gate: on first run, `mypy_misc: 87 > ceiling 77` — investigated, and all 10 excess `[misc]` errors were in `services/radar/feed.py` and `services/standup/assembler.py` ("Assignment to variable 'e' outside except: block"), files explicitly out of my scope (another lane's uncommitted WIP in this shared worktree, evidenced by concurrent session logs `2026-09-24-1941-prog-code-1592-log.md` / `2026-09-24-1944-prog-code-1587-log.md` and `tests/unit/services/radar/test_workitem_outcome_1587.py` appearing mid-session). Re-ran after that lane's fix landed: **mypy gate: all 24 ratcheted codes at ceiling (total=1121; misc=77)**. Not my regression, not my fix — flagged transiently, resolved by the other lane before I finished.
- `venv/bin/ruff check` on my touched files → all clean. `venv/bin/ruff format --check` initially flagged `canonical_handlers.py` and `test_effective_user_id_1601.py`; ran `ruff format` on both (cosmetic only — wrapped one long f-string/assert), re-verified clean, then re-measured/updated the census line numbers again (ruff format shifted `canonical_handlers.py` by 2 more lines: 4878→4876) and re-ran the full test set — all green.

## Verified how

- **#1565**: method — hand-built `temporal_summary` fixtures driven through `CanonicalHandlers._handle_temporal_query` directly (both an ad-hoc scratch script and the 9 pinned pytest cases in `test_agenda_render_faces_1576.py`), asserting on the actual returned `result["message"]` string. Layer — the handler's real render logic and its real helper (`_meeting_face`, new `_all_day_through_label`), not a mocked formatter. Denominator — 3 render shapes covered (multi-day all-day, single-day all-day, timed-with-end), matching the two shapes named in the issue plus the single-day edge case I added; did not sweep EMBEDDED/GRANULAR (out of scope — they don't have this bug, confirmed by reading their code paths, not just assumed).
- **#1601**: method — direct pytest exercise of `IntentService.process_intent` with a hand-built `RequestContext(user_id=None, ...)`, inspecting the real `effective_user_id` value passed to `_process_intent_internal`; confirmed red pre-fix (reverted the line, re-ran, got the literal `'None'` failure) and green post-fix. Layer — the actual expression inside `process_intent`, not a reimplementation of its logic. Denominator — 3 of the 3 cases named in the acceptance criteria (None ctx.user_id → None; real UUID ctx.user_id → stringifies; no-ctx fallback unchanged); did not implement the broader "sweep for other str(...) coercions" acceptance item (that's a class-wide sweep, explicitly out of my dispatched scope — reporting only, not fixing).
- **Sweeps/gates**: method — ran the exact commands given (`scripts/run-sweep.sh smoke`, `scripts/run-sweep.sh ratchets`, `venv/bin/ruff check`/`format --check`), quoting actual terminal output above, not recalled from memory. Layer — full pytest collection + the pinned mypy-gate venv, not a partial subset. Denominator — smoke sweep's own selected set (566 tests, 14263 deselected per its own marker filter) and the ratchet gate's full 24-code mypy census; both are the tool's own stated totals, not a manual sample.

## Files touched

- `services/intent_service/canonical_handlers.py` — #1565 fix + `_all_day_through_label` helper.
- `services/intent/intent_service.py` — #1601 fix (line ~756).
- `tests/unit/services/intent_service/test_agenda_render_faces_1576.py` — 9 new #1565 tests.
- `tests/unit/services/intent_service/test_effective_user_id_1601.py` — new file, 3 tests.
- `tests/test_architecture_enforcement.py` — census line-number re-measurement (collateral, same commit as the line-shifting change per the test's own instruction).

## Discovered issues (report only, not filed — Lead's call)

- `tests/e2e/test_original_message_1460_e2e.py::test_setup_request_reaches_setup_flow` fails in this worktree/seat due to an invalid Anthropic API key reaching the LLM client (401), unrelated to my diff. Pre-existing environment issue for this seat, not a code regression.
- `services/radar/feed.py` / `services/standup/assembler.py` briefly carried a real mypy `[misc]` defect ("Assignment to variable 'e' outside except: block" — reading a variable after its except block ends) from concurrent uncommitted work in this shared worktree; resolved by that other lane before I finished, confirmed via the final `run-sweep.sh ratchets` output (misc=77, at ceiling). Noting only because it transiently blocked my ratchet run — no action needed from me, not mine to fix.

## Handoff note

Did NOT commit or push (per instructions — shared worktree, Lead commits). My clean, coherent file set:
- `services/intent_service/canonical_handlers.py`
- `services/intent/intent_service.py`
- `tests/unit/services/intent_service/test_agenda_render_faces_1576.py`
- `tests/unit/services/intent_service/test_effective_user_id_1601.py`
- `tests/test_architecture_enforcement.py`

Everything else showing in `git status` (`services/domain/models.py`, `services/mcp/consumer/google_calendar_adapter.py`, `services/radar/*`, `services/standup/assembler.py`, `web/api/routes/radar.py`, the other lane's test files/session logs, `dev/active/canonical-retest-serving-llm.json`) is NOT mine — pre-existing/concurrent work from another lane in this shared worktree, untouched by me.

## Memory & briefing surfaces referenced this session

- **Referenced**: `docs/internal/architecture/current/intent-routing-stack.md`-adjacent knowledge not directly opened (not needed — scope was a leaf render + one identity-coercion line, not dispatch/classification); GitHub issues #1565 and #1601 (full thread incl. PM's revision-then-confirmation on #1565) — read in full per "investigate before extending," which changed my copy choice (fabrication branch closed, real 2-day all-day event).
- **Loaded but not referenced**: CLAUDE.md's worktree/mailbox/sign-off sections (not applicable — I'm a scoped subagent, not committing/pushing).
- **Wanted but not found**: an explicit CXO ruling on the exact "current meeting is an all-day block" copy (PM's #1565 comment gave one short form; the dispatch brief gave a fuller form with "(through Thursday)"; I used the fuller form and marked `# CXO copy pass pending (#1565)` in-code since I had to choose between them).
