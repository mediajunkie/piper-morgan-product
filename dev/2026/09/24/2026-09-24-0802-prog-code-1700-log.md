# 2026-09-24 08:02 — Coding Agent (prog) — issue #1700

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated)
**Issue**: #1700 — CLI-BROKEN-IMPORT: cli/commands/notion.py imports Batch-1-deleted notion_spatial — cli notion command dead on main; cli/ is a census/sweep blind spot
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (no commit/push — Lead reviews and commits)

## Ruling read (decisions.log, 2026-09-24 08:0x, Lead)
cli/ is a LIVE OPERATOR SURFACE, not a retirement candidate. Consequences: (1) cli/ joins
delete-module-safely sweep roots + every census denominator; (2) an import-smoke test imports
every `cli.commands.*` module so an ImportError fails CI; (3) a cli command whose backing
module is gone is fixed to the live path or disposed WITH a record.

## Work

### 1. Import-smoke test FIRST (the instrument)
Created `tests/unit/cli/test_cli_commands_import_1700.py` — discovers `cli/commands/*.py` from
the filesystem (glob, not a hand-kept list), `importlib.import_module`s each, marked `smoke`.
Also asserts the discovered set matches the filesystem (denominator can't silently drift) and
that discovery isn't empty (m-44: state the denominator).

**Pre-fix run** (the actual pre-fix state, not assumed):
```
FAILED test_cli_command_module_imports_cleanly[cli.commands.keys]
  ModuleNotFoundError: No module named 'services.security.key_rotation_service'
FAILED test_cli_command_module_imports_cleanly[cli.commands.notion]
  ModuleNotFoundError: No module named 'services.features.notion_queries'
2 failed, 7 passed in 1.23s
```
Two failures, not the one the issue named. `standup.py` imports
`services.integrations.mcp.skills.standup_workflow_skill.StandupWorkflowSkill` and imports
CLEANLY — confirms the ruling's evidence that standup's import is live.

### 2. notion.py — investigated and fixed (not disposed)
Read the whole command. `NotionSpatialIntelligence` was assigned to `self.spatial_intelligence`
in `__init__` and NEVER referenced again anywhere in the file (cmd_test's "Spatial Intelligence
Test" section is pure decorative print statements — "Note: Actual spatial intelligence testing
would go here" — no call into it). It also imports `services.features.notion_queries`
(`NotionCanonicalQueryEngine`, `enhance_with_notion_intelligence`) — that module was deleted
**2026-07-18** in `026d658182` (#1436 Tier-3: "file_repository_old, notion_queries,
share_query_pattern (fabricated IDs)... deleted") — also never referenced beyond the import.

**Decision: FIX by removing both dead imports + the dead attribute assignment.** This is not a
re-point (nothing in the command actually consumed either deleted module's functionality) — it's
dead-code excision that happens to also fix the ImportError. `cmd_status` (the only subcommand
that only touches `self.notion_domain_service`) now genuinely works.

**Discovered, NOT fixed** (out of #1700's scope — a design gap, not an import break, and the
import-smoke test cannot see it): `cmd_test`/`cmd_search`/`cmd_pages`/`cmd_create` all call
`self.adapter.*` — `self.adapter` is never assigned anywhere in `__init__` or elsewhere
(pre-existing `AttributeError`, unrelated to either deletion). `NotionDomainService` (the live
class that IS assigned, as `self.notion_domain_service`) has no `search_notion` method at all —
fixing this isn't a rename, it needs either a `search_notion` capability added to the live
service or a rewrite of those four subcommands. Confirmed via mypy (see below) — 10
`attr-defined` errors, unchanged before/after my fix, all pre-existing.

### 3. keys.py — second broken import, found by the smoke test, not named in the issue
`services.security.key_rotation_service.KeyRotationService` — deleted the **same day** as
notion_queries (`026d658182`, #1436 Tier-3: "key_rotation_service (+intent note to live
service) deleted"). The intent note survives at
`services/security/user_api_key_service.py:541`: `rotate_user_key()` is the live
single-key-rotation home the Tier-3 rider pointed at.

Grepped `rotation_service` usage in keys.py: `rotation_service = KeyRotationService()` at line
118 (pre-fix) was constructed and **never called** — the actual rotation flow
(`retrieve_user_key` / `validate_user_key` / `store_user_key`) runs entirely through
`key_service` (`UserAPIKeyService`, already live and already imported).

**Decision: FIX by removing the dead import + the dead local.** Same shape as notion.py — the
issue's title only named notion.py, but the ruling's consequence (3) is general to cli/, and the
smoke test's job (per the dispatch) is to catch every cli/commands/*.py break, not just the
named one. Fixed so the smoke test is green across the board, not just for the named module.

Both fixes carry a comment block citing the deletion commit and the "why this is a clean
removal, not a re-point" evidence, so a future reader doesn't have to re-derive it.

### 4. Sweep roots / census denominators
- **`.claude/skills/delete-module-safely/SKILL.md`**: cli/ was **already** added to the item-6
  sweep-roots list in the Batch-3 disposal commit (`22f43b0285a`, 2026-08-30) — confirmed via
  `git log -p`. Nothing to re-add. I DID add: a new bullet under "Verify AFTER each excision"
  documenting that cli/ now has a mechanical CI gate (this test), not just prose discipline, and
  a v1.2 changelog entry naming the two extra stale imports this pass found. Version bumped
  1.1 → 1.2, updated date 2026-09-18 → 2026-09-24.
- **Census/production-root scripts** — grepped `scripts/*.py` for `SEARCH_ROOTS`/
  `PRODUCTION_ROOTS`/`("services", "web")`-shaped denominators and read each candidate's
  docstring/scope statement before touching anything:
  - **`scripts/reachability-map.py`** — `SEARCH_ROOTS = ["services", "web", "main.py"]`,
    literally the phrase named in the dispatch. **Changed**: added `"cli"`. Verified the script
    still runs (`venv/bin/python scripts/reachability-map.py cli/commands` — scope line now
    reads `... scanned under ['services', 'web', 'main.py', 'cli']`, cli/commands modules show
    real importer counts instead of a blind "0/unknown").
  - **`scripts/check_unscoped_reads.py`** (`SCAN_ROOTS = ["services", "web"]`) — **left
    unchanged**, per the dispatch's own instruction and confirmed by reading its docstring:
    ADR-079 D2 credential/owner-scoping ratchet, a TEST/CORE-shaped enforcement gate with its
    own frozen scope statement, not a general dead-code census.
  - **`scripts/check_silent_death.py`** — **left unchanged**: docstring states "Scope: the
    Census A core-path module set (see CORE_PATHS)... Extending scope means re-freezing the
    ceiling" — a frozen, named-scope ratchet, not the production-root denominator.
  - **`scripts/census_principal_blind_tests.py`** — **left unchanged**: its denominator is
    `tests/**/*.py` (a TEST-file census, not a production-code census) — doesn't apply.
  - **`scripts/principal_threading_lint.py`** (default `[services, web]`) — **left unchanged**:
    an ADR-071 principal-threading-pattern lint over request/intent-handling code; cli/ commands
    don't thread a principal through a context dict from a web/intent boundary the way this
    lint targets, so extending it wasn't a fit. Flagging this one as a judgment call rather than
    a hard negative — Lead/Arch should sanity-check if cli/ ever grows request-shaped context
    handling.
  - **`scripts/check_conversion_methods.py`** (comment: "denominator: services/ + web/") —
    **left unchanged**: PM-056 DB-model-conversion-method census; cli/ defines no DB models.
  - **`scripts/check_mypy_gate.py`** — **left unchanged**: its own header states "over
    services/ + web/" as the #1436-ratified mypy-gate scope; not a dead-code census, and the
    dispatch's own gate list runs plain mypy on touched cli files separately (below), not this
    gate.

### 5. Gates run
- **New smoke test**: 9/9 passed (7 pre-existing + notion + keys, all green post-fix).
- **Second instrument** — `venv/bin/python -c "import cli.commands.<x>"` loop over all 8
  commands: all clean.
- **`scripts/run-sweep.sh smoke`**: `537 passed, 1 skipped, 13840 deselected` (was 528 before
  the +9 cli tests joined the smoke marker set — consistent with 9 new smoke-marked tests).
- **`ruff check .`**: All checks passed.
- **`ruff format --check .`**: 1 file needed reformatting (the new test file — trailing-comma /
  wrap style) → ran `ruff format` on it → re-checked clean, `1796 files already formatted`.
- **mypy (`venv-mypy-gate/bin/python -m mypy --config-file mypy-gate.ini`) before/after** on the
  three touched files, using `git show HEAD:<path>` copies in scratch for "before" (no working-
  tree git ops): identical error sets for `notion.py` (10 `attr-defined` "no attribute
  'adapter'" + 1 `union-attr`, all pre-existing, unchanged) and `keys.py` (4 pre-existing errors,
  unchanged) before and after — only the line numbers shifted from the added doc-comment lines.
  `reachability-map.py`: zero errors either way. **Zero new mypy errors introduced.**
- **`venv/bin/python scripts/check_silent_death.py --count`**: `192` — at ceiling, not above.

## Discovered work (report only, not filed — Lead's call)
1. `cli/commands/notion.py` — `self.adapter` is referenced by `cmd_test`/`cmd_search`/
   `cmd_pages`/`cmd_create` but never assigned anywhere (pre-existing `AttributeError` at
   runtime, invisible to the import-smoke test). The live `NotionDomainService` also has no
   `search_notion` method, so this isn't a one-line rename — it's a real design gap between
   what these four subcommands assume and what the live domain service offers.
2. `cli/commands/standup.py` — mypy flags `self.conversation_queries` (lines ~98/107/118) as
   never assigned — same attr-defined shape as the notion.py `self.adapter` bug, in the command
   the ruling cited as evidence cli/ is live. Not import-breaking, so invisible to this smoke
   test; a runtime `AttributeError` waiting in the same family.
3. `cli/commands/keys.py` — pre-existing mypy errors independent of my fix: `validate_user_key`
   called with an unexpected `test_key` kwarg, `store_user_key` called with an unexpected
   `skip_validation` kwarg, and `"str" has no attribute "key"` twice (lines ~227/251/255/270
   post-fix) — signature drift against the live `UserAPIKeyService`, not something #1700 asked
   me to touch.
4. `cli/commands/issues.py` — several pre-existing mypy errors (nullable-arg mismatches, two
   missing var-annotations, one `attr-defined` on a `None`-typed helper) — unrelated to #1700,
   noted only because the before/after mypy run surfaced them.

None of the above are import-time breaks; none are touched in this pass. Flagging per CLAUDE.md
discovered-work discipline — Lead decides whether these warrant their own issues.

## Files changed (no commit/push — Lead's set)
- `tests/unit/cli/test_cli_commands_import_1700.py` (new)
- `cli/commands/notion.py`
- `cli/commands/keys.py`
- `scripts/reachability-map.py`
- `.claude/skills/delete-module-safely/SKILL.md`

## Verified how
- **Method**: ran the new parametrized smoke test before AND after the fix (pytest output
  quoted above, not recalled); ran a second, independent instrument (`python -c "import
  cli.commands.X"` loop) over every command; ran `scripts/run-sweep.sh smoke`,
  `ruff check .`, `ruff format --check .`, and `venv-mypy-gate` mypy on `git show HEAD:`
  snapshots vs. the working tree for a genuine before/after (no working-tree git ops used);
  ran `scripts/check_silent_death.py --count`.
- **Layer**: import-time module resolution (smoke test + `-c import` loop — proves the module
  loads, not that every code path works — m-43: this is NOT a runtime-behavior test, and I say
  so explicitly for the `self.adapter`/`self.conversation_queries` findings above, which import-
  clean but are runtime-broken); static type-check (mypy) for signature-level regressions;
  static lint/format (ruff); a frozen-scope ratchet (`check_silent_death.py`) for the silent-
  death ceiling.
- **Denominator**: 8 of 8 `cli/commands/*.py` modules (excluding `__init__.py`) covered by the
  smoke test, discovered from the filesystem, count asserted non-empty and matched against disk
  in the test itself. 3 of 3 touched files mypy-diffed before/after. 7 of 7 candidate
  "production-root census" scripts found by grep were individually read and classified
  (1 changed, 6 left with stated reasons) — not assumed from name-matching alone.

## Memory & briefing surfaces referenced this session
**Referenced**: CLAUDE.md sign-off/worktree/subagent rules (governed how I worked — no commit,
no destructive git); decisions.log tail (the #1700 ruling itself, read in full before acting);
delete-module-safely SKILL.md (sweep-root/verification conventions, extended in place).
**Loaded but not referenced**: MEMORY.md index (browsed for orientation, nothing in it was
load-bearing for this specific fix-vs-dispose decision).
**Wanted but not found**: nothing — the ruling and the issue together were sufficient; no gap.
