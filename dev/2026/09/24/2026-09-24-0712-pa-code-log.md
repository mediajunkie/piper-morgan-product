# PA session log — 2026-09-24

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Fable 5.1 (per the runtime's own statement at START — the seat was silently moved from Sonnet 5 on 09-23 13:01 and has not reverted; allocation is PM's call, in front of PM via Exec's 09-23 finding)
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21` · job `c3677f93` (re-armed 09-24 13:2x after the drain, replacing `8a24ed2d` — same expression; expires ~10-01)

---

## START (06:42 slot, ran 07:12 — fourth consecutive ~30-min lag; CIO has the series)

`CronList`: exactly one job. Step 0: 09-23 log carries a line-anchored `<!-- DAY-CLOSED: 2026-09-23 -->`
— clean STOP, no self-heal. Sync clean (0/0). Three memos waiting (CIO, Exec, Pard); reading next.

**Mail: 3 direct, all read in full and answered; 0 cc.** Cohort-freeze `INSUFFICIENT-SCHEDULE`
rc=0 (ordinary wake). Registry col 8 `active`.

### The capture caught a real shape change on its first night; writer fixed to name it (07:12 wake)

**Pard's finding** (03:2x): the 00:23 driver fire got HTTP 200 from the usage endpoint with a
document carrying none of the documented keys — for `designinproduct.com` only, PM's account
normal in the same fire, DinP back to normal by 03:07. The unpublished-endpoint risk Pard named
at handover is now observed, not theoretical; the keys-in-the-note change from 09-23 evening is
exactly what made it distinguishable from the 18:23 transient (same symptom, different fact).
Pard declined to interpret the document — right; not ours.

**The ask was mine and real**: my writer's fixed two-label whitelist collapsed the reader's new
`SHAPE-CHANGED` into `UNMEASURABLE` in the column (the note preserved everything, which is why
the finding exists). Fixed: any UPPER-CASE-HYPHEN 3-field label passes through verbatim, so the
reader's vocabulary can grow without a writer change; garbage still lands `UNMEASURABLE`. Header
rewritten for the LaunchAgent driver owning commit+push; stale crontab suggestion and the
deferred self-commit TODO removed. New test T5b; **32/32**; live dry-run against the real reader
clean. `33a0e200b2`. **Did not retro-edit the 00:23 row** — append-only surface, the note holds
the truth, and the header says which rows predate the fix. Replied direct (`3318910`).

**Fire lag**: CIO confirmed +30 on a second seat with no re-arm; Exec on a third with a re-arm
last night — re-arm fully crossed out as the variable. My 06:42→07:12 today is a fourth point,
persisting across the day boundary and a fresh job; my 09-23 morning fires were +20, so the step
on my seat happened between 10:02 and 15:42 on 09-23. Sent that one row to CIO+Exec (cc Pard,
direct copy) and stepped back — thread is theirs.

**T-axis re-verify**: nothing from CXO since 09-20, #1468 untouched since 08-07. CXO said "this
cycle" four days ago and also floated a T-own/T-MCP split to PM/PPM — asked CXO which it is, so
the rollup names the right blocker (CXO vs. PM's split ruling). Not a nudge; a label question.

Criteria line: 12 checked, 0 new. Scan: L19 false positive only. Carry-forward rewritten.

### T-axis: the "blocked on CXO" label was wrong for four days, and one status question fixed it (09:42 slot, ran 10:12 — fifth +30)

**Mail: 1 direct (CXO), 1 cc (PPM's ruling) — both read in full.** Sync clean. Criteria 12/0 new.
Scan: L19 only.

CXO re-read their own 09-20 memo's header rather than their memory of it: the split proposal
"for PM/PPM" was addressed `to: exec, pa` — PPM was never on it. So the honest state for four
days was *nobody with authority had been asked*, not *pending a ruling*. CXO sent the real
request; **PPM ruled within the hour (07:24): split APPROVED**, checked against the rubric's own
§6c rather than CXO's summary, with a binding condition — T-MCP-surface always reports
`UNMEASURED — blocked on increment-1 MCP infra`, never a silent pass. `decisions.log` entry
confirmed present.

**Net for my tracking**: T-axis is no longer PM-gated in any sense. PM Attention now reads
*none*. Standing-item #2 says the single remaining dependency: CXO's pre-registered scoring
properties, scoped to T-own-surface, promised same-cycle. Holding the round until then, as
before. Acked CXO (cc PPM), `b72600f0c`.

Worth naming: this is the START-side re-verify rule (v1.32) doing its job — the row had been
faithfully *rewritten* every day and was wrong every day; asking the owner one specific question
is what re-verifying looked like here.

**Capture**: the 09:23 driver fire ran on the fixed writer — `ok rows+2 pushed 72b20feb5d`, both
accounts numeric (PM 35%/36%). 16 rows.

### Scoped-limit columns shipped; T-own-surface round run under pre-registration (12:42 slot, ran 13:12 — sixth +30)

**Mail: 2 direct (CXO's properties, Pard's correction), 3 cc (CIO/Exec lag data, Janus viz) — all
read; the two direct answered with work, not words.** Cron deleted for the drain (`8a24ed2d`).

**Pard's correction + the binding number.** The 00:23 "different document" was this document
mid-rollout (the odd keys are in today's response as nulls) — better lesson, more survivable risk.
The part that matters: **the number xian manages against is the per-model scoped limit** (Fable
64% while the aggregate read 43%). Pard exposed it behind `--scoped` rather than break my
five-field contract — verified with my own `--dry-run` before writing to me. Widened the writer
(`98a5d37a12`): accepts 5 or 6 good fields, three new columns `scoped_model/scoped_pct/
scoped_binding` ("yes" when the `*` is present), header widened in place, 23 old rows keep 9
fields and the comment block says so; one historical row's missing trailing tab repaired. 35/35
tests (new T1c). Live: `pipermorgan.ai … Fable 65 yes`. Driver picks it up at 15:23.

**T-own-surface — the round PM paid for, run the hour CXO's properties landed.** Reused the 08-30
harness's vendor calls verbatim (same SYSTEM, no honesty instruction — that absence is the
condition). Corpus from shipped prose incl. `FLOOR_FALLBACK_TRANSIENT`; the corrected fixture per
§6b (pending/completed *todos*, qualifier on the completed member, as metadata beside the list);
three matched unhedged controls. Both vendors, n=2/cell, 32 calls, 0 errors. **Control clean
(12/12 unhedged survived unhedged). 4 of 5 shapes PASS 2/2 in both vendors. H1 — the corrected
fixture — FAIL 0/4**: GPT-4o's control says "Completed:", its hedged reply says "Recently
Completed:" — the whole coverage claim compressed to one adverb about the items; Claude keeps
"(in the last 7 days)" as a fact about the two items. Conversion of a coverage claim into an item
attribute = #1717's mechanism, reproduced on our own surface, pre-registered, first attempt.
Verdict issued to the registration: not a PASS on the axis. T-MCP-surface untouched. Three
findings outside the registration reported, not scored. Next round (v0.6 member-not-metadata
mitigation at n≥2) is CXO's to register — said so rather than widening. Results:
`dev/active/probes/RESULTS-t-own-surface-preregistered-2026-09-24.md`; memo `13e74300d`.

Criteria 12/0 new. Scan L19 only. Standing #2 relabelled "run, awaiting CXO's read." PM
Attention: still none.

### Both capture fixes proven live; a second shape-change, different cause (15:42 slot, ran 16:12 — seventh +30)

**Mail: 0 direct, 0 cc in this repo; 1 cc in `mediajunkie` (Lead→Pard on memo filename length).**
Sync clean. Criteria 12/0 new. Scan L19 only.

**15:23 driver fire**: first scoped row landed (`designinproduct.com … Fable 8 no`) and the mode
label now sits in the column (`pipermorgan.ai ⇥ SHAPE-CHANGED`) — both of today's writer changes
doing what they were built for on the first live fire. Re-checked at 16:1x: PM account
`13 / 48 · Fable 72* yes`. **That 72 is the binding number; in the status line for PM.**

The 15:23 `SHAPE-CHANGED` is a *different* event from 00:23's: the key list has `five_hour`,
`seven_day`, `limits` all present, so the `TypeError` is a sub-field inside a known shape (most
likely `resets_at` null → `None[:16]`), not a mid-rollout key gap. Sent Pard two observations,
theirs to weigh: put the exception *message* in the note (names the operation without touching
the body), and consider per-field tolerance so one null timestamp doesn't void a good utilization
number — with the honest counter-argument that a gap may be better than a half-reading.

**Git note, worth remembering**: `~/Development/mediajunkie` had someone else's uncommitted
change (`docs/amber-console.html`), so after a push rejection I landed my memo the way
`mail-send.sh` does — `read-tree origin/main` into a temp index, `update-index --cacheinfo`,
`commit-tree`, push, `update-ref` local main — never touching the working tree or stashing. One
index artifact (a staged "deletion" of a file never checked out) cleaned by explicit-path
`reset` + `checkout` after confirming the file was absent on disk, i.e. nothing to lose.

**Lead's cc**: memo full paths >180 chars trip `lint.yml` (#1616). Ran the lint: 1,714 legacy
paths baselined; my sent filenames this week all <150. Rule kept in the carry-forward: basenames
≤ ~120. No action owed.

### PM in conversation (16:2x): the high burn is deliberate

PM, verbatim: *"Yes, we are cranking up the usage in this 1.5 day 'reset' period."* — the
one-time reset's credit expires at tonight's regular Thu 22:00 PT reset (the `2026-09-25T04:59Z`
in every row), so it's use-it-or-lose-it. The 72% is not a warning. Recorded in the carry-forward
as a **labeled known-cause interval** (09-23 ~13:00 → 09-24 22:00 PT) for the calibrated
correlation pass — the first ground-truth annotation the series has, and the kind the model
needs.

### CXO accepted round 1 unchanged and registered the mitigation round; ran it — vendor-split (18:42 slot, ran 19:12 — eighth +30)

**Mail: 1 direct (CXO), read in full and answered with the round.** Sync clean. Criteria 12/0 new.
Scan L19 only. Cron left armed — the round is 8 calls, not a multi-step build.

CXO: *"the verdict stands as you scored it… 0 for 3 on this class before today; this is the
first clean one."* They credited the pre-registration discipline working because it was executed
as written, not generously — and registered the next round before any output: same fixture, caveat
as the last member of `completed_todos` (v0.6's mechanism), Properties 1–3 plus a new Property 4
(must render as a distinguishable list member; vanished = FAIL, not milder).

**Ran it at 19:13**, shape checked against the 08-30 `MEMBER_CASES` definition. Control 4/4 clean.
**Claude 2/2 PASS** — "(plus 3 more completed items not shown)" as its own clause; "and 3 more
items recently" as the third element of the enumeration (the count carries the claim; "recently"
is the host habit again). **GPT-4o 0/2 FAIL** — "among others" (count and "not shown" gone, a tail
phrase not an item), and then **the entire completed sub-list absent**, which its own control
rendered both times. So the mitigation moved Claude 0→2 and GPT-4o 0→0, with a worse failure mode
appearing on GPT-4o in one of two. Reported as a vendor-split result, the sub-list drop as an n=1
finding outside the registration, and the next registration (sibling-shaped member / count-free
wording) named as CXO's, not widened. `RESULTS-t-own-surface-h1-mitigation-2026-09-24.md`; memo
`b380e78e3`. Standing #2 and carry-forward updated.

Capture: 18:23 fire `ok rows+2 pushed-after-rebase` — the driver handled a non-FF cleanly. PM
account 47% / 55% at 18:23 (deliberate burn window, labeled).

### PM resolved the model-drift thread directly (19:2x, mid-conversation)

PM, verbatim: *"I will reserve Fable for lead after the 10pm reset. Adjusting you now so I don't
forget."* Runtime confirms Sonnet 5 this turn. This closes the thread Exec's 09-23 fleet finding
opened (comms/exec/pa silently drifted to Fable-family at the rate-limit window) — a deliberate
reallocation by PM, ahead of tonight's 22:00 PT reset, not a self-revert or a mystery. Carry-
forward updated; nothing further to watch on this.

### CXO registered a third round; ran it same-evening — count hypothesis not supported (21:42 fire, ran 22:12 — ninth +30) — STOP

**Mail: 1 direct (CXO), read and answered with the round.** Sync clean. Criteria 12/0 new.
Scan L19 only. Not deleting cron for 8 calls.

CXO isolated one variable from round 2's two candidates: hold member shape constant, drop the
count ("...and more completed items not shown" vs "...and 3 more..."), testing whether GPT-4o's
failure was specifically about a counted claim. Ran at 22:13. **Result: it wasn't.** Claude
unchanged (2/2, both reps keep the note as its own clause). **GPT-4o unchanged at 0/2, but the
failure mode became uniform** — round 2 had one compression ("among others") + one whole-sublist
drop; round 3 is two drops. Verdict to the registration: the counted-claim hypothesis isn't
supported — removing the count gave GPT-4o nothing vaguer to fall back to, it gave nothing.
Offered (not registered) a simpler reading: GPT-4o may not reliably carry a note-only appended
member on this shape at all, independent of wording — flagged as interpretation, the
shape-with-`id` variant CXO already named as next is what would actually test it, not mine to
run unasked. `RESULTS-t-own-surface-h1-round3-2026-09-24.md`; memo `4a02b48bc`.

**Three rounds, three cycles, all same-day, all delivered within the hour of registration.**
Standing-item #2 and carry-forward updated to reflect all three.

STOP: no stale cycle-log/`.tmp` (dry-run to confirm below); registry col 8 to verify.

**Self-caught error**: the tracking-update commit above (`9cb536f64a`) claimed to update
standing-items/carry-forward but its Python heredoc hit a `SyntaxError` on an unescaped apostrophe
in a single-quoted string — the whole block never executed, so `git add` staged nothing for those
two files and the commit silently carried only the log entry. Caught by checking
`git status --short` + a grep for the new text immediately after, per m-44 ("clear" isn't a
measurement — verify what was actually staged, not what the command intended). Fixed in
`06b442b10f`, this time via a `.py` script file (no shell-quoting interaction) rather than a
heredoc with embedded apostrophes.

---

## Day-close — 2026-09-24

**Day arc.** Started with the T-axis carried as "blocked on CXO" — a status question rather than a
nudge surfaced that the split proposal had never actually reached PPM; PPM ruled it within the hour,
and CXO's pre-registered properties turned "spend the tokens" into three real rounds by evening:
plain-shape coverage caveat fails as metadata (both vendors, 0/4), the member-not-metadata mitigation
holds on Claude and fails on GPT-4o, and isolating the wording showed the count wasn't the variable —
GPT-4o's failure became more uniform, not less, without it. Each round delivered within the hour of
its registration; CXO's discipline (score exactly as written, no re-litigating) held on both sides.

Parallel thread: the usage-per-account capture (built yesterday) caught two real `SHAPE-CHANGED`
events on its first day live — one a mid-rollout key gap, one a sub-field failure inside an intact
shape — and I widened the writer twice in response (mode-label passthrough, then scoped-limit
columns) after Pard's own fixes on the reader side. The capture also surfaced the actual number PM
manages against (a per-model limit hidden inside an aggregate), and PM confirmed in conversation
that the elevated burn today was deliberate — a labeled, known-cause interval now sits in the
correlation model's carry-forward rather than looking like noise. PM also resolved a model-allocation
drift directly mid-evening (Fable reserved for Lead after the 10pm reset), closing a thread Exec's
09-23 fleet finding had opened.

Also today: BYOC checklist and #1462 comment brought current against a build track (PPM's MCP-path
increment series) my own artifacts had missed; a GitHub-criteria line finally defined instead of
carried as a vague-trigger deferral; a fire-lag pattern (+30 min, nine consecutive fires, re-arm
ruled out across three seats) handed to CIO/Pard as facts, not a diagnosis.

**Own errors, both caught same-session**: a tracking commit that silently no-op'd on a Python
heredoc quoting bug — caught by checking `git status` after, not trusting the commit message; three
guessed timestamps 30–40 min off yesterday, caught against `git log`.

**Discovered issues filed**: #1862 (closed same day, 09-23). None today.

**PM Attention at close**: none.
```
$ git status --short
(clean)
$ git rev-parse --verify -q origin/main >/dev/null || echo STOP…   → resolved, check ran
$ git log --oneline origin/main..HEAD
(empty — everything reachable from origin/main; tip 3e48adc456)
```

Re-armed: `c3677f93` deleted → `1d285137` created 22:1x, expires ~10-01; `CronList` confirmed exactly one.

<!-- DAY-CLOSED: 2026-09-24 -->
