# 2026-09-24 1816 — Coding Agent (prog) — #1867 — log

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated)
**Issue**: #1867 — guided-process census + enforcement test (ADR-059 onboarding-on-ice registry visibility)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead

## Task

Lead dispatched: build a mechanical census of guided processes
(`ProcessType` / `ProcessRegistry`) with LIVE/DARK status, an enforcement
test pinning every session-start call site to a registered process, and
report any live #1856-class defect found (dark process, session created,
no later turn can reach it) without fixing it unless it's a genuine
one-liner.

## Read first

- `docs/internal/architecture/current/intent-routing-stack.md` (partial —
  first 347 of 949 lines; the guided-process registry layer sits ABOVE this
  doc's whole chain and the doc has zero mentions of "onboarding" anywhere,
  confirmed by grep — so no existing claim in it needed correcting; no edit
  made, per the task's "only if the census changes what it says").
- ADR-059 (`docs/internal/architecture/adrs/adr-059-workflow-dispatcher-offer-consolidation.md`)
  — PM's 2026-03-19 direction to put onboarding "on ice," Q2 leaves the
  guided-process-registry slot's long-term fate (re-register on the
  dispatcher, option c) as an open Arch question.
- `gh issue view 1867` — full text, both asks.

## Census (read-only, quoted)

**Registry** (`services/process/registry.py`): `ProcessRegistry` is a
singleton (`get_instance()`), priority-ordered
(`ONBOARDING:10, STANDUP:20, SLOT_FILLING:25, CLARIFICATION:30, PLANNING:40,
FEEDBACK:50`). `register(handler)` appends to `self._handlers`;
`check_active_processes`/`check_suspended_processes` only ever iterate
`self._handlers` — a `ProcessType` with no handler in that list is
structurally invisible to the whole guided-process layer, full stop.

**Registration sites** (grep: `register_default_processes\|registry\.register(`):

| ProcessType | Adapter class | Registered? | Decided at |
|---|---|---|---|
| ONBOARDING | `OnboardingProcessAdapter` | **DARK** | `services/process/adapters.py:609` — `# registry.register(OnboardingProcessAdapter())` commented out (ADR-059) |
| STANDUP | `StandupProcessAdapter` | LIVE | `services/process/adapters.py:610`, inside `register_default_processes()`, called from `services/container/initialization.py:109` at app startup |
| SLOT_FILLING | `SlotFillingProcessAdapter` | LIVE | `services/intent/intent_service.py:346`, inside `IntentService.__init__` (NOT via `register_default_processes` — separate call site) |
| CLARIFICATION | *(none exists)* | DARK | No adapter class anywhere in the tree — `ProcessType` docstring calls it "Future (Advanced Layer)" |
| PLANNING | *(none exists)* | DARK | same — vestigial future member |
| FEEDBACK | *(none exists)* | DARK | same — vestigial future member |

Verified live-vs-declared agreement by actually CALLING
`register_default_processes()` + the slot-filling registration line against
a scratch (non-singleton) `ProcessRegistry` — see "What I built" below.
CLARIFICATION/PLANNING/FEEDBACK have zero session-start sites anywhere
(grepped `ProcessType.CLARIFICATION` etc. — only appear in
`registry.py`'s priority dict and `off_topic.py`/`escape.py`'s copy tables);
not a live defect, just unbuilt future surface.

**Session-START sites** (grep set — the exact greps I ran, documented
verbatim in the enforcement test's docstring too):
```
grep -rn 'create_session(\|_start_standup_conversation(\|start_filling(\|offer_onboarding(\|start_onboarding(' \
  --include='*.py' services/ web/
```
plus targeted follow-ups (`ProcessType\.`, `GuidedProcess\b`,
`register_default_processes\|registry\.register(`) to map every hit to its
process and confirm reachability.

| Site (file:line) | Process | Registered? | What the user would see |
|---|---|---|---|
| `services/intent_service/canonical_handlers.py:4819` (`_handle_add_project`) | ONBOARDING | **DARK** | **LIVE, user-reachable defect.** A "add a project" turn with no extractable name creates an onboarding session (state `GATHERING_PROJECTS`) and asks *"I can add a project — I just need its name in the same message. Say: add project [name] with repo [owner/repo]…"*. This is #1856's OWN rewrite: it deliberately bypasses `ProcessRegistry` — the session is private bookkeeping the SAME handler reads back next turn via `_pending_ask()`, gated on the next turn re-entering `_handle_add_project`, which needs an "add"/"create"/"new project" token. The ask's copy primes exactly that token, so the common case closes the loop. But a user who replies with a bare name and none of those words (e.g. just pastes the name) never re-enters the handler — `_pending_ask()` never runs — and the onboarding session silently orphans: the #1856 shape, recurring, inside the branch #1856 itself shipped to fix it. **Not a one-liner** — needs either a durable per-turn carrier (the #846/#1190 pending-action idiom other surfaces already use) or re-registering `OnboardingProcessAdapter` on the workflow dispatcher (ADR-059 Q2 option c) — an Arch-level call, reported not fixed. |
| `services/conversation/conversation_handler.py:249` (`_check_portfolio_onboarding` → `.offer_onboarding(`) | ONBOARDING | DARK | **Present but currently unreachable.** `_check_portfolio_onboarding`'s own (and only) call site, in `_respond_to_greeting`, is commented out: `# ADR-059: Portfolio onboarding offer disabled (onboarding on ice) — Was: _check_portfolio_onboarding(user_id, session_id)`. No user can trigger this today. A landmine: if anyone ever uncomments that call, the offer fires straight into the dark process again with the SAME unreachable-follow-up shape `_check_pending_onboarding_offer` (intent_service.py:3377) was built to catch — and that function is itself dead for the same reason. |
| `services/onboarding/portfolio_handler.py:127` (`offer_onboarding` body → `self.manager.create_session(`) | ONBOARDING | DARK | Dead code — only reachable through the site above, which is itself unreachable. |
| `services/onboarding/portfolio_handler.py:233` (`start_onboarding` body → `self.manager.create_session(`) | ONBOARDING | DARK | **Fully orphaned** — confirmed by grep that `start_onboarding` has ZERO callers anywhere in `services/`/`web/`. Filed as discovered work (dead-code removal candidate), not part of the enforcement ratchet's "live defect" framing — it can never fire for a real user. |
| `services/intent_service/workflow_entries.py:67` (`slot_filling_adapter.manager.start_filling(`) | SLOT_FILLING | LIVE | fine |
| `services/intent_service/workflow_entries.py:539`, `services/intent/intent_service.py:2079,4726,4753,4949` (`_start_standup_conversation(`) | STANDUP | LIVE | fine (5 call sites, all reachable, all registered) |

`ProcessRegistry.check_suspended_processes` (used by
`_check_suspended_session_reentry`, the "welcome back" resume offer) only
iterates registered handlers — confirmed it can never surface a dark
ONBOARDING resume, so that surface is unaffected by the dark status. Good:
one fewer place to worry about.

## What I built

1. **`services/process/guided_process_registry.py`** (new module) —
   `GUIDED_PROCESSES: tuple[GuidedProcessInfo, ...]`, one row per
   `ProcessType` member (name, adapter class name or `"(none)"`, LIVE/DARK
   status, the ADR/issue + file:line source deciding it).
   `compute_live_registered_types()` re-derives the live set by ACTUALLY
   CALLING `register_default_processes()` (the real function) against a
   scratch, non-singleton `ProcessRegistry` (singleton saved/reset/restored
   around the call so the app's real registry is never touched), plus
   mirroring the one slot-filling registration line
   `IntentService.__init__` performs (too heavy to construct a real
   `IntentService` just to probe the registry — LLM classifier + DB-backed
   conversation manager). A canary
   (`_assert_slot_filling_registration_line_present`) reads the real
   source of that exact line so the mirror can't go silently stale if it
   ever moves. Verified interactively: `live_process_types() ==
   compute_live_registered_types()` → `True` (both `{slot_filling,
   standup}`); `GUIDED_PROCESSES` covers every `ProcessType` member.

2. **`tests/test_architecture_enforcement.py::TestGuidedProcessStartersRegistered1867`**
   (appended, class only) — 5 tests:
   - `test_census_matches_measured_sites` — the grep-based scan (regex per
     known call shape, filtering `def`/`async def` lines so a method's OWN
     definition never counts as a call site) reproduces `KNOWN_SITES`
     exactly (10 sites) — a new/vanished site fails loud.
   - `test_every_known_site_names_a_live_process` — **every** `KNOWN_SITES`
     entry must name a LIVE process, **no exclusion for the dark ones**.
     Per #1867's explicit instruction ("the test must be RED for it — do
     not allowlist it"), this is INTENTIONALLY RED right now: 4 of 10 sites
     fail (the three ONBOARDING sites above + the dead `start_onboarding`
     body). I first wrote this with a `KNOWN_DARK_SITES` exclusion that
     made it pass — caught that this was exactly the allowlisting the issue
     said not to do, and rewrote it with no exclusion.
   - `test_known_dark_sites_are_exactly_the_dark_subset` — tightness check,
     independent of the red test above: `KNOWN_DARK_SITES` must equal the
     actual measured-dark subset of `KNOWN_SITES`, so the red set can't
     silently drift wider or narrower than what's documented.
   - `test_guided_process_census_covers_every_process_type` — `GUIDED_PROCESSES`
     has exactly one row per `ProcessType` member.
   - `test_declared_live_set_matches_live_registration` — the module's own
     "derived from the real registry" contract, exercised as a test.

## Result

`pytest tests/test_architecture_enforcement.py -q -o addopts=` (repo's
`pytest.ini` sets `--maxfail=1`, overridden to see the whole file):
**1 failed, 61 passed** in 12.26s — the 1 failure is
`test_every_known_site_names_a_live_process`, exactly as designed
(EXPECTED red, documented in its own docstring as the deliverable, not a
bug).

`tests/unit/services/process/` (existing adapter/registry/escape/off-topic
tests, unaffected by this change): **184 passed, 7 skipped** in 4.49s.

`scripts/run-sweep.sh smoke`: **559 passed, 1 skipped, 14145 deselected**
in 32.73s — my new class isn't `smoke`-marked, so it doesn't gate CI; ran
it anyway to confirm zero collateral damage to the gating subset.

`venv/bin/ruff check` + `ruff format --check` on both changed files: clean
after one `ruff format` pass — the diff (`ruff format --diff` first, to
confirm scope) touched ONLY lines I had just added (one long regex split,
one set-comprehension reflow) — no reformatting of pre-existing code in the
3770-line shared test file.

mypy (venv-mypy-gate, `mypy-gate.ini`): `services/process/guided_process_registry.py`
alone, `--follow-imports=silent` (so only my file's own lines are checked,
not the whole transitively-imported graph — mypy-gate.ini sets
`follow_imports = normal`, which by default would also report ~850
pre-existing errors across ~100 unrelated already-imported files, e.g.
`intent_service.py`, that I did not touch): **0 errors, exit 0**. I did
NOT run the full `scripts/check_mypy_gate.py` per-code ratchet (the real
gate invocation is `mypy services/ web/`, ~900s timeout budget) — the
module is additive-only (touches no file any ratchet ceiling already
covers), so it cannot move any per-code count; the isolated check above is
what I can actually attest to. **Verified how**: isolated-module mypy run,
exit code + stdout observed this turn; NOT the full-tree gate (named
explicitly rather than implied).

## Discovered work (report only, not filed as GH issues — Lead's call)

1. `services/onboarding/portfolio_handler.py::start_onboarding` (line 218)
   has zero callers anywhere in `services/` or `web/` — confirmed by grep,
   not merely commented out. Dead-code removal candidate.
2. `services/conversation/conversation_handler.py::_check_portfolio_onboarding`
   (and the `offer_onboarding`/`handle_offer_response`/
   `_check_pending_onboarding_offer` chain it and `intent_service.py:3377`
   feed) is entirely dead — its only call site is commented out per
   ADR-059. Candidate for the same cleanup pass as #1 if Arch decides
   onboarding stays "on ice" rather than getting re-registered (ADR-059 Q2).
3. The live defect at `canonical_handlers.py:4819` (see census table above)
   — reported per #1867's own instruction, not fixed (needs an Arch-level
   call per ADR-059 Q2, or a proper #846/#1190-style carrier).

## Verified how

- **Method**: read the actual source of `services/process/registry.py`,
  `adapters.py`, `intent_service.py`, `canonical_handlers.py`,
  `conversation_handler.py`, `workflow_entries.py`, `portfolio_handler.py`;
  ran the greps quoted above against the live worktree; ran the new module
  interactively (`live_process_types() == compute_live_registered_types()`
  → `True`); ran the new test class and the full architecture-enforcement
  file, the process unit-test directory, the smoke sweep, ruff
  check+format, and an isolated mypy pass, all this turn, output pasted
  above.
- **Layer measured**: static source read + grep (the census) is a
  mechanical, exhaustive-over-its-pattern-set scan, NOT a runtime trace — a
  call site's reachability (e.g. whether `_handle_add_project` really is
  hit in production) is argued from the surrounding dispatch code and cross-
  checked by the census module ACTUALLY invoking `register_default_processes()`,
  not merely asserted from a comment. Test execution (pytest, ruff, mypy)
  is real process output, not inferred.
- **Denominator**: the grep pattern set is stated explicitly in both this
  log and the enforcement test's docstring — 6 regex patterns over every
  `*.py` in `services/` + `web/` excluding `tests/`, yielding exactly 10
  session-start call sites, all ten triaged above. `ProcessType` enum
  membership (6 members) is fully covered by `GUIDED_PROCESSES` (asserted
  by a dedicated test). The full-tree mypy ratchet gate was NOT run (stated
  above, with why); everything else reported was run to completion this
  turn.

## Files touched

- `services/process/guided_process_registry.py` (new)
- `tests/test_architecture_enforcement.py` (appended `TestGuidedProcessStartersRegistered1867`
  + one top-of-file import `from services.process.registry import ProcessType`)
- This session log (new)

No other files touched. Did not edit
`docs/internal/architecture/current/intent-routing-stack.md` — confirmed by
grep it has zero existing mentions of onboarding/ADR-059/guided-process
registration, so there was no existing claim for this census to correct
(the doc's scope is the classification/dispatch chain, not the
guided-process layer that sits above it).
