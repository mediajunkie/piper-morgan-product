# 2026-09-24 Session Log — Coding Agent (prog) — Issue #1791

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated)
**Issue**: #1791 — Personality preferences have no per-user store
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Constraint**: no commit/push, no git index changes — Lead reviews and commits.

## Task

Give `web/personality_integration.py::PiperConfigParser` a real per-user store,
built on #1574's `UserPreferenceManager` (`users.preferences["upm"]`), while
`config/PIPER.user.md` stays the instance-wide DEFAULT for users with no saved
profile. Remove #1734's `require_admin` gate on the PUT in the same change
(AC's own stated exit condition). No migration/backfill of the existing file
into any user row (unknowable ownership).

## Investigation (Step 1 — readers/writers, before touching anything)

Found THREE separate, non-interacting personality systems in this codebase —
this is the actual shape of #1791, not apparent from the issue text alone:

1. **System 1 — `PiperConfigParser` (THIS issue's target).** File-based,
   instance-wide (`config/PIPER.user.md`, YAML block). Readers/writers:
   - `web/api/routes/personality.py` — GET/PUT `/api/v1/personality/profile`,
     POST `/api/v1/personality/enhance` (lines 55-234)
   - `cli/commands/personality.py` — `PersonalityCLI` (show/set/preset/test),
     default `user_id="default"` throughout
   - `web/startup.py:274-299` — app.state wiring (singleton parser + enhancer)
   - `templates/personality-preferences.html` — the 4-slider UI (warmth,
     confidence_style, action_orientation, technical_depth), fetches
     `/api/v1/personality/profile` (load L438, save L571) and `/enhance` (L617)
     — #1751 already removed the hardcoded `"default"` id from all three call
     sites; verified still true by reading the template.
   - Does **not** feed any prompt. Confirmed via grep: `config_parser` /
     `personality_enhancer` / `PiperConfigParser` have no callers outside the
     three above (routes, CLI, startup wiring, and their own tests).

2. **System 2 — `PersonalityProfile.load_with_preferences()`
   (`services/personality/personality_profile.py`) — ALREADY per-user, and is
   the REAL prompt-shaping path.** Reads `users.preferences` directly (5
   questionnaire dims: `communication_style`, `work_style`, `decision_making`,
   `learning_style`, `feedback_level` — the actual #1422 questionnaire, set by
   `scripts/preferences_questionnaire.py`), maps them to the same 4 dims via a
   documented semantic bridge (`dev/2025/10/27/personality-systems-divergence-analysis.md`,
   issue #269, Oct 2025 — Chief-Architect-review-pending, never resolved).
   Callers: `services/intent/intent_service.py:1207` (`formality_baseline`,
   feeds prompt generation), `services/intent_service/intent_hooks.py:113`
   (preference-detection baseline), `web/api/routes/preferences.py:207`
   (`GET /api/v1/preferences/profile`). **Untouched by this issue** — it does
   not go through `PiperConfigParser` at all and was already per-user before
   #1791 was filed. The issue's "the questionnaire, #1422" framing actually
   points at this system, not at the one #1791's AC names — worth Lead/Architect
   awareness, but out of scope for this AC (which explicitly names
   `PiperConfigParser` L50-75).

3. **System 3 — `PersonalityProfileRepository` + `PersonalityProfileModel` DB
   table** (`services/personality/repository.py`, alembic
   `f3a951d71200_add_personality_profiles_table_pm_155`) + a third enhancer
   (`services/personality/response_enhancer.py::ResponsePersonalityEnhancer`).
   Fully built, own migrated table, PIPER.user.md override-layering — **zero
   production callers**, grep confirms only its own test files reference it.
   Likely the abandoned "unified model" attempt the divergence analysis doc's
   Option 2 called for. Dead code — filed as discovered work below, not
   touched (out of scope, no issue currently tracks its removal).

Net: #1791's fix is scoped correctly to System 1 only; Systems 2/3 are
independent and this change must not touch them.

## Discovered work (report only, not fixed here)

- Systems 2 and 3 above (the semantic-bridge divergence + the orphaned DB-backed
  personality system) are pre-existing findings, already documented in
  `dev/2025/10/27/personality-systems-divergence-analysis.md` (Oct 2025, still
  "Awaiting Chief Architect Decision" per its own footer). Not re-filing a new
  issue — flagging that the decision is still outstanding and this fix makes a
  fourth per-user personality shape exist alongside the other three's structure
  (System 1 now per-user like System 2, System 3 still separate).

## Plan

1. `services/domain/user_preference_manager.py`: add `PERSONALITY_PROFILE` key
   + `get_personality(user_id)` / `set_personality(user_id, profile_dict)`,
   mirroring the `#1574`/`#1876` idiom (`get_reminder_timezone` etc.) — `None`
   from `get_personality` is load-bearing (means "never saved"), not an error.
2. `PiperConfigParser.load_personality_config` / `save_personality_config`
   become `async`. UUID `user_id` → per-user store (`UserPreferenceManager`,
   write/read-through DB). Non-UUID `user_id` (CLI's literal `"default"`) →
   unchanged legacy file path — this **is** the instance-wide default, so
   routing the literal "default" caller there is not a special case, it's the
   same rule (no real per-user identity → no per-user row).
3. Update the three route handlers to `await` the now-async calls; drop
   `require_admin` on PUT → `get_current_user` (write is now scoped to the
   caller's own row, no more global blast radius).
4. Update `cli/commands/personality.py` call sites to `await` /
   `asyncio.run(...)`.
5. Rewrite `TestKnownLimitationDataIsInstanceWide` (1751 file) → per-user
   isolation pin. Rewrite `TestAdminGateOnThePutSurvives` (1751 file) and the
   whole of `test_personality_put_admin_gated_1734.py` (gate is gone) →
   retarget to the new per-user-write-no-admin-needed behavior, same
   discipline #1751 already used once on that same file ("RETARGETED", not
   deleted).
6. New tests: two users answer differently → each gets own profile + default
   fallback for a third, unanswered user; enhancer shapes per-user.
7. `docs/ALPHA_KNOWN_ISSUES.md` — update the #1791 rows.

## Work log

Implemented per plan. Files touched (Lead to review/stage/commit — I made no
git-index changes):

- `services/domain/user_preference_manager.py` — `PERSONALITY_PROFILE` key +
  `get_personality`/`set_personality` (mirrors `get_reminder_timezone`/#1876
  idiom; `None` = never saved, load-bearing).
- `web/personality_integration.py::PiperConfigParser` — `load_personality_config`
  / `save_personality_config` now `async`; UUID `user_id` → per-user store,
  non-UUID (CLI's `"default"`) → unchanged legacy file path (not a special
  case — it's the same "no real principal → instance default" rule). Added
  `load_personality_config_scoped` (returns `(config, "user"|"instance")`)
  and `write_scope_for` so the routes can report an honest `scope` instead of
  the old hardcoded `"instance"` literal.
- `web/api/routes/personality.py` — GET/PUT/`/enhance` `await` the new async
  calls; PUT's `Depends(require_admin)` → `Depends(get_current_user)` (#1734's
  own stated exit condition, now true); `scope` in all three responses is real.
- `cli/commands/personality.py` — CLI methods made `async`, `main()` wraps each
  dispatch in `asyncio.run(...)`. `main()` itself stays sync (pinned by
  `tests/unit/cli/test_cli_commands_runtime_shape_1873.py`, still passes).
- `tests/unit/web/api/routes/test_personality_put_admin_gated_1734.py` —
  RETARGETED (not deleted, matching this file's own prior precedent when
  #1751 retargeted it once already): pins "any authenticated user PUTs
  successfully into their own row only; admin status is irrelevant; the
  shared overlay is never touched; require_admin is provably gone from the
  route's dependency tree."
- `tests/unit/web/api/routes/test_personality_principal_from_session_1751.py`
  — `TestKnownLimitationDataIsInstanceWide` → `TestPerUserDataSeparationNowExists`
  (per that class's own instruction to rewrite, not delete, if this day came);
  `TestAdminGateOnThePutSurvives` → `TestAdminGateIsRemovedFromThePut`. Module
  docstring's DENOMINATOR updated. One other docstring (cross-user-addressing
  test) reworded since "the #1734 gate admits" is no longer the operative
  reason that test matters.
- New: `tests/unit/services/domain/test_personality_preference_persistence_1791.py`
  — real-Postgres test through `UserPreferenceManager`, mirroring
  `test_preference_persistence_1574.py`'s harness exactly (restart survival,
  two real users never collide, `None` for never-saved, JSONB collision safety
  against sibling `upm` keys).
- New: `tests/unit/web/test_personality_integration_1791.py` — `PiperConfigParser`
  + `PersonalityResponseEnhancer` unit tests below the HTTP layer: two users'
  different saved profiles round-trip independently, scope flips
  instance→user after a save, a never-saved user gets the instance default,
  a non-UUID caller still uses the file unchanged, and — the AC's "enhancer
  shapes replies per user" requirement literally — two users with different
  `warmth_level` get *different enhanced text* from the same input string.
- `docs/ALPHA_KNOWN_ISSUES.md` — moved #1791 out of "Known Issues" / the
  multi-tenancy table into "Fixed in 0.8.14", with the no-migration caveat
  stated for testers (re-save your sliders once) and the System-2/System-3
  distinction NOT mentioned (too deep for a tester-facing doc — noted here
  instead).

### Verification (all commands run this session, in this worktree)

- `python -m pytest tests/unit/web/test_personality_integration_1791.py -v` →
  **5 passed**
- `python -m pytest tests/unit/web/api/routes/test_personality_principal_from_session_1751.py tests/unit/web/api/routes/test_personality_put_admin_gated_1734.py -v`
  → **20 passed**, then again after `ruff format` reformatted those 2 files +
  the new one → **25 passed** (re-run to confirm formatting didn't change
  behavior)
- `POSTGRES_PORT=5433 python -m pytest tests/unit/services/domain/test_personality_preference_persistence_1791.py -v`
  (real local Postgres, `docker compose ps` confirmed `piper-postgres` healthy)
  → **5 passed**
- Full grep-matched personality sweep (22 files: CLI, consciousness, #1422,
  #1791 x2, conversational_floor, #1613, #814, #1510, all of
  `services/personality/`, `test_personality_preferences.py`, app_shell #1171,
  #1751, #1734, #1876, #1733, #1791 web) → **427 passed**
- `tests/unit/services/domain/` full directory → **88 passed**
- `ruff check .` → **All checks passed**
- `ruff format --check .` → 3 files needed reformatting (my 3 new/touched test
  files); ran `ruff format` on them, re-`ruff format --check .` → clean; ran
  `./scripts/fix-newlines.sh` per house convention; re-ran the routes test
  suite after reformatting to confirm no behavior change (still 25 passed)
- `mypy` on the 4 touched source files, filtered to lines IN those files
  (mypy follows imports and reports ~900 pre-existing errors elsewhere in the
  tree — not this issue's concern): `web/api/routes/personality.py` and
  `cli/commands/personality.py` → **zero errors**. `user_preference_manager.py`
  → 5 pre-existing errors, all at lines far from my additions (289/324/618/
  626/632; my code is at 113-144 and 1154-1188) — confirmed pre-existing, not
  introduced. `personality_integration.py` → 1 pre-existing `yaml` stub-missing
  note (present before this change, unrelated to my `import uuid`/`UserPreferenceManager`
  additions).
- `python scripts/principal_threading_lint.py` → **rc=0**
- `python scripts/check_silent_death.py --count` → **192** (≤192 gate, unchanged
  from baseline — no new silent-death sites introduced)
- `POSTGRES_PORT=5433 ./scripts/run-sweep.sh smoke` → **559 passed, 1 skipped**
  (pre-existing skip, unrelated warnings from other modules)
- Manual CLI smoke: `python -m cli.commands.personality show --user
  not-a-real-uuid` → ran end-to-end through the new async plumbing, printed
  the instance-default config correctly.

### Discovered work (report only — not fixed, no new issue filed)

1. **Two other, independent personality-preference systems exist** beyond
   #1791's `PiperConfigParser` target (full detail in Investigation section
   above): `PersonalityProfile.load_with_preferences()`
   (`services/personality/personality_profile.py`, already per-user, feeds the
   REAL prompt-shaping path via `services.intent.intent_service`'s
   `formality_baseline` and `services.intent_service.intent_hooks`), and a
   fully-built but **zero-caller** `PersonalityProfileRepository` +
   `PersonalityProfileModel` DB table + `ResponsePersonalityEnhancer`
   (`services/personality/repository.py`, `response_enhancer.py`) — likely
   the abandoned "unified model" attempt from
   `dev/2025/10/27/personality-systems-divergence-analysis.md` (issue #269,
   Oct 2025), whose own footer still says "Awaiting Chief Architect Decision."
   That decision is still outstanding. #1791's fix does not touch either
   system — flagging for Lead/Architect awareness, not filing a new issue
   since the divergence doc already tracks the underlying question.

## Memory & briefing surfaces referenced this session

**Referenced**: `docs/internal/architecture/current/intent-routing-stack.md`
was NOT loaded (this change never touches classification/dispatch — confirmed
via investigation that PiperConfigParser doesn't feed the LLM prompt path, so
the mandatory-read trigger doesn't apply here; noting the check was made, not
skipped). `dev/2025/10/27/personality-systems-divergence-analysis.md` —
informed the discovered-work finding and confirmed scope boundaries.
`tests/unit/services/domain/test_preference_persistence_1574.py` and
`tests/unit/web/api/routes/test_preferences_timezone_1876.py` — both directly
copied as the harness pattern for the new test files (real-DB test shape;
"degrades to in-memory without a live DB" precedent).

**Loaded but not referenced**: CLAUDE.md's worktree/mailbox/sign-off sections
(not applicable — this is a no-commit subagent task).

**Wanted but not found**: nothing — the #1574/#1876 precedent gave a complete
template for both the store design and the test harness.

