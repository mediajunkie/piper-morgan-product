# 2026-09-24 1652 — Coding Agent (prog) — #1799

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated)
**Issue**: #1799 — Priority-metadata source failure degrades honestly in ONE of three renders
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (Lead's — dispatched, not committing)

## Scope (per dispatch)

Own `services/intent_service/canonical_handlers.py`'s PRIORITY renders only. Do NOT
commit/push/touch the git index — Lead reviews and commits. Ignore concurrent dirty
files from other lanes (web/personality_integration.py + personality routes;
services/mcp/consumer/consumer_core.py; services/intent_service/action_registry.py).

Lead's dispatch narrowed the issue's own AC (which also names
`_synthesize_focus_recommendation` and a CXO ruling for EMBEDDED-register copy) to
just the three `_get_priority_metadata` consumers, explicitly directing "reuse the
GRANULAR render's exact sentence/constant; no new copy" for both fixes — bypassing
the CXO-ruling AC item. Followed that narrower scope; did not touch
`_synthesize_focus_recommendation` (issue AC item, not in this dispatch).

## Pre-read

- GitHub issue #1799 (full body) — the dict-flag sentinel family shape, #1777 pointer.
- `docs/internal/architecture/current/intent-routing-stack.md` (partial — first 339
  of 893 lines; enough to confirm this work sits inside surface 4's category-handler
  territory, no routing-surface change needed).
- `grep -rn "source_failed" services/intent_service/canonical_handlers.py` — found the
  three sentinel emission sites (1737-1749) and the one existing correct consumer
  (1231, pre-fix line numbers).
- `docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md` §3 — the
  four-provenance-state table (`fresh` / `verified_empty` / `source_failed` /
  `not_attempted`) confirming `source_failed` must render "I couldn't reach X," never
  "there are none."

## The three consumers — before / after

| Consumer | Spatial pattern | Line (pre-fix) | Rendered on `source_failed=True` BEFORE | AFTER |
|---|---|---|---|---|
| `_format_detailed_priorities` | GRANULAR | 1202-1250 | Checked the flag (1231) — "*I couldn't check your high-priority GitHub issues just now — try again in a moment.*" (correct pre-#1799) | Unchanged text, now sourced from the shared `_PRIORITY_SOURCE_FAILED_NOTE` constant |
| `_format_standard_priorities` | DEFAULT | 1273-1309 | Read `high_priority_issues` (empty from the failure sentinel) → silently omitted the "**Urgent GitHub Issues:**" section entirely. Indistinguishable from a clean check that found nothing. | New `elif priority_metadata.get("source_failed")` branch renders the shared note (same styling as GRANULAR: `\n\n*{note}*`) |
| `_format_consolidated_priorities` | EMBEDDED | 1252-1271 | `len(priority_metadata.get("high_priority_issues", []))` → `0` → the "+N urgent GitHub issues" clause silently dropped; rendered only `base` (e.g. "Top priority: X"). | `source_failed` checked BEFORE the count is derived; returns `f"{base} — {note}"` |

## Fix

Added one shared class-level constant, `CanonicalHandlers._PRIORITY_SOURCE_FAILED_NOTE`
(canonical_handlers.py:1202-1212, just above `_format_detailed_priorities`):

```python
_PRIORITY_SOURCE_FAILED_NOTE = (
    "I couldn't check your high-priority GitHub issues just now — "
    "try again in a moment."
)
```

This is the exact GRANULAR sentence, byte-for-byte (verified via diff-empty render
comparison in tests). All three renders now consult `self._PRIORITY_SOURCE_FAILED_NOTE`
— one helper the three share, not three copies, per the dispatch AC. `_get_priority_metadata`
itself (the sentinel producer) was untouched — the dict-flag form stays as-is per the
dispatch note that #1800 tracks the typed replacement.

Files touched:
- `services/intent_service/canonical_handlers.py` (the three renders + shared constant)
- `tests/unit/services/intent_service/test_priority_degrade_all_renders_1799.py` (new)

## Tests

New file: `tests/unit/services/intent_service/test_priority_degrade_all_renders_1799.py`
— 15 tests, 5 classes:
- `TestGranularSourceFailed` / `TestStandardSourceFailed` / `TestEmbeddedSourceFailed`:
  per-render source_failed / genuine-empty / populated, direct formatter calls
  (mirrors the #1425 test's style).
- `TestSharedConstantNotThreeCopies`: all three renders literally contain the same
  constant string.
- `TestPriorityQuerySeamSourceFailed1799`: patches `_get_priority_metadata` at its
  seam (`patch.object(canonical_handlers, "_get_priority_metadata", new=AsyncMock(...))`)
  and drives the real `_handle_priority_query` entry point for all three spatial
  patterns (GRANULAR / None=DEFAULT / EMBEDDED), pinning the regression at the
  handler boundary, not just inside each formatter unit.

```
$ venv/bin/python -m pytest tests/unit/services/intent_service/test_priority_degrade_all_renders_1799.py -v
...
============================== 15 passed in 0.42s ==============================
```

## Gates run

- New test file: 15/15 passed (above).
- Every test grep-matched on `_get_priority_metadata` / `format_detailed_priorities`
  / `1777` (9 files: test_priority_honest_degrade_1231, test_status_truth_1547,
  test_canonical_handlers, test_degenerate_title_chat_1628, test_guidance_user_clock_1868,
  test_render_truncation_sweep_1762, test_affirmative_false_claims_1425,
  test_sentinel_consumer_sweep_1777, test_gather_cap_denominator_1776):
  **300 passed** in 3.06s.
- `scripts/run-sweep.sh smoke`: **559 passed, 1 skipped**, exit 0.
- `venv/bin/ruff check .`: All checks passed (tree-wide).
- `venv/bin/ruff format --check .`: initially flagged my own two files (pure
  reformatting of code I'd just written — the shared constant's parenthesized string
  and a few long test lines); ran `ruff format` on those two files only (never
  touched any other lane's dirty files); re-checked tree-wide — clean, 1796 files
  already formatted.
- mypy before/after on `canonical_handlers.py` (CI-faithful `venv-mypy-gate/bin/python`,
  `mypy-gate.ini`, per `scripts/run-sweep.sh ratchets`' documented gate — NOT the dev
  venv, which over-reports): swapped the file to `git show HEAD:...` (pre-fix)
  content, ran the gate, restored my edited content, ran again. **117 errors before,
  117 after**, and a line-number-normalized diff of the two error lists is empty —
  identical error set, no new mypy errors introduced by this change.
- `venv/bin/python scripts/check_silent_death.py --count`: **192** (≤ 192 gate, exact
  ceiling, not exceeded).
- `venv/bin/python scripts/check_unscoped_reads.py --count`: **45** before AND after
  (same before/after-swap method as mypy) — did not rise.

## Verified how:

- **Method**: ran each gate command directly this turn and quoted its actual terminal
  output above (test runs, ruff, mypy-gate diff, the two checker scripts' `--count`
  output) — no check reused from earlier in the session.
- **Layer**: unit-level formatter renders (direct string assertions on all three
  functions' output) PLUS one handler-boundary seam test per spatial pattern
  (`_handle_priority_query` with `_get_priority_metadata` patched) — covers both the
  render logic and the wiring that reaches it, not just a curl/import-level smoke.
  mypy and the two checker scripts measure the static-analysis layer, not runtime
  behavior — reported as such, not conflated with the test-pass claims.
- **Denominator**: the new test file covers exactly the 3 consumers named in the
  issue + dispatch (not `_synthesize_focus_recommendation`, out of this dispatch's
  scope). The grep-matched regression sweep covered all 9 files referencing the
  touched symbols/issue number (300 tests, all passing) — not a claim about the full
  suite, which the separate `smoke` run (559 passed) covers on its own narrower
  (CI-gating) denominator. mypy/unscoped-reads counts are codebase-wide static counts,
  compared before/after this one file's change only (other lanes' concurrent dirty
  files were left untouched and not included in either measurement).

## Discovered issues

None. (The issue's own AC items outside this dispatch's scope —
`_synthesize_focus_recommendation`'s `urgent_items`/`open_issues` defaulting to 0 on
failure, and the CXO ruling on EMBEDDED-register copy — are already tracked in #1799
itself, not new discoveries; left them for Lead/CXO per the dispatch's explicit
narrowing.)

## Handback

Reported full three-consumer table, shared helper, test tails, gate results, and
Verified how: to Lead via SubagentHandback. Did not commit or push (dispatch scope:
Lead reviews and commits this file set).
