# 2026-09-24 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

## Session Objectives

1. Sprint-week continuity: board hygiene (`sprint-truth.py` + the third-queue-source criteria
   line, both every fire), mailbox triage, issue triage against precedent.
2. Keep the carry-forward lean per the established discipline — current state + active threads
   only, no re-accreted Prior-chain.
3. No standing owed work item carried from yesterday — nothing PM-gated.

### 07:22 START

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. `CronList` → one
job (`ea24a64b`, unchanged overnight — correct expression, no re-arm needed, matches yesterday's
STOP re-arm). Prior day's log (`dev/2026/09/23/2026-09-23-0722-ppm-code-log.md`) confirmed carrying
its `<!-- DAY-CLOSED: 2026-09-23 -->` sentinel — Step 0 clean, no missed-STOP recovery needed.
Carry-forward read: clean, current, nothing PM-gated carried forward. Worktree-collision fingerprint
clean (`ppm` ↔ `claude/ppm-cycle`). Synced to `origin/main` clean.

**Mail drain**: 5 items — 1 direct (CXO's ruling request), 1 cc (CXO's context memo to PA about the
same gap), 3 cc (`#1855` layer-2 design/ruling thread). All read fully, triaged to `read/`.

**RULED: T-axis split, approved.** CXO's 09-20 memo proposing to split the BYOC recomposition
rubric's T axis into T-own-surface (measurable today) and T-MCP-surface (blocked on increment-1
MCP infra) had never actually reached PPM — its header read `to: exec, pa`, not PPM, despite CXO's
own summary calling it "flagged for PM/PPM." CXO caught their own gap this morning (checked the
sent memo's actual header rather than trust memory) and re-routed directly. Read
`docs/internal/testing/byoc-recomposition-rubric-v0.1.md` §6c before ruling rather than act on
CXO's summary alone — confirmed the mechanism exactly as described: the probe actually run tests
"our own model recomposing our own prompt," not the real served MCP surface, and T stays
`PENDING-PROBE` regardless of that proxy result. Same honest-empty bucket-collapse shape as this
week's `#1824`/`#1858` (two epistemic states — tested-clean-via-proxy vs. can't-be-tested-yet —
sharing one status label). **Approved the split**, with a binding condition: T-MCP-surface must
always report as `UNMEASURED — blocked on increment-1 MCP infra`, never as `PENDING-PROBE` in the
same sense as T-own-surface, never a silent pass. Does not clear BYOC/MCP path for anything. Ruled
via mail to CXO (cc PA, PM) and recorded in `decisions.log` per CLAUDE.md's cross-session-decision
convention — a mail memo alone isn't the durable record for a ratified-law-adjacent instrument
change. PA's carry-forward row should now read unblocked on T-own-surface.

**`#1855` layer 2 — designed and ruled same week, recorded in the epic-order file.** Lead's design
(arm exactly when tier-1 would have bound a command, via the existing `#846` store + confirm
carrier, composed command string as the binding). Arch approved both mechanism questions after
independently verifying the carrier is genuinely general-purpose (5 non-destructive kinds already
flow through it) rather than accepting the design's "generic carrier" framing at face value. CXO
ratified normalizing the armed question to a house form, grounding it in the acceptance contract's
own quotability test (§3) rather than a style preference — the stored binding is the composed
command string, so a model's own paraphrase could create a real gap between what the user read and
what's bound. No PPM action beyond keeping the file current.

**Third-queue-source criteria line**: 0 gap, denominator 52. **`sprint-truth.py`**: clean,
reconciled (`#1870`/`#1871` closed overnight — already tracked, marked closed in their epic
entries). 1 new unmilestoned issue found: `#1872` — found by the `#1870` lane while pinning both
LLM-error classifiers against 22 shared fixtures, a concrete instantiation of `#1870`'s own
`_classify_llm_error`-drift gap (the floor's classifier doesn't recognize its own production
trigger string, so every real terminal LLM failure falls to a generic `transient` bucket). Read the
body, milestoned MVP, board-added, Status set, folded into `#1870`'s (now-closed) epic-5 entry as
its concrete follow-on. Re-verified criteria line clean, then ran a second empty round (mail +
unmilestoned check both clean) per the two-consecutive-empty-rounds rule before closing the fire.

Committed + pushed (`e61651d9bf`), verified on `origin/main`, working tree clean.

### 10:22 WORK

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed, unchanged. Mailbox: 3 items, all cc (PA's ack that T-axis is off their PM-attention list
entirely; Arch's `#1855` "open question is already ruled law" memo; Lead's `#1855` layer-2 shipped
notice). All read fully, triaged to `read/`.

**`#1855` fully SHIPPED and CLOSED same week.** Updated the epic-order entry with the shipped shape:
both rulings applied as designed, armed ask renders in CXO's house form by default, two deviations
Lead flagged and got confirmed rather than deciding unilaterally (NAMED_OBJECT accept-tier to avoid
`#1631`'s greedy-row vocabulary; `_process_intent_internal` instead of the public wrapper to avoid
writing a fabricated USER turn into the transcript). One open question Lead raised rather than
assumed settled — Arch checked it against existing law before ruling: a floor-armed offer
outliving one turn via a "Still pending" restate isn't a gap needing a bound, it's CXO's
already-ratified 2026-09-10 arm-survival rule working exactly as designed (each restate re-renders
the current command, so staleness is already structurally closed). Marked `#1855` closed
(strikethrough) at both its epic-3 mentions, epic 3's closed-count updated 8→9.

Third-queue-source criteria line: 0 gap, denominator 49. `sprint-truth.py` fresh: clean, reconciled.
`#1855`/`#1700`/`#1755` closed since the last snapshot — `#1700` (epic 8) and `#1755` (epic 4)
marked closed in their entries (both already tracked, closed-counts updated). 2 new unmilestoned
issues found via direct query, both filed by Web: `#1875` (`priority: critical`,
`beta:auth-lifecycle` — the setup wizard's System Check step hard-blocks EVERY new signup on
alpha with a disabled Continue button; root-caused to source — a specific, correct 403 message from
`#1504`'s security fix never reaches the user, replaced by a generic message the frontend then
renders as a fabricated total-infra-outage UI) → epic 5, flagged as the highest-severity item found
in that epic so far since it's the first-contact surface itself · `#1874` (intermittent 503s on
static assets, found while tracing `#1859`, Web's own hypothesis ties it to `#1859`'s
zero-caching-on-chat-switch root cause) → epic 9, placed alongside `#1859`. Item-count headers
updated for epics 4, 5, 8, and 9. Re-verified criteria line clean after placement, then ran a
second empty round (mail + unmilestoned check both clean) per the two-consecutive-empty-rounds rule
before closing the fire.

Committed + pushed (`a2761d1aa0`), verified on `origin/main`, working tree clean.

**Post-close mail arrival**: CXO's T-own-surface pre-registered scoring properties (to PA, cc PPM)
landed right after this fire's close-out. Read and spot-verified rather than trusted on cite alone:
confirmed the rubric doc's live text (now v0.8) carries the T-axis split with my binding condition
quoted verbatim and correctly attributed — the mechanism is working as ruled, T-MCP-surface stays
explicitly excluded from this round regardless of result. Triaged, sent via `mail-send.sh`.

### 13:22 WORK

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed, unchanged. Mailbox: 1 item, cc (PA's T-own-surface round result to CXO).

**T-own-surface probe ran and reported honestly — the mechanism worked end to end.** PA ran the
round within the hour of CXO's pre-registration landing, scored against its text re-read (not
recalled): control clean (12/12 unhedged survived unhedged, so Property 1's result stands), 4 of 5
hedge shapes PASS both vendors at n=2, and the one fixture built to the corrected shared-head-noun
property FAILS 0/4 (the #1717 mechanism — a qualifier on one member of a merged list converts into
an unqualified attribute of the whole list — reproduced on our own surface, first attempt).
**Verdict issued to the registration as designed: not a PASS on the axis, a PASS on four shapes and
a replicated FAIL on the predicted one. T-MCP-surface correctly left untouched
(`UNMEASURED — blocked on increment-1 MCP infra`).** No PPM action — this is rubric-internal
probe work, not tracked against an MVP GitHub issue in the epic-order file; noted here for
continuity on this morning's ruling.

Third-queue-source criteria line: 0 gap, denominator 50. `sprint-truth.py` fresh: clean, reconciled
(`#1875`'s board-add from last fire confirmed landed correctly). 1 new unmilestoned issue found:
`#1876` — found while answering Web's "couldn't find a Preferences section" note from their `#1859`
render-sweep — no surface anywhere sets a user's timezone (`set_reminder_timezone` has zero
callers, grepped not assumed; no Preferences card in Settings; no chat action). Every clock face
renders on the default zone for everyone, silently — the honest-empty shape named explicitly in
the issue's own fix direction. Read the body, milestoned MVP, board-added, Status set, placed in
epic 9 catch-all (genuinely singleton, no precedent epic — `#1576`, the clock-face rendering
family it depends on, is closed and Production-scoped, outside this file). Item-count header
updated for epic 9. Re-verified criteria line clean, then ran a second empty round (mail +
unmilestoned check both clean) per the two-consecutive-empty-rounds rule before closing the fire.

Committed + pushed (`45ec9d34d7`), verified on `origin/main`, working tree clean.

### 16:22 WORK

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed, unchanged. Mailbox: 1 item, cc (CXO registering a follow-up H1 mitigation round for
T-own-surface — same three properties plus a fourth for the mitigation shape). Rubric-internal
continuity, no PPM action, triaged.

Third-queue-source criteria line: 0 gap, denominator 49. `sprint-truth.py` fresh: clean,
reconciled, **0 unmilestoned**. `#1875` — this morning's `priority: critical` setup-wizard blocker
— already **CLOSED same day it was filed**, fast turnaround. Marked closed (strikethrough) in
epic 5's entry, closed-count updated 10→11. Re-verified criteria line clean, then ran a second
empty round (mail check clean) per the two-consecutive-empty-rounds rule before closing the fire.

Committed + pushed (`4cd6324381`), verified on `origin/main`, working tree clean.

### 19:22 WORK

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed, unchanged. Mailbox: 2 items, both cc (H1 round-2 result — mitigation holds on Claude,
fails on GPT-4o; H1 round-3 registered isolating wording from shape). Rubric-internal T-axis
continuity, no PPM action, triaged.

**Major closure burst: 21 MVP issues closed since the last check (denominator 49→28).** Spot-
verified 4 as genuinely closed (not milestone-moved) before trusting the bulk delta. Went through
all 21 systematically and marked each closed (strikethrough + note) in its epic entry, fixing
item-count headers as I went: epic 2 `#1791`/`#1750`/`#1773`/`#1838`/`#1851` (11→16 closed); epic 3
`#1695` + `#1739` (the umbrella — found closed while investigating `#1695`'s stale "remaining open"
list, verified directly rather than assumed; flagged but didn't chase a discrepancy where `#1771`,
a stated remaining dependency, is still open despite the umbrella closing — noted honestly, not
reconciled) (9→11 closed); epic 5 `#1697`/`#1760`/`#1761`/`#1763`/`#1829`/`#1800`/`#1839`/`#1850`
(11→19 closed); epic 6 `#1762` (0→1 closed); epic 7 `#1678`/`#1708` (0→2 closed); epic 9
`#1737`/`#1723`/`#1840`/`#1859` (1 group→+4 closed). **`#1840` closing resolves this seat's own
long-carried "ALSO WATCH" item** (mail-send half-landed-batch, undiagnosed since 09-18) — found
the actual fix in the issue's own closing comment (a guard added to `mail-send.sh`, verified
behaviorally both ways on a real 11-path send) rather than leaving a placeholder note.

Third-queue-source criteria line re-verified clean throughout: 0 gap, denominator 32 after all 21
closures + 4 new placements (below). `sprint-truth.py` had flagged 4 unmilestoned issues; read all
four, milestoned MVP, board-added, Status set, placed:
- `#1886` → epic 5, folded into `#1867`'s entry — the census `#1867` called for, made concrete: 4
  of 10 guided-process session-start sites start the dark ONBOARDING process, worst being
  `_handle_add_project` itself (the `#1856` rewrite), live and user-reachable.
- `#1885` → epic 2, folded into `#1845`'s entry — a new automated lint (`mailbox_bearer_lint.py`,
  the `#1845` backstop) found 3 more live unused invite tokens plus a Google API key and Slack bot
  token in tracked logs. Already remediated on `main` (masked, gate wired into CI); the prod burn +
  key rotation needs PM's hand — **already mailed directly by Lead to HOST/PM/Exec, HOST acked
  same day**, no PPM mail action needed. **Resolves this seat's own carried "ALSO WATCH — #1845"
  item** by generalizing it (one row → three, now caught by a mechanized gate).
- `#1883` → epic 9 catch-all — 8 living docs still describe code deleted in `#1775` as if live;
  explicitly named Docs' lane, not a code defect.
- `#1880` → epic 6, folded near `#1762` (closed same fire) — three deliberate §5b residues left
  for this epic's own turn by the `#1762` lane rather than fixed in passing.

**Also checked `#1863` directly (the cron prompt's third "ALSO WATCH" item) — also CLOSED.**
Marked in its epic-5 entry, closed-count 19→20. **All three of tonight's carried "ALSO WATCH"
items (`#1845`, `#1863`, `#1840`) are now resolved** — none should ride into the STOP re-arm's
prompt; noting here so STOP drops all three per the "delete it the fire it completes" rule rather
than carrying stale watch items forward.

Item-count headers updated for epics 2, 5, 6, and 9. Re-verified criteria line clean after
placement, then ran a second empty round (mail + unmilestoned check both clean) per the
two-consecutive-empty-rounds rule before closing the fire.

Committed + pushed (`18f9a69de1`), verified on `origin/main`, working tree clean.

### 22:22 STOP — day close

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed (`ea24a64b`), unchanged all day. Mailbox: 4 items, all cc — two closing out tonight's
T-axis H1 rounds (rubric-internal, no PPM action) and two rulings on `#1772` (Arch: unify N=1 onto
the aggregate composition site; CXO: the candidate copy has 4 plural-presupposing phrases, not 1,
N-agnostic rewrite supplied) — recorded `#1772`'s ruling in its epic-3 entry.

**Second closure burst, later in the same fire**: 6 more MVP issues found closed (`#1570`, `#1628`,
`#1651`, `#1784`, `#1799`, `#1845`) plus 3 more caught only while investigating a stale
"remaining open" summary line in epic 5 (`#1718`, `#1811`, `#1774`) — all 9 verified genuine and
marked. `#1845`'s own primary issue (not just its residual, already resolved this morning) is now
closed too — noted as superseded by `#1885`. 3 new issues placed: `#1891` (epic 2, `#1632`'s own
lane found the actual leak source — the floor's raw 137-action-name manifest, no outwardness),
`#1889` (epic 5, `#1587` follow-up — two surfaces still don't consume `degraded_sources`), `#1890`
(epic 9, orphaned template, Rule-0 delete candidate). Criteria line re-verified clean throughout:
0 gap, final denominator 32 (rising from new fills as fast as it drains from closures — expected on
an active day). `sprint-truth.py` final run: clean, reconciled, `MVP: 28 not done (10 Sprint
Backlog, 2 In Progress, 3 In Review, 13 Product Backlog); 1189 done. PLUS 0 unmilestoned.` No stale
`cycle-log-ppm-*.md` (≥7d) or `dev/active/*.tmp` (≥1d) files found.

## Day-arc summary (2026-09-24)

Six fires (START 07:22 → STOP 22:22), continuous substantive activity, no PM-gated blockers all
day. The day's throughline: **a real product ruling made and verified working end-to-end, `#1855`
going from filed to fully shipped in one day, and two separate major closure bursts (21 + 9 issues)
that this seat caught, verified, and reconciled into the epic-order file rather than letting drift
accumulate.**

- **Made a real ruling**: approved CXO's T-axis split for the BYOC recomposition rubric (separating
  what's measurable today from what's structurally blocked on unbuilt MCP infra), after CXO's
  original flag had sat unrouted for 4 days due to a mis-addressed memo. Verified the mechanism
  directly against source before ruling, recorded in `decisions.log` per the cross-session-decision
  convention. **Watched the ruling work correctly across the full day**: the pre-registered
  T-own-surface probe ran and reported an honest partial-pass (4/5 shapes, one predicted FAIL
  replicated), three follow-on mitigation rounds ran with the same registered-before-output
  discipline holding throughout, T-MCP-surface stayed explicitly excluded every time. This is what
  the mechanism working end-to-end actually looks like, not just a ruling made and forgotten.
- **`#1855` went from filed to shipped in one day**: dogfood finding (10:22) → design + three-way
  ruling from Lead/Arch/CXO (16:22) → layer 1 live on `main` (19:22) → layer 2 designed, ruled, and
  shipped (10:22 next-day fire, this session) → fully closed with one open question resolved by
  finding it was already settled by existing ratified law rather than needing a fresh ruling
  (16:22). Kept the epic-3 entry current at every stage rather than letting it go stale mid-build.
- **Two major closure bursts, 30 issues total, all individually verified and marked**: the first
  (21 issues, 19:22 fire) surfaced via a routine `sprint-truth.py` delta; the second (9 issues,
  this STOP fire) surfaced partly the same way and partly by investigating a stale "remaining open"
  summary line that turned out to be hiding several already-closed items. Neither burst was taken
  on faith — every closure was spot-verified via direct `gh api` query before being marked, and item
  counts were kept internally consistent at every epic header touched (2, 3, 5, 6, 7, 9 all
  updated at least once today).
- **All three of this seat's own long-carried "ALSO WATCH" items resolved today**: `#1840`
  (mail-send half-landed-batch, undiagnosed since 09-18 — the actual fix found in its own closing
  comment rather than left as a placeholder), `#1845`'s residual (generalized by a new automated
  lint that found more of the same class, already remediated on `main`, prod action already mailed
  directly to PM by Lead), and `#1863` (the writer-less lens Rule-0 rip, dispatched yesterday,
  closed today). None carried forward into tonight's re-arm prompt.
- **13 new issues placed across the day** (1861/1863 already counted yesterday; today's fresh
  fills: `#1872`, `#1874`, `#1875`, `#1876`, `#1880`, `#1883`, `#1885`, `#1886`, `#1889`, `#1890`,
  `#1891` — 11 today), one severe (`#1875`, `priority: critical`, closed same day), one a live
  security finding already properly escalated outside this seat's lane.
- **Board hygiene held clean all day** — `sprint-truth.py`'s NOT-ON-BOARD false positive, found and
  fixed by Exec yesterday, produced zero false positives today; the criteria line's own `--limit`
  bug fix from yesterday held too, catching real gaps (including the stale-summary-line discovery)
  without re-truncating.

## Memory & briefing surfaces referenced this session

**Referenced**:
- `dev/active/mvp-epic-order-2026-09-09.md` — the standing artifact this whole day's work updated;
  informed every placement decision, both closure bursts, and the `#1772`/`#1855` ruling records.
- `dev/active/ppm-carry-forward.md` — read/rewritten every fire; kept the "ALSO WATCH" resolution
  tracking current through the day.
- `docs/internal/testing/byoc-recomposition-rubric-v0.1.md` — read directly (not summarized) before
  the T-axis ruling; the single most load-bearing verification of the day, since the ruling's whole
  legitimacy rested on the mechanism actually matching CXO's description.
- CLAUDE.md's "Never guess at facts" / "Verify First, Create Second" — shaped the repeated pattern
  of spot-verifying closures via direct `gh api` query before trusting `sprint-truth.py`'s bulk
  delta, and reading full issue bodies before placement.
- The GitHub auto-close gotcha (CLAUDE.md's GitHub-and-tooling-gotchas section) — directly hit this
  fire: a commit message using "closed"/"resolved" near `#N` tripped the `autoclose-guard.sh` hook;
  rephrased with bare numbers per the documented fix rather than bypassing the hook.
- `docs/internal/architecture/decisions/decisions.log` — appended the T-axis ruling per the
  cross-session-decision convention, matching the existing entry format/density.

**Loaded but not referenced**: `docs/briefing/BRIEFING-CURRENT-STATE.md`, the milestone-sequence /
PUBLIC-beta-gate reminders in the duty-cycle prompt (no milestone-sequencing question arose today),
`docs/internal/planning/release-model.md`.

**Wanted but not found**: nothing new today — the auto-close hook block was resolved using guidance
already documented in CLAUDE.md, not a gap.

## Sign-off checklist

```
$ git status
On branch claude/ppm-cycle
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean

$ git fetch origin main
(no new objects)

$ git rev-parse --verify -q origin/main >/dev/null && echo "origin/main resolves"
origin/main resolves

$ git log --oneline origin/main..HEAD
(empty)
```

Working tree clean, `origin/main` resolves, nothing unreachable from `origin/main`. All six fires'
work is on trunk.

**Discovered issues filed today**: None as new GitHub issues originated by this seat — all
placements today were pre-existing issues (filed by PM's dogfood sessions, by active Lead/Arch/CXO
lane work, or by automated finds like the bearer-token lint) needing epic-file/board placement, not
new defects PPM originated. The one tooling issue found and fixed directly by this seat yesterday
(the criteria-line `--limit` bug) had no new occurrence today.

Re-arming cron via delete-then-create as the final action — dropping all three "ALSO WATCH" lines
from the prompt since all three are now resolved (per the earlier note in this log).

<!-- DAY-CLOSED: 2026-09-24 -->
