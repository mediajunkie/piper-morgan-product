# Session Log — 2026-09-24 18:16 — Coding Agent (prog) — #1735

- **Role**: Coding Agent (prog)
- **Model**: Opus (dispatched by Lead, tier stated)
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
- **Issue**: #1735 — personalization "learning loop" disconnected at every joint
- **Scope**: Part 1 re-census at HEAD; Part 2 fix the defects (not the design decision); decision memo.
- **Constraint**: do NOT commit/push, do NOT touch the git index — Lead reviews and commits.

---

## Part 1 — re-census at HEAD (verify-first)

Findings recorded in the decision memo
`dev/2026/09/24/1735-learning-loop-census-and-decision-2026-09-24.md`.

## Work entries

- 18:16 — session start, read #1735 + comment, began census.
- 18:19 — **RED baseline captured.** New pins
  `tests/unit/services/intent_service/test_auto_apply_preferences_1735.py` driven with a real
  auto-apply-ready `PreferenceHint` against a real per-run DB user: **5 failed, 1 passed**. Load-bearing
  failure `assert None == 'DETAILED'` at the STORE layer — while `result["success"] is True` and
  `result["errors"] == []` both passed in the same run. The handler reported a clean success with an
  empty store. Defect confirmed at HEAD `29c64a86d3`, 16 days after the 09-08 trace.
- 18:2x — **Fix landed** in `services/intent_service/preference_handler.py::apply_auto_preferences`:
  (1) pattern carries the gate's real shape (`confidence` from the hint, `pattern_data` with
  `preference_key=personality_{dimension}` — the same key `confirm_preference` writes);
  (2) USER scope with `session_id` deliberately not forwarded (`set_preference` routes on session_id
  presence *before* reading `scope`, so the old code would have landed every live auto-apply in the
  in-memory session map even after the key bug was fixed);
  (3) the return value of `apply_preference_pattern` is now READ — rejection logs WARNING and produces
  an `errors` entry with a reason, never a fabricated `applied` row. A below-bar hint stays a silent
  skip on purpose (a correct skip reported as an error is noise).
  **GREEN 9/9** in that file.
- 18:2x — **Part 2(b): no residual defect found.** All three readers #1735 names
  (`calendar_setup_offered`, `slack_default_channel`, `notion_database`) already route through
  `get_preference(key, user_id=…)` → `_ensure_user_loaded` → `users.preferences["upm"]`; #1574 fixed
  the mechanism under all three at once. What was missing was *evidence* — #1574's own pins use the
  generic accessor with key `"timezone"`. Added `TestNamedResidualReadersAreDbBacked`: three pins at
  the typed-accessor layer across a fresh-manager boundary. Green.
- 18:2x — Adjacent suites: `tests/unit/services/personality/ + domain/ + intent_service/` →
  **4913 passed** in 179.61s, exit 0.
- 18:3x — Decision memo written:
  `dev/2026/09/24/1735-learning-loop-census-and-decision-2026-09-24.md` — four-joint re-census table
  (09-08 vs NOW, each row with file:line + a quoted probe), what was fixed, and the ONE remaining
  question (`personality_*` has two durable writers and zero readers) with a recommendation (option A,
  bridge `_create_from_preferences`, ~30 lines) and its cost/risk.

## Verification tails

```
# my pins (RED baseline, before the fix)
5 failed, 1 passed in 1.08s
#   assert None == 'DETAILED'   <- the store layer; result["success"] was True in the same run

# my pins (after)
9 passed in 2.43s

# tests/unit/services/personality/ + domain/ + intent_service/
4913 passed, 18 warnings in 179.61s (0:02:59)        [exited with code 0]

# tests/integration/services/personality/test_preference_detection_e2e.py
10 passed in 0.66s

# scripts/run-sweep.sh smoke
559 passed, 1 skipped, 14161 deselected, 90 warnings in 34.71s   [exited with code 0]

# scripts/run-sweep.sh ratchets
67 passed in 22.19s
mypy gate: all 24 ratcheted codes at ceiling (total=1121; ...)    [exited with code 0]

# scripts/check_silent_death.py
silent-death guard: 190 unannotated broad no-reraise handlers on the core path
  (ceiling in scripts/ratchet_ceilings.json is 190 — UNCHANGED, no new best-effort except added,
   no `# silent-ok:` annotation needed, no ceiling lowered)

# ruff
All checks passed!   /   2 files already formatted

# mypy (venv-mypy-gate/bin/mypy --config-file mypy-gate.ini, same invocation both times)
before: 853 total across the transitive graph; 4 in the two touched modules
after:  853 total across the transitive graph; 4 in the two touched modules
```

⚠️ **Attribution caveat (stated, not assumed)**: this worktree also held another lane's in-flight
edits (`services/connectors/binding_repository.py`, `server_ref_resolver.py`,
`binding_write_guard.py` and their tests) throughout. Every suite above passed, so nothing is masked
by them — but the 4913 figure is not an isolated measurement of my change alone.

## Files (mine only — the connectors/* changes in `git status` belong to another lane)

- `services/intent_service/preference_handler.py` (M)
- `tests/unit/services/intent_service/test_auto_apply_preferences_1735.py` (new)
- `dev/2026/09/24/1735-learning-loop-census-and-decision-2026-09-24.md` (new)
- `dev/2026/09/24/2026-09-24-1816-prog-code-1735-log.md` (new — this file)

## Discovered issues (report-only, not filed)

1. Two timezone homes: `users.preferences["timezone"]` (#1572 login capture, read by
   `get_user_timezone`, used by `todo_handlers` for due-date interpretation) vs
   `users.preferences["upm"]["standup_reminder_timezone"]` (#1876, what the chat action and
   `PUT /api/v1/preferences/timezone` write). Saying "set my timezone to Helsinki" in chat does not
   change todo due-date interpretation. Grep-layer finding, not run end-to-end.
2. `PreferenceConfirmation` is constructed and discarded in `apply_auto_preferences` — no audit trail
   for an auto-applied preference.
3. `_SESSION_HINTS` (`preference_handler.py:37`) is a module-level dict with TTL enforced only on read
   — unconfirmed suggestions are never collected.
</content>
</invoke>
