# Session Log — 2026-09-24 0708 — Coding Agent (prog) — #1855 layer 2

**Role**: Coding Agent (prog)
**Model**: Opus (dispatched by Lead, tier stated)
**Issue**: #1855 layer 2 — the floor ARMS what it offers
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Constraint from dispatch**: do NOT commit/push, do NOT touch the git index. Lead reviews and commits.

## Plan

1. Mandatory pre-reads (routing stack, design doc, Arch approval memo, layer 1 as shipped, accept path, layer-1 tests + #1856 handler path)
2. Implement the `arm` outcome at the seam + callback threading from IntentService (four doors)
3. `ARMED_QUESTION_FORM` module constant (default None)
4. `FLOOR_BOUND_OFFER_KIND` accept path via the confirm carrier
5. `revise_draft()` log-only detector guard
6. Tests: tests/unit/services/intent_service/test_floor_armed_offer_layer2_1855.py
7. Docs: intent-routing-stack.md #1855 paragraph
8. Gates

## Work

- (0708) Session start. Pre-reads: intent-routing-stack.md (#1855 para), the layer-2 design,
  Arch's both-approved ruling memo, layer 1 as shipped (`unarmed_offer.py`, `_armed_offer_signal`,
  `FloorContext.armed_offer`, the seam call in `respond()`), the accept path
  (intent_service.py 1222–1706 + `run_confirm_pending_action_workflow` + `acceptance.py`),
  layer-1 tests, and the #1856 handler path.
- Key read-not-guessed finding: the accept seam threads `pending_offer.get("question")` into
  `evaluate_acceptance(armed_question=...)` — NOT `offer_message`. The armed record therefore
  stores the rendered ask under `question` (with `offer_message`/`ask_rendered` mirroring the
  design's names).
- (0815) Implementation complete:
  - `unarmed_offer.py`: catalogue restructured into `COMMAND_FAMILIES` (`CommandFamily` NamedTuple
    with name/matches/bind/template), `bind_catalogued_command`, `FLOOR_BOUND_OFFER_KIND`,
    `ARMED_QUESTION_FORM` (default None), `build_floor_bound_offer_record`, `_try_arm`, and the
    `arm_offer` parameter on `enforce_armed_offers` (third outcome).
  - `conversational_floor.py`: `FloorContext.arm_offer`, threaded into the seam call;
    `revise_draft()` gained the log-only `floor_offer_in_revise_draft` guard.
  - `intent/intent_service.py`: `_floor_arming_callback`, threaded at all four floor doors;
    `floor_bound_offer_abandoned` in the off-intent log-name map.
  - `workflow_entries.py`: `_run_floor_bound_offer` branch on the confirm carrier.
  - `destructive_confirm.py`: the kind joins `_CONFIRM_KINDS` (#1664 honest snapshot rendering).
  - `docs/internal/architecture/current/intent-routing-stack.md`: new layer-2 paragraph.
  - New suite `tests/unit/services/intent_service/test_floor_armed_offer_layer2_1855.py` (33 tests).
- Deviation from the dispatch prompt, deliberate and reported: the carrier calls
  `_process_intent_internal`, not `process_intent`. The public wrapper records the message it is
  given as a USER turn and saves it (#563/#1122), so re-running the command through it would write
  a sentence the user never typed into the durable transcript and save the reply twice.
- Gates: 4967 passed on the intent/standup slice; 919 on every conversational_floor-naming test;
  arch enforcement 53 passed; smoke 529 passed/1 skipped; ruff check + format clean tree-wide;
  mypy per-file counts identical before/after; threading lint rc=0; silent-death 192 (one new
  broad except, annotated `# silent-ok:` per the ratchet's own mechanism).
- (0845) Final re-run after formatting: 5030 passed
  (`tests/unit/services/{intent_service,intent,standup}/` + `test_architecture_enforcement.py`
  + `test_completion_ratchets.py`). Handing back to Lead for review and commit; nothing staged,
  nothing committed, index untouched.
