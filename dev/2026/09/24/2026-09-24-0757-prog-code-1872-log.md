# Session Log — 2026-09-24 0757 — Coding Agent (prog) — Issue #1872

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated)
**Issue**: #1872 — floor's LLM-error classifier doesn't recognize its own production trigger string
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle, not committed by me — Lead reviews/commits)

## Task

Fix at the producer (`services/llm/clients.py`) with a typed exception, then
delegate the floor's text classification to a shared function in
`user_friendly_errors.py`, add Gemini's invalid-key pattern, write tests, run
the full gate list, update the routing-stack doc.

## What shipped

1. **`services/llm/clients.py`**: new `AllProvidersFailed(RuntimeError)` class
   carrying `attempts: list[tuple[provider, reason]]`, `.no_providers`
   (honest, read off `attempts`), `.primary_reason` (first attempt's reason).
   `_complete_raw`'s terminal "all providers failed" raise now builds
   `attempts` as tuples instead of pre-joined strings and raises the typed
   exception. `str(exc)` verified byte-identical to the pre-#1872 message.

2. **`services/ui_messages/user_friendly_errors.py`**: new exported
   `classify_llm_error_text(text) -> bucket` — the floor's old string-matching
   branches, moved here verbatim (order preserved). New `GEMINI_INVALID_KEY_
   PATTERN` and `INVALID_KEY_PATTERN` constants (the latter = translator's
   original invalid-key regex + the Gemini addition), consulted by BOTH the
   translator's own pattern dict (replaced inline regex with the named
   constant) and `classify_llm_error_text`'s rejected_credential check.

3. **`services/intent_service/conversational_floor.py`**: `_classify_llm_error`
   now only does the two TYPE-based checks (`ConsentUnreadableError`,
   `AllProvidersFailed`) and delegates every string case to
   `classify_llm_error_text`. Removed ~35 lines of duplicated string-matching
   logic.

4. **Tests**:
   - New `tests/unit/services/llm/test_all_providers_failed_1872.py` (10
     tests): exception shape (str byte-identity, RuntimeError subclass,
     attempts round-trip, no_providers/primary_reason), and real
     `_complete_raw` integration (single-provider failure, primary+fallback
     failure with order preserved, floor classifies the real raised object
     correctly end-to-end).
   - `tests/unit/services/intent_service/test_llm_error_classifier_agreement_1870.py`:
     updated the `1870_gemini_400_invalid` pinned fixture from
     `("transient", "unknown")` → `("rejected_credential", "llm_key")`
     (resolved via item 3's shared pattern). Added `PINNED_TYPED_FIXTURES` (4
     fixtures constructed via the real `AllProvidersFailed` type) + a new
     `TestTypedExceptionResolvesStandoutFinding` class asserting the
     resolution and that none of them read "transient" unless genuinely
     transient. Docstring's FINDINGS section rewritten: (a) and (d) moved
     from "not fixed" to "FIXED", (b)/(c) untouched (explicitly not resolved,
     per the issue's own instruction not to pick a copy winner).
   - `tests/config/test_llm_key_validation_reasons_1870.py`: the Gemini
     save-time-validation test's assertion flipped (was asserting the gap —
     `humanize_validation_result(result) != INVALID_KEY_SENTENCE` — now
     asserts the fix: `== INVALID_KEY_SENTENCE`). Docstring updated to say
     FIXED by #1872 item 3.

5. **`docs/internal/architecture/current/intent-routing-stack.md`**: new
   subsection "The floor's LLM-ERROR path — a fifth, orthogonal
   classification, not a routing surface (#1872)" inserted before "## The
   vocabularies", describing the producer fix, the delegation, and which
   pinned disagreements stay unresolved and why.

## Catch/match site audit (grep "All configured LLM providers failed" across services/ web/ tests/)

- `services/llm/clients.py:442` — the producer (now `AllProvidersFailed`).
- `web/api/routes/intent.py::_extract_degradation_message` — string-matches
  `error_str` (from `str(error).lower()`); unaffected since `str()` is
  byte-identical. Verified via `test_classification_failure_honest_error_1414.py`
  (passes unchanged).
- `services/ui_messages/user_friendly_errors.py` — the translator's own
  pattern dict entry `r"all configured llm providers failed"`; unaffected,
  still matches the byte-identical string for any case none of the more
  specific earlier patterns claim.
- Six test files that string-match or `pytest.raises(RuntimeError, match=...)`
  against this exact text (`test_provider_selection_1415.py`,
  `test_serving_record_1676.py`, `test_llm_error_buckets_1824.py`,
  `test_llm_error_classifier_agreement_1870.py`,
  `test_llm_key_honest_degrade_1381.py`) — all pass unchanged; `RuntimeError`
  subclassing was the deliberate choice (not bare `Exception`) specifically
  so the two `pytest.raises(RuntimeError, ...)` sites keep matching.
- No other production code catches this by type or string (only generic
  `except Exception` sites throughout, unaffected by subclassing).

## Mismatch list before/after (test_llm_error_classifier_agreement_1870.py)

| Fixture | Before | After | Resolved by |
|---|---|---|---|
| `1870_gemini_400_invalid` | floor=transient, translator=unknown (shared gap) | floor=rejected_credential, translator=llm_key (AGREE) | item 3: shared `INVALID_KEY_PATTERN`/Gemini addition |
| `real_all_providers_failed` (bare `Exception`, no type) | floor=transient, translator=llm | **unchanged, deliberately** | text alone still can't disambiguate 3 causes — this is why the fix is type-based |
| 4 new `1872_typed_*` fixtures (real `AllProvidersFailed`) | n/a (didn't exist pre-#1872; the type didn't exist) | invalid-key→rejected_credential/llm_key, connection→transient/llm (honest), quota→quota_exhausted/llm_key, zero-providers→no_provider/llm | items 1+2: typed dispatch |
| (b) bare status words, (c) config_endpoint | disagree | **unchanged, deliberately** | CXO/PM copy calls, explicitly out of scope |

Net: 1 existing mismatch resolved (Gemini), 4 new fixtures added all agreeing/honest where previously the type didn't exist to test, 2 categories of disagreement explicitly left alone per the issue's instruction not to pick a copy winner.

## Test tails (all green)

- `tests/unit/services/llm/test_all_providers_failed_1872.py` — 10 passed
- `tests/unit/services/intent_service/test_llm_error_classifier_agreement_1870.py` — included in the 4483-pass `tests/unit/services/intent_service/` run
- `tests/unit/services/intent_service/test_llm_error_buckets_1824.py` — included, passed
- `tests/unit/web/test_any_provider_gate_1823.py` — 71 passed (combined run), untouched by design
- `tests/unit/web/api/test_classification_failure_honest_error_1414.py` — passed
- `tests/config/test_llm_key_validation_reasons_1870.py` + `_1718.py` — passed (after the one deliberate flip)
- `tests/security/test_store_user_key_honest_validation_message_1718.py`,
  `test_validate_rotate_detailed_validation_1870.py`, `test_user_api_key_service.py`,
  `test_key_storage_validation.py` — passed
- `tests/unit/services/intent_service/` (whole dir, covers every
  `conversational_floor`-naming test) — 4483 passed
- `tests/unit/services/llm/` (whole dir) — 111 → 125 passed (with new file)
- `tests/unit/services/ui_messages/` (whole dir) — 64 passed
- `scripts/run-sweep.sh smoke` — 529 passed, 1 skipped, 0 failed
- `ruff check .` — All checks passed
- `ruff format --check .` — 1794 files already formatted (after auto-fixing 2 files mid-session)
- `venv/bin/python scripts/principal_threading_lint.py` — rc=0
- `venv/bin/python scripts/check_silent_death.py --count` — 192 (≤192 threshold, unchanged)
- mypy on the 3 touched service files: 2 pre-existing `union-attr` errors in
  `clients.py` at lines 250/279 (`self._output_filter.filter` — `Any | None`),
  confirmed via `git diff -U0` to be outside every edited hunk (my hunks: 67-70,
  382-437). 0 errors in `user_friendly_errors.py` and `conversational_floor.py`,
  before and after. **0 new mypy errors introduced.**

**Not run**: `tests/intent/` (contract suite) — started speculatively, killed
after ~8 min (not in the task's required list; grepped and confirmed no file
in that directory references `AllProvidersFailed`, `_classify_llm_error`,
`classify_llm_error_text`, or `user_friendly_errors`, so it's not exercising
this change).

## Verified how

- **Method**: ran the exact pytest/ruff/mypy/lint commands listed above this
  turn and quoted their actual tails; constructed the typed exception and the
  4 illustrative scenarios from the issue directly in a Python REPL before
  writing any test, to confirm the fix's behavior first-hand.
- **Layer**: pure-unit for the classifier/exception-shape tests (no network);
  the `_complete_raw` integration tests patch only `_call_provider`, so real
  selection/fallback-ordering/consent-filtering code runs; `scripts/run-sweep.sh
  smoke` exercises a broader slice including route-level tests.
- **Denominator**: every existing test file that string-matches or type-matches
  the producer's message (6 files, listed above) re-run and green; the full
  `tests/unit/services/intent_service/` directory (4483 tests, covers every
  `conversational_floor`-naming test per the task's request) and
  `tests/unit/services/llm/` + `tests/unit/services/ui_messages/` directories
  in full, not just the named files. mypy denominator: the 3 touched files
  only (not the whole tree — the whole-tree run surfaces ~900 pre-existing
  errors in unrelated modules mypy pulls in transitively, none touched by
  this diff).

## Discovered issues (report only, not filed — flagging for Lead)

1. `web/api/routes/intent.py::_extract_degradation_message` (line ~136) checks
   `"database" in error_str or "connection" in error_str or "timeout" in
   error_str` BEFORE the LLM-aware branch (line ~144). A real
   `AllProvidersFailed` whose `Details:` suffix contains "Connection error"
   (the exact CLAUDE.md env-var-shadowing symptom this issue's own example
   cites) would match the database/connection branch first and get "Database
   service is temporarily unavailable. Please ensure Docker containers are
   running and try again." — wrong advice for an LLM outage. Pre-existing
   (unchanged by #1872, since `str()` is byte-identical), not caused by this
   change, but worth a follow-up issue since #1872's own fix makes the
   Details-carrying string more likely to trigger it precisely in the
   scenario the issue describes.

## Memory & briefing surfaces referenced this session

**Referenced**: `docs/internal/architecture/current/intent-routing-stack.md`
(mandatory pre-read, floor's error path — confirmed no existing coverage,
added new subsection); `tests/unit/services/intent_service/test_llm_error_
classifier_agreement_1870.py` (the pinned mismatch table this issue shrinks —
read in full before touching); CLAUDE.md worktree/sign-off discipline (this
agent doesn't commit — Lead reviews).

**Loaded but not referenced**: role-table briefings, mailbox discipline
sections (no mail sent — pure implementation task, Lead-dispatched).

**Wanted but not found**: none — the pre-read list in the dispatch prompt was
sufficient; no gaps hit during implementation.
