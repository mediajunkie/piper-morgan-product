# Session Log — 2026-09-03 18:43 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: #1688 — FTUX empty-state interview (Leg D increment 1, cold-start reflection), web-chat half
**Branch**: claude/lead-cycle (Lead's worktree; commit, no push, no close-keywords)

## Binding inputs read (in order)
1. `gh issue view 1688 --comments` — issue body + 5 comments (incl. copy-spec landing + PPM ruling comment)
2. CXO copy spec v0.2: `docs/internal/design/ftux-mcp-first-turn-copy-2026-09-02.md`
3. CXO v0.2 amendment memo: `mailboxes/lead/inbox/amend-cxo-to-lead-cc-ppm-arch-pm-1688-copy-v02-...-2026-09-03.md`
4. PPM scope ruling: `mailboxes/lead/inbox/ruling-ppm-to-lead-cc-cxo-arch-pm-1688-why-asking-promise-is-out-of-scope-cut-it-2026-09-03.md` — **CUT `why_asking` entirely**
5. FTUX model + surface mapping docs

## Scope-honesty verification (MCP half)
- `services/mcp/` is CONSUMER-side only: `consumer/` (github/calendar/slack/notion adapters, mcp_client for
  self-hosted github-mcp-server per ADR-070C), `protocol/` (protocol_client, service_discovery — client role).
- No FastMCP / served-server module anywhere in services/ or web/; `mcp_server_url` feature flag is the URL of
  the EXTERNAL github-mcp-server the app consumes (intent_service.py:6631 comment confirms).
- Increment-1 MCP prerequisites per PPM ("hosted MCP server + fail-closed caller identity + users table +
  1-2 read tools"): NONE exist. → MCP half reported blocked-on-infra; building the WEB-CHAT half completely
  per Lead's instruction.

## Build plan (web-chat empty-state half)
- Extend `services/intent_service/first_contact.py` (sibling of #1536 mechanism it extends):
  copy literals (v0.2 minus why_asking), `is_cold_user` (zero configured integrations via
  IntegrationStatusService.get_all — binding-first, #1547), `ftux_interview_greeting` (first-exchange +
  cold gate, fail-closed), `build_ftux_interview_offer` (#846 carrier, #1654 idiom, #1665 verbatim question),
  `handle_ftux_interview_turn` (offer-seam binding: decline/exit/command release; accept re-asks verbatim;
  answer BINDS into ConversationContext then routes to floor for engaged reply).
- `conversation_context.py`: `ftux_interview_answer` field + #953 persistable-state round-trip (session-scoped).
- `context_assembler.py`: merge bound answer into floor domain context + provenance.
- `conversational_floor.py`: _format_domain_context guidance — within-session use, no persistence claims
  (#1648), no promise trigrams, no example reply sentences (#1655).
- `conversation_handler._respond_to_greeting`: cold first-exchange greeting → interview owns the moment
  (replaces canned greeting; no competing question); carrier offer rides result.
- `canonical_handlers._handle_conversation_query`: pass through offer.
- `intent_service.py`: arm offer at canonical seam; kind branch at offer seam; `_pending_flags` +
  `_abandon_names` entries.
- Tests: `tests/unit/services/intent_service/test_ftux_interview_1688.py` — literals pinned, promise
  language pinned ABSENT, cold/non-cold gating, binding semantics, floor guidance; run first-contact/floor/
  1635/seed-guard/1654 suites + ruff.

## Work

### Shipped (all tests green before commit)
- `services/intent_service/first_contact.py` — #1688 interview section: pinned CXO v0.2 literals
  (opening_line + question; `why_asking` CUT per PPM ruling, not present anywhere), `is_cold_user`
  (zero configured integrations via IntegrationStatusService.get_all, fail-closed both directions),
  `render_ftux_interview` (deterministic, the two strings only), `build_ftux_interview_offer`
  (#846 carrier, kind `ftux_interview_question`, #1665 verbatim question on the record),
  `ftux_interview_greeting` (first-exchange + cold gates), `handle_ftux_interview_turn`
  (offer-seam binding: decline/bare-exit/pre-classifier-claimed-command/principal-mismatch release
  UNBOUND; bare accept re-asks verbatim + re-arms; substantive answer binds WHOLE into
  ConversationContext then routes to floor — no new extraction regex, ratchet untouched).
- `services/intent_service/conversation_context.py` — `ftux_interview_answer` field + #953
  persistable-slice round-trip (session-scoped; mid-session restart survival is not cross-session).
- `services/intent_service/context_assembler.py` — bound answer rides every floor-bound turn
  (outside category dispatch, #1536/#1566 pattern) + provenance source entry.
- `services/intent_service/conversational_floor.py` — `_format_domain_context` guidance for the
  bound answer: within-session use, never claim saved/remembered, never promise future-session
  recall; rule stated without reciting examples (#1655) and without promise vocabulary (#1570).
- `services/conversation/conversation_handler.py` — cold first-exchange greeting → interview
  REPLACES the canned greeting (owns the empty moment; no competing question); carrier +
  `ftux_interview_question_pending` flag ride the result.
- `services/intent_service/canonical_handlers.py` — `_handle_conversation_query` passes the offer
  through.
- `services/intent/intent_service.py` — canonical seam arms the offer (greeting handler has no
  store access); kind branch at the offer seam (bound answers → `_handle_unknown_intent` floor
  dispatch, deterministic, never re-classification); `_pending_flags` + `_abandon_names` entries.
- Tests: `tests/unit/services/intent_service/test_ftux_interview_1688.py` (29 tests — copy pins
  verbatim, promise language pinned ABSENT incl. floor guidance, cold/non-cold/warm/no-principal
  gating, greeting ownership, all seam release/bind/re-ask paths, assembler + floor within-session
  use, #953 round-trip, wiring pins). Updated `test_conversation_context_persist_953.py` pinned
  key set (deliberate slice extension, commented).

### Test evidence
- `tests/unit/services/intent_service/ + test_architecture_enforcement.py + test_prompt_seed_guard.py`:
  **3735 passed** (includes dispatch-site ratchet — untouched — and seed guard).
- Constraint/neighbor batch (seed guard, 1635 coming-soon suppression, 1536/1615/1590 first-contact,
  1654, 1665, 1416, 1648, 1544, 1639, 1529, offer accept/decline): **210 passed**.
- `tests/unit/services/radar/ + tests/conversation + tests/unit/services/intent`: **110 passed**.
- Calendar threading + standup + pattern-073 + pre-classifier: **455 passed**.
- Pinned ruff 0.6.9 (scratchpad ruffenv): `format --check .` clean (after formatting my 2 files),
  `check .` all passed.

### MCP half — blocked on infra (reported, not scaffolded)
`services/mcp/` is consumer-side only (adapters + protocol client; `mcp_server_url` = the external
github-mcp-server we consume). No hosted/served MCP server, no fail-closed caller identity, no MCP
users table, no served read tools exist. Per Lead's instruction: web-chat half built completely;
MCP half named as blocked.

### Not staged (not mine)
`dev/active/canonical-retest-serving-llm.json`, `uploads/ac61e65c-…/` — pre-existing untracked
files from other sessions; left alone.

## Discovered issues filed
None (no new tracking issues needed; the MCP-infra gap is already the subject of PPM's increment
sequencing and reported to Lead in the wrap report).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (worktree/commit/mailbox discipline, seed-guard + ratchet constraints,
  evidence discipline); issue 1688 + comments (spec skeleton + PPM ruling); CXO copy spec v0.2
  (literals); CXO amendment + PPM ruling memos (why_asking cut); #1654 carrier idiom in
  todo_handlers.py (handler shape); #1536 first_contact.py + tests (mechanism + test conventions);
  #1655 seed-guard test (guidance discipline); #1664/#1650 confirm-kind notes (open-question
  semantics); #1665 rendered-ask rule (question stored verbatim); m-43/m-44 (layer-honest test
  header). ftux-experience-model + surface-mapping docs (interview owns the empty moment).
- **Loaded but not referenced**: memory index entries beyond the above; MCP server instructions
  (Airtable/Figma/OpenLaws).
- **Wanted but not found**: a written probe record for which messages PreClassifier deterministically
  claims (relied on #1654's docstring probe notes from 2026-08-22).
