# Session Log — 2026-09-03 21:53 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Make PPM's 1688 HOLD ruling mechanically real — gate the FTUX
empty-state interview (merged 9067e4382) behind `PIPER_FTUX_INTERVIEW`,
default OFF, so the next deploy cannot carry it live.

## Work

- **Gate point chosen**: the entry decision in
  `services/intent_service/first_contact.py::ftux_interview_greeting` — a
  single `ftux_interview_enabled()` check as the FIRST gate, ahead of the
  newness and cold probes. One gate suffices structurally: with the flag
  off no interview renders and no #846 carrier ever arms, so the
  offer-seam handler in `services/intent/intent_service.py` (kind
  `ftux_interview_question`) is unreachable — it only fires on a pending
  offer the greeting alone can arm.
- Flag: `PIPER_FTUX_INTERVIEW`, truthy vocabulary `{"1","true","on","yes"}`
  matching `PIPER_INVERSION_SHADOW` (inversion_shadow.py), read at call
  time never import time. Comment block carries the ruling reference: held
  per PPM 2026-09-03 (1658 precedent); flip-on is a PM/PPM decision.
- Tests (`tests/unit/services/intent_service/test_ftux_interview_1688.py`):
  - autouse fixture sets the flag ON file-wide so the existing 29 pins
    keep pinning the build-as-merged behavior (minimal, honest change).
  - New `TestFlagDefaultOff` (3 pins): flag vocabulary; default-off gate
    short-circuits BEFORE the cold probe (explosive status boundary +
    `assert_not_called` — the explosion alone can't distinguish because
    `is_cold_user` swallows exceptions); default-off cold first-exchange
    greeting is **byte-identical** to
    `format_greeting_conscious(calendar_summary=None)` (deterministic —
    no randomness in the composer), with no interview strings, no
    `ftux_interview_offer`, no pending flag.

## Evidence

- `pytest tests/unit/services/intent_service/test_ftux_interview_1688.py -q`
  → 32 passed (29 existing + 3 new)
- `pytest tests/unit/services/intent_service/ -q` → **3696 passed**
- `pytest tests/test_architecture_enforcement.py tests/test_prompt_seed_guard.py -q`
  → **45 passed** (both ratchets + seed guard)
- Pinned ruff 0.6.9 (scratchpad ruffenv): format applied, `check` → All
  checks passed.

## Discovered issues filed

None.
