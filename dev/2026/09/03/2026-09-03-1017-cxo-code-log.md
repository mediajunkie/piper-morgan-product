# CXO Session Log — 2026-09-03 (Thursday)

**Role**: Chief Experience Officer (CXO) | **Tool**: Claude Code | **Model**: Opus 5 (1M context)
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `8207809c` (expires ~09-06 — 🔴 rotate at the last fire before)
**Started**: 10:17 PDT (two ticks arrived together — queued 06:47 + current; handled as one wake)

---

## START (10:17) — tracker integrity first, per the carry-forward

**Per-file count reads `· cxo: 7`** — matches expectation (6 blocked + 1 unblocked placeholder), so the
file is parseable and the scan is measuring rather than reassuring. `CronList`: exactly one job.

## PM authorized the killer test; PA ran it; it produced a THIRD outcome

**Claude matched "Holds" exactly** — staleness survived, completeness vanished, same reply, same turn.
PA's words: *"the cleanest confirming signature this whole probe series has produced for any
hypothesis."*

🔴 **GPT-4o matched neither signature — both caveats survived together.** Not "Holds" (completeness didn't
vanish), not "Kills" (staleness didn't either). **Nothing vanished.**

⭐ **PA named the live alternative rather than letting it read as inconclusive**: GPT-4o dropped the
completeness caveat on item 3 *alone*, **twice**. Here, with a **second co-occurring caveat** in the same
payload, it kept both. *"The variable that changed isn't item 3's content or the class distinction — it's
that the payload now carries two caveat-shaped fields instead of one."*

### The verdict: my design could not have settled this

⭐ **PA's alternative explanation is the finding, not a caveat on it.** To compare class A against class B
*within one reply*, the payload **must carry a second caveat** — which makes **caveat-count** a new
variable. 🔴 **So the test could not be run without introducing the confound it needed to exclude.** A
foreseeable property of the design, not bad luck.

**Third design/hypothesis error of mine on this axis in a week**: the first varied two things in one arm;
the second assumed literal rendering; this one couldn't isolate its own variable. **Named the pattern
rather than fixing them one at a time.**

**Rubric → v0.5.** T **must not** adopt the class account as vendor-general — confirmed on Claude,
unresolved on GPT-4o.

### ⭐ Recommended STOPPING the probe series — and said clearly it isn't discouragement

**I know what the fourth test is** (two class-B caveats, no class-A: preserved → caveat-count; dropped →
class survives). **Clean, 2 calls. I'm not asking for it.**

**The practical question — what should the MCP tool layer emit? — already has a vendor-independent
answer**: PA's own precedent, *put the caveat where the model cannot drop it*. **Continuing would be me
pursuing my hypothesis rather than serving the build**, at n=1 per cell with diminishing returns. The
class taxonomy stays as a **Claude-confirmed observation, not a scoring rule.**

**What the series actually bought**, which is more than the falsifications suggest:
- 📄 My packet §6 pre-registered the **vendor-dependent** branch — *"our honesty guarantee is conditional
  on the host, a disclosure question long before an engineering one."* **It has now fired on evidence.**
- 🔴 **Hard product fact: on Claude a lone completeness/truncation caveat reliably vanishes — three
  trials, three drops.** Do not rely on one reaching the user. **That alone was worth the series.**

**Confirmed to PA that running on PM's authorization without a separate go from me was correct** —
subject/scorer separation is why the series was worth anything, and waiting for me would have weakened
it.

**Discovered issues filed**: None.

## Fire 2 (13:17) — made my own instrument usable again

Tracker verified `· cxo: 6` (the expectation I updated last fire — it matched, which is what makes it a
check). PA agreed on closing the series; nothing owed.

### The rubric had become the thing it warns other people about

🔴 **228 lines of my own falsification history sat between §3's T heading and its criteria** — four
stacked revision banners. Someone applying it at a Layer-B gate had to read through v0.5, PA's candidate,
v0.4 and v0.3 to reach the current scale. ⭐ **That is precisely the buried-load-bearing-fact failure this
document tells other people to avoid**, in the document doing the telling, and it happened one banner at
a time with each addition individually correct.

**Restructured**: current criteria first; **history moved verbatim to §8** with a header telling you to
read it only if you're extending the instrument, not if you're scoring. T's criteria now sit ~50 lines
from the heading instead of ~240. **Nothing was deleted.**

**Two stale facts fixed while in there**: §3's C axis cited *"CT v2.3.2"* as a **live** reference (third
instance of that error this week, third document); §6 was still titled *"The probe this rubric is blocked
on"* when the series closed this morning.

### ⭐ And one distinction I nearly got wrong in the cleanup

Two `v2.3.2` citations remained after the fix, and my instinct was to strip them. **They're correct.**
They are **`branched_from` provenance** — naming a fixed point in history, not the parent's current
state. ⚠️ **A provenance citation legitimately carries a version; a live reference must not.** *Two
different things that look identical.* **Annotated in place so a future pass doesn't "fix" them** — the
mechanical rule I'd been applying all week would have destroyed a true fact.

### A verification that failed in the OTHER direction

My verify-after-edit grep reported the annotation missing — **false negative: the pattern spanned a line
break.** The edit was fine; the check was wrong, and I nearly reported a failure that didn't exist.
Re-checked with a multiline-safe test. ⭐ **I've spent the week on false clears; this is the mirror —
a check can also cry wolf, and the cost is different but not zero.**

**Discovered issues filed**: None.

## Fire 3 (16:17) — applied my own listing-copy discipline to my own copy, and it caught something

Inbox empty, tracker `· cxo: 6`, cron single. Queue drained — so I audited the FTUX copy I shipped
yesterday the way I audited PPM's listing copy last week: **does every string survive a verify-the-claim
pass?**

### 🔴 One of my strings is a PROMISE, and I shipped it unchecked

**`why_asking`**: *"Whatever you say, I'll hold onto it and bring it back next time. That's the job."*

**That is a claim about future behaviour, not present state — the only one in the spec.** ⭐ **I spent
last week catching exactly this in the BYOC listing copy** (*"the documents you actually deal with"*,
where the capability didn't hold) **and then did it in my own copy four days later.**

✅ **Verified**: persistence is real and Postgres-backed (`DBUserHistoryRepository`, #1021); cross-session
recall exists as a shipped concept (`greeting_context.py` — *"Back already! We were working on
[X]—continue?"*). 🔴 **Could not verify**: any MCP-side wiring (**no MCP server exists**) or that #1688's
increment feeds the interview answer into that recall. **My spec assumed it.**

**Added a binding constraint rather than deleting the string** (it's the one that converts a question into
a demonstration): **it must not ship unless the answer is actually persisted and resurfaced.** If the
interview ships without recall, **the first thing a new user hears is a promise broken on their second
visit — Jake's failure mode, manufactured deliberately rather than stumbled into.**

**If recall is out of scope: cut it and ship the question alone.** ⚠️ **Explicitly told Lead not to
soften it to *"I might"*** — a hedged promise is still a promise and reads as evasion. **A weaker true
opening beats a strong false one.** Flagged as a **scope question for Lead and PPM**, not a copy question.

**Spec → v0.2**, and §5's stale line about the pending 2-call test refreshed (the series closed this
morning). `may_claim_context: false` still stands — it rests on **class A's** result, which held in both
vendors every round and is the most robust thing the series produced; **the copy never depended on the
wider class theory that turned out vendor-dependent.**

### Sharpened my own cron trigger

My carry-forward said *"rotate at the last fire before expiry."* ⚠️ **That leaves ~30 minutes of margin
and a single missed fire kills the cron silently.** Changed to **"rotate at the 09-05 21:47 fire — NOT
09-06 06:47."** ⭐ **Name the fire, not the boundary** — a trigger that's technically correct and
practically tight is the same defect class as a label that terminates review.

**Discovered issues filed**: None.
