# Session Log — Coding Agent (prog), 2026-09-13 18:32 PDT

- **Role**: Coding Agent (prog-code)
- **Model**: claude-opus-5
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (Lead Dev's, Model A)
- **Branch**: `claude/lead-cycle`
- **Task**: #1756 (read-lane pre-classifier patterns claim destructive asks) + #1757
  (portfolio archive/hide/restore family greed) — the #1527 greedy-pattern family.

## Plan

1. Read both issues + prior art commit `95839e7aa` (#1527 positive-evidence fix). DONE.
2. Census first with numbers, through the real `PreClassifier.pre_classify_with_pattern_list`
   and `detect_multiple_intents`. Treat issue example lists as claims to verify.
3. Guard EXISTING patterns only — no new extraction patterns; ratchet ceiling unchanged.
4. Red-first pins, both entry paths. Verify destructive phrasings land where the #1190
   destructive-confirm gate sees them.

## Entries

- Session start (18:32) — synced worktree to `origin/main` (was 1 behind, ff to `c531babc2`).

- **1756 + 1757 shipped (18:32–18:55)** — census first, then guards on existing
  patterns, then red-first pins, then the sweeps.

### Census (real `PreClassifier`, both entry paths, 2026-09-13)

142 shapes probed (100 destructive + 42 archive-family), plus 50 read controls.

| Lane | Wrong claims (pre-fix) | Post-fix |
|---|---|---|
| STATUS_PATTERNS | 30/30 | 0/30 |
| TEMPORAL_PATTERNS | 24/24 | 0/24 |
| MEMORY_PATTERNS | 15/15 | 0/15 |
| CALENDAR_QUERY_PATTERNS | 5/6 | 0/6 |
| INSIGHT_PULL_PATTERNS | 0/5 | 0/5 (no work needed) |
| PORTFOLIO archive family (1757) | 28/28 | 0/28 |
| read controls (STATUS/TEMPORAL/MEMORY) | — | 0/50 changed |
| archive project controls | — | 0/14 changed |
| GITHUB_QUERY (out of scope) | 2/6 genuine | unchanged → filed #1794 |

All three of #1756's example claims verified true (0% stale — #1527's own list
had been 25% stale, so this was worth checking). All five of #1757's named
pattern families verified greedy.

**Beyond the issue**: CALENDAR_QUERY_PATTERNS is a fourth read lane with the
same failure on the same vocabulary ("delete my meetings this week" is a
sibling of the issue's "delete the meeting tomorrow") — fixed in the same
guard. GITHUB_QUERY is a fifth but with disjoint vocabulary and some
legitimately-gated claims, so filed separately (#1794).

### Fix

- `DESTRUCTIVE_ASK_BLOCKERS` + `_is_destructive_ask` helper, applied to the four
  read lanes at BOTH entry surfaces. Narrowed by POSITION (verb heads the turn
  or sits under a request frame), not vocabulary — so reads that MENTION a
  destructive verb keep their claim.
- `PROJECT_NOUN_REQUIRED` (1527's lookahead, reused as that commit anticipated)
  on the six archive/hide/put-away/restore/unarchive/bring-back patterns.
- No new patterns. `TestExtractionPatternRatchet` pre-classifier count measured
  at **567 = ceiling**, unchanged.

### Verification

- Red-first A/B: fix stashed → **218 failed** / 120 passed; restored (byte-identical
  to backup) → **338 passed**.
- `run-sweep.sh unit tests/unit/services/intent_service`: **4302 passed**, 3 deselected.
- `run-sweep.sh ratchets`: **58 passed** + mypy gate all 24 codes at ceiling (total 1347).
- `run-sweep.sh smoke`: **506 passed**, 1 skipped.
- `ruffenv2/bin/ruff format --check .`: 1756 files clean. `ruff check`: clean.
- Broader `tests/unit/services` (minus intent_service): 4932 passed, 2 failed —
  both confirmed PRE-EXISTING by A/B isolation, filed as #1796.

### Discovered work filed

- **#1794** (MVP, Product Backlog) — GITHUB_QUERY read lane claims destructive asks.
- **#1795** (Dot Releases, Product Backlog) — "restore my reminders" now claims
  the reminder listing lane (1757 fallout; neither claim was ever correct).
- **#1796** (Fast Follow, Product Backlog) — two pre-existing unit failures
  outside the gating suites.
