# Session Log — 2026-09-13 11:37 — prog-code

**Role**: Coding Agent (prog)
**Model**: claude-opus-5
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (Lead Dev's, Model A)
**Branch**: `claude/lead-cycle`
**Issue**: #1762 — render-truncation sweep (~18 "...and N more" sites). Opens EPIC 6 (rendered-deliverable).

## Task

Phase 1: CENSUS of every truncation site. Classify each:
- (a) BOUNDED — small by construction; render==data is the right cheap fix (the #1738 shape)
- (b) GENUINELY LONG — cap exists because the set can be large; epic-6 design question, do NOT design here
- (c) NOT A CONTEXT SOURCE — render provably never reaches next-turn context

Phase 2: fix class (a) ONLY, mirroring #1738 (commit 39e7aa864). Pin at the honest layer
(a test that the FULL set reaches next-turn context, incl. a real `build_recent_history` assertion).

## Entries

- Session start (11:37) — created log, read #1762 fully, read precedent commit 39e7aa864.

### Phase 1 — CENSUS (complete)

**Mechanism re-verified this session (not remembered):** `services/intent/intent_service.py:865`
→ `conv_ctx.turns[-1].response = result.message`. That is the ONE per-turn channel into
`build_recent_history`. So any handler whose `message` becomes `result.message` has its RENDER,
and only its render, become the model's data next turn.

**Count check (the issue's "~18" is a claim, not a given):** the issue's grep listed ~18 hits,
two of which were stale line numbers. Re-grepped 2026-09-13: **20 explicit "…and N more" /
"+ N more" elision-marker sites**, plus **21 silent slice caps** in the same render
neighborhoods (no marker at all — strictly worse than an honest elision), plus **~12
gather-layer caps** in `context_assembler.py` which are a *different class* (see (d)).

#### Class (a) — BOUNDED, user-owned → render == data (FIX NOW): 13 sites

| # | site | truncated | cap | marker? | source bound |
|---|---|---|---|---|---|
| a1 | `canonical_handlers.py:4414` | portfolio SEARCH results | `[:5]` | **silent**, and the copy claims `Found {len(results)} projects` | DB portfolio — **the third branch of the very handler #1738 fixed** |
| a2 | `canonical_handlers.py:886` | `_format_consolidated_status` projects | `[:3]` | `+ N more` | PIPER.md `user_context.projects` (hand-authored) |
| a3 | `canonical_handlers.py:907/917` | `_format_standard_status` projects | `[:5]` | `…and N more` | same |
| a4 | `canonical_handlers.py:938` | `_format_project_list_embedded` projects | `[:3]` | `+ N more` | same |
| a5 | `canonical_handlers.py:1261/1265` | `_format_standard_priorities` priorities | `[1:4]` | `…and N more` | PIPER.md `user_context.priorities` |
| a6 | `canonical_handlers.py:2171/2174` | `_handle_project_setup_request` names | `[:5]` | `…and N more` | PIPER.md projects |
| a7 | `canonical_handlers.py:2490/2496` | `_format_agenda_standard` todos | `[:5]` | `…and N more` | user's own todos |
| a8 | `canonical_handlers.py:2559/2562` | `_format_agenda_granular` medium todos | `[:5]` | `…and N more` | same (the `high` band already renders FULL — the caps are arbitrary) |
| a9 | `canonical_handlers.py:2566/2569` | `_format_agenda_granular` low todos | `[:3]` | `…and N more` | same |
| a10 | `canonical_handlers.py:2578` | `_format_agenda_granular` priorities | `[:3]` | **silent** | PIPER.md priorities — in GRANULAR, the *most detailed* mode |
| a11 | `canonical_handlers.py:2792/2797` | `_format_retrospective_standard` completed | `[:8]` | `…and N more` | one day's completed todos |
| a12 | `conversational_floor.py:845/848` | **due reminders in the DATA CHANNEL** | `[:5]` | `…and N more` | `get_due_reminders` is **uncapped**; only past-due items |
| a13 | `reminder_clear.py:598` | clarify candidate list | `[:6]` | **silent**, copy says "you have: X" | user's own reminders/todos |

a12 is the sharpest: it is not `turn.response`, it is the **LLM system prompt's domain-context
block**, and the directive two lines above it orders the model to *"Briefly surface them in your
reply … do not wait to be asked"* — i.e. the prompt instructs surfacing of items the same prompt
elided. Verified `get_due_reminders` (`todo_handlers.py:378-412`) applies no limit and
`reminder_count` already carries the true count, so this is a pure render cap over an
uncapped set.

#### Class (b) — GENUINELY LONG → epic-6 design question (DO NOT FIX HERE): 11 sites

| site | truncated | cap | why (b) |
|---|---|---|---|
| `intent/intent_service.py:6853` | GitHub open issues | `[:5]` of `total_count` | 179 open in this repo alone |
| `intent/intent_service.py:7151` | open PRs | `[:5]` of `limit=100` | unbounded |
| `intent/intent_service.py:7218` | milestones | `[:5]` | repo-scale |
| `intent/intent_service.py:7326` | releases | `[:5]` | grows monotonically forever |
| `intent/intent_service.py:7402` | labels | `[:20]` | repo-scale |
| `intent/intent_service.py:7513` | branches | `[:20]` | unbounded |
| `consciousness/search_consciousness.py:85` | external search results | `[:10]` | unbounded (Notion search) |
| `conversational_floor.py:977/990/1003` | composted insight bands | `[:10]/[:10]/[:5]` | grows with every session, forever |
| `conversational_floor.py:1204` | completed todos | `[:5]` | completion history is unbounded |
| `conversational_floor.py:1271` | blocked GitHub issues | `[:10]` | repo-scale |
| `canonical_handlers.py:838/999/1196/1271/1848` | GitHub issue previews | `[:3]/[:5]` | repo-scale |

Note the GitHub six already do the *honest* half correctly (they state `total_count` from the
source, not the slice length — m-44 clean) but not the *cashable* half.

#### Class (c) — NOT A CONTEXT SOURCE (proven unreachable, not assumed): 4 sites

| site | proof |
|---|---|
| `consciousness/files_consciousness.py:188` (`format_files_conscious`) | whole-repo grep excluding tests/venv: 3 hits, **all import/re-export statements** (`consciousness/__init__.py:52,143`, `intent/intent_service.py:25`). **Zero call sites.** |
| `consciousness/standup_consciousness.py:197` (`format_blockers_conscious`) | reachable only via `format_standup_conscious` (**zero references repo-wide outside its own def + tests**) and `StandupToChatBridge` (exported in `personality/__init__.py`, **never instantiated in production**) |
| `canonical_handlers.py:2066/2070` (`_format_project_setup_guidance` has-projects branch) | the function's only two call sites are `self._format_project_setup_guidance(None)` (2139, 2155) → `has_projects = None and …` → falsy → **the truncating branch is dead** |
| `_format_embedded_status` / `_format_embedded_priorities` | zero non-def refs — dead functions (no marker sites inside, listed for completeness) |

#### Class (d) — GATHER CAP, not a render cap (the distinction matters): `context_assembler.py`

A gather cap truncates **before any render exists**, so "render == data" is not even expressible
there — the data itself is short. Fixing the render is a no-op; the fix is a contract decision
about what the true set is. Sites: `:580` `user_ctx.projects[:5]`, `:911`
`_get_projects_cached(limit=5)`, `:901/:905` `limit=10`, `:974` `limit=5`, `:1280` `pending[:10]`,
`:1348` `completed[:10]`, `:1475` `projects[:10]`, `:1488` `priorities[:5]`, `:1571` `blocked[:10]`.

Two sub-shapes, and only one is honest:
- **Counted** (`pending_todo_count` #1544, `completed_todo_count` #1639): the floor states the
  row-derived total, so the *count* is true even though the *items* are short. m-44 clean.
- **Uncounted** (`user_projects[:5]`, `projects[:10]`, `priorities[:5]`): no companion count, so
  the floor's denominator silently becomes the cap.

Related two-layer finding: `_get_todays_todos` (`canonical_handlers.py:2394`) gathers
`limit=10`, then `_format_agenda_granular` prints `**Total**: {len(todos)}` — **the slice length
presented as the truth**. Fixing the render (a7-a9) is still correct under §5b but does not close
this; filed separately.

### Phase 2 — FIX (class (a) only, mirroring #1738's shape)

All 13 class-(a) sites fixed. Three files:
`services/intent_service/canonical_handlers.py` (a1-a11), `conversational_floor.py` (a12),
`reminder_clear.py` (a13). Class (b) untouched; class (c) untouched and filed; class (d)
untouched and filed.

**Red-first**, `tests/unit/services/intent_service/test_render_truncation_sweep_1762.py`:
pre-fix **19 failed / 2 passed** — the 2 passes are `TestClassBBoundaryDeliberatelyUnchanged`,
green controls that pin the scope boundary so a later sweep can't cross into class (b)
without the epic-6 design. Post-fix **21/21**.

Two of the 21 are `build_recent_history` pins in the #1738 idiom (real function, in-memory
`ConversationContext`): the 6th portfolio search match and the 7th agenda todo now reach
next-turn assistant content.

**Five legacy tests pinned the defect and were inverted** (each carries an inline note
saying what it used to assert and why that was wrong):
- `test_agenda_query.py::test_format_agenda_standard_many_tasks` (was `"... and 3 more" in result`)
- `test_canonical_handlers.py::TestRetrospectiveQuery::test_format_standard_with_many_tasks` (was `"and 4 more"`)
- `test_setup_routing_814.py::test_truncates_long_project_list` → `test_long_project_list_is_rendered_in_full` (was `"... and 2 more"`)
- `test_reminder_surfacing_1566.py::test_reminder_count_beyond_display_cap_is_stated` → `test_reminder_count_is_stated_and_every_reminder_rendered`
- `test_reminder_vocabulary_render_1569.py::Test1566Canaries::test_truncation_denominator_still_stated` → `test_denominator_still_stated_and_no_reminder_elided`

The last two are the interesting ones: they pinned **m-44** ("the denominator rides with a
truncated list"). That property is preserved — both still assert the count is stated — but
the clause asserting a truncation EXISTS is gone, because on the floor's data channel an
honest count of items the model was never shown is still a promise only the prompt can see.

**Verification.**
- `run-sweep.sh unit` new suite: 21 passed (post-fix); 19F/2P pre-fix
- `run-sweep.sh unit` neighborhood (1738 / 1431 / agenda x3 / floor / reminder_clear x2): 170 passed
- `run-sweep.sh unit tests/unit/services/intent_service`: 3893 passed / 3 deselected (pre-inversion run flagged exactly the 4 legacy pins above)
- `run-sweep.sh unit` intent_service + onboarding + database + consciousness + personality: **4352 passed, 218 skipped, 3 deselected**
- `run-sweep.sh ratchets`: **58 passed**
- `run-sweep.sh smoke`: 506 passed, 1 skipped
- ruff 0.6.9 (`ruffenv2/`, already present, not perishable): `check` clean, `format --check` clean on all 9 touched files

Remaining `and N more` test assertions repo-wide are all class (b)
(`test_handlers_milestones_releases_1039.py`, `test_handlers_labels_branches_1040.py`) —
correct, those must stay capped.

### Discovered work filed

- **#1775** — dead render family (the 4 class-(c) sites). Milestone Fast Follow, board
  Status = Product Backlog (verified via the project-item query, not just the mutation's
  return). Sharpest row: `_format_embedded_status` / `_format_embedded_priorities` are dead
  siblings one word away from the live `_format_consolidated_*`, which may be an incomplete
  rename rather than dead code — flagged as check-first.
- **#1776** — gather-layer caps without a companion count (class (d)), incl. the
  `**Total**: {len(todos)}` slice-length-as-truth. Milestone MVP, board Status = Product
  Backlog (verified).

Board writes used per-item `updateProjectV2ItemFieldValue` mutations only — no
`updateProjectV2Field` / option-list touch (the 1175-item wipe hazard).

### Left for the class-(b) design question (NOT designed here — needs CXO/PM)

Recommendation carried into the report; see "Class (b)" table above for the 11 sites.

