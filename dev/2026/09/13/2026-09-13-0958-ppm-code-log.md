# PPM session log — 2026-09-13

**Role**: Principal Product Manager (PPM) · slug `ppm-code` · **Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` · job `dedc5304` (re-armed 09-12 22:22 STOP; unchanged today)

### 09:58 START — session resumed after a Claude Code re-auth interruption

PM had to sign back into Claude Code, interrupting the morning's usual 06:52 START. This is the
actual first fire of the day, run at 09:58 instead.

**Heartbeat**: first attempt FAILED to land (`dev/heartbeats/2026-09-13/ppm.tsv` not on
`origin/main`) — the commit and local merge succeeded, but the push raced against Web's own
session-start push and lost. Diagnosed via `git rev-list --left-right --count origin/main...HEAD`
(1 ahead / 2 behind), fetched + merged (Arch's and Web's session-start logs, no conflict), pushed
again — landed clean, verified the actual heartbeat row present on `origin/main` before moving on.
A second push race followed immediately after (Web pushed again), resolved with a plain
fast-forward merge; ended at 0 ahead / 0 behind.

**Cron**: exactly one job (`dedc5304`), survived the re-auth. Prior day's log confirmed carrying
`DAY-CLOSED`.

**Mailbox + drift**: one item — CIO's own v1.35 fix to CXO's earlier denominator refinement,
cc-only, no PPM action. Filed to `read/`, sent, verified landed. `sprint-truth.py`: `MVP: 44 not
done (30 Sprint Backlog, 3 In Progress, 7 In Review, 4 Product Backlog); 1158 done` — **0
unmilestoned**, all board items reconciled, no drift overnight.

**Extended same-fire**: while merging through several push races (three total this morning — Web,
Arch, Exec, Docs, Host, CIO, PA, CXO all starting up around the same window; none of mine, all
resolved by fetch+merge+retry), noticed Exec had published `dev/active/epic-accounting-2026-09-13.html`
for PM — a live GitHub-API epic-by-epic accounting, explicitly meant to answer PM's "I keep hearing
'green for the first time' ... I suspect slippery denominators" concern. Per AUDIT BIAS (audit
before accepting elevation of my own artifact), read it in full rather than treat "for PM" as a
reason to skip review.

**Found a real gap**: the doc reports "nine epics" and lists 1-9, omitting epic 10 (`#1737`,
Composer UX polish) entirely — confirmed live (`#1737` open, MVP milestone). Exec's own doc had
already caught and fixed one mention-vs-membership bug on epic 2 (mid-write) and explicitly flagged
in its own methodology note that "PPM re-homed six singletons yesterday and the doc may lag" — this
is exactly that risk landing. Also found, via a full member-by-member `gh issue view` check against
my own file's actual backtick-quoted membership, that epics 1/3/5's reported *totals* look inflated
by 1-2 (9 vs true 8, 13 vs true 11, 15 vs true 14) while every reported *open* count was correct (4,
3, 6) — consistent with the same inline-mention leak recurring in cells that happened not to change
the open number, so less visible than the epic-2 case.

Sent a correction to Exec, cc PM, naming the epic-10 omission as the headline finding and the
smaller denominator pattern as a lower-stakes secondary note — explicit that this doesn't change any
of the doc's actual conclusions (epic 3 blocked on PM's standup retest, epic 1 taxing everything
after it), just the totals in three cells. Framed as continuing the same discipline Exec's own doc
started, not a gotcha.

**Accepted one real correction to my own file**: epic 9's item count (`#1423` alone) undercounted
its own two named confirmed instances (`#1420`/`#1422`, both already closed) — Exec's fuller count
(3 items) is more accurate, since those instances are what the inventory epic exists to cover, not
incidental mentions. Updated to match.

**Refreshed live closure state** across epics 1, 3, and 5 while cross-checking — a lot had closed
since last night without my file catching up: epic 1 now 4 of 8 closed (`#1711`/`#1637`/`#1748`/
`#1749`), epic 3 ran all the way to its floor Saturday (8 of 11 closed, only the `#1739` umbrella +
`#1771` + `#1695` remain — and per Exec's doc, `#1739`'s real remaining dependency is PM's own
`#1617` standup retest), epic 5 is 8 of 14 closed and is the epic Lead is now actively working
(opened right after epic 3 hit its floor). Also confirmed Lead's `#1687` secret-rotation comment
is posted — noted in the epic-order file and flagged for tonight's cron re-arm (the WATCH FOR line
can drop once PM does the actual ~3-minute rotation).

Committed and pushed the epic-order refresh (one more push race, resolved the same way). Mail sent
in one 3-path call (Exec's inbox, PM's inbox, PPM's sent mirror) — landed clean, no warnings.

Nothing else unblocked this fire.

### 10:22 WORK — quiet fire, mailbox drained clean

**Heartbeat + sync**: clean, one cron job (`dedc5304`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
44 not done (30 Sprint Backlog, 3 In Progress, 7 In Review, 4 Product Backlog); 1158 done` — **0
unmilestoned**, no drift.

Two mail items, both cc-only: CXO's design input to PA for a future BYOC/MCP-surface honesty probe
(§6b, rubric v0.7) — reusing Lead's #1717 transcripts to note recomposition altered rather than
dropped content in both vendors, but explicitly disqualifying it from lifting the probe's own
`PENDING-PROBE` status (different surface, no floor prompt on BYOC) — research/rubric domain, no
tracked GitHub issue, no PPM action. CIO's methodology-54 filing (Arch's skew-finding lesson,
checked against m-44/m-46/m-49 and found genuinely distinct) — Arch/CIO's own record, no PPM
action. Both filed to `read/`, sent, landed clean.

Nothing else unblocked. Committed and pushed carry-forward + this log entry.

### 13:22 WORK — a real epic-6 design ruling, plus a Lead-dark scare that resolved itself

**Heartbeat + sync**: clean, one cron job (`dedc5304`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
46 not done (30 Sprint Backlog, 3 In Progress, 7 In Review, 6 Product Backlog); 1159 done` — **2
unmilestoned**.

**Five mail items.** Two were Exec's own escalation-then-correction: an alert to PM that Lead had
gone dark 12 hours with two missed fires (the only role in that state among eleven checked),
followed 25 minutes later by a correction — Lead's cron had survived PM's re-sign-in fine; the real
cause was a sustained classifier outage gating Bash/`CronCreate`/every state-changing action on
five seats today (exec, cxo, cio, pa, web — Lead's was just the longest). Exec named the belt
finding worth keeping: a role can be alive, synced, and structurally unable to commit, and the belt
correctly reads that as STALE by its own contract — the fix isn't the threshold, it's being honest
in the alert's own wording about what "stale" can mean. Both cc-only, no PPM action, resolved
before I got to them.

**Three mail items were a genuine epic-6 design thread, addressed to PPM.** Lead's `#1762` census
narrowed the open question to one PPM/CXO call: where does an "…and N more" claim's unrendered
remainder live so a follow-up turn can actually cash it. CXO answered the copy half (already
governed by the contract's §5b: *"That's 5 of 340 — say the word and I'll pull the rest,"* offer
the affordance never the syntax, counts inherit source provenance) and flagged a real gap nobody
had named: today's arm survives only one turn, but a capped-list offer is exactly the kind a user
answers *late* — read five, think, come back. Arch confirmed the architecture half (GatherOutcome
is the remainder's home, already implied by three separately-ratified pieces of law) and concurred
on GitHub-six-first as the build's actual starting scope.

Confirmed GitHub-six-first (no reordering needed — it's the epic's own natural start) and made the
one call left for me: skip the capped-list offer entirely when the hidden remainder is ≤3 items,
just render them. Added CXO's late-follow-up finding to epic 6's acceptance test in the order doc
so the first build can't pass while only exercising the immediate-follow-up case. Replied to all
three, cc PM (a real product ruling, not routine).

**Triaged the 2 unmilestoned issues** (`#1779`/`#1780`, docs-drift found during `#1720`'s own
lane): milestone Ongoing, Sprint=FLYWHEEL, Status=Sprint Backlog — matching `#1720`'s precedent
exactly rather than defaulting to MVP. Neither belongs in the epic-order file (Ongoing-milestone
work, not MVP).

Committed and pushed the epic-order file. Mail sent in one 15-path call — landed clean, no
warnings, verified via `git cat-file -e` on all 15 paths.

### 16:22 WORK — CXO catches a stale line in my own file, three more issues triaged

**Heartbeat + sync**: clean, one cron job (`dedc5304`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
49 not done (29 Sprint Backlog, 3 In Progress, 6 In Review, 10 Product Backlog + 1 not on the
board); 1161 done` — **2 unmilestoned**, **1 not on the board**.

**CXO wrote directly to PPM**: verified this morning's epic-6 memo landed accurately in the order
doc (reading the file, not the memo's summary, as usual), then found a genuine stale defect —
epic 6's entry still read *"name the copy-owner before scoping the fix"* eleven lines above the
same entry's own *"Copy shape (CXO): 'That's 5 of 340...'"* line. The flag had been satisfied in
substance (CXO's copy landed) but never discharged on paper, so a later reader would see an open
request sitting above a delivered answer with no way to tell it was stale rather than a real gap.
Fixed immediately — epic 6's copy owner now reads CXO, matching epic 5's already-filled line.

**Triaged three more issues**: `#1785` already carried MVP milestone (the filing convention is
holding), but was missing from the board — the same drift shape as yesterday's `#1772`. Fixed and
folded into epic 1 (now 9 items) — it's the belt-cost question (211 canonical tests making real
LLM calls on every push, ~20 runs/day). Its own filing account ("found while confirming E2E went
green after PM's secret rotation") is the first real evidence the `#1687` rotation actually
happened, not just was requested — noted in the epic-order file, and the standing WATCH FOR line
in the cron prompt can drop at tonight's STOP. `#1786`/`#1787` (a mypy-gate transitive-pinning gap
and an untracked venv directory, both found in the same lane clearing `#1436`) got milestone
Production rather than MVP, matching `#1436`'s own milestone exactly — neither belongs in this
file.

Replied to CXO confirming the fix. Committed and pushed the epic-order file. Mail sent in one
9-path call — landed clean, no warnings.
