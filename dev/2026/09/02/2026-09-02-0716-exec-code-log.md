# Session Log: 2026-09-02-0716-exec-code

**Role**: Chief of Staff (Exec)
**Model**: Claude Code (Opus 5)
**Date**: Wednesday, September 2, 2026
**Start Time**: 7:16 AM PDT (PM engaged directly)

⚠️ **CREATED LATE, at the 08:32 fire — this is a Step 0 gap and it is mine.** PM engaged at ~07:16
and I worked ~2 hours (two issues filed, three memos, a cross-machine git recovery) before creating
today's log. CLAUDE.md is explicit: *"ANY first message in a conversation is a session start, even if
it's just a greeting."* PM's first message was a stray keystroke — I treated it as noise and skipped
the protocol instead of treating it as the session start it was. The morning below is backfilled from
the actual work, not reconstructed from memory.

## Session Objectives

1. Ship #058 publishes today — unblock PM's edit.
2. Duty-cycle fires.
3. Sort the misfiled strays out of PM's faoilean branch before anything pushes to main.

## Work Log

### 7:16 AM — PM engaged; Ship #058 unreachable

**PM could not edit the Ship**: admin composer 404, and no `058` files found after syncing. Two
separate causes, and **I got the diagnosis wrong twice before finding it.**

1. ❌ **First read — "it's gitignored."** Wrong; tested and refuted immediately.
2. ⚠️ **Second read — "wrong directory."** The draft was in `dev/active/` while every prior Ship
   lives in `docs/public/comms/drafts/` at draft time (#057 created there `e6d382215`, archived to
   `published/` on publish). **Moving it was correct on convention and was NOT the cause of the 404.**
   I presented it as the fix. It wasn't.
3. ✅ **Actual cause**: `/api/compose` resolves the draft **body** live from GitHub, but resolves the
   **calendar** via `loadCalendar()` from `data/editorial-calendar.csv` — *"copied from product repo
   at prebuild"* (`editorial-calendar.ts:4`). **A build-time snapshot.** #058's row was added 09-01;
   the deployed snapshot predates it; no row → no entry → 404. ⭐ **That module's line 91 already
   warns about this exact trap** — editing the stale bundled CSV and *"changing nothing while looking
   like a fix."* Someone met it before me and left a note I read past.

**Also fixed**: Amber's PM checkout was **29 commits behind**; ran `sync-pm-local.sh` → 0 behind.
⚠️ **But PM was on `faoilean`, not Amber** — I fixed a checkout PM wasn't looking at. Different
machine, and my script only reaches Amber.

### faoilean recovery — and the near-miss that matters most today

PM's `git pull` had been failing with `fatal: stash failed`. **Root cause: a stale
`.git/index.lock`** from a crashed process — every operation needing an index write failed, including
the autostash. Not a stash problem at all. Cleared after confirming no live git process.

🔴 **THE NEAR-MISS.** PM's local `main` had diverged: **4 commits ahead, 3,128 behind.** The four read
`Merge…`, `manifest updates`, `Merge…`, `syncing faoilean`. **I drafted a `git reset --hard
origin/main` off those subject lines.** I asked PM to run `git diff --stat origin/main...HEAD` first —
and it contained **a Cova pitch deck, `dev/active/Treatment`, a learning-data JSON, and 19 original
PNGs (~60MB)**, none of it on origin.

**A `--hard` reset would have destroyed all of it permanently** — committed objects on a branch
existing nowhere else. **I read the labels instead of the contents**, and the only thing that stopped
it was asking PM to confirm rather than trust the subjects, which I nearly didn't because four commits
saying "merge/manifest/merge/syncing" looked self-evidently like noise.

Recovered safely: branch `faoilean-local-2026-09-02` created first (zero-risk, makes everything
unloseable), then targeted MANIFEST restores, then `git merge origin/main` with three MANIFEST
conflicts resolved to origin's copies. PM is current with all four commits preserved.

⚠️ **Pre-empted one collision the merge would have hit**: PM's untracked
`mailboxes/pard/inbox/MANIFEST.md` shadowed a real file on origin. Checked the six loose PNGs too —
none collided.

**Also**: five of PM's six untracked PNGs were already on origin under post-specific renamed paths;
`trust-check.png` I could not find and flagged as possibly unique — **PM later confirmed it had been
published**, so my "unique until proven otherwise" was correctly cautious and factually wrong.

### Filed and routed

- **`piper-morgan-website#37`** — publish should archive the source image. ⭐ **The finding is that
  Step 9 already specifies it** (`docs-notify.js:88`) and `grep` returns that one line **inside a
  string printed to a human** — no code moves anything. A documented step with no mechanism, which is
  PM's own 08-29 rule. PM chose *move to staging* over *delete*, on the standing bias that deleting
  the wrong thing beats keeping too much.
- **Composer 404 → Web**, with the layer split, a rebuild ask, and the design question (should the
  composer read the calendar live, since `fetchLiveCalendar()` already exists in that module). Left
  the framing to Web rather than filing in their repo on my read.

### 9:02 PM — Fire (20:32 cron, STOP) — day close

**Cron**: one `b55d60bf`. **STEP ONE**: 11 rows, **0 stale**. **Mail**: 15 drained.
**Step 1a-bis**: now prints a per-file row count — CIO shipped that today off CXO's finding that a
**malformed markdown table silently truncated mid-parse and a bare run still said clean.** `arch: 0`
rows in tonight's output is that case, visible now instead of hidden.

## ✅ Ship #058 PUBLISHED

`https://pipermorgan.ai/shipping-news/weekly-ship-058-what-we-actually-had` · calendar row
`status=published` · hashId `201e33efbf5c`. **Docs ran an independent audit beyond the template pass
and fact-checked four load-bearing claims against primary sources** — including my issues-closed=24,
where a raw `gh` search returned 25 and resolved to an exact match once a `NOT_PLANNED` duplicate
closure was excluded. No discrepancies. LinkedIn leg owed to Dispatch-PM (Ships are LinkedIn-only, no
Medium).

## 🔴 CIO corrected my premise, and it is the failure I named about myself the night before

I proposed adding commit-recency to `freeze-check`, evidenced by *"22 heartbeat refs, 1 git-log ref —
heartbeat-dominant by a factor of 22."* **That was a substring count over source text**, not a
reading of the code. CIO checked what the function does: **`age_of()` already took the max of THREE
signals, two of them commit-based.** They replayed the Arch case — `age_of arch` returns 0h, commits
captured correctly.

⚠️ **My own memo said "it did NOT flag" for that case — consistent with the tool WORKING — and I
wrote it down as evidence of a gap.** The refutation was inside my own sentence.

⭐ **The meta-failure**: my 09-01 day-close recorded *"my verification patterns keep being narrower
than the thing I am verifying"* as that week's **third** instance. **I then did it a fourth time
within 24 hours and shipped it to another role as the evidence base for a build request.** Naming a
failure is not fixing it, and my rule was too broad to bite. Narrower operative version: **never let a
substring count stand in for reading the function.** CIO's real finding — the grep matched only
`(role):` and missed the bare `role:` form — was found by testing, not reading.

## 🔴 Docs corrected website#37, same class, committed while filing it

My issue specified the archive destination **from `docs-notify.js:88`**. Docs checked their own
commits and found **the documentation stale**: the image/`.md` split ran as documented through 08-29,
then on 08-31/09-01 both files moved together into `published/`. Their call, and it's right: *"I'd
rather the automation match my real behavior than formalize a destination I've already stopped
using."* Issue amended. **I read the doc and inferred the practice — the exact defect the issue exists
to fix.**

**Right in that issue**: no code executes Step 9; Docs confirmed it has always been a manual `git mv`.

## Also today

- **Web fixed the composer** — it now reads the calendar live rather than from the build-time
  snapshot. The design question I put to them, answered by building it.
- **PM's voice pass** landed after a long git recovery (stale `index.lock`, a duplicate file I left,
  a near-miss `--hard` reset averted by a diff). PM's rewritten closing is better than mine.
- **Two skill defects of mine corrected**: the agent-gloss rule (blog convention wrongly imported) and
  both places carrying it.

**Queue**: nothing unblocked and undone. Strays still to sort out of PM's faoilean branch when PM has
a moment — Cova deck, `Treatment`, learning JSON, 19 PNGs.

<!-- DAY-CLOSED: 2026-09-02 -->
