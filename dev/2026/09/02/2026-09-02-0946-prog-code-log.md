# Session Log — 2026-09-02 09:46 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree; commit, do NOT push per delegation)
**Task**: Build the PRE-CLAIM SHADOW PROBE — measurement backbone for the
pre-classifier narrowing schedule (PM-ratified 2026-08-29, decisions.log:
pattern claims must meet ~100% precision by shadow divergence; below-bar
patterns get deleted on evidence).

## Design (as briefed, mirroring #1668 disciplines)

- On turns surface 1 (pre-classifier) CLAIMS — both entry surfaces
  (`pre_classify` + `detect_multiple_intents`) — under sampling
  (`PIPER_PRECLAIM_SHADOW` default OFF, `PIPER_PRECLAIM_SHADOW_SAMPLE`
  default 1.0), fire-and-forget one constrained inversion-router consult
  and log agree/disagree with the CLAIMING PATTERN-LIST NAME attached.
- FAIL-OPEN everywhere; default-off byte-identical; measurement only —
  no change to what the pre-classifier claims.
- Report helper aggregates per-pattern-list claim counts, agreement rates,
  precision-vs-bar readout.

## Work

- 09:46 Read decisions.log 08-29 entries, inversion_shadow.py,
  pre_classifier.py (both entry surfaces), intent-routing-stack.md (full),
  inversion_live.py, TestInversionShadowNoExecutionBoundary +
  TestExtractionPatternRatchet, classifier.py claim sites (classify L447,
  classify_multiple L1024), intent_service.py post-turn shadow call site.
  Key findings for the build:
  - Pattern-count ratchet counts only CLASS-BODY `*PATTERNS` lists in
    PreClassifier — a body refactor of `pre_classify` is ratchet-safe.
  - Boundary test: new module must join ALLOWED_REFERRERS (named-file
    amendment, the inversion_live.py precedent).
  - Identity threading: `pre_classify` → delegator over new
    `pre_classify_with_pattern_list` (each return site names its list);
    `MultiIntentResult` gains `pattern_lists` (default [], additive).

- 10:00 BUILT + GREEN. Files:
  - `services/intent_service/preclaim_shadow.py` (NEW) — scheduler + task +
    compare_claim + aggregate/render helpers. Env: PIPER_PRECLAIM_SHADOW
    (default OFF), PIPER_PRECLAIM_SHADOW_SAMPLE (default 1.0), shared
    PIPER_INVERSION_LOG_UTTERANCE privacy knob. Events:
    preclaim_shadow_{agreement,disagreement,incomparable,check_failed}.
  - `services/intent_service/pre_classifier.py` — pre_classify → delegator
    over pre_classify_with_pattern_list (34 return sites annotated via
    verified mechanical transform, resolution printed + hand-checked);
    MultiIntentResult.pattern_lists (identity-keyed, aligned
    post-subsumption); _pattern_list_name helper (class-dict identity,
    no parallel name table).
  - `services/intent_service/classifier.py` — probe scheduled at BOTH claim
    sites (classify Stage-1 branch; classify_multiple multi-rules branch,
    primary claim + all_pattern_lists), fail-open try/except.
  - `tests/test_architecture_enforcement.py` — preclaim_shadow.py joins the
    no-execution-boundary allowlist (third named file) + liveness assert.
  - `scripts/preclaim_shadow_report.py` (NEW) — thin CLI over module
    aggregation; JSON-lines + console-line parsing; --bar (default 1.0),
    --json; zero-events exits 1 ("measured nothing", m-44).
  - `tests/unit/services/intent_service/test_preclaim_shadow.py` (NEW) —
    29 tests covering all five pins.
  - `docs/internal/architecture/current/intent-routing-stack.md` — surface-1
    cell note + full probe block (same commit, per consult rule).
  - decisions.log entry appended (probe shipped; design calls recorded).
- Evidence: test_preclaim_shadow 29 passed; test_architecture_enforcement
  44 passed (both ratchets green, pre-classifier count unchanged at 567;
  dispatch ratchet untouched); full intent_service unit suite 3661 passed,
  3 deselected (-m "not llm", env-stripped, POSTGRES_PORT=5433); adjacent
  suites (test_pre_classifier, test_multi_intent, 1394/1044/1570 floor
  tests) 93 passed; ruff check + format clean; report script verified
  end-to-end on a synthetic log (table + verdicts correct).
- Discovered work: local mypy gate (scripts/check_mypy_gate.py) fails on
  this seat with multi-code drift — A/B/A via stash shows output BYTE-
  IDENTICAL before/after my change (not mine); matches the documented
  platform-skew condition (2026-08-31 prog log: "CI is the authority",
  local venv differs from CI-replica). No issue filed; flagged to Lead in
  the report for a call on whether the documented skew set has widened.

## Memory & briefing surfaces referenced this session
- **Referenced**: decisions.log 08-29/08-08/08-09 entries (narrowing policy,
  probe-row condition, measure-before-narrowing); intent-routing-stack.md
  (4-surface chain, consult rule, same-commit doc update);
  inversion_shadow.py + inversion_live.py (mirror disciplines, ContextVar/
  observer idioms, env-var vocabulary); TestExtractionPatternRatchet
  (class-body-only counting → safe refactor shape); m-43/m-44 (layer note,
  incomparable buckets, denominator discipline); ab-a-isolation idea (stash
  A/B for the mypy gate); CLAUDE.md env-strip + bare-issue-number rules.
- **Loaded but not referenced**: mailbox/briefing surfaces (subagent task,
  not consulted); Amber worktree/mailbox discipline sections.
- **Wanted but not found**: a documented statement of which structlog
  renderer production logs use (JSON vs console) — report parser handles
  both defensively instead.

Wrap: committing to claude/lead-cycle per delegation (no push — Lead owns
the branch push).
