# Session Log: 2026-09-02-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Wednesday, September 2, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/01/2026-09-01-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-01** (six fires, mostly quiet; PM checked twice on what could be unblocked (same three PM-gated items both times, reported plainly); PM gave unprompted positive feedback on the 08-29 above-the-fold redesign, logged as a real confirmation signal in standing-items and the browser-automation-pilot memory).

## Session Objectives

1. Normal duty-cycle drain (mail loop → task loop → idle) — no PM-assigned task this fire, cron-triggered.

## Work Log

### 06:52 AM - Session Start

- `CronList`: exactly one job (`9b17d54f`, `22 6,9,12,15,18,21 * * *`) — no duplicates.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-01 -->` — no self-heal needed.
- Fingerprint check both worktrees: `web` / `claude/web-cycle` — correct, no collision indicators.
- Synced both worktrees clean: product repo (fast-forward, cohort mail traffic overnight), website repo (already up to date).
- Created this session log.
- Cohort-freeze check (post-sync): clean, exit 0.
- Mail: empty.
- Standing items: all three still genuinely PM-gated (obs-pass, site
  walkthrough, Buttondown), unchanged. Emitted the START heartbeat for
  the day. Returning to idle.

### 09:52 AM - Fire 2: composer 404 bug, real fix shipped and deployed

- `CronList`/sync/freeze-check all clean, both repos.
- Mail: two memos from Exec. **Direct ask**: PM was blocked editing
  Ship #058 in the admin composer this morning (404) — Exec root-caused
  it precisely (calendar entries looked up via a build-time CSV
  snapshot; the draft body reads live, the calendar row doesn't; any
  row added since the last deploy is invisible), documented two of
  their own false starts so I wouldn't repeat them, and asked for a
  rebuild plus a judgment call on the fix shape. **Second memo**
  (website#37, publish should archive the source image) — addressed to
  Docs for a shape check, Web's to eventually build, nothing owed
  today, moved to read.
- **Investigated before deciding**: read `editorial-calendar.ts` and
  found `loadCalendarLive()` already exists — same request-time
  GitHub-API pattern already proven in production on `/admin/calendar`
  and `/admin/publish-queue`, with a 15s TTL cache and honest
  (never-silent) fallback-and-report on failure.
- **Decision**: full switch, not the miss-only hybrid Exec offered as
  an alternative. Checked both of Exec's "against" arguments against
  the actual code rather than take them at face value — token
  dependency turned out to already exist (same token the draft body
  reads with, same request); rate/reliability concern already bounded
  by the existing TTL cache and already proven safe in prod. A hybrid
  would've added a second code path without adding real safety.
- Fixed `src/pages/api/compose.ts` (both GET and POST handlers now
  async-aware, calling `loadCalendarLive()`). `tsc --noEmit` and
  `npm run build` both clean.
- **Real local verification, not just compile-clean**: started a local
  dev server. No `GITHUB_DRAFT_TOKEN` available in this environment, so
  this exercised the fallback path specifically — confirmed the API
  correctly reports `source: {kind: 'snapshot', reason:
  'GITHUB_DRAFT_TOKEN not set'}` (honest, never silent) and correctly
  finds a known draft's entry via the new async lookup, returning full
  content. Caught and fixed a `PIPER_PRODUCT_ROOT` sibling-path mismatch
  along the way (default resolution assumed a directory layout that
  doesn't match this worktree's actual layout) to get a fully clean
  local confirmation rather than stop at a partial one.
- Filed `piper-morgan-website#38` with the full root-cause, fix
  reasoning, and verification evidence, in my own framing as Exec asked
  rather than inherit theirs. Committed and pushed (`fda78ca`) —
  double-checked the commit-message auto-close-keyword gotcha didn't
  fire on `fix(compose): ... (#38)` by verifying the issue's actual
  state after push rather than assuming, since CLAUDE.md's own
  documented incident shows GitHub's keyword matching is looser than
  intuition suggests.
- **Confirmed the deploy actually succeeded**, not just that it started
  — polled `gh api .../commits/fda78ca/status` via a background
  until-loop rather than blindly sleep, got `success`.
- Reported to Exec cc Docs/Comms/PM, explicitly flagging the one honest
  gap: no live admin credentials for the composer, so PM's exact
  authenticated scenario (opening #058's URL) wasn't independently
  re-confirmed — reported precisely rather than either overclaim or
  invent access I don't have. `mail-send.sh`, `9c3849926`. Updated
  carry-forward (`b7b2cfb21`).
- No other unblocked work. Returning to idle.

### 11:38 AM - PM direct request: piper-ship image relocation (discussion, not yet build)

- PM asked to display the `piper-ship.webp` image fully (uncropped) on the Shipping News landing
  page, and remove it from individual Weekly Ship posts — but explicitly said "let's discuss
  first," since Dispatch was actively publishing this week's Ship and PM didn't want a code change
  colliding with that.
- Investigated current state before discussing rather than guess: confirmed the image currently
  appears only in `/shipping-news`'s OG metadata (not on-page), and is shown small+cropped
  (`object-cover`) on every individual Ship post via `ShipPostContent.tsx`. Same single static
  asset either way — `publish-post.js`'s `SHIP_IMAGE_SLUG` handling wouldn't need to change.
- Checked the actual collision risk rather than reassure blindly: `git log` showed Ship #058's
  content commit had already landed directly in the website repo this morning via the composer
  (confirming yesterday's compose fix worked in practice, unprompted). Confirmed publishing does
  write straight into this repo, so PM's caution is well-founded — but the specific files a
  template change would touch (the two React components) are disjoint from what publishing writes
  to, so the actual collision risk reads as low. Said so plainly, including the limit: no full
  visibility into Dispatch's own automation this week.
- Responded with grounded findings (not a design doc) and one clarifying question — whether PM
  wants a big banner treatment or something more contained. Did not implement anything, per PM's
  explicit "discuss first."
- **Waiting on PM** for design direction before doing anything further.

### 03:52 PM - Fire 4

- `CronList`/sync/freeze-check all clean, both repos. Mail empty.
  Standing items unchanged, all PM-gated. No update on the piper-ship
  discussion — still waiting on PM's direction. Quiet fire, returning
  to idle.

### 06:52 PM - Fire 5

- `CronList`/sync/freeze-check all clean, both repos. Mail empty.
  Standing items unchanged, all PM-gated. No update on the piper-ship
  discussion. Quiet fire, returning to idle.
