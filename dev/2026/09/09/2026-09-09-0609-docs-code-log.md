# Session Log: 2026-09-09-0609-docs-code

**Role**: Documentation Management
**Model**: Claude Code (Sonnet 5)
**Date**: Wednesday, September 9, 2026
**Start Time**: 6:09 AM PDT

## Session Objectives

1. PM-initiated entrance (ahead of the 06:57 cron fire): publish today's Weekly Ship post.
2. Standard duty-cycle floor as time allows.

## Work Log

### 6:09 AM - Session Start (PM-initiated, pre-cron)

- Created session log (first fire/entrance of the day, no log existed yet for 09-09).
- Synced product worktree (7 behind → 0) and website worktree (0 behind).
- Cron `433c7e09` confirmed single healthy job via `CronList`.
- Yesterday's day-close verified clean (`<!-- DAY-CLOSED: 2026-09-08 -->` present).
- Mail loop: 1 FYI (CIO's flywheel-synthesis ack, no action) triaged; 1 real item — Comms'
  PUBLISH-READY memo for Weekly Ship #059, matching PM's direct ask.

### 6:15 AM - Proofread and publish: Weekly Ship #059 "The Verifier Is Not Exempt"

- Checked the calendar row (Step 0): status `ready-for-docs`, pubDate today, theme=ship,
  cartoon=piper-ship (shared Ship asset). Confirmed template/voice-guide unchanged since
  yesterday's read (checked file mtimes) rather than re-read verbatim.
- **Independent verification, not just trusting Comms' report**: acronym linter clean (7
  NO-GLOSS advisories are the established, confirmed-correct Ship-genre convention — bare role
  short-forms, third-person institutional register); 0 semicolons; word count 1,638 (within
  norm); both claimed typo fixes verified present in the text.
- **Checked a real question the template raises but Comms' note glossed over**: the template's
  written "Footer convention for Ships" rule says a Ship's footer should tease the next
  Thursday item regardless of category — this draft's footer has no such teaser. Checked live
  precedent (Ship #058's actual published footer) before flagging: #058 also has no teaser,
  confirming this is established (if template-text-inconsistent) practice, not an omission
  specific to this draft. Did not block or edit on it — noted as a template-drift observation,
  not a defect.
- Verified all 7 links in the draft's body resolve live (200): the 5 published-pieces links, the
  embedded image, and the Previous-Ship link.
- Ran `publish-post.js --dry-run` — clean HTML conversion, script correctly auto-detected
  category=ship and reused the shared `piper-ship.webp` (no per-post image prep needed). Ran the
  real publish: CSV row appended, `blog-content.json` updated (hashId `d5d96e000593`), sync+fetch
  clean (10 syndication duplicates correctly skipped). Committed and pushed from the website
  worktree (`d983687`).
- **A real, live render problem found and correctly not accepted at face value**: polled for 200
  and got it quickly, but per m-43/this project's own "curl 200 is not a render test" discipline,
  did an actual content check — found the live page's title was literally "Ship Not Found" with a
  404 embedded, despite the 200 status. Investigated rather than assume broken: read the site's
  `shipping-news/[slug]/page.tsx` source, confirmed the JSON entry's `slug`/`category` fields were
  correct (ruling out a real data bug), compared response headers against a known-working Ship
  (#058) and found both tagged `x-vercel-cache: PRERENDER` — concluded this was a genuine deploy-
  propagation delay (the initial 200 was a stale cached not-found fallback), not a defect in my
  publish. Re-polled with a real content check (not just status) rather than a fixed guess-length
  wait; confirmed independently myself (title, image, and a body-text fragment all genuinely
  present) before treating it as resolved — did not report "live" on the first 200 alone.
- **A real, now twice-observed git gotcha**: committed the calendar CSV/view.html changes
  together with the draft's `git mv` rename in one call — `git status` showed all 3 as staged
  immediately before the commit, but the commit itself only picked up the rename (1 file). Caught
  immediately by re-checking `git status` after the commit (not assuming success from the command
  exiting 0), fixed with an explicit re-add + separate commit. This is the identical shape to a
  similar incident on the "More Than Anyone Ever Reported to Me" publish a few days ago — worth
  recording as a standing discipline: **after any commit that includes a `git mv`-staged rename
  alongside separately-staged modifications, always verify via `git status` that everything
  actually committed**, don't trust a clean exit code.
- Updated the calendar (status→published, blogURL/blogPath set), archived the draft to
  `drafts/published/` per Step 9, whole-file validator clean throughout.

### 6:57 AM - Fire 2 (cron, first scheduled fire of the day)

- Cron `433c7e09` confirmed single healthy job. Product worktree 29 behind, synced clean. Website
  worktree 1 behind, synced clean. Merge-keeper clean. Heartbeat self-suppressed correctly.
  Omnibus currency: 09-08 (yesterday), correct.
- **Mail loop found a real self-caught lapse**: Comms' original PUBLISH-READY memo for Ship #059
  was still sitting in the inbox — I'd read and acted on it directly this morning (via PM's
  in-conversation ask) but never moved it to `read/`, exactly the "act-on-a-memo-during-a-PM-
  session but forget to triage it" gap my own carry-forward already names as a standing lesson.
  Fixed now.
- **Real actionable item**: Dispatch-PM reported the LinkedIn syndication leg live for Ship #059
  (`ship` theme → LinkedIn is the whole obligation, Medium correctly empty), plus a self-owned
  correction: they'd twice wrongly claimed LinkedIn in-body images can't carry a hyperlink/alt
  text, when the platform's image editor does support both — this leg is confirmed correct on the
  live page. Checked Ship #058's calendar row for precedent before deciding whether to fill
  `altText`/`caption`: both empty there too, confirming the established Ship convention (no hero
  caption) rather than a gap — left them empty for #059 to match. Updated status→distributed,
  `linkedinURL`/`liPubDate` set, `canonicalSite`→distributed. Whole-file validator clean. Verified
  `git status` immediately after committing (given this morning's gotcha) — both files landed
  correctly this time, no `git mv` in this batch to trigger the same failure mode.
  **Noted but not chased**: Ship #058's own row has `canonicalSite=distributed` with an empty
  `linkedinURL` — a pre-existing data gap, out of scope for this fire, added to carry-forward as a
  watch item rather than guessed-and-fixed.
- 1 more FYI on the flywheel challenge round (Lead's engineering-lane concurrence on D4/D2, no
  action needed) — triaged.
- Nothing else unblocked. Returning to idle for the 09:57 fire.

### 9:57 AM - Fire 3 (WORK)

- Heartbeat self-suppressed correctly, marker updated. Product worktree 38 behind, synced clean.
  Website worktree 1 behind, synced clean. Merge-keeper clean.
- **A real, substantive item in my own lane**: Web's reply to a Dispatch-PM proposal (standardize
  `<figure>`/`<figcaption>` markup for captioned images — a real accessibility gap plus a
  documented 07-16 incident where the missing structure caused a false "no caption" report) named
  the in-body-teaser half as "Docs's call, not mine to make unilaterally." The original proposal
  was never actually in my own inbox despite being cc'd — found and read it directly from Web's
  `read/` folder rather than act on the reply alone without full context, per "investigate before
  extending."
  Investigated the actual mechanism rather than guess: read `convertToHtml()` in
  `publish-post.js` directly and found the variance Dispatch-PM measured (two blocks on 09-02, one
  joined paragraph on 09-09) is source-adjacency (a blank line between image and caption markdown
  splits them; no blank line joins them via the generic multi-line-paragraph rule), not renderer
  inconsistency. Verified my own most recent draft (Ship #059) already uses the adjacent, no-
  blank-line pattern — already my convention, nothing to change on my end. Sent a substantive
  reply to Web (cc Exec for the Dispatch-PM relay, Comms, PM): confirmed the root cause, confirmed
  my convention needs no change, and gave a concrete implementable spec (a new block-type check in
  `convertToHtml()`, same shape as the file's existing pattern-detection blocks) rather than leave
  it as an abstract ask — explicitly not urgent, implementation routed to Web since it's a
  converter-logic change in the website repo, not mine to freelance.
- Omnibus currency: 09-08 (yesterday), correct.
- Nothing else unblocked. Returning to idle for the 12:57 fire.

### 12:57 PM - Fire 4 (WORK)

- Heartbeat self-suppressed correctly, marker updated. Product worktree 44 behind, synced clean.
  Website worktree 1 behind, synced clean. Merge-keeper clean.
- **The figure/figcaption spec closed out — same day, faster than expected for something I'd
  called not-urgent.** Web implemented the in-body-teaser fix exactly per my spec (`5cb3a93`),
  reusing the file's existing detector conventions rather than inventing new ones, added 2
  regression-suite entries, ran all 21. **Verified independently rather than trust the report**:
  read the actual commit diff (matches the described implementation precisely) and ran the real
  test suite myself — confirmed 21/21 passing, including both new entries. Both halves of
  Dispatch-PM's original proposal are now genuinely done (hero template `fbfe813`, in-body
  converter `5cb3a93`). Triaged, no reply needed.
- Omnibus currency: 09-08 (yesterday), correct.
- Nothing else unblocked. Returning to idle for the 15:57 fire.

### 15:57 PM - Fire 5 (WORK)

- Heartbeat self-suppressed correctly, marker updated. Product worktree 52 behind, synced clean.
  Website worktree 0 behind. Merge-keeper clean. Mail loop empty.
- Omnibus currency: 09-08 (yesterday), correct.
- Quiet fire, nothing unblocked. Returning to idle for the 18:57 fire.
