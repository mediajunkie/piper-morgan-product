# Session Log: 2026-09-09-0649-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5, 1M context)
**Date**: Wednesday, September 9, 2026
**Start Time**: 6:49 AM PDT
**Prior session**: `dev/2026/09/08/2026-09-08-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-08** (day dominated by the FTUX render-check thread — confirmed CXO's interview copy renders exactly as specified, found and reported an unasked-for third paragraph, picked up a reachability question directly and found a real stale artifact filed as #1733, thread deepened into two more real defects filed by Lead (#1734/#1735) and a final copy decision; zero code changes, all three standing items unchanged).

## Session Objectives

1. Normal duty-cycle drain (mail loop → task loop → idle) — no PM-assigned task this fire, cron-triggered.

## Work Log

### 06:49 AM - Session Start

- `CronList`: exactly one job (`4e7e1685`, `22 6,9,12,15,18,21 * * *`) — no duplicates.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-08 -->` — no self-heal needed.
- Fingerprint check both worktrees: `web` / `claude/web-cycle` — correct, no collision indicators.
- Synced both worktrees clean: product repo, website repo.
- Cohort-freeze check: `rc=0`, INSUFFICIENT-SCHEDULE note (overnight window, 0 scheduled fires in window — not a freeze signal).
- Created this session log.
- **Mail loop**: genuinely empty (`web` inbox holds only MANIFEST).
- **Task loop**: all three standing items unchanged, still genuinely PM-gated
  (obs-pass, site walkthrough, Buttondown). No unblocked work.
- Emitted the START heartbeat for the day. Returning to idle.

### 06:56 AM — PM direct report: sitewide dark-mode contrast bug, root-caused and fixed

PM sent a screenshot (direct conversation, not mail) of the freshly-published Ship
#059 post in dark mode — nav correctly dark, but content area rendering washed-out
light-gray text on a light-gray background, nearly illegible. Message was addressed
to "Docs" — flagged the mix-up plainly and picked up the substance anyway since it's
squarely Web's lane.

**Investigated before fixing, not guessed**: confirmed dark mode was genuinely
active (checked `ThemeToggle.tsx`'s icon logic — sun icon shown means current theme
is dark). Traced `ShipPostContent.tsx`'s structure: outer wrapper uses
`bg-gray-50 dark:bg-dark-bg`, while its `.prose dark:prose-invert` content correctly
inverts text to light-on-dark coloring — meaning the mismatch (light text on a
still-light background) pointed at the background class specifically not applying.

**Root cause, confirmed via the compiled Tailwind config, not assumed**:
`tailwind.config.ts` defines the dark-background color token as `dark-background`
(`#0F172A`) — but `dark:bg-dark-bg` (referencing a token named `dark-bg`, which
doesn't exist) was used at every call site instead. Tailwind's JIT compiler silently
drops classes referencing undefined tokens rather than erroring, so the class
compiled to nothing, sitewide, for as long as it's existed — `git log -S` on the
real token name shows it's been `dark-background` since the initial commit
(2025-08-08), so this was never a regression, just always-broken.

**Scope was much larger than the one reported page**: `grep` found **8 files, 10
occurrences** — every regular blog post (`BlogPostContent.tsx`), every Ship post
(`ShipPostContent.tsx`), the Shipping News landing page, and 5 admin pages (login,
calendar, compose, publish-queue, AdminGate). Fixed all 10 occurrences to the real,
already-existing token name rather than add a duplicate alias — investigated the
config before extending it. Verified count matched exactly (10 before, 10 after,
0 remaining broken references) before committing.

Production build clean. Confirmed the fix actually compiles real CSS
(`.dark\:bg-dark-background{background-color:#0f172a}`, matching the site's
established dark-background color used elsewhere via CSS custom properties) rather
than trust a clean build alone. Committed (website `70791c3`) with full root-cause
evidence in the commit message, pushed, deploy verified `success` via GitHub
commit-status API poll. Reported to PM in chat — fix live, scope larger than the
one reported page (10 occurrences, 8 files, sitewide).

**Commits**: website `70791c3` — deployed, `success` confirmed.

### 09:49 AM — Fire (cron 09:22 slot): figure/figcaption accessibility fix shipped

Both worktrees synced clean, cohort-freeze check clean. Mail: **one direct memo** —
Dispatch-PM's well-evidenced proposal to emit `<figure>`/`<figcaption>` for
captioned images (measured live: zero anywhere on the site currently; caption is a
loose sibling `<p>`, not programmatically associated with the image; a real 07-16
defect already traced to this gap, now papered over by a fragile Tailwind-class
heuristic in Dispatch's own cross-post skill). **One cc**: CXO's structural voice
watch on Lead's landed directives — Lead-owned governance thread, nothing asked of
Web, skimmed and triaged.

**Investigated the actual scope before deciding what to do**: found the hero-image
block in `BlogPostContent.tsx` matches Dispatch's exact quoted markup — outer
layout div, inner image-container div, sibling caption `<p>`. Confirmed
`ShipPostContent.tsx` doesn't have the same pattern (its own image block was
removed 09-03). Dispatch's proposal explicitly scoped item 1 (hero) as "in your
hands, probably small" and item 2 (in-body markdown teaser) as "as much Docs's
convention as your template" — did only the part that was genuinely mine, left
the other for Docs rather than guess at a markdown-renderer fix.

**Shipped the hero fix**: wrapped the existing image-container div + caption `<p>`
in `<figure>`/`<figcaption>`, same Tailwind classes moved onto the new elements —
zero visual change, purely semantic. Production build clean. **Verified with a
real browser render, not just a clean build** (this component is `'use client'`,
so a curl check would have missed it) — found a live post with both `featuredImage`
and `imageCaption` set, confirmed via Playwright exactly one `<figure>` and one
`<figcaption>` with the correct text, screenshot confirms the visual output is
unchanged. Shipped website `fbfe813`, deploy confirmed `success`.

Replied to Dispatch-PM — but caught before sending that Dispatch-PM has **no
`mailboxes/` directory** in this repo (checked `DIRECTORY.md` rather than assume
my usual pattern applied); the ratified path is `to: dispatch-pm` delivered to
`mailboxes/exec/inbox/` for Exec to relay, not a direct write to `dispatch-dinp/`
(a different, last-resort fallback). Corrected the routing before sending — cc'd
Exec/Docs/Comms/PM, sent via `mail-send.sh` (`7b222f9db`), triaged both memos,
MANIFEST regen (`06485d505`) — same recurring `inbox/MANIFEST.md` false positive,
verified zero diff. Standing items unchanged.

**mail: 1 direct, 1 read in full; 1 cc, skimmed.** Genuinely at (0,0) after.

**Commits**: website `fbfe813` (figure/figcaption fix) — deployed, `success`
confirmed.

### 12:49 PM — Fire (cron 12:22 slot): in-body teaser fix, the other half of Dispatch-PM's proposal

Both worktrees synced clean, cohort-freeze check clean. Mail: **one direct memo** —
Docs picked up the in-body-teaser question I'd deferred to them this morning, read
`convertToHtml()` directly rather than guess, root-caused the variance Dispatch-PM
measured as source-adjacency (blank line vs. no blank line between image and
caption controls whether the existing paragraph collector joins them), confirmed
adjacent-no-blank-line is already their authoring convention, and handed back a
concrete implementation sketch with the disposition explicit: convention theirs
(done), implementation mine (a `convertToHtml()` change in the website repo).

**Verified Docs' claim against the actual source before implementing**: read
`scripts/publish-post.js`'s standalone-italic detector and multi-line-paragraph
collector directly — confirmed an image-only line falls through neither of the
existing block-type checks and lands in the generic paragraph collector alongside
a following non-blank caption line, exactly as described.

**Implemented matching existing conventions, not inventing new ones**: reused the
same `it1`/`it2` regex-and-guard pattern already used by the standalone-italic
detector for the caption side, added a new check ahead of the paragraph fallback
for an image-only line (linked or plain) immediately followed by such a caption
line. First draft had a fabricated `website #1737` citation in the code comment —
**caught it myself before committing**: checked `gh issue list` for any actual
issue matching this thread, found none, corrected the comment to cite the actual
mail thread instead of a number I'd invented.

**Tested against the project's own existing regression harness** rather than write
a throwaway script: found `scripts/test-publish-post-corpus.js` already existed
with 19 entries. Ran it first for a clean baseline (all pass), added two new
corpus entries (linked-image+asterisk-caption matching Docs' exact real pattern;
plain-inline-image+underscore-caption for the other branch), ran the full 21-entry
suite — all pass, zero regressions. Separately verified against Docs' own literal
example string as one more concrete check. Shipped website `5cb3a93` (a script
change, not a deployed page — no Vercel check applies; takes effect next time
`publish-post.js` runs for a new post).

**A genuine oddity, investigated rather than shrugged off**: after pushing, found
the exact same 4 corpus files as untracked, uncommitted stray copies in *this*
(product) repo's `scripts/publish-post-corpus/` — a location with zero prior git
history here and no `publish-post.js` of its own. Checked whether the two
worktrees were somehow the same physical location (`realpath`, inode comparison)
— confirmed genuinely different directories, ruling out a symlink/mount
explanation. Couldn't determine the exact mechanism; the practical fix was simple
(the files were untracked, never committed or pushed, harmless) — removed them
from the product repo and re-confirmed the real, correct copies are intact and
committed in the website repo. Noting this honestly rather than letting an
unexplained observation go unrecorded.

Replied to Docs cc Exec/Comms/PM confirming both halves of Dispatch-PM's proposal
are now shipped, sent via `mail-send.sh` (`970c751a2`), triaged the memo, MANIFEST
regen (`c2d94626f`) — same recurring `inbox/MANIFEST.md` false positive, verified
zero diff. Standing items unchanged.

**mail: 1 direct, 1 read in full; 0 cc.** Genuinely at (0,0) after.

**Commits**: website `5cb3a93` (in-body teaser figure/figcaption fix + 2 corpus
regression entries).

### 15:49 PM — Fire (cron 15:22 slot), genuinely quiet

Both worktrees synced clean, no collision indicators. Cohort-freeze check
clean, exit 0. Mail empty, standing items unchanged (all three still
PM-gated). Heartbeat self-suppressed. Returning to idle.

### 18:49 PM — Fire (cron 18:22 slot), genuinely quiet

Both worktrees synced clean, no collision indicators. Cohort-freeze check
clean, exit 0. Mail empty, standing items unchanged (all three still
PM-gated). Heartbeat self-suppressed. Not the last scheduled fire of the
day (21:22 remains) — returning to idle, not STOP.
