# Session Log — 2026-09-09 18:32 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Assignment**: Two sequential items from PPM's ordered epics (dev/active/mvp-epic-order-2026-09-09.md):
1. #1734 [SECURITY] — personality API PUT rewrites global config/PIPER.user.md; gate or scope it
2. #1637 — 6 standing tests/intent failures + cross-contamination poisoning combined runs

## Item 1 — #1734

- Verified the filing's claims against current code:
  - `web/personality_integration.py:50-75` — `load/save_personality_config` take `user_id` and ignore it entirely; `_save_config_file` writes `config/PIPER.user.md` (global). CONFIRMED.
  - `web/api/routes/personality.py:65` — PUT `/api/v1/personality/profile/{user_id}` has NO auth dependency at route level; any authed user's save hits the shared file. CONFIRMED.
  - `/enhance` (same file, POST) reads only — consumes `load_personality_config`; feeding it from a per-user store would require rewiring its read path (data-model decision).
- Shape chosen: **(b) admin-gate the PUT** via `require_admin` (services/auth/auth_middleware.py:572, the #1508/#1598 idiom). Smallest diff, kills the hazard, preserves the page for admins, defers the per-user data model to the ADR-075 successor design. GET and /enhance untouched (read-only).
- Test idiom reused from `tests/unit/web/api/routes/test_admin_readonly_routes_gated_1598.py` (patch `_user_is_admin`, override `get_current_user`, real `require_admin` logic runs).

- Shipped (18:35):
  - `web/api/routes/personality.py` — `Depends(require_admin)` on the PUT only; docstring records the 1734 rationale and the un-gate condition (per-user store).
  - `tests/unit/web/api/routes/test_personality_put_admin_gated_1734.py` — 7 pins: non-admin PUT 403 + overlay hash provably unchanged, own-user_id-in-path not a bypass, no payload echo in the 403, admin PUT 200 + persists, non-admin GET 200, GET wiring dependency-free, unauthenticated PUT 401 before admin lookup + file untouched.
  - Evidence: 7/7 new tests pass; `tests/unit/web/ + tests/test_architecture_enforcement.py` = 743 passed; ruff check + format clean on both files.

## Item 2 — #1637

- Current state vs the issue's 2026-08-16 claims (reproduced before touching anything):
  - **Set 1 (standalone failures): still exactly 6**, same files — one intra-file drift: test_no_cli_bypasses now fails at `test_all_commands_import_intent` asserting on keys.py (the issue's "keys.py assertion", same root). Baseline: `6 failed, 179 passed, 2 skipped`.
  - **Set 2 (combined-run poisoning): NO LONGER REPRODUCES.** A/B'd both ways on this commit: combined invocation (`tests/unit/services/intent_service tests/unit/services/intent tests/intent`) with PRE-fix tests/intent = same 6 failures, **0 unit flips** (4036 passed — the three previously-flipping files exist and their 144 tests all pass); with post-fix tests/intent = 4042 passed, 0 failed. The poisoning drifted away in the month since filing; nothing live left to fix.
- The 6, individually diagnosed and fixed (all stale pins or test bugs; zero product-code changes):
  1. test_bypass_prevention::test_middleware_is_registered — magic counts (nl==4, exempt==12) vs grown exempt list (13). Now compares against `IntentEnforcementMiddleware.NL_ENDPOINTS/EXEMPT_PATHS` (the config source) — can't go stale, still proves the endpoint reports real config.
  2. test_enforcement_integration::test_monitoring_endpoint_accessible — pinned pre-#1598 contract (unauthenticated 200). #1598 admin-gated the route; renamed to `test_monitoring_endpoint_exists_and_is_gated`, pins 401 (mounted but gated; 404 = route gone, 200 = gate off). Admin-side 200 + payload pinned in test_admin_readonly_routes_gated_1598.py.
  3. test_future_nl_endpoints::test_all_nl_routes_in_middleware_config — regex captures decorator paths, blind to router prefixes ("/intent" vs configured "/api/v1/intent"). Now suffix-aware. ALSO unmasked "/query" (GET /api/v1/knowledge/query — structured typed params, not NL): documented false-positive allowlist. Loop de-masked (collect-all, not assert-first).
  4. test_no_cli_bypasses::test_all_commands_import_intent — keys.py (#270 credential mgmt) has no NL surface; exempted with rationale. Unmasked standup.py (flag-driven, no NL; API twin /api/standup is middleware-enforced) — was ALWAYS failing, hidden behind keys.py by the per-file assert. Loop de-masked.
  5. test_handler_error_handling::test_status_query_empty_projects — pinned pre-ADR-059 `action_required == "configure_projects"`; ADR-059 put onboarding on ice, branch now returns generic floor response with NO action_required. Pin updated to current contract.
  6. test_no_timeouts::test_query_fallback_handles_misclassifications — env-dependent: message matched no pre-classifier pattern → real LLM classifier → uninitialized container → INTENT_CLASSIFICATION_FAILED. The failing layer (classifier) wasn't the layer under test (QUERY dispatch fallback, m-43). Classification now stubbed via classify_multiple mock; dispatch fallback verified for real (probed first: succeeds without LLM).
- Isolation hardening: test_no_web_bypasses.py built `TestClient(app)` at MODULE level = collection-time side effect before any unit test runs in a combined invocation — the exact shape that made the 2026-08-16 poisoning possible. Moved to a class-scoped fixture.
- No skips added; the 2 standing skips are pre-existing (PIPER_TEST_REPO env gate; a self-skipping review sentinel in test_future_nl_endpoints) and present in the baseline.
- Green target proof: tests/intent standalone `185 passed, 2 skipped, 0 failed`; combined `4042 passed, 2 skipped, 0 failed`; architecture ratchets 49 passed; ruff check+format clean on all 7 touched files.
