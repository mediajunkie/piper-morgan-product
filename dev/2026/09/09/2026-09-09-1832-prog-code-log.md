# Session Log — 2026-09-09 18:32 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Assignment**: Two sequential items from PPM's ordered epics (dev/active/mvp-epic-order-2026-09-09.md):
1. #1734 [SECURITY] — personality API PUT rewrites global config/PIPER.user.md; gate or scope it
2. #1637 — 6 standing tests/intent failures + cross-contamination poisoning combined runs

## Item 1 — #1734

- Verified the filing's claims against current code:
  - `web/personality_integration.py:50-75` — `load/save_personality_config` take `user_id` and ignore it entirely; `_save_config_file` writes `config/PIPER.user.md` (global). CONFIRMED.
  - `web/api/routes/personality.py:65` — PUT `/api/v1/personality/profile/{user_id}` has NO auth dependency at route level; any authed user's save hits the shared file. CONFIRMED.
  - `/enhance` (same file, POST) reads only — consumes `load_personality_config`; feeding it from a per-user store would require rewiring its read path (data-model decision).
- Shape chosen: **(b) admin-gate the PUT** via `require_admin` (services/auth/auth_middleware.py:572, the #1508/#1598 idiom). Smallest diff, kills the hazard, preserves the page for admins, defers the per-user data model to the ADR-075 successor design. GET and /enhance untouched (read-only).
- Test idiom reused from `tests/unit/web/api/routes/test_admin_readonly_routes_gated_1598.py` (patch `_user_is_admin`, override `get_current_user`, real `require_admin` logic runs).

- Shipped (18:35):
  - `web/api/routes/personality.py` — `Depends(require_admin)` on the PUT only; docstring records the 1734 rationale and the un-gate condition (per-user store).
  - `tests/unit/web/api/routes/test_personality_put_admin_gated_1734.py` — 7 pins: non-admin PUT 403 + overlay hash provably unchanged, own-user_id-in-path not a bypass, no payload echo in the 403, admin PUT 200 + persists, non-admin GET 200, GET wiring dependency-free, unauthenticated PUT 401 before admin lookup + file untouched.
  - Evidence: 7/7 new tests pass; `tests/unit/web/ + tests/test_architecture_enforcement.py` = 743 passed; ruff check + format clean on both files.

(entries continue below as work lands)
