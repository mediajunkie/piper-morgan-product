# Session Log — 2026-09-09 15:34 — Coding Agent (prog)

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Start #973 (MEM-CACHE-AUDIT) — oldest never-started item. Verify-first freshness audit of the 149-day-old issue against current code, then execute whatever survives.
**Branch**: claude/lead-cycle (Lead's worktree), 0 behind origin/main at start

## Freshness audit of #973 (created 2026-04-13, zero comments, milestone: Production)

Issue scope (no phases in the issue itself — the whole issue IS the audit/doc task):
document each `context_assembler.py` method STABLE vs DYNAMIC, order stable content
first, add cache-readiness TTL notes, no behavioral change — all "to prepare the
assembler for future caching (Redis TTL) without implementing caching yet."

Claim-by-claim verdicts against current code:

1. **"Currently the context assembler doesn't make this distinction explicit"** —
   **SUPERSEDED by #984** (CONTEXT-CACHE, closed 2026-05-12). `ContextCache` (Redis
   TTL, fail-open) is implemented and wired: 3 method-level cached gatherers (trust
   3600s, calendar 60s, reminders 30s) + 8 source-level cached helpers (pending 30s;
   completed/projects/user_context/blocked_items/high_priority_issues/
   active_milestones/recent_activity 300s), TTL table PM-approved 2026-05-12
   (`_TTL_*` constants, context_assembler.py:72-87). Follow-ons #983 #985 #986 #1155
   #1625 #1645 refined it.
2. **AC1 (per-method STABLE/DYNAMIC docs)** — ~80% superseded by richer reality:
   every cached method documents its actual TTL + invalidation strategy (TTL-only
   vs eager), strictly stronger than a STABLE/DYNAMIC binary. **Residue**: module
   docstring principle 3 still said "Cache-ready — design for Redis TTL caching
   later (not implemented yet)" — a live falsehood since 2026-05-12, also quoted
   into five-layer-context-mapping.md:188. And the deliberately-uncached methods
   (identity/discovery capabilities, memory, insight-pull) had no explicit
   "per-request, why" note.
3. **AC2 (stable content assembled first)** — **SUPERSEDED BY ARCHITECTURE**. The
   assembler is a per-category dispatch + cross-cutting riders (#1566 reminders,
   #1536 first-contact, #1688 ftux) returning a data dict for floor injection.
   Caching happens per-source inside `get_or_compute`; dict-merge order has zero
   caching consequence. The April premise (stable-prefix-first prompt assembly,
   Anthropic prompt-caching style) would live in the floor renderer/LLM client,
   not this file. Not executed; reported for rescope.
4. **AC3 (what TTL would be appropriate)** — **DONE**, better than asked: actual
   PM-approved TTLs with rationale comments, not hypothetical notes.
5. **AC4 (no behavioral change)** — governs the residue; respected.

Aug 29-31 disposal-campaign overlap check (decisions.log): spatial cold-island +
census disposals + #1613 pooling — none touch context_assembler/context_cache;
the 07-29 Arch spatial review confirms context_assembler is on the LIVE chain.

## Work executed (doc-only residue, zero behavioral change)

- Module docstring: principle 3 corrected to reflect implemented #984 caching,
  with a STABLE/DYNAMIC summary keyed to the TTL table (satisfies AC1 in
  current terms).
- One-line DYNAMIC/per-request notes added to `_gather_identity_context`,
  `_gather_discovery_context`, `_gather_memory_context`,
  `_gather_insight_pull_context` docstrings.
- No code paths touched. Tests + ratchets run per standard env.

## Recommendation (for Lead/PPM)

Close 973 as superseded-by-984 with residue shipped this session; AC2's
stable-first ordering is moot in the current architecture — if the underlying
concern (prompt-prefix caching for the floor prompt) still matters, it is a NEW
issue against the floor renderer, not this assembler.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (verify-first, stale-info-update norm, bare issue
  numbers in commits, no close-keywords); decisions.log (Aug 29-31 disposal
  records, 07-29 spatial review — live-chain confirmation for context_assembler);
  five-layer-context-mapping.md (issue reference, stale quote at line 188).
- **Loaded but not referenced**: memory index (MEMORY.md).
- **Wanted but not found**: none — #984's in-code comments were sufficient.
