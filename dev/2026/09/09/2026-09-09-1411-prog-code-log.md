# Session Log — 2026-09-09 14:11 — Coding Agent (prog)

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree; commits staged, NOT pushed per delegation)
**Task**: #1739 — the ONE acceptance contract (predicate + adoption ratchet + READ-tier adoptions)

## Governance inputs (read in full)
- gh issue 1739 (umbrella + axes)
- Arch ruling 2026-09-09: predicate-as-single-source RIGHT; three binding sequencing
  conditions: (a) input-adequacy before DESTRUCTIVE-adjacent adoption (arm-site stores
  rendered ask), (b) adopt by EffectClass ASCENDING (READ first), (c) shrink-only
  KNOWN_UNADOPTED ratchet (MAX_DISPATCH_SITES idiom) so the contract is a chokepoint.
- CXO pass 2026-09-09: strictness scales on BOTH ratified axes (EffectClass AND
  Outwardness — decide_consent's two axes, 08-15); question-form = different speech act
  (answer state, restate arm, arm survives); four-verdict table; PROCEED_WITH_DISCLOSURE
  must never become an arm; user-facing test = quote the acceptance back.
- Exec round memo: PM 09-09 verbatim failing exchanges (`are we done with that standup?`
  fired; bare `yes` rejected at #1694).

## Machinery surveyed
- detect_confirm_response / detect_offer_response / ACCEPT_PATTERNS / DECLINE_PATTERNS /
  CONFIRM_ACCEPT_RE / is_prose_reply — services/intent_service/soft_invocation.py
- decide_consent 36-cell matrix + EffectClass/Outwardness — consent_gate.py, shared_types.py
- Generic offer seam + CONFIRM branch — services/intent/intent_service.py ~1060-1470
- #1617 site — services/standup/conversation_handler.py _handle_refining (substring
  acceptance_words check; `done` in "are we done with that standup?" fires COMPLETE)
- #1651 standup_todo_offer, #1665 (CLOSED — arm sites store "question"), #1666 (CLOSED)
- Ratchet idioms — tests/test_architecture_enforcement.py (MAX_DISPATCH_SITES,
  KNOWN_UNMIGRATED)

## Work log
- 14:11 session start; read governance memos + issue + machinery.
- 14:20–15:05 built #1739 per both governance passes:

### 1. The predicate — `services/intent_service/acceptance.py`
- `evaluate_acceptance(message, *, effect, outwardness, armed_question, taught_accepts)`
  → verdicts ACCEPT / DECLINE / STATE_QUESTION / PASS (CXO: a question is a
  different speech act — never accept, never decline, arm survives).
- `acceptance_tier(effect, outwardness)` — TWO-AXIS strictness (CXO's binding
  correction): DESTRUCTIVE any-outwardness and WRITE×OUTWARD → NAMED_OBJECT
  (crisp full-message only); READ and WRITE×PRIVATE → LOW_CEREMONY (generic
  vocabulary + seam-taught full-message phrases); undeclared → NAMED_OBJECT.
- Input adequacy with teeth (Arch (a)): NAMED_OBJECT accept against a falsy
  armed_question is REFUSED (PASS). LEGACY_UNTHREADED sentinel marks the
  pre-contract `detect_confirm_response` alias path only.
- Ordering: prose floor (1631) → question-shape guard → tier-scaled accept →
  decline → PASS. Question shapes: terminal "?" or hard interrogative opener
  (are/is/am/was/were/does/did/wh-*/how; do/can/could/etc. deliberately
  excluded — they open imperatives ("do it")).
- ABSORBED 1650: `detect_confirm_response` is now a delegate over the
  predicate (DESTRUCTIVE tier); vocabularies stay declared in
  soft_invocation.py, composed only by the predicate. `detect_offer_response`
  stays byte-identical legacy for unadopted seams (pinned).

### 2. The ratchet — `TestAcceptanceContractRatchet` (test_architecture_enforcement.py)
- KNOWN_UNADOPTED_DETECTOR_SITES (file, detector) pairs + KNOWN_BESPOKE_
  VOCABULARY_FILES, both SHRINK-ONLY with staleness guards + vacuity guard
  (m-44) + adopted-seam regression pins (evaluate_acceptance present;
  acceptance_words banned).
- RED-PROOF WATCHED FIRING: planted `_ratchet_red_proof_1739.py` calling
  detect_offer_response → test failed listing the file by name; removed →
  5 passed. Evidence in transcript.

### 3. READ-tier adoptions (Arch (b): ascending)
- **Standup REFINING tail (#1617 site)**: substring word-list REPLACED by the
  predicate (READ tier, taught chips full-message only). PM's verbatim
  "are we done with that standup?" → honest status answer + draft + restated
  ask, flow STAYS in REFINING (pinned). Arm sites store rendered ask in
  conversation.context["refining_ask"] (+ deterministic per-state default).
- **Generic offer seam, READ kinds** (intent_service.py): registry-declared
  axes looked up; STATE_QUESTION re-arms the offer (arm survives) and routes
  the turn; bare-exit + abandonment paths guarded against the re-arm case.
- **Contextual last_offer binding (#852)**: predicate at READ tier;
  armed_question = LastOffer.offer_text; question-forms can no longer bind
  the continuation hint ("yes, what's on it?" steal fixed); one-turn expiry
  invariant unchanged (arm-survival at this store = remainder).
- **Soft-offer arm site**: now stores "question" (input adequacy, was the
  one READ arm site missing it).

### 4. DESTRUCTIVE-adjacent: NOT adopted this lane (per instruction)
Input adequacy HOLDS for all of them (#1665 closed: every confirm arm site
stores "question" — destructive_confirm, consent_gate, drafted_issue,
repo_clarification, reminder_clear, standup_todo_offer). They QUALIFY under
Arch (a); they get axis-(a) question-guard + prose floor for free via the
absorbed alias; full axes/ask threading deferred per ascending order.

### Test evidence
- tests/unit/services/intent_service/ FULL: 3807 passed
- tests/unit/services/intent/: 53 passed
- standup + process (1617 tail release): 597 passed, 7 skipped
- targeted post-format sweep (standup + 1650 + 1666 + 1654 + 1739 + arch
  enforcement + completion ratchets + seed guard): 656 passed
- pinned ruff 0.6.9: check clean + format clean on all touched files
- new pins: 72 (contract file) + 13 (standup REFINING class) + 5 (ratchet)

### Discovered issues filed
None filed — residues recorded on the ratchet's status table + report to
Lead (LOW-tier greedy-row vocabulary before any WRITE-PRIVATE adoption is
CXO-owned; REFINING ask polarity note; contextual-store arm-survival).

