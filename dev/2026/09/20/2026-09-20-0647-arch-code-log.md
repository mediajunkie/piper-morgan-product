# Session Log: 2026-09-20-0647-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Opus 5 — observed from harness environment block)
**Date**: Sunday, September 20, 2026
**Start Time**: 6:47 AM PT (PM-initiated, ahead of the 06:27/06:57 cron fire)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

## Work Log

### PM asked whether his Q5 answer got lost in relay. It didn't — the ambiguity was mine (06:47)

**PM's question**: *"I thought I already addressed Q5. Perhaps I gave my answer to lead or exec, and
it wasn't conveyed to you?"* — with an explicit ask to check before he chases it.

**Searched, and the answer is clean: nothing was lost.**
- `decisions.log` — all 8 entries dated 2026-09-19 read; **no Q5 ruling**, and none of Lead's four
  relayed PM rulings (#1818, #1812 step 5, Slack sponsorship, #1785) is Q5.
- Lead's 09-19 session log — **no Q5 mention at all**.
- Exec's 09-19 session log **line 53 has it**: *"Yesterday's queue largely cleared: **PM ruled Q5**
  and the scope-guard, Web closed #35…"* — "yesterday" from a 09-19 log = **09-18**.
- Q5 appears in **neither** the 09-18 nor 09-19 attention rollup — consistent with Exec marking it
  ruled and dropping it from the board.

**So PM's answer landed on 09-18 — directly with me, in conversation** (*"Idle is a legitimate
state"*). Exec independently logged it as ruled. **No memo went astray and nobody failed to relay
anything.**

⚠️ **The confusion is my own imprecision, and I'd rather name it than let PM go hunting.** I closed
five consecutive status lines with some form of *"still yours: the Q5 one-liner."* **That phrasing
reads as "Q5 is unanswered."** What I meant every time was the *denominator* half — a follow-on
question I raised in response to his ruling, which only I have been tracking, and which he has been
asked exactly once. A shorthand that names the parent item when the open part is a child of it will
reliably make the recipient think they dropped something they didn't.

**Fix applied to my own practice**: name the open *sub*-question in status lines, never the parent
item — *"the Q5 denominator"*, not *"Q5"*. Same class as this seat's #4 failure mode (fixing the
record but leaving the reading surface wrong): the standing-items row was accurate all along; the
line PM actually read was not.

### I ruled #1818 on a name instead of a definition, and caught it before it shipped (06:57 fire)

Mail: 2, both direct, both #1818. Inbox drained to zero at trunk. Criteria line: 10, no new.

**Lead's interim trace found my ruling's premise was softer than I'd stated** — the disposition
registry is **dispatch-inert** (`get_disposition` has zero call sites; my gate would be its *first*
runtime consumer). Lead framed that as strengthening the ruling. It was the thread that unravelled it.

Lead's memo pointed at **#1773**, and Lead explicitly flagged their own summary of it as
*not re-verified*. So I read #1773's body — which named `_requires_canonical_handler` as **the live
routing authority.** ⚠️ **I had not read that function when I ruled.**

```
Returns True ONLY for intents that need side effects, database writes,
or deterministic fast-path responses.          # :14940-14941
if category == "PORTFOLIO":  return True       # ALL operations
if category == "EXECUTION":  return True       # create issue, manage todos
```

🔴 **`CANONICAL` means "the LLM cannot do this alone." It does NOT mean "this costs no LLM call."**
For a *spend* gate those are near-opposite: EXECUTION and PORTFOLIO are canonical **because they
write to the database and create issues.** **My ruling, implemented literally, would have opened a
keyless path to issue creation** — materially worse than the refusal it was softening.

**Where it bit is the part worth keeping.** Not in a value I misread — **in a name I trusted.** I
reasoned from the word *canonical* and from `ACTION_REGISTRY`'s own comment `fast-path/deterministic`,
which is true of *some* canonical pairs and false of most. That is my own week-old rule one level up:
*a grep line-hit is not a read* → **a name is not a definition, and a registry's comment describing
an authority is not the authority.** Fifth honest-empty instance in eight days.

**Corrected ruling**: the property doesn't exist and must be **created** — a distinct
"this turn spends nothing" predicate **defaulting to `False`**, so a handler is not exempt unless
deliberately marked *and* proven. The default *is* the safety property. What survives: property-not-
list, and the ordering cure (pre-classify first; `PreClassifier` is regex, zero LLM calls — verified,
and still verified).

⭐ **Lead's ratchet becomes the definition, not a regression test.** Their instrument — drive each
pair keyless with `complete` rigged to explode — is right, but the expected result is no longer
mostly-pass: EXECUTION/PORTFOLIO should blow up, and **those explosions are the measurement.** The
set that survives *is* the spends-nothing set. Nothing else defines it today. Better than what my
ruling would have produced, and it's Lead's design.

⭐ **And Lead's deliberate banking was right for a reason neither of us could see**: they banked the
hand-trace ("a hand-trace that missed one transitive call would be worse than the two-hour wait").
**A hand-trace would have run under my wrong premise and confirmed it.**

**#1773 sequencing, now answerable**: decoupled — it no longer blocks #1818, because the gate should
not consume the registry at all. Also caught: #1773's body cites the function at `:14641-14658`; it
is at `:14935`.

**Corrected at all three surfaces within the fire** — urgent memo to Lead (who was mid-build), the
#1818 issue comment, and an **inline marker on the wrong decisions.log entry** plus a full correction
entry. Second time in three days I've had to do the inline-marker move; it is becoming the reflex it
should be.
