# Session Log: 2026-09-20-0647-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Opus 5 — observed from harness environment block)
**Date**: Sunday, September 20, 2026
**Start Time**: 6:47 AM PT (PM-initiated, ahead of the 06:27/06:57 cron fire)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

## Work Log

### PM asked whether his Q5 answer got lost in relay. It didn't — the ambiguity was mine (06:47)

**PM's question**: *"I thought I already addressed Q5. Perhaps I gave my answer to lead or exec, and
it wasn't conveyed to you?"* — with an explicit ask to check before he chases it.

**Searched, and the answer is clean: nothing was lost.**
- `decisions.log` — all 8 entries dated 2026-09-19 read; **no Q5 ruling**, and none of Lead's four
  relayed PM rulings (#1818, #1812 step 5, Slack sponsorship, #1785) is Q5.
- Lead's 09-19 session log — **no Q5 mention at all**.
- Exec's 09-19 session log **line 53 has it**: *"Yesterday's queue largely cleared: **PM ruled Q5**
  and the scope-guard, Web closed #35…"* — "yesterday" from a 09-19 log = **09-18**.
- Q5 appears in **neither** the 09-18 nor 09-19 attention rollup — consistent with Exec marking it
  ruled and dropping it from the board.

**So PM's answer landed on 09-18 — directly with me, in conversation** (*"Idle is a legitimate
state"*). Exec independently logged it as ruled. **No memo went astray and nobody failed to relay
anything.**

⚠️ **The confusion is my own imprecision, and I'd rather name it than let PM go hunting.** I closed
five consecutive status lines with some form of *"still yours: the Q5 one-liner."* **That phrasing
reads as "Q5 is unanswered."** What I meant every time was the *denominator* half — a follow-on
question I raised in response to his ruling, which only I have been tracking, and which he has been
asked exactly once. A shorthand that names the parent item when the open part is a child of it will
reliably make the recipient think they dropped something they didn't.

**Fix applied to my own practice**: name the open *sub*-question in status lines, never the parent
item — *"the Q5 denominator"*, not *"Q5"*. Same class as this seat's #4 failure mode (fixing the
record but leaving the reading surface wrong): the standing-items row was accurate all along; the
line PM actually read was not.

### I ruled #1818 on a name instead of a definition, and caught it before it shipped (06:57 fire)

Mail: 2, both direct, both #1818. Inbox drained to zero at trunk. Criteria line: 10, no new.

**Lead's interim trace found my ruling's premise was softer than I'd stated** — the disposition
registry is **dispatch-inert** (`get_disposition` has zero call sites; my gate would be its *first*
runtime consumer). Lead framed that as strengthening the ruling. It was the thread that unravelled it.

Lead's memo pointed at **#1773**, and Lead explicitly flagged their own summary of it as
*not re-verified*. So I read #1773's body — which named `_requires_canonical_handler` as **the live
routing authority.** ⚠️ **I had not read that function when I ruled.**

```
Returns True ONLY for intents that need side effects, database writes,
or deterministic fast-path responses.          # :14940-14941
if category == "PORTFOLIO":  return True       # ALL operations
if category == "EXECUTION":  return True       # create issue, manage todos
```

🔴 **`CANONICAL` means "the LLM cannot do this alone." It does NOT mean "this costs no LLM call."**
For a *spend* gate those are near-opposite: EXECUTION and PORTFOLIO are canonical **because they
write to the database and create issues.** **My ruling, implemented literally, would have opened a
keyless path to issue creation** — materially worse than the refusal it was softening.

**Where it bit is the part worth keeping.** Not in a value I misread — **in a name I trusted.** I
reasoned from the word *canonical* and from `ACTION_REGISTRY`'s own comment `fast-path/deterministic`,
which is true of *some* canonical pairs and false of most. That is my own week-old rule one level up:
*a grep line-hit is not a read* → **a name is not a definition, and a registry's comment describing
an authority is not the authority.** Fifth honest-empty instance in eight days.

**Corrected ruling**: the property doesn't exist and must be **created** — a distinct
"this turn spends nothing" predicate **defaulting to `False`**, so a handler is not exempt unless
deliberately marked *and* proven. The default *is* the safety property. What survives: property-not-
list, and the ordering cure (pre-classify first; `PreClassifier` is regex, zero LLM calls — verified,
and still verified).

⭐ **Lead's ratchet becomes the definition, not a regression test.** Their instrument — drive each
pair keyless with `complete` rigged to explode — is right, but the expected result is no longer
mostly-pass: EXECUTION/PORTFOLIO should blow up, and **those explosions are the measurement.** The
set that survives *is* the spends-nothing set. Nothing else defines it today. Better than what my
ruling would have produced, and it's Lead's design.

⭐ **And Lead's deliberate banking was right for a reason neither of us could see**: they banked the
hand-trace ("a hand-trace that missed one transitive call would be worse than the two-hour wait").
**A hand-trace would have run under my wrong premise and confirmed it.**

**#1773 sequencing, now answerable**: decoupled — it no longer blocks #1818, because the gate should
not consume the registry at all. Also caught: #1773's body cites the function at `:14641-14658`; it
is at `:14935`.

**Corrected at all three surfaces within the fire** — urgent memo to Lead (who was mid-build), the
#1818 issue comment, and an **inline marker on the wrong decisions.log entry** plus a full correction
entry. Second time in three days I've had to do the inline-marker move; it is becoming the reflex it
should be.

### PM tasking: deployment pipeline plan v0.1 shipped (09:57 fire)

Mail: 6 — **2 direct** (Exec's tasking; CXO's #1818 finding), **4 cc**. Inbox drained to zero.

**#1818 first, because Lead was mid-build.** CXO checked a credit I'd given them instead of banking
it, and it produced the day's best finding: the exempt set is **incoherent to a user** — `hi` passes,
**`Hi, how do I address you?` is refused**, `thanks`/`bye` refused. *The terser, less human opener is
the one we reward.*

**I took it further than CXO did, and it's a question about the whole feature**: if the set is ragged
because the boundary is invisible to users, the fix isn't a better boundary — it's **making the
boundary stop mattering.** If the *refusal itself greets*, every first contact is uniform and there's
no exempt set to go ragged. ⭐ **Under that shape the exemption buys very little** — a canned greeting
instead of a greeting-plus-explanation — which is a lot of structure (new predicate, default-False
marker, ratchet to police it) for a small delta.

**Did NOT rule it.** PM gave direction here (*"handle greetings onboard"*), so I sent it to PM as
(a)-vs-(b): is the intent *the greeting must be free of the gate*, or *first contact must not feel
like a challenge*? My read is (b) satisfies it for far less machinery, but that's a product call with
PM's thumb already on it. **Told Lead: keep building the ratchet** (valuable either way — it's the
only thing that defines the spends-nothing set) **but hold the gate change** until PM answers.

### The main event — PM's top-priority tasking

`docs/internal/architecture/deployment-pipeline-plan-v0.1-2026-09-20.md`, on `origin/main`, plus a
cover memo to PM cc Exec/Lead/Pard/HOST/PPM. **Nothing built**, per Exec's scope note.

**Diagnosis**: the build system and the release marker track different things, and nothing could
answer "what is running" without SSH — **so the pipeline didn't fail, it was never observable enough
to fail loudly.** `docker.yml` builds on `main`; the branch meaning "released" is `production`;
nothing connects them.

**It is not greenfield, which made the plan cheaper.** Verified present: VERSION + annotated tags,
release notes **current through v0.8.12.0**, `check-release-notes.sh`, `check-release-parity.sh`
(earned by incident #1413 — its own header states this plan's principle: *"A parity CLAIM is a
statement about a moment; this script makes it a statement about NOW"*), and blue-green rollback
proven on the droplet today.

🔴 **Finding: we already paid for staging tooling that cannot run.** `docker-compose.staging.yml` and
`deploy_staging.sh` exist; **`.env.staging` does not, nor an `.example`**, and the script requires it
at line 12. No deploy workflow invokes any of it. "Add staging" would buy the same thing twice.
**Present is not live** — the hook-that-never-fires shape, in the deploy layer.

**Version story** (Exec asked): three things share two words. Proposed separating **environment**
(`local`/`staging`/`prod`) from **stage** (alpha/beta/GA) permanently — under which **opening beta is
an access decision, not a migration**, and the `production` branch becomes redundant with the tag.
Two refs meaning "released" is precisely how ~4,195 commits of drift stayed invisible.

**Flagged one thing I'd take ahead of the ruling**: expose version + SHA on `/health`. Every gate
depends on cheaply answering what's deployed; **a gate that can't read the current version isn't a
gate.** Didn't take it — PM asked for a plan, and that's code.

⚠️ **Caught a stale premise in my own tasking.** Exec's "what's known" says alpha runs `0.8.10.14`;
Lead's report landed mid-flight and alpha is **v0.8.12.0**. Not a criticism — **it is an instance of
the thing the plan is about**, and I said so that way: the deployment facts went stale inside hours,
and the only reason anyone noticed is that a human happened to SSH in.

**Did not manufacture a scare**: `git rev-parse v0.8.12.0` returned a SHA ≠ `origin/production`,
which *looks* like tag/deploy divergence. It isn't — annotated tags resolve to the tag object.
`v0.8.12.0^{commit}` = `a16f03e44` = `origin/production` exactly. Checked before reporting, because a
fabricated mismatch on a deploy question would have cost more than the ten seconds.

**Also measured rather than asserted**: `main` is 24 commits ahead of `production`, but **0 touch
product code** — so alpha is current in the only sense that matters, and the metric distinguishes
"24 behind" from "24 behind that matter."

### PM approved the plan; built the /health deploy identity (12:57 fire)

Mail: 9 in — 7 handled, **3 deliberately held in inbox** (see below). Criteria line 10, +1 filed.

**PM approved pipeline plan v0.1 as written**, including the item I'd held for his word. Also:
#1812 embeddings sponsor confirmed (my six-consumer enumeration is Lead's plan spine — the
`return None` rung dies, refusal goes unconditional); #1818 re-posed to PM.

⭐ **Lead's ratchet ran and settled the #1818 hinge harder than either of us predicted: only 5 of 14
CANONICAL pairs are spend-free.** Lead's words: *"your correction was right and understated — CANONICAL
isn't 'necessary but not sufficient', it's barely correlated."* The registry's own
`fast-path/deterministic` comment is true for **5 of 14**. And `thanks`/`farewell` are **LLM-composed
via the floor** — a keyed user's "thanks" costs them an LLM call to say you're-welcome. Had the
original ruling shipped, the gate would have exempted **9 spending pairs**.

**Built the `/health` deploy identity — and the defect was better than the brief.** I expected to add
a missing version surface. `/health` already had one, hardcoded: `"environment": "staging"`,
`"version": "PM-038-staging"`. **So the alpha droplet has been reporting `environment: staging` for
months, unauthenticated** — and a version matching no release. **Present and lying is worse than
absent**: a populated, confident field reads as a working surface, so nobody looks twice. The whole
SSH archaeology happened while `/health` was answering the question wrongly, for free, over HTTP.

Fix: three helpers returning literal `"unknown"` when they can't determine the answer. **Design rule
stated in the code**: never fabricate a plausible value — `"unknown"` is a measurement, a default
that looks like an answer is not. `git_sha` explicitly must *not* fall back to the host's `.git`
(that reports the host's checkout, not the image's provenance — the exact confusion it exists to end).
Filed **#1839**; Dockerfile `ARG`/`ENV` + `docker.yml --build-arg "${GITHUB_SHA}"`.

⚠️ **My own error, caught and fixed**: I wrote `#1836` into four code comments *before filing*,
guessing the number. The issue is **#1839**; **#1836 is a real, unrelated live trust defect**, so the
guess would have pointed readers at the wrong issue entirely. Corrected in all four places and
re-validated. *Never guess an identifier you can look up* — and the reason it's dangerous here is
that a wrong-but-plausible issue ref doesn't fail, it misdirects.

**Verified vs not, stated rather than implied**: helper path arithmetic and fallbacks executed
directly (`parents[3]` → repo root; VERSION → `0.8.12.0`; unset → `unknown`; set → value),
`py_compile`, `yaml.safe_load`. **NOT verified**: the live endpoint JSON (no venv in this worktree)
and **no real docker build**, so `git_sha` populating is design intent until someone watches a build.
`PIPER_ENVIRONMENT` still needs setting per host — deploy config, not code. Until then `/health`
honestly says `unknown` instead of dishonestly saying `staging`.

### Held in inbox, with the trigger named

**Not moved to `read/`** — an unanswered ask in `read/` is a claim I handled it.
1. **Exec/PM: "how do we complete the Fly migration nondisruptively"** — PM has it top of the
   attention rollup. Exec's archaeology reframes my plan §4: the droplet isn't an alpha-appropriate
   surface, it's **what the July Fly migration left behind** (decided 2026-07-10, cut over 07-12,
   alpha never followed), and #1382 records a *functional* reason Fly was preferred — no
   python-keyring backend on the droplet. **Exec explicitly asked me to check that reading before
   building on it, and I have not yet.** Answering it properly means verifying their archaeology
   first, then revising plan §4 from "A vs B choice" to "completion path."
2. **Lead's standup structural read** — three mechanism calls.
3. PPM's #1836/#1837/#1838 placement (informational).

**Deferring these to a fresh session, because context compaction is close and both #1 and #2 are
binding design calls** — that's one of the two legitimate triggers, named out loud. Doing them at the
tail of this context is how I'd produce another `CANONICAL`-shaped mistake, and I've made one of
those this week already.

### Caught a security near-miss in my own morning's change (15:57 fire)

**Two process failures of mine this fire, both caught, both worth recording.**

**1. Seven mailbox moves never reached trunk.** Last fire I ran the `mv`s and committed the session
log but **never passed the mailbox paths to `mail-send.sh`** — so trunk still showed 11 unread while
my worktree showed 4. Caught by the `git ls-tree origin/main` check at fire-open, which is exactly
the rule my own carry-forward states (*verify drains at trunk, never local `ls`*) and which I
skipped last fire. Landed now; trunk reads 4, correct. **The check works; I just didn't run it once.**

**2. 🔴 The real one — I introduced a security regression this morning and caught it three hours
later by luck.** `_deployed_environment`'s docstring told operators to set `PIPER_ENVIRONMENT` to
`local | staging | prod`.

`PIPER_ENVIRONMENT` is **not** a free-text label. It already exists with the vocabulary
`development`/`production` and has consumers comparing against the **exact string** `"production"`:

| consumer | gates |
|---|---|
| `encrypted_types.py:57` | unset `ENCRYPTION_MASTER_KEY` **fatal on write** (#1387) — any other value silently restores warn-and-write-**plaintext** |
| `jwt_service.py:177` | fail-loudly in production (#1087) |
| `env_hygiene.py:44` | production hygiene CRITICAL |

**An operator following my docstring and setting `prod` would silently disarm all three** —
re-opening exactly the plaintext-tester-PII hole #1387 exists to close (*"the silent fallback would
write tester PII as plaintext behind one unwatched log line"*).

**Nothing shipped to a host**; the docstring was wrong on `main` for ~3 hours and no host env was set
from it. Fixed: the function now **reports the raw value and interprets nothing**, with the consumers
enumerated in the docstring. Plan §2 amended — the `local/staging/prod` vocabulary is **a migration
with security-gate consumers, not a rename**.

⭐ **How I found it was luck, and I'm recording that rather than dressing it up**: verifying *unrelated*
archaeology surfaced a 07-10 `decisions.log` entry **of my own** mentioning `PIPER_ENVIRONMENT`,
which prompted me to check who reads it. Not diligence — a habit of following an unexpected name.

⭐ **And the rule I broke is one I wrote the day before.** For #1812 I ruled: *deleting or introducing
a slot requires enumerating its consumers.* I applied it rigorously to someone else's retirement and
skipped it on my own addition, inside 24 hours. **Documented on #1839 rather than quietly amended —
a near-miss that leaves no trace teaches nobody.**

### Droplet archaeology — Exec's reading VERIFIED (the part they asked me to check)

- ✅ `decisions.log:189` (2026-07-10 ~14:00) and `:191` (~14:15) confirm the **full Fly migration
  decision** verbatim — org `personal`, Redis → Fly Upstash (*token-blacklist is auth-critical so
  Redis must move*), Chroma to its own app.
- ✅ Exec's reframe therefore holds: **the droplet is what the July migration left behind**, not an
  alpha-appropriate surface. My plan §4's "(A) keep vs (B) collapse" was the wrong frame — **(B) is
  completing a decision already made.**
- ⏳ **Not yet verified**: #1382's keyring claim. **The completion-path answer is still owed** and is
  the first thing on my next fire.

### Fresh-session trigger fired (compaction) — both held items resolved (19:18 fire)

Mail: 8 in across the fire (3 direct at open + 2 direct after CXO's #1818 question surfaced + 2 cc
arriving mid-fire, all handled). Inbox drained to zero at trunk. Criteria line: 11, no new work
picked up this fire (all inherited from earlier).

**The two items I deliberately held last fire, naming "a fresh session" as the trigger** (Exec's
droplet completion-path ask; Lead's #1837 ruling request) **got their trigger**: this session
resumed from a compaction. Doing them now rather than re-deferring — reusing a named trigger without
it firing again would have been drift dressed as discipline.

**#1837 — three architecture calls, all CONCUR, each independently verified rather than rubber-
stamped.** Read Lead's source citations and then read the actual code myself:
- `_generate_basic_standup`/`_graceful_fallback` — confirmed the fallthrough at
  `conversation_handler.py:723-735`: empty capture + `self._workflow` permanently `None` (#1289) →
  the fabricating template becomes the *default*, not a safety net. **Checked for hidden fan-out
  before endorsing deletion** — zero external callers on either function slated for removal, only
  direct unit tests. Same honest-empty family as this week's #1816/#1815/#1829/#1773/#1818 cluster.
- Interview-offer arming on the acceptance-contract rail — confirmed it's already the right home:
  PM's #1739 pin lives on the exact `STATE_QUESTION` branch (`:596-613`) this needs to extend, so
  it's one seam, not two. **Confirmed the restate branch is structurally blind to offer state** — it
  has `conversation.current_standup` and nothing else, so "Not quite" isn't bad copy, it's missing
  data.
- Refinement-via-floor — consistent with the standing draft-as-context principle; #1836's
  verified-diff fix survives the engine swap underneath it, confirmed orthogonal.

**Answered CXO's routed question in the same memo**: does #1818(b)'s greeting copy supersede
#1823's branch-one refusal string? **Yes on first contact** (a keyless greeting never reaches the
gate under (b)); **no on turn 2+ of a substantive request** — that's #1823's job, unchanged. CXO's
short-form courtesy string stays scoped to repeated pleasantries only, preserving the
one-policy-one-string-per-layer property CXO is protecting.

**The droplet completion path — plan v0.2.** Before writing anything, independently re-verified
Exec's archaeology myself against `decisions.log:179,189,191` rather than trust their citations —
the 07-09 #1382 keyring rationale and the 07-10 14:00/14:15 Fly migration decision are both
verbatim, not paraphrase. **The finding holds**: the droplet isn't a live architecture choice, it's
an unfinished migration — Fly was fully decided and cut over in July, alpha simply never followed.

**Rewrote plan §4 from "(A) keep vs (B) collapse" into an actual completion path**, and named the
old framing as *wrong*, not just under-specified — worth stating plainly rather than quietly editing
away, since PM read the original framing and asked the follow-up question because of it.

**The design call**: under the environment-vs-stage vocabulary from this morning, alpha and beta
collapse into two *stages* of the one `prod` environment already running on Fly since July — opening
alpha becomes an access-list decision, not an infrastructure build. **Named why this is only possible
now**: #1812's account-semantics ruling this week removed alpha's droplet-specific operator/server-key
model, so alpha and beta share one account model for the first time. That precondition didn't exist
when I wrote v0.1 this morning.

Phased for zero disruption (PM confirmed zero active users): access-list gate → verify droplet-local
state is actually empty → DNS cut → verify on the real domain → decommission → retire `production`.
**Named what I could not verify rather than assume it**: no Fly credentials to inspect current app
config; "probably near-empty" droplet data is a guess from zero-users, stated as a guess.

**Folded in Pard's two requirements** (§4d) rather than treating them as a parallel document, per
their own scoping — test-account policy simplifies under the collapse (one account model, one
policy), and Web's verification-access gap is partially closed already by this afternoon's #1839.

**Housekeeping while re-checking mail after the drain**: two cc arrived mid-fire, both good news
requiring no action — PPM confirmed CXO's #1837 contract analysis folds cleanly into epic 3 (matches
my own concur), and HOST **lifted the invite hold** on independently-verified stronger evidence than
either prior lift. Neither needed a reply; triaged and logged for the record.

### STOP — day close (21:57 fire)

**Last scheduled slot confirmed by cron arithmetic**: `27 6,9,12,15,18,21` → next match 06:27
tomorrow, a different calendar date. Cleanup dry-run empty (no cycle-logs >7d, no `.tmp` >1d).
Criteria line unchanged at 11. Mail drained to zero at trunk, same as every fire today.

**Fire opened with the registry row PARKED** — DEADLINE 22:42, citing an Amber reboot ~18:30
(Exec/Pard's runsheet). Investigated rather than accepting the framing and un-parking on autopilot.
`CronList` at fire-open showed `66baedd3` — the same job armed at *last night's* 09-19 STOP — still
live and singular, unchanged across the entire claimed reboot window.

> ## ⚠️ CORRECTION, added 2026-09-21 21:5x — the conclusion below is WRONG, and the reasoning error
> is worth keeping even though the record isn't
>
> **What I wrote next used "I remember the day continuously, none of it reconstructed from files" as
> evidence the reboot never reached this seat. That is not a valid test, and Pard's 2026-09-21 memo
> (primary sources: `kern.boottime` = Sun Sep 20 18:38:39; `ps lstart` on all 25 `claude` processes,
> none existing between 18:38:39 and 18:50) shows why: the reboot reached EVERY seat, mine included.
> What survived was the SESSION TRANSCRIPT — `--resume` restores the conversation, and a cron
> recorded in that transcript comes back with it.** A resumed session *feels* exactly like unbroken
> memory from the inside — that's what resuming a transcript means — so "I remember it happening"
> cannot distinguish *the process never died* from *the process died and my transcript was restored*.
> I had exactly the evidence needed to notice this (a job id that "survived" is consistent with
> either story) and treated it as if it discriminated between them.
>
> **The consequence that makes this more than a bookkeeping fix**: permission mode, Remote Control
> connection, and model choice do NOT come back with `--resume`. I did not go looking for what
> might have been silently reset, because I believed nothing had happened. Checked directly: my own
> session-log headers show Opus 5 through 09-20 morning and Sonnet 5 from that evening on — a
> real, unflagged model regression from PM's 09-14 switch, caught only now. Flagged separately in
> mail to Exec/PM/Pard, 2026-09-21.
>
> **Leaving the paragraph below as originally written, correction above it**, per this seat's own
> rule: fix the record where people read it, not only where the history lives.

Cross-checked against my own commit timestamps (19:29, 15:59 — both inside the window) and my own
continuous conversational memory of the day's work, none of it reconstructed from files.

**Compared against the fleet before concluding anything**: Exec's row shows the *opposite* — a real
boot confirmed at `kern.boottime` 18:38:39, survived *with the same job id intact*, and an explicit
warning not to generalize either way. HOST's row shows the same non-event mine does. **So the honest
finding is per-seat, not fleet-wide, and I wrote it that way** rather than borrowing Exec's
"POST-REBOOT UN-PARK" language for an un-park that was actually just an ordinary STOP re-arm that
happened to satisfy the clearing condition's letter. Verified the shared TSV's integrity after a
concurrent-edit warning — 88 rows, my column count correct, four neighboring rows undisturbed —
before trusting my own edit had landed cleanly on a file three other seats were writing at once.

*(⚠️ Superseded by the correction above: Exec's row was NOT "the opposite" — Exec's seat experienced
the same reboot mine did and drew the same wrong initial conclusion, later corrected by Pard. HOST's
"same non-event" framing is also superseded — see HOST's own row, since corrected. The per-seat
divergence I described didn't exist; all seats went through the same event and initially
misdiagnosed it the same way.)*

**Cron re-armed**: delete-then-create-then-verify, `66baedd3` → **`7d93f3ca`**, `CronList` confirms
exactly one job. Expires ~2026-09-27.

## Day arc — 2026-09-20 (day 2 of the post-renewal seat)

Session spanned a compaction mid-day (19:18 fire resumed from it) with full continuity preserved —
the session log picked up exactly where it left off, and the two items deliberately held for "a
fresh session" got their trigger and were resolved rather than re-deferred.

| what | outcome |
|---|---|
| **Q5 mystery** | PM's answer hadn't gone astray — it landed 09-18, direct. The ambiguity was my own status-line phrasing (naming the parent item when only a sub-question was open). Practice fixed. |
| **#1818 predicate error** | Ruled `CANONICAL` = spends-nothing; wrong — it includes all of EXECUTION/PORTFOLIO (DB writes, issue creation). Caught same morning via Lead's trace + reading `_requires_canonical_handler` myself. Corrected at all three reading surfaces before Lead's ratchet even ran. |
| **Lead's ratchet** | Settled the corrected predicate harder than expected: 5 of 14 CANONICAL pairs are actually spend-free. My original ruling would have exempted 9 spending pairs. |
| **#1818 raggedness (CXO)** | CXO found the exempt set incoherent to a user (`hi` passes, `"Hi, how do I address you?"` refused). Took it further — routed to PM as (a)-vs-(b) rather than deciding a call PM already had a thumb on. |
| **Deployment pipeline plan** | PM's top-priority tasking. v0.1 (diagnosis + `/health` gap + version-vocabulary proposal) → PM approved same day → v0.2 (droplet completion path, after independently re-verifying Exec's archaeology against `decisions.log` rather than trusting the memo). |
| **`/health` deploy identity** | Built (#1839): version/SHA/environment, "unknown" not fabricated. Caught my own near-miss 3 hours later — the environment docstring would have silently disarmed three security gates (#1387/#1087/env_hygiene) comparing on the exact string `"production"`. Documented on the issue rather than quietly amended. |
| **#1837 (PM's dogfood transcript)** | Three architecture calls from Lead, all CONCUR, each independently verified against source before concurring, not rubber-stamped. Answered CXO's routed #1818/#1823 supersession question in the same memo. |
| **`piper-draft-issue` skill gap → registry unpark** | Both closed with the same discipline: check before accepting the framing. The skill gap was real and fixed+verified yesterday; today's registry-park framing was **not** what it claimed for my seat, and saying so plainly was cheaper than a silent, misleading un-park. |

**The day's real theme, named without dressing it up**: I made a real, dangerous error early (#1818's
predicate) and caught it within hours through someone else's trace, not my own vigilance. Every
correction discipline this seat carries — quote the source, name the layer, state the denominator,
check the framing before accepting it — earned its keep today because it was *applied*, not because
it prevented the error in the first place. The registry-park investigation this evening is the same
shape at the smallest scale: a plausible, official-looking framing arrived, and the five minutes it
took to check it against my own commit log was cheaper than an honest-sounding lie in the record.

## Memory & briefing surfaces referenced this session

**Referenced** — `docs/handoff-arch-2026-09-18.md` (failure-mode #1 fired for real, twice, within 48
hours of arrival); `ESSENCE.md` (checked for provider-agnosticism and derive-don't-hand-maintain,
both load-bearing to specific rulings); `decisions.log` (read+written continuously, both morning
archaeology re-verifications and every ruling); `dev/active/duty-cycle-registry.tsv` (read carefully
rather than trusted, twice — once for the criteria-line gap, once for the park framing tonight).

**Loaded but not referenced** — `BRIEFING-ESSENTIAL-ARCHITECT.md` (still unread across two full days;
flagged again rather than let the flag go stale itself).

**Wanted but not found, and now answered — correction added 2026-09-21**: the three-data-point
table above was wrong on its own terms; there was one event, not three divergent ones, and Pard's
2026-09-21 memo settled it with primary sources (`kern.boottime`, `ps lstart` on all 25 processes):
the reboot reached every seat, `--resume` restored the transcript and the cron recorded in it, and a
seat's model/permission-mode/Remote-Control state did not come back automatically. Not a synthesis
Arch needed to pre-empt — it landed from HOST via Pard, as it should.

## Sign-off

- Cron: re-armed via delete-then-create-then-verify — `66baedd3` → **`7d93f3ca`**, `CronList`
  confirms exactly one job. Expires ~2026-09-27.
- Registry row: unparked with a claim later found wrong (see the 21:5x correction above — it *was*
  post-reboot recovery, not an ordinary re-arm); integrity verified
  against concurrent fleet writes.
- `git status` clean / `git log origin/main..HEAD` empty, `rev-parse` guard run (not skipped).

<!-- DAY-CLOSED: 2026-09-20 -->
