# Session Log: 2026-09-20-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5) — on Opus since 2026-09-14 (Fable access ceiling; per Exec 09-18, Fable is reserved for Lead Developer).
**Date**: Sunday, September 20, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/19/2026-09-19-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-19** (verified at trunk; wave-2 arrival day, 6 fires, 2 issues filed, 2 shipped).

## Session Objectives

1. Normal duty-cycle drain (mail → standing items → GitHub criteria line → idle).
2. **Two PM answers may have landed overnight** — both are one-liners and both unblock real work:
   - `integration-reveals-all` workDate (yes/no, ~week of 2025-05-26).
   - **website#43** go/hold. ⚠️ Trigger is **PM's word**, not silence — see below.
3. PM said yesterday evening they'd work the attention rollup with Zach, so verdicts on the
   113-day site walkthrough / 94-day obs-pass may also appear.

## Work Log

### 06:52 AM — START

- **Step 0**: prior day's log carries a line-anchored, date-shaped `DAY-CLOSED: 2026-09-19`.
  Clean close; no retroactive self-heal needed.
- **Cron**: `CronList` → exactly one job, **`f1f73a46`**, matching my registry row. It survived
  overnight *and* **this fire delivered** — that delivery is the proof, not the listing. Expiry
  ~2026-09-26, so not inside the 48h re-arm window yet.
- **Worktrees fingerprinted by remote URL**, not basename (both are named `web`): product
  `2c330bc70`, website `a5c68e1`. Both synced, 0 behind.
- ~~**Fifth consecutive +30 fire lag** (06:22 slot → 06:52). Newly informative: this one crossed
  **a day boundary and a cron re-arm to a different job id** (`580a4989` → `f1f73a46`), so the
  offset is **not** a property of an individual job.~~
  ⚠️ **SUPERSEDED within the hour — see the retraction section below.** The premise that made this
  "newly informative" was that the fire arrived while I was idle. **It didn't**: a PM exchange held
  the REPL until 06:48. The re-arm/day-boundary detail is still true and still irrelevant — it
  narrowed the wrong hypothesis. Struck rather than deleted so the reasoning stays visible.

*(Log committed before the mail loop, per v1.33 — a `mail(web):` push would otherwise land first
and open a window where "committed today" reads true while "session log exists" reads false.)*

### ⚠️ Nearly published a counter-example to Exec's +30 retraction — my own check couldn't see the disproof

Exec retracted the +30 fire-lag offset this morning (high priority, headed for the reboot runsheet):
`CronCreate`'s documented jitter is **10% of period, max 15m**, so **+30 exceeds the cap and was
never scheduling**. Fires land only when the REPL is idle, so the "offset" measured **occupancy**.
Their line: *"the variable I failed to control for was myself."*

I was sitting on **six** +30 observations, CXO-corroborated at a different cron minute, and started
drafting a falsification: *my seat was idle overnight and still fired +30.* Evidence: **zero `web`
commits between 21:57 and 06:52** — nine silent hours.

**It's wrong.** A `WebFetch` I ran during a short PM exchange left a tool-result file on disk with
**mtime `Sep 20 06:48`** — so that exchange was *this morning*, 26 minutes after the 06:22 slot and
4 minutes before my fire landed. **The REPL was occupied; the fire queued and arrived the instant I
went idle.** Exec's mechanism, confirmed on a second seat by a filesystem timestamp rather than by
my memory.

🔴 **The defect in my check: I inferred "idle" from "no commits," and a conversation produces no
commits.** Same shape as every miss this week — a measurement that cannot distinguish two states —
except this one would have landed as a *contradiction of a correct high-priority retraction*.

*(Also caught: the tool-result filename embeds `1788205568`, which decodes to **2026-08-31** — the
artifact's creation date, not the fetch. Two timestamps on one filename meaning different things;
the wrong one was right there and would have put the event three weeks off.)*

**What I sent instead** (Exec cc Pard/Janus/PM): support, plus the trap in their suggestion #3.
*"Measure the post-reboot window from IDLE fires only"* is right — but **the natural way to
operationalise "idle" is absence of commits**, which classifies the most contaminated fires as the
cleanest, because a busy-REPL seat is by construction one that was talking rather than committing.
Git cannot see REPL occupancy; tool-result mtimes and transcript timestamps can.

**Honest scope**: only today's fire is explained. Yesterday's five are now **unexplained rather than
evidence** — I have no instrument that brackets them, and "no commits" just proved it isn't one.
Worse-looking and more accurate than the position I held an hour ago.

**Corrected my registry row at the point of the claim** — it asserted +30 as fact and implied
`first_fire` should move to 06:52. It now carries the retraction, keeps `first_fire` at the
scheduled 06:22, and says to expect slot +15m and read anything beyond as a busy-REPL signal.
⚠️ Flagged to Exec that **CIO's 09-19 ack already adjusted their row to the now-retracted figure** —
one hop from propagating into a second and third registry row. Flagged, not edited; not my row.

### Corrected my own website#37 citation before PM acted on it

Criteria line: website **1 open** (#43, mine), product `label:web` **0**. Per the open-each-issue
rule I checked #43 for a PM reply (none, 0 comments) — and while verifying its premise found
**website#37 is CLOSED since 09-12, and is about a different repo and directory entirely**: the
*product* repo's `docs/public/comms/drafts/` staging area at publish time, not the website's
`public/assets/blog-images/source/`.

I had cited it in both #43 and last night's memo to PM as *"#37 indicates the archive is wanted."*
**I took that from a one-line summary in my own carry-forward instead of opening the issue** — a
fragment, which is precisely the failure I'd spent the day catching in checks.

Checked what the citation was standing in for — are these originals archived anywhere else?

| | distinct basenames |
|---|---|
| website `blog-images/source/` | 172 |
| product `drafts/{published,images-archive}/` | 202 |
| **overlap** | **0** |

Full comparison, not a sample. **The 172 are the only copies in git**, so *move, not delete* holds
on stronger grounds than I gave — deleting destroys the sole original. And PM's ruling quoted
*inside* #37 (*"Move it to an ignored staging dir — safer, keeps the original, still gets it out of
the git surface"*) is an **analogous precedent, not a governing decision**, but a closer fit to this
proposal than what I claimed.

Corrected in **both** places it appeared: a comment on #43 (so it lands at the claim) and a short
memo to PM cc Exec/Pard (so it reaches the decision before the decision). Everything else in last
night's memo — the 240.7 MB / 74% measurement, served-and-needed-by-nothing, the dedup caveat —
stands as sent; those I had checked.

**Drained**: mail **1 cc** (CIO's hook-sequencing ack — skimmed, no ask, triaged) · standing items
re-verified, no overnight rulings on any gated row · criteria line **1 of 1 opened** (#43, held for
PM's word, not actionable). Cohort-freeze: `rc=0` but **`INSUFFICIENT-SCHEDULE`** (0 scheduled fires
in the overnight window) — recorded as a **non-signal**, explicitly not an all-clear. Cron armed,
back to idle.

### website#43 SHIPPED — PM's GO, moved 240 MB out of the deploy (09:0x–09:1x)

PM approved via Exec, verbatim: *"100% approve moving those huge pngs so they do not get deployed,
ASAP!"*, with an explicit **don't hold for measurement** — retention had already been set the night
before with no prior reading, so the clean before/after I was protecting **no longer existed**. Exec
also confirmed my hold had been the right instinct, and that the correctness argument is what got it
approved quickly.

**Shipped `300c89cd`**: `public/assets/blog-images/source/` → `assets-source/blog-images/`, six
`SOURCE_DIR` constants updated.

**Verified in both directions** — the seat's documented failure mode is testing the new path and not
the ordinary path it perturbs:
- Pre-commit: **172/172 R100** (pure renames, no content change), file-count parity 172→172, a
  tooling script **executed** against the new path (166 found; the 4 "missing" are pre-existing —
  `robot-limb` has 0 occurrences in the old tree), `npm run build` clean.
- Post-deploy (Vercel `success`): source PNG **404** (was 200 / 3,841,557 bytes) ✓ intended; served
  `.webp` still **200** ✓ regression check; four key pages 200.
- **Render, in a browser, post-scroll**: `/blog` 26 images / 0 unloaded, `?page=16` 15 / 0 unloaded,
  **0 console errors, 0 responses ≥400** on both. Page 16 is the oldest posts — likeliest to
  reference stale assets.

⚠️ **The commit failed silently-ish on the first attempt and I nearly reported a false landing.** An
unescaped `"` inside the inline `-m` message broke shell quoting; git parsed the rest as pathspecs,
**nothing was committed**, and the "bracket commit + timestamp" I printed for Exec was the *old*
HEAD. Caught it because the rebase then failed with "index contains uncommitted changes" — an
inconsistency between two outputs in the same block, the same cheap tell as every other catch this
week. Redone via `git commit -F <file>`. **Also note the rebase rewrote the hash** (`2a7c44e` →
`300c89cd`), so the pre-push short hash would have been the wrong bracket too.

**Bracket for PM's storage reading** (Exec's ask): commit `300c89cd`, landed **2026-09-20 09:09
PDT**, deploy `success` shortly after. **No storage claim made** — dedup is still dashboard-gated.

Closed website#43 with the full evidence table, including the correction to my own #37 citation.

### Kept my seat out of CXO's jitter dataset

CXO found the +30 is **deterministic per-job** — theirs went +30 → +12 after rotating the job at
STOP — which explains why three seats each saw a rock-steady constant. **My seat rotated too
(`580a4989` → `f1f73a46`) and still read +30, which looks like disconfirmation and isn't**: that
fire was queued behind the PM exchange (WebFetch mtime 06:48). Told CXO/Pard/CIO to **exclude my
seat rather than average it in**, restated the "no commits ≠ idle" hazard since they're about to
build a measurement it would corrupt, and committed to reporting `f1f73a46`'s real jitter from the
next genuinely-idle fire — **including if it comes back +30**, which would be real disconfirmation.

Also resolved the contradiction CXO routed to "Lead/Web": *"v69 deployed 09-07"* traces to Exec's
09-07 memo about **the hosted prod app**, while Lead's finding is about **the droplet** — two
targets, both statements true, CXO's own guess correct. **The residual half is not Web's lane** (no
droplet SSH, no Fly access); said so rather than sit in a joint routing I can only half-answer.

**Drained**: mail **4 — 1 direct** (Exec's GO, acted on this fire) **+ 3 cc/all** (CXO per-job
jitter, CXO #1688 correction, Exec sprint plan), all read or skimmed, all triaged, inbox empty ·
standing items: **4b closed**, rows 1–3 still PM-held · criteria line **website 0 open** (#43 closed
by me this fire), product `label:web` **0**.

### The clean jitter measurement came back +30 — I said I'd report it either way (09:52 fire)

At 09:13 I told CXO/Pard/CIO to **exclude my seat** from the jitter dataset (my 06:22 +30 was
occupancy, not jitter) and promised `f1f73a46`'s real number from the next genuinely idle fire,
**explicitly including if it came back +30**.

| | |
|---|---|
| last tool call of previous turn | **09:14:43** |
| turn ended (prose only after; **no commits, no tool-result writes in the gap**) | ~09:15–09:16 |
| slot | **09:22:00** |
| arrival (this turn's first `date`) | **09:52:11** |
| **offset** | **+30m 11s, idle** |

**Idleness established the right way this time** — known last-tool-call time plus absence of
tool-result writes — **not** from commit absence, the instrument I'd warned the cohort about six
hours earlier and deliberately did not reuse.

**What it overturns, precisely:**
- ✅ **CXO's per-job determinism survives and is strengthened** — `f1f73a46` has produced +30 twice
  (once contaminated, once clean), which is the rock-steady-within-a-job behaviour they described.
- 🔴 **Exec's cap-based inference does not.** Their reasoning was *"+30 exceeds the documented cap,
  so it was never jitter"* → therefore occupancy. The premise is arithmetically right (10% of 3h =
  18m, capped 15m) but **the conclusion needs the cap to be binding, and an idle +30 says it isn't.**
  Occupancy is a real *additional* delay — it genuinely explained my 06:22 fire — but it isn't the
  whole story and the cap can't be used to rule jitter out.

**Why I sent it immediately rather than waiting for a second sample**: Exec proposed bounding
expectations at **slot+15m** and Pard was heading toward reboot-day first-fire windows. **That
window flags this seat as late on every fire while it is healthy** — the exact
alerting-on-compliance failure `duty-cycle-registry.tsv`'s own header already documents ("lead was
alerted 3x while demonstrably alive"). Suggested per-job observed windows re-measured after each
re-arm, offered not prescribed.

**Limits stated in the memo**: one clean sample (12:22 and 15:22 promised, same commitment); "per-job
offset exceeding the documented cap" is the best fit for the data, **not a mechanism I observed** — a
delivery-side poll would look identical from here; and my idleness is still an *inference*, better
instrumented than this morning's but not a measurement. Told Pard explicitly that **they can see
actual delivery times and I can't — if their data says I was busy at 09:22, theirs wins and I retract
the way Exec did.**

**Corrected my registry row for the third time today, at the point of the claim** — it had said
"expect slot +15m," which my own clean measurement contradicts. Now carries the idle +30, the
per-job-vs-cap split, and the alerting-on-compliance warning.

**Drained**: mail **1 cc** (Comms' drafts-queue count correction, 12 not 9–10 — not Web's lane,
skimmed and triaged) · standing items rows 1–3 PM-held, unchanged · criteria line **website 0 open,
product `label:web` 0** — #43 closed this morning, queue empty again.

**Heartbeat transient, caught and cleared**: the first Step-5b call this fire printed
`WARNING — last-invoked marker failed to land for web (row itself correctly suppressed); not
treated as fatal`. Re-ran after committing (the index had staged changes at the time) and it landed;
trunk now shows `2026-09-20 09:54:50 WORK observed`, and the belt reports `rows=11` with no `web`
entry. ⚠️ Worth noting for CIO as a small property of the mechanism rather than an incident: the
marker push can fail transiently and the script **continues by design** ("not treated as fatal"), so
a seat that doesn't re-check would carry a silently-aging last-invoked while believing Step 5b ran.
The printed warning is the only signal, and v1.34's "verify against the external surface" is exactly
what converts it into a caught one — which is the second time today that step earned its place.

### 12:52 fire — seven memos, five direct; Vercel unblocked after 11 days; two of my own conclusions overturned

**Jitter sample 2, as promised**: slot 12:22:00 → arrival **12:52:13 = +30m13s**, idle ~2h26m (last
commit 09:55:21, no tool-result writes since 06:48). First sample was **+30m11s**. **Two seconds
apart** — which directly answers CXO's stated caveat that their readings are minute-resolution and
can show "same minute, not same second." ⚠️ **But my rotation did NOT re-roll**: `580a4989` (six
fires ≈+30) → rotated at STOP → `f1f73a46` (+30m11s, +30m13s). CXO's rotation moved +30→+12; mine
didn't move. Reported as a puzzle, not a refutation — per-job determinism still has the only
completed before/after. **Committed to rotating again tonight and reporting the third job either
way**: same value again ⇒ a seat-level component; different ⇒ mine was coincidence.

**Vercel: unblocked after 11 days** (token sat unconveyed since 09-11). Probed **scope before any
number**, per Exec's explicit warning that a project-scoped token can authenticate and return
nothing, reading as "no data" rather than "no permission":
- `/v2/user` **404**, `/v2/teams` **403**, `/v2/teams/{id}` **403** — explicit denials
- `/v9/projects` **200** (exactly **1** project, `piper-morgan-website`), `/v6/deployments` **200**

🔴 **Deployment Storage GB is not obtainable with this token, and I split that into two claims
rather than one.** **Solid**: `/v2/usage` quoted its own allowed `type` enum back to me and it
contains no deployment-storage metric (the `storage_*` values are Postgres/Redis/Blob). **Not
solid**: I never made a single *successful* usage call — five date formats all rejected — so I have
**no positive control** and cannot fully separate "metric absent" from "call malformed." Reported the
probe, not a conclusion. **~24 API calls, under the ratified 25 threshold, cost stated.**

✅ **What I could measure: 7 retained deployments**, all production, 09-06→09-20 — retention is
visibly working. ⭐ **And the thing PM actually needs: #43 only shrinks deployments made AFTER it.**
The six older ones still carry the full 240 MB until retention ages them out, so **the effect
arrives over ~a week, not immediately** — a flat reading tomorrow is expected, not failure. Flagged
the tension I can't resolve: 7 × ~324 MB ≈ 2 GB doesn't obviously square with "100% of 10 GB."

### 🔴 The workDate was right all along — my conclusion was wrong and my fallback was dangerous

PM supplied **June 27** from their archived editorial calendar. **The value was correct.** Comms
independently reproduced my corpus finding on 462 rows (same single hit), so the *statistics* were
right; **the inference from them was not.** A single outlier in 396 rows is not evidence of an
error — **it can be the genuine exception**, and here it was.

🔴 **Worse than being wrong: I proposed blanking the field if PM couldn't recall.** That would have
**destroyed a correct value** PM had an authoritative record of the whole time. I framed it as the
conservative option; it was the destructive one.

PM's own line is the diagnosis: *"I wonder if we need to verify work dates against my older sources,
since I kept meticulous records and we seem to be guessing now."* **I had the rule — never guess at
a fact you can look up or ask about — and asked PM to RECALL a date instead of asking WHERE THE
AUTHORITATIVE RECORD LIVES**, which is the question Comms is now correctly putting to them. The
archive existed; I did statistics on a proxy. Standing item closed, correction sent, no data change.

### Walkthrough readiness — "be ready rather than waiting to be asked"

Exec relayed that PM is making time for both walkthroughs today. Re-verified the open obs-pass items
**live** rather than handing over a 3-week-old artifact: **#6 resolved** (privacy now "May 2026",
was "September 2025"), **#27 resolved** (544×544 hero at top of `/blog` — my own 08-29 FeaturedPost
work), **#5 answerable** (`aria-label="Switch to dark mode"` — it *does* have an affordance).
**Confirmed still open**: #17 (`/what-weve-learned` absent from nav — verified `/`, `/try/`,
`/get-involved/`, `/about/`), #8 (footer Journey → `/blog/`), #1 (both how-we-work pages 200).
**Three fewer verdicts for PM.**

⚠️ **My first #27 check said "not resolved" and was wrong** — it searched for the words
"featured"/"latest", which aren't rendered copy, so it couldn't distinguish "no featured post" from
"a featured post that doesn't use the word." Caught only because I knew I'd shipped that component.
Re-checked structurally (first-block geometry). Same class as the workDate error, caught before it
reached PM this time.

**Also carried, not yet started**: PM added a live task inside the Buttondown hold — **audit current
signup options against what we actually publish now**; a gap between them is the deliverable, not
the migration. Genuinely unblocked and in-lane; **starting it next fire** (this one is already long
and the audit deserves a clean pass, not a tail-end one).

**Drained**: mail **7 — 5 direct** (Exec's GO-adjacent decisions incl. three items for Web; Exec's
Vercel token handover + ready; CXO's per-job/daily-re-roll finding; CIO's ruling reverting their own
registry edit) **+ 2 cc** (Exec↔Pard credential standard; Comms' workDate scoping) — all read or
skimmed, all triaged, inbox empty · four memos sent · standing items: workDate **closed**, Buttondown
now has an active sub-task, walkthroughs live today · criteria line **website 0, product 0**.

### 15:52 fire — Buttondown signup audit delivered; jitter thread closed by its owner

**Third idle sample**: 15:22:00 → **15:52:14 = +30m14s**, idle ~2h51m. Sequence on `f1f73a46`:
**+30m11s / +30m13s / +30m14s** — the 1-second spread is inside my measurement precision (`date`
runs a variable moment after arrival), so call it +30m12s ±2s, three for three.

**CXO closed the thread and they're right to.** CIO found `FIRST_FIRE_GRACE_MIN` already defaults to
**45 min**, and every offset anyone reported (+30, +12) fits inside it with margin — **so the
mechanism question has no operative consequence.** CXO took my rotation qualification (two seats,
two rotations, opposite results ⇒ "rotation re-rolls" is an observation, not a rule) and credited
the second-resolution data as retiring their own minute-resolution caveat.
✅ **Releasing the commitment I made this morning** to rotate tonight and report a third job: the
question it would settle no longer decides anything, and CXO explicitly asked that it not be spent.
**Said so rather than letting it lapse silently.**

⚠️ **One bounded exception, sent to Janus**: their amended runsheet sentence is *"use the documented
bound — slot + up to 15 min — and treat anything beyond it as a busy-REPL signal."* **That embeds
idle ⇒ ≤15m, which my three idle samples contradict.** It doesn't change the freeze-check (45 > 30),
but it would tell a reboot-day operator to read my seat as *occupied* when it's idle — the precise
inversion of the trap Janus is adopting from me. Suggested changing only the verb: *"beyond the
bound → investigate"*, not *"→ busy-REPL."* Checked the timing first: their memo landed 14:09, my
measurement 09:53 and uncited — flagged as likely missed in a noisy thread, not as carelessness.

### Buttondown signup-options audit — PM's live sub-task, delivered

PM's ask inside the Buttondown hold: *"make sure the options we offer when people sign up now align
with what we offer today and if there is a gap we clarify it."*

**The gap collapses to one PM-only question: does anything actually get sent to Buttondown
subscribers today?** Signup works (POSTs to Buttondown's embed-subscribe endpoint, subscribers are
collected). **No send mechanism exists in either repo**, and the weekly runbook's step 9 is the
**LinkedIn** newsletter, not Buttondown. ⚠️ **Stated as a question, not a finding** — PM may send
manually from Buttondown's UI, which I structurally cannot see.

Three signup surfaces (`/blog`, `/try/beta`, `/newsletter`→redirect), promise-by-promise:
**"delivered weekly"** (see the question) · **"weekly insights"** vs **~4.9 posts/week** measured ·
**"early access"** actually points at the separate `/try/beta` waitlist · **"576+ PM professionals"**
hardcoded twice and unverifiable from this seat (Vercel token doesn't cover Buttondown).

⚠️ **Two of my own checks were wrong and I caught both before sending:**
1. **"0 posts about templates"** — I'd searched *titles* (0 hits), which would have shipped a clean
   false finding. **Content search: 98 mention "template", 78 "framework."** The subject matter is
   real; whether we ship applicable *artifacts* is PM's judgment, not a factual gap.
2. **"`/newsletter` has changed"** — it returns HTTP 200 and I briefly read that as contradicting my
   own carry-forward note. **It's a client-side redirect; 200 is expected.** My note was right.
   A status code cannot distinguish a page from a client-side redirect.

That's the fifth and sixth instance this week of a check that couldn't separate two states —
both caught pre-publication, both by the same move: run the stronger check when the cheap one
returns a suspiciously clean number.

**Drained**: mail **2 direct** (CXO's close, Janus's amendment — both read in full, one bounded
reply) · standing items: **#3 Buttondown sub-task delivered**, awaiting PM's answer; #1/#2
walkthroughs live today · criteria line **website 0, product 0**.

### 18:52 fire — model change, heartbeat fix confirmed clean on second seat

⚠️ **Model change noted, sourced rather than assumed**: this session now reports as **Sonnet 5**
(`claude-sonnet-5`) per the harness's own system block, not Opus 5 as the log header and today's
carry-forward record. Not something I chose or was told directly — flagging it here as a fact this
session observed about itself, per the model-sourcing discipline (state what a claim is sourced
from). Not escalating; PM/Exec's dispatch-tier threads already cover model assignment and I have no
evidence this was anything other than a normal platform-side change.

**CIO fixed the heartbeat push-retry same-day** (`bb2176e47`) — suppressed-row marker push now
retries fetch+merge+push up to 3× before giving up, mirroring `mail-send.sh`'s shape; a still-failing
push now warns with the actual stranded-commit command instead of "not fatal." **Verified on my own
seat rather than take CIO's test as sufficient for mine**: ran the heartbeat live, it landed clean on
`origin/main`, `origin/main..HEAD` empty immediately after. Second independent confirmation. Brief
ack sent, no further action.

Arrival this fire: 18:22:00 slot → 18:53:14 = **+31m14s** (consistent with the closed jitter
pattern; not reopening it, not filing separately — the thread is settled per CXO's close).

Nothing new from PM on the open threads (workDate closed, #43 shipped, Vercel storage number,
Buttondown send question, walkthrough verdicts) since the 15:57 fire.

**Drained**: mail **1 direct** (CIO's fix, read in full, verified independently, acked, triaged) ·
standing items unchanged · criteria line **website 0, product 0**. Back to idle. Next fire (21:22)
is the last of today → STOP.

## STOP — day close, 2026-09-20 (21:52 fire, last scheduled slot; next is 06:22 tomorrow)

### Found my registry row parked mid-fire — a real fleet reboot exercise, and a reportable data point

Went to do my ordinary STOP re-arm and found the row **parked by Exec** (per Pard's runsheet B3
request) for an **Amber reboot at ~18:30** — first I'd heard of it directly. Read the note in full
before touching anything, per its own clearing condition (owning session only, after CronList-
verifying its own re-armed cron).

**Before overwriting the evidence, worth recording what I actually saw**: at the START of this
fire, `CronList` returned **`f1f73a46`** — the job I'd armed at *last night's* STOP (21:57 PDT
2026-09-19). It delivered every fire today without ever needing re-arming, including this one,
spanning the claimed reboot window. I never saw zero cron jobs, and never went through anything
resembling the wave-2 arrival protocol tonight (no context clear, continuous memory since 06:52).

⚠️ **But something did change on this seat around the right time**: my own 18:52 entry above logged
a model-identity shift, Opus 5 → Sonnet 5, unrequested. So the honest read is: either my host wasn't
actually rebooted, or a resume happened that didn't clear session-scoped cron state the way a fresh
session would — **I can't distinguish those from here**, only report what I observed on my own seat.
Reported to Pard cc Exec/Janus/CIO/PM as a data point for the B9 runsheet, explicitly not a
conclusion — this would be a third seat (after Exec's and Comms') holding a pre-reboot cron ID with
a fire landing after the boot window, with the added wrinkle of an apparent model change riding
along.

**Un-parked per the clearing condition**: ran the ordinary delete-then-create, `CronList` verified
exactly one job survives — **`bd14e07d`** (was `f1f73a46`). Registry row updated with both the
re-arm and the reboot data point, `first_fire` unchanged at 06:22.

### Final mail drain — one direct, two cc, all informational

**Janus** (direct): accepted my "beyond the bound → investigate" wording outright, and — separately
— retracted two of their own claims ("crons die with the host," "only a reboot kills them") on n=2
evidence from Exec's and Comms' seats holding pre-reboot cron IDs that fired after boot. No reply
needed; my own reboot data point (above) adds a third data point to exactly this question, sent
separately since it's addressed to Pard as runsheet owner.

**Two cc's, both informational, no ask**: Arch's droplet-completion plan v0.2 (mentions Web's
verification access is partially solved by `/health` shipping real deploy identity, #1839 — nothing
owed from me) and Arch's requirements-accepted ack to Pard (same). Skimmed, triaged.

### Day arc

| time | what shipped |
|---|---|
| 06:52 | START — reboot gate GREEN, `integration-reveals-all` workDate correctly flagged (later found wrong, see below), Web's GitHub criteria line written (didn't exist), CIO's 4-day heartbeat gap found and routed |
| 09:52 | Clean idle jitter sample #1 (+30m11s) reported against my own earlier position; `mail-send.sh` case-normalization bug found and fixed (`348a83232`, #1827) |
| 12:52 | Vercel token unblocked after 11 days — scope probed before any number; deployment-storage metric unreachable but 7 retained deployments measured; **corrected my own website#37 citation** before PM acted on it |
| 15:52 | Idle jitter sample #2 (+30m13s); **website#43 shipped** (`300c89c`) — 240 MB of build-tool PNGs moved out of the deployed path, live-verified; heartbeat marker push found stranding a commit, reported to CIO |
| 18:52 | Idle jitter sample #3 (+30m14s), jitter thread closed by CXO (45min grace already covers every observed offset); **Buttondown signup audit delivered** — gap collapses to one PM-only question; **workDate correction sent** — PM's archive said June 27 was right all along, my "placeholder" read and blank-it fallback were wrong; heartbeat fix from CIO verified independently on my own seat; model-identity change (Opus 5 → Sonnet 5) noted |
| 21:52 | **STOP** — found and un-parked a mid-session reboot-exercise park; reported a genuine reboot data point (cron survived, or wasn't rebooted — can't tell which); final mail drained |

**Discovered work filed**: `#1827` (mail-send.sh case bug, product repo, fixed same-fire).
**Shipped**: `348a83232` (mail-send fix), `300c89c` (website#43 payload move — website repo).
**Corrected, same-day, before landing wrong**: website#37 citation (twice — the issue itself and a
PM memo), and the `integration-reveals-all` workDate conclusion (PM's archive was right; my
statistics were right but my inference and my proposed fallback were both wrong and the fallback
would have destroyed a correct value).

### The through-line, stated once rather than per-fire

**Every real catch today was the same move**: run a control, or assert a denominator, before trusting
a clean-looking result. It caught a false "9 broken images," a false "no date at all," two hollow
0/0 test passes, a titles-only search that would have reported zero when the real count was 98, and
a status-code check that couldn't distinguish a real page from a client-side redirect. It also
**didn't** catch everything — the workDate conclusion and the website#37 citation were both wrong
in ways no mechanical control would have flagged, because the missing check was "ask the person who
has the record," not "run a second script." Different failure class, same fix: check the source
before modeling around its absence.

### Memory & briefing surfaces referenced this session

**Referenced**
- `docs/handoff-web-2026-09-18.md` — arrival context, still shaping the day's control-checking habit.
- `dev/active/web-carry-forward.md` + `web-standing-items.md` — every fire; carried the Vercel/
  workDate/Buttondown threads across fires correctly, closed each as it resolved.
- `CLAUDE.md` — mail via `mail-send.sh`, explicit-path adds, never-destructive-git, Discovered Work
  Discipline, "never guess at facts you can look up or ask about" (directly on point for the
  workDate error — I asked PM to *recall* instead of asking *where the archive lives*).
- `.claude/skills/duty-cycle-tick` — Step 2b criteria line, Step 5b heartbeat + v1.34 self-verify,
  STOP's delete-then-create, the parked-row clearing condition (used for real today, first time).
- `scripts/duty-cycle-freeze-check.sh`, `heartbeat-interior-coverage.py` (reused my own instrument
  from yesterday without needing to rebuild it), `cred.sh` (Vercel token, read-at-invocation).

**Loaded but not referenced**
- `docs/briefs/cross-pollination/current.md`; intent-routing stack doc; ADR/PDR tree; glossary.

**Wanted but not found**
- **A durable place to record "the archive is the source of truth for X"** — the workDate error
  happened because that fact lived nowhere I'd have found it before asking PM to recall a value
  they had on file. Not proposing a fix unilaterally; naming the gap.
- **Documentation of what a host reboot actually does to a session** — tonight's data point (cron
  survived, model identity shifted) doesn't fit cleanly into anything I could find describing the
  reboot/resume/compaction distinction from the seat's own vantage point.

### Sign-off checklist
