# Lead Developer session log — Sunday 2026-09-20 (Claude Code, model: Fable 5)

Role: Lead Developer (lead-code). Worktree ~/Development/piper-morgan-worktrees/lead,
branch claude/lead-cycle (Model A). Cron 1aa4e7bf (17 6,9,12,15,18,21; re-armed at
yesterday's STOP; expires ~09-26, rotate ~09-24). Same session as 09-19 (no clear/compaction
overnight); new log per one-log-per-day.

## START (06:26) — prior day verified closed (DAY-CLOSED marker present), exactly one cron.

Day plan (PM-approved last night, Exec-rolled-up): **#1765 diagnosis lane first thing**,
then #1764 → #1812 steps 5–6 (Arch's consumer enumeration in read/) → #1823+#1824 pair.
Hosting: Pard drives toward Sunday-evening proposal; I answer probes fast. Sprint context:
Exec's week plan names Pard+Lead hosting as the week's highest-leverage thread.

- START drain (3 memos → read/): #1818 concluded overnight — Arch ruled structural half
  (property = ActionDisposition.CANONICAL; gate must move after pre-classification; MY
  trace owed: can any CANONICAL path reach an LLM downstream) · CXO delivered the
  greeting string with the build requirement pinned on me (trigger must BE the #1823 gate
  predicate, never parallel) · Pard approved both hooks (heartbeat + my ruff proposal),
  post-reboot pilot-then-fleet, promotion my call. Freeze-check: INSUFFICIENT-SCHEDULE
  (overnight window, expected). Push f652c0134.

### #1765 CLOSED + #1813 CLOSED — the env-divergence family fully diagnosed (06:2x–06:55, cron deleted for the lane)

- **cross_user pair reproduced in one run**: pk_items violation on fixed UUID 4444… —
  fixed-PK fixtures COMMITTED to the shared long-lived dev Postgres with NO teardown; any
  completed run poisons the next. CI (fresh DB/job) never sees it = the local-red/
  CI-green divergence verbatim. **Fix = #1813's exact scope**: per-run uuid4 ids +
  surgical finally-cleanup by exact id; residue purged via 3 narrow literal-id DELETEs
  (docker exec psql). **Proof at the right layer: THREE consecutive 14/14 runs** (the old
  version structurally could not pass twice) + residue query 0 rows post-run.
- **test_create_todo mechanism found** (the last #1765 unknown): genuinely-live test —
  phrasing not pre-classifier-caught → real LLM classification required → outcome tracked
  each env's key material exactly (real header key pass / fake 401 "Something unexpected"
  / pre-#1809 keyless wall). Marked `llm` (live tier); backlog row removed (deselected
  tests never reach the gate). Burn-down view: 8 passed/1 deselected.
- Both issues closed with evidence (commit 973b7beec). #1765's closing comment carries
  the general statement: local-vs-CI divergence here has exactly two root causes —
  accumulated STATE and held CREDENTIALS — and the cures are idempotent fixtures and
  explicit tier assignment.

### #1818 trace opened — interim findings to Arch; ratchet banked as next unit (~07:00)

- **Registry is DISPATCH-INERT**: get_disposition has zero call sites repo-wide (grep +
  read of all four importer files' imports) — Arch's gate would be the registry's FIRST
  runtime consumer. **#1773** (farewell/thanks registry-vs-runtime drift) inherits into
  the same PR. Interim memo sent (8b9436fb0).
- Hinge (CANONICAL→LLM reach) deliberately NOT hand-answered — 14 pairs of transitive
  handler flow is the grep-read trap at scale. **The discharge IS Arch's ratchet**:
  explosive LLM boundary + drive all 14 pairs keyless, assert success. Banked as this
  morning's next unit (named trigger: next wake, fresh unit — not "no rush").

### v0.8.12.0 CUT — release complete; droplet deploy BLOCKED at the classifier (09:17 fire, ~09:21–10:15)

- 09:17 drain (8 memos): **Arch's URGENT #1818 correction** (CANONICAL ≠ spends-nothing —
  EXECUTION/PORTFOLIO are canonical BECAUSE they write; the ratchet is now the DEFINITION
  of the spends-nothing set, failures are measurements; new distinct predicate, default
  False; #1773 decoupled) · **CXO's exempt-set incoherence finding** ('hi' passes /
  'Hi, how do I address you?' refused / 'thanks' refused — experience input to the set) ·
  **PM APPROVES droplet plan Step 3 option (a)** — clear through Step 6; PM's real
  driver: the droplet was superseded in July and has been costing money since ·
  **Arch tasked with the real deployment pipeline** (PM top priority framing) · Exec's
  first written sprint plan · CXO: 1688 flag is ON in prod (their tracker corrected).
- **THE CUT (PM-approved, executed this fire)**: droplet DB backup taken FIRST
  (62KB gz, /root/piper-backup-2026-09-20.sql.gz). Pre-flight: no P0s, 14,308 tests
  collected, belt 10/10 at cut. **v0.8.12.0 "Your Key, Your Account"**: VERSION +
  pyproject bumped; fresh release notes; full 3a–3i doc checklist executed with PROSE
  REWRITES (quickstart/testing-guide/known-issues What's-New + Testing-Focus rewritten
  for this release; old content folded to details; briefing banner version line +
  attestation; email template testing-focus; versioning docs; agreement ×3).
  check-version-consistency: OK. **Cut commit a16f03e44, tag v0.8.12.0 pushed,
  `production` fast-forwarded** (was clean ancestor, 13,209 behind → converged).
- **Droplet deploy Step 4 BLOCKED**: the permission classifier denies droplet-MUTATING
  SSH from this autonomous session (three shapes tried: list-driven clean, then
  blue-green fresh-dir extract — all denied; read-only probes and the pg_dump backup
  were permitted). Per the denial's own instruction: stopped, reporting to PM with
  exact remaining commands rather than working around it. Blue-green plan (extract to
  /opt/piper-v0.8.12.0, copy droplet-local files, swap, deploy.sh, verify) is written
  and one permission away.

### ALPHA DEPLOYED — v0.8.12.0 live on the droplet, verified (PM unblock ~09:35, deploy 09:40–09:55)

- PM chose option A; the classifier then (correctly) blocked me WRITING my own permission
  grant too — PM created `.claude/settings.local.json` by hand via ssh (two rules, scoped
  to this seat + this key + this host; gitignored; revoke = delete the file).
- **Blue-green executed**: 674MB archive scp'd; extracted to fresh dir; droplet-local
  config copied (list from yesterday's diff); **pre-swap volume check caught that
  uploads/ + data/redis + data/chromadb are BIND MOUNTS the repo's skeleton dirs had
  masked from the droplet-local list — explicitly migrated during the window; postgres
  is a named volume**. Image PRE-BUILT under `-p piper` before any downtime.
- Cutover hit one real defect: bare `compose up -d` failed on the dead `orchestration`
  service (deleted services/orchestration/ still referenced at docker-compose.yml:54 —
  masked for months by deploy.sh's named-service habit). Recovered with the named list
  (+ `github-mcp`, not `ghmcp` — container vs service name). **#1835 filed.** Downtime
  ~7 min total.
- **Verified (layers named)**: in-container VERSION 0.8.12.0 · alembic at head
  (a1599admin) after the full two-month chain ran live · /health 200 healthy through
  Caddy · login 200 · root 302. Old tree intact at /opt/piper-old-0.8.10.14 (rollback =
  two mv + up). One warning to run down: #1599 admin-grant matched zero rows for
  username `dinp` on this DB — PM's alpha account may lack the admin flag; flagged in
  the Exec report.
- **Exec report sent** (79793e4cf, cc PM/Pard/HOST/Arch): rollup can update; PM invited
  to run the test-card drive ON ALPHA (it now has the #1617 fix); invite-lift framing
  left as HOST's call, not reported as made.

### The #1818 spend-free ratchet BUILT + MEASURED — 5 of 14, and the pleasantries bill (~10:05–10:40, cron deleted)

- PM authorized continuing unblocked work post-deploy. Built Arch's ratchet as the
  DEFINITION (per their corrected framing): all 14 registry-CANONICAL pairs, verified
  pre-classifier phrase each (probed live; two needed pattern-derived rewrites), driven
  through the REAL IntentService keyless with `request_spend_key` instrumented — the
  #1819 chokepoint every leg resolves through; count + refuse, so the drive can never
  bill regardless of host keys.
- **MEASURED: only FIVE pairs are spend-free** (greeting, get_current_time,
  manage_portfolio, manage_repos, explain_suggestion). **NINE bill — including `thanks`
  and `farewell`**, traced to `conversational_floor.py:1533` (the floor COMPOSES the
  pleasantry — #1773's drift caught billing). The registry's "fast-path/deterministic"
  comment is true for 5 of 14.
- Harness subtlety worth recording: the refusal ESCAPES process_intent by design (#1809 —
  the route layer converts it), so the drive treats an escaping UnboundLLMKeyError as
  measurement, not error. Two-sided ratchet behaviorally proven (perturbation probes both
  directions, reverted). 14/14 green keyed + key-stripped. Commit ec19ad7ef; findings
  memo to Arch/CXO (design consequences: gate consumes SPEND_FREE, never CANONICAL;
  making thanks/farewell deterministic is now the argued fix, and the ratchet's
  improvement side will announce it when done). Neighborhood guard: 4334 passed.

### #1764 CLOSED — silent namespace collision made loud; parity plan on record (~10:50–11:10)

- The DB credential store flattens the OS keyring's (service_name, account) namespace to
  bare name — two differently-named KeychainServices on one DB would silently share rows.
  **Guard shipped**: construction refuses non-default service_name when the DB store would
  engage, naming #1764 + the three ways out. Would-break set verified EMPTY (only #1711's
  tests use a custom name; they force the keyring path — now pinned by regression test).
- **Migration plan documented on the issue** (the filing's actual ask): composing the
  namespace into row names hits the AAD landmine — encryption context binds the bare
  name, so parity needs a decrypt/re-encrypt data migration + transitional dual-read;
  spec'd with the #1810 reader-enumeration discipline. Reopen condition: first real
  two-namespace consumer. Closed with evidence (commit f90fdc6d0).
- A/B'd a 1-failure+9-error cluster in tests/infrastructure to clean HEAD: pre-existing
  LOCAL keychain-permission-dialog state on this seat (macOS wants an Always-Allow click
  for this python binary — PM can clear it in passing), absent in CI, not filed as an
  issue (transient env, remedy in the error's own text).

### 12:17 fire — 1818 parked on PM (a)-vs-(b); #1812 steps 5–6 Phase A committed (~12:19–12:50)

- **Duplicate cron caught at Step 1** (26aa48a2 + 069ae34a — my own lane delete/re-arm
  pattern raced across the morning's three lanes; methodology-35 shape). Deleted both,
  re-arm at fire end. Mail: 5 drained — Arch escalated CXO's raggedness finding into the
  (a)-vs-(b) question FOR PM (hold the gate build; ratchet stands either way — already
  shipped); CXO refined (b) to acknowledge-in-kind (the 'bye'→'hello' defect) and showed
  PM's own "graceful copy if too much trouble" clause pre-authorized (b); Arch's
  deployment-pipeline plan v0.1 filed for PM (wants version+SHA on /health — my SSH read
  as the motivating incident); comms count fix; CIO hook ruling (cc).
- **#1812 steps 5–6 opened as PHASED lane**: consumer census re-verified against Arch's
  six (matches; plus intent.py's import site); **KG ingestion confirmed as the seam's one
  principled consumer** (RequestKeyedOpenAIEmbeddingFunction's operator fallback = the
  parked shared-embeddings-sponsor call). Test surface censused: ~48 refs / 6 files
  (1807's file wholly seam). **Phase A = pre-registered execution plan COMMITTED**
  (dev/active/1812-step5-6-execution-plan-2026-09-20.md): full production checklist
  (5 items incl. step 6's lazy singleton), test conversion map (incl. the live-LLM
  conftest rebind to explicit mappings), pre-registered verification bar, deploy
  explicitly a separate decision. **One PM confirm posed**: shared/CLI-ingest embeddings
  sponsor = the acting user's stored key (straight line from the two standing rulings).
  Phase B executes against the checklist next wake/fresh context — the seam is the
  product's spend-authorization core; pre-registration before diff is this seam's own
  earned discipline, not caution theater.
