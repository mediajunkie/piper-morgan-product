# Lead Developer session log — Sunday 2026-09-20 (Claude Code, model: Fable 5)

Role: Lead Developer (lead-code). Worktree ~/Development/piper-morgan-worktrees/lead,
branch claude/lead-cycle (Model A). Cron 1aa4e7bf (17 6,9,12,15,18,21; re-armed at
yesterday's STOP; expires ~09-26, rotate ~09-24). Same session as 09-19 (no clear/compaction
overnight); new log per one-log-per-day.

## START (06:26) — prior day verified closed (DAY-CLOSED marker present), exactly one cron.

Day plan (PM-approved last night, Exec-rolled-up): **#1765 diagnosis lane first thing**,
then #1764 → #1812 steps 5–6 (Arch's consumer enumeration in read/) → #1823+#1824 pair.
Hosting: Pard drives toward Sunday-evening proposal; I answer probes fast. Sprint context:
Exec's week plan names Pard+Lead hosting as the week's highest-leverage thread.

- START drain (3 memos → read/): #1818 concluded overnight — Arch ruled structural half
  (property = ActionDisposition.CANONICAL; gate must move after pre-classification; MY
  trace owed: can any CANONICAL path reach an LLM downstream) · CXO delivered the
  greeting string with the build requirement pinned on me (trigger must BE the #1823 gate
  predicate, never parallel) · Pard approved both hooks (heartbeat + my ruff proposal),
  post-reboot pilot-then-fleet, promotion my call. Freeze-check: INSUFFICIENT-SCHEDULE
  (overnight window, expected). Push f652c0134.

### #1765 CLOSED + #1813 CLOSED — the env-divergence family fully diagnosed (06:2x–06:55, cron deleted for the lane)

- **cross_user pair reproduced in one run**: pk_items violation on fixed UUID 4444… —
  fixed-PK fixtures COMMITTED to the shared long-lived dev Postgres with NO teardown; any
  completed run poisons the next. CI (fresh DB/job) never sees it = the local-red/
  CI-green divergence verbatim. **Fix = #1813's exact scope**: per-run uuid4 ids +
  surgical finally-cleanup by exact id; residue purged via 3 narrow literal-id DELETEs
  (docker exec psql). **Proof at the right layer: THREE consecutive 14/14 runs** (the old
  version structurally could not pass twice) + residue query 0 rows post-run.
- **test_create_todo mechanism found** (the last #1765 unknown): genuinely-live test —
  phrasing not pre-classifier-caught → real LLM classification required → outcome tracked
  each env's key material exactly (real header key pass / fake 401 "Something unexpected"
  / pre-#1809 keyless wall). Marked `llm` (live tier); backlog row removed (deselected
  tests never reach the gate). Burn-down view: 8 passed/1 deselected.
- Both issues closed with evidence (commit 973b7beec). #1765's closing comment carries
  the general statement: local-vs-CI divergence here has exactly two root causes —
  accumulated STATE and held CREDENTIALS — and the cures are idempotent fixtures and
  explicit tier assignment.

### #1818 trace opened — interim findings to Arch; ratchet banked as next unit (~07:00)

- **Registry is DISPATCH-INERT**: get_disposition has zero call sites repo-wide (grep +
  read of all four importer files' imports) — Arch's gate would be the registry's FIRST
  runtime consumer. **#1773** (farewell/thanks registry-vs-runtime drift) inherits into
  the same PR. Interim memo sent (8b9436fb0).
- Hinge (CANONICAL→LLM reach) deliberately NOT hand-answered — 14 pairs of transitive
  handler flow is the grep-read trap at scale. **The discharge IS Arch's ratchet**:
  explosive LLM boundary + drive all 14 pairs keyless, assert success. Banked as this
  morning's next unit (named trigger: next wake, fresh unit — not "no rush").
