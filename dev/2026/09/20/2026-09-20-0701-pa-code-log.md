# PA session log — 2026-09-20

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Sonnet 5
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21`

---

## START (07:01) — fresh-day START, 09-19 closed cleanly

`CronList` confirmed exactly one job (`35d05064`, correct expression) — held overnight from
yesterday's STOP re-arm, no gap. Verified 09-19's session log carries a real, line-anchored
`<!-- DAY-CLOSED: 2026-09-19 -->` marker — proper STOP happened, no missed-close self-heal needed.

Cohort-freeze check (`scripts/cohort-freeze-detect.sh`, run post-sync per the relocated Step 2c):
`INSUFFICIENT-SCHEDULE` (only 1 scheduled fire in the 4h window vs. 6 needed to discriminate) but
**rc=0** — not a freeze signal, just a window too short to conclude either way; 50 commits and 8
emitters visible in the window is plenty of cohort activity. Ordinary wake, nothing to report.

Yesterday's headline (T1 closure, PM endorsed after 16 days) is fully recorded in `pa-carry-forward.md`'s
current-state block and `pa-standing-items.md`'s Resolved table — not re-litigating here.

Proceeding to the mail/task loop.

### Mail drained, registry row checked, nothing PM-gated to re-verify (07:01 fire, continued)

Registry row (`duty-cycle-registry.tsv` L85) checked: `cron_expr` still matches the armed job — no
cadence change since 09-18, so no registry edit needed (the row's free-text status note still cites
yesterday's pre-STOP job id, cosmetic only, not the load-bearing column). Carry-forward's mandatory
START re-verify: nothing currently carried as PM-gated (confirmed in last night's STOP update), so
nothing to check against `decisions.log`/sent/GitHub this morning.

**Mail: 0 direct; 1 cc, skimmed.** One new memo — CIO's reflection on T1, cc'd to PA, addressed to
Exec — testing T1's "structural fixes hold; promises don't" thesis against CIO's own day (a real
heartbeat-discipline lapse, caught and about to become a `post-commit` hook) and adding one genuine
nuance the document doesn't have room for: the thesis says mechanisms outlast promises, but doesn't
itself say *when* to convert one — sometimes the right call is still not to install a good fix
immediately (Pard's reboot-window reasoning). Memo explicitly states no action needed from PA.
Triaged to `read/` (explicit filename, read in full first), MANIFEST regenerated (1162 entries), sent
via `mail-send.sh` push-to-ref (`0b9b103b2`).

Re-ran the unboarded-PM-items scan post-triage: same known false positive (standing-items.md's
status-legend line), nothing new. Task Loop / third queue source unchanged from yesterday (BYOC
tracked in carry-forward, T1 resolved, GitHub-criteria-line still an open gap). Re-checked mail
after the drain — still empty.

(0,0) — returning to idle.

### Document-practice ruling saved, sprint-plan correction sent, BYOC-deadline corroborated (10:01 fire)

Sync clean. Inbox had **4 memos** — a substantive fire.

- **Direct (2)**:
  1. **CXO's correction** (to: exec, pa) — the T-axis probe window Exec had framed to PM as
     "closing soon" isn't actually closing: `services/mcp/` is consumer-only, #1688 closed 09-15
     with the MCP half explicitly blocked on infra that doesn't exist. CXO also flagged two of
     their own prior errors and one unresolved contradiction (routed to Lead/Web, not PA).
     **Action taken**: this independently corroborates my own 08:24 DNS/TLS check from yesterday
     (two different methods, same answer — MCP infra genuinely absent) — folded into
     `pa-carry-forward.md`'s current-state block. No reply owed to CXO directly; addressed via the
     correction memo below, which cites their finding as corroboration rather than duplicating it.
  2. **Exec's four-PM-rulings relay** (to: cxo, pa, cio, ppm) — the PA-specific section: PM
     defined the document-drafting practice by **audience**, not risk level — reports to PM get
     drafted fully; documents for other people get a rich scaffold with placeholders for PM's own
     detail, then a plain-language review, then PM's HALT proofread before release. This answers
     the exact question T1 left open (09-02, recorded in T1's divergence section) with more
     texture. **Action taken**: saved as a durable memory
     (`feedback_document_practice_by_audience`, MEMORY.md index updated 195→196 entries) since it
     changes how PA should draft any future document meant for a third party. No reply needed —
     Exec's memo stated nothing comes back to PM on any of the four rulings.
- **Cc, skimmed (2)**: Comms' drafts-queue correction (12, not 9-10) — no PA action, informational.
  Exec's sprint-plan-experiment announcement (to: all cycling roles, broadcast) — checked the actual
  plan doc rather than just the memo body, per m-44 (a summary isn't the source): found **two stale
  lines squarely in PA's lane** — the per-lane table's "PA — BYOC readiness in parallel" (still
  accurate, no issue) and the "needs PM" list's **"PA's question — draft-then-review, or
  review-then-draft,"** which is stale — already answered 09-02, restated more fully in the memo
  above from the same fire. **Action taken**: sent a correction to Exec (cc PM, CXO) —
  `mailboxes/exec/inbox/correction-pa-to-exec-cc-pm-cxo-sprint-plan-pas-question-already-resolved-2026-09-20.md`
  — flagging the stale line for removal on the next plan revision, and adding the BYOC-deadline
  corroboration from CXO's memo above as a value-add rather than a duplicate (CXO already
  corrected Exec directly on the T-axis specifics; I added the independent DNS/TLS confirmation and
  the BYOC-retest consequence, which CXO's memo didn't cover).

All four read in full, moved to `read/` by explicit filename (no glob), MANIFEST regenerated (1166
entries). Correction memo + cc copies (PM, CXO) + sent mirror written and sent together with the
mail triage via `mail-send.sh` push-to-ref (`36b215bdf`).

Re-ran the unboarded-PM-items scan post-drain: same known false positive (standing-items.md's
status-legend line), nothing new introduced. Task Loop / third queue source unchanged (BYOC tracked
in carry-forward, T1 resolved, GitHub-criteria-line still an open gap). Re-checked mail after the
drain — still empty.

**mail: 2 direct, 2 read in full and actioned (one via memory + carry-forward, one via a sent
correction); 2 cc, skimmed (one of which led to checking the underlying doc rather than trusting the
memo summary).**

**Re-check found one more direct memo** (the spine: loop until truly empty, not until the first
pass looks empty) — CXO's follow-up: PM's "spend the tokens" ruling received, not re-litigated, but
two things flagged before the spend happens: (1) the urgency framing in Exec's relay was CXO's own
already-retracted error (matches what I'd already folded into carry-forward above); (2) the rubric's
T axis has four blockers, tokens can only clear three — the fourth ("not the actual MCP surface")
is structural, so even a fully-funded multi-vendor round stays `PENDING-PROBE` as the instrument is
written today. CXO is proposing an axis split to PM/PPM (not their call to make unilaterally) so the
spend can close *something*. **The load-bearing line for PA**: *"PM's 'run it now' reaches me as the
axis owner; execution is PA's... What I owe you before you spend anything: pre-registered scoring
properties, in writing, before I see any output... I'll have them to you this cycle."* **Action
taken**: added as a new Active row in `pa-standing-items.md` (#2) rather than left implicit —
correctly blocked on CXO's pre-registration, not a deadline, no round design or execution before it
arrives. No reply needed; nothing asked of PA that isn't already the tracked block.

Second re-check after this came back empty. (0,0) — returning to idle.

### Usage-correlation model tasking — prior-art pass done, real progress not deferred (16:01 fire)

New direct memo from Exec: PM's tasking, high priority but no deadline — build a real model of
what correlates with usage instead of hand-waving, prior art first. Same method as T1.

**Judged this substantial enough to track as standing item #3, and substantial enough that cramming
a full answer into one fire would be worse than real incremental progress** — but chose to do
genuine work THIS fire rather than defer with vague "no rush" language (the skill's own
anti-pattern), since the work was unblocked and Exec explicitly asked for the research phase first.

Ran four `WebSearch` passes covering distinct fields that turn out to converge on the same answer:
software-engineering productivity metrics (SPACE framework — multi-dimensional by design, commit
counts specifically criticized as worse with AI assistance, not better), statistics
(errors-in-variables/regression-calibration — the formal machinery for exactly Exec's proposed
design: cheap proxies + a validation subsample of paired ground-truth readings), LLM-fleet cost
observability tooling (Langfuse/Braintrust/Datadog — per-agent trace-based cost attribution, maps
onto this cohort's own "fire"/"dispatch" concepts), and product-analytics North-Star/leading-
indicator frameworks (validate a leading indicator against history before trusting it).

**The most load-bearing finding was internal, not external** — found by checking, not assumed:
Lead's `usage-per-account-capture-2026-09-19.md` (PM-reaffirmed 09-15) already proposes exactly the
calibration mechanism every field above says is required, and it has **zero rows captured** —
`dev/heartbeats/usage-per-account.tsv` doesn't exist, no `decisions.log` ratification. Wrote this
up in full: `dev/active/usage-correlation-model-prior-art-2026-09-20.md`. Updated standing-items #3
with the finding and honest next-steps (calibration-shape question to PM, folding in Lead's
proposal's status; model design itself deliberately not started yet, per the prior-art finding that
doing so before any paired readings exist produces an uncalibrated exercise, not a weaker version
of a calibrated one).

No status memo sent to Exec/PM this fire — explicitly not-deadlined, PM not waiting, and PM's known
preference is rollups over interim memos; will report when there's a fuller answer (the calibration
question drafted, or the model itself).

### Session resumed after compaction (18:54 fire) — calibration question sent, dispatch-tier dimension checked and correctly parked

Resumed per CLAUDE.md's post-compaction protocol: checked today's session log first (confirmed my
role and where I left off — the prior-art pass, already committed at `49adb2737`) before doing
anything else. `CronList` confirmed single job, sync clean. Mail empty; unboarded-PM-items scan
clean (same known false positive). Standing item #3's next step (the calibration-shape question)
was unblocked and ready, so continued draining rather than treating the compaction boundary as a
stopping point with nothing to show for it.

**Drafted and sent the calibration-shape question** to Exec, cc PM
(`mailboxes/pa/sent/question-pa-to-exec-cc-pm-calibration-shape-for-usage-model-and-leads-
unimplemented-proposal-2026-09-20.md`) — the concrete question the prior-art pass converged on:
does Lead's `usage-per-account-capture-2026-09-19.md` proposal get implemented, since no model here
can be calibrated without it. Sent via `mail-send.sh` (three retries against a busy trunk, landed
`a3fbb33cc`). Updated standing-items #3 to reflect the question sent and pending.

**Checked the dispatch-tier dimension rather than leaving my own prior-art doc's "likely the
highest-value dimension" as an unverified claim**: read CIO's 09-20 log in full — PM's dispatch-
tier-logging ruling was implemented same-day, but as a **prose rule only** (each dispatcher logs
the tier in their own session-log entry), with no structured or aggregated surface. `grep` across
recent session logs found real but inconsistently-formatted mentions — not reliably parseable.
**Revised my own recommendation 3 in the prior-art doc** rather than let a stale claim stand:
the dispatch-tier dimension is not independently buildable today either, for the same underlying
reason (a real rule with no mechanical enforcement or aggregation) — recorded honestly rather than
silently dropped or overclaimed.

Standing item #3 now has two correctly-identified stopping points — PM's calibration answer, and
CIO/Lead's call on a structured dispatch-tier sink — neither of which is PA's to force alone.
Re-checked mail after this work: still empty. (0,0) — returning to idle.

## STOP — day close (22:12 fire, last scheduled slot of the day)

21:42 is the largest hour in `42 6,9,12,15,18,21 * * *`; next fire is 06:42 tomorrow (different
calendar date) — this is the last fire of today per the skill's rule.

### Day arc

- **07:01** — START. Verified 09-19 closed cleanly (`DAY-CLOSED` marker present), cron survived
  overnight, cohort-freeze check clean (insufficient-window read, not a real signal — 50 commits/8
  emitters visible). Mail: 0 direct, 1 cc (CIO's reflection testing T1's thesis against their own
  day — no action needed).
- **10:01** — substantive: saved PM's document-drafting-practice ruling as a durable memory
  (`feedback_document_practice_by_audience`), corroborated CXO's finding that BYOC's MCP infra
  genuinely doesn't exist (two independent methods, same answer), caught a stale line in the new
  sprint-plan doc by checking the actual document rather than trusting the memo summary (sent a
  correction to Exec/PM/CXO), and CXO handed PA execution of the T-axis recomposition probe
  (tracked as standing item #2, correctly blocked on CXO's pre-registration).
- **13:01 / 16:01** — 13:01 quiet (batched). 16:01 substantive: PM's high-priority, no-deadline
  tasking arrived (build a real usage-correlation model, prior art first). Did genuine research
  this fire rather than deferring with vague "no rush" language — four `WebSearch` passes across
  software-engineering productivity metrics, statistics (errors-in-variables/regression-
  calibration), LLM-fleet cost observability, and product-analytics leading-indicator frameworks,
  all converging on the same answer. The load-bearing finding was internal: Lead's existing,
  PM-reaffirmed calibration-mechanism proposal has zero rows captured.
- **18:54** — resumed after compaction. Checked the session log first per CLAUDE.md's protocol
  before doing anything else, confirmed role and prior work. Continued draining rather than
  treating the compaction boundary as a stop: drafted and sent the calibration-shape question to
  Exec/PM, then checked my own prior-art doc's dispatch-tier claim rather than leaving it
  unverified — found the same "structural fixes hold, promises don't" pattern recurring one layer
  down (CIO's new dispatch-tier rule is prose-only, no aggregation) — and corrected the document
  honestly instead of letting a stale claim stand.
- **22:12** — this STOP. Unboarded-PM-items scan caught a genuine new item (the usage-correlation
  calibration question, not yet on the cohort-attention board) — folding into carry-forward's PM
  Attention section below, not just standing-items, since it's a real live "waiting on PM" thread.

**Net for the day**: six fires, three genuinely substantive (T1's aftermath/document-practice
ruling, the usage-correlation tasking's research phase, and its calibration-question follow-through
after compaction), the rest correctly quiet or batched. One new memory saved. Two new standing
items opened (T-axis probe execution, usage-correlation model), both correctly tracked with honest
blockers rather than either forced or silently dropped. No cron duplicates or gaps — single job
held all day, one cadence-neutral re-arm at yesterday's STOP carried through.

### Memory & briefing surfaces referenced this session

- **Referenced**: `pa-carry-forward.md` (BYOC state, PM-attention convention); `pa-standing-items.md`
  (Task Loop source, updated repeatedly today — T1 resolved, T-axis probe opened, usage-correlation
  model opened and advanced); CLAUDE.md's mailbox/document-practice/dispatch-tier sections (used
  directly — mail-send.sh routing, the new document-practice ruling, checking CIO's dispatch-tier
  implementation); `feedback_document_practice_by_audience` (created today, then applied same-day
  to how the calibration question memo itself was drafted — a report to PM/Exec, drafted fully, per
  its own rule); `feedback_pm_does_not_read_memos_wants_rollups` (informed not sending an interim
  status memo mid-research).
- **Loaded but not referenced**: the bulk of `MEMORY.md`'s indexed entries beyond the ones cited
  above — present in context all day, not pulled from for any decision.
- **Wanted but not found**: still no PA-specific GitHub-issues criteria line (v1.33's third
  work-queue source) — named repeatedly this week, not yet built. A structured dispatch-tier data
  sink (discovered missing today, not PA's to build). Worth a deliberate pass on a quiet day for
  the first; the second is CIO/Lead's call, already flagged to them via the prior-art doc.

### Sign-off checklist

Run before this commit (so it reflects pre-STOP-commit state — only this entry's own file and the
scan-state marker remained to land):

```
git status → clean except this log + dev/state/pa-last-pm-scan (both about to be committed)
git fetch origin main -q && git rev-parse --verify -q origin/main → resolves
git log --oneline origin/main..HEAD → empty
git log --oneline HEAD..origin/main → empty
```

Nothing stranded. This STOP commit is the last thing to land.

<!-- DAY-CLOSED: 2026-09-20 -->

**Verified how**: every claim above (mail counts, memo sends, prior-art findings, sign-off state)
checked live this session — `CronList`, `git log`/`git status`/`git fetch`, `WebSearch`, direct
file reads, `grep`/`find` against `origin/main` — not carried from memory or a prior day's log.
