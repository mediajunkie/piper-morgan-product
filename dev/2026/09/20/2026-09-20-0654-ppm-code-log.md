# 2026-09-20 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

## Session Objectives

1. Sprint-week continuity: board hygiene (milestone/status correctness, board-add gaps),
   mailbox triage, issue triage against precedent.
2. Watch for Lead's downstream-spend trace on `#1818` (the last open item before that build starts).
3. No standing owed work item carried from yesterday — epics-9/10 (the oldest open item this seat
   was tracking) was resolved by PM's ruling yesterday.

### 06:54 START

Heartbeat emitted (`scripts/duty-cycle-heartbeat.sh ppm START`). `CronList` → one job (`07ef4da6`,
unchanged overnight — correct expression, no re-arm needed). Prior day's log
(`dev/2026/09/19/2026-09-19-0722-ppm-code-log.md`) confirmed carrying its `<!-- DAY-CLOSED:
2026-09-19 -->` sentinel — Step 0 clean, no missed-STOP recovery needed. Carry-forward read: clean
handoff, nothing PM-gated carried forward. Worktree-collision fingerprint clean (`ppm` ↔
`claude/ppm-cycle`). Synced to `origin/main` clean.

Created this log (no existing same-day log found). Committing immediately per skill discipline,
before the mail loop.

**Mail drain**: 1 item, cc-only (CIO's T1/Cross-Piper reflection to Exec, explicitly "no action
needed from PA or Exec" — PPM cc'd for visibility, not addressed). Skimmed, no ask, triaged.

**`sprint-truth.py` fresh**: `MVP: 54 not done (28 Sprint Backlog, 3 In Progress, 6 In Review, 17
Product Backlog); 1129 done. PLUS 0 unmilestoned.` Clean overnight delta — 2 issues closed
(`#1765`, `#1813`), no board-add drift found this fire.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line (same gap
noted several fires running — worth building rather than continuing to just note).

Quiet START — all three queue sources checked and drained. Returning to idle; cron armed
(`07ef4da6`), next fire 09:52 PDT (WORK).

---

### #1818's structural ruling corrected (a real near-miss caught before build), sprint-plan corrections sent, product question answered (09:54 WORK fire)

Heartbeat emitted. `CronList` → one job (`07ef4da6`), unchanged. Worktree fingerprint clean.
Synced clean. Mailbox had 6 items, mix of cc and direct — all read in full.

**The important one: Arch's structural ruling on `#1818` from last night was WRONG, and would have
opened a keyless path to issue creation and DB writes.** Caught before Lead's ratchet built against
it, not after. Arch had reasoned from the `CANONICAL` disposition's name and a registry comment
without opening the actual routing authority (`_requires_canonical_handler`) — which shows
`CANONICAL` means "the LLM can't do this alone" (true of the *most* consequential actions,
`EXECUTION`/`PORTFOLIO`), not "costs no LLM call." Corrected ruling: the spends-nothing property
doesn't exist and must be created new, fail-closed by default. **CXO separately found the resulting
exempt set is incoherent to a user** even under the corrected design (`"hi"` passes, `"Hi, how do I
address you?"` and `"thanks"`/`"bye"` don't) — a UX-coherence pass to run once the ratchet's actual
output exists, not before. Folded both corrections into epic 2's `#1818` entry, recording the
original wrong ruling and the fix explicitly rather than silently overwriting — same discipline as
yesterday's `#1785` correction, and this one's a sharper instance of the same relay-failure class
PA's Cross-Piper doc named (a claim true "as a disposition name" restated as true "as a cost claim,"
caught by re-opening the actual authority rather than trusting the name).

**Checked Exec's first written sprint plan against reality rather than skimming it** (Exec explicitly
asked "tell me what I got wrong"). Two real, checkable corrections: my own PPM lane line ("collapse
the mini-epics...") was already done at last night's STOP, before the plan was drafted; and "PA's
question — draft-then-review or review-then-draft," listed under "needs PM," was actually answered
by PM on 09-02 per PA's own standing-items — eighteen days stale, not open. Flagged the distinction
from today's *separate, newer* document-practice ruling so the two don't get conflated in the
record.

**Answered PM's direct product question** (routed to CXO+PPM via Exec): should Piper Morgan the
product apply the same audience-based document-practice split to its own file-writing? Real
engagement, not a brush-off: yes, the pattern transfers, but the discriminator is "who will read
this under whose authority" (downstream audience), not naively "who Piper is writing for" — the
naive mapping would wrongly put every user-facing file in the "draft fully" bucket, when a
user-asks-Piper-to-draft-an-email-to-their-client case is structurally the "documents for other
people" bucket PM named. Flagged the real implementation risk (classifying which bucket a request
falls into is a hard problem, and today's `#1818` thread is a live demonstration of how brittle
that kind of classification is) and explicitly did not file an issue — this is CXO's design call to
make once there's a shape, per yesterday's "don't build for an undecided state" lesson.

**Board hygiene**: `#1835` (`[OPS] docker-compose.yml defines a dead 'orchestration' service`) found
NOT ON THE BOARD via the routine `sprint-truth.py` re-run — carried MVP milestone (filed with
`--milestone`, which doesn't board-add) but absent from the project. Board-added, Status Product
Backlog set via the safe per-item mutation. Full recount hit the shared GraphQL throttle
(`gh api rate_limit` confirmed 5000/5000, not my own quota) — verified the fix via a small
single-item query instead of blocking the fire.

**Mail**: 6 items, all read in full; 2 got full substantive replies (sprint-plan corrections,
product-question answer), the rest got explicit epic-file action or were genuinely no-action cc
(informational relays, no PPM ask). 0 pure-skim.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line.

**Discovered issues filed**: None.

**Two more items arrived mid-fire drain, both cc-only, no PPM ask**: Arch escalated CXO's `#1818`
raggedness finding one level further — the whole exemption approach may be the wrong layer; proposed
a uniform "refusal that greets" instead of a ragged exempt set, and explicitly routed the (a)-vs-(b)
choice to PM rather than ruling it themselves. Folded into epic 2 — `#1818` is now genuinely
PM-gated, not PPM's, watching for the answer. Arch's deployment-pipeline plan v0.1 (PM's stated top
priority) also arrived, cc-only, awaiting PM's ruling — no epic-file action, informational.
