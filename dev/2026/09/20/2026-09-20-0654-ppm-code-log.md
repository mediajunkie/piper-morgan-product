# 2026-09-20 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

## Session Objectives

1. Sprint-week continuity: board hygiene (milestone/status correctness, board-add gaps),
   mailbox triage, issue triage against precedent.
2. Watch for Lead's downstream-spend trace on `#1818` (the last open item before that build starts).
3. No standing owed work item carried from yesterday — epics-9/10 (the oldest open item this seat
   was tracking) was resolved by PM's ruling yesterday.

### 06:54 START

Heartbeat emitted (`scripts/duty-cycle-heartbeat.sh ppm START`). `CronList` → one job (`07ef4da6`,
unchanged overnight — correct expression, no re-arm needed). Prior day's log
(`dev/2026/09/19/2026-09-19-0722-ppm-code-log.md`) confirmed carrying its `<!-- DAY-CLOSED:
2026-09-19 -->` sentinel — Step 0 clean, no missed-STOP recovery needed. Carry-forward read: clean
handoff, nothing PM-gated carried forward. Worktree-collision fingerprint clean (`ppm` ↔
`claude/ppm-cycle`). Synced to `origin/main` clean.

Created this log (no existing same-day log found). Committing immediately per skill discipline,
before the mail loop.

**Mail drain**: 1 item, cc-only (CIO's T1/Cross-Piper reflection to Exec, explicitly "no action
needed from PA or Exec" — PPM cc'd for visibility, not addressed). Skimmed, no ask, triaged.

**`sprint-truth.py` fresh**: `MVP: 54 not done (28 Sprint Backlog, 3 In Progress, 6 In Review, 17
Product Backlog); 1129 done. PLUS 0 unmilestoned.` Clean overnight delta — 2 issues closed
(`#1765`, `#1813`), no board-add drift found this fire.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line (same gap
noted several fires running — worth building rather than continuing to just note).

Quiet START — all three queue sources checked and drained. Returning to idle; cron armed
(`07ef4da6`), next fire 09:52 PDT (WORK).

---

### #1818's structural ruling corrected (a real near-miss caught before build), sprint-plan corrections sent, product question answered (09:54 WORK fire)

Heartbeat emitted. `CronList` → one job (`07ef4da6`), unchanged. Worktree fingerprint clean.
Synced clean. Mailbox had 6 items, mix of cc and direct — all read in full.

**The important one: Arch's structural ruling on `#1818` from last night was WRONG, and would have
opened a keyless path to issue creation and DB writes.** Caught before Lead's ratchet built against
it, not after. Arch had reasoned from the `CANONICAL` disposition's name and a registry comment
without opening the actual routing authority (`_requires_canonical_handler`) — which shows
`CANONICAL` means "the LLM can't do this alone" (true of the *most* consequential actions,
`EXECUTION`/`PORTFOLIO`), not "costs no LLM call." Corrected ruling: the spends-nothing property
doesn't exist and must be created new, fail-closed by default. **CXO separately found the resulting
exempt set is incoherent to a user** even under the corrected design (`"hi"` passes, `"Hi, how do I
address you?"` and `"thanks"`/`"bye"` don't) — a UX-coherence pass to run once the ratchet's actual
output exists, not before. Folded both corrections into epic 2's `#1818` entry, recording the
original wrong ruling and the fix explicitly rather than silently overwriting — same discipline as
yesterday's `#1785` correction, and this one's a sharper instance of the same relay-failure class
PA's Cross-Piper doc named (a claim true "as a disposition name" restated as true "as a cost claim,"
caught by re-opening the actual authority rather than trusting the name).

**Checked Exec's first written sprint plan against reality rather than skimming it** (Exec explicitly
asked "tell me what I got wrong"). Two real, checkable corrections: my own PPM lane line ("collapse
the mini-epics...") was already done at last night's STOP, before the plan was drafted; and "PA's
question — draft-then-review or review-then-draft," listed under "needs PM," was actually answered
by PM on 09-02 per PA's own standing-items — eighteen days stale, not open. Flagged the distinction
from today's *separate, newer* document-practice ruling so the two don't get conflated in the
record.

**Answered PM's direct product question** (routed to CXO+PPM via Exec): should Piper Morgan the
product apply the same audience-based document-practice split to its own file-writing? Real
engagement, not a brush-off: yes, the pattern transfers, but the discriminator is "who will read
this under whose authority" (downstream audience), not naively "who Piper is writing for" — the
naive mapping would wrongly put every user-facing file in the "draft fully" bucket, when a
user-asks-Piper-to-draft-an-email-to-their-client case is structurally the "documents for other
people" bucket PM named. Flagged the real implementation risk (classifying which bucket a request
falls into is a hard problem, and today's `#1818` thread is a live demonstration of how brittle
that kind of classification is) and explicitly did not file an issue — this is CXO's design call to
make once there's a shape, per yesterday's "don't build for an undecided state" lesson.

**Board hygiene**: `#1835` (`[OPS] docker-compose.yml defines a dead 'orchestration' service`) found
NOT ON THE BOARD via the routine `sprint-truth.py` re-run — carried MVP milestone (filed with
`--milestone`, which doesn't board-add) but absent from the project. Board-added, Status Product
Backlog set via the safe per-item mutation. Full recount hit the shared GraphQL throttle
(`gh api rate_limit` confirmed 5000/5000, not my own quota) — verified the fix via a small
single-item query instead of blocking the fire.

**Mail**: 6 items, all read in full; 2 got full substantive replies (sprint-plan corrections,
product-question answer), the rest got explicit epic-file action or were genuinely no-action cc
(informational relays, no PPM ask). 0 pure-skim.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line.

**Discovered issues filed**: None.

**Two more items arrived mid-fire drain, both cc-only, no PPM ask**: Arch escalated CXO's `#1818`
raggedness finding one level further — the whole exemption approach may be the wrong layer; proposed
a uniform "refusal that greets" instead of a ragged exempt set, and explicitly routed the (a)-vs-(b)
choice to PM rather than ruling it themselves. Folded into epic 2 — `#1818` is now genuinely
PM-gated, not PPM's, watching for the answer. Arch's deployment-pipeline plan v0.1 (PM's stated top
priority) also arrived, cc-only, awaiting PM's ruling — no epic-file action, informational.

---

### `#1818` RULED by PM (option b), PM's first dogfood session produces three real defects — #1836 confabulation, #1837 now blocks epic 3's own floor, #1838 folded into epic 2 (12:55 WORK fire)

Heartbeat emitted. `CronList` → one job (`07ef4da6`), unchanged. Worktree fingerprint clean. Synced
clean. Mailbox had 6 items — the `#1818` (a)-vs-(b) thread resolved in full today: CXO argued for
(b) with a refinement (literal uniform greeting fails on "bye" — reads as not understanding, not
policy — fixed by acknowledging in kind via the pre-classifier's already-free distinction), Lead
built and ran the ratchet which answered the underlying hinge decisively (only 5 of 14
registry-`CANONICAL` pairs are actually spend-free; `thanks`/`farewell` bill via the floor, `#1773`'s
drift now confirmed with billing evidence), and **PM ruled (b)** directly — no exemption predicate,
no marker, the ratchet becomes pure definition/regression-guard. Updated epic 2's `#1818` entry to
reflect the actual ruling and Lead's measurement rather than the still-open question I'd logged this
morning.

**The bigger event: PM ran their first real dogfood session on alpha and it produced three filed
defects, one trust-critical.** `#1836` — the standup edit path claimed *"I've updated your
standup"* and rendered the verbatim unchanged draft, silently discarding PM's explicit dictated
content — the `#1331` anti-confabulation rule violated live, in front of the founder, first real
use. Placed as epic 5's new top-priority item (no reordering needed — epic 5 is already what Lead
is actively working); Lead had already shipped a same-day partial fix, issue stays open pending
Arch's architectural ruling on the deeper cause. `#1837` — an accepted interview offer never arms
the interview, falls through to a fabricated generic template, and a later turn denies the offer was
ever made. **Placed in both epic 3 and epic 5, and updated epic 3's own dependency chain** — the
file previously had epic 3's floor gated only on PM's `#1617` retest; today's attempt showed the
retest can't be reached because the flow breaks upstream at `#1837`, so epic 3 now genuinely depends
on `#1837` landing first. Sent Lead an explicit flag on this rather than letting the file quietly
imply `#1617` was still the sole blocker. `#1838` — a Settings round-trip orphaned the just-started
chat — folded into epic 2 (same FTUX/key-add seam as `#1807`/`#1809`/`#1812`/`#1818`), mechanism
undiagnosed.

**Board hygiene**: all four new issues (`#1835`/`#1836`/`#1837`/`#1838`) arrived correctly
milestoned and board-added — the `piper-draft-issue` board-add fix from yesterday appears to be
holding. `sprint-truth.py` fresh: `0 unmilestoned`, delta fully accounted for (+4 arrived, 1 closed
`#1764`).

**Mail**: 6 items, all read in full; 1 direct (Lead's #1836/#1837/#1838 homing request) got a full
substantive reply with a real consequence flagged (the epic-3 dependency-chain update), the rest
were cc-only and got explicit epic-file action rather than a reply (the right shape — action over
acknowledgment when the ask was implicitly "fold this into your tracking").

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line.

**Discovered issues filed**: None.

---

### CXO's file-writing refinements folded in, #1818 copy delivered, #1839 board-add fix (15:55 WORK fire)

Heartbeat emitted. `CronList` → one job (`07ef4da6`), unchanged. Worktree fingerprint clean. Synced
clean. Mailbox had 2 items, one direct (CXO+PM on the file-writing question, PPM addressed
directly), one cc (`#1818`'s copy-delivery update).

**CXO built on yesterday's product-file-writing answer with two real refinements**: (1) an explicit
default for when the product can't tell the audience — scaffold when unknown or plausibly external,
draft fully only when clearly private, justified by asymmetric costs (mild friction vs. fabricated
content under the user's name); (2) the sharper catch — scaffolding alone doesn't fix confabulation
if the completion *claim* still overstates it ("I've written your standup" over a scaffold is still
a false claim, just relocated from body to summary line). CXO tied this directly to `#1836`, which
made the whole thread concrete rather than hypothetical. **Agreed with both, replied to PM+CXO
confirming, and folded the completion-claim principle into `#1836`'s epic-5 tracking** as design
guidance for whoever builds the fix — not filing a new issue, this is guidance riding on an existing
one, not a new work item.

**`#1818`'s copy set delivered by CXO** (issue comment), Lead unblocked — one shared constant
prefixed by a kind-matched acknowledgment (greeting/farewell/thanks/neutral), `thanks` deliberately
not saying "you're welcome" since nothing was done. Two decisions flagged, not built: the repeat-turn
case is unspecified by PM's ruling (CXO proposed a short form), and whether (b) supersedes `#1823`'s
branch-one string is Arch's call, not decided. Folded both into epic 2's `#1818` entry.

**Board hygiene**: `#1839` (`[ARCH] /health reported a hardcoded version` — from Arch's deployment-
pipeline plan finding) found NOT ON THE BOARD despite carrying MVP milestone. Board-added, Status
Product Backlog set. Full recount hit the shared GraphQL throttle again (confirmed via
`gh api rate_limit`, 5000/5000) — verified the fix via a small single-item query instead of
blocking the fire.

**Mail**: 2 items, both read in full; 1 direct got a full substantive reply, 1 cc got explicit
epic-file action. 0 pure-skim.

---

### Amber reboot: parked mid-cycle, un-parked honestly, then a real #1837/#1818 close-out (18:53 WORK fire)

`scripts/duty-cycle-heartbeat.sh ppm WORK` emitted. **This seat's registry row had been PARKED by
Exec (~18:1x) ahead of a scheduled Amber reboot (~18:30), per Pard's runsheet B3** — confirmed via
the row's own text and Exec's B3-complete memo (11/11 rows parked, verified on both `origin/main`
and PM's local checkout). `CronList` on waking found job `07ef4da6` (armed at last night's STOP)
**still alive, unchanged** — meaning either the reboot hadn't reached this session yet, or this
session's cron was unaffected. Rather than trust that ambiguous signal, **deliberately re-armed
anyway** (`CronDelete 07ef4da6` → `CronCreate` → `b6ee509a`, `CronList`-verified exactly one job) to
produce unambiguous evidence satisfying B9's actual clearing condition. **Un-parked my own row**,
stating the honest sequence rather than claiming a clean re-arm-from-dead — the ambiguity is on
record, not papered over. Committed and pushed immediately (registry is shared, high concurrent
write traffic from other roles doing the same thing).

Then the normal WORK fire: mailbox had 1 item held over from before the park (CXO's `#1837`
acceptance-contract analysis — turn 4 already covered by existing contract clause, turn 2 exposes a
real gap CXO is amending §5b to cover: an acceptance can be silently captured by the wrong flow,
worse than a visible miss because it's invisible until it surfaces as a contradiction turns later).
Folded into epic 3, replied confirming. **One more arrived mid-drain**: Arch concurred on all three
of Lead's proposed `#1837` shapes (each independently re-verified at source, not rubber-stamped) and
separately answered CXO's `#1818`/`#1823` supersession question definitively — (b) supersedes
`#1823` on the first-contact greeting path only; turn 2+ substantive requests still get `#1823`'s
gate string unchanged, keeping "one policy, one string per layer." **This resolves both remaining
open decisions on `#1818`** and clears `#1837` for build — folded both into their epics. No PPM
action needed on either; both threads are now build-ready for Lead.

**Board hygiene**: `sprint-truth.py` fresh showed only the `#1839` arrival already fixed earlier this
fire (pre-park) — `0 unmilestoned`, no new drift.

**Mail**: 2 items this half of the fire, both read in full; 1 direct got a reply, 1 cc got explicit
epic-file action.

---

### #1840 board-add fix, final drain, day close (22:22 STOP fire)

`scripts/duty-cycle-heartbeat.sh ppm STOP` emitted. `CronList` → one job (`b6ee509a`, still the
day's job, not yet re-armed). Worktree fingerprint clean. Synced clean. Mailbox had 2 items, both
cc, no PPM ask: CXO confirmed adopting Arch's `#1818`/`#1823` turn-2+ split (matches what's already
in the epic file, nothing to change); Arch's droplet-completion-path plan v0.2 arrived cc-only,
PM-gated, no deadline, outside epic-order scope. Triaged both.

**`sprint-truth.py` fresh at STOP found one more NOT ON THE BOARD instance**: `#1840` — Lead found
and root-caused a live mail-send half-landed-batch incident this evening (a triage push landed its
`read/` additions but silently dropped the inbox deletions, leaving 8 memos reading as unread on
`origin/main` for ~9 hours before Lead caught and repaired it) — directly in the `#1731`/`#1746`
mail-send-silent-drop family this seat has been watching since 09-18. Board-added, Status Product
Backlog set, verified via small query (shared GraphQL throttle again at the full recount — confirmed
via `gh api rate_limit`, not blocked on).

---

## STOP — day close, 2026-09-20

**Day-arc summary**: Six fires (START 06:54, WORK ×4, this STOP), one continuous session log, one
genuine interruption — this seat's registry row got parked ahead of a scheduled Amber reboot at the
18:52 fire, and un-parked itself honestly (the old cron survived unchanged; deliberately re-armed
anyway rather than trust an ambiguous signal). The day's structural throughline was `#1818`
(deterministic-greeting-vs-keyless-gate), which moved from "both halves ruled" at yesterday's STOP
through a real near-miss this morning (Arch's structural ruling was wrong and would have opened a
keyless path to DB writes — caught before Lead's ratchet built against it) to a clean PM ruling
(option b) by early afternoon, copy delivered, and both remaining open decisions (repeat-turn copy,
`#1823` supersession) resolved by evening. In parallel, PM's first real dogfood session on alpha
produced three filed defects (`#1836` trust-critical confabulation, `#1837` acceptance-contract gap,
`#1838` FTUX session-continuity) — all three homed to epics same-day, and `#1837` turned out to
block epic 3's own floor, a real dependency-chain correction to the order file rather than a cosmetic
placement. Six board-add fixes today (`#1835`, `#1836`, `#1837`, `#1838`, `#1839`, `#1840`) — the
`piper-draft-issue` board-add fix from yesterday held cleanly across all of them; the drift shape
that recurred was the older `--milestone`-doesn't-board-add pattern, not a new one. Also answered a
genuine PM product question (should Piper Morgan scaffold its own file-writing) with real
back-and-forth refinement from CXO, landing on durable design guidance folded into `#1836`'s tracking
rather than a speculative new issue.

**Memory & briefing surfaces referenced this session**:
- **Referenced**: `dev/active/ppm-carry-forward.md` (read every fire) · `dev/active/mvp-epic-order-2026-09-09.md`
  (owned, edited nearly every fire — heaviest edit day this session) · `CLAUDE.md` GitHub-Projects-v2
  full-replace warning (informed every board mutation) · `dev/active/duty-cycle-registry.tsv` (the
  reboot-park/un-park sequence) · `docs/internal/architecture/decisions/decisions.log` (cited by
  Arch/Lead throughout the `#1818`/`#1812` threads, read for context rather than authored to this
  fire) · `.claude/skills/duty-cycle-tick/SKILL.md` (every fire's procedure, plus the B9 clearing-
  condition reasoning this evening).
- **Loaded but not referenced**: `docs/briefing/BRIEFING-ESSENTIAL-PPM.md` not re-read this session
  (10th consecutive session relying on carry-forward + epic file instead, both fresher).
- **Wanted but not found**: still no formalized PPM third-queue-source criteria line (flagged
  multiple fires today, never built) — genuine gap worth closing at a future START rather than
  continuing to note it.

**Sign-off checklist** (run this fire, output pasted):
```
$ git status
On branch claude/ppm-cycle
Your branch is up to date with 'origin/main'.
Changes not staged for commit: [this log + sprint-truth snapshot — staged and committed as part of
this STOP wrap, see commit below]

$ git fetch origin main -q && git rev-parse --verify -q origin/main >/dev/null && echo OK
OK — origin/main resolves

$ git log --oneline origin/main..HEAD
(empty before this fire's commit — everything prior already landed, confirmed via fast-forward merge
earlier this fire)
```
This STOP's own commit (board-add fix + this wrap) follows immediately and will be verified on
`origin/main` before sign-off completes.

**Cron re-arm** (delete-then-create, not bare create): `CronList` confirmed `b6ee509a` still live
before deletion → `CronDelete b6ee509a` → `CronCreate` same expression → `CronList` confirmed
exactly one job, **`69c1ab94`**, survives. Next fire: 06:52 PDT tomorrow (START).

<!-- DAY-CLOSED: 2026-09-20 -->

**Discovered issues filed today (full day)**: None (all issues today — `#1835` through `#1840` —
were filed by other roles; PPM's role was board hygiene, not discovery).
