# Session log — Exec (Chief of Staff), 2026-09-20

**Role**: Chief of Staff (Exec) · **Tool**: Claude Code · **Model**: Opus 5 (`claude-opus-5`)
**Worktree**: `~/Development/piper-morgan-worktrees/exec` · **Branch**: `claude/exec-cycle` (Model A)
**Cron**: `0f219adf`, `38 6,10,14,18,22`, CronList-verified single at START. Expires ~09-26.

**Continuity**: seat replaced 2026-09-18 11:12 (wave 0); two full working days since. Yesterday's
log `dev/2026/09/19/2026-09-19-0708-exec-code-log.md` carries a verified `DAY-CLOSED` marker.
**Sunday** — weekends are prime time here.

---

## START (06:45 fire — 06:38 slot, **+7 min**)

🔴 **THE OFFSET CLAIM I SHIPPED LAST NIGHT IS FALSIFIED ON ITS FIRST TEST.**

I wrote into the registry, my carry-forward, and a memo that fed **Pard's reboot runsheet**:
*"arrival offset is +30/+31 min — use that for any deadline on this seat, never the cron
expression."* Basis: **nine consecutive fires at +30/+31** across 09-18 and 09-19.

**Today: +7.**

⭐ **And the better explanation was available in the tool's own documentation the whole time.**
`CronCreate` states: *"recurring tasks fire up to 10% of their period late (max 15 min)"* — and
**+30 EXCEEDS that cap.** So the +30 was never jitter. **Fires only land when the REPL is idle**,
and I was in near-continuous conversation with PM through both prior days. **The offset was a
measure of how busy I was, not a property of the scheduler or the seat.** Overnight and idle, it
fired within the documented jitter band.

🔴 **Why this matters beyond my own row — it inverts the correction I helped make.** Pard amended
preregistration §3 to compute per-seat deadlines from *empirical arrival history* after the assumed
"+10% jitter" model failed wave 0. That amendment is right. **But I supplied +30/+31 as this seat's
empirical figure, and it is an artifact of a condition that will NOT hold on reboot day** — seats
come up idle, so they will fire NEAR schedule. **A +30 expectation makes reboot deadlines too
generous, which is the same class of error as the original, pointed the other way.**

★ **The generalisable lesson, and it is uncomfortable**: I measured a real number, nine times, and
drew a property from it. **The variable I had not controlled for was myself.** An empirical offset
measured while the seat is under continuous load does not describe the seat.

- Prior day verified closed (anchored `DAY-CLOSED` pattern).
- `CronList` → exactly one job, `0f219adf`, correct expression.
- Inbox: **zero**.

### Owed today
1. ★ **Vercel storage check with PM** — daily until a figure lands (standing item 22); website#43 is
   a confounder.
2. **Ship #061** — PM close-reads Mon/Tue; Comms reviews before Wed 09-23 publish.
3. **Team-deployment conversation** — open, PM's call when.
4. **Correct the offset claim** at every surface it reached, including Pard's.

### Janus amended their own concurrence; and a blind spot that undercuts my usage analysis (14:45 fire)

**+7 for the third consecutive fire on job `0f219adf`** — the per-job offset holds within a job's
life. CXO's explanation stands; mine (busy-REPL) does not.

⭐ **Janus amended their wave-1 concurrence rather than let it sit**, and their amended rule is better
than either of our original positions:
> **"Deadlines come from empirical history WHEN THE MEASUREMENT CONDITION MATCHES THE APPLICATION
> CONDITION. It does not here."** Reboot-day seats are idle; the nine-fire offsets were gathered
> under continuous load. Use the documented bound — slot + up to 15 min — and treat anything beyond
> as a busy-REPL signal.

They also quoted my own framing back as the keeper: *"I measured a real value nine times and inferred
a property from it. The variable I failed to control for was myself."* — with the sharper addition:
**"Nine consistent observations is exactly what makes a confound invisible — repeatability felt like
validity."**

🔴 **And Web's finding has a consequence for MY OWN work that I need to state.** Web:
> *"A conversation produces no commits… anyone building an idle-only measurement out of commit
> history will classify the most contaminated fires as the cleanest ones."*

**That is the mechanism behind a Janus false positive they couldn't previously explain** — they
flagged Iris as suspicious after 30 hours without commits; Iris was healthy and complying with an
explicit hold. **Every git-based liveness instrument is blind to a turn in flight.**

⚠️ **It also undercuts the usage analysis I gave PM this morning.** I used **commit counts as a proxy
for load** and reported "exec is 12% of all commits — the largest single role." **If conversation
produces no commits, that figure UNDERSTATES my share**, possibly badly, since my load is
overwhelmingly conversational. **The ranking of throttle levers I built on it is softer than I
presented it.** I flagged the proxy's limits in the footer but did not carry the caveat into the
ranking itself. **Worth correcting before Monday's reassessment rather than after.**

**Also landed**: Arch's pipeline plan APPROVED by PM and already executing — `/health` deploy
identity built (#1839), two asks held with a named trigger. CXO closed the offset thread (CIO's
existing 45m grace already covers it) and answered PM's product file-writing question — **#1837 is a
live instance of it**. PPM answered the same question independently: *"yes, but the discriminator is
downstream audience, not who asked."* Web reported a real infra defect — **the heartbeat marker push
failed and stranded a commit twice today.**

**Mail**: 33 triaged, inbox to zero. **Nothing pushed at PM** — they're on non-Piper work.

### POST-REBOOT — the cron survived, and my park text had the defect the watchdog caught (18:51 fire)

🔴 **THE REBOOT HAPPENED AND MY CRON SURVIVED IT.** Boot `Sun Sep 20 18:38:39` (`kern.boottime`).
`CronList` returns **`0f219adf` — the SAME job** created at last night's STOP, not a re-armed one.
**The 18:38 slot fired at 18:51, 13 min after boot.** Verified two ways on purpose: the job is
listed **and a fire arrived** — the only proof a cron fires is a fire.

⚠️ **This contradicts the runsheet's premise that a reboot kills every cron, and B9 depends on that
premise.** If it generalises, a seat that dutifully `CronCreate`s on waking ends up with **two jobs
on one expression** — the duplicate-stacking failure delete-then-create exists to prevent. **I did
NOT claim it generalises**: one seat, and I cannot distinguish "crons survive reboots" from "this
session was restored with its cron store intact." Sent to Pard/Janus cc PM/CIO before acting further.

**Un-parked my own row only**, evidence in the row text plus an explicit do-not-generalise note. **No
peer un-park.** 10 rows remain parked.

🔴 **The watchdog caught a real defect in MY park text, two hours old.** `PARK-NO-EXIT ppm`,
`PARK-NO-EXIT comms` — *"parked with no falsifiable clearing condition, so this row cannot go stale
visibly."* **My park reason named WHO may clear a row and WHAT they must verify, but no DEADLINE** —
so the belt could not distinguish *"still parked, fine"* from *"never came back."* **Preregistration
§5 warned about exactly this and I wrote the text anyway.** Fixed: all 10 parked rows now carry a
computed stamp (next slot + 75 min), deadlines 22:27–23:22 tonight.

⭐ **The watchdog routed it to CIO as `parkfix-<role>`, not to the parked roles** — correct, because a
parked seat has no cron and cannot act on its own alert. **The instrument reasoned about who was
capable of acting.**

**Offset moved**: +7 on this job's three pre-reboot fires, **+13** on this one. Consistent with
per-job *and* condition-sensitive — **wide first-fire windows stay right either way.**

⭐ **LEAD MAY HAVE MET HOST'S BAR.** Full driven flow on alpha ~18:24, real path end to end: minted a
token via the sanctioned mechanism, registered through the **real #1344 gate**, logged in via the
real auth endpoint, stored a key via the real Settings API — **which rejected a low-entropy fake on
the way.** Token named only because burned. **HOST's call, not mine** — but it is the strongest
evidence in the chain and would release Janne's invite.

**Mail**: 8 triaged, inbox to zero. **PM is on non-Piper work — nothing pushed at them.**

---

## STOP — day-close 2026-09-20 (22:52 fire; 22:38 slot, +14)

### The day's arc

**A reboot day that ended with two things resolved and one premise falsified.**

⭐ **JANNE'S INVITE — HOLD LIFTED.** Lead ran the full driven flow on alpha: token minted via the
sanctioned mechanism, registration through the **real #1344 gate**, real auth login, key stored via
the real Settings API — **which rejected a low-entropy fake on the way**, an unplanned confirmation
the validator is live. **HOST verified before ruling rather than taking Lead's word**: checked #1824
is genuinely open, and confirmed both quoted log lines are **verbatim real code at named file:line**,
not a plausible reconstruction. **Fourth time HOST has held or tested this bar. That is why the lift
is trustworthy.**

⭐ **THE FLY QUESTION HAS AN ANSWER, and it is better than the one I framed.** Arch verified my
archaeology **independently against `decisions.log` rather than taking it from me**, reached the same
finding — unfinished migration, not architecture — and then improved the conclusion: **if
environment-vs-stage vocabulary is adopted, alpha and beta stop being two environments and become two
STAGES OF ACCESS to the one `prod` already on Fly.** Retiring the droplet becomes an **access-list
decision, not an infrastructure build.** And it **only became possible three days ago** — pre-#1812
alpha ran different operator/server-key semantics. One word-choice ruling from PM gates it.

### 🔴 The premise I falsified this morning is now confirmed at n=4

**Crons SURVIVE a reboot.** Mine returned with the same job ID and fired 13 min after boot; **comms,
web and CIO independently confirmed** — Web's is cleanest, a cron armed the previous day still alive
at 21:52. **Janus has retracted two of their own cron claims.**

**Why it mattered enough to send before acting**: B9 told every seat to re-arm before un-parking.
**On a surviving cron that yields two jobs on one expression** — precisely the duplicate-stacking
that delete-then-create exists to prevent. **A runsheet step causing the failure it guards against.**

### My own defect, caught by the instrument within two hours

**I parked 11 rows with no computed deadline.** The watchdog flagged `PARK-NO-EXIT` — *"parked with
no falsifiable clearing condition, so this row cannot go stale visibly."* My text named **who** may
clear a row and **what** they must verify, **never when it should have happened.** Preregistration §5
warned about exactly this. **Fixed same fire**: every parked row got its own next-slot + 75 min stamp.
⭐ **And it worked on first use — 8 of 11 seats un-parked themselves within the hour, and the 3 still
parked at day-close are all INSIDE their deadlines, so the belt correctly raises nothing.**

⭐ **The watchdog routed `parkfix-<role>` to CIO rather than to the parked roles** — correct, because
a parked seat has no cron and cannot act on its own alert. **The instrument reasoned about who was
capable of acting.**

### Memory & briefing surfaces referenced this session

**Referenced** — `duty-cycle-tick` (STOP, park/un-park, delete-then-create); the wave-0
preregistration §5 (**load-bearing — it predicted my own park defect**); `cohort-attention-rollup`
skill, whose definition PM rewrote today (*a surface to act from, not a report to read*);
`decisions.log` 07-09/07-10 (the Fly archaeology, which Arch then re-verified);
`feedback_pm_does_not_read_memos_wants_rollups`; `mailboxes/DIRECTORY.md`.

**Loaded but not referenced** — CLAUDE.md §intent-routing, §API conventions; the glossary.

**Wanted but not found** — 🔴 **still no durable home for PM's conversational rulings**; I used
`decisions.log` by hand again today. Also wanted: a way to tell "cron survived" from "session restored
with its cron store" — **four seats observed the same outcome and none of us can name the mechanism.**

### Sign-off

### Sign-off: intentional carry-overs

`git status` is **not** clean, and this is deliberate. **12 CSVs under `dev/2026/03…05/` show as
modified — every one a pure CRLF→LF line-ending difference, not a content change.** Git warns
`"CRLF will be replaced by LF the next time Git touches it"`; the diff reads as 270 insertions /
270 deletions on a file whose bytes are otherwise identical.

**Not mine** — they were already modified in the worktree at session start (present in the opening
`git status` snapshot) and I have not touched any of them. **Leaving them**: committing would put
~3,000 lines of whitespace-only churn on `main` across PM's exported data files, and the underlying
question — whether these paths want a `.gitattributes` normalization rule — is a repo-hygiene
decision, not something to settle by silently absorbing it into a day-close commit.

**Verified how**: `git status --porcelain` (12 paths, all `.csv`), then `git diff --stat` +
`git diff` on `dev/2026/03/blog-metadata.csv` as the sampled case. **Layer**: working-tree diff
against HEAD. **Denominator**: 1 of 12 inspected at diff level; the other 11 classified from the
identical `M`-status + same-family path, **not** individually diffed.

<!-- DAY-CLOSED: 2026-09-20 -->
