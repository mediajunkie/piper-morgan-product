---
role: Chief Experience Officer (CXO)
slug: cxo-code
model: Opus 5
date: 2026-09-20
worktree: ~/Development/piper-morgan-worktrees/cxo (Model A)
branch: claude/cxo-cycle
cron: 47 6,9,12,15,18,21 * * * (job 23d4c124; armed 2026-09-19 22:2x, expires ~2026-09-26)
---

# CXO session log — 2026-09-20

## START (06:59)

✅ **Prior day verified CLOSED first** — `<!-- DAY-CLOSED: 2026-09-19 -->` present in
`dev/2026/09/19/2026-09-19-0717-cxo-code-log.md`. **No missed close to run.**
✅ **Cron single**: `23d4c124`, correct expression — **the job I armed at last night's STOP survived
the night.** ⭐ **Arm-date is recorded and verified this time** (09-19 22:2x, expires ~09-26), which
closes the gap that stood open all of yesterday.

### ⏱️ The +30 offset BROKE this morning — and that falsifies what I reported to Pard

**Yesterday: five fires, `09:47→10:17`, `12:47→13:17`, `15:47→16:17`, `18:47→19:17`, `21:47→22:17` —
exactly +30, no drift.** 🔴 **Today: `06:47→06:59`. +12.**

⭐ **So "a constant +30 delivery offset" is falsified as a standing property**, and I reported that
pattern to Pard last night. **Correcting it this fire rather than letting a false lead sit in their
queue** — a correction that isn't delivered hasn't happened.

📄 **And the mechanism is documented in the `CronCreate` tool description itself**, which I should have
read before escalating: *"The scheduler adds a small **deterministic** jitter on top of whatever you
pick: recurring tasks fire up to 10% of their period late (max 15 min)"* and *"Jobs only fire while the
REPL is idle (not mid-query)."*

**That explains both observations without a scheduler defect:**
- **Deterministic** jitter is per-job, so one job gives the *same* offset every fire — which is exactly
  the "exactly +30, no drift" I found remarkable. ⭐ **The consistency was the determinism, not a
  systemic offset.**
- **I rotated the job at last night's STOP** (`7bb7c53a` → `23d4c124`). **New job → new deterministic
  jitter → different offset.** **+30 → +12 is what that predicts.**
- The portion beyond the 15-minute cap is **idle-only delivery**: yesterday I was working continuously
  across fires, so each fire waited for the REPL.

⚠️ **What I got wrong, precisely**: I treated a stable number as evidence of a systemic property, when
stability was the *expected* behavior of a documented per-job mechanism. 🔴 **I checked whether the
pattern was cohort-wide (it was confounded, and I said so) but never checked whether the pattern was
EXPECTED.** ⭐ **Denominator discipline on the cohort axis, none on the "is this already documented"
axis** — and the answer was in the tool description I invoke every fire.

**Queue** — draining now; entries follow per work unit.

### 🔴 The START re-verify found THREE stale rows, and one had reached PM as a deadline

⭐ **This is the step's whole justification, and it has never paid out like this before.** Checked
gated rows against GitHub rather than re-reading them, per the mandatory START re-verify.

**1. The T-axis "closing window" was FALSE — and it was framing a live PM decision.**
📄 Exec relayed to PM: *"the probe window closes when #1688's MCP arm starts writing output… spend
the re-run before it closes, or accept that ratified law cites a gate that can't gate."* 🔴 **That
urgency came from MY row's wording.** ✅ **Verified this fire**: `services/mcp/` is `client.py` +
`consumer/` + `protocol/` — **entirely consumer-side, no served server, nothing scaffolded**; #1688's
own 09-04 comment says *"none of increment 1's requirements exists"*; **#1688 closed 09-15 with the
MCP half blocked on that infra.** ⭐ **Nothing is racing. PM was choosing against a clock that doesn't
exist.** **The decision is still real — it just isn't timed.**

**2. The #1688 flag has been ON IN PROD since 09-07, and I had it wrong TWO different ways.**
📄 The 09-08 comment: *"**PM OVERRULED the hold** — EXECUTED same evening. **v69 deployed**…
**`PIPER_FTUX_INTERVIEW=1` set and VERIFIED in the running env**… **LIVE on the hosted app**."*
❌ My row's *"OFF by a ruling"* **conflated code default with running env** and cited a hold PM had
overruled four days earlier. ❌ **My own amendment last night** — *"ABSENT, the build predates it"* —
**was also wrong: v69 deployed 09-07, after the flag landed 09-03.**
🔴 **So I corrected a stale row into a differently-wrong row and shipped that to Exec inside an answer
PM had asked for.** ⭐ **Twice on one row I reasoned from a document instead of opening the issue —
which my own tracker names as my #1 error mode, in those words.**

**3. #1807 is CLOSED** while my row still read *"TIME-CRITICAL, lane closing now, blocked on Lead."*

**Consequence for yesterday's answer to Exec**: instance ② was **not** candidate B — **zero of three
now are**, which weakens the deploy gap's claim on my complaint *further* than my correction of Exec
did. ⚠️ **And one contradiction I flagged rather than resolved**: *"v69 deployed 09-07"* vs **Lead's
"no droplet deploy since July."** **Both in front of me; I won't pick the half that suits my memo.**
Corrections sent to Exec + PA, cc PM/Lead/Web (`c76ede217`). **All three rows fixed; guards clean.**

⭐ **The pattern, stated once**: all three are the same shape — **I trusted my own prior record over
the source.** **Nothing but the START re-verify would have caught it**, and I've re-checked every row
citing an issue state rather than fixing only the ones that bit.

### 🔴 #1818 — Arch's predicate was wrong, and the exempt set is incoherent as first contact

**Arch self-corrected before Lead's ratchet could encode it**: `CANONICAL` means *"the LLM can't do
this alone"* — **it returns True for ALL of EXECUTION and PORTFOLIO**, which are canonical *because*
they write to the DB and create issues. ⭐ **Implemented literally, the ruling would have opened a
keyless path to issue creation.**

**Arch wrote *"CXO, your build requirement is unaffected."*** ✅ **Audited the credit rather than
banking it** — they're right (my trigger rides on predicate A, untouched) — 🔴 **but auditing it
surfaced something neither memo raises.**

**The ratchet defines the spends-nothing set mechanically. It cannot say whether that set is coherent
to a user.** Verified in source: **`hi` passes; `Hi, how do I address you?` is REFUSED** (compound
greeting → `_requires_canonical_handler` returns False → floor → spends); **`thanks` and `bye` are
REFUSED** (marked `CANONICAL` in the registry but **no branch in the routing authority** — I read the
whole function — which is #1773's drift landing on first contact).

⭐ **So first contact forks on whether the user appended a clause, and the terser, less natural opener
is the one we reward.** **A user models "pleasantries," not dispositions.** 🟡 **Also flagged that
#1818's founding premise — *"a greeting spends nothing"* — is unverified: the routing authority's own
stated reason for greeting being canonical is *"has onboarding/calendar side effects,"* which is not a
cost claim.** ⚠️ **Not refuting it — asking that the ratchet list it as a question it answers rather
than a premise it assumes.** Sent to Lead + Arch before the ratchet lands (`6d3713142`).

### ⏱️ The +30 offset — corrected before the cohort bakes it in

**CIO had already moved their registry `first_fire` 10:07 → 10:37 on Pard's confirmed +30, and flagged
it for others.** 🔴 **My +12 this morning is disconfirming**, and the one thing that changed is that I
**rotated my job at last night's STOP**. ⭐ **A deterministic per-job jitter explains every observation
— including why it felt environmental — and predicts the number moves on rotation, which is what
happened.** ⚠️ **Stated the part that does NOT fit: +30 exceeds the documented 15-min cap, so I can
show the number isn't stable across a rotation but cannot give the mechanism.** 📌 **The concrete risk:
`first_fire` feeds the watchdog's should-be-cycling gate, every job rotates within 7 days, and a tuned
constant goes stale silently.** **Suggested a tolerance over a tuned constant — structural vs promise,
applied to our own telemetry.** ✅ **Re-checking at 09:47 and reporting either way.**

### Queue state

- **Mail**: **4 drained to zero** — 2 direct read in full (Arch's URGENT correction, Lead's interim
  trace), 2 cc read for asks (CIO's hook ack — which is what surfaced the +30 propagation; CIO's T1
  reflection).
- **Standing-items**: **18 rows**, three corrected this fire. Both guards clean.
- **GitHub criteria line**: **denominator 2** — #1174, #1108, both last-updated 09-11, unchanged.

**Discovered issues filed**: None.
