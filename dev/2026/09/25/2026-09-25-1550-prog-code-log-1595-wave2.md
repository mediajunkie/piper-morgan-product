# Session Log — prog (Coding Agent) — 2026-09-25 15:50

**Role**: prog
**Model**: Sonnet 5
**Dispatched by**: Lead Developer, for #1595 unit 1 (wave 2: read_temporal flip-group)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Note**: Not committing — Lead reviews and commits.

## Task
Wave 2, `read_temporal`: grouping ONLY (no flag/env/Fly/deploy changes).
1. Add "read_temporal" to FLIP_GROUPS with rationale.
2. Assign flip_group="read_temporal" to changes_query entry + calendar cohort (12 rail keys total).
3. Do NOT touch strategic cohort (unit 2).
4. Re-run audit gate — expect 84/93 grouped.
5. Add wave-2 pinning tests.
6. Update docs (intent-routing-stack.md, inversion_live.py docstring).
7. ruff format/check, run test suites + sweep gates.

## Progress
- Read intent-routing-stack.md (Phase 2.2 flip-1 + #1667 flip-group sections), the Lead's
  epic-0 scope doc, FLIP_GROUPS block + WorkflowEntry.__post_init__ in workflow_dispatcher.py,
  and _READ_QUERY_FLIP_GROUPS / _CALENDAR_QUERY_COHORT / changes_query entry in workflow_entries.py.
- Ran baseline audit: 72/93 grouped, 21 unassigned (17 no-group-no-category incl. 8 strategic +
  9 temporal, 4 category-only-b all temporal).
- Added "read_temporal" to FLIP_GROUPS (workflow_dispatcher.py) with rationale comment.
- Assigned flip_group="read_temporal" to changes_query_entry (workflow_entries.py ~1341).
- Added `_CALENDAR_QUERY_FLIP_GROUPS` map (mirrors `_READ_QUERY_FLIP_GROUPS`) and wired it into
  the calendar cohort registration loop.
- Confirmed all touched entries are effect=EffectClass.READ (per existing per-cohort comments,
  re-verified nothing changed).
- Re-ran audit: 85/93 grouped, unassigned = exactly the 8 strategic ops, 0 category-only
  leftovers. NOTE: dispatch prompt estimated 84/93 grouped and "12 rail keys" — actual is
  85/93 and 13 rail keys (the calendar cohort's 9 aliases + changes_query's 4 aliases = 13,
  not 12 — the prompt's own listed calendar-cohort items already summed to 9). Reported as
  discrepancy, not silently corrected to match the estimate.
- Updated tests/unit/services/intent_service/test_inversion_flip_groups_1667.py:
  - test_wave_1_vocabulary: grew FLIP_GROUPS assertion to include read_temporal (necessarily —
    it's the closed-vocabulary pin).
  - test_wave_1_assignments_by_class: removed changes_query/week_calendar from the
    "deliberately ungrouped" list (now grouped); left the 4 strategic ops.
  - test_ungrouped_op_with_a_category_is_still_swept_by_that_category: week_calendar no longer
    fits this scenario (0 category-only-ungrouped ops remain after wave 2) — rewrote to inject
    a synthetic WorkflowEntry + ACTION_REGISTRY row via monkeypatch.setitem so the general
    property (ungrouped+categorized READ op still swept by category) stays pinned independent
    of which real op happens to be in that state. Added a companion test asserting the b-list
    is empty over the real registry (states the wave-2 design outcome directly).
  - Added TestWaveTwoReadTemporal: (a) all 13 real read_temporal keys assigned, (b) READ-only
    invariant re-asserted for the new group, (c) exact-membership check, flag-parsing
    recognizes "read_temporal" via live_categories()/resolve_live_match, typo
    "read_temporl" reported by unrecognized_flag_tokens, and an e2e group-flip consult test
    for meeting_time (same shape as the wave-1 group-flip pin).
- ruff format + ruff check --fix on workflow_dispatcher.py, workflow_entries.py,
  inversion_live.py, and the test file — all clean.
- Updated docs: intent-routing-stack.md's "Wave-1 groups:" paragraph gained a wave-2
  paragraph naming read_temporal, the two cohorts, #1887, and that this unit is grouping-only
  (flag stays unset pending PM-gated shadow score). inversion_live.py's module docstring's
  "THREE NAMING SURFACES" bullet updated to name read_temporal.
- Test run: tests/unit/services/intent_service/test_inversion_flip_groups_1667.py — 40 passed
  (33 baseline + 7 new).
- Test run: tests/unit/services/intent_service/ (full dir) — 4761 passed, 16 warnings, exit 0.
- Test run: tests/test_architecture_enforcement.py — 63 passed, 1 xfailed, exit 0.
- Sweep gate (`scripts/run-sweep.sh ratchets`) completed exit 0: 73 passed, 1 xfailed
  (completion ratchets + architecture enforcement); mypy gate (#1436) — all 24 ratcheted codes
  at ceiling, total=1121, no code moved. No typing fix needed.

## Final state
- `git diff --stat`: 5 files changed, 210 insertions(+), 26 deletions(-) — intent-routing-stack.md,
  inversion_live.py, workflow_dispatcher.py, workflow_entries.py, test_inversion_flip_groups_1667.py.
- No flag/env/Fly config/deploy file touched (confirmed via `git diff --name-only`).
- Final audit: 85/93 grouped (was 72/93), read_temporal=13/93, unassigned=8 (exactly the
  strategic cohort), 0 category-only leftovers (was 4).

## Handed back to Lead
Work complete. Reported via SubagentHandback with full diff stat, audit table, gate output,
and the 84/12 vs 85/13 discrepancy from the dispatch prompt's estimate.

## Discovered work
None filed — the estimate discrepancy (84→85, 12→13) is a documentation nit in the dispatch
prompt/scope doc's own arithmetic, not a code or process defect; flagged in the handback for
the Lead to correct in the scope doc if desired, not filed as a tracking issue.
