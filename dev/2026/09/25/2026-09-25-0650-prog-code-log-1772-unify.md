# 2026-09-25 0650 — prog (Coding Agent), Sonnet 5 — #1772 unify N=1 onto aggregate site

Role: prog (Coding Agent). Dispatched by Lead Developer, from the Lead's worktree
(`/Users/xian/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle`).
Model: Sonnet 5. Do NOT commit/stage — Lead reviews and commits.

## Task

#1772: implement Arch's mechanism ruling (unify N=1 onto the same aggregate
composition site as N>=2, removing the `len(_failed_sources) == 1` special case
in `conversational_floor.py`) + CXO's copy ruling (N-agnostic rewrite of the
aggregate wording, binding, verbatim). Both rulings read in full before starting:
- `mailboxes/lead/read/rule-arch-to-lead-cxo-cc-ppm-1772-mechanism-unify-n1-onto-the-aggregate-site-2026-09-24.md`
- `mailboxes/lead/read/rule-cxo-to-lead-arch-cc-ppm-1772-not-acceptable-as-is-n-agnostic-rewrite-attached-2026-09-24.md`

Also read `docs/internal/architecture/current/intent-routing-stack.md` (mandatory
pre-read for floor/chat-response work) and the two measurement docs
(`dev/2026/09/24/1772-candidate-measurement-2026-09-24.md`,
`dev/2026/09/15/1772-scope-leak-measurement.md`) for context.

## What changed

**`services/intent_service/conversational_floor.py`**:
- Removed the `len(_failed_sources) == 1` branch at the #1717 composition site
  (~line 1390-1417). Every N >= 1 now renders through the ONE aggregate directive,
  using CXO's N-agnostic wording pinned verbatim:
  `- DATA CHECKS FAILED this turn — could not check: {list}. If this becomes
  relevant, name what wasn't checked in ONE sentence — never one caveat per item.
  Don't claim it's empty or fine, and never invent details to fill the gap.`
  The wrinkle-1 scope line immediately after is byte-identical, untouched.
- `SourceFailedDirective` NamedTuple: removed the `directive` field (now just
  `flag` + `check_name`). Removed the five per-source `directive` strings from
  the `SOURCE_FAILED_FLAGS` registry entries; kept the per-entry provenance
  comments (#1425/#1536/#1645/#1573).
- Updated the registry header comment (contract bullets, ~line 120-149) and the
  composition-site comment to state the unified rule and cite the measurement
  (verbatim N=1: 5/10 then 2/10 leaks on anthropic; aggregate shape: 0/25 across
  both providers).

**Tests updated** (found via `git grep -n "\.directive\|DIRECTIVES\|Reminder
check FAILED\|DATA CHECKS FAILED" -- tests`, plus a broader sweep for the other
four per-source strings — the task's file list missed two live consumers,
`test_projects_lane_honesty_1645.py` and `test_todo_scope_framing_1544.py`,
found by grepping the literal old-copy strings across `tests/` after the named
files were green):
- `test_source_failed_composition_1717.py` — rewritten: `TestSingleFailureUnchanged`
  → `TestSingleFailureUsesAggregate` (N=1 now asserts the aggregate line with
  the single check_name, no other check_name leaking in, full sentence pinned
  verbatim via new `AGGREGATE_TAIL` constant); `test_aggregate_copy_absent_at_single_failure`
  inverted to `test_aggregate_copy_present_at_single_failure`; `AGGREGATE_PREFIX`
  kept; added `test_full_aggregate_sentence_verbatim_at_n_equals_one` pinning the
  whole new sentence (8/21 lesson). Removed the `DIRECTIVES` dict (no more
  `.directive` field to build it from).
- `test_source_failed_registry_1717.py` — docstring updated; removed the
  `directive`-list assertions from `test_registry_entries_pin_the_conventions`
  (field no longer exists).
- `test_sentinel_consumer_sweep_1777.py` — `test_the_flag_renders_as_an_honest_degrade_not_an_empty_list`
  now asserts the aggregate marker + check_name instead of `.directive`.
- `test_reminder_surfacing_1566.py`, `test_reminder_vocabulary_render_1569.py`,
  `test_context_assembler.py`, `test_projects_lane_honesty_1645.py` (two classes:
  projects + completed-todos), `test_todo_scope_framing_1544.py` — each had a
  literal old per-source string ("Reminder/Todo/Project/Completed-todo check
  FAILED") asserted at N=1; all six updated to assert the new aggregate line.

**Docs**:
- `docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md` §4 —
  added a dated note (2026-09-25, #1772) and changed "When more than one
  reportable failure exists" to "When one or more reportable failures exist",
  explaining the N==1 carve-out that used to sit outside this section is gone.
- `docs/internal/architecture/current/intent-routing-stack.md` — checked via
  grep (`N=1|N≥2|_failed_sources|SOURCE_FAILED|1717|source_failed|DATA CHECKS
  FAILED`); the doc does NOT describe the N=1/N≥2 split anywhere, so no update
  needed there.

## Verification

- `venv/bin/python -m pytest tests/unit/services/intent_service/test_source_failed_composition_1717.py tests/unit/services/intent_service/test_source_failed_registry_1717.py tests/unit/services/intent_service/test_sentinel_consumer_sweep_1777.py tests/unit/services/intent_service/test_reminder_surfacing_1566.py tests/unit/services/intent_service/test_reminder_vocabulary_render_1569.py -q -p no:cacheprovider` → 71 passed.
- Full `tests/unit/services/intent_service/` sweep found two more live consumers
  (`test_context_assembler.py::test_floor_renders_todo_check_failed_line`,
  then after fixing that, the broader grep found `test_projects_lane_honesty_1645.py`
  x2 and `test_todo_scope_framing_1544.py` x1) — fixed each, then reran the
  whole directory: **4746 passed, 15 warnings** (warnings pre-existing/unrelated
  — asyncio marker mismatches, unclosed client sessions in unrelated tests).
- `tests/test_architecture_enforcement.py`: **63 passed, 1 xfailed** (pre-existing
  xfail, unrelated to this change).
- Rendered a real N=1 and N=3 output via a scratch script calling the actual
  `ConversationalFloor._format_domain_context` — both match CXO's binding
  wording verbatim, wrinkle-1 line byte-identical. See handback report for the
  literal text.
- `scripts/run-sweep.sh ratchets` — launched in background, full output +
  gate-line grep will be in the handback report to Lead.

## Verified how

Method: ran the exact pytest/sweep commands and read their full terminal output
in this session (quoted above and in the handback). Layer: unit-test assertions
against the real renderer (`ConversationalFloor._format_domain_context` /
`_build_prompt`) plus a standalone scratch-script call to the same method —
deterministic-renderer layer, NOT a live LLM completion (no live calls were made
per the dispatch's instruction #5). Denominator: 4746/4746 intent_service unit
tests green, 63/63 (+1 xfail) architecture-enforcement tests green, all touched
test files re-verified individually before the full-directory run.

## Wrap

`git diff --stat` (10 files, 206 insertions / 133 deletions):
```
 docs/internal/design/gather-outcome-user-facing-contract-2026-09-09.md      |  21 +++-
 services/intent_service/conversational_floor.py                            |  85 +++++++-------
 tests/unit/services/intent_service/test_context_assembler.py               |  10 +-
 tests/unit/services/intent_service/test_projects_lane_honesty_1645.py      |  22 ++--
 tests/unit/services/intent_service/test_reminder_surfacing_1566.py         |  11 +-
 tests/unit/services/intent_service/test_reminder_vocabulary_render_1569.py |   5 +-
 tests/unit/services/intent_service/test_sentinel_consumer_sweep_1777.py    |  11 +-
 tests/unit/services/intent_service/test_source_failed_composition_1717.py  | 125 +++++++++++++--------
 tests/unit/services/intent_service/test_source_failed_registry_1717.py     |  42 ++++---
 tests/unit/services/intent_service/test_todo_scope_framing_1544.py         |   7 +-
```

Handed back to Lead Developer via SubagentHandback with the full report (diff
summary, N=1/N=3 rendered lines, ratchet-sweep gate lines, test counts,
Verified-how line). No commit/stage performed per dispatch instructions —
Lead reviews and commits. No discovered-work issues filed (none found outside
the assigned scope).

## Memory & briefing surfaces referenced this session

- Referenced: `docs/internal/architecture/current/intent-routing-stack.md`
  (mandatory pre-read; confirmed it does not describe the N=1/N≥2 split, so
  no doc update was owed there) — informed scope of doc updates.
- Loaded but not referenced: none beyond the task's named surfaces.
- Wanted but not found: n/a.
