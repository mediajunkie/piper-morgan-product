# Session Log — prog (Coding Agent), #1595 unit 3b

**Header**: role=prog, tool=code, model=Sonnet, dispatched by=Lead Developer, date=2026-09-25, start=18:55 PDT
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle) — per instructions, NOT committing/staging/touching git index. All work is file edits only; Lead will commit.

## Task

Unit 3b of #1595: put the clear-reminders family's DESTRUCTIVE operation on the #1677
`FLIP_WRITE_ALLOWLIST`, per Arch's floor ruling (Q1: FLOOR not ceiling — a DESTRUCTIVE op may enter
the allowlist individually-verified, same as WRITE), including Arch's one build-time condition:
confirm-prompt provenance must match legacy dispatch's slot-extraction path.

No LLM calls. No flag/env/Fly/deploy changes. No git commits (Lead's job).

## Reading (before work)

- `mailboxes/lead/read/rule-arch-to-lead-cc-ppm-pm-1595-...md` — Arch's Q1/Q2 ruling (read, captured above)
- `docs/internal/architecture/current/intent-routing-stack.md` — #1677 entry + create_reminder paragraph
- `create_reminder` allowlist commit precedent: `git show a3180731c4`
- #1606 (`gh issue view 1606 --comments`)
- #1605 clear-family design in `workflow_entries.py` / `todo_handlers.py`

## Work log

### Determined the operation to allowlist

Checked `ACTION_REGISTRY`/grammar via `venv/bin/python scripts/inversion_phase1_shadow_score.py --dry-run`
(`delete_todo [rail] aliases=['remove_todo', 'cancel_todo', 'delete_reminder', 'remove_reminder',
'cancel_reminder']`) and today's shadow report
`docs/internal/architecture/current/inversion-phase1-shadow-score-2026-09-25.md`:
- `please clear the reminders except for "Review the PR" -` → `delete_todo` @0.9 (issue-1606 row)
- `delete my hydrate reminder` → `delete_todo` @0.9 (grammar-fidelity row)

`clear_reminders_delete` (the #1605 either/or DESTRUCTIVE leg) is offer-only
(`action_triggered=False`) and structurally unreachable by a fresh classification — only
`delete_todo` is a name the router can draw for this shape. Allowlisted `delete_todo`.

### Code changes

- `services/intent_service/workflow_dispatcher.py`: `FLIP_WRITE_ALLOWLIST` now
  `{"create_todo", "create_reminder", "delete_todo"}`, with a full three-condition + build-time
  condition comment block for `delete_todo` beside the constant.
- `services/intent_service/workflow_entries.py`: `delete_todo_entry` gains
  `flip_write_allowlist_key="delete_todo"` + matching comment (registered / effect-by-behavior /
  reaches consent+confirm / the confirm-provenance proof pointer).
- No changes needed in `inversion_live.py` — `_effect_guard_passes`/`flip_write_allowed` are already
  generic over effect tier (checks `effect == READ` or allowlist membership); DESTRUCTIVE needed no
  special-casing, confirmed by reading the guard before touching anything.

### Confirm-provenance proof (Arch's build-time condition)

Traced: `consult_inversion_live` REPLACES the classifier draw for the turn — the SAME `intent`
variable set at `intent_service.py:2269` (inversion) or `:2294` (legacy `classify_multiple`) flows
into the identical `#1190` gate block (`intent_service.py` ~L2900-3020), which calls
`build_todo_delete_confirmation` unconditionally on provenance. That function resolves its title via
`intent.context.get("original_message")` → falls back to `intent.original_message`
(`destructive_confirm.py:508-512`); both `consult_inversion_live` (`original_message=message`,
`inversion_live.py:595`) and the legacy classifier set the raw user text there. Wrote
`TestConfirmProvenanceParity` in the new test file: two tests build "delete my hydrate reminder"
through each provenance independently (both assert `== 'Delete todo: "hydrate"? (yes/no)'`), plus a
third self-contained test that runs both legs inside one test and compares the two strings directly
for equality — all three pass.

### Existing test fixed (pre-existing assumption invalidated)

`test_inversion_write_allowlist_1677.py::TestDispatchGuard::test_unallowlisted_destructive_still_cannot_flip`
used `delete_todo` as its "nearest miss" example of a registered-but-unallowlisted DESTRUCTIVE op —
now false since this unit allowlists it. Swapped the example to `close_issue` (DESTRUCTIVE,
registered, unallowlisted) and updated the docstring to say why.

### Closed-set / denominator updates

`test_inversion_write_allowlist_1677.py`: updated `test_allowlist_is_exactly_create_todo_and_create_reminder`
(renamed to include delete_todo) and `test_no_other_rail_entry_declares_a_key` (denominator now THREE
entry objects / 12 alias keys) to include the delete_todo alias family and assert pairwise distinctness.

### New test file

`tests/unit/services/intent_service/test_inversion_write_allowlist_delete_todo_1606.py` (mirrors
`test_inversion_write_allowlist_create_reminder_1559.py` structurally, adapted for DESTRUCTIVE):
TestEntryDeclaration (+ needs_confirm assertion), TestDispatchGuard (named flip / category sweep /
sub-threshold / unnamed-doesn't-flip / default-empty-dark), TestFlippedTurnReachesTheRail (arms the
confirm, deletes nothing on the classifying turn, a real "yes" then completes the same flow;
consent-gate spy asserting effect=DESTRUCTIVE + verdict reached), TestRouterChoiceSet (grammar name),
TestConfirmProvenanceParity (the build-time proof). 18 tests total in the new file, all passing.

### Docs

Added a paragraph under the #1677 entry in `intent-routing-stack.md` (delete_todo allowlisted
2026-09-25, unit 3b, Arch's floor ruling, confirm-provenance proof, flag unset, pins).

### Gates (all green — see handback for quoted lines + exit codes)

`ruff format` + `ruff check --fix` (clean); `tests/unit/services/intent_service/` full (4795 passed);
`tests/test_architecture_enforcement.py` (63 passed, 1 xfailed); `scripts/run-sweep.sh ratchets`
(73 passed, 1 xfailed + mypy gate "all 24 ratcheted codes at ceiling"). `--audit`'s NAMED-WRITE
ALLOWLIST line now prints `['create_reminder', 'create_todo', 'delete_todo']`.

No LLM calls made. No flag/env/Fly/deploy changes. Did not commit/stage/touch the git index per
instructions — handing back to Lead for review + commit.
