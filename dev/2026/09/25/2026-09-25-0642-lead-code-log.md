# Lead Developer — session log 2026-09-25

**Role**: Lead Developer (lead-code) · **Model**: Fable 5.1 (claude-fable-5-1, from the harness's own statement) · **Host**: Amber, Model A worktree `~/Development/piper-morgan-worktrees/lead` on `claude/lead-cycle` · **Cron**: recurring `470fd4e1` (`17 6,9,12,15,18,21 * * *`, armed 09-23 21:2x, expires ~09-30, rotate ~09-28)

## START — 06:17 fire (arrived 06:42)
- Step 0: prior STOP ran (DAY-CLOSED 2026-09-24 in yesterday's log, 20:5x; 21:17 fire found it closed).
- Sync: 0 behind after merge; 63 commits on main since yesterday's last fire (Docs' 09-23+09-24
  omnibus gap closed; Exec/PPM/CIO/CXO/Docs STOPs with cron re-arms; Pard's seat loaded; Web START).
- Collision check: dir `lead` ↔ `claude/lead-cycle` (Model A — permanent pairing, not a gate).
- Inbox: 4 memos (Web closing cold-cache, HOST 1845 lint review, Arch 1772 rule, CXO 1772 rule).
- Carry-forward read: `dev/active/lead-carry-forward.md` (refreshed at STOP 09-24).

## Fire 1 (06:17, arrived 06:42) — drain
- 06:4x — memos read (4). Web #1859 closing: informational, no action. Arch + CXO ruled on #1772
  (unify N=1 onto the aggregate site; CXO's N-agnostic string binding) → dispatched a Coding Agent
  subagent, **Sonnet**, to implement + update the 1717 tests + contract §4 (no live completions).
- 06:49 — **HOST's 1845 second review**: the lowercase-token gap fixed in
  `scripts/mailbox_bearer_lint.py` (IGNORECASE + mixed-case / all-hex / grouped-lowercase
  rejections — the naive pass matched hyphenated prose 357×), 22 matcher cases pinned in
  `tests/unit/scripts/test_mailbox_bearer_lint_1845.py` (`81ff27ea7d`). The pass found HOST's own
  memo quoting the dead example token in full → masked in all three copies (`5323c3ce3`).
  **Main's Code Quality had been red since 22:10 PT** (first run after HOST's push; gate correct, 3
  hits) through ~35 pushes from seven seats — nobody looked. Filed **#1892** (every START should read
  main's gating-workflow conclusions); memo to HOST cc Exec/CIO. Correction to my own 09-24 #1845
  closure: I verified "lint live in CI" by config presence; the behavioral proof arrived overnight.
- 06:57 — **#1772 LANDED** (`422d32f1db`, Sonnet lane, Lead-reviewed: diff read line by line;
  8 pinned test files + enforcement suite re-run by me — 74 pass, 1 pre-existing isolation failure,
  see #1893): every N≥1 renders the aggregate line with CXO's N-agnostic wording; registry loses the
  `directive` field; contract §4 updated. Own miss: my verify chain piped pytest into `tail`, so the
  failure didn't gate the push — bisected after (fails identically at HEAD~1 alone; passes in the
  full run CI uses). Rule for the rest of the day: `pytest …; test ${pipestatus[1]} -eq 0`.
  Measurement of the new string still owed (needs ~20 completions from PM). Deploy → v135 running.
- 06:57 — **#1731 CLOSED, mechanism found**: sandbox repro (bare remote + two clones) shows
  the #1310 reconcile restores a sent tracked path to local HEAD (pre-send content), so a same-fire
  re-edit or re-pass silently REVERTS the earlier send on origin — PPM's "unchanged from before the
  call" was a revert. Fix = stale-base guard in `mail-send.sh` (`fe92cd9aaa`): refuse a present path
  whose origin copy moved past HEAD unless the worktree already holds origin's content. 7-sequence
  probe kept at `dev/2026/09/25/1731-stale-base-guard-probe.sh` (also covers the cross-agent
  clobber). Tried fixing the reconcile instead: staging the pushed blob is clean on ff but git
  refuses a true merge with staged entries, and every role branch has local commits.
- Filed: #1892 (red main unwatched), #1893 (test isolation).
- 07:02 — **Deployed Fly v135 (complete)** = origin/main `1cce6c645d` (health 0.8.14.0, git_sha verified):
  #1772 unified composition + CXO's string live; #1893 fix (`5f86b65891`) landed after the cut —
  rides the next deploy (test-only impact). Code Quality **green on main again** (`f2185f3cfd`
  success at 14:00Z) after three ruff-format fixes on lane output — standing rule added: `ruff
  format --check` on staged .py before committing lane work.
- 07:02 — **#1893 CLOSED at cause**: `register_default_workflows` skipped against a
  name-bound `get_registered_workflows` that a test patch could freeze at lazy import; now skips
  against `WORKFLOW_REGISTRY` through the module. 64 registry-touching test files + enforcement:
  1989 passed, exit gated on `pipestatus`.
- Queue state: (0,0) — waits only (PM: #1772 budget, #1885 burn + key check, #1852 consoles held
  for desk time, step-11 gate; Arch/CXO/HOST/Docs items unchanged in the carry-forward). Idle to
  the 09:17 fire; cron armed.

## Fire 2 (09:17, arrived 09:42) — drain
- PM (in chat, 07:1x–07:3x): MVP tracker rebuilt on live numbers → v35, then v36 on PM's catch
  that sprint weeks run Fri→Thu (the Mon–Thu bucket had inflated last week to +28; real: +9,
  five of seven sprint weeks in the black, discovery not slowing). Epic section added from PPM's
  order doc. PM: "Exec and I will review the week and share a plan… proceed with current
  priorities, no radical shifts expected." PM will grab TSVs (board Status per item is the one
  thing REST can't give me — sprint-truth's token is rate-limited).
- Memos (4): Arch confirms #1772 verbatim (nothing owed); HOST acks the tripped memo; Exec's
  Ship #062 kickoff (Lead named for epic status in user-visible terms); Exec owns #1892's rollup half.
- 09:48 — **#1894**: not a link change — external 403/503 flakiness at a ceiling ~2 under
  the count (102 → green six minutes later, no commit). Fixed at cause: 403/503 accepted (bot-blocks
  and outages aren't broken links); ceiling stays 90 until the next run prints the new count, then
  shrink-lock (`9edf5e4c21`). Note: `gh run list --workflow=` returns stale runs — use the API by id.
- 09:48 — **Workstream #062 filed** to Exec cc PM (`mailboxes/lead/sent/workstream-062-lead-2026-09-25.md`):
  product line first (8 user-visible items), epic table, setbacks incl. the red-main night.
- 09:48 — **Main red a SECOND time** on HOST's ack memo: its placeholder example
  `XXXX0000…` is valid Crockford. Fixed at cause — <8 distinct chars = mock (`2b1741c4eb`) — and
  structurally: **mail-send now runs the bearer lint on passed paths before the push** (`15d338f5f4`,
  `--files` mode; first live send through it = the HOST memo, `e89619e0f`). Lint suite 41 green.
- 09:50 — **#1894 CLOSED at cause**: two post-change runs read 63; ceiling shrink-locked
  90 → 63 (`74c46e3f34`). Gotcha recorded on the issue: `gh run list --workflow=` and the API's
  `branch=main` filter both served 09-13 runs today — query by `created=>=`.
- 09:50 — dispatched a Coding Agent subagent, **Sonnet**, for #1880 residues 1+2 (calendar
  free-blocks render-whole; clarification turns keep top-5 display but say "N more not shown" and
  carry the FULL match list in metadata). Residue 3 stays until #1776 per the issue.
- 09:59 — **#1880 residues 1+2 landed** (`7bfac28f13`; lane 4807/4807 + my re-run 181/181
  exit-gated). Issue stays open for residue 3 (rides #1776). Rides the next deploy with #1893.

## Fire 3 (12:17, arrived 12:42)
- Memos (4): HOST ack2 (no action) · CIO: the `--branch main` staleness is transient caching, not
  deterministic (noted — I saw it three times today; read the timestamp) · Exec: #062 timeline moved
  to ~10:45 — mine was filed 09:45 · HOST fielded Agent 360 v0.5.
- 12:44 — **Agent 360 v0.5 response sent** (`0bbfc2835`, `mailboxes/lead/sent/agent-360-response-lead-2026-09-25.md`),
  answered now rather than banked: 5.6 answered honestly (no CI-reading habit until 06:47 today; adopted
  as a START line), 5.4/9.2 = "a CI completion claim names the run id".
- Deploy of origin/main (#1880 residues + #1893) → v136 running in the background.
- 12:46 — **Fly v136 live** = origin/main `32b0b6c075` (health 0.8.14.0, git_sha matches): #1880
  residues 1+2 and the #1893 fix. Nothing on main undeployed. Queue (0,0); idle to 15:17.

## Fire 4 (15:17, arrived 15:42)
- Exec ×2: PM's sprint-planning outcome — necessity triage (PPM) + **PM's restated sequencing rule,
  no exemptions: epic 0 (#1595) is the current epic NOW**; Lead+Arch attest membership.
- 15:45 — **Attested #1595 Phase 2 MVP-necessary** (memo to Exec cc PM/Arch/PPM, `cecd2dc22`).
  Measured state first: live flag on Fly = 3 read waves + create_todo (read via `fly ssh printenv`);
  audit 72/93 READ keys wave-addressable, 21 unassigned by name; write allowlist = create_todo.
  Wrote `dev/2026/09/25/inversion-epic0-remaining-scope-2026-09-25.md`: five units (wave 2
  read_temporal → wave 3 read_strategic → writes one verified op at a time → multi-intent question
  → Phase 3 deletion ratchet) + the exit test from the epic's own acceptance criteria.
- 15:45 — dispatched a Coding Agent subagent, **Sonnet**, for unit 1: `read_temporal` group
  (12 keys), grouping only — the flag flip waits on a PM shadow-scoring budget.
- 15:55 — **#1595 unit 1 LANDED** (`526cbe4c96`, Sonnet lane, Lead-reviewed; my re-run 103/103
  + enforcement, exit-gated): `read_temporal` = 13 keys (lane corrected my 12 — calendar cohort is 9
  aliases), 85/93 wave-addressable, category-only leak list empty. Flag untouched. Shadow-score
  denominator for the flip: Phase-0 corpus 108 rows, 13 TEMPORAL — 108 Haiku-class router calls;
  budget line to PM tonight.
- 15:55 — dispatched a Coding Agent subagent, **Sonnet**, for unit 2 (`read_strategic`, 8 keys →
  93/93). Grouping only.
- 16:03 — **#1595 unit 2 LANDED** (`3ecd26c337`, Sonnet lane, Lead-reviewed; my re-run 111/111 + enforcement,
  exit-gated): `read_strategic` = 8 keys → 93/93, ungrouped 0. Lane did NOT write its own session log
  (prog-role rule miss; noted here so the omnibus has it): 5 files, +269/−56, 4769/4769 unit, mypy 24/24
  at ceiling. Wave 2+3 tokens both NOT in the live flag — shadow score first.
- 16:04 — dispatched a Coding Agent subagent, **Sonnet**, for #1595 unit 3: `create_reminder`
  onto the #1677 named-write allowlist (Arch's three conditions RE-RUN per op, declaration + pins,
  flag untouched). Own-log required this time (unit 2's lane skipped it).
- 16:13 — **#1595 unit 3 LANDED** (`a3180731c4`, Sonnet lane, Lead-reviewed; my re-run 145/145 + enforcement,
  exit-gated): `create_reminder` allowlisted per #1677 with the three conditions re-run (handler
  `todo_handlers.handle_create_reminder` ~L624 persists one row, deletes nothing; rail block entry-agnostic
  → consent gate). Lane self-caught an enforcement trip (the token `inversion_router.py` in a comment —
  that test regex-scans prose, not imports) and reworded. Own log written this time.
  Three units on main, undeployed (registry declarations only; behavior identical until the flag names them).
- 16:15 — Arch attested #1595 necessary independently (ratchet read at source). Memo to Arch cc PPM
  (`e19f98977`): Q1 may DESTRUCTIVE enter the #1677 allowlist (my read: floor, the gate is the
  guarantee); Q2 two-part turns vs the single consult (I'll build option (a) — split at surface 1, consult
  per sibling — next fire unless Arch objects). Deploy of the three inert units → v137 in background.
- 16:15 — dispatched a Coding Agent subagent, **Sonnet**, for the exit-test completeness audit:
  do all 8 Exhibit-A failures + every pin row have a judged Phase-0 corpus row; deposit HAND_ROWS for the
  missing ones with citations (REVIEW where the destination is open), regenerate, dry-validate. No LLM.
- 16:18 — **Fly v137 live** = origin/main `fd535398c9` (health git_sha matches): the three inversion
  units as inert declarations; live flag re-read after deploy = unchanged
  (`read_status,read_referent,read_synthesis,create_todo`). Flipping wave 2/3 + create_reminder is now a
  `fly secrets set` away, gated on the shadow score.
- 16:23 — **Exhibit-A completeness audit LANDED** (`98a90a4a20`, Sonnet lane, Lead-reviewed; corpus test
  22/22 exit-gated): 8 verbatims deposited, corpus 108→116; pins covered 2/2 + 1/1. Lane's two
  judgment calls (same-class variants not added) accepted. Budget line for PM: **116 Haiku-class router
  calls** for the full per-category shadow score (the flip gate for wave 2/3 + create_reminder).
- 16:37 — PM: "run it". **Shadow score run** (117 calls, 0 errors, served gpt-4o-mini 116/116 —
  the dev key's provider, named): 45/57 asserted. Read: create_reminder (3/3 @1.0) + read_strategic
  licensed; **read_temporal HELD** — the gate's Δ column compared different denominators (m-44 in our own
  instrument); on the baseline's 4 shared rows the router is 3/4 ("what's on my calendar today?" →
  week_calendar). Report + my read on main (`0ca2a29293`), comment on #1595.
- 16:37 — **Flag flip classifier-DENIED** ("Feature Flag Writes") — not worked around; the
  one-liner is on #1595 and in chat for PM. Dispatched a Coding Agent subagent, **Sonnet**: scorer
  shared-subset denominator + sharpen meeting_time/week_calendar registry descriptions (no LLM; the
  14-row re-score waits on PM).
- 16:43 — **PM flipped the flag** (`! fly secrets set …` in-session): create_reminder + read_strategic
  LIVE on v137; verified `printenv` on the machine, health green, no unrecognized-token line. #1559
  commented: closes on PM's live turn (test-card row to add). PM approved the 14-row TEMPORAL re-score;
  waits on the scorer/description lane (same file).
- 16:50 — **TEMPORAL re-score** (14 calls, PM-approved): shared subset 4/4 vs 4/4, "what's on my
  calendar today?" → meeting_time @1.0; read_temporal licensed (`43e12de2d2`). Scorer gained
  `--category` (partial runs labelled as partial). **Fly v139 live** = origin/main `43e12de2d2` (the
  sharpened grammar is now the served grammar; v138 was PM's secrets-set restart); flag intact
  (create_reminder + read_strategic). Test card v11: row 10 = #1559 verbatim. PM: testing tomorrow.
- 17:35 — **PM flipped read_temporal** (in-session `!`): verified on the machine — all five read waves +
  create_todo + create_reminder live on v139. 93/93 READ keys routable by the inversion. #1595 commented.
