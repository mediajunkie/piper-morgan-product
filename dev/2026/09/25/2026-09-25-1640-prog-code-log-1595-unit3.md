# Session Log — prog (Coding Agent), 2026-09-25 16:40 PT

**Model**: Sonnet 5 (claude-sonnet-5)
**Role**: prog (Coding Agent), dispatched by Lead Developer
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
**Task**: #1595 unit 3 — add `create_reminder` to the named-write allowlist (#1677's procedure). Declaration + pins only. No flag/env/Fly/deploy changes.
**Do NOT commit/stage/touch git index** — Lead reviews and commits.

## Reading done before starting
- `docs/internal/architecture/current/intent-routing-stack.md` — the chain overview, #1677 named-WRITE allowlist entry (lines 720-771), the #1667 wave/flip_group mechanism, #1595 wave 2/3 sections.
- `dev/2026/09/25/inversion-epic0-remaining-scope-2026-09-25.md` — unit 3 scope: `create_reminder` first (closes #1559), then clear-reminders family later. Named-write, never a group.
- `services/intent_service/workflow_entries.py` — `create_reminder_entry` (~1547-1571) and `create_todo_entry` (~1573-1628, the exact precedent with its three-condition comment block).
- `services/intent_service/workflow_dispatcher.py` — `FLIP_WRITE_ALLOWLIST` constant (line 124) + its 3-condition comment (90-123), `flip_write_allowed()` (127-141), `WorkflowEntry.__post_init__` guard (225-280).
- `services/intent_service/inversion_live.py` — dispatch-time effect guard consulting `flip_write_allowed`.
- `tests/unit/services/intent_service/test_inversion_write_allowlist_1677.py` — full read. create_todo-specific by construction (docstring, `_MSG`/`DEFECT_SHAPES`, hardcoded `frozenset({"create_todo"})` assertions, `TestFlippedTurnReachesTheRail._run` etc. all keyed to create_todo phrasings).
- `services/intent/intent_service.py` lines 2894-2970 — the shared consent-gate dispatch block (entry-agnostic: any rail entry with `needs_consent` goes through the same `consent_gate.evaluate_consent` call regardless of action name).
- `services/intent_service/todo_handlers.py` lines 530-656 — `handle_create_reminder` body (re-read to re-verify effect by behavior).
- `gh issue view 1559 --comments` — the corpus row this unit exists for: "remind me at <time> <day> to X" misses the deterministic pre-classifier (adjacency gap in REMINDER_PATTERNS, frozen by policy), so it's an LLM-classifier coin flip; the structural fix is the create_reminder rail flip.

## Plan
1. Re-run Arch's three conditions for `create_reminder` (registered / effect-by-behavior / reaches-consent), write results into the entry's comment block, same shape as create_todo's.
2. Add `flip_write_allowlist_key="create_reminder"` to the entry, no `flip_group`.
3. `FLIP_WRITE_ALLOWLIST` → `frozenset({"create_todo", "create_reminder"})`, comment block updated with the same 3-condition verification note.
4. Tests: update the existing file's constant/denominator assertions (they hard-code the old singleton set) + add a sibling file for create_reminder-specific dispatch/rail/defect-shape coverage.
5. `--audit` line quoted after the change.
6. Docs: one sentence in intent-routing-stack.md under #1677.
7. ruff format/check + full test run + architecture enforcement + sweep ratchets.

## Work log

### Arch's three conditions, re-run for `create_reminder` (not cited)
1. **Registered**: `get_action_workflows()["create_reminder"]` exists, `action_triggered=True`.
   Alias family (from `ActionMapper.ACTION_ALIASES`, action_mapper.py:89-91): `create_reminder`,
   `set_reminder`, `add_reminder` — all canonicalize to `"create_reminder"`, the same name
   `ACTION_REGISTRY` files it under (`("EXECUTION", "create_reminder")`, action_registry.py:199)
   and the same name `derive_routing_grammar()` emits as canonical.
2. **Effect correct by behavior**: read `todo_handlers.py:530-656`
   (`TodoIntentHandlers.handle_create_reminder`). Line 624:
   `todo = await self.todo_service.create_todo(user_id=user_id, text=text, priority="medium",
   reminder_date=reminder_dt, due_date=reminder_dt)` — persists exactly one row; nothing is
   deleted anywhere in the function (the two honest-ask early returns for missing task/missing
   time persist nothing at all). WRITE, not DESTRUCTIVE, not READ.
3. **Reaches consent**: `services/intent/intent_service.py` lines 2894-2937 — the rail dispatch
   block reads `_rail_entry.needs_consent` / `.effect` / `.outwardness` off *whichever* entry
   `intent.action` resolved to and calls `consent_gate.evaluate_consent(...)` before dispatch.
   This block is entry-agnostic (no action-name branching) — it already ran identically for
   `create_reminder` before this unit (registered by #1560), and the same spy idiom
   `test_inversion_write_allowlist_1677.py::TestFlippedTurnReachesTheRail` uses for `create_todo`
   is mirrored for `create_reminder` in the new sibling test file and passes.

**Verdict**: all three hold. Added `flip_write_allowlist_key="create_reminder"` to
`create_reminder_entry` (services/intent_service/workflow_entries.py). No `flip_group` — matches
the #1677 rule that no wave sweeps a write in.

### Changes made
- `services/intent_service/workflow_entries.py`: `create_reminder_entry` gains
  `flip_write_allowlist_key="create_reminder"` + a comment block re-running the three conditions,
  same shape as `create_todo_entry`'s.
- `services/intent_service/workflow_dispatcher.py`: `FLIP_WRITE_ALLOWLIST` →
  `frozenset({"create_todo", "create_reminder"})`, with the verification note for the new entry
  appended to the existing comment block.
- `tests/unit/services/intent_service/test_inversion_write_allowlist_1677.py`: updated the two
  assertions that hard-coded the old singleton set (`test_allowlist_is_exactly_create_todo_today`
  → renamed `test_allowlist_is_exactly_create_todo_and_create_reminder`;
  `test_no_other_rail_entry_declares_a_key` now asserts TWO distinct entry objects, one per alias
  family). This file stays create_todo-specific everywhere else (by construction — its
  `_MSG`/`DEFECT_SHAPES`/`TestFlippedTurnReachesTheRail` are all keyed to todo-create phrasings).
- New sibling file `tests/unit/services/intent_service/test_inversion_write_allowlist_create_reminder_1559.py`:
  mirrors the create_todo file's dispatch-guard, rail-e2e, and grammar-membership sections one for
  one, using the #1559 phrasing `"remind me at 3pm tomorrow to call the vendor"`. Covers: named
  flip works; category surface (`EXECUTION`) sweeps it in too; sub-threshold blocks; a wave flag
  (`read_status`) does NOT sweep it in; default-empty stays byte-identically dark; the flipped
  turn reaches the same handler, writes the row, and `consent_gate.evaluate_consent` is spied
  exactly once with `EffectClass.WRITE` / `Outwardness.PRIVATE`.
- `docs/internal/architecture/current/intent-routing-stack.md`: one paragraph under the #1677
  entry recording the 2026-09-25 allowlisting, condition re-run, no flip_group, flag unset.

### Gates (all read in full, quoted)
- `venv/bin/ruff format` on the 4 touched .py files: `4 files left unchanged`.
- `venv/bin/ruff check --fix` on the same 4 files: `All checks passed!`.
- `venv/bin/python -m pytest tests/unit/services/intent_service/test_inversion_write_allowlist_1677.py tests/unit/services/intent_service/test_inversion_write_allowlist_create_reminder_1559.py -q -p no:cacheprovider`: `34 passed in 1.89s`.
- `venv/bin/python -m pytest tests/unit/services/intent_service/ -q -p no:cacheprovider`: `4780 passed, 16 warnings in 129.13s` (all 16 warnings pre-existing pytest/mock artifacts unrelated to this change, not in touched files).
- `venv/bin/python -m pytest tests/test_architecture_enforcement.py -q -p no:cacheprovider`: FIRST RUN caught a real regression —
  `TestInversionShadowNoExecutionBoundary::test_only_the_shadow_observer_may_import_the_router`
  failed because my comment in `workflow_entries.py` used the literal token `inversion_router.py`
  inside prose (the test's `ROUTER_TOKENS_RE` is a naive `\binversion_router\b|\bRoutingDecision\b`
  content scan over ALL of `services/`+`web/`, not an AST import check — it does not distinguish
  a comment from an import). Fixed by rewording the comment to say "the grammar-derivation
  module's own docstring" instead of naming the file. Re-run: `63 passed, 1 xfailed in 11.06s`.
- `scripts/run-sweep.sh ratchets` (full output read, matching lines quoted):
  ```
  73 passed, 1 xfailed in 18.07s
  --- mypy per-code gate (#1436) ---
  mypy gate: all 24 ratcheted codes at ceiling (total=1121; arg-type=364, assignment=227, attr-defined=69, call-arg=3, call-overload=6, dict-item=5, has-type=2, index=10, list-item=2, misc=77, no-redef=3, operator=63, override=17, return=1, return-value=48, union-attr=141, valid-type=14, var-annotated=69)
  ```
  Exit status separately verified: `EXIT: 0`.
- `venv/bin/python scripts/inversion_phase2_gate.py --audit` — no LLM, registry read only. Quoted:
  ```
  NAMED-WRITE ALLOWLIST (#1677)              : ['create_reminder', 'create_todo']
    rail keys declaring an allowlist key     : ['add_reminder', 'add_todo', 'create_reminder', 'create_todo', 'new_todo', 'set_reminder']
    ⚠️ These flip when a flag token names the OPERATION or its registry CATEGORY (create_todo is EXECUTION — flipping that category flips this write too). No flip_group sweeps them in.
  ```
  READ-side counts unchanged from unit 2's landed state (93/93 grouped, 0 unassigned) — confirms
  this unit touched only the write allowlist, nothing on the READ flip surface.

No LLM calls anywhere in this session — everything above is deterministic router fakes, registry
reads, or the real (non-mocked) `todo_service.create_todo`/`consent_gate.evaluate_consent` code
paths exercised against sqlite-in-memory/monkeypatched boundaries.

### Verified how
- **Method**: ran the actual commands quoted above, this turn, and read their full output
  (including the one that failed on first pass and was fixed).
- **Layer**: unit-test layer with deterministic router fakes for the flip-routing claims (stated
  explicitly in both test files' module docstrings, m-43) — these prove the *routing path*, not
  the live constrained router's draw distribution against real #1559 phrasings (that is
  observable only live, in `inversion_live_decision` telemetry, out of scope for a
  declaration-only unit). The consent-gate and entry-registration claims are proven against the
  real (non-mocked) production code path, not a fake.
- **Denominator**: 2 production files changed (workflow_entries.py, workflow_dispatcher.py) + 1
  doc + 1 existing test file updated + 1 new sibling test file (9 new/updated test cases,
  34 tests total passing in the two allowlist-specific files); full intent_service unit suite
  (4780 tests) and full architecture-enforcement suite (63 + 1 xfail) both re-run green; ratchet
  sweep (73 tests + mypy gate) green. No flag, env, Fly config, or deploy file touched — confirmed
  via `git diff --stat` (4 files) + untracked-file listing (this log + the new test file only,
  plus a pre-existing unrelated untracked `dev/active/canonical-retest-serving-llm.json` not
  created by this session).

## Sign-off
Did NOT commit, stage, or touch the git index per dispatch instructions — Lead Dev reviews and
commits. `git status` at handback: 4 modified tracked files + 2 new untracked files (this log,
the new test file) + the pre-existing unrelated untracked JSON file.
