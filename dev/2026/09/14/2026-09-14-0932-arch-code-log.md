# Session Log: 2026-09-14-0932-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code — **Opus (switched by PM 2026-09-14; Fable 5 access limit reached)**. Prior
fires this session ran on Fable 5.
**Date**: Monday, September 14, 2026
**Start Time**: 9:32 AM PT (06:27 fire did not run — session gap)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

## Session Objectives

Duty-cycle. Pending on PM: Q5, scope-guard ruleset bypass (#1744), Bets 001-003. Watch: #1788
close-out, epic-6 GitHub-six build, PostToolUse warn (#1798), three Rule-0 lanes.

## Work Log

### 9:32 AM - Session Start (START; catch-up, 09-13 retro-closed)

- START heartbeat first action. Cron `bdaa46a4` singular (survived; un-rotated from 09-13's
  interrupted STOP — deferred, not missed). Step 0: 09-13 retro-closed with the interruption
  stated.
- **#1788 DocumentDB RE-RULED — Lead's disagreement was correct.** Verified their three facts
  myself first: domain `Document` has 0 importers outside models.py; `DocumentDB` is the
  ADR-071 D2 owner-anchor row (chromadb_base_id / owner_id / is_global_pm_domain, no content
  column); domain `Document` is content-bearing. **My discriminator was sound and I applied it
  to ONE SIDE** — measured DB-side importers and stopped. Re-ruled cat (2) with its own reason
  line (live anchor row, dead name-matched twin — the inverse sub-shape of the five), and
  **"liveness is measured on BOTH sides" adopted into the ruling itself**, not a footnote.
  Lead's two evidence corrections accepted as sharper than my originals ("zero live consumers";
  TaskRepository.create_from_domain). Dead domain `Document` named as a separate orphan, NOT
  bundled into #1797. 4/4 delivered.
- CIO's #1798 filed with both my interim conditions added. Inbox 0.

### 09:57 AM — WORK fire (mail empty → own-lane work: standing-items refresh)

Mail empty; ran the three-source check per PM's work-queue ruling and found the work in source
2 (carried state). **Standing items refreshed after a 45-day gap** — and the gap had been
hidden by its own most-recent row: the Architectural Review row, dated 08-31, read as an
in-flight review for two weeks after that review closed. **Staleness presenting as currency**
is the failure mode worth naming — the file's freshest-looking row was its most misleading.

Found by `gh` re-verification, not by reading: **#973 is CLOSED** and was sitting in Active as
open work — the SECOND instance of the exact defect this file's own header records (#972, six
weeks). Recorded as such rather than quietly deleted; the lesson is that a stale row reads
exactly as credible as a live one, which is why the convention is re-derive-against-gh.

Now 5 dated rows, each with a clearing condition that can actually fire: review CLOSED (with
the flywheel successor's ratification noted); #1744 blocked-on-PM with the two clearing routes
named; epic-6 design inputs banked (incl. the two-mechanism finding); #1797/#1798 watch;
ADR-068 + #1459 unchanged and re-verified open.

### 12:57 PM — WORK fire (quiet)

One drained: CXO converting their own twice-repeated "§6 isn't discharged" status into a
mechanism ("not discharged is a status, not a spec — a rule with no mechanism is exactly the
failure this contract documents in other people's work"), and finding in the process that case
3 was NEVER blocked on epic-6 — all four provenance states are already representable in
`context_assembler` today (#1544's zero-count for verified_empty, #1645's source_failed flag).
Useful for my epic-6 read: the epic's job is narrower than "make provenance representable" —
it's threading what already exists to where the renderer and the next turn can reach it. No
arch action owed; banked. Inbox 0.

### 15:57 PM — WORK fire (#1810 RULED — a live cross-user credential write)

Six drained; the fire's work was Lead's escalation. **#1810: every user's LLM key is written
TWICE — once under their own id, once to a GLOBAL unprefixed slot that the app resolves as "the
server's key," last-writer-wins.** Lead correctly refused to fix around the architectural
question: what legitimately needs a server-resolved key at startup with no user context?

**Ruled: NOTHING.** Traced rather than reasoned — the `LLMConfigService()` construction the
comment points at is inside main.py's `keys` CLI handler (operator context, not startup);
#1807's operator path is explicitly env-only and DB-free, so the one legitimate server-key case
never reads the global slot; `LLMClient._init_clients()`'s user-less call is the eager
constructor, i.e. the leak's mechanism, not a justifying consumer. **Delete both writes.**
Framing: ESSENCE commitment 1 applied to credentials — a shared mutable slot every user writes
to is a tenancy hole wearing plumbing's clothes, and the explanatory comment is exactly why it
stayed invisible (m-52 at the code layer: a summary read in place of its contents).
**#1809 ruled in the same memo as the same principle one layer down**: invert the default so an
unbound path REFUSES rather than spends. Sequencing concurred: 1810 → 1809 → 1791.
**Exec's HOLD on Janne's invite concurred** — the first external tester's onboarding is the
trigger that fires the cross-user write; flagged to PM as the one item needing their word.
7/7 delivered; decisions.log entry; verified-how states source-not-live-probe.

### ~16:05 — PM RATIFIED THE HOLD (in-conversation, relayed same-minute)

PM, verbatim: **"yes hold under we stanch this leak"** (typo for "until"; quoted as typed).
Relayed 7/7 to Exec + HOST (token holder) cc Lead/PPM/CXO/PM. **Clearing condition written so
it can fire**: #1810's global write gone AND that absence OBSERVED via a real setup flow — not
merged, not reported (m-49; same discipline as #1744). **Referent stated precisely**: "this
leak" = #1810, not the whole family; #1809 is next in sequence, and if PM meant the family the
hold simply extends on their word. Better to state a read and be corrected than pass along an
ambiguous instruction.

### 18:57 PM — WORK fire (#1810 closed → LIFT recommendation; epic-count provenance)

Six drained. **#1810 closed and deployed (v107)** — I verified the fix at trunk myself rather
than accept the report: grepped setup.py at origin/main for all three global-write markers,
zero hits. Lead's red-first test pins the behavior.
- **Recommendation to PM: LIFT the hold, with one condition** — make Janne's key step a
  required, stated part of onboarding. Lead's residual (#1809, unbound paths still resolve the
  server key) is real but narrow-edged: it does not touch him AT ALL if he configures his own
  key, and #1807 already refuses-not-charges his chat turns. Better to convert the residual
  into a documented onboarding step than hold a tester against a hazard his own onboarding
  neutralizes. **Layers stated honestly**: verified at source + test layers; NOBODY has run a
  live v107 setup flow, and I can't. Alternative (hold for #1809) named as defensible so PM
  chooses rather than defaults. 7/7.
- **PM asked PPM why 12 epics when they last heard 6.** Not my question to answer — but part of
  the delta is MY 09-09 cause-factoring adopted as epic boundaries, at PM's own directive. Sent
  PPM the provenance + the distinction I'd want PM to have (a count rising because work got
  grouped by root cause is the pile becoming legible, not scope creep) + the honest
  counter-argument (cause-partitioning can over-split; check my grouping rather than accept it
  because it's mine). 4/4. Explicitly did not pre-empt PPM's reasoning.

## Active pattern families this session

Multi-Agent Coordination (029/059/010/021/037). Investigation (006/041-043/060).
