# Session Log — 2026-09-14 21:48 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Sonnet 5 (claude-sonnet-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)
**Task**: Discharge the #1810 clearing condition (Arch, PM-ratified) by OBSERVATION, not test pin —
gates an external alpha tester's invite. Condition: "Clears when #1810's global write is gone AND
that absence has been OBSERVED — not when a fix merges, not when a lane reports done.
Concretely: the double-write in `web/api/routes/setup.py` is deleted, and someone completes a
setup flow and confirms no global unprefixed entry was created."

Given as established (not to re-derive): fix is in `dff0c50a6` (ancestor of origin/main, deployed
v108); unit pins exist in `tests/unit/web/api/routes/test_setup_global_key_write_1810.py`; prod
`KeychainService.get_api_key` for anthropic/openai both ABSENT right now (read-only prod check,
already done by Lead). Exec's position (agreed): a unit test pin is a different layer than an
observed live flow — condition not yet met without the live observation.

My job: drive the REAL setup HTTP routes end-to-end on a local server (main.py subprocess,
ANTHROPIC_* stripped, real Postgres 5433, ephemeral port), forced onto the DB-backed credential
store (PIPER_CREDENTIAL_STORE) to match the prod backend layer, with a throwaway account, and
observe via KeychainService: per-user entry PRESENT, global unprefixed entry ABSENT.

## Investigation (21:48–)

- Read `web/api/routes/setup.py` full route file. Confirmed the #1810 fix comment at
  ~:1021-1034: the global unprefixed write is gone; `complete_setup` calls
  `UserAPIKeyService.store_user_key(..., validate=False)` (per-user keychain write via
  `self._keychain.store_api_key(provider, api_key, username=user_id)`) and separately
  writes `authorized_llm_providers` / `default_llm_provider` per-user via
  `keychain.store_api_key(..., username=req.user_id)` — no bare-provider call anywhere
  in the file. `git merge-base --is-ancestor dff0c50a6 HEAD` confirmed (already an
  ancestor of `claude/lead-cycle`, which tracks `origin/main`).
- Read `tests/unit/web/api/routes/test_setup_global_key_write_1810.py` — confirms the
  unit-pin layer (calls `complete_setup()` as a Python function directly, forces
  `PIPER_CREDENTIAL_STORE=db`). This is the layer Exec's position (agreed) says is
  insufficient alone — a function call, not the real HTTP route + real subprocess.
- Read `services/infrastructure/keychain_service.py`: `_get_key_name()` composes
  `"{username}_{provider}_api_key"` per-user vs. bare `"{provider}_api_key"` global —
  this is the exact string #1810 is about. `PIPER_CREDENTIAL_STORE=db` forces the
  `EncryptedDBCredentialStore` backend (#1382, `secure_credentials` table) — the same
  backend prod resolves to (dead OS keyring on hosted Linux).
- Read `services/security/user_api_key_service.py::store_user_key` — confirms it calls
  `KeychainService.store_api_key(provider, api_key, username=user_id)` (per-user only)
  plus a DB `user_api_keys` metadata row; `validate=False` (as passed by
  `complete_setup`) skips the live provider API call but NOT the format/strength
  validator (`APIKeyValidator`) — so throwaway keys must be validator-shaped, matching
  the test pin's `_valid_key()` generator (`sk-...` len>=20, `sk-ant-...` len>=100).
- Read `services/auth/invite_token_service.py` + `scripts/mint_invite_tokens.py`:
  tokens are 24-char Crockford Base32 rows in `invite_tokens`, consumed atomically
  inside `create_user`'s transaction. No HTTP mint endpoint exists — minted a
  throwaway row directly via `docker exec piper-postgres psql` (local dev DB only).
- Confirmed environment: `docker ps` shows `piper-postgres`/5433, `piper-redis`/6379,
  `piper-chromadb`/8000 all up (4 weeks, healthy). Dev server already running on 8001
  (PID 30735) — untouched throughout, used an ephemeral ancillary port instead.
- Baseline check of the LOCAL shared dev Postgres before touching anything: `users`
  table = 2117 rows, 0 with `setup_complete=true` (so `require_setup_incomplete` on
  `/complete` would not 403); `secure_credentials` table = **0 rows total** — i.e. no
  global `openai_api_key`/`anthropic_api_key` slot pre-existing locally either.

## Live-harness run (21:49–21:53)

Method: real `main.py` subprocess (`--no-browser`), env: `ANTHROPIC_API_KEY` /
`ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_CUSTOM_HEADERS` all
stripped via `env -u`, `POSTGRES_PORT=5433` (real local Postgres, the shared dev DB —
not a throwaway/sqlite DB), `PIPER_CREDENTIAL_STORE=db` (forces `EncryptedDBCredentialStore`
— matches the prod backend layer), `ENCRYPTION_MASTER_KEY` set to the same 43-char
test value the #1810 unit pin uses, ephemeral `PIPER_PORT=59855`. Started, polled
`/health` → **200 healthy after 1s**. Server log line confirmed the credential-store
backend selection: `Keychain service initialized backend=EncryptedDBCredentialStore
(#1382 hosted fallback) os_backend=Keyring`.

**Recovered incident, self-caught before it mattered**: first attempt at driving
`create-user` used a shell variable named `USERNAME`, which in **zsh is a read-only
dynamic special parameter bound to the real macOS account** (`$UID`/`$USERNAME`-class)
— assignment silently no-ops (no error). The request that actually reached the server
carried username/email `xian`/`xian@test.invalid` instead of the intended throwaway
name. Caught immediately by checking `declare -p USERNAME` after the anomalous
response. Verified via SQL that exactly one row existed with `username='xian'`
(the just-created throwaway one — the real PM account, if any, uses a different
username in this table) before deleting it + the row's consumed invite token
(FK order: `invite_tokens` before `users`). Verified 0 rows remained for
`username='xian' AND email='xian@test.invalid'` afterward. Re-ran cleanly using
`TESTUSER` for the variable name. Filed here per Discovered Work Discipline as a
process note, not a product bug — no app code involved, purely a shell-scripting
hazard for future live-harness runs.

**Corrected run — HTTP transcript** (throwaway token `C0BQ...80YK` minted via direct
`INSERT INTO invite_tokens`, username `u1810obs9efa17651a2e`):

1. `POST /api/v1/setup/create-user` → **200**, `{"success":true,"user_id":
   "9cf56346-662c-4eba-9b2a-b2939e769038","message":"Account created:
   u1810obs9efa17651a2e"}`. Confirmed by direct SQL: `invite_tokens.used_at` now
   non-null for that token (atomic consume worked); `users` row present,
   `setup_complete=false`, `is_alpha=true`.
2. `POST /api/v1/setup/complete` with `user_id` + `default_llm_provider=openai` +
   validator-shaped throwaway `openai_key` (40 chars, `sk-...`) and `anthropic_key`
   (110 chars, `sk-ant-...`) → **200**, `{"success":true,"message":"Setup complete!
   Redirecting to login...","redirect_url":"/login"}`. Server log shows no
   `*_key_storage_failed`/`setup_completion_failed` lines for this request.

**Observation — through the app's real `KeychainService`, same env the server ran
under** (`PIPER_CREDENTIAL_STORE=db`, same `ENCRYPTION_MASTER_KEY`):
```
openai:    per_user_present=True   global_present=False
anthropic: per_user_present=True   global_present=False
```
Corroborated directly at the storage layer — `SELECT name FROM secure_credentials`
after `/complete` returned exactly 5 rows, ALL prefixed with the user_id
(`{uid}_anthropic_api_key`, `{uid}_authorized_llm_providers_api_key`,
`{uid}_cli_session_api_key`, `{uid}_default_llm_provider_api_key`,
`{uid}_openai_api_key`) — **no bare `openai_api_key` or `anthropic_api_key` row was
created.**

## Cleanup + verification by count (21:52–21:53)

- Keychain: `delete_api_key` for openai/anthropic/authorized_llm_providers/
  default_llm_provider (all `True`) + `delete_cli_token` (`True`) — 5/5 deleted,
  confirmed via `credential_deleted_db` log lines naming each key. Post-delete
  `get_api_key` re-check for openai/anthropic (per-user) → both `None`.
- DB: `DELETE FROM user_api_keys WHERE user_id=...` → 2 rows; `DELETE FROM
  invite_tokens WHERE token=...` → 1 row; `DELETE FROM users WHERE id=...` → 1 row
  (FK-ordered: token row before user row, same lesson as the incident cleanup).
- Counts verified back to exact baseline:
  - `users`: 2117 total / 0 `setup_complete` (unchanged from pre-run baseline).
  - throwaway user row: 0.
  - `user_api_keys` for that user_id: 0.
  - `invite_tokens` for that token: 0.
  - `secure_credentials`: **0 rows total** (matches pre-run baseline of 0 — no
    residue of any kind, per-user or global).
- Server teardown: `kill` on the ephemeral-port PID; `pgrep -fl "main.py
  --no-browser"` → none afterward. Original dev server on 8001 (PID 30735)
  confirmed still listening, untouched throughout.
- Scratchpad temp files (script + response captures under `1810-*`) removed;
  pre-existing scratchpad files from other sessions left alone.

## Layer statement (honest, per task instruction)

This observes the **deployed code path** (`dff0c50a6`, ancestor of `origin/main`,
already running as prod v108) against a **prod-matching credential backend**
(`EncryptedDBCredentialStore`, forced via `PIPER_CREDENTIAL_STORE=db`) on a **local
server process** driven through the **real HTTP routes** (`create-user`, `complete`)
with a **real local Postgres** (the shared dev DB on 5433, not a throwaway DB) — it is
**NOT a production setup run**. It does not exercise fly.dev, the hosted container's
actual dead-OS-keyring auto-detection (I forced the DB backend explicitly rather than
relying on auto-detect, since this Mac's OS keyring is alive), or any production data.
Combined with Lead's already-established prod-side observation (prod
`KeychainService.get_api_key("anthropic"|"openai")` both absent, read-only, no write
attempted) — the pair together are: (a) prod currently has no global entry, and
(b) the code path that used to create one, run for real through the actual HTTP
routes against a backend matching prod's, provably does not create one and does
create the correct per-user one. That is what "observed," as opposed to "test-pinned
only," was asking for.

## Discovered work

None filed as a tracked issue — the `$USERNAME` zsh-special-parameter hazard above is
recorded here as a process note for future live-harness scripts (avoid that variable
name in zsh), not a product defect warranting a GitHub issue.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-stripped launch idiom #1258; never-guess-at-facts;
  pause-before-irreversible; m-43/m-44 layer+denominator discipline); prior session
  log `dev/2026/09/12/2026-09-12-1338-prog-code-log.md` (the live-harness subprocess
  idiom this run mirrored); #1810 issue context embedded in the unit-pin test file's
  docstring and the fix commit's inline comment in `setup.py`.
- **Loaded but not referenced**: memory index entries broadly; other agents'
  duty-cycle context.
- **Wanted but not found**: nothing — this was a self-contained verification task
  with all needed context in-repo.

