# Lead Developer session log — Monday 2026-09-14 (Claude Code, model: Opus)

Role: Lead Developer (lead-code). Worktree ~/Development/piper-morgan-worktrees/lead,
branch claude/lead-cycle (Model A). Cron 22689706 (17 6,9,12,15,18,21; rotated 09-13,
expires ~09-20, next rotation ~09-18).

## START (06:29) — synced (0 behind), inbox 1 (CIO). Sunday closed with: both rotations
done and verified (E2E green first time ever; the leaked DB password dead), belt 19/25 green
with zero unexplained reds, epic 6's first build in, and ~10 issues closed. Carry-forward
holds the queue.

- Fire 1 (06:31) — WORK. Inbox 1→0: CIO added both of Arch's conditions to the hook header
  (named temporary interim, #1798 pointer, and the 2-commit-split workaround documented next
  to the block message so nobody rediscovers it at 23:00) and filed **#1798** with the two
  real fixes + an acceptance criterion that a REAL triggered test must confirm PostToolUse
  surfaces before the interim BLOCK is retired. Nothing owed back.
  MVP measured: **51 open** — up, and honestly so: the weekend's lanes filed ~25 discovered
  issues and PPM triaged them all into epics. Closure and discovery both counted.
  NOW RUNNING: #1777 — the agenda response CRASHES on a todo-source failure (a bare len()
  over the #1425 None sentinel), killing the whole response AFTER the formatter had honestly
  composed "I couldn't check your tasks." The #1776 lane guarded the one site it was already
  rewriting; this lane does the SWEEP of every other sentinel consumer, classifying each as
  crashes / silently-misreports / safe, and is told to STOP rather than invent copy if a fix
  needs CXO's §6.1 case-3 call.

- 06:52 — #1777 CLOSED + DEPLOYED v104 (health 200). Sweep found something sharper than the
  filed crash: **the crash site was already guarded, and the guard was itself a misreport** —
  #1776's `else 0` stops the TypeError then reports a FAILED source as contributing zero,
  beside a sibling saying None for the same condition in the same dict. Two more of the shape
  four lines apart. **Why it survived eight prior #1425 rounds: `or []` cannot raise, so it
  READS as a guard — but it's a default, and the default is a claim about the user's day.**
  17 safe sites pinned as GREEN CONTROLS so the denominator is the whole space.
  **AC3 answered, and it's the finding of the day: mypy ALREADY reported every drift site —
  those five lines ARE the census.** The #1436 gate couldn't act because it ratchets per-code
  AGGREGATES and they sat under ceilings. Annotations corrected, ceilings LOWERED same commit
  (377→374, 240→239, 49→48). Path-level-zero scoping is an Arch call → #1800. Per-site
  discipline is refuted by the record: eight issues each guarded what they touched and this
  crash still happened four lines below a comment describing it.
  Stopped rather than invented: 1-of-3 formatters discloses a dict-flag failure; the terse
  register needs CXO's §6.1 case-3 copy call → #1799.
  Lane independently re-hit my documented gh-in-sandbox gotcha (empty output, exit 0, piped) —
  the entry is already in the gotchas doc, now confirmed by a second seat.

- Fire 2 (09:31) — WORK. Inbox 2→0. Exec alerted "SEVEN roles did not start"; **Janus corrected
  it from Amber: nothing died.** All 24 tmux sessions alive, same creation timestamp as the
  three that fired; the panes state the cause verbatim — the account's **Fable 5 ceiling**.
  Four of the seven had already recovered before the memo was two minutes old.
  I sent the corroboration nobody else could: **my seat is the control.** Same host, tmux
  generation, cron mechanism, skill, worktree model as the stalled seats; one variable differs
  (PM moved me Fable→Opus ~11:36 yesterday); outcome differs. Consistent with the ceiling
  diagnosis, independent of the pane text.
  The keeper, Janus's words: *"a fire that is admitted and then refused produces the same
  nothing as a fire that never ran."* Same shape as my Saturday dark night (armed cron, signed
  out, fired nothing). **Three different causes of IDENTICAL silence in four days** — dead,
  refused, signed-out. Silence is not a diagnosis; the belt can say a seat produced nothing,
  never why, and the remedies differ. Flagged as worth writing down durably.
  Also carried Pard's framing: model tier is a CAPABILITY property — a persistent session
  inherits its launch model with no fallback, a pinned headless fire is immune to another
  tier's ceiling. Nobody designed that asymmetry; it's a provisioning question.
  NOW RUNNING: #1792 — a blacklist outage tells users "Token has been revoked" (a specific,
  alarming, FALSE claim about their own security state) instead of "can't verify right now."

- 09:51 — #1792 CLOSED + DEPLOYED v105 (health 200). Reproduce-first earned its keep: **the
  issue cited ONE fail-closed site; there are TWO, and the live repro ran through the one NOT
  cited** (_check_database, reached because _redis_available was False). Fixing only the cited
  site would have left the reproduced path still lying. Shape: fail-closed with the WRONG
  MESSAGE — and no security tension to trade, since raising refuses exactly as return-True
  did. Proven independently by the red-first split: the "still refuses" and "genuinely
  revoked" tests were ALREADY GREEN pre-fix; only the honest-message tests were red.
  Precedent followed not invented (require_admin's 503 + copy), with one divergence stated:
  no /login redirect, because login never consults the blacklist so the user would loop.
  Two more catches: validate_token's generic except would have rewritten the new exception
  into TokenInvalid (a DIFFERENT false claim); the refresh route had been 401'ing AND
  CLEARING auth cookies on a store outage. Live: 200,401,401,401,401 → 200,503,503,503,503.
  Filed #1802 for the real TestClient defect two layers down (honest now, still broken —
  the right order).
- 09:51 — Took the lane's "not filed (trivial)" note rather than letting it evaporate:
  _unauthorized_response carried the same implicit-Optional annotation the lane had COPIED
  into its helper (which is how it surfaced). Fixed, **assignment ceiling 239→238** in the
  same commit. Small, but the shape matters: an uncorrected drift instance propagates by copy.

- Fire 3 (12:31) — WORK. Inbox 6→0. Two big ones:
  🔴 **EXEC'S FINDING LANDS ON ME AND I OWN IT**: my 48 subagent dispatches in four days (30 on
  Saturday alone, vs 10 in the preceding nine days) INHERITED Fable, exhausted the account
  tier, and **arch + web — who dispatched nothing — had their Monday fires refused.** A cost
  my seat imposed and could not see: nothing in seat config, the belt, or any lane report
  shows "this dispatch spends a pool two other roles depend on."
  LEVER ADOPTED: the Agent tool takes `model:` — a dispatch does NOT have to inherit; I just
  never passed it. Pard's "pinned fire is immune" was available at my call site all along.
  Policy in the carry-forward Standing section: pin explicitly every time, default mechanical
  lanes to a cheaper tier, reserve top tier for novel design + say why. Two caveats sent with
  it: I cannot verify the saving (no usage API — activity claim, not spend), and **quality is
  what to watch, not cost** — if cheaper lanes stop catching what today's caught, the saving
  is false and I want to hear it early.
  ✅ **ARCH RE-RULED DocumentDB — my pushback was right**, and they checked all three
  load-bearing facts themselves before conceding. Registered with its OWN reason line (the
  INVERSE shape: live DB class, dead domain twin) so the registry distinguishes the two
  sub-shapes, with the both-sides rule written in beside them. **#1788 CLOSED; PM-056 job 2
  is now honestly GREEN** — checker 0 issues, down from 13 across 7 models.
  ⚙️ Did this one MYSELF rather than dispatching — fitting, given the finding.

- 13:02 — 🔴 **PM DIRECTIVE: server-key exposure is now the highest-priority issue.** Filed
  **#1807** with PM's verbatim words. The mechanism, stated for the record: resolve_request_
  api_key goes header → stored → **SERVER KEY** for ANY authenticated caller. #1320 closed the
  ANONYMOUS case; the authenticated-but-keyless case was never closed. The docstring calls the
  fallback "safe, because the caller is a known, authenticated identity" — **that conflates
  authentication with BILLING AUTHORIZATION.** True enough when the only authenticated
  identity was PM; false the moment a second person logs in. Janne's invite is ready to send,
  so this is LIVE exposure, not theoretical. Lane dispatched (top tier — security + an
  unruled design question, per my own pinning policy which says say why).
  Also per PM: **#1785 CLOSED "no"** (suite stays on its conversation-code trigger; the waste
  half was the real defect and it's fixed). **#1791 escalated to MVP** — PM: "per user! we
  can't ship an app that leaks between users anywhere!" The #1751 honesty test that pins two
  users reading identical bytes was written to FAIL when this lands; that moment is now.
  ROUTED to Arch+PPM as PM asked: both holes are epic-2-class arriving AFTER epic 2 closed.
  Three shapes offered (reopen / successor tenancy-hardening epic / MVP singletons), my weak
  preference stated as the successor epic, ordering consequences left to PPM.

- 13:11 — #1802 CLOSED (66fb7bfbc pushed; **deploy DEFERRED** — the #1807 lane has WIP in this
  same tree, so a full sweep and ruff are contaminated; targeted verification only: 27 passed
  on the three affected files, ratchets 58, all 24 mypy codes at ceiling).
  Fix: _check_database uses session_scope_fresh (#442) — the DB-side twin of #1452's Redis
  guard, which is the exact asymmetry the issue named. 200,503,503,503,503 → 200×5.
  **Three findings worth more than the fix:** (1) 🔴 TokenBlacklist.initialize() is NEVER
  CALLED anywhere in the running app — Redis has silently never been used for blacklist
  storage in production; every check took the DB path (#1808, with the same dead-loop defect
  still live on the WRITE paths); (2) conftest's autouse _1452_session_scope_nullpool
  monkeypatches session_scope for EVERY test, masking this defect suite-wide; (3) two sibling
  tests silently stopped exercising their failure paths after the switch — they stubbed only
  session_scope, so the unstubbed session_scope_fresh MagicMock auto-returned truthy and they
  kept passing while testing nothing. Found by RUNNING the suite, not inspecting it.
  ⚠️ **MY PROCESS SLIP, owned: I ran two lanes concurrently in one worktree.** I dispatched
  #1807 while #1802 was still live, violating my own one-lane rule. The 1802 lane navigated it
  well (scoped its staging, traced the stray failures to the other agent's WIP, said so) — but
  that was its diligence covering my error, and the failure mode it avoided is exactly the
  cross-contamination the rule exists for. Rule restated: ONE lane at a time in this worktree.
  📊 **Pinning-policy data point: this lane ran on the CHEAPER tier and caught all three of
  the above.** No quality regression visible. Continuing the policy.

- 13:25 — **#1807 CLOSED + DEPLOYED v106** (health 200). PM's top priority is closed BY DEFAULT:
  flag off ⇒ nobody spends the server key, PM included. Red-first written before any
  production edit, driven through the real route observing the ACTUAL client selector:
  "an authenticated user with no key of their own consumed the SERVER's Anthropic client."
  Controls (stored key / BYOC header / #1320 anonymous) green throughout.
  Copy pinned as distinct from both siblings — never "sign in" (they ARE signed in), never
  "try again" (retrying without a key changes nothing).
  Operator gate: identity follows the EXISTING resolve_pm_owner_id convention; the second
  knob (PIPER_OPERATOR_SERVER_KEY, absent⇒off) is the lane's OWN design choice and was
  flagged as such, not dressed as convention. Fail-closed by construction: is_operator is an
  injected arg, so forgetting to wire it REFUSES.
  ⚠️ **Four pre-existing assertions PINNED THE HOLE AS INTENDED BEHAVIOR** — the tests were
  defending the exposure. Amended with inline notes.
  Verified: 37 new + 6 no-regress, full unit 11192 (1 unrelated pre-existing, A/B/A-confirmed,
  #1811), ratchets 58 + 24 codes at ceiling, smoke 521, ruff 1763 clean.
  🔴 **#1810 IS WORSE THAN #1807 WAS** — escalating to PM now.

- Fire 4 (15:31) — WORK. Inbox 3→0.
  ✅ **PPM created EPIC 12 (Tenancy hardening)** — the successor-epic shape I weakly preferred,
  with the reasoning I gave: epic 2 stays closed for what it CONTAINED, and PM's escalation
  gets explicit tracking instead of Product-Backlog overflow. Owns 1807 (closed) · 1791 ·
  1750/1751 · and PPM folded in 1809 + 1810 unprompted. Also fixed a board gap: CLOSED issues
  can be missing from the board too — same shape as last week's open-issue version.
  ✅ **EXEC: HOLD Janne's invite until #1810.** Right call and I support it — his onboarding is
  literally the trigger that would overwrite the global key slot.
  🔎 **CXO's copy concern — CHECKED against shipped code rather than accepted or dismissed.**
  Half covered: the lane did NOT route the keyless state through the error table; it added
  explicit copy at both raising entry points, pinned never to say "sign in" (they ARE) or "try
  again" (retrying changes nothing). Half NOT covered, verified by grep:
  UserLLMKeyRequiredError is caught in EXACTLY TWO places — the same two that raise it. The
  table still has no "none configured" pattern; it's unreachable today only because coverage
  is two-entry-point-narrow, which is a coincidence of scope, not a guarantee.
  ⚠️ **The interaction worth the memo: fixing #1809 by inverting the "unbound ⇒ server key"
  default converts silent-spend paths into RAISING paths — which is exactly when CXO's gap
  stops being theoretical. The copy must land BEFORE or WITH #1809, not after.** Asked CXO for
  the table entry in their voice now rather than under time pressure later.

- 17:29 — **PM's key question answered from EVIDENCE, filed as #1812.** I could not find a
  principled need for a product-owned key: background/scheduled jobs make ZERO LLM calls
  (grep 0 across composting, inversion shadow, ethics cleanup); startup only WIRES a filter
  onto the client object, never completes; setup validates the key BEING SUBMITTED, paid for
  by that key. The only real consumers are the PM convenience fallback (closed by #1807),
  Slack inbound (a user turn through a door that doesn't carry the user's key), and any path
  that forgets to bind (#1809, default-open). **None is "the product needing an API for its
  own purposes."** Likely origin: clients.py:675 constructs a module-level singleton AT IMPORT
  TIME — a client existing before any user does necessarily needs a key belonging to nobody.
  That's the pre-BYOC world made structural, when there was only one person and no
  operator/user distinction to draw. **So the question becomes: delete the concept, not gate
  it.** Stated limits: I did NOT exhaustively trace MCP consumers or the ethics detector's
  call context.
- 17:29 — 🔴 **PM OVERRODE the epic shape: REOPEN epic 2, don't use a successor.** Verbatim: "the
  truth is more important than the feeling of progress." **My recommendation was wrong and I
  own it** — my stated reason was "it keeps 'epic 2 closed' true (it was, for what it
  contained)," which optimizes for a SCOREBOARD property. Epic 2 was NOT complete; we closed
  it before discovering the work — a different fact from "complete, then new work arrived."
  I spent the weekend insisting denominators be honest and then applied the principle to
  counts but not to my own epic boundary. Relayed to PPM (epic 12 is 2h old, cheap to unwind),
  carrying the correction myself since PPM adopted my reasoning in good faith.

- 17:30 — **PM RULED: the server key is not a real concept; don't support it in any sense.**
  Recorded in decisions.log as a ruling (not a preference) and written up on #1812 as a
  6-step dependency-ordered plan: (1) #1810 stop the global write · (2) CXO's "no key
  configured" copy — a PREREQUISITE not a follow-up · (3) #1809 invert the default so unbound
  is an ERROR · (4) Slack inbound resolves a real user's key or refuses · (5) REMOVE #1807's
  PIPER_OPERATOR_SERVER_KEY (under this ruling it's a transitional seam, not a setting) ·
  (6) the module-level singleton at clients.py:675, the root.
  ⚠️ Flagged the operational consequence on the issue and to PM: once step 3 lands, **PM's own
  hosted usage requires PM's own stored key**, same as any user. Intended per the ruling, but
  it should be a deliberate moment, not a surprise mid-session.
  Step 1 DISPATCHED (#1810, cheaper tier — the ruling removed the design question, leaving
  mechanical work). Scoped deliberately narrow: remove the write, CENSUS the readers without
  changing them (that's #1809), report existing stored entries by count/owner and **NEVER
  print key material**, and **do NOT delete any stored credential** — purging is destructive
  and PM-gated. Check the other providers for the same double-write shape.

- 17:32 — **PM supplied the PRINCIPLE, not just the ruling: "We don't use the business model of
  reselling tokens or anything like that."** That settles the whole question — a product-owned
  spendable balance is only coherent under a resell/included-usage model. We're BYOC: every
  call is the user's, billed to the user. So the product has no reason to hold spendable
  credit AT ALL, which reframes what I filed this week: #1807 was a DEFECT, not a config
  choice; #1810 is a cross-user credential write, not an awkward cache.
  **My own gap, named: I spent the day fixing WHO may spend the operator's money without ever
  asking why the operator has money to spend.** PM asked it; the business model answered it.
  Recorded BOTH in decisions.log — the rationale (so nobody re-derives the phantom) and PM's
  falsifiable clearing condition ("unless someone can describe some sort of minimal bootstrap
  situation"), with the burden of proof on the claimant and the census result attached: no
  such case exists; the one plausible candidate (a startup self-test) doesn't exist and would
  belong in CI with a test key anyway.

- 17:35 — Tracker refreshed for PM and republished (v106-epic2-reopened), same URL. It had been
  stale since Saturday's day-close and was actively WRONG in the way that matters most: it
  still said "EPIC 2 security/tenancy: FULLY CLOSED" while PM had just ruled it reopened.
  Rewrote header, banner (the server-key phantom in PM's own framing — their question had no
  good answer, and the census is why), and three epic rows. The verification row now says
  "nothing waits on you right now" honestly, and names the ONE thing that will: a deliberate
  moment to store PM's own LLM key before the server-key deletion completes.
  Did tracker work — not another lane — because #1810 is still running and one-lane-at-a-time
  is the rule I broke earlier today.

- 17:48 — **#1810 CLOSED + DEPLOYED v107** (health 200). The cross-user credential write is gone:
  setup no longer writes the global unprefixed slot. Red-first captured the actual clobber
  before the fix. Per-user write untouched.
  **The asymmetry worth stating: removing the WRITE stops new clobbering; the READERS still
  resolve whatever is already there.** Census reported and deliberately NOT changed (that's
  #1809): get_api_key() — which has no user parameter AT ALL · LLMClient._init_clients feeding
  the import-time singleton · knowledge-graph ingestion's direct global read · the wizard's
  pre-login existence checks. That gap IS the rest of #1812's six steps.
  Nothing purged — existing entries reported, not deleted; purging a credential is destructive
  and PM-gated, and hosted was deliberately not probed from this worktree.
  Two MORE writers to the same slot found and flagged rather than fixed: main.py's `keys add`
  CLI and llm_config_service's migration helper — single deliberate operator writes, not a
  double-write inside a user flow. They die with the concept in #1812.
  unscoped_reads ceiling 54→52 (matching the two removed hits). Filed #1813 (cross-user
  isolation tests use hardcoded UUIDs with no teardown — poison subsequent runs).
  JANNE'S INVITE: the blocker Exec held it on is now cleared. Flagging to Exec/HOST that the
  hold can lift — though #1809 still means unbound paths resolve the server key, so I'd want
  Exec's call on whether that's enough to unblock onboarding.

- Fire 5 (18:34) — WORK. Inbox 6→0. **CXO's memo contained a LIVE TRAP and I fixed it myself
  (v108 deployed, health 200) rather than queueing it.** user_friendly_errors.py:46's recovery
  told an out-of-quota user to "remove the current key to fall back to the built-in model" —
  a fallback #1807 DELETED. So a tester whose key ran dry, following OUR OWN ADVICE, would
  delete their key and land in the keyless refusal. The advice didn't just go stale; it
  actively routed them somewhere worse. CXO called this the higher-priority half of their memo
  and they were right: the entry I ASKED for is unreachable today, this one is reachable by
  any tester whose key runs dry.
  Also landed CXO's keyless table entry — deliberately WEAKER than my entry-point copy,
  and their reasoning is the good part: surface-neutral because a stray raise could come from
  a background job; NO "Nothing was charged" because a generic handler can't cash that claim
  (their §5b applied to their own copy); recovery promises the ACTION only, never an outcome.
  CXO gave copy and explicitly left the regex to me — so the pattern is matched to what
  request_key.py ACTUALLY raises, and the test drives a REAL exception instance rather than a
  hand-written string, which is the only way to pin that.
  Also in: HOST corrected their own "ready to send" claim; PM RATIFIED Exec's invite hold
  (Arch relayed, with the clearing condition to be OBSERVED not assumed); Arch ruled #1810's
  global slot has NO legitimate consumer — delete the write AND invert the default; CIO
  checked their own dispatch exposure adopting the same discipline; Janus sent the dispatch-
  tier guidance I asked to own.

- Fire 6 (21:49) — WORK. Inbox 6→0. The substantive one: **EXEC DECLINED TO CALL THE CLEARING
  CONDITION MET, correctly.** Arch's condition (PM-ratified) asked that someone COMPLETE A
  SETUP FLOW and confirm no global entry was created; what existed was a test pin. That is the
  m-43 layer distinction, applied to a gate I'd have been tempted to call satisfied.
  **Took the observation myself rather than leaving it.** Half done immediately, read-only
  against PRODUCTION through the same KeychainService the app uses:
  backend=EncryptedDBCredentialStore · GLOBAL anthropic: absent · GLOBAL openai: absent.
  No key material printed. Other half dispatched: a real setup flow end-to-end on the deployed
  code with the store forced onto the DB-backed backend to match prod, confirming BOTH that
  the per-user entry APPEARS and that no global one does — the pairing matters, because an
  absent global proves nothing if setup simply failed.
  Constraints set: not against prod · must NOT consume Janne's minted token · never print key
  material · clean up and verify by count · STATE THE LAYER (deployed code path on a local
  server with a prod-matching backend is NOT a production setup run) · STOP and say where if
  the flow can't complete, because a partial run reported as success is worse than no run.
  The call stays Exec's and HOST's — I bring evidence, they decide. Also in: PPM explained the
  6→12 epic jump AND owned the epic-12 mistake; Arch supplied the factoring provenance behind
  part of the jump; Arch recommends LIFT with one onboarding condition; HOST says hold until
  the bar is actually met. CXO's rider: whoever runs the flow closes their oldest unobserved
  claim at zero marginal cost — folded into the lane's brief.
