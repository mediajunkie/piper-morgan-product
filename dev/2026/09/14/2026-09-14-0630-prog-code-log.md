# Session Log — Coding Agent (prog) — 2026-09-14

**Role**: Coding Agent (prog)
**Tool**: Claude Code
**Model**: claude-opus-5
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (Lead Dev's, Model A)
**Branch**: `claude/lead-cycle`
**Assignment**: Lead Dev → GitHub Issue #1777 — sweep of #1425 `None`-sentinel consumers

---

## Task

Issue #1777: `_handle_agenda_query`'s `agenda_sources["todos"]` took a bare `len()`
over the #1425 `None` sentinel (the "this source failed" value), so a todo-source
failure crashed the whole agenda response *after* the formatter had honestly
composed "I couldn't check your tasks." The #1776 lane guarded that one call site
and filed #1777 to hold the **sweep of other sentinel consumers**. That sweep is
this session's job.

Damning shape from the filing: the crashing line sat four lines below a sibling
whose comment explains that exact crash.

Plan:
1. Census every consumer of the #1425 degrade sentinel (by consumption kind + file)
2. Classify: (a) crashes; (b) silently misreports (fabricated zero); (c) safe
3. Fix (a) and (b) via the honest-degrade path already in the system
4. STOP + report on anything needing a design call (CXO contract §6.1 case 3)
5. Pin each fixed site with a test driving the sentinel through the REAL path

---

## Timeline

- **06:30** — Session start. Read #1777 in full (`gh api ... --jq '.body'`;
  note: `gh issue view` piped output was flaky in this shell, `gh api` was reliable).
  Created this log. Beginning the census.

- **06:30–06:38 — the census.** Traced the #1425 honest-degrade machinery from the
  `SOURCE_FAILED_FLAGS` registry (`conversational_floor.py:152`, #1717) back to its
  producers. The family splits in two, and only one half can crash:

  * **Bare-`None` sentinel** (4 producers) — a function whose success value is a
    list returns `None` to mean "failed". `len()`/iteration over it raises.
  * **Dict-flag sentinel** (6 producers) — `{"source_failed": True}` /
    `{"<lane>_source_failed": True}`. Structurally crash-proof; its failure mode
    is a *dropped* disclosure, not a TypeError.

  Every consumer of the bare-`None` family traced by call site. **Finding: the
  crash class was already down to one site (the #1776 guard), but the
  FABRICATED-ZERO class had three — and the #1776 guard itself was one of them.**
  `len(todos) if todos is not None else 0` stops the crash and then reports the
  failed source as having contributed zero todos, beside a sibling that says
  `None` for the same condition in the same dict.

  The other two are in `_handle_retrospective_query`, four lines apart, both
  `len(completed_todos or [])`. **`or []` is why they survived every prior #1425
  round: it cannot raise, so it reads as a guard. It is a default, and the
  default it supplies is a claim about the user's day.** Exact structural twin of
  #1777, one handler away, in the same file.

- **06:38 — red first, through the real path.** Wrote
  `tests/unit/services/intent_service/test_sentinel_consumer_sweep_1777.py`.
  Every test makes `AsyncSessionFactory.session_scope` raise, so the handler's own
  `except` *produces* the sentinel rather than a mock returning one (m-43: a
  stubbed sentinel pins the consumer's guard and proves nothing about the path,
  and the #1777 crash lived on the path). **6 failed, 9 passed** — all six
  `assert 0 is None`, the fabricated-zero class; the nine green controls confirm
  the formatters really are honest via the genuine failure path.

  Then reproduced the ORIGINAL crash by temporarily reverting the #1776 guard:

  ```
  services/intent_service/canonical_handlers.py:2759: in _handle_agenda_query
      "todos": len(todos),  # TEMP REVERT
  E   TypeError: object of type 'NoneType' has no len()
  [warning] Could not fetch todos for agenda (source failed): todo source is down
  ```

  The warning line is the point: the honest-degrade machinery ran to completion,
  and then the payload assembly killed the response.

- **06:39 — fixes.** Three class-(b) sites → `None` instead of a fabricated `0`.
  Plus the annotation repairs described in the finding below. Updated the #1776
  pin (`test_gather_cap_denominator_1776.py:347`) from `== 0` to `is None`.

- **06:39 — the AC3 finding, which is the most useful thing here.** #1777 asked
  whether "sentinel guarded at every consumer" is mechanically enforceable.
  **It already is. The mechanism already runs on every push and already reported
  every drift site in this file — including #1777's own crash — and could not act
  because the #1436 gate ratchets per-code AGGREGATES and these sat under
  ceilings of 377 / 240 / 49.** Measured pre-fix:

  ```
  canonical_handlers.py:2710/2714/2718: Argument 2 to "_format_agenda_*" has
    incompatible type "list[dict[Any, Any]] | None"; expected "list[...]"  [arg-type]
  canonical_handlers.py:2864: Incompatible return value type (got "None",
    expected "list[dict[Any, Any]]")                                 [return-value]
  canonical_handlers.py:3216: Incompatible types in assignment (expression has
    type "None", variable has type "int")                              [assignment]
  ```

  Those five lines *are* the census. So the answer is not "new ratchet" or "typed
  sentinel object" or "per-site discipline" — it is **scope the gate we already
  have**. Filed as #1800 for Arch (choosing the scope unit is an architecture
  call, not a coding-agent one). Corrected all five annotations so the declared
  contracts stop lying, and lowered the three ceilings in the same commit per the
  gate's shrink-only rule: arg-type 377→374, assignment 240→239,
  return-value 49→48.

- **06:42–06:44 — verification.** `ratchets` 58 passed + mypy gate all 24 codes at
  ceiling. `smoke` 506 passed / 1 skipped. `unit` over
  `tests/unit/services/intent_service` 4317 passed. `ruff format --check .` clean
  repo-wide after formatting the two touched files.

- **06:45 — discovered work filed** (both MVP + Product Backlog, verified on the
  board; #1777's own row confirmed untouched and the Status option count still 6):
  * **#1799** — the same shape in the DICT-FLAG family: `_get_priority_metadata`'s
    failure is read by 1 of 3 formatters. GRANULAR discloses; STANDARD and EMBEDDED
    silently omit. **Stopped rather than fixed**: the EMBEDDED terse register needs
    a CXO copy call (GatherOutcome contract §6.1 case 3, the discriminating case),
    and inventing that copy is not mine to do.
  * **#1800** — the mechanical-enforcement finding above.
