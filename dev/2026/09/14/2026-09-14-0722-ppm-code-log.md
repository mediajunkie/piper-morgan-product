# PPM session log — 2026-09-14

**Role**: Principal Product Manager (PPM) · slug `ppm-code` · **Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` · job `b72aa208` (re-armed 09-13 22:22 STOP; unchanged today)

### 07:22 START — recovered from this morning's 7-role delayed-start event

**Heartbeat + sync**: clean, one cron job (`b72aa208`, survived overnight intact), 0 ahead/0
behind. Prior day's log (2026-09-13) confirmed carrying its `DAY-CLOSED` sentinel.

**Worth an honest note on my own seat**: my expected fire was ~06:52 (cron `52 6,9,12,15,18,21`);
this START actually ran at 07:22, ~30 minutes late. Exec's alert (read this fire, below) named PPM
as one of seven roles that hadn't started this morning as of ~07:20 — arch, cio, cxo, docs, pa,
ppm, web all STOPped cleanly last night (21:53–22:42) and produced nothing until well past their
expected 06:xx fires, while comms/lead/host fired normally. The cron object itself was intact
(`CronList` showed the same job on arrival), so this matches the emerging pattern from the last two
days: an armed cron surviving is not proof the schedule actually fires on time. No corrective
action available from inside this seat — this fire firing at all is the recovery.

**Mail**: one item, Exec's own escalation of the above to PM, cc'ing CIO/HOST/Lead/PPM. Filed to
`read/`, sent, landed clean — no PPM action needed beyond the self-acknowledgment above, since I am
literally the evidence this seat resumed.

**sprint-truth.py**: `MVP: 52 not done (28 Sprint Backlog, 3 In Progress, 6 In Review, 15 Product
Backlog); 1165 done` — **1 unmilestoned** (`#1798`, a git-hooks/process-infrastructure finding from
CIO's own lane). Triaged: milestone Ongoing (not MVP), Sprint=FLYWHEEL, Status=Sprint Backlog,
matching `#1720`'s precedent exactly. Not part of the MVP epic-order file.

Nothing else unblocked this fire.

### 10:22 WORK — the morning's alert corrected, DocumentDB resolved

**Heartbeat + sync**: clean, one cron job (`b72aa208`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
52 not done (28 Sprint Backlog, 3 In Progress, 6 In Review, 15 Product Backlog); 1166 done` — **1
unmilestoned**.

**Correcting my own START entry**: Janus (resident on Amber, investigating directly) found this
morning's "7 roles down" alert was wrong in the cause, though right in the raw observation. All
24 tmux sessions were alive the whole time; the actual cause was an account-level Fable 5
model-tier ceiling hitting arch and web specifically — a fire that gets admitted and then refused
at the model layer produces the same silence as a fire that never ran, which is indistinguishable
to any commit/heartbeat-based instrument. CIO's seat was never even due (a false positive baked
into the belt's 06:xx assumption, since CIO doesn't run that cadence). The other four — pa, cxo,
**ppm**, docs — self-recovered within 18 minutes with no real problem at all. Worth being precise
about my own status here rather than let this morning's framing stand: I wasn't part of a real
outage, I was part of the noise around counting a number that was still moving. Lead corroborated
independently with a clean natural experiment (their own seat, on the exact same host/cron/skill,
differed only by being on Opus rather than Fable since yesterday's model switch — fired every
tick, no interruption).

**Resolved the DocumentDB disagreement from last night's log**: Arch checked Lead's three
load-bearing facts personally and re-ruled — DocumentDB is cat (2), not cat (1). Arch's original
census had measured only DocumentDB's live DB-side importer; Lead's find (the domain twin is also
dead, and the two classes aren't even the same shape) was correct, and the generalizable "liveness
must be measured on both sides" principle is now written into the ruling itself, not left as a
footnote. Updated epic 11's entry with the full resolution — `#1788` is one registry entry from
green. One new orphan surfaced (dead domain `Document`, credited to Lead, not part of `#1797`'s
set) — noted for whoever picks it up, not filed by me.

**Triaged the 1 unmilestoned issue plus a second that appeared mid-fire**: `#1798` (git-hooks
infrastructure, Ongoing/FLYWHEEL, matching `#1720`'s precedent) and `#1801` (the auto-generated
weekly docs-audit issue, Ongoing/Q-Recurring-Audits, matching every prior FLY-AUDIT instance
exactly). Neither belongs in the MVP epic-order file.

Also read (no action, cc-only): CXO's runnable fixtures for the acceptance contract's remaining
three cases — genuinely useful design work (found case 3 was never actually blocked on the
GatherOutcome epic, unlike CXO's own earlier belief), explicitly optional and unblocked-by-nothing.
Lead/Arch's own domain.

Committed and pushed the epic-order file. Mail sent in one 8-path call — landed clean, no warnings.

### 13:22 WORK — PM escalates tenancy, epic 12 created; seven issues triaged

**Heartbeat + sync**: clean, one cron job (`b72aa208`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
50 not done (27 Sprint Backlog, 3 In Progress, 6 In Review, 14 Product Backlog); 1169 done` — **7
unmilestoned**.

**The real work this fire**: Lead wrote directly, cc'ing Arch/Exec/CXO/PM, with a structural
question only PPM could answer. PM had escalated two new tenancy holes — `#1807` (BYOC key
resolution fell through to PM's own server key for ANY authenticated caller, not just anonymous
ones; the invite for the first external tester is ready to send, so this was live exposure) and
`#1791` (personality preferences have no per-user store at all) — as "our fundamental value and
promise," verbatim, not a sprint line item. Epic 2 (Security/tenancy) had closed two days ago.
Lead refused to guess and asked for a ruling: reopen epic 2, open a successor epic, or leave the
two as MVP singletons.

**Ruled**: successor epic. Created epic 12 (Tenancy hardening), matching Lead's own stated weak
preference — it keeps "epic 2 closed" true for what it actually contained while giving PM's
newly-named class its own explicit tracking rather than Product-Backlog overflow. Moved
`#1750`/`#1751` in from epic 2's old parking note (the two items I'd deliberately NOT folded into
epic 2 two days ago — PM's escalation is exactly the calculus-change that note anticipated).
`#1807` was already closed by the time I got to this (Lead's lane moved fast) but missing from the
board entirely — fixed, same drift shape as `#1772`/`#1785` just on a closed item. Two more
findings from that same implementation lane (`#1809`: the fix is entry-point-scoped, Slack inbound
still reaches the server key; `#1810`: setup flow stores a global unprefixed key copy that each
new user's setup silently overwrites) folded straight into epic 12.

**Triaged the remaining unmilestoned issues**: four docs-drift findings from the weekly audit
(`#1803`–`#1806`) went Ongoing/FLYWHEEL, matching `#1720`'s clean precedent. `#1811` (a
calendar-context test mocking `_get_todays_todos`'s return shape wrong) is the same test-theatre
class as `#1760` already in epic 5 — confirmed pre-existing and unrelated to `#1807` via the
issue's own A/B/A stash test, folded in there.

Replied to Lead confirming the ruling, cc'd to everyone on the original thread plus PM (a real
structural decision, not routine). Committed and pushed the epic-order file. Mail sent in one
16-path call — landed clean, no warnings, verified via `git cat-file -e` throughout.

### 16:22 WORK — epic 12 moves fast: the invite gets held, tracking only this fire

**Heartbeat + sync**: clean, one cron job (`b72aa208`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
53 not done (32 Sprint Backlog, 3 In Progress, 6 In Review, 12 Product Backlog); 1170 done` — **0
unmilestoned**, no drift.

**Six mail items, all epic-12 developments from the last three hours**. Lead escalated `#1810`
past what was originally asked: the leak runs both directions, and the inward one is worse — every
completed setup silently overwrites the global "server key" slot, last-writer-wins, which would
have meant Janne Lammi (the first external tester, invite ready per HOST) billing PM's account on
his own first action while silently displacing PM's key. Exec joined that thread with HOST's
ready-to-send token before it went out and called the HOLD explicitly; HOST corrected their own
two-day-old "ready" claim the same fire and marked the roster row HOLD, with a note that whoever
clears it should re-verify `#1810`'s closure directly rather than trust the file's memory of it.
Arch ruled the actual fix — traced rather than assumed that the global slot has no legitimate
consumer post-`#1807` — delete the write entirely, both provider branches, and ruled the
sequencing (`#1810`→`#1809`→`#1791`) is one architectural principle applied at three depths, not
three separate calls. CXO, mid-writing the copy Lead asked for, found a second live trap: the
*existing* out-of-quota recovery message tells users to remove their key to fall back to a
built-in model that `#1807` already removed for non-operators — reachable today, not gated behind
anything.

**No new PPM ruling needed** — Exec explicitly named this in their own memo: *"PPM has already
created epic 12 (tenancy hardening) for the family, which answers the 'where does this sit'
question."* My job this fire was keeping the standing artifact current against fast-moving
ground truth, not deciding anything new. Updated epic 12 with the full escalation, the hold, and
CXO's unfiled finding (noted for whoever picks it up, not filed by me since I didn't do the
verification work behind it).

Committed and pushed the epic-order file. Mail sent in one 12-path call — landed clean, no
warnings.
