---
role: Chief Experience Officer (CXO)
slug: cxo-code
model: Opus 5
date: 2026-09-14
worktree: ~/Development/piper-morgan-worktrees/cxo (Model A)
branch: claude/cxo-cycle
cron: 47 6,9,12,15,18,21 * * * (job 904f12cd, re-armed at the 09-13 STOP; expiry ~09-20)
---

# CXO session log — 2026-09-14

## Fire 1 (07:17) — START

✅ **Prior day (09-13) verified CLOSED** — first action, before anything else.
✅ **Cron single**: `904f12cd`, correct expression.

Tracker `· cxo: 11`, both guards clean. State-currency green (2 verifiable claims, neither stale).
MVP **52**. UX queue **2** (both with my halves delivered; #1108's build unowned, #1174 discovery
complete).

### 🔴 I gave myself a false positive by running a check out of its specified order

**My first self-verify this fire returned:**

```
BELT-INVISIBLE cxo — alive (0h since last commit/session-log signal) but no heartbeat row for
2026-09-14; last invoked 8h ago (2026-09-13) — past threshold: the writer ran before, then stopped
```

⚠️ **Correct, and a false positive — because I ran it in the wrong place.** 📄 The skill specifies the
self-verify *"right after the heartbeat command."* **I folded it into my START check batch instead**, so
it measured a day whose heartbeat row didn't exist yet. ✅ **Ran the heartbeat, re-ran the check in its
specified position: clean.**

⭐ **The lesson is small and exact: a check's POSITION IN THE SEQUENCE is part of the check.** ⚠️ **On
the first fire of any day, a pre-heartbeat self-verify reports BELT-INVISIBLE by construction** — the
same false-positive-by-ordering class as the NO-SESSION-LOG race I diagnosed on 09-11, **arriving this
time from my own sequencing rather than from the tool.**

🔴 **And the near-miss is the part worth recording**: the message reads *"the writer ran before, then
stopped"* — **case-(c) language, the exact shape of the five lapses I spent last week cataloguing.** **It
would have been very easy to report this outward as a real finding**, and I'd have been wrong in precisely
the way I corrected two colleagues for. **What stopped it was checking the skill's wording before
believing my own alarm.**

## Fire 2 (10:17) — turned "not discharged" into fixtures, and found I'd been wrong about why it wasn't

Inbox empty, tracker 11, both guards clean. MVP **52**. UX queue 2. **Quiet on all three sources.**

🔴 **I have twice told Lead the contract's §6 isn't discharged and twice left it there.** ⭐ ***"Not
discharged" is a status, not a spec*** — and **a rule with no mechanism is precisely the failure this
contract keeps documenting in other people's work.** **So I wrote the mechanism.**

### ⭐ The finding: case 3 was never blocked on the epic

**I'd been treating the verified-empty/source-failed discriminator as waiting on the GatherOutcome
model.** 📄 **Traced `context_assembler.py` instead — all four provenance states are representable
TODAY**, just not by one typed field:

- **`verified_empty`** → ⭐ **an empty list PLUS a zero count** (`:1301`, #1544 — *"returning None here
  made verified-empty indistinguishable from never-gathered"*)
- **`source_failed`** → a dedicated `{"<lane>_source_failed": True}` (`:1405`, #1645)
- **`not_attempted`** → **the key is simply absent**

🔴 **So the discriminating case is constructible right now and I was wrong that it wasn't.** ⭐ **A
blocker I'd been carrying in my own tracker dissolved on one file read** — the same shape as #1166 and
#1174 last week, where my rows were false about their own subjects. **Three in eight days; the pattern is
that I record a status once and then reason from the record instead of the source.**

### The fixtures — two, not one

**A (cases 3 + 4)**: *"what's my status?"* with `pending_todos: []` + count 0, `projects_source_failed`,
and the completed-todos key **absent**. **B (case 2, Exec's rider)**: ⚠️ **cannot ride A** — the rider
case needs a failed source **irrelevant** to the question, and A's question makes everything relevant.
So: *"what's on my todo list?"* with todos populated and projects failed.

**Pre-registered the scoring before any run** (0 for 3 on recomposition). ⭐ **The property implementers
will find least intuitive: a hedged verified-empty is an AUTO-FAIL** — *"nothing pending,"* not *"I don't
see any pending todos."* **The anti-fabrication instinct pushes toward hedging everything, and §3 says
resist it exactly there.** **And for A the case IS the conjunction**: definite and hedged, two slices,
**one reply.**

**Contract → v0.5, §6a.** 🔴 **Sent to Lead as an OFFER, explicitly unblocked-by-nothing, with the
sequencing named** — if it waits for the epic's turn that's correct, and I said so rather than plant a
trigger. **Same two limits restated: single-shot per cell, and my read would again be design-intent
rather than independent, since I wrote both the criteria and the copy rules they test.**

**Discovered issues filed**: None.

## Fire 3 (13:17) — a live lane needed copy it didn't have, and #1791 exposed a defect in my own

Tracker **11 → 13**, both guards clean. MVP **51**.

**Lead's tenancy memo went to Arch and PPM** (epic shape / ADR-075 fork — **neither mine**). ⭐ **But two
CXO consequences fall out of it, and one is time-critical because a lane is closing #1807 right now.**

### 🔴 The #1807 safe default lands a keyless tester in copy the file itself calls a lie

**Removing the server-key fallback creates a state the error table has no pattern for: *the caller has no
key at all*.** 📄 `user_friendly_errors.py:36–60` covers **out of quota**, **invalid key**, **all
providers failed** — **not "none configured."** ⚠️ **So a keyless tester most likely lands in the third**:
*"I couldn't reach a language model just now… usually a brief outage — try again in a moment."*

🔴 **False diagnosis, no path forward** — it isn't an outage and retrying never fixes it. ⭐ **And that
file's own comment says so about the adjacent case**: *"'temporarily unavailable, try again' here is a lie
that leaves the user with no path forward."* ⚠️ **The fix would route a new permanent-config state into
copy its own author already identified as a lie for this shape — and the first external tester's invite is
ready to send.**

**Drafted a pattern in the table's own idiom**, with the second recovery sentence carrying 📌 PM's policy
in user-facing form: ⭐ ***"It's yours and it bills to you, not to us."*** **Told him to match the regex to
whatever his lane actually raises — I'm giving copy, not a pattern match.**

### ⚠️ And #1791 exposes a defect in MY OWN FTUX copy, seven days old

📄 My 09-08 line: *"(Running with a default configuration — nothing here needs setting up first.)"* **It's
gated on `PersonalizationContextRepository` (per-user, DB). But `PiperConfigParser.__init__` takes no
`user_id` at all** — it loads `config/PIPER.user.md` if present, else the default
(`piper_config_loader.py:34–44`).

🔴 **So the notice can be TRUE about the store it checks and FALSE about the configuration delivered.**
⭐ **I wrote that line arguing it was "a true, checkable claim about the product." It is checkable — and I
checked it against the wrong denominator.** ⚠️ **The same defect I've spent the week naming in other
people's mechanisms, in a string of mine.**

🔴 **Stated the limit rather than the alarm**: **I do not know whether that overlay exists in prod.**
**Absent from my worktree, and it's gitignored — so my worktree proves nothing.** ⚠️ **If absent, this is
latent, not live.** **One `ls` on the instance settles it and I can't run it.** **No copy change proposed
— the right fix depends on #1791's resolution, and if the overlay goes per-user my line is fine as
written.**

**Explicitly untouched**: the epic-shape question (PPM's), the ADR-075 fork (Arch's), and whether PM's own
use should require a stored key — 📄 **Lead already flagged that last one as a product call, correctly.**

**Discovered issues filed**: None (both ride existing issues).

## Fire 4 (16:17) — asked for one table entry, found the entry NEXT to it had become a trap

Tracker 13, both guards clean. MVP **53**. **Six memos drained.** ⭐ **My morning "time-critical" framing
resolved itself through others**: Exec put a **HOLD on the Jannes invite until #1810**, HOST corrected
their own *"ready to send"* claim, Arch ruled the global slot has **no legitimate consumer**, and PPM
created **epic 12 (tenancy hardening)**. **Nothing owed from me on any of it.**

**Lead checked my copy concern against what actually shipped** and reported it honestly in halves:
✅ **covered at both entry points** with copy better than my morning draft — *"I can't run this without an
LLM key of your own — Piper doesn't bill anyone else's account"* — 🔴 **but NOT durably**:
`UserLLMKeyRequiredError` is caught in exactly the two places that raise it, so **a third caller would
land in the generic handler.** **He asked for the table entry in my voice, ahead of #1809.**

### 🔴 Writing it surfaced something reachable TODAY

📄 `user_friendly_errors.py:46` still says: *"…**or remove the current key to fall back to the built-in
model**."*

⚠️ **#1807 removed that fallback for anyone who isn't the designated operator.** 🔴 **So an out-of-quota
tester who follows our own recovery advice deletes their key and lands in the keyless refusal.** ⭐ **The
advice doesn't merely go stale — it routes the user into a WORSE state than the one they started in.**

**Proposed a replacement recovery with no fallback offered, because there isn't one.** ⭐ **Flagged it as
the higher-priority half**: the entry Lead asked for is unreachable today by his own account; **this one
is reachable by any tester whose key runs dry.**

### The entry itself — three deliberate differences from Lead's

⭐ **Surface-neutral** (*"That needs"*, not *"I can't run this"*) — a stray raise could be a background job.
🔴 **It does NOT say "Nothing was charged."** ⚠️ **Lead's `/documents` version can — it knows the refusal
preceded any provider call. A generic handler catching an unknown caller cannot cash that.** ⭐ **Same
sentence, different warrant — my own §5b rule turned on my own copy.**
🔴 **The recovery promises no outcome.** His *"I'll pick right back up"* is warranted where the key is the
only blocker; **from the table we don't know that.**

**Inherited both his pins** (never *"sign in"*, never *"try again"*) **and echoed his phrasing on purpose
— so the two layers read as one product rather than two authors.**

**Discovered issues filed**: None (the trap rides #1807's thread; Lead's lane owns the file).
