# Session Log — Coding Agent (prog) — 2026-09-14 12:56 PDT

- **Role**: Coding Agent (prog)
- **Model**: claude-sonnet-5
- **Tool**: Claude Code
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (Lead Dev's, Model A)
- **Branch**: `claude/lead-cycle`
- **Assignment**: #1802 — "a bare TestClient gets one authenticated request." Verify the
  diagnosis already filed in the issue (split out of #1792), then fix. Two layers: (1)
  `_redis_available` stays silently False because `TokenBlacklist.initialize()` never runs
  under a bare TestClient; (2) `_check_database` uses the unguarded `session_scope()`, whose
  asyncpg connection is bound to a dead event loop from request 2 onward.

⚠️ **Noticed mid-session**: `dev/2026/09/14/2026-09-14-1305-prog-code-log.md` — another
`prog` subagent's log, working #1807 (LLM key fallback), in this SAME worktree, started a
few minutes after I did. Distinct filename per subagent-instance-per-day appears to be the
working convention (multiple `prog-code-log.md` files already existed today at 0630 and
0935 from earlier subagent runs). Kept my own git staging strictly scoped to my own files
throughout, per the multi-agent coordination protocol — never touched anything under
`services/llm/`, `web/utils/llm_key.py`, or other #1807-shaped paths.

---

## Context loaded

- `gh api repos/mediajunkie/piper-morgan-product/issues/1802` (the `gh issue view --comments`
  piped form returned empty per the known tooling hazard — used `gh api` instead, full body
  read, no comments).
- `services/auth/token_blacklist.py` (whole file) — `TokenBlacklist.initialize()`,
  `is_blacklisted`, `_check_database`, `_add_to_database`.
- `services/database/session_factory.py` — `AsyncSessionFactory.session_scope()` (draws from
  the global `db` singleton) vs `.session_scope_fresh()` (#442, per-call fresh engine bound
  to the currently-running loop).
- `services/cache/redis_factory.py` — `RedisFactory.initialize()`'s #1452 same-loop guard
  (the precedent the issue names; confirmed it's Redis-side only, no DB-side equivalent).
- `services/database/connection.py` — the global `db` singleton `_check_database` used to
  draw from via `session_scope()`.
- `services/auth/container.py`, `web/startup.py` — confirmed `TokenBlacklist.initialize()` is
  **never called anywhere in the codebase**, not even at production startup (grepped for
  `.initialize()` call sites; none exist). This is a broader latent finding than the issue's
  literal framing ("skipped under a bare TestClient" implies it runs in production) — filed
  as discovered work rather than expanding this issue's scope (see below).
- `tests/unit/web/test_blacklist_unavailable_honest_1792.py` — the sibling #1792 pin; reused
  its real-component harness pattern (real `AuthMiddleware`/`JWTService`, mocked stores raising
  for real) as the model for this issue's test.
- `tests/conftest.py` lines 680-737 — found the autouse `_1452_session_scope_nullpool` fixture
  that monkeypatches `AsyncSessionFactory.session_scope` to a NullPool engine for **every**
  test in the suite, specifically to cure this exact poisoned-pool pathology for other
  callers. Its side effect: a naive regression test built directly on `AsyncSessionFactory`
  would not reproduce the bug under pytest at all (verified by hand — see Work log), so the
  pin needed its own real-semantics stand-in that bypasses that one patched attribute only.

## Work

- **Reproduced red**, standalone script (before any fix): 5 sequential authenticated GETs
  through a bare `TestClient` (real `AuthContainer`-style wiring, `.initialize()` never
  called) → `[200, 503, 503, 503, 503]`, exact issue signature. Verbatim errors matched the
  issue precisely: req 2 `Task ... got Future ... attached to a different loop`; req 3+
  `sqlalchemy.dialects.postgresql.asyncpg.InterfaceError: cannot perform operation: another
  operation is in progress`. Diagnosis in the issue CONFIRMED correct — proceeded to fix.
- **Fixed `_check_database`** (`services/auth/token_blacklist.py`): switched
  `session_scope()` → `session_scope_fresh()` — the #442 manual opt-in, DB-side twin of
  #1452's Redis guard. Re-ran the standalone repro: `[200, 200, 200, 200, 200]`.
- **Made the silent flag loud** (AC item 2): added `self._initialized` sentinel (set in
  `initialize()`'s `finally`, regardless of Redis outcome) and `_warn_if_never_initialized()`,
  called from both `is_blacklisted` and `add`'s database-fallback branches. Purely additive
  logging (`token_blacklist_never_initialized`) — does not change which branch is taken, only
  whether an operator can tell "never initialized" apart from "initialized, Redis down."
- **Updated the #1792 sibling test's mock** (`tests/unit/web/test_blacklist_unavailable_honest_1792.py`,
  `_blacklist_database_raises()`): it only stubbed `session_scope` to fail; after the fix,
  `_check_database` calls `session_scope_fresh`, so its `database_branch` parametrize case
  silently stopped exercising the failure path (a MagicMock's un-stubbed `session_scope_fresh`
  auto-mocks a truthy `entry`, turning the outage into a false 401 "revoked" — caught by
  running the suite, not inspection). Fixed by stubbing both methods on the same failing
  scope. Reran: all 8 tests in that file pass.
- **Wrote the pinned regression test**,
  `tests/unit/web/test_bare_testclient_five_requests_1802.py`: five sequential authenticated
  requests through a bare `TestClient`, asserting `[200]*5`. Had to construct a
  `_RealSessionSemantics` stand-in (documented at length in the file) rather than use
  `AsyncSessionFactory` directly, because of the `_1452_session_scope_nullpool` masking
  fixture described above — verified by hand that a naive version of this test passed
  identically whether `_check_database` called `session_scope` or `session_scope_fresh`,
  which would have been a non-pinning test. The stand-in's `.session_scope` reproduces the
  real pre-fix pathology (draws from the real global `db` singleton) and its
  `.session_scope_fresh` IS the real untouched production method. Confirmed genuinely
  red-first against the *actual* source fix (not just the standalone script): stashed
  `services/auth/token_blacklist.py`'s changes, reran the pytest file → failed with the exact
  `asyncpg.InterfaceError` chain; popped the stash back → passes.
  Second test in the same file, `test_never_initialized_fallback_is_logged_not_silent`,
  pins the loud-flag change via `structlog.testing.capture_logs()`.
- **Discovered work not fixed here** (in scope per task: "if it changes production behavior,
  report instead" / general discovered-work discipline):
  - `TokenBlacklist.initialize()` is never called anywhere in the running application —
    `_redis_available` is always `False` in production too, not just under a bare
    TestClient. The DB fallback happening to work in production is incidental: a single
    long-lived event loop per uvicorn worker means the global `db` engine never sees a loop
    change, so the #1802 symptom never surfaces there — but Redis is never actually used for
    blacklist storage, silently, always. This is a materially different and larger finding
    than the issue's own framing implies (which reads as though initialize() runs in
    production and only skips under TestClient). Flagged for a follow-up issue rather than
    fixed here — wiring `AuthContainer.get_token_blacklist().initialize()` into
    `web/startup.py`'s phase list changes production behavior (Redis would start actually
    being used) and is out of this issue's stated scope.
  - `_add_to_database` and `_cleanup_database` (`services/auth/token_blacklist.py`) have the
    identical dead-loop defect `_check_database` had (both still call `session_scope()`), just
    on write/cleanup paths instead of the read path this issue's repro exercises. Left
    unfixed per explicit task scope ("adopt ... where `_check_database` needs it"); flagged as
    a sibling gap.

- **Found and fixed the same masking-mock gap in a second sibling test**,
  `tests/unit/services/auth/test_token_blacklist.py::TestTokenBlacklistOperations
  ::test_security_fail_closed_on_error` — same shape as the #1792 file's fix: only
  `session_scope` was stubbed to fail, `session_scope_fresh` fell through to an
  unconfigured MagicMock and stopped raising. Stubbed both. Full file: 17/17 pass after.
- **Filed discovered work**: `bd` (beads) is not installed in this environment
  (`scripts/bd-safe` exists but requires the `bd` binary, absent) — used `gh issue create`
  instead, consistent with how every other GH interaction in this session went. Filed
  **#1808** ("TokenBlacklist.initialize() is never called anywhere... `_add_to_database`/
  `_cleanup_database` share #1802's dead-loop defect"), milestone MVP, Status = Product
  Backlog (set via `updateProjectV2ItemFieldValue` against the Status field's existing
  option, never touching the option list itself).
- **Verification sweeps** (env: `POSTGRES_PORT=5433`, ANTHROPIC_* stripped per the CLAUDE.md
  server-launch gotcha, applied here to the sweep/pytest invocations too):
  - Red-first (source fix stashed, test kept): `test_five_sequential_authenticated_requests
    _all_succeed` FAILS with the exact `asyncpg.InterfaceError: cannot perform operation:
    another operation is in progress` chain. Fix restored (`git stash pop`): passes.
  - `tests/unit/web/test_bare_testclient_five_requests_1802.py` +
    `test_blacklist_unavailable_honest_1792.py` + `test_token_blacklist.py`: **27 passed, 0
    failed**.
  - `scripts/run-sweep.sh unit tests/unit/web tests/unit/services/auth`: 793 passed, 1
    failed — the 1 failure (`test_documents_per_user_key_1185.py::test_none_when_no_header
    _and_no_stored`) traced to the OTHER concurrent prog session's in-progress #1807 change
    in `services/llm/request_key.py`/`web/utils/llm_key.py` (confirmed via `git status` +
    reading the traceback — raises `UserLLMKeyRequiredError` from their new code, nothing to
    do with token_blacklist.py). Not mine to fix; left untouched.
  - `scripts/run-sweep.sh ratchets`: 58 passed; mypy gate — all 24 ratcheted codes at
    ceiling (total=1341), no new drift.
  - `scripts/run-sweep.sh smoke`: 519 passed, 1 skipped, 0 failed.
  - `ruffenv2/bin/ruff format --check .`: 2 files would reformat — both the OTHER agent's
    WIP (`test_authenticated_keyless_server_key_1807.py`, `web/utils/llm_key.py`); all 4 of
    my touched files individually confirmed already-formatted + `ruff check` clean.
- **Commit**: staged in one call (my 4 files only: `services/auth/token_blacklist.py`,
  `tests/unit/services/auth/test_token_blacklist.py`,
  `tests/unit/web/test_blacklist_unavailable_honest_1792.py`,
  `tests/unit/web/test_bare_testclient_five_requests_1802.py`, plus this session log),
  committed bare in the next, referencing #1802 without a closing keyword near the number.
  Not pushed, per instructions.

## Memory & briefing surfaces referenced this session

- **Referenced**: `docs/agent-protocols/*` not directly opened (task was fully scoped by the
  dispatching prompt); CLAUDE.md's server-launch env-var-stripping gotcha (applied to every
  pytest/script invocation); the "commit-discipline" stage-then-commit-bare rule (followed);
  discovered-work-capture discipline (issue #1808 filed immediately on finding the gap
  rather than deferred).
- **Loaded but not referenced**: the cron/duty-cycle sections, mailbox-discipline detail
  beyond the general principle (no mailbox message was owed here), worktree-model-A gotchas
  (no hook/worktree anomaly hit).
- **Wanted but not found**: no `mailboxes/prog/inbox/` directory exists for this role slug —
  confirmed via `ls` at session start (harmless; this subagent's assignment came directly
  from the dispatching prompt, not via mailbox).
