# Session Log: 2026-09-14-0932-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code — **Opus 5** (moved off Fable 5 by PM this morning: "we hit a limit in our access to the Fable model so I've moved you to Opus for now"). Prior fires this arc ran on Fable 5.
**Date**: Monday, September 14, 2026
**Start Time**: 9:32 AM PDT
**Prior session**: `dev/2026/09/13/2026-09-13-0957-web-code-log.md` — **DAY-CLOSED: 2026-09-13** (quiet Sunday; morning re-auth survived, WYSIWYG toggle quality-banked to a fresh session with the trigger named, zero code changes).

## Session Objectives

1. Self-report the cause of the gap HOST's freeze-check correctly flagged (they cc'd me explicitly so I could, per Lead's model from yesterday).
2. Normal duty-cycle drain.
3. Queue: WYSIWYG toggle (quality-banked to a fresh session — **this is that session**), website#35, Vercel Q1.

## Work Log

### 09:32 AM - Session Start (new session on Opus; gap self-reported)

- `CronList`: exactly one job (`a8417203`) — **survived**, so this was NOT a Gap-C
  dead-cron case.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-13 -->` — clean close,
  no self-heal needed.
- Fingerprint both worktrees: `web` / `claude/web-cycle` — correct. Both synced clean.
- Cohort-freeze check: `rc=0`.

**The gap, measured rather than asserted** (HOST's finding, cc'd to me for exactly
this): last output on `origin/main` was `stop(web)` at **2026-09-13 21:53**; now
09:32, so an **11.7-hour gap** — the 06:22 slot genuinely did not fire, and the
09:22 slot arrived only alongside PM's model-switch note. HOST's alarm was
**correct, not a false positive** (contrast the 09-11/12 belt findings, which were
race-induced false positives — this one is real).

**Cause, knowable from this turn and worth stating plainly**: PM moved this seat
from Fable 5 to Opus 5 this morning. A model switch restarts the Claude Code
session, and `CronCreate` jobs are session-scoped — the 06:22 fire had no live
session to land in. The cron visible now (`a8417203`) is the *same id* I armed at
yesterday's STOP, which is the interesting part: **the job object survived the
switch, the delivery of its 06:22 firing did not.** Reported to HOST/CIO/PM rather
than let the belt's correct alarm sit unexplained — and noting for CIO's belt work
that "cron present" was not sufficient evidence of "fires delivered" here.

Reported to HOST cc CIO/PM (`mail-send.sh`, `03883c89d`); triaged HOST's memo;
MANIFEST regen (`df22c1b7f`). **mail: 1 direct, 1 read in full; 0 cc.** At (0,0).

### 09:32–10:2x AM — WYSIWYG toggle BUILT (the banked work, in its named session)

This is the fresh session yesterday's quality-banking named as the trigger, so the
toggle got built rather than re-deferred.

**Shipped** (website `bb579b5`): a three-way **Source / Split / Preview** toggle on
the Body field, replacing the fixed side-by-side split. Split stays default;
Preview is the full-width rendered read PM asked for ("literally easier to read").
Caret + scroll preserved across toggles — the hard part, since the textarea
*unmounts* in Preview and its selection can't be read at toggle time; captured
continuously via `onSelect`/`onScroll`, restored in a `useLayoutEffect` (pre-paint,
so the caret never visibly lands at 0 first). Preview scroll follows the caret by
proportional line mapping — an honest approximation, documented as such, since
`mdToHtml` is regex-based and tracks no line numbers. View preference persists in
localStorage via a lazy initializer (no visible flip on load).

**Deliberately not a true WYSIWYG**, with the reasoning written into the code so it
isn't re-litigated later: re-serialization would normalize markdown (noisy diffs on
files Comms edits directly, more 409s) and could silently break publish-post.js's
house conventions — including the figure/figcaption rule shipped 09-09. Markdown
stays the single stored representation.

**Verified in a real browser, not just compiled**: hit the `PIPER_PRODUCT_ROOT`
resolution gotcha from 09-02 again and solved it safely — pointed the dev server at
an **isolated temp copy** of the drafts tree so no test action could touch a real
draft (also learned: `next start` is fail-closed on admin auth; `next dev` is the
mode that's open locally). Against a live 7157-char draft: caret set 4294–4306 →
Preview (textarea confirmed unmounted; preview scrolled to 1242, not 0, so the sync
works) → back to Source: caret exactly 4294–4306, scroll preserved, preference
survived a reload, zero page errors. `tsc` + production build clean. Screenshot
confirmed the rendered view reads as intended.

### 09:52 AM — Fire (cron 09:22 slot), quiet; seat visible again

Both worktrees synced clean, mail empty, freeze check `rc=0` — and notably
`web` now appears in the emitters list, so the staleness HOST flagged this
morning is resolved in the instrument, not just in my account of it.

Queue: website issues down to **#35 alone** (holding on PM's back/forward
navigation answer — a pending PM question, holds only that thread). Standing
items unchanged: obs-pass + site walkthrough + Buttondown PM-gated, Vercel Q1
access-blocked. The WYSIWYG toggle shipped and deploy-confirmed last fire, so
the banked item is discharged. Nothing unblocked remaining. Heartbeat
self-suppressed. Returning to idle.

### 12:52 PM — Fire (cron 12:22 slot): HOST closed the loop, nothing owed

Both worktrees synced clean, freeze check clean. Mail: **one direct memo** —
HOST acknowledging the self-report and, usefully, generalizing it for the belt:
*only a live fire, not `CronList` presence, proves delivery*, now carried
forward rather than re-derived. They note three different causes landing on
that same shape in four days (Lead's Saturday auth outage, this morning's
Fable-ceiling switch, the wider incident). Nothing asked of Web — read, triaged
(`0454ec3ef`), MANIFEST regen.

Queue unchanged: website#35 alone, holding on PM's navigation answer. Standing
items unchanged. **mail: 1 direct, 1 read in full; 0 cc.** At (0,0).

### 15:52 PM — Fire (cron 15:22 slot), genuinely quiet

Both worktrees synced clean, freeze check clean, mail empty. Issue queue still
#35 alone (PM-gated). Standing items unchanged. Heartbeat self-suppressed.
Returning to idle.

### 18:52 PM — Fire (cron 18:22 slot), genuinely quiet

Both worktrees synced clean, freeze check clean, mail empty. Issue queue still
#35 alone (PM-gated). Standing items unchanged. Heartbeat self-suppressed. Not
the last scheduled fire of the day (21:22 remains) — returning to idle, not STOP.

### 21:52 PM — STOP (day-close)

Last scheduled fire (21:22 slot; next 06:22 tomorrow). Both worktrees synced
clean, freeze check clean (`web` present in emitters), final mail sweep empty,
cleanup dry-run nothing stale.

**Day arc**: Opened on a genuine 11.7-hour dark gap that HOST's freeze-check
correctly caught — the first TRUE positive that belt has produced, after two
race-induced false ones on 09-11/12. Self-reported the cause from my own
evidence rather than confirming HOST's number back at them: PM's Fable→Opus
switch restarted the session, and `CronCreate` jobs are session-scoped, so the
06:22 firing had nowhere to land. The durable finding — **the cron object
survived the restart while its firing did not, so `CronList` presence was not
evidence of delivery** — HOST has now carried into the belt's model, noting
three different causes landing on that same shape in four days.

Then the substantive work: **the WYSIWYG toggle, built in the very session its
quality-banking named as the trigger.** Source/Split/Preview with caret and
scroll preserved across toggles (the hard part — the textarea unmounts in
Preview, so selection must be captured continuously and restored pre-paint),
proportional preview scroll-sync, persisted view preference. Browser-verified
against a live 7157-char draft on an isolated temp product root so no real
draft could be touched; caret returned exactly, zero page errors; deploy
confirmed `success`. Three quiet fires after.

**Queue at close**: website#35 alone (PM's navigation answer, or close on the
fix's merits). Standing items unchanged: obs-pass, site walkthrough, Buttondown
PM-gated; Vercel Q1 access-blocked; integration-reveals-all workDate on PM
recall. The WYSIWYG item is discharged.

**Commits**: website `bb579b5` (toggle, deployed); product-repo log/mail
commits.

## Memory & briefing surfaces referenced this session

**Referenced**: the quality-banking rule (yesterday's named trigger honored
today rather than re-deferred — the whole point of naming it); "never guess at
facts you can look up" (measured my own gap from `origin/main` timestamps
before answering HOST); m-43 layer-naming (browser-verified the caret claim
rather than trusting the build; stated plainly what the proportional scroll-sync
is and isn't); pause-before-irreversible (isolated temp product root so no test
action could mutate a real draft); the 09-02 `PIPER_PRODUCT_ROOT` gotcha from
my own carry-forward (hit it again, recognized it immediately).

**Loaded but not referenced**: publish/draft skills (no drafting today).

**Wanted but not found**: nothing.

## Sign-off checklist

- `git status` both repos: clean, up to date with `origin/main`.
- `origin/main` resolves in both: confirmed.
- `origin/main..HEAD`: empty in both.
- Cleanup dry-run: nothing stale.
- Cron re-arm: pending (next step, delete-then-create).

<!-- DAY-CLOSED: 2026-09-14 -->
