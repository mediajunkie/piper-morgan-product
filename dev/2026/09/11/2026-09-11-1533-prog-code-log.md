# Session Log — 2026-09-11 15:33 — Coding Agent (prog), Claude Code

**Role**: Coding Agent, delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Branch**: claude/lead-cycle (Lead's worktree, per delegation)
**Task**: (1) Fix #1711 test CI failure; (2) audit standing-red 'Tests' and 'E2E & AAXT Tests' workflows (#1747)

## Item 1 — #1711 test portability (commit 36cb6191b)

- Root cause: on a dead/fail keyring backend (CI ubuntu), `KeychainService._verify_keyring_backend`
  routes to the #1382 DB store (CI has `ENCRYPTION_MASTER_KEY`) or the no-secure-store refusal, so
  `store_api_key`/`get_api_key`/`delete_api_key` never reach `_keyring_call` — the patched hanging
  keyring function is never invoked. 5 of the 15 tests failed in CI, all one mechanism.
- Fix (test bug, not platform-behavior skip): `_isolate` fixture now sets
  `PIPER_CREDENTIAL_STORE=keychain` (forces the OS-keyring path, the only path the #1711 guard
  exists on); `service` fixture depends on `_isolate` so the env var precedes store selection.
- Verified 15/15 under: native macOS backend; `PYTHON_KEYRING_BACKEND=keyring.backends.fail.Keyring`;
  and fail backend + CI's `ENCRYPTION_MASTER_KEY`. Ruff clean; arch-enforcement 49 passed; lint
  ratchets 40 passed.

## Item 2 — #1747 audit (commit 3093eb47d)

- **Tests workflow first-red**: last green run 31233572835 (`fcbba9f3f`, 08-08 01:50Z); first red
  31240693716 (`5df97f2fd`, 08-08 04:59Z). That commit removed 3 backlog entries as
  "keyless-verified" — but `tests/conftest.py` loads ANTHROPIC_API_KEY from the macOS Keychain, so
  the local verification was never keyless (m-43 layer error). Those 3 have been the gate's NEW
  failures in every run since; the 5 × 1711 tests joined them this week (total NEW=8 in latest run
  34639901479). Gate also demanded SHRINK-LOCK removal of 7 now-passing entries.
- **Fix**: removed the 7 shrink-lock entries; restored the 3 (2 × `fixture` LLM-env-assumption,
  1 × `triage`). Gate replay against the latest CI sweep output minus the fixed 1711 nodes:
  exit 0, backlog 51.
- **E2E & AAXT first-red**: 0 successes in the entire 1000-run visible window (back to 2026-06-12).
  Current mechanism: the workflow's repo secrets are dead — OpenAI 429 `credit_balance_exhausted`,
  Anthropic 401 "API key is invalid". E2E Task Lifecycle 1f/8p; Canonical 47f/164p; AAXT nightly
  failing too. Not fixable in-repo — secrets rotation is PM/Lead lane.
- Discovered-work items listed in the report back to Lead (secrets rotation; db_user_history
  CI-only search miss; CI DB-store fake-credential pollution; llm-marker policy for e2e tests).

## Memory & briefing surfaces referenced this session
- Referenced: CLAUDE.md (#1382 keychain resolution order, env-strip idiom, m-43/m-44 framing);
  #1747 issue body; scripts/check_fullsuite_backlog.py header (#1452 gate contract).
- Loaded but not referenced: briefing docs (not loaded — delegated task, scope was self-contained).
- Wanted but not found: none.
