# Session Log — 2026-09-11 ~07:00 — Coding Agent (prog) — Claude Code
Model: Fable 5 (claude-fable-5)
Delegated by: Lead Developer
Task: #1617/#1739 follow-on — PM live PARTIAL (v71, 06:54): "are we done with that standup?" against the armed
overdue-todo offer fell to the legacy detector and released to the classifier, which read it as a completion
attempt ("I couldn't find a todo matching 'that standup?'"). Extend acceptance-contract adoption to the seam.

## Diagnosis (verified in source, not assumed)
- The offer copy PM saw is `standup_todo_offer.offer_question` (services/intent_service/standup_todo_offer.py:134);
  arm site is the standup handler (services/intent/intent_service.py:~3950), kind `standup_todo_offer`,
  workflow `standup_complete_todo`, registry axes WRITE x PRIVATE (workflow_entries.py:1603).
- At the generic offer seam (intent_service.py:1405-1433) only READ-axes kinds + the CONFIRM carrier consult
  evaluate_acceptance; WRITE x PRIVATE kinds fall to legacy detect_offer_response → PM's question returned None
  → pop stood → turn routed → classifier → complete_todo fuzzy title match on "that standup?". Exactly the
  ratchet table's UNADOPTED row "generic offer seam, non-READ kinds — blocked: LOW-tier vocab".

## Work
- (in progress) Adopt standup_complete_todo answer turns onto evaluate_acceptance at declared axes.

## Work completed (~07:20)
- services/intent_service/standup_todo_offer.py: added `state_question_reply` (seam-composed honest answer for
  state questions naming the offer's referents — standup done / todo still open — with the stored ask re-rendered
  verbatim; None for other questions → caller falls back to visible re-arm + "Still pending:" suffix). Module
  docstring gained the State-question bullet.
- services/intent/intent_service.py: generic offer seam — new branch for workflow `standup_complete_todo`
  consulting evaluate_acceptance at its registry-declared axes (WRITE x PRIVATE → LOW_CEREMONY). Tier judgment in
  the branch comment: declared tier, NOT the stricter escape hatch — NAMED_OBJECT cannot parse the 1651-pinned
  verbatim accept "Yes mark the overdue todo done." and refusing it re-releases that turn to the fuzzy classifier
  (the original 1651 harm); the LOW greedy residue inherited is byte-identical to the legacy detector replaced.
  Arm survival: VISIBLE (stated per CXO 09-10 ruling) — referent questions answered at the seam + re-render;
  others re-arm with the confirm-style restate suffix; no stored ask → pop stands.
- tests/test_architecture_enforcement.py: TestAcceptanceContractRatchet docstring — table row split (standup
  overdue-todo offer answers = ADOPTED W/PRIV; other non-READ kinds remain UNADOPTED), dated 09-11 paragraph with
  the tier judgment and why the intent_service.py detect_offer_response baseline row REMAINS (verify_inference /
  standup-interview residues + axis-less kinds still legacy — the row states the true remaining debt, m-44).
- tests/unit/services/intent_service/test_standup_todo_offer_1651.py: 4 new pins — PM's verbatim 09-11 exchange
  (9 overdue todos, "important thing" due 2026-08-10; question → honest status + offer re-rendered verbatim,
  classifier explosively patched → then "yes" marks the bound todo done), the "no" decline half, the todo-referent
  "Not yet" variant, and the unrelated-question visible-restate survival.

## Evidence
- 1651 suite: 23 passed. 1739 suite + both ratchets: 85 passed. Full enforcement file: 49 passed.
- Full intent_service unit tree + inversion phase2 gate: 3836 passed.
- standup + 1665 + 1605 suites: 508 passed. todo/standup -k selection: 342 passed.
- ruff 0.6.9 check + format --check: clean on all 4 changed files.
