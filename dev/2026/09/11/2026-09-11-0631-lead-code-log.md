# Lead Developer session log — 2026-09-11 (Friday)
Role: Lead Developer (lead-code) · Model: Fable 5 (restored by PM 9/11 ~06:55; Fable 5.1 grayed out — Pard checking the account upgrade) · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron 28c6042f (expires ~9/16, rotate ~9/14)

## Fire 1 (06:31) — START; the queued mypy env-drift diagnosis re-dispatched

Synced (36), inbox zero. First act per last night's plan: the Architecture Enforcement red —
CI's counts jumped to the documented local-skew signature (+2/+21/−1), so the working hypothesis
is CI env drift, not 23 coincidental real errors. Lane re-dispatched fresh (prior attempt died
on the Fable cap, nothing committed) with the boundary-run diff + commit-list falsification +
local A/B + pin-and-echo fix. Belt otherwise 2/3 green. Intake item after this: #1711.

## ~07:3x — THE ANSWER, AND IT'S MY ERROR: the "macOS skew" I wrote on 09-01 was NEVER MEASURED

Hypothesis (a) WRONG, (b) right: real drift the whole time. The replica venv reproduces CI
exactly on all 24 codes and at both boundary commits — there is no skew. My unverified gotchas
entry became a LENS that made four days of real red invisible, to me twice in writing. Corrected
in place with evidence + the operative rule (disagree with CI → suspect your venv). The red was
21 true positives in github_adapter (1723/1709 lane): iterating an Optional[Dict] types elements
as str; a non-array payload would AttributeError live. Fixed via a list wrapper (mock seams
survive) + protocol_client's str|int keying. SECOND defect: arg_type's ceiling was UNREACHABLE
(378 set against a measured 379, then −1 off the wrong base) — rule recorded: ceilings from
MEASURED counts, never base-minus-delta. attr_defined ceiling 190→149 (cleared 41 pre-existing).
Memo to Arch with the diff-review ask (their lane's code). Verified 54+205, pushed.
METHODOLOGY: a documented false clear is worse than a bad measurement — it re-emits to every
reader. m-44's sequel.

## ~08:0x — PM's 1617 live test: PARTIAL PASS — the harm is DEAD (nothing fired), the experience
wrong (question fell to legacy classifier as a completion attempt: the standup's overdue-offer
kind is in the documented UNADOPTED remainder). Not the Inversion (completion isn't flipped),
not a new flaw — a known-unadopted seam met PM's question first. Adoption-extension lane running
with PM's verbatim exchange as the pin. PM's "idle?" answered with receipts (the diagnosis had
just landed). Model: Fable 5 restored, 5.1 grayed (Pard checking).

## ~09:0x — the failing seam ADOPTED (verified 3836+54, pushed). Lane corrected MY diagnosis
(generic offer seam non-READ branch, not reminder kinds) and made a well-evidenced tier
deviation (LOW at declared axes — stricter would refuse 1651's pinned verbatim acceptance and
reintroduce that harm; the greedy surface widened by ZERO, byte-identical patterns). PM's
exchange pinned end-to-end incl. explosive-classifier proof the fuzzy fallback never sees the
turn. Honest ratchet caveat: the intent_service unadopted row CANNOT shrink yet (other residues
remain — documented, m-44). Staged for next deploy; PM retest ONLY after deploy + my live
replay passes (new policy). Belt fully green as of this morning's fix.

## ~09:4x — v72 DEPLOYED (adoption + morning's mypy fixes live). NEW STANDING RULE from PM:
deploy-by-default — hold only for named cause, escalate holds to Exec immediately (memory +
decisions.log + carry-forward). PM also named the overinterpret-own-notes pattern; my epic-1/2
answer therefore goes out SOURCE-CHECKED or explicitly unverified (GitHub rate-limited this
minute — answering from my own direct actions + flagging the unverified rows, recheck when the
limit clears). Next: the 1617 live replay (v72 is deployed — my own policy's precondition), then
1711.

## Fire (09:31→10:0x) — epic-state SOURCE-CHECK done (all five open: 1687/1711/1690/1733/1741 —
answer delivered to PM with unverified rows labeled); Code Quality's new red root-caused: PM's
inbox triage MOVED ~200 grandfathered August files, each move minting a new un-baselined path
(208 offenders!) — renaming history was wrong-scale, BASELINE REGENERATED (1728→1699 lines,
same files, read/ < inbox/ so Windows risk unchanged; the gate still catches genuinely-new
names — the 4 I shortened first prove it). LINT PASS. The 1617 LIVE REPLAY lane is running
against v72 per the pre-run policy. Arch APPROVED the adapter fix ("the wrapper is the shape")
+ the skew lesson lands in the gotchas class. Ship-060 kickoff in — report owed next fire.

## ~10:3x — THE REPLAY PASSED 3/3 (byte-identical, DB-verified no-write on the question, yes
still completes) — 1617's retest gate satisfied, PM invited via the issue + next check-in.
1730 probe note: the gibberish phrasing routed CONVERSATIONALLY (good behavior — the classifier
coached rather than declined), so the honest-decline seam went unexercised; an imperative-write
phrasing would reach it — probe-reachability finding, not a defect. Ship-060 FILED (the
intake-rule week). Day continues: 1711 next intake.

## Fire (12:31) — intake: #1711 lane running (the Keychain hang — bounded-timeout guard on every
keyring path, loud actionable message, fail-through to the resolution order, never die). 6 cc's
filed (the watchdog session-log race thread — CIO shipped a grace window same-day; the
find-corroborate-fix loop ran cohort-wide without Lead involvement, which is the point).

## ~13:1x — #1711 CLOSED (verified 55 + 15/15, pushed): bounded keyring guard everywhere, loud
actionable timeout, fail-through to /setup, memoized (one 5s cost max). Bonus: conftest's
collection-time keychain read meant PYTEST ITSELF would hang on rebuilt venvs — covered
transitively. Epic 1's last code item done; remainder = 1687's formal close-out (wants a green
belt snapshot — pending the CQ re-run post-baseline-regen). MVP → 25.

## Fire (15:31) — the 1687-five are ALL GREEN, and snapshotting them for the close-out exposed
MY OWN DENOMINATOR ERROR: the 'Tests' and 'E2E & AAXT' workflows are STANDING-RED (every
visible run) and were never in 1687's four-workflow scope — so my every "belt fully green"
claim since (ship-060, PM briefs) was a subset phrased as a total. The m-44 shape, recommitted
by the person who documented it. Filed #1747 owning it. Worse: MY fresh 1711 test is among the
CI failures (ubuntu has no macOS keyring backend — portability bug, fix in flight). Lane
running: 1711-test fix + full first-red/inventory/classification audit of both workflows incl.
the local-vs-CI mechanism question. 1687's close-out WAITS for the honest full-belt picture.

## ~16:3x — the 1747 audit landed: Tests-red = my 1711 portability (FIXED — the test never
reached the guard under CI's fail-backend→db-store route; forced the keychain path) + shrink-
lock + 3 env-only (backlog-restored via the gate's sanctioned path — next run should green).
E2E-red = NEVER GREEN IN 1000 RUNS, single mechanism: DEAD CI SECRETS (OpenAI 429 exhausted,
Anthropic 401 invalid) — ESCALATED to Exec/PM per the new channel (only PM can mint). Two
issues filed (credential-pollution test-isolation defect; the CI-only search miss). The
local-vs-CI mechanism is the week's sharpest: env-strip was NEVER keyless — conftest reloads
the real key from Keychain at session start, so "keyless-verified" claims were wrong-layer.
Pushed all three lane commits after 54-ratchet verify.

## Fire (18:31) — **THE TESTS WORKFLOW IS GREEN** (run for 1a1422c3: success) — first green
since AUGUST 8. The 1711 portability fix + backlog reconciliation did it. Six of seven
workflows now green; the seventh (E2E) waits only on PM's secret rotation. One cc filed
(CIO's watchdog grace-widening — their lane). Day close approaching; carry-forward current.

## Fire (21:47) — day close, quiet. Carry-forward day-close refresh done (verified in-file this
time). FRIDAY: the skew self-correction · seam adoption + 3/3 replay · 208-path baseline regen ·
v72 deployed · deploy-by-default adopted · 1711 closed · the denominator own-goal owned (1747) ·
Tests GREEN first time since Aug 8 · E2E's 3-month mechanism found (dead secrets, escalated) ·
the keyless illusion exposed · ship-060 filed. Deck for PM: 90s standup + 3min secret rotation.
