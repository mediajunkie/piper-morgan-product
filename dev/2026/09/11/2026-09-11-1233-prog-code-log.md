# Session Log — 2026-09-11 12:33 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer (epic 1 remainder intake)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1711 — macOS Keychain ACL hang: bounded-timeout guard around all keyring calls in KeychainService

## Plan
- Verify-first: confirmed blocking path — main.py → LLMDomainService.initialize() ("Validating LLM providers...", services/domain/llm_domain_service.py:75) → validate_all_providers() → get_configured_providers() → LLMConfigService.get_api_key() (keychain-first, llm_config_service.py:231) → KeychainService.get_api_key() → keyring.get_password() (keychain_service.py:196) → SecItemCopyMatching → indefinite ACL-dialog block.
- Audit: ALL runtime keyring ops live in services/infrastructure/keychain_service.py (get_password L196, set_password L165, delete_password L230; get_keyring L90 is local backend resolution, no Security-item query). No other module calls keyring directly. tests/conftest.py reads keychain at collection via KeychainService — guard covers it transitively.
- Fix: thread-based bounded join (~5s default, PIPER_KEYCHAIN_TIMEOUT_SECONDS configurable), loud actionable error, fail-through to env fallback (resolution order unchanged), process-wide hang memoization (one 5s hit max; avoids stuck-thread accumulation).
- Tests: new tests for timeout/fallthrough/config/fast-path; ratchets + seed guard + credential-resolution suites must stay green.

## Work

- 12:33-12:38 — #1711 implemented and verified.
  - `services/infrastructure/keychain_service.py`: added `_keyring_call()` bounded-join guard (daemon worker thread; keyring's macOS backend is an uninterruptible C call), `KeychainTimeoutError`, `KEYCHAIN_HANG_GUIDANCE` (literal-pinned actionable message), `PIPER_KEYCHAIN_TIMEOUT_SECONDS` (default 5.0s; <=0 disables), process-wide hang memoization (`_keychain_hung` module flag — one timeout hit max per process, no stuck-thread accumulation; several KeychainService instances exist per process so per-instance would re-pay). Wrapped all three keyring op sites: get_password, set_password, delete_password. `get_api_key` catches KeychainTimeoutError → returns None → LLMConfigService proceeds to env-var fallback (resolution order and #1382 db-store semantics untouched). Writes raise loudly; delete returns False.
  - `tests/infrastructure/test_keychain_timeout_1711.py`: 15 tests — hang bounded, message phrases pinned, memoization (incl. cross-instance), write/delete timeout behavior, env fallthrough via LLMConfigService, keyless→None (/setup funnel), fast paths unchanged, backend errors propagate, timeout config (default/override/invalid/disable).
  - Overhead measured: wrapped call ~60us vs real keychain read ~0.58ms — ~10% of a single credential op, ops are startup/settings-rare. Negligible.
  - Regression: 15/15 new; 90 passed 2 skipped (keychain + llm_config + provider_selection_1415 + secure_credential_store_1382 + user_api_key suites); 55 passed (architecture_enforcement + completion_ratchets + prompt_seed_guard). Pinned ruff 0.6.9 check + format both clean.
