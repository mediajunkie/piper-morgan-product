# Lead Developer session log — 2026-09-21

**Role**: Lead Developer (lead-code) · **Host**: Amber, worktree `~/Development/piper-morgan-worktrees/lead`, branch `claude/lead-cycle` (Model A)
**Model**: Claude Fable 5 (observed at session start)
**Cron**: 15125195 (`17 6,9,12,15,18,21`), armed at yesterday's STOP, CronList-verified one job
**Continuity**: yesterday closed properly (`DAY-CLOSED: 2026-09-20` verified at START); registry row active (un-parked 09-20 21:52 per clearing condition)

## START (06:17 slot, worked 06:24)

- Sync clean; freeze-detect INSUFFICIENT-SCHEDULE (0 scheduled overnight fires — cannot
  discriminate, noted per m-44, not an all-clear).
- Queue per carry-forward (Monday-first, all three spec-complete): **#1812 Phase B** →
  **#1837 build** → **#1818(b) wiring**. This session is the "fresh context" each was
  banked against.
- Criteria line (third queue source): new issues in the current sprint milestone — checked
  each fire.

### #1812 steps 5–6 LANDED and #1812 CLOSED — the server-key concept is deleted (06:17 fire, worked 06:30–07:25)

- Executed the pre-registered plan (`dev/active/1812-step5-6-execution-plan-2026-09-20.md`)
  verbatim, including the SECOND FACE: operator seam deleted end to end (env flag, both
  gates, resolver rung, explicit None binding — now raises at bind time); step 6 became
  the amputation the plan predicted (`_init_clients` + the three server-client attrs
  gone; `LLMClient` construction reads no credential; Gemini leg = pure honest refusal).
- Landed as 3 commits (`41b5bc205..99ba5d879`) — split for the broad-staging hook's
  20-file threshold, tree whole at part 3/3 where the full bar ran.
- **Verified how**: guard rails 150/150 · unit+intent sweep 11,290 passed · spend-free
  ratchet 14/14 membership UNCHANGED · arch enforcement 53/53 · smoke 530 · mypy gate
  24/24 ceilings · security 127 · census zero live refs. Three ratchets LOWERED
  in-commit (silent_death 194→193, unscoped_reads 52→48, mypy_assignment 238→237).
- Two sweep failures verified PRE-EXISTING at origin/main baseline (throwaway worktree,
  same failure both trees): filed **#1841** (pm039 live-LLM drift: gpt-4o consistently
  says manage_portfolio for "show me all project plans") and **#1842** (classification-
  accuracy live suite hits the deprecated #322 container fallback — keyed-lane red
  nobody sees, #1608 shape).
- #1823 trace §2 caveat dissolved — noted on #1823; gate builds directly on
  `provider_spend_entitled` now.
- **Deploy rides the next cut** (pre-registered; spend-authorization change).
- Discovered-work filed: #1841, #1842. Dispatches: none (no subagents this unit).

### #1837 shapes 1+2 LANDED — acceptance arms the interview; the template is dead (06:17 fire cont., worked 07:2x–07:4x)

- Commit `44e779fa3` (+ a stray-format fix the pipefail gate caught pre-push, not my file).
- **Found turn 3's literal mechanism during the build**: the quick-bypass substring list
  matched `"just"` inside PM's "ready (as I just said)" — bypassed the interview into the
  empty-capture template. Word-boundary matching now; same disease class as
  `_apply_refinement`'s substrings (shape 3's target).
- State machine gained GENERATING→GATHERING_YESTERDAY and REFINING→GATHERING_YESTERDAY
  (the honest re-entry paths); `start over` truly starts over.
- 5 new #1837 pins incl. PM's exact 4-turn regression; 9 old-contract tests amended.
  Suites: 4,869 + smoke 530 + enforcement/ratchets 58 — green.
- GH comment with evidence on #1837. Shape 3 (refinement via floor) = next unit.

### #1818(b) SHIPPED and #1818 CLOSED — the gate acknowledges before it names the policy (06:17 fire cont., worked 07:5x–08:0x)

- Commit `99c1122bc`: `services/ui_messages/keyless_pleasantry.py` (CXO's copy verbatim,
  ONE shared constant + structural pin), gate wiring at the /intent refusal handler,
  kind classification via the pre-classifier's own patterns (#1416 residue rule, zero
  LLM). Arch's seam split honored: repeat pleasantry → short form; repeat substantive →
  the gate's string (#1823's later).
- 27 new tests; spend-free ratchet 14/14 membership UNCHANGED (the canary); web sweep
  859; smoke 531. Issue closed with evidence.
- **Monday-first queue: 3/3 landed by 08:00** (#1812 closed · #1837 shapes 1+2 ·
  #1818 closed). Remaining from the trio: #1837 shape 3 (refinement via floor).

### #1837 shape 3 LANDED; #1837 + #1836 CLOSED — refinement via the floor, toy NLU retired (06:17 fire cont., worked 08:0x–08:2x)

- Commit `5d52431d9`: `ConversationalFloor.revise_draft` (artifact op, user's own key,
  strict output contract); `_apply_refinement` deleted; chip actions = strict prefix
  parsers, key-free, pinned never to touch the floor; keyless free-form edit refuses
  honestly naming the key-free alternatives. Verified-diff honesty engine-independent.
- **Discovered live defect while writing the regression test**: the shared
  `ACCEPT_PATTERNS` `^please\s` row fires on short imperative requests — "please remove
  the fluff" FINALIZES the draft (the contract preamble's own #1631 class, under the
  prose floor). Filed **#1843** for the #1739 contract's owners rather than patching
  shared vocabulary from a standup commit.
- Suites 4,875 · smoke 531 · enforcement/ratchets 58. #1837 closed (all 3 shapes, all
  4 ACs, PM's 4-turn regression pinned); #1836 closed with it (immediate fix shipped
  09-20; deeper cause now retired).
- **The whole Monday queue is drained by 08:25**: #1812, #1818, #1836, #1837 CLOSED;
  #1841, #1842, #1843 filed. All four fixes ride the NEXT cut (alpha still v0.8.12.0).
- Cron re-armed at idle: 15125195 (deleted at drain start, Rule 1) → **de0622d6**,
  CronList-verified exactly one; registry row updated. Exec update memo sent (verified
  on origin) with the v0.8.13.0 cut recommendation for PM.

### v0.8.13.0 CUT + DEPLOYED TO ALPHA + full alpha-docs audit (PM-directed; worked 08:3x–09:0x)

## Release v0.8.13.0 Completion Matrix

Phase 1 — Pre-flight
- [x] Cut commit: product content at `39925528e`; tag on `cb7ea2270` (release commit)
- [x] Tests: 14,332 collected; parity script PARITY OK; no open P0s

Phase 2 — Version bump
- [x] pyproject.toml + **VERSION file** (the latter caught stale by the audit — it's what
      #1839's /health reads; `check-version-consistency.py` now green; skill amended v1.1)
- [x] Release notes written fresh ("Nothing Invented, Nothing Borrowed")

Phase 3 — Documentation (+ the PM-directed audit beyond the checklist)
- [x] releases/README · ALPHA_QUICKSTART (strings AND prose; **tester URL corrected
      fly.dev→alpha.pipermorgan.ai** — three-way contradiction with docs/README + the
      invite flow; noted inline for reversal if the hosting sort rules otherwise)
- [x] **ALPHA_TESTING_GUIDE rewritten hosted-only (closes #1804)** — 934→~330 lines; the
      clone/Docker/wizard chapters, the stale-production clone command, the "hosted
      planned for later in 2026" claim, and local dev DB creds all gone; /setup curled
      live to confirm the invite-code field before describing it
- [x] ALPHA_KNOWN_ISSUES (fixed-in-0.8.13 section; #1843/#1824 added; hosted workarounds)
- [x] ALPHA_AGREEMENT (3 spots) · BRIEFING banner + attestation · versioning.md (+ the
      MISSING 0.8.12.0 history row) · **VERSION_NUMBERING timeline unstuck from "0.8.9.2
      CURRENT"** (three releases stale) · docs/README (LLM line → user-keyed dual-provider)
- [x] **email-template v3.0 (closes #1830)** — hosted invite flow; mint checklist per the
      trust-zone split

Phases 4–6
- [x] Committed explicit paths; tagged `v0.8.13.0` at `cb7ea2270`; pushed main + tag
- [x] production fast-forwarded (lockstep), PARITY OK re-run after every push
- [x] GitHub Release published + body updated after the notes amendment

Phase 7 — Audit + deploy
- [x] Stale-version grep: only deliberate historical mentions
- [x] **DEPLOYED to alpha (blue-green-lite)**: backup (config+image+code+DB dump at
      /root/alpha-deploy-backup-20260921-1547) → archive origin/production → build with
      `--build-arg PIPER_GIT_SHA` → named-services up (#1835 shape) → migrate OK (no-op,
      as the notes claim — verified against git, 0 alembic commits) → encryption
      round-trip OK → PIPER_HOST=0.0.0.0 → cross-container path OK
- [x] **#1839 gap FOUND AND FIXED during verification**: Saturday's deploy-identity landed
      on a router NO APP MOUNTS (m-49 in my own work; caught by curling the artifact).
      Fix `2ff920b74`: `deploy_identity()` on the SERVED /health + route-level pin; second
      droplet build; **live: `curl https://alpha.pipermorgan.ai/health` → version 0.8.13.0,
      git_sha 2ff920b74, environment production** (gates armed).
- [x] pm-test-card row 1: #1617 standup retest READY (deployed)
- Issues: #1804 + #1830 auto-closed by the release commit (evidence comments added);
  #1839 evidence comment (left open for Arch's vocabulary question). Skill v1.1 +
  runbook amended with everything this cut taught.
- HOST notified (cc Exec+PM, verified on origin): invite now lands on fixed code; email
  template v3.0 is the invite baseline. Cron re-armed at idle de0622d6 → **5a7ebd28**
  (CronList-verified one); registry current.

### #1823 + #1824 SHIPPED TOGETHER and CLOSED — the gate matches the spend; five causes, four honest sentences (09:17 fire, worked 09:5x–10:4x)

- Commit `9ec028406`. **#1823 branch one**: `resolve_user_llm_binding` (Slack-arm mirror);
  an OpenAI-only user passes /intent + the /documents family on their own key; CXO's
  ruled neutral copy everywhere (the five vendor-specific strings retired; search's
  OpenAI-specific string correctly stays). **#1824**: the auth bucket split into
  rejected_credential / insufficient_permission / not_configured / config_endpoint with
  CXO's copy verbatim — AND the live symptom's second cause fixed: the
  IntentProcessingError wrap destroyed `details["original_error"]` (str(APIError) =
  "API Error [CODE]"), so the alpha 401 fell to "Something unexpected happened"; the
  wrap is now `_wrap_processing_error`, unit-tested, cause carried through to the
  humanizer's honest invalid-key copy. Humanizer gains the 403 entry.
- Landed TOGETHER per PPM's ordering rule. 32 new tests (provider-aware stored-key
  double — the single-value mock would have masked the fix); sweep 11,381; smoke 532;
  spend-free canary membership UNCHANGED; mypy 24/24 ceilings.
- #1842's denominator widened on the issue (test_direct_interface fails at baseline,
  same signature — verified in a throwaway worktree).
- Deploy: rides the NEXT cut. PM-test-card row added: invalid-key retest post-deploy.
