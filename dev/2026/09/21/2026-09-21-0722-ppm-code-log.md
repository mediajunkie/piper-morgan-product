# 2026-09-21 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

## Session Objectives

1. Sprint-week continuity: board hygiene (milestone/status correctness, board-add gaps),
   mailbox triage, issue triage against precedent.
2. No standing owed work item carried from yesterday — `#1818`/`#1823` fully resolved, `#1837`
   build-ready. Watching for Arch's ruling on `#1836`'s deeper cause and whoever picks up `#1838`.

### 07:22 START

Heartbeat emitted (`scripts/duty-cycle-heartbeat.sh ppm START`). `CronList` → one job (`69c1ab94`,
unchanged overnight — correct expression, no re-arm needed). Prior day's log
(`dev/2026/09/20/2026-09-20-0654-ppm-code-log.md`) confirmed carrying its `<!-- DAY-CLOSED:
2026-09-20 -->` sentinel — Step 0 clean, no missed-STOP recovery needed. Carry-forward read: clean
handoff, nothing PM-gated carried forward. Worktree-collision fingerprint clean (`ppm` ↔
`claude/ppm-cycle`). Synced to `origin/main` clean — noted CXO's overnight finding that yesterday's
Amber reboot never actually reached their seat either (same as Arch/Host/Web/Comms), consistent
with this seat's own ambiguous-but-safe re-arm last night.

**Mail drain**: 0 items, inbox empty.

**`sprint-truth.py` fresh**: found 2 unmilestoned issues (`#1841`/`#1842`, both filed by Lead this
morning during `#1812`'s own Phase B verification — pre-existing test-infra defects, not
regressions). Checked precedent (`#1372`/`#1356`/`#1506`, all incidentally-discovered test-suite
defects milestoned `Production`) rather than guessing — set both to `Production`, board-added,
Status Product Backlog, verified. Re-ran: `0 unmilestoned` confirmed.

**`#1812` (epic 2, the operator-key-seam retirement) closed overnight** — verified via the issue's
own closing evidence ("the server key is not a real concept — now structurally, not just by
ruling"), not assumed from the milestone-count delta alone. Updated epic 2's tracking to reflect the
close and note the two test-infra issues found during its verification.

**Standing-items**: none owed. Third queue source: still no formalized PPM criteria line.

Quiet START — all three queue sources checked and drained. Returning to idle; cron armed
(`69c1ab94`), next fire 09:52 PDT (WORK).

---

### Five real closures land — #1818, #1823, #1824, #1836, #1837 all shipped (10:22 WORK fire)

Heartbeat emitted. `CronList` → one job (`69c1ab94`), unchanged. Worktree fingerprint clean. Synced
clean. Mailbox empty.

**`sprint-truth.py`'s delta line showed something worth taking seriously before quoting it**: `not
done 58 → 53 (-5)`, five closures in one morning. Verified each individually rather than trust the
count — `#1818`, `#1823`, `#1824`, `#1836`, `#1837` all confirmed `CLOSED` with real closing evidence
read directly (not assumed from state alone): `#1818` "(b) implemented with evidence above"; `#1823`
branch one shipped (`9ec028406`), the gate now matches the spend; `#1824` the four-bucket classifier
split shipped, same commit, `return "auth"` no longer exists in the function, pinned by test; `#1836`
closed alongside `#1837`'s completion (the deeper substring-NLU cause retired by shape 3); `#1837`
all three of Lead's shapes landed. This is the entire `#1818`/`#1823`/`#1824` family this seat has
tracked since 09-18/09-19, plus both of PM's dogfood-session defects from epic 5/3 — a genuinely
large single-morning close-out, not routine churn.

**Consolidated all five into the epic-order file** rather than scattering thin updates: epic 2 gets
`#1818`/`#1823`/`#1824`'s close-out plus the new `#1843` (found during `#1837`'s shape-3 build — a
regression test phrasing an edit politely triggered `ACCEPT_PATTERNS`'s over-broad "please" match,
finalizing a standup draft instead of editing it). Epic 3 gets `#1837`'s close-out and the important
consequence: **the epic's own blocker is cleared** — `#1739`'s dependency chain reverts to its
original shape, `#1617`'s standup retest is reachable again (not re-running it myself, just noting
it's unblocked). Epic 5 gets `#1836`+`#1837`'s joint close-out, confirming CXO's completion-claim
principle from yesterday held: the fix needed both halves (honest re-entry + honest completion
claim), and that's exactly what shipped.

**Board hygiene**: two more unmilestoned issues found same fire — `#1843` (the `ACCEPT_PATTERNS`
defect above) and `#1844` (the recurring `FLY-AUDIT` weekly docs audit). Fixed: `#1843` → MVP
(real product defect, found during active epic-3 build), `#1844` → `Ongoing` (established precedent
from this week's bulk retriage). Both board-added, Status Product Backlog, verified. `0 unmilestoned`
confirmed after.

**Mail**: 0 items, inbox empty both checks. No triage needed this fire.

---

### A real security finding: invite codes exposed in the public repo, already remediated (13:22 WORK fire)

Heartbeat emitted. `CronList` → one job (`69c1ab94`), unchanged. Worktree fingerprint clean. Synced
clean. Mailbox empty.

**`sprint-truth.py` fresh found 3 unmilestoned issues** — one genuinely serious. Read all three in
full before triaging rather than fixing board fields blind. `#1845` (Lead, found pulling a thread on
a code string in HOST's morning memo): **Janne's invite code — a bearer credential for account
creation — appeared in full across at least 4 mailbox memos since 09-13, and this repo is confirmed
PUBLIC** (`gh repo view --json visibility`). A second, independent defect stacked on top: the code
was minted against the wrong instance entirely (Fly, not alpha's droplet), so it would have bounced
at the gate on send day regardless of the exposure. **Already remediated same-fire by Lead, not just
discovered**: confirmed the exposed code was never consumed on either instance (no unauthorized
account exists), minted a correct replacement, delivered PM in-conversation only — one residual
(burning the exposed Fly-side row) needs a seat with Fly-write access. Milestoned MVP (matching
`#1816`'s precedent — a security defect surfaced during real operational work on the live alpha
rollout, not a pre-launch gate item), board-added, folded into epic 2 with the full remediation
trail so it isn't rediscovered. **Standing-rule proposal in the issue** (bearer credentials never
travel through `mailboxes/`) is PM's to ratify, not a PPM call.

`#1846`/`#1847` (both from today's Weekly Docs Audit follow-up — stale environment-variables doc,
pattern-frontmatter status-field gap) matched the established docs-audit precedent cleanly —
`Ongoing`, board-added, no epic-file action needed.

**Also noted, not chased**: `#1808` closed this fire (a TokenBlacklist dead-Redis-path defect) —
wasn't in this seat's epic tracking to begin with, so nothing to update; not backfilling a closed
item retroactively.

**Board hygiene**: all three fixed, `0 unmilestoned` confirmed after.

**Mail**: 0 items, inbox empty both checks.

---

### Quiet WORK fire (16:22) — 3 more closures, none tracked here, same discipline as this morning's #1808

Heartbeat, `CronList` (one job, `69c1ab94`), sync clean, mailbox empty. `sprint-truth.py` fresh:
`0 unmilestoned`, 3 closures (`#1778`/`#1781`/`#1782`, GitHub-companion-count MVP items) — none were
in this seat's epic tracking while open, same gap shape as `#1808` this morning. Not backfilling
closed items retroactively; noting the pattern is now two-for-two today, which is the same
symptom the still-missing third-queue-source criteria line would catch at filing time rather than
after the fact. No action otherwise.
