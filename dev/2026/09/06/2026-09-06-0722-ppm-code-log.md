# PPM session log — 2026-09-06

**Role**: Principal Product Manager (PPM) · slug `ppm-code` · **Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` · job `be717929` (re-armed 09-05 22:22 STOP; unchanged today)

### 07:22 START

**Heartbeat + sync**: clean, one cron job (`be717929`), 0 ahead/0 behind. Prior day's log
(2026-09-05) confirmed carrying its `DAY-CLOSED` sentinel before starting.

**Mailbox**: empty. `#1386`/`#1688` both unchanged from yesterday — no PM word on the overrule
call, now four days open, still no urgency attached, not chasing.

**A real jump in `sprint-truth.py`'s numbers, checked before reporting rather than assumed**:
`MVP: 50 not done (31 Sprint Backlog, 2 In Progress, 16 In Review, 1 Product Backlog); 1115 done`
— up from 39, with unmilestoned dropping 17→2. Traced the cause rather than flag it as drift:
commit `672aaf2b9` (PM, direct, not via a role agent) added
`dev/active/piper-morgan-MVP-open-2026-09-06.tsv` — a 50-row personal export matching this new
count exactly, consistent with PM doing hands-on milestone triage directly (moving previously-
unmilestoned issues into MVP) as part of the acceptance-testing pass PM had flagged wanting to do.
No PPM action implied — PM's own direct board work, not something to redo or question. Noting the
new baseline accurately for future counts to compare against.

`#1386`: no movement since 08-28, criterion 6 only.

### 10:22 / 13:22 / 16:22 WORK — quiet, no drift any of the three fires

Clean sync each time, empty mailbox each time, `#1386`/`#1688`/`sprint-truth.py` all unchanged from
the START fire's new baseline. Per no-churn discipline, no dedicated entries beyond this line.

### 19:22 WORK — cohort standing rule triaged, one more count-drift catch

**Heartbeat + sync**: 1 commit behind, fast-forwarded clean. One mailbox item: Exec's cohort-wide
standing rule (PM-ratified) — probes/experiments under ~25 API calls proceed without asking,
report the cost with the result; production data, live users, and new vendors stay asks regardless
of size. Informational, no PPM action (I don't run API-call probes myself) — triaged.

**Also caught #1723 via the unmilestoned-count discipline** (2→3, one new item): Arch's
GitHub-operations Protocol cleanup, already mostly shipped same-day, this issue tracking the 3
remaining live-called-but-unbacked operations. Checked #1709 (same class, sibling issue) for
precedent before triaging — MVP, closed — and matched it: MVP / In Progress / Beta Blockers - Hard
Gates Only. Board mechanics only, posted a triage comment; the actual implementation is Arch/Lead's.

`#1386`/`#1688`: both unchanged, no PM word on the overrule call. `sprint-truth.py`:
`MVP: 49 not done (30 Sprint Backlog, 2 In Progress, 16 In Review, 1 Product Backlog); 1116 done`
before #1723 landed (one item closed elsewhere overnight, unrelated to my own work).

### 22:22 STOP — a real correction to #1386's framing, ruled and closed same-fire

**Heartbeat + sync**: clean, one cron job, 0 ahead/0 behind. Two items, both a direct hit on my own
watched gate: PM walked the open decision board and re-scoped #1386's criterion 6 — it fires at MVP
milestone close (now `due_on` 2026-10-30, real date), not now, and criteria 2/4/5 need fresh
re-execution at that point since their evidence is point-in-time and has expired. Exec's framing
correction was pointed but fair: my own "only criterion 6 remains" shorthand was checkbox-accurate
and meaning-incomplete — the checkboxes don't encode that three of them are perishable.

**CXO caught a real gap in Exec's ruling and routed the call to me**: criterion 3 (my own
co-authored scenarios) was left off the fresh-run list as "definitional," but its own checklist
text says the scenarios were "executed against the deployed Fly artifact" — the same point-in-time
shape as 2/4/5, and actually the oldest evidence in the gate by 40+ days. **Verified this directly
against `#1386`'s own text before ruling**, not on either account. Agreed with CXO: criterion 3
joins the fresh-run set, scenario definitions stay as-is, only the execution evidence is stale.
Named the cost honestly (three manual scenarios cost more to re-run than an automated suite check)
rather than wave it away. Posted on #1386 and sent to Exec/CXO/PM (`073437d5b`), verified landed.

**Also fixed a small real staleness in my own `release-model.md`** while verifying the new MVP
due date via the GitHub API directly — the doc said "no fixed date set," which stopped being true
today. Committed separately (`988c0382a`).

## Day summary

Seven fires (START 07:22, WORK 10:22/13:22/16:22/19:22, this STOP). A week-closing day that
started with a real, self-verified fact (PM's own direct milestone triage explained a big
`sprint-truth.py` jump, traced via `git log` rather than flagged as drift), continued with one more
proactive count-drift catch (#1723, matched to #1709's precedent), and closed with the week's most
consequential #1386 development: PM's own re-scope of the gate, and a real correction to it that I
verified and ruled on rather than deferred. **The takeaway worth keeping**: "only criterion 6
remains" was true and incomplete for over a week, and neither the staleness nor the fix came from
me noticing on my own — Exec named the framing gap, CXO named the criterion-3 gap. Both times the
right response was verifying the claim myself before accepting or ruling, not defending the prior
framing or rubber-stamping the correction.

`sprint-truth.py` at close: `MVP: 50 not done (30 Sprint Backlog, 3 In Progress, 16 In Review, 1
Product Backlog); 1116 done`, 2 unmilestoned. **#1386, corrected framing**: criteria 3 and 6 fire
at MVP close (2026-10-30); 2, 4, 5 re-run fresh then; only criterion 1 stands unqualified. **#1688's
overrule call**: four full days open, no urgency, carrying forward. No OWED items carried into
tomorrow's cron prompt — but tomorrow's watch-for line needs rewriting to reflect the corrected
#1386 shape, not "only criterion 6."

**DAY-CLOSED**
