# Session Log — 2026-09-10 15:33 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle, 0 behind origin/main at start)

## Task
Finish three-workflow belt repair:
1. Code Quality — mailbox filename lint red (malformed `mailboxes/ppm/inbox/read/` files + `xian (ceo)/inbox/` overlong names)
2. Router Pattern Enforcement — one direct GoogleCalendarMCPAdapter import in services/
3. Architecture Enforcement — mypy drift: union-attr 162>161, truthy-function 0<1 (ceiling lower)

## Work

### Gate 1 — mailbox filename lint: GREEN (15:33–15:50)
- 9 flagged paths = 6 basenames (all CXO memos, 09-08..09-10). Root cause: CXO composed subjects long enough that the 27-char `mailboxes/xian (ceo)/inbox/` prefix pushed full paths past 180; three of the six also had their PPM copies sitting in the MALFORMED `mailboxes/ppm/inbox/read/` dir (25-char prefix), which tipped those over too.
- Shortened all 6 basenames (max 138 chars, worst full path now 165 ≤ 180), renamed EVERY tracked copy repo-wide (6-9 copies each, found via `git ls-files -z` basename match, space-safe), relocated all 6 PPM copies to the correct `mailboxes/ppm/read/`. Shipped as 6 per-memo `mail-send.sh` calls: 1a0a489f5, 6e22eeb75, 239808351, 8c1126ada, cac06b0b2, 7c5305881.
- Verify: `mailbox-filename-lint: no new over-length paths (1724 baselined).` exit 0.
- DISCOVERED WORK → filed #1743: `mailboxes/ppm/inbox/read/` still holds 188 files (parallel mis-located read archive; real archive `ppm/read/` has 795). Out of scope here.
- Pre-existing worktree oddity (not mine, left alone): unstaged deletion of `mailboxes/lead/inbox/finding-cxo-to-lead-...mail-send-strand-check...2026-09-09.md` — mail-send flags it as "left behind" on every send; Lead should reconcile.

### Gate 2 — router pattern enforcement: GREEN (15:50)
- Violation was `services/mcp/consumer/mcp_client.py:15` — a DOCSTRING line, not code: the 2026-08-30 historical-correction note (1699 surgery record) names `GoogleCalendarMCPAdapter` in prose, and the checker's `GoogleCalendarMCPAdapter(?!Router)` pattern matches any line incl. comments. The only real import of the adapter is `calendar_integration_router.py:73` — the sanctioned router home, already in EXCLUDED_FILES.
- Fix: reworded prose to "the google_calendar_adapter's eager ``MCPConsumerCore()``" — pointer preserved, no exclusion added (excluding mcp_client.py would ungate a future genuine direct import in a real transport module).
- Verify: `✅ No direct adapter imports detected (489 files checked)` exit 0.
- Concurrency note: Lead landed 697b24f0f (15:33, main) rewording a DIFFERENT prose false-positive in the same docstring (`query_router.py` mention); complementary, no collision — both are in this branch.

### Gate 3 — mypy signature-drift gate: fixed, ceilings lowered (16:00–16:40)
- CI-replica venv rebuilt per gotchas: clean py3.11 + mypy==2.3.0 + sqlalchemy==2.0.23 + pydantic==2.12.5 + fastapi==0.115.14 (scratchpad).
- Attribution baseline: 076ed8fd1 (08-30 Batch-3 disposal, the commit that set union_attr ceiling 161). Pinned venv reproduces CI EXACTLY for union-attr at both ends (161@076ed8fd1, 162@HEAD → skew 0 for this code).
- The ONE new [union-attr]: `services/intent/intent_service.py:434` — `"question": detection.offer.offer_message` — added by d277bfb7a (09-09, 1739 READ-tier seam adoptions, Arch condition (a)). It duplicated the pre-existing baselined non-narrowing pattern: `detection.has_offer` doesn't narrow `detection.offer: Optional[WorkflowOffer]`.
- Fix-forward (no masks): bound a narrowed local `offer` after the guard (`if not detection.has_offer or offer is None`) and switched all 6 uses in the soft-offer block. Removes the new error AND 5 pre-existing same-root errors → union_attr 162→156, and one [arg-type] (Optional passed to `format_offer(offer: WorkflowOffer, ...)`) → arg_type −1 (A/B stash-verified: exactly that one line).
- Ceilings lowered in same commit per gate rules: union_attr 161→156, truthy_function 1→0 (drift already removed upstream; gate's own message), arg_type 378→377 (my fix's −1 off CI's at-ceiling 378).
- Local full-gate residue is the DOCUMENTED macOS skew only (arg_type +2, attr_defined +21, misc −1) — none of those codes changed in my diff (A/B evidence); CI (ubuntu) is the authority on absolutes, deltas verified here.
- Pinned ruff 0.6.9: `ruff format --check .` → `1740 files already formatted` exit 0 (after formatting my own edit); `ruff check .` → `All checks passed!` exit 0.
- Tests: `pytest tests/unit/services/intent_service/test_soft_invocation.py tests/unit/services/intent_service/test_offer_binding_1529.py tests/test_completion_ratchets.py tests/test_architecture_enforcement.py tests/test_prompt_seed_guard.py -q` → **175 passed**.
- Final re-verify all gates at final tree: lint exit 0, direct-imports exit 0, pinned-venv measure union_attr=156 arg_type=379(local; CI-predicted 377) truthy=0.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (mail-send workflow + per-memo push norm, space-in-dirname quoting, bare-number commit discipline, discovered-work discipline, m-43/m-44 layer+denominator framing); `docs/internal/operations/github-and-tooling-gotchas.md` (mypy toolchain/platform sensitivity, CI-pin method); `scripts/mail-send.sh` header comments (reconcile + local-branch-lag); ab-a-isolation pattern (stash A/B for arg-type attribution).
- **Loaded but not referenced**: memory index (MEMORY.md) role/feedback entries beyond quoting discipline; briefing docs.
- **Wanted but not found**: a documented location for the CI-replica venv (rebuilt from scratch; gotchas entry describes pins but no cached venv path).

