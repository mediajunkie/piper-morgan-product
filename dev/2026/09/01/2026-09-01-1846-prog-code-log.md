# Session Log — 2026-09-01 18:46 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: 1717 — verify what the floor ACTUALLY produces when multiple
source_failed honest-degrade directives compose (the cheap verification step
PPM named; CXO verified source structure only, explicitly not model output).
**Boundary**: evidence only — no aggregation fix, five directive sites untouched.

## Work

### Layer 1 — mechanical pins (committed)
- New `tests/unit/services/intent_service/test_source_failed_composition_1717.py`
  (11 tests, all passing): all-five flags → all five directives in
  `_format_domain_context` output AND in the composed `_build_prompt` prompt,
  additive (`count("check FAILED:") == 5`, no aggregation/cap); 2-of-5 and
  1-of-5 (parametrized) shapes exact with no bleed; zero-flag negative.
  Docstring states these pin CURRENT behavior and are the tests that MUST
  fail when the CXO+Lead aggregation fix lands.

### Layer 2 — live probe (scratchpad only, NOT committed, per task)
- In-process harness (`probe_1717_live.py` in session scratchpad): real
  `ConversationalFloor` → real `LLMClient`, keys resolved via
  KeychainService (priority 1, env-stripped so ANTHROPIC_BASE_URL etc. can't
  reroute the SDK). Fixture user_id=None, category CONVERSATION, message
  "good morning, what's my status?".
- 3× all-five-flags + 1× one-flag contrast on the server default provider
  (openai/gpt-4o), then the same 3+1 with provider forced to anthropic
  (claude-sonnet-4-6) — the value #1415 per-user selection would supply.
- Verbatim transcripts: scratchpad `1717-live-probe-evidence.txt`; full
  raw output in `probe_1717_transcripts*.txt`. Reported verbatim to Lead.

### Headline observation (observation, not verdict — CXO judges voice)
- Neither model produced the five-item litany CXO predicted. All 6 five-flag
  replies synthesized to ONE aggregate sentence naming 3–4 data categories
  ("reminders, GitHub, projects, todos"), never five separate failure
  clauses, never a bullet list of broken subsystems. Honesty guard held:
  no reply claimed any list was empty.
- Wrinkles for CXO: anthropic's ONE-flag contrast spilled beyond the armed
  flag ("I don't have your projects or todos in front of me this turn" when
  only the reminder check had failed); anthropic run 3 asserted "Nothing's
  lost on your end" (unverified reassurance).

## Discovered issues filed
None (the contrast-spill and reassurance wrinkles are inside 1717's evidence
scope and delivered to Lead/CXO in the report rather than filed separately).

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-stripping gotcha + Keychain-first key
  resolution — shaped the probe harness; bare-number commit rule); CXO memo
  `watch-cxo-to-lead-…-1717-…-2026-09-01.md` (the five sites + the exact
  question); `test_projects_lane_honesty_1645.py` (test shape/m-43 layer
  framing to mirror).
- **Loaded but not referenced**: memory index (MEMORY.md).
- **Wanted but not found**: nothing.

## Wrap
- Commit: test pins + this log only. No push (per task — Lead owns merge).
- Working tree otherwise clean; transcripts live in scratchpad + report only.
