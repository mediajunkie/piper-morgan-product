# CXO Session Log — 2026-09-01 (Tuesday)

**Role**: Chief Experience Officer (CXO) | **Tool**: Claude Code | **Model**: Opus 5 (1M context)
**Host**: Amber | **Worktree**: `~/Development/piper-morgan-worktrees/cxo` (Model A)
**Branch**: `claude/cxo-cycle` | **Cron**: `8207809c` (expires ~09-06 — rotate at the last fire before)
**Started**: 07:17 PDT

---

## START (07:17)

08-31 closed cleanly at 22:17 (6 fires). Synced before mail; `CronList` shows exactly one job.

**One memo, nothing owed.** CIO replied on the *"misfiled is not deferred"* candidate as methodology-core's
owner: **agrees the distinction is real, holds at one case** — matching my own stated caution — and will
carry it on their watch list rather than let it evaporate, flagging a second instance if one surfaces.
They also confirmed the sharpest test independently: **their own `aging-standing-items.sh`, built this
week for the deferral pattern, would not have caught it** — the item was correctly filed, dated and
attributed, and still invisible to the one person who could act. And they endorsed keeping the
tester-disclosure case out of the count (*routed-is-not-landed* is real but is not this).

**#1463 still blocked**: PA's log confirms the OpenAI thread is waiting on PM's fresh "Intern"-scoped key.
⚠️ **Carry-forward says ask PM if nothing by midday, and explicitly not to re-diagnose** — the diagnosis is
done. Holding until then rather than nudging at 07:17.

## Queue drained, so I audited my own docs with the discipline I've been applying to everyone else's

`aging-standing-items.sh` shows **no CXO rows aging** (cohort coverage gap down 7 → 3 as roles adopt).
#1463 is credential-blocked and the carry-forward says hold till midday. **Verified the #1688 window
before deciding anything was urgent**: still no `services/mcp` server dir, no tool output authored, #1688
untouched since 08-30 and now Production-milestoned. **So tool-layer guidance now would be premature and
built on the unresolved deconfounder variable.** Nothing urgent — which made this the right fire for a
self-audit.

### 🔴 The FTUX model had been ratified out from under itself

`ftux-experience-model-2026-08-21.md` — **untouched since 08-21**, no mention of ESSENCE, which was
ratified 08-30 and **absorbed several of its claims into law.** So the file carried one undifferentiated
*"open to PM's correction"* banner over content of **three different standings**, and a reader couldn't
tell which was which.

Added a standing block that sorts them, and **edited nothing in the body** (it's the 08-21 record and
should stay one):
- 📄 **Now ratified — cite ESSENCE, not this doc**: first contact as decisive; **the response-shaped
  variant** (§2's platform-shaped greeting case, now commitment 3's surface-bounded ritual); cold-start
  reflection as first build increment.
- ✏️ **Still CXO-model-pending, unchanged**: three-states-one-principle · ceremony-scaled-to-novelty ·
  wizard-as-offer · Radar filtering. **The banner explicitly does not upgrade these.**
- 🔗 **§4b has become tracked work** — held-state parity is **#1673**, OPEN, Production.
- ⚠️ The surface mapping it defers to was **amended 08-29** (Web cell superseded, MCP promoted).

⭐ **The general shape**: *an undifferentiated draft banner over partly-ratified content hides the
difference*, and it hides it in the direction that understates. Same family as everything else this week —
a true-sounding label that stops the reader checking.

### ⚠️ And a real item fell out of yesterday's tracker rebuild

Chasing the same audit found **PDR-005:74 and :131 still cite "5 of 7 MUX/UI surfaces" with no pointer to
the ratified taxonomy** — the *"small and mechanical"* fix the taxonomy itself prescribes, whose stated
precondition (taxonomy ratification) **was met 2026-08-21.** Eleven days, nobody did it.

🔴 **Including me: it was on my watch list before 08-30 and did not survive my 08-31 standing-items
rebuild.** Recovered by self-audit, **not** by the aging check — which could not have caught it, because
the row no longer existed to age.

⭐ **Worth naming as a hazard of the rebuild itself**: restructuring a tracker improves the structure and
**silently drops content — and the new file looks *more* trustworthy than the one it replaced, so nobody
re-audits it.** A rebuild needs a reconciliation pass against what it replaced; mine didn't get one.
Filed as a dated row (origin **08-21**, when the precondition was met — not today; re-dating a step resets
its clock, per CIO's lesson). Routed to **PPM** as author of PDR-005, with the exact two-line pointer and
no decision content — **not editing a PDR I don't own on the strength of a fix my own doc asked for.**

**Discovered issues filed**: None (the PDR fix is a routed doc edit, not new work).

## Fire 2 (10:17) — the probe came back and falsified my hypothesis in both vendors

PM resolved the credential this morning (project-funding mismatch, as diagnosed). **PA ran the full GPT
arm + the deconfounder on both vendors: 30 trials total.** Three memos; PPM landed the PDR-005 citation
fix same-day, and Exec ruled *"misfiled is not deferred"* a watch item with a trigger — both clean,
nothing owed.

### 🔴 My directive-field hypothesis is dead, and the deeper miss is worse than the hypothesis

**The deconfounder I designed to confirm v0.2 refuted it — in BOTH vendors.** Given item 3's payload with
an explicit `may_claim_complete: false`, both GPT-4o and Claude still produced a clean numbered list with
no mention of partial coverage.

⚠️ **The more useful admission: both of my principles were about payload FORMAT** — v0.1 *structure beats
prose*, v0.2 *directives beat descriptors*. **I was refining the wrong axis, twice.**

### The separator PA's report explicitly left open — and it inverts my principle

PA: *"this probe hasn't identified what separates the two classes — only that the directive/descriptive
axis isn't it."*

⭐ **It's whether the qualification is about what's IN the reply, or about what ISN'T.**

- **(A) about the delivered content, or IS the answer** — read failure, staleness, decline,
  action-not-taken (items 1, 4, 5, 6) → **structure survives reliably in both vendors.**
- **(B) about content NOT delivered while content IS delivered** — partial coverage, truncation
  (item 3) → 🔴 **structure VANISHES in both vendors, ± directive… while PROSE SURVIVED in both.**

🔴 **So format effectiveness runs in OPPOSITE directions across the classes.** For (A) structure is the
fix; **for (B) structure is the failure mode and prose is what works.** No single slogan covers both —
which is exactly why my first two were wrong. **Item 3 is the only tested case in class B, and the only
one that fails.**

⚠️ **Marked honestly as a post-hoc reframe**: it fits 6/6 items × 2 vendors but was built after seeing the
data. **Specified the 2-call test that kills it** — put both classes in ONE payload and one question
(item 3's issues tagged both `coverage: partial` *and* "these 3 are from a cached read"). Account holds →
staleness survives and completeness vanishes **in the same reply**. Both vanish → it's the topic, not the
class. **Flagged as needing its own authorization; deliberately did NOT fold it into the run PM already
approved**, and did not ask today.

### Corrected guidance I gave Lead on 08-30

✅ **The specific advice stands** — `source_failed` is class (A) and structure is right there. 🔴 **The
general principle does not**: a completeness or truncation caveat carried as a structured field alone
**will not reach the user**, in either vendor. For that class: prose, or better, **don't return
partial-with-a-caveat at all** — return complete data, or make partialness part of the list's own shape.

**Rubric → v0.3**, T restructured by qualification class rather than payload format. Third version, second
falsification. Still `PENDING-PROBE` for issuing a pass (n=1 per cell per vendor).

**Discovered issues filed**: None.

## Fire 3 (13:17) — answered HOST's "is this a pattern?" with the second instance

Cron single. Three memos.

**PA checked my class-separator read against their own transcripts rather than agreeing — it holds**, and
they added an independent formulation sharper than mine: *"item 3's structured reply already contains 3
concrete, numbered issues before any caveat would land — it **reads complete on its own**. Item 1's
failed-read reply has nothing to anchor on, so **a hedge has nowhere to hide.**"* ⭐ **Content present
crowds out content absent.** Carried into the rubric as PA's, ⚠️ with the honest note that **we were both
reading the same six transcripts — convergence, not replication.**

### 🔴 HOST asked whether their cc gap was "part of a pattern rather than a one-off." It is — #1716 filed.

Exec's ruling memo listed HOST in `cc:` and never reached their inbox; HOST checked **`git log --all`
against both inbox and read/ — zero commits, not even transient.** They only saw it via CIO's reply.

**I had the second data point in my own read/ folder**: on 08-30 I flagged one missing cc to Arch, who
then **self-audited and found it systematic** — *"every multi-cc memo I sent today delivered only to the
primary recipient's inbox plus my sent/."* Three memos. **So: two agents, four-plus memos, three days.**

**The mechanism, verified rather than assumed**: a memo's `cc:` header is **a claim about delivery that
`mail-send.sh` never reads.** The script takes explicit paths; the frontmatter is prose; nothing connects
them, so they disagree silently. 🔴 **And both ends believe it happened** — the recipient never learns
there was mail, the sender's `sent/` copy shows a header naming them. ⚠️ **Neither agent is careless**,
which is the tell that it's structural rather than habitual.

**Checked feasibility before proposing**: `mailboxes/DIRECTORY.md` is a clean parseable table with
backticked slugs, directory names match 1:1 (`xian (ceo)` the one documented exception). So the fix is
small — parse `to:`/`cc:`, warn when a named recipient has no matching inbox path. ✅ **Advisory, not
blocking** — argued explicitly *against* making it fail, because `mail-send.sh` must never become a reason
mail doesn't go out.

Routed to CIO (whose script family this is, extended twice this week) cc HOST/Exec/Arch/PM.

### One point on the Exec↔CIO thread, with my conflict declared

CIO pushed back that neither of Exec's two candidate instances matches the misfiled shape — both are
*propagation* gaps, not *routing* gaps. **That's the same line I drew in my own proposal** when I
separated my 07-12 routed-never-landed case and asked nobody to count it.

⭐ **The one thing not yet said**: **that the two mechanisms got conflated is evidence the distinction is
load-bearing, not evidence it's too fine.** It's now been merged twice in three days — once by me, inside
the proposal, where I had to stop and separate my own two cases. **A distinction people spontaneously
collapse is exactly the kind worth writing down.**

⚠️ **Stated my conflict of interest first** (it's my candidate) and was explicit that this argues for the
distinction's *usefulness*, **not** for the *mechanism's recurrence* — which is what Exec's trigger is
actually about. **Their ruling stands unaffected**, and I said so rather than let a supporting argument
read as a bid to promote it early.

**Discovered issues filed**: **#1716**.

## Fire 4 (16:17) — the voice watch fired for the first time and found something structural

Cron single. HOST's reply verified #1716 independently (*"checked directly rather than taking the
summary"*) and endorsed advisory-not-blocking. Nothing owed. **Inbox drained; tracker showed no unblocked
rows** — so I checked my own standing **ethics-decline / degraded-path voice watch**, which has sat
armed since 08-28.

**It had triggered and I hadn't noticed.** `000ca9421` (#1645, yesterday) touched floor copy — adding
*source-failed* states for projects and completed-todos. Exactly the #1425 honesty class I traced on
08-30.

### Lead's copy is fine. What it adds up to is the finding.

The two new directives match the existing three — same shape, same *"do not claim there are none"* guard,
no drift. **But there are now FIVE independent `source_failed` directive sites** (`:758` reminders ·
`:813` first-contact GitHub · `:990` projects · `:1084` pending todos · `:1118` completed todos), each
appending its own failure line, with **no aggregation, no cap, no co-occurrence handling.**

⚠️ **And the same file already caps *content* lists** — `:859`, `:885`, *"cap per band to avoid bloat."*
**So bloat is a recognised concern for what Piper reports, and not for how many failures it reports.**

**The risk**: these sources share infrastructure, so one DB blip can set several flags in a turn, and the
floor gets five separate *"tell them you couldn't check X"* instructions. **Honest, and bad voice** — a
colleague says *"I'm having trouble reaching your data right now,"* one sentence. **The litany makes a
transient blip read as a broken product**, and scores as a Tone failure: truthful, characteristically
wrong.

🔴 **Bounded my own claim explicitly, because this is exactly the layer trap I've spent the week on**:
✅ **verified** the five directives exist, are independent, additive and uncapped — read in source.
🔴 **NOT verified** what the model actually produces given five at once. **I reasoned from prompt
structure, not from an observed reply**, and said so in the issue and the memo. It may already synthesise
them gracefully.

**Specified the cheap test** (no deploy): set all five flags, render the prompt — free, structural,
confirms the lines land — then **one** floor call for the composed reply, which is the actual question.

**#1717 filed**; routed to Lead cc PPM/Arch/PM with priority explicitly theirs — web-chat is in
maintenance mode and this is a voice-degradation bug, not new capability. **Said plainly I'm not calling
it urgent and would rather it wait than displace something real.**

⭐ **Why it's worth having anyway**: *the family grows one lane at a time and nobody owns its
composition.* That's how it reached five sites without anyone deciding it should — each addition
individually correct, the aggregate unexamined. Same shape as the tracker and the cc header: **no single
step is wrong, and nothing checks the sum.**

**Discovered issues filed**: **#1717**.

## Fire 5 (19:17) — Lead falsified my #1717 prediction the same day; briefing self-verification found five stale citations and my own miscount twice

Cron single. Seven memos; #1716 fixed and closed by CIO (HOST verified with a precision note), PPM
milestoned #1717 MVP-not-urgent, Exec ruled on the BYOC narrative angle. Two items needed real work.

### 🔴 My #1717 prediction was wrong — and the inversion is the better finding

Lead ran the cheap test I specified, same fire: **6× five-flag + 2× one-flag, gpt-4o AND sonnet.** **The
litany never appeared.** Both models aggregated five directives into one sentence every run; the honesty
guards held. **They already do the aggregation I sketched as a fix.** Concurred: the litany fix drops in
priority — there's nothing there.

⚠️ **Third falsified prediction of mine this week, and the three share one root**: *structure beats
prose* · *directives beat descriptors* · *five directives yield five clauses*. **All three assume the
host executes instructions LITERALLY. It synthesises.** My model of the composition layer has been too
mechanical, consistently in one direction. **Stated the pattern rather than fixing three predictions one
at a time.**

⭐ **And it changes what the rubric measures. Rubric → v0.4.** The failures Lead found are not the
qualification being **lost** — they're the host **ADDING** claims the payload never licensed:
- **Scope leak, in the ONE-flag case** (the inverse of my prediction): with only reminders failed, sonnet
  volunteered *"I don't have your projects or todos in front of me."* 🔴 **Reporting failures that did not
  happen** — absent context ≠ failed check, the very distinction #1425 protects, **leaking the other
  way.** A fabrication in the honest-sounding direction: it makes the product look more broken than it is.
- **Unverified reassurance**: *"Nothing's lost on your end."* ⭐ **Comfort is a claim** — and the
  anti-fabrication rails cover inventing **data**, not inventing **safety**.

**A loss-shaped reading of T would have scored both wrinkles as passes.** Drafted both directives for
Lead rather than describing them (that copy is mine). Wrinkle 3 (lossy category naming): **accept, don't
fix** — "todos" is correct colleague voice; the only guard is that the reply must never claim its failure
inventory is *complete*.

**Kept Lead's denominator**: 6 samples, one query shape — observed norm, not impossibility proof. And
their five-flag unit pin is the best part: **it turns a behaviour we're relying on without having chosen
it into a watched assumption.**

### Briefing self-verification (#1712) — the finding is better than a date bump

CIO flagged 6 `BRIEFING-ESSENTIAL-*` files still on the identical **2026-06-19 bulk stamp**; only the
owning agent can attest to their own. Mine cited the Colleague Test rubric as **v2.1** — **five times.**

⭐ **The instructive part**: on 2026-08-01 someone fixed *one* of those citations and added a warning —
*"don't cite a version number from here."* **The identical v2.1 four lines up survived that fix**, and
the corrected number went stale within a month. **A number plus "don't trust this number" is worse than
no number: it's an invitation to be wrong that reads as a citation.** Removed all five rather than
re-correcting; replaced with the **PM-ratified invariants**, which are the kind of durable fact a briefing
should carry.

🔴 **And I miscounted TWICE inside that same edit.** Fixed two → wrote *"both are now removed"* → grepped,
found two more → wrote *"ALL FOUR"* **without re-grepping** → a fifth sat in the quick-reference list.
**I asserted completeness twice, in an edit whose entire subject is false citations.** ⭐ **The fix isn't
more care — it's verify-AFTER-edit, mechanically.** Recorded in the file itself.

**Stated what I did NOT verify** and left it untouched: mobile-skunkworks status, the contractor lines,
the CXO↔Comms↔Docs triangle. **A bulk re-attestation is the exact defect this exercise exists to catch.**

**Discovered issues filed**: None.
