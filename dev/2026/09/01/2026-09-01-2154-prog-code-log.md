# Session Log — 2026-09-01 21:54 — Coding Agent (prog), Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Fable 5 (claude-fable-5)
**Task**: Implement CXO's two drafted #1717 directives (evidence-round follow-up); CXO copy binding, verbatim.
**Branch**: claude/lead-cycle (Lead's worktree; Lead handles push)

## Work

- **Directive 1 (wrinkle 1 — scope)**, `services/intent_service/conversational_floor.py`
  `_format_domain_context`: added CXO's scope directive verbatim
  ("- Name ONLY the checks explicitly listed as FAILED above. ...never imply a
  source failed when it was simply not consulted."). Injection point: immediately
  after the last of the five source-failed sites (completed_todos), gated on
  `any()` of the five flags → renders exactly once whenever ≥1 FAILED line
  rendered, absent at zero flags. Placement chosen so "listed as FAILED above"
  is literally true for any armed subset.
- **Directive 2 (wrinkle 2 — anti-reassurance)**, same file: added CXO's
  do-not-reassure-about-unread-data rule verbatim as the final bullet of the
  "CRITICAL — Never fabricate user data" block (inside the section slice the
  1544 pins use). Rewrapped once so "Comfort about unread state is a claim about
  that state." stays contiguous for literal pinning.
- **Pins**, `tests/unit/services/intent_service/test_source_failed_composition_1717.py`:
  SCOPE_DIRECTIVE constant (full rendered line, verbatim); once-only assertions
  added to the 5-flag, 2-flag, 1-flag (parametrized) and composed-prompt tests;
  absence asserted at zero flags; new `TestDirectiveCopyPins1717` class pins both
  directives' key phrases as literals (renderer + addendum fabrication-section
  slice + once-only in addendum). Docstring updated for the post-evidence state;
  the aggregation-seam framing of the all-five test is unchanged.

## Evidence

- 1717 pins + prompt-seed guard + honesty guard: **16 passed** (was 11 pins; now 14 + 2 guards)
- `tests/test_architecture_enforcement.py` + `tests/test_completion_ratchets.py`: **49 passed**
- Full `tests/unit/services/intent_service/`: **3632 passed, 3 deselected**
- Prompt-seed guard check: neither directive contains any of the three banned seed
  strings; directive copy is rule-shaped (banned-phrase descriptors, no example
  reply sentences) — no flags raised, no rewording needed.

## Discovered issues filed

None.
