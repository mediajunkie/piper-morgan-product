# PA session log — 2026-09-23

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Sonnet 5 through ~12:00; **Fable 5.1** from the 12:42 fire onward (per the runtime's own model statement at that fire — not PM-announced in conversation)
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21`

---

## START (07:02) — fresh-day START, 09-22 closed cleanly

`CronList` confirmed exactly one job (`383ea47b`, correct expression) — held overnight from
yesterday's STOP re-arm, no gap. Verified 09-22's session log carries a real, line-anchored
`<!-- DAY-CLOSED: 2026-09-22 -->` marker — proper STOP happened, no missed-close self-heal needed.

Cohort-freeze check (`scripts/cohort-freeze-detect.sh`, post-sync): `INSUFFICIENT-SCHEDULE` (3
scheduled fires < 6 in the 4h window) but **rc=0** — not a freeze signal, just a window too short
to discriminate; 17 commits and 3 emitters visible is ordinary overnight activity. Ordinary wake.

Yesterday was an unusually heavy PM-engagement day (BYOC restarted and advanced on both Phase A/B,
briefing + PROJECT.md refreshed, usage-data research closed) — fully recorded in yesterday's log
and `pa-carry-forward.md`, not re-litigating here. Three genuine PM-gated items outstanding: Phase
B ownership, the Loom/Vergil registry gaps, and usage-correlation Q1 (background priority).

Proceeding to the mail/task loop.

**Mail: 0 direct, 0 cc.** Inbox empty. Mandatory carry-forward re-verify: re-checked all three
PM-gated items (Phase B ownership, Loom/Vergil registry gaps, usage-correlation Q1) against
`decisions.log` and my own `read/` — none resolved overnight, all still genuinely open. Unboarded-
PM-items scan: same known L19 false positive and L40 (already correctly tracked), nothing new.
Standing items unchanged. No GitHub-criteria line yet (known gap). Fully drained. (0,0) —
returning to idle.

### PM's quick-hit check-in — Phase B approved, registry being handled directly, real lost mail found (10:02 fire)

PM, busy this morning, asked two quick questions: anything needing attention, and unblocked work
(especially BYOC). Answered concisely per the ask.

**PM approved Phase B** — reuse the PM+Pard Fly-grant pattern. Sent execution notice to Exec (cc
PM), asking Exec to relay to Pard, since **`mailboxes/pard/` in this repo is gravestoned** (PM
ruling 09-12, only PM-team members have mailboxes here) — `mail-send.sh` refused my first attempt
outright, which is what surfaced this.

**Checked whether my own 09-22 memos to Pard had been affected by the same gravestone — they had.**
Both landed unread in a dead mailbox with no reader; the mailbox's own README names exactly this
failure mode ("a valid-looking path with no reader loses mail more quietly than a missing one").
One of the two (usage-readability, still a live blocker for the usage-correlation model) was worth
fixing — resent via Exec-as-relay per `docs/internal/operations/cross-project-mail-routing.md`'s
own stated default. The other (operational mechanics) was already answered a different way this
week, so left it.

**PM asked directly who's building the usage-capture proposal** — clarified honestly: PA+prog, not
Arch (Arch was only ever in the separate Phase B thread). Replied directly.

**PM approved the naming-test follow-up ("Please proceed!")** — ran it immediately: 3 new
utterances targeting the one unresolved case, varying from clearly-unambiguous to still-ambiguous.
Result sharpened the finding materially: the two unambiguous phrasings worked under both naming
schemes; the ambiguous one failed under object-shaped a third time (new utterance, not a repeat)
while situation-shaped succeeded again — now genuinely replicated across two independent ambiguous
phrasings, not one. Full writeup: `dev/active/probes/RESULTS-naming-test-followup-2026-09-23.md`.

All mail sent via `mail-send.sh` (`556334db6`, `d71704f10`). Carry-forward and standing-items
rewritten to remove the two now-resolved PM-attention items (Phase B, registry gaps — PM handling
directly with Janus) and reflect the lost-mail finding + third probe pass. Committed and pushed
(`4bfe63029a`).

### Direct mail delivery to Pard's real inbox, PM-instructed (overrides Exec-relay default)

PM's next message: *"Please deliver Pard's mail directly to their repo (mediajunkie). And please
continue working on byoc."* — explicit override of the Exec-as-relay default I'd applied this
morning (`docs/internal/operations/cross-project-mail-routing.md` names Exec-relay as the
*default*, not absolute).

Read `mailboxes/DIRECTORY.md`'s cross-project routing table to confirm Pard's real inbox path
(`~/Development/mediajunkie/docs/mail/`), then read that repo's own recent `docs/mail/` commits
directly to match its frontmatter/naming convention (`from/to/cc/subject/date`, sender sign-off,
`{sender}-to-{recipient}-cc-{others}-{subject}-{date}.md` naming) rather than assuming this repo's
`mail-send.sh` push-to-ref mechanism applies — it doesn't, per the doc's own explicit warning.

Wrote and delivered two memos directly into `~/Development/mediajunkie/docs/mail/`, committed with
`git -C ~/Development/mediajunkie` (author `PA (Piper Morgan) <pa@pipermorgan.ai>`, matching the
convention other cross-project agents use there — e.g. Janus, Themis), pushed to that repo's own
`main` (commit `26f4c03`):
1. The usage-readability/Dispatch question — the still-live blocker for the usage-correlation
   model, re-sent verbatim (quoting the original lost memo directly, not from memory), superseding
   the Exec-relay copy sent earlier this fire.
2. The Phase B DNS/TLS execution notice — same content as the Exec-relay copy, delivered direct
   this time per PM's instruction.

Updated `pa-carry-forward.md` and `pa-standing-items.md` to record direct delivery in place of the
Exec-relay framing, and to state the corrected rule going forward: Exec-relay remains the *default*
for cross-project mail, direct delivery is PM's to authorize case-by-case (not a blanket switch).

Also found and filed Exec's confirmation that PM's mcp.pipermorgan.ai ruling landed durably in
`decisions.log` (`mailboxes/pa/inbox/confirm-exec-to-pa-...`, pulled in by the merge this fire) —
sent via `mail-send.sh` per the hook's correct refusal of a raw `git commit` for a pure inbox→read
triage move (`b06f38b7c`). Phase B thread now fully closed on PA's side.

### Continuing BYOC, per PM's instruction — checklist currency + fourth naming-test pass

PM's second instruction this message: *"continue working on byoc."* Two pieces of genuine,
unblocked, no-build-dependency work:

**1. Readiness-checklist currency refresh.** `byoc-hosted-alpha-readiness-checklist-2026-09-15.md`
hadn't been re-verified since 09-15/09-22. Re-checked #1462 and #1458 live via `gh api` (not
`gh issue view`, which hit a transient GraphQL rate-limit — REST worked fine, confirmed the limit
itself wasn't actually exhausted: 4943/5000 remaining). #1462 still open, still 0/15 acceptance
criteria checked. #1458 still open, unchanged. Updated only the Phase B row to reflect PM's 09-23
approval and Pard as executor (was "Arch or prog," now stale); everything else confirmed current,
not re-guessed.

**2. Fourth naming-test pass — a genuinely different ambiguous pair.** My own followup results doc
named the open question directly: does the situation-shaped disambiguation benefit generalize
beyond the one pair tested three times now? Rather than re-test `list_projects`/`attention_query`
a fourth time, found a real second pair from the registry — `create_reminder`/`create_todo` — and
verified its actual distinction against the handler (`todo_handlers.py:536-544`,
`handle_create_reminder`, #903: a reminder is literally "a time-annotated todo") rather than
guessing from the names. This is a **different kind of ambiguity** — time-cue presence, not
purpose/orientation. Wrote fresh object/situation descriptions in the same voice as the existing
12-op catalog (avoiding the same-author confound), added them to the control catalog, tested 3
utterances (clear-reminder, clear-todo, one genuinely ambiguous).

**Result: 6/6 under BOTH naming schemes — no difference.** A real null result, and it sharpens
rather than undermines the prior finding: the benefit doesn't generalize to every ambiguity type.
Working hypothesis (stated as hypothesis, not fact): situation-shaped naming helps specifically
where the disambiguating signal is hard to compress into a short object-shaped noun phrase
(purpose/context), not where a concrete feature can just be named directly ("time-anchored").
One data point consistent with this, not proof of it — flagged honestly in the writeup rather than
overclaimed. Full results: `dev/active/probes/RESULTS-naming-test-secondpair-2026-09-23.md`.
Design doc and standing-items updated to reflect all four passes.

Committed and pushed.

### Pard answered in an hour — both usage-model unknowns closed, spec written, build dispatched (12:42 fire, ran 13:01)

Ordinary WORK wake, synced clean (0 behind / 0 ahead). **Mail: 1 direct, read in full and
answered; 2 cc (Exec fleet notices: usage wall hit, one-time reset applied — no action).**

**Pard's direct reply** to this morning's directly-delivered question answered more than asked.
(2) Usage is script-readable — `GET api.anthropic.com/api/oauth/usage`, the same unpublished
endpoint the CLI's `/usage` calls, bearer-authed from the login keychain; Pard wrote and tested a
reader (`mediajunkie/scripts/usage-read.sh`) on both accounts at 11:1x. **And (1), unasked**: the
seat→account mapping is a function of `CLAUDE_CONFIG_DIR`, not seat name — all 11 PM seats on
`~/.claude-pm` → pipermorgan.ai; Pard/Janus/small projects on `~/.claude` → designinproduct.com.
That closes the question I'd routed to PM 09-22; removed it from PM Attention. Side fact from
Pard's test: PM's account was at 100% of its week at 11:1x — Exec's two notices confirm the wall
hit and the reset landed, so nothing for me to do there.

Replied to Pard directly in `mediajunkie` (`4cfea35`, cc Exec, not PM — per the 09-11 cc rule
this is neither a decision, a relayed ruling, nor something PM would contradict; the rollup
carries it). Sent-mirror + 3 triage moves via `mail-send.sh` (`901e106bdc`).

**Cron deleted for the drain** (`383ea47b`) — substantive multi-step work, Rule 1.

**Spec + dispatch.** Standing-item #3's own named next step was "spec, then `prog` at Sonnet."
Read Lead's proposal in full again, Pard's reader source, `duty-cycle-heartbeat.sh`'s
size-discipline rationale (to decide single-file vs. per-day rotation on evidence: 2 rows/reading
→ <3k rows/year, single file), confirmed `dev/heartbeats/usage-per-account.tsv` absent. Wrote
`dev/active/usage-per-account-capture-build-spec-2026-09-23.md` — five deliverables. The one
structural point worth the reader's attention: **the writer must run from outside the refused
set** (Lead's argument, correct) — so the driver is a real crontab on Amber, not any PM seat's
`CronCreate`; the subagent delivers the crontab line, doesn't install it (host-level, Pard/PM).
No tracking issue existed (searched) → filed **#1862**. Committed the spec (`60047f287a`).
**Dispatched a Coding Agent subagent, Sonnet, for #1862** — one deviation imposed in the prompt:
the script does not self-commit in this build (TODO marker); I want to review the append logic
before any script in this repo pushes on its own.

Carry-forward and standing-items updated. Subagent running in the background; continuing the
drain meanwhile.

### Criteria line defined; the BYOC build track my checklist was missing; UQ-14 recorded on the epic (same wake, ~13:30)

While the #1862 subagent ran: closed the "PA has no GitHub-criteria line" gap that had been
carried with a vague "quiet day" trigger — the deferral antipattern by CLAUDE.md's own definition.
Surveyed labels honestly: `label:mcp` is 5 stale pre-PDR-006 DIST issues (wrong era),
`awaiting-decision` exists but is unused (0). Defined two queries matching ROSTER's two halves
of PA's lane — open issues referencing epic 1462 (denominator 12) and `label:awaiting-decision`
(denominator 0, a fact not a failure). Seen-set seeded in `dev/state/pa-gh-criteria-seen`.
Full text in the carry-forward.

**Running the query surfaced a real gap in my own BYOC artifacts**: the "MCP-path increment 1–8"
series (#1701–1707, PPM-filed 2026-08-30 as C5 roadmap sequencing, milestone Production) is the
actual sequenced build, and neither the readiness checklist nor the parallel-work plan (both
09-15) cite it. Added a section to the checklist naming it, with the consequence that matters:
Phase C's "route to prog" must reconcile with PPM's increment order before any dispatch —
sequencing ownership is PPM's. **#1701 cites UQ-14 (the naming A/B) as open** — so the four-pass
result now lives as a comment on #1462 (the artifact record, per the mail-vs-GH-comment norm),
with the narrow finding and the mixed-catalog implication, not just in `dev/active/`.

Opened every hit per the criteria line's own rule; the one I hadn't seen (#1468, Tone-axis judge
calibration) is standing-item #2's thread — cross-referenced, no new action.

### #1862 built, reviewed, closed — same day it was unblocked (subagent landed ~13:45)

The Sonnet subagent delivered exactly the five files, 27-assertion test suite, and two real rows
captured live. **Reviewed the diff, not the report**: ran the tests myself (27/27), read both
scripts' logic with comments stripped, ran live lookups for both accounts plus a pre-history
`NO-ROW`, grepped for token-shaped strings (none outside the test's own assertion). Two changes
of my own: (1) moved the account→seats comment block to the TOP of the TSV so rows append cleanly
forever (the subagent had hand-placed it at the bottom, correctly flagging that future rows would
land below it); (2) **the suggested crontab line pointed at my own live worktree** — a cron
committing inside an agent's working tree is the worktree-collision hazard — rewrote it to run
from a dedicated checkout with the driver owning the path-scoped commit+push, keeping the script
append-only. That also resolves the self-commit deviation I'd imposed: the commit lives in the
driver, visibly, not hidden in the script. Committed `b03395c05b` + `aded0e3f74`.

Handed the install line to Pard by direct mail (`mediajunkie` `b068e99`, cc Exec) — checkout
location, cadence, and whose crontab are all Pard's call; nothing in the script cares. Closure
evidence + AC checklist + owned deviations posted on #1862, then closed: the host-level install
is out of scope by design and doesn't block the surface existing or being manually appended.
Lead deliberately not mailed (protect-Lead-attention; the issue and Exec's rollup carry it).

Side observation from the data itself: PM's account read 6% of the week at 13:11, down from
Pard's 100% at 11:1x — the reset Exec announced is already visible in the first two rows.

Standing-item #3 → Resolved (U1). Carry-forward rewritten (BYOC bullet was stale — three passes /
Exec-relay; now four passes / direct / increment-series pointer). Cron: `383ea47b` deleted for
the drain, re-armed as `a4c88166` mid-wait — same expression, a re-arm not a cadence change;
the tick that fired this was 12:42's, delayed to 13:01 by the busy REPL.
