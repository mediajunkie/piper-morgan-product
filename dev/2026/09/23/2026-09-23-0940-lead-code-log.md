# Lead Developer session log — 2026-09-23 (Claude Code)
Role: Lead Developer (lead-code) · Model: Fable 5 (claude-fable-5) until ~12:45, then **Fable 5.1 (claude-fable-5-1)** — PM moved the session mid-day ("Move you to Fable 5.1"), system-attested 12:4x · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron: 0cce894b (17 6,9,12,15,18,21), armed at START-backfill

*(Header + START entry restored 12:5x: the file was truncated by a hung background command's `cat >` — a shell-precedence bug in my own 09:45 START command — and later appends landed under no header. Content below is complete; only the header/START block was lost and is reconstructed here from the same morning's events.)*

## MIGRATION AFTERMATH DAY — PM ran Test 1 on the Fly-served alpha; findings triaged; STOP backfilled

### START (09:4x, PM-engaged: Test-1 results in) — retro-close of 09-22 done first (Step 0)
- Prior day lacked DAY-CLOSED → retroactive close written into the 09-22 log (annotated marker).
- **PM RAN TEST 1 on alpha: ROW 1 PASSES #1617's criterion** — turn 2 reached the issue rail
  first try (crisp confirm), no standup swallow. #1739's last dependency discharged. Full
  transcript analysis: 6 findings filed (#1855 unarmed floor offers · #1856 add-project flow
  arg-drop + verbatim-repeat loop · #1857 name-extraction greed · #1858 404-close indeterminate
  copy · #1859 white flash on chat switch · #1860 standup-initiation coverage gap) + evidence
  comments on #1843 (exact-match accepts, by design) and #1828 (floor said Monday on a
  Wednesday). White-flash cutover-header hypothesis KILLED with evidence (bare Caddy
  reverse_proxy; asset headers unchanged) → routed to Web's render sweep.

### STOP-backfill executed in full + the mechanism proposal (PM directive, 10:0x–10:2x)
- **Backfilled from the missed 09-22 STOP**: retro-close (day-arc + memory-eval + sign-off +
  annotated DAY-CLOSED marker — written earlier this morning, committed now after the slow
  post-commit hook stalled the first attempt) · carry-forward FULL REWRITE with Exec's
  spring-clean applied (resolved threads deleted; current state + live threads only) · registry
  trim: checked-not-needed (lead row = 215 chars) · cron re-armed **0cce894b** (delete-then-
  create n/a — none existed; that absence WAS the root cause) · registry row updated with the
  cause on the record.
- **Root cause, stated plainly**: Rule-1 deleted the cron for the evening drain; "re-arm at
  idle" is vigilance-dependent and the drain ended by the session going idle mid-conversation —
  no cron, no 21:17 fire, no STOP. Step 0's detect-and-backfill worked as designed at START.
- **Mechanism proposal SENT to CIO (skill owner) cc Exec/PM** (ac685b4ff): Rule-1's delete
  becomes **delete-AND-swap** — a one-shot STOP backstop at the day's last slot created in the
  same action, making the day-close structurally unloseable by drains; idempotent with later
  re-arms; plus the deeper optional question (is Rule-1's delete protecting against a hazard
  the REPL-busy runtime already prevents?). Adopted as personal practice immediately — baked
  into the new cron prompt itself.
- PM test-session processing (earlier this turn-block): row 1 PASSED/struck, #1739 discharged,
  #1855–#1860 filed, #1843/#1828/#1617/#1739 commented, card + artifact republished (v2).
- **PM ratified the book-end framing** ("Rule 1 only works if it is book-ended by restoring a
  cron when finally ready to go idle") — relayed verbatim to CIO cc Exec/PM (fad90a965) as the
  invariant the skill amendment should lead with.
- **Test-2 key-isolation nuance added to card + artifact v3**: PM holds BOTH provider keys; a
  valid OpenAI key could serve the request via the #1823 any-provider gate and mask the #1824
  error path — the invalid Anthropic key must be the only stored key during the test.
- **Card clarity fix after PM's (justified) confusion**: rows 1 (#1824) and 2 (#1822) were
  blurring in chat — the card + artifact (v4) now spell the distinction (error-message/web/
  broken-key vs success-path/Slack/valid-key), and **#1822 is UNPARKED** conditional on PM's
  Slack link: PM holding a real OpenAI key dissolves the "needs a separate account" park
  reason. One-sitting key-juggling sequence documented on both rows.

### #1858 build (12:3x–, PM asked for unblocked work while managing other things)
- Mechanism traced: adapter `_verified_write` → write response unparseable (in-band 404) →
  read-back unparseable → `verified=False` → router raises the unverified RuntimeError → floor's
  "may or may not" copy. Fix: `GitHubWriteResult.not_found` (definitive ONLY on positive
  not-found evidence from BOTH legs, update-shaped writes only) → router raises typed
  `GitHubIssueNotFound` → `_unverified_write_result` returns the plain "There's no issue #N"
  result before the unverified branch (zero call-site changes). Tests + battery next.
- 12:17 wake arrived mid-drain (PM: usage limit hit and reset). 12:4x: PM moved the session to
  Fable 5.1. Inbox 11 (two Arch rulings, Web finding, CIO shipped v139) — read after #1858 lands.
- **#1858 CLOSED** (~12:5x): definitive not-found bucket (adapter both-legs evidence → typed
  router error → plain floor sentence); 10 new tests, 508 neighborhood green, smoke 532, gate
  at ceiling. Live retest = PM's turn-2 script after the next deploy (card row to add).

### Inbox drained 12 (13:0x–13:1x) + two rulings executed
- Arch GO on #1774 (full family, spatial cross-check clean; lens surface = separate Rule-0
  census) · Arch ruled #1841 semantics (search_documents) + pointed deposit mechanics · CIO
  shipped Rule-1 book-end as skill v1.39 · Pard ACCEPTED the §4e lane (token facts → CI deploy
  → staging; parity gate wired) — supersedes my carry-forward's "mine" note · Web BLOCKED: no
  alpha account exists (needs an invite minted = Fly-DB write = PM keystroke post-revocation) ·
  PPM's #1595 flag → RULED: own top slot ("epic 0, the interpretation spine"; status sourced
  as-of 8/24 with wave-1 traffic explicitly unverified) · Exec's #1744 relay → not mine, its own
  body says close after the memo lands · joint model-belt classification by 09-27 ahead of a
  Lead trial the week of 09-28 (cc, noted).
- **Deposits EXECUTED (#1841 + #1860)** via the phase-0 generator (HAND_ROWS + regenerate,
  104→107); shadow baseline re-frozen from measured production outcomes.
- **Instrument found DEAD and repaired**: all three Inversion scripts errored 107/107 since
  #1812 (no request key bound — the server-key fallback they leaned on is gone). Built
  `scripts/dev_key_binding.py` (keychain-sourced developer keys → request binding, refuses
  loudly); shadow score re-run: 39/49 matched, 0 ERROR; router already routes both #1860
  phrasings correctly and shares gpt-4o's drift on #1841's — deposit is the right instrument.
  Comparability caveat recorded in the run doc (served gpt-4o-mini; 08-14 model unrecorded).
  **#1861 filed** for the 3 remaining dead probe scripts.

### #1774 EXECUTED (13:0x–13:2x — time labels corrected, see 13:53 note) — 13 modules + 18 test files gone, zero surprises this time
- Rule-1 delete-AND-swap applied at drain start: recurring 0cce894b deleted, one-shot STOP
  backstop **d83f2df6** armed for 21:17 (my own morning proposal, first use).
- Cut-time sweep per delete-module-safely (module path, package re-export CLASS names,
  test-side referents, docs): articulation = class family inside orientation.py, first_contact's
  mention is prose; intent_types' only live re-export repointed → shim retired.
- Design record written BEFORE the cut (ideas per module, successors named, hash 67adfc2023).
- Cut in 3 sub-20 batches (the broad-staging hook blocked a 30-file stage — my own git rm read
  as a sweep; unstaged, surgery, re-batched). Surgery: 2 inits, conversation_context repoint,
  classifier comment, 4 stale unarmed-ask ratchet rows, arg-type ceiling 374→372, 3 current/
  docs annotated. Battery: collection 13,946 clean (−448 retired), 778 neighborhood green, smoke
  532, gate at ceiling (debt 1309→1307), ruff clean (2 pre-existing unformatted files are CIO's
  scripts — not touched).
- **#1774 CLOSED** with evidence; **#1863 filed** (lens surface, own Rule-0 census per Arch).
- Noted, not mine: ruff would reformat scripts/belt-mechanical-reasoning-proxy.py +
  scripts/trim-registry-history.py (CIO's, pre-existing on main).

### Parallel drain under the open reset (13:2x– — corrected) — PM: "maximum use of this reset"
- Dispatched two Coding-Agent subagents: **Sonnet** → #1861 (wire dev_key_binding into the 3
  dead probe scripts — mechanical, fully specified); **Opus** → #1856 (add-project flow
  arg-consumption + repeat-loop, red-first from PM's verbatim turns, flow-contract check
  included). I take **#1574 build** (design from 09-22 evening) myself.

### #1574 BUILT + LIVE-VERIFIED (13:2x–13:4x — corrected) · preferences routes were dead on arrival · #1861 landed
- **#1574**: manager gains read-through/write-through to users.preferences["upm"] (namespaced;
  #280/#1510 keys untouched), session scope in-memory by design, canonical cache key (callers
  pass str AND UUID — the two maps would have diverged and last-writer-wins the JSONB; caught
  while reading /stats, pinned by test). 7 real-DB tests (per-run UUID users, finally-cleanup):
  restart survival, cold-process clobber guard, collision safety, version/TTL, scope boundary,
  non-UUID stays memory, mixed-id one-map. **Live falsification through the real server**:
  seeded a pref from a SEPARATE process → running server's /stats returned it (hydrated from DB,
  never written by the server). Neighborhood 141 green, smoke 532, index ceiling 44→43 locked.
- **Live bug found in the way**: every /api/v1/preferences/* route did `current_user.id` on
  JWTClaims (field is user_id) → 500 forever; /stats also called .value on a value. Fixed (4
  sites + 1), real-TestClient pin proves red-on-old/green-on-fix; /profile 200 live for the
  first time. Issue creation deferred: **GitHub API limit exhausted** (5k/hr shared; two
  subagents + cohort) — file when reset.
- **#1861 (Sonnet subagent) landed**: 3 probe scripts wired, live-verified (zero key-bound
  errors, real llm_usage lines); subagent found a pre-existing bug in
  surface1_counterfactual_probe (patches PreClassifier.pre_classify but classifier.py:419 calls
  pre_classify_with_pattern_list → AssertionError row 1) — to file. Subagent correctly flagged,
  not touched, MY in-flight #1574 files + a session-log filename collision with the Opus agent.
- Working tree now holds Opus's in-flight #1856 edits (canonical_handlers, portfolio_service,
  its test) — deliberately unstaged; they currently trip the unarmed-ask ratchet (new question
  literals) — the agent's own checklist item, will surface in its report.
- Server on 8001 restarted twice on current code (old 09-19 process was holding the port).
- Issue actions via REST (GraphQL bucket exhausted, core fine): #1864 filed+closed (preferences
  routes), #1865 filed (probe patch mismatch), #1574 + #1861 CLOSED with evidence.
- **Cluster 1 tz family dispatched**: **Sonnet** → #1556 (naive datetimes in 3 modules) ·
  **Sonnet** → #1577 (document_service rolling-24h 'yesterday' → temporal_utils, user tz
  threaded if accepted). Unique log filenames mandated (prior collision). Opus #1856 still
  running. I take #1575 (calendar adapter's two 'today's) — disjoint file.
- **#1575 CLOSED** (~13:4x — corrected): one `_now_user_local` seam in the calendar adapter (free blocks +
  naive-range now on the USER's clock; get_todays_events already was); router threads the
  effective user id through free-blocks. 3 pins (Tokyo-vs-UTC 8h gap), 38 calendar tests green,
  smoke 532. Audit F3a/F3d remain with #1576/#1588's family.

### Parallel lanes landing (13:4x–13:5x — corrected) — #1856, #1588, #1556, #1866 closed; #1867 filed
- **#1856 (Opus) REVIEWED + LANDED**: the deeper cause — the add-project flow's process adapter
  is deregistered (ADR-059), so its question was unanswerable by construction; fix at the
  claiming handler (inline args → create+link in one turn; no-name → imperative + cancel, never
  the same line twice). 40 red-first tests. Ceilings drop re-measured in an isolated worktree at
  clean HEAD (index 39, call-overload 9 — attribution confirmed) → locked. **#1867 filed**: no
  registry of live-vs-dark guided processes; flows still start sessions on the dark surface.
- **#1588 (mine) CLOSED**: audit found only the two #1536 sites (no siblings); precedence flipped
  to param-wins/context-fills at both; 3 pins, 46 green.
- **#1556 (Sonnet) REVIEWED + LANDED**: 12/12 sites → utc_now, 11 red-first tests w/ AST guards;
  bonus **#1866** (todo_knowledge_service unimportable for a YEAR via a typing.List shadow —
  the whole Todo↔KG integration was dead code) fixed inline, closed with the product question
  left open.
- Dispatch tiers logged at dispatch. Still running: #1577 (Sonnet). Not yet dispatched: #1576.
- **#1577 (Sonnet) REVIEWED + LANDED** (~13:5x — corrected): document_service consumes temporal_utils'
  single 'today' with the user's tz threaded; frozen-clock red-first test; fail-safe except is
  logged (warning), not silent. **Cluster 1: 5 of 6 closed today** (#1574 #1556 #1575 #1577
  #1588). **Dispatched #1576 (Opus)** — render faces across standup/agenda/calendar (judgment-
  bearing: template-render tests, user-tz labeled faces, contract check).
- Day tally so far: cutover aftermath + retest triage (6 filed) + **13 issues closed** (#1858
  #1774 #1853 #1842 #1574 #1864 #1861 #1856 #1588 #1556 #1866 #1575 #1577) + 3 filed today
  beyond the morning six (#1861 #1863 #1865 #1867) + corpus deposits + instrument repair.

### ⚠️ Timestamp correction (13:53, from `date`)
Every time label from the #1774 execution entry through #1577's landing was ESTIMATED, not read
from `date` — I wrote 16:0x/17:0x while the real clock was ~13:2x–13:5x. The entire parallel
drain (#1774 cut, #1574 build, four subagent lanes reviewed+landed, #1575/#1588 by hand) ran in
~75 minutes of wall time, which is exactly why my sense of elapsed time was wrong: subagent
wall-clock and my turn count don't track each other. Labels above corrected in place with the
word "corrected"; ordering is intact. Rule re-learned the hard way: run `date` before writing
any timestamp (memory: feedback_verify_timestamps_never_guess). Going forward this session,
every entry header carries a `date`-sourced time.

### 13:5x–14:0x (`date`-sourced) — #1863 census delivered; #1499 split into fix-lane + Rule-0
- **#1863**: full reader census on the issue + memo to Arch: zero writers, one behavioral
  consumer (the #822 affinity boost, can't fire on None → absent ≡ empty), and the persistence
  is a JSONB KEY (layer4_state.lens_stack), not a column — no migration. GO requested.
- **#1499 (route audit)**: dispatched **Opus** for the non-deletion half (OAuth-start collapse,
  /api/v1/version for the two 404ing pages, dead UI calls, stale auth-exempt entries, doc drift);
  **Rule-0 request to Arch** for Class 2 (six unmounted routers, 59 dead defs) + shadow files.
- Running: #1576 (Opus), #1499 (Opus). Cron: recurring deleted for the drain; one-shot STOP
  backstop d83f2df6 armed 21:17.

### 14:0x–14:2x (`date`) — #1522's ratchet built; two lanes running
- **Surface-reachability ratchets LIVE** (test_completion_ratchets.py, gated): unmounted
  module-level routers (allowlist 4, each issue-tagged) + tracked shadow files (allowlist 6),
  explicit-list + ceiling, both directions tight; blind spot (class-scoped routers) stated
  in-file; negative-probed (a fresh dark router fails by name). Two of the audit's four legs;
  templates/assets legs named as the remainder on #1522.
- Running: #1576 (Opus, render faces — has started touching datetime_utils for a shared face
  helper), #1499 (Opus, non-deletion fixes — admin.py + settings_integrations in flight).
- **#1533 assessed** (14:0x): Guards 1–3 already implemented (TestPrincipalThreadingGuards1532),
  classify_conscious deleted (#1768), raw principal reads consolidated (principal_threading_lint
  rc=0; the one context-dict reader keeps its documented system fallback). Remaining = the 33
  probe-blind suites → **dispatched Sonnet** for the audit's named worst offender
  (test_multiuser_contracts.py: 26 calls, zero authed) with a teeth-proof requirement.
  Three lanes running (#1576 Opus, #1499 Opus, #1533 Sonnet).

### 14:1x (`date`) — #1499 non-deletion half LANDED (Opus lane, reviewed)
- 12 files: OAuth-start collapse (one implementation), /api/v1/version (deploy_identity verbatim,
  two-layer pin: route + template render), 2 dead UI links removed with Jinja notes, 2 stale
  auth-exempt entries pruned (near-miss caught: setup's callback lives at a prefixed path — pinned),
  conventions doc +88 with every claim re-verified (4 stale audit claims corrected). 48 re-run at
  review, lane's own 77/534/60 green. Issue commented; Class 2/5 disposal + ui.py exception class
  + /api/admin/* deprecation window remain with Arch.
- Running: #1576 (Opus), #1533 (Sonnet).
- **#1533 worst-offender LANDED** (14:19, Sonnet lane, reviewed): 14 authenticated variants
  (two real principals, same session) on the multi-user contracts suite; teeth-proof: forced-
  equal ids → 13/13 fail. 14 re-run at review. Issue kept open (32 suites remain, denominator
  stated). One lane left running: #1576.

### 14:2x (`date`) — #1576 LANDED: Cluster 1 is 6 of 6; agenda's 'TBD' was a key mismatch
- Opus lane reviewed + committed (14 files): 13/14 faces converged (aware ISO + browser
  toLocale where the browser renders; %Z-labeled user-zone faces where the server renders);
  found + fixed the pre-existing defect that made the agenda print 'TBD' for every calendar
  user (two key-name mismatches in series; free blocks never written); re-pointed two #1425
  tests #1575 had stranded on a dead seam (wall-clock-dependent). 479 at review, smoke 534,
  ratchets 60, gate at ceiling (4 ceilings moved, each measured vs clean HEAD). **#1576
  CLOSED**; **#1868** (GUIDANCE config-tz site) + **#1869** (Slack raw-ISO, zone chain decision)
  filed; three copy decisions routed to CXO.
- **Audit Cluster 1 COMPLETE** (#1574 #1556 #1575 #1577 #1588 #1576). All lanes landed; none
  running. Day: **18 issues closed** (#1858 #1774 #1853 #1842 #1574 #1864 #1861 #1856 #1588
  #1556 #1866 #1575 #1577 #1576 + morning's cutover-adjacent) — count re-verified below at STOP.

### 14:4x (`date`) — v0.8.14.0 "On Your Clock" CUT, tagged, released
- Commit `5912d6749a` on origin/main; annotated tag `v0.8.14.0` → same commit; GitHub release
  published (https://github.com/mediajunkie/piper-morgan-product/releases/tag/v0.8.14.0).
  `check-release-parity.sh v0.8.14.0` → PARITY OK (content-identical to origin/main).
- **`production` NOT advanced** — deliberate; retires with the droplet at step 11; deploys come
  from origin/main. Skill Phase 5 is therefore N/A this cut, stated rather than skipped.
- **Deploy is PM's keystroke** (Pard's sheet) until §4e's CI path lands. Live at cut: v0.8.13.0
  + 09-21 closures, sha 609a07b9 (last verified 09-22; the `/health` re-read this turn was
  classifier-denied as a compound command — not re-verified, stated as such).
- Test card v5 (file + artifact): surface note updated; rows 3–6 added (#1858, #1574, #1576
  family, #1856), all gated "after the v0.8.14.0 deploy"; rows 1–2 unchanged and live.

## Release v0.8.14.0 Completion Matrix
Phase 1 — Pre-flight
- [x] Cut commit: `5912d6749a` (HEAD = origin/main, 0 behind at stage time)
- [x] Tests: 5,088-test targeted battery + 534 smoke + 24 mypy ceilings green through the day's lanes
- [x] No open P0s (KNOWN_ISSUES "Blocking: none"; open-bug list checked via `gh api`)
- [x] Parity gate: PARITY OK on the tag
Phase 2 — Version bump
- [x] VERSION + pyproject.toml → 0.8.14.0 (`check-version-consistency.py` OK)
- [x] Release notes written fresh (not copied)
Phase 3 — Documentation (version strings AND prose)
- [x] docs/releases/README.md (current line + history row)
- [x] ALPHA_QUICKSTART — What's New / Testing Focus / What's Working rewritten for 0.8.14
- [x] ALPHA_TESTING_GUIDE — What's New rewritten; six new test walks; nav links; footer
- [x] ALPHA_KNOWN_ISSUES — rewritten against live GitHub state (158 lines): #1418/#1105/#1216/
      #1256/#1793/#1110/#1258/#1429/#1430 moved out (closed); #1855/#1859/#1860/#1852/#1869/#1838 in
- [x] ALPHA_AGREEMENT_v2 (3 places) · BRIEFING-CURRENT-STATE (Version line, Last Updated, THREE
      history rows — 0.8.12/0.8.13 had never been added) · BRIEFING-piper-alpha version line ·
      email template (5 refs + dates) · versioning.md + VERSION_NUMBERING (current + row +
      CURRENT marker moved) · docs/README
Phase 4 — Git ops: explicit-path commit; tag at HEAD; main + tag pushed
Phase 5 — Production branch: N/A by decision (retiring); not advanced
Phase 6 — GitHub Release: published, --latest
Phase 7 — Audit: stale-grep of tester docs → 0 current-claim hits (history mentions only);
  content audit: each What's New/Testing Focus/Known Issues section reread — describes 0.8.14.
  Deployment surfaced to PM via test card v5 + this turn's reply.

### 14:4x (`date`) — post-cut drain: four lanes dispatched (all **Sonnet**, tier stated)
- decisions.log: #1869 zone source for Slack faces = the #1574 preference store (one clock per
  user; Slack `users.info.tz` demoted to a future seed path) — recorded so the lane isn't
  ruling-blocked; Arch may overrule.
- Dispatched Coding Agents (Sonnet ×4): **#1865** probe patch-site fix + end-to-end run;
  **#1868** GUIDANCE time-of-day onto the user's clock via `now_in_zone`/`user_timezone_name`;
  **#1869** Slack standup labeled faces per the ruling; **#1533 batch 2** — script re-census of
  probe-blind suites (denominator stated) + top-5 fixed in the established shape with
  teeth-proofs. Subagents never commit; I review + commit explicit file sets, batches <20 files.

### 14:4x (`date`) — #1522 legs 3+4 · #1865 landed · Pard's staging finding fixed same hour
- **#1522 ratchet legs 3+4** (own work, no lane): `test_dark_templates_ratchet` (19 dark of 63
  — python `.html` literals as roots, Jinja include/extends edges) + `test_dark_assets_ratchet`
  (10 dark of 63 tracked under web/static + web/assets, basename-referenced anywhere in
  templates/web/services). Allowlists pin the exact sets with owning notes; negative-probed
  (a throwaway dark template and a throwaway css file each failed by name). `run-sweep.sh
  ratchets`: 62 passed, mypy 24/24 at ceiling. All four audit legs are now ratcheted;
  disposal of the 29 pinned items is the Rule-0 item with Arch. Commit `a2…` (in main).
- **#1865 landed** (Sonnet lane, reviewed): probe now patches `pre_classify_with_pattern_list`
  (the only Stage-1 call in `classify()`; `pre_classify` is a delegator over it). First
  genuinely-bypassed run: 13/36/3/0 of 52. Kept the 08-08 file as history (restored via
  checkout after diffing — the regenerated content was preserved to the new dated file first);
  new run saved as `…results-2026-09-23.md` with a provenance note that the 08-08 numbers were
  measured with surface 1 still active; default output path now dated per run. Not closed yet
  — closing with evidence below.
- **Pard finding → FIXED `907d0f87e7`**: staging's first deploy failed at `a1599admin` because
  its strictness was keyed on `FLY_APP_NAME` ("any Fly app" once staging existed). Re-keyed on
  the users table's state (empty → warned no-op; populated-without-PM → raise); `import os`
  gone. Verified by executing the real `upgrade()` through an alembic Operations context
  against dev Postgres with a TEMP `users` table shadowing the real one (3 states, rolled
  back) — now `tests/database/test_migration_a1599admin_guard_keys_on_db_state.py` (3 passed).
  Stated cost: a populated non-prod DB re-walked across this revision now raises. Latent hole
  noted on #1599 (from-scratch rebuild w/o restore → PM never gets is_admin; cure = runtime
  bootstrap, the issue's recorded future plan). Reply to Pard delivered to
  `mediajunkie/docs/mail/` (d044f17 — `mailboxes/pard/` is gravestoned + hard-refused by
  mail-send; learned by the refusal, copies to arch/exec/PM sent here `9389753757`).
- Lanes still running: #1868 (Sonnet), #1869 (Sonnet), #1533 batch 2 (Sonnet).

### 14:5x (`date`) — #1869 CLOSED
- Sonnet lane reviewed; I moved the lane's local ISO-parse helper onto `datetime_utils` as
  `format_iso_as_user_datetime` (one parser shared with the time-only twin; 3 pins added).
  Both Slack/GitHub render sites labeled on the user's clock, honest omission on unparseable.
  71 skill tests + 18 datetime_utils green; mypy 0 attributed to changed files. Lane flagged
  `unscoped_reads` reading 45 vs ceiling 46 on the dirty tree — attributed to the in-flight
  #1868 edit (the third `load_standup_config` read the ceilings comment predicted); ceiling
  lowers in the #1868 commit.
- Running: #1868 (Sonnet), #1533 batch 2 (Sonnet).

### 14:5x (`date`) — #1533 batch 2 LANDED + ratcheted; carry-forward rewritten
- Sonnet lane reviewed (462 test lines, 5 files, +1 authenticated class each, teeth-proofs
  logged per file). I promoted its scratch census into `scripts/census_principal_blind_tests.py`
  (repo-relative, `--count`, `--json`; my run 1036/92/30) and added
  `test_principal_blind_suites_ratchet` at ceiling 30 — #1533's remaining work is now a
  shrink-only number, not a list in a session record. 66 + 2(det) passed at review. Issue
  commented with the denominator and the empty-string vacuous-probe finding.
- `dev/active/lead-carry-forward.md` rewritten now rather than at STOP (state moved a lot:
  cut, staging, Pard's inbox location, Cluster 1 done, all four #1522 legs live).
- Running: #1868 (Sonnet) — the last lane.

### 14:5x (`date`) — #1868 CLOSED; all four post-cut lanes landed
- Sonnet lane reviewed: trimmed reviewer-directed comments to code-state ones, moved the face
  onto `format_user_time`. 4338 intent_service unit + 536 smoke + ratchets 10/10 at review;
  mypy 118→117 file-attributed vs HEAD's copy of the file.
- **Own miss, fixed within minutes**: my #1533 commit re-dumped `ratchet_ceilings.json` while
  the #1868 lane's `unscoped_reads` 46→45 edit was already in it, so the lowered ceiling
  reached main one commit before its fix (`test_unscoped_reads_ratchet` would have read 46>45
  on main for that window). Landing #1868 closed it. Lesson: when a shared JSON is rewritten
  by script, diff it against HEAD before staging — the same shape as "verify show --stat
  post-commit" for a file two lanes touch.
- Lanes: none running. Post-cut tally: #1865 #1869 #1868 closed; #1533 batch 2 landed +
  ratcheted (30); #1522 all four legs live; a1599admin staging blocker fixed.

### 14:5x (`date`) — #1533 batches 3+4 dispatched (**Sonnet ×2**, tier stated)
- Batch 3: the five 13-call suites (accuracy/bypass/performance contracts, cli/direct
  interface), established shape + teeth-proofs; must not touch the census or ceilings.
- Batch 4: ranks 6–15 TRIAGED into false-dark (fix the census, pin it) / blind-by-design (in-file
  marker the census reports separately — denominator stays honest) / true-blind (fix). Owns the
  census script; must not touch the top-5 files. I lower the ceiling at commit.

### 15:0x (`date`) — Code Quality red since 09-21 13:57 — format drift, fixed for my share
- CI watch (Monitor) flagged `Code Quality: failure`. Workflow history: last green `d23f15702`
  2026-09-21 13:57; every run since red (100+ runs, ~2 days, nobody looked — a red that no
  one owns is the same as no gate). Cause: `ruff format --check` drift in two CIO scripts
  (`belt-mechanical-reasoning-proxy.py`, `trim-registry-history.py`, 09-21) and, added today,
  five of my/lanes' files (I never ran `ruff format` on the lanes' output — only `ruff check`).
  Formatted 7 files with ruff 0.6.9 (= CI's pin), pushed. The census script formats when batch
  4's lane commits (in flight — not touching its file). Standing fix for me: `ruff format
  --check <files>` in every review, alongside `ruff check`.
- Briefing Current Position/Focus refreshed post-cut (15:00 attest); 09-22 banner archived.

### 15:1x (`date`) — #1533 batches 3+4 LANDED; blind 30 → 14 (ceiling lowered from a measured count)
- Two Sonnet lanes reviewed and committed as two commits (6 + 11 files). Batch 3: five 13-call
  suites, +1 authenticated sibling each, teeth-proofed. Batch 4: three-way triage of ranks
  6–15 — 5 true-blind fixed, 2 by-design (in-file marker → new BY-DESIGN bucket, denominator
  intact), 3 census-fidelity fixes (archive/ exclusion, positional user_id, `current_user=`)
  with 25 census tests. Ran `ruff format` on all 14 files this time (2 reformatted) — the
  Code Quality single remaining hit (census script) is now clear on main.
- Measured at review: census 1015/91/14/2; 133 passed + 1 skipped across touched files + the
  ratchet suite; deterministic tier of the contracts files 16 passed. Issue commented.
- Worktree clean of tracked changes. Nothing running. Queue: #1533 remaining 14 (1–2-call files,
  triage shape) · epic 3 after #1855 ruling · disposals after Arch's rulings.

### 15:1x (`date`) — Exec's fleet-model finding answered; #1533 batch 5 dispatched (**Sonnet**)
- Exec found 3 Sonnet seats silently on Fable-family at the rate-limit moment and marked my
  5→5.1 bump "not verified". Replied (cc PM — relays PM's in-conversation directive): mine was
  PM's "Move you to Fable 5.1" at 12:4x, recorded in this log's header. Memo → read/.
- Batch 5 (Sonnet): the last 14 blind files (all single-call), three-way triage with evidence;
  load/regression collection status to be checked before writing anything.

### 15:3x (`date`) — #1533 CLOSED: blind 14 → 0, ceiling 0
- Batch 5 (Sonnet) reviewed + committed (16 files incl. ceiling): the 14 one-call files
  triaged (3 true-blind fixed incl. two pass-through pins, 9 by-design, 9 census-fidelity),
  and the census's own fidelity fixes (two new keyed surfaces, tokenized scan, JWT-bearer route
  recognition) surfaced 6 more files — triaged in the same batch rather than left as a
  denominator jump. Lane caught a bug in its own new code (`tokenize.TokenizeError` → the
  real `TokenError`) via its own test before hand-back.
- Final census 1015/101/0/11; 219 passed at review; smoke 537/1 skipped. All three remaining
  items on the issue body are done (guards, classify_conscious, authed variants) → closed with
  evidence. Day's #1533 arc: 1 → 30 → 14 → 0 across five batches, 19 files fixed, 11 marked,
  9 census-only. No product defect in the class — it was test-blindness throughout.
- Worktree clean. Queue: epic 3 after #1855 ruling · disposals after Arch's rulings · step 11.

### 15:3x (`date`) — #1718 dispatched (**Sonnet**)
- Queue re-scan: everything left was ruling- or PM-gated, so pulled the next unblocked
  Beta-Blocker in my lane — #1718 (save-time key validation discards the provider's reason;
  route the existing `ValidationResult` through `user_friendly_errors.py`'s existing buckets,
  same copy as the runtime path). Route + render pins required.

### 15:4x (`date`) — #1502 CLOSED (own work, while the #1718 lane runs)
- `request.state.is_admin` was read by files.py (download/preview) and the template context and
  set by nothing; the templates already rendered admin affordances on it. Wired in the auth
  middleware from `users.is_admin` via the existing `_user_is_admin` (fail-closed, silent-ok
  annotated); ui.py reads the wired flag, claims no longer consulted. 5-test pin against the
  real middleware; 541 neighboring + smoke 537; silent_death 192 unchanged; mypy 3→3.
- Behavior change named on the issue and mailed to HOST (cc PM: it's PM's account that gains
  cross-owner file reads — #357's designed policy). Offered an audit line at the bypass sites
  if HOST wants one.

### 16:0x (`date`) — #1718 CLOSED (Sonnet lane, reviewed); #1870 filed
- Save-time key validation now reports the provider's reason through the runtime translator's
  own sentences (invalid vs quota). Lane found two deeper defects under the issue's text
  (OpenAI validator labeled every non-200 as auth; neither validator kept the provider body)
  and a worse live symptom (settings page said "Key saved" for every outcome). Reviewed:
  trimmed reviewer-directed comments — my collapse script de-indented one line (parse error),
  caught by ruff and repaired before commit. 638 passed across the touched suites, smoke 537,
  mypy −3, silent_death 192, format-checked all 13 files. KNOWN_ISSUES BYOC caveat removed.
- Filed #1870 (gemini/perplexity siblings, validate/rotate bool paths, classifier drift).
- Worktree clean. Queue: epic 3 after #1855 · disposals after Arch · step 11. Nothing else
  unblocked in my lane at this read — will re-scan open issues.

### 16:0x (`date`) — #1835 CLOSED (own); three lanes dispatched (**Sonnet ×3**)
- Deleted the dead `orchestration` compose service; added a smoke-tier pin that every
  `build:` in every `docker-compose*.yml` points at an existing Dockerfile (negative-probed
  against HEAD's file). The pin also caught `docker-compose.staging.yml`'s two dead contexts
  (already documented dead in the pipeline plan) — allowlisted with a stays-tight check;
  disposal of that file + temporal/traefik with Arch, noted on the issue.
- Dispatched: **#1758** todo-priority word boundaries (extractor tightening, ratchet-checked);
  **#1857** resolver-side project-name normalization (no new extraction pattern; no silent
  pick on ambiguity); **#1729** doc-summary list flattening — find the layer, fix the helper
  not the case. Distinct file sets stated in each brief.

### 16:0x (`date`) — Arch rulings landed: #1863 GO, #1499 Class 2 GO, #1855 layer 1 GO (+ delete 'actionable')
- Both memos read → read/. **#1499 Class 2 dispatched now (Sonnet)**: extract `deploy_identity`
  first, fresh sweep, delete six routers + six shadow files, ceilings lowered to measured,
  disposal record with the feedback-prefix landmine. Distinct files from the three running
  lanes.
- **#1855 layer 1 and #1863 QUEUED behind the running lanes**, not dispatched yet: both touch
  `conversation_context.py` (LastOffer vs lens fields) and #1855 touches the floor, which #1729's
  render trace may also reach — sequencing over collision. Dispatch as soon as #1729 lands
  (#1855 → Opus: floor seam, design-sensitive; #1863 → Sonnet after #1855).

### 16:1x (`date`) — #1758 + #1729 CLOSED; #1855 layer 1 dispatched (**Opus**)
- **#1758**: lane's word-boundary fix used three `re.search` literals and RAISED the extraction
  ratchet ceiling 11→14 — the one direction the gate forbids. Reverted the ceiling (diffed
  first), rewrote the match as a punctuation-stripped word set (zero regex literals), tracked
  `_extract_priority` in the span at ceiling 11. 73 + ratchet 3 green. Known gap ('buy a high
  chair') pinned as a gap, not hidden.
- **#1729**: real cause was server-side — the bullet branch's leading-glyph guard never matched
  `to_markdown()` output, so every real summary got period-split; fixed with the caller-less
  `has_markdown_formatting()` (completed, not duplicated) and a CommonMark marker. Trimmed a
  30-line reviewer-directed comment to four lines. 99 + 60 + smoke 539.
- **#1855 layer 1 → Opus** (floor seam; design-sensitive; design + Arch ruling + ratchet
  precedent in the brief). Still running: #1857 (Sonnet), #1499 Class 2 (Sonnet). #1863 next.

### 16:1x (`date`) — #1857 CLOSED (Sonnet lane, reviewed)
- Resolver-side normalization at the one function all 7 lookup sites converge on; exact-normalized
  first, single-candidate fallback, None on ambiguity. Lane verified routing was already correct
  (no corpus row, no extraction change) and ran a real handler A/B against a live row. 29 new +
  213 existing + 53 enforcement + smoke 539; mypy diff 0. Lane used the shared stash stack with
  the tagged-apply-drop protocol; stash list empty after, other lanes' staged work intact.
- Running: #1499 Class 2 (Sonnet), #1855 layer 1 (Opus). #1863 after #1855.

### 16:2x (`date`) — #1499 Class 2 landed — with an OWN INCIDENT, named plainly
- **What I did wrong**: my 16:09 log commit (`1eb7c946ff`) swept the #1499 lane's already-STAGED
  `git rm`s (10 files, 5,018 deletions) into a "docs(lead): … log" commit, because `git add
  <log>` then `git commit` commits the whole index — and a concurrent lane had staged. Worse
  than misattribution: the deletions reached main BEFORE the extraction that made them safe, so
  `web/api/routes/admin.py` on main imported `deploy_identity` from a deleted module from
  16:09 until the repair commit at ~16:15 (any CI run or deploy from that window would have
  failed at import). Repaired by landing the lane's remaining 11 files (extraction + repoint +
  tests + disposal record). No deploy happened in the window (alpha still serves 609a07b9).
- **The rule this earns, for me**: in a shared worktree with lanes that may stage, NEVER bare
  `git commit` after `git add <mine>` — always `git diff --cached --name-only` first and, if
  anything not mine is staged, commit with explicit pathspecs (`git commit -- <paths>`) or
  unstage theirs. The same "check the index before you act" shape as the hook lesson.
- Lane substance: fresh sweep found the six-router premise shifted — 4 deleted, 1 already gone
  (08-30), **SlackWebhookRouter NOT deleted** (live via `socket_mode_runner.py`; only its FastAPI
  mount is dead — narrower follow-up). Six shadow files gone. `unmounted_routers` 4→0,
  `shadow_files` 6→0. The lane's mypy shrinks were NOT in the committed ceilings file — measured
  myself on a detached worktree at clean origin/main and locked in: arg_type 371, assignment 229,
  attr_defined 141, index 10, union_attr 151.
- Running: #1855 layer 1 (Opus). #1863 after it.

### 16:3x (`date`) — #1855 layer 1 LANDED (Opus lane); #1863 dispatched (**Sonnet**)
- Reviewed the seam diff (respond(), after push appendage), grep for "actionable" empty, 102
  passed across the new suite + enforcement + the 1570 floor-entry test that caught the lane's
  own getattr bug, smoke 539, silent_death 192. Committed with explicit pathspecs (7 files).
  Issue commented (stays open for layer 2); Arch/CXO/PPM memo sent incl. the `Do you want me
  to…?` widening question and #1499's SlackWebhookRouter correction.
- #1863 → Sonnet: fresh sweep, rip, hydrator legacy-key pin (Arch's condition), design record;
  told NOT to touch the index or ceilings.
- 16:3x: Code Quality still red after the format fix — second cause was `ruff check` I001
  (import order) in 4 files; my per-file `ruff check <files>` had passed because none of those
  were mine. Fixed; `ruff check .` + `ruff format --check .` both clean on the full tree now.
  Lesson folded into the earlier one: run BOTH checks tree-wide before pushing, not per-file.

### 16:5x (`date`) — #1863 CLOSED (Sonnet lane, reviewed); all lanes drained
- Rip landed (17 files, −409 lines) with the hydrator legacy-key pin Arch required; two census
  deltas reported by the lane before acting (the #821 slot-prompt feed; the orphaned enum).
  Measured on the tree with only this lane dirty: ratchets 63, mypy gate 24/24 at ceiling
  (1253), smoke 539, collection 14,270. Committed with explicit pathspecs via a zsh array
  (the unquoted-`$files` pathspec bit me again first — nothing staged, retried correctly).
- **No lanes running. Post-cut afternoon tally**: closed #1865 #1869 #1868 #1533 #1502 #1718
  #1835 #1758 #1729 #1857 #1863; #1855 layer 1 landed (open for layer 2); #1499 Class 2 landed
  (#1499 stays open: SlackWebhookRouter member-strip + ui.py class + /api/admin deprecation);
  #1522 all four legs; a1599admin staging fix; v0.8.14.0 cut. Filed #1870.
- Queue now genuinely gated: v0.8.14.0 deploy (PM) · staging retry (PM/Pard) · §4e (Pard) ·
  #1855 layer 2 (after layer 1 proves out live) · #1499 tail (narrow tasks; could dispatch) ·
  step 11 (~09-29). Next: #1499 SlackWebhookRouter member-only strip is unblocked and small.

### 16:5x (`date`) — HOST + CXO rulings executed (own work)
- HOST: #1502 gate is 4 sites not 2, one a WRITE — audit line at all four (emits only on an
  actual bypass; the write folds owner_id/is_admin/cross_owner into its existing log). 3 pins;
  530 routes tests. Issue comment corrects my "read-only" characterisation. Replied.
- CXO: #1855 contract sentence ratified (executable on main); datetime copy 1–2 ratified,
  #3 amended → all-day events render "All day" (constant in datetime_utils; adapter + handler
  face; the old empty-face pin flipped). 235 time/calendar tests; smoke 539. Replied, with the
  `Do you want me to…?` widening put to CXO as a copy ruling.
- Four inbox memos → read/. Inbox empty. No lanes running.
- 16:5x: Code Quality's THIRD cause after format + I001: the mailbox filename-length gate
  (#1616) — 11 delivered paths over 180 chars (8 other seats', 3 involving me; inbox→read
  moves re-mint paths). Regenerated the baseline per the gate's own rule (delivered files are
  not renamed) — the same move CIO/PPM made on 09-1x. Rule for me going forward: filename
  ≤ 180 INCLUDING the `mailboxes/xian (ceo)/inbox/` prefix (26 chars) when PM is a recipient.

### 16:5x (`date`) — #1499 SlackWebhookRouter member strip dispatched (**Sonnet**)
- The narrow follow-up the Class-2 lane stopped for: strip only the dead FastAPI members, keep
  the live Socket-Mode class, member table with caller evidence, identity-binding guard test
  must pass unchanged. Told not to touch the index.
