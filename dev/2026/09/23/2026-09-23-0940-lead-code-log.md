# Lead Developer session log — 2026-09-23 (Claude Code)
Role: Lead Developer (lead-code) · Model: Fable 5 (claude-fable-5) until ~12:45, then **Fable 5.1 (claude-fable-5-1)** — PM moved the session mid-day ("Move you to Fable 5.1"), system-attested 12:4x · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron: 0cce894b (17 6,9,12,15,18,21), armed at START-backfill

*(Header + START entry restored 12:5x: the file was truncated by a hung background command's `cat >` — a shell-precedence bug in my own 09:45 START command — and later appends landed under no header. Content below is complete; only the header/START block was lost and is reconstructed here from the same morning's events.)*

## MIGRATION AFTERMATH DAY — PM ran Test 1 on the Fly-served alpha; findings triaged; STOP backfilled

### START (09:4x, PM-engaged: Test-1 results in) — retro-close of 09-22 done first (Step 0)
- Prior day lacked DAY-CLOSED → retroactive close written into the 09-22 log (annotated marker).
- **PM RAN TEST 1 on alpha: ROW 1 PASSES #1617's criterion** — turn 2 reached the issue rail
  first try (crisp confirm), no standup swallow. #1739's last dependency discharged. Full
  transcript analysis: 6 findings filed (#1855 unarmed floor offers · #1856 add-project flow
  arg-drop + verbatim-repeat loop · #1857 name-extraction greed · #1858 404-close indeterminate
  copy · #1859 white flash on chat switch · #1860 standup-initiation coverage gap) + evidence
  comments on #1843 (exact-match accepts, by design) and #1828 (floor said Monday on a
  Wednesday). White-flash cutover-header hypothesis KILLED with evidence (bare Caddy
  reverse_proxy; asset headers unchanged) → routed to Web's render sweep.

### STOP-backfill executed in full + the mechanism proposal (PM directive, 10:0x–10:2x)
- **Backfilled from the missed 09-22 STOP**: retro-close (day-arc + memory-eval + sign-off +
  annotated DAY-CLOSED marker — written earlier this morning, committed now after the slow
  post-commit hook stalled the first attempt) · carry-forward FULL REWRITE with Exec's
  spring-clean applied (resolved threads deleted; current state + live threads only) · registry
  trim: checked-not-needed (lead row = 215 chars) · cron re-armed **0cce894b** (delete-then-
  create n/a — none existed; that absence WAS the root cause) · registry row updated with the
  cause on the record.
- **Root cause, stated plainly**: Rule-1 deleted the cron for the evening drain; "re-arm at
  idle" is vigilance-dependent and the drain ended by the session going idle mid-conversation —
  no cron, no 21:17 fire, no STOP. Step 0's detect-and-backfill worked as designed at START.
- **Mechanism proposal SENT to CIO (skill owner) cc Exec/PM** (ac685b4ff): Rule-1's delete
  becomes **delete-AND-swap** — a one-shot STOP backstop at the day's last slot created in the
  same action, making the day-close structurally unloseable by drains; idempotent with later
  re-arms; plus the deeper optional question (is Rule-1's delete protecting against a hazard
  the REPL-busy runtime already prevents?). Adopted as personal practice immediately — baked
  into the new cron prompt itself.
- PM test-session processing (earlier this turn-block): row 1 PASSED/struck, #1739 discharged,
  #1855–#1860 filed, #1843/#1828/#1617/#1739 commented, card + artifact republished (v2).
- **PM ratified the book-end framing** ("Rule 1 only works if it is book-ended by restoring a
  cron when finally ready to go idle") — relayed verbatim to CIO cc Exec/PM (fad90a965) as the
  invariant the skill amendment should lead with.
- **Test-2 key-isolation nuance added to card + artifact v3**: PM holds BOTH provider keys; a
  valid OpenAI key could serve the request via the #1823 any-provider gate and mask the #1824
  error path — the invalid Anthropic key must be the only stored key during the test.
- **Card clarity fix after PM's (justified) confusion**: rows 1 (#1824) and 2 (#1822) were
  blurring in chat — the card + artifact (v4) now spell the distinction (error-message/web/
  broken-key vs success-path/Slack/valid-key), and **#1822 is UNPARKED** conditional on PM's
  Slack link: PM holding a real OpenAI key dissolves the "needs a separate account" park
  reason. One-sitting key-juggling sequence documented on both rows.

### #1858 build (12:3x–, PM asked for unblocked work while managing other things)
- Mechanism traced: adapter `_verified_write` → write response unparseable (in-band 404) →
  read-back unparseable → `verified=False` → router raises the unverified RuntimeError → floor's
  "may or may not" copy. Fix: `GitHubWriteResult.not_found` (definitive ONLY on positive
  not-found evidence from BOTH legs, update-shaped writes only) → router raises typed
  `GitHubIssueNotFound` → `_unverified_write_result` returns the plain "There's no issue #N"
  result before the unverified branch (zero call-site changes). Tests + battery next.
- 12:17 wake arrived mid-drain (PM: usage limit hit and reset). 12:4x: PM moved the session to
  Fable 5.1. Inbox 11 (two Arch rulings, Web finding, CIO shipped v139) — read after #1858 lands.
