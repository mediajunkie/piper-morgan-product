# 2026-09-23 1333 — Coding Agent session log (issue #1861)

**Role**: Coding Agent (prog)
**Model**: Sonnet 5 (claude-sonnet-5)
**Issue**: #1861 — wire `developer_keys_bound()` into three remaining dead probe scripts
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (dispatcher owns commit/push — I do NOT commit)

⚠️ **Naming collision note**: I originally wrote this log to
`dev/2026/09/23/2026-09-23-1330-prog-code-log.md`. Mid-session that file was silently overwritten
on disk by a concurrent Coding Agent subagent working issue #1856 (add-project flow), which picked
the identical timestamp-based filename in the same worktree. Per the harness's own guidance ("if the
change looks wrong, say so rather than undoing it yourself"), I did not revert it — that would only
clobber the #1856 agent's log in turn. I moved my own record here instead. Flagging for the
dispatcher: two concurrent subagent dispatches into the same worktree landed on the same
`HHMM`-based session-log filename; a collision-resistant naming scheme (PID/issue-number suffix)
would prevent this recurring.

## Task

Three probe scripts still die with "request context never bound a key" after #1812 removed the
server-key fallback: `scripts/run_probe_set_v0_1.py`, `scripts/routing_probe_1283.py`,
`scripts/surface1_counterfactual_probe_1283.py`. Wire `developer_keys_bound()` into each entry
point exactly as `scripts/inversion_phase1_shadow_score.py` does it.

## Investigation

Read `scripts/dev_key_binding.py` (the helper) and `scripts/inversion_phase1_shadow_score.py`
(the reference pattern).

Checked each target script's `if __name__ == "__main__":` block and dry-run support:
- `run_probe_set_v0_1.py`: no `--dry-run` flag; always makes ~20 real LLM calls
  (`main()` → `asyncio.run(main_async(...))`). → `require=True`.
- `routing_probe_1283.py`: no `--dry-run` flag; 38-row corpus, always real LLM calls. → `require=True`.
- `surface1_counterfactual_probe_1283.py`: HAS `--dry-run` (keyless, no LLM calls). →
  `require=not args.dry_run`, matching the reference script's exact idiom.

## Changes (minimal — entry-point blocks only)

All three: added `sys.path.insert(0, str(PROJECT_ROOT / "scripts"))` + `from dev_key_binding import
developer_keys_bound` + wrapped the existing `sys.exit(...)` call in
`with developer_keys_bound(require=...):`. No other lines touched.

## Verification

- `py_compile` all three: OK.
- `--help` all three: OK (unchanged usage text).
- `ruffenv2/bin/ruff format --check` + `ruff check`: clean on all three.
- Env-stripped real runs (`env -u ANTHROPIC_API_KEY -u ANTHROPIC_BASE_URL -u ANTHROPIC_AUTH_TOKEN
  -u ANTHROPIC_CUSTOM_HEADERS POSTGRES_PORT=5433 venv/bin/python scripts/<name>`):
  - `routing_probe_1283.py`: ran 20s (30/38 rows), multiple successful `llm_usage` (openai/gpt-4o)
    log lines, zero "never bound a key" occurrences.
  - `run_probe_set_v0_1.py`: ran 25s (14+ of 20 probes), multiple successful `llm_usage` lines,
    zero "never bound a key" occurrences.
  - `surface1_counterfactual_probe_1283.py`: key retrieval from keychain confirmed at process
    start (anthropic + openai both retrieved), zero "never bound a key" occurrences — but the run
    then hit a **pre-existing, unrelated** `AssertionError` on the very first corpus row
    ("bypass not consulted") before any LLM call fired. Root-caused (read-only) to
    `services/intent_service/classifier.py:419` calling
    `PreClassifier.pre_classify_with_pattern_list(...)`, while the probe script only monkeypatches
    `PreClassifier.pre_classify` (a different method) — the patch never takes effect against the
    real call site. This is independent of #1861/#1812 (untouched code path in `run()`'s body) and
    out of scope per the dispatcher's "do not restructure anything else" instruction. Not fixed;
    reported to dispatcher for a separate issue.

None of the three runs are full/complete (deliberately time-boxed per the acceptance criteria's
cost-minimization intent, since none of the scripts support row-limiting and none approach
"hundreds of calls"). No stranded background processes remain (all killed after their sleep window).
