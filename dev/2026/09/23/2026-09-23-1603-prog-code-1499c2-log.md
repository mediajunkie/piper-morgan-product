# Session Log — Coding Agent — #1499 Class 2 (six unmounted routers + shadow files)

**Date**: 2026-09-23
**Role**: Coding Agent (prog), dispatched by Lead Developer
**Model**: Sonnet (dispatched by Lead, tier stated)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
**Issue**: #1499 Class 2 — delete six unmounted routers + shadow files (backup/dead files)
**Authority**: Arch's Rule-0 ruling 2026-09-23,
  `mailboxes/lead/inbox/rule-arch-to-lead-cc-pm-1863-1499-both-GO-verified-2026-09-23.md` — GO on
  all six + shadow-file sweep, with two conditions: (1) extract `deploy_identity()` + three
  private helpers out of `staging_health.py` BEFORE deleting it (admin.py imports it at two
  sites: `/health` and `/api/v1/version`); (2) fresh sweep at cut time, re-verify each router is
  still unmounted now.

**Do NOT commit or push** — Lead reviews and commits my explicit file set.

**Predecessor context**: a sibling prog-1499 session (log
`dev/2026/09/23/2026-09-23-1358-prog-code-1499-log.md`, now merged at `ef62551457`) did the
non-deletion half of #1499 — collapsed the Slack OAuth routes, added `/api/v1/version` on
`admin.py` calling `deploy_identity()` (this is the second `deploy_identity()` call site Arch's
ruling names), removed dead UI links, pruned stale auth-exempt entries, and annotated
`web-routes-conventions.md` with 🔴 UNMOUNTED markers on the three deliberate exceptions,
deferring disposal to this Class 2 pass.

---

## Plan

1. Read `web-routes-conventions.md` current state + `test_completion_ratchets.py` allowlists.
2. Fresh sweep per router: confirm still unmounted (`_defined_module_level_routers` /
   `_mounted_routers` test helpers + grep).
3. Extract `deploy_identity()` + helpers from `staging_health.py` into new module,
   repoint `admin.py`'s two imports.
4. Per file: grep every reference, fix/delete each, then `git rm`.
5. Update `UNMOUNTED_ROUTER_ALLOWLIST` + `SHADOW_FILE_ALLOWLIST`, lower ceilings to measured.
6. Disposal record doc.
7. Full gate run.

## Work log

### Fresh sweep — two of six candidates were NOT what the six-week-old audit said

1. **`services/api/slack_monitoring.py`** — already deleted 2026-08-30 (commit `6729d39521`,
   an unrelated earlier disposal batch). Nothing to do.
2. **`services/integrations/slack/webhook_router.py` (`SlackWebhookRouter`) — HARD STOP,
   not deleted.** `socket_mode_runner.py:124-126` instantiates it LIVE to process `/piper`,
   `/standup`, `/link` Slack slash commands over Socket Mode; `test_slack_identity_binding_guard.py`
   pins it as the sanctioned caller-home for the #1466 security invariant
   (`redeem_link_code`). Only its FastAPI `APIRouter` mount is dead (never mounted anywhere,
   confirmed via the ratchet's own documented KNOWN BLIND SPOT — class-scoped routers were
   never in that AST scan's denominator). Full-file deletion would have broken live Slack
   commands and deleted a security-guard's required caller. Left the file completely
   untouched; flagged prominently in the disposal record with a recommended narrower
   follow-up (strip only `self.router`/`_register_routes`/`register_webhook_routes`/
   `get_router`/`get_webhook_urls`, keep the class). Did not attempt that surgery myself —
   out of scope for a batch deletion pass, and touches a security-invariant-guarded class.

The other four were confirmed genuinely dark (re-verified via `test_unmounted_routers_ratchet`
+ fresh `grep -rn` sweeps) and deleted:

### `staging_health.py` — extraction then delete

Extracted `deploy_identity()` + its three private helpers (`_deployed_version`,
`_deployed_git_sha`, `_deployed_environment`) verbatim into new
`services/api/health/deploy_identity.py` (Arch's condition 1 — `admin.py` imports it live at
two sites: `/health` and `/api/v1/version`). Repointed both `admin.py` import sites. Verified
against the new module (`test_health_deploy_identity_1839.py` 6 tests,
`test_version_route_1499.py` 7 tests, all pass) BEFORE deleting the old file. Then `git rm
services/api/health/staging_health.py` (11 routes: `/`, `/liveness`, `/readiness`,
`/comprehensive`, `/mcp`, `/slack`, `/metrics`, `/ethics-metrics`, `/ethics-audit`,
`/ethics-summary`, `/ethics-learning`).

Fixed dangling references: `test_health_deploy_identity_1839.py` + `test_version_route_1499.py`
docstrings (no longer credit the deleted module), `test_mcp_error_scenarios.py` (a comment
named `staging_health._check_mcp_health` as tracking an open follow-up — corrected),
`scripts/migrate_personal_data.py` (a historical-provenance comment citing the also-deleted
`PIPER.md.backup-20251101` shadow file — corrected, no functional impact since the script
never reads that path at runtime), `docs/internal/architecture/current/web-routes-conventions.md`
`/health` section rewritten from "pending a ruling" to "deleted, extraction noted."

### `feedback_api.py`, `loading_demo.py`, `conversation_context_demo.py` — delete

All three confirmed zero non-allowlist importers, no dedicated test files. `feedback_api.py`
(6 routes) flagged in the disposal record as the collision-armed twin of the MOUNTED
`web/api/routes/feedback.py` (same `/api/v1/feedback` prefix, `web/app.py:286`) — Arch's
explicit ask. `loading_demo.py` (8 routes) + `conversation_context_demo.py` (6 routes)
deleted, `web-routes-conventions.md`'s two demo sections rewritten from "unmounted, pending"
to "deleted," rationale kept struck-through per the doc's own established pattern
(`admin_compose.py` precedent).

### Shadow files — all six deleted

`config/PIPER.md.backup-20251101`, `webhook_router.py.security-fix-backup`, four
`backup_before_phase2_*.sql` — all confirmed tracked and present, `git rm`'d.

### Allowlists + ceilings

`UNMOUNTED_ROUTER_ALLOWLIST` and `SHADOW_FILE_ALLOWLIST` in `test_completion_ratchets.py`
→ both empty typed sets (kept, not removed, so a NEW dark router/shadow file fails loud).
Also noted in a new comment that `SlackWebhookRouter` is deliberately NOT in the allowlist —
it was never in this scan's denominator. Measured: `unmounted_routers` 4→0, `shadow_files`
6→0. `scripts/run-sweep.sh ratchets` then surfaced 5 mypy ceilings that shrank from deleting
~2,100 lines of routed code: `mypy_arg_type` 372→371, `mypy_assignment` 231→229,
`mypy_attr_defined` 148→141, `mypy_index` 37→10, `mypy_union_attr` 156→151. All six ceiling
changes recorded with a dated, attributed `_comment` line in `scripts/ratchet_ceilings.json`.

### ⚠️ Commit-attribution incident (discovered mid-session, not caused by me)

My ten `git rm`'d paths (staged only — I do not commit) got **accidentally swept into an
unrelated commit**, `1eb7c946ff` ("docs(lead): 1758/1729 closed, 1855 dispatched — log"),
which was evidently meant to touch only one session-log file (13 insertions) but landed
5,018 deletions across all ten of my staged paths too — a broad `git add`/`git commit -a`
in this shared worktree scooping up my already-staged deletions. Net effect is functionally
correct (the paths really are gone, as intended) but the commit message misattributes them
away from #1499 Class 2. Did NOT attempt any history rewrite (destructive, out of scope, and
the commit had already propagated through subsequent merges by the time I noticed). Full
account in the disposal record. This is exactly the "commit only own files" hazard CLAUDE.md
names — flagging for Lead's awareness, not something I fixed.

### Disposal record

`docs/internal/architecture/design-records/disposal-record-1499-class-2-dark-routers-2026-09-23.md`
— full accounting of all six candidates, the extraction, the shadow files, the ceiling deltas,
and the commit-attribution incident.

### Gates (all env-stripped)

| Gate | Result |
|---|---|
| `test_completion_ratchets.py -k "unmounted or shadow"` | 2 passed |
| `test_completion_ratchets.py` + `test_architecture_enforcement.py` (before #1855 lane's concurrent edits landed) | 63 passed |
| Same, re-run after #1855 lane's concurrent uncommitted edits appeared in the shared worktree | 1 unrelated failure: `test_silent_death_ratchet` (193 vs ceiling 192) — traced via `scripts/check_silent_death.py --list` to `services/intent_service/conversational_floor.py`, a file I never touched; the #1855 lane's in-flight work, not mine. Deselecting it: 62 passed, 1 deselected. |
| `test_version_route_1499.py` + `test_health_deploy_identity_1839.py` + `test_admin_cache_routes_gated_1508.py` | 13 passed |
| `scripts/run-sweep.sh smoke` | 539 passed, 1 skipped, 13707 deselected |
| `scripts/run-sweep.sh ratchets` | 63 passed; 5 mypy ceilings lowered (see above) |
| `pytest --collect-only -q` | 14,247 collected, no ImportError |
| `ruff format --check` + `ruff check` (7 touched .py files) | all formatted, all checks passed |
| `scripts/principal_threading_lint.py` | exit 0, no output |

## Memory & briefing surfaces referenced this session

- **Referenced**: `delete-module-safely` skill (loaded mid-task — its "mixed file gets
  surgery, not wholesale deletion" and Rule 0 "flag a sweep surprise in the execution memo"
  directly shaped the webhook_router.py hard-stop and how I reported it); CLAUDE.md STOP
  Conditions #1 ("infrastructure doesn't match gameplan assumptions"); CLAUDE.md "Verify
  First, Create Second"; m-43/m-44 (layer + denominator framing used throughout the disposal
  record and this log); the shared-worktree "commit only own files" discipline (recognized
  the commit-attribution incident for what it was rather than assuming I'd made an error).
- **Loaded but not referenced**: mailbox/duty-cycle protocol sections (I don't send mail or
  cycle); Model A worktree setup detail (already in a live worktree, dispatcher's problem).
- **Wanted but not found**: nothing — the authority memo and prior #1499 non-deletion-lane
  log gave complete context.
