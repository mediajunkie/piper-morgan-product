# Session log — Coding Agent (prog), 2026-09-23

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated — Sonnet)
**Issue**: #1863 — Rule-0 rip: the writer-less lens surface
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)

## Task

Rip the writer-less conversational-lens surface — `ConversationTurn.lens` / `current_lens` /
`lens_stack` and the #953 persisted slice — per Arch's GO (`mailboxes/lead/read/rule-arch-to-lead-
cc-pm-1863-1499-both-GO-verified-2026-09-23.md`) on the Lead's reader census in issue #1863.
Not committing/pushing — Lead reviews and commits.

## Fresh sweep (Step 1) — delta vs the census

Ran `grep -rn "lens" services/ web/ --include='*.py'` fresh (6 hours after the census). Confirmed
no writer of `ConversationTurn.lens` appeared (zero `add_turn(... lens=...)` call sites; only two
`conv_ctx.add_turn` call sites in intent_service.py, neither passing `lens=`).

**Delta found and reported before acting**: the census's item 4 ("workflow_entries.py:54/72/91 —
reads ctx.get('active_lens') ... threads it to WorkflowOfferService → same consumer as 3") undersold
the reach of that thread-through. Tracing `ctx.get("active_lens")` in `start_meeting_workflow`
showed it *also* feeds `slot_filling_adapter.manager.start_filling(active_lens=...)` →
`SlotFillingSession.active_lens` → `slot_extractor.get_next_prompt_group` / `slot_prompts.
format_confirmation_with_prompt` (Issue #821's independent lens-aware-prompt-phrasing machinery) —
a consumer the census didn't name. Verified it fails closed identically (`slot_template.py`'s
`prompt_for_lens`: `if lens and self.lens_prompts` short-circuits on None), same "absent ≡
always-empty" family as #822. Confirmed the ONLY producer of the `"active_lens"` context-dict key
anywhere in the codebase is intent_service.py:572 (from `current_lens`) — no other producer, so the
whole chain (including #821's feed) dies cleanly with `current_lens`. **Decision**: left #821's own
machinery (SlotFillingSession.active_lens field, slot_prompts.py, slot_template.py's lens-prompt
dicts, slot_extractor.py) untouched — it's a general, independently-testable capability outside
this issue's GO, not a writer-less field of its own (yet). Flagged as discovered work in the design
record, not actioned.

Second delta: `ConversationalLens` enum in `services/shared_types.py` was not named in the census's
explicit small-fry list, but is a direct, unavoidable consequence of the cut — it was never wired
as a real type (`ConversationTurn.lens: Optional[str]`, never `Optional[ConversationalLens]`), had
zero production readers, and becomes fully orphaned once its only referents (decorative docstrings,
test literals) go with this cut. Removed it; documented the removal + rationale in the design
record so the idea is recoverable.

### Fresh-sweep table (reader / line / post-cut behavior / delta vs census)

| Reader | Line(s) | Post-cut behavior | Delta vs census |
|---|---|---|---|
| `ConversationContext.current_lens` property | conversation_context.py:359 (pre-cut) | Property deleted | None — matches census #1 |
| `intent_service.py` #820 seam | intent_service.py:515-528, 572, 587 | Read + thread-through deleted; `detect()` called without `active_lens` | None — matches census #2 |
| `soft_invocation.py` #822 affinity boost | soft_invocation.py:382-471 (pre-cut) | Branch, `_LENS_WORKFLOW_AFFINITY`, `_LENS_AFFINITY_BOOST`, `lens_boosted` log field all deleted | None — matches census #3 |
| `workflow_entries.py` ctx.get("active_lens") thread | workflow_entries.py:54/72/91 (pre-cut) | Read + thread-through to `start_filling()` and returned context dict deleted | **Delta**: also feeds #821's `SlotFillingSession.active_lens` → lens-aware prompt phrasing (slot_extractor.py/slot_prompts.py), not just WorkflowOfferService. #821 machinery itself left intact/unfed (out of scope). |
| `lens_stack` persistence | conversation_context.py:277 (write), 303-305 (read), pre-cut | Both lines removed from `to_persistable_state`/`apply_persisted_state`; no migration (JSONB key, not column) | None — matches census #5 |
| Small-fry: `temporal_reference`/`topic`/`entity_references`/`last_temporal_reference` | conversation_context.py (fields + properties) | All deleted; `last_topic` (reads `.topic`, not separately named by census) also deleted as a necessary consequence | Minor — `last_topic` property wasn't named explicitly but had to go with `.topic` |
| `ConversationalLens` enum | shared_types.py:510-526 (pre-cut) | Deleted | **Delta**: not named in census; zero production readers (decorative only), fully orphaned once test literals removed with this cut |

## What was removed

- **`services/intent_service/conversation_context.py`**: `ConversationTurn.lens` /
  `.temporal_reference` / `.entity_references` / `.topic`; `ConversationContext.lens_stack`,
  `.current_lens`, `.last_temporal_reference`, `.last_topic`; `add_turn`'s corresponding kwargs;
  the lens-stack clear in `_prune_old_turns`; `lens_stack`'s two lines in
  `to_persistable_state`/`apply_persisted_state`. Net -109 lines (doc comments added back some).
- **`services/intent/intent_service.py`**: the #820 `current_lens` read block, its thread into
  `detect(active_lens=...)`, `pending_offer["active_lens"]`, `dispatch_context["active_lens"]`.
  Net -23 lines.
- **`services/intent_service/soft_invocation.py`**: `detect()`'s `active_lens` param,
  `_LENS_WORKFLOW_AFFINITY`, `_LENS_AFFINITY_BOOST`, the boost branch, `lens_boosted` log field.
  Net -26 lines.
- **`services/intent_service/workflow_entries.py`**: the `ctx.get("active_lens")` read and its two
  uses in `start_meeting_workflow`. Net -3 lines.
- **`services/shared_types.py`**: `ConversationalLens` enum (17 lines), replaced with a removal-note
  comment.
- **`services/conversation/conversation_manager.py`**, **`workflow_dispatcher.py`**,
  **`slot_filling/slot_template.py`**: stale docstring/comment references to `lens_stack`/
  `ConversationalLens` corrected for accuracy (no behavior change).
- **`services/database/repositories.py`**: NOT touched — the `_LAYER4_KEY` = `"layer4_state"`
  repository layer is opaque to dict contents; no migration needed (verified: `lens_stack` was a
  JSONB key, not a column).

## Hydrator pin (Arch's condition)

New file `tests/unit/services/intent_service/test_layer4_hydration_ignores_legacy_lens_keys_1863.py`
(69 lines, 3 tests): builds `layer4_state` dicts carrying the legacy `lens_stack` key, a
hypothetical `current_lens` key, and both together (nothing-but-legacy-keys case); asserts
`apply_persisted_state` hydrates without raising, the keys leave no attribute on the restored
context, and every OTHER key in the same blob still hydrates correctly (proving the ignore is
surgical, not a silent whole-dict swallow).

## Tests

**Deleted** (existed only to exercise the removed surface):
- `test_soft_invocation.py::TestLensBoostedConfidence` (6 tests, #822 affinity boost)
- `test_formality_soft_invocation.py::TestFormalityWithLensBoosting` (2 tests, #822 + formality interaction)
- `test_soft_invocation_integration.py::TestLensContextInSoftOffer` (2 tests, #820 pending_offer field) — the second test's general "no offer → pending_offer None" behavior is already covered redundantly elsewhere in the same file (6+ other assertions of `pending_offer is None`), confirmed before deleting.
- `test_conversation_context.py::TestLensFieldsExist` (10 tests) and `::TestLensStackPruning` (1 test) — replaced by header-comment pointing to the design record; also removed `test_turn_stores_temporal_reference`, `test_last_temporal_reference`, `test_last_temporal_reference_skips_none` (exercised removed small-fry fields).

**Updated** (referenced removed surface incidentally):
- `test_conversation_context_persist_953.py`: rewrote every test to drop `lens_stack` from state
  dicts/assertions; `test_excludes_turns_and_provenance`'s expected key-set updated;
  `test_partial_legacy_state_leaves_missing_fields_default` now uses `last_floor_category` as its
  example key instead of `lens_stack`.
- `test_workflow_dispatcher.py::test_meeting_workflow_starts_slot_filling`: removed the now-vestigial
  `"active_lens": "people"` context-dict entry (harmless either way — the mock accepted it — removed for honesty).

**NOT touched** (confirmed independent of the removed surface, would have been over-scoped):
- `tests/unit/services/database/test_conversation_context_state_953.py`,
  `test_conversation_context_state_roundtrip_1180.py`, `tests/unit/services/conversation/
  test_context_state_wiring_953.py` — these exercise `ConversationRepository.save/load_context_state`
  with raw dict literals (no `ConversationContext` import); `"lens_stack"` in them is just an
  arbitrary example key string, not a reference to the removed field. Verified via import check.
- `tests/unit/services/intent_service/test_ftux_interview_1688.py:635` — uses `{"lens_stack": []}`
  as an example of "legacy state missing the ftux key"; still passes unmodified since the key is
  now simply ignored (same behavior class the new hydrator-pin test formalizes).
- `tests/unit/services/slot_filling/test_lens_aware_prompts.py`,
  `tests/unit/services/personality/test_grammar_context.py`/`test_grammar_bridge.py`,
  `tests/unit/services/home/test_home_state_service.py`, `tests/unit/services/mux/lenses/*` —
  confirmed unrelated (Issue #821's own lens param, GrammarLens, home-state "lens" items, MUX
  perceptual lenses respectively — different features that happen to share the word "lens").

### Test run tails

`grep -rln "lens" tests/` (initial scope list) → filtered to files that actually reference the
removed surface (11 relevant files identified out of ~30 matches; rest confirmed unrelated above).

```
$ pytest tests/unit/services/intent_service/test_conversation_context.py \
    tests/unit/services/intent_service/test_conversation_context_persist_953.py \
    tests/unit/services/intent_service/test_layer4_hydration_ignores_legacy_lens_keys_1863.py \
    tests/unit/services/intent_service/test_soft_invocation.py \
    tests/unit/services/intent_service/test_soft_invocation_integration.py \
    tests/unit/services/intent_service/test_formality_soft_invocation.py \
    tests/unit/services/intent_service/test_workflow_dispatcher.py \
    tests/unit/services/intent_service/test_ftux_interview_1688.py -q
264 passed, 9 warnings in 2.10s
```

```
$ pytest tests/unit/services/database/test_conversation_context_state_953.py \
    tests/unit/services/database/test_conversation_context_state_roundtrip_1180.py \
    tests/unit/services/conversation/test_context_state_wiring_953.py \
    tests/unit/services/intent_service/ -q
4437 passed, 16 warnings in 111.43s
```

```
$ pytest tests/unit/services/slot_filling/ tests/unit/services/conversation/ -q
256 passed in 1.58s
```

```
$ pytest tests/test_architecture_enforcement.py tests/test_completion_ratchets.py -q
63 passed in 16.66s
```

```
$ scripts/run-sweep.sh smoke
539 passed, 1 skipped, 13730 deselected, 90 warnings in 26.59s
```

```
$ scripts/run-sweep.sh ratchets
63 passed in 16.62s
mypy gate: all 24 ratcheted codes at ceiling (total=1253; ...) -- no code read BELOW its ceiling; no ceiling edits made.
```

```
$ pytest --collect-only -q 2>&1 | tail -2
14270 tests collected in 8.66s
```

```
$ ruff format --diff <15 touched files>
15 files already formatted
$ ruff check <15 touched files>
All checks passed!
```

```
$ venv-mypy-gate/bin/mypy <8 touched service files> --ignore-missing-imports | tail
Found 853 errors in 97 files (checked 8 source files)
```
Only 3 of those 853 land in the 8 touched service files, and all 3 are on lines I did NOT edit
(`conversation_context.py:330` `is_active` union-attr — pre-existing, untouched property;
`conversation_manager.py:468` int/float assignment — pre-existing, untouched line;
`workflow_dispatcher.py:399` missing var-annotation — pre-existing, untouched line). Confirmed via
`git diff --stat` that none of these 3 lines are in my diff. The ratchet gate matching its exact
ceiling (no "BELOW" report) independently confirms no net-new mypy errors were introduced.

```
$ venv/bin/python scripts/principal_threading_lint.py; echo rc=$?
rc=0
```

```
$ venv/bin/python scripts/check_silent_death.py --count
192
```

## Design record

`docs/internal/architecture/design-records/design-record-lens-surface-rip-1863-2026-09-23.md`
(163 lines) — what #763/#820/#822/#953/#821 each wanted; why no writer ever existed post-#1768;
exact removal list; legacy-row handling (no migration, isinstance-guard already ignores unknown
keys); what a future re-implementation needs (writer, data-model decision, re-ruled product case
for the affinity boost, re-wiring to #821's still-intact machinery, recoverable enum shape);
discovered-work note on #821's now-fully-unfed lens-aware-prompt machinery.

## Files changed (not committed — Lead reviews and commits)

```
 services/conversation/conversation_manager.py      |   6 +-
 services/intent/intent_service.py                  |  23 +---
 services/intent_service/conversation_context.py    | 109 ++++++----------
 services/intent_service/soft_invocation.py         |  26 ----
 services/intent_service/workflow_dispatcher.py     |   2 +-
 services/intent_service/workflow_entries.py        |   3 -
 services/shared_types.py                           |  26 ++--
 services/slot_filling/slot_template.py             |   8 +-
 tests/.../test_conversation_context.py              | 139 ++-------------------
 tests/.../test_conversation_context_persist_953.py  |  25 ++--
 tests/.../test_formality_soft_invocation.py         |  34 +----
 tests/.../test_soft_invocation.py                   |  47 +------
 tests/.../test_soft_invocation_integration.py       |  60 +--------
 tests/.../test_workflow_dispatcher.py               |   1 -
 14 files changed, 100 insertions(+), 409 deletions(-)
```
Plus 2 new files: `test_layer4_hydration_ignores_legacy_lens_keys_1863.py` (69 lines),
`design-record-lens-surface-rip-1863-2026-09-23.md` (163 lines). Total 16 files, well under the
<20 batch limit. No `git add` run — index left alone per dispatch instructions. Two pre-existing
untracked paths (`dev/active/canonical-retest-serving-llm.json`, `uploads/ac61e65c-.../`) are NOT
mine and are excluded from this file set.

`docs/internal/architecture/current/intent-routing-stack.md`: checked for "lens" mentions —
none found, so no update needed there.

## Discovered issues

One, reported only (per dispatch instructions), documented in the design record's "Discovered
work" section: Issue #821's slot-filling lens-aware-prompt machinery
(`SlotFillingSession.active_lens`, `slot_prompts.py`, `slot_template.py`'s
`lens_prompts`/`lens_group_priority`, `slot_extractor.py`'s lens-aware group ordering) is now
permanently unfed — its one real-world feed (transitively, via the #820/#763 chain through
`workflow_entries.py`) is gone with this cut. Behavior is unchanged (it was already always-None in
production before this cut, via the same transitive chain), but it is now a second,
structurally-similar writer-less-field candidate for a future Rule-0 census — distinct from #1863's
scope and not GO'd here. Not filed as a `bd` issue per this dispatch's "report only" instruction;
flagging for the Lead to decide whether to track it.

## Verified how

- **Method**: ran every command shown above this session, in this worktree, after making the
  edits (not recalled from memory or an earlier run); quoted actual output, not summarized.
- **Layer**: unit-test suites (pytest, real assertions against the edited code, not just import
  checks); static analysis (ruff format/check, mypy against the ratchet gate's exact venv);
  architecture/completion ratchet tests (which assert exact ceilings, not just "some tests pass");
  collection sanity (proves no import-time breakage across the full 14270-test suite, not just the
  files I touched); two dedicated lint scripts (principal-threading, silent-death count) run
  directly, not inferred from prior runs.
- **Denominator**: 8 production service files + 6 modified test files + 2 new files = 16 files
  touched, all read and edited directly (none touched blind via search-replace across the repo);
  fresh-sweep grep covered all of `services/` + `web/` (both python roots) for "lens", cross-checked
  every hit against the census table, with the two deltas above reported before acting; the 853
  total mypy findings were the full-repo baseline (97 files) — only the 3 pre-existing, untouched
  hits inside my 8 files were individually checked against `git diff` line ranges to confirm zero
  net-new.

## Memory & briefing surfaces referenced this session

- **Referenced**: GitHub issue #1863 body + comments (census + Arch GO — primary task spec);
  `docs/internal/architecture/current/intent-routing-stack.md` (checked for lens mentions, none
  found); the design-record template at
  `docs/internal/architecture/design-records/design-record-grammar-conscious-family-2026-09-23.md`
  (format/tone model for this issue's design record); delete-module-safely skill principles
  (extract-before-delete rationale, cited in my design record's header).
- **Loaded but not referenced**: CLAUDE.md's full worktree/mailbox/sign-off discipline sections
  (not applicable — this dispatch explicitly says do not commit/push); MEMORY.md project/feedback
  index (no specific entry was load-bearing for this mechanical rip task).
- **Wanted but not found**: none — the issue + Arch's GO memo were sufficient to scope and execute
  without needing additional context surfaces.
