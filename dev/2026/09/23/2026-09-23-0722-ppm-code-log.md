# 2026-09-23 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *`

## Session Objectives

1. Sprint-week continuity: board hygiene (`sprint-truth.py` + the third-queue-source criteria
   line, both every fire), mailbox triage, issue triage against precedent.
2. Keep the carry-forward lean per yesterday's spring-clean — current state + active threads
   only, no re-accreted Prior-chain.
3. No standing owed work item carried from yesterday — nothing PM-gated.

### 07:22 START

Heartbeat emitted (`scripts/duty-cycle-heartbeat.sh ppm START`). `CronList` → one job (`970d5675`,
unchanged overnight — correct expression, no re-arm needed). Prior day's log
(`dev/2026/09/22/2026-09-22-0713-ppm-code-log.md`) confirmed carrying its `<!-- DAY-CLOSED:
2026-09-22 -->` sentinel — Step 0 clean, no missed-STOP recovery needed. Carry-forward read: clean,
lean (35 lines, matching yesterday's spring-clean), nothing PM-gated carried forward. Worktree-
collision fingerprint clean (`ppm` ↔ `claude/ppm-cycle`). Synced to `origin/main` clean — noted
CXO's report that the registry CSV-quoting bug (CIO fixed once already this week) recurred
overnight, already routed to CIO, not a PPM item.

**Mail drain**: 0 items, inbox empty.

**`sprint-truth.py` fresh**: `MVP: 53 not done (26 Sprint Backlog, 3 In Progress, 6 In Review, 18
Product Backlog); 1142 done. PLUS 0 unmilestoned.` No delta overnight. **Third-queue-source
criteria line**: 0 gap, denominator 30 — clean.

Quiet START — all three queue sources checked and drained. Returning to idle; cron armed
(`970d5675`), next fire 09:52 PDT (WORK).

### 10:22 WORK

Heartbeat emitted. Synced clean. Mailbox: 0 items. `sprint-truth.py` at 10:23 (pre-throttle):
`MVP: 53 not done; 1142 done. PLUS 0 unmilestoned.` Third-queue-source criteria line found **6
issues** (`#1855`-`#1860`), all filed within one minute of each other (16:49:53–16:50:28 UTC),
identified by their own bodies as **PM's live Test-1 dogfood session on alpha, 2026-09-23
~9:29–9:42 AM** — same pattern as the 09-21/09-20 dogfood sessions (`#1836`-`#1838`, `#1818`).
Read each issue's title/body before touching anything. Milestoned MVP, board-added, Status=Product
Backlog for all six via per-item `updateProjectV2ItemFieldValue` mutations (never the banned
full-replace `updateProjectV2Field`) — pre-checked Status was null on all six before mutating.
Verified via a small combined GraphQL query after the full board pull hit the shared cohort-wide
GraphQL throttle (confirmed shared, not personal: `gh api rate_limit` showed full quota at the
time).

Read full bodies, placed all six into `dev/active/mvp-epic-order-2026-09-09.md`: `#1855`→epic 3
(generalizes the `#1837` turn-2 acceptance-binding gap CXO's new contract rule already covers)
· `#1856`→epic 5 (`#1836`-family template-loop shape in the name-gathering flow) · `#1857`→epic 4
(`#1843`'s extraction-greed family, gate-legal as a corpus row per `TestExtractionPatternRatchet`)
· `#1858`→epic 5 (misreports a definitive GitHub 404 as indeterminate — honest-empty class, not
epic 2's auth-bucket class despite citing `#1824` as the bucket-discipline precedent) · `#1859`→
epic 9 catch-all (chat-switch white-flash render regression, Web lane, Lead already ruled out the
obvious cutover-header cause same-day) · `#1860`→epic 4 (standup verb-initiation coverage gap,
self-identified "corpus-deposit lane" in its own title).

Re-ran the criteria line to confirm zero gap and found **7 more — all pre-existing, none from
today's dogfood, oldest filed 2026-07-10**: `#1386`, `#1570`, `#1595`, `#1623`, `#1625`, `#1628`,
`#1632`. Read all seven, placed six: `#1386`(BETA-GATE, cross-cutting)→epic 9 · `#1570`(floor-bound
QUERY false-negative + context-annotation leak)→epic 5 · `#1623`(`#1529`/`#1617` family, active
gathering flow losing turns)→epic 3 · `#1625`(reminder surfacing relentless, PM design ruling)→
epic 6 · `#1628`(chat-side listing titles render verbatim outside `#1622`'s guard seam)→epic 6 ·
`#1632`(capability-legibility catalog missing outwardness)→epic 2. **`#1595` (EPIC:
Understanding-Layer Inversion) NOT bucketed** — epic-scale itself (Arch-ratified 08-09, "spine of
the moved-beta month"), six weeks untracked in this file; flagged as a top-of-file note instead of
forced into one of the 10, and routed to Lead/Arch via mail (`mail(ppm): #1595 six weeks
untracked...`, pushed `a79e670bc`) for a sequencing call this file shouldn't make unilaterally.

All 13 item-count headers updated in the same pass. Re-ran the criteria line one final time: **0
gap, denominator 59** (cross-checked against the milestones API's `open_issues` count, which
matched exactly). Used the REST `issues`/`milestones` endpoints for all of this fire's verification
after `gh project item-list`/`sprint-truth.py` started failing with a misleading "unknown owner
type" error — traced to the same shared GraphQL throttle (a raw `gh api graphql` owner-type probe
returned the real `RATE_LIMIT` error gh's project subcommand was mangling); not a gh bug, not a
personal-quota issue, not blocking — REST worked throughout.

**Discovered work filed**: none new as GitHub issues — the 7 pre-existing gaps were already-filed
issues missing epic-file placement, not new defects, and `#1595`'s tracking gap was handled via
mail (not a new issue; the epic itself already has one).

Committed + pushed (`475b13d6f7`), verified on `origin/main`, working tree clean.

### 13:22 WORK

Heartbeat emitted (synced first — stale-marker refusal, standard fix). `CronList` → one job,
unchanged. Mailbox: 3 items — 1 direct (Lead, reply to this morning's `#1595` flag), 2 cc (Exec
fleet notices on the usage-wall reset, both informational, no action). All three read fully,
triaged to `read/`, MANIFEST regenerated.

**Lead's ruling on `#1595`, acted on in full**: its own top-level slot above the 10 numbered
epics — "epic 0, the interpretation spine" — never folded into one of them, because it's the
default sink other epics' interpretation-layer findings deposit into by rule (the supersession
gate). Replaced this morning's flag-note with epic 0's full entry: the structural reasoning, three
live examples (`#1841`, `#1854` — correctly Production-scoped, outside this file — `#1860`), and
Lead's own build-status line verbatim (Phases 0-2 complete per 8/24 attestation; wave-1 traffic
**unverified since 8/24**, stated as such, not guessed at; waves 2+ not started).

**Real bug found and fixed in this seat's own third-queue-source tool**: `gh issue list` defaults
to `--limit 30` with no warning. The criteria line as originally written (adopted 2026-09-22) had
no `--limit` flag, so every run since adoption — including this morning's "denominator 30, 0 gap"
— was silently checking only the first 30 of ~57-59 MVP-open issues. Caught when a GraphQL-vs-REST
cross-check (done to route around this morning's throttle) returned mismatched counts for the same
query at the same moment. Fixed: `--limit 500` added, documented in the carry-forward with the
finding. **Low actual blast radius**: only ran twice pre-fix, and the 13 gaps it eventually caught
(via the REST workaround, not this tool) were all pre-existing, filed well before the tool existed
— the bug delayed discovery, didn't create new gaps. Re-verified post-fix: 0 gap, denominator 59.

**`sprint-truth.py`**: re-run found 5 of this morning's 6 board-adds (`#1855`/`#1856`/`#1857`/
`#1859`/`#1860`) reported `NOT ON THE BOARD` again — the known recurring drift shape. Re-added all
5 (`gh project item-add`) and re-set Status=Product Backlog via safe per-item mutation
(pre-verified null before mutating, per this seat's standing discipline). **Re-run still reported
the same 5 as missing** — directly queried each issue's own `projectItems` connection instead of
`gh project item-list`'s output, and found **all 5 genuinely ON the board with correct Status**,
contradicting the tool's report. Working hypothesis, not yet proven: `gh project item-list`'s
backing data has read-after-write lag relative to the direct issue→projectItems GraphQL edge — a
tooling-reliability finding, not a real second removal. Did not blindly re-add a second time (risk
of duplicate items once the index catches up). Worth a dedicated check next fire rather than
resolving from one instance.

Also found 2 open issues carrying no milestone via direct query (`sprint-truth.py` only reports the
count, not the numbers): `#1863` (Rule-0 reader-census for the writer-less `lens` surface, split
from `#1774` per Arch's 09-22 ruling — this seat's own previously-carried "ALSO WATCH" item now has
its own issue) and `#1861` (Lead's own dev-instrument repair, `#1595`-adjacent, "red nobody sees"
shape matching epic 1's own family). Both milestoned MVP, board-added, Status=Product Backlog,
placed in the file — `#1863` as a note on `#1774`'s existing entry (epic 5), `#1861` folded into
epic 1 alongside its own `#1687`/`#1747` family. Item-count headers updated for epics 0 (new), 1,
and 5.

Committed + pushed (`c3e4d23db0`), verified on `origin/main`, working tree clean.

### 16:22 WORK

Heartbeat emitted, synced clean, one cron job confirmed. Mailbox: 3 items, all cc (`#1855`'s
design/ruling thread — Lead's proposal, Arch's ruling, CXO's ruling — none addressed directly to
PPM). Skimmed all three for asks (none), triaged to `read/`.

**`#1855`'s epic-3 entry updated with the resolved design**: Lead's design doc landed same-day,
Arch approved layer 1 (output-seam rewrite, unarmed offer-questions → imperative suggestions,
logged to the `#1595` corpus sink) and ruled DELETE (not build) on the never-instantiated
`LastOffer.offer_type="actionable"` reservation (verified via `grep -rn` — one hit, the dead
comment itself). CXO ratified the contract sentence and named `#1855` as `#1837`'s mirror from the
opposite direction (the offer being the lie, vs. `#1837`'s binding being wrong). Layer 2 (real
arming) explicitly deferred, not ruled today. No PPM action — recorded the resolution so the file
stays current with what's actually ready to build.

Criteria line re-run clean: 0 gap, denominator 52 (down from 59 — several closures since the last
check, expected). `sprint-truth.py` re-run: **`#1863` reported `NOT ON THE BOARD` again** — a
second, independent instance of the same shape found at 13:22 (`#1855`/`#1856`/`#1857`/`#1859`/
`#1860`, that time). Direct per-issue query again shows it genuinely on the board with correct
Status, contradicting the tool. Two confirmed instances same day is enough to stop calling it a
"working hypothesis, not yet proven" — filed a memo to CIO (`mail(ppm): sprint-truth.py
NOT-ON-BOARD false positive...`, pushed `466c079df`) naming both instances, the ~3h-then-resolves
pattern, and asking for someone with visibility into `gh project item-list`'s backing to look at
the mechanism rather than each role independently rediscovering this. Did not re-add `#1863` a
second time, consistent with the 13:22 decision.

Also noted from the delta: `#1718`, `#1729`, `#1758`, `#1835` closed since the last snapshot — all
four already tracked (epics 4/5/9), no epic-file action needed, just closures landing as expected.

Committed + pushed (`59848863bc`), verified on `origin/main`, working tree clean.

### 19:22 WORK

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed, unchanged. Mailbox: 7 items — 1 direct (an automated scope-guard flag), 6 cc (the
`sprint-truth.py` false-positive thread resolving, and `#1855`'s continuing design thread). All
read/skimmed, triaged to `read/`.

**Scope-guard flag on `#1744` — verdict set `true-positive`.** `#1744` is its own declared synthetic
test fixture for the delivery path; timeline shows it was closed/reopened 3x around the scan window
(23:03, 23:06, 23:45:43Z — the last matching the scan timestamp exactly), consistent with active
manual testing of the mechanism rather than a stale real issue. The flag correctly caught
checklist-complete-while-open at scan time; state has since resolved per the fixture's own
instructions. Mechanism worked as designed.

**`sprint-truth.py`'s NOT-ON-BOARD false positive: closed the loop.** CIO checked ownership
(`git log --follow` — Exec's tool, not CIO's) and routed my finding to Exec with a concrete fix
suggestion (cross-check via direct per-issue GraphQL before flagging, exactly what I'd been doing
by hand). **Exec fixed it same-day**: every NOT-ON-BOARD candidate now gets a direct per-issue
cross-check first, with three distinct output states (confirmed-absent/index-lag/cross-check-failed)
instead of one bare flag. Verified this fire — `sprint-truth.py`'s output now reads `[reconciled: 51
open issues by milestone, all present on the board]`, no false positive.

**`#1855` layer 1 SHIPPED same day, on `origin/main`.** Updated the epic-3 entry with the actual
shipped shape (seam at `ConversationalFloor.respond()`, new `unarmed_offer.py`, fail-safe unproven-
arm-treated-as-unarmed, `actionable` reservation deleted and pinned by a grep-test) plus the
same-day fifth-opener addition ("Do you want me to…?", Arch ruled add / CXO ratified). Layer 2
stays open; `revise_draft()` flagged as a second free-prose seam. **`#1863`'s note updated**: rip
dispatched (Lead, Sonnet lane) on Arch's two conditions, in flight not yet closed.

Third-queue-source criteria line: 0 gap, denominator 51. `sprint-truth.py` fresh: clean, reconciled,
3 unmilestoned issues found via direct query — all three from today's active lane work (`#1499`/
`#1856` lanes). Read all three, milestoned MVP, board-added, Status set: `#1870` (left open by
`#1718`, same honest-error-surfacing class, three named gaps in Gemini/Perplexity validators and
`_classify_llm_error` drift) → epic 5 · `#1867` (guided flows can start sessions on a deregistered
onboarding process, `#1856`'s structural root cause one layer down) → epic 5 · `#1871`
(`standup_workflow_skill` calls a `SlackDomainService` method that doesn't exist — the Slack-post
leg of the standup skill can never have worked live, masked by tests mocking the missing method
directly) → epic 9 catch-all. Item-count headers updated for epics 5 and 9. Re-verified criteria
line clean after placement, then ran a second empty round (mail + unmilestoned check both clean) per
the two-consecutive-empty-rounds rule before closing the fire.

Committed + pushed (`aef9d7b8ee`), verified on `origin/main`, working tree clean.

### 22:22 STOP — day close

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. One cron job
confirmed (`970d5675`), unchanged all day. Mailbox: 0 items. Criteria line: 0 gap, denominator 54.
`sprint-truth.py`: clean, reconciled, `MVP: 54 not done (23 Sprint Backlog, 3 In Progress, 6 In
Review, 22 Product Backlog); 1152 done. PLUS 0 unmilestoned.` The delta's "arrived: #1867 #1870
#1871" confirms this evening's three placements landed correctly. Two consecutive empty rounds
(mail + both checks), drained. No stale `cycle-log-ppm-*.md` (≥7d) or `dev/active/*.tmp` (≥1d)
files found — nothing to clean up.

## Day-arc summary (2026-09-23)

Six fires (START 07:22 → STOP 22:22), all quiet-to-substantive WORK in between, no PM-gated
blockers all day. The day's throughline: **13 new-issue placements plus a live tooling-defect
find-and-fix cycle**, both flowing through this seat's own third-queue-source criteria line and
direct-verification discipline.

- **Board hygiene**: 16 total issues milestoned MVP, board-added, Status-set, and placed into
  `dev/active/mvp-epic-order-2026-09-09.md` today — 6 from PM's live Test-1 dogfood session
  (`#1855`-`#1860`, 10:22), 7 pre-existing gaps the criteria line caught after a real bug fix
  (`#1386`/`#1570`/`#1595`/`#1623`/`#1625`/`#1628`/`#1632`, 10:22), and 3 more surfaced by today's
  active Lead/Arch/CXO lane work on `#1499`/`#1856` (`#1867`/`#1870`/`#1871`, 19:22). Plus 2 more
  (`#1861`/`#1863`, 13:22) from a direct-query sweep. Epic-count headers kept internally consistent
  throughout.
- **`#1595` got its own top-level "epic 0" slot** — flagged this morning as too epic-scale to
  bucket, ruled by Lead this afternoon (interpretation spine, default sink for other epics' routing
  findings), recorded with Lead's own build-status line stated honestly (wave-1 traffic explicitly
  "unverified since 8/24," not guessed at).
- **`#1855` went from filed to shipped in one day** — dogfood finding (10:22) → design + three-way
  ruling from Lead/Arch/CXO (16:22) → layer 1 live on `main` plus a same-day fifth-opener addition
  (19:22). Kept the epic-3 entry current at each stage rather than letting it go stale mid-build.
- **Two real tooling-reliability findings, both closed the same day they were found**: (1) this
  seat's own third-queue-source criteria line had a silent `--limit 30` truncation bug since
  adoption yesterday — caught via a GraphQL-vs-REST cross-check, fixed, low blast radius confirmed
  (the gaps it eventually caught were pre-existing, not created by the bug). (2) `sprint-truth.py`'s
  `NOT ON THE BOARD` line produced 2 confirmed false positives (`#1855`-family at 13:22, `#1863` at
  16:22) — filed to CIO, correctly routed to Exec as the tool's actual owner (checked via `git log
  --follow` rather than assumed), and Exec shipped a direct per-issue cross-check fix the same
  evening. Verified clean at both the 19:22 and 22:22 fires.
- **One scope-guard automated flag drained**: `#1744` (a declared synthetic test fixture) verdicted
  `true-positive` after checking its timeline against the scan window, not just its current state.
- **Discovered-work discipline**: no new GitHub issues needed filing today — every gap found was
  either an already-filed issue missing epic placement, or (for `#1595`/`sprint-truth.py`) routed
  via mail to the right owner rather than worked around silently.

## Memory & briefing surfaces referenced this session

**Referenced**:
- `dev/active/mvp-epic-order-2026-09-09.md` — the standing artifact this whole day's work updated;
  informed every placement decision and the epic-0 addition.
- `dev/active/ppm-carry-forward.md` — read/rewritten every fire; the third-queue-source criteria
  line block lives here and was itself corrected mid-day.
- CLAUDE.md's "Never guess at facts" / "Verify First, Create Second" sections — informed the
  repeated pattern of checking `git log --follow` for tool ownership (CIO's memo modeled this too),
  cross-checking `sprint-truth.py`'s flags via direct GraphQL before acting, and reading full issue
  bodies before placement rather than guessing from titles/numbers.
- The duty-cycle-tick skill's "two consecutive empty rounds" rule — applied literally at both the
  16:22 and 19:22 fires after each surfaced new work mid-round.
- CLAUDE.md's mail-vs-GH-comment norm and the "state the denominator" discipline (m-43/m-44) —
  shaped the CIO memo's framing (2 of 2, no base rate claimed) and this log's own phrasing.

**Loaded but not referenced**: `docs/briefing/BRIEFING-CURRENT-STATE.md`, the milestone-sequence /
PUBLIC-beta-gate reminders in the duty-cycle prompt (no milestone-sequencing question arose today),
`docs/internal/planning/release-model.md`, the ADR/PDR-vs-decisions.log surface (no cross-session
architectural decision of PPM's own to record today — today's decisions were Lead/Arch/CXO's, mailed
and recorded in the epic file, not new ADRs).

**Wanted but not found**: nothing — the criteria-line bug fix and the sprint-truth.py false-positive
both resolved same-day through documented channels (carry-forward correction, mail to the right
owner), no gap in available surfaces this session.

## Sign-off checklist

```
$ git status
On branch claude/ppm-cycle
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean

$ git fetch origin main
(no new objects)

$ git rev-parse --verify -q origin/main >/dev/null && echo "origin/main resolves"
origin/main resolves

$ git log --oneline origin/main..HEAD
(empty)
```

Working tree clean, `origin/main` resolves, nothing unreachable from `origin/main`. All six fires'
work is on trunk. No NOTICE memo needed — nothing held on the branch.

**Discovered issues filed today**: None as new GitHub issues — all 16 placements were pre-existing
issues (filed today by PM's dogfood session, by Lead/Arch/CXO's active lane work, or filed
previously and found via the criteria line) needing epic-file/board placement, not new defects PPM
originated. Two tooling defects found (criteria-line `--limit` bug, `sprint-truth.py` false
positive) were fixed directly (own tool) or routed to the correct owner via mail (Exec's tool),
per the "fix at the source" discipline rather than filed as tracking issues.

Re-arming cron via delete-then-create as the final action.

<!-- DAY-CLOSED: 2026-09-23 -->
