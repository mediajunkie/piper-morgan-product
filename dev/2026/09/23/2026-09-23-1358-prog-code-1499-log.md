# Session Log — Coding Agent — #1499 (stale-routes audit, non-deletion half)

**Date**: 2026-09-23
**Role**: Coding Agent (prog), dispatched by Lead Developer
**Model**: Opus 5 (1M context) — `claude-opus-5[1m]` (observed at session start)
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
**Issue**: #1499 — Stale/duplicate route audit (2026-08-07 PM-directed subagent audit)
**Scope**: "Suggested fix order" items **2, 3, 5** + Class 3 dead UI calls + Class 4 doc drift.
**Out of scope (explicit)**: Class 2 unmounted-router deletion/mounting (Rule-0 ruling with Arch,
requested separately); Class 5 backup-file deletion; anything under `services/standup/`, the
calendar adapter, or standup/agenda templates (a concurrent Coding Agent holds #1576 in this
same worktree).

**Do NOT commit or push** — dispatcher reviews and commits.

---

## Plan (read-first, then red-first)

Read in full before touching anything (per "Verify First, Create Second"):
- Issue #1499 body via REST (`gh api …/issues/1499 --jq .body`) — done
- `docs/internal/architecture/current/web-routes-conventions.md` — done
- Both Slack OAuth-initiation implementations + their two UI callers — done
- `deploy_identity()` in `services/api/health/staging_health.py` + its live consumer
  `web/api/routes/admin.py` — done
- The two `/api/v1/version` callers' JS (field names they consume) — done
- `services/auth/auth_middleware.py` exempt categories — done

### Findings from the read pass (before any edit)

1. **`/slack/connect` vs `/slack/authorize` differ in response SHAPE, not behavior.**
   - `connect_slack` (`:347`) → `{"auth_url", "state"}`; consumed by
     `templates/integrations.html:742` (`data.auth_url`).
   - `get_slack_oauth_url` (`:2304`) → `{"success", "authorization_url", "state"}`; consumed by
     `templates/settings_slack.html:963` (`data.success && data.authorization_url`).
   - Both carry the #1324 fallback chain (`SLACK_SETTINGS_REDIRECT_URI` → `SLACK_REDIRECT_URI` →
     `{_base_url()}/api/v1/settings/integrations/slack/callback`). `authorize`'s copy was patched
     2026-08-07; `connect` had it from #1324.
   - Only real internal difference: `authorize` passed `SlackOAuthHandler(SlackConfigService())`,
     `connect` passed `SlackOAuthHandler()`. **`oauth_handler.py:64` is
     `config_service or SlackConfigService()`** → identical. No behavior lost by collapsing.
   - ⇒ Keep `connect_slack` as the single implementation; `authorize` becomes a thin
     shape-adapter delegating to it. Both paths stay routable.

2. **`/api/v1/version`** — both callers (`templates/settings-index.html:296`,
   `templates/account.html:255`) read exactly `data.version` and `data.environment`.
   `deploy_identity()` returns `{environment, version, git_sha}` — field names already match.
   Mount point: `web/api/routes/admin.py`'s prefix-less router (mounted at `web/app.py:308`,
   already imports `deploy_identity`, already hosts the live `/health`).

3. **`/api/v1/learning/health`** — the audit said "route commented out at `learning.py:530`".
   **Stale**: `grep -n health web/api/routes/learning.py` returns **zero** matches today; there is
   no commented-out route left to re-enable. And the caller is an `<a href>` link in the footer,
   not a `fetch`. ⇒ remove the dead link.

4. **`/privacy-policy`** — no route, and **no `privacy-policy.html` template exists**
   (`ls templates/ | grep -i privacy` → only `privacy-settings.html`). The markup literally ships
   the placeholder text `(if exists, or link to external)`. ⇒ remove the dead link + placeholder;
   writing a privacy policy is not a coding-agent call.

5. **Auth-exempt precedent**: `auth_middleware.py:70` already carries a pruned-entry comment from
   the #1504 audit ("no /register route is mounted anywhere — a dead exempt entry is a hole
   waiting for a route"). Same shape, same treatment for the two OAuth-callback entries.

---

## Work log

### Unit 1 — collapse `/slack/authorize` → `/slack/connect` (#1499 item 2)

`web/api/routes/settings_integrations.py` — `get_slack_oauth_url` (~:2304) went from a
47-line second implementation to a 10-line shape adapter: `result = await
connect_slack(current_user=current_user)`, re-keyed to `{success, authorization_url,
state}`. `connect_slack` (~:347) is now the only copy of the #1324 fallback chain; its
docstring says so. Both paths stay routable; neither template changed.

Verified no behavior lost: `authorize` built `SlackOAuthHandler(SlackConfigService())`
and `connect` builds `SlackOAuthHandler()` — `oauth_handler.py:64` is
`config_service or SlackConfigService()`, so identical.

Red-first: `tests/unit/web/api/routes/test_slack_oauth_route_collapse_1499.py`, 6 tests,
real TestClient. **Honest red:** 5 of 6 passed pre-fix — the two copies *agreed* on
2026-09-23, so no behavioral test could be red. The red one was
`test_only_one_route_computes_the_redirect_uri`, which is the actual defect (two copies
to keep in agreement). The 5 behavioral tests are regression-locks, not the red.

Collateral: `test_settings_slack.py::test_handles_oauth_generation_failure` patched
`SlackConfigService` — a collaborator the surviving implementation never touches directly
(`SlackOAuthHandler` binds its own at import time), so post-collapse it asserted against
an impossible failure. Rewritten to fail the handler itself; message assertion updated
from "Failed to generate" to `connect_slack`'s "Failed to start Slack OAuth".

### Unit 2 — `/api/v1/version` (#1499 item 3)

Added at `web/api/routes/admin.py:119` on the prefix-less router (mounted `web/app.py:308`),
returning `deploy_identity()` verbatim — the same helper the served `/health` uses, so the
two cannot drift (#1839 lesson reapplied). Field names `version`/`environment` already
match what both pages read; no template change needed. Not auth-exempt (both callers are
authed pages; #1308 says the exempt list shouldn't grow without cause). Module docstring
route list updated.

Red-first: `tests/unit/web/api/routes/test_version_route_1499.py`, 8 tests, two layers —
route (TestClient: 404→200, field values, unknown-not-a-literal, shares identity with
`/health`) **and** template (`render()`: both pages fetch the path and read those field
names). Either layer alone is insufficient: a 200 with wrong key names still shows
"unknown" in the footer. 5 of 8 red pre-fix.

Collateral: `test_admin_cache_routes_gated_1508.py::test_router_route_count_denominator`
(an m-44 guard requiring every admin-router route be classified) failed on the new route —
working as designed. Added `/api/v1/version` to `UNGATED_READONLY` with a comment
distinguishing it from `/health`: no route-level `require_admin`, but **not** auth-exempt
either, unlike `/health`.

### Unit 3 — dead UI links (#1499 Class 3)

- `templates/learning-dashboard.html:605` — removed the `/api/v1/learning/health` footer
  link. **Audit was stale**: it said "route commented out at `learning.py:530`"; today
  `grep -n health web/api/routes/learning.py` returns zero matches — nothing to re-enable,
  so removal was the only honest option. (It was an `<a href>`, not a `fetch`.)
- `templates/privacy-settings.html:162` — removed the `/privacy-policy` link, which
  shipped with its own unresolved placeholder text "(if exists, or link to external)". No
  route, no template. Not stubbed: policy content is a product/legal call.
- Both replaced with Jinja comments recording why and what would restore them.

### Unit 4 — stale auth-exempt entries (#1499 item 5)

`services/auth/auth_middleware.py:128` — pruned `/slack/oauth/callback` and
`/github/oauth/callback` from `EXEMPT_OAUTH_CALLBACK_PATHS`, with a comment naming #1499
and mirroring the #1504 `/api/v1/auth/register` precedent already in the file.

⚠️ **Near-miss caught by checking instead of assuming**: `web/api/routes/setup.py:1361`
*does* define a Slack OAuth callback. Enumerating `setup.router.routes` shows it resolves
to `/api/v1/setup/slack/oauth/callback` (the router carries a `/api/v1/setup` prefix), so
it is exempt via that entry's startswith match — **not** via the bare path I removed,
which only ever matched the unmounted #1496 router. Pinned as its own regression test so
the prune can't be re-broken by someone reading the names.

Took the brief's "simpler and honest" option on the general guard: pinned the two specific
paths, and wrote `TestGeneralExemptGuardIsAFollowUp` stating in the class docstring *why*
the general check isn't here (the plugin half of the mounted set only resolves at app
startup; a static version would false-positive on exactly the paths that matter) rather
than letting the absence read as coverage.

### Unit 5 — doc drift, `web-routes-conventions.md` (#1499 Class 4)

Every claim re-verified before editing, not taken from the audit:
- All three "deliberate exceptions" annotated 🔴 UNMOUNTED, rows kept, disposal explicitly
  deferred to the Arch Rule-0 ruling (#1499 Class 2).
- `/health` attribution repointed to `web/api/routes/admin.py:56`; noted that
  `staging_health.py`'s *router* is the dead part while its `deploy_identity()` helper is
  live and now feeds two surfaces; noted the root-level exception itself remains correct.
- `admin_compose.py` struck through with its actual fate: deleted by **#1307** in
  `0466fd09d8` (auth-exempt + writable + un-env-gated security gap) — confirmed via
  `git log --diff-filter=D`.
- New "Known violations — documented, NOT endorsed" section: the 8 `/api/admin/*` routes
  listed with line numbers, plus why #1499 does **not** rename them (external callers
  unestablishable from the repo; a rename trades a naming violation for live breakage —
  that wants a deprecation window).
- Also added `ui.py`'s root-level page routes as an undocumented de facto exception class.
  **Corrected the audit's number**: it said 35 (counting `@router.get("/` source
  occurrences); the live router has **27** of 28 paths non-`/api/`. Stated the method and
  flagged the class for an Architect decision rather than deciding it.
- Header now carries a standing note that a doc's "this route is live" is a claim about
  the mount graph and goes stale silently.

### Gates (all env-stripped)

| Gate | Result |
|---|---|
| New + touched tests (9 files) | **77 passed** |
| `scripts/run-sweep.sh smoke` | **534 passed, 1 skipped**, 13534 deselected |
| `scripts/run-sweep.sh ratchets` | **60 passed**; mypy gate all 24 codes at ceiling (total=1300) — **no ceiling moved**, nothing to lower |
| `ruff format --check` + `ruff check` (my 8 .py files) | 8 already formatted; all checks passed |

Ratchet ceilings untouched: no drop to record, no rise to fix.

### Concurrent-agent separation

A second Coding Agent holds #1576 in this same worktree. Its files —
`services/domain/standup_orchestration_service.py`, `services/utils/datetime_utils.py`,
`web/api/routes/standup.py`, `services/mcp/consumer/google_calendar_adapter.py`, its two
`*_1576.py` tests and its log — were never touched and are left as found. Noted because
the final `ruff format --check` flags `google_calendar_adapter.py` as needing reformat:
**that is the #1576 agent's in-flight file, not mine.** Ruff is clean on all 8 of mine.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md "Verify First, Create Second" (drove re-verifying every audit
  claim — caught the stale `learning.py:530` citation and the wrong ui.py count); m-43
  "name the layer" (two-layer version test: route + template render); m-44 "state the
  denominator" (the explicit not-implemented general exempt guard; the #1508 denominator
  test); `feedback_ui_fix_requires_template_render_test` (template `render()` over grep);
  #1839's `test_health_deploy_identity_1839.py` (TestClient pattern, docstring shape).
- **Loaded but not referenced**: mailbox/duty-cycle protocol sections; worktree Model A
  and sign-off discipline (dispatcher commits, not me).
- **Wanted but not found**: a machine-readable mounted-route inventory. Its absence is
  exactly why the general exempt-list guard is deferred rather than written; #1499 Class 2
  should produce one.
