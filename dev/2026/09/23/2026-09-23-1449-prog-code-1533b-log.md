# Session Log — 2026-09-23 14:49

**Role**: Coding Agent (prog)
**Model**: Sonnet (dispatched by Lead, tier stated: Sonnet 5)
**Issue**: #1533 (principal-dropping audit) — batch 2
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle)
**Scope discipline**: tests only, no commit/push — Lead reviews and stages my explicit file set.

## Context

#1533's audit found 33 of 49 test files driving principal-keyed paths
(`{user_id or 'anonymous'}:{session_id}`) never pass a non-None `user_id`.
`tests/intent/contracts/test_multiuser_contracts.py` (`TestMultiUserContractsAuthenticated`)
was already fixed as the worst single offender. This batch: re-census (files
moved since the 2026-08-08 audit), then fix the next 5 blind suites by call
count.

## Step 1 — Re-census

**Method**: grepped `services/` for the key idiom `or 'anonymous'` to enumerate
the keyed surfaces first, confirmed only two composite-key builders exist:

- `services/intent_service/conversation_context.py:376` `_context_key` — backs
  `get_or_create_context`/`clear_context` (the conversation-turn registry, #817).
- `services/intent_service/soft_invocation.py:563` `WorkflowOfferService._key` —
  backs the offer-window throttle. (The separate `_offer_key` — pending-offer
  store — is deliberately session-scoped only, #846; not principal-keyed.)

`process_intent` (`services/intent/intent_service.py:736`) is the sanctioned
entry point that drives `get_or_create_context` on every call via its outer
turn-recording seam (~L765-819), so it's the surface to search test callers
against.

Wrote a Python census script (scratchpad, not committed) that: finds every
test file calling `process_intent(`, `get_or_create_context(`, `clear_context(`,
`should_offer(`, or `record_offer(`; for each call site extracts the balanced-
paren call text and checks for a `user_id=<non-None>` kwarg. Also verified none
of the flagged files pass identity via `RequestContext(...)` instead (which
`process_intent` also accepts and would have been a false-blind) — zero hits
across the top 10 files.

**Denominator**: 1033 `.py` files scanned under `tests/` · 92 files call a
principal-keyed surface · **35 files are blind** (call the surface at least
once, never with a real `user_id`).

**Census table (blind files, sorted by call count desc)**:

| Calls | File |
|---|---|
| 22 | tests/unit/services/intent_service/test_offer_accept_decline.py |
| 18 | tests/unit/services/intent_service/test_contextual_offer_continuation.py |
| 13 | tests/intent/test_direct_interface.py |
| 13 | tests/intent/test_slack_interface.py |
| 13 | tests/intent/test_cli_interface.py |
| 13 | tests/intent/test_web_interface.py |
| 13 | tests/intent/contracts/test_performance_contracts.py |
| 13 | tests/intent/contracts/test_accuracy_contracts.py |
| 13 | tests/intent/contracts/test_error_contracts.py |
| 13 | tests/intent/contracts/test_bypass_contracts.py |
| 7 | tests/intent/test_execution_analysis_handlers.py |
| 6 | tests/unit/services/intent_service/test_multi_intent_orchestration.py |
| 5 | tests/unit/services/intent_service/test_soft_invocation_colleague.py |
| 5 | tests/unit/services/intent/test_floor_entry_context_1570.py |
| 4 | tests/archive/load/test_cache_effectiveness.py |
| 4 | tests/unit/web/api/routes/test_intent_conversation_ownership_1532.py |
| 3 | tests/unit/web/api/routes/test_intent_session_expired_1520.py |
| 3 | tests/unit/web/api/routes/test_intent_anonymous_key_gate_1320.py |
| 2 | tests/intent/test_no_timeouts.py |
| 2 | tests/unit/services/intent_service/test_render_truncation_sweep_1762.py |
| 1 each | tests/unit/test_slack_components.py, tests/load/test_concurrent_load.py, tests/load/test_error_recovery.py, tests/load/test_memory_stability.py, tests/regression/test_critical_no_mocks.py, tests/archive/load/test_sequential_load.py, tests/unit/web/test_byoc_key_usable_as_fallback_provider_1815.py, tests/unit/web/test_keyless_pleasantry_1818.py, tests/unit/web/test_byoc_user_key_reaches_llm_gate_1814.py, tests/unit/web/test_any_provider_gate_1823.py, tests/unit/web/test_authenticated_keyless_server_key_1807.py, tests/unit/services/intent_service/test_truncated_render_provenance_1738.py, tests/unit/services/intent_service/test_spend_free_canonical_ratchet_1818.py, tests/unit/services/intent_service/test_create_reminder_rail_1560.py, tests/unit/services/llm/test_consent_read_provenance_1816.py |

`test_multiuser_contracts.py` correctly does NOT appear (already fixed).
`test_floor_history_principal_1394.py` (the reference shape) also correctly
does not appear.

**Tie-break at 13** (8-way tie: 4 interface files + 4 contracts files):
picked `test_web_interface.py` and `test_slack_interface.py` over
`test_direct_interface.py`/`test_cli_interface.py` for production-surface
diversity (Web API + Slack are both real prod channels; CLI/Direct are
dev-tooling), and `test_error_contracts.py` over the other 3 contract files
(distinct property under test — error-handling graceful-degrade — vs.
performance/accuracy/bypass, and it uses the shared `tests/conftest.py`
`intent_service` fixture rather than the interface files' local-mock
fixture, giving genuine fixture-shape diversity in the batch too).

## Step 2-4 — Fixed 5 files

For each: added one `Test*Authenticated` class following the established
shape (two distinct real `user_id`s via `uuid4()`, one shared `session_id`,
assert the composite-key contexts are distinct and turns don't leak),
unmarked (deterministic tier, #1831 stub where applicable), plus the
standing-review-rule docstring line copied from `test_multiuser_contracts.py`.

### 1. tests/unit/services/intent_service/test_offer_accept_decline.py (22 calls)
- Added `TestOfferAcceptDeclineAuthenticated` (+1 test, 21→22 total in file... file
  had 20 tests before, now 21).
- Property: the offer accept/decline SHORTCUT path (bypasses classification
  entirely) still runs the outer turn-recording seam and must not drop
  user_id. Pending-offer store itself confirmed session-scoped by design
  (`WorkflowOfferService._offer_key` — user_id accepted for signature parity
  only, never mixed into the key) — test re-arms it between the two users'
  turns rather than asserting isolation on it.
- Run: `pytest tests/unit/services/intent_service/test_offer_accept_decline.py -v`
  → **21 passed** (20 original + 1 new).
- TEETH-PROOF: set `user_b = user_a` → `test_accept_and_decline_do_not_leak_turns_across_authenticated_users`
  **FAILED** on `assert ctx_a is not ctx_b` (contexts collapsed to the same
  object, both turns present in one context). Reverted → **21 passed** again.

### 2. tests/unit/services/intent_service/test_contextual_offer_continuation.py (18 calls)
- Added `TestContextualOfferAuthenticated` (+1 test).
- Property: the #852 contextual-offer pair (`last_offer` write at canonical
  seam / read at next-turn start) — the audit's named live split-brain
  risk — proven not to leak `last_offer` between two authenticated users
  sharing a session_id, and that each user's own offer survives the other
  user's intervening turn (stronger than "B doesn't see it": also proves A's
  copy wasn't destroyed, ruling out "both reads happened to come back empty").
- Run: **15 passed** (14 original + 1 new).
- TEETH-PROOF: `user_b = user_a` → test **FAILED** (user B's "yes" picked up
  user A's continuation hint — `context_arg` was not None). Reverted → **15
  passed**.

### 3. tests/intent/test_web_interface.py (13 calls)
- Added `TestWebInterfaceAuthenticated` (+1 test), own local fixtures mirroring
  the file's existing per-test-mocked-classifier shape (not the shared
  `tests/conftest.py` fixture — this file constructs its own `IntentService()`).
- Property: two real users sharing a session_id through the Web API call
  shape get isolated turn history.
- Run: **15 passed** (14 original + 1 new).
- TEETH-PROOF: `user_b = user_a` → test **FAILED** (`ctx_a is not ctx_b`
  assertion tripped, both turns landed in one context — visible in the
  captured `ConversationContext` repr showing 2 turns). Reverted → **15
  passed**.

### 4. tests/intent/test_slack_interface.py (13 calls)
- Added `TestSlackInterfaceAuthenticated` (+1 test), same shape as web
  interface (file structure is near-identical).
- Run: **15 passed** (14 original + 1 new).
- TEETH-PROOF: `user_b = user_a` → test **FAILED**. Reverted → **15 passed**.

### 5. tests/intent/contracts/test_error_contracts.py (13 calls)
- Added `TestErrorContractsAuthenticated(BaseValidationTest)` (+1 test),
  unmarked — uses the shared `tests/conftest.py` `intent_service` fixture
  and the same tolerant-call wrapper pattern as
  `test_multiuser_contracts.py`'s `TestMultiUserContractsAuthenticated`
  (tolerates only the #1831 "No LLM providers configured" failure).
- **Finding during authoring** (not a defect, a test-design correction):
  the original suite's empty-string message (`message = ""`) never reaches
  `process_intent`'s turn-recording seam at all —
  `services/intent/intent_service.py` ~L813 guards `add_turn` with
  `if message and (...)`, and `""` is falsy in Python. An authenticated
  test built on `message = ""` would trivially pass (both users' turn
  lists stay `[]`) regardless of whether user_id is threaded — a config
  check, not a verification, exactly the m-44 trap the standing rule warns
  against. Switched the new test's probe to whitespace-only (`"   "`,
  truthy) to keep the "malformed input" spirit while actually exercising
  the seam. **Did not touch the original 13 empty-string tests** — out of
  scope (product code unchanged; this is a per-instruction test-only
  batch) — flagging for Lead in case the empty-string choice across the
  whole suite is worth a look.
- Run (new test alone, unmarked): **1 passed**.
- Run (full file, including the 13 `llm`-marked original tests + zzz +
  new): **15 passed** in 42.6s (real LLM calls for the 13 marked tests).
- TEETH-PROOF: `user_b = user_a` → test **FAILED** (`ctx_a is not ctx_b`).
  Reverted → passed again.

## Batch verification

```
pytest tests/unit/services/intent_service/test_offer_accept_decline.py \
       tests/unit/services/intent_service/test_contextual_offer_continuation.py \
       tests/intent/test_web_interface.py \
       tests/intent/test_slack_interface.py -q
→ 66 passed in 4.69s

pytest tests/intent/contracts/test_error_contracts.py -q
→ 15 passed, 14 warnings in 42.62s

scripts/run-sweep.sh smoke
→ 536 passed, 1 skipped, 13571 deselected, 90 warnings in 24.93s
  (unrelated to this batch — none of the new tests carry @pytest.mark.smoke;
   this confirms the rest of the gating suite is unaffected by the edits)
```

## Remaining blind count after this batch

35 blind files found in re-census − 5 fixed this batch = **30 files still
blind** (down from 33 at the 2026-08-08 audit's snapshot, though the
denominator itself has moved — 92 keyed files now vs. 49 then — so the
33→30 comparison is illustrative, not apples-to-apples; the census script
and its raw JSON output are the durable artifact for the next batch to
re-run rather than diff against this count).

## Files touched (my explicit set — Lead to review/stage)

- `tests/unit/services/intent_service/test_offer_accept_decline.py`
- `tests/unit/services/intent_service/test_contextual_offer_continuation.py`
- `tests/intent/test_web_interface.py`
- `tests/intent/test_slack_interface.py`
- `tests/intent/contracts/test_error_contracts.py`
- `dev/2026/09/23/2026-09-23-1449-prog-code-1533b-log.md` (this log)

**Not mine — found already present in the worktree at session start, left
untouched**: `scripts/ratchet_ceilings.json`,
`services/integrations/mcp/skills/standup_workflow_skill.py`,
`services/intent_service/canonical_handlers.py`,
`services/utils/datetime_utils.py`,
`tests/unit/integrations/mcp/test_standup_workflow_skill.py`, plus
untracked `dev/2026/09/23/2026-09-23-1447-prog-code-1869-log.md`,
`tests/unit/services/integrations/mcp/test_standup_skill_labeled_faces_1869.py`,
`tests/unit/services/intent_service/test_guidance_user_clock_1868.py` — these
carry issue numbers #1869/#1868, evidently a concurrent session's work in
the same shared worktree. Confirmed via `git status --short` at session
start and again at wrap; did not stage, edit, or read their diffs.

## Product defects found

None. The empty-string-message observation above is a test-design gap
(the new authenticated test's own trap to avoid), not a product defect —
skipping turn-recording for a genuinely empty message looks intentional
(nothing to record) rather than a principal-dropping bug.

## Verified how

- **Method**: ran each fixed file's full pytest suite (not just the new
  test) after the change, both directly and via `scripts/run-sweep.sh
  smoke`; ran each new authenticated test a second time with `user_b`
  forced equal to `user_a` to confirm the assertion fails (teeth), then
  reverted and re-ran to confirm it passes again — both tails captured
  above per file.
- **Layer**: unit/integration test execution against the real
  `IntentService.process_intent` → `get_or_create_context` seam (not a
  mock of that seam) — the composite-key collapse is exercised through
  the actual production code path, not asserted against a stub.
- **Denominator**: re-census covered all 1033 `.py` files under `tests/`;
  92 call a principal-keyed surface; 35 blind before this batch, 30 blind
  after (5 fixed). Batch stayed at 5 files fixed + re-census script (not a
  test file) — well under the 20-file cap.
