# Session Log — Coding Agent (prog) — 2026-09-23 16:09 PDT

- **Role**: Coding Agent (prog)
- **Model**: Opus (dispatched by Lead, tier stated)
- **Issue**: #1855 — the floor never offers what it hasn't armed (LAYER 1 + LastOffer disposition)
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (branch `claude/lead-cycle`)
- **Constraint**: do NOT commit or push. Lead reviews and commits an explicit file set.

## Scope

Layer 1 only (output-seam enforcement) + delete `LastOffer.offer_type="actionable"`.
Not layer 2 (real arming).

## Pre-reads completed

1. `docs/internal/architecture/current/intent-routing-stack.md` (surfaces 0–4; surface 4 is
   the floor). Note the `TestUnarmedAskSiteRatchet` boundary text names LLM free text as
   **"Present, not Enforced"** — #1855 is precisely the runtime closure of that named gap.
2. `docs/internal/design/design-1855-armed-floor-offers-2026-09-23.md`
3. `mailboxes/lead/read/rule-arch-to-lead-cxo-cc-ppm-pm-1855-approve-layer1-delete-actionable-not-build-it-2026-09-23.md`
4. `TestUnarmedAskSiteRatchet` (tests/test_architecture_enforcement.py:2862)
5. `LastOffer` (conversation_context.py:51), `WorkflowOffer`/`set_pending_offer`/
   `peek_pending_offer` (soft_invocation.py:44/639/651), `acceptance.py`
6. `interview_offer_accepted` (#1837) — standup/conversation_handler.py:376/652,
   intent_service.py:4208

## Baselines captured BEFORE any edit

```
scripts/check_silent_death.py --count   → 192
mypy (floor + conversation_context + intent_service), file-attributed errors → 138
  conversational_floor.py   0
  conversation_context.py   1
  intent/intent_service.py  137
```

## Seam investigation (findings)

- `ConversationalFloor.respond()` is the **single output seam** — every production floor
  door goes through it. Real call sites: `services/intent/intent_service.py` **2151**
  (ethics denial), **15255** (`_handle_floor_with_context`), **15458** (guidance),
  **15578** (`_handle_unknown_intent`). (`conversational_floor.py:765` is class-docstring
  example text; `standup/conversation_handler.py:1139` constructs a floor for
  `revise_draft`, a different method.)
- The success return is `respond()`'s `return FloorResponse(...)` at **1674**, after
  `strip_scaffolding_artifacts` / `strip_placeholder_slots` / `_maybe_append_push`.
  The error path at 1716 returns canned fallback copy with no offer-questions.
- **Armed signals readable at the seam**: `FloorContext` carries only
  session/user/history/domain_context — no rail access. The three rails resolve as:
  1. `WorkflowOffer` (#846) — **readable**, via `WorkflowOfferService.peek_pending_offer`
     (already a sanctioned non-dispatch reader, #1753/#1770). Soundness is the same
     argument `_apply_soft_offer` makes: the store is popped before classification
     (#1529), so a live entry was armed THIS turn.
  2. `LastOffer` (#852) — **readable**, via `IntentService._peek_last_offer` (#1770),
     same always-cleared-at-turn-start invariant.
  3. `interview_offer_accepted` (#1837) — **NOT readable at this seam, and structurally
     need not be**: it lives in `StandupConversation.context`, set only via
     `start_conversation(initial_context=...)` at intent_service.py:4208. An active
     standup conversation is claimed by the process registry ABOVE classification, so
     those turns never reach `ConversationalFloor.respond()`. The standup *invitation*
     (the thing that is armed while a floor turn could still run) arms through rail (1)
     and IS seen. Contract still expressible + pinned by test via the explicit field.

## Work log

- Fire 1 (16:09) — pre-reads, seam census, baselines. See above.
- Fire 1 (16:20) — layer 1 built, tests green, all gates at baseline. Files:

  | File | What |
  |---|---|
  | `services/intent_service/unarmed_offer.py` (NEW) | detector + three-tier rewrite + `enforce_armed_offers` |
  | `services/intent_service/conversational_floor.py` | `FloorContext.armed_offer`; enforcement at `respond()`'s success return, after the push appendage |
  | `services/intent/intent_service.py` | `_armed_offer_signal()` + threaded at all 4 floor doors |
  | `services/intent_service/conversation_context.py` | reserved `offer_type` value + dead comment DELETED |
  | `tests/unit/services/intent_service/test_floor_unarmed_offer_seam_1855.py` (NEW) | 44 tests |
  | `docs/internal/architecture/current/intent-routing-stack.md` | #1855 block (same change, per the consult rule) |

### Verification

```
new suite                         44 passed
A/B (enforcement neutered)        1 failed → red-before, green-after (measured, not reasoned)
11 existing floor suites          162 passed
every test file naming conversational_floor   862 passed
acceptance / soft-offer / offer -k slice      627 passed
intent_service + intent + standup unit dirs   4929 passed
tests/test_architecture_enforcement.py        53 passed
scripts/run-sweep.sh smoke        539 passed, 1 skipped
ruff format / ruff check          clean
principal_threading_lint.py       rc=0
check_silent_death.py --count     192 → 192 (no rise)
mypy file-attributed              138 → 138 (floor 0, unarmed_offer 0, conv_context 1, intent_service 137)
```

### Judgment calls for Lead review

1. **Tier-1 slot binding adds 2 anchored regexes** (quoted name, `owner/name` token) read over
   the FLOOR'S OWN sentence, not a user utterance. Gated by a round-trip through #1856's real
   `extract_add_project_slots` — we never suggest a phrasing the real extractor can't parse
   (#1108). Alternative was bracket-template-only, which would not have satisfied the fixture's
   `add project One Job with repo Design-in-Product/one-job`. Ratchet is untouched (new module,
   not a named span) but flagging the cost explicitly.
2. **Family kept to exactly the four ratified openers.** `Do you want me to …?` and
   mid-sentence offers are deliberately uncovered. Widening is a reviewed decision.

### Discovered (report only, not filed)

- `services/mux/pull_mode.py` has **no importers outside tests** (its `CORRECTION_INVITATION`
  is an offer-question that never reaches any seam). Already a known mux cold-code candidate
  from Arch's 2026-08-07 sweep — not re-filed.
- `ConversationalFloor.revise_draft()` (#1837 standup draft refinement) is a SECOND LLM-prose
  surface that returns user-facing text and does NOT pass `respond()`. Out of #1855's scope
  (the standup is armed), but it is the one other place the floor speaks freely.

## Memory & briefing surfaces referenced this session

- **Referenced** — `docs/internal/architecture/current/intent-routing-stack.md` (surface map;
  told me the floor is surface 4 and that its ask compliance is Present-not-Enforced);
  `design-1855-armed-floor-offers-2026-09-23.md` (the rule and the three tests);
  Arch's #1855 ruling memo (delete-don't-build, and the narrow-detector approval);
  `TestUnarmedAskSiteRatchet` (detector discipline + the boundary paragraph this closes);
  `TestExtractionPatternRatchet` (scoped the extraction cost of tier 1); CLAUDE.md
  (verify-first, name-the-layer, state-the-denominator).
- **Loaded but not referenced** — `acceptance.py` tier table beyond the ACCEPT/PASS split;
  the #1595/#1667/#1677 inversion blocks of the routing doc.
- **Wanted but not found** — a single shared helper for the #1856 "To do that, say: …"
  imperative. None exists; the copy is inline in `CanonicalHandlers`. Worked around with a
  duplicated constant plus an equality test so drift is loud.
