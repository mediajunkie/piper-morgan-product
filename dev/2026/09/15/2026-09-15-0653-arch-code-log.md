# Session Log: 2026-09-15-0653-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Opus — PM switch 2026-09-14, Fable 5 limit)
**Date**: Tuesday, September 15, 2026
**Start Time**: 6:53 AM PT
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

## Session Objectives

Duty-cycle. LIVE: Janne's invite re-held on #1814. Pending PM: Q5, scope-guard ruleset (#1744),
Bets. Watch: #1814 fix + observed re-lift bar, epic-6, #1797/#1798.

## Work Log

### 6:53 AM - Session Start (START) — two urgent items

- **Heartbeat FAILED to land** and said so loudly ("the belt will read arch as stale and the
  cause will not be visible. Investigate now"). Retried: landed, verified at trunk. Transient
  push race (4 roles in one minute). **First time I've watched that failure path fire** —
  banked to CIO as watched-it-fire evidence, with the argument against a silent retry (it would
  convert this into the invisible-success shape the whole belt thread is about).
- **URGENT: backed Lead's re-hold of Janne's invite** (7/7) — HOST had correctly lifted it last
  night on met evidence; #1814 then landed: `LLMConfigService.get_api_key()` takes no user and
  never consults `UserAPIKeyService`, so with the global slot correctly empty, **a BYOC user's
  own key is stored and never read** — the invite's one condition ("configure your key first")
  is now the path into a false "not configured" wall.
  **Owned my share, which is the ordering**: on 09-14 I ruled *delete the global write* and
  sequenced 1810→1809→1791 **without requiring that every reader of that slot be shown to have
  another source first**. I had `get_api_key(provider)`'s missing user parameter in front of me
  and read it ONLY as the leak's mechanism, not also as the BYOC path's missing half — one
  artifact, two meanings, and I took the one that supported the ruling I was writing. Same
  one-sided-measurement shape as the DocumentDB miss the day before.
  **Rule proposed into the record**: deleting a write requires enumerating that slot's readers
  with their post-deletion source NAMED — not "the readers still resolve whatever is there,"
  which is true and insufficient the moment the slot empties.
  Re-lift bar restated: an observed flow including a BYOC user storing a key AND completing a
  substantive query — the absence-of-global check alone would pass while the wall stood.
- Five drained. CXO's self-correction (their rider was NOT satisfied) is what surfaced #1814 —
  twice in two days the demanded observation caught what the reports didn't.

### 09:53 AM — WORK fire (#1816 + #1815 Gap 2 ruled — a consent boundary failing OPEN)

Six drained. **#1814 cleared by observation (v110) — the invite blocker is gone**; Exec's
retraction and HOST's re-hold both landed correctly in the interim (the system held the line
twice in twelve hours without anyone sending prematurely).

**The fire's work: Lead inverted their own premise and found a consent boundary failing OPEN.**
Verified both sides of the call myself: `KeychainService.get_api_key` swallows Exception →
returns None, so the #1415 F1 fail-CLOSED branch is UNREACHABLE in production and control
reaches `return list(all_configured)` — everything authorized. Its tests pass because only an
injected raising double reaches the closed branch. **Four rulings:**
1. Fix the CONSENT reader, **never the credential primitive** — None-on-failure is correct for
   credentials (#1711); changing it to patch a caller would break a sound contract and spread
   the blast radius. Consent gets its own absent-vs-unreadable read path.
2. **Lead's durable finding adopted as the stated root**, and it is COUSIN 1 AT THE SECURITY
   LAYER: a boundary whose "I don't know" and "no restriction" are the same value fails open on
   the first hiccup, silently, with green tests. Same shape as verified_empty/source_failed
   collapsing; same cure; "a convention is not a model" verbatim. Only the consequence differs —
   a dishonest sentence there, an unauthorized provider here.
3. **Gap 2: fail-closed must REFUSE, not degrade** — F1's "server-default only" target predates
   PM's #1812 ruling that the server key isn't a concept, so the original closed state is now
   incoherent. Closed means closed, not reassigned to the operator's key.
4. Consent-from-key-presence KEPT but as a **dated assumption with its invalidation trigger
   named** — valid only while no de-authorize surface exists (ESSENCE consent-invariance: an
   inferred consent carries its own expiry or becomes a claim the user never made).
7/7 delivered; decisions.log entry; verified-how credits Lead's live drive as theirs, not mine.

### 12:53 PM — WORK fire (conceded to CXO; named my own failure mode as a property, not an error)

Four drained. **CXO corrected my #1816 ruling's §3**: I wrote that their copy "already covers
the user-visible state" for a consent-read failure. It doesn't — their strings cover "you have
no key"; a consent-read failure is a user who HAS one. Built as written, my ruling would have
told them to add a key they already added — **the #1108 "recommends a known-failing action"
shape, arriving in CXO's own copy by way of my ruling.** Ruling amended: the consent-read-failure
state is a NAMED COPY GAP, CXO's to write, and a dependency on #1816 rather than an assumption
inside my memo.

**Named the pattern, because three is a property**: 09-13 DocumentDB (measured one side), 09-14
#1810 (one artifact, two meanings, took the fitting one), 09-15 this (asserted coverage from
knowing the artifact exists). **All three are m-52, and in all three the summary was my own
memory of something I had genuinely read once.** The rulings themselves have held; what keeps
failing is the supporting clause about what some OTHER artifact covers.

**Proposed a mechanical fix on my own output rather than promising more care**: a ruling of mine
that asserts "X already covers Y" must QUOTE X inline, or say "unverified — X's owner should
confirm." Same shape as the "Verified how:" field applied to cross-references. It would have
caught all three, because in each case I could not have produced the quote. Offered to CIO if it
generalizes past me. 7/7.

Also: HOST lifted the invite again (1814 bar met, 1816 checked as not touching Janne — the
third careful lift/re-hold cycle in 24h, each on evidence); CIO wrote my heartbeat evidence into
the script itself; Lead turned my n=1 heartbeat anecdote into a measured 50% rate on the
provider production uses.

## Active pattern families this session

Multi-Agent Coordination (029/059/010/021/037). Investigation (006/041-043/060).
