# Session Log — 2026-09-15 12:03 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)
**Task**: #1772 — MEASURE the N=1 scope-directive leak CXO found in #1717's own scoring
(2026-09-12), before anyone designs a fix. Produce a rate, not an anecdote. Explicitly
NOT a fix task.

## Method discovery

- Read `gh issue view 1772 --repo mediajunkie/piper-morgan-product --comments`, fell
  back to `gh api repos/.../issues/1772` (view returned truncated/mixed output as
  warned) + `/comments` for the full body and PPM's board-triage comment.
- Read `dev/2026/09/12/2026-09-12-1357-prog-code-log.md` and
  `dev/2026/09/12/1717-aggregate-transcripts-for-cxo.md` — the #1717 in-process harness
  shape: real `ConversationalFloor` → real `LLMClient`, KeychainService keys, env-stripped
  launch, `served` dict (#1620) for attribution. Also read
  `dev/2026/09/01/2026-09-01-1846-prog-code-log.md` — the PRE-#1717 live probe, which
  already recorded (as an aside, not a rate) that anthropic's one-flag reply "spilled
  beyond the armed flag ('I don't have your projects or todos in front of me this turn'
  when only the reminder check had failed)" — same phenomenon shape as #1772, from
  before the fix existed.
- Read current `services/intent_service/conversational_floor.py` (SOURCE_FAILED_FLAGS
  registry, the single composition site ~line 1250-1290) to confirm the code shape
  matches the transcripts' documented N=1/N=2 behavior at current HEAD (979 commits
  past the #1717 fix `26dc7b03e`; only 2 commits touch the file in that gap, neither
  touching the composition site — confirmed by direct read, then by the byte-check).

## Work

### Harness reconstruction (scratchpad, not committed — same disposition as 09-12/09-01)
- `probe_1772_scope_leak.py`: `ConversationalFloor(llm_client=LLMClient())`,
  `FloorContext(user_message="good morning, what's my status?", domain_context=<flags>,
  intent_category="conversation", user_id=None)`. `LLMClient.complete` wrapped to inject
  `served={}` per call (attribution, never inference). Provider forcing via monkeypatching
  `llm._config_service.get_default_provider`. Launched with
  `env -u ANTHROPIC_API_KEY -u ANTHROPIC_BASE_URL -u ANTHROPIC_AUTH_TOKEN
  -u ANTHROPIC_CUSTOM_HEADERS`.
- Validated wiring with 1 call per condition (4 calls) before spending the rest of the
  budget — server-default resolved openai/gpt-4o, forced resolved anthropic/
  claude-sonnet-4-6, both clean composes. Then ran the remaining calls to reach the
  pre-registered N.

### Run (30 completions total, exactly the approved budget, no scale-up)
- N=1 (`source_failed` only): 10 × openai/gpt-4o, 10 × anthropic/claude-sonnet-4-6.
- 2-flag control (`source_failed` + `projects_source_failed`): 5 × each provider.
- All 30 succeeded first attempt, all `served`-attributed, no fallback/COMPOSE_FAILED.

### Scoring
- Rule: a reply is a LEAK if it CLAIMS an unarmed source failed/was unavailable ("I
  don't have X in front of me"); NOT a leak if it merely OFFERS to check an unarmed
  source ("want me to check your todos?") — an offer isn't a claim, and matches CXO's
  own reading of the original flagged reply.
- **N=1: anthropic 5/10 (50%) leak, openai/gpt-4o 0/10 (0%).** One additional anthropic
  reply (not counted) made an unscoped general-failure claim without naming a specific
  source — flagged separately as "soft," not folded into the rate.
- **2-flag control: 0/5 both providers** — the N≥2 aggregate path (what #1717 actually
  rewrote) does not leak in this sample; the leak is specific to the N=1 single-directive
  path.
- Caught my own mid-draft miscount (first pass said 6/10 from an inconsistent
  keyword scan; re-read all 10 replies against the stated rule and corrected to 5/10
  — idx 1,2,3,7,8 are leaks, idx 5 is the soft case, idx 0/4/6/9 are invitational) before
  finalizing the document, so the shipped doc carries only the corrected count, not a
  visible correction narrative.

### Item 4 — prompt-layer pre/post-#1717 comparison (cheap, deterministic; no extra calls)
- Re-ran the byte-identity check CXO flagged as unmeasured, at CURRENT HEAD (not
  assumed from the 09-12 doc's 66-commits-back check): `git show 26dc7b03e^:....py`,
  imported both module versions in one process, compared `_format_domain_context()`
  output directly. **N=1: byte-identical.** (2-flag and 5-flag differ, as expected —
  that's exactly what #1717 rewrote.)
- Did NOT run pre-fix DELIVERED replies (would cost additional live calls; task scoped
  "prompts only unless you report first"). Cited the 09-01 pre-fix anecdote as
  circumstantial support, explicitly labeled as an anecdote, not a second rate.

### Deliverable
- `dev/2026/09/15/1772-scope-leak-measurement.md`: layer statement, method, call-count
  table, leak scoring rule, rate table, per-reply scoring table, all verbatim leaking
  replies, the soft case, the 2-flag control transcripts, the prompt-comparison result,
  one one-line mechanism observation for CXO/Arch (no fix proposed), full raw
  transcripts (30/30), denominator statement.

## Discovered issues filed
None. This is measurement evidence for CXO/Arch to act on, not new work to triage
separately — consistent with how the #1717 probes handled the same boundary.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (env-strip launch discipline, never-guess-at-facts,
  m-43/m-44 layer+denominator discipline, bare-number commit rule, stage-then-commit
  discipline); #1717 transcript doc + 09-12 prog log (harness shape, exact wrapper
  pattern for `served`); 09-01 prog log (pre-fix anecdote, harness precedent);
  `test_source_failed_composition_1717.py` (registry/composition contract, confirms
  reading of the code).
- **Loaded but not referenced**: memory index (MEMORY.md).
- **Wanted but not found**: nothing.

## Wrap
- Verified how: 30/30 completions' raw JSON quoted verbatim into the deliverable;
  rate computed by re-reading every reply against the stated scoring rule (not a
  keyword count); layer = in-process floor compose (real LLM, real keys), not the HTTP
  stack, not a delivered production turn; denominator = 30 calls, split 20/10 across
  N=1/2-flag and 10/10/5/5 across the two providers, stated per cell throughout.
- Commit: measurement doc + this log on `claude/lead-cycle`, staged in one call and
  committed bare in the next (mailbox-adjacent discipline applied generally), referencing
  1772 without a closing keyword adjacent to the number. NOT pushed per task instruction.
- No production code touched — `services/intent_service/conversational_floor.py` was
  read only, never edited. `ruff format --check` not run (no `.py` files under
  version control were modified — the two scratchpad probe scripts are outside the repo
  in the session scratchpad directory, not committed).
