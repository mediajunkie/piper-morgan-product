# Session Log — Coding Agent (prog) — 2026-09-15 12:30 PDT

- **Role**: Coding Agent (prog), deployed by Lead Developer
- **Model**: claude-opus-5 (Claude Code)
- **Worktree**: `/Users/xian/Development/piper-morgan-worktrees/lead` (Lead's Model-A worktree)
- **Branch**: `claude/lead-cycle`
- **Task**: Fix #1816 (consent boundary fails OPEN) + #1815 Gap 2 (the closed state must refuse,
  not degrade), per Arch's ruling memo of 2026-09-15.

## Inputs read

- `gh issue view 1816` — the defect: `KeychainService.get_api_key` swallows `Exception` → `None`;
  `provider_selection.py:78` `return list(all_configured)` on falsy `raw` → fail-OPEN.
- `gh issue view 1815 --comments` — Gap 2, plus Lead's inversion comment.
- `mailboxes/lead/read/ruling-arch-to-lead-...-1816-fix-the-CONSENT-reader-not-the-credential-primitive-...md`
  — four rulings (consent reader not the primitive; honest-empty family framing; refuse-don't-degrade;
  date the consent-from-key-presence inference).
- `mailboxes/lead/read/correct-cxo-to-arch-lead-...-my-copy-does-not-cover-a-consent-read-failure-2026-09-15.md`
  — CXO corrects Arch's "my copy already covers it": it does not, and CXO supplies the string this
  state actually needs.

## Work log

- Fire 1 (12:30) — session start, read 1816/1815/both memos, mapped the call graph.
- Fire 1 (12:36) — **RED-FIRST captured**, both halves, with a REAL `KeychainService` (macOS
  keyring live, `_db_store=None`, `_no_secure_store=None`), not an injected double:

  ```
  --- A: real KeychainService, underlying keyring RAISES (#1816) ---
    backend in play: _db_store=None _no_secure_store=None
    KeychainService.get_api_key(...) -> None   (the swallow, :308-311)
    resolve_authorized_providers -> ['anthropic', 'openai']
    🔴 FAIL-OPEN: an unreadable consent list returned the FULL configured set.

  --- B: consent-read failure at the LLM client (#1815 Gap 2) ---
    _complete_raw -> 'served'; providers attempted: ['anthropic']
    🔴 DEGRADED: the consent-read failure was swallowed at clients.py and the turn was
       served by the operator's own initialized client.
  ```

  B is the part the static reading did not predict: the closed state wasn't merely narrowing
  to a server default, it was being *swallowed at `clients.py:364`* by
  `except (ValueError, Exception)` and re-selected as "whichever client is initialized".

- Fire 1 (12:50) — **implemented.** Post-fix, same probe: A raises `ConsentUnreadableError`;
  B raises with `attempted: []`. `get_api_key` still returns `None` — primitive untouched,
  as ruled.
  - `keychain_service.py`: `SecretProvenance` (PRESENT/VERIFIED_ABSENT/STORE_FAILED) +
    frozen `SecretRead` + `read_secret()`. `_fetch_from_store()` extracted so the two
    readers share *where the bytes come from* while keeping two deliberately different
    failure contracts. `get_api_key`'s contract byte-identical.
  - `provider_selection.py`: `resolve_authorized_providers` raises `ConsentUnreadableError`
    on STORE_FAILED; `server_default` **removed** from the signature (the abolished concept
    is gone from the API, not just unused); per-user slot failure does NOT fall through to
    the global slot; dated inference comment at the verified-absent branch.
  - `request_key.py`: `ConsentUnreadableError(LLMKeyRequiredError)`.
  - `clients.py`: `except LLMKeyRequiredError: raise` before the blanket selection handler.
  - `conversational_floor.py`: `FLOOR_FALLBACK_CONSENT_UNREADABLE` (CXO's copy) + type-based
    classification branch.
  - `intent_service.py`: re-raise the family instead of wrapping into `IntentProcessingError`.
  - `web/api/routes/intent.py`: `_create_consent_unreadable_response` + route branch.

- Fire 1 (12:55) — **one real design bug caught by the guard rails, not by me.** My first
  capability detection was `getattr(kc, "read_secret", None)`. `Mock(spec=KeychainService)`
  grows that attribute the moment it exists on the class, returns a `Mock`, and
  `Mock.store_failed` is truthy — so nine #1814/#1815 pins started refusing every turn.
  That is not a test artifact: `keychain=` is an injection point, and any partial stand-in
  would have hit it in production. Fixed by validating the RESULT (`isinstance(read,
  SecretRead)`) instead of probing for the name, with the reasoning in the docstring.

- Fire 1 (13:00) — verification complete; `decisions.log` appended; #1817 filed as the
  dated inference's invalidation trigger (Arch ruling §4).

## Verification

**Verified how:** commands run this session, in this worktree, on this diff.

| Check | Result |
|---|---|
| Red-first probe (real keychain, both halves) | fail-OPEN + operator-client degradation reproduced, then both green |
| `run-sweep.sh unit` (llm, configuration, auth, security, infrastructure, intent, intent_service, web) | **5448 passed**, 3 deselected |
| new file `test_consent_read_provenance_1816.py` | **23 passed** |
| guard rails #1807/#1320/#1814/#1815/#1343 + copy pins | **143 passed** (in the above) |
| `run-sweep.sh ratchets` | **58 passed**; mypy gate (`venv-mypy-gate/`) **all 24 codes at ceiling**, total=1341 |
| `run-sweep.sh smoke` | **529 passed**, 1 skipped (was 525 — +4 new smoke pins) |
| `ruffenv2/bin/ruff format --check .` | 1768 files already formatted |
| `ruff check` on all touched modules | All checks passed |

**Layer measured (m-43):** the real keychain→consent-resolver seam (where the defect lived),
plus the config-service, LLM-client, intent-service and HTTP-route seams. Not a unit test with
an injected keychain — that measures the other branch and reports clean, which is precisely
what let this survive.

**Denominator (m-44):** one consent slot (`authorized_llm_providers`) on one resolver, plus
the four seams above. **Not** covered: the `default_llm_provider` slot's three `except`
branches in the same module (selection, not consent — cannot widen access); other
`get_api_key` consumers for the same absence-means-permission shape (unaudited, plausible);
a live billable vendor call; Slack/MCP entry points, which do not route through `/intent`.

## Discovered work

- **#1817 filed** — invalidation trigger for the consent-from-key-presence inference.
- **Flagged, not fixed**: the copy shipped is CXO's verbatim string from their 2026-09-15
  memo, which is a *proposal*, not copy from a shipped surface. The two `suggestions` strings
  on the route response are phrases lifted from that same sentence. CXO should confirm.
- **Not audited** (named in the test's denominator): other `KeychainService.get_api_key`
  consumers for the same absence-means-permission shape. #1816's own "Verified how" flagged
  this too; it remains open.

## Memory & briefing surfaces referenced this session

- **Referenced** — `CLAUDE.md` (commit discipline: stage-then-bare-commit; silent-death/ratchet
  norms; "Verified how" as a required field; m-43/m-44 layer-and-denominator framing, which
  shaped both the test docstring and this log). `scripts/run-sweep.sh` header comments (why the
  mypy gate must run under `venv-mypy-gate/`, not the dev venv — I would otherwise have used
  `venv/`). Arch's ruling memo and CXO's correction memo (the specification).
- **Loaded but not referenced** — the mailbox DIRECTORY, the duty-cycle/worktree sections of
  CLAUDE.md.
- **Wanted but not found** — a canonical list of which `KeychainService.get_api_key` callers
  are consent-shaped vs credential-shaped. #1816 names the gap; nothing enumerates it, so
  "are there more of these?" is still an unanswered question rather than a bounded one.

