# PPM session log — 2026-09-15

**Role**: Principal Product Manager (PPM) · slug `ppm-code` · **Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A) · branch `claude/ppm-cycle`
**Cron**: `52 6,9,12,15,18,21` · job `e61f6248` (re-armed 09-14 22:22 STOP; unchanged today)

### 07:22 START — a fast overnight sequence: the invite re-held and cleared before I woke to it

**Heartbeat + sync**: clean, one cron job (`e61f6248`), 0 ahead/0 behind. Prior day's log confirmed
carrying its `DAY-CLOSED` sentinel.

**Mailbox had eight items, all closing out an overnight emergency that resolved before I got to
it.** Last night's `#1810` fix (deleting the global key-write) had a sequencing hazard nobody
caught: it removed the write before verifying the resolver could read a per-user key at all, so a
BYOC user's own stored key was never used — and the invite's own onboarding condition (configure
your key first) walked the first external tester straight into a false "not configured" wall.
Lead, Arch, and Exec each independently named their own piece of the sequencing gap before being
asked. HOST re-held the invite immediately on waking to it and required a sharper bar this time
(an actual BYOC query succeeding on a stored key, not just an empty-slot check). Fixed and
confirmed the same morning — deployed v109, observed via a real turn that hit the network (a 401
from a throwaway key, proving resolution actually happened) — invite blocker gone. Two residuals
filed as `#1815`. CXO separately found the fix exposed a latent copy conflation (a failed key
lookup and a verified-absent key produce the same exception, so existing copy can only assert the
stronger claim) — not live today.

**Triaged 2 issues**: `#1814` (closed, missing from the board — same drift shape as this whole
week) and `#1815` (the two residuals), both folded into epic 2 (Security/tenancy, reopened
yesterday). Updated the epic-order file with the full sequence — worth keeping as a record, not
just an outcome, since it's the same discipline from last night (name the layer, no substituted
observations, the bar-setter doesn't lower their own bar) holding a second time on a tighter clock.

Everything else this fire was cc-only, correctly not mine to touch: CIO's write-up of PM's
dispatch-tier ruling into both durable homes (audit-cascade + CLAUDE.md), and CXO's pre-registered
FTUX/keyless-copy properties ahead of Lead's transcripts landing.

No reply from PM yet on last night's epics-9/10 choice — noting for later fires, not chasing.

Committed and pushed the epic-order file. Mail sent in one 16-path call — landed clean, no
warnings, verified via `git cat-file -e` on all 16 paths.

### 10:22 WORK — the tenancy family grows again: #1816, a consent boundary failing open

**Heartbeat + sync**: clean, one cron job (`e61f6248`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
56 not done (33 Sprint Backlog, 3 In Progress, 6 In Review, 14 Product Backlog); 1172 done` — **0
unmilestoned**.

**Five mail items, another chapter of the same tenancy investigation.** Lead confirmed CXO's
pre-registered layer-match table held exactly (the FTUX render-on-deployed-path claim closes; the
production-flag and real-cold-user claims don't) — worth noting as the discipline actually working:
a table published before the evidence arrives can't be rationalized toward after the fact. While
investigating `#1815`'s undecided Gap 2, Lead inverted a premise from their own brief: the consent
fail-closed branch doesn't fire in production at all — `KeychainService.get_api_key`'s broad
exception swallow means a real keyring failure falls through to "everything authorized." Arch
confirmed by reading both sides of the call and ruled four things at once: fix belongs at the
consent reader, never `#1711`'s sound credential-read contract; the shape is the honest-empty
family one layer down (same `None`-does-two-jobs defect as `verified_empty`/`source_failed`
collapsing); `#1815`'s Gap 2 rules together with it — refuse the turn, don't degrade to the server
key PM already abolished via `#1812`; and the consent-from-key-presence inference stays acceptable
today only as a *dated* assumption with a named invalidation trigger, not a design. CXO caught a
real defect in Arch's own ruling text before anyone built to it — "CXO's copy work already covers
the user-visible state" is false, existing copy would tell a consent-read-failure user to add a
key they already have, the same absence-vs-failure conflation from this morning's `#1814` thread
arriving a third time. HOST checked `#1816` directly against Janne's own path (rather than assume
unrelated) before lifting the invite again.

**Only PPM action**: `#1816` was already board-present with MVP milestone but stuck at Product
Backlog/no-sprint despite being an actively-investigated epic-2 item — bumped to Sprint Backlog/
Beta Blockers to match `#1809`/`#1815`. Updated epic 2's entry with the full four-part ruling and
added explicit item-list entries for `#1814`/`#1815`/`#1816` (16 items now, 9 closed). No PPM
ruling needed anywhere in this thread — every call was Lead/Arch/CXO/HOST's own domain.

Still no reply from PM on last night's epics-9/10 choice — third fire watching, not chasing.

Committed and pushed the epic-order file. Mail sent in one 10-path call — landed clean, no
warnings.

### 13:22 WORK — consent fail-open thread closes same-day; #1772 gets a real measurement

**Heartbeat + sync**: clean, one cron job (`e61f6248`), 0 ahead/0 behind. `sprint-truth.py`: `MVP:
54 not done (32 Sprint Backlog, 3 In Progress, 6 In Review, 13 Product Backlog); 1176 done` — **1
unmilestoned**.

**Two threads, both resolved by the time I read them.** First: Lead shipped the `#1816` fix live
(v111) using CXO's own proposed copy verbatim, ahead of formal ratification — told CXO directly
rather than let them find it, reasoning that a live fail-open boundary outranks unratified
wording. CXO confirmed the message with one clause cut (an instruction the user can't act on —
"if it keeps happening it's worth reporting" — is the same defect as a wrong instruction, just
quieter) and caught a second real bug in their own copy: a `suggestions` field held a sentence
fragment from CXO's own memo prose sitting in a slot that renders actionable chips, not text — the
#1108 failure, in CXO's own copy, located wrong rather than transcribed wrong. Arch conceded a
third instance of the same cross-reference failure mode in three days (asserting what another
artifact covers from memory of having read it once) and proposed a mechanical fix for their own
future rulings: quote the artifact inline or mark the claim unverified.

**Second: the N=1 scope-leak anecdote CXO deliberately declined to over-claim from got measured
properly.** Lead ran n=10 per cell instead of n=1: 50% leak rate on claude-sonnet — production's
actual default provider — and 0% on gpt-4o. The N≥2 aggregate path stays clean on both. Also found
`'calendar'` isn't a registered `SOURCE_FAILED_FLAGS` check at all — a different failure shape than
originally filed, since the model is naming a data category with no flag rather than misreading
which flags were set.

**Triaged the 1 unmilestoned issue** (`#1817`, the invalidation-trigger tripwire Arch's `#1816`
ruling required in §4) and, while updating epic 2, found `#1815`/`#1816` had both closed since this
morning's check — updated all three plus epic 5's `#1772` entry with the real measurement data. No
PPM ruling needed anywhere this fire.

Still no reply from PM on the epics-9/10 choice — fourth fire watching, not chasing.

Committed and pushed the epic-order file. Mail sent in one 8-path call — landed clean, no warnings.

### 16:22 WORK — PDR-006's held wording-proposal finally unblocked, by PA

**Heartbeat + sync**: clean, one cron job, 0 ahead/0 behind. Mail had 5 items; 4 were cc-only
(Lead's `#1814`-shaped condition-check finding, Arch's five-cause classifier-bucket read, CXO's
four-bucket copy work — all Lead/Arch/CXO's own domain, no PPM ruling needed) and one was a direct
memo from PA.

**The direct one was real PPM work.** PA re-checked PDR-006's ChatGPT-equivalence success
criterion against `#1462`'s own body rather than trusting the doc's current wording, and found the
"untested mechanism" premise my held wording-proposal had been waiting on no longer holds — the
honest-decline-survives-recomposition mechanism was actually tested 2026-08-02, 6/6, via a
failure-shaped payload framing (not a protocol-level MCP error), and the result never propagated
into the doc. Read the actual criterion text in `docs/internal/product/pdr/
PDR-006-hosted-mcp-plugin-distribution.md` before touching it. Revised it directly: moved from
"under active question" to "provisionally confirmed, pending one deployed-host retest," and
narrowed "equivalent core capabilities" to "honest-equivalent" — naming the specific property PA's
test actually confirmed rather than implying a fuller parity nobody has tested. Added a changelog
row. Does not close Phase-0-gate tracking; the deployed-host retest against a live
`mcp.pipermorgan.ai` is still owed, same caveat PA and CXO both named independently.

Committed (`1373049`) and pushed clean, no race. Thanked PA directly, cc CXO, explaining the
wording choice and what it does/doesn't settle. Sent in one 10-path batch (the memo + 2 inbox
copies + 4 read-mail drains, old inbox path and new read path together) — verified via `git
cat-file -e` against `origin/main` rather than trusting a local `ls`: PA OK, CXO OK, all 4 old
inbox paths correctly cleared.

Still no reply from PM on the epics-9/10 choice — fifth fire watching, not chasing. No new issue
number yet for the classifier-bucket-collapse issue Lead mentioned intending to file.

### 19:22 WORK — classifier-bucket-split thread resolves cleanly; quiet fire otherwise

**Heartbeat + sync**: clean, one cron job, 0 ahead/0 behind. Prior day's log (2026-09-14) verified
carrying its DAY-CLOSED sentinel before starting. `sprint-truth.py`: `MVP: 55 not done (33 Sprint
Backlog, 3 In Progress, 6 In Review, 13 Product Backlog); 1176 done` — **0 unmilestoned**.

**All 3 mail items were the classifier-bucket-split thread continuing, cc-only, no PPM ruling
needed.** Lead refuted Arch's hypothesis that `#1814` was caused by the `"not initialized"` auth
bucket, checking the actual FTUX transcript rather than reasoning from the classifier alone —
`#1814` routed through `no_provider`, returning before the auth list is ever reached. Arch accepted
the refutation and caught the sharper point: the four-bucket split still stands on its own
criterion (the honest sentence differs), just not on an incident it didn't cause — a true
conclusion resting on a wrong reason is the error worth naming, since nobody revisits a conclusion
that holds and the bad reasoning underneath calcifies. CXO scored the real FTUX transcripts against
this morning's pre-registration: keyless refusal passed 4/4, but found two live copy defects
source-reading alone couldn't produce — the keyless-refusal string ("I can't run *this* without an
LLM key") is literally false ahead of a bare greeting, which needs no key at all since the greeting
handler is deterministic; and the "running with a default configuration" notice fires right after a
user successfully configures their own provider and key, asserting something about a store they
can't see that reads as contradicting what they just did. Also closed an 8-day-old gap in CXO's own
tracker: the FTUX interview flag is OFF in what a real deploy carries by a **PPM 2026-09-03 HOLD
ruling**, not unverified as CXO had tracked since 09-07 — this file's own prior ruling turned out to
be the answer.

Folded a tracking paragraph into epic 2 for visibility (no issue number yet — Lead hasn't filed the
classifier-bucket-split issue mentioned at 16:22; confirmed genuinely unfiled via this fire's
0-unmilestoned count, not assumed). No PPM ruling needed anywhere in the thread.

Still no reply from PM on the epics-9/10 choice — sixth fire watching, not chasing.

Committed and pushed the epic-order file + carry-forward + this log entry together.

### 22:22 STOP — day closed clean, one board-add fix, cron re-armed

**Heartbeat + sync**: clean, one cron job (`e61f6248`), 0 ahead/0 behind. Mailbox empty.

**`sprint-truth.py` found one board-add drift**: `#1818` (`[ROUTING] Should a deterministic
greeting pass the keyless #1807 gate?`) carried MVP milestone but was absent from the project
board — the same `--milestone`-doesn't-board-add shape hit repeatedly this week, now on a fourth
issue (`#1772`/`#1785`/`#1807`/`#1818`). Fixed same-fire: `gh project item-add`, then Status set to
Product Backlog via the safe per-item mutation (verified against a small single-item GraphQL query
after the full-board re-pull hit the shared cohort-wide GraphQL burst throttle — confirmed via `gh
api rate_limit` showing full quota before treating it as the known shared throttle rather than a
real limit, per the prompt's own TOOL OUTAGES guidance). Folded `#1818` into epic 2's tracking
paragraph: a genuine, deliberately-unrushed design question (should a zero-cost deterministic
greeting pass the keyless gate?) split out of this evening's copy fix rather than decided under
pressure — CXO's corrected copy is already true either way, so nothing else waits on the ruling.
Needs CXO/Arch to decide, not PPM.

**Day summary, 2026-09-15 — a full six-fire day, every fire quiet on PPM rulings but not on
substance.** The day opened with an overnight #1810 sequencing hazard already resolved before
waking (a key-write deleted before its reader was verified, walking Janne's invite into a false
wall — fixed same-morning, v109) and closed with a design question deliberately deferred rather
than rushed (`#1818`). In between: `#1816`'s consent-fail-open fix shipped and closed same-day
(v111), `#1772` got upgraded from an N=1 anecdote to a real n=10 measurement (50%/0% leak split by
provider), PDR-006's stale "under active question" wording finally got unblocked by PA's direct
finding and revised to match the actually-tested mechanism, and the evening's classifier-bucket
thread produced a real methodological catch (Arch's own hypothesis refuted, then Arch naming the
sharper hazard: a true conclusion resting on a wrong reason survives longer than it should). No PPM
ruling was needed on any of the substantive threads today — Lead/Arch/CXO/HOST each owned their own
calls cleanly, self-reported plainly when wrong, and caught each other's defects before they
shipped. PPM's own actions were entirely bookkeeping: four board-add fixes across the week's drift
shape, epic-order currency, one direct wording revision on a held item (PDR-006), and consistent
mailbox triage. **PM's reply on the epics-9/10 choice, offered 2026-09-14 evening, still hasn't
landed after six fires of watching** — noted plainly, not escalated; nothing is blocked on it.

**Cron re-armed** `e61f6248` → **`c6df1cc8`**, `CronList`-verified exactly one job. Prompt updated:
epic 2's item count (13→18, reflecting today's `#1814`/`#1815`/`#1816` closures plus `#1818`'s
addition), the epics-9/10 watch line's date bumped to reflect six fires of no reply, a new
`#1818` watch line added (drop once CXO/Arch rule), and a TOOL OUTAGES addendum for the GraphQL
rate-limit pattern hit this fire folded into the COUNTS section rather than left as a one-off.

**DAY-CLOSED**
