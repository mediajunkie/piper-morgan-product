# Session Log — 2026-09-15 07:07 — Coding Agent (prog) — Claude Code

**Role**: Coding Agent (prog), delegated by Lead Developer
**Model**: Sonnet 5 (claude-sonnet-5)
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (claude/lead-cycle)
**Task**: Re-run the FTUX observation to prove #1814 is actually fixed — by OBSERVATION, not test pin.
Gates an external alpha tester's invite (currently held). #1814 existed while the #1810 unit pins were
all green — pins passed, a real user hit a wall — so the proof that matters is a driven flow. Reusing
yesterday's harness exactly (`dev/2026/09/15/2026-09-15-0630-prog-code-log.md`,
`dev/2026/09/14/2026-09-14-2148-prog-code-log.md`): real `main.py` subprocess, `ANTHROPIC_*` stripped,
real local Postgres 5433, ephemeral port, `PIPER_CREDENTIAL_STORE=db`, throwaway invite token + account,
cleanup verified by count.

Fix under test: `a3404c609` ("fix(llm): make credential resolution principal-aware so a BYOC user's own
key is actually used (1814)") — confirmed via `git merge-base --is-ancestor a3404c609 HEAD` to be an
ancestor of this worktree's HEAD (`1317a2dc2`), which is itself 0 commits behind `origin/main`.
`LLMConfigService.get_api_key` now has a Priority 0 that reads the same `anthropic_client_for_request`
ContextVar the Anthropic client reads; `_init_clients` deliberately does NOT (to avoid baking a user's
key into the long-lived singleton).

Target observation: Case 2 from yesterday (signed-in user, own stored key, "What can you do?" —
floor-routes to LLM). Yesterday: `FLOOR_FALLBACK_NO_PROVIDER` — the bug. Today: expect the request to
reach the provider and come back with a genuine auth/invalid-key error (PASS — proves the user's key was
resolved/selected/sent), since the throwaway key is syntactically valid but not billable/real.

## Pre-run investigation

- Read `services/ui_messages/user_friendly_errors.py` in full. **Correction to the task brief's
  framing, confirmed via `git log -p`**: the recovery text quoted in the task ("Top up the key's
  billing, or replace it with a funded one under Settings → LLM API Keys.") is the **`insufficient_quota`
  entry's** recovery (rewritten 2026-09-14, commit `ce8fd6f90`), not the **`invalid_api_key`** entry's.
  The `invalid_api_key` entry (pattern `invalid_api_key|incorrect api key|invalid.*x-api-key|
  authentication_error|invalid api key provided`) has recovery "Check or replace it under Settings →
  LLM API Keys." and has been unchanged since 2026-07-14 (`c65eb2dcb`). Since our throwaway key is
  syntactically valid but not a real Anthropic credential, the live Anthropic API will return an
  **authentication error** (401, `authentication_error`/`invalid x-api-key`), which matches the
  `invalid_api_key` pattern — NOT the quota pattern. Reporting this distinction explicitly rather than
  forcing the observed copy to match the brief's quote (never-guess-at-facts; anti-sycophancy).
  Confirmed the corrected quota-recovery text is real and present, for completeness, even though it's
  not the pattern this case will hit.
- Read `web/api/routes/intent.py:111-167` (`_extract_degradation_message`) — unwraps
  `error.details["original_error"]` first (needed for wrapped `APIError`s), lowercases, routes any
  string containing "llm"/"openai"/"anthropic"/"api key"/"api" through
  `UserFriendlyErrorService.make_error_user_friendly`, concatenates `"{message} {recovery}"`. This is
  the exact function whose output becomes the degradation response the user sees.
- Confirmed docker services (`piper-postgres`, `piper-redis`, `piper-chromadb`, `piper-ghmcp`) healthy,
  up 4 weeks. Confirmed the pre-existing dev server (PID 30735) listening on 8001, untouched.
- Worktree freshness: `git fetch origin main` → 0 commits behind at run start.

## Live-harness run (07:08–07:13)

Baseline (before touching anything): `users` 2215 total / 0 `setup_complete=true` / `secure_credentials`
0 rows / `invite_tokens` 235 rows.

Started `main.py --no-browser` subprocess, ephemeral `PIPER_PORT=60696`, `ANTHROPIC_*` stripped,
`POSTGRES_PORT=5433`, `PIPER_CREDENTIAL_STORE=db`, `ENCRYPTION_MASTER_KEY` = repo-standard 43-char test
value. Healthy after ~3s. Server log confirmed `backend=EncryptedDBCredentialStore`.

**Recovered incident, same class as last night's, caught before it mattered**: first `create-user`
attempt used a shell variable named `USERNAME` for the throwaway username. In zsh this is a read-only
special parameter bound to the real macOS account — the assignment silently no-op'd and the request that
actually reached the server carried `username=xian`. Caught immediately: the response echoed back
`"username":"xian"` in a 422 validation error (missing `password_confirm`), before any user row was
created. Verified `SELECT count(*) FROM users WHERE username='xian'` = 0 both before and after. Re-ran
using `TESTUSER` instead. No app-side effect occurred; recorded here as a repeat process note (same
hazard yesterday's log flagged), not a new discovery.

**User A (keyed)**: minted invite token via direct `INSERT INTO invite_tokens`. `create-user` → 200,
`user_id=1df455e5-ed38-4a76-bc53-8fdf36a068d6`. `/complete` with `default_llm_provider=anthropic` + a
validator-shaped 110-char `sk-ant-...` throwaway key (same generator shape as the #1810 unit pin,
`sk-ant-` prefix + 100–110 chars total) → 200. Logged in via `/api/v1/auth/login` (Bearer JWT, not
cookie — confirmed `intent.py` accepts either). Turn 1 ("Hi there!") → deterministic greeting + ADR-075
notice, **verbatim identical to yesterday's Case 1**. Turn 2 ("What can you do?") — the case-2 target:

**Verbatim reply**:
> "I can't generate responses right now because my LLM connection isn't working. This blocks most of my
> core functionality. The issue could be an expired API key, a deprecated model, or a configuration
> problem. Please check your LLM API key in Settings — once that's resolved, I'll be back to full
> capability."

Server log for this request (three consecutive lines, `services.llm.clients` then
`services.intent_service.conversational_floor`):
```
{"provider": "anthropic", "task_type": "conversation", "error": "Error code: 401 - {'type': 'error',
'error': {'type': 'authentication_error', 'message': 'API key is invalid.'}, 'request_id': None}",
"event": "llm_primary_failed", ...}
{"event": "All LLM providers failed: [\"anthropic: Error code: 401 - {'type': 'error', 'error': {'type':
'authentication_error', 'message': 'API key is invalid.'}, 'request_id': None}\"]", "level": "error", ...}
{"error": "All configured LLM providers failed. Details: anthropic: Error code: 401 - ...", "error_type":
"auth", "event": "conversational_floor_error", ...}
```

**This is the PASS, and here is exactly what it proves**: the request reached the real Anthropic API
with `provider=anthropic` and got a genuine 401 `authentication_error` ("API key is invalid.") back — not
the pre-flight `FLOOR_FALLBACK_NO_PROVIDER` gate failure from yesterday, which never touched the network.
`services.intent_service.conversational_floor._classify_llm_error` classified it `error_type="auth"` (not
`"no_provider"`), which selects `FLOOR_FALLBACK_AUTH` — the reply text above — over
`FLOOR_FALLBACK_NO_PROVIDER" ("I don't have an LLM provider configured yet...", yesterday's bug text).
The user's own stored key was resolved, selected, and sent. #1814 is fixed at exactly the layer this
observation can see.

**Mechanism correction to the task brief**: the invalid-key copy the user actually sees on this floor-
routed path is `ConversationalFloor`'s own `FLOOR_FALLBACK_AUTH` constant
(`services/intent_service/conversational_floor.py:593-598`), selected by `_classify_llm_error`'s
"auth" branch (:626-640, matches on "401"/"authentication"/"invalid api key"/etc.) — **not**
`user_friendly_errors.py`'s `invalid_api_key` entry, and not the quota entry either. The floor catches
its own LLM exception and returns a `FloorResponse` directly; it never raises up to
`web/api/routes/intent.py`'s top-level handler, so `_extract_degradation_message` /
`UserFriendlyErrorService.make_error_user_friendly` is never invoked for this call. The
`user_friendly_errors.py` entries are real and correctly current (see Pre-run investigation above) but
govern a different call surface (exceptions that propagate to the route's top-level handler), not this
one. Reporting this precisely rather than describing the `user_friendly_errors.py` copy as what the user
saw here, which would be inaccurate.

Cleaned up User A fully (global `require_setup_incomplete` gate blocks a second `create-user`/`complete`
while any user has `setup_complete=true`, confirmed structurally last night — had to reach 0 again).
Keychain: `delete_api_key` for anthropic/authorized_llm_providers/default_llm_provider (3/3 `True`;
openai `False` — correctly, never stored) + `delete_cli_token` (`True`). DB, in FK-safe order: `conversation_turns` (2 — the two turns from Case 2's session, deleted explicitly
before their parent row since `conversation_turns` cascade-deletes only on `conversations.id`, and I
targeted by `conversation_id IN (SELECT id FROM conversations WHERE user_id=...)` to avoid relying on
ON DELETE CASCADE), `conversations` (1), `personalization_contexts` (1 — the ADR-075 seeded-default row
from Turn 1),
`learned_patterns` (2), `audit_logs` (1), `user_api_keys` (1), `invite_tokens` (1), `users` (1) — no FK
errors, `users` deleted last successfully.

**User B (keyless)**: minted a second token. `create-user` → 200, `user_id=0cb32c66-e420-4fe0-bb2c-
0f58132b725c`. `/complete` called with **only** `user_id` (both key fields `Optional`, same supported
skip-the-key path as yesterday). Logged in. Turn 1 ("Hi there!"):

**Verbatim reply**: "I can't run this without an LLM key of your own — Piper doesn't bill anyone else's
account. Add your Anthropic API key in Settings and I'll pick right back up." —
`clarification_type=user_key_required`, matching yesterday's Case 3 **verbatim**. Server log:
`intent_user_key_required_1807`, no floor/LLM log lines at all — confirms the #1807 gate still fires
before classification/LLM, unchanged by the #1814 fix (no-regress). `personalization_contexts`=0 (ADR-075
notice correctly never seeded — `process_intent` never reached), `conversations`=1 (auto-created before
the gate, same as yesterday), `user_api_keys`=0 (never stored).

## Cleanup + verification by count (07:11–07:13)

- User B: keychain — only `cli_token` existed (`True`); anthropic/openai/authorized_llm_providers/
  default_llm_provider all `False` (correctly, never stored). DB: `conversations` (1),
  `invite_tokens` (1), `users` (1); all other tables 0 (nothing to delete).
- Re-verified both UUIDs return 0 across `users`/`user_api_keys`/`audit_logs`/`learned_patterns`/
  `personalization_contexts`/`conversations`/`invite_tokens` after cleanup.
- Global counts back to exact baseline: `users` 2215 / `setup_complete=true` 0 / `secure_credentials` 0 /
  `invite_tokens` 235 (both minted tokens confirmed 0 remaining for their specific values).
- Ephemeral server (PID 14479, port 60696) killed; confirmed no `main.py --no-browser` process remains.
  Original dev server (PID 30735, port 8001) confirmed still listening throughout and afterward,
  untouched.
- Scratchpad temp files (`ftux_*` under the session scratchpad) removed. One `rm` call silently no-op'd
  because it referenced a shell variable (`$SCRATCH`) from a prior, separate Bash call — shell state
  doesn't persist between tool calls in this harness. Caught by re-listing the directory, fixed with an
  explicit full path in the next call. No production or shared-state impact either way (`-f` suppressed
  the error rather than doing anything destructive).

## Layer statement

This observes the **deployed code path** (`a3404c609`, ancestor of `origin/main` at `1317a2dc2`, 0
commits behind at run time — deployed as v109) against a **prod-matching credential backend**
(`EncryptedDBCredentialStore`, forced via `PIPER_CREDENTIAL_STORE=db`) on a **local server process**
(`main.py` subprocess) driven through the **real HTTP routes** end to end (`create-user`, `complete`,
`login`, `intent`) with a **real local Postgres** (the shared dev DB on 5433). **Not a production
first-contact run** — no fly.dev, no prod OS-keyring auto-detection, no production data, no billing-
capable Anthropic key (by design — the task explicitly does not require one; the throwaway key's
rejection by the real API is itself the proof). All three cases produced genuine, complete replies this
time — unlike yesterday, nothing here needed to be flagged as "could not complete."

## Discovered work

None filed. The repeated `$USERNAME`/zsh-special-parameter and cross-call-shell-variable hazards are
process notes (both already known classes — the first documented in last night's log, the second a
generalization of it), not new product defects.

## Memory & briefing surfaces referenced this session

- **Referenced**: CLAUDE.md (env-stripped launch idiom; never-guess-at-facts — specifically for not
  forcing the observed copy to match the task brief's misattributed quote; anti-sycophancy — reporting
  the mechanism correction plainly; pause-before-irreversible; m-43/m-44 layer+denominator discipline);
  yesterday's two session logs (harness method template, `$USERNAME` hazard warning that then recurred
  and was caught the same way); #1814 issue/commit `a3404c609` (fix mechanism, read in full before
  designing the observation); `services/intent_service/conversational_floor.py` and
  `services/ui_messages/user_friendly_errors.py` (read in full to correctly attribute which mechanism
  produced the observed copy).
- **Loaded but not referenced**: memory index entries broadly; other agents' duty-cycle context.
- **Wanted but not found**: nothing — self-contained observation task, all context in-repo.

## Sign-off

Per the task instruction, this session does NOT push to `origin/main` — committed locally on
`claude/lead-cycle` only (session log + transcript file), staged in one call and committed bare in the
next per mailbox/commit-shape discipline (this isn't a mailbox path, but the same two-step shape is
harmless and consistent). `git log --oneline origin/main..HEAD` will show these commits pending push;
intentional per the task brief, not an omission.
