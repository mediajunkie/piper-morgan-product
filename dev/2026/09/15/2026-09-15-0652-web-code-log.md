# Session Log: 2026-09-15-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Code (Opus 5) — moved off Fable 5 on 2026-09-14 (access ceiling).
**Date**: Tuesday, September 15, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/14/2026-09-14-0932-web-code-log.md` — **DAY-CLOSED: 2026-09-14** (opened on a real 11.7h dark gap — HOST's freeze-check's first true positive — self-reported as the Fable→Opus session restart dropping a cron firing while the job object survived; then built the WYSIWYG Source/Split/Preview toggle in the very session its quality-banking had named as the trigger, browser-verified and deployed).

## Session Objectives

1. Normal duty-cycle drain (mail → queue → standing items → idle).
2. Open: website#35 (PM's navigation answer), Vercel Q1 (access-blocked), integration-reveals-all workDate (PM recall).

## Work Log

### 06:52 AM - Session Start

- `CronList`: exactly one job (`97871a22`) — survived overnight, and today's 06:22
  fire delivered, so yesterday's model-switch gap did not recur.
- Step 0: yesterday's log carries `<!-- DAY-CLOSED: 2026-09-14 -->` — clean close,
  no self-heal needed.
- Fingerprint both worktrees: `web` / `claude/web-cycle` — correct. Both synced clean.
- Cohort-freeze: `rc=0`, INSUFFICIENT-SCHEDULE (overnight window — expected, not a signal).
- Created this session log.
- **Mail loop**: genuinely empty.
- **Task loop**: issue queue is website#35 alone, still holding on PM's
  back/forward-navigation answer (a pending PM question — holds only that
  thread). Standing items unchanged: obs-pass, site walkthrough, Buttondown
  PM-gated; Vercel Q1 access-blocked. Nothing unblocked.
- Emitted the START heartbeat. Returning to idle.

### 09:52 AM — Fire (cron 09:22 slot), genuinely quiet

Both worktrees synced clean, freeze check clean (full cohort emitting), mail
empty, issue queue #35 alone, standing items unchanged. Heartbeat
self-suppressed. Returning to idle.

### 10:2x AM — P0: text typed BACKWARDS in the compose editor (my regression, fixed)

PM reported typing "odd" produced "ddo" in the blog-editing interface. **My own
regression from yesterday's `bb579b5`** (the Source/Split/Preview toggle) — one
day old, in PM's primary authoring tool.

**Mechanism**: the caret-restore `useLayoutEffect` declared deps
`[bodyView, body]` while *its own comment* said "must fire on remount, not on
every keystroke." Every keystroke changed `body` → effect re-ran → restored the
caret to the **pre-keystroke** position held in `caretRef` → the next character
landed in front of the last. The code and the comment directly contradicted each
other and I shipped it; the comment was the correct intent.

**Fix** (`45ab4a9`): guard on a `restoredViewRef` rather than the deps array.
`body` must stay in the deps (the preview scroll math reads it), so a
view-change check — not dep removal — is what enforces the intent.

**Verified how**: Playwright driving a real dev server against an **isolated temp
`PIPER_PRODUCT_ROOT`** (copy of the drafts tree — no real draft reachable by any
test action). Layer is **real browser keystrokes**, not compile or render.
4/4 pass. Then **reverted the guard and re-ran to prove the test can actually
detect the bug** — reproduced PM's exact symptom (`"odd"→"ddo"`,
`"hello"→"olleh"`, caret pinned at origin) — then restored (sha-verified) and
re-confirmed 4/4. Denominator: 4 cases — type-at-start, type-mid-document,
toggle caret round-trip (the `bb579b5` feature, confirmed still intact), and
typing after a toggle. `tsc` clean for this file (7 pre-existing strict-null
errors elsewhere, 0 here); production build clean.

`bb579b5..45ab4a9` is a direct parent-child pair, so the regression window is
exactly those two commits — no other work implicated. Deploy poll running.

**The lesson worth keeping**: yesterday I browser-verified the *feature* (caret
survives a toggle — which passes, and still passes) and never tested the
*ordinary* path the feature shares state with. A test of the new behavior is not
a test of the behavior it perturbs. m-43 layer-naming would not have caught this;
"what else touches this state" is the question that would have.

**Deploy**: `45ab4a9` Vercel state=**success**. Fix is live.

**Blast radius — checked, not assumed.** Corrupted text was actually *saved to
disk*, not just mistyped on screen. `git log --all` for "edit via admin UI"
commits since `bb579b5` deployed: **exactly 2 commits, exactly 1 file** —
`docs/public/comms/drafts/the-bug-that-was-misdiagnosed-twice.md`. That is the
whole denominator. PM had hand-repaired `ddo`→`odd` in `9e8f969e9`; two
artifacts survived — a doubled space (`"the  odd part"`) and `Docgg'` (was
`Docs'`). Both restored against the pre-corruption text in `a1972e53b`'s parent
rather than guessed; damage repair only, no voice or content edits.

**Second lesson, and the one I'd have missed**: my first instinct was to report
"fixed and deployed" and stop. The bug corrupted *persisted* content, so the
fix doesn't undo the damage — a code fix and a data repair are separate
obligations, and shipping the first while the second sits on disk would have
left PM with a draft headed for publication carrying `Docgg'`. Ask "did this bug
write anything down?" before calling a fix complete.
