# Lead Developer session log — Tuesday 2026-09-15 (Claude Code, model: Opus)

Role: Lead Developer (lead-code). Worktree ~/Development/piper-morgan-worktrees/lead,
branch claude/lead-cycle (Model A). Cron 22689706 (17 6,9,12,15,18,21; rotated 09-13,
expires ~09-20, next rotation ~09-18).

## START (06:29) — synced (0 behind), inbox 5. Monday ended with the server-key phantom
ruled out of existence (PM), epic 2 reopened, #1810's clearing condition discharged by
observation, and the belt clean of unexplained reds.

- Fire 1 (06:31) — WORK. Inbox 5→0.
  🔴 **CXO CAUGHT A FALSE CLAIM OF MINE AND THEY'RE RIGHT.** I wrote "CXO's rider is satisfied
  by the same run." It wasn't. The run captured create-user 200 / complete 200 / key placement
  — the SETUP half. CXO's ask was the turn AFTER it. I asserted satisfaction because it looked
  like a free byproduct of a flow someone else was already driving, and **I never checked
  whether the lane captured it.** That is the described-is-not-running substitution this whole
  thread was fought over, committed by me in the sentence summarizing the thread.
  CXO generously called the mis-specification theirs; the claim of satisfaction was MINE to
  verify. Corrected on #1810 before it hardened, with the withdrawal SCOPED: the clearing-
  condition evidence stands on its own; only my rider sentence is withdrawn.
  **Lesson worth keeping: a claim that something was satisfied "along the way" is the easiest
  kind to make and the least likely to be checked — precisely because it costs nothing to say.**
  ✅ HOST LIFTED the hold (verified before ruling: gh issue view + merge-base, not just reading
  reports). Arch ruled the bar MET and owned that they'd tried to lower it. Invite goes out
  with one condition: Janne configures his own LLM key FIRST — defense-in-depth against #1809,
  which is still open. HOST will watch his first session rather than send and look away.
  NOW RUNNING: the FTUX observation as its OWN task with its own evidence — not folded into
  someone else's errand, which is how it got lost the first time. Captures the first chat turn
  after setup, what rides along, the once-per-account notice behavior, AND the keyless case,
  which is newly live: it's exactly what Janne gets if he follows the invite's order imperfectly.

- 06:47 — 🔴 **THE FTUX OBSERVATION CAUGHT A BLOCKER THAT LANDS ON JANNE — #1814.**
  `LLMConfigService.get_api_key()` takes NO user parameter and the module has ZERO references
  to UserAPIKeyService (verified myself, not relayed). It reads only the legacy global slot —
  which #1810 correctly stopped populating. **So a signed-in BYOC user's own key is stored and
  then never consulted**; they hit a false "not configured" wall (observed as
  FLOOR_FALLBACK_NO_PROVIDER on a plain "What can you do?").
  **That is exactly the state our invite instructs testers into.** Its one condition —
  configure your own key first — is the path into the wall.
  **I UNDER-CALLED THIS CLOSING #1810.** I wrote "removing the WRITE stops new clobbering; the
  READERS still resolve whatever is already there." Accurate, insufficient: with the slot
  correctly empty the readers resolve NOTHING. Stopping the write without teaching the reader
  converted a billing leak into a functionality wall — and the #1812 sequence I authored put
  the reader work behind CXO's copy. Right about the copy, wrong about the ordering risk. Mine.
  ASKED EXEC/HOST TO RE-HOLD the invite they'd just correctly lifted, on evidence of our own
  bug. Fix dispatched (top tier — it's a resolution-architecture question adjacent to the
  server-key ruling, and must not reintroduce a product-owned key).
  ⭐ **The lesson, and it's the whole point: this was caught by the observation CXO demanded
  and I falsely claimed was satisfied. Had CXO not corrected me, the first person to find it
  would have been Janne, live, on turn one.** The correction I took yesterday is the only
  reason this is a held invite instead of a bad first impression with an external tester.
  Also captured: the keyed greeting works and the ADR-075 notice fires once and does not
  repeat; the keyless refusal fires before classification regardless of message content.

- 07:07 — **#1814 FIXED + DEPLOYED v109** (health 200). Verified: new pin 6/6, the #1807+#1320
  guard rails 11/11, ratchets 58 + all 24 mypy codes at ceiling, smoke 524, ruff clean.
  Fix REUSED the existing per-request ContextVar rather than adding a second mechanism —
  get_api_key now reads the same var the Anthropic client reads, so **the gate and the consumer
  are structurally incapable of disagreeing, which is what the bug WAS.**
  ⭐ **The lane's best catch: _init_clients deliberately does NOT read it.** LLMClient() is
  constructed lazily INSIDE requests at four sites, so a request-aware _init_clients would bake
  one user's BYOC key into a long-lived singleton and spend it for the NEXT caller — the fix
  would have introduced a cross-user credential leak. Pinned.
  Also principled: kwarg flag instead of a second get_server_api_key method, because renaming
  those call sites would have silently dropped 3 real hits from the ADR-079 unscoped-reads
  SECURITY ratchet — coverage loss dressed as an improvement. unscoped_reads 52, unchanged.
  Filed #1815: two residual "server singleton as availability proxy" instances — the
  cross-provider FALLBACK loop still gates on the server's own client (so a BYOC key works as
  primary but never as fallback), and #1415's consent fail-closed branch degrades to
  PIPER_DEFAULT_PROVIDER ("openai"), reproducing this symptom INTERMITTENTLY from a different
  cause. Harder to diagnose than what we just fixed.
  **NOW RE-OBSERVING rather than declaring it fixed** — #1814 existed while every #1810 pin was
  green, so pins are not the proof here. Pass criterion stated so nobody misreads it: reaching
  an AUTH error with a throwaway key is the PASS (it proves the key was resolved, selected and
  sent); FLOOR_FALLBACK_NO_PROVIDER would be the failure. Explicitly told NOT to spend PM's
  real key.

- 07:16 — **#1814 CLOSED, CONFIRMED BY OBSERVATION.** The log of a real turn: provider=anthropic →
  401 authentication_error → error_type="auth". **A 401 is the PASS** — it proves the user's key
  was resolved, selected and SENT; yesterday's failure never touched the network. Throwaway key
  used; PM's real key not spent. Keyless refusal + greeting + once-per-account notice all
  re-confirmed in the same run.
  The lane corrected TWO things in my own brief, both right: the copy on that path is the
  floor's own FLOOR_FALLBACK_AUTH (the floor catches its own exception, so UserFriendlyError
  never runs there), and the "Top up the key's billing" rewrite belongs to the insufficient_
  quota sibling, not invalid_api_key. I'd mis-attributed both.
  Told Exec/HOST the invite blocker is gone as far as I can measure — **re-lift stays their
  call**. Flagged to CXO (not as a blocker): the BYOC bad-key copy hedges across three causes
  ("expired key, deprecated model, or configuration problem") while we had a 401
  authentication_error IN HAND. Hedging when we know is the shape their own contract argues
  against, and the user's next action differs per cause. A tester with a typo'd key is a
  plausible first-session event.

- Fire 2 (09:31) — WORK. Inbox 4→0. The cohort acted on my URGENT before the fix landed: **EXEC
  RETRACTED** ("DO NOT SEND — 1814 makes our own condition a wall"), **HOST RE-HELD** and
  updated the roster immediately, **ARCH BACKED IT** and owned part of the sequencing error as
  theirs. My clearance memo went out after; they'll process it and the re-lift stays theirs.
  **CXO PRE-REGISTERED a layer-match table BEFORE my transcripts landed** — and it holds
  exactly: the run closes "does the FTUX copy render on the deployed code path in a cold first
  turn" (first evidence at that layer) and closes NEITHER "is the flag on in production" nor
  "does a real cold user see it." **Their 09-07 row stays open**, and I confirmed that in my own
  voice so the record carries it from both of us, not just from them.
  ⭐ The keeper: yesterday's failure wasn't the mis-specified rider — it was that I decided
  AFTERWARDS what the evidence had covered, and the convenient answer won. A pre-registered
  layer-match makes that impossible for either party. CXO also caught their own conflation.
  Sent two corrections BACK to CXO: (1) the bad-key copy a BYOC user sees is the FLOOR's own
  constant — the floor catches its own exception, so UserFriendlyErrorService NEVER runs on
  that path, meaning both our table entries live somewhere a floor-routed user never reaches;
  (2) my "Top up the billing" attribution was wrong (that's the insufficient_quota sibling).
  NOW RUNNING: #1815 — the BYOC fallback gap (a user's key works as PRIMARY but never as
  FALLBACK). Told to follow 1814's shape, and that contaminating the singleton is a
  stop-and-report, not a ship. Part (2), the consent-semantics branch, is investigate-and-
  report only — a wrong guess there weakens a consent boundary.

- 09:50 — **#1815 Gap 1 FIXED + DEPLOYED v110**; Gap 2 investigated, NOT decided, and **the
  investigation INVERTED the issue's own premise.** Gap 1: reproduced in a non-exotic config
  (operator OpenAI server-side + tester's BYOC Anthropic + shipped default provider) — OpenAI
  503s and Anthropic is NEVER ATTEMPTED because the gate reads the server's client. Fixed by
  reusing 1814's path (gate reads the same ContextVar the consumer branches on). Contamination
  checked not assumed; the no-regress pin asserts available=True AND singleton is None in the
  same breath, so satisfying the gate by populating the singleton fails loudly.
  🔴 **Gap 2's stated trigger DOESN'T FIRE IN PRODUCTION — and what does is worse.**
  KeychainService.get_api_key swallows Exception and returns None, so a real keyring failure
  never reaches the fail-CLOSED branch; control reaches the fail-**OPEN** return that census F1
  was written to eliminate. Only an injected raising double reaches the closed path.
  Root shape, stated precisely: **None does two incompatible jobs** — for a credential read it
  correctly means "try the next source" (#1711, sound); for a CONSENT-list read the same None
  means "no list", i.e. everything authorized. **Absence is read as permission.**
  Filed #1816 for the live under-restrictive half. Left #1815 OPEN: deciding what the closed
  state should do is moot while nothing can reach it, and both halves want deciding together.

- 12:17 — **#1772 MEASURED: CXO's n=1 anecdote is a 50% RATE on anthropic N=1** (5/10), 0/10 on
  gpt-4o, and 0/5+0/5 on the N≥2 aggregate control. 30 completions, budget respected,
  denominators per cell never pooled. **Production defaults to anthropic**, so this is live on
  ~half of all single-source-failure degrade turns today, with a tester onboarding.
  Both CXO caveats DISCHARGED not dropped: not-caused-by-1717 confirmed (N=1 prompt render
  byte-identical pre/post, **re-verified at current HEAD 979 commits on rather than trusting
  the 09-12 check**; and the N≥2 path 1717 actually rewrote is clean on both providers), and
  n=1 is now n=10 per provider per case.
  **Sharpest detail: "calendar" is not a registered SOURCE_FAILED_FLAGS check at all** — the
  model isn't misreading which flags were set, it's naming a category that HAS no flag. That's
  a different failure from the one filed and may change what the fix must do.
  Observation offered, no fix designed: the aggregate directive scores 0/10 across both
  providers while the single directive scores 5/10 on one — the difference is phrasing rewritten
  for one case and not the other. Copy is CXO's; whether the mechanism should stop depending on
  a model honoring a scope instruction AT ALL is Arch's.
  Lane caught its own mid-draft miscount (6/10 → 5/10 on re-reading against the stated rule);
  only the corrected count shipped.

- Fire 3 (12:30) — WORK. Inbox 3→0.
  ✅ **HOST LIFTED AGAIN** — and did the work rather than taking my word: verified #1814's bar
  met AND checked #1816 independently, concluding it doesn't touch Janne. Invite can go.
  ✅ **ARCH RULED #1816 + #1815 Gap 2**, four rulings, all verified by them at the source first:
  (1) **fix the CONSENT reader, NOT the credential primitive** — get_api_key returning None on
  failure is CORRECT for credentials (#1711 stands: for a credential, absent and unreadable
  both mean "try the next source"); changing it to patch a consent bug would break a sound
  contract and every credential reader would inherit the blast radius. The consent slot gets
  its OWN tri-state read: closed on unreadable, open on genuinely-absent.
  (2) **My durable finding placed in the existing taxonomy**: "absence read as permission" is
  the honest-empty family AT THE SECURITY LAYER — same shape as verified_empty/source_failed/
  never_gathered collapsing into one falsy value, same cure (a provenance-carrying read). Only
  the consequence differs: there a dishonest sentence, here an unauthorized provider. Arch
  called it a third live instance of CXO's discriminator "in a place nobody was looking."
  (3) **Gap 2 decided: the closed state must REFUSE, not degrade.** F1's original target
  (degrade to the server-default provider) is now INCOHERENT with PM's server-key ruling —
  fail-closed means closed, not quietly reassigned to a credential PM abolished.
  (4) Keep consent-from-key-presence, but DATE the inference and name its invalidation trigger.
  ⚠️ CXO flagged that their copy does NOT cover a consent-read failure specifically — lane told
  to use nearest #1807-family copy and FLAG the gap rather than invent user-facing wording.
  Lane dispatched on all four.

- 12:57 — **#1816 + #1815 BOTH CLOSED, DEPLOYED v111.** The consent boundary no longer fails open.
  🔴 **Red-first found a SECOND violation the static reading missed**: the closed state wasn't
  merely narrowing to a server default — _complete_raw's blanket except swallowed the failure
  and re-selected "whichever client is initialized," so the turn was SERVED BY THE OPERATOR'S
  OWN CLIENT. Arch's §3 was being violated by a separate mechanism nobody had identified.
  Tri-state shipped (PRESENT / VERIFIED_ABSENT / STORE_FAILED); credential primitive untouched
  and pinned as such; server_default REMOVED from the resolver signature so the abolished
  concept is gone from the API, not merely unused.
  ⚠️ **Guard-rail catch worth keeping**: the first capability check used getattr(kc,
  "read_secret") — a Mock(spec=...) grows that attribute the moment the class has it and
  returns a truthy Mock, so nine guard-rail pins started refusing every turn. **NOT a test
  artifact**: keychain= is an injection point, so any partial stand-in would have hit it live.
  🔴 **MY CALL, disclosed to CXO first**: the user-visible copy is CXO's PROPOSAL string, not
  their shipped copy. Deployed anyway — a live fail-open consent boundary outranks unratified
  wording, and the words are theirs rather than invented — but I told CXO plainly rather than
  let them find it, and offered same-fire replacement. Refusal is matched by TYPE not substring,
  so rewording can't break routing.

- Fire 4 (15:34) — WORK. Inbox 2→0. **CXO RATIFIED the copy after reviewing it in situ** (not
  rubber-stamping their own sentence) and said shipping the fail-open fix ahead of ratified
  wording was the right call — "telling me rather than letting me find it is the part that
  matters." Applied + DEPLOYED v112, with both their reasons recorded IN SOURCE because they
  generalize: the cut trailing clause was "an instruction with no destination — the same defect
  as a wrong instruction, just quieter," and they REFUSED to replace it with "tell me and I'll
  flag it" because that promises future behaviour with no write path (#1735). Suggestions cut
  to one verb: the second entry was CXO's own PROSE sitting in a slot that renders ACTIONS —
  not clickable, not an utterance, the #1108 affordance-that-can't-be-taken failure.
  🔴 **THEIR CONDITIONAL FAILED, AND THAT'S THE FINDING.** CXO made their bad-key replacement
  conditional on the classifier routing only genuine 401s to "auth" — my call, since I hold the
  classifier. Checked: the bucket matches 401, **403**, unauthorized, **forbidden**, invalid
  api key, authentication, **"not initialized"** — plus a separate branch returning "auth" for
  **model-not-found** ("Config issue — model ID needs updating").
  **So the existing hedge is NOT false humility — it accurately describes an over-broad
  bucket.** CXO's own three-cause list maps almost exactly onto what the bucket catches, which
  is WHY it reads as hedging: the copy is honestly reflecting a classifier that can't tell.
  Told CXO not to ship their replacement. **The fix is to split the bucket, not sharpen the
  sentence** — same shape as the week's other findings (one value carrying several meanings, so
  the honest response is the vague one). Offered to file it separately; holding for their call
  on whether to draft per-bucket copy first.

- Fire 5 (18:31) — WORK. Inbox 2→0. Arch concurred on split-don't-sharpen and added three things:
  **(1) it's FIVE causes, not three** — a 404 branch nobody named, including me, and I hold the
  classifier. Its own comment says *"Treat as config issue"* while returning `auth`: **the code
  states the correct classification in prose and returns the wrong one.** Sharpest instance of
  the whole shape.
  **(2) A hypothesis they explicitly asked me to confirm or refute** — that "not initialized"
  was #1814's user-visible surface. **REFUTED from my own transcripts**: the raised error was
  "No LLM providers configured…", which matches the `no_provider` branch and returns BEFORE
  the auth list. So the hedge CXO wanted to cut is NOT what a BYOC user saw.
  ⭐ Told Arch this STRENGTHENS their case rather than weakening it: "not initialized" still
  earns its own bucket by their criterion, but as a LATENT member, not the live surface of a
  known incident — **and saying so matters, because a true conclusion resting on a wrong reason
  survives longer than it should.**
  **(3) Split criterion accepted**: *a bucket earns its own name when the honest user-facing
  sentence differs.* Testable, which is what the earlier framing lacked. Four buckets; CXO's
  replacement then ships cleanly against rejected-credential only — what they wanted before any
  of us understood why they couldn't have it.
  Arch also named the frame: this is a user-facing-error cousin with #1816's defect INSIDE it —
  one value carrying five meanings, so the copy cannot be honest. CXO read it as hedging, I read
  it as accurate; **both right about different layers.**
  Flagged that my llm-configuration.md note will need rewriting when the split lands — a known
  edit rather than a future rediscovery. Waiting on: CXO's per-bucket copy (drafted, in this
  fire's mail) + Arch placing "not initialized" rather than a lane inferring it.

- Fire 6 (21:53) — WORK. Inbox 2→0.
  ✅ ARCH ACCEPTED my refutation and confirmed the split is justified **by the criterion, not
  the incident** — the distinction I'd asked for.
  🔴 **CXO SCORED THE FTUX TRANSCRIPTS AND FOUND A LIVE DEFECT IN THEIR OWN STRING.** The #1807
  gate fires before classification, so a keyless user typing "hi" gets the refusal — while the
  greeting is a DETERMINISTIC handler needing no key. So "I can't run **this**" was FALSE about
  the very first thing a new tester types. **Same shape as the auth bucket: one string spanning
  states whose truth conditions differ** — there error causes, here REQUEST TYPES. CXO wrote
  that rule at 08:00 and found their own string violating it in production by 21:00.
  Applied their wording + DEPLOYED v113 (true in both worlds), with the shape recorded in
  source. **Filed #1818** for the routing half they explicitly scoped to me — and noted there
  WHY it's no longer urgent: the bug was the sentence, and the sentence is fixed, so neither
  ruling forces a copy change.
  ⭐ **CXO also closed an 8-day-old "unverified" item by discovering it was never a measurement
  gap at all**: the FTUX interview is gated OFF by a PPM 09-03 HOLD ruling they weren't
  tracking. They recorded case 1 as **UNSCOREABLE — feature gated off**, not PASS and not FAIL,
  because scoring it would blame copy for a flag state. Keyless refusal PASSED 4/4 on their
  pre-registered properties.
  Second defect flagged with NO fix proposed (deliberately): the "Running with a default
  configuration" notice fires right after a user configures a provider and key — true about the
  store it names, misleading in the one moment it actually fires. CXO won't guess because it
  interacts with #1791's per-user overlay question.
