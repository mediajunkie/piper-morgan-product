# Session Log — 2026-09-18 12:45 — Coding Agent (prog-code)

**Role**: Coding Agent (prog), working in Lead Dev's worktree
**Worktree**: /Users/xian/Development/piper-morgan-worktrees/lead (branch claude/lead-cycle, 0 behind origin/main at start)
**Model**: Fable 5 (claude-fable-5)
**Task**: #1809 — invert the unbound-key default at the LLM client chokepoint (step 3 of #1812 plan). Unbound ContextVar must raise (LLMKeyRequiredError family), never fall back to server key, except the designated-operator opt-in (#1807 double gate). Census unbound paths (Slack inbound, background/scheduled, KG ingestion), bind or refuse honestly. Red-first evidence required. Do NOT push; commit on claude/lead-cycle.

## Work log
- 12:45 — Session start. Log created. Read #1809 body (gh api; `gh issue view` body was empty), #1812 body + comments (PM ruling ×2), request_key.py, clients.py, llm_config_service.get_api_key, Slack response_handler/webhook_router, KG ingestion, the #1807/#1320/#1814/#1815/#1816 suites.
- 12:54 — **RED-FIRST EVIDENCE (pre-fix tree)**: probe output:
  ```
  nothing bound: None
  unbound anthropic_client_for_request returned: THE-SERVERS-OWN-ANTHROPIC-CLIENT
  IS the server's own client (silent spend): True
  ```
  i.e. an unbound call receives the server's own client — the silent spend #1809 names. (The full new suite could not collect pre-fix because `UnboundLLMKeyError` didn't exist yet — the probe above is the red run for the behavioral pin.)
- 12:55–12:59 — Implemented the inversion:
  - `services/llm/request_key.py`: `_UNBOUND` sentinel as ContextVar default; `UnboundLLMKeyError` (LLMKeyRequiredError family, message matches CXO's "no LLM key configured" error-table entry); `anthropic_client_for_request` raises on unbound and on explicit-None-without-opt-in; explicit None + `PIPER_OPERATOR_SERVER_KEY` → server client (the #1807 seam, kept until #1812 step 5); `get_request_api_key` contract preserved (None for unbound AND explicit-None — its two readers ask "own key?", the spend distinction lives at the chokepoint).
  - `services/llm/clients.py`: `_complete_raw` re-raises `LLMKeyRequiredError` from the primary call AND the fallback loop (refusal ≠ provider failure, #1815 Gap 2 at the call layer); stale `_anthropic_complete` comment fixed.
  - `services/intent_service/classifier.py` + `conversational_floor.py`: `except LLMKeyRequiredError: raise` carve-outs before the blanket handlers (refusal must reach the boundary; floor change is inert on web where the resolver refuses pre-floor).
  - `services/integrations/slack/response_handler.py`: resolves the sender's Piper principal EARLY, fetches THEIR stored Anthropic key, binds it around classify+dispatch+respond; keyless/unlinked senders are left unbound (lazy refusal at the spend point — template turns still work); `LLMKeyRequiredError` at the boundary becomes an honest in-channel reply (unlinked → #1466 deep-link decline; linked-keyless → CXO copy via UserFriendlyErrorService). Inner blanket handlers re-raise the family.
  - Tests: new `tests/unit/services/llm/test_unbound_key_refusal_1809.py` (10 tests) + `tests/unit/services/integrations/slack/test_slack_inbound_key_binding_1809.py` (5 tests, driven through the real `handle_spatial_event`). Amended the unbound-arm assertions in `test_byoc_key_usable_as_fallback_provider_1815.py` and `test_request_key_resolve_1185.py` (both used to assert the server client comes back after reset; the inverted, stronger form is the refusal).
  - Guard rails re-run: 102 passed (1807 ×4 files, 1320 ×3, 1814, 1815, 1816, 1185, new 1809 suites).
- Census (denominator: every path to `_anthropic_complete`; `Anthropic(` is constructed NOWHERE else in services/web — verified by grep):
  1. `/api/v1/intent` + 5 `/api/v1/documents/*` routes — bind via resolver (#1807). Unchanged.
  2. Slack inbound `handle_spatial_event` — NOW binds sender's own stored key or refuses honestly (this change).
  3. Slack webhook_router slash subcommands (calendar/status/priority/help via CanonicalHandlers) — zero LLM references in canonical_handlers.py (grep -i llm/complete: 0).
  4. Background/scheduled (blacklist_cleanup, attention_decay, ethics_audit_cleanup, composting_scheduler+pipeline): 0 LLM references per file (re-verified, not inherited from the 09-14 census).
  5. Startup: OutputFilterWiringPhase only attaches the filter to the singleton (`set_output_filter`); no completion. Singleton construction (`clients.py:734`) needs no key to exist — `_init_clients` tolerates all-None. Non-LLM singleton uses unaffected by the inversion.
  6. KG ingestion `_analyze_document_relationships` — reaches the chokepoint via container LLM service; its callers are the bound document routes. NOTE (finding, not fixed): `ingestion.py:50` reads the server's OpenAI key from keychain for ChromaDB EMBEDDINGS — a server-owned-credential consumer outside the Anthropic chokepoint (also classifier's KG search path). #1812 umbrella territory.
  7. In-request shadow tasks (preclaim/inversion shadow via asyncio.create_task) inherit the bound context at task creation — billed to the turn's user; no unbound background LLM caller found.
  Residue (findings): `_openai_complete`/`_gemini_complete` still read server-owned clients (no per-request path — pre-existing, #1815 documented it); `validate_provider` pings the provider with the server-slot key via httpx (validation, not completion; empty slot post-#1810).
- 13:00–13:10 — Sweeps. First `ratchets` run tripped the mypy arg-type ceiling (375 > 374): my `_send_llm_key_refusal(key_err, slack_context)` call sat in the OUTER except where `slack_context` is `dict|None` (and could be unbound). Restructured: the `LLMKeyRequiredError` catch moved to an inner try inside the narrowed scope — fixes the type error AND the latent NameError. One legacy test amended: `test_request_key_1162.py::test_client_selection_uses_user_key_when_bound` pinned "absent → server client"; now pins the refusal (+ missing `import pytest`).
- **Sweep results (all post-restructure)**:
  - `run-sweep.sh unit "tests/unit/services/llm tests/unit/web tests/unit/services/auth tests/unit/services/integrations tests/unit/services/intent_service"` → **5687 passed, 3 skipped, 3 deselected** (3:15)
  - `run-sweep.sh ratchets` → **58 passed** + mypy gate **all 24 ratcheted codes at ceiling** (total=1341; arg-type=374) under `venv-mypy-gate/`
  - `run-sweep.sh smoke` → **530 passed, 1 skipped**
  - `ruffenv2/bin/ruff format --check .` → clean after formatting my 2 files; 1 pre-existing drift (`scripts/check-narrative-survey-coverage.py`, untouched) → filed #1820
  - Guard rails by name: test_operator_server_key_1807, test_authenticated_keyless_server_key_1807, test_documents_keyless_refusal_1807, test_keyless_and_quota_copy_1807, test_request_key_anonymous_gate_1320, test_intent_anonymous_key_gate_1320, test_setup_status_exempt_1320, test_byoc_user_key_reaches_llm_gate_1814, test_byoc_key_usable_as_fallback_provider_1815, test_consent_read_provenance_1816, test_request_key_resolve_1185, test_request_key_1162 — all green.
- 13:12 — Discovered work filed: **#1819** (KG embeddings spend a server-owned OpenAI key, milestone MVP) and **#1820** (pre-existing ruff format drift, milestone Ongoing); both added to project 1 with Status=Product Backlog (per-item `gh project item-edit`, never the option list). Implementation-evidence comment posted on #1809 (incl. the Slack whose-key design question — safe default implemented: sender's own key or honest refusal; connector-owner sponsorship left as a product decision).
- 13:15 — Commit on claude/lead-cycle (staged in one call, committed bare in the next per hook discipline). NOT pushed per task instruction.

## Memory & briefing surfaces referenced this session
- **Referenced**: CLAUDE.md (commit discipline: stage-then-bare-commit; auto-close keyword avoidance; discovered-work discipline; name-the-layer/denominator); #1809/#1812 issue bodies + comments (the plan, PM ruling, operator seam scope); `services/llm/request_key.py` docstrings (#1807/#1320 history — load-bearing for where the seam lives); #1815/#1814 test suites (isolation fixtures reused; Gap 2 refusal-propagation precedent reused 4×); #1466 suite (Slack handler test staging, principal passthrough); error-table copy (#1812 step 2 prerequisite, matched by message wording); assign-sprint-safely rule (per-item board mutation only).
- **Loaded but not referenced**: memory index entries broadly; cross-pollination brief (not read — task-scoped subagent session); BRIEFING-CURRENT-STATE (not read for same reason).
- **Wanted but not found**: a canonical doc listing which suites constitute "the #1807 ×3 guard rail" (there are 4 files matching 1807; ran all); a stated convention for which milestone a chore like #1820 belongs to (guessed Ongoing).

---

## Session 2 — 15:45 — Coding Agent (prog-code), #1819

**Task**: #1819 — OpenAI/Gemini client legs have no per-request key path; KG embeddings (`ingestion.py:50`) spend a server-owned OpenAI key outside the #1809 chokepoint. Census first, then extend the per-request path per-provider where reachable, pin refusals where not; KG disposition is a design finding if background. Commit on claude/lead-cycle, no push.

### Work log
- 15:45 — Session start (same-day log resumed per one-log-per-role-per-day). Read #1819 body (gh api), decisions.log PM ruling (2026-09-14 ×2), starting census.
- 15:50–16:05 — **Census** (denominator: every path to an OpenAI/Gemini credential — completion legs + embeddings):
  1. Completion legs reachable from user requests: /intent + 5 documents routes → provider selection can pick openai (env default IS openai); with a server env/keychain OpenAI key, unbound turns billed it. Gemini: server global genai config only; setup stores NO gemini rows; header is anthropic by construction → no binding site can produce a per-request gemini key.
  2. Embeddings: `collection.add` ← CLI `piper documents add` ONLY (operator terminal; `upload_pdf` has 0 production callers). `collection.query` ← GET /api/v1/documents/search → find_decisions (USER request, the one documents route with no key binding at all) + CLI decide/context/review/status. Radar = relational only. **Issue-body correction**: classifier's `get_ingester` import was DEAD (0 uses, AST-verified) — classifier KG context is KnowledgeGraphService (own node store, no embeddings). Import removed in-commit.
- 15:55 — **RED evidence (pre-fix tree probe)**: `_openai_complete` unbound → "served-by-server-key", server client spent: True; `_gemini_complete` unbound → "served-by-server-gemini-config"; embedding property → keychain `call('openai')`, EF built with `sk-SERVER-KEYCHAIN-OPENAI`.
- 16:05–16:30 — Implemented:
  - `request_key.py`: ContextVar provider-keyed (str normalizes to `{REQUEST_KEY_PROVIDER: key}`); `request_spend_key(provider)` = the one spend decision (bound key / operator-None+gate1 / refuse — #1809 messages verbatim); `provider_spend_entitled`; `PER_REQUEST_CLIENT_PROVIDERS = {anthropic, openai}`; `anthropic_client_for_request` delegates; empty mapping binds nothing-spendable, never operator.
  - `clients.py`: `_openai_complete` → fresh per-request OpenAI client from bound key (never stored on self, #1814); `_gemini_complete` → pinned refusal for a bound gemini key (genai.configure is process-global — cross-request credential hazard; no plumbing built), operator binding proceeds on global config; `_is_provider_configured` entitlement-aware; `_complete_raw` primary-leg refusal falls through to the entitlement-gated fallback loop (anthropic-only user on an openai-default instance gets SERVED — #1815 scenario), no entitled candidate → refusal surfaces (#1809 pin preserved).
  - `llm_config_service.get_api_key` step 0 per-provider. `ingestion.py`: `RequestKeyedOpenAIEmbeddingFunction` (no key held; per-spend resolution; operator seam = keychain-then-env); `_analyze_document_relationships` re-raises the refusal family. `document_service.find_decisions` refusal carve-out (non-refusal failures keep fallback_mode). `web/utils/llm_key.py`: `expand_llm_key_binding` (+stored openai rides along; fetch failure = fewer providers = fail-closed) + `resolve_user_openai_key`; intent route + documents `_resolve_key_or_refuse` widen granted resolutions; `/documents/search` binds stored-openai/operator/403 (OpenAI-specific copy). CLI documents commands: explicit `request_api_key(None)` operator binding (gate 1 enforced at chokepoint); ingest refusal fires BEFORE any chunk lands — no partial KG state.
  - Amendments (each with in-file #1819 comment): test_clients_gemini (operator fixture), test_provider_selection_1415 + test_serving_record_1676 (BYOC mapping fixture), test_byoc_key_usable_as_fallback_provider_1815 (per-provider `_stored_key`; primary now refuses-not-spends the operator's openai key — `calls == 0` pin; server-client-existence ≠ availability), tests/conftest.py (llm-marked live tests get operator binding via collection hook), NEW tests/unit/services/intent_service/conftest.py (37 floor/offer tests across 9 files were making LIVE OpenAI calls on machine keys — refused post-inversion; operator binding restores pre-#1819 behavior explicitly → filed #1821).
  - Ratchet interactions: silent-death 195>194 → the deferred-not-swallowed primary handler annotated `# silent-ok:` (always re-raises below); mypy assignment 239>238 → typed `_embedding_function` attr properly.
- **New suites**: test_provider_request_key_1819.py (17), test_embedding_request_key_1819.py (10), test_documents_search_embed_key_1819.py (9) = 36 new tests.
- **Sweeps**: unit (llm+web+kg+intent_service) **5239 passed, 0 failed**; unit (llm+web+kg+integrations+auth) 1416 passed; ratchets **58 passed + mypy gate all 24 codes at ceiling (total=1341, assignment=238)**; smoke **530 passed, 1 skipped**; ruff format --check . clean except pre-existing #1820 drift; ruff check on all touched files: All checks passed. Named guard rails one run: **154 passed** (1807×4, 1320×3, 1814, 1815, 1816, 1809×2, 1185×2, 1162, +1819×3).
- Live verification: chromadb 0.4.22 `get_or_create_collection` accepts the wrapper EF (pm_knowledge created, count 0).
- **Filed**: #1821 (live-LLM unit tests, Ongoing), #1822 (Slack openai binding extension, MVP), #1823 (openai-only user refused at /intent — product decision, MVP); all on project 1 Status=Product Backlog via per-item `gh project item-edit`. Evidence comment posted on #1819 incl. the SPONSORSHIP question (shared pm_knowledge embeddings under the operator seam post-#1812-step-5; per-user query embeddings not consent-gated) — reported for Arch/#1812, not decided.
- Commit on claude/lead-cycle (staged one call, committed bare the next). NOT pushed per task instruction. NOTE: origin/main moved +11 during the session (other agents); worktree left un-merged deliberately — Lead owns the branch.

## Memory & briefing surfaces referenced this session (session 2)
- **Referenced**: #1819/#1812 bodies + decisions.log PM ruling ×2 (the concept ruling + clearing condition); #1809 session-1 log entry in THIS file (census reuse, amendment precedent, sweep recipe); request_key.py/clients.py docstring history (#1162/#1320/#1807/#1814/#1815 — load-bearing for where each seam lives); #1815 suite fixtures (mixed-instance world reused); run-sweep.sh header comments (denominator discipline); CLAUDE.md (stage-then-bare-commit, no closing keyword near issue number, per-item board mutation, name-the-layer).
- **Loaded but not referenced**: memory index; cross-pollination brief (task-scoped subagent session).
- **Wanted but not found**: a doc stating which milestone chores/test-hygiene filings default to (guessed Ongoing again, same gap as session 1); a stated convention for whether dir-level test conftests need Arch review.

---

## Session 3 — 18:47 — Coding Agent (prog-code), #1821

**Task**: #1821 — the #1819 containment (`tests/unit/services/intent_service/conftest.py`'s operator-binding autouse fixture) legalized the 37-test live-LLM spend instead of removing it. Diagnose why each test reaches the boundary, convert per-class, remove the containment binding, prove the sweep passes with NO key resolvable anywhere.

### Work log
- 18:47 — Session start (same-day log, session 3). Read #1821 body (`gh issue view` worked this time). Read the #1819 containment conftest + request_key.py docstring history in full.
- 18:50–19:00 — **Diagnostic method**: disabled the containment fixture temporarily, ran the 9 named files. First pass (env keys stripped but keychain live) gave 1 failure before `pytest.ini`'s `--maxfail=1` stopped collection — traced to `_openai_complete` → `request_spend_key("openai")` → `UnboundLLMKeyError` (line 330, the fully-unbound branch), never a real client construction. Re-ran with `-o addopts=...--maxfail` removed and a real (keychain-loaded) key present (safe: `request_spend_key` raises BEFORE any vendor client is built) → **exactly 37 failures, all `request_key.py:330`** — matches the issue's count and file list exactly, confirmed against the current tree rather than inherited from the issue body.
- **No-key condition established without touching real credentials**: `PIPER_CREDENTIAL_STORE=db` forces `KeychainService` off the OS keyring; the DB fallback construction fails (`ENCRYPTION_MASTER_KEY` unset) → `_no_secure_store` set → `get_api_key` returns `None` for every provider, and `tests/conftest.py`'s keychain-loader refuses to populate env vars from it. Combined with `env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY`, this is a legitimate "no key resolvable anywhere" state — verified directly (`k.get_api_key('openai')` → `None`) rather than assumed from the CLAUDE.md note about Amber's keychain (which turned out stale: real openai/anthropic keys ARE loadable via the OS keyring in this session, confirmed live — the note describes an earlier provisioning state, not now).
- **Classification (all 37 tests, all 9 files)**: **Class B — 37 of 37. Class A — 0. Class C — 0.** None of the 37 assert on the floor's actual composed prose; every one pins offer/pending-state/routing mechanics (`ctx.last_offer`, `intent_service.workflow_offer_service._pending_offers[...]`, `result.intent_data["category"]`, `result.pending_offer`) that are upstream of, or independent from, whatever the floor's LLM says. Read representative failing tests in all 9 files to confirm (not just the 5 files with the "explosive LLM" docstring idiom) — e.g. `test_offer_accept_decline.py::test_sure_accepts_unregistered_workflow_routes_to_floor` asserts only `result.success` and `category == "unknown"`, never `result.message` content. The 5 newer files (1653/1652/1753/1770/1651) already tried to make the LLM boundary "explosive" via `IntentClassifier(llm_service=_ExplosiveLLM())` — but that only covers the CLASSIFIER's LLM entry point; the floor's own lazily-constructed `LLMClient()` (`ConversationalFloor._get_llm_client()`, no injection point from `IntentService`) is a SECOND, unguarded boundary. Root cause across all 9: `IntentService` has 4 independent call sites (`_handle_floor_with_context`, `_handle_unknown_intent`, and two more) that each build `ConversationalFloor()` bare — none accept an injected `llm_client` — so no per-test constructor-injection idiom (the kind `test_conversational_floor.py` et al. already use correctly) was available to these 9 files' authors.
  - Per-file counts (of the 37): test_contextual_offer_continuation.py 8, test_reminder_clear_verb_anchor_1653.py 6, test_offer_accept_decline.py 6, test_standup_offer_flag_1652.py 5, test_soft_offer_survival_clobber_1753.py 4, test_soft_offer_last_offer_clobber_1770.py 3, test_standup_todo_offer_1651.py 2, test_offer_binding_1529.py 2, test_soft_invocation_integration.py 1.
- **Fix**: replaced the operator-binding conftest with a filename-scoped autouse fixture (`_stub_floor_llm_boundary_1821`) that monkeypatches `services.llm.clients.LLMClient.complete` to a canned async string, ONLY for the 9 named files (checked via `request.node.fspath.basename`) — a no-op for every other file in the directory, so the ~27 files that construct `ConversationalFloor(llm_client=<their own fake>)` directly (the correct, pre-existing idiom) are untouched. This cuts the path at the floor's LLM boundary (Class B treatment: "cut the path earlier" — one level above the vendor-SDK-constructor idiom #1814/#1815/#1819 use, since none of these 37 tests need real provider-selection/fallback fidelity, just a floor turn that completes without a network call) while leaving all the REAL surrounding routing/offer-state/context-assembly logic intact (patching `IntentService._handle_unknown_intent` etc. directly, by contrast, would have skipped real post-floor processing these tests actually pin).
  - **Verified the patch is actually exercised, not a false-clear** (m-43/m-44 discipline): instrumented the stub with a stderr print, ran with `-s`, counted hits — **exactly 37**, matching the pre-fix failure count 1:1.
- **No-key proof** (`PIPER_CREDENTIAL_STORE=db` + `env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u ANTHROPIC_BASE_URL -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_CUSTOM_HEADERS`, `-o addopts` keeping `--import-mode=importlib` but dropping `--maxfail=1`):
  - The 9 files directly: **126 passed, 0 failed** (was 37 failed / 89 passed pre-fix under the keyed-boundary-unstubbed condition).
  - Also re-ran the 9 files with REAL keychain keys present (the "keyed developer machine" scenario the issue describes) — **126 passed** — proves the fix is key-presence-independent, not an artifact of the no-key condition.
  - `scripts/run-sweep.sh unit tests/unit/services/intent_service` (env `-u OPENAI_API_KEY -u ANTHROPIC_API_KEY ... PIPER_CREDENTIAL_STORE=db`, the full floor/offer neighborhood, not just the 9 files) → **4317 passed, 3 deselected, 0 failed**.
  - `scripts/run-sweep.sh ratchets` → **58 passed** + mypy gate **all 24 ratcheted codes at ceiling** (total=1341, unchanged from session 2) under `venv-mypy-gate/`.
  - `scripts/run-sweep.sh smoke` → **530 passed, 1 skipped** (unchanged baseline).
  - `ruffenv2/bin/ruff format --check .` → clean except the pre-existing #1820 drift (`scripts/check-narrative-survey-coverage.py`, untouched, already filed); `ruff check` on the touched conftest.py: All checks passed.
  - Named guard rails (`-k "1807 or 1320 or 1814 or 1815 or 1816 or 1809 or 1819 or 1162 or 1185"` over llm/web/auth/integrations/knowledge_graph/web-routes) → **150 passed, 0 failed**.
- **Amended**: `tests/unit/services/intent_service/conftest.py` only — no changes to any of the 9 test files themselves (their existing assertions were already correct; the defect was entirely in the shared fixture layer, so fixing it there is both minimal-footprint and consistent with every one of the 37 being Class B).
- **Discovered work**: none new — #1820 (ruff drift) already filed and untouched; no other gaps found during this pass.
- Commit on claude/lead-cycle (staged in one call, committed bare in the next). NOT pushed per task instruction.

## Memory & briefing surfaces referenced this session (session 3)
- **Referenced**: #1821 body (issue + comments); the #1819 containment conftest's own docstring (why it existed, what it deliberately did NOT fix); `test_conversational_floor.py` et al. (the correct pre-existing `ConversationalFloor(llm_client=...)` injection idiom, used to confirm the fix's scoping was safe); `test_byoc_key_usable_as_fallback_provider_1815.py` docstring (the "only the vendor SDK constructor stubbed" idiom referenced in the task, used to decide Class A vs B rather than to implement — none of the 37 needed it); CLAUDE.md m-43/m-44 (verified the stub was actually hit, not just a silent pass — instrumented count, not assumption); CLAUDE.md "never guess at facts" (checked keychain state live with `KeychainService` instead of trusting the CLAUDE.md note about Amber's keychain being empty, which was stale).
- **Loaded but not referenced**: memory index; cross-pollination brief (task-scoped subagent session); BRIEFING-CURRENT-STATE.
- **Wanted but not found**: nothing new this session — same gaps as session 2 (milestone convention for hygiene filings; conftest-review convention) remain, not re-flagged since already noted.
