# Lead Developer session log — Friday 2026-09-18 (Claude Code, model: Opus)

Role: Lead Developer (lead-code). Worktree ~/Development/piper-morgan-worktrees/lead,
branch claude/lead-cycle (Model A). Cron 4de17177 (17 6,9,12,15,18,21; re-armed 11:55 on
resume from the 09-16 standdown; expires ~09-25, rotate ~09-23).

## RESUME (11:49) — PM's direct instruction, first fire since the 09-16 standdown.
Synced (merged, 0 behind), inbox 7. Priorities executed in gate order:
1. **Restart-gate handoff WRITTEN AND PUSHED** (docs/handoff-lead-2026-09-18.md) — the reboot
   is gated on GREEN per resident and read RED=24 this morning. Wrote for a successor with no
   memory: in-flight (#1812 steps 4–6, the auth-bucket split, #1818), cron state, parked items
   with dates, and — per Exec's ask — how THIS SEAT specifically gets things wrong (the
   satisfied-along-the-way claim, paired-fix sequencing, decrement-vs-remeasure, tier
   inheritance, two-lanes-one-worktree), each with its worked incident.
2. **CRON RE-ARMED**: job 4de17177, same expression, CronList-verified single. Registry row
   un-parked with the new id (exact-phrase python replace, not sed).
3. Sprint closeout (Sep 11–17) with PM's new primary-goal question: NEXT, this session.

- Fire 2 (12:45) — WORK. Inbox zero, sync clean. **#1809 DISPATCHED** — its prerequisite (the
  "no key configured" error-table entry, CXO's copy, 09-14) is met, so the unbound-default
  inversion is finally unblocked. Brief holds the sequence discipline this family earned:
  unbound becomes an ERROR at the chokepoint (the exact inversion Arch named as the durable
  fix when the entry-point-scoped #1807 shipped); the operator seam STAYS until step 5; the
  singleton is step 6 and explicitly not this lane's; Slack inbound gets the safe default
  (refuse honestly) with the whose-key-does-a-Slack-sender-spend design question REPORTED,
  not decided. Census re-verified, not inherited from the 09-14 sweep. Top tier pinned —
  this is the family's chokepoint change, not mechanical adoption; reason logged per policy.

- 13:24 — **#1809 CLOSED + DEPLOYED v114** (health 200). The default is INVERTED: unbound is an
  ERROR at the chokepoint, never a fallback. The careful part: the operator seam survives as
  an EXPLICIT None binding — now distinguishable from unbound — re-checked against gate 1 at
  the chokepoint, so a stray request_api_key(None) can't smuggle the server key, and unbound
  refuses even with gate 1 on because gate 2 was never consulted.
  ⭐ **Slack inbound got BOUND, not just refused** (better than the brief asked): sender's
  Piper principal via #1466 → their own stored key → bound around classify+dispatch; the two
  refusal states (unlinked / linked-keyless) fire LAZILY at the spend point so template turns
  still work. The sponsorship design question (whose key does a Slack sender spend?) reported
  to PM/Arch, not decided.
  Three legacy tests were PINNING the unbound-returns-server-client contract — amended with
  rationale. That's the second time this family's tests defended the hole (#1807 had four).
  Census RE-verified: all 4 scheduled jobs + composting still 0 LLM refs; shadow tasks inherit
  the bound context (billed to the turn's user); the singleton constructs keyless fine.
  Filed #1819 — the openai/gemini legs have NO per-request path at all and KG embeddings spend
  a server-owned OpenAI key bypassing the chokepoint entirely. That's the remaining half of
  the class and next in queue.
  My verify: 15 new pins + ALL FIVE family guard-rail suites (45) + 5687 unit + ratchets 58 +
  smoke 530. #1812 remaining: Slack sponsorship call · step 5 (operator seam) · step 6
  (singleton root) · #1819.

- Fire 3 (15:45) — WORK. Inbox zero. **#1819 DISPATCHED** (top tier, reason: the ContextVar
  going per-provider is a real design extension, not idiom adoption — the single-provider
  constant was BY CONSTRUCTION and the extension must not fork a second parallel var).
  Census-first brief: who actually calls the openai/gemini legs and WHEN; extend the
  per-request path only where user-reachable; pin gemini as unreachable rather than build
  plumbing for it if the evidence says so; KG embeddings get bind-if-request-scoped,
  safe-default-plus-report if background — with skip semantics checked downstream first,
  because embeddings silently failing must not corrupt the KG.

- 16:47 — **#1819 CLOSED + DEPLOYED v115** (health 200). The server-key class is now closed on
  EVERY provider leg: ContextVar provider-keyed (string form normalizes, zero call-site churn);
  ONE spend decision shared by all three completion legs and the new no-key-holding embedding
  function; Gemini PINNED unreachable rather than built (genai.configure is process-global —
  a per-request key there is a cross-request mix-up waiting to happen; building the plumbing
  would have been the disease). Census corrected the issue's premise TWICE (dead classifier
  import; ingest is CLI-only, not background). UX win fell out: anthropic-only user on an
  openai-default instance now gets SERVED via entitlement-gated fallback instead of refused.
  🔴 **#1821 — 37 unit tests across 9 files were making LIVE OpenAI calls on machine keys on
  EVERY sweep**, surfaced the instant the inversion refused them. All month, every run-sweep,
  billing someone's key. Contained via operator-binding conftest; hygiene defect tracked.
  That finding alone may explain a chunk of the mystery spend Exec could only proxy by
  dispatch counts.
  My verify: 36 new pins, family guard-rail set 154 in ONE run, 5239 unit, ratchets 58 +
  24 codes at ceiling, smoke 530. #1812 remaining: Slack sponsorship + step 5 (operator seam)
  + step 6 (singleton) + the two new product calls (#1822 anthropic-only mapping, #1823
  any-provider-key-suffices).

- Fire 4 (18:46) — WORK, day-close shape. Inbox zero. Carry-forward FULLY REWRITTEN (was 09-14
  stale — named the rotated-out cron and closed waits as open); tracker synced + republished
  (v115-friday-close): epic-2's new tenancy class shown DRAINED, the verification row now
  lists PM's Friday-planning decisions instead of stale asks, and the #1821 spend finding
  put where PM will see it. **#1821 hygiene lane DISPATCHED (cheap tier — mechanical
  conversion with a built-in tripwire: the #1809 unbound sentinel makes any stray live call
  RAISE, so the no-key proof run is self-enforcing).** End state specified: containment
  conftest REMOVED, suites green with no key resolvable anywhere.
