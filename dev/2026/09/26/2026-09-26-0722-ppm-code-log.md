# 2026-09-26 — PPM (Principal Product Manager) — Claude Code session log

**Role**: Principal Product Manager (PPM), slug `ppm-code`
**Model**: Claude Sonnet 5
**Worktree**: `~/Development/piper-morgan-worktrees/ppm` (Model A, branch `claude/ppm-cycle`)
**Cron**: `52 6,9,12,15,18,21 * * *` → cut to `52 6,14,21 * * *` at 07:22 (see below)

## Session Objectives

1. Sprint-week continuity: board hygiene (`sprint-truth.py` + the third-queue-source criteria
   line, both every fire), mailbox triage, issue triage against precedent.
2. Keep the carry-forward lean per the established discipline — current state + active threads
   only, no re-accreted Prior-chain.
3. Watch for PM's word on the 4 post-MVP proposals from yesterday's MVP-necessity triage
   (`#1423`/`#1890`/`#1849`/`#1892`) — externally blocked, not a self-deferral.

### 07:22 START

Heartbeat refused once (stale marker, standard sync-and-retry fix), then landed. `CronList` → one
job (`6c1bcfb4`, unchanged overnight — correct expression, no re-arm needed, matches yesterday's
STOP re-arm). Prior day's log (`dev/2026/09/25/2026-09-25-0722-ppm-code-log.md`) confirmed carrying
its `<!-- DAY-CLOSED: 2026-09-25 -->` sentinel — Step 0 clean, no missed-STOP recovery needed.
Carry-forward read: clean, current, nothing PM-gated carried forward except the explicit external
wait on PM's post-MVP-proposal decision. Worktree-collision fingerprint clean (`ppm` ↔
`claude/ppm-cycle`). Synced to `origin/main` clean.

**Mail drain**: 2 items, both cc. **Acted on Exec's usage-throttle directive immediately** — PM,
verbatim, "we need to throttle back": 20% of the week's credits burned in 31 hours, and this seat
was the single top usage contributor (17.3%). Cut own cron cadence from 6x/day (`52
6,9,12,15,18,21`) to 3x/day (`52 6,14,21`) — a ~50% reduction matching the directive's own example
("if you run 5-6, consider 3"). Delete-then-create verified exactly one job survives
(`6c1bcfb4`→`50af07b9`). Updated `dev/active/duty-cycle-registry.tsv`'s own row for both the new
cron expression and `threshold_h` (7→9, matching the new largest in-window gap of 8h between
6:52 and 14:52) — not just the job id, per the skill's own discipline for cadence changes. This is
explicitly about wake frequency, not doing less real work — noted so a future fire doesn't
misread the reduced cadence as license to defer approved/blocking work.

**Second memo**: Lead's `#1595` unit-4 confirm-pause sequencing proposal to Arch, cc'd for context
since this seat owns `#1606`'s row. No PPM action — awaiting Arch's ruling; will fold into the
epic-file entry once it lands, same as the earlier unit-4 correction this week.

Third-queue-source criteria line: 0 gap, denominator 29. `sprint-truth.py` fresh: clean,
reconciled, no delta, 0 unmilestoned. Two consecutive empty mail rounds, drained. Per the throttle
directive's second ask (hold non-essential audits/big synthesis passes), did not resume Friday's
epic-file reconciliation sweep this fire — board hygiene is clean and nothing is blocking, so
there's no case for it today.

Quiet START overall — cadence change was the substantive action, everything else routine.
Returning to idle; cron armed (`50af07b9`), next fire 14:52 PDT (WORK).

### Mid-day — direct PM engagement (not a cron tick), between START and the WORK fire

PM engaged directly in conversation with three fresh raw GitHub Projects TSV exports
(`MVP-open-9-26.tsv`, `MVP-closed-9-18-to-9-24.tsv`, `MVP-opened-9-18-to-9-24.tsv`) and one ask:
*"Let's review the epic structure and make sure the plan is tight."*

**Definitive audit, not a heuristic one**: abandoned an initial proximity-text "is this issue
described as CLOSED nearby" check after it produced both false positives and false negatives (a
shared paragraph's "CLOSED" language satisfying the window check for an unrelated adjacent number).
Replaced it with a bulk GraphQL state+milestone check against every backtick-quoted `#NNNN`
reference in the file lacking strikethrough (93 candidates) — found **52 were actually closed
issues never marked**, a far larger staleness gap than the heuristic pass had found.

**Fixed all 52 confirmed primary-entry cases** (citations of other epics' numbers left as bare
mentions, per the file's existing convention — only an issue's own dedicated tracking paragraph
gets struck) across epics 1, 2, 3, 4, 5, 6, 9, 10.

**Recomputed item/closed-count headers precisely** by reading true section membership (grep-based
counts are unreliable — they get inflated by citations of other epics' own numbers):
- Epic 1: 13 items, 11 closed — verified accurate as-is, no change
- Epic 2: 23→28 items (7 previously-uncounted folded-in members surfaced), 17→22 closed
- Epic 3: 14→15 items (`#1623` folded in), 11→12 closed
- Epic 4: 12 items, 8 closed — verified accurate as-is, no change
- Epic 5: 32→35 items (3 previously-uncounted folded-in members surfaced), 29→30 closed
- Epic 6: 6→7 items (2 open items found uncounted), 3→5 closed
- Epic 9: 12 items, notation simplified from the confusing "1 closed group + 7 closed", 9 closed
- Epic 10: 2 items, 0 open — verified accurate as-is, no change

**One genuine coverage gap found and closed**: `#1897`, filed same-day by the `#1595` unit-4 build
itself (Opus lane) — the only miss against the live 30-issue MVP-open set, too recent to have
synced. Added to epic 0's section with full context (surface-1 read-lane claiming only half a
two-part turn; needs the router to return a plan, Arch's option (b)).

**Final verification**: third-queue-source criteria line re-run clean, 0 gap, denominator 30 —
matches PM's own fresh export exactly. Committed (`37c245c751`) and pushed; merged with unrelated
concurrent commits from other seats (no conflicts, this file untouched by anyone else since the
prior merge-base) and re-pushed clean (`f5343cd7f1`).

**Also drained, same sitting**: 5 mailbox items that had landed during the audit — Exec's
ground-truth routing memo (the TSVs' own provenance + a closed-count discrepancy flag, which the
14:52 WORK fire below resolves), two Arch rulings on `#1595` unit-4/`#1897` grammar shape (cc-only,
no PPM action), Lead's unit-4 landing note, and Pard's commit-attribution incident (broadcast,
informational). All moved to read via `mail-send.sh`, no warnings on the second attempt after
correctly passing both sides of each inbox→read move.

**Structural assessment PM also asked for** ("make sure the plan is tight") — reported the
mechanical staleness fix as complete; flagged the deeper velocity/epic-count structural question
(is 11 epics still right given a week of real data) as not yet done, offered to continue into it.
No further instruction received before the 14:52 tick fired.

### 14:52 (running ~15:2x) WORK

Heartbeat clean (`WORK`). Synced clean, no divergence. `CronList` → one job (`50af07b9`),
unchanged, correct expression, no re-arm needed. Step 0: prior day's log confirmed carrying its
`<!-- DAY-CLOSED: 2026-09-25 -->` sentinel.

**Mail drain, 5 items** (see the mid-day section above for the 3 already handled inline during the
direct engagement — this fire found 2 NEW arrivals plus needed to actually reply/triage the
Exec/Lead thread that had landed, since the direct-engagement work only read it for context):

**The live one**: Exec's `verify(...)` memo asked for PPM's **independent** confirmation of a
corrected Ship #062 closed/filed count before PM sends the draft to Comms — root cause was two
stacking `gh` bugs (an unscoped `--limit`-less pull silently caps at 30 rows; the `closed:`/
`created:` search qualifier evaluates its date range in **UTC, not PDT**, silently excluding
Thursday-evening-Pacific closures from a PDT-labeled window). Lead's own re-derivation (bulk pull +
Python `zoneinfo` bucketing) and Exec's rerun both converged on **91 closed / 57 filed** for Fri
09-18 00:00 → Thu 09-24 23:59:59 Pacific — up from the original 43/34.

**Ran a genuinely third, different method** rather than re-running either of theirs: GitHub's own
`closed:`/`created:` search qualifier, scoped directly to the exact UTC window the PDT boundary
converts to (`2026-09-18T07:00:00Z`..`2026-09-25T06:59:59Z` — confirmed Sep 2026 is still PDT,
no DST fallback yet). **Result: 91 closed, 57 filed — matches exactly.** Extra check: diffed PM's
export's 53 closed-issue URLs against my independently-derived 91-set — all 53 present, zero
missing, corroborating Lead's diagnosis from the export side rather than the API-pull side. Also
verified the unscoped-pull 500-row cap is real (1208 total historical MVP closures exist, an
unscoped `--state closed` pull truncates at 500) but irrelevant once properly window-scoped —
worth naming since it's the same failure class as the original 30-row bug, just a bigger ceiling.

Replied to Exec, cc PM + Lead (criterion (a)/(b): PM is holding a public post on this number) —
confirmed 91/57 safe for the Ship draft, seconded Lead's flag that the draft should state the
numbers in words ("91 closed against 57 filed") rather than a bare signed delta, since "+34" vs
"−34" are both defensible depending on sign convention. Sent clean via `mail-send.sh`, no warnings.

**Also drained**: Pard's attribution-correction broadcast (ppm's own misattributed-commit share
corrected 9→13, informational, no action) — moved to read.

**Board hygiene**: `sprint-truth.py` reconciled clean — 30 not done (10 SB / 2 IP / 3 IR / 15 PB),
1214 done, 0 unmilestoned. Delta since 07:25 START is exactly `#1897` arriving (Product Backlog
14→15) — consistent with the mid-day reconciliation work. Third-queue-source criteria line: 0 gap,
denominator 30, matches PM's export exactly.

**`#1595` unit-4 sequencing watch item RESOLVED, dropped from carry-forward** — Arch approved all
three of Lead's proposed rules, then separately ruled the `#1897` grammar shape (additive
`outcome="plan"` field, never touching the existing single-op contract). Both already folded into
epic 0's narrative during the mid-day reconciliation pass.

**Still externally blocked, carrying forward**: PM's ruling on the 4 post-MVP proposals
(`#1423`/`#1890`/`#1849`/`#1892`) — re-checked live this fire via `gh issue view`, all four still
open and MVP-milestoned, no PM word yet.

Carry-forward rewritten to reflect current state. Cron re-verified armed (`50af07b9`), next fire
21:52 PDT (STOP).
