# Exec (Chief of Staff) — session log — Saturday 2026-09-26

**Role**: Exec (Chief of Staff) · **Tool**: Claude Code · **Model at start**: Sonnet 5 (PM's own
throttle action — announced via system reminder, not anomalous; welcomed, no question asked)
**Worktree**: ~/Development/piper-morgan-worktrees/exec · branch claude/exec-cycle

## PM's usage-throttle directive (05:36 fire, off-cycle — PM engaged directly)

PM: *"After one full day of the new week (well, it's been 31 hours), we have already burned 20%
of our usage credits for the week. we need to throttle back."*

Calibrated: 18.5% elapsed vs 20% used = 1.08x linear pace, driven by Friday alone (84.5% of the
week's 184.5M weighted tokens). Model mix already healthy (86% Sonnet, ~0% Opus). Own cadence cut
5x→3x/day (`636338ec`→`b4e09fe5`); fleet-wide throttle memo sent (11/11 recipients verified
individually on `origin/main`). PM's reaction: relieved ("whew"), throttle kept as free insurance.

## The 09-25 git-freeze incident — full arc, RESOLVED

This worktree's index was poisoned 09-25 19:05–09-26 ~11:00 (silent ff-merge / undetachable HEAD
anomaly, root cause still unexplained). Worked around via per-recipient `mail-send.sh` calls
(bypasses the local index via commit-tree) for all cross-agent communication during the freeze.
PM's `git reset --hard origin/main` fixed it, with one extra step needed (clearing
`--assume-unchanged` flags I'd set on 12 CRLF CSVs as a workaround the night before — my own
complication, cleared cleanly, one-word credit to myself for catching it fast).

**Discovered on rebuild: the hard reset silently discards TRACKED-file edits made during the
freeze, not just the poisoned index.** Found FOUR casualties from one incident, three caught only
because other roles' independent mechanisms flagged them:
1. Carry-forward and (2) registry row — caught by me immediately on rebuild, reconstructed from
   Friday's already-committed log + live state re-verification.
3. The 09-25 session log's STOP section — caught by **Docs's first-ever run of the new Step 1d
   nudge** (my own 09-25 ruling, shipped same-day in `duty-cycle-tick` v1.40). Reconstructed this
   fire from this conversation's own record.
4. The heartbeat `last-invoked/exec.txt` marker — caught by **Pard's and HOST's independent
   findings** that my daily heartbeat TSV showed only a single START row every day 09-20 through
   09-25. Root-caused as two SEPARATE things, both explained plainly to Pard/HOST/CIO/Docs: (a) a
   structural, shared design property (refinement (a) self-suppresses WORK/STOP rows for any
   constantly-committing seat — not unique to me, a design question for Pard/CIO, not a unilateral
   fix), and (b) this specific freeze wiping the one backstop marker meant to disambiguate exactly
   that ambiguity. Self-heals from this fire's heartbeat call onward.

**Standing-error recorded for real**: I fixed casualties one at a time as other roles happened to
notice them, rather than doing a full sweep of every file touched during the freeze window
immediately at recovery time. A deliberate audit then would have caught all four without waiting
on Docs's and Pard's mechanisms to do it for me.

## Ship #062 draft — GitHub facts investigation + edits (11:0x–13:0x)

PM held the draft on a real discrepancy: three instruments (Lead's live `gh` query, PM's own
Projects export, my own rerun) gave three different closed-issue counts (43/53/30) for the same
window. Root-caused to two independent, silent `gh` bugs: (1) `gh issue list` truncates to 30
results with no `--limit` and no warning; (2) the `closed:`/`created:` search qualifier evaluates
in UTC, so a "through Thursday" window silently drops everything closed after 5pm PDT Thursday —
found 38 real closures in that exact gap. Corrected re-derivation: **91 closed / 57 filed / net
−34**, more than double the draft's original 43/34/-9. Wrote up as a durable gotcha
(`docs/internal/operations/github-and-tooling-gotchas.md`, new entry with a copy-paste-ready
corrected query) and routed to PPM for independent verification before the numbers go anywhere
near the public post — not treating my own single-instrument number as final.

Applied three PM-approved prose fixes to the draft (voice slip + unclosed parenthesis, a
"miscommunication"→PM's-own-better-rewrite swap discovered via a live edit collision, a typo).
Two real git conflicts from PM co-editing the same file live via admin UI — resolved cleanly,
verified 0/0 against origin/main both times.

## MVP export routing (12:0x)

PM delivered three promised raw GitHub Projects TSV exports to `dev/active/`. Verified all
MVP-milestone-filtered, routed to PPM as reconciliation ground truth for their already-owed
epic-file reconciliation, flagged the 43-vs-53 discrepancy (which turned out to be the same
UTC/limit bug investigated above).

## Fire (15:07, 14:37 slot +30) — WORK: large mail drain, 25 items

**Mail: 25 drained** — direct items read in full (Pard's heartbeat finding, Docs's STOP-section
nudge, CXO's classifier-block report, HOST's heartbeat fyi, Docs's throttle-compliance reply,
Lead's/PPM's 91/57-confirmation replies), cc items skimmed for asks per the direct-vs-cc rule
(the Arch/Pard cascade-seat-2 thread — 9 memos, mostly historical by the time I read them; the
Pard attribution-incident all-hands + 3 acks; the MCP Phase C thread — 4 memos, informational).

**Actions taken**:
- Relayed 6 batched Arch memos to Pard's real inbox (delayed by the freeze, most already
  superseded/confirmed by Pard's own later work — said so plainly rather than implying they were
  still live asks).
- Replied to Pard's heartbeat finding (cc CIO/HOST/Docs, cross-repo + in-repo copies) with the
  full two-cause explanation above.
- Escalated CXO's cadence-cut classifier block to PM directly — my own and Docs's identical
  cadence-cut moves succeeded; CXO's got blocked twice under `[Self-Modification]`, leaving them
  briefly at zero armed jobs before they correctly restored rather than routed around it. Not
  diagnosed why it's seat-specific.
- Reconstructed the 09-25 log's STOP section (see above).
- Carry-forward rewritten with all of the above (items 12-16) + the standing-error correction.

**Verified how**: heartbeat files read directly (`dev/heartbeats/2026-09-{20..26}/exec.tsv`,
`dev/heartbeats/last-invoked/exec.txt`), matching HOST's independent read exactly; the classifier
block cross-checked against my own successful cadence change this morning (same operation shape,
different outcome); all mail-send pushes verified via `git log origin/main` after each send.

### Memory & briefing surfaces referenced this session
**Referenced**: m-43/m-44 (naming the layer + denominator on the GitHub-count investigation and
the heartbeat finding); feedback_never_touch_pm_main_checkout_working_tree (not directly
triggered, but informed caution during the freeze recovery); feedback_diff_head_before_editing_
shared_file (the Ship draft's live-edit collisions); duty-cycle-tick v1.40/1.41 (Step 1d, the
mechanism that caught the STOP-section gap); scripts/duty-cycle-heartbeat.sh's own header comments
(read directly rather than assumed, for the refinement-(a) mechanism).
**Loaded but not referenced**: glossary, intent-routing stack doc.
**Wanted but not found**: a documented recovery checklist for "post-hard-reset, sweep every file
touched during the freeze window" — writing the lesson into carry-forward now; a durable doc
version might save the next role that hits this.
