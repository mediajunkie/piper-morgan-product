# Lead Developer — session log 2026-09-26

**Role**: Lead Developer (lead-code) · **Model**: Fable 5.1 (claude-fable-5-1, harness-stated) · **Host**: Amber, Model A worktree `~/Development/piper-morgan-worktrees/lead` on `claude/lead-cycle` · **Cron**: recurring **`8d0210ef`** (`17 6,12,21 * * *` — throttled), replaced `470fd4e1` (`17 6,9,12,15,18,21`) at 06:43 per PM's usage directive relayed by Exec 05:4x; **expires ~10-03; restore the 6/day expression after Mon 09-28**.

## START — 06:17 fire (arrived 06:43)
- Step 0: prior STOP ran (DAY-CLOSED 2026-09-25 at 21:5x).
- Sync 0 behind; 56 commits since STOP (Docs' 09-25 omnibus + a nudge to Web/Exec for missing STOPs;
  Arch on a LaunchAgent, cadence cut 6→3; Comms published). CI: Code Quality green on main (`30c52c9654`).
- Inbox: 1 — **Exec relays PM's throttle directive** (20% of the week's credits in 31 h; Friday = 84.5%;
  Lead was 9.9% of it; tiers healthy at 86% Sonnet). Applied: cadence 6→3/day (`470fd4e1` → `8d0210ef`,
  reason: PM directive); one lane at a time, no speculative audits; approved work continues.
- Carry-forward read; tomorrow's order stands: unit 4 (write Arch the confirm-pause question first),
  MCP Phase C units 0–1, #1772 guard when unit 4 blocks.
- 06:44 — Unit 4 sequencing question to Arch as a proposal (`883b87836`): reads first, first
  pausing sibling ends the turn, later siblings NAMED as deferred (never silently queued); memo date
  generated, not typed. Unit 4 waits on Arch.
- 06:44 — dispatched ONE Coding Agent subagent, **Sonnet** (throttle: one lane at a time), for MCP
  Phase C unit 0: skeleton FastMCP server, resources-only capability set, fail-closed 401 on every MCP
  path before unit 1 exists, `main_mcp.py` + `fly.mcp.toml`, no deploy.
- 06:55 — **MCP Phase C unit 0 LANDED** (`03ca5da67c`, Sonnet lane, Lead-reviewed): FastMCP skeleton,
  resources-only (FastMCP advertises tools/prompts unconditionally — handlers stripped, capability object
  pinned), `main_mcp.py` + `fly.mcp.toml`, `/health` via the shared deploy_identity helper. **My review
  change**: the 401 gate matched the MCP path exactly (allow-by-default elsewhere) → inverted to
  deny-by-default (only `/` and `/health` open; test added). 72/72 + enforcement exit-gated; 0 mypy
  errors in the new module (gate venv). Deploying dark to `piper-morgan-mcp` — first machine lights TLS.
- 06:58 — **mcp.pipermorgan.ai is LIT, fail-closed** (`piper-morgan-mcp` v1, origin/main
  `03ca5da67c`): TLS now terminates (Pard's finding held); `/health` 200 with git_sha; `POST /mcp initialize`
  without a bearer → 401 + `WWW-Authenticate: Bearer`; unknown path → 401. Nothing served. Unit 1 next.
- 06:59 — dispatched ONE Coding Agent subagent, **Sonnet**, for MCP unit 1: `mcp_access_tokens`
  (hash-only at rest, operator-minted like invite tokens), `MCPTokenVerifier` via the SDK's bearer auth,
  `current_user_id()` sourced only from the verified token, two-caller isolation test. No deploy.
- 07:32 — **MCP unit 1 LANDED** (`3633802894`, Sonnet lane, Lead-reviewed; 83/83 + enforcement
  exit-gated; bearer lint OK): `mcp_access_tokens` migration `n1462mcpt`, hash-only at rest, mint script
  mirrors the invite mint (raw token printed once, masked echo, never written), `MCPTokenVerifier` via
  the SDK's RequireAuthMiddleware (no auth_server_provider → no /authorize,/token routes), unit 0's
  unconditional 401 → deny-by-default gate + SDK bearer check on /mcp; two-caller isolation over a real
  MCP client. Lane fixed a real SQLite-vs-Postgres tz bug in the expiry compare and self-corrected an
  unscoped-reads ratchet trip (global-ok: the owner is the query's OUTPUT). Deploying alpha (release
  command runs the migration) then the MCP app.
- 07:38 — **Deployed**: alpha v142 (`9472ef914e`; release command applied `n1462mcpt`, alembic head
  confirmed on the machine) and MCP v2. **First live probe found a defect**: garbage bearer → **500**, not
  401 — the MCP app has NO secrets (no DATABASE_URL); asyncpg fell back to a root-owned cert path and the
  exception escaped the verifier. Nothing served, but 500 ≠ the contract. Fixed: `verify_token` wraps
  `_verify_token`, any backend fault → refuse + `mcp_identity_refused reason=backend_fault` (masked
  token); pinned by test; silent-death ratchet unchanged at 190. Redeploying MCP (v3).
  **PM-hand item**: `DATABASE_URL` on `piper-morgan-mcp` (same DB as alpha; a secrets write).
- 07:40 — **MCP v3 live** (`e91db886bb`): garbage bearer → **401**; no bearer → 401; unknown path → 401;
  `/health` 200. The identity boundary is correct in both directions on the live endpoint. Units 0+1 done.
  Next (single lane, after PM's DATABASE_URL + tester pick): unit 2 resources — profile, #1510 verified
  store + PIPER.md priorities, one connector read. Idle to 12:17.
- 08:43 — **PM set DATABASE_URL on piper-morgan-mcp** (in-session `!`, value never on screen).
  Verified: machine reads the flycast URL; a garbage-bearer probe now refuses at **warning** (not_found —
  the lookup RAN) where the 14:40Z one refused at **error** (backend_fault). Identity resolution is live
  against the real store. Dispatched ONE Coding Agent subagent, **Sonnet**, for unit 2: three resources
  (profile · #1510 verified store + PIPER.md priorities per CXO · github issues via bindings), honest-empty
  and connect_required shapes, render-whole cap stated, no tools — the tester pick doesn't gate it.
- 08:54 — **MCP unit 2 LANDED** (Sonnet lane, Lead-reviewed; 92/92 + enforcement exit-gated; pure reads,
  identity only from `current_user_id()`, no mutation tempted): three resources with honest-empty /
  connect_required shapes and the cap stated. Lane finding: the #1510 store today holds only
  `reminder_clear_verb:*` confirmations (working mode / standup prefs aren't wired through it) — read
  generically so whatever is confirmed shows. Deploying MCP v4. Remaining on the slice: unit 3 (first
  contact) = PM's tester+client pick → mint their token → they connect → the named-gap list stated to HOST.
- 08:58 — **MCP v5 live** = origin/main `65740438ae` (v4 was PM's DATABASE_URL restart): health sha
  matches, resources/list without a bearer → 401. First probe read 000 — scale-to-zero cold start (two
  machines, min_running 0); fine for a dark slice, but unit 3 should pin one machine warm before the
  tester's first contact (`min_machines_running = 1` in fly.mcp.toml). **Phase C units 0–2 built and live
  in one throttled morning.** Unit 3 gated on PM's tester+client pick.
- 10:39 — **PM's rulings** (in chat): PM = MCP tester #1, ChatGPT first then Claude; **PA drives the MCP
  testing program (skunkworks); Lead returns to MVP critical-path epics.** ChatGPT-first = the OAuth AS
  (unit 4) is on the path (Arch's Q1 trigger fired). Handoff memo to PA cc Arch/Exec/PM (`a4a7a7ca8`):
  live state, runbook, mint, warm-pin, named gaps, and the unit-4 ownership question (my lean: one Lead lane
  for the identity boundary; PA's call). No token minted — ChatGPT can't take a bearer. Carry-forward:
  MCP off my queue unless PA asks; queue = epic 0 unit 4 (Arch's sequencing answer pending) → #1772 guard.
- 10:41 — Pard's authorship incident read (232 commits fleet-wide authored "Pard" 09-25 16:40 →
  09-26 09:15 via a shared `.git` config; 32 mine; reverted). My seat's identity verified restored
  (`git config user.name` → mediajunkie; last 3 commits mediajunkie). Nothing owed.
- 10:41 — **Arch APPROVED unit 4's three sequencing rules** (verified at source, not on trust) →
  dispatched ONE Coding Agent subagent, **Opus** (stated: epic 0's most delicate unit — sequential rail
  dispatch, confirm turn boundary, #1896 lift scoped to handled turns), behind the flag, default-empty
  byte-identical, no second dispatch site.
- 11:18 — **#1595 unit 4 LANDED** (`3d8168b1e1`, Opus lane, Lead-reviewed; my re-run 160/160 + enforcement
  exit-gated; rail extraction compared statement-by-statement — logic moved, category tail stays in the
  caller, not literally verbatim, full suites green). Lane's real finding, measured by me on 4 phrasings:
  surface 1 never emits a write sibling → read+write turns are ONE intent → the other half is lost on every
  path (legacy included). **#1897** filed (unit 4b = option (b), Arch's grammar; the dispatch loop is
  ready). Memo to Arch cc PPM (`84c333c8bf`). Rides the next alpha deploy.
- 11:22 — **Alpha v143 live** = origin/main `9d348a02a2` (health sha matches; flag intact, 7 tokens):
  #1595 unit 4 behind the flag. Queue: #1772 guard held per the sequencing flag; unit 4b + 1606's
  `set_default_repo` wait on Arch; MCP with PA. Idle to 12:17.
- 12:25 — PM (in chat): **#1772 go** → dispatched ONE Coding Agent subagent, **Sonnet**, for the
  post-compose scope guard (fail-safe; over-trigger floor tests first; under-trigger tests from every leaking
  reply in the three measurement docs). **#1625**: no ruling needed — both halves built (context_assembler
  mentioned-set + radar feed pin); added as test-card row 11 (v13). **#1885**: the issue's two-line Python
  snippet can't be pasted as one line → `mint_invite_tokens.py --burn-unused MASK,…` (masked predicate,
  unused only, masked output, dry-run default), **alpha v144** (`4d901f6c23`, health sha matches; the mode
  verified present on the machine); readable one-liner given to PM in chat + on the issue. PM's TSVs pulled
  (29 open: 3 In Review, 2 In Progress, 10 Sprint Backlog, 15 Product Backlog) — tracker Status pass at 12:17.
- 12:36 — **#1772 guard LANDED** (Sonnet lane, Lead-reviewed; 140/140 + enforcement exit-gated): post-compose
  scope guard in `respond()`, 8/8 corpus leaks caught, 13 legitimate mentions kept, armed+unarmed kept,
  fallback + `scope_guard_dropped` metadata, byte-identical when nothing armed. Deploying alpha (v145).
  Closure = a ~10-call live measurement under the guard (ask PM).
