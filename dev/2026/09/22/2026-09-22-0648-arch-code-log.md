# Session Log: 2026-09-22-0648-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Sonnet 5 — intended per PM's 2026-09-21 ruling, not a regression; see below)
**Date**: Tuesday, September 22, 2026
**Start Time**: 6:48 AM PT
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

**Day 4 of the post-renewal seat. Fly cutover migration day — the runbook I reviewed last night
executes today.**

## Work Log

### START (06:48 fire)

**Cron**: `CronList` → exactly one job, `3cc1dc3d`, `27 6,9,12,15,18,21`. Re-armed at last night's
STOP, expires ~09-28. No action.

**Prior day closed properly**: `2026-09-21`'s log carries `<!-- DAY-CLOSED: 2026-09-21 -->`.

### My model flag last night got a clean answer — correcting the record, not escalating (06:48 fire)

Two replies waiting: Pard traced the actual mechanism (a 09-20 roster-pin probe, `--resume ... --model
claude-sonnet-5`, appended a turn to 21 sessions that the 18:50 resume then inherited — real, and
theirs, now fixed) and reports **PM ruled directly, 2026-09-21**: *"PM agents are on the models they
need to be now: all on Sonnet except Lead, who is on Fable. That is our intent, as Opus is seeming to
be the most token-expensive model."* Exec separately relayed the same question to PM because my
instance was categorically different from the others (a deliberate 09-14 decision reverted, not a
drifted default) and got the same answer from the other direction.

**Clean resolution — nothing to restore.** My header above already reads "intended," not
"regression." This was the right thing to flag last night regardless of the answer: Exec's own words,
*"thank you for checking your own headers rather than trusting memory."* A flag that resolves to "no
action needed" isn't wasted motion when the alternative was running on a silently-reverted decision
indefinitely.

### 09:48 fire — step 8 confirmed clean, write-path gap filed, carry-forward spring-cleaned

Mail: 8 in, all handled. Criteria line: 9, no change.

**My mcp_server_ref finding resolved clean.** Lead ran the exact query live on the droplet
pre-restore: **zero literal rows, and the entire `connector_bindings` table is one binding total
(github)**. The backfill's narrow scope was sufficient by emptiness, not by luck at the row level —
step 8 executes as config-only with a measured yes, not a "should be." Filed **#1850** for the
underlying gap anyway (Lead's suggestion, took it since I'd already traced both the write path and
the backfill's scope): nothing enforces ADR-070's shape at write time, so the next literal row for
calendar/notion/slack is one un-updated caller away from the same failure class as the original
07-12 incident. Not urgent — today's data is clean — but real and worth tracking rather than closing
silently because this pass happened to be safe.

**Spring-cleaned carry-forward per PM's context-floor directive** (top priority today, 80% of
weekly usage). Read the whole file before touching it rather than trim at the margins — found two
fully-resolved sections duplicating history that already lives in decisions.log and session logs: a
four-day-old "Prior IN FLIGHT" block and a month-old Architectural Review narrative ("most of it is
DONE"). Deleted both whole, plus closed items (#1818, #1837) still sitting in the "current" section.
**190 lines → 64.** Kept: genuinely open items, the seven standing hard rules (compacted to rule +
one line — full narrative stays in decisions.log, not duplicated here), current environment facts.

Migration thread (eight of today's memos) is execution chatter between Lead/Pard/Exec/HOST — path A
chosen, rehearsal proven against staged artifacts, freeze window down to ~2 minutes. Nothing
architectural asked of me; watching, not driving. Sent Lead a short ack on the step-8 result and
#1850 rather than let a direct, substantive reply go unacknowledged.

### 12:48 fire — migration succeeded, plan v0.3 closes the morning-after gap

Mail: 5 in, all handled. Criteria line: 10 (only #1850, mine, from last fire — no new work).

**The Fly cutover succeeded.** PM logged into `alpha.pipermorgan.ai` on Fly as a real user, real
OAuth app, real session — the completion path from my own plan v0.2, executed and closed the same
week it was written.

**Lead surfaced the real gap that migration's success exposes**: path A's access-list gate was
window-scoped for the freeze; once revoked, alpha has no deploy path at all. Rather than let this
sit as a future problem, folded it into the plan as **§4e (v0.3)** — continuous with §3b's existing
"tag → image → promote" design, not a fresh thought, so a new issue would have fragmented one design
across two documents.

**Adopted Lead's proposed shape** (push-triggered CI deploy via the existing `FLY_API_TOKEN`) but
verified the load-bearing claim myself rather than take the summary: re-read Exec's full 09-07
`decisions.log` entry. Token name and functionally-permanent 2126 expiry confirmed. **Carried
forward, not resolved, the two things Exec explicitly left open at the time**: whether the token
actually deploys (checked via `tokens list`, never driven) and where the secret should live (routed
to CIO/Pard 09-07, no resolution since — checked, not assumed silent-because-done). Also confirmed
no workflow currently runs `flyctl` at all, so "no deploy path today" is a measured fact, not an
inference from Lead's framing.

**Named a real synergy rather than let two threads sit adjacent and unconnected**: this workflow is
the natural home for #1839's `PIPER_GIT_SHA` build arg — same CI job, so deploy identity and the
actual deployed artifact become one event instead of two things that can drift.

**Deliberately not building it.** Exec's own 09-07 entry supplied the reason better than I could:
*"I nearly shipped PM a recurring duty for a failure mode we could delete instead."* Design is
written; §6 item 5 asks PM who builds it, with the two unverified facts named for whoever does.

Also struck three now-resolved items in plan §6 (the migration approval, the #1818/#1823/#1837
pressure flag) rather than leave a live document showing stale open questions next to a note saying
the migration already ran.

### 15:48 fire — §4e builder named, plan closed out cleanly

Mail: 2 in, both handled. Criteria line: 10, no change.

**PM ruled: Pard builds §4e**, per Lead's recommendation and Pard's own volunteered sequencing
(token facts → CI deploy → staging). My design stands as the design of record, unmodified. Updated
the plan (struck §6 item 5, named the builder, linked the ruling) rather than let a live document
show a stale open question next to its own resolution.

**Folded in Lead's one refinement**: §3c's "gated on CI green + parity" was a phrase, not a wired
check — named the actual script (`scripts/check-release-parity.sh`, which already exists and
already refuses unexplained content gaps) so the gate has a concrete implementation rather than an
aspiration.

**Checked #1849 before citing it** rather than take Lead's parenthetical on faith: it's real, open,
and exactly what Lead described — Fly's build path leaves `git_sha` `unknown`, the Fly-side twin of
#1839. Confirmed it closes as a side effect of the CI job design already written, not a separate fix
someone needs to remember.

**Nothing further owed by arch on this thread.** Updated carry-forward's migration line from
"executing" to "succeeded, builder named" — a two-day design-to-decision arc, cleanly closed.
