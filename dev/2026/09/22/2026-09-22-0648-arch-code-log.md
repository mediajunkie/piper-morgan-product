# Session Log: 2026-09-22-0648-arch-code

**Role**: Chief Architect (arch)
**Model**: Claude Code (Sonnet 5 — intended per PM's 2026-09-21 ruling, not a regression; see below)
**Date**: Tuesday, September 22, 2026
**Start Time**: 6:48 AM PT
**Worktree**: `/Users/xian/Development/piper-morgan-worktrees/arch` (Model A) · **Branch**: `claude/arch-cycle`

**Day 4 of the post-renewal seat. Fly cutover migration day — the runbook I reviewed last night
executes today.**

## Work Log

### START (06:48 fire)

**Cron**: `CronList` → exactly one job, `3cc1dc3d`, `27 6,9,12,15,18,21`. Re-armed at last night's
STOP, expires ~09-28. No action.

**Prior day closed properly**: `2026-09-21`'s log carries `<!-- DAY-CLOSED: 2026-09-21 -->`.

### My model flag last night got a clean answer — correcting the record, not escalating (06:48 fire)

Two replies waiting: Pard traced the actual mechanism (a 09-20 roster-pin probe, `--resume ... --model
claude-sonnet-5`, appended a turn to 21 sessions that the 18:50 resume then inherited — real, and
theirs, now fixed) and reports **PM ruled directly, 2026-09-21**: *"PM agents are on the models they
need to be now: all on Sonnet except Lead, who is on Fable. That is our intent, as Opus is seeming to
be the most token-expensive model."* Exec separately relayed the same question to PM because my
instance was categorically different from the others (a deliberate 09-14 decision reverted, not a
drifted default) and got the same answer from the other direction.

**Clean resolution — nothing to restore.** My header above already reads "intended," not
"regression." This was the right thing to flag last night regardless of the answer: Exec's own words,
*"thank you for checking your own headers rather than trusting memory."* A flag that resolves to "no
action needed" isn't wasted motion when the alternative was running on a silently-reverted decision
indefinitely.

### 09:48 fire — step 8 confirmed clean, write-path gap filed, carry-forward spring-cleaned

Mail: 8 in, all handled. Criteria line: 9, no change.

**My mcp_server_ref finding resolved clean.** Lead ran the exact query live on the droplet
pre-restore: **zero literal rows, and the entire `connector_bindings` table is one binding total
(github)**. The backfill's narrow scope was sufficient by emptiness, not by luck at the row level —
step 8 executes as config-only with a measured yes, not a "should be." Filed **#1850** for the
underlying gap anyway (Lead's suggestion, took it since I'd already traced both the write path and
the backfill's scope): nothing enforces ADR-070's shape at write time, so the next literal row for
calendar/notion/slack is one un-updated caller away from the same failure class as the original
07-12 incident. Not urgent — today's data is clean — but real and worth tracking rather than closing
silently because this pass happened to be safe.

**Spring-cleaned carry-forward per PM's context-floor directive** (top priority today, 80% of
weekly usage). Read the whole file before touching it rather than trim at the margins — found two
fully-resolved sections duplicating history that already lives in decisions.log and session logs: a
four-day-old "Prior IN FLIGHT" block and a month-old Architectural Review narrative ("most of it is
DONE"). Deleted both whole, plus closed items (#1818, #1837) still sitting in the "current" section.
**190 lines → 64.** Kept: genuinely open items, the seven standing hard rules (compacted to rule +
one line — full narrative stays in decisions.log, not duplicated here), current environment facts.

Migration thread (eight of today's memos) is execution chatter between Lead/Pard/Exec/HOST — path A
chosen, rehearsal proven against staged artifacts, freeze window down to ~2 minutes. Nothing
architectural asked of me; watching, not driving. Sent Lead a short ack on the step-8 result and
#1850 rather than let a direct, substantive reply go unacknowledged.

### 12:48 fire — migration succeeded, plan v0.3 closes the morning-after gap

Mail: 5 in, all handled. Criteria line: 10 (only #1850, mine, from last fire — no new work).

**The Fly cutover succeeded.** PM logged into `alpha.pipermorgan.ai` on Fly as a real user, real
OAuth app, real session — the completion path from my own plan v0.2, executed and closed the same
week it was written.

**Lead surfaced the real gap that migration's success exposes**: path A's access-list gate was
window-scoped for the freeze; once revoked, alpha has no deploy path at all. Rather than let this
sit as a future problem, folded it into the plan as **§4e (v0.3)** — continuous with §3b's existing
"tag → image → promote" design, not a fresh thought, so a new issue would have fragmented one design
across two documents.

**Adopted Lead's proposed shape** (push-triggered CI deploy via the existing `FLY_API_TOKEN`) but
verified the load-bearing claim myself rather than take the summary: re-read Exec's full 09-07
`decisions.log` entry. Token name and functionally-permanent 2126 expiry confirmed. **Carried
forward, not resolved, the two things Exec explicitly left open at the time**: whether the token
actually deploys (checked via `tokens list`, never driven) and where the secret should live (routed
to CIO/Pard 09-07, no resolution since — checked, not assumed silent-because-done). Also confirmed
no workflow currently runs `flyctl` at all, so "no deploy path today" is a measured fact, not an
inference from Lead's framing.

**Named a real synergy rather than let two threads sit adjacent and unconnected**: this workflow is
the natural home for #1839's `PIPER_GIT_SHA` build arg — same CI job, so deploy identity and the
actual deployed artifact become one event instead of two things that can drift.

**Deliberately not building it.** Exec's own 09-07 entry supplied the reason better than I could:
*"I nearly shipped PM a recurring duty for a failure mode we could delete instead."* Design is
written; §6 item 5 asks PM who builds it, with the two unverified facts named for whoever does.

Also struck three now-resolved items in plan §6 (the migration approval, the #1818/#1823/#1837
pressure flag) rather than leave a live document showing stale open questions next to a note saying
the migration already ran.

### 15:48 fire — §4e builder named, plan closed out cleanly

Mail: 2 in, both handled. Criteria line: 10, no change.

**PM ruled: Pard builds §4e**, per Lead's recommendation and Pard's own volunteered sequencing
(token facts → CI deploy → staging). My design stands as the design of record, unmodified. Updated
the plan (struck §6 item 5, named the builder, linked the ruling) rather than let a live document
show a stale open question next to its own resolution.

**Folded in Lead's one refinement**: §3c's "gated on CI green + parity" was a phrase, not a wired
check — named the actual script (`scripts/check-release-parity.sh`, which already exists and
already refuses unexplained content gaps) so the gate has a concrete implementation rather than an
aspiration.

**Checked #1849 before citing it** rather than take Lead's parenthetical on faith: it's real, open,
and exactly what Lead described — Fly's build path leaves `git_sha` `unknown`, the Fly-side twin of
#1839. Confirmed it closes as a side effect of the CI job design already written, not a separate fix
someone needs to remember.

**Nothing further owed by arch on this thread.** Updated carry-forward's migration line from
"executing" to "succeeded, builder named" — a two-day design-to-decision arc, cleanly closed.

### 18:48 fire — two real rulings, Exec's don't-economize directive noted

Mail: 3 in, all handled. Criteria line: 10, no change (Lead's routed issues aren't `architecture`-
labeled). **Exec's fleet directive**: PM says don't self-throttle or postpone work over usage right
now (reset plan covers the runway) — context-floor discipline stays top priority regardless. Applied
tonight: did both rulings properly rather than deferring either.

**#1774 (dead grammar-conscious family) — GO on the full family, cross-check genuinely resolved.**
Lead's census found the orphaned family bigger than filed and flagged it borders the protected
spatial disposition by name (`place_detector`). **Didn't take that as a reason to hedge or as a
reason to wave it through — checked the actual files.** There is exactly one `class PlaceDetector`
in the repo, and it's tonight's dead module; the spatial layer's real live file (`place_service.py`)
never references it. The collision was a documentation imprecision in an otherwise-careful design
record, not a live module hiding behind a shared name — fixed inline so the next cross-check doesn't
inherit it. **Ruled GO** on the confirmed-plus-newly-found family; `intent_types.py` stays surgery,
never rm.

**Ruled item 2 (writer-less lens surface) separately rather than bundle it into the GO.** Traced the
actual read sites rather than trust the description: `current_lens` is always `None` in production
by construction, the honest-empty family for at least the fifth time this week. Ruled *rip, don't
complete* — reviving it means re-litigating an already-deliberately-deleted vocabulary with no
product case made. But held the cut itself for its own Rule-0 census: it's schema-touching on two
persisted surfaces, a different class of change from a dead-module delete, and rushing the one item
that touches a persisted shape would have been the wrong place to move fast. **Named this as a
complexity deferral, not a pacing one** — directly relevant given Exec's directive landed the same
fire; the two are not in tension, and saying so explicitly matters more today than most days.

**#1841/#1854 (classifier semantics + corpus mechanics) — ruled clean.** Didn't accept "genuinely
arguable" as a reason to punt: compared `manage_portfolio`'s own canonical utterance ("List my
projects" — entities) against the pm039 test family's five other siblings (all document-genre
searches) and found the actual discriminator — "plans," not "project." Ruled `search_documents`.
**For the deposit mechanics, verified the real generator and output paths rather than gesture at
"my lane"** — found `build_inversion_corpus_phase0.py`'s actual docstring already specifies the
exact mechanism Lead described (hand-inline unstructured sources with citation, regenerate, never
hand-edit the output), so I could point at it with confidence instead of re-deriving a process that
already existed and worked. Named the one thing I did NOT verify (the armed file's internal row
schema) rather than imply full coverage.

Both rulings posted to their issues (artifact record) and to decisions.log (durable technical
record), following mail (signal) — three-surface distribution, deliberately, same discipline as
#1823/#1829 this week.

### 21:57 fire — STOP, day close

**Last scheduled slot confirmed by cron arithmetic**: `27 6,9,12,15,18,21` → next match 06:27
tomorrow, a different calendar date. Cleanup dry-run empty. Criteria line unchanged at 10. Mail
drained to zero at trunk on the final check, same as every fire today.

**Cron re-armed**: delete-then-create-then-verify, `3cc1dc3d` → **`a767ab79`**, `CronList` confirms
exactly one job. Expires ~2026-09-29. Registry row updated on top of a concurrent fleet write —
integrity re-verified (88 rows, 8 columns, four neighboring roles undisturbed) before trusting the
edit landed cleanly, same discipline as every prior STOP this week.

## Day arc — 2026-09-22 (day 4 of the post-renewal seat)

The day the pipeline I designed actually ran. Started with a clean resolution on a self-raised flag
(the Opus→Sonnet question — right to check, wrong initial conclusion, corrected same-morning); spent
the middle of the day watching the Fly cutover succeed and closing the one design gap it exposed
(§4e, builder named by end of day); closed with two independent architectural rulings that both
turned on the same discipline — checking a name against the actual referent instead of trusting the
label.

| what | outcome |
|---|---|
| **Model flag (Opus→Sonnet)** | Resolved same-morning: PM ruled Sonnet fleet-wide the day before, independent of my flag. Right to check ("thank you for checking your own headers rather than trusting memory" — Exec); wrong specific conclusion, corrected in the record. |
| **Context-floor spring-clean** | Carry-forward 190→64 lines, per PM's top-priority directive. Deleted two fully-resolved sections whole rather than trim at the margins. |
| **Fly cutover migration** | SUCCEEDED. PM logged into `alpha.pipermorgan.ai` on Fly as a real user. The completion path from my own plan, designed Saturday, executed and closed by Tuesday. |
| **§4e (post-migration deploy path)** | Design-complete, folded into the same plan rather than a fresh issue. PM named Pard as builder by end of day — the whole arc from "gap surfaces" to "owner named" inside one day. |
| **#1774 (dead-code family)** | GO on the full family, bigger than filed. The spatial-boundary cross-check Lead asked for resolved clean — checked the actual files rather than the shared name, found a real documentation imprecision in a ratified-law-adjacent record, fixed it. |
| **Lens surface** | Ruled *what* (rip) but held *when* (own Rule-0 census) — a genuine complexity deferral, distinguished explicitly from a pacing one on the exact day Exec told the fleet not to pace. |
| **#1841/#1854 (classifier drift)** | Ruled by comparing the competing intent's own canonical phrasing, not the two label names in isolation. Pointed at real, already-working deposit mechanics instead of re-describing a process from memory. |

**The day's theme, stated once rather than padded**: three separate times today (the spatial
cross-check, the classifier semantics call, the deposit-mechanics pointer) the fast answer and the
correct answer diverged, and the divergence was only visible by opening the actual file/utterance
rather than reasoning from its name or its label. That is the same discipline this seat has been
sharpening all week, applied to genuinely new material rather than to my own prior mistakes for
once — a good sign the practice generalizes rather than only firing in the presence of guilt.

## Memory & briefing surfaces referenced this session

**Referenced** — `docs/internal/architecture/design-records/spatial-cold-island-per-connector-
place-modeling.md` (read in full for the cross-check, corrected in place); `decisions.log`
(2026-08-15 spatial ruling, 2026-09-07 Fly token entry — both read in full rather than cited from
memory); the deployment-pipeline plan (now v0.3, amended twice today as real events overtook it);
`docs/internal/architecture/deployment-pipeline-plan-v0.1-2026-09-20.md`.

**Loaded but not referenced** — `BRIEFING-ESSENTIAL-ARCHITECT.md`, now unread across four full days.
Naming this a fourth time without reading it would itself become the thing rule 6 warns about — a
cheap check that's easy to keep deferring. Committing to actually reading it at tomorrow's START,
not re-flagging.

**Wanted but not found** — nothing new today.

## Sign-off

- Cron: re-armed via delete-then-create-then-verify — `3cc1dc3d` → **`a767ab79`**, `CronList`
  confirms exactly one job. Expires ~2026-09-29.
- Registry row: updated over a concurrent fleet write; integrity re-verified before trusting it.
- `git status` clean / `git log origin/main..HEAD` empty, `rev-parse` guard run.

<!-- DAY-CLOSED: 2026-09-22 -->
