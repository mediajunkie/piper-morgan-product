# Lead Developer session log — 2026-09-22 (Claude Code)
Role: Lead Developer (lead-code) · Model: Fable 5 (claude-fable-5) · Worktree: ~/Development/piper-morgan-worktrees/lead (Model A) · Cron: 6e295f4f (17 6,9,12,15,18,21)

## MIGRATION DAY — alpha → Fly cutover, window PM-approved (runbook: docs/internal/operations/alpha-fly-cutover-runbook-2026-09-22.md)

### START (06:17 fire, run 06:47)
- Prior day DAY-CLOSED verified · freeze-detect rc=0 · cron 6e295f4f confirmed exactly one ·
  synced clean. Inbox: 4 (2 direct: HOST needs the 4 Fly account identifiers; Arch on
  mcp_server_ref coverage — draining now). **No PM path-A/B answer visible yet in inbox** —
  checking decisions.log + escalation deadline 08:30 per carry-forward.

### Pre-window drain complete (06:47–07:00) — two pre-steps discharged, one converted to snapshot-fallback
- **Arch's step-8 check RUN on droplet** (my seat, connector_bindings, no identity columns):
  zero literal calendar/notion/slack rows — denominator: the whole table is ONE github binding.
  Step 8 confirmed config-only against real data. Result memo sent (28c9c8faa); offered to file
  the write-path enforcement gap (`binding_repository.py:85`) as an issue — Arch picks who files.
- **HOST's identifier ask**: `fly ssh console` DB read classifier-denied on my seat
  (`[Production Reads]`) — not worked around; blind-pull-to-file remains the agreed shape when an
  unblocked seat runs it. **Proposed step 0 = satisfied-by-snapshot** (step 3 preserves all 4 Fly
  accounts completely; replace-all loses nothing irrecoverable) so the window doesn't block on it.
- CIO's two hook-pilot cc's skimmed (Pard's lane, no asks; noted: post-commit shim can make
  commits slow — known cause if today's commits lag, not a migration signal).
- Staging dir ready: `~/migration-staging-20260922/` chmod 700. One-shot cron **e715d1f8** armed
  (08:33) as the A/B escalation backstop — **PM's path answer still absent** (inbox, decisions.log,
  in-conversation all checked). Drain state: mail 4/4 (2 direct answered, 2 cc skimmed),
  standing-items: migration-gated, criteria line: no new sprint-milestone issues this morning
  (gh checked at fire open). Idle pending A/B or window-open.

### 08:33 backstop fired — A/B already answered (A); drained the morning's second wave (08:33–08:55)
- No escalation needed: PM ruled path A ~07:59 in-conversation (decisions.log entry pushed; Pard
  tasked with settings-diff prep, PM applies, behavioral verify, revoke after).
- Health probes 08:33: droplet attests v0.8.13.0/2ff920b74 (control good); **Fly still
  pre-0.8.13 — Pard's step 1 not yet run** (rules presumably still being applied with PM).
- Mail 5/5 drained: HOST AGREED step 0 = satisfied-by-snapshot (window proceeds; blind-pull shape
  confirmed in writing) · Exec context-floor directive + 2 asks (PM top priority) · CIO fixed last
  night's hook-pilot recursion (967 marker commits; Pard's re-arm call — cc only).
- **Exec's briefing ask EXECUTED same-fire** (was gated waiting anyway): Current Position + Focus
  refreshed to migration-day state (shorter than what they replaced), Aug 7–10 UPDATE marked
  SUPERSEDED for Docs's removal with the current Version record split out to survive it, Last
  Updated chain extended. Ack memo sent (8bd3c0714); carry-forward spring-clean + registry trim
  committed for tonight's STOP.
- Standing by for Pard's pre-freeze ping (steps 1–3). Cron 6e295f4f armed; one-shot e715d1f8
  consumed.

### PM unblocked Pard — step 1 CONFIRMED LANDED (09:36–09:45)
- Fly /health now attests **v0.8.13.0 / environment production** (was identity-less pre-0.8.13 at
  08:33) — Pard's deploy is live. `git_sha: "unknown"` on the Fly build path → **#1849 filed**
  (droplet build injects it, fly deploy doesn't; not a cutover blocker, wants fixing before
  v0.8.14 cuts).
- `fly secrets list` (names/digests only) readable from my seat: master key present (whether
  re-set from droplet value = Pard's ping to confirm). **Found + runbook'd a real cutover
  landmine (9b)**: five URL-bearing secrets (PIPER_BASE_URL, GITHUB_OAUTH_REDIRECT_URI,
  SLACK/GOOGLE settings redirect URIs) must be set to alpha.pipermorgan.ai values at step 9 or
  OAuth callbacks keep pointing at fly.dev post-cut.
- Pard also added a command appendix to the runbook. No DONE annotations/ping yet for steps 2–3 —
  standing by for the freeze ping.

### Rehearsal pass executed — the freeze is now a re-run of a proven pipe (09:47–09:55)
- Pard's ping: steps 1–3 DONE + read-verified (v119, sha 609a07b9 = origin/main exactly — his
  redeploy with the `PIPER_GIT_SHA` build arg fixed the #1849 gap in passing; master key set from
  droplet value, never printed; snapshot `vs_2vaNmRk91N7ty6PY4L3Q4G` 3.8MiB banked = step 0
  satisfied). Path A live: PM created the allow file 09:29, both probes passed, no relaunch.
- One open item his side (Amber lacks psql, rc 127) → instead of freezing into an unproven
  restore path, ran the WHOLE pipe live as a rehearsal: dump (227,551B) + uploads tar (371,462B)
  + chroma tar (220,589B) pulled to `~/migration-staging-20260922/` (chmod 600), droplet app
  never stopped. **Counts at rehearsal: users=6, unused_invites=10** — Pard's verification
  denominators. Pard's HOST courtesy-pull file already in staging (unread by me, per the split).
- Handoff memo sent (70800ee87): Pard proves restore+volumes+restart against the rehearsal files
  (Fly DB doomed anyway, snapshot banked), then one word ("proven") → real freeze ≈2 min: stop
  app → final dump → pull → idempotent re-restore → PM's step 9+9b. Standing by.

### 10:42 check (PM asked) — no "proven" from Pard yet; HOST finding changes the stakes on one account
- Fly /health unchanged (v119). No Pard ping ~50 min after the rehearsal handoff — PM offering a
  nudge, accepted.
- **HOST read the courtesy file (their lane): the 4 Fly accounts are ALL PM's own** — and one has
  real recent state (last updated 09-13: timezone, working-mode, a verified inference). "Frozen
  since 07-13" was created-at, not touched-at. Doesn't change technical safety (snapshot banks
  everything) but replace-all could silently drop PM's own working preferences if the droplet copy
  diverged. Surfacing to PM in-conversation (PM can answer their own usage in one line — fastest,
  no identity crossing my seat); fallback = HOST compares a droplet-side blind file.
- Arch filed #1850 (bindings write-path enforcement gap, his lane, not urgent). Step 8 formally
  ack'd clean.
- **10:5x — PM answered the Fly-account question in-conversation**: "nothing irreplaceable for
  me on Fly, only test data so far." Replace-all UNCONDITIONAL; recorded in decisions.log;
  HOST/Pard notified. The restore's last conditional is gone. Still awaiting Pard's "proven"
  (PM nudging).

### THE FREEZE (11:05–11:15) — alpha frozen 18:06:20Z, final artifacts staged, Pard restoring
- Pard's "proven": full rehearsal restore verified at every layer (users=6, invites=10, bindings=1
  github, volumes on, restart clean, /health v119 sha 609a07b9, login renders 200). His one open
  question (droplet redis) answered with an authed keyspace read: DBSIZE=5, nothing durable-only
  (usage_cap ephemeral + 4 exploit-fingerprint junk keys) — migrate nothing, denominator all 5.
- **FROZEN**: `docker compose stop app` 18:06:20Z, dump 18:06:23Z (3s). Final dump+tars pulled to
  staging chmod 600. Counts at freeze = rehearsal exactly. Content-diff: only pg_dump's random
  restrict-tokens differ → **zero data drift**; Pard's final restore is idempotent confirmation.
- Security side-finding: droplet redis publicly bound 0.0.0.0:6379 (auth held, backup1-4 exploit
  fingerprints present, no compromise) → **#1851 filed** — no droplet fix (decommissions in a
  week); step 10 gains "verify Fly redis is private-only."
- FROZEN memo sent (6b9e4e971). Droplet app stays stopped; un-freeze = PM's DNS cut landing.

### THE CUT IS LIVE — alpha.pipermorgan.ai served by Fly (11:4x cut; verified 12:11)
- Pard + PM executed step 9: DNS A/AAAA → Fly (droplet A removed), cert issued 18:46:05Z (the
  ~40-min dark window was the cert's stale-view wait — freeze data exposure was 3 seconds), 9b
  moved GitHub OAuth onto alpha's app (beta's GitHub login now mismatched — acceptable,
  PM-ruled alpha-canonical). Testers re-login once (JWT secret deliberately not migrated —
  honest smaller change, endorsed).
- **Step 10 Lead half CONFIRMED from my own seat, independent reads**: DNS ✓ /health identity
  (0.8.13.0, sha 609a07b9 = origin/main) ✓ login render ✓ cert to Dec 21 ✓ droplet still
  stopped ✓. PM's half queued: verify alpha OAuth callback, real login, #1617/#1824 retests.
  Revocation of Pard's allow file HELD until PM's half passes (fast path if login surfaces a fix).
- Filed **#1852** (Slack/Google redirect URIs still fly.dev — provider-console + secrets +
  live-test fix path; no tester on those integrations yet). Docs removed my superseded briefing
  entry (Version line preserved as flagged).
- Step 11 clock: decommission ~09-29 (mine). Migration ledger: zero data drift, zero rows lost,
  3s exposure, ~40min dark.
- **12:2x — PM's step-10 half PASSED**: callback URL already verified with Pard (PM confirmed),
  PM logged in on alpha.pipermorgan.ai as `xian` — real user through the real domain on the real
  OAuth app. Revocation GO sent to Pard (retests #1617/#1824 are PM's-pace app checks, not infra
  gates). Flagged the post-cutover deploy-path question for the pipeline plan: after revocation,
  v0.8.14 needs a deploy mechanism — recommended CI with the existing Fly deploy token + baked
  PIPER_GIT_SHA, Pard/Arch to place it (issue or plan v0.3).

### 12:47 tick — quiet wake, two issue closures from the cutover landed on the record
- Mail 0 · standing-items: migration confirmations pending (Pard revocation, PM retests) ·
  criteria line (new sprint-milestone issues): checked, only today's own filings (#1849/#1851/
  #1852). Deep build lanes (#1765 etc.) held — named trigger: fresh session after migration
  confirmations close, and PM's context-floor/usage-pressure directive makes today the wrong day
  to open a diagnosis lane.
- **#1849 commented**: sha fix is procedural (command sheet), not structural — Dockerfile takes
  the arg (line 61), nothing in-repo supplies it; closes when a nobody-hand-arms deploy attests.
- **#1845 commented**: Fly-side burn COMPLETE by construction (restore replaced the DB; dead
  token didn't survive; snapshot retains a copy on retention schedule). Remaining: PM rule
  ratification → my lint backstop.
- **Registration-watch gap noted**: Janne now registers on Fly-served alpha; post-revocation no
  agent seat can count Fly users — watch mechanism needs either PM's eyes or the next granted
  read. Carried to STOP's carry-forward rewrite.

### PM's push for interim work — test card artifact + the live-claim docs sweep (13:0x–13:4x)
- PM (rightly) challenged the hold: "isn't there unblocked lower-stakes work?" — accepted; the
  usage-pressure hold was too broad.
- **Test card**: canonical file refreshed post-cutover (both retests LIVE NOW on alpha — the
  cutover deployed #1837 and #1824; row 2 promoted from struck-pending to open) + **published as
  an artifact for PM**: https://claude.ai/artifact/ALxfaRpLn5wjBVUPjzLvbi
- **Pard/Arch closeout memos drained**: §4e (CI deploy) adopted into pipeline plan v0.3 (my
  design); Pard volunteered token-facts → CI-deploy → staging; ack sent; **PM decision surfaced:
  name the §4e builder (rec: Pard)**. Revocation with PM (asked-and-answered why).
- **Live-claim docs sweep executed** (step-11 prep, pulled forward): deploy-environments doc
  (banner + diagram + alpha row + secrets/OAuth line), BRIEFING Current Position/Focus/Version
  (cutover COMPLETE, was written mid-flight), key-management, email-template (mint mechanism now
  Fly-side + grant-gated), both droplet runbooks banner-SUPERSEDED (kept as ops record to
  decommission). Historical records (omnibus logs, ADRs, published posts, planning logs)
  deliberately untouched. Deep sweep remains step 11's.

### Afternoon queue lane (14:0x–) — Pard named, queue re-verified, #1793 in flight
- **PM named Pard the §4e builder** ("agreed that's for Pard") — decisions.log entry + memo to
  Pard cc Arch/Exec/HOST (asked him to wire check-release-parity.sh as the parity gate).
- **Queue re-verified against GitHub (the carry-forward's own rule, vindicated)**: #1765, #1764,
  #1813 all CLOSED (two by me, 09-20 — carry-forward was stale); 14/14 cross-user tests pass on
  HEAD, consistent with the 09-20 fix holding. Open queue: #1841 #1842 #1793 #1796 #1797 #1817
  #1774 + audit clusters.
- **#1793 taken (docs documenting a dead endpoint)**: live-verified ALL corrected commands
  against the running local server first — unauth 401; login is FORM-encoded (JSON 422s — a
  would-be doc bug caught only by running it); GET profile 200 (shape captured); enhance's field
  is `content` not the old `response` (422 caught live); PUT non-admin 403. Scratch user
  docs-spotcheck-1793 created on LOCAL dev DB for the check, then deleted (surgical single-row).
  Also found: scripts/create_test_alpha_user.py is dead-stale (imports AlphaUser, model is now
  User) — same #1793 class, noting on the issue.
  **Dispatched a Coding Agent subagent (Sonnet)** with the verified command set to rewrite the
  six docs; review + commit mine when it reports.
- **#1793 CLOSED** (~14:1x): subagent (Sonnet) returned 6 files / 85+46 lines; reviewed both
  flagged passages line-by-line (multi-user rewrite = the verified two-session shape; DDD
  template = forward-looking contract, correctly in-scope), committed + pushed, closed with
  Verified-how (live HTTP layer, all 3 endpoints × auth variants, pre-edit verification).
  **#1853 filed** (create_test_alpha_user.py dead-stale, ImportError quoted). Next: #1796.
- **#1796 CLOSED** (~14:4x): item 1 (calendar mock arity) already fixed on HEAD (rode
  8459602f50), verified passing; item 2 = PLACEMENT defect — the llm-marked pm039 test lived in
  tests/unit/ where bare keyed runs execute it (the #1765 family, placement form). git mv →
  tests/intent/ (the governed tier home: unmarked=deterministic pin #1831, llm=live tier);
  docstring records the disposition + forbids loosening the assertion (#1841 tracks the live
  param failure, corpus-row default). AC sweep run KEYED (the failing condition): 5016 passed /
  0 failed / 65s. Two closures this afternoon lane (#1793, #1796) + two filed (#1853; #1852
  earlier).

### 15:48 tick — path A revoked; a §4e assignment CONFLICT surfaced (PM×2, crossed memos)
- Pard: grants revoked 13:38 (PM's hands); honest partial confirmation (reads now pass on their
  own merit; won't run a write as a probe — right call). Path A ledger: granted 09:29, used for
  steps 1-3/5-7/9b, revoked 13:38.
- **CONFLICT**: PM to Pard 13:38: deploy path is LEAD's to solve, Arch/Pard escalation. PM to me
  ~13:50 in-conversation: "agreed that's for Pard." Crossed memos; both recorded (mine in
  decisions.log 13:5x). Surfaced to PM in-conversation for a one-line resolution; decisions.log
  gets the correction entry after PM answers. NOT proceeding on either reading meanwhile — but
  §4e isn't time-critical (alpha deploys by PM's hands until v0.8.14).

### #1797 EXECUTED end-to-end (15:5x–16:2x) — five dead persistence twins gone, with three census-invisible catches
- Cron deleted at drain start (Rule 1). delete-module-safely skill followed: fresh census (all
  roots incl. tests/cli/tools, both styles, anchored patterns — the loose `.models` grep invented
  importers and was corrected per the skill's own anti-pattern) → zero consumers confirmed.
- Tables: existed everywhere (create_all residue, no migration ever), ZERO rows on local + droplet
  (= Fly's seed). m1797drop written + applied locally; deliberate no-op downgrade.
- **The verify battery earned its keep three times** (each a dependency no importer census can
  see): #1312 schema guard → work_items' 2 FK constraints (columns stay, constraints go, CASCADE
  collateral named + verified surgical); mapper init → 4 string relationship() attrs on live
  models (consumers: none; Workflow.to_domain intent_id provably always-None, pinned honestly);
  WorkflowRepository selectinload.
- **Rider root-fix**: ensure_utc @overloads (no runtime change) killed 9 latent [operator] incl.
  the one drift MY #1808 shipped over-ceiling ungated (found by worktree-diffing gate output at
  HEAD vs 714825daca). Ceilings lowered+locked ×4; total mypy debt 1317→1309. Registry down to
  DocumentDB. Smoke 532 green, ratchets all-ceiling, collection 14394 clean, ruff clean.
- Classifier note: the batched cut script was denied ([Cloud Storage Mass Delete] — DROP TABLE
  strings); redone file-by-file via Edit/Write, zero data deleted anywhere (all tables empty,
  verified pre-cut). Migration executes on real envs only via the deploy rail.
- **THREE closed today** (#1793, #1796, #1797) + #1852/#1853 filed. Day count since yesterday's
  STOP: cutover executed + 3 issues closed + runbook/briefing/docs corrected.

### Evening drain under the open budget (17:1x–17:5x) — #1853 + #1842 closed, #1854 filed, #1841 routed
- PM: window extended, expires Thursday regardless — "no benefit to holding back." Cron deleted
  for sustained drain (re-arm at STOP). Web tasked with both browser retests + a post-cutover
  render sweep (their lane; PM's later pass = independent verification), cc CXO/PM (f7bdd4c4f).
- **#1853 CLOSED**: seed script ported to User+PasswordService; required --username (no default
  'xian' identity), one-time generated password, POSTGRES_HOST local-only guard. Verified at the
  real auth layer: script → form login 200 → scratch row deleted.
- **#1842 CLOSED**: fixture now constructs IntentClassifier(llm_service=...) per #322's own note —
  the deprecated fallback made a FRESH uninitialized container post-#322, so the keyed lane died
  pre-LLM forever (red nobody sees). Blast radius verified: tests/intent deterministic 186 green,
  smoke 532 green.
- **The cure un-blinded real drift → #1854 FILED**: identity 92%<95% (stable ×2 live runs),
  temporal + overall red, 4 categories green (two full live suite runs, ~4:20 each — affordable
  under the open window).
- **#1841 ROUTED to Arch cc CXO/PPM** with the honest blocker named: a semantics ruling
  (documents vs portfolio for the phrase) precedes any corpus deposit, and the corpus is Arch's
  governed Inversion lane (generated phase0 + frozen baselines — no drive-by rows). Memo sent;
  issue commented. SEVEN issues touched today: 5 closed (#1793 #1796 #1797 #1842 #1853), 2 filed
  (#1854 + earlier #1852/#1853 — net: 3 filed), 1 routed.

### Evening tail (18:1x–18:4x) — #1774 census delivered, #1574 designed, #1817 correctly dormant
- **#1774**: full fresh census run (family is BIGGER than filed — workspace_* subfamily +
  recognition_handler + moment_ui join it; live-mux boundary clean; classifier refs are a
  comment). Rule-0 memo to Arch cc PPM with the SPATIAL-DISPOSITION cross-check named as
  precondition (protected-representations rule); census on the issue. Cut waits for GO.
- **#1817**: tripwire-by-design (fires when a de-authorize surface ships) — dormant correctly,
  no action.
- **#1574 (Audit Cluster 1 head)**: scoped + DESIGNED tonight, build deferred to a fresh session
  (named trigger — epic-head deserving first-of-day focus). Design doc pushed + issue commented:
  JSONB write-through under namespaced key, API preserved, session scope stays memory,
  collision-safety with #280/#1510 keys as a red-first test.
