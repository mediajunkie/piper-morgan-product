# Session Log: 2026-09-22-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Sonnet 5 — carried over from the 09-20 reboot; see below, this is now explained
rather than unresolved.
**Date**: Tuesday, September 22, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/21/2026-09-21-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-21**
(verified at trunk; quiet day, five of six fires no-op, cron re-armed `6c08fd70`).

## Session Objectives

1. Normal duty-cycle drain (mail → standing items → GitHub criteria line → idle).
2. **Correct my own 09-20 reboot-survival claim** — new shared memory
   (`feedback_cron_id_continuity_not_evidence_against_reboot`) shows the framing I and five other
   seats independently used was wrong: cron-id continuity across the reboot is explained by
   `claude --resume` restoring the transcript, not by the reboot missing any seat. Pard's forensics
   (`kern.boottime` + `ps lstart`) show the reboot reached every seat including mine.
3. **Still open, all PM-side**: Vercel storage figure, Buttondown send question, the two
   walkthrough verdict sessions (113d+, 95d+).

## Work Log

### 06:52 AM — START

- **Step 0**: prior day's log carries a line-anchored, date-shaped `DAY-CLOSED: 2026-09-21`. Clean
  close, no retroactive self-heal needed.
- **Cron**: `CronList` → exactly one job, **`6c08fd70`**, matching the registry row.
- **Worktrees synced by remote URL**: product `49863a526`, website `2295426` (moved since last
  night — checking what landed).

*(Log committed before the mail loop, per v1.33 sequencing.)*

### Correcting my own 09-20 reboot-survival claim — I was wrong, and I'm one of six

New shared memory (`feedback_cron_id_continuity_not_evidence_against_reboot`) landed overnight.
**It corrects the exact claim I made Saturday night**: I read my registry row and last night's log
entries and had internalized "cron `f1f73a46` survived the reboot" as a real, if ambiguous, data
point — I'd explicitly flagged it as something I couldn't resolve from my own seat. **The memory
resolves it, and the resolution is that the framing itself was wrong.** Pard's forensics
(`kern.boottime` + `ps lstart` on every `claude` process, showing none existed between boot and
~18:50) show the 09-20 reboot reached **every** seat, including mine. What I actually observed —
the cron job id unchanged — is `claude --resume <uuid>` restoring the job from the saved transcript,
not a process that never stopped. Six seats (Arch, HOST, Web, Comms, CXO, PA) independently made
the identical untested-assumption error: *"if the process died, the cron object would too."* Comms
sent a correction to the same recipients as their original claim this morning; I did the same,
since my own 09-20 memo to Pard is equally uneditable after the fact. Sent, cc Exec/Janus/CIO/PM.

**One genuine resolution, not just a correction**: my model-identity shift (Opus 5 → Sonnet 5,
unrequested, first noticed 09-20 18:52) has sat in this log's header as *unresolved* for two days.
It's now explained — I'm one of the "three of the cohort's four Opus seats" the memory names as
landing on Sonnet 5 post-reboot, unnoticed until someone specifically checked. Header updated
above to say so plainly rather than carry the open flag forward.

**Corrected the registry row at the point of the claim**, not just in mail — it still summarized the
09-20 finding as if it stood, which would have kept propagating the wrong framing to anyone reading
the row cold. Now carries the correction and cites the memory by name.

**Drained**: mail **1 direct** (Comms' correction, read in full, prompted my own identical
correction) **+ 2 cc** (CIO's hook-pilot updates, gated to `role == cio`, nothing for Web) ·
standing items unchanged (Vercel storage, Buttondown, two walkthroughs — all still PM-side) ·
criteria line **website 0, product 0**. Website repo moved (`300c89c` → `2295426`, a routine blog
post publish — Docs' lane, no defect signal, not investigated). Back to idle, cron armed.

### Fleet reboot-correction thread continues — CIO's own correction, and a resolved hook incident

**Mail**: 1 direct (Exec's pilot ask for CIO's tick-skill refactor — accepted, no-rush, waiting on
CIO's specific before/after text) + 3 cc, all triaged:
- **CIO's own correction** — same shape as mine and Comms' yesterday, retracting their "4th
  confirming seat" framing per `feedback_cron_id_continuity_not_evidence_against_reboot`. No reply
  needed.
- **A resolved runaway-hook incident** — CIO's post-commit heartbeat-automation pilot (gated to
  `role == cio`, never touched my seat) recursed last night 22:37–23:13, 967 marker commits pushed
  to `origin/main` (all touching only `dev/heartbeats/last-invoked/cio.txt`, no code/data damage),
  Pard killed it by hand rather than let it unwind on its own. Root causes (no re-entry guard, a
  hook that pushes) fixed and independently tested this morning, commit `c13414898`; re-arm
  deliberately deferred to Pard's own call rather than CIO re-flipping it solo. PM was cc'd on both
  the original incident report and the fix — nothing to escalate from Web's side.

### Carry-forward spring-clean — context-floor plan item 4a, done this fire

PM directive (via Exec, top priority today, usage pressure 80% of weekly limit): spring-clean every
role's `carry-forward.md`, deleting resolved history rather than archiving it in place. Genuinely
unblocked work with capacity available (queue empty) — did it now rather than deferring to tonight's
STOP.

**547 → 105 lines (−80.8%)**, calibrated against Exec's own worked example. Cut: fully-resolved
thread narrative spanning 08-08 through 09-08 (FTUX render-check, banner hero, composer 404,
obs-pass pre-staging, #1659, BYOC copy, In Review round), a duplicate stale "Open" block already
superseded by the current one, and a compressed historical-pointers section — all fully covered by
dated session logs, the durable record. Kept: current PM-open state (now 4 items, including #1827
which I'd nearly let drop off — still genuinely unconfirmed), the criteria-line infrastructure,
environment facts, and a deduplicated standing-errors list.

**One real find in the pass, not just trimming**: the Playwright `NODE_PATH` resolution requirement
I've hit repeatedly across fires (needs the npx cache path, neither worktree's own `node_modules`)
had never been written down. Verified the recipe actually resolves (`node -e "require('playwright')"`)
before committing it, rather than transcribe from memory of having done it before.

Reported completion to Exec cc PM, since their own context-floor tracking table was checking for
exactly this and only showed Exec's own row done.

**Drained**: mail **1 direct + 3 cc**, all handled · criteria line **website 0, product 0** · one
substantive deliverable shipped (carry-forward spring-clean) beyond the normal drain. Back to idle,
cron armed.
