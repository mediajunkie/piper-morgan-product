# Session Log: 2026-09-22-0652-web-code

**Role**: Unicorn Web Designer (Web)
**Model**: Claude Sonnet 5 — carried over from the 09-20 reboot; see below, this is now explained
rather than unresolved.
**Date**: Tuesday, September 22, 2026
**Start Time**: 6:52 AM PDT
**Prior session**: `dev/2026/09/21/2026-09-21-0652-web-code-log.md` — **DAY-CLOSED: 2026-09-21**
(verified at trunk; quiet day, five of six fires no-op, cron re-armed `6c08fd70`).

## Session Objectives

1. Normal duty-cycle drain (mail → standing items → GitHub criteria line → idle).
2. **Correct my own 09-20 reboot-survival claim** — new shared memory
   (`feedback_cron_id_continuity_not_evidence_against_reboot`) shows the framing I and five other
   seats independently used was wrong: cron-id continuity across the reboot is explained by
   `claude --resume` restoring the transcript, not by the reboot missing any seat. Pard's forensics
   (`kern.boottime` + `ps lstart`) show the reboot reached every seat including mine.
3. **Still open, all PM-side**: Vercel storage figure, Buttondown send question, the two
   walkthrough verdict sessions (113d+, 95d+).

## Work Log

### 06:52 AM — START

- **Step 0**: prior day's log carries a line-anchored, date-shaped `DAY-CLOSED: 2026-09-21`. Clean
  close, no retroactive self-heal needed.
- **Cron**: `CronList` → exactly one job, **`6c08fd70`**, matching the registry row.
- **Worktrees synced by remote URL**: product `49863a526`, website `2295426` (moved since last
  night — checking what landed).

*(Log committed before the mail loop, per v1.33 sequencing.)*

### Correcting my own 09-20 reboot-survival claim — I was wrong, and I'm one of six

New shared memory (`feedback_cron_id_continuity_not_evidence_against_reboot`) landed overnight.
**It corrects the exact claim I made Saturday night**: I read my registry row and last night's log
entries and had internalized "cron `f1f73a46` survived the reboot" as a real, if ambiguous, data
point — I'd explicitly flagged it as something I couldn't resolve from my own seat. **The memory
resolves it, and the resolution is that the framing itself was wrong.** Pard's forensics
(`kern.boottime` + `ps lstart` on every `claude` process, showing none existed between boot and
~18:50) show the 09-20 reboot reached **every** seat, including mine. What I actually observed —
the cron job id unchanged — is `claude --resume <uuid>` restoring the job from the saved transcript,
not a process that never stopped. Six seats (Arch, HOST, Web, Comms, CXO, PA) independently made
the identical untested-assumption error: *"if the process died, the cron object would too."* Comms
sent a correction to the same recipients as their original claim this morning; I did the same,
since my own 09-20 memo to Pard is equally uneditable after the fact. Sent, cc Exec/Janus/CIO/PM.

**One genuine resolution, not just a correction**: my model-identity shift (Opus 5 → Sonnet 5,
unrequested, first noticed 09-20 18:52) has sat in this log's header as *unresolved* for two days.
It's now explained — I'm one of the "three of the cohort's four Opus seats" the memory names as
landing on Sonnet 5 post-reboot, unnoticed until someone specifically checked. Header updated
above to say so plainly rather than carry the open flag forward.

**Corrected the registry row at the point of the claim**, not just in mail — it still summarized the
09-20 finding as if it stood, which would have kept propagating the wrong framing to anyone reading
the row cold. Now carries the correction and cites the memory by name.

**Drained**: mail **1 direct** (Comms' correction, read in full, prompted my own identical
correction) **+ 2 cc** (CIO's hook-pilot updates, gated to `role == cio`, nothing for Web) ·
standing items unchanged (Vercel storage, Buttondown, two walkthroughs — all still PM-side) ·
criteria line **website 0, product 0**. Website repo moved (`300c89c` → `2295426`, a routine blog
post publish — Docs' lane, no defect signal, not investigated). Back to idle, cron armed.

### Fleet reboot-correction thread continues — CIO's own correction, and a resolved hook incident

**Mail**: 1 direct (Exec's pilot ask for CIO's tick-skill refactor — accepted, no-rush, waiting on
CIO's specific before/after text) + 3 cc, all triaged:
- **CIO's own correction** — same shape as mine and Comms' yesterday, retracting their "4th
  confirming seat" framing per `feedback_cron_id_continuity_not_evidence_against_reboot`. No reply
  needed.
- **A resolved runaway-hook incident** — CIO's post-commit heartbeat-automation pilot (gated to
  `role == cio`, never touched my seat) recursed last night 22:37–23:13, 967 marker commits pushed
  to `origin/main` (all touching only `dev/heartbeats/last-invoked/cio.txt`, no code/data damage),
  Pard killed it by hand rather than let it unwind on its own. Root causes (no re-entry guard, a
  hook that pushes) fixed and independently tested this morning, commit `c13414898`; re-arm
  deliberately deferred to Pard's own call rather than CIO re-flipping it solo. PM was cc'd on both
  the original incident report and the fix — nothing to escalate from Web's side.

### Carry-forward spring-clean — context-floor plan item 4a, done this fire

PM directive (via Exec, top priority today, usage pressure 80% of weekly limit): spring-clean every
role's `carry-forward.md`, deleting resolved history rather than archiving it in place. Genuinely
unblocked work with capacity available (queue empty) — did it now rather than deferring to tonight's
STOP.

**547 → 105 lines (−80.8%)**, calibrated against Exec's own worked example. Cut: fully-resolved
thread narrative spanning 08-08 through 09-08 (FTUX render-check, banner hero, composer 404,
obs-pass pre-staging, #1659, BYOC copy, In Review round), a duplicate stale "Open" block already
superseded by the current one, and a compressed historical-pointers section — all fully covered by
dated session logs, the durable record. Kept: current PM-open state (now 4 items, including #1827
which I'd nearly let drop off — still genuinely unconfirmed), the criteria-line infrastructure,
environment facts, and a deduplicated standing-errors list.

**One real find in the pass, not just trimming**: the Playwright `NODE_PATH` resolution requirement
I've hit repeatedly across fires (needs the npx cache path, neither worktree's own `node_modules`)
had never been written down. Verified the recipe actually resolves (`node -e "require('playwright')"`)
before committing it, rather than transcribe from memory of having done it before.

Reported completion to Exec cc PM, since their own context-floor tracking table was checking for
exactly this and only showed Exec's own row done.

**Drained**: mail **1 direct + 3 cc**, all handled · criteria line **website 0, product 0** · one
substantive deliverable shipped (carry-forward spring-clean) beyond the normal drain. Back to idle,
cron armed.

### Tick-skill Phase B pilot — day 1, verified clean

CIO's pilot package landed (Steps 2 and 3 extracted from `duty-cycle-tick/SKILL.md` to a new
`duty-cycle-tick-design-notes.log`, context-floor item 2b). Read both real diffs on `origin/main`
rather than trust the summary, per CIO's own ask — matched their description exactly in both cases:
- `7098528e2` — removes the self-labeled "RETIRED... DO NOT RUN THIS" v1.19 hook-probe saga; the
  operative line survives verbatim ("Verify that hook exists; do not probe").
- `280cace60` — removes the five-round DAY-CLOSED regex correction narrative; I'm one of the three
  named in that history (CXO/HOST/Web, 07-30), so I could confirm the extracted summary is faithful
  to what actually shipped, not just plausible-sounding.

**Ran the real functional test, not just a diff read**: pulled the live Step 0 grep pattern straight
out of the current skill file and executed it against real data — matches yesterday's genuinely
closed log, correctly doesn't match today's still-open one. **Byte-for-byte identical to the pattern
I've been typing from memory every START since 09-19** — the extraction changed zero characters of
what I actually execute. Confirmed the design-notes file exists, populated, and `SKILL.md`'s byte
count matches CIO's reported figure (68,560).

**Day 1 verdict: clean**, reported to CIO cc Exec/PM as a real positive data point, not a final
sign-off — continuing to watch across normal fires per the original ask (report after a few days'
real cycling).

**Drained**: mail **1 direct** (CIO's pilot package, acted on substantively — this was the fire's
real work) · criteria line **website 0, product 0** · no movement on any PM-side open thread. Back
to idle, cron armed.

### ⚠️ Found a defect in this morning's hook-incident fix — the new re-entry guard is not role-scoped

Quiet fire (mail empty, criteria line empty, no thread movement) until `duty-cycle-heartbeat.sh web
WORK --if-quiet` returned:

```
heartbeat: HEAD is already a heartbeat marker commit ('hb-last-invoked(arch): suppressed WORK
2026-09-22 15:51:07 PDT') — refusing to react to my own output
```

**That is `arch`'s commit, not `web`'s.** My routine sync had just pulled it in as the tip of
`origin/main` moments before my own heartbeat call. Read the actual guard (CIO's belt-and-suspenders
fix from this morning's runaway-hook incident, `scripts/duty-cycle-heartbeat.sh`): it matches
`hb(*` / `hb-last-invoked(*` for **any** role, not scoped to `$ROLE`. The message asserts "my own
output" — the code never checks whose output it is.

**Consequence, verified rather than assumed**: my own `dev/heartbeats/last-invoked/web.txt` sat at
the 12:52 fire's timestamp — **3 hours stale** — because this fire's call silently no-op'd
(`exit 0`, no error). The belt read `web` clean at the time regardless, because clean-reading keys
on recent commits, not this marker — but that's exactly the row-presence-vs-interior-coverage gap
my own instrument from 09-19 exists to catch, recurring one layer down.

Reproduced twice on unchanged state before reporting (not a one-off race): same refusal both times,
same HEAD, same message. In a shared repo with 11 seats pushing small frequent heartbeat commits,
HEAD landing on *some other* role's marker at the moment a seat runs its own heartbeat is not a rare
race — it will recur routinely, silently suppressing the belt's actual liveness signal under the
exact incident-prevention mechanism meant to keep it trustworthy.

**Reported to CIO cc Pard/Exec/PM** with full reproduction. Not attempting a workaround (e.g.
committing something trivial first to move HEAD) — that would mask the defect rather than surface
it, and this is shared infrastructure running on 11 seats, not mine to patch unilaterally.

### Heartbeat re-entry guard fix confirmed live; genuine alpha-account gap found and reported

**CIO fixed my re-entry-guard finding same-day** (`48106b2efb`) — verified the fix is actually in
the synced worktree rather than trust the memo: read the current guard code directly, confirms
scoping to `$ROLE` for both `hb(...)` and `hb-last-invoked(...)` shapes, comment cites my finding by
name and states the corrected recursion-only rationale. CIO's own memo also described a 6-scenario
behavioral test (own marker blocks, another role's marker now proceeds, ordinary commits proceed,
adversarial role-name-substring case doesn't falsely match) — didn't re-run all six myself, since
the code read matches the described fix exactly and re-deriving an already-demonstrated test adds
nothing.

**Fleet directive**: PM says don't self-throttle on usage — a one-time reset covers any wall hit
before Thursday. Context-floor plan stays top priority regardless. No behavior change needed on my
side; hasn't been holding back.

**Lead asked Web to spot-check the newly Fly-cutover alpha** (two scripted retests + a render
sweep) ahead of PM's own fresh-eyes pass. Read the full test card (`dev/active/pm-test-card.md`)
before acting, not just the memo's summary — matched exactly.

**Genuine credential gap, checked rather than assumed.** Lead's memo assumed my test account "rode
the migration" from the old alpha droplet. **No such account is documented anywhere** — checked
both existing credential files at `/Users/xian/.piper-shared/` directly (both explicitly scoped to
the *local dev server*, `localhost:8001`, not alpha), searched my own session-log history (nothing),
and read the relevant shared memory (same local-dev-server scope, explicitly not the marketing site
either). **Did not guess** whether either dev-server account might also work on alpha — a wrong
guess against a real hosted account isn't a cheap experiment to run speculatively.

**Did the part I could verify without an account**: pre-auth render check on
`alpha.pipermorgan.ai/login` via real Playwright — clean (logo, both fields, Log In button, invite-
only signup link matching the documented no-self-serve model), 0 console errors, 0 responses ≥400.
Both scripted retests and the authenticated half of the render sweep (chat, settings, uploads)
remain genuinely blocked. Reported precisely to Lead cc PM/CXO with what I checked and what would
unblock it — same discipline as the 08-29 precedent (report the gap, don't invent or DB-inject).

**Drained**: mail **3 — 1 direct** (Lead's ask, substantively worked and reported) **+ 2 cc**
(CIO's fix confirmation — verified in code, not just trusted; Exec's fleet directive — noted, no
behavior change needed) · criteria line **website 0, product 0** · no movement on other open
threads. Back to idle, cron armed.

## STOP — day close, 2026-09-22 (21:52 fire, last scheduled slot; next is 06:22 tomorrow)

### Final mail — one direct, Janus closing their own aggregation error

Janus corrected a level up from the individual reboot-survival retractions: their 09-21 plan-doc
entry aggregated four separately-wrong claims (Exec, Comms, CIO, Web) into a stronger *"this
falsifies the reboot premise"* framing — worse than any one error, since correlated claims from the
same untested assumption aren't independent confirmation. Explicitly said nothing needed from
recipients; triaged, no reply.

### Day arc — a genuinely substantive day, not a quiet-hold day

| time | what shipped |
|---|---|
| 06:52 | START — **corrected my own 09-20 reboot-survival claim** (`feedback_cron_id_continuity_not_evidence_against_reboot` landed overnight); the model-identity shift finally explained, no longer carried as unresolved |
| 09:52 | **Carry-forward spring-cleaned, 547→105 lines (−80.8%)** — context-floor plan item 4a, done same-fire rather than deferred; one real find in the pass (undocumented Playwright `NODE_PATH` requirement, verified before committing) |
| 12:52 | **Tick-skill Phase B pilot, day 1** — read both real diffs (not the summary), ran a live functional test of the surviving Step 0 grep against real positive/negative logs, reported clean |
| 15:52 | **Found a real defect** in that morning's heartbeat-recursion fix — the new re-entry guard wasn't role-scoped, silently suppressed any seat's heartbeat off any other seat's marker commit. Reproduced twice, reported precisely, not worked around |
| 18:52 | **CIO fixed the guard same-day**, verified in code · **genuine credential gap found and reported** rather than guessed past — no documented alpha test account exists post-cutover, checked three sources before concluding that, did the pre-auth render check that didn't need one |
| 21:52 | **STOP** — Janus's aggregation-error correction triaged, cron re-armed |

**Discovered work**: one real defect found and fixed same-day (heartbeat re-entry guard scoping) —
no tracking issue needed since CIO owned and shipped the fix directly, same-thread. **Nothing filed
as GitHub issues this fire** — the criteria line stayed empty all day (website 0, product 0
throughout), genuinely nothing in-lane to pick up there.

**The through-line, if there is one**: three separate times today the same move paid off — *read
the actual artifact/code/diff, not the summary of it* (the tick-skill diffs, the heartbeat guard's
real condition vs. its own printed message, the credential files' actual documented scope vs. an
assumption in someone else's memo). None of these were dramatic; all three would have been wrong
conclusions carried forward silently if I'd trusted the shorter path.

### Cron re-armed

Delete-then-create at STOP, `CronList`-verified exactly one job: **`0c6d934b`** (was `6c08fd70`).
Registry row updated (quoted-field convention preserved, matching other rows' current shape),
`first_fire` unchanged at 06:22.

### Memory & briefing surfaces referenced this session

**Referenced**
- `feedback_cron_id_continuity_not_evidence_against_reboot` (new today) — corrected my own claim
  and shaped how I read three subsequent related memos without re-deriving the same error.
- `project_web_browser_lane_test_account` — read directly rather than from memory of it, which is
  exactly what surfaced that it doesn't cover alpha and let me report precisely instead of guessing.
- `dev/active/exec-carry-forward.md` — calibration reference for how aggressively to cut my own.
- `CLAUDE.md` — "never guess at facts you can look up or ask about" was the operative rule behind
  both the credential-gap report and not touching the registry's quoting convention blindly.
- `.claude/skills/duty-cycle-tick` — STOP's delete-then-create; the spring-clean directive itself
  rode on top of the existing carry-forward mechanism rather than replacing it.

**Loaded but not referenced**
- Intent-routing stack, glossary, ADR/PDR tree — no code/content work in that space today.

**Wanted but not found**
- Still nothing addressing yesterday's gap (a durable place recording "the archive is source of
  truth for X"). Not urgent enough to chase on its own; would fold naturally into whatever surface
  eventually gets built for it.

### Sign-off checklist

```
$ git status --porcelain
 M dev/2026/09/22/2026-09-22-0652-web-code-log.md
(the log file below, mid-append -- checklist capturing itself writing, same as prior nights)

$ git fetch origin main && git rev-parse --verify -q origin/main
916dcecf51706531df09bdf495383a1864e65d41

$ git log --oneline origin/main..HEAD
(empty above = everything reachable from origin/main)

$ git rev-parse --abbrev-ref @{u}
origin/main
```

Both worktrees clean; website worktree (`2295426`) untouched today beyond reads/verification.
Cron: `CronList` → exactly one job **`0c6d934b`**, `22 6,9,12,15,18,21 * * *`, re-armed
delete-then-create at 21:52 (was `6c08fd70`), expires ~2026-09-29.
STOP cleanup: both stale-scratch checks returned nothing — no deletions in this commit.

<!-- DAY-CLOSED: 2026-09-22 -->
