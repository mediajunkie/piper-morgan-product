# PA session log — 2026-09-22

**Role**: Piper Alpha (PA) · **Tool**: Claude Code · **Model**: Sonnet 5
**Host**: Amber · **Account**: xian@pipermorgan.ai
**Worktree**: `~/Development/piper-morgan-worktrees/pa` (Model A, stable) · branch `claude/pa-cycle`
**Cron**: `42 6,9,12,15,18,21`

---

## START (07:12) — fresh-day START, 09-21 closed cleanly

`CronList` confirmed exactly one job (`eb24aeee`, correct expression) — held overnight from
yesterday's STOP re-arm, no gap. Verified 09-21's session log carries a real, line-anchored
`<!-- DAY-CLOSED: 2026-09-21 -->` marker — proper STOP happened, no missed-close self-heal needed.

Cohort-freeze check (`scripts/cohort-freeze-detect.sh`, post-sync): `INSUFFICIENT-SCHEDULE` (5
scheduled fires < 6 in the 4h window) but **rc=0** — not a freeze signal, just a window too short
to discriminate; 45 commits and 4 emitters visible is plenty of cohort activity. Ordinary wake.

Yesterday's headline (a real reboot-reasoning correction — the Amber reboot reached every seat,
cron-id continuity was consistent with it not against it; saved as
`feedback_cron_id_continuity_not_evidence_against_reboot`) is fully recorded in yesterday's log,
not re-litigating here. Registry row already correctly reflects Exec's correction.

Proceeding to the mail/task loop.

**Mail: 0 direct, 0 cc.** Inbox empty. Mandatory carry-forward re-verify: re-checked Q1
(usage-correlation calibration question) against `decisions.log` and my own `read/` — still
genuinely open, no ruling, no reply. Unboarded-PM-items scan: same known L19 false positive,
nothing new. Standing items unchanged: BYOC tracked in carry-forward, T-axis probe blocked on
CXO's pre-registration, usage-correlation model still waiting on Q1. No GitHub-criteria line yet
(known gap). Fully drained. (0,0) — returning to idle.

### PM engaged directly (08:02) — context-floor spring-clean done, carry-forward 538→52 lines

PM checked in live (remote control), same morning Exec's fleet-wide context-floor directive
landed (item 4a: spring-clean own carry-forward — top priority per real usage pressure, 80% of
weekly limit this morning). Read the directive in full before answering PM, rather than reply
from memory of what it probably said.

**Did the actual cleanup rather than defer it** — re-verified #1458's live GitHub state
(`gh issue view`, still OPEN) before carrying it forward, deleted everything resolved/superseded
(the entire 08-26→09-08 BYOC historical narrative, the three-week-stale "Active state 09-01" stub,
August-vintage methodology notes now either obsolete or already encoded in the duty-cycle-tick
skill itself, closed items). Kept only the two genuinely open PM-attention items and four current
facts. 538 → 52 lines. Committed (`af302e7e9`) and pushed. Triaged Exec's directive, no reply
needed (informational, action was the response).

PM then asked three real questions. **(1) Can another agent build Lead's usage-capture proposal
via an outcome-oriented spec + subagent, without diverting Lead?** Re-read Lead's proposal in full
before answering rather than from memory — found the two real blocking unknowns (seat→account
mapping; where the authoritative usage number is readable / whether Dispatch can read it) are
explicitly named in Lead's own doc as *"xian's/Pard's knowledge,"* not Lead's — so this barely
touches Lead at all. Asked PM directly for the mapping since PM was right there. **(2) A
consolidated overview doc for the usage-correlation model** — none existed (prior-art and
first-pass were two separate documents); wrote one
(`dev/active/usage-correlation-model-overview-2026-09-22.md`), committed (`3261470a3`), pushed,
sent to PM via `SendUserFile`. **(3) A BYOC key artifact/overview** — checked `dev/active/` and
`docs/internal/architecture/`: no single one-pager exists, only two working documents
(`byoc-parallel-work-plan-2026-09-15.md`, `byoc-hosted-alpha-readiness-checklist-2026-09-15.md`).
Said so honestly rather than manufacture one PM didn't ask for yet — sent both existing docs,
offered to build a proper overview when the topic resumes per PM's own stated sequencing.

### Registry trim applied, Pard question sent, PM's live ruling folded into tracked state (10:12 fire)

Synced clean. One new memo (Exec, cc PM/CIO): CIO's registry-history trim tool is ready, opt-in.
Checked my own row's length (`awk`, not guessed) — 2545 chars, worth doing. Read the tool's own
docstring before running it (understood the split point: the literal `was: active:`/`was: parked:`
marker, not a bare occurrence of "was"). Dry-ran first, verified the result looked sane, executed:
2545 → 1668 chars, 876 chars of history preserved verbatim in
`dev/state/registry-history/pa.log`. Verified the trimmed row still reads coherently (not cut
mid-sentence) and the registry's overall row count (88) and other rows were undisturbed before
committing.

**Folded this morning's live PM conversation into tracked state** — it happened outside the
duty-cycle mechanism, so this WORK fire was the first chance to record it properly.
`pa-standing-items.md` #3 now reflects PM's actual ruling (build the usage-capture proposal, via
spec + `prog` dispatch, not Lead) rather than the stale "Q1 pending" framing from yesterday.

**Drained the Pard question rather than let "I'll route this separately" linger unactioned** — sent
`mailboxes/pard/inbox/question-pa-to-pard-cc-pm-exec-where-is-usage-readable-and-can-dispatch-read-
it-2026-09-22.md` (cc PM, Exec): where the authoritative usage number is readable, and whether
Dispatch can read it — the second of the two blocking unknowns named in Lead's own proposal.
Sent via `mail-send.sh` (`405b86bf5`).

Triaged the registry-trim announcement to `read/`, sent via `mail-send.sh` (`15018f567`). Re-checked
mail after both sends: empty. Unboarded-PM-items scan: same known L19 false positive, nothing new.

### PM engaged again, live — Anthropic usage-data research, roster documentation, cross-project checks (11:28 fire)

PM read the three sent artifacts, praised them, then gave four real asks: (1) research whether
Anthropic's dashboard has downloadable/API usage data — directly relevant to Q1's calibration
question; (2) document that the whole 11-role Piper Morgan team works from
`xian@pipermorgan.ai`; (3) check the wider cross-project agent registry with Janus and the
Code-agent operational mechanics with Pard; (4) confirmed BYOC as next priority once current
commitments close.

**(1)** Four live `WebSearch` queries rather than one — found three distinct surfaces (Console
manual CSV export; the Admin Usage-Cost API, gated to Organization accounts, NOT individual
Free/Pro/Max; and Claude Code's own team-analytics dashboard at `claude.ai/analytics/claude-code`,
Team/Enterprise-gated, with per-user data collection reportedly on by default since 2026-07-11 —
the strongest match for "a recent email"). Wrote up findings + the specific things only PM can
check (account type, plan tier) — `dev/active/anthropic-usage-data-research-2026-09-22.md`. This
could be a materially better calibration source than Lead's manual-capture proposal if real.

**(2)** Verified the 11-role roster against `ROSTER.md` (not from memory) before writing anything.
Added a "Team & Accounts" section to `docs/briefing/PROJECT.md`. **PM corrected mid-turn**: I'd
described Exec/Chief of Staff as a category separate from "6 leadership roles" — PM clarified
Exec IS leadership (sits alongside PM supervising the rest), matching `ROSTER.md`'s own 7-role
Tier 1. Fixed immediately, committed separately (`cda4b5809`) so the correction is its own clean
commit rather than folded silently into the original.

**(3)** Sent two verification memos rather than publish PM's from-memory list as fact: to Janus
(the ~16-agent cross-project registry — confirm/correct, ask about a canonical registry doc on
their side) and to Pard (whether the wider Code-agent fleet's operational mechanics match ours,
and whether the two Anthropic accounts share any Amber-level infrastructure — flagged the 09-14
shared-ceiling incident as a reason not to assume cross-account isolation without checking). Both
cc PM, sent via `mail-send.sh` (`e142cf2cc`).

**(4)** BYOC confirmed as next priority once current commitments close — acknowledged, no action
needed yet; still finishing the usage-correlation model's build-spec work first.

All three PROJECT.md/research-doc commits pushed and verified on `origin/main` before replying.

### PM corrected roster wording, closed usage-research thread, pivoted PA to BYOC (continued, same fire)

**Mid-turn correction**: PM caught that I'd described Exec/Chief of Staff as a category separate
from "6 leadership roles" — Exec IS leadership (sits alongside PM supervising the rest), matching
`ROSTER.md`'s own 7-role Tier 1. Fixed immediately in `PROJECT.md`, own clean commit (`cda4b5809`)
so the correction is traceable rather than silently folded into the original edit.

**PM confirmed both Anthropic accounts are personal Max x20** (not Organization/Team/Enterprise) —
closes the usage-data research cleanly: rules out the Admin API and Claude Code's team-analytics
dashboard, the two surfaces that could have beaten Lead's manual-capture proposal outright. Updated
the research doc with an honest "Closed" section rather than leave dangling "what PM needs to
check" text now answered. No change to the actual plan — both remaining paths (Lead's proposal,
Exec's transcript parser) read local data, unaffected by account type. Standing-items #3 updated
to reflect this and demote the thread to background priority per PM's own next instruction.

**PM's next instruction: full pivot to BYOC** — advance what's ready, batch what needs PM's
attention through Exec's rollup rather than ad hoc memos. Re-read both 09-15 planning docs in full
(not from memory, a week old) before acting. **Re-verified rather than trusted the week-old
snapshot**: `mcp.pipermorgan.ai` still not deployed (live `dig`/`curl`), #1458 and #1462 both still
OPEN via `gh issue view`, both now carrying explicit PUBLIC-BETA-GATE milestone language that
wasn't there 09-15 (ESSENCE's front-loading formalized into the milestone description itself since
then) — a real, positive change worth noting, not just a null result. Checked for any Phase B
(DNS/TLS) activity in the past week — none found. Confirmed `#1458`/`#1462` still correctly absent
from `mvp-epic-order-2026-09-09.md` (by design now, not an oversight — front-loaded into
Production, not MVP).

**Batched Phase B ownership as the one real PM-attention item** — updated `pa-carry-forward.md`'s
PM Attention section (now the headline entry) and sent Exec a direct heads-up
(`mailboxes/exec/inbox/notice-pa-to-exec-cc-pm-byoc-restarted-batching-for-your-rollup-2026-09-22.md`,
`26b92711c`) rather than rely solely on the passive carry-forward-read mechanism for a fresh thread
restart Exec may not be tracking yet. Explicit in the memo: this is routing into Exec's system, not
an ask for Exec to act personally.

**Started advancing Phase A (tool-catalog naming-test design)** — the one piece with zero
dependency on anyone else's time. Read PDR-006's actual naming question in full before designing
anything (PPM's 07-30 finding: situation-shaped tool names help human legibility but may route
*worse* for LLM-side tool selection — "test selection accuracy on both namings before committing,"
shares a rig with the recomposition probe). Dispatched an Explore subagent to inventory Piper's
actual current tool catalog (names, descriptions, current naming style) rather than design a test
against an imagined catalog — running in background, not yet returned.

### Explore agent returned; naming-test design written and committed (continued, same fire)

Real catalog data: **~53 distinct operations** (`workflow_entries.py`'s `WORKFLOW_REGISTRY`),
**~110+ alias keys**, and — confirming PPM's own framing exactly — **100% object-shaped naming,
zero situation-shaped names exist anywhere today** (`create_issue`, `close_issue`,
`update_document`, etc., all `verb_object` derived from the internal data model).

Wrote the full test design grounded in this real data rather than an imagined catalog:
`dev/active/byoc-tool-catalog-naming-test-design-2026-09-22.md`. 12-operation sample spanning
CRUD/query/conceptual/control categories, matched-effort dual catalogs (object vs. situation
naming), ~30 realistic utterances, both vendors, an explicit control operation (`meeting`,
offer-only, should show no naming effect — a confound check on the harness itself). Named what
this design shares and doesn't share with CXO's recomposition-probe rig (different question,
possibly reusable harness code) without making running this depend on CXO's schedule. Explicitly
labeled DESIGN DONE, NOT YET RUN — running it (live API, both vendors) is its own next unit of
work, not done this fire.

Updated `pa-standing-items.md` #1 (BYOC) to reflect the restart and this concrete progress.
Committed and pushed all three artifacts (`cb73efdfe`, `844ea334b`), sending the design doc to PM.

### Naming-test probe actually run — first pass, cost-narrowed (13:12 fire)

Task Loop drain, WORK fire: BYOC's next unblocked step (running the designed naming-test probe)
had zero external dependency, matching PM's "advance what you can." Checked for a reusable
two-vendor probe harness before building from scratch — found `probe_b_recomposition_2026-08-30.py`
from the #1463 series, reused its Keychain-credential-resolution pattern (the authorized
`lead/venv/bin/python` interpreter, `keyring.get_password("piper-morgan", ...)`) rather than
re-deriving it.

**Deliberately narrowed scope from the design's ~96-call plan to 24** (Claude only, one utterance
per operation, not 2-3, no GPT arm) — cost-efficiency is a standing principle, and the closest
precedent (#1463's own recomposition probe) required explicit authorization for every spend
extension rather than treating a design doc as blanket permission. Verified Keychain access
worked before writing the full script, not assumed.

**Result: situation-shaped 12/12, object-shaped 10/12** — the opposite direction from PPM's
stated PDR-006 worry (that situation names might route *worse*). **Did not stop at the headline
number** — read both failure transcripts directly from the saved JSON before writing anything up,
and found a real confound: I wrote both catalogs myself in one sitting, and the situation-shaped
descriptions naturally came out more disambiguating (scenario-framing forces stating *when* to use
a tool; terse object nouns don't). One failure's utterance also lexically echoed its
situation-shaped name almost verbatim ("take it off my list" / `remove_something_from_my_list`),
which could be doing real work independent of any naming-scheme effect. **Wrote this up honestly
as a real-but-confounded first data point**, not a clean confirmation of either direction, and
named the actual next step that would control for it (an independent author writing the
object-shaped side) — without executing that step or deciding unilaterally it's worth more spend.

Full writeup: `dev/active/probes/RESULTS-naming-test-first-pass-2026-09-22.md`. Updated the design
doc's status and `pa-standing-items.md` #1. Committed and pushed (`f9860b1ce`), verified on
`origin/main`.
